# foundation-design 版本紀錄

新到舊。版號規則與兩個住址（本檔與 `SKILL.md` frontmatter 的 `metadata.version`）見專案的 skill 同步規範。

**Version**: 6.6.23 — 修正 b3w 判定標記的一處條文讀歪點，不新增判準、只重排既有結構（來源見 ticket Solution 出處表）。`references/dimension-product-notes.md`〈複合產物與多子樹分列〉首段，6.6.22 補的新句只以排除句（不是待建就等於改寫產物）與條件句（說不出失效樣子填待定）點名「改寫產物」「待定」，全段沒有一句正面指出「待建」的部分在權威預設產物對本形態成立時該填「照預設」，讀者因此把判定限縮在改寫產物與待定之間；補一句明示權威預設產物成立時填「照預設」，含尚未存在需補建的情形，「照預設」判定後仍可能落為地基票（`SKILL.md`〈與 orchestration 的協作〉已有此結構），「尚未存在」不是排除「照預設」的理由；「待定」句補使用條件限定——依〈產物欄的五種合法答案〉「該判而未能判」，僅適用尚未能判定的情形，已判定為「照預設」或「改寫產物」者不因待建而回頭改填「待定」（FD-17a）。同段移除「範例中的寬度截斷規則填改寫產物」的自我指涉句，改寫為不依賴範例的一般陳述。`SKILL.md`〈產物欄的五種合法答案〉「照預設」列「何時使用」欄補「（含尚未存在、需要補建者）」，與上述新句對齊。全部新增字句已逐句 `git grep -F` 驗證出處存在於既有非範例段落，未新增判準、未變更任何既有判準的讀法。

**Version**: 6.6.22 — 修正 b3z 判定標記的兩處條文讀歪點，不新增判準、只重排既有結構（來源見 ticket Solution 出處表）。`references/dimension-product-notes.md`〈複合產物與多子樹分列〉首段，範例「填『改寫產物：寬度截斷規則待落為文件』是正確答案」原未說明待建部分為何填改寫產物，讀者將其泛化為「待建的部分一律填改寫產物」；補一句明示待建部分仍依〈產物欄的五種合法答案〉逐列使用條件判定，範例填改寫產物是因符合該列「維度成立但預設產物對本形態不適用或有害」，說不出失效樣子時應填「待定」（FD-17a）。`SKILL.md`〈與 orchestration 的協作〉表後「交回前先為這兩個維度各建一張盤點票」句，原未限定適用範圍，讀者將其套用到「規劃波之後」列（該列本 skill 自行驅動，不涉及交回）；句首補「本段僅適用『規劃波進行中』列」，句末補「規劃波之後」與「其餘」兩列不適用本段（FD-10b）。全部新增字句已逐句 `git grep -F` 驗證出處存在於既有非範例段落，未新增判準、未變更任何既有判準的讀法。

**Version**: 6.6.21 — 修正 b3y 判定標記的兩處條文讀歪點，不新增判準、只重排既有結構（來源見 ticket Solution 出處表）。`SKILL.md`：〈產物欄的五種合法答案〉「待定」列，觸發條件（權威缺席／說不出失效樣子）與排除句（權威存在但無可用部分屬判準層）原寫於同一段未分隔，讀者將排除範圍誤讀擴大至整個待定列、推導不出時改填「無」；觸發條件後插入「判不出來一律填『待定』，任何情況下都不得以『無』代之」一句（原僅見於表後段落，只餵入表本身時讀不到），排除句末補「那是判準層的結論，不是產物欄的答案，兩層分開填」明示排除只及於判準層缺口，不及於推導失效樣子的產物層判斷（FD-05b）。〈與 orchestration 的協作〉表「規劃波之後」列，排除句「不含本 skill 自身產出的地基票」原藏於「定義同上列」括號中段、粗體主詞未含，讀者引用該列時連同排除句一併略去而誤判已建票；排除句移入粗體主詞本身（FD-10c）。全部新增／移動字句已逐句 `git grep -F` 驗證出處存在於既有非範例段落，未新增判準、未變更任何既有判準的讀法。

**Version**: 6.6.20 — 引文修正：〈維度與產物〉表後增列句原寫依〈本 skill 不做什麼〉「指名既有權威」的做法，該節查無此字串（字面只出現在 frontmatter description），改為該節原句「本 skill 的職責是指名它們」，判準不變。

**Version**: 6.6.19 — 修正 b3x FD-2 判定標記的一處條文讀歪點，並處置判定同批發現的一處條文風險（來源見 ticket Solution 出處表）。`SKILL.md`〈維度與產物〉「照同一形式增列一行」句後補一句：依〈本 skill 不做什麼〉「指名既有權威」的做法，權威來源欄填一個 skill／方法論／可觀測性規則類別中的既有項目，指名不到才轉入〈權威缺席時〉「框架內確實無承接者」——本表未列出這個維度，不等於指名不到既有權威（FD-21b：讀者把表外項目直接等同於無承接者）。`references/dimension-product-notes.md`〈複合產物與多子樹分列〉「複合產物落票時」句，「『待定』的部分依……建補票」改為「『待建』的部分依……建補票」，取同段前句已用的「待建」一詞，消除與〈產物欄的五種合法答案〉表列答案「待定」同名異義（FD-22a 條文風險，未造成本批判定失敗）。全部新增／修改字句已逐句 `git grep -F` 驗證出處存在於既有非範例段落，未新增判準、未變更任何既有判準的讀法。

**Version**: 6.6.18 — 修正讀者探針第三輪判定標記的兩處條文讀歪點，不新增判準、只重排既有結構。`SKILL.md`：〈與 orchestration 的協作〉表「規劃波進行中」「規劃波之後」「其餘」三列判別欄，「該版票」的範圍由僅存在於 `examples-process.md` 排除句的隱性定義，改為在判別欄正向寫出——指 `version-bootstrap` pipeline 自身的產出（即功能票），不含本 skill 自身前一輪產出的地基票（FD-10b）；〈工作流〉步驟 5「指名複核者的人工複核」段，複核者／複核依據／複核產出由同一句內以括號並列的寫法改為 1. 2. 3. 編號列，複核者一項並排寫在單一括號內的「已訂者用該角色；未訂時退至……」兩分支，改寫為先查後退的先後順序句，不再以同一括號容納兩個分支（FD-14b）。`references/examples-process.md`：〈人工複核「複核者」欄的判斷順序〉【驗證】段引文同步 SKILL.md 新文字；段末段落刪除重複並列兩分支的括注「（權威已訂審查角色時用該角色，未訂時用本框架既有審查代理人）」，同段開頭已有的先後順序句已完整表達同一件事。全部新增字句逐句 `git grep -F` 驗證出處存在（來源見 ticket Solution 出處表），未變更任何判準的正確讀法。

**Version**: 6.6.17 — WRAP 複核 6.6.15 落地的 FD-05b／FD-13a 兩處，以及 `dimension-product-notes.md` 事件流仲裁「無」理由句一處依據範圍問題（複核判定見 ticket Solution）。`SKILL.md`：〈權威缺席時〉「框架內確實無承接者」列，「物件」改「產物（如 seeder）」，與同儲存格既有用詞一致，消除與同節「對象」一詞的近義矛盾讀法；〈移植前置條件〉降級段刪除「改把應建票的內容直接記入降級記錄本身」一句——`git grep -F` 0 命中既有出處，屬 6.6.15 條目未揭露的新判準，與該版本開頭「逐題不新增判準、只重排既有結構」的整體描述不符，只保留既有條文可支撐部分（步驟 4 與各處建票在本專案不成立、記下缺哪一項與哪幾步不成立、不套用形態 4），產物落點另由框架 issue 追蹤，條文不寫 issue 編號。`references/dimension-product-notes.md`：〈判定「失效不存在」的理由〉「套用於事件流仲裁」段，「無」的理由句改為指向本段判準本身敘明，不再引用〈事件流仲裁的四種答案形態〉表「無」列的範例句，消除依據只在範例內的問題。更正 6.6.15 條目：FD-13a 部分實際新增前述未揭露判準，非純粹重排既有結構。

**Version**: 6.6.16 — WRAP 複核 6.6.14 落地的 FD-20a／FD-20b 兩段（複核判定：FD-20a 範例理由句被升為單軸選擇準則、FD-20b「套用於事件流仲裁」段把事件流數量等同競爭狀態存在，兩者皆屬新判準且部分不正確，來源見 ticket Solution）。`dimension-product-notes.md`〈事件流仲裁的四種答案形態〉表前段：「不看事件流數量」的單軸準則改寫為路由——照預設／改寫產物依 `SKILL.md`〈產物欄的五種合法答案〉既有二分，適用與否逐節查事件流負載仲裁方法論開頭段「各節標明的『實例化條件』」，不在本節另立準則；對象是否存在（維度成立與否）與稀缺通道形態（產物是否改寫）分屬兩問，前者路由至〈判定「失效不存在」的理由〉「套用於事件流仲裁」段。〈判定「失效不存在」的理由〉「套用於事件流仲裁」段：判準線由「事件流數量達兩條」改為「兩個以上請求來源競爭同一個稀缺通道」，並引事件流負載仲裁方法論〈1. 通道與仲裁器〉協調圖列「同一個 domain 的同一條事件流，來自不同租戶的請求彼此競爭」為據——單一事件流但有並行請求者，競爭狀態仍存在，舊判準線會誤判「無」；對象改寫句（事件流仲裁對象是競爭狀態本身）與 TAD-03a 相關段不動，皆判定階段 1／正確予以保留。`SKILL.md`〈維度與產物〉事件流仲裁列「適用條件」欄同步由「有兩條以上事件流的專案」改為「有兩個以上的請求來源會對同一個稀缺通道發出請求的專案」，消除與上述新判準線的字面不一致（原欄位是探針誤讀「事件流數量＝對象存在」的直接來源之一）。全部新增引用句已逐句 `git grep -F` 驗證來源存在；scratchpad 探針答案鍵與節錄檔的舊引文更新不在本次範圍，留待節錄檔重建時處理。

**Version**: 6.6.15 — 修正讀者探針閘門第二輪判定（3/3 門檻）標記的九處條文讀歪點，逐題不新增判準、只重排既有結構（來源見 ticket Problem Analysis）。`SKILL.md`：〈工作流〉步驟 3「已存在於 X」由同行未編號的兩件事拆為 (1)(2) 兩個編號子句（FD-01b）；〈權威缺席時〉「框架內確實無承接者」列，產物層由「該產物本專案有沒有」二元存在問句改為直接指向〈產物欄的五種合法答案〉四種答案形態（改寫產物／已存在於 X／無／待定，不含「照預設」——無權威可提供預設產物），並補「無」須符合〈判定「失效不存在」的理由〉舉證要求、不得僅因物件不存在就代入（FD-05b）；〈產物欄的五種合法答案〉「照預設」列「必須附帶」欄由「無」改「不需附帶」，消除與答案形態「無」同字異義的衝突（FD-06b）；〈與 orchestration 的協作〉表「文件回答不了地基問題的既有實作」列，把「無法回答維度表任一格」這一否定加「任一」的雙讀句改寫為明寫量詞「維度表中至少一格的產物，既有文件答不出」（PM 依 examples-process.md〈任一格即命中〉段裁定本意為既有量詞——FD-09a）；同表「規劃波之後」列判別加註「這是唯一判別依據，與是否已有相當程度的既有實作無關」排除語感誤讀，「其餘」列判別由反向定義「以上皆不命中」改寫為正面條件「該版尚未建票，且不屬於前兩列」（FD-09b）；「規劃波之後」列本 skill 角色欄，做什麼／不做什麼／狀態評語三句改為 (1)(2) 編號拆項、禁止句獨立成句（FD-10b）；〈移植前置條件〉決策文件項的四類並列改編號並各附觸發情形，明示「缺 ticket 系統不屬於這四類」；降級段補「缺的若是 ticket 系統」的明確處置（步驟 4 與建票動作不成立，不套用形態 4——形態 4 觸發條件是 skill 缺席不是 ticket 系統缺席）（FD-13a）；步驟 5 人工複核者欄「或本框架既有審查代理人」改寫為「先查……已訂者用該角色；未訂時退至……——兩者是先後查找順序而非任選其一」（FD-14b）。`references/examples.md`：FD-01b、FD-09a 對應引文同步；〈萃取揭露規格漂移〉判準來源引言補一句路由至 `component-contract-design` skill `references/step-3-fill-contract.md`〈範本中元件契約欄位表以外的章節〉，明示規格本身也指向 token 名、不複述數值（原句主詞僅「契約填寫時」——FD-02a，未新增判準，路由既有權威）。`references/examples-process.md`：FD-09a 引文同步；〈人工複核「複核者」欄的判斷順序〉引文與附註「兩個既有角色之一」改寫為「依序查定後選中的那一個角色」，消除任選讀法（FD-14b）。全部修改逐句 `git grep -F` 驗證未破壞既有引用鏈；未涉及 `dimension-product-notes.md` 事件流仲裁段落。

**Version**: 6.6.14 — 補齊讀者探針閘門盤點中「依據只在範例內」的兩則（FD-20a、FD-20b，來源票 Problem Analysis）。`dimension-product-notes.md`〈判定「失效不存在」的理由〉：事件流仲裁的對象由「事件流」與「稀缺通道」兩個獨立詞改寫為「多個事件流競爭同一個稀缺通道」這一個競爭狀態（單一事件流不構成此對象存在）——原寫法字面上會推出「有一條事件流即對象存在、不得填無」，與適用條件「有兩條以上事件流的專案」矛盾，判定為真衝突並修正對象定義；同節新增「套用於事件流仲裁」段，把「僅一條事件流時填無＋重評條件」的結論從範例表移到本節條文，路由回〈事件流仲裁的四種答案形態〉表既有理由句。〈事件流仲裁的四種答案形態〉表前新增照預設／改寫產物選擇準則的通用段（看稀缺通道形態、不看事件流數量），原本只在範例表「改寫產物」列的理由句「預設產物假設多通道並存，單通道專案套用會產生恆為零的分片與跨通道欄位」同步移入條文。全部新增引用句已逐句 `git grep -F` 驗證來源存在。`SKILL.md` 僅同步 `metadata.version`，正文未改。

**Version**: 6.6.13 — 落檔前一輪讀者探針閘門盤點中「需新增判準」項目的獨立評估者建議（五則：C-01b、C-09b、C-13c、C-15、C-11a）。`examples.md`〈問作用不問存在〉第二則檢驗問句（C-01b）由「哪一種錯誤更難被發現」的論證敘事換成問步驟 3 要求的確認動作，改引〈工作流〉步驟 3「確認該處確實承擔該產物，不建票」與〈判準的通用形式〉。`examples-process.md`〈文件回答不了地基問題〉（C-09b）刪除【產出】表「相當程度的實作」列與對應第二則檢驗問句——「相當程度」定義未有依據句、範例自行承載了判準，是否需要定義另立 spawn request 交後續分析。`examples-process.md`〈降級記錄須為缺項對照步驟表〉（C-13c）標題改〈降級記錄須記缺項與不成立步驟〉，【產出】候選改以「只記缺項」與「缺項與不成立步驟皆記」區分內容而非格式，不對記錄格式（對照表／散文）下判準。`handoff-mode.md`〈萃取的前提是既有 artifact 可信〉表下（C-15）新增判準：「不可得」以本次地基波內能否取得決策判定，須已詢問、不設等候期限，不以決策者是否仍在組織內判定；範例【輸入】【產出】【驗證】同步改為「已詢問但本次地基波內未獲回應」。`dimension-product-notes.md`〈複合產物與多子樹分列〉首段末（C-11a）新增複合產物落票判準：UI 維度依既有「四塊各自的實作票」，其餘維度同一維度內兩個以上需建票的子產物按 `.claude/pm-rules/task-splitting.md`〈拆分後檢查清單〉B（共用檔案者合併）與 `.claude/rules/core/cognitive-load.md`〈速查三閾值〉（超標者拆）判定票數，本 skill 不另立通則；`examples-process.md`〈產物落為地基票的粒度〉【產出】改為 UI 四塊候選對照、資料庫維度標明兩候選不構成對照，【驗證】補一則相反分支問句（非 UI 維度共用檔案子產物是否各自成票）。全部新增引用句已逐句 `git grep -F` 驗證來源存在。`SKILL.md` 僅同步 `metadata.version`，正文未改。

**Version**: 6.6.12 — 讀者探針閘門第一輪盤點（來源 ticket 的 Problem Analysis）標記本 skill 63 個判準中 18 個「條文查無」或「部分覆蓋」，逐項落檔。13 項屬階段 1（權威已寫明，補句或補路由）：`examples.md`〈萃取揭露規格漂移〉補路由至 `component-contract-design` skill〈畫布漂移〉（畫布為準只限視覺值、規格改寫指向 token 不複述數值）；〈權威形態不符時的正確取用〉〈產物有而判準無〉〈UI 維度適用條件不限時的處置〉〈產物欄「無」與「待定」的拆兩問判斷〉四則各補一段引言句或腳注，路由至 `SKILL.md`〈權威缺席時〉既有句（答得出具體形狀即完成、兩者獨立）；`examples-process.md`〈規劃波進行中與之後〉〈執法載體掃描範圍〉〈人工複核「複核者」欄〉三則各補一句明文（地基票不計入該版票已建、部分涵蓋即不通過、複核者兩來源為先後順序且個人不合格）；`handoff-mode.md`〈但補特徵測試有規模閘門〉段落內補句明文規模閘門判準對象是抽取範圍覆蓋率非總行數；`dimension-product-notes.md`〈可觀測性維度〉〈UI 維度〉各補一段標明具體失效形狀為示範產出、可套用部分是推導問句本身。順帶修正前一輪落地時遺留的兩處引導漂移：`examples.md`拆兩問 Q2 引文與現行 `SKILL.md` 逐字不符（漂移於後續版本演進插入子句）已同步；`dimension-product-notes.md`〈可觀測性維度〉Q3 補「改寫產物」與「照預設」的擇一條件引用。5 項屬階段 2（需新增判準，僅寫草稿交 WRAP，未落檔）：空殼 scaffold 是否算相當程度實作、複合產物是否依產物切一律分票（非 UI 維度通則）、降級記錄格式是否須為對照表、決策者無回應多久算不可得。逐項處置表、階段 2 草稿全文與會改變哪些判定的推演見來源 ticket Problem Analysis。

**Version**: 6.6.11 — PM 依 WRAP 評估結果裁決：6.6.10 標註的三處「本則推導無條文依據句」缺口，根源是 foundation-design 自己一直沒有〈產物欄的五種合法答案〉「無」列理由欄的合格判準，不是任何維度權威缺席，補一條本 skill 自有判準即可三處一起處置，不牴觸「不重新定義任何維度的判準」（此判準管本 skill 自己的附帶欄合格條件，不涉及任何維度判準本身）。`dimension-product-notes.md`〈各維度要防的失效（權威出處）〉表後新增〈判定「失效不存在」的理由〉小節：填「無」的理由須指出權威原句所講的對象在本專案不存在（六維度對象逐字對照原句：UI＝語意、測試＝行為、資料庫＝正式狀態、DevOps＝版本與依賴、可觀測性＝系統、事件流仲裁＝事件流與稀缺通道）；某一種發生途徑不存在（背景執行緒／獨立部署動作／圖形介面框架）不構成失效不存在；失效目前尚未發生不構成失效不存在；對象存在時不得填「無」。〈可觀測性維度〉【驗證】改回三問（甲的理由是否構成失效不存在、乙可否填無、乙的具體化該走哪種答案），刪除標註；〈UI 維度〉【驗證】改回兩問（甲的理由是否構成失效不存在、乙的具體化步驟依據哪條），刪除標註，SR-1 缺口一併解決；`examples.md`〈UI 維度適用條件不限〉在既有問句前補一問（甲以無圖形介面框架為由填無是否成立）。`examples.md`〈拆兩問〉整則情境由「純函式庫盤點 DevOps 維度」換為「不保留跨次執行狀態的 CLI 格式轉換工具盤點資料庫維度」——原情境的步驟 1 推導（沒有獨立部署動作因此 DevOps 失效不存在）經查證與 reliability.md「CI gate 不以獨立部署為前提」矛盾，屬內容正確性錯誤而非缺引導，換情境後步驟 1 改用「正式狀態在本專案不存在」，可通過新判準逐字對應；原 Q1、Q2 逐字保留，新增一問測試步驟 1 理由是否指出對象不存在；刪除「本則推導無條文依據句」標註，原「本則檢驗問句已刪除」註記的「CI gate」改「資料庫維度產物」。步驟 3「產物層已判無，判準層不需再檢查」是否與〈權威缺席時〉「兩者獨立」牴觸，另立 ANA 票追蹤（本筆步驟 3 沿用原文）。`SKILL.md`〈產物欄的五種合法答案〉「無」列必須附帶欄補路由至新小節；全部新判準句逐句 `git grep -F` 確認皆為單一權威定義（來源見 ticket Solution）。

**Version**: 6.6.10 — 6.6.9 標註的三處「本則推導無條文依據句」逐一查證是否已有可引原句：`dimension-product-notes.md`〈可觀測性維度〉grep「背景執行緒」「非同步任務」「每次輸出」等詞於 `.claude/skills/` 範例外零命中，仍無可引原句，維持標註；〈UI 維度〉（SR-1）新查得元件庫雙向約束方法論〈多形態的契約結構〉「同一語意可落在不同容器」與〈形態因素先決〉CLI 合法形態兩句，主詞貼近失效定義但描述跨裝置形態具體化、與本例「單一 CLI 形態內樣式數量唯一是否等於失效不存在」軸不同，引用即屬橋接，維持標註；`examples.md`〈拆兩問〉步驟 1 通讀 `saas-tech-selection` reliability.md，查得反向訊號——Day one 最小集「CI gate：測試+lint 通過才能 merge」不以獨立部署動作為前提，與本例結論方向相反，指向內容正確性層級的問題（是否應重新設計本例結論）而非缺引導，超出本票補依據句範圍。三處皆判定需新判準或需重新檢視內容正確性，本筆未變更 `dimension-product-notes.md`／`examples.md` 任何文字；候選方向與會使哪類判定改變的推演記錄於來源 ticket Problem Analysis。`SKILL.md` 僅同步 `metadata.version`，正文未改。

**Version**: 6.6.9 — 6.6.8 落地後第二輪非撰寫者複核，7 問中僅 2 問合格，依「預期答案裡的每個判定詞都要直接出自引文，不准補寫橋接句、不准另找旁證」收斂規則封頂修補輪數：不合格問句一律刪除，不再改寫。`dimension-product-notes.md`〈可觀測性維度〉Q1、Q2 皆刪除（Q2「甲答錯」與【產出】仍矛盾，Q1 判定詞不在引文內），【驗證】改標「本則推導無條文依據句」，格式對齊既有〈UI 維度〉標註。`examples.md`〈UI 維度適用條件不限〉Q1（「甲：無」是否合格）與 Q3（自證問句，判定失效存在的依據）刪除，Q2 保留不動；Q3 缺口併入〈UI 維度〉同一缺引導追蹤，不另立標註。〈拆兩問〉Q3（框架無 CI gate 判準時是否標「待定」）刪除——條文依據其實存在（`SKILL.md`〈權威缺席時〉「框架內確實無承接者」列「兩者獨立」），缺口在問句寫法本身而非條文缺席，故不標「無條文依據句」，僅原位置註記可引條文；Q4（步驟 1 判定失效不存在的依據）刪除並標「本則推導無條文依據句」（引文只定義失效本身，推不出「無獨立部署動作故失效不存在」這一步，且與依賴失敗一段無因果）。Q2 出處補檔名 `SKILL.md`（原引文缺書名）。三處「本則推導無條文依據句」（可觀測性維度、UI 維度、拆兩問步驟 1）合併追蹤於來源 ticket 的 spawn 票。逐問表與複核紀錄見來源 ticket Solution。本筆依約束不動 `SKILL.md` 正文，僅同步 `metadata.version`，補齊 6.6.6 起延後的版號同步。

**Version**: 6.6.8 — 6.6.7 落地後由非撰寫者複核，7 問中 4 問合格、3 問仍在「只讀引文推得出預期答案」欄不合格，依「預期答案裡的每個判定詞都要直接出自引文，不准補寫橋接句、不准另找旁證」的收斂規則做第二輪修正（此輪為修補輪數上限，之後仍不合格者直接刪除問句而非再改寫）。`dimension-product-notes.md`〈可觀測性維度〉Q2：預期答案與【產出】矛盾（【產出】的甲已作答但答錯，非跳過作答），改「甲跳過推導問句」為「甲答錯」。〈UI 維度〉Q1：依據句「套到別的形態時預設產物常需改寫」的對象是「預設產物」，推不出預期答案「失效定義裡的『樣子』須改寫對應到具體形態」（對象從產物換成失效定義），且「不是原權威預設的視覺元件外觀」查無出處；兩條依據句皆補不出這一步具體化，問句整條刪除，改於【驗證】段標註「本則推導無條文依據句」並記錄不同軸判準缺口，另建 spawn request 追蹤補條文。`examples.md`〈UI 維度適用條件不限〉同型缺口的乙候選補一則可完全由引文讀出的問句（失效定義本身「同一語意長出多種樣子」），並在問句內明寫「具體化到欄位順序與色碼語意」這一步仍無依據句、不另立問句頂替。〈拆兩問〉Q4：依據句「壞的版本怎麼在使用者看到之前被攔下、依賴失敗時系統怎麼不跟著一起死」只有前半（部署攔阻）能被「本專案沒有獨立部署動作」這個情境推出，後半（依賴失敗連鎖）與有無獨立部署動作無關，純函式庫仍可能有依賴；預期答案與引文皆收窄為只保留前半句。範圍外兩處逐字化：`dimension-product-notes.md`〈可觀測性維度〉【產出】「出事了沒人知道」改「系統壞了怎麼知道、知道了怎麼定位」；〈UI 維度〉【產出】「同一概念長出多種樣子」改「同一語意長出多種樣子」（UI Q1 問句本身因整條刪除而不需同步改字）。逐問句引文、出處與範例外命中數見來源 ticket Solution 第二輪複核表。本筆依來源 ticket 約束仍不動 `SKILL.md`，metadata.version 落差狀態不變。

**Version**: 6.6.7 — 修補 6.6.5 落地的驗證段依據句，依 `chained-examples.md`〈驗證段機械檢查：依據句可溯性〉逐問表六條規則獨立複核後仍不合格的 5 問與 2 個自證問句。`dimension-product-notes.md`〈可觀測性維度〉Q2 引文改逐字複製（原引文用刪節號與『』替代原文的完整句與「」，且預期答案含「背景服務崩潰」一詞在條文與本 skill 全文皆查無出處，屬自行發明）；〈UI 維度〉Q1 補第二條依據句「套到別的形態時預設產物常需改寫」，使「樣子須改寫對應到本專案具體形態」這個判定詞有出處。`examples.md`〈UI 維度適用條件不限時的處置〉Q1 引文改引 `SKILL.md` 維度表「適用條件」欄實際儲存格「不限（權威非 SaaS 特定）」與〈產物欄的五種合法答案〉表「無」列「判定後不產出」（原引文為欄名與儲存格拼接，範例外零命中）；同段【產出】乙候選的「UI 防的是『同一概念長出多種樣子』」改為條文原句「同一語意長出多種樣子」；原 Q2「產出是否有依據句」為自證問句，改寫為「乙判定『改寫產物』而非『照預設』的依據是什麼」並引〈產物欄的五種合法答案〉表「改寫產物」列。`examples.md`〈產物欄「無」與「待定」的拆兩問判斷〉Q1、Q3 引文的內層引號由『』還原為 `SKILL.md` 原文的「」（原改寫後範例外零命中）；【產出】步驟 1「DevOps 防的是『部署後才發現壞掉、或壞掉後救不回來』」改為條文原句「壞的版本怎麼在使用者看到之前被攔下、依賴失敗時系統怎麼不跟著一起死」；原 Q4「步驟 1 是否有可溯的依據句」為自證問句，改寫為「步驟 1 判定『該失效在本專案不存在』的依據是什麼」並引上述改正後的原句。逐問句引文與命中數見來源 ticket Completion Info。本筆依來源 ticket 約束不動 `SKILL.md`，metadata.version 與 CHANGELOG 版號的落差維持 6.6.6 已宣告的延後同步狀態，留待下一次變更 `SKILL.md` 時一併收斂。

**Version**: 6.6.6 — `references/adjacent-assets-boundary.md` `version-bootstrap` 列補寫兩份文件對「UI」一詞的分工與銜接點：`version-bootstrap` Step 2 呈現通道表第一列「由本應用程式渲染的視窗、頁面、彈窗、疊層、應用內提示，含唯讀畫面」判 UI 類，只控制三項前置檢查與 Step 4.5（僅含 UI 提案的版本）的地基波；本 skill 維度表 UI 列適用條件「不限（權威非 SaaS 特定）」，涵蓋全部呈現形態的一致性判定。銜接點為呈現通道表第二列既有的「交 `foundation-design` UI 維度判改寫產物」動作句，本 skill 承接後依 `dimension-product-notes.md`〈改寫產物是主路徑〉推導問句「這個維度要防的失效，在本專案會長成什麼樣子」處理，不因缺圖形介面而判「無」。動因：跨文件分析確認兩端判定各自成立（`version-bootstrap` 的三項前置與地基波確實只適用圖形介面；本 skill 的 UI 維度「不限」也確實成立），缺的只是本 skill 端未寫明分工，本次只補交界說明、不新增判準、不改兩端既有判定句。`version-bootstrap` 端的銜接動作句已存在，不需反向路由，未變更該 skill 檔案。本筆未同步 `SKILL.md` metadata.version（並行編輯中，留待下一筆一併同步），本檔與 frontmatter 版號的暫時落差待下一次 `SKILL.md` 變更時收斂。

**Version**: 6.6.5 — 補條文：`dimension-product-notes.md`〈改寫產物是主路徑〉新增〈各維度要防的失效（權威出處）〉表，六維度逐一引維度表指名權威的原句（元件庫雙向約束方法論、`tdd`／`saas-tech-selection` skill、事件流負載仲裁方法論、專案可觀測性規則）。動因：`dimension-product-notes.md` 兩則範例（可觀測性、UI）與 `examples.md` 一則範例（DevOps 拆兩問）的驗證段先前以「可觀測性防的是『出事了但沒人知道』」「UI 防的是『同一概念長出多種樣子』」「DevOps 防的是『部署後才發現壞掉、或壞掉後救不回來』」為依據句自證，三句在條文皆查無出處，範例代替條文承載了推導問句的起點判準（依 chained-examples〈驗證段機械檢查：依據句可溯性〉，此屬缺引導）。三則範例的驗證段已改引用新表；`examples.md` 另一則 UI 範例（適用條件不限時的處置）同步補一句引用，避免同型缺口。`SKILL.md` 僅同步 metadata.version，正文逐字未改（token 收斂後餘量為零，新增內容全數落在 references）。全檔（`examples.md`／`dimension-product-notes.md`）既有驗證段依據句可溯性重跑一次，結果見來源 ticket Solution 自檢表。

**Version**: 6.6.4 — 對齊待定列與〈非 SaaS 專案的形態轉換〉指同一狀態的用詞。一輪讀者探針把 `dimension-product-notes.md` 的「無可用部分」與 `SKILL.md`〈產物欄的五種合法答案〉待定列的「無可用判準」判成兩件不相關的事，以「權威無可用部分」為由直接填待定——正是待定列明文排除的推理，只讀 `SKILL.md` 一側的讀者無法把題幹用詞對回排除句。`SKILL.md` 待定列改「無可用判準」為「無可用部分」，`examples.md` 對應〈驗證段機械檢查：依據句可溯性〉的依據句同步改寫以免斷鏈；`dimension-product-notes.md` 原文未改（已是統一後的用詞）。CHANGELOG 6.5.1／6.5.2／6.2.0 三筆歷史記錄保留原詞，不回溯改寫。判準表其餘欄位逐字未改。

**Version**: 6.6.3 — 修正 6.6.2 的兩處過度刪除（PM 驗收未通過，2026-09-15）。token 收斂重新收斂為（5612 -> 4998，門檻 5000）：**逐字還原**〈與 orchestration 的協作〉「交回下游不等於下游覆蓋全部維度」段落被刪除的「實查 `version-bootstrap` 的步驟，`DevOps` 與 `可觀測性` 兩個維度在其全文 0 命中」句（原刪除誤判其為可省略的查證過程，實為後句「這兩個維度」的指涉來源，刪除後指涉懸空）；「判別不用身份也不用二元存在性」段落被刪除的說明句因還原後仍超標，改為**逐字外移**至新建 `references/situation-judgment-notes.md`，原處留最小路由句（原刪除誤判其為舉例敘事，實為判準的引導依據，屬〈超標了怎麼辦〉診斷表「選路之後才用到的細節」而非「論證與實證敘事」，處置應為外移而非刪除）。〈本 skill 不做什麼〉節被刪除的「各自完整…缺口在可發現性，不在能力」段，grep 確認 CHANGELOG／references 皆無同義記錄，改為**逐字外移**至新建 `references/entry-point-rationale.md`，原處留最小路由句。「本文件指名權威用名字」段落刪除的兩次實測後果句維持刪除——該事實已完整見於本檔 6.0.0 條目（「路徑是它現在放在哪…可攜性閘門把 25 處路徑判為指名他專案的檔案而擋下推送」），不重複記錄。判準表（維度表、產物欄五種合法答案、權威缺席時四形態、工作流、移植前置條件）逐字未改。

**Version**: 6.6.2 — token 收斂（5612 -> 4978，門檻 5000）。〈與相鄰資產的交界〉整節逐字外移至新建 `references/adjacent-assets-boundary.md`（該節原文即自述「執行時不需要先讀本節，不確定某件事該由本 skill 還是相鄰資產處理時再查」，屬拆分前已存在的讀者情境，非本次新造）；〈按需讀取〉表新增一列路由。另依 `splitting-an-existing-skill.md`〈補範例後超標：觸發條件與處置順序〉步驟 1 先行處置：〈本 skill 不做什麼〉節移除一段純敘事（「各自完整，但沒有任何一處回答…缺口在可發現性，不在能力」，說明性重複前一句已成立的結論）與指名權威用名字段落中重複 6.0.0 已記錄的實測敘事（框架改版移動 hook 位置、可攜性閘門擋下 25 處路徑）；〈與 orchestration 的協作〉節移除兩處敘事——「判別不用身份也不用二元存在性」段落中對 AI session 身份判定與 changelog 誤觸的舉例說明，以及「交回下游不等於下游覆蓋全部維度」段落中「實查 version-bootstrap 的步驟…0 命中」的查證過程（該事實已完整保留於新建的 `adjacent-assets-boundary.md` version-bootstrap 列）。判準表（維度表、產物欄五種合法答案、權威缺席時四形態、工作流、移植前置條件）逐字未改。〈按需讀取〉表首句「下表四份」因新增一列而改為「下表各份」，避免可變計數內嵌數量（同〈集合命名用角色、不內嵌數量〉判準）。

**Version**: 6.6.1 — 補一處缺引導：〈與 orchestration 的協作〉「交回下游不等於下游覆蓋全部維度」段補一句，定義 DevOps／可觀測性盤點票的驗收條件與完成定義——不另立新判準，直接依循既有〈產物欄的五種合法答案〉與〈工作流〉步驟 2、4：完成步驟 2 產物欄判定；判定為「已存在」或「無」即本票完成；判定為「照預設」「改寫產物」或「待定」則依各答案形態的必附項落為地基票並登記 `blockedBy`，本票完成標準為該登記動作已完成。新約束套〈何處需要範例〉判準：純路由既有已封閉的判準與表格，未引入新的主觀判讀點，判定不需要範例。

**Version**: 6.6.0 — 一輪審查逐節掃描確認 13 處缺範例位置（判準通則問作用不問存在、UI 適用條件不限、產物欄無與待定拆兩問、權威缺席查證對象、文件回答不了地基問題任一格即命中、規劃波進行中與之後、產物落票粒度、移植前置條件的執法載體掃描範圍與降級記錄、複合產物多子樹分列、改寫產物是主路徑推導問句的兩個維度示範、萃取的前提決策者不可得、補特徵測試規模閘門）與後續一輪修補新識別的 1 處（工作流步驟 5 人工複核「複核者」欄），依 `.claude/skills/skill-design-guide/references/chained-examples.md` 三段鏈式格式全數補齊範例。`examples.md` 既有五則案例同步改寫為三段鏈式格式（含去除語言相依路徑與框架路徑，如色碼常數表位置、資料填充模組路徑改為符號名描述）；`dimension-product-notes.md`〈改寫產物是主路徑〉兩則個案改寫為含驗證段的完整範例，〈事件流仲裁的四種答案形態〉表移除專案層指涉；`handoff-mode.md` 新增「決策者不可得」與「規模閘門」兩處範例。**體量處置**：`examples.md` 補入範例後量測達 5744 tokens，超出單檔 5k 門檻，依 `splitting-an-existing-skill.md`〈補範例後超標：觸發條件與處置順序〉依讀者情境（維度判斷 vs 流程操作）拆出新檔 `examples-process.md`，兩檔分別為 3653／2386 tokens；`SKILL.md`〈按需讀取〉表同步補列新檔。範圍不含 version-bootstrap（由並行票處理）與一項仍未歸入任何修補範圍的工作流缺引導（另行提報後續處理）。

**Version**: 6.5.3 — 補六處缺引導（既有審查確認 agent 依此文件執行時被迫自行發明規則，皆判定不改變既有判定者，直接寫入）。**〈權威缺席時〉形態 2「權威存在但形態不符」**：改寫後產物的合格判準改為回答 `dimension-product-notes.md`〈改寫產物是主路徑〉推導問句，答得出具體形狀即完成；同步移除〈工作流〉步驟 3 與本形態互指卻各自無終止條件的循環引用，步驟 3 改為單向指向本形態、本形態明寫「不需再回頭比對步驟 3」。**形態 4「工作流自身引用的 skill 缺席」**：決策記錄補四欄定義（維度清單、已產出產物內容、缺席 skill 名稱、重排觸發條件），觸發條件固定為該 skill 出現在執行 session 的 `.claude/skills/`。**步驟 5 人工複核**：補驗收條件須寫的三項（複核者、複核依據、複核產出），機械檢查形態仍留給各維度權威自行設計。**baseline 表達形式**：補違規計數上限／檔案清單白名單兩種形態的擇一依據，`handoff-mode.md` 規模閘門段落改指回本定義避免兩處各說一次。**`handoff-mode.md` 步驟 1 盤點表欄位**：補值／位置／是否被斷言涵蓋三欄定義。**`handoff-mode.md` 末段三種情形抽取**：由僅列舉情形改為各自附處置（共用可變實例改抽不可變值或工廠函式、編譯期常數位置抽取後仍須維持編譯期常數形式、平台設定檔或行內樣式改指單一事實來源或由建置產生）。

**Version**: 6.5.2 — 依獨立評估者 WRAP 裁決落地兩則原文矛盾修法（起草草稿未採用，改採 WRAP 定稿）。**命名時值變不變**：`handoff-mode.md` 新增〈命名與收斂分兩步〉——第 2 步仍逐值命名、值不變，疑為雜訊的值記入決策文件、收斂另開票與命名同批建立（安全性掛在行為中立，命名與改值同步發生會使畫面變化無法歸因）；來源為設計畫布尚無程式碼綁定時例外，萃取當下即可歸納，`examples.md`〈萃取不等於照抄〉補一句範圍界定並指回新段落。**填「無」或「待定」**：改用產物層／判準層兩問取代「能否具體說出理由」的自評式判準——`dimension-product-notes.md`〈非 SaaS 專案的形態轉換〉改為先答〈改寫產物是主路徑〉推導問句（失效在本專案長成什麼樣子），失效不存在才標「無」，權威無可用判準另標「無判準」建框架缺口票；`SKILL.md`〈產物欄的五種合法答案〉表「無」「待定」兩列同步改寫，移除「待定」列原「判準缺席」括注的舊語意（單獨的判準缺席不再是填待定的充分條件）。

**Version**: 6.5.1 — 修正兩處文字不一致：`dimension-product-notes.md` 引用錨點「產物欄的四種合法答案」改為現行標題「產物欄的五種合法答案」；`troubleshooting.md` 末列「不在五個維度內」與 `SKILL.md`〈本 skill 不做什麼〉節「五個維度全部已有既有權威」皆改為引用〈維度與產物〉表而非寫死數字（維度表現為六列；依〈集合命名用角色、不內嵌數量〉同一判準，與 6.4.0/6.3.1 兩筆同類）。另有兩處原文矛盾（handoff-mode「值不變」vs examples.md「萃取不等於照抄」；dimension-product-notes「無可用部分標無」vs SKILL.md「判不出來一律待定」）待獨立評估者裁決後另筆記錄，本次未落地改文。

**Version**: 6.5.0 — 依一輪低階 model 讀者探針（同一份 SKILL.md 派 3 個實例，指令逐字相同、無審查框架、禁臆測填補）處置四項。**「權威缺席時填『待定』不得填『無』」由 0/3 帶走改為進主路徑**：〈產物欄的四種合法答案〉擴為五種，「待定」成為表列答案形態之一，原本置於〈權威缺席時〉之後的獨立說明段併入表下並移除，避免兩處各說一次；工作流步驟 2 的括號同步列入「待定」。探針顯示原文對該規則已用粗體兩次加一整段專講而三份無一帶走——歸因為位置而非強調密度，處置因此是移位不是再加強調。**新增〈按需讀取〉表**（四份 reference 各自何時讀），原本三份 reference 散在三個章節內文提及，讀到其中一節的人不知道另兩份存在。**新增〈與相鄰資產的交界〉表**並首次指名 `dart-style-guardian`——原文載體表列「執法工具」一類、工作流步驟 5 要求「接入執法載體」、Common Issues 有一列專講執法載體失效，三處皆用類別名從未指名實例，探針 Q5 三份無一提及它。交界表刻意不列 `tdd`／`saas-tech-selection`／`design-decision-framework`／`project-init`：維度表與開頭已逐列指名並給判別句，重列只會讓讀者多讀一次而不多知道一件事。**體量**：`Common Issues` 外移至新建的 `references/troubleshooting.md`，〈已知的證據基礎限制〉併入 `references/examples.md` 並更名〈已驗證與未驗證的專案形態〉，共外移 773 tokens；淨增 27 tokens，SKILL.md 4872 tokens。

**Version**: 6.4.0 — 維度表新增一列「事件流仲裁」：權威為事件流負載仲裁方法論（判準在此，本 skill 不複述），預設產物為通道清單＋每通道的仲裁器落層＋九格卸載順序表，適用條件為有兩條以上事件流的專案（不限 SaaS）。`references/dimension-product-notes.md` 補該維度的四種答案形態範例，改寫產物的典型為單線程桌面 App 只有注意力通道。frontmatter description 同步補列；「本表列的是五個常見維度」改為不寫死數字（新增一列後原數字即失真，依〈集合命名用角色、不內嵌數量〉同一判準）；移植前置條件的權威清點勾選項同理改為不寫死 skill／方法論數量。動因：事件流負載仲裁方法論定稿後，地基盤點原無一格問「多條事件流搶同一通道時誰裁決」。

**Version**: 6.3.1 — 〈維度與產物〉UI 列指向元件契約程序的括注由「十一欄位」改為「元件契約欄位表」。該欄位數已因 `component-contract-design` 改版而變動，內嵌數量在成員數改變時即失真——與本庫〈集合命名用角色、不內嵌數量〉同一判準。改寫由另一個 consumer 專案完成並經框架 canonical 傳入，本專案取回。

**Version**: 6.3.0 — token 收斂（6,615 -> 4,795，門檻 5,000）：〈維度與產物〉節的非 SaaS 處置細節、複合產物、多子樹分列、改寫產物範例、無 UI 框架元件庫產物外移至新建 `references/dimension-product-notes.md`；〈接手模式〉整節外移至 `references/handoff-mode.md`；〈Examples〉五則案例外移至 `references/examples.md`。入口檔三處保留短版路由並補內文提醒，判準表（維度表、四種合法答案、權威缺席時）因跨處被大量引用而全數留在入口檔。跨檔指涉逐一改具名（`SKILL.md`〈某節〉／`references/<檔名>.md`〈某節〉），不留位置詞。

**Version**: 6.2.0 — 維度與產物表補「適用條件」欄；資料庫／DevOps／可觀測性三列的權威 `saas-tech-selection` 明示為 SaaS/伺服器端專案預設形態，非 SaaS 專案（CLI 工具、函式庫、單機 app）改依既有「改寫產物」機制取可用部分，無可用部分則改指專案自身 CLAUDE.md 技術選型記錄或標「無」+ 理由；不新增第二套權威。實測依據：非 SaaS 專案（如 Go CLI 工具）走此路由會被引導至 managed database／k8s 等不適用選型。本筆原編為 6.1.0，與 canonical 上另一筆獨立的 6.1.0 撞號——兩者由不同 consumer 各自從 6.0.0 前進一步，版本號是單調計數器不表達分叉，合併時以 canonical 為共用時間軸故本筆順延為 6.2.0。

**Version**: 6.1.0 — UI 列補反向路由：元件庫那塊實作前的契約程序指名 `component-contract-design` skill，預設產物的元件庫依賴補「契約齊全」。動因：多輪審查發現本 skill 自稱地基單一入口，UI 列卻零次指向契約程序，即本檔 Examples 自述的「錯誤的承接宣告誘發漏做」形態

**Version**: 6.0.0 — 引用形式由檔案路徑改為指名身分（skill 用名字、方法論與規則用標題）。路徑是「它現在放在哪」會隨改版移動，名字是「它是什麼」不會；且 skill 在本系統以名字載入，寫路徑等於繞過既有載入機制。實測依據：框架改版移動 hook 位置使註冊失效、可攜性閘門把 25 處路徑判為指名他專案檔案而擋下推送。

**Version**: 5.0.1 — 「無 UI 框架的元件庫產物」自「已知答不出來」改為指向權威。上游 component-library 方法論 1.8.0 已補〈Web/HTML 端 L2 特別判準〉的「元件規則 SSOT」與「元件邊界的可驗產物」兩條，該缺口不再存在。

**Version**: 5.0.0 — 判準通則化為「問作用不問存在」（四處存在性判準改為有效性）；資料庫列補備份還原驗證；形態 3 拆判準層與產物層；萃取前提補第四種「決策者不可得」；命名前提補規模閘門；〈老專案模式〉更名〈接手模式〉。依據為六個 repo 的實跑驗證。
