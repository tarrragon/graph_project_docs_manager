# 分層測試策略：依架構層級選測試方法

## 目的

依架構層級選擇測試方法的決策指引。不同層的程式碼性質不同（UI 是互動流程、UseCase 是業務行為、Domain 是規則邏輯），統一測試手法會造成過度測試或覆蓋不足。不依架構分層制定策略，重構時大量測試壞掉（測太細）或上線後出 bug（測太粗）。依本文五層分工表和判斷流程，為每層選擇適合的測試方法。

---

## 五層測試分工表

| Layer                   | 測試類型     | 覆蓋率要求    | 觸發條件（何時寫測試）                              | 跳過條件（何時不測）                           |
| ----------------------- | ------------ | ------------- | --------------------------------------------------- | ---------------------------------------------- |
| 1 UI/Presentation       | 整合測試     | 關鍵流程 100% | 流程失敗影響核心業務、多步驟操作、涉及金流/敏感資料 | 靜態展示頁面、簡單列表                         |
| 2 Application/Behavior  | 條件單元測試 | 複雜邏輯 100% | 轉換含條件判斷、計算邏輯、多來源資料、邏輯 >10 行   | 簡單 DTO 欄位直接映射（由 UseCase 層間接覆蓋） |
| 3 UseCase               | BDD 測試     | 行為場景 100% | 所有業務場景，無例外                                | 無（必測層）                                   |
| 4 Interface             | 不測試       | 0%            | —                                                   | 介面只定義合約，無可測試行為                   |
| 5 Domain Implementation | 單元測試     | 分支 100%     | 業務規則驗證、計算邏輯、狀態轉換、不變量檢查        | 純資料容器 Entity                              |

UseCase 層每個 UseCase 至少涵蓋：1 個正常流程 + 2 個異常流程 + 3 個邊界條件。只 Mock 外層依賴，使用真實 Domain Entity。

---

## 判斷流程

先定位程式碼所屬層級（目錄結構反映架構層級），再問該層一個問題：

1. **UI 層**：這是關鍵互動流程嗎？→ 是：寫整合測試（必須覆蓋操作行為——tap→navigate / submit→callback / error→fallback，不只覆蓋元件存在性） / 否：跳過，讓人工測試覆蓋
2. **Behavior 層**：這裡有複雜轉換邏輯嗎？→ 是：寫條件單元測試 / 否：跳過，讓 UseCase 層間接覆蓋
3. **UseCase 層**：直接寫 BDD 測試，不問
4. **Interface 層**：不測試，不問
5. **Domain 層**：這裡有複雜業務規則嗎？→ 是：寫單元測試 / 否：跳過，純資料容器由上層覆蓋

本判斷流程判定完成後，若專案已導入測試對象目錄，將判定結果登載為對應測試對象的「測試形態」欄（見 `references/test-object-catalogue.md` 契約欄位表）。

---

## 覆蓋率語意

覆蓋率數字背後有實際語意，不是為報告好看：

| 層級           | 覆蓋率指標          | 語意                                                      |
| -------------- | ------------------- | --------------------------------------------------------- |
| UseCase        | 行為場景覆蓋率 100% | 所有業務場景都有測試（非程式碼行數百分比）                |
| Domain         | 分支覆蓋率 100%     | 每個分支代表一個業務決策，都必須驗證                      |
| 整體新增程式碼 | 80% 以上            | 扣除跳過層（Interface、簡單映射、純資料容器）後的合理基線 |

---

## 測試穩定性原則

測試因業務改變而失敗，代表測試在保護業務正確性——這很好。測試因重構內部實作而大量失敗，代表測試耦合了實作細節——這是設計問題。

| 層級          | 穩定性預期 | 說明                                               |
| ------------- | ---------- | -------------------------------------------------- |
| UseCase BDD   | 高         | 關注行為，重構內部邏輯只要業務行為不變，測試不需動 |
| Domain 單元   | 高         | 只有規則本身改變才需更新                           |
| Behavior 條件 | 中         | 只測複雜轉換邏輯，重構簡單映射不影響測試           |
| UI 整合       | 低         | UI 變動頻繁，只測關鍵流程以控制維護成本            |

---

## 分層之外的補位形態

五層分工假設「假設正確、驗實作」；部分情境跳出這個假設，各自需要專屬形態：

**stub 盲區與語意級假後端**：bug 的成因是「我們對外部服務行為的假設錯誤」時，由測試餵資料的 stub 從結構上驗證不出來——stub 回放的就是那個假設、假設與斷言出自同一人之手，測試必然綠。補位是**有狀態的語意級假後端**——「語意級」對比「回放級」（逐請求回放預錄回應）：它持有狀態、模擬已被證實的外部行為（不是想像的行為），讓多個元件走完整互動鏈的流程測試；並與週期性的真實後端驗證測試配對，讓外部行為漂移有地方現形。判讀訊號：整合測試的「後端」沒有自己的狀態機、只在回放——它驗的是自己的假設。

**角色化替身（依回饋分層分工）**：〈掛載點〉的回饋分層（互動元件層／服務受理層／event 效果層）承擔者不同，測試替身的角色需求因此依層分工，不是一組角色套用全部：

| 層 | 所需角色 |
|----|---------|
| 一、互動元件層 | **零角色**——不注入任何服務替身、不做 provider override，只建構元件本身並觸發互動 |
| 二、服務受理層 | 已受理（回傳受理訊號但最終結果未到）、處理中（持續回報進度但未完成）、永不受理（連受理訊號都不發） |
| 三、event 效果層 | 成功、失敗（業務層級失敗，非例外）、逾時、拋出平台例外 |

第一層的零角色是最容易被漏掉的一項：它看起來像沒有工作要做——沒有角色可列、沒有替身可寫——因此最容易被跳過建立形態。但它其實是「拔掉服務還看得到反應嗎」這個判準問句的機械化實現：不提供任何 override、連替身都不給，觸發互動後斷言回饋仍存在。現行元件測試普遍以完整 provider 環境（含可用替身）建構 widget tree，驗的是「整個環境配合下元件有回饋」，不是「元件自身就有回饋」——兩者外觀相似、判準完全不同；且零角色恰是三層中最容易寫的一類測試（不需注入、不需狀態機），正因為容易，更沒有人會特地為它建立形態。

第二層需要可停留的狀態，「回傳假資料」做不到：已受理／處理中／永不受理驗證的是服務生命週期的中間狀態——事件送出後、結果尚未到達之前那段時間，服務側應提供「收到了、正在處理」的回饋。這與既有替身「回傳假資料」的能力不同：回傳假資料是呼叫一次方法、立即得到一個完整終局結果（成功或失敗），中間沒有停留；可停留的狀態要求替身能表達「已回應但未完成」（如受理結果的 Future／Stream 仍 pending）或「持續回報但未完成」（如處理中的進度事件持續 emit）。現行替身建構當下即決定單一終局結果，沒有表達「尚在途中」的能力，是這個角色集合要補的缺口。

與 stub 盲區的關係是同根不同尺度的元件級特化延伸：語意級假後端解決整合層「只扮演可用」的盲區（有狀態、跑完整互動鏈）；角色化替身解決元件層同一盲區在三個回饋層各自的表現——第一層的盲區來自測試環境本身（provider 掩蓋了元件是否自帶回饋），非 stub 造成；第二層的盲區是 stub 只回放終局、不模擬中間狀態；第三層的盲區是 stub 只扮演成功、不扮演失敗。兩者不互相取代，各自覆蓋不同尺度的驗證對象。

角色以行為定義，具體建構子名只作示例（以下為 Dart）：

```dart
// 第二層：服務受理層（可停留狀態）
Fake{Service}.acknowledged();      // 已受理，最終結果仍 pending
Fake{Service}.processing(stream);  // 處理中，持續回報進度
Fake{Service}.neverAcknowledges(); // 永不受理，連受理訊號都不發

// 第三層：event 效果層（處理結局）
Fake{Service}.succeeds(result);
Fake{Service}.fails(errorResult);
Fake{Service}.timesOut(delay);
Fake{Service}.throwsPlatformError(exception);
```

**不變式 INV-FEEDBACK-001 的驗證形態**：〈角色化替身〉回答「各層測試替身有哪些角色」；不變式——元件在每一次互動都必須發出第一層回饋，即使服務因防抖、節流或冪等而丟棄該次呼叫——進一步指定「用哪個角色驗哪一種違反路徑」，是角色化替身在不變式驗收上的延伸應用，不新增角色。

驗證形態分別覆蓋下列情境，各自對應〈角色化替身〉表中的一層角色：

- **服務刻意不回應時，第一層回饋仍須出現**：注入第二層「連受理訊號都不發」的角色（永不受理），觸發互動後斷言第一層回饋（如 pressed 視覺）仍在單一影格內出現。此情境驗的是「服務刻意不執行」與「服務不可用」同構——第二層角色缺席不能讓第一層跟著缺席。
- **等待窗口內連續觸發，每一次都各自有回饋**：不注入任何角色（第一層零角色），在同一等待窗口內對同一元件連續觸發多次，逐次觸發後各自斷言第一層回饋出現。此情境驗的是回饋不因窗口內重複觸發而只在首次或末次出現——每次觸發都是獨立的第一層事件。
- **disabled 狀態的成因須排除防抖**：不注入任何角色（第一層零角色），在等待窗口進行中的當下檢查畫面上可互動元件，斷言其 enabled 狀態不因防抖、節流或冪等窗口而被設為 false（阻擋整畫面等待態的既有取消鈕豁免路徑除外）。此情境驗的是「防抖決策發生在服務側」的歸屬邊界——disabled 若在元件層被設，即代表決策被誤植回元件。

時間相關斷言遵守 `.claude/rules/core/test-assertion-design-rules.md` 規則 D1：連續觸發與等待窗口內的檢查一律以固定影格推進驗證，不以 `Stopwatch.elapsed*` 搭配 `lessThan` 作 pass-fail 判準。

不以驗證形態的情境數量為段落標題或跨檔錨點（依 `.claude/references/reference-stability-rules.md` 規則 10，情境數量可能隨規格演進增減）；角色沿用〈角色化替身〉既有的行為語意描述，不重複定義具體建構子名。

**characterization test（重構與 legacy 適配）**：接手零測試的纏結程式、或重構前無法先寫正確性測試時，先對現有程式跑一次、把實際輸出記錄成預期值——斷言對象是「現在的行為」而不是「正確的行為」。它是重構的安全網：拆依賴的過程紅燈代表行為改了（不一定是 bug、但影響範圍現形）；拆完、正確性測試補上後，characterization test 的歷史使命完成、視重疊程度逐步移除。與正確性測試混用會讓紅燈無法歸因。

**跨邊界驗證**：五層分工表以架構層級為軸，隱含假設是「程式碼的行為留在 app process 內、由測試框架完整操控」。功能一旦滿足下列任一判準即跳出這個假設：呼叫離開 app process（跨行程/跨執行環境）、需人工回應系統 UI（權限對話框、通知中心、原生選擇器等）、依賴平台授權狀態（已授予/已拒絕/尚待決定）、依賴外部程序（作業系統行程、CLI 工具、外部服務）。這類程式碼可能出現在任何一層——UI 層觸發、UseCase 層協調、Interface 層呼叫平台 API 皆有可能——因此跨邊界驗證不是新增第六層，而是與既有補位形態同性質的**正交屬性**：判準命中時，不論所在層級為何都須疊加此驗證形態，五層分工表本身不變。

驗證形態是雙軌並行，不是二選一：**fake 覆蓋 App 端邏輯**——App 呼叫平台 API 前後的判斷、狀態轉換、錯誤處理，用 fake（非回放級 stub）驗證邏輯正確性，可自動化、可在既有測試套件中執行；**實機驗證覆蓋平台端事實**——平台 API 實際回傳什麼、UI 實際如何反應、授權流程實際如何運作，只有在真實裝置/真實作業系統上操作才能確認，無法以自動化測試替代。兩軌覆蓋不同的正確性面向：純自動化測試驗不出平台行為是否符合假設（假設本身可能是錯的），純實機操作則無法在每次變更後重複執行；只做其中一軌都會留下分層測試觸及不到的盲區。

產出回寫規格：實機驗證測得的平台事實，不是量測完就結束的一次性動作——須回填對應規格文件的驗證章節，成為已確認事實供後續實作與測試引用；尚未完成實機驗證的項目，應在規格中明確標記為待驗證，不得以 fake 測試通過視為已驗證。

驗證形態（fake / 實機 / 兩者）判定完成後，若專案已導入測試對象目錄，登載為對應測試對象的「驗證形態」欄（見 `references/test-object-catalogue.md` 契約欄位表）。

---

## 技術性必檢項目

不分層級都要納入的基礎驗證，容易被忽略但上線後常出問題：

- **Null 值和空集合**：輸入為 null / 空陣列 / 空字串時的行為
- **邊界值**：零、負數、最大值、溢位
- **異常處理**：網路錯誤、儲存失敗、逾時
- **資料驗證**：格式、範圍、必填欄位

---

## 與 monitor 專案的對應

monitor 專案非典型 Clean Architecture（monorepo 含多語言 SDK + collector），五層對應需適配：

| 五層      | monitor 對應元件                                                     | 測試方法                                                              |
| --------- | -------------------------------------------------------------------- | --------------------------------------------------------------------- |
| UseCase   | collector handler（接收+驗證+儲存+查詢）、各 SDK 的 flush/batch 邏輯 | BDD 測試（GWT 場景）                                                  |
| Domain    | schema validator、event 建構邏輯、query 篩選邏輯                     | 單元測試（分支覆蓋 100%）                                             |
| Behavior  | SDK 的 event→JSON 序列化、collector 的 response 格式轉換             | 條件單元測試（有複雜邏輯時）                                          |
| Interface | `schema/event.schema.json`（契約定義）                               | 不測試（由 protocol integration 驗證契約實現）                        |
| Transport | SDK↔collector HTTP 通訊層                                            | 見 `protocol-integration-testing.md`（Mock 從結構上無法驗證協議契約） |

---

## 檢查清單

測試設計時確認：

- [ ] 已定位程式碼所屬架構層級？
- [ ] 已依判斷流程決定測試方法（而非預設全部單元測試）？
- [ ] UseCase 層每個場景至少 1 正常 + 2 異常 + 3 邊界？
- [ ] UseCase 層只 Mock 外層依賴、使用真實 Domain Entity？
- [ ] Domain 層複雜規則分支覆蓋率 100%？
- [ ] 技術性必檢項目（Null / 邊界 / 異常 / 驗證）已納入？
- [ ] 跳過的層級確認由上層間接覆蓋？

---

## 相關文件

- `references/phase2-test-design.md` — Phase 2 測試設計指引（通用三層金字塔，本文是五層展開的 refinement）
- `references/doc-handoff.md` — doc→TDD 銜接（UC 場景的整合/單元分工標準）
- `references/bdd-behavior-testing.md` — BDD 行為測試深度指引（Sociable/Solitary、分層 Mock）
- `references/protocol-integration-testing.md` — Protocol integration 三層策略（Mock 遮蔽機制）
- `references/test-object-catalogue.md` — 測試對象目錄（本文件回答「該層怎麼測」，該檔回答「要測哪些對象」；本文件的測試形態判定、驗證形態判定分別對應該檔契約欄位表的兩欄）

---

**Last Updated**: 2026-09-08
**Version**: 1.5.0 — 「角色化替身（依回饋分層分工）」後補「不變式 INV-FEEDBACK-001 的驗證形態」：列出服務刻意不回應、等待窗口內連續觸發、disabled 成因排除防抖三種驗證情境，逐一接線至角色化替身表中對應層的角色，並補時間相關斷言遵守規則 D1 的一句。情境數量不作段落標題或跨檔錨點（reference-stability-rules 規則 10）。
**Version**: 1.4.0 — 「stub 盲區與語意級假後端」後補「角色化替身（依回饋分層分工）」：依〈掛載點〉三層歸屬列出各層所需角色（第一層零角色／第二層可停留受理狀態／第三層四種結局），並釐清第一層為何最容易被漏、第二層「可停留狀態」與「回傳假資料」的差別，以及與 stub 盲區形態同根不同尺度的關係。角色以行為定義、語言無關，具體建構子名只作示例（Dart）。
**Version**: 1.3.0 — 「判斷流程」節與「跨邊界驗證」節各補一句反向路由：判定完成後登載為新增的 `references/test-object-catalogue.md` 對應契約欄位（測試形態欄、驗證形態欄），相關文件補一列。
**Version**: 1.2.0 — 節名「分層之外的兩個補位形態」改為不寫數字（reference-stability-rules 規則 10：計數會隨補位形態增減而失效）；新增第三個補位形態「跨邊界驗證」：判準（呼叫離開 app process／需人工回應系統 UI／依賴平台授權狀態／依賴外部程序）、正交屬性定位（與既有兩形態同性質，不新增第六層）、雙軌驗證（fake 覆蓋 App 端邏輯 + 實機驗證覆蓋平台端事實）、產出回寫規格驗證章節。
**Version**: 1.1.0 — 新增「分層之外的兩個補位形態」：stub 盲區與語意級假後端（假設與斷言同源、stub 結構上驗不出假設錯誤；補位是有狀態假後端跑流程測試 + 真實後端驗證配對）、characterization test（重構與 legacy 適配、斷言現狀不斷言正確、安全網用完即退場）。同步上游 testing 教材在分層策略之後長出的內容。
**Version**: 1.0.0 — 從 blog hybrid-testing-strategy-methodology 提煉，整合 monitor 專案對應
**Source**: blog repo: `content/record/hybrid-testing-strategy-methodology.md`
