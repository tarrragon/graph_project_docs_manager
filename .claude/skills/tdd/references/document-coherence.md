# 文件連貫性紀律：分級、住址、各 Phase 檢查點

## 目的

TDD 流程的文件鏈很長：Proposal → Spec → UC → 種子包 → feature-spec → 測試 → 實作與註解 → traceability → work-log。Brooks 在《人月神話》對多文件維護的結論是：同時維護多份文件不可能持續，文件之間的進度必然產生落差，而有落差的文件最終完全沒有人更新；他給的方向是把文件盡可能併入程式自身。本 reference 把這個結論落成三條紀律——文件分級、資訊住址、各 Phase 檢查點——目標是讓每份文件的**同步期待與守護機制匹配**：期待最新就要有機制守著、沒有機制就明示降級，不把同步寄託在紀律上（同步對改程式的人當下零收益、收益結構決定它不會自發發生）。

---

## 文件分級表

三級、依「同步期待 × 守護機制」分：

| 級別                 | 同步期待                           | 必要條件                                          |
| -------------------- | ---------------------------------- | ------------------------------------------------- |
| **活文件**           | 永遠反映現狀                       | 有機制守著：會紅的測試、編譯器、CI 比對腳本、lint |
| **scaffold**         | 只在被消費那一刻正確、消費後即過期 | 標記一次性身分與消費時點、過期後不得被當權威引用  |
| **append-only 記錄** | 記錄寫下當時的事實、永不回改       | 標明時點、讀者知道它是史料不是現狀                |

TDD 文件鏈逐份分級：

| 文件                           | 級別             | 機制 / 標記                                                                                                                                                                                        |
| ------------------------------ | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Proposal                       | append-only 記錄 | 需求決策史；範圍變更走新 proposal、不回改                                                                                                                                                          |
| Spec（domain）/ UC             | 活文件（有條件） | 裝有 spec skill 時由 `/spec validate` + 邊界回補流程守著（gap → DOC ticket 回補、即開文件修正任務把缺口補回 UC）；回補**只修這裡**、不修下游副本。無 spec skill 時機制缺席、降級為快照（見表下註） |
| domain-map                     | 活文件（有條件） | spec skill 的覆蓋比對腳本；無此機制時降級為快照（見表下註）                                                                                                                                        |
| TDD 輸入種子包（Ticket 內）    | **scaffold**     | 消費者是 Phase 0/1；Phase 1 完成即過期。GWT（Given-When-Then）種子是 UC 的複本、過期後與 UC 的差異不是 bug、是身分——爭議時以 UC 為準                                                               |
| feature-spec（Phase 1 產出）   | **scaffold**     | 消費者是 Phase 2 測試設計；Phase 3 綠燈後權威轉移到測試與程式碼、文件頂部標記 `status: archived (superseded by tests)`。之後讀它是讀史料                                                           |
| 測試（名稱 + GWT + assertion） | 活文件           | CI / pre-commit 自動跑——整條鏈裡唯一改壞當下會發聲的載體、**行為的權威載體**                                                                                                                       |
| 實作註解                       | 依判斷標準逐則歸類   | 見「註解判斷標準」節                                                                                                                                                                                   |
| traceability.yaml              | 活文件（有條件） | 條件是 Phase 4 用腳本比對實際測試存在性；沒有比對機制時降級為快照、標盤點日期——`covered` 是宣稱、不是現狀                                                                                          |
| work-log                       | append-only 記錄 | 時序史料                                                                                                                                                                                           |

表下註：

- **「快照」**是 append-only 記錄的一種——記盤點當天的事實、標盤點日期、不承諾反映現狀；「降級為快照」即把該文件從活文件改掛到第三級。
- **domain-map** 是 spec skill 產出的領域覆蓋對照表、僅裝有 spec skill 的專案有此列。
- **本表自成權威**：它是 skill 內的語境化版本、與任何外部原則卡的措辭差異不視為漂移、以本檔為準。
- **本表的分級是預設值**：專案在提案階段對「文件維護到什麼程度」做過決策時、以 proposal 的決定覆寫對應列。這個決策的來源：同專案裝有選型訪談類 skill 時由其 reliability 維度問出、經決策記錄的防護底線流入 proposal 驗收條件；未安裝時可在任何提案流程做成。覆寫的執行點在 doc-handoff 的 Phase 0 映射（見該檔）——讀 proposal 的文件維護決策、逐列套用到本表、結果記進種子包 Phase 0 預填的「文件分級」欄。覆寫的**權威副本是 proposal**（append-only）：種子包的分級行只是該 ticket 的材料化、Phase 1 後過期；Phase 1 之後（含 Phase 4 檢查點）查分級以 proposal 決策為準、預設表僅在無決策時生效。

**漂移只發生在錯配格**：被期待最新、卻沒有機制的文件。發現一份文件跟現狀對不上時、第一個動作是分級、不是補一條「記得同步」的規則。scaffold 標記的防護範圍要說清楚：它防的是**事後誤引**、不防**消費窗口內的上游改版**——長工期 ticket 進行中 UC 改版時、通知責任在 UC 端（改 UC 時掃一遍進行中的 ticket）、不在標記。

---

## 資訊住址表：每類資訊唯一權威載體

複製出去的每一份都是一個不會被履行的同步義務。每類資訊指定一個權威載體、其他位置**引用、不複製**：

| 資訊                                              | 權威載體                                 | 其他位置的形式                                                         |
| ------------------------------------------------- | ---------------------------------------- | ---------------------------------------------------------------------- |
| 行為（做什麼、輸入輸出、順序約束）                | 測試（名稱承載意圖、assertion 承載內容） | Spec/UC 是上游來源；種子包與 feature-spec 是 scaffold 期複本、標記過期 |
| 介面形狀                                          | 程式碼簽名 + 型別                        | Spec 介面段在實作落地後改為指涉、不逐欄複寫                            |
| 為什麼（repo 內的決策脈絡）                       | Spec Purpose / UC 前置敘述               | 測試名稱說得出行為的意圖；work-log 記決策過程                          |
| 為什麼（repo 外的事實：法規、外部契約、硬體限制） | 實作處的 doc comment                     | 測試可同時存在（測試守行為、註解留出處）                               |
| 覆蓋映射（UC 場景 ↔ 測試）                        | traceability.yaml                        | 前提是有比對機制；否則它只是快照                                       |

測試作為行為權威有兩個限定：它是**現狀的權威、不是正確性的權威**——意圖的權威在 UC/Spec、測試可能鎖住錯誤的實作；UC 與測試本體不一致時、是意圖與現狀分歧、屬需求裁決不是文件同步問題、修測試還是修 UC 由人決定（scaffold 的「以 UC 為準」「以測試為準」管的是各 scaffold 的過期爭議、不管這條）。且權威限於測試覆蓋的範圍——未覆蓋的行為、權威回到 UC/Spec、界線由 traceability 的 gap 標記呈現。

---

## 各 Phase 連貫性檢查點

每個 Phase 的驗收多問一組問題、目標是「覆蓋完整 + 表達目的與商業邏輯 + 無重複」：

- **Phase 0 / doc-handoff**：種子包寫入 Ticket 時、標明「scaffold：Phase 1 消費後過期、爭議以 UC 為準」。功能規格種子維持路標引用（現行設計）、不擴大成全文複製。
- **Phase 1（/spec init + validate）**：feature-spec 引用 Spec 的段落用指涉（SPEC-ID + 章節）、不整段複寫；只有「本 ticket 對 Spec 的增量決策」寫在 feature-spec 裡——它才是這份文件的固有資訊。validate 通過後、確認每個 GWT 場景都回答得出「這個行為為誰存在」（Purpose 接得上）、答不出的場景是需求缺口不是文件措辭問題。
- **Phase 2（測試設計）**：GWT 落成測試時、測試名稱要承載場景的意圖（讀名稱知道這是刻意規則、不是實作巧合）；名稱說不出意圖、回 UC 補、不是在測試旁寫敘述性註解。落成後 feature-spec 的場景區段已被消費——它與測試的後續差異以測試為準。
- **Phase 3（實作）**：註解逐則過判斷標準（見下節）；發現 UC 沒寫的邊界、回補 UC + traceability 的 boundaries 區、**不**回改種子包與 feature-spec（scaffold 不回補）。
- **Phase 4（重構 + 收尾）**：(1) traceability 逐 entry 用實際測試檔比對——`covered` 的 entry 找不到對應測試即降回 `gap`；此比對應落成腳本、只能人工逐條對照時依分級表把 traceability 降級為快照並標盤點日期。(2) feature-spec 標記 archived——標記前確認其增量決策已回到活載體（回補 Spec/UC、或測試名稱已承載該意圖），否則 archive 等於把別處沒有副本的設計意圖流放進史料。(3) 註解掃一輪：漂移的（描述已不符現狀）修或刪、防護動機的轉測試、重複測試已守內容的刪。(4) 抽查三個測試名稱問「拿到紅燈的人讀得出為什麼嗎」。

---

## 註解判斷標準（Phase 3 / 4 用）

檢視一則註解的第一個評估：**它有沒有解釋到這個行為、這個事件、或這個 flag 的商業邏輯**。有——留、並確認與現狀相符；沒有——先重新檢討寫它的動機、再決定去留：

1. **動機是防護**（怕有人改壞）：註解不參與執行、改壞當下不發聲；先問約束能不能消除（常是結構選擇的產物）、不能消除就交給一條會紅的測試（即防護型測試）、測試名稱承載「為什麼這個行為是刻意的」。
2. **動機是說明**：把型別名稱唸出來、看註解還剩什麼資訊；剩不下就刪——那是型別與命名已經講完的話。
3. **來源在 repo 外**（法規條號、外部契約、硬體限制）：這是 doc comment 的正當內容；可與測試並存（測試守行為、註解留出處）。

錯誤的註解比沒有註解更糟：它帶著權威外觀、讀者依它行動、而它不會發聲。

---

## 重複與割裂的判定

- **重複**：同一段敘述出現在兩份以上文件、且各份都被期待維護。修法二選一——指定權威載體其他處改引用、或把副本降級為標記過期的 scaffold。
- **割裂**：讀者要跨多份文件才能拼出一個行為的全貌。修法是讓權威載體自足（測試名稱 + assertion + reason 讀完即知道行為與意圖）、其他文件當導覽不當內容。
- **判定動作**：對任一段文件內容問兩題——「這段資訊的權威載體是誰」、「這份文件過期時誰會發現」。兩題都答不出來、這段內容就在錯配格裡。

---

**Last Updated**: 2026-08-08
**Version**: 1.0.4 — 覆寫句補條件語（選型訪談類 skill 未安裝時、決策可在任何提案流程做成）與執行點指涉（doc-handoff Phase 0 映射、含 Phase 0 被豁免時移到種子包產出步驟）——1.0.3 的覆寫句原是無執行者的被動宣稱、且在 doc 最完整的路徑上被 Phase 0 豁免規則反向保證不發生。冷讀輪另修：表下註從單段四主題拆成條列、覆寫句指明種子包「文件分級」欄為結果載體。
**Version**: 1.0.3 — 分級表明示為預設值、專案級覆寫來自提案階段的「文件維護到什麼程度」決策（選型訪談 reliability 維度問出、經決策記錄流入 proposal 驗收條件）——把分級從「無人決定過的隱形預設」變成「有人決定過的設計、預設只是沒決策時的 fallback」。
**Version**: 1.0.2 — 多輪審查第三輪修正：住址表補「測試權威的兩個限定」（現狀的權威非正確性的權威、UC 與測試分歧屬需求裁決不是同步問題；權威限於覆蓋範圍、界線由 gap 標記呈現）；Phase 4 檢查點 (1) 補「比對應落成腳本、人工對照時依分級表降級」（否則檢查點自己就是靠紀律的同步）、(2) 補 archive 前置確認（增量決策先回到活載體）；scaffold 標記的防護範圍宣告（防事後誤引、不防消費窗口內的上游改版）；Spec/UC 表格 cell 收短、「快照」定義與 domain-map 說明抽成表下註、並宣告本表自成權威（與外部原則卡的措辭差異不視為漂移——取代 1.0.1 那種一次性對齊、消滅跨 surface 的無機制同步期待）。
**Version**: 1.0.1 — 審查修正：Spec/UC 與 domain-map 的機制欄改為條件式——原寫法指名 spec skill 的工具（`/spec validate`、覆蓋比對腳本）、單獨安裝 tdd 的專案這兩格機制不存在、Spec/UC 會靜默落入本文件自己定義的錯配格；改為「有 spec skill 時由它守、沒有時依本表紀律降級為快照」、正好示範本 reference 的降級處置。活文件機制枚舉補 lint（與來源原則卡對齊——兩份三級表曾在同一天漂移一個詞、是「雙副本必漂移」的即時實證）。
**Version**: 1.0.0 — 初始建立。依《人月神話》文件維護論點 + 對本 skill 文件鏈的盤點（GWT 四份副本、traceability 狀態欄是宣稱、feature-spec 無生命週期、邊界回補單向）落成三條紀律：文件分級（活文件要機制、scaffold 標消費時點、記錄不回改）、資訊住址（行為的權威載體是測試）、各 Phase 檢查點（含註解判斷標準）。
