# tdd 版本紀錄

新到舊。版號規則與兩個住址（本檔與 `SKILL.md` frontmatter 的 `metadata.version`）見專案的 skill 同步規範。

**Version**: 2.10.6 — 落檔上一輪盤點延後的四則新判準草稿（WRAP 複核後三則查得既有出處只需路由或改寫、一則半句屬新判準，用戶就用戶偏好項裁定）。`references/doc-handoff-uc-to-gwt-conversion.md`：〈GWT 種子與行為單元的分工邊界〉括號句「場景為列索引，非欄位本身」改為依範本資料列判定——場景欄只填 `{編號}` 即為列索引，判準看欄內容而非欄名，填了編號以外內容的欄不論欄名為何都不是列索引。`references/phase2/rules.md`：Q_new3〈行動路徑〉補分類判準——存在性驗證對應〈確定性斷言的基礎形態〉「結構驗證」、值驗證對應同表「功能正確性」，並依用戶裁示明列範圍斷言與長度或筆數等於固定值者皆屬值驗證；Q_new4〈行動路徑〉補路由（此表即〈產出（Layer 1）〉所列非功能性需求測試對照表）與用戶裁示（組 2 未觸發時仍產出、標「不適用」，取代原本隱含的「不觸發即不產出」讀法）；〈前置條件驗證強制規則〉表後補歸類判準——依驗證方式欄對照斷言檢查內容，不看作用元素，同一元素的「存在」與「可操作」拆為兩條前置條件。`.claude/methodologies/bdd-testing-methodology.md` 同型表格與本表分歧，未同步改動，另案處理。
**Version**: 2.10.5 — 落檔讀者探針審查閘門第 4 批 tdd-1 盤點命中的讀歪結構：既有權威可用改寫或補句消解者落檔，需新增判準且查無非範例出處者草稿停手，留存於觸發本次修補的內部工作記錄，未落入本檔。`references/test-object-catalogue-completeness-criteria.md`：C3 判準名稱由「來源唯一」改為「規格條目不被兩行引用」，消解名稱與機械檢查方式分處兩欄時可能被讀成「每行僅一個來源」的字面歧義。`references/test-object-catalogue.md`：契約欄位表後補〈驗證形態的選值判準〉段，路由 `layered-test-strategy.md`〈跨邊界驗證〉既有的雙軌定義（fake 覆蓋 App 端邏輯、實機驗證覆蓋平台端事實）——原選值依據只在已刪範例的【驗證】段成立，條文本身未寫。`references/test-object-catalogue-starting-points.md`：起點 B 步驟 B3 改寫為三步驟編號，消解單一格內三句混寫、標記作用對象（拆分前的整則測試）不明的讀法風險。`references/bdd-behavior-testing.md`：〈BDD→單元拆分標準〉訊號表「Given 設置過長」列補入〈多訊號同時成立時〉段既有的後置條件（移除已拆出邏輯的 Given 設置後仍 > 10 行 Mock，才考慮拆分 Module），消解只讀表格未讀後段落而漏掉後置條件的風險。`references/doc-handoff.md`：〈前置閘門〉「涉及 UI」段補呈現通道表兩列字面重疊時的優先序句，路由 version-bootstrap `references/step-rationale.md` 既有判準（依查看／操作當下的呈現位置，不依版面是否由本應用程式產生）——原優先序只以括號夾在呈現通道表列舉中間，未寫成獨立判定句。另有 4 則讀歪點（When／Then 書寫邊界的 app 自有狀態旗標歸屬、終端回饋訊息三類驗收不可合併為一條斷言、characterization 條目 C2「事件輸出記入觸發它的進入點」缺理由、單一 Given 訊號是否適用多訊號段後置步驟）條文全域查無非範例出處，屬新增判準需另行評估，草稿留存於觸發本次修補的內部工作記錄，未落入本檔。
**Version**: 2.10.4 — 落檔讀者探針審查閘門第 4 批盤點命中的讀歪結構：既有權威可用改寫或補句消解者落檔，需新增判準且查無非範例出處者草稿停手，留存於觸發本次修補的內部工作記錄，未落入本檔。`references/phase2/rules.md`：Q8 標題「是否被至少一個測試的斷言直接覆蓋」改為「是否被至少一個測試的斷言驗證」，與緊接的行動路徑「透過公開入口呼叫，確認私有方法的邏輯被斷言驗證」用字一致——原標題「直接」與行動路徑「透過公開入口」（間接驗證）字面矛盾，讀者可能誤將「呼叫並附帶一個不鑑別內部邏輯的斷言」計入覆蓋。Q10〈行動路徑〉補一句路由：涉及日誌等非功能性需求的路徑，驗證方式依同組〈非功能性需求測試推導表〉——原句只要求「建立對照表、補充測試案例」，未路由至定義具體驗證方式（如 Mock logger 驗證 call count 和參數）的既有表格。
**Version**: 2.10.3 — 落檔上一輪盤點延後的兩則新判準草稿（獨立 WRAP 評估者複核後定案，PM 裁定：探針雖 3/3 通過未觸發，但屬實際條文缺陷，本輪修補）。`references/phase1-split-methodology.md` 與 `references/phase1/rules.md`〈版本號分配規則〉兩處逐字相同位置（架構層級執行順序表之後、框架整合提示之前）補一句路由：架構層級執行順序表是「情況 / 版本/批次分配」表在說明欄所列依賴成立時算出的批次，不是另一套判準；任務直接依賴與該列說明欄不符時，不沿用本表，改依分配原則表排在其全部直接依賴任務中最晚批次的下一批；並補判「否」反例（評分顯示元件僅依賴 Domain 值物件、未依賴 UseCase，排第二批而非本表固定的第三批）。原草稿「以較晚者為準」一句經複核為分配原則表逐字結果，非新增 max 判準，未採用。`references/phase1/rules.md` Q5〈行動路徑〉取代「靜默」段：警告子句「catch 後回傳預設值者記 warning」明確限定於「任一項不成立」分支，三項同時成立時即使 catch 回傳預設值仍可選靜默（註解說明原因即滿足 `.claude/rules/core/observability-rules.md` 規則 1 表首句「否則需附註說明原因」），消解原文「回傳預設值者記 warning」語序造成的無條件讀法；同步路由改指規則 1 表「catch 後 return 預設值」列（原引「空 catch」列，情境不符）；補判「否」反例（清除本機過期紀錄，僅 (a) 不成立，不可靜默）。`.claude/rules/core/observability-rules.md` 未改動——WRAP 評估兩份規則於交集情境本不衝突，規則 1 表首句已提供「附註說明原因」出口，問題只在 tdd 側 Q5 子句射程與引錯列。
**Version**: 2.10.2 — 落檔讀者探針審查閘門第 4 批盤點命中的 7 則標記，三則屬既有權威缺補句（階段 1，落檔）、兩則屬依據只在範例內但既有條文已足以推出正確判定（查明後維持現狀不改檔）、兩則屬需新增判準（階段 2，草稿停手未落檔）。`references/doc-handoff-uc-to-gwt-conversion.md`：〈無 UC 步驟的 FR → GWT 種子〉「一步驟多結果的拆分」句後補一句，明寫主詞「一個場景步驟」不限本節、UC 場景步驟出現同樣情形時同樣適用（原段落位置與主詞射程不一致，屬讀法風險）。`references/task-granularity-rules.md`：〈禁止的模式〉表後補一句，釐清「每 1-2 個操作一個實作 Ticket」不牴觸 1:1 映射——CRUD 操作拆出多個行為單元後，1:1 仍以行為單元為單位，不得合併兩個行為單元進同一張實作 Ticket，路由至 `phase1/rules.md`〈行為單元識別〉既有句。`references/phase2/rules.md`：Q_new3〈行動路徑〉補存在性驗證「足夠」的判定（目標功能失效時該驗證會翻紅即為足夠，不強制改驗值本身），為既有「不足」條件之邏輯反面，路由依據不變（`.claude/rules/core/test-assertion-design-rules.md`〈鑑別力規則〉）。`test-object-catalogue.md`〈反例欄填法範例〉「須另寫出誤判理由（指出兩者驗證對象差異）」與 `phase2/rules.md` Q7〈組 1：中間步驟測試〉「單一委派直接測最終結果」兩則，查明其明文雖只在可能被移除的範例區塊內，但緊鄰的既有條文（〈三、契約欄位表〉反例列問句、組 1 觸發條件句）已足以推出探針的正確判定，不受該範例存廢影響，維持現狀不改檔。`phase1-split-methodology.md`〈分配原則〉表與〈架構層級執行順序〉表對同一任務給出不同批次時何者優先、`phase1/rules.md` Q5 靜默三條件與 `.claude/rules/core/observability-rules.md` 規則 1「catch 後回傳預設值必須記 warning」在三條件同時成立時的適用關係，條文全域查無優先序判準，屬新增判準需另行評估，草稿留存於觸發本次修補的內部工作記錄，未落入本檔；後者另涉修改 `.claude/rules/core/` 資產，草稿另交 PM 裁決前不逕改規則檔。
**Version**: 2.10.1 — 落檔上一輪盤點中唯一延後的新判準草稿（獨立 WRAP 評估者複核後定案，非用戶程式碼風格偏好，由 PM 裁定）：`references/phase1/rules.md` Q5〈行動路徑〉後補「靜默」判準段——定義「靜默」為不記日誌且不通知使用者；(a)(b)(c) 三項 AND 成立才可選靜默（不寫入持久狀態／結果列在規格正常流程／呼叫端有處理分支），任一項不成立即選日誌；選靜默時 catch 區塊須以註解說明原因，路由 `.claude/rules/core/observability-rules.md` 規則 1 空 catch 列，消解與該規則的潛在衝突。既有反面範例「快取 miss」補上「規格正常流程列有快取 miss 分支」的前提，使 (b)(c) 可從範例輸入逐字判定；新增判「否」反例（設定檔讀取失敗回預設值，正常流程未列該結果，不可選靜默）。
**Version**: 2.10.0 — 落檔讀者探針審查閘門盤點命中的 7 則部分覆蓋範例，六則屬既有權威缺路由或缺補句（階段 1，落檔）、一則屬需新增判準（階段 2，草稿停手未落檔）。`references/phase1/rules.md`：Q1〈行動路徑〉補「比對相似度須排除語義不同的情形」，路由至 `PC-006-premature-unification-abstraction.md`〈判斷標準〉「各呼叫的語義不同」；Q2〈行動路徑〉同源補「語義不同者各自實作，不共用」。`references/phase2/rules.md`：Q8 補「本題沿用組 1 觸發條件，簡單委派（只呼叫一個子方法）不觸發本題」；Q_new3〈行動路徑〉補「存在性驗證不足」的判準，路由至 `.claude/rules/core/test-assertion-design-rules.md`〈鑑別力規則〉；Q11〈行動路徑〉補「本題檢查對象是目標版本資料工廠，遷移函式測試的輸入端使用舊版欄位不算殘留」；〈前置條件驗證強制規則〉表後補「前置條件不成立時不得執行對應的 When」。Q5（何時可選靜默）條文全域查無、非漏找，屬新增判準需另行評估，草稿留存於觸發本次修補的內部工作記錄，未落入本檔。
**Version**: 2.9.0 — 落檔上一輪盤點中兩則屬新增判準的條文查無項目（獨立評估者複核採用改引既有他處條文的做法，非新增第二套定義）：`references/doc-handoff.md`〈前置閘門〉表後補兩句——「完整場景步驟」改引 `doc-handoff-uc-to-gwt-conversion.md`〈轉換方法〉表（須同時可取出場景步驟動作與預期結果）；「涉及 UI」改引 `version-bootstrap` SKILL.md Step 2〈UI 類判別〉呈現通道表（操作或預期結果落在第一列「圖形介面」才涉及 UI，判定不以畫面是否新增或改動為條件），取代原本只在範例內成立、且與呈現通道表定義衝突的隱含判準；同步改寫〈前置閘門判定範例〉輸入（補呈現通道）與產出／驗證段落，並追加相反分支驗證題（終端互動不觸發本檢查項）。另修正〈執行步驟〉與檢查清單「前置閘門三項全通過」與閘門表實為 4 列的計數不一致，改為不寫死數字、直接引用閘門表
**Version**: 2.8.0 — 讀者探針審查閘門盤點命中：範例教了判定但條文查無或僅部分覆蓋，逐項補條文依據（兩階段處置：權威已寫明者落檔補句或補路由；需新增判準者草稿停手，不逕改檔）。`references/test-object-catalogue.md`：〈斷言來源欄範例〉引言句補一句連結「三、契約欄位表」既有必填後果至兩欄重疊填寫情境；〈六、派發語言〉範本後補段落，路由至 skill-design-guide `references/chained-examples.md`〈【驗證】段〉執行期行為允許／禁止表（When 不寫觸發機制的具體實作方式、Then 不寫框架內部表示方式）與本 skill `phase2-test-design.md`〈GWT 格式〉Given/Then 定義（狀態轉換測試 Given 對前一狀態、Then 對後一狀態）；另兩則判定（反例欄應填具體誤判情境而非僅類別名；斷言來源與來源規格條目重疊時的照抄／分填規則）經查已由對應範例緊接【輸入】上方的引言句涵蓋，該類引言句視為條文，未改檔。`references/doc-handoff-uc-to-gwt-conversion.md`：〈GWT 種子與行為單元的分工邊界〉範例前段落補句，路由至 `doc-handoff.md`〈TDD 輸入種子包格式〉種子包範本表頭定義「欄位僅限 Given/When/Then/來源四欄」。另兩則判定（完整場景步驟須具備動作與預期結果；含使用者操作即涉及 UI）條文全域查無、非漏找，屬新增判準需另行評估，草稿留存於觸發本次修補的內部工作記錄，未落入本檔。
**Version**: 2.7.0 — 修補範例把條文沒有的判準藏進範例（本輪批次稽核命中，7 處各自的判準只活在範例驗證段、條文查無依據句，經獨立評估者逐項 WRAP 定稿）。`references/test-object-catalogue-completeness-criteria.md`：C3 補「比對的單位是規格宣告，不是欄位字串」——同一規格條目內可獨立驗證的多個宣告得依分支分行，同一宣告的不同驗收面向（如終端回饋訊息三類驗收）不得以附註分行偽裝成不同字串。`references/test-object-catalogue.md`：連動改寫〈測試對象單位判定範例〉的分支標記格式與檢驗問句，指向新增的 C3 判準。`references/doc-handoff-uc-to-gwt-conversion.md`：檔頭句改為「一個或多個」GWT 種子；補〈一步驟多結果的拆分〉條款（不同 Given／When 的預期結果各拆一種子；同一組 Given／When 的多個結果亦拆多個種子）；「跨步驟狀態累積」補「依賴」判準（缺該狀態則 When 無法執行或 Then 不同才算依賴，未依賴者仍可寫入 Given，非禁止）；連動改寫兩則範例的檢驗問句。`references/test-object-catalogue-starting-points.md`：起點 B 步驟 B3 補「混合測試（含可追溯與追溯不到斷言）拆為兩則」規則，可追溯斷言不隨判定一併刪除；連動改寫〈追溯不到規格來源的判定範例〉。起點 C 步驟 C2「每個公開介面一條」改為「每個可呼叫的公開進入點一條」（明列公開方法／模組層級公開函式／以函式實作的公開存取子；事件等輸出記入觸發它的進入點，不另立條目）；連動改寫〈characterization 條目粒度範例〉標題與檢驗問句。`references/doc-handoff.md`：Phase 1 映射表「UC 前置/後置條件→驗收標準清單」列補「不可量化時原文照抄並標〔不可量化〕，不自行發明門檻值」；連動改寫〈驗收標準轉換範例〉。`references/bdd-behavior-testing.md`：〈BDD→單元拆分標準〉訊號表下方補「多訊號同時成立時，不移動 Module 邊界的動作優先於拆分 Module」（依同檔〈Sociable vs Solitary 選擇框架〉既有原則排序，非新增判斷基礎）；連動改寫〈訊號優先序範例〉兩則檢驗問句。七項定稿條文與 WRAP 評估過程（含被否決的候選解讀）另存於觸發本次修補的內部工作記錄，不重複於本檔。
**Version**: 2.6.0 — 依讀者任務三問重切超標檔案並外移版本紀錄（不刪範例、不壓縮規則文字，搬移程序依 skill-design-guide `references/splitting-an-existing-skill.md`）。`references/doc-handoff.md`（9160→約 4800 tokens）拆出 `doc-handoff-uc-to-gwt-conversion.md`（UC 步驟→GWT 轉換規則：轉換方法、無 UC 步驟的 FR 種子、轉換注意事項、GWT 種子與行為單元的分工邊界）與 `doc-handoff-test-tier-assignment.md`（整合測試 vs 單元測試分工：分工決策框架、判斷原則、跨邊界步驟：平台決定、與 UC 資訊鏈的關係），兩者各為執行步驟 3、4 循序執行中讀者在該步驟才需要的完整程序。`references/test-object-catalogue.md`（7401→約 5000 tokens）拆出 `test-object-catalogue-starting-points.md`（五、三種起點的推導程序，起點 A/B/C 三選一互斥）與 `test-object-catalogue-completeness-criteria.md`（四、齊全判準，填寫與驗收分屬不同讀者時刻）。三檔（`doc-handoff.md` 7 條、`test-object-catalogue.md` 5 條、`bdd-behavior-testing.md` 2 條，附帶處理）原內嵌於檔尾的版本紀錄外移至本檔〈各 reference 檔案獨立版本紀錄〉節，三檔改採本檔統一版號、檔尾僅留 Last Updated 與指向本檔的一句。全站跨檔引用（含 `doc-handoff.md`↔`test-object-catalogue.md` 互指、`bdd-behavior-testing.md`→`doc-handoff.md` 的分工框架引用）已改指向對應新檔精確章節。
**Version**: 2.5.0 — 補齊 spec/UC 轉測試缺範例與既有範例改寫（審查子票命中，agent 依此推導元件與測試時被迫猜測或讀出兩種做法）。`references/doc-handoff.md`：〈前置閘門〉補判定範例（場景步驟完整性、涉及 UI 判準）、〈Phase 1 輸入映射〉補〈驗收標準轉換範例〉、〈轉換注意事項〉補範例（跨步驟狀態累積、角色切換標注）、〈轉換範例〉改寫移除請求路徑語法、〈GWT 種子與行為單元的分工邊界〉改寫為反向檢驗問句、〈與 UC 資訊鏈的關係〉改寫移除儲存技術名並補整合測試分工範例、〈跨邊界步驟：平台決定〉補判定範例、〈追溯矩陣初始化〉補建立範例。`references/test-object-catalogue.md`：補測試對象單位判定、反例欄填法、斷言來源欄、追溯不到規格來源、characterization 條目粒度共五則範例，〈六、派發語言〉三則既有範例全數改寫為三段鏈式格式並移除框架相依用語（原「解析完成回調觸發」「元件樹」「查表 helper」）。`references/bdd-behavior-testing.md`：〈BDD→單元拆分標準〉補訊號優先序範例。三檔合計新增十二則、改寫六則範例，皆依 `.claude/skills/skill-design-guide/references/chained-examples.md` 三段鏈式格式撰寫，範例不含本專案識別符。
**Version**: 2.4.0 — 補齊 spec/UC 轉測試四處缺引導（審查與走查子票命中，agent 依此推導時被迫猜測或讀出兩種做法）。`references/doc-handoff.md`：新增〈無 UC 步驟的 FR → GWT 種子〉節（FR 前提/觸發條件/宣告結果對映 Given/When/Then 的表格，無觸發條件時標「僅整合測試覆蓋」，同步泛化種子包表格的來源欄以容納 FR 來源）、〈轉換注意事項〉補「替代／例外場景歸屬」條款（歸屬分岔點所在步驟的種子，跨步驟分岔另立獨立種子）、〈新專案起手模式〉新增〈模組群組切法〉節（以 spec `domain` 為單位，超出任務拆分閾值再依架構層切分）。`references/test-object-catalogue.md`：「四、齊全判準」補「多來源取最具體」段（C3 的判斷依據——具體性順序：畫面狀態矩陣列優先於使用案例步驟、使用案例步驟優先於規格功能需求，其餘同義來源括號附註不另立行）。四處原僅有映射方向、範例或「不重複」要求，未定義判斷規則。

**Version**: 2.3.0 — 新增 `references/test-object-catalogue.md`：doc-handoff 回答「規格如何映射為測試輸入」、layered-test-strategy 回答「該層用什麼方法測」，兩者皆假設「要測哪些對象」已定，本檔補上這一步——測試對象單位判定（規格條目，附與元件契約不重複的論證）、來源類別表、契約欄位表（各欄問句與缺失後果）、齊全判準 C1-C4（與既有覆蓋率判準為補充關係）、三種起點（有規格／有測試無契約／有程式碼無測試）推導程序、派發語言句型。doc-handoff 與 layered-test-strategy 各補一句反向路由指向本檔（避免既有資產指向新資產、新資產不指回的單向接線）。SKILL.md 相關資源補一行路由。

**Version**: 2.2.10 — `references/doc-handoff.md` 與 `layered-test-strategy.md` 隨框架 canonical 更新（由另一 consumer 撰寫並經 canonical 傳入）；本專案取回後補號，前一版兩側同號而內容不同，依版號無從察覺

**Version**: 2.2.9 — 清理 1 處 language-constraints.md 規則 2 禁用詞基線債務（`數據` → 資料，`references/cases/cross-module-shared-strategy-gaps.md` 段落標題），純用語修正，內容與判準未改。

**Version**: 2.2.8 — 補記 2.2.7 之後累積但未 bump 版號的變更（版號未動使發佈庫與本庫的內容分歧無法由版號察覺）。`hooks/layer-boundary-validator-hook.py` 與 `references/phase4-refactor.md` 移除註解／版本紀錄行內的專案 ticket ID，指令行為未變。同時清掉只存在於發佈庫、本庫已不再持有的 `examples/flutter-sdk-tdd-walkthrough.md`——該檔以另一專案的 SPEC 與 ticket 編號逐段敘述，本 skill 宣告 `metadata.portable: true`，此類內容正是可攜性閘門要擋的形態

**Version**: 2.2.7 — 版本紀錄搬到同目錄的 `CHANGELOG.md`。skill 是 runtime 整份載入的檔案，而沒有任何規則要求任何人讀版本紀錄，留在 SKILL.md 等於每次叫用都付一次無效讀取。SKILL.md 末尾留一行指過去，版號的兩個住址改成「CHANGELOG.md 最上面那一條 + frontmatter 的 metadata.version」。skill 的指令內容一個字都沒改。

**Version**: 2.2.6 — 術語校正：判準全數改為判斷標準（動作修飾語縮為「X 標準」、狀態義改為「X 條件」）。判準的語域在哲學與教育評量、工程讀者解析不了——五份低階模型探針一致回報非通用

**Version**: 2.2.5 - `references/phase1-split-methodology.md` 檔尾的 `**Source**` 指向已併入本 skill 的 `tdd-phase1-split/SKILL.md`，該檔不再存在故被判為斷鏈。此為歷史遷移軌跡（記錄本文件的來源），加 `broken-link-exempt` marker 並在行內註明原檔已併入，保留可追溯性。
**Version**: 2.2.4 - 多輪審查鏈路實證修正：v2.2.3 宣稱的「Phase 0 讀取」在檔案層並不存在——覆寫是三處互相引用、零處有執行載體的宣稱、且新專案起手模式的 Phase 0 豁免讓它在 proposal 最完整的路徑上必然不發生（本 skill 自己的 document-coherence 所批評的「被期待卻沒有機制」形態）。修法：doc-handoff v1.1.2 補 Phase 0 映射列與豁免不豁免覆寫的規則、document-coherence v1.0.4 補執行點指涉與跨 skill 條件語。
**Version**: 2.2.3 - 「程度決策歸提案階段」對齊：document-coherence v1.0.3 分級表明示為預設值、proposal 有「文件維護到什麼程度」決策時以 proposal 覆寫（決策由 saas-tech-selection v1.1.0 的 reliability 訪談問出、經決策記錄流入 proposal 驗收條件、Phase 0 讀取）；doc-handoff v1.1.1 對 saas-tech-selection 的引用加條件語（未安裝非死鏈）。
**Version**: 2.2.2 - 多輪審查第二、三輪修正。phase2/rules v2.3.0：讀者分層指引與轉換條件的 DQ 範圍補上 Q12-Q14（原停在 Q7-Q11、執行者可合法跳過新組——雙處複製當天漂移的實證）、組 4 補恆觸發宣告與擴充指引註解、Q12 補防護型測試定義、防呆補 protocol integration 指涉、檔級版本記錄補上（原停在 2026-06-14）。document-coherence v1.0.2：測試權威兩個限定（現狀 vs 正確性、覆蓋範圍界線）、Phase 4 比對要落成腳本否則依分級降級、archive 前置確認（增量決策先回活載體）、scaffold 標記防護範圍宣告、表格 cell 收短與表下註、宣告本表自成權威以消滅跨 surface 無機制同步期待。test-naming-conventions：例句補批次合併情境（v2.2.1 漏報：該檔與 layered-test-strategy 在 2.2.1 批已各有 gloss 修正與死鏈移除、此處補記）。layered-test-strategy：「語意級 vs 回放級」對比前移、移除不存在的 `專案方法論目錄的 ` 死鏈。
**Version**: 2.2.1 - 多輪審查修正三處：(1) Q13 的 flaky 分類改回教材四類原名（計時依賴 / 環境差異 / 資源競爭 / 非確定性輸出）——v2.2.0 曾未標示地重切成三類、環境差異被誤併進外部服務、資源競爭無落點；計時依賴主策略同步改為事件驅動取代固定等待、clock 注入降為補充。(2) document-coherence v1.0.1：Spec/UC 與 domain-map 的機制欄改條件式——原寫法指名 spec skill 的工具、單獨安裝 tdd 時這兩格機制不存在、Spec/UC 靜默落入本文件自己定義的錯配格；活文件機制枚舉補 lint 與來源對齊。(3) 檢查清單第 12 項同步四類措辭。
**Version**: 2.2.0 - 測試設計理念對齊上游 testing 教材的現行水位。對照盤點發現本 skill 的測試 references 是 2026-06 從教材提煉的快照、之後教材長出的內容沒有跟上（快照漂移、正是 document-coherence 講的錯配）。四處補強：(1) phase2/rules.md 新增組 4「防護視角與訊號品質」（Q12 每條測試答「未來哪種改壞要被擋」、TDD 紅燈是破壞實測的制度化、綠燈後補的防護型測試要手動故意破壞驗一次；Q13 flaky 三源自查——計時 / 執行順序 / 外部服務、非確定約束不掛提交閘門；Q14 手寫測試資料是真實環境的乾淨子集、parser / 驗證類補錄製或生成資料）+ 檢查清單三項、phase2-test-design.md 以 blockquote 指涉不複寫；(2) test-naming-conventions v1.1.0 加「名稱承載意圖」（失敗輸出只給症狀、名稱回答為什麼這是刻意的、名稱失職時防護會被紅燈的人親手解除）與「名稱之外的測試文字」（reason / skip / 註解 / 分析詞彙的落點表）；(3) layered-test-strategy v1.1.0 加「分層之外的兩個補位形態」（stub 盲區與語意級假後端、characterization test）；(4) 已知議題記錄：phase2-test-design.md 與 phase2/rules.md 存在金字塔 / GWT / 場景設計的重複段落、屬雙活文件錯配、本次以指涉止血、結構收斂留待下輪。
**Version**: 2.1.0 - 新增 `references/document-coherence.md` 文件連貫性紀律 + 核心理念補「文件連貫性原則」。依《人月神話》多文件必漂移的論點對本 skill 文件鏈盤點出四個錯配（UC 場景複製進種子包與 feature-spec 形成四份行為敘述副本、traceability 的 covered 狀態是人工宣稱、feature-spec 無生命週期定義、邊界回補不及於已複製副本），落成三條紀律：文件分級（活文件要機制、scaffold 標消費時點、append-only 不回改）、資訊住址（每類資訊唯一權威載體、行為的權威載體是測試）、各 Phase 連貫性檢查點（Phase 4 用實際測試檔驗 traceability、feature-spec 標 archived、註解逐則過商業邏輯判斷標準）。doc-handoff v1.1.0 同步（種子包標記 scaffold 身分）。
**Version**: 2.0.0 - 全面重整：消滅孤立文件、統一 scripts/、集中 cases、子命令加入 Read 指示
**Specialization**: TDD 全流程指導（Phase 0-4）

---

## 各 reference 檔案獨立版本紀錄（外移前的歷史記錄）

以下三檔曾各自維護獨立於本檔版號的內部版本計數，原內嵌於檔案尾端；2.6.0 版外移至此（同 2.2.7 版對 SKILL.md 本身的處置——整份執行的檔案，敘事留在正文等於每次叫用都付一次無效讀取）。外移後三檔改採本檔統一版號，以下條目為外移時點前的歷史存檔，新到舊。

### references/doc-handoff.md（外移前，獨立版號）

**Version**: 1.1.7 — 補齊 spec/UC 轉測試缺範例（審查子票命中，agent 依此推導時被迫猜測）：〈前置閘門〉補判定範例（場景步驟完整性、涉及 UI 判準）；〈Phase 1 輸入映射〉補〈驗收標準轉換範例〉（可量化與不可量化兩種原文的轉換示範）；〈轉換注意事項〉補〈轉換注意事項範例〉（跨步驟狀態累積、角色切換標注）；〈轉換範例〉改寫，移除請求路徑語法，示範一步驟拆多種子的判準；〈GWT 種子與行為單元的分工邊界〉改寫為反向檢驗問句（種子表是否含行為單元欄：否）；〈與 UC 資訊鏈的關係〉改寫，移除儲存技術名，併入整合測試 vs 單元測試分工的節點判斷範例；〈跨邊界步驟：平台決定〉補判定範例；〈追溯矩陣初始化〉補建立範例（entry 以場景為單位而非種子為單位）。六則新增、三則改寫，涵蓋九個決策點（另〈GWT 種子與行為單元的分工邊界〉一則改寫為分工邊界示範，非九點之列）；原僅有規則句或既有範例含技術相依內容，讀者依文字約束仍可能推導出兩種以上做法。
**Version**: 1.1.6 — 補三處缺引導：〈轉換方法〉後新增〈無 UC 步驟的 FR → GWT 種子〉節（FR 前提/觸發條件/宣告結果對映 Given/When/Then 的表格，無觸發條件時標「僅整合測試覆蓋」）、同步將〈TDD 輸入種子包格式〉GWT 種子表的「來源 UC 步驟」欄泛化為「來源」以容納 FR 來源；〈轉換注意事項〉新增「替代／例外場景歸屬」條款（歸屬分岔點所在步驟的種子，跨步驟分岔另立獨立種子）；〈新專案起手模式〉新增〈模組群組切法〉節（以 spec 的 `domain` 為單位，超出任務拆分閾值再依架構層切分）。三處原僅有映射方向或範例，讀者須自行發明規則。
**Version**: 1.1.5 — 〈GWT 種子與行為單元的分工邊界〉節補一句反向路由：GWT 種子與行為單元拆解完成後，若專案已導入測試對象目錄，登載為目錄條目，見新增的 `references/test-object-catalogue.md` 起點 A 步驟 A6-A7（該檔回答本檔與 layered-test-strategy 皆未涵蓋的「要測哪些對象」）。
**Version**: 1.1.4 — 回填〈整合測試 vs 單元測試分工〉分工決策框架表與章節末路由句對 `layered-test-strategy.md` 的精確錨點（節標題「分層之外的補位形態」+ 詞條「**跨邊界驗證**」，取代 1.1.3 遺留的 TODO 暫代），並修正 1.1.3 條目中與正文相反的序數措辭（「加第三值」改為「補判準」，避免誤讀為第三或第四個同軸選項）。
**Version**: 1.1.3 — 〈整合測試 vs 單元測試分工〉判斷原則補「平台決定」判準：既有「自己決定／串接決定／配置決定」三值皆假設判定權威落在 app process 之內，跨邊界步驟（呼叫離開 app process）三值都給不出答案；新增小節說明平台決定不是與三值並列的選項，而是與既有三值正交的另一軸（先問「決定發生在 app process 內還是外面」），分工決策框架表同步加一列指向 `layered-test-strategy.md` 的跨邊界補位形態（該文件補位章節尚在補齊中，暫以檔案層級引用 + TODO 標記，待章節標題定案後回填精確連結）。
**Version**: 1.1.2 — 文件分級覆寫補執行載體：Phase 0 輸入映射表加「Proposal 文件維護決策 → 逐列覆寫 document-coherence 分級表、結果記進種子包」一列；新專案起手模式加「覆寫不隨 Phase 0 豁免消失」（proposal 越完整越觸發豁免、而覆寫輸入正來自 proposal、跳過 Phase 0 不能連帶跳過它）。修的是鏈路實證抓到的斷點：覆寫原本是三處互相引用、零處有執行指令的宣稱。冷讀輪抓到指令指向不存在的欄位（同類斷點在修復裡復發）：種子包 Phase 0 預填補「文件分級」行（預設生效也要標明——沒標與沒做在種子包上長得一樣）、執行步驟 5 補覆寫鉤子、檢查清單加一項。
**Version**: 1.1.1 — 相關文件對 saas-tech-selection 的引用加條件語（未安裝該 skill 時非死鏈）、與 spec skill 的跨 skill 引用處置一致。
**Version**: 1.1.0 — 種子包格式頂部加 scaffold 標記（消費時點 + 爭議以 UC 為準）、檢查清單同步加一項。動機：GWT 種子是 UC 場景的完整複本、UC 回補修訂後種子包沒有機制跟上、未標記身分時它讀起來像權威文件；依文件分級原則（`references/document-coherence.md`）把它明示為一次性 scaffold、消滅同步期待而不是新增同步義務。
**Version**: 1.0.0 — 初始建立。補齊 doc→TDD 銜接缺口（WRAP 評估結論：銜接放 TDD 端）
**Source**: 上游需求銜接模式驗證

### references/test-object-catalogue.md（外移前，獨立版號）

**Version**: 1.2.2 — 補齊 spec/UC 轉測試缺範例、既有範例改寫（審查子票命中，agent 依此推導時被迫猜測或讀出兩種做法）：〈一、測試對象的單位判定〉補〈測試對象單位判定範例〉（含多子句 FR 拆行且各行來源附分支註記以符合來源唯一）；〈三、契約欄位表〉補〈反例欄填法範例〉與〈斷言來源欄範例〉；〈起點 B〉補〈追溯不到規格來源的判定範例〉；〈起點 C〉補〈characterization 條目粒度範例〉；〈六、派發語言〉三則既有範例全數改寫為三段鏈式格式並各補【驗證】段，範例一移除已預設實作路徑的敘述（原「解析完成回調觸發」）、範例三移除框架相依用語（原「元件樹」「查表 helper」）。五則新增、三則改寫，原僅有規則句或既有範例缺【輸入】【驗證】段，讀者依文字約束仍可能推導出兩種以上做法。
**Version**: 1.2.1 — 「四、齊全判準」C1-C4 表後新增「多來源取最具體（C3 的判斷依據）」段：同一行為同時載於多個來源類別時，取最具體條目作「來源規格條目」欄位值（具體性順序：畫面狀態矩陣列 > 使用案例步驟 > 規格功能需求），其餘同義來源以括號附註同欄不另立行。原表僅要求「不重複」未定義如何選，讀者須自行發明優先序。
**Version**: 1.2.0 — 「三、契約欄位表」新增子節〈與追溯矩陣的界線〉：追溯矩陣（形狀由 doc skill 的 `TRACEABILITY_SCHEMA` 定義）與本目錄不同構且不應被改成同構，附五維度對照（問什麼／用途／單位／消費者／生命週期），並寫明本目錄的來源類別不必逐一在追溯矩陣有對應軸——進入該 schema 的判準為三條 AND（有工具消費場景、粒度與既有軸一致、無其他權威載體），畫面狀態轉換、互動規則、元件契約三類皆不滿足，其權威載體分別是規格文件的狀態矩陣／互動規則章節與元件庫規格的逐元件測試契約欄位。Why 段寫明不寫此界線的兩種相反誤讀（把本目錄欄位塞進封閉集合的 schema 而撞上 conformance 強制層／反過來認為本目錄只該列該 schema 已有的項目而漏掉三類來源）。追溯矩陣側同步補對稱的界線說明與回指路由。章節編號未變動。
**Version**: 1.1.0 — 「二、來源類別」表追加「終端回饋訊息」一列（既有六列未動），並於表後補三段：與元件契約類別的分界（對象是每一則訊息而非元件，兩者增長來源不同，合併會使新增訊息無列可站）、該類別「測試形態」欄為組合值且不得拆成三行（拆行違反 C3 來源唯一）、規格明文裁定不需要的驗收項填「跳過條件與理由」欄不留空白、以及「反例」欄的常見填法。「六、派發語言」追加三類驗收的句型範例（既有兩例未動）；檢查清單與相關文件各補一項。章節編號未變動。
**Version**: 1.0.0 — 初始建立：測試對象單位判定、來源類別、契約欄位表、齊全判準 C1-C4、三種起點推導程序、派發語言句型。
**Source**: 本框架測試對象目錄設計分析（2026-09）

### references/bdd-behavior-testing.md（外移前，獨立版號）

**Version**: 2.1.0 — 〈BDD→單元拆分標準〉補〈訊號優先序範例〉：示範「Given 設置過長」與「步驟正確性由自己決定」兩訊號同時成立時的優先序（先拆單元測試，Module 是否需要拆分留待其後再判斷）。原表僅列訊號與動作對照，兩訊號同時成立時何者優先未言明，讀者須自行發明順序（審查子票命中）。
**Version**: 2.0.0 — Sociable/Solitary 選擇框架改寫。舊版寫「只在演算法、加密等場景使用 Solitary」並把「任何測試需跟著改」判為要重新設計邊界，等於把 Solitary 判成錯誤。新版三件事：(1) 三個症狀取代進入訊號清單，並明說它們是**診斷工具而非選型公式**——都要有一套跑著的測試才觀察得到；(2) 每個症狀的處方第一順位是不移動邊界的解法（集中替身設定 / 補可觀察性 / 只換慢的那一個）；(3) 明說射程是維護經濟性、而那已經是古典派立場，倫敦學派的設計發現主張不被這三個症狀裁決。判別程序加上 verdict rule：判的是耦合在什麼上、不判對錯
