# multi-round-review 版本紀錄

新到舊。版號規則與兩個住址（本檔與 `SKILL.md` frontmatter 的 `metadata.version`）見專案的 skill 同步規範。

**Version**: 1.75.1 — 兩張 principle 卡各補一段邊界。`review-frames-ask-efficacy-never-cost`：那個偏斜是一個沒被算過的假設，而不是算過之後的成本選擇——「代價側產出少」一量就翻轉。`proxy-is-cheaper-than-the-thing-it-stands-for`：它的三條修法預設「代理確實比較便宜」，而那個前提要驗過才算數；「這是理性的成本選擇」是一個可以被驗的宣稱、不是一個解釋，沒驗就套用會讓一個該修的偏斜看起來合理。

**Version**: 1.75.0 — Round N 規劃判讀新增一個停止前置條件：看 finding 的方向分佈。frame 集合會朝同一個方向長（問規則的效力、不問規則的代價），成因兩端同向——找到漏抓是產出、找到誤報是否定產出，而規劃者在找「還有什麼沒被檢查」、執行者的價值由 finding 數體現。零 finding 與零 frame 在報告上同形，所以代價側交白卷讀起來像那一側沒問題。偵測是逐則標方向欄，修法是 frame 清單成對排；「找誤報會減少產出」這個估計本身是錯的，實測兩次撤回之後的替代 finding 都更強。新增 principle 卡 `review-frames-ask-efficacy-never-cost`。同批 `fabricated-observation-hides-among-cited-facts` 補兩節：兩個子形態（來源不存在／來源存在而說的是別的事），以及分類欄位本身就是偵測程序——第二個實例是被那張四欄表自己抓出來的。

**Version**: 1.74.0 — 收尾清單新增一項：每一步標明用的是本體還是代理。代理比本體便宜而兩者的產物在報告上同形（都是一個零、一次通過），且收尾階段的成本壓力最高，所以在那個結構下拿代理是理性的——「下次更仔細」對它無效，問題不在注意力的總量而在那個時點的成本結構。四個實例的差價都是一條指令或一個檔，小到不像一個決定。修法方向因此是改成本結構（降低本體的取用成本、讓兩者的產物長得不一樣）而不是改注意力。新增 principle 卡 `proxy-is-cheaper-than-the-thing-it-stands-for`。

**Version**: 1.73.0 — 兩張 principle 卡各補一節。`disclosure-yields-one-bucket-classification-yields-layers` 加第二個軸：前三格回答「這個零可不可信」，而「這個零是什麼」是獨立的一軸（沒發現問題／零本身就是 finding／零是正面結果），兩軸在報告措辭上同樣被壓成一句「零命中」——「我沒找到問題」與「我找到了『沒有』這件事」同形，而後者通常是那一輪的產出。`fabricated-observation-hides-among-cited-facts` 加放大器：來源方越可靠，第五類越不會被發現，因為相符是常態而常態不產生訊號；協作對象的可信度因此不能當成放行的理由。

**Version**: 1.72.0 — 查核責任分類由四欄擴為五欄，補上「別人量的、我引用」。它與第四類的失效模式相反：來源真實且通常是對的，所以查了也對、沒有人會再查；危害不在數字錯，在把「別人驗過」記成「我驗過」，下游因此把那個數字當成過了兩道查核而實際只過一道。**外部查核偵測不到它**（去驗那個數字它是對的，而通過本身沒有回答這是誰驗的），所以它要寫成契約欄位（附上自己那一次跑的輸出），追問對它無效。第二欄的定義同步補第二層——在不在對話紀錄裡，以及在誰的對話紀錄裡。產生它的機制是語氣一鍋端，與零 finding 那一條同源。`fabricated-observation-hides-among-cited-facts` 卡同步。

**Version**: 1.71.0 — 質疑步驟補第二個施力點與四欄分類。還沒起疑、只是想確認的時候，問一個只有握著原始輸出才答得出來的後續問題（回報者為了回答就得回去看，而一回去看就發現那份輸出不存在），不要問「你確定嗎」——第二次斷言與第一次在文字上無法區分。查核責任分四欄：引用的／我量測的（工具輸出在對話紀錄裡、可原地重跑）／我推導的／我以為觀測到的（那次呼叫從未發生，撤回）。第二欄的定義是分界線，而兩欄的分類法會把第四類歸進第一類因為它看起來有來源。實測一次：一個 reviewer 在三則獨立訊息裡引用同一組數據並據此報了實測否證，回去查才發現那份輸出從未進過它的脈絡，四項全部撤回，而同份報告裡其他數字經查為真替它背書。新增 principle 卡 `fabricated-observation-hides-among-cited-facts`。

**Version**: 1.70.0 — 收尾清單新增一道獨立 pass：本批改動的形式一致性通讀（只問「同一件事我寫了幾種形式」，範圍限本批改動、問題限形式一致性）。它與類別掃描是 AND 不是取代——類別掃描的輸入是 pattern，而同源修法寫出的兩種形式都是新寫的、都不命中任何舊 pattern；實測三個批次七個缺陷，作者自查抓到的兩個恰好都有 pattern 可比對、獨立審查抓到的五個恰好都沒有，分界是有沒有 pattern 而不是認不認真。寫成取代會讓不量測任何東西的那一個成為主導閘門。三批皆出現而未觀察到隨批次遞減，卡上沒有比率宣稱。產出契約的三格同批補上第二批數據與診斷用法：「沒查」那一格立刻兌現，而「不適用」的佔比是 frame 相依的——集中在單一 frame 代表那批判準該搬家，各 frame 平均偏高才是整體錯配。新增 principle 卡 `one-fix-writes-two-forms-of-the-same-thing`。

**Version**: 1.69.0 — 整合工作流新增一步：要質疑一個 finding 時要它交出物件、不要它交出判斷。關於物件的斷言可以在抽象層完全合理，而構造那個物件是唯一能推翻它的動作，且沒有人要求構造時回報者沒有理由去做（他的主張在他自己的推理裡自洽）。追問的動詞用「寫出來 / 算出來 / 構造一個」而非「確認 / 說明 / 檢查」，並加一句「我要能自己驗證」把產物的接收者換成追問者；回報者更早輪次量過的數字要明確要求代入並印出計算——實測一次那個值一直在它手上而本輪判斷沒有用到它。新增 principle 卡 `ask-for-the-object-not-the-verdict`。同批把 `concept-used-but-never-named` 的機制一般化：每一種檢查都有入口、零命中只證明從那個入口走不到，路由的死名與孤兒是同一機制在另一個入口上的實例，處置是雙向各驗一次並在報告上分開寫。

**Version**: 1.68.0 — 產出契約的零 finding 那一條從揭露義務改成分類義務。原本寫「零命中要明寫、不可省略」擋掉了省略而擋不住一鍋端，且一鍋端比省略難察覺因為它已經照契約寫了。契約改成給定三格窮盡的類別（造過情境判定通過／判準只有二元事實造不出情境／沒查），少了第三格沒查的會被歸進最接近的一格而最接近的是第一格。實測一次六十六條判準回報三十四條零命中、格式完全合規，追問後拆成 17 / 16 / 0，而分類是被問句逼出來的、報告者本來只有一堆。同批補一個契約槽位：結案之後才想到的改判登記而不補進 finding 清單，因為清單的計數已經被引用過、硬塞會讓它與據此做的決定一起失真。新增 principle 卡 `disclosure-yields-one-bucket-classification-yields-layers`。

**Version**: 1.67.0 — 收尾清單第一行補作用域的定義：「全批」是成品的全部、不是這一輪改過的檔。指令可以完全正確而作用域窄一圈，而在改過的檔裡命中歸零、報告讀起來就是乾淨的；錯誤的作用域來自手邊那份改動清單，它在「這次變更有沒有引入新問題」這個用途上正是對的。連帶給正對照加位置要求——放在沒被這一輪改動過的檔裡，放在改過的檔裡它會命中而作用域缺口原樣留著。`fix-the-class-not-the-cited-instances` 卡同步。

**Version**: 1.66.0 — 收尾清單補一層語意：執行本身有深淺、「掃了」不是二元的。那三次的層級分別是不掃、掃了但只掃同形、掃同形再加逐項對照，而第三次執行了掃描並抓到同形殘留仍然漏掉一處異形的。所以清單要把層級逐條寫出來——寫成「掃一遍」的那一行會停在它最便宜的解釋上。`fix-the-class-not-the-cited-instances` 卡同步。

**Version**: 1.65.1 — `review-needs-claim-support-frame` 的排程判斷標準補一條：「多輪全過」的作用域是跑過的那幾個 frame，而報告上不會出現那個限定詞。收尾時要自己把跑過的 frame 列出來並問哪些維度整批沒有人負責——列出這件事沒有任何一個 reviewer 承擔。

**Version**: 1.65.0 — 停止訊號重排並降級第四條。「新 frame 想不出來」量的是判斷者而非稿件（想得出多少 frame 由執行者的經驗決定），四次實跑零次成立，所以「任二齊備」的分母其實一直是前三條，而四條平鋪並列還讓執行者以為要追求全滿。判別新增訊號的問句寫進卡與 SKILL.md：這一條量的是被判斷的對象、還是判斷者自己。整合工作流的「掃那一類」升為三行收尾清單（特徵字串掃全批 / 逐條列出不讀計數 / 逐項對照實例數與已修數）——同一條規則讀過三次而只有執行了掃描的那次成功，且字串掃描抓得到同形殘留、抓不到異形殘留。新增 principle 卡 `truncation-drops-the-frame-output-first`，並在產出契約與 reviewer prompt 兩處加入「產出排在報告哪個位置」。`replacement-exemption-comes-from-its-fix-list` 補「替補結構失敗比它取代的做法嚴重一階」，反模式段的「多輪全過」補一則乾淨實例。

**Version**: 1.64.0 — Round 3 outbound 的知識卡缺口補第二個方向：從用法盤點到名字（用法盤點表 / tag 對正文 / 枚舉卡對照用法），加反向術語探針。起因是一個跑過五輪審查的集合被外部提問撞出一個零次進正文、卻在五篇裡反覆執行的運算名稱；既有檢查全部以文中的名字為入口，對它沒有入口。新增 principle 卡 `concept-used-but-never-named`。

**Version**: 1.63.2 — 三份翻譯探針對 `description-as-recall-trigger` 裡「沒有問就不知道的斷點」給了三種不同的意思（沒人問／缺了那個問題／讀者不知道該問什麼）。那句話把誰沒問、不知道什麼都省略了，中文允許、英文強制決定，所以這一類要靠翻譯才現形。就地展開成「讀者不曾遇到那個問題，就永遠不會知道有那件事」。

**Version**: 1.63.1 — `description-as-recall-trigger` principle 卡的 H1 停在被自己推翻的版本（「Description 是 Recall Trigger、不是文章摘要」），而同一次改動已把結論改成體例由內容定位決定。slug 不動、只改 H1。

**Version**: 1.63.0 — description 的體例改成依內容定位分兩種。查閱型內容維持觸發條件式（使用者帶著症狀或術語來），教材改成說這一篇涵蓋什麼——觸發條件式在教材上是缺陷，因為那句話寫成「遇到 X 時」之後文章就得是 X 的答案，而照問題組織的教材會產生沒有問就不知道的斷點。1-C 的檢查多一個前置判定（這一批是教材還是查閱型），並加一條整批訊號：一個分類的 description 幾乎每一篇都以「時」收尾，那是規範長出來的形狀、要回頭改規範。principle 卡 description-as-recall-trigger 同步

**Version**: 1.62.0 — B⁶ 術語探針的回答改成分四類讀，並在 prompt 要求裡加上「有把握的那些，寫出它指的東西」。第四類是收斂度看不到的一種：少數份有把握、而它們一致指向一個與作者用法不同的東西，代表這個詞在同一個領域裡已經有主人。它與非通用詞在收斂度上同區（多數份沒把握），只看數字會判成邊緣而換個同義詞了事，可是兩者的代價方向相反——無主的詞讓讀者查不到於是他停下來，有主人的詞讓他查得到另一個東西於是他不停下來。principle 卡 term-probe-measures-register-not-invention 同步

**Version**: 1.61.0 — 產出契約新增一條「探針的自評欄不是結果欄」，並在 2-B⁗ 那一列標註。探針回報的「有把握 / 看不出來」答的是這段文字有沒有給它材料，不是它答得對不對——後者它答不了，因為它看不到答案而那正是設計條件。一次二十六條路由的三份獨立量測裡三份自評欄幾乎一致（各二十四條有把握），逐條比對後才出現一條三份全錯、兩份方向答反的路由，而那一條的自評欄與答對的十七條無法區分。同批更新 principle 卡的「組成」方向維度。

**Version**: 1.60.0 — 同批 principle 卡的修正（條件分支降級為五種關係之一、判準補射程、新增「每一條判定都要附它的輸入」段）。另記一則方法論：本輪的 3-B steelman 回查原文推翻了該卡核心資料點的編碼，而 3-H 個案實跑顯示判準的兩個軸各只有一個分支而實際有五種——**兩者都在 Round 1-2 的十二個 frame 下結構性不可見**，因為前兩輪只檢查已寫的內容對不對，不檢查那個編碼是怎麼來的、也不拿極端個案走一遍。

**Version**: 1.59.0 — 修正 1.58.0 引入的判定條件與量測範圍。2-B′ 的路由承諾原本寫成句法規則，多輪審查找出假陰性，改成內容測試（遮住目的地能不能預測那一篇會給什麼），並註明用關鍵詞比對會高估、用句法規則會漏。版本紀錄另修一處量測範圍漂移：冷讀量測是二十七篇，三十九篇是後來那次承重術語掃描的範圍，1.58.0 把兩次併成了一次。

**Version**: 1.58.0 — Round 2-B′ 冷讀 frame 的篇章層加一題「這一篇讓你具備什麼能力」。冷讀預設「讀得懂」等於「有內容」，而互相引用的集合裡有一種稿件讀得懂而說不出要交付什麼——一次二十七篇的冷讀量測裡，內容段全部在講鄰居的短文，冷讀把每一段的內容都答對而最後一題寫「看不出來」並自列三種互斥的可能。同一題另外驗路由承諾：判斷標準是遮住目的地之後預測不預測得出那一篇會給什麼，句型只影響命中率，所以這一維用關鍵詞比對會高估、用句法規則會漏。新增 `cross-links-eat-the-article-they-live-in` principle 卡。

**Version**: 1.57.0 — 套新的取捨門檻回頭修剪本輪加進來的內容。定位閘門刪掉「規範裡的每一條規則都誕生自某一個定位」整段（那是為什麼會發生，屬歸納層）與微案例那個實例；2-D 刪掉「為什麼其餘 frame 全數通過」的說明與量測數字，留判斷標準、七種裝置、產出契約、處置；2-B⁷ 刪掉分離度數字與判讀說明，留設計條件與操作。刪掉的全部在對應的歸納卡裡查得到，skill 這一側改成一句話帶過並指過去。門檻是「一個沒讀過那張卡的 agent，照這一段做不做得出動作」——做得出就夠了，多的是說明

**Version**: 1.56.3 — 定位例外的措辭修正：一份歸納問題與解法的記錄兩邊都不是——讀者不是來理解一個主題的，而可執行的那一半住在另一個位置。上一版寫成「落在手冊那一側」，那個歸類會讓人以為處理方式該寫進那份記錄裡

**Version**: 1.56.2 — 定位的例外從「目錄頁」擴成「整個子集合也可能落在手冊那一側」：一份規則語料的消費者是即將動手而要查一條規則的人，它解釋理由是為了讓規則套得準、不是為了讓人讀懂一個主題。2-D 掃體例時要排除的因此不只目錄頁

**Version**: 1.56.1 — 定位閘門與 principle 卡補一條例外：同一個目錄底下可能兩種定位並存。一個內容分類裡的文章是教材，而它的目錄頁是那個分類的操作手冊（成員表、路由、待辦），使用者的動作是照著它走到某一篇——目錄頁該有的正是文章不該有的。所以定位按稿件要讀者做什麼判、不按目錄判，而 2-D 掃體例時要排除目錄頁，否則會把它正確的形式報成違規

**Version**: 1.56.0 — 定位成為 frame 之前的閘門，而不是清單裡的另一項。新增「派發之前先定這一批的定位」節，放在 frame 表之前：寫下一句話（誰讀、讀完要做什麼），並用它重新過一次 frame 的觸發條件。理由是這一輪量到的機制——規則的觸發條件會先於它自己的判斷標準生效：要求補微案例的那一條觸發於「判讀 / 選型型內容」，於是一篇讓人理解某個現象的文章形狀對上了就照樣被要求補一則演示判錯代價的短敘事，而那條規則自己明寫著要挑後果最不直觀的。定位判定為人類教材時，2-B‴ 的微案例、2-B″ 的可執行走查、1-D 的下游任務三個 frame 的預設都來自 agent 指令或工程決策內容，要重判。

新增 **2-D 體例與定位一致**（一律跑）。它抓的東西每一條規則都遵守了，所以其餘 frame 全部通過——它們問「這一條遵守了嗎」，而失配的稿件確實遵守了。判斷標準可機械執行：**讀者讀完之後那個動作落在哪裡**，在世界上動手是手冊、在腦中重新歸類是教材。掃的是裝置不是語氣，七種形態列在 frame 裡，其中序列連接詞（先、之後、最後）最容易漏——把步驟編號換成它只改掉一半。處置不刪資訊，改的是框架。

新增 **2-B⁷ 類型探針**：受測單位從一段文字或一個詞擴到整篇的定位。問法不能點名類型，五個問題沒有一個提到手冊或教材；第五題要求逐字抄出判斷依據，直接交出裝置清單。設計條件同 B⁶ 的控制詞——同批混入一份無疑義的手冊與一份無疑義的教材且不標示，控制項在「隨時翻」軸上分開，測試項才可歸因，實測分離度五比零對五比零。判讀時「會照著做」與「會放在手邊」是兩條軸，只翻一條要當成沒改完。

附 principle 卡 positioning-decides-form-before-any-rule-applies。

**Version**: 1.55.0 — 新增 principle 卡 article-writes-the-topic-not-the-authors-path-to-it 並接進三處。**frame 表補 2-C 列**（一律跑）——這個 frame 本來只存在於 Round 2 的內文而不進派發判定表，缺列等於它永遠不會被明確判定要不要跑，與 v1.48.0 補 2-B⁵ 列同一個成因。**Round 2-C 擴成題目範圍檢查**：把標題與各段標並排、逐個問這一節在不在標題的範圍內，產出是逐段標表；抓的兩種形態是段標帶推導的連接詞（把連接詞之後刪掉，前半通常就是完整的段標）與整節在講比標題更泛的東西（移出去獨立成篇、待辦條目要寫出實際內容）。**Round 1-A 新增可信度斷言 grep**，與物理化錯配共用一組關鍵詞而問的是不同的問題：那條問動詞對不對（證據「撐得住」寫成證據「可靠」），這條問這句話該不該寫——可信度是讀者讀完之後產生的感想、作者寫出來等於替讀者下結論而讀者手上的材料沒有變多；兩條互補不互相取代，順序是先問該不該寫、決定要寫之後再問動詞。實測是三篇短文跑過三輪十三個 reviewer 與十六份探針、全部通過，隨後由作者以外的眼睛一次指出三個同源缺陷——這一類在既有 frame 下不落空，因為每一句都合規

**Version**: 1.54.0 — 新增兩張 principle 卡並接進三處。**criteria-fold-away-the-condition-that-produces-the-symptom** 接進 Round 3-H：個案實跑的產出價值集中在把停頓點並排，而最高頻的形態是判斷標準只收「表徵」一個輸入、把產生表徵的那個條件從輸入端折疊掉（處置句寫著只有某一種執行環境才有的操作名詞）；派發前先數一個便宜的指標——分支數少於正文列出的種類數時就有種類落在所有分支之外，個案要刻意挑落在那些種類上的。實測是三篇短文的末節判斷標準通過了字句層、冷讀逐格、情境可想像性與 steelman 四種 frame，而九個具體個案只有兩個走通。**fix-the-class-not-the-cited-instances** 接進整合工作流（新增第 6 步）與 reviewer prompt 的產出契約（新增一欄）：finding 清單列的是抽樣位置，而清單的形式在暗示完整性，於是照著逐處修會留下同一類在沒被點名的地方——留下的舊實例比新引入的變體難抓，因為這一類已經回報過、後續輪次不再掃它。實測是同一個動詞的同一處論元結構歧義散在三個位置，四份理解探針一致指向其中一行、修好並經驗證翻轉，另外兩處由後續的翻譯探針換一個角度才撞見

**Version**: 1.53.0 — 新增基本原則「規模與來源是兩件事」與 principle 卡 review-scale-does-not-buy-independent-origin：一個 session 派出的所有 reviewer 與探針構成單一來源，增加數量提高的是覆蓋的面而不是視角的來源數。實測為兩個併行執行者各自跑完四輪、各自字句層掃描回報乾淨，交換檢視時各自一眼看到對方一處違規；其中一處的判斷標準可機械執行（問這個動詞照字面成立需要什麼物理條件），寫的人套在自己的小節標題上判定通過、另一個來源套同一標準當場判定不通過——差別在寫的人讀到標題時取回的是意圖而不是字面。Round 1-A 的同源上限段補上「同源的範圍包含整棵派發樹」。段名「四大基本原則」內嵌成員數、加第五條即失真，改為「基本原則」
**Version**: 1.52.1 — 上一版新增的段落自己命中否定起手（「要留的不是譯文而是一個問句」把核心概念擠到「而是」之後），改成重點在前

**Version**: 1.52.0 — B⁵ 翻譯探針補上不可讓渡的設計條件（譯者只能拿得到文字）與兩條判讀規則。新增 principle 卡 probe-independence-is-not-transferable：翻譯探針的訊號來自譯者只拿得到文字、這個條件不可讓渡。十份探針對照（指令逐字相同、只差讀到的範圍）結果完全分離——有脈絡的 5/5 譯成作者意圖的讀法且零人標記不確定，無脈絡的 5/5 譯成另一個讀法。推論是「寫作當下同時產出兩種語言互相對照」對最該偵測的那一類全盲，因為會傷到讀者的歧義正好是作者毫無困難就能解決的那一種。判讀另加兩條：譯文通順而原文成分不在裡面要當成命中、「沒有標記不確定」讀成沒有資訊

**Version**: 1.51.0 — portable 修復：axis-named-by-proxy-not-mechanism 從跨 skill 相對連結改為本 skill 自帶副本（複製到空白專案時原本會斷鏈），Round 3-B steelman 段的反引號提及同步改成連結

**Version**: 1.50.0 — Round 2 新增 **B⁶ 術語探針** frame：受測單位從一段文字縮成一個詞，測它在目標語域裡有沒有穩定所指——作者與同源 reviewer 讀到高頻用詞會自動補上作者給的定義，B⁗ / B⁵ 與 keyword bank 都覆蓋不到這一層。設計上比 B⁗ 多一個條件：同批混入至少兩個已知通用的術語且不標示，控制詞收斂才代表分歧可歸因到詞本身。回答分三類讀，而非通用不等於自創（先查別的領域有沒有穩定用法）。frame 的第二半是替換程序：先列詞形分佈、再按義項給對映、分不出義項的給最長展開式、無可靠中譯的保留英文並連回知識卡。附 principle 卡 term-probe-measures-register-not-invention

**Version**: 1.49.2 — Round 1-A 的物理化錯配 grep 收進站得住家族並去掉清單裡重複的三個詞；替換改為依主詞分流（論證與結論用成立、證據用可靠、正文用自足、選型用適用）

**Version**: 1.49.1 — 術語校正：判準全數改為判斷標準（動作修飾語縮為「X 標準」、狀態義改為「X 條件」）。判準的語域在哲學與教育評量、工程讀者解析不了——五份低階模型探針一致回報非通用

**Version**: 1.49.0 — 同一次書單重審回流兩項。(1) **Round 1-A 的位置與集合指涉補體裁條款**：pattern 的成員從論述型內容歸納，審查對象換體裁時要先人工讀三到五篇列出實際形態、再問 pattern 涵蓋幾種；回報這一類 clean 之前跑這一步，修完之後回頭比對命中數（數字幾乎沒動就是修的東西與量的東西不是同一批）。(2) **Round 1-C 的宣告核對加「可數宣稱的觸發核對」**：會隨內容變動的數字有兩個獨立失效面（值算錯了、沒有人會回頭算），逐條問計數單位定義在哪、什麼事件會改變它、寫下的觸發涵不涵蓋那些事件；觸發條款裡的排除句要單獨驗，而內部自洽（分項加起來剛好等於總數）只證明出自同一次計算、不證明計算的時點——實測審查者驗了加法就判定通過，外部重算得到另一個值。

**Version**: 1.48.0 — 多輪審查對 v1.47.0 新增內容的修正，三條都在 B⁵ 那一段：(1) **frame 表補 2-B⁵ 列**（中文稿件、且已通過 2-B⁗；非中文不跑）——表自己的契約要求「判定為不跑的也要寫」，缺列等於這個 frame 永遠不進派發判定；(2) **B⁵ 的指令補回括號例示**（單複數 / 主詞是誰 / 並列範圍 / 字面義還是比喻義），原本住在 SKILL.md 的是 principle 卡明說會退化成「回報譯詞選擇」的那一版，照它派發的 reviewer 拿到降級指令；(3) 「譬喻必然現形」改「會現形」並補實測對照。另修物理化錯配 bullet 落後一個成員（三種 → 四種），以及 comprehension-sampled 鏡像卡補第六種用途段（本 skill 的 translation 卡宣稱「第六種用途」而那張鏡像卡只到五種）。

**Version**: 1.47.0 — Round 2 新增 **B⁵ 翻譯探針** frame（中文稿件專用）：派 N 個低階模型用逐字相同的指令把同一段翻成英文，判斷標準同樣是份與份之間的分歧。機制是 B⁗ 繞得過而它繞不過——理解探針的回答形式是摘要而摘要允許保持模糊，翻譯每個詞都要落地成另一個語言的具體形式，中文可以不決定的語法項（主詞、單複數、所有格方向、並列轄域、字面義還是譬喻義）英文全部強制決定。抓的層級因此不同：B⁗ 抓命題與脈絡層、B⁵ 抓詞與語法層，順序不可顛倒。指令的產出是「我必須自己決定而原文沒給」那一欄、譯文是副產品，但譯文仍要並排逐句對照（實測三份全錯的那個發現沒出現在任何註記裡）。由使用者提案並實測。

**Version**: 1.46.0 — Round 1-A 的 bank 摘要同步 compositional-writing v0.81.0：完整清單成為十七類、未列出類別加「脈絡懸置」（前向懸置「等下再討論」/ 位置回指「前面兩個小節」/ 壓縮總結「用一句話總結」/ 列舉鳥瞰「分成兩組、第一組四本講……」——判定看指涉攜不攜帶內容名、修法是關係鏈加錨點路由，原則是文章的連貫靠鞏固形狀、不靠讀者的工作記憶）。description 枚舉同步。

**Version**: 1.45.0 — 整合工作流新增第 4 步「reviewer 提出的是替代結構而非修一處時，採納之前先驗它」，Round 3-B 加「本批在前幾輪採納的替代結構是必查對象」。從一次三輪審查抽出：Round 2 兩個 reviewer 收斂提出的骨架，理由是它一次解掉六項已記錄的問題（逐項可核對、全部屬實），被直接採納並擴散到二十餘個檔；Round 3 的個案實走、對抗 steelman、跨篇前提並排三條路徑獨立推翻它——維度不正交、仲裁規則要求比較三個不同量綱的數字、兩個計量單位是回溯量而用途宣稱是前瞻、且對反例免疫。**關鍵是重問殺死前任的問題驗不到它**：那個問題正是替補結構被設計來回答的，會逐項答對而通過。新增 principle 卡 replacement-exemption-comes-from-its-fix-list。

**Version**: 1.44.0 — fix 工作流補「修法生效要另外量測」：修完不等於修好，而兩者在產物裡看起來一樣（finding 劃掉、檔案有 diff、commit 訊息寫了修什麼，都不證明缺陷不再發生）。驗收是拿當初暴露缺陷的指令逐字重跑、看回答有沒有從缺陷態翻轉；只對有明確錯誤答案的 finding 適用。由 v1.42.0 的修後驗證抽出——四題三題翻轉，第四題兩份回報給了不同理由而其中一個經查證不成立（宣稱段落位置太後面、實測在全檔前百分之九），照那個理由會搬動段落，而成立的理由指向完全不同的修法。這一步擋兩件事：宣告修好而沒修好、照錯的理由再修一次；並把歸因先於處置推到驗證階段（歸因對象從回報的內容變成回報給出的原因）。附一條停止條件：驗證通過的部分不要再動。

**Version**: 1.43.0 — 「給 reviewer 的話」補一句退路：指示裡沒有指名 frame 時，不要自己挑、也不要整份跑，回去問派你來的人。由 v1.42.0 的修後驗證探針抽出——兩份探針都指出原句「你拿到的 prompt 會指名一個 frame」是一個假設，而派發草率正是這份文件要防的失效，那時 reviewer 手上沒有任何指示。猜一個跑完的成本比問一句高得多，猜錯的那份報告還會被當成該維度已經覆蓋。（同批記錄一則歸因：另一份探針把同一個風險歸因為「澄清出現在文件中後位置」，實測該節在第 35 行 / 全檔 371 行、前百分之九，該理由不成立、未據以修改。）

**Version**: 1.42.0 — 三個新段落，全部由對本檔自己跑的十三份讀者探針抽出。(1) **「誰做哪一段」**：全檔原本沒有任何一句分派責任（grep 證實只有兩處提到「主 context」、皆為附帶說明），兩份角色歸屬探針獨立判定「被派出去的 reviewer 會以為整份文件都要自己跑一遍」，其中一份還指出 reviewer 讀到「Reviewer prompt 結構」節可能誤以為自己也該再派一批 agent。補一張主 session 對 reviewer 的責任表、一段給 reviewer 的話（明令不再往下派）、以及主 session 不外包的四項。(2) **「這一批要跑哪些 frame」**：Round 2 與 Round 3 的變體各有不同啟動條件而沒有決策表——三份派發模擬探針產出三種不同計畫（10 / 11 / 14 個 agent、frame 組合各異），全部自行補上判定。逐 frame 給啟動條件，並要求判定為不跑的也要寫理由（否則「沒跑」與「判定不需要」分不開）。(3) **「每個 frame 的產出契約」**：原本只有七處有契約（路由逐條表、旅程逐跳表、冷讀逐格表等），其餘 frame 的產出與完成標準探針一律回報「文件沒說」。補三件必要指定（產出形態與欄位、窮盡還是抽樣、零 finding 怎麼表達）與四欄最低限度。同批：Round 1-A 的字句層清單標明自己是摘要並指出缺哪三類，description 的 bank 枚舉補齊為十六類並改成指向權威節。

**Version**: 1.41.0 — B⁗ 補規則類文件這個對象：對文章而言探針是真人讀者的代理，對規則類文件而言讀它並執行的本來就是模型，所以量到的是實際執行輸入、嚴重度更高、且「一份讀錯就是判決」不需要靠變異度推論。這類對象的指令加一欄「照這一節工作我的第一個具體動作是什麼」——實測該欄單獨產出五處而其餘各欄全數放行（改名後沒跟著改的路徑、查無對應檔案的專有名詞、寫成形容詞並列的步驟），因為那些句子讀得懂、只是落不到實體上。新增 principle 卡 rule-must-point-at-something-openable。

**Version**: 1.40.0 — B⁗ 補「歸因先於處置」這一步：探針回報的是資料不是結論，判定「讀者讀錯了」之前先在原文裡搜那個讀法對應的字串（找得到逐字對應＝文章自己這樣寫的、修那句並找出它跟哪裡衝突；找不到而材料足以推出＝表述有歧義；兩者皆無＝模型的問題）。跳過這一步的動力來自收斂本身——幾份一致時「讀者讀錯了」這個結論順到不需要查證，而查證成本只是一條 grep。這一條由 v1.38.0 自己的歸因錯誤抽出：當時把五份一致回報的一句話寫成「5/5 一致誤讀」，而那句話在原文裡逐字存在、是作者寫在該節結尾的摘要。同批把 principle 卡兩份鏡像的判讀段對齊（skill 側先前停在被推翻的收斂度分級表）。

**Version**: 1.39.0 — B⁗ 三處修正，全部由使用者對 v1.38.0 的判定推翻。(1) **判讀從收斂度分級改成一份讀錯就是判決**——收斂度決定證據強度、不決定要不要修，把統計門檻當處置門檻會放掉大部分 finding；理由不在統計裡（引導讀者是寫作者的職責），而模型群體的變異度低於真人、五份只有一份搞錯代表真人裡的比例只會更高；例外用操作測試分界（文章有沒有給出足以排除那個讀法的材料）。(2) **用途從單一擴成五種**（理解正確性 / 重點涵蓋度 / 主旨明確度 / 閱讀負擔 / 語氣與讀者定位），派發方式相同、換的是問題；重點涵蓋度的判斷標準與其餘四項不同，比對的是作者意圖清單與讀者回報清單的差集。(3) **v1.38.0 對量測結果的歸因是錯的**——「5/5 一致誤讀」實際是作者自己在同一節寫了兩句相反的話（正文說會消失、四行後的結尾摘要說「永久記錄」），五份忠實回報了結尾那句；量到的機制因此是「正文與結尾摘要衝突時讀者帶走摘要」，而壓縮到三件事時活下來的也是摘要版。principle 卡連同軸名一起更名（分歧只是訊號之一、判斷標準是讀者實際帶走什麼）。

**Version**: 1.38.0 — Round 2 新增 B⁗「低階 model 讀者探針」：判斷標準從 reviewer 的判斷移到多份獨立理解之間的分歧，因此不依賴任何一份察覺自己被卡住。低階在這個用途上是優點不是妥協——推理能力越強越容易讀到殘缺句子時自動補完，而成本與速度決定能不能大量同步派發，判斷標準需要多份才成立。五個設計條件缺一即退化（指令逐字相同 / 不給審查框架 / 明令標記不確定且禁止推測填平 / 問理解不問評價 / 判斷標準是分布）；判讀按收斂度分級，全體一致誤讀必改。由使用者提案並實測：一篇剛通過三輪九個高階 reviewer 的稿件，五個 Haiku 仍找出 5/5 一致誤讀一處（把「沒有操作能單獨刪掉這一筆」壓平成「永久記錄」，而該節列了它會消失的途徑），共十處修正全是補句子成分與具名指涉、無新增段落；另一個自我實例是命中了物理化錯配清單裡沒有的借詞「做功」——探針不依賴任何關鍵字清單。新增 principle 卡 comprehension-sampled-not-judged。

**Version**: 1.37.0 — Round 1-A 的「虛構經驗與出處」補三個掃描面與批次盤點紀律：第一人稱 grep 只覆蓋一面、第三人稱角色（PM / reviewer / 同事 / 新人 / 士氣）無穩定關鍵詞而動機句常掛在他們身上、判定單位是生成批次不是關鍵字命中（實測命中 6 篇、同批 31 篇）。由待驗清單的實際盤點抽出。同步 compositional-writing v0.78.0。

**Version**: 1.36.0 — Round 1-A 字句層 bank 加「虛構經驗與出處」（AI 生成內容）：第一人稱複數經驗宣稱與精確出處宣稱預設待驗；經驗宣稱是證據宣稱、虛構是造假；先判定事件存不存在再改姿態、引文逐字核對。從一篇早期文章的虛構團隊敘事（使用者指出）與虛構引文出處（來源驗證抓到）抽出。同步 compositional-writing v0.77.0。

**Version**: 1.35.0 — Round 1-A 字句層 bank 加「位置與集合指涉」grep（前者 / 後者 / 前兩本 / 另外 N 本 / 其他 N 篇 / 下一本 / 這一側）：解碼材料要在讀者已讀的文本裡、位置與數量是作者地圖的 derivation（實測同一篇三次計數三次全錯）、集合指涉即使計數正確也要給成員或方向（數字只證明數過、成員的方向才是讀者要用的資訊）；四類合規（緊鄰具名清單、全稱比較、已具名集合的向後回指、時間距離遠指）、命中順手驗計數、集合列不動是分類規劃要拆的訊號。由使用者兩輪指正抽出（先推翻「前兩本」類前向指涉、再推翻「集合指涉計數正確可保留」）。同步 compositional-writing v0.76.0。

**Version**: 1.34.0 — B′ 冷讀 frame 加「閱讀節奏與引導」兩問：冷讀原本只量「看不看得懂」（理解的可能性），補量閱讀的成本與斷點——(1) 讀起來累不累（未具名指涉的攜帶量、連續高密度句、思考時間有沒有被襯出來；中文單音節節奏快、該慢的位置刻意加字合法且必要）、(2) 卡住的讀者有沒有被接住（前置概念的主動提醒與路由、識讀落差的分層切入點；檢查的工作由文章做完、不要求讀者自我檢討）。預期 finding 類型加兩項（閱讀壓迫、前置概念缺主動提醒）。由使用者對一整批寫作修正的收攏抽出——密度過高加不指稱人事物讓每一行都要求高理解力與記憶力、通過「看得懂」審查的文章仍可能整篇壓迫。對應 compositional-writing v0.74.0 的 writing-designs-the-reading-process principle 卡。

**Version**: 1.33.0 — G frame 的「兩個住址」修掉兩個會讓它失效的條件。**搜尋條件原本寫「對整批列出所有帶表格的章節與卡」**，而一次實測的六個住址全是編號清單、沒有一個是表格——那個條件從幾個恰好用表格承載的實例歸納，把載具形式當成判斷標準，照它跑會漏掉全部（實際漏掉了其中一組的第四處，而那一處的標題直接宣稱該主題、八個檔案連進去）。改成「宣告一組固定成員、要求逐項填寫的段落，不論排成表格、清單或散文」。**載體判斷標準原本只說引用數決定住址搬不搬**，而實測顯示要數的是指向那組成員的引用、不是指向那一頁的（整頁入連由該頁主題帶來、與這組成員無關，兩者相差一個量級而據此選錯了理由）。同批補：計數只算同層住址（不同層對齊語彙而不收斂、混算會讓收斂範圍多出不該進去的對象）、兩套各有對方缺的成員時載體要先補齊、補齊時檢查新成員的名字在載體上有沒有被佔用的近義詞。

**Version**: 1.32.0 — G frame 補上「兩個住址」這一半：原本只管零住址（一個判斷被多篇當前提而無人承接），而同一個盲區來源（檢查單位是單篇）還會產出另一種形態——一個對象被兩篇各自分解一次，且**雙向連結齊備仍然失效**，既有的跨篇維度（連結有效性、引用準確性）與逐篇審查全部通過。補操作（把鍵欄描述同一類對象的表並置、雙向對映 + 動作測試、對映前對齊粒度與層級）與修法（選單一載體、不同步也不並陳、卡通常是住址、明示簡化的導航層不觸發）。從一批三章改寫的三輪審查抽出：該批自身有三組實例（章對章的資產類別、章對卡的證據包欄位、卡對卡的術語分工），而前兩輪的所有 frame 都沒抓到。

**Version**: 1.31.0 — B′ 句子層執行面補全（Round 3 self-application 抓到執行位缺口）：四條件名稱內嵌 + 有 compositional-writing 時同步 invoke（原本 portable 邊界後只剩兩條件的簡化版）、抽離單位明定為消費單位（一項 / 一列含鍵欄、鍵欄與 title 是檢索鍵位、義務是識別充足——照字面逐格掃鍵欄會假陽性）、執行協議加「每格記錄復原出的命題」（模型會用文風先驗補完歧義後回報沒問題、yes/no 回報驗不出）、成因名稱帶例示。

**Version**: 1.30.0 — B′ 句子層顆粒度結案（第二個 batch 驗證完成）：篇章層與句子層併入同一個 frame、由同一個冷讀 reviewer 執行——實測一輪 34 個表格格抓出 9 格不通過、成因收斂為「以數量命名的縮略回指（四條件 / 兩步驟）」與「跨卡借術語不帶定義」兩類；句子層的產出契約是逐格判定表（沒有逐格表的「句子層 clean」不成立）、取代 v1.29.0 的「待驗證 + 手動抽樣」過渡做法。

**Version**: 1.29.2 — B′ 顆粒度句的殘片舉例修正：原寫法把「反比結構」「解釋不成無意」描述成抽離後無法回收、經引用審查比對 git 實況、該 checklist 項的先行詞在同項前分句——準確形態是「已定義概念的壓縮重述、抽離後要先解壓才可判讀」、據此改寫舉例。

**Version**: 1.29.1 — Round 3-G 補「缺章宣稱要全站反證」：G frame 的「該有而不存在」finding 採納前要搜前提原話、可能的 slug 形式與同義詞——實測一次「缺章候選」（命中是候選不是判決的共同前提）的主題已有 slug 逐字對應的既有卡、outbound reviewer 全站掃描漏掉；整合者預設處置從「登記待辦」改「登記待驗證」、驗證通過才升待辦。錯誤的缺章宣告誘發重複建卡、比漏報更貴——與 Round 1-C 宣告核對「錯誤的缺口宣告比沒有宣告更糟」同構、這裡發生在 reviewer 產出側。

**Version**: 1.29.0 — Round 2-B′ 冷讀 frame 補「顆粒度邊界」：本 frame 停在篇章層、句子層（checklist 項 / 表格格 / 判斷標準句這類設計上會被抽離消費的句子、殘片指涉抽離後無法回收）有同型問題、偵測法是單句抽離重讀；從一次三輪審查中篇章層冷讀 reviewer 全數放行、由使用者異源抓到壓縮句缺陷的實測抽出。句子層顆粒度併入或另立、依 2 次門檻待第二個 batch 驗證；過渡做法是對單句消費位密集的稿件手動抽樣跑抽離重讀。

**Version**: 1.28.0 — Round 3-B steelman 補「未言明的實作前提」這塊天生打不到的地方（前提不是主張、躲得過所有針對主張的檢驗；可辨識訊號是費力）與「軸名取了代理變數」的偵測歸屬；新增 Round 3-H 個案實跑 frame（instance 軸、判斷標準型內容必跑，兩次實測都產出該批最高價值 finding、且是唯一會抓到「照做會違約」的 frame）。新增 `unstated-implementation-premise-under-a-correct-axis` principle 卡。

**Version**: 1.27.0 — Round 1-E 斷言層加「讀者可重建性」檢查：結論式條列逐條問「讀者只憑文中已給的材料能不能自己得出這條」、重建不了的不是字句問題、是缺推導材料——修法是展開成讀者位置的走查（讀者位置 / 動作加材料 / 結論後置）、不是修字句。從一個「三個毛病」段落的兩版改寫抽出：三條結論都對、但正文只給了被批評的那行註解本身、讀者無從驗證；走查版由使用者判定「說得清楚非常多」並要求固化。這補的是「說明不清楚、讀者可能無法理解的簡單敘述」這類 finding 的標準修法路徑——先前 reviewer 抓得到「不清楚」、但沒有改寫範式可引用。同步 compositional-writing v0.50.0 的 `assertion-list-needs-reader-walkthrough` principle。

**Version**: 1.26.0 — 敘事姿態 bank 修正 v1.25.0 的「判斷標準放開頭」措辭：把結論抽到開頭直接給是灌輸、跟懸念是同一個缺陷的兩個方向（都讓結論與推導脫節）、v1.25.0 立規範時自己踩進了另一個方向。改為「標題承載結論、開頭承載情境定位、判斷標準由推導交付」、違規清單加「未經推導的開頭結論摘要與『觸發場景 / 整理目的』欄位組」。由使用者對套用了 v1.25.0 規範的改寫成品指出、同步 compositional-writing v0.49.0。

**Version**: 1.25.0 — Round 1-A 字句層 bank 加「敘事姿態」grep（問句標題 `rg "^title:.*[？?]"`、問句段標、敘事轉折詞「才想清楚 / 還是被退」、檢討類「我」密度）：教學與檢討內容寫給帶問題來的讀者，標題承載結論、判斷標準放開頭、檢討用客觀條件視角；操作型自問句合規、標題 / 段標 / 結論位扣住答案的問句違規。從一篇 work-log 檢討通過多輪審查、語氣問題由使用者指出的事故抽出，根因三層——規範缺位（規則當時不存在於任何規範、compliance reviewer 產生不了 finding）、frame 射程（枚舉不含懸念與第一人稱、persona 檢查掛在批次流程而單篇不進）、同源文風默認（問句標題與三幕劇是生成端高頻默認）。防線主力在生產側（寫作規範與模板）、本 grep 是補位。同步 compositional-writing v0.48.0 的 `write-for-readers-not-audiences` principle；metadata.version 同步修正漂移（1.23.0 → 1.25.0、changelog 實際已到 1.24.0）。

**Version**: 1.24.0 — 三處：停止判讀在四個訊號之外補「整體通讀」前置條件（所有 frame 跑過、所有 finding 修完，不等於內容現在是好的——兩次各自正確的修法可以合成對兩個 frame 都不可見的缺陷，而逐條檢查修法產物看不見它，因為它在條目之間），同一輪要重讀共用產物（入口頁 / 待辦清單 / 索引被每輪修改卻不在任何一輪的審查範圍裡）；整合工作流補「修法落在別人的段落上時讀完那一段再改」與除籍紀律（刪待辦要說出它被哪一次改動完成）；新增「重組後遺症 frame」給拆章 / 併章 / 整節重寫用（原篇敘事斷裂、指向原處的外部連結、搬運掉東西、新篇的定位宣告對自己全部內容成不成立、待辦除籍）。新增 `sequential-fixes-compose-into-defects` principle 卡。從一組資安章節連續跑八輪審查後的回看抽出——四處缺陷同一成因，其中一處是拆章時刪掉的待辦列裡有一半還沒完成。
**Version**: 1.23.0 — 新增「判斷兩個維度該不該合併：隔離實驗」段。frame 清單變長之後會反覆出現「這兩個維度是不是同一件事、能不能合併省一輪」，而憑推理判會錯——判的人同時懂兩個維度，看不出哪些東西是靠另一個維度才看得見的。做法是同一批稿件派兩個 reviewer 各自被硬性限制在一個維度內（限制要寫進 prompt 且夠硬，否則 reviewer 會自己補足另一邊），兩邊都對每個 finding 標記交叉可見性，並讓其中一邊回答方法論問題（執行過的人比事後看報告的人更清楚那個維度實際在做什麼）。判讀看互斥率與動作性質（時序 vs 全域）。附一次實測的數據與「結論與實驗前的預期不同」這個結果本身。

**Version**: 1.22.0 — 拆開「理解完整性」這個實測發現有效但混了兩件事的維度。Round 1-C 加**宣告核對**（掃「本站尚未寫 / 尚無對應章節 / 已列入 backlog / 不在本章範圍」逐條反證）——它與目的地承接是同一種動作（離開讀者視角、對全站窮盡查證），可機械化；錯誤的缺口宣告比沒有宣告更糟，因為它讓後續審查不再查那一項，實測抓過一篇新章三處寫「本站尚未寫」而該主題另有一整篇專章。Round 2-B 的走路線改成**必須指定讀者身分、起點與帶著的問題**（缺任一件就沒有判斷標準、退化成逐篇讀），產出是逐跳表、沒有逐跳表的「已走過」不成立，並建議同批走三到四條不同起點的路線（斷點常只在某一條上出現）。兩者明令不可混在同一個 reviewer：走路線沒有檢查表、宣告核對有，並行時後者會靜默擠掉前者。隔離實驗數據：冷讀 14 項有 7 項是路線讀者看不見的，走路線 8 項有 6 項是冷讀者看不見的，兩個 frame 誰都不涵蓋誰。新增 `declared-composition-is-not-performed-composition` principle 卡。

**Version**: 1.21.0 — Round 3 新增 G frame「共同前提盤點」（跨篇、批次寫完之後跑）：前面所有 frame 的檢查單位都是單篇（outbound 也是單篇對單篇），這個 frame 的單位是整批——把各篇的前置段、寫作邊界宣告與「這要靠 X 才做得到」的前提句抽出來並排，找同一個判斷被三篇以上當前提而沒有任何一篇承接的情形。它在單篇視角下不落空，所以逐篇審查不論多細都看不到；前置段是最常見的藏身處（它確實是本篇的適用性閘門、主題也對得上，「不屬於這篇」那條因此不觸發）。判別缺卡還是缺章：定義重複＝術語，判斷的各角重複＝缺章。這個 frame 的 finding 形態特殊——產出是「該有而不存在的篇章」，修法多半是登記待辦而非當場補。新增 `shared-premise-has-no-home` principle 卡。從一個資安模組四輪審查後盤點出十一項待辦裡有五項同形態、且全部由審查登記而非寫作當下浮現的實測抽出。

**Version**: 1.20.0 — Round 3 新增 F frame「誤用 / 激勵梯度」（審查對象是規則 / 協議 / 規範 / 流程時啟動）：前面所有 frame 問規則對不對，這個 frame 問「想少做事的執行者會怎麼合規地執行它」；三題推演（最省力路徑 / 與意圖的差距 / 規則擋不擋得住）、補痕跡不補語氣、塌陷方向沿零後續結論走；四個高頻形態（不適用清單無舉證要求＝關機鍵、數量上限被當配額、三級量表中值、暫緩條款無觸發觀察者）。Reviewer prompt 關鍵設計加「判定型規則要指定痕跡」。新增 `judgment-rules-must-specify-their-trace` principle 卡。從本 skill 自身內容跑該 frame、一輪十一項 finding 全部收斂成同一形狀的實測抽出——收斂本身是停止訊號（逐條補限定句會讓規則膨脹到需要導讀、補到三條就抽共用原則）。同輪對 Round 1-C 與 B‴ 各補一項痕跡要求：路由維度的產出是逐條表（抽查與逐條在不列條目的報告裡無法區分）、B‴ 的三種不適用要指出哪一段構成該形態。

**Version**: 1.19.0 — Round 3 self-application 回饋：Round 1-C 路由維度補第三道關卡**可達**（目的地有這個主題但埋在第三層小節時、讀者到站體驗與落空相同、驗收走到落點第一屏）與第四種處置**外部目的地**（會改版 / 下架而自己無法讓它保持承接、要寫明「到那裡要拿到什麼」）。B‴ frame 三處補強：微案例從一句描述擴成三項可執行檢查（四拍齊不齊 / 挑的形態對不對 / 第三拍有沒有來源）——原本跑 B‴ 的 reviewer 查不出「挑到後果最直觀的那一類」與拍數缺漏；地基斷言從梯度改成單峰（零經驗端缺術語入口而非情境、判定「不需補」要說出落在哪一端）；情境與實作的分界改用解析度。B‴ 末句的路由本身違反本 skill 剛加的路由維度（唯一連結指向不承接該主題的 SKILL.md、真正承接的檔案寫成 code 格式逃過檢查）、已改指承接的檔案。

**Version**: 1.18.0 — 多輪審查回饋修正 B‴ frame 的三處過時：地基斷言從二元（「能補的人本來就會判斷了」）改成梯度（補情境需要該領域的事故經驗、而那正是判斷的輸入之一，可用程度隨既有經驗遞減、最需要它的那一端拿到最少）；檢查成分從兩個補成三個（形態與觸發事件是分類語言回答進入條件、微案例是後果的敘事化）；v1.16.0 changelog 的同一句二元說法同步。這三處的共同成因是上游卡改了、下游執行端沒跟——而 B‴ 是 reviewer 實際讀的那一份，錯的斷言留在執行端比留在文件端影響大。
**Version**: 1.17.0 — `teaching-cite-strips-identity-and-scale` principle 卡補「本卡的邊界」：它管的是**引用既有 case 記錄**時該剝離什麼、不是禁止教學文出現敘事；作者自己寫的無身分短敘事不在範圍（不對應任何 case 記錄、無第二住址與漂移問題、本來就無身分而無映射成本）。讀成「教學文不該有敘事」是過度推論、實測發生過——一輪教材補寫因此只補分類語言（形態、觸發事件）、漏掉讀者要的「出事會長什麼樣」、直到提需求的人用原本的詞再問一次才發現。分類語言回答「我是不是這一類」、敘事回答「動作晚了會怎樣」。對應 report 卡 #224 的同步修訂。
**Version**: 1.16.0 — Round 2 新增 B‴「情境可想像性」frame（判讀 / 選型型文章專用）：掃機制陳述、問沒有實務經驗的讀者想不想像得出什麼時候會用到；跟 B（術語層）與 B″（操作型）分工明確、補位的是「判讀型內容的可用性」這一軸；含系統形態 / 觸發事件兩成分、判讀訊號欄的時序陷阱、三種不適用形態，程序與正反例路由到 compositional-writing 的 judgment-content-needs-scenarios。從兩篇判讀類章節「三輪十個 reviewer 全過、使用者一讀就問什麼情況會需要這個」的事故抽出（對應 report 卡 #241）。
**Version**: 1.15.0 — Portable 修正：三處指向外部 report 路徑的連結（outside-in reader frames / 常識是相對於讀者背景的 / 操作指引要帶環境專屬工具路徑）抽成 `references/principles/` 內的三張原則卡，改用相對連結——原本的絕對路徑複製到別的專案後是死鏈，違反 skill 的 portable 邊界。三張卡去專案化（移除模組名與卡號、保留論證與判讀徵兆），卡名與來源 slug 同名，鏡像工具的精確匹配自動生效、不必加 mapping。同步修正 frontmatter 的 version 欄位與末尾版本紀錄脫節（停在 1.12.0）。
**Version**: 1.14.0 — Round 1-C 加「路由目的地承接驗證」維度（必跑）：逐條掃 out-of-scope / 下一步路由 / 交接欄位、去目的地實際找出承接該主題的檔案，連結檢查只驗存在、這一維驗承接；code 格式的模組名（`` `05-deployment-platform` ``）不是連結、連存在都不驗、風險最高；落空分三種處置（指錯改指正確落點、該有但沒寫列 backlog 並判斷要不要先建簡版、不該路由則刪），指錯最易被誤判成「還沒寫」、所以先全站搜該主題。從密碼學選型章把金鑰託管送去部署平台、而六個 KMS / Vault 服務頁其實都在該章自己模組底下、三輪十個 reviewer 沒抓到、由使用者提問浮現的事故抽出（對應 report 卡 #240）。
**Version**: 1.13.0 — Round 3-B steelman 補「承重論點的 steelman 要用兩次」：claim-driven batch 的承重論點（錯了下游要大改的核心宣稱）該在動筆前先 steelman 當生產閘門、Round 3 steelman 是第二次收尾；只在 Round 3 才挑戰承重論點＝太晚、錯誤已寫進 N 檔跨檔回改；承重論點常是全稱 / 唯一性宣稱、反證靠枚舉反例；挑戰交對抗 / 異源（同源對地基有盲區）。從神經多樣性方法論「衝突只有一組」錯論點寫進 6 檔、Round 3 才抓的事故抽出（對應 report 卡 #236）。
**Version**: 1.12.0 — Round 1-F dogfood 回饋：加第六維度「數據時效標示」（文章老化後未標時效的財務數字會誤導）+ N/A 處理規則（N/A 要附理由、跟「未檢查」不同）；dogfood 實測 5 篇 x 6 維度 = 30 項檢查全通過，驗證框架的判讀覆蓋度到位。D4 關係人交易的範圍邊界（加盟食材加價是否算 D4 還是 D1/D3）標記為觀察、目前被其他維度覆蓋
**Version**: 1.11.0 — Round 1 新增 F reviewer「商業分析嚴謹度 audit」（conditional opt-in、商業/財務分析內容專用）：五維度檢查（分母與口徑 / 結構性 vs 一次性拆解 / 基準適用性 / 關係人交易 / 正常化 EPS）、同步 invoke `business-analysis` skill 的 7 步驟作為 checklist；「跟既有 skill 的關係」段加 business-analysis 垂直協同（跟 compositional-writing 相同模式）；從商業分析 18 篇教學系列的多輪審查實證抽出（卜蜂獲利拆解的分析→預測→驗證循環確認了五維度的判讀價值）
**Version**: 1.10.1 — Round 1-A 對讀者喊話 grep 補裸第二人稱（`你的|你在|你把`）：原 `你天天|你會|你可能` 只抓「你 + 明顯動詞」的祈使 / 預測句型、抓不到裸『你的』『你在』；同步 compositional-writing v0.29.0；register 類 grep 非窮舉、真防線是異源冷讀
**Version**: 1.10.0 — Round 1-B 補「教學層 case 引用敘事重量」檢查（審查事故觸發：reviewer 偵測到帳目搬運、修法停在通用化「113 張 → 上百張」、使用者兩次指正才補完階梯——規模鋪陳整句刪、產品身分與領域詞泛化為無身分載體）：帳目 / 規模鋪陳 / 產品身分逐句掃、修法階梯「先刪、刪不動才泛化」、泛化有下限（論證要留具象載體）、判定用「拿掉後論證還成立嗎」機械測試取代「規模感幫論證」直覺；新增 `teaching-cite-strips-identity-and-scale` principle 卡
**Version**: 1.9.0 — Round 1-E 補兩個檢查點（教學模組重寫 retrospective 觸發）：斷言層加「判斷標準成熟度」——維度清單（判斷看 A / B / C、無條件→行動映射）是判斷標準的空殼、機制正確仍可能空殼、驗收用重算測試（實證：重寫 batch 機制全數重建後仍有一段停在維度清單、Round 3 才抓到——此檢查點讓它在 Round 1 現形）；模組層加「推導源頭」——分析模組入口要能一句話說出推導起點、各篇判斷標準能折算回共同機制、主題集合結構下跨篇判斷標準矛盾不可見（實證：跨篇矛盾「免費選擇權 vs 訂單信用」的可見性建立在兩篇同屬一個推導體系上）
**Version**: 1.8.0 — Round 1 新增 E reviewer「斷言支撐 / 知識類型 audit」（漏抓事故觸發：採購 planning 模組三輪全過、merge 後仍被使用者判定「講故事不是商業分析教學」——歷輪 finding 全落在字句與結構層、無任何 frame 負責斷言支撐與模組級知識類型）：斷言層抽 3-5 個承擔判斷標準的斷言問「靠什麼成立」（機制 / 量化 / 來源 vs 口吻與權威）、模組層對照分類 house style；跟 steelman「閾值有無源頭」的差別是句子層誠實 vs 判斷標準層可推導；經驗談 / 訪談素材的 batch 此 frame 必排第一輪；反模式加「把多輪全過當知識類型對」（finding 類型分佈全集中表面層 = 深層無人在看）；新增 `review-needs-claim-support-frame` principle 卡
**Version**: 1.7.0 — Reviewer prompt 關鍵設計補「SRP 違反要標路由目的地」：reviewer 標記「不屬於」時同時標建議目的地（已有文章 / 新文章 / 新分類 / 留原處加標記）、避免修改者選最省力的刪除而非最正確的路由；新增 `misplaced-content-routing` principle 卡
**Version**: 1.6.0 — Round 2-A cadence + 反模式段補「均勻修法複製新模具」：為破舊 cadence 模具立的單一生成端規則（如「段首一律目標詞先行」）均勻套整批會收斂出更密的新模具、同源自審在「已修」錯覺下看不到、修法要輪替 framing + 修法產物進整組跨卡異源重掃；新增 `uniform-remediation-recreates-homogenization` principle 卡
**Version**: 1.5.1 — changelog cross-reference 修正：1.5.0 條同步對象版號筆誤 v0.11.0 改 v0.24.0、1.4.1 條的 v0.18.0 依 compositional-writing changelog 重編（0.18.0 重號整理）改 v0.23.0
**Version**: 1.5.0 — Round 1-A 地區用語 grep 加慣用語層（`rg "拍腦袋|拍板|靠譜|給力|接地氣|一波|死磕|躺平|內卷"`）：慣用語直譯是開放集合、同源 reviewer 對這層有結構盲區、回報「clean」不可當真、需目標地區讀者冷讀；同步 compositional-writing v0.24.0 的 `regional-idioms-evade-keyword-bank` principle
**Version**: 1.34.0 — Round 1-A 字句層 bank 加「物理化錯配」grep（`rg "撐得住|撐不住|撐得起|撐不起|能撐|撐住|撐起|撐不久|站得住|站不住|站得起|站不起|掛在|扛|垮|頂得住|咬得|咬合|啃"`）：用詞搭配錯位的第三種、也是三種裡唯一有穩定關鍵詞的一種，判斷標準可機械執行（問這個動詞照字面成立需要什麼物理條件）；危害多一層——借來的物理動詞聽起來已經像答案、讓「支持什麼、到什麼範圍」不必被回答。同步 compositional-writing v0.51.0。

**Version**: 1.4.2 — Round 1-A 字句層 bank 加「用詞搭配錯位」grep（`rg "說完的話|背後.{0,8}的話|想告訴|潛台詞|訊號很直接"`）：抽象概念配不貼合屬性的謂語（擬人化 + 形容詞誤搭）、無穩定關鍵詞真防線是異源冷讀；同步 compositional-writing v0.32.0 的新 frame
**Version**: 1.4.1 — Round 1-A 字句層 bank 加「泛用詞濫用」grep（`rg "坑|東西|搞|弄|處理一下|情況"`）：同一泛用詞蓋不同具體情境、依情境換精確詞、「坑」繁中少用；同步 compositional-writing v0.23.0 的新 frame
**Version**: 1.4.0 — 三輪硬底線：「三大基本原則」升為「四大」、新增第四條「至少三輪」；Round N 判讀段改為 Round 3 結束後才開始；evidence 補 dotfile 31 篇 43 finding 實證
**Version**: 1.3.0 — Round 2-B reader-persona 加「術語知識卡覆蓋」維度（常識是相對於讀者背景的）
**Version**: 1.2.0 — B″ executable-walkthrough 加環境分支強調（同根因二次返工的防護）
**Version**: 1.1.0 — 新增五個 outside-in reader frame：Round 1 加 downstream-task、Round 2 加 reader-persona register + executable-walkthrough、Round 3 加 persona-coverage + search-landing；從 infra 模組生產週期 retrospective 抽出（6 個由使用者而非 reviewer 發現的盲點）
**Version**: 1.0.0
