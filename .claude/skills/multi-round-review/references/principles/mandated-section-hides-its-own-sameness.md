# 規範指定了某個位置要交付什麼，全批長成同一個模板時沒有任何一道檢查管得到

> **角色**：本卡是 `multi-round-review` 的支撐型原則（principle）、被 2-A Cadence frame 引用。
>
> **何時讀**：跑 cadence frame、掃到一批稿件在同一個位置有同樣的結構，而那個位置是規範要求的時候。

規範指定位置的時候說的是**那裡要交付什麼**：每條跨篇路由要交代目的地會給什麼、每張卡要有角色與何時讀的起手段、每份文件開頭要交代適用範圍。規範不說那裡要長什麼樣，因為形態本來就該隨內容變。

而生成端會替它補上一個形態，並且**整批補同一個**。補完之後這個位置在每一篇裡都合規、都交付了規範要的東西，而讀者連讀十幾篇時拿到的是同一段話重複十幾次。

同骨化與合規在這個位置上無法區分，因為它們是同一件事的兩面——**規範說了要交付什麼，所以形態重複沒有違反任何一條規則**。

## 這個 frame 跑過了仍然漏掉的原因

cadence frame 的對象正是一批三篇以上的稿件，掃的正是同位置同句型。它會掃到。

掃到之後 reviewer 的下一個動作是回去對照規範，而規範明文要求這個位置要有東西。**「這個重複是規範要求的」於是成為豁免理由，而規範要求的是內容不是形態。** 判定停在這裡，重複不進 finding。

三件事讓它更難現形：

- **逐篇讀不會顯形。** 那個段落在每一篇裡只出現一次，單篇冷讀、單篇合規檢查、單篇的理解探針全部通過。
- **內容確實不同。** 各篇連到不同的目的地、各自複述自己那一篇的內容，逐字比對不會相同。相同的是骨架。
- **沒有任何一條規則說「形態不可以整批相同」**，所以零 finding 與沒有問題同訊號。

同族的第二種形態換一個位置：**規則自己的作用單位**沒有寫明：一條「每段首句先說這個概念是什麼」的規則，作用單位是段落，而一篇文章的第一段也是段落，於是它在那裡同樣生效、產出每一篇的第一句都是結論——即使同一份規範另有一條寫著未經推導的開頭結論要抽掉。兩條規則在同一個位置給出相反的指示，而各自單獨讀都通過。

## 判讀方式

一句話：**規範在這個位置要求的是「要交付什麼」還是「要長什麼樣」。**

要求的是交付，而產出全批同形時，那個同形沒有任何規則背書——它是生成端的省力路徑，不是合規的結果。

偵測要把同一批的那個段落抽出來並排，逐篇讀不會顯形：

```bash
# 先普查段名，<目錄> 換成這一批的所在位置
# -I 是不印檔名（rg 的 -h 是 help）；LC_ALL=C 不可省，
# 少了它 sort 與 uniq 在部分語系下會把不同的非 ASCII 段名視為等值而併成一組
LC_ALL=C rg -I '^## ' <目錄>/*.md | LC_ALL=C sort | LC_ALL=C uniq -c | LC_ALL=C sort -rn | head

# 再抽出可疑的那一個段落，連段名與後續句型一起看
rg -A 6 '^## <上一步命中的段名>' <目錄>/
```

第一條的判讀是**計數接近檔案數的那幾個段名**。一次實測的輸出是一個段名出現 21 次、其餘每個段名各出現一次，而該目錄正好有 21 篇——落差這麼大的時候不必訂門檻，看第一列就夠。第二條再去讀那些段落的句型，判斷相同的是骨架還是內容。

對規則本身另問一句：**這條規則的作用單位是什麼，它有沒有寫在規則裡。** 沒寫的時候它會在每一個符合字面的位置生效，包含規則沒打算管的那一個。

## 處置

**審查側**：把「這個重複是規範要求的」從豁免理由改成**加強掃描的訊號**。規範要求的位置是同骨化最容易發生的位置，理由正是它有一條規則保證每一篇都會有它。

**規範側**：指定一個位置要交付什麼的時候，同一段寫明「這裡不規定形態」，並給至少兩種明顯不同的形態當範例。只給一個範例等於指定了形態，而給零個範例會讓生成端自己挑一個並整批沿用。

**規則側**：每一條會被反覆套用的規則寫出它的作用單位（一句、一段、一節、一篇、一批），以及它不管哪一個單位。

**處置不是刪掉那個段落**，那會讓規範要的交付整批消失。動的是形態：有的篇適合寫成一段散文、有的適合兩三句、有的本來就只有一個去處而不需要清單。
