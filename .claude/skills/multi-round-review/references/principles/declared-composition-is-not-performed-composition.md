# 宣告的組合不等於執行的組合：顯著的那半擠掉費力的那半

> **角色**：本卡是 `multi-round-review` 的支撐型原則（principle）、被 Round 2-B 的走路線 frame 引用。
>
> **何時讀**：一個 reviewer prompt 同時要求兩件性質不同的事時；或回頭發現某個維度的產出長期偏少而另一個偏多時。


## 論述基礎與限制

本卡從一次 skill 組合的執行落差抽出、且是自我示範：把 `neurodivergent-output`（塑形 AI 輸出的規則集）與 `5w1h-decision`（把決策拆成 Who／What／Why 等欄位的規則集）兩個 skill 同時啟用，兩者的協作設計明文寫了「帳本（每則回覆末尾累積的跨訊息決策記錄）的決策行、用壓縮的 5W1H 填寫」。實際執行時，只跑了神經多樣性的跨訊息帳本（顯著、視覺化），從沒把決策行套上 5W1H 的欄位結構（費力、不顯眼）——卻一路回報「兩個都開、協作啟用」。不是自己抓到，是使用者指出「只看到帳本、沒看到 5w1h」。限制：本卡談「組合的執行落差」，不是「組合的設計」（設計見 「#235」）；適用於「兩個以上行為被宣告同時生效」的情境，單一行為不涉及。

---

## 核心原則

**組合兩個行為時，宣告「都啟用」不等於「都執行」——要在每個輸出點驗證兩半都真的現形，不是宣告了就算。** 組合的兩半在執行時受力不均：顯著、省力、視覺化的那半會被做出來，費力、不顯眼、要額外轉換的那半會靜默掉。而執行者常憑「我開了兩個」就回報「兩個都在」——他查的是**宣告**，不是**輸出裡的實際現形**。宣告是必要條件、不是充分條件。而且光靠事後在輸出點檢查是**偵測**、不是**預防**——更強的做法是把費力那半接到一個本來就每次觸發的行為上，讓它跟著自動發生（見修法）。

---

## 反模式：顯著的那半當成整體的證據

失效鏈有三步：(1) 把組合寫進 skill / 規則（宣告協作）；(2) 執行時只做出顯著的那半；(3) 自審看到顯著那半在、就判「組合有跑」。事故實例：帳本（顯著）每則都出現，讓「協作啟用」看起來成立，但 5w1h 結構化（費力）從沒套上——顯著那半的存在被當成整個組合的合規證據。這跟 「規範化 ≠ 自審」 同構：把規則寫下來（宣告）不等於遵守它。也是 「自審偵測方法要對齊規則類型」 的實例——自審查「有沒有宣告啟用」、而不是「輸出裡有沒有現形」，對執行落差結構性失明。

---

## 修法：把費力那半接到會觸發的行為上，別只事後檢查

事後在輸出點檢查是**偵測**、不是**預防**——它仍然依賴你記得檢查、也依賴你一開始就做了那半。更強的修法是**設計**：讓組合的費力那半接到一個「本來就每次都會觸發」的行為上，跟著它自動發生、不靠記憶。

1. **把費力那半接到會觸發的顯著行為上（預防、優先）**：找出組合裡「本來就每則都會跑」的那半（實例是跨訊息帳本），把費力那半**寫進它的觸發點**，讓費力那半的產出＝顯著那半產出的一部分，而不是另外要記得做的附加步驟。實例：把「決策行用 5W1H」寫進帳本規則**本身**（帳本每則都跑 → 5W1H 跟著每則跑），不放在一個獨立的 Collaboration 附錄——附錄會被跳過。
2. **降低費力那半的啟動成本**：費力那半會靜默掉，一部分因為它費力。指定一個最小形式（如 What／Why／下一步三行、不帶完整鷹架），降低每次執行的門檻。
3. **事後逐半驗證當 backstop（偵測）**：pre-send check 每個組合行為加一條「這則有沒有現形」，當接不進觸發時的最後一道。它是 backstop、不是主防線——別只靠它。

---

## 跟其他原則的關係

| 原則                               | 關係                                                                                                          |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| 「整合互斥規則集」                 | 一體兩面——#235 是組合的**設計**（怎麼把互斥規則整合），本卡是組合的**執行落差**（整合好了但沒真的跑其中一半） |
| 「規範化跟自審是兩種認知任務」     | 同構——寫下規則 / 協作（宣告）≠ 遵守它（執行）；本卡是它在「組合」情境的版本                                   |
| 「自審偵測方法要對齊規則類型」     | 提供本卡「為何沒自己抓到」的機制——自審查宣告、不查輸出現形，對執行落差失明                                    |
| 「多階段流程的 artifact 欄位契約」 | 同「宣告的介面 ≠ 實際交付」家族——#163 是欄位契約缺口，本卡是組合行為的執行缺口                                |
| 「L1 + L2 疊加時的訊號一致性」     | 疊加的各層要一致現形——本卡是「某一層根本沒現形」的更前一步失效                                                |

---

## 判讀徵兆

| 訊號                                                    | 該做的事                                                                           |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 同時啟用兩個 skill / 規則、回報「都在 / 協作啟用」      | 別憑「開了兩個」就算數；檢查這則輸出裡兩半都真的現形                               |
| 組合裡有一半顯著（視覺 / 省力）、一半費力（要額外轉換） | 費力那半會靜默掉、優先驗它；顯著那半在不證明費力那半在                             |
| Collaboration 寫成獨立附錄、靠記憶每則執行              | 把費力那半接到會觸發的行為（如帳本規則本身），別放附錄；pre-send 檢查只當 backstop |
| 自審回報組合合規、但拿不出費力那半的實際輸出痕跡        | 自審查的是宣告不是輸出（#232）；改查輸出裡的可見痕跡                               |
