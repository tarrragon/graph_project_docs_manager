# 互動回饋：點擊確認、等待指示、結果通知

本 reference 為「設計或審查使用者動作的系統回應」情境的入口檔。原一份檔案因補齊範例後超出體量門檻，依讀者任務拆為四檔（同組，逐檔互指）：

| 檔案 | 內容 |
| --- | --- |
| `interaction-feedback.md`（本檔，入口） | 核心原則／三層回饋模型／按鈕級回饋（按鈕流程與六種按鈕狀態） |
| `interaction-feedback-waiting-and-notification.md` | 時間門檻與回饋策略／畫面級回饋／Spinner vs Skeleton／結果通知的形式選擇 |
| `interaction-feedback-component-semantics.md` | 元件語意與版面檢查（切換標籤／可點性／選中態／溢出／版面保障） |
| `interaction-feedback-checklist.md` | 反模式速查／檢查清單／參考來源（三份內容檔共用） |

適用：審查按鈕回饋完整性；設計非同步操作的按鈕生命週期與防重複提交。判斷 loading indicator 的時機與形式、連線 / 配對 / 同步類多步驟流程的畫面級回饋、操作結果的通知形式選擇，見 `interaction-feedback-waiting-and-notification.md`；元件語意與版面（切換標籤、可點性辨識、選中態對比、溢出 affordance、回饋文字的版面保障），見 `interaction-feedback-component-semantics.md`。
不適用：畫面狀態機的完整設計（狀態矩陣範疇 — 本文的畫面級回饋聚焦「每個狀態給什麼指示」，狀態的枚舉與退出路徑歸狀態矩陣）；錯誤訊息的文案內容（錯誤恢復範疇）。

> **自包含聲明**：本檔（核心原則／三層回饋模型／按鈕級回饋）閱讀不需要先讀其他 reference；三層回饋模型第 2 層的分帶細節與畫面級 / 通知形式的完整規則在 `interaction-feedback-waiting-and-notification.md`，本檔在對應處已具名指路，不需要讀者自行猜測該開哪一檔。

---

## 核心原則

**每個使用者動作都需要即時、誠實的系統回應。** 沒有回饋的按鈕等同壞掉的按鈕 — 使用者無法區分「系統在處理」和「按鈕壞了」。對應 Nielsen 十大可用性啟發法第一條（系統狀態可見性）。「誠實」的含義：指示與實際狀態一致 — 假進度條、與實際無關的百分比，被識破後信任一次性崩壞。

## 三層回饋模型

使用者按下按鈕後，回饋依時間順序分層：

| 層          | 時間點    | 回答           | 實作                            |
| ----------- | --------- | -------------- | ------------------------------- |
| 1. 點擊確認 | 0-100ms   | 「收到操作了」 | 視覺狀態變化 / ripple / 觸覺    |
| 2. 等待指示 | 100ms-10s | 「正在處理」   | 依 `interaction-feedback-waiting-and-notification.md`〈時間門檻與回饋策略〉分帶決定：100-400ms 用動畫過渡、400ms 以上才用 spinner / loading 狀態 / 進度條（含延遲顯示機制） |
| 3. 結果通知 | 操作完成  | 「結果是什麼」 | 畫面更新 / SnackBar / 錯誤訊息  |

第一層必須在 100ms 內出現 — 平台原生元件的 pressed / ripple 互動態已滿足，自建元件（自繪按鈕、canvas UI）才需要自行驗證延遲。第三層的四種結果都要設計：成功（讓使用者看到改變）、失敗（原因 + 下一步）、空結果（區別「沒找到」和「沒載入」— 明確文字 + 下一步建議）、部分成功（摘要先告知整體、明細列失敗項與修正動作，讓使用者只處理失敗部分）。

**結果通知的鏈路前提**：第三層假設「結果會正確到達 UI」。UI 與執行端分屬不同 context 時（browser extension 的 popup / content script / service worker、跨 process 架構），結果要跨訊息通道才到 UI — 通道語意錯誤（async listener 在共享通道上把 undefined 搶先當回應送回）或事件訂閱缺失（handler 沒注入、事件沒人接）會讓通知消失、甚至反轉成「操作成功、UI 報失敗」— 比零回饋更糟，使用者會重做操作並失去對介面的信任。跨 context 的結果通知要有端對端驗證（操作成功 → UI 顯示成功），不只測 UI 收到資料後的呈現。

## 按鈕級回饋

**非同步按鈕**（API / DB / 權限請求 — 點擊後需等待外部回應）：

```text
[idle] → 點擊 → [loading: disabled + spinner] → 完成 → [idle + 結果通知]
```

- loading 期間 disabled（防重複提交 — UI 層降低發生率、最終防線是伺服器端冪等，兩層都要做）
- 按鈕文字可變（「送出」→「送出中...」）
- 結束後必須恢復 idle — 不論成功失敗；配 timeout 兜底（取值略大於 client / 後端逾時，逾時後恢復 + 錯誤訊息）
- 結果通知獨立於按鈕（SnackBar 或畫面更新），不塞在按鈕裡

### 三段鏈式範例：非同步按鈕 timeout 取值

情境：判斷按鈕的 timeout 兜底該取多大，不能直接等於後端逾時值。驗證段的判定屬「設計產出物」。

【輸入】

```
SPEC 片段：一個非同步按鈕呼叫的後端 API，逾時設定為 30 秒
```

【產出】

```
形態：決策表
判斷依據                         | 結果
按鈕 timeout 是否應等於後端逾時值 | 否（等於後端逾時值時，按鈕的 timeout 兜底與後端
                                    逾時幾乎同時觸發，沒有緩衝時間讓正常的後端錯誤
                                    回應先送達）
按鈕 timeout 應如何取值           | 略大於後端逾時值，讓後端有機會先回傳明確的錯誤
                                    訊息（優於單純的「沒回應」）
結論                             | 後端逾時 30 秒時，按鈕 timeout 設 35 秒（30 秒 + 5 秒緩衝）
```

【驗證】

```
檢驗問句
Q 按鈕 timeout 是否可以直接等於後端逾時值（30 秒）？
  預期：否——會與後端逾時同時觸發，讓後端來得及回傳的明確錯誤被按鈕的逾時兜底
  提前蓋過，使用者只看到通用逾時訊息，看不到後端原本要回傳的具體原因

Q 緩衝時間是否有固定的計算公式（如固定加 5 秒或乘以固定倍數）？
  預期：否——本規則只要求「略大於」，具體緩衝量由規格撰寫者依該操作的重要性與
  使用者等待耐性決定，本例採加 5 秒是其中一種合理取法，不是唯一公式
```

**同步按鈕**（導航 / 切換 / 開關 — 本地立即完成）：

```text
[idle] → 點擊 → [視覺回饋 + 執行] → [完成]
```

- 不需要 loading 狀態
- 防連點用 leading-edge debounce（第一次點擊立即執行、之後約 300ms 內忽略）或導航鎖（in-flight flag）。trailing debounce（等 300ms 沒新點擊才執行）會給每次操作加 300ms 延遲、違反 100ms 門檻 — 防的是第二次點擊、第一次要立即生效。不用 disabled 防連點（會讓按鈕每次點擊閃跳灰態）。

**六種按鈕狀態** — 每種狀態傳達一種系統訊息、缺少任何一種使用者就失去對應判斷依據：

| 狀態     | 傳達訊息   | 設計要點                                                                                  |
| -------- | ---------- | ----------------------------------------------------------------------------------------- |
| Default  | 可以按     | 一眼可辨識為按鈕                                                                          |
| Hover    | 可互動元素 | 僅桌面端（行動裝置無 hover）                                                              |
| Active   | 收到點擊   | 對應第一層點擊確認；視覺差異要夠大、可配觸覺                                              |
| Focus    | 焦點在此   | 鍵盤導航必要（WCAG 要求可見焦點指示）                                                     |
| Disabled | 現在不能按 | 說明原因（常駐文字優先 — tooltip 對行動端與鍵盤使用者不可達）；條件與使用者無關時考慮隱藏 |
| Loading  | 正在處理   | 自動帶 disabled、有 timeout、結束必恢復                                                   |

**與元件契約的分工**：時間門檻與通知形式（本節與 `interaction-feedback-waiting-and-notification.md` 的〈時間門檻與回饋策略〉〈結果通知的形式選擇〉）仍由本 skill 產出；每形態（手機／平板／桌機／CLI）的回饋通道與狀態來源，由 `component-contract-design` skill 承接的元件契約〈回饋契約〉欄承載，交規格撰寫者逐元件填寫，本 reference 不重述。

實作落點在共用 Button 元件層、不是逐顆按鈕 — 平台元件庫已內建前四種，通常只需補 Loading / Disabled 的語意。估工按「元件層一次工 + 各按鈕接狀態來源」，不是「按鈕數 × 狀態數」（後者高估數倍、讓完整狀態設計看起來做不起）。

**元件語意與版面檢查**（切換標籤現態／動作歧義、非互動指示與按鈕形態、選中態對比、水平溢出 affordance、關鍵回饋文字版面保障、版面擠壓診斷分層，共六項判準）獨立為 `interaction-feedback-component-semantics.md` — 狀態清單之外，同一元件在靜態呈現時另有一組容易誤讀的語意落差，見該檔。

## 參考來源

見 `interaction-feedback-checklist.md`〈參考來源〉（三份內容檔共用，避免同一份清單在多檔各自維護而漂移）。
