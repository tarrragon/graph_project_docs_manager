# Gate 與 Fallback：關卡失敗時使用者的路

本 reference 為「設計或審查使用者必須通過的關卡」情境。

適用：生物辨識 / 密碼認證、網路連線檢查、系統權限請求（相機 / 位置 / 通知）、硬體或版本條件檢查；審查「條件不滿足時使用者被擋死」的缺口；規劃實機測試時判斷哪些關卡在開發環境測不到。
不適用：畫面內部的狀態轉換設計（屬狀態矩陣範疇）；錯誤發生後的恢復流程（屬錯誤恢復範疇）— gate 管「進得去嗎」，錯誤恢復管「進去之後出錯怎麼辦」。

> **自包含聲明**：閱讀本文件不需要先讀其他 reference。

---

## 核心概念

Gate 是使用者操作流程中「必須通過才能繼續」的關卡。Gate 設計的核心責任是確保使用者在每種結果下都有路可走，而非只設計「通過」的情境。Fallback 是 gate 未通過時的替代路徑：降級功能、替代驗證方式、手動重試、或放棄返回。

## 每道 gate 的必答問題

設計每道 gate 時回答三個問題（成功 / 失敗 / 不確定），任何一題答不出來就是設計缺口：

**成功時做什麼** — 通過後進入下一步。最直覺、最不容易遺漏。

**失敗時做什麼** — 未通過時的替代路徑。最容易遺漏的一題。替代路徑的選項：降級功能（部分可用）、替代驗證（密碼代替 Face ID）、手動重試、放棄操作（返回上一頁）。實例：一個自用工具 app 的 biometric gate 設了「只接受生物辨識」，Face ID 不可用時使用者直接被擋住 — 沒有密碼 fallback、沒有跳過、沒有返回；修復只是改一個參數讓系統 fallback 到裝置密碼，但這個決策本該在企劃階段做、而非實機測試時才發現。

**使用者不知道發生什麼時做什麼** — gate 處理中（loading）或結果不確定（timeout）時的呈現：認證彈窗因系統延遲未出現、請求已發未回應、權限對話框被其他 dialog 遮擋。這個狀態下使用者需要：知道系統在做什麼（loading 指示）、可以取消等待、超過合理時間有提示（timeout 訊息 + 重試選項）。

把三題答案整理成 gate 設計表、作為規格的一部分。失敗欄和不確定欄為空的 gate 就是 UX 死胡同候選。

失敗欄的列粒度以 fallback 是否相異為準：fallback 不同的失敗類型各自一列；fallback 相同的類型合併為一列，列名須逐一列出涵蓋的每個成員類型（例如「iOS 拒絕一次不再顯示、Android 拒絕兩次不再顯示」），不可用單一總稱概括（如「永久拒絕」「平台權限限制」這類看似涵蓋、實際沒有指出成員的說法）。本檔有失敗類型分類表的 gate（如〈生物辨識的失敗情境與平台行為〉），表中每一類都須出現在某一列，不適用的類型在表下註明理由，不可不列。

### 三段鏈式範例：gate 設計表的失敗欄粒度

情境：判斷生物辨識與相機權限兩個 gate 的「失敗」欄該分幾格。驗證段的判定屬「設計產出物」（見 `.claude/skills/skill-design-guide/references/chained-examples.md`〈各段允許與禁止內容〉【驗證】段的判定規則）。

【輸入】

```
SPEC 片段：一個需要生物辨識登入的畫面，登入失敗的實際情況涵蓋「光線不足導致辨識失敗」
與「使用者尚未在裝置設定生物辨識」兩種；另一功能需要相機權限，使用者可能是第一次
被系統詢問後拒絕、也可能先前已拒絕過一次（不會再跳系統對話框）
```

【產出】

```
形態：決策表

生物辨識失敗欄的粒度判定：
  兩種情況是否分屬〈生物辨識的失敗情境與平台行為〉表中不同的失敗類型 | 是
    （光線不足＝暫時性；未設定＝使用者未設定）
  兩種類型的合理 fallback 是否相同                                   | 否（暫時性→重試可能成功；
                                                                        未設定→引導設定或替代認證）
  另兩類（持續性、硬體不可用）是否也須出現在表中                      | 是——即使本次 SPEC 輸入未提及，
                                                                        依失敗欄粒度規則四類都須交代；
                                                                        兩類 fallback 相同（皆需要替代
                                                                        認證），合併為一列
  結論                                                               | 生物辨識「失敗」欄不可收成一格
                                                                        「提示輸入裝置密碼」，依 fallback
                                                                        是否相異分列：暫時性、未設定各自
                                                                        一列，持續性與硬體不可用 fallback
                                                                        相同、合併一列並列名寫出涵蓋類型

相機權限失敗欄的粒度判定：
  使用者是否可能處於「系統對話框不再顯示」狀態     | 是；且此狀態由平台決定——iOS 拒絕一次即不再
                                                      顯示，Android 11 起同一權限拒絕兩次後不再顯示
  「系統對話框仍可顯示」與「不再顯示」的合理處置是否相同 | 否（仍可顯示：僅能靠使用者再次主動觸發
                                                            功能後才重問；不再顯示：僅能導至系統設定）
  結論                                             | 相機權限「失敗」欄須依「系統對話框仍可顯示」
                                                      （如 Android 首次拒絕）與「不再顯示」（iOS
                                                      拒絕一次、或 Android 拒絕兩次後）分列，不可
                                                      合併成一格「說明原因 + 設定連結」；iOS 沒有
                                                      「仍可顯示」這一列

依上述判定填出的 gate 設計表：

  Gate                       | 成功         | 失敗                                    | 不確定
  -------------------------- | ------------ | ---------------------------------------- | -------------------
  生物辨識-暫時性             | 進入主畫面   | 提示重試                                 | 顯示「驗證中」
  生物辨識-未設定             | 進入主畫面   | 引導設定生物辨識或改用替代認證           | 顯示「驗證中」
  生物辨識-持續性／硬體不可用 | 進入主畫面   | 改用替代認證（裝置密碼）                 | 顯示「驗證中」
  網路連線                   | 開始載入資料 | 顯示離線提示 + 重試                      | 顯示 loading + 取消
  相機權限-對話框仍可顯示     | 開啟掃描功能 | 說明原因，使用者再次主動觸發功能後才重問  | 等待系統對話框
                                （如 Android 首次拒絕；iOS 無此列）
  相機權限-對話框不再顯示     | 開啟掃描功能 | 說明原因 + 導至系統設定連結（iOS 拒絕    | 等待系統對話框
                                一次、或 Android 拒絕兩次後皆屬此列）
  藍牙                       | 開始裝置搜尋 | 提示開啟藍牙 + 連結                      | 顯示搜尋中 + 取消
```

【驗證】

```
檢驗問句
Q gate 設計表的生物辨識失敗欄是否維持單一儲存格？
  預期：否——依 fallback 是否相異拆列：暫時性、未設定各一列，持續性與硬體不可用 fallback
  相同、合併一列並寫出涵蓋類型，與〈生物辨識的失敗情境與平台行為〉表四類全數交代一致

Q gate 設計表的相機權限失敗欄是否因為平台不同而有不同列數？
  預期：是——依系統對話框能否再次顯示分列，而非籠統寫「首次／永久」；iOS 拒絕一次即不再
  顯示，沒有「對話框仍可顯示」這一列，與〈Gate 類型與各自的設計重點〉權限 gate「拒絕後」
  的平台差異一致，避免兩處對同一情境給出不同答案
```

## Gate 類型與各自的設計重點

**認證 gate**（生物辨識、密碼、PIN、OAuth）：fallback 設計取決於安全需求 — 銀行 app 可能要求雙重驗證、沒有更低層級 fallback；自用工具的使用者就是 owner、可用性優先於認證強度。取捨要顯式記錄（見下方記錄格式）。

**網路 gate**：狀態是連續的（連線中 / 已連線 / 斷線 / 不穩定），比二元結果的 gate 難處理。三種策略依功能對即時連線的依賴程度選擇：

- **Offline-first**：核心操作在本地完成、網路只負責同步。前提是資料可本地存儲且衝突可解決（筆記、待辦）。不需要 gate — 網路狀態不阻擋操作，UX 重點是同步狀態可見（已同步 / 待同步 / 失敗）。
- **Retry with feedback**：功能需要網路但可等待（即時連線類）。斷線時顯示狀態 + 手動重試 + 退出路徑；自動重試要有上限；間隔依失敗成因選（瞬斷用固定間隔、伺服器過載用遞增，見 `error-recovery.md`〈重試機制〉間隔策略）。
- **Degraded mode**：部分功能依賴網路。斷線自動降級、清楚標示哪些功能不可用，不阻擋其餘操作。呈現快取內容時須標示內容可能不是最新。

判別順序：核心操作能否在本地完成且衝突可自行解決？是 → Offline-first。否 → 斷線時是否仍有可操作的功能或可呈現的既有內容？是 → Degraded mode；否 → Retry with feedback。

連線存在但品質差（高延遲、頻繁斷開）比完全離線更難：每次請求可能成功可能逾時。處理：合理的逾時時間、逾時後顯示狀態與重試選項（不自動重試、避免在不穩定網路上累積請求）、loading 狀態提供取消。

### 三段鏈式範例：網路 gate 策略選擇

情境：判斷一個「離線也能看到既有清單，但清單需要與伺服器保持一致」的功能該選哪種網路策略。驗證段的判定屬「設計產出物」。

【輸入】

```
SPEC 片段：一個離線也能瀏覽既有清單、但清單內容需要與伺服器保持一致
（伺服器端可能被其他使用者修改）的功能
```

【產出】

```
形態：決策表
條件                                     | 判定
核心操作能否在本地完成且衝突可自行解決   | 否（內容依賴伺服器端其他使用者的變更，
                                            本地無法獨立判定最新狀態）
斷線時是否仍有可操作的功能或可呈現的既有內容 | 是（可先顯示既有快取內容，同步恢復後更新）
結論                                     | 依判別順序，第一問否、第二問是 → 採 Degraded mode
                                            （非 offline-first）：斷線時顯示快取內容並標示「未同步
                                            最新變更」，而非宣稱本地內容即為最新狀態
```

【驗證】

```
檢驗問句
Q 是否因為「離線也能瀏覽」就直接判為 offline-first？
  預期：否——offline-first 的前提是本地資料可獨立存在且衝突可解決；本情境的內容
  一致性依賴伺服器，判準是「核心操作能否在本地完成且衝突可自行解決」，
  不是「離線時能不能看到東西」

Q Degraded mode 的呈現是否需要標示目前內容可能非最新？
  預期：是——與本檔〈網路 gate〉「呈現快取內容時須標示內容可能不是最新」一致
```

**權限 gate**（相機、位置、通知、麥克風）：特殊性在使用者可以永久拒絕 — 拒絕後再次請求不會彈系統對話框，唯一路徑是引導到系統設定手動開啟。第一次請求的時機和說明直接影響授權率：

- 時機：多數場景推薦「功能使用時即時請求」（使用者在操作 context 中、理解為什麼需要）而非首次開啟一次性請求（使用者尚未使用功能、傾向拒絕）。首次開啟請求只適合權限少（1-2 個）且與核心功能直接相關（相機 app 請求相機）。時機判定以權限為單位：同一權限取第一個需要它的功能判斷首次請求時機，之後其他功能沿用已取得的授權狀態（已拒絕者走下方〈拒絕後〉），不另立請求時機。
- Pre-permission 說明畫面：觸發系統對話框前先用自己的畫面解釋用途 —「需要相機來掃描 QR code」而非 API 名稱；提供「稍後再說」；可視覺化展示授權後的功能。
- 拒絕後：不自動重問。系統對話框能否再次顯示由平台決定——iOS 拒絕一次即不再顯示；Android 11 起同一權限拒絕兩次後不再顯示。仍可顯示時，只在使用者再次主動觸發該功能、並先經 pre-permission 說明畫面後才再請求；不再顯示時唯一路徑是導至系統設定。兩種情況皆提供功能降級（掃碼改手動輸入）、適當時機用非侵入提示再說明、提供直接跳轉 app 設定頁的按鈕。

### 三段鏈式範例：權限請求時機

情境：一個地圖類 app 有兩個不同權限的功能，一個是核心功能、一個是次要分頁功能，判斷各自該在什麼時機請求。驗證段的判定屬「設計產出物」。

【輸入】

```
SPEC 片段：一個地圖類 app，核心功能是顯示使用者附近的地點（需要定位權限）；另有一個
「附近優惠推播」次要分頁功能，需要通知權限
```

【產出】

```
形態：決策表
條件                       | 判定
兩個功能是否需要同一個權限 | 否（核心功能需要定位；次要分頁功能需要通知，是不同權限）
各自是否與核心功能直接相關 | 定位（核心功能）：是；通知（次要分頁功能）：否
請求的權限數量是否為 1-2 個 | 是（各自皆僅一項）
結論                       | 兩者是不同權限，各自依是否為核心功能判斷首次請求時機：
                             定位權限可在首次開啟請求；通知權限延後到使用者實際開啟
                             「附近優惠推播」該功能時才請求。不會出現「同一權限依功能
                             分次請求」的情況——同一權限的請求時機只判定一次（見上方
                             〈權限 gate〉時機句），此處是不同權限各自判定
```

【驗證】

```
檢驗問句
Q 是否因為地圖類 app 本來就需要定位，就把通知權限也提前到首次開啟請求？
  預期：否——通知權限與定位權限是不同權限、服務不同功能，通知權限是否核心功能相關
  要單獨判斷，不能因為 app 整體性質或另一個權限的判定結果就一併提前

Q 若「附近優惠推播」改用的也是定位權限（例如推播依定位觸發），核心功能已在首次開啟
  取得定位授權後，「附近優惠推播」還需要再請求一次嗎？
  預期：不需要——同一權限只判定一次首次請求時機，取得授權後其他功能沿用該授權狀態，
  不另立請求時機（見〈權限 gate〉時機句）
```

**環境 gate**（OS 版本、特定感測器、藍牙開啟）：fallback 通常有限 — 硬體不存在無法軟體模擬，但至少告知為什麼功能不可用、而非靜默禁用。

**破壞性操作確認 gate**：操作不可逆，或會覆蓋、清空、取代既有資料時（與下方〈檢查清單〉對應項同一範圍，與操作名稱是否誠實無關），執行前攔一道描述具體後果的確認（「將清空現有 N 筆」、不只問「確定嗎」）。使用者直覺與實際後果的落差只決定文案要多具體：落差越大，文案越須點出直覺沒預期到的後果（「匯入」須寫明會覆蓋）。這類 gate 的特化要求是**確認機制本身的故障模式也要設計**：確認元件沒渲染、事件沒綁上、回應逾時，一律視為未確認、不執行破壞 — 安全預設倒向不可逆性低的那邊（不清空可以重試、清空無法還原）。程式碼層檢查訊號：確認邏輯的 else / null 分支走向哪邊。

### 三段鏈式範例：破壞半徑與直覺落差

情境：一個「匯入」功能，名稱給人加法的印象，實際語意卻是取代。驗證段的判定屬「設計產出物」。

【輸入】

```
SPEC 片段：一個「匯入」功能，選擇檔案後會把匯入內容寫入使用者現有的同名項目
```

【產出】

```
形態：決策表
條件           | 判定
操作名稱給人的直覺印象 | 加法（「匯入」聽起來是新增內容進來）
操作實際語意           | 取代（同名項目的既有內容會被匯入內容覆蓋，無法復原）
直覺與實際的落差       | 大（使用者依名稱直覺會預期是安全的新增動作）
結論                   | 需要攔一道確認，且確認文案須描述具體後果（「將覆蓋 N 個同名項目的
                         現有內容，此動作無法復原」），不能只問「確定要匯入嗎」
```

【驗證】

```
檢驗問句
Q 判斷是否需要確認，依據是使用者直覺與實際後果的落差大小嗎？
  預期：否——依〈破壞性操作確認 gate〉，是否確認只看操作是否不可逆或覆蓋既有資料，
  名稱誠實的「刪除」同樣需要確認；落差只決定文案具體度，本例落差大，文案須寫明「覆蓋」

Q 確認文案是否只問「確定嗎」？
  預期：否——須描述具體後果（覆蓋範圍、不可復原），對照本節「不只問確定嗎」的要求
```

另外兩類實務常見：**付費 gate**（失敗路徑的目標是引導付費而非替代功能、取捨依商業模式）、**版本相容 gate**（提示更新，注意強制更新會擋住無法更新的舊裝置使用者）。

## 安全 vs 可用性的顯式記錄

Gate 嚴格度的決策（例如「只接受生物辨識、不允許密碼 fallback」）涉及安全和可用性的取捨，應在功能規格中顯式記錄，讓 code review 和後續維護者理解決策背景：

```text
Gate: biometric authentication
Decision: allow device credential fallback
Security trade-off: device credential (PIN/password) is weaker than biometric
Rationale: self-hosted tool, user = owner, availability > auth strength
Risk accepted: someone with device PIN can access the app
```

沒有記錄的嚴格設定通常不是決策、是預設值或直覺 —「安全性更高」的原始意圖沒有對照過「條件不滿足時使用者完全無法使用」的代價。

## 生物辨識的失敗情境與平台行為

失敗有四類，各自合理的 fallback 不同：

| 失敗類型     | 例子                             | 合理 fallback      |
| ------------ | -------------------------------- | ------------------ |
| 暫時性       | 光線不足、手指潮濕               | 重試可能成功       |
| 持續性       | 戴口罩、手指受傷                 | 需要替代認證       |
| 硬體不可用   | 無生物辨識模組、模擬器、MDM 禁用 | 需要替代認證       |
| 使用者未設定 | 有硬體但沒設 Face ID / 指紋      | 引導設定或替代認證 |

平台 API 行為（以 Flutter `local_auth` 的 `biometricOnly` 參數對應）：

- **iOS（LocalAuthentication）**：`deviceOwnerAuthenticationWithBiometrics` 只接受生物辨識；`deviceOwnerAuthentication` 失敗後系統自動彈出裝置密碼。認證 UI 由系統控制、多次失敗會自動鎖定。
- **Android（BiometricPrompt）**：`BIOMETRIC_STRONG` / `BIOMETRIC_WEAK` / `DEVICE_CREDENTIAL` 三個等級可用 `|` 組合，`BIOMETRIC_STRONG | DEVICE_CREDENTIAL` 即「先生物辨識、失敗 fallback 到裝置密碼」。廠商間辨識品質差異大。

## 開發環境 vs 真機的行為差異

開發環境的 gate 行為比真機寬鬆，讓缺口在開發階段不可見 — 和測試中 mock 掉外部依賴、遮蔽真實協定行為是同一類問題。三個機制：模擬器不支援硬體功能（生物辨識、藍牙、push）導致 app 跳過認證走預設路徑；debug build 的權限行為可能不同；test 環境 mock 掉所有 gate（fake service 永遠成功）。

對策是在規格中建一張差異表，「模擬器行為」和「真機 release」不同的行 = 必須上真機驗證的項目：

| Gate         | 模擬器行為            | 真機 release    | 風險                            |
| ------------ | --------------------- | --------------- | ------------------------------- |
| 生物辨識     | 跳過（硬體不可用）    | 正常            | 模擬器上看不到 fallback 缺失    |
| 網路連線     | 通常正常（host 網路） | 行動網路 + WiFi | 模擬器網路狀態不代表行動網路    |
| 相機權限     | 無相機 / 虛擬相機     | 正常            | 無法測試真實權限流程            |
| 藍牙         | 不支援                | 正常            | 藍牙相關功能完全跳過            |
| Push 通知    | 不支援（iOS 模擬器）  | 正常            | 通知觸發的導航路徑不可測        |
| App 簽名驗證 | debug 簽名自動通過    | release 簽名    | 簽名相關 gate 只在 release 生效 |

用法：開發時對照哪些 gate 尚未被真實驗證；實機測試計畫對每一行設計案例（涵蓋失敗路徑）；code review 看到嚴格 gate 設定時提示「模擬器上看不到這個問題」。

## 檢查清單

- [ ] 每道 gate 都回答了成功 / 失敗 / 不確定必答問題？
- [ ] 失敗路徑至少有一個選項（替代驗證 / 降級 / 重試 / 返回）？
- [ ] 不確定狀態有 loading 指示 + 取消 + timeout 提示？
- [ ] Gate 嚴格度的取捨有顯式記錄（決策、代價、接受的風險）？
- [ ] 權限請求在功能使用時觸發、有 pre-permission 說明？
- [ ] 權限被永久拒絕後有引導到系統設定的路徑？
- [ ] 網路類功能選定了策略（offline-first / retry / degraded）並涵蓋「連線但品質差」？
- [ ] 不可逆、覆蓋 / 清空 / 取代既有資料的操作有後果描述的確認、且確認機制故障時預設不執行？
- [ ] Gate 行為差異表列出了哪些項目必須上真機驗證？
