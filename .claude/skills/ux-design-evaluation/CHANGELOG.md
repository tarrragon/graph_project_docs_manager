# ux-design-evaluation 版本紀錄

新到舊。版號規則與兩個住址（本檔與 `SKILL.md` frontmatter 的 `metadata.version`）見專案的 skill 同步規範。

**Version**: 1.11.5 — 修 `input-mechanism.md`〈IME 安全清單〉一則讀歪點：非 secret confidential 欄位的規則與結果原本分寫兩處——表前段只到「改依內容是否為自然語言決定」未寫出結果，結果句「自然語言時兩者保留開啟」留在表後段，中間夾著只適用 secret 類欄位的設定表，讀者只讀表前段會得不到結果、改拿表格（三列全關）當替代答案，誤把 secret 限定套到非 secret 欄位。改寫為：表前句明寫「下表只適用 secret 類欄位，非 secret 的 confidential 欄位規則見表後段」；平台學習開關限制句移到表後、緊接個人化學習設定；非 secret 規則與結果合併為單一段落，不再分寫兩處。判準未變，僅重排陳述順序與明示表格射程。SKILL.md `metadata.version` 1.11.4 → 1.11.5。

**Version**: 1.11.4 — b2x 第二輪重跑 9 則讀歪點盤點：獨立判定者標記為「讀者失誤」但上一輪 3/3 通過、這輪條文題目皆未變卻失敗的 4 則，用戶裁示定性為條文問題非讀者問題（條文要寫到最弱讀者也讀不歪），全數以重排結構方式收斂，未新增判準；1 則屬題目設計（計數器長度會變化），已記於來源 ticket，本版不改條文。**interaction-feedback-component-semantics.md**：六項判準第 2 項改寫順序，先列兩種合格形態與「與是否同列無關」的排除句，再接理由，避免讀者只讀到理由句、沒讀到接在句尾的豁免。**gate-fallback.md**：失敗欄列粒度規則「列名寫出涵蓋的類型」改為「須逐一列出涵蓋的每個成員類型」，並排除單一總稱概括的寫法（如「永久拒絕」）。**input-mechanism.md**：搜尋觸發模式判斷依據句補「依上表對應『按送出』列」的直接結論句，避免結論被夾在雙重否定子句中；Special keys 排列依據改寫為編號優先序（有使用頻率資料一律依頻率排列，即使該鍵為完成核心操作的必經鍵也不因此提高順位；僅使用頻率無法確定時才退回臨界路徑）；IME 安全清單非 secret confidential 段落（含表格前後各一處）改寫為「兩項控制分屬不同判準、互不影響」，避免「不希望被記住」的理由被泛化套用到自動校正與輸入建議。**interaction-feedback-waiting-and-notification.md**：時間門檻表改寫為顯式「下界含、上界不含」的區間寫法，並補一句說明「+」號代表同時採用的組合策略而非擇一、「進度指示」不要求精確可計算的百分比（與「進度百分比」不同）。**screen-state-matrix-selection-prestate.md**：「未選定」狀態的兩項要求（四欄填寫、退出路徑）由同一句改為編號並列，避免退出路徑要求被讀漏。全部改寫文字已用 `git grep -F` 逐句查證命中原句唯一。SKILL.md `metadata.version` 1.11.3 → 1.11.4。

**Version**: 1.11.3 — 讀者探針閘門第二輪盤點：清除全部「依據只在範例內」標記，並處置 2 則條文風險，共 7 項全數以補句方式收斂於既有條文（無需新判準）。**error-recovery.md**：〈Degraded mode〉「靜默隱藏」句補「從未用過」的判斷單位（是否曾被使用過，非上線時間長短或採用率），排除「用得少即可視為未用過」的誤讀。**interaction-feedback.md**：〈按鈕級回饋〉timeout 兜底句補「沒有固定公式或倍數」，明示緩衝量無可推導依據，只要求晚於後端逾時值。**input-mechanism.md**：Special keys 排列依據句補「沒有使用頻率資料不是不排列的理由，退回臨界路徑判定即可」；搜尋觸發模式表後補一句判斷依據澄清（是「搜尋成本」而非「是否為本地」，本地全文搜尋等高運算量情境不因資料在本地就判為低成本）；〈判斷搜尋成本的依據〉範例結論的「或 debounce」不受表格支持（表格將 debounce 定位為「遠端搜尋的常見選擇」，本情境無 API 呼叫次數考量），改為只採「按送出」並附理由。**interaction-feedback-waiting-and-notification.md**：〈畫面級回饋〉維度表後補一句，澄清「每個中間狀態都必須有」退出路徑是說明畫面級的一般需求，不是要求本節回饋表另填退出路徑欄（權威值仍在 `screen-state-matrix.md`），解消與檔首〈不適用〉範疇宣告的表面衝突。**interaction-feedback-component-semantics.md**：六項判準第 2 項補「兩者擇一即合格、與是否和按鈕同列無關」，排除「同列有按鈕時圓點／文字 chip 一律不合格」的誤讀。全部新增句已用 `git grep -F` 逐句查證命中原句唯一。SKILL.md `metadata.version` 1.11.2 → 1.11.3。

**Version**: 1.11.2 — 讀者探針閘門第一輪盤點（14 則 B 組）階段 2 剩餘 11 則，經獨立評估者 WRAP 定稿後落檔（來源見對應 ticket 的〈階段 2 WRAP 評估〉前半／後半）；另併入用戶裁示的退出路徑欄格式定案（E-09）。**input-mechanism.md**：CLI 四維決策範例情境句移除「示範決策表值只寫中性描述」的失真主張（條文自身以 `visiblePassword` 等框架列舉值書寫，範例另立相反慣例卻無依據），Keyboard 決策欄與驗證題改為描述明文/無自動校正的行為特徵而非「中性詞 vs 框架列舉名」二分；表單範例同一驗證題（決策欄是否寫框架列舉名）整題移除。〈場景一：表單〉驗證時機段補「未列舉規則依判定所需資料分類」三分支規則（本地可算 → 格式驗證；需其他欄位 → 交叉驗證；需查伺服器 → 業務驗證），範例產出表欄名與 Q1 預期同步改寫，檢查清單同步。**interaction-feedback-waiting-and-notification.md**：〈畫面級回饋〉範例 Q2 收斂回本節職責（只驗證 UI 呈現欄的重連按鈕是否照錄 SPEC），退出路徑歸屬改路由至 `screen-state-matrix.md`〈填寫範例〉修復後矩陣，不新增「error 類狀態」外延判準。**interaction-feedback.md**：〈按鈕級回饋〉timeout 兜底句補「讓後端的明確錯誤先回來」的路由句（連到 waiting-and-notification 檔既有的 connecting timeout 句），範例產出結論列與 Q2 預期改為只確認「略大於」無固定公式，不再暗示緩衝量有可推導依據。**screen-state-matrix.md**：〈四欄定義〉退出路徑列補格式規則「{觸發事件} → {目標狀態}」（缺任一段視為未定義，E-09，用戶裁示格式）；〈填寫範例〉步驟 3 移除只在範例內成立的「可用操作與退出路徑同名」規則句與對應驗證題（B-14c，該規則與範例自身的 error/disconnected「重新連線」及 back 欄位歸屬矛盾，改採縮減範例並修正 back 應歸「可用操作」欄非「顯示」欄，同步消解與 waiting-and-notification 範例的顯示元素不一致）；〈容易漏列的狀態：initializing〉判準由「請求／查詢對象是否有獨立生命週期」改寫為「查詢對象開啟當下是否可能尚未能回應」（原判準會誤判常駐就緒的遠端 REST 服務），範例 Q1 預期與檢查清單同步；〈反模式〉「進入條件不可逆」補「反向操作」定義（使進入條件不再成立的操作，明文排除單純導覽返回），範例 Q1 預期同步引用新定義。**error-recovery.md**：〈重試機制〉範例線框與 Then 移除「立即重試」按鈕（該按鈕原只綁定指數退避場景，固定間隔場景無此要求，屬未經證據的範圍擴張），改保留「返回首頁」並路由至〈檢查清單〉「行動選項中有退出路徑」，新增「若改採指數退避則須加倒數與立即重試按鈕」的相反分支句。**navigation-patterns.md**：〈iOS HIG vs Material Design 慣例差異〉適配判準由「系統行為類／視覺風格類」二分（否定分支「僅涉及視覺呈現」對結構類差異如 top tabs 無鑑別力）改寫為單一問句「是否改變使用者操作平台系統機制得到的結果」，否定分支為餘集，涵蓋未列舉元件；範例 Q1 預期同步。**gate-fallback.md**：〈破壞性操作確認 gate〉首句由「破壞半徑大於直覺預期」改為「不可逆或覆蓋既有資料」（與下方〈檢查清單〉對應項對齊、與操作名稱是否誠實無關），直覺落差改為只決定文案具體度；檢查清單對應項補「不可逆」；範例 Q1 預期改為確認判準不依落差大小。全部新增條文句已用 `git grep -F` 逐句查證可被對應範例引用。SKILL.md `metadata.version` 1.11.1 → 1.11.2。

**Version**: 1.11.1 — 讀者探針閘門第一輪盤點（14 則 B 組「條文查無／部分依據」）階段 1 修補：3 則已有可溯條文、僅缺路由或方向句，補句並附出處（不改變任何既有判定）。**input-mechanism.md**：CLI 四維決策範例的情境句補「Submit 整行送出」理由出自〈四個決策維度〉Submit model 小節的顯式出處；IME 安全清單非 secret confidential 欄位句補自然語言／非自然語言的明確方向（自然語言保留開啟、非自然語言依 IME policy「會被破壞」關閉），取代原本只寫「見下方範例」的隱式路由。**navigation-patterns.md**：go/push/pushReplacement 決策方法句補一句——三種堆疊操作皆定義在單一堆疊內，tab 間切換因每個 tab 維護獨立堆疊而不屬其中任何一種，出處指回〈導航模式分類〉。其餘 11 則（B-03b／B-04b／B-05／B-12b／B-13／B-14c／B-15／B-16／B-19b／B-24／B-29）需新增判準或會改變既有判定，屬階段 2，草稿與影響範圍記入來源 ticket 的 Problem Analysis，本版不落檔。SKILL.md `metadata.version` 1.11.0 → 1.11.1。

**Version**: 1.11.0 — 落檔範例藏判準稽核（DOC-3 分票）剩餘 12 則缺條文，經獨立評估者 WRAP 定稿（8 則修改後採用、2 則字面微調後採用、2 則照採）。**gate-fallback.md**：〈每道 gate 的必答問題〉補失敗欄列粒度規則（fallback 相異各一列、相同合併一列並寫出涵蓋類型），gate 設計表新增「生物辨識-持續性／硬體不可用」合併列；〈Gate 類型與各自的設計重點〉權限 gate「拒絕後」句改為依平台區分（iOS 拒絕一次即不再顯示、Android 11 起拒絕兩次後不再顯示），gate 設計表相機權限欄改依「系統對話框是否仍可顯示」分列；「時機」句補「同一權限只判定一次首次請求時機」，範例換題材為兩個不同權限（定位／通知）；網路 gate 補三策略判別順序與 Degraded mode「標示內容可能非最新」，範例與【驗證】Q2 同步；Retry with feedback 句的「間隔遞增」改「間隔依失敗成因選」，與 `error-recovery.md` 對齊。**interaction-feedback-component-semantics.md**：六項判準第 1 項「三值以上無連動增減」補「先看能否一列內完整並排不截斷」判準（能→segmented control，不能才用動詞句），範例同步；第 5 項補「先縮格式、再最小寬度」順序，讓步規則路由至 `component-contract-design`〈空間不足策略〉欄。**input-mechanism.md**：〈IME 安全清單〉補 confidential 判準（使用者不希望被記住）與 secret／非 secret 分流（secret 四項全關、非 secret 僅個人化學習強制關，自動校正與輸入建議依是否自然語言決定，平台無法單獨關閉學習時須顯式記錄），範例結論改為日記欄保留自動校正與輸入建議，檢查清單新增一項。**error-recovery.md**：〈行動與錯誤層級對位〉補層級集合定義（至少含資料層、網路層、執行環境層）；〈重試機制〉間隔策略補依失敗成因選擇的規則（瞬斷用固定間隔、伺服器過載用指數退避），範例 Then 末句同步收窄。**interaction-feedback-waiting-and-notification.md**：〈時間門檻與回饋策略〉補「須顯式記錄選用的統計量」句。**navigation-patterns.md**：〈Deep link〉堆疊重建補「日常使用的一部分」操作定義（app 內是否有既有入口可重複到達），範例移除「通常」類假設改用該定義。**SKILL.md**：〈與相鄰資產的交界〉範例改為填入回饋契約表「反應」與「時間門檻」兩欄（原誤填「回饋通道」欄），並標明選用統計量，註明四段涵蓋另有來源。全部變更逐則依 `chained-examples.md`〈驗證段機械檢查：依據句可溯性〉自檢通過。衍生發現（第 1 項二值/三值連動分支處理邏輯不一致）另立票處理，本版不含。SKILL.md `metadata.version` 1.10.1 → 1.11.0。

**Version**: 1.10.1 — 修補範例藏判準稽核（DOC-3 分票）發現的 2 則範例與條文字面相反問題，條文本身不動。**navigation-patterns.md** C2〈tab 結構下的決策交界〉：【產出】決策表「使用者按 back 期望回到哪裡」與「是否為切換工作區」兩列原答「回到首頁 tab」，與〈導航模式分類〉既有條文「在 tab 內按 back 回上一層、而非切到上一個 tab」相反，改為與該條文一致；【驗證】段原已正確，未改動。**interaction-feedback-component-semantics.md** C6〈第 2 項——非互動指示與按鈕形態的判斷〉：【產出】結論原判「不合格」，與條文第 2 項明列「文字 chip、圓點」為明顯非按鈕形態相反，改判「合格」並改寫【驗證】兩題引用該條文句。其餘 12 項發現（C3-C5、C8、W27-W31、W34-W36）屬缺條文，草稿已交 PM 簽核，本版不含。SKILL.md `metadata.version` 1.10.0 → 1.10.1。

**Version**: 1.10.0 — 補齊範例後，`interaction-feedback.md` 與 `screen-state-matrix.md` 超出體量門檻（來源見對應 ticket），依讀者任務重切（不刪範例、不壓縮規則文字），量測指令為 `skill-design-guide` SKILL.md〈Progressive Disclosure〉的分段估算式。**interaction-feedback 拆為四檔**：入口 `interaction-feedback.md`（核心原則／三層回饋模型／按鈕級回饋，2433 tokens）、`interaction-feedback-waiting-and-notification.md`（時間門檻與回饋策略／畫面級回饋／Spinner vs Skeleton／結果通知的形式選擇，3764 tokens）、`interaction-feedback-component-semantics.md`（元件語意與版面檢查六項判準，2870 tokens）、`interaction-feedback-checklist.md`（反模式速查／檢查清單／參考來源，三檔共用不拆列不複製，1887 tokens）。三層回饋模型第 2 層與時間門檻表已互相具名指路。**screen-state-matrix 拆為兩檔**：入口 `screen-state-matrix.md`（其餘 12 節不動，4877 tokens）、`screen-state-matrix-selection-prestate.md`（選取類前置呈現：獨立成列與判定條件，2014 tokens）。SKILL.md〈觸發路由〉互動回饋列拆為兩列（按鈕流程／時間門檻通道症狀對應入口＋waiting 檔；元件語意版面症狀對應 semantics 檔）；Directory Index、〈與相鄰資產的交界〉表、SKILL.md 自身三段鏈式範例的跨檔具名引用同步更新。全部新檔與兩份入口檔已用同一量測指令複驗通過 5000 tokens 門檻。SKILL.md `metadata.version` 1.9.0 → 1.10.0。

**Version**: 1.9.0 — 依 `chained-examples.md` 規格補齊審查發現的缺範例位置（來源見對應 ticket），並改寫審查判定需改寫的既有範例為三段鏈式格式（【輸入】【產出】【驗證】）。**SKILL.md** 新增〈與相鄰資產的交界〉表下的三段鏈式範例（回饋時間門檻須為具體值）。**screen-state-matrix.md**：〈填寫範例〉改寫（步驟 1 改示範從視覺呈現反推狀態、修正 connecting 列「取消連線」在可用操作欄與退出路徑欄用詞不一致的問題），新增〈容易漏列的狀態：initializing〉判定「獨立生命週期」範例、〈反模式〉判定「反向操作」範例。**gate-fallback.md**：〈每道 gate 的必答問題〉gate 設計表改寫為生物辨識/相機權限失敗欄粒度判定，新增網路 gate 策略選擇、權限請求時機、破壞性操作確認三則範例。**input-mechanism.md**：Special keys 段新增排列依據範例；CLI 決策表與表單 Keyboard type 欄位表改寫（移除框架列舉值，改中性描述，補「帳號」規則交界情境）；新增驗證時機、搜尋觸發模式、IME confidential 欄位三則範例。**error-recovery.md**：〈行動與錯誤層級對位〉新增判斷錯誤層級範例；〈重試機制〉改寫為策略選擇 + 重試狀態 UI 合一範例；〈Degraded mode〉新增判斷「從未用過的功能」範例、防抖動 N 值下界範例。**navigation-patterns.md**：go/push/pushReplacement 方法表改寫為語意優先、框架名降為註記欄；常見誤用改寫為決策表 + Given/When/Then；新增 tab 結構決策交界、iOS/Material top tabs 差異類型判斷、deep link 堆疊重建三則範例；檢查清單同步改用語意名稱。**interaction-feedback.md**：〈畫面級回饋〉連線流程狀態表改寫為三段鏈式格式；元件語意與版面檢查新增第 1 項邊界情境範例、第 2 項與第 5 項範例；新增 p90/p95 選用、非同步按鈕 timeout 取值兩則範例。全部範例均去識別化，不含本專案識別符。SKILL.md `metadata.version` 1.8.0 → 1.9.0。

**Version**: 1.8.0 — `screen-state-matrix.md` 補兩則缺引導（來源：WRAP 分析，UC 步驟未對應矩陣、選取類前置呈現判法不一致）。**新增〈UC 步驟對應檢查〉**（置於〈從 BDD 操作盤點展開〉後）：矩陣填完後逐一核對 UC 主要成功場景每一步驟是否對應到矩陣的某狀態或某可用操作，並明訂無 UC 文件時改以 BDD 操作盤點作替代來源、兩者皆缺時須註記「本檢查未執行」不可留白視為通過。**新增〈選取類前置呈現：獨立成列與判定條件〉**（置於〈容易漏列的狀態：initializing〉後）：三項判定條件（未決定因非等待外部事件／選擇動作由使用者觸發／選定前後呈現或操作有實質差異）皆成立即須獨立成列並補齊四欄；補〈跨畫面共用的選取值〉子節，要求記錄設定入口與清除時機；附一則去識別化三段鏈式範例（清單頁／詳細頁共用選取值，驗證段採檢驗問句形態）。檢查清單同步新增三項對應勾選。 — 補齊六則缺引導（G28–G33，來源見對應 ticket）。**G28 screen-state-matrix**：〈路由可達性檢查〉後新增〈無 UI 入口路由表〉，路由／原因／觸發來源三欄固定格式，取代原檢查清單「或明確標記」這句沒有載體的話。**G29 input-mechanism**：〈四個決策維度〉Special keys 段補排列依據（依使用頻率、拇指近處優先，無法確定時以臨界操作路徑排序）與長按語意（只用於重複觸發，不承載第二功能）；套用〈何處需要範例〉判準後，排列依據的頻率排序在使用頻率無客觀資料時仍可能分歧，轉為缺範例，移交後續缺範例修補票。**G30 error-recovery**：〈Degraded mode〉防抖動的 N 值補下界規則（不小於該功能單次請求逾時值）；套用判準後，同一下界之上仍可能選出不同 N，轉為缺範例，移交後續缺範例修補票。**G31 interaction-feedback 結果通知**：〈補充判斷標準〉補高頻高代價象限（以 SnackBar + 復原為主，復原不可行才升級 Dialog）；套用判準後與既有低頻高代價／高頻低代價兩象限同構，未命中，不轉缺範例。**G32 interaction-feedback 按鈕級回饋**：元件語意與版面檢查第 1 項的三種消歧做法補選擇順序（先看值數，二值一律 switch；三值以上依是否連動增減元素分流；動詞句僅作權宜過渡）；套用判準後，三值以上「是否連動增減元素」邊界情境仍可能分歧，轉為缺範例，移交後續缺範例修補票。**G33 interaction-feedback 空間競爭**：第 6 項補空間競爭規格的載體路由（不另立格式，一律路由至 `component-contract-design`〈空間不足策略〉欄）；套用判準後為封閉路由指示，未命中，不轉缺範例。六則檢查清單同步補對應項。

**Version**: 1.6.0 — 修補審查發現的四則原文矛盾（用戶簽核後定案，簽核結果與提案有三處不同，詳見對應 ticket）。**C01 退出路徑**：〈四欄定義〉保留「完成後自動轉場」為退出路徑內容之一，但補註它不滿足〈檢查清單〉「至少一條使用者可自行觸發的退出路徑」要求、需視情況另補使用者可觸發路徑（不刪除該詞，因〈退出路徑的例外邊界〉仍討論自動轉場，刪除會使該詞失去定義出處）。**C02 回饋時間門檻**：〈三層回饋模型〉第 2 層「等待指示」實作欄改為依〈時間門檻與回饋策略〉分帶決定（100-400ms 動畫過渡、400ms 以上才用 spinner），與同檔延遲顯示機制一致，不再暗示 100ms 起即掛 spinner。**C03 輕微錯誤行動數**：〈三層結構〉改為依「就地提示（欄位驗證、toast 類）」與「佔據整個畫面的錯誤狀態」分流——前者可省略說明層、只需一個行動，後者三層都要且行動合計至少兩個，解消與〈Error loop 的逃生口〉「每個 error 畫面至少兩個選項」的字面衝突（兩處原用詞粒度不同，非誰對誰錯）。**C12 跨檔範例欄位值不一致**：〈畫面級回饋〉的連線流程狀態表移除可用操作／退出路徑欄，改為只列 UI 回饋形式並指向 `screen-state-matrix.md`〈填寫範例〉修復後矩陣（E2）作為可用操作與退出路徑的唯一權威值，避免兩處各自演進後再度分歧。

**Version**: 1.5.0 — 依一輪低階 model 讀者探針（3 個實例，指令逐字相同）處置三項。**description 由 989 字元收斂至 242**（門檻 250）：原值為門檻近四倍，且已被凍結於 `.claude/hooks/skill-description-baseline.json`，本次修正後同步移除該 baseline 條目——凍結值留著等於替回退預先簽名。收斂捨棄的觸發詞見下段。**排除項補轉介地址**：原文「不涵蓋視覺風格設計與使用者研究方法」為明示排除但無去向，兩名探針在無提示下各自指出不知道該找誰；改為視覺風格走 `foundation-design` 的 UI 維度（執法層在 Dart 專案為 `dart-style-guardian`）、使用者研究本框架無承接者需自行取用外部方法，並明寫 WCAG 對比是例外仍屬本 skill。**新增〈與相鄰資產的交界〉表**：探針 Q5 三份皆只找到 `component-contract-design` 一個。四列各自寫出「交界處讀者會踩到什麼」而非只寫職責分界——例如本 skill 判定的時間門檻是元件契約〈回饋契約〉欄的輸入，所以產出必須是可被引用的具體值。**description 捨棄的觸發詞**：死胡同以外的同義詞、`initializing`、`hash 路由`、`tab bar`、`service worker`、`Bottom Sheet`、`debounce`、`防連點`、`佔位`、`placeholder`、`ellipsis`、`省略號`、`截斷`、`完成宣告`、`可點性`。捨棄判準為「保留的上位詞已能命中同一情境」，其中`完成宣告`與`防連點`為唯一兩個無上位詞可代者，兩者仍留在正文〈跨維度快速自檢〉可被全文檢索命中，但**skill 選用階段的匹配力確有下降**，此為 250 字元預算下的取捨，不宣稱無損。

**Version**: 1.4.5 — 讓號並補記本地兩項變更：其一，「十一欄位齊全」改「元件契約欄位表齊全」，並於 `references/interaction-feedback.md` 六種按鈕狀態表下補與元件契約〈回饋契約〉欄的分工句——時間門檻與通知形式仍由本 skill 產出，每形態的回饋通道與狀態來源由 `component-contract-design` skill 承接；其二，評估流程補一句反向路由：畫面狀態矩陣填完、進入元件級實作前，元件契約由該 skill 承接。動因是本 skill 對該 skill 只有入度無出度，使用者做完畫面級狀態設計後沒有訊號接上元件層契約。兩項原標 1.4.4 與 1.4.3，其中後者與上游同日獨立標記的 1.4.3 撞號；兩側內容皆在本 skill，本地兩項合併讓號至本版。

**Version**: 1.4.3 — 清理 2 處 language-constraints.md 規則 2 禁用詞基線債務（`代碼` → 程式碼，`references/input-mechanism.md`），純用語修正，內容與判準未改。

**Version**: 1.4.2 — 版本紀錄搬到同目錄的 `CHANGELOG.md`。skill 是 runtime 整份載入的檔案，而沒有任何規則要求任何人讀版本紀錄，留在 SKILL.md 等於每次叫用都付一次無效讀取。SKILL.md 末尾留一行指過去，版號的兩個住址改成「CHANGELOG.md 最上面那一條 + frontmatter 的 metadata.version」。skill 的指令內容一個字都沒改。

**Version**: 1.4.1 — 術語校正：判準全數改為判斷標準（動作修飾語縮為「X 標準」、狀態義改為「X 條件」）。判準的語域在哲學與教育評量、工程讀者解析不了——五份低階模型探針一致回報非通用

**Version**: 1.4.0 — 三輪 agent 審查（compliance / cadence 冷讀對齊 / self-application steelman outbound）修正：宣告層與內容層對齊（description / Triggers / 路由表症狀欄補 v1.2-1.3 新增檢查的入口、「不涵蓋」聲明修正為視覺風格 — WCAG 對比屬檢查範圍、元件語意段主標去半套）；steelman 修正（WCAG AA 分字級 4.5:1 / 3:1、觸控底線標派系 44pt HIG / 48dp Material、選中態補主題成對機制條件、溢出手段補捲軸指示、toggle 消歧補動詞標籤選項、完成證據補 API cursor + 全量場景邊界、debounce 慣例值去門檻化、跨平台適配補系統行為 / 視覺風格判斷標準、navigation 補參考來源）；快速自檢 3-in-1 拆分、佔位掃描補操作提示
**Version**: 1.3.0 — 元件語意與版面檢查加第六項「sizing 套件不驗證空間分配」（換算工具在常數層、空間分配在 layout 協商層，兩層獨立；版面擠壓先分換算錯 vs 分配錯、引入套件時記錄它不保證的層）；檢查標題去計數化（五個檢查 → 元件語意與版面檢查）；檢查清單同步
**Version**: 1.2.0 — 從一次 mobile app 驗收的六個實際發現補「元件語意與版面」檢查層（互動回饋 reference 加五項：切換元件標籤的現態 / 動作歧義、非互動指示與動作按鈕同形、選中態底色文字色成對設計、水平溢出捲動 affordance、關鍵回饋文字版面保障；反模式表加四行含佔位 handler 掃描）；快速自檢同步擴充
**Version**: 1.1.0 — 從一個 Chrome extension 專案的實際事故補 web / 多 context 維度（原案例庫全為 mobile app、系統性缺這一面）：狀態矩陣加 initializing 狀態（查詢對象獨立生命週期）、互動回饋加結果通知鏈路前提與完成宣告窮盡證據、gate 加破壞性操作確認與 fail-safe 預設、錯誤恢復加行動層級對位、導航加 hash SPA 路由辨識；跨維度快速自檢同步擴充
**Version**: 1.0.0 — 從單一模組的簡略版（ux-interaction-feedback：按鈕級 + 畫面級回饋）擴充為全維度 UX 設計評估 skill：新增畫面狀態矩陣、gate fallback、輸入機制、錯誤恢復、導航模式五份 reference，互動回饋 reference 併入通知模式選擇與延遲分布判讀；建立四支柱與事前 / 事後兩條評估流程
