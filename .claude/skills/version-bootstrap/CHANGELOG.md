# version-bootstrap 版本紀錄

新到舊。版號規則與兩個住址（本檔與 `SKILL.md` frontmatter 的 `metadata.version`）見專案的 skill 同步規範。

**Version**: 1.7.1 — 純搬移解消 1.7.0 新增判準表後的體量超標（實測 5066 tokens，超出 skill-design-guide L2 5000 門檻）。依 `splitting-an-existing-skill.md`〈外移什麼、留什麼〉四問處置，不刪任何判斷條件文字：(1) Step 1 跨提案依賴檢查段落的 `depends_on` 欄位權威來源說明（doc skill 採用與否兩分支）與「動機案例」實證敘事，逐字移入新建 `references/step-rationale.md`〈Step 1：跨提案依賴檢查腳本〉一節，原處留一句指路，保留腳本指令與 `[WARNING]` 二擇一處理（選路所需判斷條件，Q1/Q2 命中，留正文）；(2)「反應式工作（不納入 bootstrap）」整節（三類處理方式對照表）逐字移入新建 `references/reactive-work.md`，`SKILL.md`〈按需讀取〉新增一列路由，原處留一句指路（該節不屬九步 pipeline 任一步，僅少數插入式情境的讀者會用到，Q3 命中，外移）。Step 2 判準表（呈現通道三分表、三項前置檢查表）與九步主幹表、各步 Checkpoint 句全數原樣保留。新增檔案觸發兄弟檔互指矩陣更新：`step-rationale.md`、`adjacent-assets-boundary.md` 的「同目錄」列補上 `reactive-work.md`。結果 4907 tokens。
**Version**: 1.7.0 — Step 2「UI 類判別」改採呈現通道三分（獨立評估者 WRAP 完整模式評估，PM 裁決採方案 E3），不再以字面關鍵字（「畫面」「頁面」「元件」「介面」「UI」等）判定：圖形介面（本應用程式渲染的視窗/頁面/彈窗/疊層/應用內提示，含唯讀畫面）判 UI 類、過三項前置檢查；非圖形的人讀呈現（終端文字輸出與互動、匯出文件版面、作業系統或宿主程式渲染的通知）判非 UI 類但須記錄呈現通道並交 `foundation-design` UI 維度判改寫產物；無人直接觀看（API 回應、機器讀取格式）判非 UI 類、略過。起因：`examples/v02-walkthrough.md` 範例一原用「版面配置／可操作元件」的語意延伸判準把「確認彈窗」FR 判為 UI 類，此判準只存在於範例、Step 2 條文查無依據句（2026-09-15 一輪跨單元正確性比對判定屬缺引導）。`references/step-rationale.md`〈Step 2 的 UI 前置檢查〉Action 段的逐字複寫關鍵字判準改指 SKILL.md 判準表，Why 段補「三項前置檢查的產物本身是圖形介面組成，故以通道而非字面判定」。範例一三段依新判準改寫，驗證段三問逐字可指回條文（自檢表詳見對應工作紀錄）。**體量**：本次修改使全檔由 4842 tokens 增至 5066 tokens，暫時超出 skill-design-guide 第 2 層 5000 tokens 門檻；PM 裁決內容先落檔完成，體量問題另立拆分票依 `splitting-an-existing-skill.md` 純搬移處置（不刪文）。
**Version**: 1.6.6 — 補 `references/version-shift-sop.md` 盤點步驟 1「其他跨版本共用契約檔案」的缺引導（跨 skill 審查 B9，WRAP 裁決採「鎖定點三問」判準）。新增〈契約檔案的界定〉節：定義句（形狀存在鎖定點的檔案）+ 三問判準（持久資料／邊界外消費方／明訂凍結，任一為是即屬契約檔案）+ 排除句（三問皆否屬純程式邏輯）+ 非封閉示例；檔案類型不決定歸屬，一律依三問判定。步驟 1 動作欄與步驟 3 判斷準則表同步改指本節，並泛化寫死的 `schema/*.schema.json`、`docs/spec/**/*.md`、`schema/event.schema.json`（其他專案殘留，本專案無 `schema/` 目錄）；判斷準則表原四列合併為三列，表頭前補硬耦合兩條件句。附兩則三段鏈式範例：正例（判準 1 持久資料命中）與前提相反對照例（同一儲存庫、各自部署的兩服務，判準 2 邊界外消費方命中，驗證段逐句指回判準 2 問句「判定單位是發布或部署單位，不是儲存庫」）。SKILL.md〈按需讀取〉列同步：四類改三類、補列契約檔案界定三問。

**Version**: 1.6.5 — 純搬移解消 1.6.4 新增路由後的體量超標（實測 5212 tokens，超出 skill-design-guide L2 5000 門檻）。〈與相鄰資產的交界〉節逐字外移至新建 `references/adjacent-assets-boundary.md`（該節原文即自述「執行時不需要先讀本節，不確定某件事該由本 skill 還是相鄰資產處理時再查」，屬拆分前已存在的讀者情境，非本次新造）；SKILL.md〈按需讀取〉新增一列路由，其餘判準／步驟／欄位文字未改。結果 4835 tokens。
**Version**: 1.6.4 — `examples/v02-walkthrough.md` 依現行九步全篇重寫（跨 skill 審查判定為需依新規格改寫）。舊版標稱「6 步 pipeline」，缺 Step 2.5／2.6／4.5，含 CLI 輸出與語言相依內容（如特定程式語言的進入點檔名），全篇無驗證段——改寫後依現行九步逐步走過，並在六個判準允許分歧的決策點（Step 2 兩處：UI 類提案判別的關鍵字延伸判斷、三項前置檢查「存在」的定義；Step 4.5：地基波依賴編排；Step 5：兩軸測試交集去重；Step 6 兩處：GREEN 票粒度、並行安全的共用檔案）各附一則三段鏈式範例（【輸入】【產出】【驗證】），範例情境技術中立且去識別化。SKILL.md〈按需讀取〉新增一列路由至該檔。**範圍外發現**：`references/version-shift-sop.md` 盤點步驟1（契約檔案未定義處）亦屬缺範例但不在本次修改範圍，已另立追蹤票承接；本次新增路由後 SKILL.md 全檔實測超出 skill-design-guide L2 5000 tokens 門檻，已另立追蹤票依 splitting-an-existing-skill.md 處置。
**Version**: 1.6.3 — 補上一版留下的缺引導（獨立評估者 WRAP 裁決，修改後採用）。**version-shift-sop.md 步驟 2「凍結時序確認」未言明本版無既定凍結點時步驟 3-4 是否仍執行**：裁決主張「無論有無明訂凍結點，步驟 3-4 一律執行」——鎖定點不因未明訂而消失，尚未建立的實作票同樣會消費該契約。步驟改名「契約鎖定點確認」，兩類都查：(a) 明訂凍結點；(b) 本版已存在會讀寫該契約的票。**期限判準改依框架既有的排序手段（Wave / blockedBy），不採時間式期限**：草稿原提「以最早消費票所屬 Wave、否則本版最後一個 Wave」為替代期限，WRAP 現實檢驗後否決——前者在移版當下多半無票可查，後者把定形推到最晚與「提前定形」的動機相反。改為步驟 4 定形票排入本版最早可排 Wave，步驟 2 找到的既存消費票 `blockedBy` 定形票，有明訂凍結點者定形票須在該點之前完成。條文「PG」改「儲存層」（技術名非專案識別符，仍應中性化）。SKILL.md〈按需讀取〉表同步改步驟名。
**Version**: 1.6.2 — 補三處缺引導（跨 skill 審查發現，Checkpoint 時序與前提缺席類）。**Step 2.6 Checkpoint 要求「契約條目與測試對應無 TODO 佔位」，但測試要到 Step 5 才設計，條文本身時序不可滿足**：改為引用 `tracking_schema.py` 的 `data_contract_entry_optional`——`tests` 欄位本屬選填，Step 2.6 合法留空；測試對應是否補齊的判準移至 Step 5 Checkpoint（新增一句收斂條件）。**Step 3 前置「確認 CLAUDE.md 教學模組對應表」未言明專案無此表時的處置**：改為判別式——有表依原文執行，無表則引用 `spec` skill 既有的維度 4 降級條款（「維度 4 skipped：無教學模組對應表」），視為合法通過；教學缺口的追蹤位置補齊為 `docs/sync-pending.md`（`teaching-sync` skill 的 SOT），不再留空泛的「標記 sync-pending」卻不指位置。**version-shift-sop.md 步驟 5 寫「CLAUDE.md 強制操作 2」（專案層識別符滲入框架檔，且未定義該識別符）**：改用與 Step 3 相同的「教學模組對應表」判準與無表跳過路徑，消解未定義引用與前提缺席兩個問題。**同批發現但未落地**：步驟 2「凍結時序確認」在本版無既定凍結點時，步驟 3-4 是否仍執行、期限如何訂，屬本票範圍外的新政策判斷（無既存權威可引用），已留供獨立評估。
**Version**: 1.6.1 — 修補三處原文矛盾與計數過期（跨 skill 審查發現）。**Step 4.5 表與 Checkpoint 塊數不一致**：表列 5 列（含 3.5 元件契約）而動作句稱「編排四塊」、Checkpoint 只驗四塊，`component-contract-design` skill 自身的交界描述（「本 skill 落在第三塊之後、第四塊之前」）確認元件契約不計入四塊——改為表格標明 3.5 為「銜接，不計入四塊」、相鄰資產交界表同步改措辭，動作句與 Checkpoint 明寫元件契約以該 skill〈契約齊全的定義〉為完成判準。**step-rationale.md〈Step 2 的 UI 前置檢查〉句尾冒號後無列表、且寫「兩項」而 SKILL.md 現為三項**：改為指回 SKILL.md 表格、不重複列舉計數（規則 10）。**version-shift-sop.md 檔頭寫「SKILL.md 六步 pipeline」而 SKILL.md 現為九步**：改引用〈全程〉步驟表，不寫死步數。
**Version**: 1.6.0 — 依一輪低階 model 讀者探針（3 個實例，指令逐字相同）處置四項，並解消體量超標。**「全自動」一詞在同一份文件內兩義，改詞消解**：行 32 寫「不是全自動 pipeline」（指整條不會自己跑完），六行後 Step 1 標題寫「（全自動）」（指這一步不需人工填內容），三名探針中兩名主動提問這兩個標籤是什麼意思。各步標籤改為「無需人工填內容」／「需人工填內容」，與 pipeline 層的敘述字面不再共用同一個詞；〈使用方式〉補一段把兩件事分開講明，並寫死「沒有任何一步會自動進入下一步」。此處刻意不採「加一句註解說明兩者不同」——歧義出在詞的成分層，註解出在段落層，`DOC-GPD-009` 記的正是這種修法無效。**新增〈全程〉表**：九步一覽含產出與適用範圍，並明寫帶小數的編號是後來插入既有序列之間、不是選配。原文九步散在 200 餘行且無總覽，探針對「Step 4.5 含 3.5 元件契約」一份漏答。**移版硬耦合盤點 SOP 外移**至 `references/version-shift-sop.md`：該 SOP 觸發條件與六步 pipeline 完全不同（決定移版時才走），原本卻與〈反應式工作〉並列於 Step 6 之後，三名探針中兩名未察覺它存在、第三名明問何時該執行它。Step 1 的依賴檢查處補就地路由，並補一句「選移版時不得整包搬走」。**新增〈按需讀取〉與〈與相鄰資產的交界〉表**：探針 Q5 三份皆只列被本 skill 呼叫的工具（`doc`／`spec validate`／`tdd`／`ticket`），無一提及 `foundation-design`／`ux-design-evaluation`／`dart-style-guardian`／`version-sequencing`。交界表特別寫明 `foundation-design` 會把 DevOps 與可觀測性的盤點票交過來，而本 skill 的九步對這兩個維度沒有承接段落——收到這類票時不該以為它們會在某一步被吸收。**體量**：原 5132 tokens 超過 L2 的 5000 上限且零 references。處置**不是刪判斷條件**：Step 2／2.5／2.6／4.5 的 Why 與 Consequence 論證外移至新建 `references/step-rationale.md`（跳過該步的後果），各步保留時機句與全部操作內容；Step 2 的 UI 判別關鍵字（「畫面」「頁面」「元件」「介面」「UI」）屬判斷條件，原樣留在正文。〈與早期手動流程的對照〉為純歷史、無操作用途，移入本 CHANGELOG 摺疊區。結果 4651 tokens，references 由 0 增為 2。**description 改寫**：原文寫「6 步 pipeline」而實際為九步，改為列出各步名稱不寫死數量（規則 10 可變計數不實例化），並補 `Do NOT use for` 路由至 `version-sequencing` 與 `foundation-design`；`metadata` 補 `category`。**標題**「6 步流程」改「各步驟細節」，同因。

<details>
<summary>與早期手動流程的對照（本 skill 出現前的做法，1.6.0 由 SKILL.md 移入）</summary>

| 早期手動流程 | /version-bootstrap |
|----------------|-------------------|
| 手動 cp 模板建 spec | Step 2 `/doc batch-init` |
| 手動讀 blog 比對 | Step 3 `/spec validate --dim 4` |
| 手動 cp 模板建 UC | Step 2 `/doc batch-init`（同步建立） |
| 手動編輯 traceability | Step 2 自動佔位 + Step 4 填寫 |
| 逐一派 sage | Step 5 批量並行派發 |
| 手動建票 | Step 6 依產出匯總 |

</details>

**Version**: 1.5.1 — 兩處「十一欄位契約」改「元件契約欄位表」，跟隨方法論 1.14.0 欄位表計數去數字化（DOC-GPD-003）
**Version**: 1.5.0 — Step 2 的 L3 元件庫章節檢查項對齊方法論 1.9.0（明示本步驟得先只到清單，契約於地基波補齊，並指名 doc 範本）；Step 4.5 四塊表插入 3.5「元件契約」（程序見 `component-contract-design` skill，checkpoint 為契約齊全），PM 工作補元件契約 DOC 票與依賴。動因：多輪審查發現本 skill 對「第四塊之前的契約產出」無承接段落
**Version**: 1.4.0 — 新增 Step 2.6「資料契約產出」於 Step 2.5 與 Step 3 間：依兩旗標判準（引用 `data-layer-contract-methodology.md` 第 2 節，不複寫）決定是否 cp 模板產出資料契約文件；契約條目登錄 traceability 第三軸 `data_contract_tests` 供 Step 5 測試設計盤點缺口
**Version**: 1.3.0 — 新增 Step 2.5「Domain 規劃」於 Step 2 與 Step 3 間：spec FR 填完後、測試設計前產出/更新 domain map（doc domain-map-template），含 saas / standalone 調和語意（domain 規劃是所有規劃波通用步驟，非 saas 專屬）；Step 5 補「消費 domain map 逐 bundle 定測試層」、Step 6 建票來源補「domain map bundle 分層 → domain/data/presentation 切分」（落地 ANA domain 規劃整合結論）
**Version**: 1.2.0 — 新增 Step 4.5「地基波（僅含 UI 提案版本）」於 Step 4 與 Step 5 間：測試設計前依 component-library 方法論〈地基波 build 順序〉編排 i18n / design-system / UX 審查 / 元件庫四塊實作（Why：測試需驗 zh/en overflow 與元件反應，依賴 i18n/元件先存在；實證地基波經指正後手動插入）；Step 2 UI 前置檢查補「design-system spec（用 design-system-spec-template）」檢查項。非 UI 版本略過
**Version**: 1.1.0 — Step 2 新增「UI 類提案元件庫前置檢查」小節：判別提案是否涉及 UI/頁面/元件，涉及則須先確認 design token 層與 L3 元件庫章節存在（缺則先補齊），才可繼續 UI 實作票規劃，落地元件庫雙向約束方法論流程整合點 1
**Last Updated**: 2026-09-15（1.7.1）
