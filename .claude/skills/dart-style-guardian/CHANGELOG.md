# dart-style-guardian 版本紀錄

新到舊。版號規則與兩個住址（本檔與 `SKILL.md` frontmatter 的 `metadata.version`）見專案的 skill 同步規範。

**Version**: 1.9.0 — 獨立複核發現〈Theme 與 Token 判準〉判準句本身未給方向，讀者須靠自備框架知識判斷「內建語意插槽是否覆蓋」，屬條文完整性缺口（既有【產出】段已隱含此判定，但條文未寫出）。`references/color-system.md` 判準句所在段落補方向句「對應的內建語意插槽存在時用主題插槽，不存在時用 token」與程序句「無法確定某語意是否有內建插槽時，查閱當前框架版本的 ColorScheme／TextTheme 官方欄位清單再判定，不憑記憶」。裁決不採覆蓋對照表——ColorScheme／TextTheme 欄位會隨框架版本增刪，寫死清單會悄悄過期（同 reference-stability 規則 10 可變計數不實例化的道理）；程序指引改指權威來源，不受版本影響。兩句皆為既有判定的條文化，不改變任何判定結果。
**Version**: 1.8.0 — 正確性比對探針發現兩則三段鏈式範例的驗證段違反 `chained-examples.md`〈各段允許與禁止內容〉與〈驗證段機械檢查：依據句可溯性〉，修正兩處：`i18n-guidelines.md`〈判準：標籤＋識別符組合時如何分割〉刪除含 Dart 插值語法 `'ISBN: $isbn'` 的檢驗問句改寫為技術中立問法，刪除預期答案引入條文查無依據的「貨幣、日期、格式化 API」一問（既有 Q1／Q2 已足以驗證標籤與數值分割的核心判斷，非缺範例）；`color-system.md`〈Theme 與 Token 判準〉將產出與驗證之間夾雜的 Dart 語法區塊移至三段鏈式結構之外標為附註查表、驗證問句移除 `ColorScheme／TextTheme` 框架名、刪除預期答案「掃描器 pattern 群組」條文查無依據的檢驗問句（該問句與主題／token 路由的核心判斷無關）；保留的判準句依 `SKILL.md`〈Key Files〉「Theme」定義與本節判準句可溯。
**Version**: 1.7.0 — 依 typography 取整表評估裁決比照 spacing 方案 D（按值來源分流），改寫 `references/typography-system.md`〈Migration Guide〉：Common Replacements 表移除 `fontSize: 11/13/15/17/19` 四個平手列，僅留 9 個精確命中列（10/12/14/16/18/20/24/28/32）；新增〈Non-Standard Values〉節，四類來源分流表與 `spacing-system.md` 同節對齊（設計來源刻意指定視為缺 token 走前置票加階；設計畫布實測值萃取時歸階；既有程式碼裸值取近似屬行為變更另開收斂票；無設計來源新寫程式碼才套用候選表，平手列由「一律取較小一階」放寬為兩階並列皆合規）。移除表後過時散文句（描述單向裁決規則，與新節矛盾）。`SKILL.md`〈五類約束與按需讀取〉字級列觸發條件補「值不在〈Type Scale〉離散階上」。比照範圍窄於 spacing 修復——typography 平手列本有一致解法，僅缺按來源分流這一層，不改判 headline/title 同值判準與 Weight Replacements（無離散階取整問題）。
**Version**: 1.6.0 — 依 G26 WRAP 裁決（方案 D）改寫 `references/spacing-system.md`〈Non-Standard Values〉：原「二選一取近似」的單一判準改為按值來源分流表——設計來源刻意指定的值視為缺 token，走 design-system 前置票加階；設計畫布實測值萃取時歸階（`foundation-design` skill `references/examples.md`〈萃取不等於照抄〉）；既有程式碼裸值取近似屬行為變更，候選階記入決策文件另開收斂票（`foundation-design` skill `references/handoff-mode.md`〈命名與收斂分兩步〉）；無設計來源的新寫程式碼才套用候選表，`6` 由單一候選放寬為兩候選（`xxs`(4) or `xs`(8)），與相鄰兩階距離相等時兩階並列皆合規。`SKILL.md`〈五類約束與按需讀取〉間距列觸發條件「不在 4dp 網格上」改為「不在〈Spacing Scale〉的八階上」，呼應候選表不再是均勻網格取整。`typography-system.md` 同構取整表另立票評估，本版未動。
**Version**: 1.5.0 — 修補一輪缺範例審查發現的四個缺範例位置與三項既有範例殘留。**Theme 與 Token 判準（#8）**：`references/color-system.md` 新增決策表，依 `SKILL.md`〈Key Files〉「Theme 由 token 組裝」判斷何時用 `Theme.of(context)`、何時用 token 類別；`typography-system.md`〈Best Practices〉6 改為引用同一決策表，不重複。**顏色歸類判準（#9）**：`references/color-system.md` 新增依〈Three-Color System〉功能分類（非業務後果）判斷 primary／positive／negative 的決策表與檢驗問句。**字級 headline 與 title 同值判準（#13）**：`references/typography-system.md` 新增依定義註解（Subsection headings vs Card titles/dialog headers）判斷 `headline4` 與 `titleLarge` 的決策表；Migration Guide 補一句說明既有取整規則（相鄰兩階等距取較小階）。**i18n Exceptions 標籤與識別符分割判準（#10）**：`references/i18n-guidelines.md`〈Exceptions〉補判準與決策表，區分「標籤需翻譯」與「識別符數值本身豁免」，並依〈Violation 3〉改用參數化翻譯修正原本互斥的字串插值範例。**他專案殘留刪除**：`SKILL.md`〈Quick Reference Card〉刪除 `package:book_overview_app` import 陳述；`i18n-guidelines.md` 刪除〈Validation Tools〉整節（`make check-i18n` 等本專案不存在的指令）、〈Supported Languages〉10 語系表與〈File Structure〉ARB 檔樹改為角色描述，Overview 補存取子示意宣告呼應既有的類別名示意處置。`references/spacing-system.md` 本版未動——Non-Standard Values 二選一與加階判準仍在獨立評估中。
**Version**: 1.4.0 — 修補一輪缺引導審查發現的六項（G21-G25、G27；G26 為新增判準會改變既有判定的一項，草稿留在 ticket Solution，本版未動 `references/spacing-system.md`）。**原生元件直用歸屬（G21）**：〈與相鄰資產的交界〉原本自稱「本 skill 抓裸值與原生元件直用」，但五類約束與 `scripts/style_checker.py` 皆無此偵測 pattern；改為明確聲明本 skill 不偵測，權威改指 `component-contract-design` 的〈用詞〉「元件庫」邊界與方法論〈禁自製元件〉，該 skill 的 `step-3-fill-contract.md`〈原生元件禁用對照表〉來源列同步移除「執法工具的偵測 pattern」的失實引用。**豁免路由（G22）**：〈全程〉步 3 補一句——「正當理由」須滿足方法論〈豁免三條件〉且由 PM 驗收核可，不是執行端自行認定。**baseline 引用錨點（G23）**：〈與相鄰資產的交界〉version-bootstrap 列原引用〈Project Calibration〉的 baseline 說明，但該節從未寫過 baseline；改指 `foundation-design/SKILL.md`〈工作流〉步驟 5「首次接入執法載體必然大量失敗」段的實際定義。**豁免與略過機制的區分（G24）**：〈Project Calibration〉補一段區分 `exempt_markers`（計入 exempt 報告計數）與 `// OK:`／`// ignore:`（整行略過、不計入任何計數），讀原始碼 `should_skip_line`／`is_exempt` 確認行為差異。**類別名示意性（G25）**：〈Key Files〉後補一段聲明全篇 `UIColors`／`UISpacing`／`UIFontSizes`／`UIBorderRadius` 為跨專案示意類別名，真實類別以校準檔 `tokens` 欄位為準。**元件文字歸屬（G27）**：〈Internationalization〉補兩段——內建預設文案的 key 歸屬判準指向方法論〈元件文字歸屬（i18n-first）〉三條件 AND，以及掃描器對 `/design_system/` 等排除目錄的既知盲區，不能以掃描通過視為已驗證。
**Version**: 1.3.0 — 修補一輪原文矛盾審查發現的四項。**間距 20 對應階（C05）**：`references/spacing-system.md`〈Common Replacements〉刪除非標準值 `20` 的重複列——該值已在同檔〈Non-Standard Values〉以「兩候選」誠實列出模糊性，前者卻宣稱單一答案，兩處互斥；4dp 網格本身（〈Spacing Scale〉）不含 20，刪除後兩表不再互斥，20 的實際取捨仍待另一份缺引導修補判定。**`.rsp` 正誤標示（C06）**：`SKILL.md`〈Responsive Font Sizes〉補上 `14.rsp` 為正確手動寫法的範例並說明理由——原文只列 `14`、`14.sp` 為錯誤示例、隻字未提 `.rsp` 手動寫法是否合法，讀者無法判斷 `references/typography-system.md`〈Manual Responsive Text〉標為「Correct」的 `14.rsp` 是否仍算違規；現與該份完整規範對齊。**錯誤訊息 i18n 呼叫層級（C04）**：`references/i18n-guidelines.md`〈Violation 2〉與 `SKILL.md`〈Violation 6〉原本分別示範「例外拋出處直接呼叫 `context.l10n!`」與「ViewModel 內並列 `context.l10n!` 與 `ErrorHandler` 兩種對等寫法」，未說明何時用哪一種；經裁決採「呈現層翻譯」，兩處統一改為非 widget 層只丟錯誤碼，翻譯留給持有 `BuildContext` 的呈現層以 `ErrorHandler` 轉譯。**垂直間距命名（C07）**：`references/spacing-system.md`〈UISpacing Constants〉原本並存「水平不加字首＋`.w`」與「垂直另立 `verticalXx` 字首＋`.h`」兩組命名，且〈SizedBox Usage〉〈EdgeInsets Usage〉範例又用第三種「不加字首不加尾綴」的寫法，`SKILL.md`〈SizedBox Spacing〉表格則只在文字註記尾綴、範例程式碼未寫出；經查證本專案既有間距 token 實作為單一命名組＋外部尾綴（無 `vertical*` 常數），三份文件已統一改為此形態，`verticalXx` 命名全數刪除。
**Version**: 1.2.0 — 依一輪低階 model 讀者探針（3 個實例，指令逐字相同）處置四項，其中三項為 3/3 命中。**新增〈起手〉節**：三名探針對「第一個具體動作」給出三個不同答案，其中一名直述「文件沒有明確指定」；對原文 grep `第一|起手|first|開始前|步驟1|workflow` 零命中，確認為文章未寫而非讀者誤讀。現明定第一動作是讀 `.claude/config/dart-style-guardian.json`，並說明不先讀它拿到的建議無法直接照做。**明定產出**：三名探針皆答不出本 skill 要產出什麼，或補一句「文件沒有寫格式」；現於開頭寫明產出是違規清單加改完的程式碼，且清單本身不是交付物。**新增〈五類約束與按需讀取〉表**：`references/i18n-guidelines.md`（1507 tokens）原本在 SKILL.md 與全 repo 的入站引用數皆為 0，對照組另三份各 3 處；三名探針列 references 時無一提及它。缺的不是規則（正文本有 i18n 一節）而是路由，故補的是路由表而非內容。〈Reference Files〉清單改為只列存在、判準指回該表，避免兩處各維護一份。**新增〈與相鄰資產的交界〉表**：探針 Q5 三份皆只找到 `component-contract-design`。四列各寫「這裡掃不到什麼」——例如組合層的重疊與截斷本 skill 掃不到、token 層未建時掃描範圍為空。**description 由 260 字元改為 240** 並補 `Do NOT use for` 路由，同步移除 baseline 中該筆凍結值。**體量**：SKILL.md 4209 tokens。

**Version**: 1.1.1 — 檔頭反向路由句「元件契約的十一欄位」改「元件契約欄位表」，跟隨方法論 1.14.0 欄位表計數去數字化（DOC-GPD-003）
**Version**: 1.1.0 — 檔頭方法論引用句補一句反向路由：本 skill 抓的是禁令（裸值、寫死文字、原生元件直用），正面對應的元件契約由 `component-contract-design` skill 承接。動因：查 UX 設計族 skill 的族內互引，本 skill 只有入度無出度——使用者被本 skill 的違規擋下後，沒有任何訊號指向「正確做法在哪定義」；上游追蹤 `tarrragon/claude#79`／`#82`。
**Last Updated**: 2026-09-07

**Version**: 1.0.0
**Last Updated**: 2026-03-02
