# skill-design-guide 版本紀錄

新到舊。版號規則與兩個住址（本檔與 `SKILL.md` frontmatter 的 `metadata.version`）見專案的 skill 同步規範。

**本檔只記變更本身**：版號、改了什麼、落在哪一節。多輪審查的 finding 分類、逐 frame 敘事、
被推翻的判定與其推導過程屬設計過程紀錄，依 `writing-the-body.md`〈嚴禁清單〉該列的處置放在
**專案的 worklog 系統**，不放這裡。

**寫每一條之前先問**：這句話裡的**每個數字與每個完成式，是同一個時點的嗎**？壓縮舊條目時，
數字取自當時、項目清單取自現在，兩者會被併進同一句而不再可分；宣告「已回溯套用」時，
做了一部分與做完在文字上同形。實測本檔在同一批修法內犯過這兩種各一次——而那一批正是在修
前三次的同型缺陷。**這一問是壓縮專用的**：既有的「同一件事寫了幾種形式」對展開有鑑別力，
對壓縮沒有——壓縮的產物只有一種形式，它只是錯的。

**Version**: 1.12.2 — 第三輪複驗（九份探針，指令與餵入範圍與前兩輪逐字相同，門檻派發前寫死）的結果與處置。

**V4 生效**：「路由表住在哪個檔的哪一節」由第一輪 2/4 判不清楚、第二輪 2/3 不清楚加 1/3 指錯章節，轉為**0/3 不清楚、0/3 指錯**。把「入口檔」改成明寫 `SKILL.md` 這一改奏效。

**V2a 形式上未達門檻，但門檻是我訂錯的。** 三輪為 3/3 漏 → 2/3 漏加 1/3 讀反 → **1/3 漏加 0/3 讀反**。門檻寫「0/3 漏且 0/3 讀反」，故判未生效；但那一份漏答的重點清單有十項且皆為實質內容——**清單有上限，滿額清單上漏一項不等於沒讀到**。誤讀是判決（讀者會照錯的做），漏答只是壓縮時的取捨，把兩者綁成一個標準會讓實質已解的項目被判成未生效。**往後這類量測誤讀與漏答分開訂**：誤讀 0/N 為硬條件，漏答只在多數漏時才算訊號。V2a 的文字不再改。

**處置切換條件未觸發**：疲勞點落在 token 估算區者 0/3（前兩輪 3/3、2/3），故不外移分段估算段。

**疲勞點三輪三個位置，每輪都落在前一輪新增的內容上。** 本輪 **3/3 一致**落在 1.12.0 新增、1.12.1 補強的 SRP 四段，三份理由一致：「層層推敲同一個概念，讀起來很密集」「句型反複在層級與拆分判準間往返，每句都帶新概念而前文未充分推導」「同時講為什麼不能某種拆法和應該怎樣拆，反向論述難以追蹤」。處置：四段散文改為一段加一張兩列對照表（什麼時候／做什麼／判錯的代價），交錯的正反論述收進表格欄位。

**又一次在補規則的同批修改裡違反該規則（第二次）。** 1.12.0 新增必查項「用詞定義與用詞使用分屬兩檔」，而新增那張表時把「兄弟檔互指矩陣」「層級混疊」兩個詞直接當列名用、未就地定義——C 批 2/3 有障礙（一份判「沒有定義，直接使用」，一份說該列需讀兩遍且只描述現象無範例）。與前一輪的〈位置性指涉〉同型：犯它的是編輯動作（新增列名時圖簡潔），讀規則時想的是撰寫動作。兩個詞已在列名後就地加括號定義。

**其餘處置**：「第二條路：整體外移時 H1 不能重複」**3/3 需讀兩遍**（既有內容），在兩則範例之前補一句分辨外移對象的兩種形狀與相反的 H1 處置；「Haiku / Sonnet / Opus 行為一致」**跨兩輪 3 份**質疑「無可執行程序卻列在清單」，移出 checkbox 改為註記，明寫「沒有這一項是刻意而非遺漏」；「兩層的成本機制不同，不可混談」**2/3 跨批**（一份想跳過、一份不確定為何出現）刪除，它不影響任何動作；「別用『反正一次只會用到其中一段』當理由」2/3 仍歧義，1.12.1 已改一次，本輪改為具體形態；檔頭「同目錄」段被 1/3 指為循環引用，明寫兩件事的分工；「本 skill 六份現況皆為後者」去計數。

**回歸檢查全數維持**：V1 0/3 回「文件沒說」、V2b 0/3 漏、V3 0/3 回「沒說」、「只能記住三件事」帶走「怎麼寫」3/3。**第一個具體動作首次收斂**——前兩輪 3 份 3 種答案，本輪 3/3 皆依 1.12.1 新增的三列起手表作答。1.12.1 刪除的〈表達方式與預設值〉整節與改為註記的消歧義段，兩處資訊皆未遺失。

**不處置並記理由**：「還沒決定自己在做哪一件事時該去哪」（1/3，再加一列會變無限迴圈）、「找不到檔案具體路徑」（1/3，portable 的必然代價）、「250 字元唯一閘門」兩種讀法（1/3，兩種讀法操作上導向同一件事）、「兄弟檔互指矩陣缺具體範例」（1/3，該列已附 grep 查法與實測後果）。

**本版未經第四輪量測。** 疲勞點連續三輪落在前一輪新增的內容上——若第四輪又落在本輪新增的兩列對照表，代表問題在該節的資訊密度上限，不在個別段落。

**Version**: 1.12.1 — 1.12.0 的複驗（九份探針，指令與餵入範圍與上一批逐字相同，門檻於派發前寫死）結果與處置。

**五項門檻：三項生效、一項部分生效、一項未生效。** V1（正文與 references 的分工）2/3 回「文件沒說」→ **0/3**；V2b（外移的判準與分界）3/3 全漏 → **0/3 漏**；V3（splitting reference 走完之後有交棒）4/4 回「沒說」→ **0/3**；V4（路由表定義）**部分生效**——三欄定義生效（3/3 皆答得出，此前無人答得出），但「路由表住在哪」2/3 仍判不清楚、1/3 指錯章節；V2a（第 2 層無 hook 執法）3/3 全漏 → **2/3 漏加 1/3 讀反，未生效**。

**V2a 是 1.12.0 自己造成的迴歸。** 一份把它讀成「第 2 層有 hook 涵蓋、第 3 層無 hook」，與原文相反——讀反比漏掉更糟，它讓讀者以為有人會擋。歸因：1.12.0 把該段從節末 **Consequence** 前移到三層表正下方，開頭寫「**本層**無 hook 執法」；在節末「本層」有上下文，移到三層表底下之後表上有三層、指涉懸空，同句又寫「涵蓋 pm-rules / rules / **references** 三處」，更容易把讀者導向第 3 層。`splitting-an-existing-skill.md`〈位置性指涉〉條講的正是跨節搬動後位置詞與代稱會失效——**1.12.0 一邊補那條規則，一邊在同一次修改裡犯它**。改為明列「第 2 層與第 3 層都沒有 hook 會擋你」，唯一有執法的第 1 層獨立成句。

**V4 的分解值得記**：三欄定義是生效的，未生效的是「這張表住在哪個檔的哪一節」。原文寫「路由表指入口檔〈按需讀取〉那張表」——章節有寫，但「入口檔」是代稱不是檔名，而檔頭「同目錄」段另寫 `writing-the-body.md` 管「路由表涵蓋章節欄的寫法」，兩者並存造成分歧。改為明寫 `SKILL.md`。

**附帶量測的最大改善**：「只能記住三件事」帶走「怎麼寫一個 skill」由 **0/3 升至 3/3**——三份的 top-3 分別是「衡量標準唯一：agent 照正文走得完嗎」「正文是操作手冊、references 是依據」「正文必須讓 agent 走完工作流程，關鍵判斷條件不能外移」。1.12.0 之前三份交集全是量測。

**疲勞點 2/3 仍落在 token 估算區**（基準 3/3），且一份指出 1.12.0 壓縮的副作用：「這是對做法的否定，但沒立即說明為什麼」——**理由移走之後，留下的主張句成了沒有理由的否定**。用一句把理由寫回（中文與 ASCII 的 chars/token 差三倍，混合內容用單一比例會落在兩個換算值之間）。

**第一個具體動作 3 份 3 種答案**（跑量測指令／混合／找不到）。1.12.0 補的「先認你在做哪一件事」沒有收斂——那句說了要分辨，沒說分辨完各自的第一個動作。補一張三列表寫出三種任務各自的起手。

**其餘處置**：新增的實證敘事（七份 references 的五類缺陷）被 1/3 指為在診斷段落佔篇幅過多——**壓縮並移入本檔**，這正是本 skill 自己的診斷一，1.12.0 新增時未套用自己的規則；「拆成兩個 skill，彼此指名互相引用」被 1/3 讀成「把 references 拆成兩份」，改為「建立第二個 skill 目錄」並明寫否定句；「不以『一次只用其中一段』定位」的反說 2/3 難追，改為正說加後果。跨兩輪各 2 份質疑的兩項一併處置：〈表達方式與預設值〉整節刪除（其路由本就完整存在於〈按需讀取〉表該列，無資訊損失），近同名章節的消歧義由獨立段落改為路由表下的一句註記。

**不處置並記理由**：〈讀者可用性〉組被 1/3 指為「本身就是一個完整的評審流程，列在檢查清單裡有點超載」——它的份量就是該有的，且已明標該組驗的是可用不是格式；「H1 是什麼」（1/3）屬對讀者的過度要求；「一半以上是否含等於」（1/3）邊界值不影響處置方向。

**本版所有處置未經第三輪量測。** V2a 已連續兩輪未達門檻。

**Version**: 1.12.0 — 新增正文與 references 的分工定位、超標的四格診斷、SRP 拆 skill 的判準，以及發布前檢查清單的〈讀者可用性〉組；`splitting-an-existing-skill.md` 補三類拆分割裂必查項與交棒出口。全部改動先對本 skill 自身跑九份低階 model 讀者探針（A 3 份讀 `SKILL.md`、B 3 份測差集、C 4 份讀 splitting reference）量現況，再依量測結果落地。

**探針量到什麼**：問「哪些內容該放正文、哪些該放 references」，**2/3 回「文件沒說」**，第三份是自己從三層表「寫什麼」欄的六個詞推導的。差集有兩項 3/3 全漏——「第 2 層無 hook 執法、完全依賴自查」與「外移的判準與分界」。「只能記住三件事」三份的交集是三層預算與兩問，**沒有一份帶走「怎麼寫一個 skill」，記住的全是怎麼量**；對一份操作手冊而言這是最重的一項。疲勞點 3/3 一致指向分段估算的論證段。「不確定為何出現在此」三份三處但同一類：維護規則與消歧義說明混在操作正文裡。C 批 4/4 一致回「走完最後一步之後文件沒說要做什麼」，2/4 指出四問框架每一格都要「指名路由表某一列」而路由表本身沒有定義（另 2/4 判為清楚，卻各指向不同位置）。

**新增〈正文是操作手冊，references 是它的依據〉**（`SKILL.md` 核心心法首節）：一張放正文／放 references 的對照表，判準收斂為一條——**agent 照正文走得完嗎**。走不完有兩種形態且修法相反：正文只給結論而把判斷條件外移，讀者選不了路，把條件搬回；正文塞進完整論證，讀者在該動手時還在讀理由，把論證外移只留主張句。

**新增〈超標了怎麼辦——不是刪，也不是硬搬〉**：四格診斷取代原本一句路由。第三格明訂**依 SRP 拆成多個 skill、彼此指名互相引用，不是把一個 skill 拆成更多檔**——SRP 的單位是 skill 不只是檔案，把兩件事的細節各自外移，職責數量沒變、正文仍要同時交代兩條路；兩種拆法的判別在**讀者要不要換任務**（換任務拆 skill、同任務的不同階段拆檔案），並列出兩邊判錯各自的代價。第四格明訂三項成因都不成立時**超標是設計問題不是編輯問題，不得為了達標而刪判斷條件或搬進 references**。另立一句：過度拆解與超標是同一個判準的兩端，預算只是其中一端的代理指標。

**新增檢查清單〈讀者可用性〉組**（新寫與**改寫**皆須）：跑低階 model 讀者探針、入口檔單獨跑一批、派發前登記意圖清單以取差集、每項 finding 先歸因再處置、修完再跑一次。明寫改寫者是最讀不出缺口的那種讀者。

**`splitting-an-existing-skill.md` 三處**：必查項表補三類——**兄弟檔互指矩陣不對稱**（附 grep 查法；漏一格使該檔讀者 3/3 不知道有第三條路徑）、**層級混疊**（子檔步驟編號與全程編號指同一件事而各編一次，位元指涉節界全對）、**用詞定義與使用分屬兩檔**（並註明檔頭總括式指路不算數，實測 3/3 判「沒說去哪查」、改為首次用點就地帶指路後 0/3）；〈兩種驗證，方法不同〉表的「可機械執行」欄改為「誰能判」，拆分可用性由「否」改為「**外部可量，作者不可判**」，並補派發方式；新增〈走完之後〉節（C 批 4/4 一致指出的缺口）。四問前補上路由表的最小定義與「四個判定各自」的語意（3/4 讀了兩次）。

**本次自身即一則實測**：加完條文後 `SKILL.md` 兩度撞門檻（4974、5022）。未直接刪減，照新寫的診斷表分流——成因三不成立（檢查清單與設計原則的讀者是同一批）、成因二不成立（依四問的問 2，每一列的讀者最後都會用到檢查清單，該留入口檔）、**成因一成立**（檢查清單多個項目掛著長篇「不涵蓋」論證，操作內容只有一句）。壓縮的六處全是探針獨立指到的位置，故為量測驅動而非為了數字。收斂後 4608 tokens / 196 行。

**未做**：本版所有改動未經複驗。本版自己新增的〈讀者可用性〉組第五項正是「修完再跑一次」，該項對本次改動尚未執行。

**Version**: 1.11.2 — `references/writing-the-body.md`〈Body 寫作〉術語對照表的禁用詞列：三個被禁詞由引號改為反引號。該列是對照表，列出禁詞本身是它的功能，但 consumer 專案的 `skill-banned-term-scan-hook.py` 掃散文用法時以引號內文字為命中，反引號則視為程式碼字面而放行；此修正原以 1.9.1 落在一個 consumer 本地，因 1.10.0–1.11.1 由另一方推送而未進發佈庫，本版補回。內容不變，只有標記形式。

**Version**: 1.11.1 — `SKILL.md`〈發布前檢查清單〉Body 組的 TOC 判準補一句排除範圍：該判準原文只寫「reference」，未答「SKILL.md 自身是否也要 TOC」，六份 reference 皆有而入口檔無這件事因此懸空過三輪審查未定案。判定為不需要：入口檔正是〈Progressive Disclosure〉表列「整份執行」的範例本身（該節逐字「它與 SKILL.md 一樣是整份進 context」），觸發後全檔一次讀完，TOC 服務的「選段跳讀」情境不存在；入口檔已有的〈按需讀取〉路由表解決的是另一個問題（去哪一份其他檔案），不是本檔內部導航，兩者不互相要求。

**Version**: 1.11.0 — `splitting-an-existing-skill.md` 五項，由外部專案以 1.9.0 實跑一次六倍門檻的拆分後回報。〈拆分特有的必查項〉新增第 4 列**相對連結的層級相依**：前三套（路由表涵蓋章節欄、檔頭同目錄索引、正文跨檔指涉）驗的是指標指向的東西在不在，這一類驗的是**指標自己搬家之後解析基準是否位移**，維度不同——內容往下搬一層時相對連結字串未變而基準點移動，該側三條 `../` 引用從入口檔出發正確、進 `references/` 即斷。本 skill 因 `references/` 為扁平結構而全站零命中，條文明載那是結構湊巧不是規則生效。〈外部引用〉列的示範路徑加 `broken-link-exempt` 註記（該檢查的 marker 作用單位是整行，非單一連結）。〈外移什麼、留什麼〉四問表補兩個缺口：問 3 加「該列須在本次拆分之前即已存在」（為本次拆分新造的列不算），並補「多數列但非全部」這個原本落在問 2 與問 3 之間的空格的處置。〈結構約定〉補「亦由此進入」的證偽規則——宣稱 X 送讀者來，X 檔裡就要有本檔的檔名或節名，`grep` 零命中即刪掉宣稱。〈一則最小走查〉補第二條路：單一 `##` 連同其 `###` 子節整體外移時，該 `##` 升作 H1、刪掉重複的 H2、`###` 各自升 `##`。

**這條紀錄補寫於改動提交之後。** 該批改動與另一批並行進行，派發時為避免兩個執行者搶同一個檔，本檔的寫入權集中給了另一方，該方的範圍不含這五項——結果是改動送出而版本紀錄缺席。並行寫入的協調成本必須有人付，把它從「搶檔案」轉嫁成「一份紀錄缺席」的代價更高，因為 CHANGELOG 不會因為少一條而報錯，而經 `skill-sync` 取得本 skill 的 consumer 只看得到這一份。往後遇同型並行，補寫責任明確給後完成的一方。

**Version**: 1.10.1 — 〈Progressive Disclosure — 三層載入〉修一處自我不一致：第 2 層門檻條文寫「SKILL.md body」，而同節給的量測指令（`wc -l`、分段估算）量的是全檔路徑（含 frontmatter），兩者差一個 frontmatter，跨專案交叉核對同一檔各自量出 152 與 148 兩個都自洽的行數才浮出。改為條文對齊指令：層 2 表格列與其下說明段落改為「SKILL.md 全檔（含 frontmatter）」，並補一句 why——frontmatter 雖常駐第 1 層，觸發後整份檔案仍會原樣再讀入 context 一次，這部分成本計入第 2 層，量測不扣除它；〈發布前檢查清單〉的對應勾選項同步改字。分段估算指令已核對與行數門檻範圍一致（皆讀同一份全檔路徑），未發現需另修之處。官方引文「Keep SKILL.md body under 500 lines」（原文用詞）維持不動。

**Version**: 1.10.0 — `patterns-and-troubleshooting.md`〈Skill 設計模式〉五個 Pattern 收斂為三個控制流形狀（循序含關卡、迭代至門檻、決策分派）加一個跨形狀可附加階段（決策留存）；新增〈怎麼選：三形狀判準表〉，任兩形狀之間給出可判邊界；原 Pattern 5 四條關鍵技巧逐條列出去處——完整文件記錄與清楚治理機制併入決策留存，先合規後執行併入形狀一，嵌入領域專業併入判準表的填寫指引；三則範例換成本庫實際 skill（`version-release`、`multi-round-review`、PM 決策路由）。`seeing-like-an-agent.md` 檔頭改寫：原自宣告為單一「為什麼」層，與後半六則含程式碼的可執行步驟不符；改為分述前後兩半各自的內容型別與對應讀者狀態。`SKILL.md`〈按需讀取〉路由表 seeing.md 一列的路由詞同步補上需要可貼用進階模式的讀者狀態，消歧義段的計數與定位描述隨 patterns.md 的形狀收斂同步更新。

**Version**: 1.9.0 — v1.8.0 暫緩的兩組判準補完，並依其中一組執行外移。(a) 「超標時該外移什麼」與「SKILL.md 該留什麼」原以互斥性當外移訊號，而互斥性對每張判準表都成立，兩個半句對同一批內容給相反處置；改為**四問依序，每一格都要求指名入口檔路由表的具體某一列**（是某列的觸發條件 → 留；每列讀者都用 → 留；指得出少數列 → 外移到那些目標檔；指不出任何列 → 沒有讀者情境，壓縮成主張句），落在 `splitting-an-existing-skill.md` 新增的〈外移什麼、留什麼〉。(g) 位置性指涉的「目標可能移動即失效」要求對未來下斷言，而同段實證是四處同型缺陷中三次由修法自身引入、當時都判過不會動；改為兩條打開檔案就看得到的條件——**跨檔**、**目標帶識別字**（`##` 標題、粗體引導句或檔名），合規只剩同檔緊接且目標無識別字一種，落在 `splitting-an-existing-skill.md`〈拆分特有的高頻缺陷〉。

同批依 (a) 判準外移三節：〈Degrees of Freedom — 自由度匹配脆弱性〉與〈Opinionated Defaults — 預設路徑引導正確做法〉移入 `patterns-and-troubleshooting.md`，〈三類 bundled resource 的分工〉移入 `creating-and-adopting-skills.md`，`SKILL.md` 各留一列路由；兩個目標檔的檔頭三段式與「本檔章節」清單、入口檔路由表的「涵蓋章節」欄同步更新，`writing-the-body.md` 與 `frontmatter-and-description.md` 的檔頭「同目錄」段與一處死名一併改指新住址。`SKILL.md` 由 4,670 tokens 降至 3,601（5k 預算的 93% → 72%）、153 行。**新 (g) 判準在本批內對自身生效四次**——「上述實證」「上面那行指令」「單檔見下」「上例三段套上面兩問」皆指向帶標題或帶粗體引導句的目標，已改具名。

**Version**: 1.8.0 — 五組判準重新設計，每組改為可由第三方跑出同一個答案的形式。(b) 引用規則由「只一層深，禁止 A → B → C」改為**一跳可達**——禁的是「只能經由另一份 reference 才被發現的內容」，reference 之間互相指路不算違規；落在 `writing-the-body.md`〈Body 寫作〉，機械檢查（`comm` 列出「在 reference 裡出現、但入口路由表沒有」的檔名）落在 `SKILL.md`〈發布前檢查清單〉Body 組。(c)「Claude 真的不知道這個嗎」改為**刪除測試**（拿掉該段，乾淨 session 重跑同一任務比對產出）。(d) 自由度判準的「走錯一步會壞掉嗎」改為兩問：產物有機械消費者或動作不可逆嗎、多次執行的產物要互相比對或彙總嗎。(e) 體量主判準改為**分段估算直接比 5k**，行數降為官方合規項；第 3 層「無上限」界定為目錄總量，單檔判準改以讀取方式定；落在〈Progressive Disclosure — 三層載入〉，該節的「語言換算表」同時改名為**分段估算表**。(f) description 三個口徑中**只有 250 字元是閘門**，另兩個降為官方硬上限與量級參考並附換算，落在 `frontmatter-and-description.md`。(a)(g) 兩組暫緩，由後續 ANA 承接。

同批把 (b)(e) 的措辭同步到三處：`seeing-like-an-agent.md` 反模式表（原列「巢狀引用（A→B→C）」，與一跳可達相反）、`creating-and-adopting-skills.md` 4d 與 `patterns-and-troubleshooting.md`〈Context 過大〉的「語言換算表」死名。**未改的兩處是歷史記述**：本檔 v1.5.0 條目與 `writing-the-body.md`〈一則完整走查〉的「改後設計（第一次）」都在敘述當時的設計，改名會讓它們宣稱當時產出了現在才有的東西。

**Version**: 1.7.3 — 修 Round 4 第四個 frame（修法自傷復跑）留下的三則。〈外部引用：指名身分，不用檔案路徑〉在全庫統一為全名（前次只改了正文兩處，CHANGELOG 兩處未動）；「亦由此進入」的擺放由要求獨立成段改為明訂兩種皆可（五份寫在「何時讀」句中、一份獨立成段，判準本就該卡在「與主路徑視覺上分得開」）；`splitting-an-existing-skill.md` 的「五份新檔」計數移除——該處與 CHANGELOG 舊條目的「八份」對同一次實測給出不同數字，而 v1.7.2 的壓縮已刪掉後者，改法依同檔〈拆分特有的高頻缺陷〉的既有條文取消計數而非改對數字。

**這一批的取樣單位是「事」不是「本批改動位置」**：前三則的復跑之所以還能找到殘留，是因為掃描範圍改為全庫的每一種形式，不限這批動過的檔。以改動位置取樣時，新形式的對照組必然在未改動處，該類永遠回報通過。

**Version**: 1.7.2 — 修 Round 4 三個 frame 的 finding。

**重組後遺症**（兩次各自正確的修法合成一個問題）：橫向入口查法的扣除規則補上「同目錄」段（該段被補成全互指之後成為最大宗的偽陽性來源，實測 5/9）；〈觸發測試〉的無程序標註由逐項改為整組（前三項的做法落在一個已被標為無管道的層，而原標註只掛在第四項）；〈Step 4：撰寫內容〉表格內編號改為 4a–4f 並修三處跨檔指涉（原有兩套 1–6 命名空間，「Step 5」同時指節與表列）；「有兩份漏列」等內嵌計數移除。

**形式分歧**（同一件事在六份之間有幾種形式）：六份「同目錄」段補齊為各列其餘五份（原有三種完整度，缺席者恆為同一份）；三處懸空 `---` 檔尾移除；「六步」計數四處全清；**「不涵蓋」的告示收斂為固定前綴 `**不涵蓋**：`**——該類是四批修法新增最多的一類內容而原有五種形式、無共同字串可掃，約定寫入〈內容品質規則〉。

**誤報**（拿判準審明顯良好的 skill）：三條判準補上缺的那句條件——TOC 加「且被選段查閱」（被整份執行的 reference 不適用，實測同一判準在兩支 skill 上判出 25 誤報 vs 8 真違規）；「時間敏感」限定為「效力有期限的內容，不是內容有時間戳」；〈外部引用：指名身分，不用檔案路徑〉的 Action 問句**提到例外清單之前**並改寫為「它要操作的東西 vs 讀者要去讀的東西」（該問句本就在條文內，被四類例外的枚舉蓋過；實測 24 個命中裡它一次判定 23 個）。

**未修**：`description < 250 字元` 的邊界問題不是加條件能解——16,000 ÷ 58 ≈ 276 顯示 250 的真身是均分配額而非單支截斷點，而「哪幾支值得多佔」的機制尚不存在，在它存在之前任何單支閘門都會把 43% 的庫判成違規。已入判準重設計票。

**Version**: 1.7.1 — 修 Round 3 嚴重必修中可機械執行的五項。〈外部引用：指名身分，不用檔案路徑〉例外 1 由具名清單改為判準（成員自述會與清單脫節），並補「skill 內相對路徑與外部路徑同形時一律補足前綴」；`patterns-and-troubleshooting.md`〈測試方法〉與〈常見問題排除〉首表補適用邊界（兩者預設經上傳分發）；〈發布前檢查清單〉補一列指向 `skill-cli-sync-check` 規則，該規則同步補回指。

**Version**: 1.7.0 — 修 Round 2 的三類缺陷。**指標**：〈按需讀取〉路由表的涵蓋章節欄改為與目標檔標題逐字相同並要求雙向齊全，跨檔指涉補檔名，兩份 reference 補「亦由此進入」。**條文未套回自身**：〈外部引用〉起手句去計數、〈拆分特有的必查項〉補〈H1 與檔名〉列、節界檢查改為排除 fenced block 的 awk 版、五份 100+ 行 reference 補章節清單。**節界**：〈一則完整走查〉提為獨立 `##`、〈觸發控制矩陣〉由 `###` 提為 `##`、兩處 stale footer 移除。

**Version**: 1.6.0 — 拆分。SKILL.md 由 17,962 字元／578 行降至 6,482／169，兩個官方門檻皆通過；references 由三份增為六份，切分判準為「一次工作會不會同時用到」。入口檔留路由與判準。拆分前把 17 處內部 `§N` 編號指涉全部改為章節名，並移除全庫章節編號。

**Version**: 1.5.0 — 修多輪審查 Round 1 的嚴重項並補三節範例（自由度三分的並列對照、Opinionated Defaults 的完整走查、拆分程序的最小走查）。兩項對外查證推翻既有寫法：v1.3.0 的「官方全篇未給任何行數限制」為誤，**兩個門檻都是官方值**；`CHARS_PER_TOKEN = 1.3` 的適用條件限「繁中為主集合」，已改為附語言換算表並明令不可無條件套用。額外檔案禁令收窄為本地判斷，`CHANGELOG.md` 移出禁止之列。

**Version**: 1.4.0 — 新增〈拆分既有 skill〉與 `references/splitting-an-existing-skill.md`。原本只說「超標就外移」，沒說怎麼外移才不會壞。該 reference 收拆分特有的必查項（當時為節界、跨檔指涉、共置關係、外部引用；〈H1 與檔名〉於 v1.7.0 補入）、兩種驗證方法、高頻缺陷、檔頭三段式、路由表的涵蓋章節欄與收尾步驟。

**Version**: 1.3.0 — 第 2 層體量判準由行數改為字元數。原表列兩個判準而 Action 只綁行數，於是實際生效的是括號裡那個代理指標。門檻 6,500 字元 = 5k tokens × 校準係數 1.3 chars/token（`file-size-guardian-hook.py` 的 `CHARS_PER_TOKEN`）。條文另載明本層零執法的事實。

**Version**: 1.2.0 — 新增〈外部引用：指名身分，不用檔案路徑〉，含判別問句「讀者是要去讀它學東西，還是要寫進它讓別的東西動起來」與正當例外。

**Version**: 1.1.0 — 新增 Opinionated Defaults 設計心法。

**Source**: Anthropic 官方 skill-creator + 官方平台文件 + Claude Code 擴展規範 + 本專案實踐
