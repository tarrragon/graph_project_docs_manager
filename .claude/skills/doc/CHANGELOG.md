# doc 版本紀錄

新到舊。版號規則與兩個住址（本檔與 `SKILL.md` frontmatter 的 `metadata.version`）見專案的 skill 同步規範。

**Version**: 1.19.0 — 落檔獨立評估者對 DOC-1 稽核第 2 階段 12 項條文草稿的 WRAP 評估結論，11 項定稿（W2、
W3、W4、W5、W6、W7、W8、W10、W12、W15、W16）：形態因素矩陣補外接裝置不構成新形態的判準與多形態展開
七項清單、回饋通道子表補三選一格式與追蹤票要求、樂觀更新政策改以「回滾後是否留下外部後果」定義低風險、
元件清單層級欄改以「是否承載本專案領域語意」判定（不採複用次數，避免推翻既有規格歸類）、內容角色補「描述
動作／對象／值」判準並解除角色與 slot 一一對應、變體與狀態矩陣分界改錨定方法論狀態集列舉、回饋契約失敗
路徑補以「使用者下一步」定義的四結局、i18n 表補呼叫端傳入文字的標註列規則、第 6 章原生佈局原語依使用位置
（元件庫內部實作 vs 頁面層排列）分流判定、第 7 章豁免條件 1 補套件提供元件與套件內部生成元件樹的分類要求。
W6、W15 兩項原草稿經 WRAP 判定會推翻既有規格既定歸類，改採替代條文（上提既有規格的語意定義為範本條文，
規格零改動）。W13（測試點粒度）僅落檔「不可用 + 替代」用字修正與替代通道須渲染驗證兩點，測試組織粒度
（一事件一條 vs 併入既有測試）屬使用者風格偏好，範本與範例對應位置留佔位待裁定。全部 11 項的範例同步
改寫（三段鏈式範例的【輸入】【產出】【驗證】對應段落）與範本條文同批完成。

**Version**: 1.18.0 — 修補元件庫規格範本 slot 契約「文字來源」強語意預設判準的條文缺口（DOC-1 稽核 C7，
Critical）：範本明寫三條件 AND 完整條文（走 i18n 系統非字面、參數可覆蓋、key 列入元件 API 契約），並補一句
「key 本身由元件持有不開放替換，可覆蓋的是插值參數」消解「參數可覆蓋」與「key 是否可換」的混淆；同步修正
範例檔對應條目「取消」按鈕一列——原「參數不開放覆蓋」易被讀成違反條文的「參數可覆蓋」，改為「無插值參數」
並加一則檢驗問句釐清「無插值參數」與「參數存在但禁止覆蓋」的差異。另修正〈框架容器「加一列安全區」的適用
範圍〉範例的判準句：原「毗鄰系統 UI」為條文未寫的抽象標準，改回引用範本〈容器亦為元件〉條文明列的「頁面根
堆疊、殼層」枚舉類別，消除依據句無法回溯條文的缺口。本版只處理範本與既有條文一致、可直接補指向或改範例的
兩項；同批稽核發現的其餘缺條文項（W2-W8、W10、W12-W13、W15-W16）已列草稿於對應 ticket，待簽核後另行整合。

**Version**: 1.17.0 — FlowStep 新增必填欄位 `traverses`：本步驟直接觸及的 domain 公開面，domain 名字串清單
（0..n）。欄位必存在，純畫面步驟（畫面狀態屬 layer 而非 domain）填空清單；經依賴邊間接到達的 domain 不列；
語意對應 domain map 的「貫穿」，貫穿數由消費端聚合。取代「每步單一 domain」的設計——單值無法表達一步觸及
多個 domain，也迫使純畫面步驟硬塞 domain。`FLOWSTEP_REQUIRED_FIELDS` 納入本欄位並於 schema 註解寫明語意；
conformance 測試改為檢查 `docs/usecases/` 下全部已回填的 UC（原只檢查一份），新增欄位契約檢查（缺欄位、
非清單、元素非字串皆攔下，空清單合法）與對應的正向對照測試；`references/usecases.md` 欄位表與
`templates/usecase-template.md` 骨架及範例補上本欄位。`tracking_schema.json` 不含必填欄位常數，無需重新
export。doc CLI 尚無 FlowStep 欄位驗證入口，本版不新增。

**Version**: 1.16.0 — 補齊元件庫規格範本兩處「判準藏在範例、條文沒有」的缺口（複驗發現讀者只讀範本主文
時各自代入不同架構假設，範例雖已內含判準但未反映到條文本身）：
(1) 狀態矩陣「來源」欄條文明寫判準——以值的儲存位置與可讀取範圍判定，推導值（如表單驗證結果）依其推導結果
的儲存位置判定、不看最初觸發者；範例檔對應條目移除「通常」這類以慣例覆蓋架構假設的寫法，改在【輸入】明寫
前提並補一列前提相反時（驗證結果只存在元件本地）判為互動瞬態的對照，驗證段檢驗問句改為引用新條文。
(2) 第 4 章尺寸契約「{視窗尺寸 N} 下的行為」選項明寫「換版型」僅限第 1 章策略 (b) 使用者可選版型觸發，依
視窗寬度自動切換者不得選；範例檔〈尺寸適應策略與「換版型」是否違反禁自動切版〉判準句改為引用此條文。

**Version**: 1.15.0 — 新增 `examples/component-library-chained-examples.md`：依 chained-examples 規格為
`component-library-spec-template.md` 24 個缺範例位置補三段鏈式範例（外置範例檔，模板子節加路由句指向對應
條目，理由：範本供複製後填寫，逐點內嵌會使複製出的規格檔夾帶大量與專案元件無關的示範內容）；同步改寫兩處
既有內嵌示範——操作機制「該形態輸入精度」欄原只有結論值（足夠/不足）缺具體精度數值，改為「具體值 → 結論」
格式並補對照範例。容器判別粒度與空間不足策略公式粒度兩個決策點因與 `component-contract-design` skill 的
既有決策點合併，模板改為路由至該 skill 對應範例，不重複收錄。

**Version**: 1.14.0 — 元件庫規格模板〈回饋契約〉子節投影方法論新增的〈掛載點〉〈失敗路徑覆蓋〉〈降級不解除回饋義務〉三子節：子節說明段補三條款各一句（不得只在服務回應之後才成立／等待與結果須同格涵蓋成功／失敗／逾時／服務不可用四段／收斂狀態值須寫明可見表現），皆引用方法論子節不複製條文；回饋事件表按下確認、等待與結果兩列的「反應」欄佔位符補對應填寫提示；第 9 章驗收標準的回饋契約項同步補掛載點與失敗路徑覆蓋兩要求，與模板子節說明保持成對。同批落成的 `component-contract-design` skill 引導層見該 skill CHANGELOG 2.1.0。

**Version**: 1.13.0 — 元件庫規格模板投影上游方法論 1.14.0 的回饋契約升格：逐元件「互動反應」子節改名「回饋契約」（欄位改為事件／觸發者／反應／動畫 token／時間門檻／回饋通道每形態一欄／對應狀態邊／填寫者，六類事件預置列），狀態矩陣新增「來源」「同步策略」兩欄並補 hover（僅指標形態）／pressed 預置列，第 1 章形態因素矩陣的回饋通道展開為事件 × 形態子表，第 2 章狀態綁定模式新增「過渡提示觸發點」「樂觀更新政策」兩列，第 9 章驗收標準同步。模板表本列描述改為「依元件契約欄位表填寫（含回饋契約）」，不再寫死欄位數（DOC-GPD-003 計數處置——集合增減成員時描述不隨之失效）。

**Version**: 1.12.1 — 模板表的元件庫規格模板列由「逐元件九欄位契約」更正為十一欄位。上游方法論 1.11.0 已將契約擴為十一欄（新增操作機制與無障礙），本列的欄位數未同步；模板檔本身早已是十一欄，故此為描述落後於產物，非產物缺欄。

**Version**: 1.12.0 — 元件庫規格模板升為 first-class 文件類型：模板表新增「元件庫規格模板」列（逐元件契約、容器元件排列不變式、禁用對照、豁免清單），使用方式補其 cp 命令，Design System 規格模板列註明其涵蓋範圍為 token 層。模板本身擴為十一欄位契約（語意與內容角色、變體、狀態集、操作機制、尺寸契約、內容政策、slot 契約、組合規則、無障礙、測試契約、反例），形態因素矩陣改為操作方式驅動並補回饋通道、最小命中區、禁放區與安全區。

**Version**: 1.11.0 — 新增 `validate <SPEC-ID>` 子命令：依 frontmatter subdomain 分派章節 schema 驗證（data-contract 驗可攜性邊界原則/A.1-A.6/B.1-B.3/適用判準兩旗標非空；非 data-contract 明確路由 `/spec validate`，exit 0/1/2），對應 `doc_system/commands/validate.py`

**Version**: 1.10.0 — Domain 列表改為指引 `doc domain` 動態查詢，移除他專案（book_overview_app）的 extraction/platform/messaging 等固定清單（違反 framework-asset-separation）

**Version**: 1.9.0 — Domain Map 模板列補 §3 bundle 實作狀態驗證要求（`ls`/`grep` 驗證存在才標「已實作」，PC-APP-012 防護收編自 book_overview_app）

**Version**: 1.8.0 — data-contract 接線 doc create CLI（取代 cp 手動流程，取得自動編號/日期/tracking）+ 新增 `doc next-id` 唯讀查詢子命令

**Version**: 1.7.0 — data contract 升為 first-class 文件類型（五種→六種）：新增 DataContract 列 + data-contract-template 模板 + 使用方式 cp 命令

**Version**: 1.6.0 — domain map 升為 first-class 文件類型（四種→五種）：新增 DomainMap 列 + domain-map-template 模板 + 使用方式 cp 命令；saas 銜接節補「domain map 不因無 saas 而略過」調和說明——非 saas 起手由 version-bootstrap Step 2.5 從 domain-map-template 新建

**Version**: 1.5.0
**Last Updated**: 2026-07-26
