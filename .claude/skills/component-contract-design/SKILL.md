---
name: component-contract-design
description: "從規格狀態表、設計畫布或既有程式碼推導元件目錄，逐元件填齊元件契約欄位表（語意、變體、狀態、操作機制、尺寸、內容政策、slot、組合、無障礙、回饋契約、測試、反例），盤點容器元件與排列不變式，產出讓畫面票只需選件與排位的元件庫規格。觸發詞：元件契約、元件庫設計、元件目錄、容器元件、按鈕重疊、文字溢位、拆解設計稿、接手既有元件庫。Do NOT use for token 萃取（用 foundation-design）或畫面級狀態矩陣（用 ux-design-evaluation）。"
license: MIT
metadata:
  portable: true
  version: 3.0.1
  category: ui-design
---

# Component Contract Design

把「這個畫面該用什麼元件、文字要不要換行、按鈕排在一起會不會重疊」這類決策，從畫面實作期前移到元件庫規格。

**產出**是一份 L3 元件庫規格檔，落在專案的 spec 目錄（L3 指專案元件庫章節；L1 通用原則、L2 語言與框架實作規範，分層見方法論〈分層架構〉）：每個元件的契約欄位表填滿、每個排列關係有容器元件承載。**目的**是讓後續的畫面票只剩「選件與排位」——畫面票寫不成那個形式，就代表契約還有缺口。

## 判準、形狀、程序分屬三處

這三件事各有各的權威來源，本 skill 只是其中之一。把它們搞混會查錯地方，也會把本 skill 當成判準的出處而不去讀方法論。

| 你要找的 | 權威在哪 | 內含 |
|---------|---------|------|
| **判準**——什麼算一個元件、容器為什麼也是元件、契約該有哪些欄位 | 元件庫雙向約束方法論 | 〈元件契約判準〉〈容器亦為元件〉〈命名與通用元件判準〉〈元件文字歸屬〉〈分層架構〉〈形態因素先決〉〈派發語言〉、工程端「禁自製元件」判準、豁免三條件 |
| **形狀**——規格檔長什麼樣、有哪些章節 | `doc` skill 的元件庫規格範本 | 第 1 章形態因素矩陣、第 9 章驗收、檔頭註解的欄位對應表 |
| **程序**——從手上的材料走到契約填滿 | **本 skill** | 下方〈全程〉 |

方法論與範本在本專案的實際路徑見 `references/addresses.md`。

其餘相鄰資產的分工：

| 相鄰資產 | 它管什麼 | 本 skill 管什麼 |
|---------|---------|---------------|
| `foundation-design` skill | 地基入口與路由：token 萃取、接手模式的盤點→命名→固化順序、特徵測試閘門、測試基座 | 消費 token 名；step 2 的路徑 C 沿用其接手順序 |
| `ux-design-evaluation` skill | 畫面級：狀態矩陣、gate、輸入、錯誤恢復、導航；回饋的時間門檻與通知形式由它判斷 | 元件級契約；把同一類失效寫成契約欄位（內容政策、空間不足策略）而非審查項；回饋契約的通道與狀態集的來源由規格撰寫者自行查形態因素矩陣填寫，不重新判斷時間門檻 |
| 執法工具（Dart 專案為 `dart-style-guardian` skill） | 抓裸值與寫死文字、偵測原生元件直用 | 容器缺件——執法工具全綠仍可能重疊，因為它只掃元件層、不掃組合層 |
| `version-bootstrap` skill 地基波 | 編排 i18n → design-system → UX 審查 → 元件庫四塊的順序 | 第三塊之後、第四塊之前的契約產出；第四塊的前置 checkpoint 是〈契約齊全的定義〉 |

## 全程

| 步 | 做什麼 | 在哪 | 產出 |
|----|--------|------|------|
| 起手 | 確認前置、複製範本 | 本檔下一節 | 專案 spec 目錄下一份空的規格檔 |
| 1 | 判定起點（有規格／有畫布／有程式碼） | `references/step-1-pick-starting-point.md` | 起點判定，與另兩者各自的排除理由 |
| 2 | 抽出元件與容器 | `references/step-2-extract-components.md` | 元件候選表、容器候選表 |
| 3 | 逐欄填契約 | `references/step-3-fill-contract.md` | 填滿的元件契約欄位表與排列不變式 |
| 4 | 驗收契約是否齊全（〈契約齊全的定義〉） | `references/step-4-verify-completeness.md` | 齊全判定；未齊全的元件不得被畫面票引用 |
| 5 | 建票與派發 | `references/step-5-write-tickets.md` | 元件票、畫面票、遷移票、決策票 |

兩份隨時可查、不屬任何一步：`references/appendix-examples.md`（各步的範例與正例、卡住時的症狀處置）、`references/addresses.md`（正文出現簡稱而不知它在哪）。

**時序**：本 skill 在 `version-bootstrap` skill 地基波的第 3 塊（UX 審查）之後、第 4 塊（元件庫實作）之前執行，互動反應規格是它的輸入。規劃波前段的 L3 章節得先只含元件清單與禁用對照，契約於此時補齊。

## 起手

**第一個動作是複製範本**：把 `doc` skill 的元件庫規格範本（地址見 `references/addresses.md`）複製到專案的 spec 目錄，檔名依專案 spec 命名慣例（有編號慣例者用編號，範本檔頭的建議檔名只是預設）。實際填寫的是這份副本，不是範本源檔。

專案已有範本格式的元件庫 spec 者不複製，就地補缺的子節；元件清單散在 design-system spec 或其他 spec 者，新建元件庫 spec 並把原清單改為指向它，清單不雙份維護。

複製之前先確認三項前置。缺料的出口一律是建前置票，不在本規格票內補：

| 前置 | 是什麼 | 缺了怎麼辦 |
|------|--------|-----------|
| design token 層 | 集中定義、程式碼只引用名字的樣式參數；顏色、間距、字級、圓角有具名常數 | 走 `foundation-design` skill 的 UI 維度建前置票，設為本規格票的 blockedBy |
| 形態因素矩陣 | 支援哪些形態的決策表，位於範本第 1 章。形態以使用者操作方式界定而非顯示空間：桌機指標加鍵盤、平板觸控、手機單手觸控各為一個形態；判準見方法論〈形態因素先決〉 | 先填範本第 1 章作為提案送簽核——範本的「預設單一形態」是提案值仍須簽核，支援幾個形態屬用戶簽核，PM 不得自行拍板。簽核回覆前，尺寸契約的「每種尺寸」與操作機制標待決，其餘欄照填 |
| 互動反應規格 | UX 審查的產出，是本程序的第三項輸入 | 建 `ux-design-evaluation` skill 的 UX 審查票。**這一項不會擋住你現在做的這張票**，詳見下表 |

互動反應規格缺席時共牽涉三張票。逐票看「誰擋它」，方向就不會弄反：

| 這張票 | 誰擋它 | 這張票該怎麼辦 |
|--------|--------|--------------|
| 本規格票（你現在做的這一張） | **沒有東西擋它** | 照跑到底。填契約時把互動反應子節標「待決」即可 |
| 元件票（step 5 才會建） | **UX 審查票擋它** | 建票時把 UX 審查票填進它的 blockedBy |
| UX 審查票 | — | 互動反應規格未產出時，現在就建它 |

## 判定起點

三種起點互斥，判定結果決定 step 2 走哪一條路徑。

| 起點 | 判定訊號 | step 2 走 |
|------|---------|----------|
| 有規格（狀態矩陣、畫面清單），元件庫尚未建 | spec 有逐畫面狀態表（一張全域狀態清單可拆成逐畫面者亦算）；元件目錄為空或只有 scaffold 產生的無呼叫端佔位元件。spec 有無元件清單不影響判定——清單無契約在三條路徑都是常態 | 路徑 A 規格推導 |
| 有設計畫布（artboard、wireframe 的統稱，設計稿為其輸出物），無規格或規格未到元件層 | 畫布是主要來源；畫面上的視覺單元尚未有語意名。設計工具的自動命名（如 Frame 123）視同未命名；設計工具內已有具名元件但無程式碼者仍走 B，具名視為候選名並經語意問句驗證 | 路徑 B 畫布拆解 |
| 已有元件程式碼與頁面，無契約或契約不全 | 三訊號任一命中即足：元件目錄非空且有呼叫端；頁面直接用原生佈局排列元件；曾發生重疊、截斷、功能補做 | 路徑 C 程式碼萃取 |

**判錯的代價高，且錯誤在後續步驟不會自我暴露。** 最貴的一種是該走 C 而走了 A：產出的契約與存量對不上、既有的重疊與截斷事故對應不到任何缺件、遷移票整批缺失，而規格本身讀起來完好——發現時已在實作期。

三種起點各自判錯的後果、多個訊號同時命中時的處置、以及三個不照表判的例外，都在 `references/step-1-pick-starting-point.md`。起點有任何猶豫先讀它，不要靠上表硬判。

## 用詞

| 詞 | 指什麼 |
|----|--------|
| 元件目錄 | 程式碼中實際存在的元件集合 |
| 元件清單 | 規格第 3 章的總表 |
| 元件庫 | 統一匯出入口涵蓋的目錄；入口外的任何 widget 檔皆屬頁面層 |
| 通用元件 | 已存在、語意涵蓋較廣、能以變體吸收差異的那個元件。「能否併入通用元件」的判準見方法論〈命名與通用元件判準〉 |
| 變體（variant） | 同一語意下行為不同的形態，以語意命名（primary／confirm／danger），不以外觀值命名。本套件內 variant 與變體同義，混用不表示兩件事 |
| 形態 | 使用者操作方式的分類（桌機指標加鍵盤／平板觸控／手機單手觸控），不是視窗寬度。同形態內的視窗尺寸差異由容器的排列不變式承載 |
| **本規格票** | **承載本次元件庫規格產出的那張 ticket，不是規格檔本身。** 中間產出物、提案值、待決項一律寫進它的 Problem Analysis。票號由 PM 於派發 prompt 或 Context Bundle 指定；未指定時停手回報，不自行選票 |

---

版本紀錄在同目錄的 `CHANGELOG.md`。
