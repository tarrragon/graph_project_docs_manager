# Step 2：抽出元件與容器

> **你在 step 2。** 上一步：`step-1-pick-starting-point.md`，你帶著一個起點判定過來。下一步：`step-3-fill-contract.md`。全程表在 `SKILL.md`〈全程〉。
>
> **本檔是入口，不含任何一條路徑的步驟。** 三條路徑各自獨立成檔：路徑 A 規格推導在 `step-2-path-a-spec-derivation.md`、路徑 B 畫布拆解在 `step-2-path-b-canvas-decomposition.md`、路徑 C 程式碼萃取在 `step-2-path-c-code-extraction.md`，三者互斥，你在 `step-1-pick-starting-point.md` 判定的起點決定走哪一份——下方〈這一步在解什麼〉的表格帶去向。本檔留下三路徑共用的內容：中間產出物的形態與落點、共用規則一（存在必要性檢視）、共用規則二（同型歸併），三份路徑檔都會回頭用它們。
>
> **同目錄**：`step-2-path-a-spec-derivation.md`、`step-2-path-b-canvas-decomposition.md`、`step-2-path-c-code-extraction.md`（本檔的三條路徑，見上）、`step-1-pick-starting-point.md`（上一步）、`step-3-fill-contract.md`（下一步）、`addresses.md`（簡稱查地址）。
>
> **簡稱查地址**：本檔出現的方法論、範本、執法工具、判別問句、統一匯出入口、本規格票，地址一律查 `addresses.md`。

## 這一步在解什麼

把手上的材料變成兩張表：**元件候選表**與**容器候選表**。三條路徑的輸入不同、步驟不同，產出的表格形態相同，step 3 一律拿這兩張表去填契約。

三條路徑的差別只在怎麼取得事實，且各自獨立成檔（三者互斥，你在 step 1 判定的起點決定走哪一份）：

| 路徑 | 事實從哪來 | 這條路徑特有的難處 | 在哪份檔 |
|------|-----------|------------------|---------|
| A 規格推導 | 逐畫面狀態表的「顯示」與「可用操作」兩欄 | 只在單一狀態出現的元件容易漏抽；頁面框架不在任何顯示欄裡 | `step-2-path-a-spec-derivation.md` |
| B 畫布拆解 | 畫布上的視覺單元 | 畫布上只有畫素沒有語意，要先命名；且畫布不是檔案系統可及物 | `step-2-path-b-canvas-decomposition.md` |
| C 程式碼萃取 | 既有元件目錄與頁面 | 事實散在呼叫端；存量的不一致是漂移而非設計 | `step-2-path-c-code-extraction.md` |

## 中間產出物的形態與落點（三條路徑共用）

**這三張表不是草稿。** step 4 的齊全判準要拿容器候選表當分母，形態未定該判準即無法機械執行。三者一律寫入**本規格票的 Problem Analysis**，不寫暫存檔——跨 session 會遺失。

| 產出物 | 由哪一步產生 | 欄位 |
|--------|------------|------|
| 元件候選表 | 路徑 A 步驟 2 | 候選名 / 來源（狀態表哪一列的哪一欄、或畫面清單、或畫布）/ 出現的畫面與狀態 / 是否與他候選總是共現。**存在必要性檢視於本表追加「判定」與「理由」兩欄**（合併或不合併、依據哪個語意或行為差異） |
| 容器候選表 | 路徑 A 步驟 4、路徑 C 步驟 4 | 容器候選名 / 所在畫面 / 佈局結構 / 全部子件 / 排列方向 / 空間不足策略（step 3 填定前留空） |
| 排列關係表 | 路徑 B 步驟 4 | 畫面 / 區塊 A / 區塊 B / 排列（並排／堆疊／格線）/ 間距的 token 名 |
| 三態盤點表 | 路徑 C 步驟 1 | 元件名 / 欄位 / 問句 / 三態（已定義於程式碼／隱含於呼叫端／空白）/ 證據（檔案與行，或呼叫端位置） |

三態盤點表另有量級要注意：元件契約欄位表逐欄展開的填寫問句遠多於欄位本身的數量，元件數乘以問句數即本表列數，中型專案的元件數即可讓列數達到另立子節的規模。它與路徑 C 步驟 2 的回收提案值放在一起，使核定者一次看到證據與提案。

## 共用規則一：存在必要性檢視

路徑 A 步驟 3 消費本節；路徑 B 經步驟 5 接回路徑 A 後同樣消費它；路徑 C 用它推得語意與內容角色、變體、反例三欄（這三欄不需程式碼證據）。

**方向是先假設能併，找不到併不進去的理由才立特化元件。** 判準是元件庫雙向約束方法論〈命名與通用元件判準〉的「能否命出設計目的」（路徑見 `addresses.md` 首列）：理由須為**語意或行為差異**（內容角色不同、退出路徑不同），外觀差異不構成理由。

| 差異類型 | 構成拆分理由嗎 | 說明 |
|---------|--------------|------|
| 退出路徑不同 | 是 | 指離開的層級（留在畫面內／離開畫面／離開專案）。只是目的地不同者為 slot，不拆 |
| 內容角色不同 | 是 | 標題一行與內文三行是兩個角色 |
| 外觀不同 | 否 | 降為變體 |
| 語氣不同 | 否 | primary／confirm／danger 是同一語意的不同語氣，變體欄已明訂此命名法。語氣不改變內容角色與退出路徑，據它拆會使每個按鈕語氣各成一個元件 |

實測案例：空狀態與阻擋狀態外觀相近，但空狀態說「這裡目前沒有內容」、阻擋狀態說「這個專案不適用」，退出路徑不同，因此是兩個元件。完整的判斷過程對照見 `appendix-examples.md`〈正例〉。

判定結果寫回元件候選表的「判定」與「理由」兩欄。與 spec 既有清單衝突時以本檢視為準，回填時標「提案」，PM 核定後去標。

## 共用規則二：同型歸併

**歸併鍵是「子件類型加排列方向加空間不足策略」，不是出現的畫面。** 一個按鈕列在七個畫面出現七次仍是一個容器元件；子件從按鈕變成徽章時，若排列方向與空間不足策略相同，仍是同一個容器，slot 的具名子件清單加一項。

歸併分兩次：第一次在 step 2（依子件類型加排列方向，並暫記空間不足策略的假設），第二次在 step 3 填定策略之後（策略相同者再併一次）。

**歸併過頭的訊號**：排列不變式填不出單一值，或 slot 子件清單長到只能寫「任意」。排列不變式的每一項須能以各處實測值直接填出同一個值；各處值不同即為歸併過頭，不得以取較大值、較小值或多數值等方式選一個代表值蓋掉其餘各處。〈策略不一致時，路徑 A 拆、路徑 C 併〉的路徑 C 提案值附各處證據並標待核定，是把選值交給核定者，不屬本條禁止。容器的 slot 子件類型須為具名元件清單——填「任意元件」視同原生佈局，一個接受任意子件的水平堆疊等於加了 token 間距的原生列，判別問句形式上滿足但無約束力。

**範例（歸併過頭：填不出單一值）**：

輸入：兩個排列關係候選（畫面甲的按鈕列、畫面乙的按鈕列），子件類型與排列方向相同；畫面甲最小間距 token 為 `space-8`，畫面乙最小間距 token 為 `space-12`，兩者空間不足策略同為「捲動」。

產出物（決策表）：

| 情形 | 判定 |
|------|------|
| 兩處最小間距 token 不同（`space-8` 與 `space-12`） | 不歸併——排列不變式的最小間距填不出單一值，是歸併過頭的訊號，兩者各自成一個容器 |
| 兩處最小間距 token 相同、僅子件實際內容不同 | 歸併——排列不變式仍可填出單一值 |

驗證（檢驗問句）：

- Q 取兩者較大的 token（`space-12`）強行當代表值、填成單一值，是否合規？　預期：否——代入值須為各處實測值，取較大值屬選代表值蓋掉其中一處
- Q 兩處 token 不同時，正確處置是維持兩個獨立容器候選還是歸併為一個？　預期：維持兩個獨立容器候選

### 策略不一致時，路徑 A 拆、路徑 C 併

**同一個觀察（兩處排列關係的空間不足策略不一樣），在兩條路徑上要做相反的事。** 這不是兩套規則打架，是同一組事實在新建與存量下的語意根本不同——新建的不一致是有人選的，存量的不一致是沒人管而長出來的：

| | 路徑 A（新建） | 路徑 C（存量） |
|---|---|---|
| 策略不一致是什麼 | 執行者當場決定的設計 | 既有程式碼在無契約狀態下各自演化的漂移 |
| 是誰決定的 | 有人決定 | 沒人決定，長出來的 |
| 處置 | **拆**：兩個排列關係選了不同策略，代表設計上就是兩個容器 | **併**：收斂為一個容器候選，策略欄填提案值附各處證據標「待核定」，PM 選定單一策略後回填各處 |

**新建與存量問句**：這個差異是有人決定的，還是沒人決定而長出來的？前者拆，後者併。

**路徑 B（經步驟 5 接回路徑 A 後）**：套用同一問句，但判斷依據限畫布本身可觀察的證據——兩處排列分屬不同 artboard 版本或不同尺寸提案，視為有人決定，比照路徑 A 拆；同一 artboard 內同型排列出現策略不一致，視為畫布產生器的雜訊而非設計決定，比照路徑 C 併，策略欄填提案值並標「待核定」，附兩處畫布截圖或座標為證據。

> 這個問句與路徑 A 步驟 4、路徑 C 步驟 3 消費的方法論〈容器亦為元件〉判別問句（「任兩個元件的相對位置是誰決定的」）**不是同一個，兩者不可互代**。前者判要不要歸併，後者判是不是容器。
