# 路徑 B：畫布拆解

> **你在 step 2 路徑 B（畫布拆解）。** 起點判定為「有設計畫布，無規格或規格未到元件層」時走本檔，判定見 `step-1-pick-starting-point.md`〈判定起點〉。入口與共用規則在 `step-2-extract-components.md`；本路徑步驟 5 接回路徑 A（`step-2-path-a-spec-derivation.md`）步驟 3，走完路徑 A 步驟 3 到 5 才進 `step-3-fill-contract.md`。全程表在 `SKILL.md`〈全程〉。
>
> **同目錄**：`step-2-extract-components.md`（入口，含中間產出物的形態與落點、共用規則一、共用規則二）、`step-2-path-a-spec-derivation.md`（路徑 A，本檔步驟 5 接回該檔）、`step-2-path-c-code-extraction.md`（路徑 C）、`step-3-fill-contract.md`、`addresses.md`（簡稱查地址）。
>
> **簡稱查地址**：本檔出現的 token 表、`foundation-design` skill，地址一律查 `addresses.md`。
>
> 溯源：原屬 `step-2-extract-components.md`〈路徑 B：畫布拆解〉整節，依讀者任務拆出獨立檔。

畫布上只有畫素，沒有語意。本路徑的核心工作是把視覺單元**命名成語意**，命名完就接回路徑 A。

**輸入形態（本路徑獨有的前置）**：三條路徑中只有 B 的第一手輸入不是檔案系統可及物——執行者開不了設計工具的原生檔。可接受的交付物三選一：

| 交付物 | 附帶要求 |
|--------|---------|
| 逐畫面的點陣圖或向量圖匯出檔 | 放進專案可讀路徑；**須另附間距實測清單或標註座標**——步驟 4 要的是間距實測值，圖檔本身量不出來，只給圖檔者視同交付不完整 |
| 設計工具的結構化匯出 | 含區塊名稱與座標的 JSON／CSV |
| 人工撰寫的逐畫面區塊描述 | 每畫面列出區塊、相對位置、間距實測值 |

三者皆無時走前置出口：建畫布交付票並設為本規格票的 blockedBy，**不在本規格票內以想像補畫布**。step 1 判定起點時同此——`SKILL.md` 起點表 B 列的訊號（如設計工具自動命名視同未命名）須在上述交付物上可觀察，觀察不到就不是路徑 B 的合格起點。

```
1 視覺單元盤點  逐畫面框出每個可獨立辨識的區塊；相同外觀的區塊先各自列，不預設同型

2 語意問句      每個區塊問：使用者看到它預期能做什麼？
                它承載標題／內文／標籤／數值／動作哪一種？

3 同型歸併      語意相同者歸併為一個元件，外觀差異降為變體；語意不同而外觀相同者不歸併

4 排列關係盤點  逐畫面記錄區塊之間的排列（並排、堆疊、格線）與間距實測值，產出排列關係表
                （欄位見 `step-2-extract-components.md`〈中間產出物的形態與落點〉）

5 接回路徑 A    專案另有狀態表者，先補跑 `step-2-path-a-spec-derivation.md`（路徑 A）步驟 2 的
                「顯示」欄與「可用操作」欄兩處候選來源（避免漏掉只在單一狀態出現、畫布未畫出的
                元件），再自路徑 A 步驟 3 起；無狀態表者直接自路徑 A 步驟 3 起。路徑 A 步驟 4
                以本路徑步驟 4 的排列關係表代替
```

**範例（視覺單元盤點：框到多細）**：

輸入：畫布上一張卡片，內含一個圖示、一行標題文字、一顆按鈕；三者在畫布圖層樹中皆為卡片的直接子圖層，彼此之間各有間距標註。

產出物（視覺單元候選清單）：

| 候選 | 判定 |
|------|------|
| 卡片（整體） | 列為一個視覺單元候選——它是可獨立辨識的外框 |
| 圖示、標題文字、按鈕 | 各自另列為候選——三者在圖層樹中各自獨立、彼此有間距標註，屬可獨立辨識的區塊 |

本步驟只盤點、各自先列，不預設同型；容器與子件的關係留給後續步驟判定。

驗證（檢驗問句）：

- Q 本步驟是否該只列「卡片」一個候選，略過內部三者？　預期：否——遺漏間距標註會讓步驟 4 排列關係盤點漏記
- Q 本步驟是否該當下判定卡片與內部三者的容器子件關係？　預期：否——容器子件判定留待後續步驟

**同型歸併（步驟 3）的反向陷阱**：畫布產生器常給語意不同的單元相同外觀（同一個圓角卡片放專案摘要也放錯誤訊息）。以外觀歸併會把兩種語意塞進一個元件，之後靠參數分岔。判斷用語意問句（步驟 2）的答案，不用畫素。

**排列關係盤點（步驟 4）與 token 的關係**：間距實測值歸納為 token 階屬 token 萃取（`foundation-design` skill 接手模式的「盤點 → 命名」與其「萃取不等於照抄」案例），本步驟只記錄「哪兩個區塊之間有間距」，值引用已萃取的 token 名；token 未萃取時停下走前置。

**畫布漂移**：畫布為準**只限視覺值**（圓角、間距、字級、顏色）——規格寫圓角 10、畫布量到 7，以畫布為準並更新規格，畫布是視覺決策的產物。行為、狀態、退出路徑的差異（規格有「切換專案」動作而畫布沒畫）以**規格**為準，標漂移待決送 PM，不從規格刪除。契約填寫時引用 token 名而非數值，視覺漂移只需改 token 表一處。

**範例（畫布漂移：排列順序算不算視覺值）**：

輸入：規格文字「操作區由左至右依序為取消、確認」；畫布上同一操作區的按鈕由左至右依序為確認、取消，與規格相反。

產出物（決策表）：

| 差異項 | 判定 |
|--------|------|
| 按鈕的視覺排列順序（左右位置） | 以規格為準，標漂移待決送 PM——排列順序牽動預設焦點與鍵盤導覽先後，屬行為差異，不在「畫布為準只限視覺值（圓角、間距、字級、顏色）」清單內 |

驗證（檢驗問句）：

- Q 按鈕排列順序是否屬於「畫布為準只限」的視覺值清單之一？　預期：否
- Q 本例該以畫布覆蓋規格，還是以規格為準並標漂移待決？　預期：以規格為準並標漂移待決，不從規格刪除

**走完之後**：路徑 B 不直接進 step 3，它在步驟 5 接回路徑 A，走完路徑 A 步驟 3 到 5 才進 `step-3-fill-contract.md`。
