# Step 3 條件式附錄：路徑 C 專屬與多形態專案

> **你在 step 3 的條件式附錄。** 只有兩種讀者需要本檔：step 2 走路徑 C（程式碼萃取）的讀者，與專案支援多個形態的讀者——兩者互不相依，各讀各自需要的一節即可。入口在 `step-3-fill-contract.md`，本檔的兩節都是從那裡讀完主體後才轉來的條件式內容。全程表在 `SKILL.md`〈全程〉。
>
> **同目錄**：`step-3-fill-contract.md`（入口，元件契約欄位填寫問句、回饋契約的填寫順序、容器元件另答排列不變式三項皆在該檔）、`step-2-extract-components.md`（上一步入口，其〈共用規則二：同型歸併〉為填定策略後的回頭處）、`step-2-path-c-code-extraction.md`（路徑 C，本檔第一節專屬其讀者）、`step-4-verify-completeness.md`（下一步）、`addresses.md`（簡稱查地址）。
>
> **簡稱查地址**：本檔出現的方法論、本規格票，地址一律查 `addresses.md`。
>
> 溯源：兩節原屬 `step-3-fill-contract.md`〈路徑 C 專屬：證據順序與漂移判定〉〈多形態專案〉，依讀者任務拆出獨立檔——兩節分屬不同的少數讀者群，此前併在入口檔會讓多數讀者（不走路徑 C 且單一形態）多讀兩節用不到的內容。

## 路徑 C 專屬：證據順序與漂移判定

**只有 step 2 走路徑 C（程式碼萃取）的讀者需要本節**，路徑 A 與 B 跳過。

行為類欄位（狀態集、內容政策、尺寸契約、組合規則）的值以既有測試與呼叫端行為為證據，無證據者標「待決」並建決策票，不猜。語意與內容角色、變體、反例三欄由存在必要性檢視推得，不需程式碼證據。

**證據順序**（衝突時取序位在前者）：

> 既有測試 ＞ 元件自身實作 ＞ 呼叫端一致的設定 ＞ spec 的結構化表格 ＞ 畫布 ＞ 規格散文

既有測試常是原作者意圖唯一的書面殘留；規格散文最容易與程式碼漂移。

**元件實作與 spec 表格衝突時**（程式碼尺寸 64 而 spec 寫 36）：

| 情形 | 怎麼填 |
|------|-------|
| 一般情形 | 行為類欄位以實作加測試為準填入，標「spec 漂移」 |
| spec 為近期且有決策記錄 | 反過來標「實作漂移」 |

**「近期」的界線**：spec 決策記錄的日期晚於該元件實作最後一次修改（以既有測試或程式碼的版本控制歷史為準）即為近期；決策記錄日期早於或等於實作最後修改，視為一般情形。

兩者都由 PM 於驗收核定，**執行者不自行選邊**。

**契約值不變的實作偏離不走漂移判定**：型別撞名改名、套件版本限制的規避、等價轉換這三類，改的是實作方式而不是契約值，填範本的〈實作註記〉即可。

> 判別問句：**改完之後，元件契約欄位表有任何一欄的值不同了嗎？** 沒有就是實作註記，有就走漂移判定。

沒有這一條時，本步只有「待決」與「漂移」兩態，這類偏離無處可歸而被歸入漂移，會為一個沒有分歧的情況觸發 PM 裁定。

## 多形態專案

支援多個形態時，畫布拆解與容器盤點按形態分別進行（每形態一套 artboard、一份排列關係表），元件候選與存在必要性檢視跨形態合一；畫面票的 how 帶形態前綴。

**元件契約欄位跨形態的共用範圍**：

| 分組 | 欄位 |
|------|------|
| 跨形態共用 | 語意與內容角色、變體、狀態集、slot 契約、反例、無障礙（朗讀標籤）、回饋契約（事件與對應狀態邊） |
| 每形態各一份 | 操作機制、尺寸契約、內容政策、組合規則、測試契約、無障礙（操作路徑）、回饋契約（通道）、容器條目 |

**形態因素矩陣的每形態填一次維度**（填欄位時引用）：

| 維度 | 內容 |
|------|------|
| 覆蓋表面對映 | bottom sheet、drawer、dialog、popover 等覆蓋表面依此對映選件，元件的組合規則引用該對映 |
| 回饋通道 | 手機按鈕被手指遮住，須震動或音效；桌機視覺即可 |
| 最小命中區 token | 可點元件的最小命中尺寸依此 token |
| 禁放區與安全區 | 手機四角與系統手勢區不放可點元件，SafeArea 由框架容器承載 |

同一語意可落在不同容器與不同操作機制：導覽項共用，承載它的容器在手機是收合抽屜、在平板與桌機是常駐側欄；數值調整在桌機是拖動滑桿、在手機是步進或輸入彈窗。

**同形態內不依寬度自動切版**：桌機常駐側欄的版型靠視窗尺寸下界與比例約束維持。設計上刻意給使用者選版型（雙欄／單欄）時，那是設定項不是斷點。同形態內的視窗或螢幕尺寸差異不是新形態，由容器的排列不變式承載——把它判為「不是新形態」也是關機判定，要記下該差異的尺寸範圍。「自動切版」指依寬度改換容器所用的元件契約（如替換為不同元件）；容器既有的空間不足策略（換行、捲動、收合擇一，見 `step-3-fill-contract.md`〈元件契約欄位填寫問句〉空間不足策略列）依寬度觸發不算切版，仍為同一容器同一組契約。

**範例（側欄收合算不算依寬度自動切版）**：

輸入：單一形態（桌機）內，主畫面在視窗寬度低於 1024px 時，常駐側欄自動收合為圖示列，寬度回升時恢復展開；形態因素矩陣未另外定義「窄桌機」這個新形態。

產出物（決策表）：

| 情形 | 判定 |
|------|------|
| 側欄依視窗寬度收合為圖示列，收合後仍為同一容器、同一組元件契約 | 合法——這是排列不變式的空間不足策略（收合），不是新形態 |
| 因寬度不同改用不同的元件組合（如窄桌機改用另一個元件取代常駐側欄） | 違規——依寬度自動切版，須改為使用者刻意選擇的設定項，或另立形態 |

驗證（Given/When/Then）：

- Given 視窗寬度降至 1024px 以下
- When 頁面重新渲染
- Then 側欄容器仍為同一元件，只是空間不足策略從並排展開換成收合為圖示列
- And 未觸發新形態的容器或元件替換
