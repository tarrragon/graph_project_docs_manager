# 引用穩定性規則

> **本檔承載原 `.claude/rules/core/document-format-rules.md` 規則 7-8**。規則 1-6、YAML frontmatter、檢查清單骨架仍保留在 document-format-rules.md 自動載入。

本文件定義「引用穩定性」相關規範：規格文件的引用穩定性（規則 7）、框架文件對專案層級識別符的引用禁制（規則 8）、環境事實的記錄形式（規則 9）、可變計數的實例化禁制（規則 10）、行號作為定位符的禁制（規則 11）。五者共享「哪些來源可信、哪些引用會失效」的核心主題，差別在失效軸——規則 7 是時間（臨時文件會消失），規則 8 是跨專案 sync（專案識別符在他專案無意義），規則 9 是跨環境（機器專屬路徑換機後解析不到），規則 10 與規則 11 是跨版本（集合本身的演進使寫死的衍生值失效：前者是集合大小，後者是段落位置）。

---

## 適用情境（何時需要讀取本檔）

| 情境 | 涉及規則 |
|------|---------|
| 編輯 `docs/spec/`、`docs/use-cases.md`、`docs/proposals/` 等規格文件 | 規則 7 |
| 編輯 `.claude/` 框架檔案（rules / pm-rules / references / methodologies / agents / skills / hooks / error-patterns / best-practices） | 規則 8 |
| Code review 時檢查跨檔案引用是否指向穩定來源 | 規則 7、規則 8 |
| 在 `.claude/` 框架檔案寫「當條件成立時該做 X」（含量化閾值），想綁 ticket ID 作 trigger | 規則 8 |
| 升級 memory 或 ticket 內容至框架層時評估引用穩定度 | 規則 7、規則 8 |
| 記錄環境實測結果（路徑、HOME、目錄結構）於任何文件或 ticket | 規則 9 |
| 收到 ticket create / claim 的機器專屬路徑 WARNING | 規則 9 |
| 校正既有的機器專屬絕對路徑殘留 | 規則 9 |
| 文件敘述描述一個會演進的集合大小（欄位數、檔案數、條文數、步驟數） | 規則 10 |
| 為表格、分組、章節以成員數命名（如「N 欄位表」「三欄制」） | 規則 10 |
| 在票面、規格或框架文件中指向另一份文件或 code 的某一段 | 規則 11 |
| 為延後決策寫 trigger，其成立條件指向某份文件的某個段落 | 規則 11（與 `decision-trigger-binding` 規則 2 併看） |

---

## 規則 7：規格文件引用穩定性

**規格文件（docs/spec/、docs/use-cases.md）禁止引用臨時性文件**

| 文件類型 | 穩定性 | 可被規格引用 |
|---------|--------|------------|
| 規格文件（docs/spec/） | 穩定 | 是 |
| Use Cases（docs/use-cases.md） | 穩定 | 是 |
| 提案文件（docs/proposals/） | 穩定 | 是 |
| CLAUDE.md | 穩定 | 是 |
| Worklog（docs/work-logs/） | 臨時 | **否** |
| Ticket 檔案 | 臨時 | **否** |
| Plan 檔案 | 臨時 | **否** |

**允許的例外**：
- 變更歷史中記錄 Ticket ID 作為「來源標注」（如：「本次變更由 0.17.0-W1-002 執行」）
- 這是記錄歷史事實，不是建立依賴

**禁止的模式**：

| 禁止 | 原因 | 正確做法 |
|------|------|---------|
| `詳見 v0.17.1 W1-001` | Ticket 可能被遷移、刪除、重新編號 | `待定義於匯出規格章節` 或直接在此定義 |
| `參考 docs/work-logs/v0.17/...` | Worklog 會被歸檔，路徑可能失效 | 引用對應的 spec 文件 |
| `依 0.18.0-W2-001 的分析結論` | 結論應提煉為規格，而非依賴分析過程 | 將結論寫入規格文件 |

**理由**：
- 規格文件是開發的**唯一穩定依據**，worklog 和 ticket 是**過程記錄**
- 過程記錄可能遺失、修改、遷移，規格文件不應依賴不穩定來源
- 如果規格需要引用的資訊尚未定義，標記為「待定義」而非指向臨時文件

---

## 規則 8：框架文件禁止引用專案層級識別符

**`.claude/` 框架文件禁止引用專案特定的 ticket ID、worklog 路徑等專案層級識別符（commit hash 條件允許，見下「實證錨點型」章節）**

`.claude/` 是**跨專案共用框架**，透過 sync 機制同步到多個專案（如 `.claude/skills/sync-push`、`sync-pull`）。專案層級識別符只存在於當前專案，sync 到其他專案後會變成死連結和誤導性資訊。

| 識別符類型 | 範例 | 可在框架文件引用？ |
|---------|------|-----------------|
| 專案 ticket ID | `0.18.0-W5-001`、`W2-007` | **否** |
| 專案 commit hash | `8f74d08`、`abc1234` | **條件允許**（實證錨點型，見下「實證錨點型：commit hash 的條件允許」章節） |
| 專案 worklog 路徑 | `docs/work-logs/v0.17/...` | **否** |
| 專案 proposals ID | `PROP-007` | **否**（除非已提煉為方法論） |
| 框架 error-pattern ID，**已到達共用通道** | `PC-050`、`IMP-003`、`ARCH-002` | **是**（已隨同步送達各 consumer，引用可解析） |
| 框架 error-pattern ID，**尚未到達共用通道** | 本地新建而尚未推上 canonical 的任何編號，含 consumer 分域形式 `PC-<CONSUMER>-0NN` | **否**（僅本地票面可引；推上共用通道後才可寫進會擴散的框架檔案） |
| Claude Code 版本號 | `CC 2.1.97` | **是**（外部平台識別） |
| 框架檔案路徑 | `.claude/rules/core/pm-role.md` | **是**（框架內部結構） |

**error-pattern ID 那兩列的判準是送達與否，不是歸屬哪一層。** 早期本表只有一列「框架 error-pattern ID → 是（框架內部分類）」，判準綁在識別符的**類別**上。該寫法早於 consumer 分域編號（`PC-<CONSUMER>-0NN` 形式），未隨之更新，照其字面會把一個本地剛建、尚未推上共用通道的分域編號判為可引。

**Why**：引用能否解析，取決於被引物是否已經到達讀者手上，而不是它屬於哪個命名空間。一個只存在於本專案的編號，在本專案內部完全有效；一旦寫進會經共用通道擴散的檔案，落到其他 consumer 手上就是懸空引用。

**Consequence**：這個誤判有一個讓人放行的理由——「我確認過它真的存在」。存在性查證會產生已盡責的感覺，而存在性恰好不是判準。寫的人查證過，不會覺得自己漏了什麼。

**Action**：寫下一個他處編號時，問「**這個檔案會傳到哪些地方，那些地方有沒有這個編號**」，而不是問「這個編號是真的嗎」或「它屬於哪一層」。三態對照與完整判準見 `.claude/error-patterns/process-compliance/PC-GPD-006-self-built-dir-copied-across-consumers-carries-foreign-ids.md` 的〈反方向：把引用寫出去〉節；本表與該節為同一判準的兩處落點，修改任一處須同步另一處。

**「共用層已建但未推」同樣不可引**：判準是送達與否，不是歸屬哪一層。一個放在共用層、命名也符合共用慣例的新編號，在推出去之前對其他 consumer 一樣查無此檔，而那個時間窗正是新編號最常被引用的時候。

**適用範圍**（與守衛 `reference-stability-rule8-guard-hook.py` 的 `SCAN_PREFIX` 判準一致，不逐目錄列舉）：

`.claude/` 全域（含所有現有與未來新增的子目錄，如 `.claude/config/`、`.claude/lib/`）皆適用規則 8；唯一豁免路徑前綴為 `.claude/handoff/archive/`（歷史紀錄，合理保留原樣）。此描述對齊守衛程式碼的 `SCAN_PREFIX = ".claude/"` + `EXEMPT_DIR_PREFIX = ".claude/handoff/archive/"` 邏輯——新增子目錄自動落入規則 8 範圍，不需回頭補表，一致性不依賴人工逐目錄同步。

| 範圍 | 適用規則 8？ | 備註 |
|------|-------------|------|
| `.claude/` 下所有子目錄與檔案（含 `rules/`、`pm-rules/`、`references/`、`methodologies/`、`agents/`、`skills/`、`best-practices/`、`config/`、`lib/` 等） | 是 | 全域判準，本列為說明範例非窮舉，新增目錄自動涵蓋 |
| `.claude/error-patterns/` 內容（檔名除外） | 是 | 檔名本身為框架內部分類（見上「識別符類型表」），非本規則的豁免對象 |
| `.claude/hooks/` Python docstring/comment | 是 | 同上，全域規則的一部分，非獨立子範圍 |
| `.claude/handoff/archive/` | **否**（唯一豁免路徑前綴） | 歷史紀錄，合理保留 |
| 專案 `docs/` | **否**（不在 `.claude/` 下） | 專案內部，可引用 |
| `CLAUDE.md` | **否**（不在 `.claude/` 下） | 專案入口，可引用 |

**Memory 和 ticket 也是專案層級**：

用戶/專案 auto-memory（`~/.claude/projects/<project>/memory/`）和 ticket（`docs/work-logs/`）**都不會跨專案 sync**，所以「把原則寫到 memory」不能取代框架規則。需要跨專案落實的原則必須寫入：
- `.claude/rules/`（自動載入規則）
- `.claude/error-patterns/`（錯誤學習經驗）
- `.claude/methodologies/`（方法論）

**禁止的模式**：

| 禁止 | 五類分類（見下「引用性質判準」） | 改為 |
|------|------|------|
| `（來源：0.18.0-W4-002）` | 論證依據型 | `（防範 Hook error 干擾代理人判斷）` |
| `（W5-021 教訓）` | 論證依據型 | `（多代理人 permissionMode 批次修復教訓）` |
| `**Ticket**: 0.17.3-W12-001`（在 error-pattern 內） | 論證依據型 | 移除整行（檔名 PC-XXX 已是足夠識別） |
| `來源：PROP-007` | 論證依據型 | 以提案內容的抽象描述取代 |
| `# 遷移後此處改用 X（W-xxx）` | 時點標注型 | `# 遷移後此處改用 X`；需要時間資訊時改標日期 |
| `W-xxx 事件中用戶發現...`（案例敘事） | 案例敘事主詞型 | 改描述性標籤，如「某次全域物件缺失崩潰事件中用戶發現...」 |

**允許的例外**：

| 例外 | 說明 |
|------|------|
| error-pattern 檔名本身 | `PC-050-premature-agent-completion-judgment.md` 是框架內部分類 |
| 觸發日期 | 「2026-04-12 新增」可保留，日期不是專案識別符 |
| 通用 CC 能力版本號 | 「CC 2.1.97 新增 /agents 分頁」屬外部平台能力 |
| 被說明對象型 ticket ID（白名單路徑） | 見「引用性質判準」第 4 類，如 `ticket-id-conventions.md` 中作格式範例展示的 ID |
| 測試資料型 ticket ID（測試路徑內） | 見「引用性質判準」第 5 類，測試檔內作為被測資料的 ID |
| 實證錨點型 commit hash（`error-patterns/` 實證段落，敘事自足） | 見下「實證錨點型：commit hash 的條件允許」章節。**注意**：此為 commit hash 專屬的獨立判準，非上方 ticket ID 五類分類的第六類 |

**理由**：
- `.claude/` 經 sync 跨專案共用，專案識別符在其他專案是死連結
- 框架文件的價值在於**抽象原則**，專案引用是耦合而非依賴
- Memory/ticket 也是專案內部，不能承擔跨專案原則的傳遞責任

### 引用性質判準：全禁原則與五類分類

> **本章節定位**：規則 8 上文是「載體 + 識別符類型」維度的禁制（哪些檔案、哪類識別符）。本章節定義「引用性質」維度的判準——ticket ID 在框架文件中的任何出現，除落入下方兩個機械可判定的豁免類別，一律禁止（全禁原則）。本章節取代原「依賴型 / 歷史錨點型」兩類判準，對應關係見章末「與舊兩類判準的對應」。**權威關係**：`.claude/agents/AGENT_PRELOAD.md` 規則 12 是本章節的速查版，供代理人主動 Read 改框架檔案的規則時查表；兩者判準內容如有出入，一律以本章節（規則 8）為準，速查版須修正對齊，不得反向要求本章節開豁免遷就速查版舊表述（曾有分析誤依速查版的舊兩類判準下結論，反向建議本章節加豁免註，方向已反轉並修正速查版）。**校準（實測後）**：規則 12 並非自動載入——`.claude/agents/*.md` 主文的 `@-import` 已實測不會展開為內容，AGENT_PRELOAD.md 不會送達 subagent context；每次派發都會注入的觸發指引在 `.claude/rules/core/document-format-rules.md`「引用穩定性規則」小節，該節指引代理人改框架檔案時主動 Read 本章節。

**Why**：框架檔案禁止以 ticket ID 承載推理依據——ticket 是過程記錄載體，只存在於當前專案，sync 到其他專案後查無此票。簡單理由應直接寫進註解，重要邏輯概念應先提煉為方法論再引用方法論路徑，兩者皆不依賴 ticket ID 即可被讀者理解。舊兩類判準的操作依據「移除 ID 後能否理解」需逐句語意判斷，守衛只能退而偵測句型（如「詳見」「來源：」），必然漏掉不用該句型的依賴型引用。

**Consequence**：語意判準的執行落差會隨 session 數與代理人數累積——不同代理人對同一句型可能給出不同分類，守衛偵測不到的漏網引用持續存在。且未命名的引用型態（測試 fixture、格式說明文件的被說明對象、案例敘事主詞）若未先分類就套用全禁，機械守衛初次執行即產生大量誤報；依 ARCH-016 失效模式，該守衛會在誤報壓力下被停用或加旁路，全禁等於沒做。

**Action**：改採全禁為預設，僅開放兩個路徑可機械判定的豁免類別（被說明對象型、測試資料型）；其餘引用一律視為需移除，依五類分類表決定移除後的具體處置。

#### 五類分類

| # | 類別 | 判準（移除 ID 後的可理解性測試） | 處置 |
|---|------|------|------|
| 1 | 論證依據型 | 移除 ID 後句子語意不完整，讀者需查該 ticket 才能理解本句 | 禁止：移除。依兩層分流補寫理由（見下） |
| 2 | 時點標注型 | 移除 ID 後語意完整，ID 僅標「何時改的」，不提供額外理解 | 禁止：移除。需要時間資訊時改用日期（規則 8 主表已允許日期） |
| 3 | 案例敘事主詞型 | ID 作為案例 / 事件的識別標籤（如「W-xxx 事件中...」），功能是命名而非導向查證 | 改描述性標籤（如「某次全域物件缺失崩潰事件」），標籤本身須不依賴 ID 即可理解 |
| 4 | 被說明對象型 | ID 是文件說明的主題本身（文件職責即展示 / 定義 ticket ID 格式），非指向其他內容的引用 | 豁免：限白名單路徑（見下） |
| 5 | 測試資料型 | ID 出現於測試檔案作為輸入值或斷言值，等同字串字面量 | 豁免：限測試路徑（見下） |

第 1-3 類判準延續舊判準的操作方法（移除測試），差異在於：第 2 類（原可保留的歷史錨點型）現改為禁止——理由已寫出時 ID 不提供額外資訊，需要時間資訊則日期即可完成同一功能且跨專案有意義；第 3 類（案例敘事主詞型）為舊判準未命名的型態，實測顯示其為 error-patterns 類文件的最大宗引用型態。

#### 機械可判定性：守衛只需判定豁免

前三類（論證依據型、時點標注型、案例敘事主詞型）處置雖不同——依序為「移除且依兩層分流補寫理由」「移除改日期」「改描述性標籤」——但**共同前提都是移除該 ticket ID**，差異僅在移除後補什麼，不影響「是否該移除」的判斷。

因此守衛不需逐句語意判斷屬於第幾類，只需判定一件事：**該 ticket ID 出現位置是否落在第 4 類或第 5 類的豁免路徑**。豁免路徑判定是路徑字串比對，不涉及語意理解，可完全機械化：

| 判定步驟 | 機制 | 是否需要語意理解 |
|---------|------|----------------|
| 1. 樣式偵測 | 命中 ticket ID 樣式（版本化或裸格式）即為候選 | 否，純正則 |
| 2. 豁免判定 | 候選所在檔案路徑符合第 4 類白名單或第 5 類測試路徑樣式 | 否，路徑字串比對 |
| 3. 判定結果 | 未落入豁免 → 觸發告警（不需再細分第幾類，處置皆為「移除」） | 否 |

守衛偵測規則與路徑樣式的具體實作獨立追蹤，不在本文件重複。

#### 守衛的實際執法邊界

以下為守衛實測行為。載明的理由：撰寫者依前述判準得知「該不該寫」，但不知道「合法的寫法有哪些」與「哪些管道其實沒被擋」——兩者只能撞牆後從錯誤訊息學到，或永遠不知道。

**三類豁免**（任一成立即放行，不需 marker）：

| 豁免 | 範圍 | 用途 |
|------|------|------|
| 白名單路徑 | `.claude/references/ticket-id-conventions.md` 一個檔案 | 第 4 類（被說明對象型）：文件職責即定義 ticket ID 格式 |
| 測試路徑 | `/tests/`、`test_*.py`、`*_test.py` | 第 5 類（測試資料型）：測資需真實 ID 形態 |
| code fence 內容 | markdown 的 ```` ``` ```` 區塊 | **格式示範的設計上合法路徑**，非繞過守衛。示範 ID 形態時應置於 fence 內 |

另有目錄豁免 `.claude/handoff/archive/`（歷史歸檔不再被消費）。

**逃生閥**：行內 marker `rule8-exempt: <category>:<reason>`，同行或下一行皆可，逐行生效非整檔放行。合法 category 有三個——`testdata`（守衛或工具自身 self-test 需真實 ID 形態）、`illustration`（規則正文示範 ID 形態且不便置於 code fence）、`relocation`（既有內容的逐字位置搬移，reason 須含來源檔路徑，形如「自 `<來源檔路徑>` 逐字搬移」）。category 不在清單內、reason 為空，或 `relocation` 類別的 reason 缺來源檔路徑，視為格式錯誤仍阻擋。

**`relocation` 類別的適用邊界**：僅限既有內容的**逐字**位置搬移——搬移後內容與來源段落逐字一致，可用 diff 直接比對。內容經任何編修（改寫、精簡、補充、格式調整）即不再適用此類別，須改走一般規則 8 判準（依五類分類逐句判定，見「引用性質判準」章節）。**Why**：本類別的可驗證性正建立在「移除來源後仍可用 diff 復原/核對」這一機械可判定基準上；一旦內容被編修，diff 比對即失去意義，此時該引用的合法性只能回到語意判準（是否落入第 1-5 類），與其他豁免類別一視同仁，不能沿用 `relocation` 的免判定待遇。**Action**：搬移前若已知會順帶編修內容，不要用 `relocation` marker；改用移除 ID 並依「兩層分流指引」處理（見前述第 1 類論證依據型的處置路徑）。

**判定為增量而非全檔**：守衛比對編輯前後的 ticket ID 集合差集，只擋新增。既有存量不重複觸發——這代表**規則 8 對存量是應然目標而非現況**（2026-08-11 量測：1607 個受掃描框架檔中 714 檔含票號，不重複票號 3584 個）。

**未涵蓋的寫入管道**（2026-08-11 實測）：

| 管道 | 是否涵蓋 |
|------|---------|
| Claude 的 Edit / Write / MultiEdit | 是（settings.json 對三個 matcher 各註冊一次） |
| Claude 的 Bash 寫入（`sed -i`、heredoc 重導向、python 腳本） | 否 |
| 用戶自行編輯 | 否（PreToolUse 只攔 Claude 的工具呼叫，此為機制邊界不可由 hook 補） |

**Consequence**：讀者若把守衛當成「框架檔案內的票號一律擋得住」，會在上述兩條管道上寫入而不自知，且不留痕跡。**Action**：走非 Edit 管道修改 `.claude/` 時，本規則的遵守回到撰寫者自律；commit 層的補位檢查另行追蹤。

**早期裁定的例外已廢止**：曾有裁定認為「Version footer 中的 ticket ID 允許保留」（理由為句子自足）。該例外已於 `AGENT_PRELOAD.md` 規則 12 v1.20.0 明文移除，現行口徑為全禁——第 2 類（時點標注型）一律移除，需要時間資訊改標日期。**本節與規則 8 正文為唯一權威口徑**，早期 ticket 內的裁定總結不再適用。

#### 兩層分流指引：簡單理由寫進註解，重要概念先寫方法論再引用

第 1 類（論證依據型）移除 ticket ID 後，原本承載的推理依據不可憑空消失，須依內容重量分流：

| 分流 | 適用情境 | 做法 |
|------|---------|------|
| 簡單理由 → 寫進註解 | 一兩句話可交代清楚，理由只在此處成立，不需外部追溯 | 直接在引用處內聯改寫為自足 WHY（範例見下「框架 code 邏輯註解的識別符衛生」） |
| 重要邏輯概念 → 先寫方法論再引用方法論 | 概念可被複用（跨檔案 / 跨情境皆適用的判準、決策框架、設計原則） | 先在 `.claude/methodologies/` 建立或擴充對應方法論，原引用處改為指向方法論路徑 |

**判別準則**：問「這段推理依據只在這一處成立，還是換個情境也適用？」只在此處成立 → 寫進註解；換情境也適用 → 先寫方法論。「不知道該寫註解還是寫方法論」本身即訊號——寫不進一兩句註解，通常代表概念重要到該提煉為方法論。

#### 第 4 類白名單（被說明對象型）

| 路徑 | 符合原因 |
|------|---------|
| `.claude/references/ticket-id-conventions.md` | 文件職責即定義 ticket ID 格式規範，文中出現的 ID 是被說明的格式範例本身，非指向其他內容的引用 |

**成員上限**：本類白名單維持不超過 3 個檔案。若後續擴張超過 3 個檔案，改採可陳述的通用條件（如「文件主體目的即為展示或定義 ticket ID 格式」）取代逐檔列舉，避免走向與 ARCH-BAL-003 相同的成員清單漂移問題。

#### 第 5 類豁免路徑（測試資料型）

路徑滿足以下任一條件即豁免：

| 條件 | 範例 |
|------|------|
| 路徑含 `/tests/` 目錄區段 | `.claude/hooks/tests/test_xxx.py` |
| 檔名符合 `test_*.py` 或 `*_test.py` 樣式 | `test_ticket_builder.py` |

具體正則與守衛整合實作獨立追蹤，不在本文件定義。

#### 判準單位：整句 / 整個引用事件

同一句或同一括號內常同時出現多個 ticket ID（如「來源：A、B 實證」）。逐 ID 個別判定會出現同句一半判「移除」、一半判「語意完整」的破碎結果，難以據以修改。移除測試以「整句 / 整個引用事件」為單位——句中任一 ID 已提供足以理解本句的說明即整句視為語意完整（進入第 2 / 3 類判斷，ID 仍須移除，差別在改寫難度）；句中完全無 ID 帶說明才整句視為第 1 類。

#### 內聯說明的實質性判準：短標籤 vs 實質說明

「括號內是否有說明」若只看「有無括號」會誤判——`W10-011（擴充註解規則）` 的括號只是主題標籤，複述標題，並未交代 W10-011 實際的規則內容，讀者仍須開啟該 ticket 才能理解，應判第 1 類；而「（worktree 隔離對 daemon-rooted dart MCP 寫入工具不生效）」的括號是完整子句，直接陳述具體事實，讀者不需開啟該 ticket，屬語意完整（進入第 2 / 3 類判斷）。判定括號內容時追加子測試——「括號內容本身是否已陳述具體事實 / 機制（一個完整子句），而非僅複述主題或標題」：

| 括號內容 | 判定 |
|---------|------|
| 複述主題 / 標題（名詞短語，未陳述事實） | 視同無說明，第 1 類 |
| 陳述具體事實 / 機制（完整子句） | 視為有效內聯說明，進入第 2 / 3 類判斷；但第 2 / 3 類本身仍禁止 ID 留存，僅差在「內容已交代清楚」影響改寫難度而非是否可保留 ID |

**與「相關文件」章節列舉的邊界**：「相關文件」章節列 `W10-011（擴充註解規則）` 判第 1 類，原因有二疊加——括號僅主題標籤，且「相關文件」章節本身的存在目的就是列出「需要時去查」的清單，脈絡本身已是指向動作。即使補上實質說明，仍建議改用檔案路徑（規則 8 主表允許框架檔案路徑）而非 ticket ID，以維持該清單「可直接點開」的操作價值。

#### 正反範例

第 1-3 類（禁止，一律移除）：

| 引用 | 類別 | 移除測試 | 修正 |
|------|------|---------|------|
| `詳見 W15-005 WRAP 方案 F` | 第 1 類 | 「詳見...方案 F」指向空 → 無法理解 | 內聯具體內容，或指向對應方法論 / SKILL 章節 |
| 「相關文件」章節列 `W10-011（擴充註解規則）` | 第 1 類 | 括號僅主題標籤，非事實子句 | 改檔案路徑（如對應 methodology 路徑） |
| `# 依 W-xxx 的分析，這裡要先檢查 None 再 dispatch` | 第 1 類 | 移除後不知為何要檢查 | 內聯自足 WHY：`# payload 為 None 時 dispatch 會拋 AttributeError，先擋掉` |
| `# 遷移後此處改用 globalThis（W-xxx）` | 第 2 類 | 移除後「遷移後此處改用 globalThis」仍完整，ID 僅標時點 | 移除 ID；需要時間資訊改標日期：`# 2026-04-12 遷移後此處改用 globalThis` |
| `W10-134 第二輪 linux 結論建議`（案例敘事） | 第 3 類 | ID 作案例識別標籤，移除後「第二輪」失去指涉 | 改描述性標籤：「效能守衛短路事件第二輪結論建議」 |

第 4-5 類（豁免，保留）：

| 引用 | 類別 | 判定 |
|------|------|------|
| `references/ticket-id-conventions.md` 內 `0.31.0-W3-001.md` 格式範例 | 第 4 類 | 文件職責即展示 ID 格式，ID 是被說明對象，白名單路徑豁免 |
| `test_ticket_builder.py` 內斷言 `assert parse("0.1.0-W3-001") == ...` | 第 5 類 | ID 為被測資料，測試路徑豁免 |

#### 三方對齊（更新）

原兩類判準曾以「歷史錨點型允許保留」統一規則 8 與規則 7 來源標注例外、PC-093 `history` 豁免、comment-writing 方法論三個既有機制。全禁原則下，框架文件（含框架 code comment）不再有「允許保留 ticket ID」的分支，三方對齊改為：

| 機制 | 適用載體 | 與本判準關係 |
|------|---------|------------|
| 規則 7 來源標注例外 | `docs/` 規格文件（不 sync） | 不受本判準約束——規格文件不 sync，來源標注保留原例外；本判準僅約束 `.claude/` 框架文件 |
| PC-093 `history` 豁免 | ticket / worklog（不 sync） | 不受本判準約束——ticket / worklog 不 sync，該豁免的適用範圍與本判準（框架文件）不重疊 |
| comment-writing 方法論 | 程式碼 comment（含框架 hooks / scripts） | 框架 code comment 記錄「為什麼這樣做」的決策脈絡時，改依「兩層分流指引」寫自足 WHY 或引用方法論，不再以 ticket ID 作脈絡標注 |

#### 與 decision-trigger-binding 規則 2 的邊界

框架檔案內寫「當條件成立時該做 X」而想綁 ticket ID 作 trigger 時，本規則硬擋。該交集的處置是收窄規則 2 的適用範圍（只約束延後決策；決策已下完的條件式操作規範改指名偵測承擔者），**本規則不開豁免**——判準全文見 `.claude/references/decision-trigger-binding-details.md` 規則 2.5 條件式操作規範。

#### 與 PC-098 的邊界（更新）

PC-098（PM 撰寫規則本能引用當下 ticket ID）禁止「規則正文 / 相關文件 / footer 嵌入當下任務 ticket ID」。全禁原則下 PC-098 的立場不變，且範圍擴大為涵蓋所有 ticket ID（不限當下任務）：

| PC-098 禁止項 | 五類分類 | 處置一致性 |
|--------------|---------|----------|
| 規則正文 `W-xxx 為 X 套用` | 第 1 類 | 禁止，一致 |
| 「相關文件」列 ticket ID | 第 1 類 | 禁止，一致 |
| Version footer `— W-xxx 落地`（無變更摘要） | 第 1 類（移除後僅剩「落地」、無摘要無法理解改了什麼） | 禁止，內聯變更摘要 |

**框架文件 footer 來源標注的處理**：footer 是 changelog 慣例的一種，本質為「何時改了什麼」的時點記錄，屬第 2 類（時點標注型）——即使變更摘要完整，ticket ID 本身仍不提供額外資訊，全禁原則下一律移除，只保留變更摘要：

| footer 形式 | 處置 |
|------------|------|
| `Version: 1.5.1 — 規則 6「以價值優先」加入（W-xxx）` | 移除 `（W-xxx）`，改 `Version: 1.5.1 — 規則 6「以價值優先」加入` |
| `Version: 1.0.0 — W-xxx 落地`（PC-098 #3 例，本已違規） | 移除 ID 並內聯變更摘要：`Version: 1.0.0 — <補上改了什麼>` |

既有檔案 footer 內殘留的 ticket ID 屬既有內容清理範圍，依框架分階段清理計畫處理，不在本次改寫範圍內逐檔追溯。

#### 功能字串（運行時輸出）子類（已併入全禁原則）

舊判準下，comment / docstring 的歷史錨點型 ticket ID 允許保留，功能字串（hook 提示、CLI 輸出、UI 文案、錯誤回饋）因面向所有運行時用戶而需額外收緊、一律清理。全禁原則下，comment / docstring 已不再有「允許保留」的分支——五類分類對所有載體一視同仁，第 1-3 類一律移除，第 4-5 類豁免僅依路徑判定、與載體是否為運行時可見無關。原「comment 可放寬、功能字串不可放寬」的差異已消失，本子類不再需要獨立判準，僅保留一項提醒：**功能字串因面向所有運行時用戶，誤導面最廣，清理優先級最高**。

#### 與舊兩類判準的對應（沿革記錄）

原「依賴型 / 歷史錨點型」兩類判準已由本五類分類取代，不再是獨立生效的判準。保留對應關係供讀者查找舊術語時定位：

| 舊分類 | 新分類 | 差異說明 |
|--------|--------|---------|
| 依賴型 | 第 1 類（論證依據型） | 判準相同（移除測試），處置從「抽象化或內聯定義」明確為「兩層分流：寫進註解或先寫方法論」 |
| 歷史錨點型 | 拆分為第 2 類（時點標注型）與第 3 類（案例敘事主詞型） | 舊分類「允許保留」的立場已改為禁止：純時點標注日期可取代同功能；案例敘事主詞型為舊判準未命名的型態，獨立出來改描述性標籤 |
| （未命名） | 第 4 類（被說明對象型）、第 5 類（測試資料型） | 舊判準缺此二類命名，機械守衛若未先命名會誤報 |

### 實證錨點型：commit hash 的條件允許

> **本章節定位**：識別符類型表原列「專案 commit hash：否」，本章節將其改為「條件允許」。此判準與上方「引用性質判準」五類分類同屬規則 8，但適用對象不同——五類分類處理 **ticket ID** 的引用性質判準，本章節處理 **commit hash** 的條件允許判準。兩者是規則 8 下並列的獨立判準，**本章節不是五類分類的第六類**：五類分類「前三類一律移除」的處置邏輯僅適用 ticket ID，不因本章節鬆動。

**Why**：ticket ID 與 commit hash 雖同屬「專案層級識別符」，功能維度不同——ticket ID 的主功能是路由（讀者須跟進該 ticket 才能理解論證，移除常使論證不完整），commit hash 的主功能是驗證（讀者不需 hash 即可理解案例敘事，hash 提供 `git show` 審計能力，移除幾乎不影響理解）。同一套「全禁」判準套用在功能維度不同的識別符上會誤傷——全面禁止 commit hash 會使 error-pattern 實證段落失去可查證性，而實證段落存在的意義正是「可查證」，移除查證手段只剩敘事文字的自我宣稱。

**Consequence**：不區分兩者會讓實證段落陷入選擇困境——要嘛違規保留 hash（規則字面禁止），要嘛全數改為描述性標籤（實作成本高，且失去 `git show` 可驗證的審計能力，這是 ticket ID 案例不具備的損失維度）。

**Action**：commit hash 改為「條件允許（實證錨點型）」，條件是「移除 hash 後周圍敘事仍可理解」——與 ticket ID 的「一律移除」邏輯不同，不要求移除，而是要求移除後不影響理解（敘事自足性），hash 本身視為附加的可驗證性標記，非理解所需的必要元件。

#### 條件定義

| 條件 | 判準 | 說明 |
|------|------|------|
| 敘事自足 | 移除 hash 後，周圍文字仍可理解案例要點（讀者不需 `git show` 即能掌握「發生了什麼」） | 若 hash 是敘事中唯一的案例識別（如僅以 hash 區分兩個相似案例），移除後讀者無法區分，需補描述性標籤使之自足 |
| 附日期 | commit hash 出現處建議鄰近標注日期（如「2026-07-28 案例」） | 日期提供時點脈絡，hash 提供驗證錨點，兩者互補而非重複 |

**判別準則**（移除測試，與 ticket ID 判準同構但結論不同）：問「移除這個 hash 後，讀者還能理解這句在說什麼案例嗎？」能 → 實證錨點型，條件允許保留；不能（hash 是唯一的案例識別）→ 需補描述性標籤補足自足性，hash 本身仍可保留（不像 ticket ID 依賴型須被移除）。

#### sync 後的失效模式：有害 vs 無害

| 識別符 | sync 後失效模式 | 有害性 |
|--------|----------------|--------|
| ticket ID（第 1/2 類，一律移除） | 死連結，讀者被引導查一個不存在的 ticket，論證斷裂 | 有害 |
| commit hash（實證錨點型，符合自足性條件） | hash 成為惰性文字，敘事本身仍完整，僅失去 `git show` 審計能力 | 無害（僅失審計，不失理解） |

類比：ticket ID 像學術論文的 in-text citation（讀者理解論證所需）；commit hash 像 footnote reference number（可供查證但非理解論證所必需）。

#### 守衛豁免的機械可判定性

沿用「引用性質判準」章節建立的原則——守衛不做語意判斷，只做機械偵測：

| 判定步驟 | 機制 | 是否需要語意理解 |
|---------|------|----------------|
| 1. 樣式偵測 | commit hash 樣式（7-40 位十六進位字元，前後無其他英數字元）即為候選 | 否，純正則 |
| 2. 位置豁免 | 候選位於 `.claude/error-patterns/` 目錄的實證 / 案例 / 事件段落（可綁段落標題關鍵字或位置標記） | 否，路徑 + 段落位置比對 |
| 3. 判定結果 | 落在豁免範圍 → 放行；其餘位置（非 `error-patterns/` 目錄，或 `error-patterns/` 內規則正文 / 防護條款而非實證段）→ 循一般規則 8 識別符判定 | 否 |

「敘事自足性」本身無法機械判定（需語意理解才能判斷「移除後是否仍可理解」），故守衛只做「位置 + 樣式」兩層機械篩選，自足性要求維持為撰寫者與 reviewer 的人工判定責任——這與「硬性強制的範圍：樣式偵測可機械化，改寫品質仍需人工判定」章節的原則一致：樣式偵測可機械化，改寫品質（本例為敘事自足性）仍需人工判定。守衛的具體正則與段落位置偵測實作獨立追蹤，不在本文件定義。

#### 與 error-patterns 目錄清理範圍的銜接

`.claude/error-patterns/` 目錄的 ticket ID 清理工作（案例敘事主詞型 ticket ID 依慣例改為描述性標籤）與本章節裁決正交：commit hash 不是 ticket ID，不在該清理範圍。commit hash 依本章節「實證錨點型」條件獨立處置——符合自足性條件即保留，不需改寫為描述性標籤。此為銜接說明，記錄於本檔供該清理工作參照。

#### 正反範例

| 引用 | 移除測試 | 判定 |
|------|---------|------|
| `commit `73e4ea3` 標題為某票收尾、內容全為另一票檔案` | 移除後「某次 commit 標題為某票收尾、內容全為另一票檔案」仍可理解 | 實證錨點型，條件允許保留 |
| `bc710aa9（2026-05-29）` 表格欄位（其他欄位已描述內容） | 移除後表格其他欄位仍完整描述案例 | 實證錨點型，條件允許保留 |
| 案例僅以 hash 區分「commit A 與 commit B 的差異」，敘事完全依賴兩個 hash 才能分辨 | 移除後無法分辨兩案例 | 不符自足性，需補描述性標籤（如「第一次 commit」「第二次 commit」）使之自足，hash 本身仍可保留 |

### 框架 code 邏輯註解的識別符衛生

> **本章節定位**：前述「引用性質判準」的五類分類已明訂——框架 code 邏輯註解與其他載體適用同一套全禁原則，第 1 類（論證依據型）依兩層分流指引處理。本章節針對邏輯註解這個特定載體，把兩層分流的第一層（簡單理由寫進註解）具體化為**自足 WHY** 撰寫法，並定義第二層跨 consumer 需要中央錨點時的**結構化溯源載體**，將 ticket ID 完全移出邏輯註解。本章節是文件慣例（降級為慣例，不上 regex hard hook 判斷「寫得是否夠自足」，理由見下「硬性強制的範圍：樣式偵測可機械化，改寫品質仍需人工判定」；但「是否含 ticket ID」本身已可機械偵測，見上「機械可判定性」）。

**Why**：框架 code 經 sync 跨專案共用，邏輯註解內的 project-local ticket ID 在其他專案斷源（該 ticket 不存在）或偽源（編號巧合對應到別的 ticket）。全禁原則下，即使句子在移除 ID 後仍完整（原歷史錨點型），留著 ticket ID 對讀者仍無操作價值——讀者無法在自己專案查到該編號，反而誤以為是有效引用。自足 WHY 把「為什麼這樣寫」的決策依據直接寫進註解，不依賴外部 ticket 即可理解，是更穩定的設計脈絡記錄方式。

**Consequence**：邏輯註解保留 ticket ID 會在 sync 後讓其他專案的讀碼開發者面對死編號——查無此 ticket，無法追溯設計脈絡，反而比沒有註解更困惑（誤以為脈絡可查卻查不到）。當設計脈絡需要可追溯的中央錨點時，缺結構化溯源載體會迫使開發者把追溯責任塞回 ticket ID，回到斷源/偽源問題。

**Action**：框架 code 邏輯註解禁嵌 project-local ticket ID。改以下兩條路徑取代：

| 取代路徑 | 適用情境 | 形式 |
|---------|---------|------|
| 自足 WHY | 設計依據可直接用一兩句話說清，不需外部追溯 | 註解直接寫「為什麼這樣寫」的決策依據，不含任何 ticket ID |
| 結構化溯源載體 | 設計脈絡需可追溯的中央錨點（跨 consumer 共享） | frontmatter provenance 欄或 commit trailer 引 `claude#NN`（見下「frontmatter provenance 欄與 commit trailer 溯源慣例」） |

**自足 WHY 正反範例**：

反例（邏輯註解嵌 project ticket ID，禁止）：

```python
# 此處改用 globalThis 取代 global（W-xxx）
target = globalThis
```

```python
# 依 W-xxx 的分析，這裡要先檢查 None 再 dispatch
if payload is None:
    return
```

正例（自足 WHY，移除 ticket ID 並把決策依據內聯）：

```python
# Service Worker 環境無 global，改用 globalThis 確保跨環境可用
target = globalThis
```

```python
# payload 為 None 時 dispatch 會拋 AttributeError 中斷 hook，先擋掉
if payload is None:
    return
```

正例（需中央錨點時，改引結構化溯源載體）：

```python
# 此防護源於 Service Worker 全域物件缺失崩潰；跨 consumer 修復追蹤見 provenance
# Provenance: claude#42
target = globalThis
```

**判別準則**：問「移除 ticket ID 後，這條邏輯註解是否已說清『為什麼這樣寫』？」

- 已說清 → 註解本身即自足 WHY，無需任何溯源載體
- 未說清（ticket ID 是唯一的脈絡來源） → 把決策依據內聯為自足 WHY；若脈絡需跨 consumer 共享追溯，補結構化溯源載體（`claude#NN`），而非留 project ticket ID

### frontmatter provenance 欄與 commit trailer 溯源慣例

> **本章節定位**：定義兩個結構化溯源載體——frontmatter `provenance` 欄（檔案層級）與 commit trailer（提交層級）——作為框架 code 邏輯註解移除 ticket ID 後的替代追溯錨點。兩者共同目標是把溯源資訊移出邏輯註解，改用跨專案穩定的錨點（GitHub Issues `claude#NN`）。

**Why**：框架變更（含 code 與 doc）需要可追溯的設計脈絡，但 project-local ticket ID 跨專案 sync 後失效（規則 8 主傷害模型）。關聯決策（issue tracker 三重用途設計）定框架採 GitHub Issues 作 canonical 錨點——issue 編號由 server 原子分配，跨 consumer 唯一且持久，是比 project ticket ID 更穩定的 provenance 載體。frontmatter 欄與 commit trailer 把這個錨點放在「不污染邏輯註解、機器可解析」的位置。

**Consequence**：缺結構化溯源載體慣例，開發者要記錄跨 consumer 設計脈絡時只能塞回邏輯註解的 ticket ID（斷源/偽源）或散落在 commit message 正文（不可機器解析、難以聚合追蹤）。有統一慣例則溯源資訊集中於可預期欄位，升格/跨 consumer 修復追蹤時可批量查詢。

**Action**：依載體類型採以下慣例。`claude#NN` 形式（如 `tarrragon/claude#42`）為首選，對齊框架 GitHub Issues canonical 決策。

**慣例 1：frontmatter provenance 欄（檔案層級）**

適用於有 YAML frontmatter 的框架檔案（error-pattern、methodology 等）需標注設計脈絡來源時。欄位為**可選**——零摩擦捕獲時不填，升格或需跨 consumer 追蹤時才 stamp。

| 欄位 | 性質 | 值形式 | 說明 |
|------|------|-------|------|
| `provenance` | 可選 | `claude#NN` 或 `owner/repo#NN` | 通用設計脈絡錨點，適用任意 code/doc 變更 |
| `canonical_issue` | 可選 | `owner/repo#NN`（如 `tarrragon/claude#42`） | error-pattern 專屬：指向 canonical 去重身份的 framework issue |

範例 1（error-pattern frontmatter 引 canonical issue）：

```yaml
---
id: PC-112
title: subagent 對非程式碼檔誤選 MCP 寫入工具
canonical_issue: tarrragon/claude#42
---
```

範例 2（methodology frontmatter 標通用 provenance）：

```yaml
---
title: knowledge-carrier-allocation-methodology
provenance: tarrragon/claude#57
---
```

**慣例 2：commit trailer（提交層級）**

適用於框架 code/doc 變更需在提交層級標注溯源，取代「邏輯註解嵌 ticket ID」。trailer 置於 commit message 末尾，鍵值對形式，機器可解析。

| trailer 鍵 | 值形式 | 說明 |
|-----------|-------|------|
| `Provenance` | `claude#NN` 或 `owner/repo#NN` | 此變更的設計脈絡錨點 |

範例 1（框架 code 變更 commit trailer）：

```
fix(hooks): Service Worker 環境改用 globalThis 取代 global

global 在 Service Worker 不存在，導致 hook 啟動崩潰。改用 globalThis
確保跨環境可用。

Provenance: tarrragon/claude#42
```

範例 2（框架 doc 變更 commit trailer）：

```
docs(references): 規則 8 補框架 code 註解識別符衛生條款

Provenance: claude#58
```

**邊界**：本慣例的 `provenance` / `canonical_issue` 載體存放於 frontmatter 與 commit trailer，**不存放於邏輯註解**——邏輯註解仍依上節「框架 code 邏輯註解的識別符衛生」要求自足。功能字串（運行時輸出）一律不嵌任何溯源識別符（含 `claude#NN`），維持運行時輸出對最終用戶無誤導。

### 硬性強制的範圍：樣式偵測可機械化，改寫品質仍需人工判定

> **本章節定位**：早期量測（naive regex 全域掃描，未先命名豁免類）曾得出「識別符衛生應降級為文件慣例、不上 regex 強制層」的結論。全禁原則 + 五類分類重新拆解了這個問題——「是否含 ticket ID 且未落在豁免路徑」與「移除後補寫的內容品質是否足夠」是兩個不同問題，前者現已可機械化，後者仍需人工判定。本章節更新舊結論，並保留原始量測資料作為「豁免類必須先命名」的實證。

**Why**：對「框架文件含 project ticket ID」做未先命名豁免類的 naive regex 偵測，誤報面過大——早期一次全域量測命中 8686 處，其中 322 處屬合法命中（`source_ticket` 等 frontmatter 欄位範例、被說明對象型格式展示、規則正文的範例引用），naive regex 無法區分這些與真正的第 1 類違規引用，會全數誤殺。此資料正是「引用性質判準」一節所述——未命名豁免類即執法會產生數千筆誤報（ARCH-016 失效模式）的先前印證。

**Consequence**：若不拆分「樣式偵測」與「改寫品質判定」兩層，會重蹈同一誤報問題；反之，若因為「改寫品質判定需要人工」而連「是否含 ticket ID」都放棄機械偵測，等於捨棄了全禁原則帶來的「可完整執法」優勢，回到判斷型規則執行落差隨 session 數累積的舊狀態。

**Action**：拆分為兩層對待：

| 層次 | 判定內容 | 是否可機械化 | 執行方式 |
|------|---------|------------|---------|
| 樣式偵測 + 豁免判定 | 是否含 ticket ID 樣式，且是否落在第 4 / 5 類豁免路徑 | 是 | 守衛前向執行（獨立追蹤，見上「機械可判定性」） |
| 改寫品質判定 | 移除 ID 後的自足 WHY 是否真的說清楚、方法論引用是否選對方法論 | 否，需語意理解 | 維持人工判定（撰寫前檢查清單 + code review） |

守衛只擋「樣式偵測 + 豁免判定」這一層；改寫品質不進入機械阻擋範圍，由撰寫者與 reviewer 依「兩層分流指引」的判別準則自律執行。

### 豁免機制

某些情境下，框架文件確實需要保留產品名稱或專案層級識別符作為情境舉例（例如測試任務的具體系統名稱、業務情境驅動的程式碼註解正例、5 Whys 真實事件分析）。完全禁止會破壞寫作教學的可讀性或事件記錄真實性。豁免機制提供合法的「具體舉例」保留路徑。

**Why**：規則 8 主目標是防止 sync 後死連結與誤導，但「具體舉例」的價值在於可讀性與場景感，與「框架描述」（如「本專案是 X」聲明）性質不同。一刀切禁止會傷及寫作教學的核心目的。

**Consequence**：缺豁免機制會出現兩種失敗模式：(1) 嚴格執行→教學範例失去場景感（如「系統 X」「平台 Y」），讀者無法建立心智模型；(2) 寬鬆執行→A 類框架描述漏網，sync 後跨專案誤導。

**Action**：依下方判別表將每處產品名稱出現分為「A 類框架描述（需泛化）」vs「B 類具體舉例（可豁免）」，B 類處加標準格式 HTML comment 豁免註解。

#### A/B 類判別表（角色二分）

| 類別 | 語意角色 | 處理 | 範例情境 |
|------|---------|------|---------|
| A 類 | 框架描述（「本專案是 X」聲明、跨專案通用 agent 的目標清單列舉） | 泛化為通用描述 | agent 定義中的「本專案」聲明 / data-miner agent 中的具體網站清單 |
| B 類 | 具體舉例（測試任務情境設定、業務情境驅動正例、業務意圖正例、過度驗證反例、5 Whys 真實事件記錄） | 加豁免註解保留 | 寫作 SKILL 的 dry-run 測試任務 / 程式碼註解範例 / 文件正例表格 / 反例 Widget Key 命名 / 事件分析鏈中的 storage key |

**判別準則**：問「若此處改為『系統 X』『平台 Y』後，讀者還能理解教學/事件意圖嗎？」

- 不能理解 → B 類（具體舉例，加豁免註解保留）
- 仍能理解（甚至更清晰）→ A 類（框架描述，泛化）

#### HTML comment 標準格式

```html
<!-- 規則 8 豁免（reference-stability-rules.md / DOC-010）：[此處 XXX] 作為 [情境類型]。[泛化會失去 YYY] 故保留。本豁免經 [評估方式] 後保留。 -->
```

**欄位填寫要求**：

| 欄位 | 內容 | 範例 |
|------|------|------|
| `[此處 XXX]` | 具體指出豁免對象（產品名稱 / 識別符） | 「以下任務描述中的某產品名稱」「`platform_icon_<product>` Widget Key」「`<product>_books` storage key」 |
| `[情境類型]` | 為何屬於 B 類（語意角色） | 「dry-run 測試的具體任務情境設定」「業務情境驅動正例 Dart 註解」「5 Whys 真實事件分析鏈中的 storage key」 |
| `[泛化會失去 YYY]` | 泛化會喪失的具體價值 | 「測試人員需具體系統名稱才能模擬撰寫情境，改為『系統 X』會失去場景感」「降低反例真實性」 |
| `[評估方式]` | 豁免決策來源（不引用專案 ticket ID） | 「跨檔評估」「規則 8 自律審視」 |

#### 撰寫慣例

**禁止**：

| 禁止 | 改為 |
|------|------|
| 在豁免註解中引用專案 ticket ID（如 `(W{N}-{seq} 評估)`） | 改為抽象描述（如「跨檔評估」） |
| 用「Why: 因為這樣比較好讀」這類無具體價值理由 | 必須說明「泛化會具體失去什麼」 |
| 一筆豁免覆蓋多處不同性質的內容 | 每個 B 類處獨立加註解，理由不可共用 |

**允許引用的權威**：

| 引用 | 屬性 |
|------|------|
| `reference-stability-rules.md` | 規則 8 本身路徑 |
| `DOC-010` | error-pattern 框架內部分類（規則 8 例外） |
| 其他 framework error-pattern ID（`PC-XXX` / `ARCH-XXX` / `IMP-XXX` / `TEST-XXX`） | 框架內部分類同上 |

#### 範例對照

合法 B 類豁免：

```html
<!-- 規則 8 豁免（reference-stability-rules.md / DOC-010）：以下測試反例中的 `platform_icon_<product>` Widget Key 命名是反例事件的真實 Key 名稱。事件分析記錄真實事件特徵；改為 `platform_icon_xxx` 會降低反例真實性。本豁免經跨檔評估後保留。 -->
```

不合法的「偽豁免」：

```html
<!-- 規則 8 豁免：此處保留產品名稱 -->                <!-- 缺欄位、無理由 -->
<!-- 規則 8 豁免：W{N}-{seq} 評估保留 -->             <!-- 引用專案 ticket ID -->
<!-- 規則 8 豁免：這樣比較好讀 -->                    <!-- 無具體價值說明 -->
```

#### 決策追溯慣例

- 豁免註解本身不含專案 ticket ID（保持規則 8 自律）
- 大規模豁免應用後，應在 `DOC-010` 觸發案例表新增一筆紀錄（採抽象模式描述）
- 後人查找豁免決策依據時，從 `DOC-010` 觸發案例表往回追溯到對應 ANA ticket（ANA ticket 位於 `docs/work-logs/`，規則 8 不適用於 `docs/`）

---

## CLAUDE.md 章節外移決策樹

**Why**：縮減 `CLAUDE.md` 體量時，章節外移目標必須依「內容是否含專案層級識別符」決定，誤將專案內容外移到 `.claude/references/`（跨專案 sync 範圍）會觸發規則 8 違反；誤將通用內容外移到 `docs/`（專案內部）則喪失跨專案知識複用。

**Consequence**：外移目標選錯會讓 `.claude/` sync 機制把專案專屬內容（如 `src/` 路徑、產品名稱）擴散到其他專案，造成跨專案誤導；或讓跨專案通用知識被困在單一專案 `docs/` 失去複用機會。

**Action**：執行 `CLAUDE.md` 章節外移前依下方決策樹判定目標，外移後執行檢查清單驗證合規。

### 觸發條件

| 訊號 | 處理 |
|------|------|
| `CLAUDE.md` 單檔行數 > 200（或某章節 > 50 行） | 評估外移 |
| 章節內容只在特定情境（如寫程式碼前）才需要 | 評估外移為 lazy-load reference |
| 章節含具體實作細節（命名清單、檔案位置表） | 評估外移以保持 `CLAUDE.md` 抽象層級 |

### 外移目標選擇決策樹

依序回答以下問題：

| 步驟 | 問題 | 是 → 目標 | 否 → 下一步 |
|------|------|----------|------------|
| 1 | 章節是否含**專案層級識別符**（`src/` 路徑、產品名稱、專案 ticket ID、專案 commit hash、專案 worklog 路徑）？ | `docs/<descriptive-name>.md` | 進入步驟 2 |
| 2 | 章節是否含**跨專案可複用的通用知識**（如 Chrome Extension 限制、Manifest V3 行為、Python 通用規範）？ | `.claude/references/<descriptive-name>.md` | 進入步驟 3 |
| 3 | 章節是否為**混合內容**（部分專案、部分通用）？ | 拆檔：專案部分放 `docs/`、通用部分泛化後放 `.claude/references/` | 留在 `CLAUDE.md`（無法外移） |

### 目錄性質對照表

| 目錄 | sync 範圍 | 可含專案識別符？ | 適用情境 |
|------|----------|---------------|---------|
| `docs/` | 專案內部，不 sync | 是 | 專案專屬規範、架構說明、需求/規格 |
| `.claude/references/` | 跨專案 sync | 否（規則 8） | 跨專案技術速查、通用 lazy-load 參考 |
| `CLAUDE.md` | 專案入口，不 sync | 是 | 高層概覽 + lazy-load 指引 |

### 外移後檢查清單

- [ ] 新檔目標目錄符合決策樹結論（`docs/` vs `.claude/references/`）
- [ ] `CLAUDE.md` 引用路徑已更新（`@docs/<檔名>.md` 或 `@.claude/references/<檔名>.md`）
- [ ] 原檔案已刪除（外移而非複製）
- [ ] 若放 `.claude/references/`，內容已通過規則 8 自檢（無 `src/` 路徑、無產品名稱、無專案 ticket ID）
- [ ] 若放 `docs/`，已在新檔頂部說明「存放位置原因」（避免後續誤搬回 `.claude/`）
- [ ] 執行 `grep -rn "<新檔關鍵字>" .claude/ docs/` 確認無殘留舊路徑引用

### 範例對照

| 章節性質 | 外移目標 | 範例 |
|---------|---------|------|
| 專案錯誤處理體系（含 `src/core/errors/` 路徑） | `docs/project-conventions.md` | 含 `src/` 路徑與專案 enum 清單，屬步驟 1 命中 |
| Chrome Extension Manifest V3 通用限制 | `.claude/references/chrome-extension-quickref.md` | 通用限制速查表（require/global/Storage API），泛化後保留 |
| Flutter 專案的命名規範 + 通用 Dart 風格 | 拆檔：專案命名放 `docs/` 下 flutter-naming-conventions 檔、通用 Dart 風格放 `.claude/references/` 下 dart-style 速查檔 | 混合內容拆檔範例（路徑為示意） |

---

## 規則 9：環境事實的記錄形式

**記錄環境事實時，操作指引用 `~` 或專案相對路徑；實測記錄保留原值但必須標註量測環境。**

規則 7 處理引用來源的時間穩定性（臨時文件會消失），規則 8 處理跨專案 sync 的穩定性（專案識別符在他專案無意義）。本規則處理第三個失效軸：**跨環境**——同一份文字在換帳號、換裝置、換容器後是否仍然成立。

### 兩類記錄，兩種處置

判準是「這段路徑是要人照著操作，還是記載當時量到什麼」。

| 類別 | 判準 | 處置 | 範例 |
|------|------|------|------|
| 操作指引 | 讀者會照著執行 | 改為 `~/...`、專案相對路徑或 `$CLAUDE_PROJECT_DIR` | `cd $CLAUDE_PROJECT_DIR/.claude/hooks` |
| 實測記錄 | 記載某次量測的結果 | **保留原值**，鄰近標注量測環境（HOME / 平台 / 日期） | 「（量測環境：`$HOME=/Users/xxx`，2026-08-10）探針回報 Base directory: /Users/xxx/...」 |

**Why 實測記錄不改寫**：改寫會讓記錄宣稱量到了從未量到的值。若該記錄同時包含輸入與輸出的對應關係（例如路徑轉換示例），改了輸入而輸出照舊，兩者會對不上，讀者無從判斷哪一邊才是真的。

**Why 操作指引必須改**：機器專屬絕對路徑換機後全數解析不到，而失效訊息 `No such file or directory` 只描述結果不含成因——它不會告訴讀者「這是在另一台機器上寫的」。接手者最省力的解釋是自己打錯路徑或環境沒裝好，不是懷疑文件。失效因此不自我揭示，只能靠事後人工全量盤點才被發現。

**Action**：寫入前先問這段路徑屬哪一類；操作指引改為環境無關形式，實測記錄補量測環境標注。已寫入的殘留依上表分類逐一處置，不要一律改寫。

### 可接受與不可接受的形式

| 形式 | 可接受 | 說明 |
|------|-------|------|
| `docs/work-logs/...`（專案相對） | 是 | 不受 `$HOME` 影響 |
| `~/.claude/skills/x` | 是 | 跨機可解析，語意是「personal 層位置」而非特定機器 |
| `$CLAUDE_PROJECT_DIR/...`、`$(git rev-parse --show-toplevel)/...` | 是 | 由執行環境解析，跨專案亦成立 |
| `${<REPO>_REPO:?<提示訊息>}/...`（跨 repo，可執行命令用；`<REPO>` 為 repo 名大寫，如 `BLOG_REPO`） | 是 | 共用資產內只留變數名，解析延後到各環境。變數定義須放不同步的本機設定（如 gitignored 的 `settings.local.json` 的 `env` 區塊），放共用檔等於把佈局假設換個地方擺。未定義時 fail-fast 優於靜默解析到錯誤路徑 |
| `<repo 識別>: <repo 內相對路徑>`（跨 repo，知識溯源用） | 是 | 例 `blog repo: content/record/x.md`。不含 HOME 之下任何路徑段，讀者定位 repo 根後即可解析 |
| `~/<Dir>/<repo>/...`（跨 repo 但 HOME 相對） | 僅限單一環境內的記錄 | `~` 只消去使用者名，消不掉 HOME 之下的佈局差異。多環境並行時同一 repo 可能位於 `~/Projects/<repo>` 與 `~/project/<repo>`，此形式必於某一側失效，且「附一句實測」不足以補救——實測在哪一側做的，結果就只在哪一側成立。跨環境共用的記錄改用上面兩列 |
| `/Users/<name>/...`、`/home/<name>/...` | 否 | 除非該路徑本身就是被討論的對象，或屬上表的實測記錄類 |
| 「實測確認 X」而未標註量測環境 | 否 | 讀者無法判斷該結論是否仍成立 |

**校正時必須當場跑一次存在性命令，不可只做字串替換，也不可引用記憶中的實測結果**：只替換字串會產出看起來已修好、實際仍解析不到的路徑，失效形態與修正前相同，且因為標記為「已處理」而更不容易再被檢查。

**同一份記錄被兩個並行環境共用時，`~` 不足以修好路徑**：`~` 只換掉使用者名那一段，HOME 之下的目錄佈局差異換不掉。

實證案例（2026-08-10 校正，2026-08-12 鑑識）：兩個 working copy 長期並行，同一個外部 repo 在一側位於 `~/Projects/<repo>`、另一側位於 `~/project/<repo>`（複數大寫 vs 單數小寫）。一次校正在前者執行，正確觀測到自身環境並把路徑寫成 `~/Projects/<repo>/...`；該值在後者 MISSING。後者接手時把它改回 `~/project/<repo>/...`，前者於是失效。兩個寫法各自在一側為真，**都不是正確答案**——把記錄從一側形式改成另一側形式只是把失效方向倒過來。

鑑識依據值得一記：判別「該 session 是否在本 working copy 執行」用的不是敘述，而是 gitignored 的 hook 日誌時段分布（本機產出、不隨 commit 移動）與 harness 依 cwd 編碼的 scratchpad 目錄名。兩者皆非模型輸入，故可用來反證記錄中的環境宣稱。

**Action**：

1. 校正路徑時把存在性命令的 raw stdout 貼進記錄，而非貼結論句，並標註是在哪一側量的。二元輸出（EXISTS / MISSING）與 realpath 是可複驗的固定值；「實測確認 X」這種結論句無論寫得多具體都不構成證據，也不揭示它只在某一側成立。
2. 跨 repo 引用（目標不在本專案 git 樹內，`$CLAUDE_PROJECT_DIR` 與 `git rev-parse` 都指不到）**目前沒有環境無關的正確寫法**。改記「用途 + 尋找方式」取代記路徑：說明要找的是哪個 repo 的哪類文件，讓讀者在自己環境解析，而不是給一條在對方環境會壞掉的絕對指引。

<!-- 命令列的 candidate-a/b 為佔位符，實際代入兩側各自的候選路徑 -->

    $ for p in ~/candidate-a ~/candidate-b; do [ -e "$p" ] && echo "$p EXISTS" || echo "$p MISSING"; done

### 強制層與其邊界

`ticket track create` 與 `ticket track claim` 各接一次掃描，命中輸出 WARNING 不阻擋（通用佔位符如 `/Users/me`、`/Users/<user>` 不列入，否則文件示例路徑會逐一觸發而使警告被當雜訊）。

**未涵蓋範圍**：worklog、框架文件、規格文件的殘留無自動偵測，仍須人工盤點。盤點時三處都要掃，只掃 ticket 目錄會漏掉另外兩處。

> 完整失效形態、症狀清單與盤點步驟見 `.claude/error-patterns/process-compliance/PC-BAL-029-machine-specific-absolute-path-records-environment-fact.md`。

---

## 規則 10：可變計數不實例化

**敘述中引用一個集合（欄位表、檔案目錄、條文清單、步驟）的大小時，不寫數字，改引用該集合的權威名稱；需要數字者只在權威表所在處定義一次，其餘位置一律引用名稱而非重複數字。**

規則 7 處理引用來源的時間穩定性（臨時文件會消失），規則 8 處理跨專案 sync 的穩定性（專案識別符在他專案無意義），規則 9 處理跨環境的穩定性（機器專屬路徑換機後解析不到）。本規則處理第四個失效軸：**跨版本**——集合本身會隨開發演進（新增、移除成員），寫死的計數會在集合下一次改版時全數靜默失效，而每一句字面完好、連結有效、grep 不報錯。

**Why**：集合大小是集合的衍生值（derivation），不是穩定事實——權威表增減一列，所有寫死該數字的敘述句立即變錯，但沒有機制把權威表的變更推送到每個引用位置。已有三次同型實證：`rules/README.md` 的 `core/` 檔數「7」在檔數增至 17 後 stale 多版（10.2.0 更正、10.3.0 再確認）；`doc` skill 的 CHANGELOG 欄位數描述「九欄位」落後於方法論已升格的「十一欄位」，達 15 天才被稽核發現；元件契約新增回饋契約欄時，「十一欄位」字面分布 17 檔 60 餘處，其中 skill description 觸發詞、契約齊全定義的分母、佔位值分組、「三欄制」判準皆以該計數為結構前提。完整實證見 `.claude/error-patterns/documentation/DOC-GPD-003-variable-count-instantiated-in-prose.md`。

**Consequence**：計數比一般 stale 引用更難處理——它常被當成名稱使用（「十一欄位表」成為觸發詞、章節名、分組名），數字一旦成為跨檔錨點，改數字等於改名，血緣擴散到所有引用該名稱的檔案。且失效不自我揭示：每句字面完好、連結有效，只能靠人工全量 grep 盤點才被發現，權威表下一次改版，同一批位置再失效一次。

**Action**：

1. 敘述引用集合大小時，改引用集合的權威名稱（如「〈元件契約判準〉欄位表全部欄位」「`rules/core/` 全部檔案」），不寫數字；讀者要數字就去權威表數，權威表變了敘述自動正確。
2. 權威表本身也不以成員數命名或分組：改用語意命名（「可填不適用的欄位」而非「六欄」；「最小適用集」而非「三欄制」）。
3. 判準問句：「這個數字在集合增減一個成員後還對嗎？」答否即為衍生值，不得實例化。
4. 已成為跨檔錨點的數字名稱（description 觸發詞、章節名、分組名）需要改名時，走 grep 全量同步、於 CHANGELOG 記血緣檔數，且改成不帶數字的名稱——只把舊數字換成新數字是把同一筆債換個數字再欠一次。

### 例外：兩類可實例化的數字

| 例外 | 判準 | 範例 |
|------|------|------|
| 凍結承諾的數字 | 數字本身有凍結承諾（規則編號、schema 版本、API 常數），變更即屬破壞性變更，非集合隨開發自然演進 | quality-baseline 規則編號、`tracking_schema.json` 的 `VERSION` |
| 實測記錄 | 依規則 9 判準，記載某次量測的結果並標註量測時點與環境，非對現況的持續性宣稱 | 「（量測環境：2026-08-10）1607 個受掃描框架檔中 714 檔含票號」 |

**判別準則**（與規則 9 同構）：問「這是對現況結構大小的持續性宣稱，還是一次性量測記錄？」前者再問判準問句（集合增減一員後還對嗎），答否即不得實例化；後者保留原值並標註時點，不受本規則約束。

### 偵測 pattern

```
rg "[一二三四五六七八九十0-9]+(欄位|檔|條|項|類|步)" .claude/rules .claude/references .claude/methodologies .claude/skills/*/SKILL.md
```

命中後仍需人工篩掉「凍結承諾數字」與「實測記錄」兩類例外，不可逕自視為全部違規。

**強制層邊界**：本規則目前**無** hook 強制層，上述 pattern 僅供權威表增減成員時的 commit 前人工掃描使用；納入 commit-level INFO 提示或阻擋需獨立評估與建票，不在本規則落地範圍內。

---

## 規則 11：行號不作為引用定位符

**指向規格文件或 `.claude/` 框架文件的某一段時，不寫 `<檔名>:<行號>`，改用標題錨點或條款名稱等取自原文的定位符。**

規則 10 處理集合大小這個衍生值，本規則處理另一個衍生值：**位置**。行號不是文件的屬性，是「該段落之前有多少行」的計算結果；被引段落一個字沒改，只要它上方插入任何內容，行號即失效。兩者同族，都是把會變動的值寫死進敘述。

**Why**：行號的失效條件比它所指涉的內容寬得多——內容改寫才該讓引用失效，但行號連無關的增補都會失效，而且是整批同時失效。實證一（票面對規格）：一份會持續增補的規格文件，其某一行在票面被引用 13 處，該檔兩次增補後那一行成為空行、原內容下移五百多行，13 處在同一天全數指錯，沒有任何一處自我揭示；同批另有指向同一檔另一行的三處，現指向與原主題完全無關的段落。實證二（規格對 code）：一份規格以 `<檔名>:<行號>` 指涉某個方法的呼叫點，實測該呼叫已下移一百多行，而原行號今為一個無關的常數宣告。

**Consequence**：失效無訊號，且指錯與指對在讀者端同形——文件讀起來完全正常，只有真的去開那一行才會發現。最嚴重的一類是行號承載仍生效的 trigger：把某項決策的推翻條件寫成「`<檔名>:<行號>` 的裁定被推翻時」，行號漂移後沒有人能到那一行確認裁定是否還在。`decision-trigger-binding` 要求 trigger 可驗證，行號使它不可驗證，而 trigger 的形式要件仍然滿足，稽核不會報。

**Action**：

1. 改用取自原文的定位符：標題錨點（`<規格編號> §2.16`）、條款名稱（`規則 8 的「引用性質判準」`）、符號名（`AppSnackBar.show`）。
2. 錨點文字寫下前以 grep 驗證其在目標檔內唯一——不唯一的錨點指向多處，等於沒有定位。
3. 錨點須通過穩定性判準（見下節），不是唯一就合格。
4. 已寫下的行號引用不逐一巡檢，於編輯該段落時順手改寫；成批失效被發現時一次修完該批，不逐處零星修。

### 唯一性與穩定性是兩個獨立判準

**唯一性決定找不找得到，穩定性決定失效時有沒有訊息量。只查唯一性會放行語意錯配的錨點。**

錨點文字須相對於**所指涉的對象**穩定。錨點含狀態、階段或版本成分（「（提案）」「（草案）」「待決」「v1.5」），而指涉的不是該狀態本身時，須把該成分排除在錨點外：

| 引用意圖 | 不穩定錨點 | 穩定錨點 |
|---------|-----------|---------|
| 指涉某項機制的規格 | `§3.2 快取策略（提案）` | `§3.2 快取策略` |
| 指涉某條規則的判準 | `規則 8（v1.5 改寫後）` | `規則 8 的「引用性質判準」` |
| 指涉某個決策的內容 | `某年某月補記（待決）` | `某年某月：Stage 4 技術維度定案` |

含狀態成分的錨點會在該項目**狀態變更**時失效，但引用者要的是**內容**——提案被採納、草案定稿、版本升號都不改變被指涉的內容，卻讓引用斷掉。反向亦成立：引用意圖確實是「那份還在提案階段的東西」時，狀態成分屬於錨點的一部分，不該排除。**判準是問「引用的是這段內容，還是這段內容當前所處的狀態？」**

### 例外：兩類不受限的位置引用

| 例外 | 判準 | 範例 |
|------|------|------|
| 版本釘住的歷史引用 | 位置由 commit hash 或 tag 釘住，指涉的是該版本的檔案而非現行檔案，位置在該版本內永不變動 | `git show <hash>:<path>` 的行號、error-pattern 實證段落中附 hash 的位置引用（另須符合規則 8「實證錨點型：commit hash 的條件允許」的自足性條件） |
| 機器產生的定位 | 由工具輸出而非人工撰寫（traceback、編譯錯誤、lint 報告、`grep -n` 結果），記錄的是產生當下的位置 | traceback 的 `File "<path>", line <N>, in <function>` |

**與規則 9 同構的第三種情形——歷史記錄**：append-only 的決策記錄檔中的行號記載的是**當時觀測到的位置**，不是**現在該去哪裡找**，處置比照規則 9 的實測記錄：**保留原值，並標明其為當時觀測**（附日期或所屬記錄段落即可）。不寫這條邊界會出現兩種相反的誤用——有人把歷史記錄的行號當現行定位符去查而撲空，有人為了合規把歷史記錄的行號改成現行值，使該記錄宣稱量到從未量到的位置（規則 9 明文禁止的同一種錯）。

**判別準則**（與規則 9、規則 10 同構）：問「這是告訴讀者現在去哪裡找，還是記載當時看到什麼？」前者受本規則約束，後者保留原值並標註時點。

### 偵測 pattern

```
rg "[A-Za-z0-9_.-]+\.(md|py|dart|ts|js|yaml|json):[0-9]+" .claude docs
```

命中後仍需人工篩掉上述兩類例外與歷史記錄，不可逕自視為全部違規；`grep -n` 輸出、traceback 貼文、code fence 內的工具輸出都會命中。

**強制層邊界**：本規則目前**無** hook 強制層，上述 pattern 僅供編輯規格或框架文件時的人工掃描使用；納入 commit-level 提示需獨立評估與建票，不在本規則落地範圍內。

---

## 補充檢查清單（搭配 document-format-rules.md 主檢查清單使用）

編輯規格文件或 `.claude/` 框架檔案時，額外確認：

- [ ] 規格文件未引用 worklog/ticket/plan 等臨時性文件（規則 7）
- [ ] 框架文件（`.claude/`）未引用專案 ticket ID / worklog 路徑（規則 8，全禁原則）
- [ ] commit hash 若出現於 `error-patterns/` 實證段落，已符合「實證錨點型」自足性條件（移除 hash 後敘事仍可理解）並鄰近標注日期；出現於其他位置仍依規則 8 一般判準
- [ ] 引用的來源屬於穩定類別（規格/提案/CLAUDE.md/框架檔案路徑/error-pattern 分類）
- [ ] 若記錄歷史來源（如變更由某 ticket 執行），已依「兩層分流指引」改寫為註解自足 WHY 或方法論引用，而非保留 ticket ID
- [ ] 內容含 `/Users/<name>/` 或 `/home/<name>/` 路徑時，已先判定屬「操作指引」或「實測記錄」，再依規則 9 分類處置（前者改環境無關形式，後者保留原值並標註量測環境）
- [ ] 改寫後的新路徑已實測在當前環境可解析，而非只去掉使用者名（規則 9）
- [ ] 寫下「實測確認 X」類結論時，已標註量測環境（HOME / 平台 / 日期）（規則 9）
- [ ] 文件敘述有引用集合大小（欄位數／檔案數／條文數／步驟數）時，已改引用權威名稱而非寫死數字，或已確認屬規則 10 兩類例外（凍結承諾數字、附時點的實測記錄）（規則 10）
- [ ] 內容以 `<檔名>:<行號>` 指向另一份文件或 code 的某一段時，已改用標題錨點／條款名稱／符號名，或已確認屬規則 11 例外（版本釘住的歷史引用、機器產生的定位、附時點的歷史記錄）（規則 11）
- [ ] 改用的錨點文字已經 grep 驗證在目標檔內唯一，且不含狀態／階段／版本成分（除非引用意圖正是該狀態本身）——唯一性與穩定性須分別確認（規則 11）

---

## 相關文件

- `.claude/rules/core/document-format-rules.md` — 文件格式規則主檔（規則 1-6 + YAML frontmatter + 檢查清單骨架）
- `.claude/rules/README.md` — 規則系統導引
- `.claude/references/framework-asset-separation.md` — 框架資產與專案產物職責分離原則
- `.claude/error-patterns/documentation/DOC-010-framework-references-project-tickets.md` — 框架誤引用專案 ticket 的錯誤模式
- `.claude/error-patterns/documentation/DOC-GPD-003-variable-count-instantiated-in-prose.md` — 規則 10 的固化來源（可變計數實例化的完整實證與同型前例）。**該檔另載讀者側處置**：本規則是撰寫端的預防（不寫死可變計數），讀者遇到「計數與其列舉並存且不一致」時該信哪一個，見該檔〈變體：計數與其列舉同時寫下卻不一致〉——結論為列舉是事實、計數是衍生值，分歧本身即衍生值出錯的證據，不得據以推論列舉有缺漏
- `.claude/error-patterns/documentation/DOC-V1-001-positional-number-reference-stale.md` — 規則 10 與規則 11 的同源前例（位置編號的衍生值失效；規則 10 為計數的衍生值失效、規則 11 為段落位置的衍生值失效，三者互為交叉引用）
- `.claude/error-patterns/process-compliance/PC-098-pm-rule-content-contains-current-ticket-id.md` — 第 1 類（論證依據型）引用的執行面 error-pattern（PM 撰寫規則本能引用當下 ticket ID）
- `.claude/methodologies/comment-writing-methodology.md` — inline comment 記錄決策脈絡（框架 code comment 改依兩層分流指引寫自足 WHY 或引用方法論）
- `.claude/rules/core/decision-trigger-binding.md` — PC-093 `history` 豁免類別定義（適用載體為 ticket / worklog，不受本章「引用性質判準」約束，見「三方對齊」）

---

**Last Updated**: 2026-09-10
**Version**: 1.15.0 — 新增規則 11「行號不作為引用定位符」：禁止以 `<檔名>:<行號>` 指向規格或框架文件的某一段，改用標題錨點／條款名稱／符號名。與規則 10 同族（皆為衍生值寫死進敘述：前者集合大小、後者段落位置），失效軸同為跨版本。含三個子節——(1)〈唯一性與穩定性是兩個獨立判準〉：唯一性只保證找得到一處，穩定性決定失效時是否有訊息量，錨點含狀態／階段／版本成分而指涉的不是該狀態本身時須排除該成分（附三組對照）；(2)〈例外：兩類不受限的位置引用〉：版本釘住的歷史引用（commit hash／tag）、機器產生的定位（traceback／lint／`grep -n`），另補與規則 9 同構的第三種情形——append-only 決策記錄中的行號保留原值並標明為當時觀測（兩種相反誤用皆須防）；(3) 偵測 pattern 與強制層邊界（目前無 hook）。檔頭定義句、適用情境表、補充檢查清單、相關文件同步更新。實證取自兩起已查證案例：一份持續增補的規格其某一行的 13 處票面引用同日全數指錯（含一處承載仍生效的 trigger，使該 trigger 形式滿足而實質不可驗證），以及一份規格對 code 呼叫點的行號引用漂移一百多行後指向無關常數宣告。依規則 8 全禁原則，本規則正文不記票號與專案路徑，實證改寫為自足敘述。
**Version**: 1.14.1 — 相關文件的 `DOC-GPD-003` 列補讀者側處置路由：本規則管撰寫端預防（不寫死可變計數），計數與其列舉並存且不一致時該信哪一個屬處置面，指向該檔新增的〈變體：計數與其列舉同時寫下卻不一致〉，維持「預防在規則、處置在 pattern」的分工
**Version**: 1.14.0 — 新增規則 10「可變計數不實例化」：文件敘述引用集合大小（欄位數/檔案數/條文數/步驟數）時禁寫死數字，改引用權威名稱，數字只在權威表定義一次；含判準問句、兩類例外（凍結承諾數字／規則 9 實測記錄）、偵測 pattern、強制層邊界（目前無 hook）。檔頭定義句、適用情境表、補充檢查清單、相關文件同步更新；與 DOC-V1-001（位置編號）、DOC-GPD-003（規則來源）互相交叉引用。
**Version**: 1.13.0 — 「守衛的實際執法邊界」節的逃生閥合法 category 由兩個擴充為三個，新增 `relocation`（既有內容的逐字位置搬移，reason 須含來源檔路徑）；並補一段適用邊界說明——僅限逐字位置搬移，內容經任何編修即不適用，因本類別可驗證性建立在 diff 可比對的機械基準上，編修後即失去此基準，須改走一般五類分類判準。
**Version**: 1.12.0 — 規則 8 末段邊界群新增「與 decision-trigger-binding 規則 2 的邊界」：說明「框架檔案內的條件式條文想綁 ticket ID」這個交集的處置為收窄規則 2 適用範圍而非在本規則開豁免，判準全文指向 `decision-trigger-binding-details.md` 規則 2.5（論證不在本檔重複）；置於邊界群而非判準章之前，避免邊界內容先於核心操作內容。
**Version**: 1.11.0 — 「引用性質判準」章節「權威關係」段補「校準（實測後）」：三探針實測證實 `.claude/agents/*.md` 主文 `@-import` 不展開為內容，AGENT_PRELOAD.md 規則 12 非自動載入速查版；改述實際每次派發都會注入的觸發指引在 `document-format-rules.md`「引用穩定性規則」小節
**Version**: 1.10.0 — 規則 8 新增「守衛的實際執法邊界」節：載明三類豁免（白名單路徑僅 1 檔、測試路徑、code fence 內容——後者明示為格式示範的設計上合法路徑而非繞過）、`rule8-exempt` marker 語法與兩個合法 category、判定為增量而非全檔（附存量量測 714 檔 / 3584 個票號，據此說明規則 8 對存量是應然目標）、兩條未涵蓋的寫入管道（Bash 寫入與用戶自行編輯）；並標示早期裁定的 Version footer 例外已由 AGENT_PRELOAD 規則 12 v1.20.0 廢止，本節與規則 8 正文為唯一權威口徑。
**Version**: 1.9.0 — 新增規則 9「環境事實的記錄形式」：定義第三個失效軸（跨環境），以「操作指引 vs 實測記錄」兩類判準給出相反處置（前者改 `~` / 專案相對 / `$CLAUDE_PROJECT_DIR`，後者保留原值並標註量測環境 HOME / 平台 / 日期）；載明改寫時必須實測新路徑可解析（僅去使用者名不足）；記錄 ticket create / claim 的 WARNING 強制層及其未涵蓋範圍（worklog / 框架文件 / 規格文件仍須人工盤點）。檔頭定義句、適用情境表、補充檢查清單同步更新。
**Version**: 1.8.0 — 「引用性質判準」章節開頭補「權威關係」說明：`AGENT_PRELOAD.md` 規則 12 為本章節自動載入速查版，判準內容如有出入以本章節為準，速查版須修正對齊；同步修正速查版中誤示「Version footer 可保留 ticket ID」的過期表述（該表述沿用已廢止的舊兩類判準，與本章節第 2 類時點標注型「一律移除」判準相反）。
**Version**: 1.7.0 — 規則 8 識別符類型表新增「實證錨點型」判準：commit hash 由「否」改為「條件允許」（敘事自足 + 附日期），新增獨立章節「實證錨點型：commit hash 的條件允許」（含條件定義、sync 失效模式有害/無害對照、守衛豁免機械可判定性、與 error-patterns 清理範圍銜接、正反範例）。明訂此為 commit hash 專屬的獨立判準，非 ticket ID 五類分類的第六類，五類「一律移除」邏輯不因此鬆動；主表「識別符類型表」「允許的例外」與補充檢查清單同步更新。
**Version**: 1.6.0 — 規則 8「適用範圍」表由 9 個目錄的枚舉清單改為對齊守衛 `SCAN_PREFIX` 的全域描述（`.claude/` 全域、唯一豁免 `handoff/archive/`），修補宣告層漏列 `config/`、`lib/` 等目錄的落差，並使一致性不再依賴人工逐目錄同步。
**Version**: 1.5.0 — 規則 8「引用性質判準」全面改寫：原「依賴型 / 歷史錨點型」兩類判準汰換為「全禁原則 + 五類分類」（論證依據型、時點標注型、案例敘事主詞型一律移除；被說明對象型、測試資料型依路徑豁免）。新增兩層分流指引（簡單理由寫進註解、重要概念先寫方法論再引用）與機械可判定性說明（守衛只需判定豁免路徑，不需語意分類）；「功能字串子類」併入全禁原則不再需要獨立判準；「為何不上 regex hard hook」改寫為「硬性強制的範圍」，拆分「樣式偵測可機械化」與「改寫品質仍需人工判定」兩層；footer 來源標注改判第 2 類（時點標注型）一律移除 ID；新增「與舊兩類判準的對應」沿革記錄章節；規則 8 主表「禁止的模式」「允許的例外」兩表補五類分類欄位與對應項；補充檢查清單同步更新。
**Version**: 1.4.0 — 引用性質判準新增「判準單位：整句 / 整個引用事件」與「內聯說明的實質性判準：短標籤 vs 實質說明」兩子節，解決 AGENT_PRELOAD 規則 12 依賴型判準字面矛盾（0.2.1-W3-094 稽核發現：規則 12 字面把「來源：ID」開頭一律列依賴型，與框架內 45 處 `> 來源：ID（機制描述）` 標準寫法衝突）：新判準以「括號是否為陳述具體事實的完整子句（非僅複述主題標籤）」取代「句型/位置」作分類依據，並區分「相關文件章節列舉」（脈絡本身即指向動作，即使補說明仍建議改路徑）與「Why/Consequence 尾隨佐證」（附加來源標注，非待查清單）；正反範例補 4 組對照。
**Version**: 1.3.0 — 規則 8 新增三章節：(1)「框架 code 邏輯註解的識別符衛生」（邏輯註解禁嵌 project ticket ID，改自足 WHY 或結構化溯源載體，含正反範例）；(2)「frontmatter provenance 欄與 commit trailer 溯源慣例」（定義 `provenance` / `canonical_issue` 欄與 `Provenance` trailer，首選 `claude#NN` 形式對齊框架 GitHub Issues canonical 決策，至少 2 範例）；(3)「為何不上 regex hard hook」（識別符衛生降級為文件慣例，引 8686 處全域命中 / 322 處 source_ticket 誤殺的誤報量測）。
**Version**: 1.2.0 — 引用性質判準新增「功能字串（運行時輸出）子類」：功能字串內 ticket ID 即使屬歷史錨點型仍應清理（運行時誤導其他專案所有用戶，誤導面比 comment 廣）+ 載體處置差異表 + 正反範例。歷史 1.0–1.1 版見 git log。
**Version**: 1.1.0 — 規則 8 新增「引用性質判準」章節（依賴型禁止 vs 歷史錨點 / 設計脈絡型允許，操作判準＝移除 ticket ID 後可否理解），對齊 PC-093 `history` 豁免 + comment-writing 方法論 + 規則 7 來源標注例外，消除三方衝突。
