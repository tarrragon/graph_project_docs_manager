---
# === Identification ===
ticket_id: "0.25.1-TD-003"
version: "0.25.1"
wave: 0
type: "technical-debt"

# === Single Responsibility ===
action: "Improve"
target: "_classifyError encoding detection reliability"

# === Execution ===
agent: "parsley-flutter-developer"

# === 5W1H Design ===
who: "parsley-flutter-developer"
what: "改進 _classifyError 方法的 encoding 判斷可靠性"
when: "未來版本"
where: "lib/presentation/import/chrome_extension_import_view_model.dart:145-152"
why: "Phase 4 評估發現：使用 .toString().contains() 判斷不夠可靠"
how: "使用異常類型判斷 -> 替換字串匹配 -> 測試驗證"

# === Acceptance Criteria ===
acceptance:
  - encoding 錯誤使用異常類型判斷而非字串匹配
  - 所有現有測試通過
  - 代碼品質檢查無警告

# === Related Files ===
files:
  - lib/presentation/import/chrome_extension_import_view_model.dart

# === Priority ===
priority: "low"
risk: "low"
estimated_effort: "30 minutes"

# === Source ===
source_version: "v0.25.1"
source_ticket: "0.25.1-W3-001"
target_version: "v0.26.x"
discovery_context: "Phase 4 重構評估時發現"

# === Status Tracking ===
status: "pending"
assigned: false
started_at: null
completed_at: null
---

# Technical Debt: _classifyError Encoding Detection Reliability

## Summary

`_classifyError` 方法使用 `.toString().contains('encoding')` 判斷編碼錯誤，這種方式不夠可靠。

## Problem Analysis

### Current Code (Line 145-152)
```dart
JsonErrorType _classifyError(dynamic error) {
  if (error is FormatException) {
    return JsonErrorType.syntax;
  } else if (error.toString().contains('encoding') || error.toString().contains('Encoding')) {
    return JsonErrorType.encoding;  // 字串匹配不可靠
  }
  return JsonErrorType.syntax;
}
```

### Problems

1. 字串匹配依賴錯誤訊息的具體文字
2. 不同語言環境可能有不同錯誤訊息
3. 異常類型判斷更加可靠

## Solution Design

```dart
JsonErrorType _classifyError(dynamic error) {
  if (error is FormatException) {
    return JsonErrorType.syntax;
  }
  if (error is FileSystemException) {
    // FileSystemException with decoding failure indicates encoding issue
    return JsonErrorType.encoding;
  }
  return JsonErrorType.syntax;
}
```

---

## Execution Log

(To be filled when implementing)

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: (pending)
**Review Status**: pending
