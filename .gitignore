# See https://www.dartlang.org/guides/libraries/private-files

# Files and directories created by pub
.dart_tool/
.packages
build/
# If you're building an application, you may want to check-in your pubspec.lock
pubspec.lock

# Directory created by dartdoc
# If you don't generate documentation locally you can remove this line.
doc/api/

# dotenv environment variables file
.env*

# Avoid committing generated Javascript files:
*.dart.js
# Produced by the --dump-info flag.
*.info.json
# When generated by dart2js. Don't specify *.js if your
# project includes source files written in JavaScript.
*.js
*.js_
*.js.deps
*.js.map

# 框架 skill 隨附的 JS 腳本不是 dart2js 產物，須排除於上述通配之外。
# 未加此例外時它們在檔案系統存在但 untracked，而框架 sync-push 推的是
# git tracked 樹（git archive HEAD）——結果是推不出去也刪不掉，且每次
# push 都被孤兒偵測誤報為「本地已刪除的殘留」。
!.claude/skills/**/*.js

.flutter-plugins
.flutter-plugins-dependencies

# ── Flutter app 補充（原檔為 Dart package 模板，以下為手動補齊）──

# IntelliJ / Android Studio：專案設定屬個人環境，不入庫
.idea/
*.iml
*.ipr
*.iws

# VS Code：只保留團隊共用的設定，個人偏好與快取排除
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/extensions.json

# macOS
.DS_Store

# 各平台編譯產物（子資料夾的 build 也一併排除）
**/build/
**/.dart_tool/

# CocoaPods lock（iOS / macOS）
ios/Podfile.lock
macos/Podfile.lock

# 其他
*.log

# FVM Version Cache
.fvm/

# 注意：lib/l10n/app_localizations*.dart 是 gen-l10n 的產物，但刻意「入庫」。
# Flutter 3.32 起不再走 synthetic package，產物落在 lib/ 下；納入版控可讓
# clone 後無需先執行生成即可開啟 IDE，語系變更也會出現在 diff 中可供審閱。
# 請勿為它們新增忽略規則。

# ── Claude 框架（.claude/）的本地執行期產物，不入庫 ──
# 清單來源：.claude/lib/sync_exclude_manifest.py 的 GITIGNORE_EXPECTED
# 框架本體要入庫，僅以下子路徑排除。
.claude/.sync-conflicts
.claude/.sync-state.json
.claude/.version-release.yaml
.claude/.zhtw-mcp-skip
.claude/ARCHITECTURE_REVIEW_REQUIRED
.claude/PM_INTERVENTION_REQUIRED
.claude/TASK_AVOIDANCE_FIX_MODE
.claude/analyses
.claude/dispatch-active.json
.claude/dispatch-active.lock
.claude/handoff
.claude/hook-logs
.claude/hook-state
.claude/logs
.claude/migration-backups
.claude/plans
.claude/pm-status.json
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/state
# 框架執行期鎖檔的類別式涵蓋。上兩行的 dispatch-active.lock / scheduled_tasks.lock
# 為逐檔列舉，屬「遇到才補」——error-patterns/.allocator.lock 即因未列而漏網。
# 改以此行涵蓋整個類別，新增的框架鎖檔不需再逐一追加。
.claude/**/*.lock
.claude/sync-preserve.yaml
.claude/sync-skills.yaml
.claude/worktrees

# Python 執行期產物（全域）
.pytest_cache
.venv
__pycache__

# project-init onboard 要求的框架規則
coverage/
htmlcov/
.claude/tool-results/

# 設計畫布的組裝產物（可由 design/gen.py 重新產生）
design/docs-graph-ui-layouts.html

# Hook 執行期日誌（框架 2.42.x 起的新路徑）
.claude/hooks/hook-logs/
hook-logs/

# skill-sync 的本地同步狀態標記（各專案自己的 sync point，不入庫）
.skill-sync-base
