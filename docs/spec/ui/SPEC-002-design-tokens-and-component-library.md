---
id: SPEC-002
title: "Design token 與元件庫：所有顏色、尺寸、元件皆具名"
status: draft
source_proposal: PROP-004
created: "2026-08-27"
updated: "2026-09-02"
version: "1.2"
owner: star-anise-system-designer

domain: "ui"
subdomain: null

related_usecases: [UC-01, UC-02, UC-03, UC-04, UC-05, UC-06]
related_specs: [SPEC-001, SPEC-004]
implements_requirements: []
depends_on_domains: [layout]
---

# Design token 與元件庫：所有顏色、尺寸、元件皆具名

## 唯一硬規則

**程式碼中不得出現裸的色碼、尺寸數字或字級數字。** 每一個值都必須先被命名，
畫面只引用名字。

```dart
// 禁止
Container(color: Color(0xFF2E6F6A), padding: EdgeInsets.all(8))
Text('...', style: TextStyle(fontSize: 13))

// 要求
Container(color: AppColors.accent, padding: EdgeInsets.all(Space.sm))
Text('...', style: AppText.body)
```

**這條規則優先於 Flutter 的 `Theme`。** `Theme` 是分發機制，不是決策位置——
它讓值傳到 widget，但它不回答「這個值是什麼、為什麼是這個」。本規格定的是
決策層：先命名，再由 `Theme` 分發。

## 為什麼在 Theme 之外先決定

| 只用 Theme | 先定 token 再進 Theme |
|-----------|---------------------|
| 值散在各個 `ThemeData` 欄位，語意由 Material 的欄位名決定（`primary`／`surface`／`onSurfaceVariant`） | 值有專案自己的語意名（`accent`／`gapEdge`／`damagedDetail`），Material 欄位只是其中一個消費端 |
| 新增一個 Material 沒有對應欄位的語意（例如「邊損壞」與「詳情損壞」兩種標記）時無處可放 | 語意先存在，需要時再決定要不要映射到 Material 欄位 |
| 換色只能整組換 seed | 可單獨調整某個語意角色 |

本 App 有數個 Material 沒有對應欄位的語意角色——破洞四類的分類色、
`severity` 兩級的標記強度、`flow 未結構化` 的引導態——它們必須有名字才存在。

## 值的來源

**token 自 `design/` 的九份 artboard 萃取，不重新發明。** 那九份是 0.1 版型
定案的產物，經過比較後選定；重新發明等於丟掉那次比較。

但**萃取不等於照抄**：artboard 的間距出現 1／2／7／8／9／10／12 七種值，
那是產生器的輸出而非設計過的尺度。token 化時須歸納出一個尺度並吸收噪音，
否則等於把雜訊固化成規格。

### 已知的顏色分佈（實測九份 artboard，1280×800 無縮放）

| 色碼 | 出現次數 | 觀察到的角色 | 採用的語意名 |
|------|:---:|------|------|
| `#5C6866` | 147 | 主要文字 | `AppColors.textPrimary` |
| `#8A9694` | 116 | 次要文字／弱化 | `AppColors.textSecondary` |
| `#E2E9E8` | 70 | 分隔線／邊框 | `AppColors.border` |
| `#0F4B47` | 59 | 深色強調 | `AppColors.accentStrong` |
| `#C7D2D0` | 47 | 邊框（較深） | `AppColors.borderStrong` |
| `#2E6F6A` | 47 | seed／強調 | `AppColors.accent` |
| `#FFFFFF` | 37 | 底 | `AppColors.surfaceBase` |
| `#E9EEED` | 35 | ~~表面層次~~ 徽章／標籤底 | `AppColors.surfaceChip` |
| `#E0EFED` | 27 | ~~表面層次~~ 圖示容器底 | `AppColors.surfaceIconTint` |
| `#F4F6F5` | 19 | ~~表面層次~~ 側欄背景（頁面級） | `AppColors.surfaceSidebar` |
| `#DCE4E2` | 6 | ~~表面層次~~ 分段控制軌道底 | `AppColors.surfaceSegmentTrack` |
| `#181C1B` | 21 | 標題文字 | `AppColors.textTitle` |
| `#2E6F3E` | 13 | 成功 | `AppColors.success` |
| `#8A5A00` / `#FBF1DF` | 11／3 | 警告／警告底 | `AppColors.warning` / `AppColors.warningSurface` |
| `#B3261E` / `#FBE9E7` | 10／6 | 錯誤／錯誤底 | `AppColors.error` / `AppColors.errorSurface` |

**角色欄是觀察不是定義**——命名時須確認每個色實際承擔的語意，而非依出現次數
推定。四個表面層次色（`E9EEED`／`E0EFED`／`F4F6F5`／`DCE4E2`）是否真的需要
四階，是本規格要回答的問題之一。

**已回答（W1-004，2026-08-27）**：需要四個，但**不是四階**。逐一 grep artboard
上下文實測後確認，四者是互不相關的四個元件角色（徽章底／圖示容器底／側欄背景／
分段控制軌道底），不構成同一序列的明度階。原表把它們併成一列標「表面層次」是
依出現次數推定的結果，正是本節警告的那種推定——四個值恰好都是淺灰綠，看起來
像一個階梯。上表已改為逐色一列並標明實測角色，刪節線保留原措辭以顯示更正處。

全部 17 色皆採用，無未採用者。命名落點：`lib/tokens/colors.dart`（三層結構，
原始值層 `_Palette` 私有、語意層 `AppColors` 公開，每個語意名的回溯映射在該檔
dartdoc 逐條列出）。

### 已知的尺寸分佈

| 類別 | artboard 實測值 |
|------|----------------|
| 字級 | 9.5、10、**10.5**、11、**11.5**、12、**12.5**、13、14、15、19 |
| 圓角 | **4**、5、**6**、7、8、**9**、**11**、**12** |
| 間距（gap） | 1、2、**5**、**6**、7、8、9、10、12、**14**、**16** |

（量測於 2026-08-27，`design/` 九份 artboard；粗體為本次補入的值。命令：

```bash
grep -ohE 'font-size: *[0-9.]+px'    design/*.dc.html | grep -oE '[0-9.]+' | sort -n -u
grep -ohE 'border-radius: *[0-9.]+px' design/*.dc.html | grep -oE '[0-9.]+' | sort -n -u
grep -ohE '(^|[^-])gap: *[0-9.]+px'   design/*.dc.html | grep -oE '[0-9.]+' | sort -n -u
```

）

**更正說明（W1-004 覆核，2026-08-27）**：原表三類皆有缺漏，字級一類的缺漏會
影響歸階決策——**字元類寫成 `[0-9]+` 只抓整數，四個半像素值（9.5／10.5／11.5／
12.5）整批消失**，而其中 `10.5` 出現 58 次，是全設計最高頻的字級，`11.5` 55 次
次之。原表把最常用的兩個字級當成不存在。

同一個抽取錯誤在本次實作票中重現了一次——實作代理人獨立量測，得到與原表同樣
的整數清單（並額外誤記一個不存在的 `9`）。**兩次量測一致不代表正確，只代表用了
同一個有缺陷的方法。** 圓角與間距的缺漏值皆為整數，屬單純漏列，不涉此機制。

`lib/tokens/typography.dart` 的階值（10／12／14／19）不受影響——半像素值落在既有
分階區間內；受影響的是該檔 dartdoc 記錄的「各階吸收哪些原始值」映射表，該表依
錯誤清單撰寫，已另票更正。

## 命名層次

三層，各自回答不同問題：

| 層 | 回答 | 例 |
|----|------|-----|
| **原始值** | 這個值是多少 | `teal600 = #2E6F6A` |
| **語意** | 這個值代表什麼 | `accent = teal600` |
| **元件** | 這個元件用什麼 | `NavItem.selectedBg = accent` |

畫面只引用**語意層與元件層**，不得引用原始值層。原始值層的存在是為了讓
「同一個顏色被兩個語意共用」這件事顯性化——若 `accent` 與 `linkText` 都是
`teal600`，換色時才知道要不要一起換。

## 元件庫的範圍

**各畫面的材料一律取自元件庫，不自行組裝。** SPEC-001 的 31 個狀態橫跨七個
畫面，若各畫面自行拼裝，同一個概念會長出七種樣子。

**元件清單與逐元件契約自 2026-09-02 起由 `SPEC-004-component-library.md` 承載**
（第 3 章元件清單總表、第 4 章逐元件九欄位契約、第 5 章容器元件排列不變式），
本節不再維護清單，避免雙份漂移。本節原列的八類元件（空狀態、阻擋狀態、載入態、
損壞標記、導覽項、專案切換浮層、節點卡、徽章）在 SPEC-004 §3.6 逐一對照其歸屬，
其中「節點卡」與「專案切換浮層」經存在必要性檢視拆分、「損壞標記」更名，
結論標「提案」待 PM 核定。實作中發現的新元件回填 SPEC-004 第 3 章，不回填本節。

**空狀態與阻擋狀態必須是兩個元件，不是一個。** 空狀態說「這裡目前沒有東西」，
阻擋狀態說「這個專案不適用本 App」——退出路徑與語氣皆不同，合併會讓
FR-03 與 FR-07 的驗收互相污染。此決定為 SPEC-004 §3.3 存在必要性檢視的第 1 項前提，
SPEC-004 不得推翻。

## 功能需求

### FR-01: 程式碼中無裸值

| 項目 | 值 |
|------|-----|
| 優先級 | P0 |
| 驗收 | `lib/` 下（`lib/tokens/` 除外）grep 不到 `Color(0x`、`#[0-9A-Fa-f]{6}`、以及 `fontSize:`／`borderRadius:`／`EdgeInsets` 後方接字面數字。此檢查可機械化，應納入 CI |

### FR-02: 每個 token 有語意名

| 項目 | 值 |
|------|-----|
| 優先級 | P0 |
| 驗收 | 顏色、字級、間距、圓角四類皆有具名常數表；語意層的每個名字可回溯到原始值層的某個名字 |

### FR-03: 畫面只用元件庫

| 項目 | 值 |
|------|-----|
| 優先級 | P1 |
| 驗收 | SPEC-001 §1–§7 的每個狀態，其顯示欄的內容皆由元件庫元件組成；新增元件須先進元件庫再被畫面引用 |

### FR-04: 間距與字級為離散尺度

| 項目 | 值 |
|------|-----|
| 優先級 | P1 |
| 驗收 | 間距與字級各自為一組有限的具名階（如 `Space.xs/sm/md/lg`），不接受任意數字；artboard 的七種間距值須歸納為階並記錄映射 |

## 設計約束

- token 定義集中於 `lib/tokens/`，該目錄是唯一允許出現裸值的位置
- `Theme` 由 token 建構，不反向——不得先設 `ThemeData` 再從中取值命名
- 本規格是 `design/` artboard 的下游：artboard 是視覺決策的產物，
  本規格是它的結構化表達。兩者不符時**以 artboard 為準並更新本規格**
- 元件庫清單不宣稱窮盡，實作中補入者須回填本規格

## 變更歷史

| 版本 | 日期 | 變更 |
|------|------|------|
| 1.0 | 2026-08-27 | 初版。起因為 0.1 開票後發現七張畫面票會各自決定尺寸與顏色；同時發現 PROP-004 §版型定案 的 token 描述（圓角 10/12、字級 12–22）與 artboard 實測（圓角 5/7/8、字級 10–19）不符，成因正是這些值沒有單一住址 |
| 1.1 | 2026-09-02 | §元件庫的範圍 的元件清單改為指向 SPEC-004（0.1.0-W1-044.1）；狀態數依 SPEC-001 v1.3 更正為 30；「空狀態與阻擋狀態必須是兩個元件」保留為 SPEC-004 存在必要性檢視的前提；`related_specs` 補 SPEC-004 |
| 1.2 | 2026-09-02 | 狀態數依 SPEC-001 v1.4（§1 新增「已選格」疊加態）更正為 31（`0.1.0-W1-048`） |
