---
id: 0.3.1-W1-086
title: 分析 tracking_schema 正則方言標示 python-re 未註明 ASCII／Unicode 語意，跨語言消費端（Dart 等）對 \d、\w 行為不一致
type: ANA
status: pending
version: 0.3.1
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W1-084 NeedsContext N3
  final_decision: 本專案以 ASCII 為準，框架缺口開 ANA
  rationale: 框架資產跨 consumer，不在本地逕改
who:
  current: 主線程
  history: {}
what: 判定框架是否應在 id_pattern_dialect 與 carrier_path_patterns 的方言說明中註明 ASCII 語意（或改用 re.ASCII 編譯並於 conformance 測試斷言），並以 observe 附於 tarrragon/claude#99；列出現有模式中受影響的 \d、\w 用法
when: 0.3.0 Corpus 實作期間或之後；不擋 0.3.0（本專案已以 ASCII 為準）
where:
  layer: 待定義
  files:
  - .claude/skills/doc/doc_system/core/tracking_schema.py
  - .claude/skills/doc/doc_system/commands/schema.py
why: 0.3.0-W1-084 NeedsContext N3：Python 3 re 對 str 的 \d 預設匹配 Unicode 數字，Dart RegExp 只匹配 ASCII；方言名稱未帶旗標，改錯無編譯期徵兆。用戶裁示本專案以 ASCII 為準（SPEC-006 D9），框架層缺口另票追蹤
how:
  task_type: Analysis
  strategy: '兩條判準逐條套用 → 盤點模式中的 \d／\w → 候選（文件註明／re.ASCII 編譯／方言改名）→ observe #99'
acceptance:
- '[ ] 框架問題兩條判準逐條套用並記錄'
- '[ ] 現有 id_pattern 與 carrier_path_patterns 中的 \d／\w 用法已盤點'
- '[ ] 至少兩個候選附利弊，結論經用戶裁決'
- '[ ] 觀測已附於 tarrragon/claude#99'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
chain:
  root: 0.3.1-W1-086
  parent: null
  depth: 0
  sequence:
  - 86
---

# Execution Log

## Task Summary

判定框架是否應在 id_pattern_dialect 與 carrier_path_patterns 的方言說明中註明 ASCII 語意（或改用 re.ASCII 編譯並於 conformance 測試斷言），並以 observe 附於 tarrragon/claude#99；列出現有模式中受影響的 \d、\w 用法

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending
