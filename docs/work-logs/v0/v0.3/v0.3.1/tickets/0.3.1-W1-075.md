---
id: 0.3.1-W1-075
title: '修復 agent-dispatch-validation-hook 審查模式關鍵字豁免：拆除全文子字串比對，審查派發只認 Dispatch-Mode: readonly 首行協議'
type: IMP
status: pending
version: 0.3.1
wave: 1
priority: P1
parent_id: null
children: []
blockedBy:
- 0.3.1-W1-074
relatedTo:
- 0.3.1-W1-072
spawned_tickets: []
source_ticket: null
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: Layer 1 版本規劃
  final_decision: 框架缺陷在本專案落地修
  rationale: 用戶裁決 0.3.0 開框架修復票；flutter_balance hook log 佐證
who:
  current: thyme-python-developer
  history: {}
what: '修復 agent-dispatch-validation-hook 審查模式關鍵字豁免：拆除全文子字串比對，審查派發只認 Dispatch-Mode: readonly 首行協議'
when: PM 以 Agent 工具派發實作代理人、PreToolUse 觸發本 hook 時
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/hooks/agent-dispatch-validation-hook.py
  - .claude/hooks/tests/test_agent_dispatch_validation_hook.py
  - .claude/pm-rules/worktree-operations.md
  - .claude/references/agent-dispatch-template.md
  - .claude/skills/ticket/CHANGELOG.md
  - .claude/skills/ticket/SKILL.md
why: 'flutter_balance 2026-09-24 hook log 佐證：0.2.2-W1-005 派發 parsley 未帶 isolation，路徑分類正確得 other=True，卻因 prompt 收尾行「填 Solution（含 Phase 4 評估結論）」的「評估」命中 REVIEW_MODE_KEYWORDS 子字串而放行；同日 W1-002 三次派發（含帶 isolation 的兩次）全記為審查放行，證明關鍵字判斷位於 isolation 判斷之前；10:45 另一筆 prompt 不含關鍵字時正常硬擋。「Phase 4 評估」是 quality-baseline 規則 2 與 dispatch 骨架要求的實作票收尾標準用語，因此幾乎每一次實作派發都被豁免——守衛失效方向為放行，與輸入本來就合規同形，一年無人察覺。canonical #36 的驗收方向明寫豁免判準須「非關鍵字比對」，落地時新增了 Dispatch-Mode: readonly 首行協議並在註解批評關鍵字法，卻未拆除關鍵字分支，等於兩套並存、寬的那套先跑。'
how:
  task_type: Implementation
  strategy: '刪除 REVIEW_MODE_KEYWORDS 與 _is_review_mode_prompt 及 main() 第一個放行分支，審查豁免只剩既有的 _is_dispatch_mode_readonly_prompt（首行 + 固定前綴 + 固定值三條件）；放行分支順序改為 readonly 首行 → isolation==worktree → 路徑分類，確保帶 isolation 的派發不會被記成審查放行。測試依 E2：prompt 含「填 Solution（含 Phase 4 評估結論）」且無 isolation 的 parsley 派發必須 exit 2；首行 Dispatch-Mode: readonly 者放行；既有審查關鍵字測試改寫為首行協議。同步 worktree-operations.md 與 agent-dispatch-template.md 中對 W10-084 關鍵字豁免的描述改為首行協議。review 骨架輸出首行宣告由 W1-074 一併處理（共用 dispatch_skeleton.py），本票 blocked-by 之以免交集'
acceptance:
- '[ ] hook 不再含 REVIEW_MODE_KEYWORDS 與全文子字串審查判定；審查豁免唯一路徑為首行 Dispatch-Mode: readonly'
- '[ ] E2 對照測試：prompt 含「Phase 4 評估結論」且無 isolation 的實作代理人派發 exit 2；首行宣告 readonly 者放行；帶 isolation 者的 log 記為 worktree 放行而非審查放行'
- '[ ] worktree-operations.md 與 agent-dispatch-template.md 對關鍵字豁免的描述改為首行協議；hook 測試與 ticket skill 測試全綠；CHANGELOG 與 SKILL.md 版本遞增'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
chain:
  root: 0.3.1-W1-075
  parent: null
  depth: 0
  sequence:
  - 75
---

# Execution Log

## Task Summary

修復 agent-dispatch-validation-hook 審查模式關鍵字豁免：拆除全文子字串比對，審查派發只認 Dispatch-Mode: readonly 首行協議

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.3.0-W1-072,0.3.0-W1-074 | chars: 1968 -->

### Task Reference
- 0.3.0-W1-074 what: 實作 ticket track dispatch 骨架的 isolation 感知：worktree 變體收尾改 finish 並註明 PM 代跑，非豁免路徑未帶 isolation 時警告
- 0.3.0-W1-072 what: 補寫 實作代理人派發位置的單一判準表（isolation:worktree／PM 前台／留 main）與「禁止在共用主工作樹切分支」條款

### Rationale Chain
- 0.3.0-W1-074 why: SKELETON_TEMPLATE_NORMAL 收尾固定寫 ticket track complete，沒有 isolation 分支。worktree 隔離派發下 complete 會被 CC runtime 隔離守衛條件性拒絕（agent-dispatch-decision.md 專節，PC-SCLK-002），框架已備 finish 別名與 PM 代跑流程，但只寫在文件與 agent-dispatch-validation-hook 的提示裡，骨架這個唯一權威輸出不知道自己正在替 worktree 派發產 prompt。結果是 PM 每次都要手改骨架收尾句，漏改則代理人在收尾被擋、整票停在 in_progress。骨架也不看 where.files：對 lib/ test/ 的票產出與 .claude/ 票相同的 prompt，PM 沒有任何提示該帶 isolation。
- 0.3.0-W1-072 why: flutter_balance 0.2.2 W1 與本專案同日各自撞到：PM 派發實作代理人時沒有單一入口可查「這次該用 isolation:worktree、主工作樹開分支、還是留在 main」。判準實際上散在四處且彼此只各講一段——風險分級表（parallel-dispatch-worktree-details）講 lib/ test/ 須 worktree；template 講 push-first 與 finish 別名；agent-dispatch-decision 講 worktree 內 complete 必敗須 PM 代跑；branch-verify-hook 拒絕訊息則建議「/worktree create 或 git checkout -b」二擇一。代理人在 main 被 branch-verify-hook 擋下後選了最近的路：在共用主工作樹 git checkout -b 並提交 7 個 commit，主工作樹 HEAD 離開 main，並行 session 被帶到錯誤分支。現行條文沒有一句「禁止在共用主工作樹切分支」。

### Related Files
- .claude/skills/ticket/ticket_system/commands/track_dispatch.py  # from 0.3.0-W1-074
- .claude/skills/ticket/ticket_system/lib/dispatch_skeleton.py  # from 0.3.0-W1-074
- .claude/skills/ticket/tests/test_dispatch_common.py  # from 0.3.0-W1-074
- .claude/pm-rules/parallel-dispatch.md  # from 0.3.0-W1-072
- .claude/references/agent-dispatch-decision.md  # from 0.3.0-W1-072
- .claude/references/parallel-dispatch-worktree-details.md  # from 0.3.0-W1-072

### Source Acceptance (reference)
- [ref] [ ] ticket track dispatch --isolation worktree 產出的骨架收尾句為 finish，含被拒時交還 PM 代跑一句；不帶時維持 complete，兩者產物不同（E1 對照測試）  # from 0.3.0-W1-074
- [ref] [ ] where.files 含非豁免路徑且未帶 --isolation 時 stderr 有 WARNING 且 exit 0、骨架照常輸出；全為 .claude/ docs/ 時無警告  # from 0.3.0-W1-074
- [ref] [ ] agent-dispatch-template.md 骨架段與兩變體常數逐字一致；ticket skill 測試全綠、CHANGELOG 與 SKILL.md 版本遞增  # from 0.3.0-W1-074
- [ref] [ ] parallel-dispatch.md〈派發前檢查清單〉之前或之內新增一張單一判準表：列（寫入 lib/ test/ 等非豁免路徑的實作派發／僅寫 .claude/ docs/ 頂層文件／唯讀審查）× 欄（isolation 值、派發前置 push-first、收尾指令 finish 與 PM 代跑、禁止事項），每格引用既有權威條文而非重述  # from 0.3.0-W1-072
- [ref] [ ] 新增明文條款「禁止代理人與 PM 在共用主工作樹（主 repo 的 checkout）切換或建立分支」，寫明後果（主工作樹 HEAD 離開 main，並行 session 與 hook 以錯誤分支為基準）與正確替代（isolation:worktree 重派或 /worktree create）  # from 0.3.0-W1-072
  ... (truncated, see source ticket)
