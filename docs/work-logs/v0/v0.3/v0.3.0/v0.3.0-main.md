<!-- 注意：本文件禁止使用 emoji（交接文件規範） -->

# v0.3.0 版本工作日誌

**版本號**: v0.3.0
**代號**: Corpus
**登記日期**: 2026-09-23
**目標**: 解析與破洞判定——逐行 frontmatter 語意解析既有語料，真破洞與合法非節點分類正確
**狀態**: active（規劃波 Step 1 完成；#99 第三項已裁決，恢復至 Step 2；schema 匯出見 `0.3.0-W1-079`）

---

## 前情提要

0.2.0（Gate）於 2026-09-23 達成契約：使用者選取的真實資料夾依兩訊號進入正確 gate 分支。本版接下一段資料管線：對可載入的專案解析語料、判定破洞。

範圍由 PROP-005〈版本序列〉§0.3 定義；阻擋清單含上游 `*_REQUIRED_FIELDS` 語意裁決，開工前須先解。整合測試兩項見 `docs/todolist.yaml` 本版 notes。

---

## 開發日誌

### 2026-09-23：登記

0.2.0 發版時凍結閘門依動詞前移的新功能票以本版為收件目標。尚未開工。
- 2026-09-24: 0.3.0-W1-016 完成 -- 補充 版本範圍收斂方法論：patch 收斂承諾為雙向（規格領先實作→實作票；實作領先規格→文件票），第一問與契約審查產出同步
- 2026-09-24: 0.3.0-W1-017 完成 -- 修正 版本範圍收斂方法論〈Patch 生命週期〉：「不開下一個 patch 承接剩餘」限定於存在開放後繼時；無後繼時依溢出算法登記 patch+1／minor+1
- 2026-09-24: 0.3.0-W1-020 完成 -- 補充 版本範圍收斂方法論：契約審查的落差清單須明寫查核範圍（納入的規格檔清單），未納入者不得寫「0 缺口」；待形式化條款不構成已發布條款
- 2026-09-24: 0.3.0-W1-040 完成 -- 同步 pubspec.yaml 版本至 0.3.0（version-release finish 啟用 0.3.0 時已 bump 但啟用提交未納入，main 上 PM 不可提交該檔）
- 2026-09-24: 0.3.0-W1-071 完成 -- 補充 版本範圍收斂方法論〈Patch 生命週期〉與 runbook 入口 B：零交付凍結容器的結案路徑（不打 tag、前移後標 completed）與判準

---

### 2026-09-24：框架修復票（主題「框架修復」，非 Corpus 契約）

兩個接手 session 一日內驗證 runbook 時露出的框架問題，依用戶裁決在本版開實作票落地（每張掛 `--dedup-checked` 對應 canonical issue，不帶 blocker；做完推技能庫與 canonical）：

| 票 | 問題 | issue |
|----|------|-------|
| `0.3.0-W1-041`（進行中） | 前移撞號：migrate 改取下一可用序號、finish dry-run 判 FAIL | #55 |
| `0.3.0-W1-042` | 建票自動提交 update-ref 中斷不清鎖、CLI 仍回 OK | #55 |
| `0.3.0-W1-043` | complete 自動提交整檔納入共用工作日誌（多 PM 吸入） | #77 |
| `0.3.0-W1-044` | finish 啟用漏提交 pubspec（提交範圍改差集） | #55 |
| `0.3.0-W1-045` | sync-pull 衝突時 base SHA 不推進 | #88 |
| `0.3.0-W1-046` | ANA 空章節可 complete（dispatch 注入段誤判、缺重現實驗必填） | #55 |
| `0.3.0-W1-047` | `version-release register` 子命令 | #16 |
| `0.3.0-W1-072` | 派發位置單一判準表（isolation:worktree／PM 前台／留 main）+「禁止在共用主工作樹切分支」條款 | #101 |
| `0.3.0-W1-073` | branch-verify-hook 拒絕訊息刪「git checkout -b」二擇一，改分角色指引（blocked-by W1-072） | #101 |
| `0.3.0-W1-074` | `ticket track dispatch --isolation`：worktree 變體收尾用 finish + PM 代跑；非豁免路徑未帶 isolation 時警告；review 骨架輸出 `Dispatch-Mode: readonly` 首行 | #101 |
| `0.3.0-W1-075` | agent-dispatch-validation-hook 拆除審查關鍵字全文子字串豁免，只認 `Dispatch-Mode: readonly` 首行；放行順序 readonly → isolation → 路徑（blocked-by W1-074） | #36 |

W1-072～075 四張來自 flutter_balance session 同日轉交（其 0.2.2-W1-005 代理人在 main 被 branch-verify-hook 擋下後，依 deny 訊息第 2 項在共用主工作樹 `git checkout -b` 提交 7 個 commit，主工作樹 HEAD 離開 main）。本 consumer 核實：風險分級表已定義 lib/ test/ 須 worktree，缺的是單一入口、禁止條款、hook 訊息與骨架三處未指向它。該次派發本應被 `agent-dispatch-validation-hook.py` 硬擋卻通過，對方 hook log 證實是 prompt 收尾「Phase 4 評估結論」的「評估」命中 `REVIEW_MODE_KEYWORDS` 子字串——這個詞是規則 2 要求的收尾用語，等於守衛對實作派發全面放行（E2 型：放行與輸入本就合規同形）。#36 落地時已加首行協議卻沒拆關鍵字分支。觀測分別附於 canonical #101（派發側）與 #36（豁免判準）。

另 `0.1.1` 於同日結案不發版（零交付），42 張前移至本版（序號保留），屬 0.1 品質改善溢出，非本版契約；Corpus 開工前先解 PROP-005 §0.3 上游阻擋，框架修復票可與之並行（`where.files` 全在 `.claude/`，與 lib/ 無交集）。派發框架票用 thyme-python-developer、worktree 隔離；合入後 `skill-sync push <skill>` 與 sync-push 需用戶授權。
- 2026-09-24: 0.3.0-W1-041 完成 -- 修復 發版前移撞號：ticket migrate 目標已存在時改取目標版本下一可用序號並記 migrated_from；version-release finish 的 dry-run 與正式執行對碰撞判 FAIL

### 2026-09-24：技能庫同步（skill-sync pull）

sync-push 的〈skill 庫檢查〉回報 9 個 skill 與發佈庫（claude-skills.git，比對時的 HEAD 是 `ab0eca110`）分歧。每一個都用兩項獨立證據判定方向：(1) `skill-sync pull` 狀態報告的三方基準判定是「local unchanged since last sync」；(2) 本地 CHANGELOG 最新一條出現在遠端 CHANGELOG 歷史中，遠端在它之上另有新條目。兩項一致，全數是遠端較新，沒有本地較新或疑似 CDN 快取的項目。pull 時沒有衝突，各 skill 的變更檔數與 pull 預覽相符；skill-sync 自身的測試 214 passed；複查報告為 `All 61 checked skill(s) are up to date`。

| 名稱 | 本地版 | 遠端版 | 方向 | 動作 | 結果 |
|------|--------|--------|------|------|------|
| broken-link-check | 2.5.0 | 2.5.1 | 遠端較新 | `skill-sync pull` | 3 檔更新 |
| compositional-writing | 1.29.0 | 1.33.0 | 遠端較新 | `skill-sync pull` | 24 檔（新增 2、修改 22） |
| multi-round-review | 2.17.0 | 2.21.0 | 遠端較新 | `skill-sync pull` | 31 檔更新 |
| saas-tech-selection | 1.4.0 | 1.4.1 | 遠端較新 | `skill-sync pull` | 7 檔更新 |
| skill-design-guide | 1.23.1 | 1.23.2 | 遠端較新 | `skill-sync pull` | 8 檔更新 |
| skill-sync | 1.24.1 | 1.24.2 | 遠端較新 | `skill-sync pull`（最後執行） | 6 檔更新，測試 214 passed |
| spec | 1.8.0 | 1.8.1 | 遠端較新 | `skill-sync pull` | 2 檔更新 |
| tdd | 2.12.0 | 2.12.1 | 遠端較新 | `skill-sync pull` | 12 檔更新 |
| ux-design-evaluation | 1.11.12 | 1.11.14 | 遠端較新 | `skill-sync pull` | 12 檔更新 |

在 9 個 skill 中，有 7 個遠端新增的條目是同一次術語校正（「判準」改為「判斷標準」）。compositional-writing 另外新增兩張 principle 卡（`ordinal-labels-are-the-authors-index`、`counted-summary-delivers-only-the-length`），multi-round-review 的 Round 1-A 摘要也配合新增對應的兩個類別。ux-design-evaluation 另有 1.11.14 的可攜性修正：原本引用其他 skill 判定規則的地方，改成直接寫出規則內容。

### 2026-09-24：Corpus 規劃波 Step 1（version-bootstrap）

| 項 | 結果 |
|----|------|
| 提案 | 僅 PROP-005 §0.3；依賴檢查 `[OK] 0.3.0 無跨提案依賴排序矛盾` |
| UI 判別 | 非 UI 版本（兩項整合測試驗解析與分類輸出；破洞報告接真資料依 PROP-005 屬 0.6+），Step 2 UI 前置與 Step 4.5 地基波不適用 |
| 上游阻擋 `flutter_balance:0.2.1-W3-1131` | 該票已 close，分析遷至 `tarrragon/claude#99`；第三項（完整性定義語意）至本日仍待裁 |
| 阻擋實際範圍 | 兩項整合測試的判定依據為 carrier 完整路徑模式與 frontmatter 有無，不依賴欄位集合；依賴裁決者僅 EVT-CORPUS-003 `lostFields` 與診斷「缺欄位」類 |
| 用戶裁決 | 先推動裁決再開工 Corpus 核心（不採「切開後開工」）；`0.3.0-W1-025` 維持溢出不入契約 |

推動動作 `0.3.0-W1-076`：以框架解析函式量測六專案語料（FlowStep 48 步鍵存在 100%、值非空 15%～50%，兩 consumer 實際採「鍵必須存在、值可為空」），觀測附於 https://github.com/tarrragon/claude/issues/99#issuecomment-5808224146 。裁決追蹤 `0.3.0-W1-078`（trigger：#99 第三項轉已裁），完成後回到 Step 2。量測時撞到 `parse_frontmatter` 對非 UTF-8 檔拋例外，建框架修復票 `0.3.0-W1-077`；Corpus 的 Dart 解析器同樣須定義非 UTF-8 檔的處置（Step 2 spec 納入）。
- 2026-09-24: 0.3.0-W1-076 完成 -- 推動 framework issue #99 第三項（完整性定義語意）裁決：附本 consumer 消費需求與實測

### 2026-09-24：#99 第三項裁決（WRAP 完整模式，用戶裁定）

| 項 | 裁決 |
|----|------|
| 語意 | 欄位必須存在，值可為 `null` 或空清單（代表明確沒有）；更嚴格的值規則屬各型別 validator |
| 五型 | PROP／SPEC／UC = {id, title, status}；DomainBundle = {id, domain}；Ticket 的權威在 ticket skill |
| traverses | 進入上游 FlowStep 集合 |
| 匯出 | 集合要匯出到 `tracking_schema.json`（現行 JSON 沒有任何完整性鍵） |

裁決記錄在 `0.3.0-W1-078` 與 https://github.com/tarrragon/claude/issues/99#issuecomment-5808438098 ；PROP-005 §0.3、domain-map §9、EVT-CORPUS-003 已回填。框架實作票為 `0.3.0-W1-079`（schema 改語意、補五型最小集、匯出 JSON）。完成後推送到 canonical 需要用戶授權。**Corpus 規劃波恢復至 Step 2**；`lostFields` 的實作以 W1-079 完成為前提，兩項整合測試不受影響。
- 2026-09-24: 0.3.0-W1-078 完成 -- 確認 #99 第三項裁決結果並回填 PROP-005 §0.3 阻擋描述，恢復 0.3.0 規劃波 Step 2

### 2026-09-24：規劃波 Step 2（SPEC-006）

建立 `docs/spec/corpus/SPEC-006-corpus-parsing-and-gap-classification.md`（FR-01～07、NFR-01）。範圍涵蓋 Corpus（解析）與 Diagnostics（破洞分類）兩個 domain：依 domain-map §2，「這算不算破洞」要同時看解析結果與 carrier，歸 Diagnostics。本版非 UI，Step 2 的 UI 前置檢查不適用。

用戶裁決四項設計決策：

| # | 問題 | 裁決 |
|---|------|------|
| D1 | JSON 的 `carrier` 是人讀描述文字，App 無法比對 | 上游 schema 新增機器可比對的路徑模式欄位（`0.3.0-W1-080`，blockedBy W1-079） |
| D2 | 沒有 frontmatter 的檔案無法用 `id_pattern` 判型，carrier 重疊時（domain-map.md 同時符合 SPEC 與 DomainBundle）歸哪一型 | 具體度較高的模式優先；有 frontmatter 時仍以 `id_pattern` 判型 |
| D3 | 47／1243／130 是 2026-08-27 的即時語料數字 | 重新量測並凍結 manifest（比照 0.2.0）；預期分類須由獨立於待測實作的規則產生 |
| D4 | 非 UTF-8 檔 | 依 carrier 分流：路徑落在 carrier 內就報破洞，原因為編碼；落在外面就不報；不採寬鬆解碼 |

D2 修正了 domain-map §7 的一處隱含矛盾：「以 id_pattern 判型、不採 carrier-first」只對有 frontmatter 的檔案成立，破洞偵測的對象正好全無 frontmatter。

W1-079 由 thyme-python-developer 在 worktree 實作。覆核時發現 `validate.py` 對 EVT 四個純量欄位的「值不能為空」檢查被移除（`category: null` 會通過驗證），已退回修正。
- 2026-09-24: 0.3.0-W1-079 完成 -- 實作 #99 第三項裁決：圖譜 schema 完整性集合改為「欄位必須存在、值可空」語意、補五型最小集並匯出至 tracking_schema.json

W1-079 合入 main（merge 9a4abb994），PM 覆核 310 passed。where.files 兩度漏列實際要改的檔案（`commands/schema.py`、`tests/test_validate_event.py`），代理人都遇 hook 停手回報，PM 補範圍後續作。收尾時身份檢查擋下 PM 代完成，改用 guard 的合法出口（`set-who --current 主線程`，history 保留 thyme），缺口即 `0.3.0-W1-074`。canonical 推送（`skill-sync push`／`sync-push`）待用戶授權。

### 2026-09-24：SPEC-006 多視角審查與 v1.1

用戶要求進 Step 2.5 前先審查。basil-writing-critic 與 linux 並行審查 v1.0，兩者結論皆為「須先修正」。兩位都指出的阻擋項：FR-06 與 FR-07 對 YAML 錯誤的處理互相矛盾。linux 另外指出三項：D2 的具體度無法實作，以及 IT-1、IT-2 都有省力路徑（實作錯了，測試仍然綠燈）。

| 裁決 | 內容 |
|------|------|
| A | 沒拿到可用 frontmatter 的檔案（五種原因，含 YAML 語法錯誤）統一依 carrier 分流；EVT-DIAGNOSTICS-001 `parseFailure` 定義同步改寫 |
| B | 具體度二層比較（字面段數多者優先，再比跨多段萬用字元少者），平手時標記 schema 歧義；W1-080 補「現有型別兩兩不平手」驗收 |
| C | IT-2 依 manifest 實體化真實檔案樹再掃描，預期值由獨立的 Python 參照實作依 FR-06 產生，manifest 必含四類樣本 |

v1.1 另依審查補上：失敗分類擴為五種、IT-1 改與框架函式逐檔比對、FR-07 總數守恆、〈前置依賴〉節。自我覆核修正 FR-04：全部失敗檔都記入 `parseErrors`，只有真破洞發出 EVT-CORPUS-003，與事件定義一致。

審查中查到 App 沒有內建型別表的實作（W1-035 降級路徑只在規格層），而五個語料專案多數沒有可用 JSON，IT-2 實際依賴它，因此建 `0.3.0-W2-001`。範圍外的「id 不合法歸類」綁票 `0.3.0-W1-081`。用戶要求 v1.1 再覆核一輪（linux）後才進 Step 2.5。

### 2026-09-24：SPEC-006 v1.2～v1.3 覆核收斂

用戶要求每一版都覆核，共再跑了三輪 linux 唯讀覆核。

| 輪次 | 覆核對象 | 高嚴重度發現 | 裁決或處置 |
|------|---------|------------|-----------|
| v1.1 覆核 | 規格本體 | N1：EVT-CORPUS-003 的發送需要 Diagnostics 先判定，與依賴方向相反，形成循環；N3：援引的降級路徑 SPEC-001 沒有定義，而且現有 JSON 全部缺路徑模式欄位 | N1：「路徑對型別」改為 Schema 公開查詢，由 Corpus 呼叫（D7）；N3：JSON 版本不高於內建版本時，從內建表補路徑模式（D8），SPEC-001 追修開 `0.3.0-W1-082` |
| v1.2 覆核 | 規格 + 同步文件 | domain-map §2 的 Diagnostics → Schema 在 v1.2 之後沒有用途 | 用戶裁決刪除這條邊；Corpus → Schema 理由、§2.6.3、§4.2 同步改寫 |
| v1.3 覆核 | 同步文件字面 | 無（結論：可進 Step 2.5） | 四個小項直接修正：EVT-CORPUS-003 補平手與取不到集合時 `lostFields` 為空、刪除過期的「型別待定」註記；EVT-DIAGNOSTICS-001 補平手也產生破洞；PROP-005 Reality Test 加註量測時點 |

**Step 2 完成**：SPEC-006 v1.3。跨三個 domain（Schema 查詢、Corpus 解析、Diagnostics 產生破洞），前置依賴為 W1-080、W2-001、W1-082。

### 2026-09-24：規劃波 Step 2.5、2.6

**Step 2.5（domain 規劃）**：domain-map §3 新增 Schema、Corpus、Diagnostics 三個 bundle 的不變式清單（16 條，每條對應 SPEC-006 FR），§8 新增 SPEC-006 FR 歸屬表（全部有歸屬），§5 新增 `lib/schema/`、`lib/corpus/`、`lib/diagnostics/` 的 import 方向底線。三個目錄尚不存在，所以 import 鏈無從驗證，改列為各實作票的驗收。發現既有 Schema 邏輯位於 `lib/screens/domain_view/`，建 `0.3.0-W1-083`（P3）評估是否遷移。

**Step 2.6（資料契約）**，兩個旗標的判定：

| 旗標 | 判定 | 理由 |
|------|------|------|
| 契約文件 | 否（不另建 data-contract 文件） | 0.3.0 沒有自有的持久化資料層，只讀取外部格式（markdown frontmatter、`tracking_schema.json`）。外部格式的權威在上游 schema 與它的 conformance 測試，App 端的消費規則在 SPEC-006 FR-01、FR-06；另建文件只會複述這兩處 |
| migration 治理 | 否 | 不寫入任何資料 |

兩旗標皆否，屬合法跳過。但 0.3.0 有四條「Python 產出、Dart 消費」的跨語言契約，改錯時沒有編譯期徵兆，依本專案慣例登記在 `docs/traceability.yaml` 第三軸 `data_contract_tests`：切分語意、carrier 路徑模式與具體度、完整性集合語意、內建型別表版本一致性。這四條的 `tests` 留空，Step 5 補齊；`doc test-map` 通過。

W1-080 第一版加了第三層具體度「字面字元數」，理由是驗收要求「六個型別兩兩不平手」。這條驗收是 PM 寫錯的：平手只在同一路徑命中多個型別時才適用，PROP／SPEC／UC／EVT 的目錄互不重疊。第三層也違反裁決 B（平手應標記 schema 歧義，不應悄悄決勝）。已修正驗收並退回：只保留二層，DomainBundle 拆成模式清單。
- 2026-09-24: 0.3.0-W1-080 完成 -- 新增 tracking_schema 各節點型別機器可比對的 carrier 路徑模式並匯出至 JSON（現行 carrier 為人讀描述文字）
- 2026-09-24: 0.3.0-W1-085 完成 -- 補充 PC-BAL-040 續案：定義檔消費端未展開的變體與 worktree 派發下自行補正不可用的邊界
- 2026-09-24: 0.3.0-W1-084 完成 -- 設計 SPEC-006 Phase 2 紅燈測試規格（5a 外圈 IT-1／IT-2 + 5b 內圈 domain unit）
- 2026-09-24: 0.3.0-W1-082 完成 -- 補寫 SPEC-001 §1：專案 JSON 缺機器可比對 carrier 欄位且版本在範圍內時，從 App 內建型別表補欄位的狀態與提示

### 2026-09-24：規劃波 Step 3～Step 6

**Step 3**：`/spec validate` 的 Layer 1 三支腳本全部通過；Layer 2 找到 5 個未回答問題（沒有 `docs/`、符號連結與大小寫、解碼以外的讀取失敗、`id` 互斥保證被打破、取消與重掃），全部採用預設答案寫入 SPEC-006 v1.4；維度 4 因沒有教學模組對應表而 skipped。

**Step 4**：UC-06 v1.3 新增〈無法判定破洞〉（驗收要求畫面與〈無破洞〉明顯不同）與〈破洞歸屬型別不明〉；UC-01 v1.4 把「失敗檔列入破洞」限縮到 carrier 內；traceability 新增 SPEC-006 × UC-01／05／06 三列映射。UC-05〈含損壞票〉依賴部分救回，屬 SPEC-006 範圍外，本版只涵蓋「偵測到損壞票」這一段。

**Step 4.5**：本版非 UI，不適用。另由 star-anise 完成 `0.3.0-W1-082`：SPEC-001 v1.21 新增「路徑模式取自內建表」常駐徽章與 §5 新狀態「無法判定破洞」，狀態總數 39 → 40，SPEC-003 v1.39、SPEC-004 v1.46 同步。代理人判讀「無法判定時只隱藏解析失敗類別，其他類別照常」，PM 依 UC-06 字面接受。

**Step 5**：sage 產出 `docs/spec/corpus/SPEC-006-test-design.md`，113 個測試案例（外圈 12、Schema 25、Corpus 60、Diagnostics 6、跨語言契約 10），FR 與不變式覆蓋矩陣無空行，traceability 第三軸四條契約已指定測試路徑。三項 NeedsContext 經用戶裁決：lostFields 空值語意以純函式測；參與路徑比對依型別表是否帶 `carrier_path_patterns` 判定，不依型別名；路徑模式以 ASCII 語意比對（SPEC-006 v1.5 D9，框架方言缺口開 `0.3.0-W1-086`）。SR-1（參照實作與凍結測資）落地為 `0.3.0-W2-002`。

**Step 6**：依 sage 的切分建議細化為每票不超過 5 個檔案（3b 派發閾值），逐檔列出實作檔、測試檔與共用 helper 的擁有者（記取 PC-BAL-040）。

| Wave | 票 | 內容 | blockedBy |
|------|----|------|-----------|
| W2 | `W2-003` | Schema 型別表模型與路徑對型別查詢（S1～S4） | — |
| W2 | `W2-004` | 型別表 JSON 解析與契約 K2 | W2-003 |
| W2 | `W2-001` | App 內建型別表與來源三分（S5、K4） | W1-080、W2-004、W2-005 |
| W2 | `W2-002` | 凍結測資與 Python 參照實作 | — |
| W2 | `W2-005` | Corpus frontmatter 切分與結果分類（C1～C3），新增 `package:yaml` | — |
| W2 | `W2-006` | lostFields 純函式（C6、K3） | W2-003 |
| W3 | `W3-524` | 節點判型與 EVT-CORPUS-003（C4、C5、C7） | W2-003、W2-005、W2-006 |
| W3 | `W3-525` | 掃描與摘要計數（C8～C11） | W3-524、W2-001 |
| W3 | `W3-526` | Diagnostics parseFailure 破洞（D1～D2） | W3-524 |
| W3 | `W3-527` | IT-1 | W2-002、W2-005 |
| W3 | `W3-528` | IT-2 | W2-002、W3-525、W3-526 |
| W4 | `W4-001` | 驗收：整合測試、Phase 4 多視角、import 方向、追溯回填 | W3-527、W3-528 |

可立即並行開工：`W2-002`、`W2-003`、`W2-005`（檔案互不重疊）。框架票 `W1-079`、`W1-080` 已完成，canonical 推送待用戶授權。
- 2026-09-24: 0.3.0-W2-003 完成 -- 實作 Schema 型別表模型與路徑對型別查詢（SPEC-006 FR-06 規則 1～6；測試群組 S1～S4）
- 2026-09-24: 0.3.0-W2-005 完成 -- 實作 Corpus frontmatter 切分與結果分類（SPEC-006 FR-01、FR-05 編碼子原因；測試群組 C1～C3）
- 2026-09-24: 0.3.0-W2-004 完成 -- 實作 型別表 JSON 解析與 tracking_schema.json 跨語言契約測試（契約 K2）
- 2026-09-24: 0.3.0-W2-006 完成 -- 實作 lostFields 純函式（SPEC-006 FR-04、D5；測試群組 C6、契約 K3）
- 2026-09-24: 0.3.0-W2-002 完成 -- 建立 SPEC-006 IT-1／IT-2 凍結測資與 IT-2 獨立 Python 參照實作
- 2026-09-24: 0.3.0-W2-007 完成 -- 補 契約 K3：tracking_schema.json 完整性集合的跨語言契約測試（completeness group）
- 2026-09-24: 0.3.0-W3-527 完成 -- 實作 IT-1 整合測試：frontmatter 解析語意與框架函式逐檔比對（PROP-005 §0.3 整合測試 1；契約 K1）
- 2026-09-24: 0.3.0-W3-524 完成 -- 實作 Corpus 節點判型與 EVT-CORPUS-003 發出（SPEC-006 FR-03、FR-04；測試群組 C4、C5、C7）
- 2026-09-24: 0.3.0-W2-001 完成 -- 建立 App 內建型別表（tracking_schema.json 副本）並由 Schema domain 載入 id_pattern／carrier 路徑模式／完整性集合
- 2026-09-24: 0.3.0-W3-526 完成 -- 實作 Diagnostics 由 EVT-CORPUS-003 產生 parseFailure 破洞（SPEC-006 FR-08；測試群組 D1～D2）
- 2026-09-24: 0.3.0-W3-525 完成 -- 實作 Corpus 掃描與摘要計數（SPEC-006 FR-02、FR-05 權限／檔案消失、FR-07、NFR-01；測試群組 C8～C11）
- 2026-09-24: 0.3.0-W3-528 完成 -- 實作 IT-2 整合測試：凍結 manifest 實體化後的破洞分類（PROP-005 §0.3 整合測試 2）
- 2026-09-24: 0.3.0-W3-530 完成 -- 補 IT-2 manifest 缺「多型別命中、具體度分出」的失敗檔合成列（例：無 frontmatter 的 docs/spec/<d>/domain-map.md 歸 DomainBundle）
- 2026-09-24: 0.3.0-W3-531 完成 -- 修正 Schema 型別表載入時驗證並預編譯 regex、具體度改用 record、補 isPathQueryAvailable 與模式錨點契約
- 2026-09-24: 0.3.0-W3-532 完成 -- 重構 ParseOutcome 改為 sealed class，並修正 frontmatter 分類的鍵型別、巢狀 YAML 與不可變性
- 2026-09-24: 0.3.0-W3-533 完成 -- 合併 版本比較邏輯兩份實作為一份（放 lib/schema，由畫面層 import），resolver 的 id_pattern 與完整性集合來源依 S5-6
- 2026-09-24: 0.3.0-W3-534 完成 -- 重構 事件建構拆出獨立檔並改收查詢結果、同一檔只查一次 carrier、FR-03 歧義檔案進掃描結果、判型改用預編譯 regex、原因文字單一來源
- 2026-09-24: 0.3.0-W3-540 完成 -- 修正 id_pattern 預編譯改住 NodeTypeEntry、事件建構改收命中子型別，移除 node_typer 的 Expando 快取與 builder 的 StateError
- 2026-09-24: 0.3.0-W3-536 完成 -- 修正 Diagnostics 結果改 sealed、undeterminedCount 必填、無法判定原因改為原因碼
- 2026-09-24: 0.3.0-W3-541 完成 -- 修正 型別表來源決議區分兩種查詢不可用（版本高於內建／沒有路徑模式），供 FR-08 原因碼取用
- 2026-09-24: 0.3.0-W3-538 完成 -- 解耦 C4／C5 規則性測試改用 TypeTableBuilder，真實 schema 相容性集中回契約測試
- 2026-09-25: 0.3.0-W3-535 完成 -- 修正 掃描器錯誤路徑不中止整輪（NFR-01）：目錄無法列出另列清單、EPERM 判為權限不足、分類正確、起訖日誌
- 2026-09-25: 0.3.0-W3-543 完成 -- 重構 抽出單一「App 已知範圍」判定式，resolver 與 schema 不相容關卡共用，原因值改名為版本不在已知範圍
- 2026-09-25: 0.3.0-W3-544 完成 -- 修正 Diagnostics 無法判定原因碼 projectVersionHigherThanBuiltin 改名為 projectVersionOutOfKnownRange
- 2026-09-25: 0.3.0-W3-537 完成 -- 改善 掃描器分批並行讀檔以縮短 7000 餘檔的總掃描時間

---

## 發版條件

1. PROP-005 §0.3 兩項整合測試通過。
2. `version-release check` 對 0.3.0 無帶 scope_blocker 的 pending 票。

---

*最後更新：2026-09-24*
