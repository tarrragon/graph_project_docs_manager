---
id: 0.3.0-W2-002
title: 建立 SPEC-006 IT-1／IT-2 凍結測資與 IT-2 獨立 Python 參照實作
type: IMP
status: in_progress
version: 0.3.0
wave: 2
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.3.0-W1-084
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W1-084 SR-1
  final_decision: 建 IMP 承接
  rationale: IT-1／IT-2 的前置測資，D3 已定案
who:
  current: thyme-python-developer
  history: {}
what: 依 SPEC-006 D3 與 SPEC-006-test-design.md §2：(1) tool/spec006_reference.py 依 FR-06 規則獨立實作分類（讀框架 tracking_schema.json 的 carrier 路徑模式、二層具體度、平手；regex 以 ASCII 語意），離線掃描五個語料專案產生 IT-2 manifest；(2) 依 §2.1 選樣並以框架 parse_frontmatter 離線產生 IT-1 預期值；(3) 兩份測資與預期值入庫 test/fixtures/spec006/，含 D3 要求的四類樣本（語料缺者以合成列補並標記）
when: 0.3.0-W1-080 已合入（carrier 路徑模式可用）；IT-1、IT-2 實作票之前
where:
  layer: 待定義
  files:
  - tool/spec006_reference.py
  - test/fixtures/spec006/it1/expected.json
  - test/fixtures/spec006/it2/manifest.json
  - test/fixtures/spec006/README.md
why: 0.3.0-W1-084 SR-1：SPEC-006 D3 要求凍結測資與獨立參照實作，未指派承接票；IT-1、IT-2 實作票皆依賴此測資
how:
  task_type: Implementation
  strategy: 讀 SPEC-006 FR-01／FR-06 與 test-design §2 → 參照實作 → 離線產生 → 覆蓋類別核對 → 入庫；參照實作本身附最小單元測試
acceptance:
- '[x] tool/spec006_reference.py 依 FR-06 獨立實作分類，不 import 待測的 Dart 實作，regex 以 ASCII 語意比對'
- '[x] IT-2 manifest 欄位符合 test-design §2.2，含真破洞、合法非節點、多型別衝突、無法讀取四類，合成列有標記'
- '[x] IT-1 樣本與預期值（切分結果與鍵集合）依 test-design §2.1 凍結，排除規則已套用'
- '[x] test/fixtures/spec006/README.md 記錄產生指令、語料量測日期與環境（規則 9）'
- '[x] 參照實作附單元測試並全綠'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T16:27:32'
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

依 SPEC-006 D3 與 SPEC-006-test-design.md §2：(1) tool/spec006_reference.py 依 FR-06 規則獨立實作分類（讀框架 tracking_schema.json 的 carrier 路徑模式、二層具體度、平手；regex 以 ASCII 語意），離線掃描五個語料專案產生 IT-2 manifest；(2) 依 §2.1 選樣並以框架 parse_frontmatter 離線產生 IT-1 預期值；(3) 兩份測資與預期值入庫 test/fixtures/spec006/，含 D3 要求的四類樣本（語料缺者以合成列補並標記）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
執行：thyme-python-developer（worktree 隔離），PM 合入 main（merge cc2869480）並代為記錄。

**第一版被退回**：第一版 IT-2 manifest 只有 19 列（17 列合成），並聲稱「語料中找不到真實的命中 carrier 案例」；IT-1 只有 1 份真實檔案。PM 以該參照實作的 scan 命令掃描五個語料，得到 7466 檔、41 個真破洞，與聲稱不符，且 19 列無法驗證 PROP-005 §0.3「五個語料專案全部文件分類正確」的契約。退回後改為全量凍結。

**第二版（合入版）**：

| 測資 | 內容 |
|------|------|
| IT-2 manifest | 7471 列：真實 7466 列（五個語料全量）+ 合成 5 列（未閉合、空或非 map、carrier 內無法讀取、平手、id 互斥衝突，皆為全量掃描後確認語料中缺席的類別）。計數：node 4676、有 frontmatter 的非節點 1498、失敗 1297（無 frontmatter 1292、YAML 錯誤 1、無法讀取 2、未閉合 1、空或非 map 1）、命中 carrier 45（真實 41）、未命中 1252、未判定 0；兩條守恆式由 freeze-it2 內建 assert 驗證 |
| IT-1 expected | 真實判別樣本 N=176（五個語料 6173 份可用檔中，天真切分與逐行語意結果不同者），只存 frontmatter 段以控制體積；另 8 個合成邊界樣本 |
| 參照實作 | tool/spec006_reference.py：scan、selftest、freeze-it1、freeze-it2；regex 以 re.ASCII 編譯；不 import Dart 實作 |

與 2026-08-27 記錄的差異（語料已演進）：文件 7106 → 7466、真破洞 47 → 41、未命中 1243 → 1252、天真切分判別樣本 130（僅 flutter_balance）→ 176（五個語料）。詳見 test/fixtures/spec006/README.md。

體積：manifest 約 3.6 MB、IT-1 expected 約 2.2 MB。

格式偏離（where.files 只列四個檔案所致，接受）：manifest 為 JSON 非 YAML；IT-1 樣本與 IT-2 型別表內嵌於 JSON，未拆成獨立檔；參照實作的單元測試為內嵌 selftest 子命令。

### 自檢結果

- [x] PM 獨立以 scan 命令掃描五個語料，數字與 manifest 一致
- [x] selftest 全數通過
- [x] manifest 結構抽查：每列帶 project、shape、expected、synthetic、source

### 收尾身份

worktree 派發由 PM 合入後以主線程認領代為收尾（0.3.0-W1-074 待處理的缺口）。

## Test Results
`python3 tool/spec006_reference.py selftest` → SELFTEST PASSED: 全部通過（PM 覆核重跑）

PM 獨立驗證：`spec006_reference.py scan --workspace ~/project/<專案>` 五個語料加總 7466 檔、真破洞 41、未命中 1252，與 manifest 真實列一致。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.3.0-W1-084 | chars: 633 -->

### Task Reference
- 0.3.0-W1-084 what: 依 SPEC-006 FR-01～FR-08、NFR-01 與 domain-map §3〈Bundle 不變式清單〉，產出紅燈測試規格：5a 外圈（IT-1 解析語意、IT-2 破洞分類的凍結測資形態、實體化流程與斷言）與 5b 內圈（逐 bundle 的 domain unit 測試案例），附 FR↔測試覆蓋矩陣與不變式↔測試對照，並為 traceability 第三軸四條跨語言契約各指定測試（來源已完成，結論見 Solution）

### Rationale Chain
- 0.3.0-W1-084 why: version-bootstrap Step 5：紅燈測試規格是 Step 6 建實作票的依據；兩軸（UC 場景 × bundle 不變式）交集去重（來源已完成，結論見 Solution）

### Related Files
- docs/spec/corpus/SPEC-006-test-design.md  # from 0.3.0-W1-084（來源已完成，結論見 Solution）
- docs/traceability.yaml  # from 0.3.0-W1-084（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 5a 外圈：IT-1、IT-2 的測資形態、凍結與實體化流程、逐項斷言，含 SPEC-006 D3 展開要求的四類樣本  # from 0.3.0-W1-084（來源已完成，結論見 Solution）
- [ref] [x] 5b 內圈：domain-map §3 不變式清單每一條至少對應一個測試案例（GWT），標明所屬 bundle 與測試檔路徑  # from 0.3.0-W1-084（來源已完成，結論見 Solution）
- [ref] [x] FR↔測試覆蓋矩陣（FR-01～FR-08、NFR-01）無空行，UC 場景與不變式兩軸交集已去重  # from 0.3.0-W1-084（來源已完成，結論見 Solution）
- [ref] [x] traceability.yaml 第三軸 0.3.0 四條契約的 tests 指定測試路徑或標 no_test_needed 附理由  # from 0.3.0-W1-084（來源已完成，結論見 Solution）
- [ref] [x] 每個守衛型測試含正向對照輸入（test-assertion-design E2）  # from 0.3.0-W1-084（來源已完成，結論見 Solution）
