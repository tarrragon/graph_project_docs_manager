---
id: 0.3.0-W3-533
title: 合併 版本比較邏輯兩份實作為一份（放 lib/schema，由畫面層 import），resolver 的 id_pattern 與完整性集合來源依 S5-6
type: IMP
status: in_progress
version: 0.3.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.3.0-W3-531
relatedTo: []
spawned_tickets: []
source_ticket: 0.3.0-W4-001
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.3.0-W4-001 Phase 4 多視角審查
  final_decision: 用戶裁決全部在發版前修
  rationale: Worth-It Filter 分類後依檔案擁有權切票並排序
who:
  current: 主線程
  history: {}
what: (1) 版本比較只留 lib/schema 一份，lib/screens/domain_view 改為 import（L4 依賴 L0 為允許方向）；更正 resolver 檔頭「避免反向依賴」的錯誤說法；(2) id_pattern 與 completenessFields 依 S5-6 固定取自專案 JSON，移除退回內建表的行為（或若退回為刻意，回頭修規格並停手上報）；(3) 版本缺席時判為查詢不可用的行為寫進 docstring
when: 0.3.0-W3-531 完成後
where:
  layer: L0 schema + L4 domain_view 轉接
  files:
  - lib/schema/schema_version.dart
  - lib/schema/schema_source_resolver.dart
  - lib/screens/domain_view/domain_view_schema_version.dart
  - test/unit/schema/schema_version_test.dart
  - test/unit/schema/schema_source_resolution_test.dart
why: 0.3.0-W4-001 Phase 4：冗餘審查（兩份逐字相同的版本比較）、linux（S5-6 偏離、版本缺席的裁決未寫明）
how:
  task_type: Implementation
  strategy: 紅燈：S5-6 案例 → 合併與修正 → 綠燈，domain_view 既有測試不變
acceptance:
- '[ ] 版本比較只有一份實作，domain_view 經 import 使用'
- '[ ] id_pattern 與完整性集合的來源符合 S5-6，有測試'
- '[ ] dart analyze 0 issues、flutter test 全綠（含 IT-1、IT-2）'
- '[ ] 本票觸及檔案中，Phase 4 文字審查指出的過期票務措辭與錯誤引用一併修正（清單見 0.3.0-W4-001 Solution）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T18:32:34'
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

(1) 版本比較只留 lib/schema 一份，lib/screens/domain_view 改為 import（L4 依賴 L0 為允許方向）；更正 resolver 檔頭「避免反向依賴」的錯誤說法；(2) id_pattern 與 completenessFields 依 S5-6 固定取自專案 JSON，移除退回內建表的行為（或若退回為刻意，回頭修規格並停手上報）；(3) 版本缺席時判為查詢不可用的行為寫進 docstring

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
合入 commit e8c6fde8a + e6cabda14（merge 000a43b35）。

- 版本比較單一實作：新檔 lib/schema/schema_version.dart 的 `isHigherThanBuiltinSchemaVersion`（docstring 寫明版本缺席由呼叫端先判定不可比較）。domain_view_schema_version.dart 以 `export ... show` 保留同名符號，消費端 6 檔未改。resolver 刪除 `_isHigherVersion`，檔頭改為「L4 依賴 L0 為允許方向」。
- S5-6：`_mergeBuiltinPathPatterns` 移除 id_pattern 與完整性集合退回內建表；專案 JSON 缺該型別時合併結果 idPattern 為 null、完整性集合為空，內建表只補路徑模式。代理人判斷此為「固定取自專案 JSON」的直接語意而未上報 NeedsContext，主線程同意。
- 鑑別測試（主線程退回補件）：S5-6b／S5-6c 以「專案 JSON 完全缺該型別條目」構造——completenessFields 非 nullable，條目存在時 `??` 永不觸發，只有條目整體缺席才能鑑別。暫時加回退回時兩案例翻紅（Expected null / Actual '^SPEC-\d+$'；Expected empty / Actual {id,title,status}），移除後轉綠。

### 自檢結果

- [x] 改動檔在 where.files 五檔內（schema_source_resolution_test 於補件時修改）
- [x] 移除行為有鑑別測試（E1），紅綠兩次輸出已核對
- [x] lib/schema 無外向 import（grep 零命中）；L4 → L0 方向合規
- [x] main 合入後 analyze 0、flutter test 1715 全綠

Phase 4：重構需要否；技術債務無新增（消除重複實作）；品質 A；結論無需再重構。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
main 合入後覆核：fvm dart analyze → No issues found；fvm flutter test → +1715 All tests passed（含 IT-1、IT-2；新增 schema_version_test 5 例、S5-6b、S5-6c）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.3.0-W3-531,0.3.0-W4-001 | chars: 1335 -->

### Task Reference
- 0.3.0-W4-001 what: (1) 全套 flutter test 與兩項整合測試綠燈；(2) 對 lib/schema/、lib/corpus/、lib/diagnostics/ 執行 Phase 4 多視角審查（/parallel-evaluation）；(3) 三個目錄的 import 方向符合 domain-map §5；(4) traceability.yaml SPEC-006 三列映射與第三軸四條契約的 tests 回填為實際檔案；(5) 工作日誌記錄版本契約達成
- 0.3.0-W3-531 what: (1) typeTableFromJson 載入時編譯並驗證所有 carrier 路徑模式與 id_pattern，壞的模式拒收並寫日誌，不再於查詢時拋 FormatException（NFR-01）；(2) CarrierPathPattern 與 NodeTypeEntry 持有編譯好的 RegExp，查詢不再重編；(3) 具體度改為具名 record 並只定義一次比較函式，比較、取最佳、判平手共用；JSON 長度不對時在解析時拒收；(4) TypeTable 新增 isPathQueryAvailable，作為查詢是否可用的單一權威；(5) 契約測試斷言所有模式以 ^ 與 $ 錨定（Dart hasMatch 為搜尋語意）。對外 API 保持相容（來源已完成，結論見 Solution）

### Rationale Chain
- 0.3.0-W4-001 why: version-bootstrap Step 6 的 W4 驗收票；PROP-005 §0.3 發版條件；quality-baseline 規則 2 Phase 4 不可跳過
- 0.3.0-W3-531 why: 0.3.0-W4-001 Phase 4：linux（模式不合法時中止整輪掃描、具體度魔術索引寫三次、錨定未核對）、parsley 與冗餘審查（regex 每次重編）、耦合審查（查詢可用判斷三份）（來源已完成，結論見 Solution）

### Related Files
- docs/traceability.yaml  # from 0.3.0-W4-001
- docs/work-logs/v0/v0.3/v0.3.0/v0.3.0-main.md  # from 0.3.0-W4-001
- lib/schema/type_table.dart  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
- lib/schema/type_table_json_codec.dart  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
- lib/schema/carrier_path_lookup.dart  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
- test/helpers/spec006/type_table_builder.dart  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
- test/unit/schema/carrier_path_lookup_test.dart  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [ ] flutter test 全綠，IT-1、IT-2 皆通過  # from 0.3.0-W4-001
- [ref] [ ] Phase 4 多視角審查結論記於 Solution，發現項已建票  # from 0.3.0-W4-001
- [ref] [ ] lib/schema、lib/corpus、lib/diagnostics 的 import 方向逐一檢查並記錄  # from 0.3.0-W4-001
- [ref] [ ] traceability.yaml SPEC-006 映射與第三軸 0.3.0 契約的 tests 指向實際存在的檔案  # from 0.3.0-W4-001
- [ref] [x] 型別表含不合法模式時，載入即拒收該模式並寫日誌，掃描不因此中止；附正向對照  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
- [ref] [x] 查詢不再重新編譯 regex  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
- [ref] [x] 具體度比較只定義一次，平手判定共用  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
- [ref] [x] TypeTable.isPathQueryAvailable 存在並有測試  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
- [ref] [x] 契約測試斷言真實 JSON 所有模式皆錨定  # from 0.3.0-W3-531（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
### 派發前盤點（主線程，2026-09-24）

檔案範圍約束（PC-BAL-040）：`isHigherThanBuiltinSchemaVersion` 的消費端另有 6 檔（domain_view_screen.dart、gate_detection_notifier.dart、gate_manifest_test.dart、domain_view_schema_drift_test.dart、gate_detection_notifier_test.dart、project_switcher_test.dart）。為守住 where.files 三檔：

- 單一實作放 `lib/schema/`（公開函式，名稱自定，建議沿用 `isHigherThanBuiltinSchemaVersion` 語意）
- `domain_view_schema_version.dart` 保留同名公開符號 `isHigherThanBuiltinSchemaVersion`，改為轉呼叫或 `export ... show` lib/schema 的實作；消費端 6 檔不改
- `schema_source_resolver.dart` 的 `_isHigherVersion` 刪除，改用同一實作；檔頭第 10 行註解改寫為「版本比較住在 L0，L4 由 import 取用」

S5-6 現況：`schema_source_resolver.dart` 的 `idPattern: projectEntry?.idPattern ?? builtinEntry?.idPattern` 與 `completenessFields` 同形退回內建表，S5-6（SPEC-006-test-design.md）要求判型用專案 JSON 的 `id_pattern`、內建表只補路徑模式。專案 JSON 該型別缺 id_pattern 時的行為：若規格未定義，停手寫 NeedsContext 上報，不自行選邊。
補充：單一實作放新檔 lib/schema/schema_version.dart（純函式，無 Flutter 依賴），測試放 test/unit/schema/schema_version_test.dart；where.files 已同步。
