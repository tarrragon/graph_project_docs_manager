---
id: 0.3.0-W3-543
title: 重構 抽出單一「App 已知範圍」判定式，resolver 與 schema 不相容關卡共用，原因值改名為版本不在已知範圍
type: IMP
status: completed
version: 0.3.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.3.0-W3-541
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.3.0 WRAP：版本缺席原因碼
  final_decision: 方案 B：改名對齊關卡，共用單一判定式
  rationale: 用戶裁決 2026-09-25
who:
  current: 主線程
  history: {}
what: (1) lib/schema/schema_version.dart 新增 isWithinKnownSchemaRange(String? version, String builtinVersion)：version 為 null 或無法解析或高於內建 → false；(2) resolver 改用它，PathPatternSource.projectVersionHigherThanBuiltin 改名 projectVersionOutOfKnownRange；(3) gate_detection_notifier 的 inRange 改呼叫同一函式（行為不變）；(4) 測試：判定式五列決策表（null、unknown、高於、等於、低於），resolver 補版本缺席與無法解析兩案例
when: 0.3.0-W3-541 已合入；與 W3-544 並行；早於 W3-542（共用 gate_detection_notifier.dart）
where:
  layer: L0 schema + L4 domain_view 閘門
  files:
  - lib/schema/schema_version.dart
  - test/unit/schema/schema_version_test.dart
  - lib/schema/schema_source_resolver.dart
  - test/unit/schema/schema_source_resolution_test.dart
  - lib/screens/domain_view/gate_detection_notifier.dart
why: 用戶裁決 2026-09-25（WRAP 方案 B）：無法判定原因碼改為「版本不在 App 已知範圍」，判定式須與 schema 不相容關卡相同。以單一函式共用取代兩處各自寫的 null 檢查加比較，使兩者結構上不可能分歧（SPEC-006 v1.7 FR-08、SPEC-001 v1.22）
how:
  task_type: Implementation
  strategy: 紅燈：判定式決策表與 resolver 缺席／無法解析案例 → 抽函式並改名 → 綠燈；gate_detection_notifier_test 與 gate_manifest_test 不改仍全綠即證明關卡行為不變
acceptance:
- '[x] isWithinKnownSchemaRange 五列決策表有測試（null、unknown、高於、等於、低於）'
- '[x] resolver 與 gate_detection_notifier 皆呼叫同一判定式，各自不再有 null 檢查加比較的重複邏輯'
- '[x] resolver 對版本缺席與無法解析回傳 projectVersionOutOfKnownRange（內建表有路徑模式時），有測試'
- '[x] dart analyze 0 issues、flutter test 全綠（含 IT-1、IT-2、閘門既有測試不改）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-25T04:48:48'
completed_at: '2026-09-25T04:48:51'
created: '2026-09-25'
updated: '2026-09-25'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

(1) lib/schema/schema_version.dart 新增 isWithinKnownSchemaRange(String? version, String builtinVersion)：version 為 null 或無法解析或高於內建 → false；(2) resolver 改用它，PathPatternSource.projectVersionHigherThanBuiltin 改名 projectVersionOutOfKnownRange；(3) gate_detection_notifier 的 inRange 改呼叫同一函式（行為不變）；(4) 測試：判定式五列決策表（null、unknown、高於、等於、低於），resolver 補版本缺席與無法解析兩案例

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
合入 commit 58c9691b3（merge 於 main）。

- lib/schema/schema_version.dart 新增 isWithinKnownSchemaRange(String? version, String builtinVersion)：null、無法解析、高於內建 → false；內部委派 isHigherThanBuiltinSchemaVersion。
- resolver 改用它，PathPatternSource.projectVersionHigherThanBuiltin 改名 projectVersionOutOfKnownRange；移除手寫的 null 檢查加比較。
- gate_detection_notifier 的 inRange 改呼叫同一函式。resolver 與 schema 不相容關卡自此共用單一判定式，結構上不可能分歧（取代原計畫的一致性契約測試）。
- 範圍偏離（主線程接受）：domain_view_schema_version.dart 的 export show 清單加入 isWithinKnownSchemaRange（一行），供 gate 沿既有轉接路徑取用；該檔未列於 where.files。
- 測試：判定式五列決策表；resolver S5-4b 版本缺席、無法解析兩案例。gate_detection_notifier_test 與 gate_manifest_test 未改且全綠，證明關卡行為不變。

### 自檢結果

- [x] 改動檔：where.files 四檔 + 一行 export（已說明）
- [x] 關卡既有測試不改全綠
- [x] L4 → L0 依賴方向合規
- [x] main 合入後 analyze 0、flutter test 1734 全綠

Phase 4：重構需要否；技術債務：消除兩處重複判定；品質 A；結論無需再重構。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
main 合入後覆核：fvm dart analyze → No issues found；fvm flutter test → +1734 All tests passed（含 IT-1、IT-2、閘門既有測試）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-25T04:48:51
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.3.0-W3-541 | chars: 512 -->

### Task Reference
- 0.3.0-W3-541 what: PathPatternSource.unavailable 拆為兩個值：projectVersionHigherThanBuiltin（專案缺欄位且版本高於內建）與 noPathPattern（專案與內建表皆無路徑模式）；isQueryAvailable 與 isPathPatternFromBuiltin 語意不變；兩種情境各一測試並附正向對照（來源已完成，結論見 Solution）

### Rationale Chain
- 0.3.0-W3-541 why: 0.3.0-W3-536 驗收發現：SPEC-006 v1.6 FR-08 定義兩種原因碼，但 resolver 把兩者合併為 unavailable，全系統沒有任何地方能產出正確原因碼。Diagnostics 不依賴 Schema（依賴邊已刪），resolver 值到 UndeterminedGapReason 的映射屬編排層（0.1.0-W1-025 承接）（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 兩種查詢不可用情境可由 resolver 結果分辨，各有測試與正向對照  # from 0.3.0-W3-541（來源已完成，結論見 Solution）
- [ref] [x] isQueryAvailable 與 isPathPatternFromBuiltin 行為不變，既有 S5 測試全綠  # from 0.3.0-W3-541（來源已完成，結論見 Solution）
- [ref] [x] dart analyze 0 issues、flutter test 全綠（含 IT-1、IT-2）  # from 0.3.0-W3-541（來源已完成，結論見 Solution）
