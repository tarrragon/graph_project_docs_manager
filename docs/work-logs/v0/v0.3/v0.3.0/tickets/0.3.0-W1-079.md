---
id: 0.3.0-W1-079
title: '實作 #99 第三項裁決：圖譜 schema 完整性集合改為「欄位必須存在、值可空」語意、補五型最小集並匯出至 tracking_schema.json'
type: IMP
status: in_progress
version: 0.3.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.3.0-W1-078
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 框架問題已裁決，進實作
  final_decision: '本 consumer 承接 #99 第三項實作'
  rationale: 裁決由本 session 作出，實作與 JSON 消費端同在本專案
who:
  current: thyme-python-developer
  history: {}
what: tracking_schema.py：完整性集合改名或註解明示空值合法（null／[]），FlowStep 保留 traverses；新增 PROP／SPEC／UC={id,title,status}、DomainBundle={id,domain}；Ticket 註明權威在 ticket skill；JSON 匯出新增完整性鍵與 empty_allowed 語意；conformance 測試含鍵缺漏應紅、null 與 [] 應綠的鑑別對照；validate.py 的 EVENT_REQUIRED_FIELDS 改讀 schema 集合，值非空保留為 EVT 附加規則；references/usecases.md 欄位表改為必存在可空
when: 0.3.0-W1-078 裁決記錄後；Corpus spec（規劃波 Step 2）需讀 JSON 鍵
where:
  layer: 待定義
  files:
  - .claude/skills/doc/doc_system/core/tracking_schema.py
  - .claude/skills/doc/doc_system/core/tracking_schema.json
  - .claude/skills/doc/doc_system/commands/validate.py
  - .claude/skills/doc/doc_system/commands/schema.py
  - .claude/skills/doc/tests/test_tracking_schema_conformance.py
  - .claude/skills/doc/tests/test_schema_export.py
  - .claude/skills/doc/tests/test_validate_event.py
  - .claude/skills/doc/references/usecases.md
  - .claude/skills/doc/CHANGELOG.md
why: '用戶 2026-09-24 裁決 #99 第三項（記錄於 0.3.0-W1-078 Solution 與 issuecomment-5808438098）；現行 JSON 無完整性鍵使 App 無法實作 lostFields'
how:
  task_type: Implementation
  strategy: worktree 隔離 → 改常數與註解 → 重產 JSON → 鑑別測試（E1/E2 正向對照）→ validate.py 改讀 schema → doc skill 測試全綠 → 回報 PM；skill-sync push 與 sync-push 需用戶授權
acceptance:
- '[x] 完整性集合的常數名或註解明示空值（null 與空清單）合法；FlowStep 集合含 traverses'
- '[x] PROP／SPEC／UC 集合為 {id,title,status}，DomainBundle 為 {id,domain}，Ticket 註明權威在 ticket skill'
- '[x] tracking_schema.json 含各型別完整性集合與空值合法語意，重產後與 .py 一致'
- '[x] conformance 測試含鍵缺漏應紅、值為 null 與空清單應綠的對照，改回舊語意時至少一項翻紅'
- '[x] validate.py 完整性判定讀 schema 集合，EVT 值非空規則保留為附加規則；doc skill 測試全綠'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T13:51:40'
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

tracking_schema.py：完整性集合改名或註解明示空值合法（null／[]），FlowStep 保留 traverses；新增 PROP／SPEC／UC={id,title,status}、DomainBundle={id,domain}；Ticket 註明權威在 ticket skill；JSON 匯出新增完整性鍵與 empty_allowed 語意；conformance 測試含鍵缺漏應紅、null 與 [] 應綠的鑑別對照；validate.py 的 EVENT_REQUIRED_FIELDS 改讀 schema 集合，值非空保留為 EVT 附加規則；references/usecases.md 欄位表改為必存在可空

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-24 13:51] worktree 隔離派發；只改 where.files 列出的 .claude/skills/doc 檔案；不執行 skill-sync push／sync-push（需用戶授權，由 PM 處理）；W1-077 同改 doc CHANGELOG，排在本票之後｜readiness=2 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
執行：thyme-python-developer（worktree 隔離），PM 合入 main（merge 9a4abb994）並代為記錄。commit 序列：c9dd7005f → d93463e52 → ed26dfbd6 → 3b1ce6777。

| acceptance | 落地 |
|-----------|------|
| 1 空值合法語意 | 新增 `find_missing_completeness_fields()`，以 `in` 判斷鍵存在，值為 None 或 [] 視為已寫出；註解寫明對應 JSON Schema `required`；FlowStep 集合保留 traverses |
| 2 五型最小集 | PROP／SPEC／UC = {id,title,status}、DomainBundle = {id,domain}，併入 `COMPLETENESS_FIELDS` 總表；Ticket 不設集合，註明權威在 ticket skill |
| 3 JSON 匯出 | `commands/schema.py` 匯出 `completeness_fields` 與 `completeness_semantics`；`test_schema_export.py::test_has_completeness_fields_and_semantics` |
| 4 鑑別對照 | `TestFindMissingCompletenessFieldsDiscrimination`：改回真值判斷後 12 項翻紅 |
| 5 validate.py | `_validate_event` 改讀 schema 集合，移除本地重複定義；四個純量欄位的「值不可為空」保留為附加規則，錯誤訊息與「缺少欄位」分開 |

覆核中發現的兩個問題：
- where.files 兩度漏列實際要改的檔案（`commands/schema.py`、`tests/test_validate_event.py`），代理人都遇 hook 停手回報，PM 補範圍後續作。成因：建票時只列了主模組，漏了它的匯出點和測試檔
- 第一版移除了 EVT 四個純量欄位的「值不可為空」檢查，使 `category: null` 可以通過驗證；退回後補回，並新增正向對照測試 `test_null_category_fails`、`test_empty_string_name_fails`（拿掉附加規則時兩項翻紅）

Phase 4 評估：重構需要＝否；技術債務＝無新增（移除了 validate.py 的重複定義）；品質 A；結論：擴充既有 SSOT 並抽出共用判斷函式，無需另外重構。

## Test Results
`uv run --project .claude/skills/doc --with pytest pytest .claude/skills/doc/tests/ -q`（worktree，PM 覆核重跑）

```
================== 310 passed, 1 skipped, 3 warnings in 1.09s ==================
```
1 skipped 為既有 traceability.yaml 按需建立檔的 skip，與本票無關。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.3.0-W1-078 | chars: 500 -->

### Task Reference
- 0.3.0-W1-078 what: 讀 #99 第三項裁決；依裁決更新 PROP-005 §0.3 阻擋（lostFields 可否實作、以何集合）、domain-map §9 對應待決項與 EVT-CORPUS-003 lostFields 定義；完成後回到 version-bootstrap Step 2（Corpus spec）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.3.0-W1-078 why: 用戶裁示 2026-09-24：先推動 #99 第三項裁決再開工 Corpus 核心；外部事件須包裝為 ticket 作合法 trigger（decision-trigger-binding 規則 2）（來源已完成，結論見 Solution）

### Related Files
- docs/proposals/PROP-005-version-sequence-by-data-pipeline.md  # from 0.3.0-W1-078（來源已完成，結論見 Solution）
- docs/domain-map.md  # from 0.3.0-W1-078（來源已完成，結論見 Solution）
- docs/events/corpus/EVT-CORPUS-003-parse-failed.md  # from 0.3.0-W1-078（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] #99 第三項裁決內容（語意、空值是否算缺、各型別集合）已摘錄入 Solution  # from 0.3.0-W1-078（來源已完成，結論見 Solution）
- [ref] [x] PROP-005 §0.3、domain-map §9、EVT-CORPUS-003 依裁決更新  # from 0.3.0-W1-078（來源已完成，結論見 Solution）
- [ref] [x] 0.3.0 工作日誌記錄恢復 Step 2  # from 0.3.0-W1-078（來源已完成，結論見 Solution）
