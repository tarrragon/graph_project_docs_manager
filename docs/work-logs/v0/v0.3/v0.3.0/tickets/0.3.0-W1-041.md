---
id: 0.3.0-W1-041
title: 修復 發版前移撞號：ticket migrate 目標已存在時改取目標版本下一可用序號並記 migrated_from；version-release finish 的 dry-run 與正式執行對碰撞判 FAIL
type: IMP
status: in_progress
version: 0.3.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: Layer 1 版本規劃
  final_decision: 撞號在框架修，改號而非覆寫
  rationale: 覆寫會毀掉已完成票；多 PM 下撞號是常態
who:
  current: thyme-python-developer
  history: {}
what: 修復 發版前移撞號：ticket migrate 目標已存在時改取目標版本下一可用序號並記 migrated_from；version-release finish 的 dry-run 與正式執行對碰撞判 FAIL
when: 0.3.0 啟用後；下一次任何版本 finish 前
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/commands/migrate.py
  - .claude/skills/ticket/ticket_system/lib/command_tracking_messages.py
  - .claude/skills/ticket/ticket_system/tests/test_migrate_collision_detection.py
  - .claude/skills/ticket/tests/test_migrate_collision.py
  - .claude/skills/version-release/scripts/version_release.py
  - .claude/skills/version-release/tests/test_finish_migrate_collision.py
  - .claude/skills/ticket/SKILL.md
  - .claude/skills/ticket/CHANGELOG.md
  - .claude/skills/ticket/references/migrate-command.md
  - .claude/skills/version-release/SKILL.md
  - .claude/skills/version-release/CHANGELOG.md
why: '0.2.1 發版前 finish --dry-run 對 0.2.1-W1-020 → 0.3.0-W1-020 印 [OK]，但 0.3.0-W1-020 已是另一 PM 建的 completed 票；單跑 ticket migrate --dry-run 才警告覆寫。finish 只換版本前綴保留序號，多 PM 或跨版本並行建票時必撞；包裝層未把 migrate 的碰撞守衛帶上。過渡期由 PM 手動遷到空號。canonical #55 已觀測。'
how:
  task_type: Implementation
  strategy: 'TDD。(1) ticket migrate：目標 ID 已存在時，改取目標版本同 wave 下一可用序號，frontmatter 寫 migrated_from: <原 ID>，輸出印出改號；--force-overwrite 語意不變。(2) version-release finish：Step 0 與 dry-run 共用同一碰撞檢查（呼叫 migrate 的 dry-run 或自行檢查目標存在），碰撞時 dry-run 列 FAIL 並印改號預覽；正式執行走改號。(3) 兩 skill 各補測試：碰撞→改號、無碰撞→原號、dry-run 對碰撞 FAIL。(4) SKILL.md／CHANGELOG 次版號 +1。migrate 的實際檔案位置以 grep "def .*migrate" 定位，where.files 有誤先 set-where。'
acceptance:
- '[x] migrate 對已存在目標改取下一可用序號並寫 migrated_from，測試覆蓋碰撞與無碰撞'
- '[x] finish --dry-run 對碰撞判 FAIL 且印改號預覽；正式執行走改號，測試覆蓋'
- '[x] 兩 skill 測試套件全綠，SKILL.md 與 CHANGELOG 已更新'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T11:28:41'
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

修復 發版前移撞號：ticket migrate 目標已存在時改取目標版本下一可用序號並記 migrated_from；version-release finish 的 dry-run 與正式執行對碰撞判 FAIL

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-24 11:25] readiness=1 validate=2

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 修復方式

**migrate.py**（`_migrate_single_ticket` / `_batch_migrate`）：
- 新增 `_increment_sequence_str` / `_resolve_available_target_id`：碰撞時在
  目標版本同 Wave 下遞增序號（保留原補零寬度），直到找到無碰撞的 ID。
- dry-run：碰撞且未帶 `--force-overwrite` 時，改判 `[ERROR]` FAIL（exit 1）
  並印改號預覽（`resolved_id`）；帶 `--force-overwrite` 時維持原
  `[WARNING]` 可放行預覽（exit 0）。
- 實際執行：碰撞且未帶 `--force-overwrite` 時，改取下一可用序號完成遷移
  （exit 0），並在票面 frontmatter 寫入 `migrated_from: <原目標 ID>`；
  `--force-overwrite` 語意不變（仍覆寫並記錄 `[AUDIT]` log）。
- `_batch_migrate` 移除碰撞 pre-scan fail-fast 區塊：碰撞不再是需要整批
  擋下的失敗，改由每筆遷移各自對當下檔案系統狀態判斷並改號，天然支援
  批次內連環碰撞（前一筆改號後的新目標仍會被下一筆的碰撞檢查看見）。
- 移除已死的訊息常數 `ERROR_MIGRATE_TARGET_EXISTS`（實際執行拒絕覆寫，
  已不再被觸發）與 `ERROR_BATCH_COLLISION`（batch fail-fast 已移除）；
  新增 `DRY_RUN_COLLISION_FAIL` / `INFO_MIGRATE_RENUMBERED`。

**version_release.py**（`migrate_overflow_tickets`）：
- 改為原樣轉印 `ticket migrate` child process 的 stdout（rstrip 後印出），
  不再自行以固定 `target_id` 字串組「已前移」訊息——碰撞改號後的實際
  目標 ID 只存在於 child process 輸出，沿用固定字串會誤報未實際使用的
  目標 ID。`result.returncode != 0` 判斷與中止邏輯不變，`finish
  --dry-run` 對碰撞的 FAIL 因此透過 `ticket migrate --dry-run` 的
  exit code 自然傳導，無需額外程式碼判斷碰撞。

### Phase 4（重構評估）

- 重構需要：否（本次改動即目標設計，未發現額外技術債務）
- 技術債務：無新增；主動清理兩個因行為變更而死亡的訊息常數
  （`ERROR_MIGRATE_TARGET_EXISTS`、`ERROR_BATCH_COLLISION`）
- 程式碼品質：A（新增函式單一職責、無巢狀超標；`_migrate_single_ticket`
  維持既有結構未過度膨脹）
- 結論：無需進一步重構

### 文件同步

- `references/migrate-command.md`〈Collision Detection〉表格與前置檢查
  SOP 註解已改寫為新行為
- `.claude/skills/ticket/CHANGELOG.md` 2.42.0、`SKILL.md` metadata.version
  同步；`.claude/skills/version-release/CHANGELOG.md` 2.7.0、`SKILL.md`
  metadata.version 同步

### 自檢結果

- 類別 A（文字品質）：Solution／Test Results 段落均含 Why（碰撞行為改變
  的動機）/ Consequence（沿用固定字串會誤報目標 ID）/ Action（改為轉印
  stdout）；無示例先於原則的段落。
- 類別 B（禁用字與字元集）：已用繁體中文、無禁用詞彙、無 emoji；
  `.claude/` 框架檔案內兩次新增 ticket ID 引用（原始碼註解、messages
  常數註解）已依規則 8 移除，改用「發版前移撞號改號機制」等描述性標籤。
- 類別 C（Schema 結構）：Solution 含修復方式、Phase 4、文件同步三子節；
  Test Results 記錄兩個 skill 的完整執行指令與通過數。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
- `(cd .claude/skills/ticket && uv run --with pytest --with pyyaml --with filelock python -m pytest -q)`
  → 4109 passed, 5 deselected（含改寫後的 `test_migrate_collision_detection.py` 8 項
  與新增的 `tests/test_migrate_collision.py` 3 項）
- `(cd .claude/skills/version-release && uv run --with pytest pytest -q)`
  → 185 passed（含新增的 `tests/test_finish_migrate_collision.py` 2 項）

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
