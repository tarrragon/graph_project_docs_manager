---
id: 0.3.0-W1-080
title: 新增 tracking_schema 各節點型別機器可比對的 carrier 路徑模式並匯出至 JSON（現行 carrier 為人讀描述文字）
type: IMP
status: in_progress
version: 0.3.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy:
- 0.3.0-W1-079
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 規劃波 Step 2 設計決策 D1
  final_decision: 上游加結構化 carrier 模式欄位
  rationale: App 依 PROP-002 只讀 JSON；App 端解析描述文字在上游改寫時會靜默失效
who:
  current: thyme-python-developer
  history: {}
what: 每個有檔案 carrier 的型別（PROP／SPEC／UC／Ticket／DomainBundle／EVT）新增機器可比對的相對路徑模式欄位（沿用 JSON 既有 id_pattern_dialect 的 regex 方言或逐段模式），人讀 carrier 描述保留；格式須可計算具體度（字面段數），供消費端在無 frontmatter 時依「較具體模式優先」歸型；匯出至 tracking_schema.json；conformance 測試以正反路徑對照驗證（含 docs/spec/README.md、domain 目錄內 README.md 應不命中 SPEC，domain-map.md 同時命中 SPEC 與 DomainBundle 時 DomainBundle 較具體）
when: 0.3.0-W1-079 合入後（同改 tracking_schema.py／schema.py／JSON／conformance 測試／CHANGELOG）
where:
  layer: 待定義
  files:
  - .claude/skills/doc/doc_system/core/tracking_schema.py
  - .claude/skills/doc/doc_system/core/tracking_schema.json
  - .claude/skills/doc/doc_system/commands/schema.py
  - .claude/skills/doc/tests/test_tracking_schema_conformance.py
  - .claude/skills/doc/tests/test_schema_export.py
  - .claude/skills/doc/references/tracking.md
  - .claude/skills/doc/SKILL.md
  - .claude/skills/doc/CHANGELOG.md
why: 用戶 2026-09-24 規劃波 Step 2 裁決 D1：App 取得 carrier 模式採上游加結構化欄位（PROP-002 讀 JSON 原則）；現行 carrier 為含 {domain}／NNN／...／「或」與括號說明的描述文字，App 無法比對，0.3.0 整合測試 2（真破洞／合法非節點分類）依賴此模式；D2 裁決較具體模式優先
how:
  task_type: Implementation
  strategy: 定模式格式（regex 或逐段）與具體度算法 → 各型別填模式 → 正反路徑鑑別測試 → 匯出 JSON → doc skill 測試全綠；推送需用戶授權
acceptance:
- '[x] PROP／SPEC／UC／Ticket／DomainBundle／EVT 皆有機器可比對的路徑模式欄位，人讀 carrier 保留'
- '[x] 模式格式可計算具體度，文件寫明算法'
- '[x] conformance 測試含正反路徑對照：domain 目錄內 README.md 不命中 SPEC；domain-map.md 同時命中 SPEC 與 DomainBundle 且 DomainBundle 具體度較高；拿掉任一型別模式時至少一項翻紅'
- '[x] tracking_schema.json 含該欄位，重產後與 .py 一致；doc skill 測試全綠'
- '[x] 具體度只採二層比較（字面段數多者優先，其次跨多段萬用成分少者優先），不加第三層；平手保留為 schema 歧義由消費端回報。conformance 測試只對「可能命中同一路徑」的型別對斷言不平手（以各型別正向樣本路徑交叉比對），並有一個測試示範兩個重疊且二層相同的模式會被判為平手'
- '[x] 同一型別有多個替代路徑時（DomainBundle 的 per-domain 與根層 domain-map.md）拆成模式清單，每個模式各自計算具體度'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T15:28:58'
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

每個有檔案 carrier 的型別（PROP／SPEC／UC／Ticket／DomainBundle／EVT）新增機器可比對的相對路徑模式欄位（沿用 JSON 既有 id_pattern_dialect 的 regex 方言或逐段模式），人讀 carrier 描述保留；格式須可計算具體度（字面段數），供消費端在無 frontmatter 時依「較具體模式優先」歸型；匯出至 tracking_schema.json；conformance 測試以正反路徑對照驗證（含 docs/spec/README.md、domain 目錄內 README.md 應不命中 SPEC，domain-map.md 同時命中 SPEC 與 DomainBundle 時 DomainBundle 較具體）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-24 15:27] worktree 隔離；具體度算法與 SPEC-006 FR-06 規則 6 一致，但框架檔內不得引用本專案識別符（reference-stability 規則 8）；不推送｜readiness=2 validate=0
- [2026-09-24 15:27] worktree 隔離；具體度算法與 SPEC-006 FR-06 規則 6 一致，但框架檔內不得引用本專案識別符（reference-stability 規則 8）；不推送｜readiness=2 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
六型別（PROP/SPEC/UC/Ticket/DomainBundle/EVT）新增 carrier_path_pattern（python-re，同 id_pattern 方言）
與 carrier_path_specificity（[literal_segment_count, cross_segment_wildcard_count, literal_char_count]）；
FlowStep 因 carrier 非檔案路徑不適用，新增 CARRIER_PATH_TYPES 集合排除之。
schema.py 匯出新增 carrier_path_types / carrier_path_specificity_semantics；tracking_schema.json 已重產。
conformance 測試新增 TestCarrierPathPatternConformance（9 個測試），既有
test_node_and_edge_tables_share_same_layer_field_name 因新欄位為 list 補 isinstance 防護。

修正：移除第三層 literal_char_count，具體度改回二層 [literal_segment_count, cross_segment_wildcard_count]。
carrier_path_pattern/carrier_path_specificity 改為 carrier_path_patterns 清單（每型別 1~N 項，各自 pattern+specificity），
DomainBundle 拆成 per-domain 巢狀（[3,0]）與根層（[2,0]）兩項。
conformance 測試改為：只對正向樣本路徑會命中對方模式的型別對（實測僅 SPEC/DomainBundle）斷言不平手；
新增打平示範測試（兩個重疊且二層相同的合成模式，證明機制本身正確判為歧義）；
新增 DomainBundle 具體度獨立回歸測試。schema.py 的 CARRIER_PATH_SPECIFICITY_SEMANTICS 改為二層敘述。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
uv run --project .claude/skills/doc --with pytest pytest .claude/skills/doc/tests/ -q  ->  319 passed, 1 skipped, 3 warnings

手動 build_schema_dict() 重產 JSON（doc schema export CLI 在本 worktree 環境仍靜默無輸出，見上一輪 NeedsContext）；uv run --project .claude/skills/doc --with pytest pytest .claude/skills/doc/tests/ -q -> 322 passed, 1 skipped, 3 warnings
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.3.0-W1-079 | chars: 890 -->

### Task Reference
- 0.3.0-W1-079 what: tracking_schema.py：完整性集合改名或註解明示空值合法（null／[]），FlowStep 保留 traverses；新增 PROP／SPEC／UC={id,title,status}、DomainBundle={id,domain}；Ticket 註明權威在 ticket skill；JSON 匯出新增完整性鍵與 empty_allowed 語意；conformance 測試含鍵缺漏應紅、null 與 [] 應綠的鑑別對照；validate.py 的 EVENT_REQUIRED_FIELDS 改讀 schema 集合，值非空保留為 EVT 附加規則；references/usecases.md 欄位表改為必存在可空（來源已完成，結論見 Solution）

### Rationale Chain
- 0.3.0-W1-079 why: 用戶 2026-09-24 裁決 #99 第三項（記錄於 0.3.0-W1-078 Solution 與 issuecomment-5808438098）；現行 JSON 無完整性鍵使 App 無法實作 lostFields（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/doc/doc_system/commands/validate.py  # from 0.3.0-W1-079（來源已完成，結論見 Solution）
- .claude/skills/doc/tests/test_validate_event.py  # from 0.3.0-W1-079（來源已完成，結論見 Solution）
- .claude/skills/doc/references/usecases.md  # from 0.3.0-W1-079（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 完整性集合的常數名或註解明示空值（null 與空清單）合法；FlowStep 集合含 traverses  # from 0.3.0-W1-079（來源已完成，結論見 Solution）
- [ref] [x] PROP／SPEC／UC 集合為 {id,title,status}，DomainBundle 為 {id,domain}，Ticket 註明權威在 ticket skill  # from 0.3.0-W1-079（來源已完成，結論見 Solution）
- [ref] [x] tracking_schema.json 含各型別完整性集合與空值合法語意，重產後與 .py 一致  # from 0.3.0-W1-079（來源已完成，結論見 Solution）
- [ref] [x] conformance 測試含鍵缺漏應紅、值為 null 與空清單應綠的對照，改回舊語意時至少一項翻紅  # from 0.3.0-W1-079（來源已完成，結論見 Solution）
- [ref] [x] validate.py 完整性判定讀 schema 集合，EVT 值非空規則保留為附加規則；doc skill 測試全綠  # from 0.3.0-W1-079（來源已完成，結論見 Solution）
### 規格依據（PM 派發前寫入）

權威：`docs/spec/corpus/SPEC-006-corpus-parsing-and-gap-classification.md` FR-06 規則 3、4、6。

- 只有 carrier 是檔案路徑的型別有路徑模式；FlowStep 的 carrier 是 UC 文件內的區塊，不設路徑模式
- 路徑比對區分大小寫，比對的是相對於工作區根目錄、以 `/` 分隔的完整路徑（含檔名）
- 具體度二層比較：以 `/` 切段；整段都是固定文字才算字面段，含任何萬用成分（例如 `{slug}.md`、`SPEC-*.md`）就不算。先比字面段數，多者優先；再比能跨多段的萬用成分數（例如 `...`、`**`），少者優先；仍相同即平手
- 正反路徑對照：domain 目錄內的 README.md 不命中 SPEC；`docs/spec/<d>/domain-map.md` 同時命中 SPEC 與 DomainBundle，DomainBundle 具體度較高；現有型別之間兩兩不得平手

### 約束

- 算法寫在框架檔內，不得引用本專案識別符（SPEC-006、ticket ID 等），依 `.claude/references/reference-stability-rules.md` 規則 8：框架檔會同步到沒有這些檔案的 consumer
- worktree 內不跑 append-log／complete；不跑 skill-sync push／sync-push
