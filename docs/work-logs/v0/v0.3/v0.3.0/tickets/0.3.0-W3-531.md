---
id: 0.3.0-W3-531
title: 修正 Schema 型別表載入時驗證並預編譯 regex、具體度改用 record、補 isPathQueryAvailable 與模式錨點契約
type: IMP
status: in_progress
version: 0.3.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.3.0-W4-001
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.3.0-W4-001 Phase 4 多視角審查
  final_decision: 用戶裁決全部在發版前修
  rationale: Worth-It Filter 分類後依檔案擁有權切票並排序
who:
  current: 主線程
  history: {}
what: (1) typeTableFromJson 載入時編譯並驗證所有 carrier 路徑模式與 id_pattern，壞的模式拒收並寫日誌，不再於查詢時拋 FormatException（NFR-01）；(2) CarrierPathPattern 與 NodeTypeEntry 持有編譯好的 RegExp，查詢不再重編；(3) 具體度改為具名 record 並只定義一次比較函式，比較、取最佳、判平手共用；JSON 長度不對時在解析時拒收；(4) TypeTable 新增 isPathQueryAvailable，作為查詢是否可用的單一權威；(5) 契約測試斷言所有模式以 ^ 與 $ 錨定（Dart hasMatch 為搜尋語意）。對外 API 保持相容
when: 0.3.0 W4 Phase 4 審查後；可與 F3a 並行
where:
  layer: 待定義
  files:
  - lib/schema/type_table.dart
  - lib/schema/type_table_json_codec.dart
  - lib/schema/carrier_path_lookup.dart
  - test/helpers/spec006/type_table_builder.dart
  - test/unit/schema/carrier_path_lookup_test.dart
  - test/unit/schema/tracking_schema_contract_test.dart
why: 0.3.0-W4-001 Phase 4：linux（模式不合法時中止整輪掃描、具體度魔術索引寫三次、錨定未核對）、parsley 與冗餘審查（regex 每次重編）、耦合審查（查詢可用判斷三份）
how:
  task_type: Implementation
  strategy: 紅燈：壞模式載入即拒收、具體度比較單一來源、錨定斷言 → 實作 → 綠燈；型別變更屬原子遷移，六檔同票
acceptance:
- '[x] 型別表含不合法模式時，載入即拒收該模式並寫日誌，掃描不因此中止；附正向對照'
- '[x] 查詢不再重新編譯 regex'
- '[x] 具體度比較只定義一次，平手判定共用'
- '[x] TypeTable.isPathQueryAvailable 存在並有測試'
- '[x] 契約測試斷言真實 JSON 所有模式皆錨定'
- '[x] dart analyze 0 issues、flutter test 全綠（含 IT-1、IT-2）'
- '[x] 本票觸及檔案中，Phase 4 文字審查指出的過期票務措辭與錯誤引用一併修正（清單見 0.3.0-W4-001 Solution）'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T18:02:12'
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

(1) typeTableFromJson 載入時編譯並驗證所有 carrier 路徑模式與 id_pattern，壞的模式拒收並寫日誌，不再於查詢時拋 FormatException（NFR-01）；(2) CarrierPathPattern 與 NodeTypeEntry 持有編譯好的 RegExp，查詢不再重編；(3) 具體度改為具名 record 並只定義一次比較函式，比較、取最佳、判平手共用；JSON 長度不對時在解析時拒收；(4) TypeTable 新增 isPathQueryAvailable，作為查詢是否可用的單一權威；(5) 契約測試斷言所有模式以 ^ 與 $ 錨定（Dart hasMatch 為搜尋語意）。對外 API 保持相容

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
合入 commit 9a8746f40（merge 於 main）。六檔與 where.files 完全一致，未觸碰 lib/corpus、lib/diagnostics、schema_source_resolver.dart。

- 壞模式拒收：`_parseCarrierPathPatterns` 逐元素捕捉 FormatException，寫 developer.log（level 900）後略過該模式；specificity 長度不為 2 同樣拒收；壞 id_pattern 拒收為 null。附正向對照（同批 Good 型別照常命中）。
- 預編譯：CarrierPathPattern 建構時編譯一次，toRegExp() 回傳同一實例（identical 斷言）。
- 具體度：PathSpecificity 具名 record + comparePathSpecificity 單一比較函式，排序、取最佳、平手判定三處共用。
- TypeTable.isPathQueryAvailable：單一權威 getter，true/false 兩案例。
- 契約測試 K2-6：真實 tracking_schema.json 全部 carrier_path_patterns 與 id_pattern 皆 ^...$ 錨定，附未錨定模式命中子字串的負向構造。
- 對外 API 相容：pattern/toRegExp/idPattern 保留；TypeTableBuilder 仍收 List<int>。
- 附帶：三處 developer.log 字串補 `// i18n-exempt: 開發者診斷 log`（hook 要求，沿用既有慣例）。W4-001 列出的過期票務措辭在本票觸及檔案中 grep 零命中。

Phase 4：本票即 W4-001 Phase 4 審查的修正落地，範圍 3 lib + 3 test；重構需要：否；技術債務：無新增；品質 A；結論：無需再重構。

### 自檢結果

- [x] 修改檔案與 where.files 逐一相符（6 檔，git diff --stat 核對）
- [x] 每個 catch 皆有 developer.log（level 900），無空 catch
- [x] 守衛類驗收皆含正向對照（壞模式、未錨定模式、isPathQueryAvailable false 案例）
- [x] lib/schema 的 import 未引入 lib/corpus、lib/diagnostics
- [x] main 合入後 analyze 0 issues、flutter test 1702 全綠
- [x] 已知錯誤模式核對：PC-BAL-040（where.files 精確列檔）本票無遺漏；規模 6 檔超過 5 屬原子型別遷移，派發前已於票面說明
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
main 合入後覆核：fvm dart analyze → No issues found；fvm flutter test → +1702 All tests passed（含 IT-1、IT-2）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.3.0-W4-001 | chars: 573 -->

### Task Reference
- 0.3.0-W4-001 what: (1) 全套 flutter test 與兩項整合測試綠燈；(2) 對 lib/schema/、lib/corpus/、lib/diagnostics/ 執行 Phase 4 多視角審查（/parallel-evaluation）；(3) 三個目錄的 import 方向符合 domain-map §5；(4) traceability.yaml SPEC-006 三列映射與第三軸四條契約的 tests 回填為實際檔案；(5) 工作日誌記錄版本契約達成

### Rationale Chain
- 0.3.0-W4-001 why: version-bootstrap Step 6 的 W4 驗收票；PROP-005 §0.3 發版條件；quality-baseline 規則 2 Phase 4 不可跳過

### Related Files
- docs/traceability.yaml  # from 0.3.0-W4-001
- docs/work-logs/v0/v0.3/v0.3.0/v0.3.0-main.md  # from 0.3.0-W4-001

### Source Acceptance (reference)
- [ref] [ ] flutter test 全綠，IT-1、IT-2 皆通過  # from 0.3.0-W4-001
- [ref] [ ] Phase 4 多視角審查結論記於 Solution，發現項已建票  # from 0.3.0-W4-001
- [ref] [ ] lib/schema、lib/corpus、lib/diagnostics 的 import 方向逐一檢查並記錄  # from 0.3.0-W4-001
- [ref] [ ] traceability.yaml SPEC-006 映射與第三軸 0.3.0 契約的 tests 指向實際存在的檔案  # from 0.3.0-W4-001
