---
id: 0.0.3-W1-001
title: 評估 markdown 渲染套件：flutter_markdown_plus 與 gpt_markdown 以真實語料實測比較
type: ANA
status: pending
version: 0.0.3
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 選型評估：訪談階段未涵蓋的 markdown 渲染議題
  final_decision: ANA 實測比較兩個候選套件，不憑套件描述決定
  rationale: flutter_markdown 官方已 discontinued；gpt_markdown 更新最頻繁但為 LLM 輸出場景設計，通用性待驗。本專案有現成真實語料，可直接實測
who:
  current: parsley-flutter-developer
  history: {}
what: 評估 markdown 渲染套件：flutter_markdown_plus 與 gpt_markdown 以真實語料實測比較
when: 訪談階段（0.0.2）未涵蓋 markdown 渲染選型；節點詳情畫面依賴此決定
where:
  layer: Infrastructure
  files:
  - pubspec.yaml
  - lib/
  - docs/tech-decisions.md
why: flutter_markdown 已被 Flutter 官方標記 discontinued，pub.dev metadata 指定接替者為 flutter_markdown_plus。節點詳情（UC-04、SPEC-001 §6）需渲染 markdown 內文，選型未定則該畫面無法實作。本專案有現成真實語料可實測，不需憑套件描述推測。
how:
  task_type: Analysis
  strategy: 兩套件各建一個最小 spike：安裝 -> 以 docs/ 真實檔案渲染 -> 記錄缺陷。測資刻意涵蓋四類難處：markdown 表格（domain-map §3）、YAML flow block（UC-01 流程拓撲）、行內程式碼與區塊、中文長段落換行。比較 API 表面、節點層級樣式覆寫能力、文字可選取性。spike 程式碼不併入主線，結論寫入 docs/tech-decisions.md 後移除。
acceptance:
- '[ ] 兩個套件皆能安裝於本專案並成功建置 macOS app'
- '[ ] 以本專案 docs/ 的真實檔案渲染，涵蓋表格、YAML flow block、程式碼區塊、中文長段落'
- '[ ] 記錄各自的渲染缺陷與不支援語法，附具體差異描述'
- '[ ] 比較 API 表面、節點層級樣式覆寫能力、文字可選取性'
- '[ ] 產出選型結論與理由並寫入 docs/tech-decisions.md'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-08-26'
updated: '2026-08-26'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

評估 markdown 渲染套件：flutter_markdown_plus 與 gpt_markdown 以真實語料實測比較

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending
