---
id: 0.2.1-W1-001
title: 改善 schema 不相容「檢視詳情」面板改放「App 已知版本範圍：不高於 <內建版本>」（SPEC-001 1.19 §1、SPEC-004 1.45）
type: IMP
status: in_progress
version: 0.2.1
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.2.0-W1-040
scope_blocker: 契約 C1：SPEC-001 §1／SPEC-004 4.23 withDetail 面板「App 已知版本範圍」規格領先實作
dispatch_reason: ''
decision_tree_path:
  entry_point: Layer 1 版本規劃
  final_decision: 規格領先實作，凍結閘門路由至 0.2.1
  rationale: 不影響 17 專案整合測試；FR-04 兩版本值顯示仍成立
who:
  current: parsley-flutter-developer
  history: {}
what: 改善 schema 不相容「檢視詳情」面板改放「App 已知版本範圍：不高於 <內建版本>」（SPEC-001 1.19 §1、SPEC-004 1.45）
when: 0.2.1 容器；0.2.0 發版後啟用時派發
where:
  layer: Presentation
  files:
  - lib/components/blocked_state.dart
  - lib/screens/domain_view/domain_view_screen.dart
  - test/unit/screens/domain_view_test.dart
  - lib/l10n/app_zh.arb
  - lib/l10n/app_en.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_zh.dart
  - lib/l10n/app_localizations_en.dart
why: 0.2.0-W1-040 依 W1-024 定案把 SPEC-001 §1 schema 不相容列的面板內容改為「App 已知版本範圍：不高於 <內建版本>」（R4 重評），SPEC-004 4.23 withDetail 變體同步。現行 UI 面板仍為兩個版本值的重複顯示，規格領先實作。17 個真實專案無 schema 不相容實例，不影響 0.2.0 契約，凍結閘門路由至 0.2.1。
how:
  task_type: Implementation
  strategy: 依 SPEC-001 1.19 §1、SPEC-004 1.45 4.23 withDetail 與 §4.0.6、SPEC-003 1.38 §3.1：BlockedState.withDetail 面板改為 Section.static[AppText.caption, AppText.mono] 一列，caption=schemaKnownRangeLabel、mono=schemaKnownRangeValue（值代入 builtinSchemaVersionProvider）；ARB 新增此二 key（zh／en，字面依 SPEC-004 §4.0.6）並移除不再使用的 schemaAppVersionLabel／schemaProjectVersionLabel（SPEC-004 §4.0.6 指名由實作票移除），gen-l10n；推定版本時面板字面不變（SPEC-003 §3.1）。widget test 斷言面板文字含內建版本值且不含舊兩個標籤。
acceptance:
- '[x] schema 不相容狀態的檢視詳情面板顯示「App 已知版本範圍：不高於 <內建版本>」單列，本體維持兩個版本值'
- '[x] widget test 斷言面板文字，flutter test 100% 通過，dart analyze 0 issues'
- '[x] Phase 4 評估結論記錄於 Solution'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T09:39:34'
completed_at: null
created: '2026-09-23'
updated: '2026-09-23'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

改善 schema 不相容「檢視詳情」面板改放「App 已知版本範圍：不高於 <內建版本>」（SPEC-001 1.19 §1、SPEC-004 1.45）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-24 09:35] readiness=1 validate=2

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
BlockedState.withDetail 的可展開詳情面板由「App 支援版本」/「專案版本」兩列改為
「App 已知版本範圍」單列（Section.static[caption, mono]，caption=schemaKnownRangeLabel、
mono=schemaKnownRangeValue(appVersion)）。appVersion 沿用既有 state.appVersion
（gate_detection_notifier.dart 已由 builtinSchemaVersionProvider 讀出的內建版本值賦值，
本票未新增額外 provider 讀取）。projectVersion 欄位與參數維持不變（message 本體仍顯示
兩個版本值，僅詳情面板收斂為單列）。

ARB：新增 schemaKnownRangeLabel／schemaKnownRangeValue（zh/en），移除
schemaAppVersionLabel／schemaProjectVersionLabel（SPEC-004 §4.0.6 指名由本實作票移除），
執行 flutter gen-l10n 重新產生 app_localizations*.dart。

測試：test/unit/screens/domain_view_test.dart 新增面板文字斷言（含新標籤與值、
不含舊兩標籤）。test/unit/components/blocked_state_test.dart 原斷言
find.text(TestCopy.longToken) findsNWidgets(2) 隨面板收斂為單列而失效
（原本 2 次命中來自面板同時渲染 appVersion／projectVersion 兩個純值 Text；
新面板值為複合字串「不高於 {appVersion}」不再與純 token 相等），改為
find.textContaining(TestCopy.longToken) findsOneWidget；該檔不在本票
where.files 內，但變更是本票必要的直接後果（100% 測試通過為品質底線），
非範圍外潤飾。

Phase 4 評估：異動限縮於一個元件方法內的靜態渲染內容替換（4 行 Section.items
從兩組 caption+mono 精簡為一組）與對應 ARB key 改名，無新增邏輯分支、無新增
狀態、無重複程式碼。判定：無需重構。技術債務：無新增。程式碼品質：A。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
fvm dart analyze：No issues found!（0 issues）
fvm flutter test --reporter compact（全量）：+1546 -0，All tests passed!
fvm flutter test test/unit/screens/domain_view_test.dart：+42，All tests passed!
fvm flutter test test/unit/components/blocked_state_test.dart：+23，All tests passed!
（首次全量執行曾見 -2：blocked_state_test.dart 一項因面板結構變更而預期內失效，
已於本票修正；app_data_table_test.dart 一項為 drag/offset 計時相關，單檔重跑
+15 全綠，判定與本票無關的既有 flaky，未變更該檔）

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending
