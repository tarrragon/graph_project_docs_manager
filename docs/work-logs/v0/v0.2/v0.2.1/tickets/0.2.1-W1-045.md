---
id: 0.2.1-W1-045
title: 撰寫 版本範圍收斂方法論的〈執行 runbook〉章節：收件側開工與發版側收尾兩個入口的逐步動作、派發對象、CLI、PM 驗收清單、失效處置
type: DOC
status: pending
version: 0.2.1
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: Layer 1 版本規劃
  final_decision: runbook 由 PM 前台撰寫（知識在本 session）
  rationale: 代理人需重讀整日工作日誌才能寫，PM 直接寫成本最低且忠實
who:
  current: rosemary-project-manager
  history: {}
what: 撰寫 版本範圍收斂方法論的〈執行 runbook〉章節：收件側開工與發版側收尾兩個入口的逐步動作、派發對象、CLI、PM 驗收清單、失效處置
when: 0.2.0 弧線完成後立即，sync-push 前
where:
  layer: Domain
  files:
  - .claude/methodologies/version-scope-convergence-methodology.md
  - .claude/pm-rules/methodology-index.md
why: 2026-09-23 本專案 0.2.0 從開工到發版一日走完，判準與 CLI 已固化，但操作順序（先建 SA 契約審查票 → 三問 → 分批 → Batch 0 決策後才凍結 → 每批 PM 以 git 平面覆核 → finish）與失效處置（代理人收尾 CAS 失敗完成狀態停在工作區、代理人自標 scope_blocker 由 PM 判、規格領先實作路由 patch、溢出目標未登記須先登記）全靠 session 記憶。flutter_balance 要接手同一模式，新 session 讀不到這些。
how:
  task_type: Documentation
  strategy: 於〈三層推進判準〉前新增〈執行 runbook〉：入口 A 收件側開工（契約審查票→三問標記→conflicts 分批→Batch 0 決策→凍結兩步→批次派發與覆核→finish）、入口 B 發版側收尾（池已近清空但版本未發：凍結→判 blocker→finish→登記後繼）。每步列動作／派誰／CLI／產出／驗收；附「PM 驗收清單」與「常見失效與處置」表。methodology-index 條目同步。三明示與 stub 規範。
acceptance:
- '[ ] 〈執行 runbook〉含入口 A 與入口 B，每步有動作／派誰／CLI／產出／驗收五欄'
- '[ ] 「PM 驗收清單」含：完成狀態以 git show HEAD 為準、覆核重跑、CAS 失敗補提交、代理人自標 blocker 須 PM 三問判定、規格領先實作路由 patch、溢出目標須先登記'
- '[ ] methodology-index 條目更新；basil 二次審查或自行依三明示檢查清單掃描並記錄'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-23'
updated: '2026-09-23'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

撰寫 版本範圍收斂方法論的〈執行 runbook〉章節：收件側開工與發版側收尾兩個入口的逐步動作、派發對象、CLI、PM 驗收清單、失效處置

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
