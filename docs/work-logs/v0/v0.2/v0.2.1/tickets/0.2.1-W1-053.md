---
id: 0.2.1-W1-053
title: 修復 version-release 溢出目標計算缺「最近的開放後繼」分支：凍結版本有已登記且未凍結的後繼時，check／finish 仍算 patch+1 而非該後繼
type: IMP
status: in_progress
version: 0.2.1
wave: 1
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
scope_blocker: 契約：步驟 10 finish 的前移目標錯誤會把 6 張票移到未登記版本，發版動作本身不可執行
dispatch_reason: ''
decision_tree_path:
  entry_point: Layer 1 版本規劃
  final_decision: 框架缺陷在本專案修並推技能庫，帶 blocker
  rationale: '發版動作依賴它；複本漂移是 #55 已記風險的首次發作'
who:
  current: thyme-python-developer
  history: {}
what: 修復 version-release 溢出目標計算缺「最近的開放後繼」分支：凍結版本有已登記且未凍結的後繼時，check／finish 仍算 patch+1 而非該後繼
when: 0.2.1 凍結後、步驟 10 finish 前，阻擋發版
where:
  layer: Infrastructure
  files:
  - .claude/skills/version-release/scripts/version_release.py
  - .claude/skills/version-release/tests/test_overflow_target_open_successor.py
  - .claude/skills/version-release/SKILL.md
  - .claude/skills/version-release/CHANGELOG.md
why: '0.2.1 凍結後 version-release check 對 6 張溢出票算出 v0.2.2（patch+1），但 0.3.0 已登記為 planned 且未凍結，依方法論〈Patch 生命週期〉與 ticket lib 的 suggest_overflow_version（先 find_open_successor 再算 patch+1／minor+1），目標應為 0.3.0。根因：version_release.py 的 compute_overflow_target_version 是 ticket lib 的鏡射複本（docstring 註明避免跨 skill import 另寫），複本缺 find_open_successor 分支。0.2.0 發版時 patch+1／minor+1 恰等於已登記後繼，缺陷未露出。若照此 finish，6 張票會被前移到未登記的 0.2.2，違反「不開下一個 patch 承接剩餘」。canonical #55 已有兩處複本漂移風險的觀測。'
how:
  task_type: Implementation
  strategy: TDD：(1) RED——新增測試：todolist 有凍結版本 X 與已登記 planned／active 未凍結後繼 Y 時，compute_overflow_target_version 對任何動詞皆回 Y；後繼皆凍結或不存在時回 patch+1／minor+1（沿用既有）；再加一支對照測試：同一 todolist fixture 下 version-release 複本與 ticket lib suggest_overflow_version 的結果逐案相等（釘住兩端）。(2) GREEN——在複本內實作與 ticket lib find_open_successor 語意相同的搜尋（讀 todolist.yaml，取版本序最近、status 為 planned 或 active、scope 非 frozen 的後繼；比較用逐段整數）。不跨 skill import。(3) SKILL.md〈發版判準：blocker 阻擋、其餘前移〉補「有開放後繼時前移目標為該後繼」一句；CHANGELOG 次版號 +1。(4) 以本專案 todolist 實跑 check --version 0.2.1 確認 6 張目標為 0.3.0。
acceptance:
- '[x] 新增測試：有開放後繼時回後繼、無後繼時回 patch+1／minor+1，皆通過'
- '[x] 對照測試：同 fixture 下複本與 ticket lib 結果逐案相等'
- '[x] 本專案實跑 version-release check --version 0.2.1，6 張前移目標皆為 v0.3.0'
- '[x] SKILL.md 補述、CHANGELOG 版號 +1；skill 測試全綠'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T09:53:35'
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

修復 version-release 溢出目標計算缺「最近的開放後繼」分支：凍結版本有已登記且未凍結的後繼時，check／finish 仍算 patch+1 而非該後繼

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-24 09:46] readiness=1 validate=2

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
在 `version_release.py` 新增 `find_open_successor`（鏡射 `ticket_system/lib/version.py` 同名函式語意：讀 `docs/todolist.yaml`，取版本序最近、`status` 為 `planned`/`active`、`scope` 非 `frozen` 的後繼；不跨 skill import，複本內重寫）與私有輔助 `_overflow_version_tuple`／`_OVERFLOW_OPEN_STATUSES`。`compute_overflow_target_version` 改為先呼叫 `find_open_successor`，命中即直接回該後繼版本（理由「路由至最近的開放後繼版本」），未命中才回退舊規則（IMP 新功能動詞 → minor+1；其餘 → patch+1），與 ticket lib `suggest_overflow_version` 語意對齊。

順帶修正：既有測試 `test_w3_654_scope_blocker_release_gate.py` 兩案（`test_e1_pending_without_blocker_goes_to_overflow_with_target_version`、`test_e1_new_feature_action_overflows_to_next_minor`）呼叫 `collect_ticket_scope_groups` 時未 patch `get_project_root`，修復前因複本不讀 todolist 而未露出，修復後會讀到本專案真實 `docs/todolist.yaml`（其中 0.3.0 為開放後繼）導致斷言失準——補上 `patch.object(vr, "get_project_root", return_value=tmp_path)` 隔離，恢復其「無其他登記版本」的原意圖。

Phase 4（重構評估）：新函式與 ticket lib 版本並列語意相同但無法共用實作（skill 邊界，docstring 已載明理由），屬既有架構限制非本次引入；本次改動未新增巢狀或分支複雜度（沿用既有回傳 tuple 型態），無需重構。程式碼品質：A（型別標註、docstring、日誌皆補齊，函式長度 < 30 行）。技術債務：無新增；既有複本漂移風險已由對照測試釘住。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
