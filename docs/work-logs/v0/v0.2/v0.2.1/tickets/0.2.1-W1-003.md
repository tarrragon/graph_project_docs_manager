---
id: 0.2.1-W1-003
title: 改善 專案切換器「目前專案」標籤與最近專案清單改由 WorkspaceState 與持久化清單驅動，取代 RecentProjectFixture
type: IMP
status: completed
version: 0.2.1
wave: 1
priority: P2
parent_id: null
children: []
blockedBy:
- 0.2.1-W1-043
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.2.0-W1-041
scope_blocker: 契約 C3：SPEC-001 §7 目前專案名與最近專案清單規格領先實作（fixture 驅動）
dispatch_reason: ''
decision_tree_path:
  entry_point: Layer 1 版本規劃
  final_decision: 切換器資料來源屬 S-36 範圍，路由 0.2.1
  rationale: 不影響 17 專案 gate 分支判定；依賴 SPEC-005 §2.4 先定案
who:
  current: parsley-flutter-developer
  history: {}
what: 改善 專案切換器「目前專案」標籤與最近專案清單改由 WorkspaceState 與持久化清單驅動，取代 RecentProjectFixture
when: 0.2.1 容器；0.2.0-W1-043（SPEC-005 §2.4 清單持久化）落地後派發（跨版本依賴，以 relatedTo 記錄）
where:
  layer: Presentation
  files:
  - lib/screens/project_switcher/project_switcher_overlay.dart
  - lib/screens/project_switcher/project_switcher_providers.dart
  - lib/workspace/workspace_repository.dart
  - lib/workspace/workspace_types.dart
  - lib/app/shell.dart
  - integration_test/app_test.dart
  - test/unit/screens/project_switcher_test.dart
  - test/unit/workspace/workspace_repository_test.dart
why: 0.2.0-W1-041 實機驗證：選取 flutter_balance 並重啟後，切換器仍顯示 fixture 專案——目前專案標籤來自 recentProjectsProvider（RecentProjectFixture）與 currentProjectIndexProvider，與 WorkspaceState 無關；最近專案清單亦為假資料。0.2.0-W1-023 已定案清單持久化走 workspace.recentProjects JSON（S-36），規格由 0.2.1-W1-043 落 SPEC-005 §2.4（原寫 0.2.0-W1-043，該票已前移為 0.2.1-W1-043）。
how:
  task_type: Implementation
  strategy: 依 SPEC-001 §7 與 SPEC-005 1.2 §2.4（loadRecentProjects／addRecentProject 簽章、key workspace.recentProjects、path+lastOpenedAt）：目前專案標籤改讀 WorkspaceState（WorkspaceReady.path 的資料夾名）；最近清單改讀 loadRecentProjects，僅在成功載入後 addRecentProject（同 path 去重、lastOpenedAt 降冪、本版不截斷）；讀取失敗或 JSON 損壞回空清單，損壞時下一次寫入以空清單為基底並記 level 900 日誌；不可用項保留於清單、由展開時探測呈現 disabled；取消載入不動清單（SPEC-003 取消載入列）；fixture 僅保留為測試假資料。widget test：restore 後標籤為存檔路徑的資料夾名；成功載入後該項移至頂端；損壞 JSON 回空清單。
acceptance:
- '[x] 重啟後切換器「目前專案」顯示 restore() 回傳路徑的資料夾名，非 fixture'
- '[x] 最近專案清單來自持久化 JSON，選取新資料夾後清單更新並跨啟動保留'
- '[x] flutter test 100% 通過，dart analyze 0 issues'
- '[x] Phase 4 評估結論記錄於 Solution'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T10:24:05'
completed_at: '2026-09-24T10:25:20'
created: '2026-09-23'
updated: '2026-09-23'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

改善 專案切換器「目前專案」標籤與最近專案清單改由 WorkspaceState 與持久化清單驅動，取代 RecentProjectFixture

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-24 09:37] readiness=1 validate=2

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
依 SPEC-005 §2.4（新接縫，落點依三條契約）與 SPEC-001 §7／SPEC-003 §3.7：

**workspace_types.dart**：新增 RecentProject（path + lastOpenedAt UTC DateTime）。

**workspace_repository.dart**：新增 loadRecentProjects()／addRecentProject(String path)，共用既有 WorkspacePreferencesPort／Handle，key workspace.recentProjects。讀取失敗或 JSON 損壞一律回空清單（不部分採用），損壞時 addRecentProject 以空清單為基底寫入並記 level 900 日誌（規格「損壞時不覆寫的邊界」）；loadRecentProjects 回傳前依 lastOpenedAt 降冪排序（不信任儲存順序）。

**project_switcher_providers.dart**：移除 RecentProjectFixture 與其 fixture provider，新增 currentWorkspaceStateProvider（StateProvider<WorkspaceState>，初始 WorkspaceUnset）與 recentProjectsProvider（StateProvider<List<RecentProject>>，初始空清單）；currentProjectNameProvider 改由 currentWorkspaceStateProvider 的 WorkspaceReady.path 取資料夾名（新 folderNameOf helper）。

**app/shell.dart**：_restoreWorkspaceAndDetect() 於 restore() 完成後寫入 currentWorkspaceStateProvider，並呼叫 loadRecentProjects() 寫入 recentProjectsProvider（與目前資料夾是否還原成功正交，兩者獨立寫入）。

**project_switcher_overlay.dart**：
- _selectProject（點擊既有最近項）：寫入 currentWorkspaceStateProvider = WorkspaceReady(path)，不重新呼叫 repository（維持既有行為，未新增可用性探測——SPEC-003 §3.7 生命週期逐項探測為未來票範圍，本票 RecentProjectItem 一律 enabled: true、不渲染健康徽章，理由已寫入程式碼註解）。
- _handleChosenState（選擇其他資料夾成功）：WorkspaceReady 分支寫入 currentWorkspaceStateProvider，detect() 之後呼叫新增的 _persistRecentProjectSelection（addRecentProject 成功後以 loadRecentProjects() 重新整理 recentProjectsProvider，對應規格「成功載入後該項移至頂端」）。

**integration_test/app_test.dart**：_StubWorkspaceRepository 補上兩個新抽象方法的空實作（WorkspaceRepository 新增抽象方法後的必要連帶修正，非本票主動擴大範圍）。

**已知範圍外（不在本票 acceptance 內，程式碼註解已標註）**：節點數／票數摘要與健康徽章計數來源為真實 domain 掃描結果（SPEC-001 §7 註記「不在本輪範圍」）；最近專案項的可用性探測（SPEC-003 §3.7 生命週期展開時逐項探測）。兩者皆非本票 where.files 範圍，未強行實作。

### Phase 4 評估

- 重構需要：否。新增的 loadRecentProjects/addRecentProject/_parseRecentProjects/_readRecentProjects/_readRecentProjectsForWrite 皆為單一職責小函式，符合五行—十行原則；RecentProject 為純資料型別；provider 拆分（currentWorkspaceStateProvider vs recentProjectsProvider）對應規格兩個正交的資料來源，未見耦合。
- 技術債務：無新增。既有的「節點數／票數摘要與健康徽章計數為 fixture 值 0/0、可用性探測未接線」為既有規格已標註的範圍外事項，非本票引入。
- 程式碼品質：A。dart analyze 0 issues；新增邏輯有需求編號註解；錯誤處理遵循既有「不拋例外、失敗回空清單/false、level 900 日誌」契約，與 §2.2 既有模式一致。
- 結論：無需立即重構。

### 自檢結果（Layer 1）

執行時機：complete 前
掃描手段：
- grep -n "數據|代碼|默認|文檔|軟件|硬件|信息|視頻" 於 5 個新增/修改的 lib 檔案 → 0 處命中
- flutter analyze → 0 issues
- flutter test（全量）→ 1556/1556 通過
- 人工對照 ticket-body-schema.md IMP 必填章節清單（Problem Analysis/Solution/Test Results 皆已填）

自檢結論：已掃描範圍內通過（未涵蓋：B2 emoji、B3 簡體/日文漢字字形逐字掃描——本票新增中文註解量小，已人工複讀確認無異體字形與 emoji）。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
worktree: /Users/mac-eric/project/graph_project_docs_manager-0.2.1-W1-003
branch: feat/0.2.1-W1-003

- flutter test test/unit/workspace/workspace_repository_test.dart --reporter compact：42/42 通過（新增 G7 群組 10 條，loadRecentProjects/addRecentProject）
- flutter test test/unit/screens/project_switcher_test.dart --reporter compact：17/17 通過
- flutter test --reporter compact（全量）：1556/1556 通過
- flutter analyze：0 issues

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-24T10:25:20
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.2.1-W1-043 | chars: 432 -->

### Task Reference
- 0.2.1-W1-043 what: 更新 SPEC-005-workspace-driven-ports.md：(1) §2.2 回饋點表新增 remove 結果時刻列；(2) 新增 §2.4 最近專案清單持久化（key workspace.recentProjects 為 JSON 陣列，每項含 path + lastOpenedAt，回饋點承擔者表）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.2.1-W1-043 why: S-21 與 S-36 的 workspace domain 側接縫在 SPEC-005 無對應描述，後續 IMP 票無法消費回饋點承擔者定義。來源：0.2.0-W1-023 Solution（來源已完成，結論見 Solution）

### Related Files
- docs/spec/workspace/SPEC-005-workspace-driven-ports.md  # from 0.2.1-W1-043（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] §2.2 表新增 remove 結果時刻列（承擔者、呼叫端投影、日誌投影三欄皆填）  # from 0.2.1-W1-043（來源已完成，結論見 Solution）
- [ref] [x] §2.4 最近專案清單持久化章節含 key 結構、JSON schema、回饋點承擔者表  # from 0.2.1-W1-043（來源已完成，結論見 Solution）
- [ref] [x] SPEC-005 version 欄遞增至 1.2  # from 0.2.1-W1-043（來源已完成，結論見 Solution）
