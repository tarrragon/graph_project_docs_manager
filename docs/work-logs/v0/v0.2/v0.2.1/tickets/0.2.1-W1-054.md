---
id: 0.2.1-W1-054
title: 修復 點擊最近專案項未走載入路徑：_selectProject 只改目前專案標籤，不載入、不寫已存路徑、不移至頂端
type: IMP
status: in_progress
version: 0.2.1
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.2.1-W1-003
discovered_during: null
scope_blocker: 契約 C3：SPEC-001 §7 目前專案名須反映實際載入專案；SPEC-003 最近專案項列與 SPEC-005 §2.4 寫入時機未收斂
dispatch_reason: ''
decision_tree_path:
  entry_point: Layer 1 版本規劃
  final_decision: 契約 C3 未收斂部分另建阻擋票，W1-003 已合入不重開
  rationale: W1-003 的 AC 未涵蓋點擊最近項路徑，已完成部分正確可合入；缺口為獨立可驗收的單一行為
who:
  current: parsley-flutter-developer
  history: {}
what: 修復 點擊最近專案項未走載入路徑：_selectProject 只改目前專案標籤，不載入、不寫已存路徑、不移至頂端
when: 0.2.1-W1-003 合入後
where:
  layer: Presentation
  files:
  - lib/screens/project_switcher/project_switcher_overlay.dart
  - lib/workspace/workspace_repository.dart
  - test/unit/screens/project_switcher_test.dart
  - test/unit/workspace/workspace_repository_test.dart
why: 0.2.1-W1-003 把目前專案標籤與最近清單接上 WorkspaceState 與持久化，但 project_switcher_overlay.dart 的 _selectProject（點擊最近專案項）只把 currentWorkspaceStateProvider 設為 WorkspaceReady(path) 並重置旗標，未經 WorkspaceRepository 載入該路徑、未寫入已存路徑、未呼叫 addRecentProject。結果：標籤顯示新專案而畫面內容仍為舊專案，重啟後回到舊專案。違反 SPEC-003 最近專案項列（浮層收合、六頁狀態重置、Domain 視圖進入 loading、成功載入後該項移至頂端）與 SPEC-005 1.2 §2.4 寫入時機（成功載入後 addRecentProject），屬 0.2.1 契約 C3「顯示目前專案名」未收斂。W1-003 執行者回報「點擊既有最近專案項維持既有行為」，既有行為為 fixture 時代的假切換。
how:
  task_type: Implementation
  strategy: _selectProject 改走與「選擇其他資料夾」成功分支相同的載入路徑：以該 path 經 WorkspaceRepository 開啟（不可讀／不存在時依 SPEC-003 選擇其他失敗列的回饋處理、清單不變）→ 寫已存路徑 → detect → 成功後 addRecentProject 並刷新清單；共用 _handleChosenState 的 WorkspaceReady 分支，不另寫一份。widget test：點擊非頂端項後該項移至頂端、已存路徑為該 path、失敗路徑清單不變。
acceptance:
- '[x] 點擊最近專案項後，實際載入該路徑（Domain 視圖進入 loading），已存路徑改為該 path，成功後該項移至清單頂端'
- '[x] 該路徑不可讀或不存在時依 SPEC-003 選擇其他失敗列回饋，不轉狀態、不寫已存路徑、清單不變'
- '[x] 載入路徑與選擇其他資料夾共用同一實作，無重複分支'
- '[x] flutter test 100% 通過，dart analyze 0 issues'
- '[x] Phase 4 評估結論記錄於 Solution'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-24T10:46:55'
completed_at: null
created: '2026-09-24'
updated: '2026-09-24'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

修復 點擊最近專案項未走載入路徑：_selectProject 只改目前專案標籤，不載入、不寫已存路徑、不移至頂端

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-24 10:28] readiness=2 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
`_selectProject` 改為 async，改走與「選擇其他資料夾」成功分支共用的載入路徑：

- `WorkspaceRepository` 新增 `openPath(String path)`，直接呼叫既有私有方法
  `_persistAndInspect(path)`（與 `chooseFolder()` 內部呼叫同一段邏輯），
  差別只在路徑來源不經 `_pickDirectoryPath()`。
- `project_switcher_overlay.dart` 抽出 `_handleChooseFolderResult(context,
  ref, result)` 承接 `ChooseFolderResult` 四變體的共用回饋處理（原本只在
  `_chooseFolder` 內），由 `_chooseFolder` 與新的 `_selectProject` 共用，
  避免同一組 switch 分支重複兩份（滿足 AC3「無重複分支」）。
- `_selectProject(context, ref, project)` 改為：
  `repository.openPath(project.path)` → `_handleChooseFolderResult`。
  成功時沿用既有 `_handleChosenState` 的 `WorkspaceReady` 分支：寫入
  `currentWorkspaceStateProvider`、重置降級/推定版本旗標、呼叫
  `gateDetectionNotifier.detect(path)`、`_persistRecentProjectSelection`
  （`addRecentProject` + 重新 `loadRecentProjects()` 更新清單，成功後該項
  移至頂端）、`_dismiss`。失敗（`WorkspaceUnavailable`）沿用既有 SnackBar
  回饋分支，不轉狀態、不寫已存路徑、清單不變——與「選擇其他資料夾」選定
  後失敗完全同一段程式碼路徑（滿足 AC1、AC2）。
- `_buildRecentProjectItem` 新增 `context` 參數傳給 `_selectProject`。

測試調整（`0.2.1-W1-003` 遺留的假 repository 未設 directoryProbe，本票起
點擊最近項會真的走探測路徑，故一併補上）：
- `_fakeRepositoryWithRecents` 補 `directoryProbe` 參數（預設可用）。
- 新增 widget test：成功載入路徑（`WorkspaceReady`、清單移至頂端、已存
  路徑寫入，經有狀態 `_StatefulPreferencesPort` 驗證）與失敗路徑（不轉
  狀態、不寫已存路徑、清單不變、顯示 SnackBar）。
- `workspace_repository_test.dart` 新增 G2b 群組 3 則測試，直接驗證
  `openPath` 與 `chooseFolder` 共用 `_persistAndInspect`（成功寫入路徑、
  失敗回 `WorkspaceUnavailable`、不呼叫 `pickDirectoryPath`）。

**Phase 4 評估**：抽出的 `_handleChooseFolderResult` 消除重複分支，符合
DRY；`openPath` 命名與既有 `chooseFolder` 對稱，語意清楚。未發現需要
進一步重構的技術債務。結論：無需額外重構。

### 自檢結果

- 類別 A（文字品質）：Solution 各段落含改動原因（why）、修復前後行為差異
  （consequence）與具體改動點（action）；無「示例先於原則」句型。
- 類別 B（禁用字/字元集）：無簡體字、日文漢字、emoji、大陸用語（數據/
  代碼/默認等）。
- 類別 C（Schema 與結構）：Problem Analysis/Solution/Test Results 三章節
  皆已填寫實質內容，無殘留佔位符；ticket body 僅使用 Schema 合法 H2
  標題，無自訂 H2。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
- `fvm dart analyze lib/ test/`：0 issues
- `fvm flutter test test/unit/screens/project_switcher_test.dart --reporter compact`：19/19 通過
- `fvm flutter test test/unit/workspace/workspace_repository_test.dart --reporter compact`：45/45 通過
- 全量 `fvm flutter test --reporter compact`：1561/1561 通過（0 失敗）

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.2.1-W1-003 | chars: 719 -->

### Task Reference
- 0.2.1-W1-003 what: 改善 專案切換器「目前專案」標籤與最近專案清單改由 WorkspaceState 與持久化清單驅動，取代 RecentProjectFixture（來源已完成，結論見 Solution）

### Rationale Chain
- 0.2.1-W1-003 why: 0.2.0-W1-041 實機驗證：選取 flutter_balance 並重啟後，切換器仍顯示 fixture 專案——目前專案標籤來自 recentProjectsProvider（RecentProjectFixture）與 currentProjectIndexProvider，與 WorkspaceState 無關；最近專案清單亦為假資料。0.2.0-W1-023 已定案清單持久化走 workspace.recentProjects JSON（S-36），規格由 0.2.1-W1-043 落 SPEC-005 §2.4（原寫 0.2.0-W1-043，該票已前移為 0.2.1-W1-043）。（來源已完成，結論見 Solution）

### Related Files
- lib/screens/project_switcher/project_switcher_providers.dart  # from 0.2.1-W1-003（來源已完成，結論見 Solution）
- lib/workspace/workspace_repository.dart  # from 0.2.1-W1-003（來源已完成，結論見 Solution）
- lib/workspace/workspace_types.dart  # from 0.2.1-W1-003（來源已完成，結論見 Solution）
- lib/app/shell.dart  # from 0.2.1-W1-003（來源已完成，結論見 Solution）
- integration_test/app_test.dart  # from 0.2.1-W1-003（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] 重啟後切換器「目前專案」顯示 restore() 回傳路徑的資料夾名，非 fixture  # from 0.2.1-W1-003（來源已完成，結論見 Solution）
- [ref] [x] 最近專案清單來自持久化 JSON，選取新資料夾後清單更新並跨啟動保留  # from 0.2.1-W1-003（來源已完成，結論見 Solution）
- [ref] [x] flutter test 100% 通過，dart analyze 0 issues  # from 0.2.1-W1-003（來源已完成，結論見 Solution）
- [ref] [x] Phase 4 評估結論記錄於 Solution  # from 0.2.1-W1-003（來源已完成，結論見 Solution）
