<!-- 注意：本文件禁止使用 emoji（交接文件規範） -->

# v0.2.1 版本工作日誌

**版本號**: v0.2.1
**代號**: Runoff-2
**登記日期**: 2026-09-23
**目標**: 0.2.0 凍結後由閘門路由至此的品質改善票；固定容器，做多少算多少
**狀態**: planned（0.2.0 發版時啟用；0.2.0 發版即關閉新收件）

---

## 前情提要

0.2.0 於 2026-09-23 Batch 0 完成後凍結 scope。此後凡不帶 `--scope-blocker` 理由、動詞屬修復／改善／分析／文件的新根票，凍結閘門路由至本版；新功能類路由至 0.3.0（登記時機依 PROP 序列）。

整合測試沿用上層 minor：17 個真實專案的 gate manifest 仍通過。依 `docs/todolist.yaml` 檔頭的 patch 版本登記規則登記，不需要 PROP。

---

## 開發日誌

### 2026-09-23：登記

首張路由票為 schema 不相容「檢視詳情」面板改放「App 已知版本範圍」（SPEC-001 1.19 §1 由 `0.2.0-W1-040` 修訂，UI 尚無對應實作）。
- 2026-09-23: 0.2.1-W1-044 完成 -- 同步 pubspec.yaml 版本至 0.2.1（version-release finish 啟用時已 bump 但啟用提交未納入，main 上 PM 不可提交該檔）
- 2026-09-23: 0.2.1-W1-045 完成 -- 撰寫 版本範圍收斂方法論的〈執行 runbook〉章節：收件側開工與發版側收尾兩個入口的逐步動作、派發對象、CLI、PM 驗收清單、失效處置
- 2026-09-23: 0.2.1-W1-046 完成 -- 修正 version-progression 三層推進判準的 patch 條與方法論矛盾，並把 patch 版本登記規則由專案 todolist 檔頭搬入框架
- 2026-09-23: 0.2.1-W1-047 完成 -- 補充 parallel-dispatch〈並行派發後驗證〉：PM 側 worktree 合入協定與驗收以 git HEAD 為準的條文

---

### 2026-09-23：版本弧線固化（供其他專案接手同一模式）

0.2.0 一日走完後盤點：判準與 CLI 已在框架，但「照什麼順序走一輪」靠 session 記憶；`version-progression` 的 patch 推進判準與方法論矛盾；patch 登記規則只在本專案 todolist 檔頭。三張 DOC 票收斂：`0.2.1-W1-045` 方法論〈執行 runbook〉（收件側開工十步／發版側收尾五步、PM 驗收路由、常見失效）、`0.2.1-W1-046` version-progression 4.3.0（判準同構、〈版本登記規則〉搬入框架、todolist 檔頭改指標）、`0.2.1-W1-047` parallel-dispatch 4.33.0（PM 側合入四步、驗收三軸、失效處置）。basil 二次審查高 2 中 12 全修。canonical 觀測：#16 版本登記無 CLI、#55 跨版本關聯不支援、#55 finish 啟用漏 pubspec。sync-push 後由 flutter_balance 新 session 以入口 B（發版側收尾：0.2.1 從未發版、池近清空、下一版無提案）驗證 runbook 可接手。

#### 下個 Session 接手 Context（2026-09-23）

本專案：0.2.1 active（收件中，11 張含 3 張規格領先實作票、3 張已完成的框架文件票）；0.3.0 planned（PROP-005 §0.3，開工前先解上游 `*_REQUIRED_FIELDS` 裁決）。若接手本專案，依方法論〈執行 runbook〉入口判定：0.2.1 屬「凍結前、票池小」，可直接入口 A 從契約審查起，契約 = 上層 0.2.0 整合測試仍通過。技能庫待 pull：compositional-writing、multi-round-review。

---

### 2026-09-24：runbook 入口 A 步驟 1 清點

入口判定：入口 A（收件側開工）。依據：0.2.1 為 active、版本條目無 `scope` 欄（未凍結）；契約已寫在版本條目 notes（上層 0.2.0 的 17 專案 gate manifest 仍通過）；10 張 pending 全數未開工，4 張 completed 皆為框架文件或 pubspec 同步，不屬契約工作。

git 清點：工作區僅 2 個 untracked（`v0.1.0/tickets/.ticket-create.lock`、`v0.1.0/tickets/0.1.0-W3-641.md`），皆在 0.1.0 目錄，與 0.2.1 無關，來源未判定，不動。

| 票 | 型 | what（摘要） | where.files | blockedBy／隱性依賴 |
|----|----|-------------|-------------|--------------------|
| W1-001 | IMP P2 | schema 不相容面板改放「App 已知版本範圍」 | `blocked_state.dart`、`domain_view_screen.dart`、`domain_view_test.dart` | 無；規格側由 W1-002 對齊 SPEC-003 |
| W1-002 | DOC P2 | SPEC-003 三處對齊 SPEC-001 1.19／SPEC-004 1.45 | `SPEC-003` | 無 |
| W1-003 | IMP P2 | 切換器目前專案與最近清單改由 WorkspaceState 與持久化驅動 | `project_switcher_overlay.dart`、`workspace_repository.dart`、`project_switcher_test.dart` | 隱性依賴 W1-043（SPEC-005 §2.4）；why 引用的 `0.2.0-W1-043` 已不存在，實為 `0.2.1-W1-043` |
| W1-020 | IMP P3 | 刪除五個無引用 l10n key | `app_zh.arb`、`app_en.arb`、三個 `app_localizations*.dart` | 引用的 `0.2.0-W1-019` 已 completed，解封 |
| W1-025 | IMP P2 | 破洞掃描接真實資料源 | `gap_report_provider.dart`、`SPEC-003` | 無 |
| W1-026 | ANA P2 | 量測提示截斷真實發生頻率 | `app_snack_bar.dart` | 無；需啟動 App |
| W1-027 | ANA P2 | Developer ID 簽章 .app 內 ExternalOpener 行為比對 | `external_opener.dart`（read） | 無；需簽章身分（本機 0 個） |
| W1-028 | IMP P1 | 實機驗證 MacosScanNotifier 四項平台事實回填 SPEC-003 | `SPEC-003`、`macos_scan_notifier.dart`（read）、`AppDelegate.swift`（read） | 通知送出依賴 W1-025 真實耗時（W1-025 why 所述） |
| W1-039 | ANA P3 | 補 PROP-005 heavy 級評估章節 | `PROP-005` | 無 |
| W1-043 | DOC P2 | SPEC-005 補 remove 回饋點與 §2.4 持久化 | `SPEC-005` | 無；被 W1-003 依賴 |

`ticket track conflicts --version 0.2.1`：W1-002／W1-025／W1-028 三票兩兩交集於 `SPEC-003`，須序列。
- 2026-09-24: 0.2.1-W1-049 完成 -- 修正 版本範圍收斂方法論兩處：入口判定的契約來源涵蓋版本條目 notes；Patch 生命週期的收件窗口改為「自身凍結前持續收件」
- 2026-09-24: 0.2.1-W1-048 完成 -- 審查 0.2.1 版本契約（上層 0.2.0 的 17 專案 gate manifest 整合測試）對現況是否仍成立

---

### 2026-09-24：步驟 2 驗收與步驟 3 三問標記

**步驟 2 驗收（W1-048）**：commit `0ebca2627` 存在，status completed，5 條驗收條件全勾，Solution 三表 + 形態裁決齊備，無衍生票（五處規格矛盾皆已由 W1-002／W1-043 承接）。PM 覆核：
- 重跑 `test/integration/gate_manifest_test.dart`：20/20 通過。
- saffron 的 manifest 表只比對了凍結 fixture，AC1 的「現況」沒有對本機實況驗。PM 補掃 `~/project/*/.claude`：17 個，專案集合與 fixture 相同；VERSION 有無與 fixture 一致；`tracking_schema.json` 僅 flutter_balance、graph_project_docs_manager 兩個有，`schema_generated_at_framework_version` 皆為 2.40.3，與 fixture 一致。兩者 `.claude/VERSION` 已由 2.59.9／2.60.3 前進至 2.60.5，saffron 已論證 gate 判定不依賴 VERSION 絕對值。結論：契約對實況亦成立，放行。

**步驟 3 三問**（契約 = 上層 0.2.0 的 17 專案 gate manifest 仍通過）：

| 票 | Q1 不做會缺契約的可觀察行為？ | Q2 不做整合測試會失敗？ | Q3 在契約邊界內？ | 判定 | 依據 |
|----|---|---|---|---|---|
| W1-001 | 否 | 否 | 版本範圍內，gate 契約外 | 溢出 | 17 專案 incompatible 實例為 0，面板不在 gate 分支判定路徑 |
| W1-002 | 否 | 否 | 同上 | 溢出 | 規格文字同步，W1-048 判定不觸及 gate 三訊號 |
| W1-003 | 否 | 否 | 同上 | 溢出 | 切換器顯示，不影響 gate 判定 |
| W1-020 | 否 | 否 | 同上 | 溢出 | 刪無引用 l10n key |
| W1-025 | 否 | 否 | 同上 | 溢出 | 破洞掃描資料源，不在 gate 判定路徑 |
| W1-026 | 否 | 否 | 同上 | 溢出 | SnackBar 量測 |
| W1-027 | 否 | 否 | 同上 | 溢出 | 發布通路驗證，gate 測試為 mock probe |
| W1-028 | 否 | 否 | 同上 | 溢出 | 平台通知事實，不在 gate 路徑 |
| W1-039 | 否 | 否 | 同上 | 溢出 | 提案文件完整性 |
| W1-043 | 否 | 否 | 同上 | 溢出 | 規格補段 |

依三問字面，全數 10 張為「溢出」，0.2.1 無必要票：凍結後阻擋數為 0，依〈發版判準〉可立即發版，10 張由發版動作前移至最近開放後繼（0.3.0）。Q3 有歧義：版本條目 notes 寫「範圍 = 凍結閘門路由至此的票」，依此讀法 10 張都在範圍內，但 Q1／Q2 仍指向溢出，且「任一項指向不屬於即溢出」。這個結果（patch 契約若只剩「上層整合測試仍通過」，patch 永遠沒有必要票）交 PM 裁決後才進步驟 4／5／7。

### 2026-09-24：裁決 B 後重跑三問、溢出分流、分批

**裁決**：採 B，屬補方法論缺口不是放寬判準。patch 契約 = (1) 上層整合測試不變式 + (2) 規格領先實作的收斂承諾 + (3) 上層契約缺陷修復；Q1 改問「不做這張票，會有已發布規格條款持續與實作不一致、或上層契約行為持續有缺陷嗎？」，Q2、Q3 不變。方法論條文由交辦 session 另票修。契約 (2) 的逐條清單 C1–C4 已寫入 `docs/todolist.yaml` 0.2.1 條目 notes。

**步驟 3 重跑**：

| 票 | Q1（patch 版） | Q2 | Q3 | 判定 | 依據 |
|----|---|---|---|---|---|
| W1-001 | 是，C1 | 否 | 內 | 必要 | SPEC-001 §1／SPEC-004 4.23 已改面板；`lib/` 無 `schemaKnownRange` 引用 |
| W1-002 | 是，C2 | 否 | 內 | 必要 | SPEC-003 三處與 SPEC-001 1.19／SPEC-004 1.45 不一致（W1-048 衝突表 1–3） |
| W1-003 | 是，C3 | 否 | 內 | 必要 | SPEC-001 §7 已發布「顯示目前專案名」，現行由 fixture 驅動——依據是既有 §7 條款，不需等 W1-043 落地才成立 |
| W1-043 | 是，C4 | 否 | 內 | 必要 | 本身是規格補段，不是「規格領先實作」；判必要的依據是 C3 實作的拓撲前置（持久化回饋點無規格則 W1-003 無可消費的定義） |
| W1-020 | 否 | 否 | 內 | 溢出 | 刪無引用 key，無規格條款指向（SPEC-004 §4.0.6 指名由實作票移除的是 `schemaAppVersionLabel`／`schemaProjectVersionLabel`，歸 W1-001，不在本票五個 key 內） |
| W1-025 | 否 | 否 | 外 | 溢出 | SPEC-001 §5 破洞報告確由 fixture 驅動，但真實資料源＝0.3.0 契約（五語料專案真破洞分類）的範圍，Q3 指向下一已知版本 |
| W1-026 | 否 | 否 | 內 | 溢出 | 量測，無規格條款未達 |
| W1-027 | 否 | 否 | 內 | 溢出 | 發布通路驗證，無規格條款未達 |
| W1-028 | 否 | 否 | 內 | 溢出 | 實機事實回填規格（方向是實作→規格），且通知送出依賴 W1-025 |
| W1-039 | 否 | 否 | 內 | 溢出 | 提案章節，非規格條款 |

與交辦 session 預期一致（4 必要／6 溢出）；依據差異兩處：W1-003 以 SPEC-001 §7 為直接依據；W1-043 以 C3 的拓撲前置為依據；W1-025 的溢出依據為 Q3。

**步驟 4 溢出分流**：6 張溢出皆為品質／能力類，目標為最近開放後繼 0.3.0（`docs/todolist.yaml` status planned，已登記）；留池，由 0.2.1 發版動作前移。框架類 0 張。

**步驟 5 分批**（拓撲 + `where.files` + 驗收容量）：

| 批 | 票 | 性質 | 交集檢查 | 拓撲 |
|----|----|------|---------|------|
| Batch 1（決策／文件） | W1-002（thyme）、W1-043（star-anise） | 規格修訂，主 repo 隔離索引提交 | SPEC-003 vs SPEC-005，無交集 | 無前置 |
| Batch 2（實作） | W1-001、W1-003（parsley，各自 worktree） | 實作 | `blocked_state.dart`／`domain_view_*` vs `project_switcher_*`／`workspace_repository.dart`，無交集 | W1-003 blockedBy W1-043；W1-001 在 W1-002 後以規格一致為前提 |

SPEC-003 三票序列：只有 W1-002 為必要票，W1-025／W1-028 溢出不排入本版批次，交集在本版不發生。兩批各 2 張，在 PM 一輪可逐份核對範圍內。
- 2026-09-24: 0.2.1-W1-050 完成 -- 補充 版本範圍收斂方法論：patch 契約的形態（不變式＋規格領先實作收斂＋上層契約缺陷修復）與三問對 patch 的 Q1 問法
- 2026-09-24: 0.2.1-W1-043 完成 -- 更新 SPEC-005 v1.2：新增 remove 回饋點列與最近專案清單持久化接縫
- 2026-09-24: 0.2.1-W1-002 完成 -- 同步 SPEC-003 三處與 SPEC-001 1.19／SPEC-004 1.45 對齊：§3.1 檢視 schema 詳情列、§2.7 版本值顯示列、§2.4 降級徽章段補推定徽章

### 2026-09-24：步驟 6 決策批次驗收與策略校正

| 票 | commit | 檔案範圍 | AC | 核對結論 |
|----|--------|---------|----|---------|
| W1-043 | `f33179fe9` | 僅 SPEC-005（1.1→1.2） | 3/3 | 放行。執行者補了四項定案來源未寫的預設（清單不截斷、讀取失敗一律回空清單、損壞內容由下次寫入取代並記 level 900、不可用項不移除），PM 對照 SPEC-003 取消載入列「清單不變」、最近專案項列「降冪、成功載入後移至頂端」、SPEC-001 §7 不可用原因常駐，無矛盾，接受 |
| W1-002 | `2509bfc0c` | 僅 SPEC-003（1.37→1.38） | 3/3 | 放行。執行者回報殘留：SPEC-001〈檢視詳情面板〉註記末句仍寫 SPEC-003「待同步」，W1-002 完成後即成過期陳述，屬 C2 → 建 `0.2.1-W1-052`（DOC，source W1-002，必要），已派 thyme |

**策略校正（Batch 2）**：
- W1-001：how 改為依 SPEC-004 §4.0.6 指名 key `schemaKnownRangeLabel`／`schemaKnownRangeValue`，並移除 SPEC-004 指名由實作票移除的 `schemaAppVersionLabel`／`schemaProjectVersionLabel`；where.files 補 ARB 與三個 `app_localizations*.dart`（原票缺，改 ARB 必觸及）。與溢出票 W1-020 同檔，但 W1-020 不在本版批次。
- W1-003：how 改為依 SPEC-005 1.2 §2.4 的 `loadRecentProjects`／`addRecentProject` 與上述四項預設；where.files 補 `project_switcher_providers.dart`（`recentProjectsProvider` 所在）與 `workspace_repository_test.dart`。

必要票更新為 5 張：W1-001／002／003／043／052。
- 2026-09-24: 0.2.1-W1-051 完成 -- 修正 版本範圍收斂方法論入口 B 第 1 步：patch 契約補寫改為比照入口 A 步驟 2 建唯讀 ANA 產出三成分
- 2026-09-24: 0.2.1-W1-052 完成 -- 更新 SPEC-001 §1〈檢視詳情面板〉註記末句：SPEC-003 同步已由 0.2.1-W1-002 完成，改寫「待同步」為已同步並引用 SPEC-003 1.38

### 2026-09-24：步驟 7 凍結、步驟 8 派發 Batch 2

凍結兩步（交辦 session 確認後執行）：`docs/todolist.yaml` 0.2.1 加 `scope: "frozen"`（`a7f153a3c`）；`set-scope-blocker` W1-001（契約 C1）、W1-003（契約 C3）。`version-release check --version 0.2.1`：阻擋 2（W1-001、W1-003）、前移 6（W1-020／025／026／027／028／039），與三問結果一致。

**發現（框架）**：check 列出的前移目標為 `v0.2.2`（patch+1），不是最近開放後繼 `0.3.0`（planned、未凍結）。原因：`.claude/skills/version-release/scripts/version_release.py` 的 `compute_overflow_target_version` 是 ticket lib `suggest_overflow_version` 的鏡射複本，未含 `find_open_successor` 分支；ticket lib 版本有。影響步驟 10：finish 若依此前移，會把 6 張移到未登記的 0.2.2，違反〈Patch 生命週期〉「不開下一個 patch 號碼承接剩餘」與本檔發版條件 3。已回報交辦 session（框架修復由其承接）；發版前 PM 以 `finish --dry-run` 核對前移目標，未修復則不執行 finish。

Batch 2：`worktree create` 建兩個 worktree（W1-001 建立時 10 秒逾時，留下 `initializing` 鎖；核對 HEAD 與檔案完整後 `git worktree unlock`）。派 parsley ×2 並行，各自 worktree，不得自行 merge／push。
- 2026-09-24: 0.2.1-W1-053 完成 -- 修復 version-release 溢出目標計算缺「最近的開放後繼」分支：凍結版本有已登記且未凍結的後繼時，check／finish 仍算 patch+1 而非該後繼
- 2026-09-24: 0.2.1-W1-001 完成 -- 改善 schema 不相容「檢視詳情」面板改放「App 已知版本範圍：不高於 <內建版本>」（SPEC-001 1.19 §1、SPEC-004 1.45）
- 2026-09-24: 0.2.1-W1-003 完成 -- 改善 專案切換器「目前專案」標籤與最近專案清單改由 WorkspaceState 與持久化清單驅動，取代 RecentProjectFixture

---

## 發版條件

1. 0.2.0 的 17 專案 gate manifest 整合測試仍通過。
2. `version-release check` 對 0.2.1 無帶 scope_blocker 的 pending 票。
3. 剩餘 pending 前移至當時最近的開放後繼版本，不開 0.2.2。

---

*最後更新：2026-09-23*
