---
id: 0.2.0-W1-023
title: 定案 workspace domain 三項接縫：清除已存路徑、健康徽章計數來源、最近專案清單持久化
type: ANA
status: completed
version: 0.2.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.2.0-W1-043
source_ticket: 0.1.0-W3-335.38
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-335.38 不在追修票範圍項
  final_decision: 另開 ANA 定承接
  rationale: PM 指派承接，非用戶風格偏好
who:
  current: saffron-system-analyst
  history: {}
what: 定案 workspace domain 三項接縫：清除已存路徑、健康徽章計數來源、最近專案清單持久化
when: W3-335.38 完成後
where:
  layer: Domain
  files:
  - docs/domain-map.md::write
why: W3-335.38 WRAP 定案時歸為 domain 規格承接、不在 SPEC-001/003/004 追修票範圍：S-21 附帶（取消載入是否清除已存專案路徑的接縫）、S-35（健康徽章計數由誰計算）、S-36 附帶（最近專案清單持久化）
how:
  task_type: Analysis
  strategy: 讀 .38 Solution S-21、S-35、S-36 → 對照 domain-map Workspace domain 公開面與 PROP-001 持久化決策 → 判定承接位置（domain-map 公開面／新建 workspace spec／tech-decisions 補記）→ 寫改法草稿
acceptance:
- '[x] 三項逐項承接位置與依據'
- '[x] 改法草稿（檔名＋章節）；需新建 spec 時列出章節骨架'
tdd_phase: phase0
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-23T17:08:54'
completed_at: '2026-09-23T17:19:01'
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
chain:
  root: 0.2.0-W1-023
  parent: null
  depth: 0
  sequence:
  - 23
scope_blocker: 契約前置：workspace 三接縫（清除已存路徑／最近專案持久化）定案
tdd_phase_source: auto
---

# Execution Log

## Task Summary

定案 workspace domain 三項接縫：清除已存路徑、健康徽章計數來源、最近專案清單持久化

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

W3-335.38 歸類的三項接縫皆涉及 workspace domain 尚未定義的操作或資料結構：

| 接縫 | 來源 | 根因 |
|------|------|------|
| S-21 取消載入是否清除已存路徑 | V4-02 G47 | `WorkspacePreferencesHandle` 只有 `readString`／`writeString`，無 `remove` 操作；`WorkspaceRepository` 無清除已存路徑的公開方法 |
| S-35 健康徽章計數來源 | V4-04 G19 | domain-map §3 Workspace 公開面未列健康計數；SPEC-001 §7 的 .41 追修已補註記但 workspace domain 側的來源定義缺席 |
| S-36 最近專案清單持久化 | V4-04 G20 | `WorkspaceRepository` 只持久化單一路徑（`workspace.path`），無清單儲存機制；SPEC-005 未描述清單持久化接縫 |

三項的共同特徵：.38 的 WRAP 裁決已定（O1），UI 規格側的改法已歸入 .41/.42 追修清單，但 workspace domain 側的承接（接縫新增、公開面擴充、持久化格式）不在 .40/.41/.42 範圍，因此需由本票定案。

### 影響範圍

| 項 | 受影響檔案 | 影響類型 |
|----|----------|---------|
| S-21 | SPEC-005 §2.2（回饋點表）、`workspace_repository.dart`（介面+實作） | 介面擴充（新增 `remove`）+ 公開方法新增 |
| S-35 | `docs/domain-map.md` §3（公開面補列）；0.1 假資料，不影響程式碼 | 文件補註 |
| S-36 | SPEC-005（新增清單持久化接縫）、`workspace_repository.dart`（清單 CRUD）、`docs/domain-map.md` §3 | 公開方法新增（介面不變） |

已驗證事實：
- `WorkspacePreferencesHandle` 只有 `readString(String key)` 和 `writeString(String key, String value)`
- `SharedPreferences` 2.5.5 有 `remove(String key)` 方法可供 adapter 實作
- W1-021 已建立 key 版本化機制（`_currentSchemaVersion = 1`、`_migrateSchema`），新增 key 不需遞增版號
- domain-map §3 Workspace 公開面現為「目前路徑、可用性狀態、開啟原始檔」，內部面為「路徑持久化、可用性探測」

### 相關 Error Pattern

無直接相關的已知錯誤模式。三項皆為新功能接縫定案，非既有實作的缺陷。

---

### 派發日誌

- [2026-09-23 17:05] readiness=0 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## 重現實驗結果

### 實驗方法

逐項查證：(1) 讀 .38 Solution 的 S-21、S-35、S-36 裁決與改法；(2) 讀 workspace_repository.dart 與 workspace_types.dart 現況介面；(3) 讀 SPEC-005 全文確認現有接縫清單；(4) 讀 domain-map §3 Workspace 行；(5) 讀 tech-decisions.md W1-021 補記確認 key 版本化策略。

### 實驗執行

**S-21 查證**：
- .38 裁決 O1：取消載入清除已存路徑，最近清單不變
- .38 改法明示：「SPEC-005 §2.2 現只有 `writeString`，清除操作的接縫與回饋點由 workspace domain 補」
- 實讀 `lib/workspace/workspace_repository.dart`：`WorkspacePreferencesHandle` 只有 `readString` 和 `writeString`，無 `remove`
- SharedPreferences 2.5.5 有 `remove(String key)` 方法（`Future<bool>`），adapter 可直接包裝
- 清除操作需同時移除 `workspace.path` 和 `workspace.schemaVersion` 兩個 key

**S-35 查證**：
- .38 裁決 O1：0.1 假資料驅動，計數語意「問題數」，來源歸 workspace domain
- .41 追修清單 #12 已處理 SPEC-001 §7 表下註記
- domain-map §3 Workspace 公開面未列健康計數
- 健康計數語意上屬 Diagnostics domain（「什麼算破洞、怎麼計數」），Workspace 僅攜帶數值供 UI 消費

**S-36 查證**：
- .38 裁決 O1：依最近一次開啟時間降冪；目前專案列入；選取後移至頂端
- 清單持久化不需新增介面方法：JSON 序列化後經既有 `writeString`/`readString` 即可
- 新 key `workspace.recentProjects` 首次 `readString` 回 null 即「空清單」初始態，不觸發 schema 版本遞增

### 實驗發現

已驗證事實：
1. S-21 的清除操作需在 `WorkspacePreferencesHandle` 新增 `remove` 方法（介面擴充）
2. S-35 的健康計數 0.1 為假資料，workspace domain 只需在公開面列出「健康計數」作為攜帶項
3. S-36 的清單持久化不需介面擴充（JSON 經既有 readString/writeString），但需在 SPEC-005 描述新 key 結構
4. 清除兩個 key 之間的中間態（path 已移除、版號尚在）在 restore 端語意正確（先判 path == null 即回 WorkspaceUnset）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 逐項定案

#### 接縫一：S-21 取消載入是否清除已存路徑

**承接位置**：SPEC-005 §2.2（回饋點表新增 `remove` 時刻）+ `WorkspaceRepository` 新增 `clearStoredPath()` 公開方法

**依據**：.38 裁決 O1 已定案；SPEC-003 UI 側改法歸 .41 追修 #6；workspace domain 側缺 `remove` 操作

**候選方案**：

| 方案 | 說明 | 利 | 弊 |
|------|------|---|---|
| A：新增 `remove` 至 Handle | 介面新增 `Future<bool> remove(String key)`；adapter 以 `SharedPreferences.remove` 實作 | 語意清晰、與 `writeString` 對稱、回饋點同形 | 所有 fake/spy 需同步 |
| B：以 `writeString` 寫空字串 | `clearStoredPath()` 呼叫 `writeString(_pathKey, '')` | 不改介面 | `restore()` 需改判斷式、語意偏移、新增分支 |

**定案**：方案 A。語意對齊、不影響 restore 既有邏輯。

**改法草稿（檔名＋章節）**：
- `docs/spec/workspace/SPEC-005-workspace-driven-ports.md` §2.2 回饋點表新增 `remove` 結果時刻列
- `lib/workspace/workspace_repository.dart`：`WorkspacePreferencesHandle` 新增 `Future<bool> remove(String key)`；`WorkspaceRepository` 新增 `clearStoredPath()` 依序移除 `_schemaVersionKey` 再移除 `_pathKey`

---

#### 接縫二：S-35 健康徽章計數來源

**承接位置**：`docs/domain-map.md` §3 Workspace 公開面

**依據**：.38 裁決 O1 已定案；.41 追修 #12 已補 SPEC-001 §7 註記；workspace domain 公開面缺「健康計數」落點

**候選方案**：

| 方案 | 說明 | 利 | 弊 |
|------|------|---|---|
| A：公開面補攜帶項 | domain-map §3 Workspace 公開面加「健康計數（攜帶，來源歸 Diagnostics）」 | .41 追修有對應落點 | 0.1 假資料不消費 |
| B：§9 待決事項補一條 | 不改公開面 | 不膨脹公開面 | 無落點，讀者需跳 §9 |

**定案**：方案 A。公開面列攜帶項是 bundle 界定標準做法（同形例：可用性狀態）。

**改法草稿（檔名＋章節）**：
- `docs/domain-map.md` §3 Workspace 行公開面欄加「健康計數（攜帶，來源歸 Diagnostics 整合定案）」
- 本票直接改，不需建 DOC 票

---

#### 接縫三：S-36 最近專案清單持久化

**承接位置**：SPEC-005 新增 §2.4 + `WorkspaceRepository` 新增清單管理方法 + domain-map §3

**依據**：.38 裁決 O1 已定案；SPEC-003 UI 側歸 .41 追修 #23；清單持久化接縫不在 SPEC-005 現有範圍

**候選方案**：

| 方案 | 說明 | 利 | 弊 |
|------|------|---|---|
| A：SPEC-005 新增 §2.4 專節 | 描述 key 結構、JSON schema、回饋點承擔者 | 與 §2.1-§2.3 平行結構 | 增篇幅 |
| B：併入 §2.2 | 在 Preferences 回饋點表加列 | 不增章節 | 混合兩個業務概念 |

**定案**：方案 A。清單持久化是新接縫，應有獨立章節。

**改法草稿（檔名＋章節）**：
- `docs/spec/workspace/SPEC-005-workspace-driven-ports.md` 新增 §2.4「最近專案清單持久化」：key `workspace.recentProjects`（JSON 陣列，每項含 `path` + `lastOpenedAt`）、回饋點承擔者表
- `lib/workspace/workspace_repository.dart`：新增 `loadRecentProjects()` 與 `addRecentProject(String path)` 方法
- `lib/workspace/workspace_types.dart`：新增 `class RecentProject`（`path` + `lastOpenedAt`）
- `docs/domain-map.md` §3 Workspace 行：公開面加「最近專案清單」、內部面加「清單持久化」

---

### Spawn 規劃表

| # | type | 標題 | 合併理由 | scope-blocker |
|---|------|------|---------|---------------|
| SP-1 | DOC | SPEC-005 v1.2：新增 `remove` 回饋點列（S-21）與最近專案清單持久化接縫 §2.4（S-36） | S-21 和 S-36 對 SPEC-005 的改動同屬一次版本更新，拆開增合併衝突風險 | 契約前置：workspace 清除路徑與清單持久化的回饋點承擔者定義 |

S-35 不改 SPEC-005，僅改 domain-map（本票直接改）。

### Spawn 落地確認

SP-1 已落地為 `0.2.0-W1-043`（DOC：SPEC-005 v1.2 新增 remove 回饋點列與清單持久化接縫 §2.4）。

### 自檢結果

| 檢查項 | 結果 | 說明 |
|--------|------|------|
| 三項皆有承接位置與依據 | pass | S-21→SPEC-005 §2.2 + WorkspaceRepository；S-35→domain-map §3 公開面；S-36→SPEC-005 §2.4 + WorkspaceRepository + domain-map §3 |
| 改法草稿含檔名與章節 | pass | 逐項列出目標檔名、章節、新增方法簽章 |
| 候選方案 >= 2 且含利弊 | pass | 三項各列 2 個候選方案 |
| Spawn 規劃已落地 | pass | SP-1 已建為 0.2.0-W1-043（DOC） |
| domain-map 改動與唯一變更理由一致 | pass | 健康計數為攜帶項（來源歸 Diagnostics），不增加變更理由；最近專案清單與清單持久化屬資料夾存取方式範圍 |
| 未越界修改非本票範圍的文件 | pass | 僅改 domain-map.md §3 一格與本票 |

multi_view_status: skipped
reason: 本票為三項已由 .38 WRAP 定案的接縫承接位置分析，裁決方向已定，本票只定「在哪個檔案的哪個章節落地」與介面形態選擇；單視角足以判定，不需多視角交叉驗證

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-23T17:19:01
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-335.38 | chars: 338 -->

### Task Reference
- 0.1.0-W3-335.38 what: 補齊 V4 歸因的 49 項 spec 真缺口：以 WRAP 定案內容並分 spec 檔寫追修草案（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-335.38 why: W3-335.36 歸類 D：49 項 SPEC-001/003/004 皆未描述，照文件實作會各自假設；用戶裁示簽核改由 WRAP 評估（風格偏好除外）。blockedBy .37，因為部分缺口與矛盾裁決相鄰，先定矛盾再補缺口避免互相推翻（來源已完成，結論見 Solution）

### Related Files
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.38.md  # from 0.1.0-W3-335.38（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 49 項逐項：承接層、WRAP 結論（或「屬實作期決定，不入規格」及理由）、追修改法  # from 0.1.0-W3-335.38（來源已完成，結論見 Solution）
- [ref] [x] 牽涉用戶寫程式碼風格偏好者標「風格偏好，交用戶」  # from 0.1.0-W3-335.38（來源已完成，結論見 Solution）
- [ref] [x] 依承接檔分組的追修清單，每組項數與改動章節；本票不改 spec、不建票  # from 0.1.0-W3-335.38（來源已完成，結論見 Solution）
