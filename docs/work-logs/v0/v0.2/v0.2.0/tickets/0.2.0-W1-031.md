---
id: 0.2.0-W1-031
title: 改為 版本啟用冪等化：start 對已 active 版本補齊缺漏副作用，finish 啟用改呼叫同一常式
type: IMP
status: in_progress
version: 0.2.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.2.0-W1-017
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶指出 pubspec 未隨啟用 bump；查證啟用有三條路徑副作用不一
  final_decision: 單一冪等常式，start／finish 共用；以真實 0.2.0 補跑為驗證
  rationale: opinionated-default-design 主張 4；與 W1-017 同型缺陷同一修法方向；不另開 bump 票
who:
  current: thyme-python-developer
  history: {}
what: 版本啟用的三項副作用（todolist status active、worklog 主檔存在、版本檔 pubspec 等於該版本；CHANGELOG 的 In Development 段可選）抽成單一冪等常式 ensure_version_activated(version)：逐項檢查、只補缺的、逐項印出 [OK]/[補]；start 對已 active 版本改走此常式而非拒絕；finish 的 activate_next_planned_version 改呼叫它。以本 consumer 的 0.2.0 為真實驗證：修後執行 start --version 0.2.0 應只補 pubspec（與 CHANGELOG 段），worklog 已存在不動
when: 0.2.0 第一張 gate 實作票派發前（pubspec 須為 0.2.0）
where:
  layer: Application
  files:
  - .claude/skills/version-release/scripts/version_release.py
  - .claude/skills/version-release/tests/test_w3_654_scope_blocker_release_gate.py
  - .claude/skills/version-release/tests/test_ensure_version_activated.py
  - .claude/skills/version-release/SKILL.md
  - .claude/skills/version-release/references/release-workflow-details.md
  - .claude/skills/version-release/CHANGELOG.md
  - pubspec.yaml
  - CHANGELOG.md
why: '0.1.0 發版實測：finish 的 activate_next_planned_version 只翻 todolist status，0.1.1 因此無 worklog 主檔（後由執行 session 補建，否則票的 auto-commit 失敗）；0.2.0 由 PM 手動改 todolist 啟用，pubspec 停在 0.1.0、CHANGELOG 無 0.2.0 段；想以 start 補跑，被「已存在（狀態: active）」拒絕。啟用有三項副作用但只有 start 一條路徑做全套，其他路徑各做一部分，且做一半後無法補齊——與同日 finish 收尾 add 清單缺陷（W1-017）同型：副作用集合與執行路徑不對齊。修法依 opinionated-default-design 主張 4（清理路徑主動掃描）：單一冪等常式，各入口共用。canonical #55。'
how:
  task_type: Implementation
  strategy: '1. 新增 ensure_version_activated(version, dry_run)：三項檢查各自獨立——(a) todolist 該版本 status 非 active → 改 active；(b) worklog 主檔（依 _resolve_worklog_pattern）不存在 → create_worklog_structure；(c) 版本檔（pubspec.yaml／package.json 依 project_type）版號 != version → bump。每項印 [OK] 已就位 或 [補] 已補齊；全部就位時印一行「無缺漏」。CHANGELOG 若無該版本 In Development 段則插入空骨架（第四項，可選但建議做）。


    2. cmd_start_version：Step 2 重複檢查中，既有版本 status 為 active 時不再 FAIL，改呼叫 ensure_version_activated 走補齊路徑；planned/pending 維持原啟動路徑但其內部也改用同一常式。


    3. finish：activate_next_planned_version 找到候選後呼叫 ensure_version_activated，不再只翻 status。dry-run 同步支援。


    4. 測試（E1／E2）：tmp fixture 四種狀態——全缺、只缺 pubspec、只缺 worklog、全就位——各斷言只補缺項且不動已就位者（E1：全就位時 pubspec mtime 不變）；start 對 active 版本不再 FAIL（E2 紅輸入：改回舊碼即翻紅）；finish 啟用後 worklog 與 pubspec 皆就位。


    5. 真實驗證：修後對本 repo 執行 start --version 0.2.0（非 dry-run），Test Results 記錄輸出：todolist [OK]、worklog [OK]、pubspec [補] 0.1.0→0.2.0、CHANGELOG [補]。pubspec.yaml 與 CHANGELOG.md 的改動隨本票 commit。


    6. 文件同步：SKILL.md 2.5.0、release-workflow-details.md〈啟動〉節補冪等語意、CHANGELOG。'
acceptance:
- '[x] ensure_version_activated 對四種缺漏組合各只補缺項，全就位時不改任何檔案（E1）'
- '[x] start 對已 active 版本走補齊路徑不 FAIL（E2：舊碼翻紅）；finish 啟用後 worklog 與版本檔皆就位'
- '[x] 對本 repo 執行 start --version 0.2.0 後 pubspec.yaml version 為 0.2.0、CHANGELOG 有 0.2.0 In Development 段，輸出逐項 [OK]/[補] 記於 Test Results'
- '[ ] SKILL.md、release-workflow-details.md、CHANGELOG 依 skill-cli-sync-check 核對'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-23T14:39:36'
completed_at: null
created: '2026-09-23'
updated: '2026-09-23'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

版本啟用的三項副作用（todolist status active、worklog 主檔存在、版本檔 pubspec 等於該版本；CHANGELOG 的 In Development 段可選）抽成單一冪等常式 ensure_version_activated(version)：逐項檢查、只補缺的、逐項印出 [OK]/[補]；start 對已 active 版本改走此常式而非拒絕；finish 的 activate_next_planned_version 改呼叫它。以本 consumer 的 0.2.0 為真實驗證：修後執行 start --version 0.2.0 應只補 pubspec（與 CHANGELOG 段），worklog 已存在不動

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-23 14:39] compare how with SPEC before implementing：以現行 SKILL.md 與 references 為準，main 上已有 W1-017（finish 差集收尾＋殘留守衛）與 W1-029（check／finish 凍結前置關卡），本票不得回退兩者行為。AC3 的真實執行：先 --dry-run 看 start 會做哪些動作，若含分支建立、tag、push、merge 或 todolist 以外的狀態翻轉，停手寫 NeedsContext 回報，不要實跑；只有補 pubspec／CHANGELOG／worklog 三項時才實跑，實跑在本 worktree 內進行，產物（pubspec.yaml、CHANGELOG.md）與程式碼同一次 ticket track commit。worklog v0.2.0-main.md 已存在，預期不動。新測試放 tests/test_ensure_version_activated.py。E1 四種缺漏組合各一則＋全就位不改檔一則；E2 為舊碼會 FAIL 的 active 版本案例。收尾用 finish；Solution 需含 ### 自檢結果、### Phase 4 結論。｜readiness=1 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.2.0-W1-017 | chars: 572 -->

### Task Reference
- 0.2.0-W1-017 what: finish 的收尾 commit 改以「本次執行前後 git status 差集」為範圍（或至少：exit 前檢查工作區乾淨，不乾淨即非 0 退出並列出殘留）；Step 4 啟用移到 commit 之前（來源已完成，結論見 Solution）

### Rationale Chain
- 0.2.0-W1-017 why: 0.1.0 發版實測：Step 0 migrate 77 張、Step 4 啟用 0.1.1 皆成功，但 Step 3 沿用寫死的 git add docs/todolist.yaml CHANGELOG.md，279 項留在工作區（77 rename、129 張被引用票 ID 改寫、todolist 二次修改），tag 指向搬移前的樹，exit 0 無訊號。PM 事後逐檔 add 補 commit。canonical #55 觀測流 2026-09-23（「finish 2.2.0 Step 3 commit 沿用寫死清單」）。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] finish 後 git status --porcelain 為空（排除執行前已存在者），commit 含前移 rename 與 todolist 啟用（E1 對照）  # from 0.2.0-W1-017（來源已完成，結論見 Solution）
- [ref] [x] exit 前殘留守衛對未追蹤殘留 exit 非 0 並列清單（E2）  # from 0.2.0-W1-017（來源已完成，結論見 Solution）
- [ref] [x] SKILL.md、release-workflow-details.md、CHANGELOG 依 skill-cli-sync-check 核對  # from 0.2.0-W1-017（來源已完成，結論見 Solution）
