---
id: 0.2.0-W1-019
title: 接線 專案切換器「選擇其他資料夾」至 WorkspaceRepository 並處理 ChooseFolderResult 四變體
type: IMP
status: in_progress
version: 0.2.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W1-028
discovered_during: null
scope_blocker: 契約前置：無選擇資料夾接線，17 專案無法被選入 gate
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W1-028 裁決移除 HomePage 時發現唯一有價值的邏輯無正式承接點
  final_decision: 建 0.1.1 IMP 票承接接線，HomePage 實作留 git 歷史
  rationale: 保留死碼等於無 trigger 延後；建票使接線需求可追蹤
who:
  current: parsley-flutter-developer
  history: {}
what: action-switcher-choose-folder 由僅 dismiss 浮層改為呼叫 WorkspaceRepository 選取資料夾，依 ChooseFolderResult 四變體切換專案或以 AppSnackBar 回饋
when: 0.1.0 發版後，資料管線接上真實工作資料夾時
where:
  layer: Presentation
  files:
  - lib/screens/project_switcher/project_switcher_overlay.dart
  - test/unit/screens/project_switcher_test.dart
why: 0.1.0-W1-028 裁決移除 HomePage 時發現：ChooseFolderResult 四變體的 UI 處理只存在於已無引用的 HomePage._chooseFolder，正式路徑（專案切換器）的選擇資料夾按鈕只 dismiss 浮層，查無承接票。0.1 畫面走假資料，不致 0.1.0 整合測試失敗
how:
  task_type: Implementation
  strategy: 先比對 SPEC-001 §7、SPEC-003 §3.7 選擇其他資料夾轉場的現行版 → 參考 0.1.0-W1-028 刪除前 lib/main.dart 的 _chooseFolder 四變體處理（git 歷史）→ 改用 AppSnackBar 而非原生 SnackBar（0.1.0-W3-177 的違規不得帶入）→ 以 fake repository 逐變體測試
acceptance:
- '[x] 選擇其他資料夾呼叫 WorkspaceRepository，ChooseFolderResult 四變體各有對應行為並依 SPEC-003 回饋'
- '[x] 四變體各一則測試，使用 AppSnackBar，flutter test 全綠且 analyze 0 issue'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-23T16:34:37'
completed_at: null
created: '2026-09-23'
updated: '2026-09-23'
protocol_version: '2.0'
chain:
  root: 0.2.0-W1-019
  parent: null
  depth: 0
  sequence:
  - 19
tdd_phase_source: auto
---

# Execution Log

## Task Summary

action-switcher-choose-folder 由僅 dismiss 浮層改為呼叫 WorkspaceRepository 選取資料夾，依 ChooseFolderResult 四變體切換專案或以 AppSnackBar 回饋

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-23 16:32] readiness=0 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-028,0.2.0-W1-020 | chars: 1288 -->

### Task Reference
- 0.1.0-W1-028 what: 裁決 HomePage 及其助手 widget 是移除、或改造為某個佔位頁的內容，並執行 lib/main.dart 的 HomePage 及其助手 widget（來源已完成，結論見 Solution）
- 0.2.0-W1-020 what: 自 app_zh.arb／app_en.arb 刪除 statInProgress、statPendingReview、statArchived、sectionRecentDocuments、changeWorkspaceFolder 並重新產生 l10n；workspaceReady、workspaceUnavailable、workspaceNotRemembered、chooseFolderUnavailableMessage 不在本票刪除，交 0.2.0-W1-019 依接線結果決定

### Rationale Chain
- 0.1.0-W1-028 why: W1-002 把 app 進入點自 HomePage 改為 AppShell 後，HomePage 不再被任何程式碼實例化。實測其剩餘引用：integration_test 一處為解釋錨點移動的註解、lib/main.dart 五處全為它自身的定義（class / constructor / State / createState / pageKey），無任何外部呼叫端。連同 _DocumentList、_StatCard 等助手 widget 約 200 行成為無引用碼。Dart 不對未使用的 public class 發出警告，fvm flutter analyze 實測 0 issues——此死碼沒有任何工具訊號，只能靠人查。（來源已完成，結論見 Solution）
- 0.2.0-W1-020 why: 0.1.0-W1-028 移除 HomePage 後，執行者 grep 查得九個 key 已無程式碼引用且不在 SPEC-003／SPEC-004 key 表；為避免與同批 0.1.0-W3-382 的 arb 改動衝突，未在該票刪除。其中四個 workspace 類 key 對應 ChooseFolderResult 回饋，可能由 0.2.0-W1-019 接線重用

### Related Files
- lib/main.dart  # from 0.1.0-W1-028（來源已完成，結論見 Solution）
- test/unit/app/home_page_workspace_test.dart  # from 0.1.0-W1-028  [路徑已不存在]（來源已完成，結論見 Solution）
- test/unit/components/app_snack_bar_source_contract_test.dart  # from 0.1.0-W1-028（來源已完成，結論見 Solution）
- lib/l10n/app_zh.arb  # from 0.2.0-W1-020
- lib/l10n/app_en.arb  # from 0.2.0-W1-020
- lib/l10n/app_localizations.dart  # from 0.2.0-W1-020
- lib/l10n/app_localizations_zh.dart  # from 0.2.0-W1-020
- lib/l10n/app_localizations_en.dart  # from 0.2.0-W1-020

### Source Acceptance (reference)
- [ref] [x] HomePage 及其助手 widget 的去留已裁決並執行，且 lib/main.dart 無任何無引用的 widget 定義  # from 0.1.0-W1-028（來源已完成，結論見 Solution）
- [ref] [ ] 五個 key 已自兩語系 arb 與生成檔移除，刪除前 grep 無引用  # from 0.2.0-W1-020
- [ref] [ ] flutter test 全綠且 dart analyze 0 issue  # from 0.2.0-W1-020
- [ref] [ ] workspace 類四個 key 的去留已依 0.2.0-W1-019 結果處置或註明保留理由  # from 0.2.0-W1-020
