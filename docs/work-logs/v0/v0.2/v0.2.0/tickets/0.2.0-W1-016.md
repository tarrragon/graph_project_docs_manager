---
id: 0.2.0-W1-016
title: 改為 溢出目標優先最近的開放後繼版本，無開放後繼才算 patch+1／minor+1
type: IMP
status: in_progress
version: 0.2.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-652
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: WRAP：patch 無限鏈的機械根因在溢出規則不看開放後繼
  final_decision: 規則改優先開放後繼；引導對開放 active 直接建議該 active
  rationale: PROP-005 patch 語意 + Linux stable／release train 基本率；D 是 B 結構的機械保證
who:
  current: thyme-python-developer
  history: {}
what: 'suggest_overflow_version 與 _suggest_next_patch 改為：先在 todolist 找版本號大於基準、狀態 planned 或 active、未 scope: frozen 的最小版本，有則路由至該版本；無才依動詞算 patch+1／minor+1。凍結閘門提示與版本歸屬引導同步採用'
when: 0.1.1 凍結後、任何 create 觸發溢出前
where:
  layer: Application
  files:
  - .claude/skills/ticket/ticket_system/lib/version.py
  - .claude/skills/ticket/ticket_system/lib/field_validators.py
  - .claude/skills/ticket/tests/test_create_version_scope_gate.py
  - .claude/skills/ticket/references/create-command.md
  - .claude/skills/ticket/SKILL.md
  - .claude/skills/ticket/CHANGELOG.md
why: '現行溢出目標「非功能→patch+1」是維護支線思維：0.1.1 一凍結，下一張非功能票即算出 0.1.2，patch 無限鏈由規則機械產生。同時 _suggest_next_patch 相對 active 算 patch+1，對開放中的 active（0.2.0）也會建議未註冊的 0.2.1 再被 fallback 拉回——引導對「直接進開放版本」的情境仍是噪音。PROP-005 原意：patch 是該 minor 實作期撞到變體的修正，minor 發布即關閉。WRAP 結論（canonical #55 觀測流 2026-09-23）：溢出優先最近的開放後繼，只有全部後繼都凍結或不存在時才生新 patch。canonical #55。'
how:
  task_type: Implementation
  strategy: '1. lib/version.py 新增 find_open_successor(base_version) -> Optional[str]：讀 todolist versions，篩 版本號 > base、status in (planned, active)、scope != frozen，回傳最小者；無則 None。純函式，todolist 缺席回 None。


    2. suggest_overflow_version(frozen_version, ticket_type, action)：先呼叫 find_open_successor(frozen_version)，命中即回 (該版本, 「路由至最近的開放後繼版本」)；未命中沿用現行動詞分類。


    3. _suggest_next_patch／_suggest_next_major：對根票的版本歸屬引導，若 active 版本本身未凍結，直接建議該 active（不算 patch+1）；若 active 凍結，走 suggest_overflow_version。消除「建議 0.2.1（未註冊）」噪音。


    4. 測試（E1／E2）：tmp todolist fixture 含 [0.1.0 completed, 0.2.0 active open, 0.1.1 active frozen]——凍結 0.1.1 的溢出目標為 0.2.0 而非 0.1.2（E1 對照：把 0.2.0 改 frozen 後結果變 0.1.2）；不帶 --version 的非功能根票 dry-run 落 0.2.0 且無「未註冊」引導；fixture 全凍結時仍算 patch+1（E2 紅輸入：確認舊路徑未被刪）。


    5. 文件同步依 skill-cli-sync-check：create-command.md 溢出目標計算表補「開放後繼優先」列；SKILL.md 2.41.0；CHANGELOG。'
acceptance:
- '[x] 0.1.1 frozen 且 0.2.0 open 時，凍結閘門提示的溢出目標為 0.2.0；把 0.2.0 改 frozen 後變 0.1.2（E1 對照）'
- '[x] 不帶 --version 的非功能根票在 active 未凍結時直接落該 active，無未註冊版本引導（以 dry-run 斷言）'
- '[x] 全部後繼凍結或不存在時仍依動詞算 patch+1／minor+1（E2）'
- '[ ] create-command.md、SKILL.md 版號、CHANGELOG 依 skill-cli-sync-check 核對'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-23T13:14:17'
completed_at: null
created: '2026-09-23'
updated: '2026-09-23'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

suggest_overflow_version 與 _suggest_next_patch 改為：先在 todolist 找版本號大於基準、狀態 planned 或 active、未 scope: frozen 的最小版本，有則路由至該版本；無才依動詞算 patch+1／minor+1。凍結閘門提示與版本歸屬引導同步採用

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-23 13:12] compare how with SPEC before implementing：how 欄凍結於建票時，權威文件（SKILL.md／references／canonical #55 觀測）之後可能已演進，以現行版為準，差異寫入 Problem Analysis。收尾用 finish。Solution 需含 ### 自檢結果 與 ### Phase 4 結論（DOC 票免 Phase 4）。｜readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-652 | chars: 1237 -->

### Task Reference
- 0.1.0-W3-652 what: validate_version_registered 接受 planned 版本；VERSION_NOT_ACTIVE 訊息改寫；退役 _suggest_next_patch 相對最新 completed 計算的誤導建議；--scope-blocker 理由持久化為 frontmatter scope_blocker 欄位供發版側查詢（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-652 why: W3-651 閘門提示的出路「--version 0.1.1（先登記）」是死路：validate_version_registered 只接受 active，planned 回 VERSION_NOT_ACTIVE，連已登記的 0.2.0 都收不了票。WRAP 重審（canonical #55 觀測流 2026-09-23）診斷為一個 status 欄位承載三種語意（執行狀態／收件資格／發版判準）；查證 lifecycle.py 對版本狀態零檢查，執行早已與版本解耦，planned 版本的票今天就能做——PC-121「planned = 要等啟用才能執行」的前提不成立。本票拆開「版本歸屬」與「收件資格」的第一步：讓 planned 版本可收票，並把 blocker 理由存成欄位，為發版側「只以 blocker 阻擋、其餘前移」鋪路。建本票時 --action 放寬 即觸發第 2 項要退役的誤導引導（建議 0.0.4 → VERSION_NOT_REGISTERED），須加 --version 0.1.0 才能建，為活的重現案例。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/ticket/ticket_system/lib/messages.py  # from 0.1.0-W3-652（來源已完成，結論見 Solution）
- .claude/skills/ticket/ticket_system/lib/ticket_builder.py  # from 0.1.0-W3-652（來源已完成，結論見 Solution）
- .claude/skills/ticket/ticket_system/commands/create.py  # from 0.1.0-W3-652（來源已完成，結論見 Solution）
- .claude/skills/ticket/conftest.py  # from 0.1.0-W3-652（來源已完成，結論見 Solution）
- .claude/skills/ticket/tests/test_version_registered.py  # from 0.1.0-W3-652（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] planned 版本以 --version 可建票；completed 版本仍拒（E2：以 tmp fixture 的 completed 條目作紅輸入）  # from 0.1.0-W3-652（來源已完成，結論見 Solution）
- [ref] [x] --scope-blocker 理由寫入 frontmatter scope_blocker 欄位，ticket track full 可見，frontmatter_validator 不報未知欄位，load_ticket 可讀回  # from 0.1.0-W3-652（來源已完成，結論見 Solution）
- [ref] [x] 非功能動詞根票不再印相對最新 completed 計算的未註冊版本引導；對照測試證明改動前後輸出不同（E1）  # from 0.1.0-W3-652（來源已完成，結論見 Solution）
- [ref] [x] create-command.md、field-semantics.md、SKILL.md 版號、CHANGELOG 依 skill-cli-sync-check 逐一核對  # from 0.1.0-W3-652（來源已完成，結論見 Solution）
