---
id: 0.2.0-W1-017
title: 修復 version-release finish 的收尾 commit 未涵蓋 Step 0 前移與 Step 4 啟用的產物
type: IMP
status: completed
version: 0.2.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.2.0-W1-029
- 0.2.0-W1-031
source_ticket: 0.1.0-W3-654
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0 發版實測暴露：新增的副作用未進收尾提交射程
  final_decision: 以執行前後差集定 staged 範圍 + exit 前殘留守衛
  rationale: 寫死清單在副作用集合成長時必然落後；差集是自描述的
who:
  current: thyme-python-developer
  history: {}
what: finish 的收尾 commit 改以「本次執行前後 git status 差集」為範圍（或至少：exit 前檢查工作區乾淨，不乾淨即非 0 退出並列出殘留）；Step 4 啟用移到 commit 之前
when: 0.2.0 發版前必須
where:
  layer: Application
  files:
  - .claude/skills/version-release/scripts/version_release.py
  - .claude/skills/version-release/tests/test_w3_654_scope_blocker_release_gate.py
  - .claude/skills/version-release/SKILL.md
  - .claude/skills/version-release/CHANGELOG.md
  - .claude/skills/version-release/references/release-workflow-details.md
why: '0.1.0 發版實測：Step 0 migrate 77 張、Step 4 啟用 0.1.1 皆成功，但 Step 3 沿用寫死的 git add docs/todolist.yaml CHANGELOG.md，279 項留在工作區（77 rename、129 張被引用票 ID 改寫、todolist 二次修改），tag 指向搬移前的樹，exit 0 無訊號。PM 事後逐檔 add 補 commit。canonical #55 觀測流 2026-09-23（「finish 2.2.0 Step 3 commit 沿用寫死清單」）。'
how:
  task_type: Implementation
  strategy: '1. finish 入口記錄 git status --porcelain 快照 S0；Step 3 commit 前再取 S1，staged 範圍 = S1 − S0 中屬 docs/ 與 CHANGELOG.md 者（逐檔 add，不用 -A／目錄）。todolist 與 CHANGELOG 仍在清單內。


    2. Step 4（activate_next_planned_version）移到 Step 3 commit 之前，使啟用進同一 commit；或 Step 4 後追加第二個 commit。擇一寫明理由。


    3. exit 前守衛：git status --porcelain 過濾 S0 已存在者後若非空，印殘留清單並 exit 非 0（不自動 add，避免吸入他人變更）。


    4. 測試：以 tmp repo fixture 模擬 finish 全流程（migrate 2 張 + 啟用），斷言 commit 後工作區乾淨、commit 含 rename 與 todolist 啟用（E1：改回舊清單即翻紅）；殘留守衛對故意留下的未追蹤檔 exit 非 0（E2）。


    5. 文件同步：SKILL.md 2.3.0、release-workflow-details.md Step 3 描述、CHANGELOG。'
acceptance:
- '[x] finish 後 git status --porcelain 為空（排除執行前已存在者），commit 含前移 rename 與 todolist 啟用（E1 對照）'
- '[x] exit 前殘留守衛對未追蹤殘留 exit 非 0 並列清單（E2）'
- '[x] SKILL.md、release-workflow-details.md、CHANGELOG 依 skill-cli-sync-check 核對'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-23T13:14:36'
completed_at: '2026-09-23T13:26:06'
created: '2026-09-23'
updated: '2026-09-23'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

finish 的收尾 commit 改以「本次執行前後 git status 差集」為範圍（或至少：exit 前檢查工作區乾淨，不乾淨即非 0 退出並列出殘留）；Step 4 啟用移到 commit 之前

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-23 13:12] compare how with SPEC before implementing：how 欄凍結於建票時，權威文件（SKILL.md／references／canonical #55 觀測）之後可能已演進，以現行版為準，差異寫入 Problem Analysis。收尾用 finish。Solution 需含 ### 自檢結果 與 ### Phase 4 結論（DOC 票免 Phase 4）。｜readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 差集基準的實作方式

`main()` 進入 finish/release 分支時，先呼叫新增的 `snapshot_git_status_paths(root)`
取得執行前 `git status --porcelain` 快照 `finish_baseline`（含 rename 兩側路徑）。

- `commit_changes(version, dry_run, baseline=finish_baseline)`：以
  `_diff_new_paths(root, baseline)` 算出「目前狀態相對 baseline 的新增路徑」，
  過濾出 `== "CHANGELOG.md"` 或 `.startswith("docs/")` 者逐檔 `git add`（不用
  `-A`／目錄），取代舊版寫死的 `git add docs/todolist.yaml CHANGELOG.md`。
  未提供 baseline 時退回舊版寫死清單行為（相容既有呼叫點，非 finish/release
  的呼叫路徑目前無其他呼叫點）。
- Step 4（`activate_next_planned_version`）與 Step 3.5（`mark_version_completed`）
  仍在 Step 3 commit 之後才寫入 `todolist.yaml`（維持既有「即使 git 操作失敗
  也執行」邏輯不變），改為在其後新增第二次 `commit_changes` 呼叫（同一
  `finish_baseline`），把這兩步的變更一併提交——採用 how 欄「Step 4 後追加
  第二個 commit」的替代方案，理由：不動既有 Step 3.5/Step 4 順序與其「即使
  git 失敗也執行」的既定邏輯，降低變更風險。
- exit 前新增 `check_residual_after_finish(root, baseline)`：對 baseline 差集
  做最終檢查（不限 docs/ 或 CHANGELOG.md），非空即列出殘留路徑並 `return 1`；
  不自動 `git add`，避免吸入非本次 finish 產生的變更（bash 規則七精神）。

### 自檢結果

- `python3 -m py_compile version_release.py` 通過
- `uv run pytest .claude/skills/version-release/tests/` 全數 160 個測試通過
- E1（`test_e1_diff_based_commit_covers_rename_and_todolist_activation` +
  `test_e1_stale_hardcoded_list_would_leave_rename_uncommitted`）：以真實 tmp
  git repo 模擬 Step 0 rename + todolist 啟用，證明差集涵蓋 rename、且與舊版
  寫死清單對照證明範圍差異；收尾後 `check_residual_after_finish` 回傳空清單
- E2（`test_e2_residual_guard_detects_untracked_non_whitelisted_file` +
  `test_e2_residual_guard_clean_tree_returns_empty`）：非白名單路徑的未追蹤
  殘留被偵測到且未被自動 add（仍為 `??` 狀態）；乾淨樹回傳空清單無誤報
- SKILL.md（2.2.0→2.3.0）、CHANGELOG.md、release-workflow-details.md 已同步
  Step 3 描述（差集範圍 + 第二次收尾提交 + exit 前殘留守衛）

### Phase 4 結論

- 重構需要：否（新增函式 `snapshot_git_status_paths` / `_diff_new_paths` /
  `check_residual_after_finish` 均 <= 20 行，職責單一；`commit_changes` 新增
  baseline 分支但保留舊行為分支以相容既有呼叫點，兩分支各自扁平無巢狀 > 2）
- 技術債務：無新增
- 程式碼品質：A（差集邏輯自描述、測試含正向對照 E1 與紅輸入對照 E2，符合
  test-assertion-design 規則 E1/E2）
- 結論：無需進一步重構

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
指令：`uv run --project .claude/skills/version-release pytest .claude/skills/version-release/tests/`
結果：160 passed（含新增 4 個測試：test_e1_diff_based_commit_covers_rename_and_todolist_activation、
test_e1_stale_hardcoded_list_would_leave_rename_uncommitted、
test_e2_residual_guard_detects_untracked_non_whitelisted_file、
test_e2_residual_guard_clean_tree_returns_empty）
另：`python3 -m py_compile .claude/skills/version-release/scripts/version_release.py` 通過

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-23T13:26:06
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-654 | chars: 735 -->

### Task Reference
- 0.1.0-W3-654 what: version-release check 改為：pending 中帶 scope_blocker 欄位者阻擋，無者列為「發版時前移」清單不阻擋；in_progress 一律阻擋。finish 對前移清單以 ticket migrate 搬至溢出目標版本（品質→patch+1、能力→minor+1，目標未登記則阻擋提示）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-654 why: version_release.py 第 782 行以「版本有 N/M 個未完成的 Ticket」為 error，把「池清空」定為發版判準；池增長恆快於完成（0.1.0：83 張 pending 中 8 張必要），版本結構性無法發佈。WRAP 三軸解耦（canonical #55 觀測流 2026-09-23）的發版側：版本的定義是契約（整合測試）不是池，票是手段；發版時未完成的非 blocker 應前移而非阻擋——Linux rc、release train、Jira release 三者共同做法。W3-652 已把 --scope-blocker 理由持久化為 frontmatter 欄位，本票以該欄位區分「阻擋」與「前移」。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] check 對帶 scope_blocker 的 pending 與所有 in_progress 報 error（E2 紅輸入）；對無 blocker 的 pending 不報 error 並列於前移清單含目標版本（E1 對照）  # from 0.1.0-W3-654（來源已完成，結論見 Solution）
- [ref] [x] finish 對前移清單逐張 ticket migrate；目標版本未登記時整批阻擋不自動登記；任一張失敗即中止不留半搬  # from 0.1.0-W3-654（來源已完成，結論見 Solution）
- [ref] [x] SKILL.md、check／finish reference、CHANGELOG 依 skill-cli-sync-check 逐一核對  # from 0.1.0-W3-654（來源已完成，結論見 Solution）
