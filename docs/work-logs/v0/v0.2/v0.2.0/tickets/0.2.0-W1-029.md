---
id: 0.2.0-W1-029
title: 修復 version-release check 對未凍結版本恆判可發布
type: IMP
status: in_progress
version: 0.2.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.2.0-W1-017
discovered_during: null
scope_blocker: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.2.0 啟用即實測 check 恆過；守衛失效方向為放行
  final_decision: 凍結為發版判定的前置條件
  rationale: blocker 計數只在凍結後有意義；未凍結的 0 是缺席不是清空
who:
  current: thyme-python-developer
  history: {}
what: 'check 的「可發布」判定加前置：目標版本須 scope: frozen；未凍結時印 FAIL「版本未凍結，契約 blocker 在凍結前恆為 0，本判定無鑑別力」並 exit 非 0。finish 同樣拒絕未凍結版本'
when: 0.2.0 發版前必須；與 W1-017 同族
where:
  layer: Application
  files:
  - .claude/skills/version-release/scripts/version_release.py
  - .claude/skills/version-release/tests/test_w3_654_scope_blocker_release_gate.py
  - .claude/skills/version-release/SKILL.md
  - .claude/skills/version-release/CHANGELOG.md
  - .claude/skills/version-release/references/release-workflow-details.md
why: '0.2.0 剛啟用即實測：check --version 0.2.0 回 exit 0「已準備好發布」——scope_blocker 只在凍結後才會存在，凍結前 blocker 恆 0，「blocker 清空」對未凍結版本沒有鑑別力，守衛失效方向為放行。這是三軸解耦落地後的邊界：發版判準依賴凍結，但 check 沒有檢查凍結本身。canonical #55 觀測流 2026-09-23（session 94 observe）。'
how:
  task_type: Implementation
  strategy: '1. check：在 collect_ticket_scope_groups 之前讀 todolist 目標版本條目，scope != frozen → 印 FAIL（含「先 todolist 標 scope: frozen 並對必要票 set-scope-blocker」處置）並列入 errors；不進入 blocker 計算。

    2. finish：同一前置，未凍結即中止於 Step 0 之前。

    3. 測試（E1／E2）：tmp fixture 版本未凍結 → check exit 非 0 且訊息含「未凍結」（E2 紅輸入）；同 fixture 加 scope: frozen → 進入既有判定（E1 對照）；finish 未凍結中止且無 migrate 呼叫。

    4. 文件同步：SKILL.md 2.4.0、release-workflow-details.md Step 1 補前置、CHANGELOG。'
acceptance:
- '[x] check 對未凍結版本 exit 非 0 並印明確處置（E2）；加 scope: frozen 後進入既有判定（E1）'
- '[x] finish 對未凍結版本於 Step 0 前中止，無 migrate 副作用'
- '[x] SKILL.md、release-workflow-details.md、CHANGELOG 依 skill-cli-sync-check 核對'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-23T13:59:26'
completed_at: null
created: '2026-09-23'
updated: '2026-09-23'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

check 的「可發布」判定加前置：目標版本須 scope: frozen；未凍結時印 FAIL「版本未凍結，契約 blocker 在凍結前恆為 0，本判定無鑑別力」並 exit 非 0。finish 同樣拒絕未凍結版本

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-23 13:58] compare how with SPEC before implementing：以現行 SKILL.md、references/release-workflow-details.md 與 canonical #55 最新觀測（check 對未凍結版本恆判可發布）為準，差異寫 Problem Analysis。W1-017 已把 finish 收尾改為 baseline 差集＋殘留守衛（已在 main），本票在其上加前置條件，不得回退其行為。凍結前置 FAIL 必須發生在 Step 0 前移之前（AC2：無 migrate 副作用），E1／E2 各一則測試。收尾用 finish；Solution 需含 ### 自檢結果、### Phase 4 結論。｜readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
新增 `check_version_frozen(version)`（scripts/version_release.py）：讀 `docs/todolist.yaml` 找目標版本 entry，`scope != "frozen"` 或未登記時回傳 FAIL 訊息「版本未凍結，契約 blocker 在凍結前恆為 0，本判定無鑑別力」。

`main()` 接線：
- `check`：`print_config_disclosure` 後、`preflight_check` 前呼叫；未過即印錯並 exit 1，不進入 blocker 計算。
- `finish`：置於 Step 0（`migrate_overflow_tickets`）之前，未凍結即中止，`migrate_overflow_tickets` 不會被呼叫（無 migrate 副作用）。

測試：`tests/test_w3_654_scope_blocker_release_gate.py` 新增 `TestCheckVersionFrozenGate`（4 則）——
- `test_e2_unfrozen_version_fails_with_explicit_message`（E2 紅輸入）
- `test_e1_frozen_version_passes`（E1 對照）
- `test_missing_version_registration_fails`
- `test_finish_aborts_before_migrate_when_unfrozen`（`mock_migrate.assert_not_called()`）

實機驗證：`version_release.py check --version 0.2.0`（本專案 0.2.0 尚未凍結）exit 非 0，印出的 FAIL 訊息與程式碼一致。

文件同步：SKILL.md 新增「發版前置關卡：版本必須已凍結」小節（2.4.0）；references/release-workflow-details.md Step 1 前補前置關卡說明；CHANGELOG.md 新增 2.4.0 條目。

### 自檢結果
- 命名：`check_version_frozen` 動詞開頭、回傳型別與既有 `check_*` 系列一致（`Tuple[bool, List[str]]`）
- 函式長度：約 30 行，含 try/except 讀檔與迴圈比對，無巢狀超過 2 層
- 無新增魔法數字／硬編碼；`"frozen"` 字面量與 todolist.yaml 既有 `scope: "frozen"` 慣例一致
- DRY：沿用既有 `get_project_root`／`normalize_version`／yaml.safe_load 模式，未重複造輪子

### Phase 4 結論
重構需要：否。技術債務：無新增。程式碼品質：A。結論：新函式與既有 `check_*` 系列（如 `check_stale_active_versions`）結構一致，接線點（`main()` 兩處）均為既有分支內插入一次前置呼叫，未改動既有邏輯路徑，無需重構。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
指令：`uv run --with pyyaml --with pytest pytest .claude/skills/version-release/tests/ -q`
結果：164 passed（含新增 4 則：test_e2_unfrozen_version_fails_with_explicit_message、test_e1_frozen_version_passes、test_missing_version_registration_fails、test_finish_aborts_before_migrate_when_unfrozen）。

實機驗證：`uv run .claude/skills/version-release/scripts/version_release.py check --version 0.2.0`
輸出：`[FAIL] 版本 v0.2.0 未凍結，契約 blocker 在凍結前恆為 0，本判定無鑑別力。請先於 todolist.yaml 標 scope: frozen，並對必要票 set-scope-blocker` / `[FAIL] 檢查失敗，請修正上述問題`，exit code 1。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.2.0-W1-017 | chars: 572 -->

### Task Reference
- 0.2.0-W1-017 what: finish 的收尾 commit 改以「本次執行前後 git status 差集」為範圍（或至少：exit 前檢查工作區乾淨，不乾淨即非 0 退出並列出殘留）；Step 4 啟用移到 commit 之前（來源已完成，結論見 Solution）

### Rationale Chain
- 0.2.0-W1-017 why: 0.1.0 發版實測：Step 0 migrate 77 張、Step 4 啟用 0.1.1 皆成功，但 Step 3 沿用寫死的 git add docs/todolist.yaml CHANGELOG.md，279 項留在工作區（77 rename、129 張被引用票 ID 改寫、todolist 二次修改），tag 指向搬移前的樹，exit 0 無訊號。PM 事後逐檔 add 補 commit。canonical #55 觀測流 2026-09-23（「finish 2.2.0 Step 3 commit 沿用寫死清單」）。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] finish 後 git status --porcelain 為空（排除執行前已存在者），commit 含前移 rename 與 todolist 啟用（E1 對照）  # from 0.2.0-W1-017（來源已完成，結論見 Solution）
- [ref] [x] exit 前殘留守衛對未追蹤殘留 exit 非 0 並列清單（E2）  # from 0.2.0-W1-017（來源已完成，結論見 Solution）
- [ref] [x] SKILL.md、release-workflow-details.md、CHANGELOG 依 skill-cli-sync-check 核對  # from 0.2.0-W1-017（來源已完成，結論見 Solution）
