---
id: 0.1.1-W3-507
title: 修正 release 的 status 決定改用 blocker 實際狀態並補對照測試
type: IMP
status: pending
version: 0.1.1
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-506
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-506 分析定位出 release 的 status 寫入點，依 escalation-flow 修法時點分離另開實作票
  final_decision: 開 IMP 票承接 release 判定修正與對照測試，修法形狀以待驗證形態寫入 how.strategy
  rationale: 擴散面判定為第一檔（should_exclude 為 False，但本 consumer 有 pytest 機械訊號會在修法錯誤時翻紅），故修法可寫在票內但須標明未驗證項與停手條件
who:
  current: general-purpose
  history: {}
what: 修正 release 的 status 決定改用 blocker 實際狀態並補對照測試
when: 0.1.0-W3-506 分析完成後，下一次有 blockedBy 非空的票被 release 之前
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/commands/lifecycle.py
  - .claude/skills/ticket/ticket_system/tests/test_release_status_semantics.py
why: 0.1.0-W3-506 實測：release 以 blockedBy 字面非空決定 status，blocker 已 completed 時仍寫 blocked；runqueue 的 status 軸為字面守衛且先於 blockedBy 軸重算，該錯值因此成為唯一權威，票從預設導航面靜默消失。同檔已引入共用 predicate is_fully_unblocked 並在三處使用，release 是唯一未使用者。現有回歸測試的 fixture 從未建立 blocker 票，對此軸零鑑別力。
how:
  task_type: Implementation
  strategy: 修法形狀為待驗證形態（escalation-flow 擴散面第一檔：should_exclude 為 False 但本 consumer 有 pytest 機械訊號）。候選：release 改呼叫 is_fully_unblocked(ticket, ticket_map, include_closed_as_resolved=True) 決定 target_status。未驗證項：(a) release 目前不載入全集 ticket_map，取得全集的成本與既有 file_lock 範圍的互動未量；(b) 是否同時要求 runqueue 的 status 軸重算，屬另一決策不在本票。停手條件：若取得 ticket_map 需擴大 file_lock 範圍，停手回報 PM 改由設計決策先行。測試側必含對照：同批 fixture 建立 blocker 票，各跑 completed 與 pending 兩種 blocker 狀態並斷言 release 產出不同（test-assertion-design E1）。
acceptance:
- '[ ] release 在 blocker 已 completed/closed 時不再寫 blocked，以隔離測試證明（blocker 票實際存在於 fixture）'
- '[ ] 新增對照測試：同批 fixture 下 blocker=completed 與 blocker=pending 兩種輸入產出不同 status，兩者各自斷言內容正確'
- '[ ] 既有 test_release_status_semantics.py 全數仍綠，且 skill 測試套件裸 pytest 全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
created: '2026-09-16'
updated: '2026-09-16'
protocol_version: '2.0'
restored_at: '2026-09-21T17:35:29'
restored_by: thyme-python-developer
restore_reason: 0.1.0-W3-649 判定確定誤關（W3-650 執行）
chain:
  root: 0.1.1-W3-507
  parent: null
  depth: 0
  sequence:
  - 507
---

# Execution Log

## Task Summary

修正 release 的 status 決定改用 blocker 實際狀態並補對照測試

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-506 | chars: 614 -->

### Task Reference
- 0.1.0-W3-506 what: 分析 release 後狀態由 blockedBy 欄位非空決定而非由 blocker 是否完成決定，使票從 runqueue 靜默消失

### Rationale Chain
- 0.1.0-W3-506 why: 實測發現：0.1.0-W3-495 的 blockedBy 為 [0.1.0-W3-494]，該 blocker 已 completed，但 release 後 status 被寫為 blocked，`ticket track runqueue` 完全不列該票（grep 命中 0）。全量掃描 0.1.0 全部票：blocked 共 2 張，其中 1 張的 blocker 已全部完成（即本案）。失效形態是靜默消失——票仍存在、欄位正常、無任何警訊，只是不再出現在可執行清單，佇列讀者無從發現。這與 runqueue 作為「迷失方向時的權威」的定位直接衝突（pm-role 檢查清單要求迷失時跑 runqueue 而非憑記憶推斷）。

### Related Files
- .claude/skills/ticket/ticket_system/  # from 0.1.0-W3-506

### Source Acceptance (reference)
- [ref] [ ] 定位並指名 release 的 status 寫入點與 runqueue 的篩選條件，說明兩者各依據什麼決定 readiness
同型路徑盤點完成：complete 一張票是否重算下游 status、set-blocked-by 是否有同樣缺口，各附實測結果
判定是否成立為框架問題（抽象可攜 + 根源在 .claude/ 通用資產兩條 AND 判準逐條套過），修法另開票不在本票  # from 0.1.0-W3-506
