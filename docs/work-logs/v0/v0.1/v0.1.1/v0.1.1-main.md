<!-- 注意：本文件禁止使用 emoji（交接文件規範） -->

# v0.1.1 版本工作日誌

**版本號**: v0.1.1
**代號**: Runoff
**開始日期**: 2026-09-23
**目標**: 0.1.0 契約下的品質改善；固定容器，範圍凍結
**狀態**: active（`scope: frozen`）

---

## 前情提要

0.1.0（Shell）於 2026-09-23 發版，tag v0.1.0。發版時未完成的票依建票動詞前移：72 張到本版、5 張到 0.2.0；本版另有 7 張是實作期衍生的票，合計 79 張。

本版的定位由 todolist 定義：它是固定容器，不再收新票，主軸是 0.2.0（Gate）。patch 是該 minor 實作期的副產品（PROP-005 原意），minor 發布後其 patch 就不再收新件。本版發版時，剩餘票前移至 0.2.0，不開 0.1.2。

---

## 開發日誌

### 2026-09-23：票池分流（`0.1.1-W2-001`）

動詞分類不看內容，所以前移進來的票需要依內容重判。依三問的順序逐張判定：

| 類別 | 張數 | 處置 |
|------|:---:|------|
| where.files 全在 `.claude/` | 15 | 框架票。附加 observe 至 9 張 canonical issue 後 close（其中 1 張原在 0.2.0） |
| 驗收依賴 0.2.0 契約或真實資料 | 10 | migrate 至 0.2.0（`0.2.0-W1-019`～`028`） |
| 其餘 | 55 | 留在本版 |

清單與每張的判定依據見 `0.1.1-W2-001` 的 Solution。

**本版實際範圍**：42 張。

- **0.1.0 契約下的品質改善：42 張**。這是本版真正要承擔的部分。
- 2026-09-23: 0.1.1-W2-001 完成 -- 分流 0.1.1 的 79 張票：框架票 observe 後關閉、0.2 能力誤分者 migrate、真 0.1 品質改善留下

### 2026-09-23：13 張非品質改善票收束進 domain-map（`0.2.0-W1-030`）

0.1.1-W2-001 分流出的 55 張中，13 張不屬 0.1.0 品質改善——後續 minor 規劃前置 9 張
（`0.1.1-W1-037`、`W3-351`、`W3-352`、`W3-365`、`W3-369`、`W3-376`、`W3-377`、
`W3-378`、`W3-384`）與編輯能力 4 張（`W3-073`、`W3-280`、`W3-281`、`W3-283`）。
其交付物是設計決策而非程式碼，決策發生在對應 minor 規劃時，不發生在任何版本
的執行期，留在 0.1.1 會使其數字含不屬本版契約的票並在每次發版時鏈式前移。

依「ticket 記執行、文件記知識」：13 張各自的問題陳述與既有分析已收進
`docs/domain-map.md` §9 待決清單，每項標明 trigger（綁對應 minor 的 PROP 補記，
編輯能力四張綁 1.0 提案），13 張逐一以 `not_executable_knowledge_captured`
關閉、`resolved_by 0.2.0-W1-030`。`CLAUDE.md` §6 現行待決段同步。

**本版範圍收斂為 42 張**，回到真實的 0.1.0 契約下品質改善範圍。

---

### 2026-09-24：結案不發版

依方法論入口 B 走到第 4 步前發現：本版零交付——完成票只有分流票 `0.1.1-W2-001`、27 張關閉、42 張未動，無任何產品變更；而 `finish` 必打 tag，`v0.1.1` 會落在已含 0.2.0／0.2.1 的 commit 上。用戶裁決結案不發版：42 張以 `ticket migrate` 前移至 0.3.0（前移前核對無撞號）、版本條目標 completed 並註明原因、不打 tag、不 bump 版本檔。不變式核對：主分支 `fvm flutter test` 1561 全綠。缺少「結案」路徑屬框架缺口，已觀測 canonical #16，runbook 補條文另票。

---

## 發版條件

1. **整合測試**：0.1.0 的整合測試仍全數通過（`fvm flutter test integration_test/ -d macos`），單元測試 100% 通過。本版沒有自己的整合測試，只有上層 minor 的整合測試。
2. **無契約 blocker**：`version-release check` 對 0.1.1 沒有帶 scope_blocker 的 pending 票。
3. **剩餘票不擋發版**：沒做完的票在發版時前移至 0.2.0，本版是「做多少算多少」。
4. **不開 0.1.2**：前移目標是開放中的後繼版本 0.2.0。「溢出目標優先選最近的開放後繼版本」這條機械防線由 `0.2.0-W1-016` 實作。

---

## 關鍵決策

| 決策 | 出處 | 內容 |
|------|------|------|
| 版本結構 | 2026-09-23 WRAP（B+D+triage），canonical #55 | 0.2.0 為 active 主軸；0.1.1 為 active 但凍結的固定容器 |
| 票池分流 | `0.1.1-W2-001` | 框架票移交 canonical issue；依賴 0.2.0 契約者 migrate；其餘留下 |

---

*最後更新：2026-09-23*
