---
id: 0.1.0-W3-192
title: 修正 add-spawn-request 的 SR 編號掃描整份 body，把散文提及的編號誤計入
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-155
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-155 撰寫 Solution 時實測命中跳號
  final_decision: IMP 把編號來源限定為 Spawn Requests 章節的實際登記項
  rationale: 掃描邊界比待量性質寬且落差無訊號；不重編既有編號因其已被引用
who:
  current: thyme-python-developer
  history: {}
what: 修正 add-spawn-request 的 SR 編號掃描整份 body，把散文提及的編號誤計入
when: 2026-09-09 0.1.0-W3-155 撰寫 Solution 時實測命中
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/commands/track_acceptance.py
why: '`track_acceptance.py:938-941` 的 SR 自動編號掃描整份 ticket body 找 `SR-\d+` 的最大值，**不區分實際登記項與散文提及**。


  **實測命中**（`0.1.0-W3-155`）：執行者在 Solution 散文中寫「SR-1／SR-2」作為預期編號，隨後實際執行 `add-spawn-request` 時編號變成 SR-3／SR-4，需事後在文中更正說明。


  **這是本專案今日反覆出現的同一族**：掃描的邊界（整份 body）比它想量的性質（已登記的 spawn request 數）**寬**，而落差不會出聲——編號照常配發、格式正確、不報錯，只是跳號。發現它的唯一途徑是有人剛好在同一份文件裡兩處都寫過編號並注意到不一致。


  危害是低的（跳號不影響 resolve，`resolve-spawn-request` 依實際編號操作），但它會讓「Solution 先寫規劃、再逐項登記」這個自然寫法產生無預警的不一致，且該不一致需要人工回頭更正文字。'
how:
  task_type: Implementation
  strategy: '`track_acceptance.py:938-941` 的自動編號以正規表示式掃整份 body 找 `SR-\d+` 最大值 +1。改為只掃 Spawn Requests 章節內的實際登記項（該章節的條目有固定格式 `- **SR-N** (時間戳)`），不掃其他章節與散文。


    **邊界要一起決定**：程式碼區塊內、以及引用他票 SR 編號的情形（如「同 0.1.0-W3-XXX 的 SR-2」）也不應計入。以「章節 + 條目格式」雙重限定即可涵蓋，不需另做程式碼區塊挖除。


    **不要改成從 `SR-1` 重新編號**——既有票的編號已被引用（本票 why 即引用 SR-3/4/5），重編會使既有引用失效。只修「下一個編號取自哪裡」，不動已配發的編號。'
acceptance:
- '[ ] SR 編號來源已限定為 Spawn Requests 章節內符合條目格式的實際登記項，不掃其他章節'
- '[ ] 散文或程式碼區塊中提及 SR-N（含引用他票編號）不再影響下一個配發編號'
- '[ ] 已配發的既有編號未被重編，既有引用仍有效'
- '[ ] 補測：body 他處含 SR-99 時，下一個登記編號仍依實際登記項遞增'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修正 add-spawn-request 的 SR 編號掃描整份 body，把散文提及的編號誤計入

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-155 | chars: 1421 -->

### Task Reference
- 0.1.0-W3-155 what: 查明並修正 ticket create 四條路徑中三條殘留 create lock，該鎖不被檢查卻在事故排查中充當假訊號（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-155 why: `.claude/skills/ticket/ticket_system` 的 create 流程取得 `.git/ticket-create-locks/<版本目錄>.ticket-create.lock` 之後，四條退出路徑中有三條未釋放它。鎖為 0 bytes、無持有行程，且**不會擋住任何後續建票**——它的代價不在阻擋，而在事故排查中作為假訊號。

**四條路徑皆為實測，非推論**：

- argparse 層拒絕（缺 `--action` / `--target`）：**不留鎖**
- checklist 層拒絕（`CHECKLIST_VALIDATION_FAILED`，缺 `acceptance` / `when` / `how_strategy`）：**留鎖**
- 建票成功：**留鎖**
- `--dry-run`（明示不寫入任何檔案）：**留鎖，且刷新既有鎖的 mtime**

由此可定位兩件事：鎖的取得在 argparse 之後、checklist 之前；且早於「是否真的要寫入」的判斷——否則 `--dry-run` 不該動到它。

**為何這條路徑經常被走到**：漏填必填欄是建票的高頻首次錯誤（本次排查中同一人連續命中兩次）。而錯誤訊息完整列出缺失欄位與修正建議，**完全未提及鎖已被取得且未釋放**。

**實際損害是誤導，不是阻擋**（建票後補測，修正建票當下的錯誤推論）：實驗確認鎖存在時 `ticket create` 照常執行、未被擋住。因此本日約 9 分鐘的建票不可用**不是這把鎖造成的**——同時間另有 `main.lock`（41 bytes）與 `HEAD.lock` 擋住 commit 步驟，那才是阻因。建票當下把兩者歸為同一原因，屬未經實測的推論，已於此更正。

真正的代價是它在事故排查中的形態：它看起來是一把鎖、位在 `ticket-create-locks/` 目錄下、命名指向建票，於是被當成阻因處理。本日至少兩個 session 各自移除它並將管道恢復歸因於該移除，而管道從未被它擋住。**一個持續產生卻不被檢查的鎖檔，在並行事故中會穩定地把排查者導向錯誤的根因。**

**待查明**：該鎖是否有任何消費端會讀取它。若無，本缺陷的形態是「產生但無人檢查的假訊號」而非「未釋放的互斥鎖」，修法方向隨之不同——前者可能該移除整個機制，後者該補釋放。相關機制已記錄於 `PC-GPD-013`。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/ticket/ticket_system::read  # from 0.1.0-W3-155（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 四條路徑（argparse 拒絕／checklist 拒絕／建票成功／--dry-run）各自的鎖取得與釋放位置已逐條查明，記錄程式碼位置  # from 0.1.0-W3-155（來源已完成，結論見 Solution）
- [ref] [x] 已查明是否存在讀取該鎖的消費端；若無，判定為假訊號並據此決定移除機制或補釋放，選擇理由記錄於 Solution  # from 0.1.0-W3-155（來源已完成，結論見 Solution）
- [ref] [x] 已判定「殘留鎖檔」的正確處置方向並記錄理由；明寫照字面刪檔會重新引入 TOCTOU（filelock 不刪檔為刻意設計），故修法不得為 release 後刪檔  # from 0.1.0-W3-155（來源已完成，結論見 Solution）
- [ref] [x] --dry-run 不應動到鎖狀態的修法方案已提出並登記 spawn request；實際行為驗收由該 IMP 票承接，不在本唯讀票內  # from 0.1.0-W3-155（來源已完成，結論見 Solution）

