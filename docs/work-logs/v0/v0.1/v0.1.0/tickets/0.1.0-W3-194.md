---
id: 0.1.0-W3-194
title: 新增 acceptance-gate-hook 的 per-section 選填章節模板殘留 WARNING
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-137
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-137 分析判定值得做，偵測基礎設施已就緒，缺口在編排層
  final_decision: IMP 新增 per-section WARNING，不阻擋
  rationale: 選填章節允許不填故不阻擋；涵蓋範圍須隨程式碼落地，否則檢查通過會被讀成全部情形已查
who:
  current: thyme-python-developer
  history: {}
what: 新增 acceptance-gate-hook 的 per-section 選填章節模板殘留 WARNING
when: 0.1.0-W3-137 判定值得做、且偵測基礎設施已就緒之後
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/hooks/acceptance-gate-hook.py
why: '`0.1.0-W3-137` 實測：**35% 的已完成 ticket 含選填章節模板殘留**（IMP 的 Problem Analysis 35%、DOC 的 Problem Analysis 56%、ANA 的 Test Results 50%）。


  **留著樣板比留空更糟**：空白章節一眼可見沒填；留著「（選填：問題如何發現？直接原因是什麼？）」則在版面上與已填內容同形，讀者需逐字讀完才發現裡面沒有資訊。接手者掃過章節標題與段落結構後，會合理認為該處已有內容。


  偵測基礎設施已就緒——`_is_section_empty` 的四層剝除能正確判定現有全部模板形態為空殼，缺口只在編排層沒有呼叫它。實作成本因此落在接線，不在偵測邏輯。


  本票採 WARNING 不阻擋：選填章節本就允許不填，強制攔截會提高 complete 門檻而收益不明。目標是讓填了樣板等於沒填這件事在 complete 當下可見，由執行者自行判斷是刪掉樣板還是補內容。'
how:
  task_type: Implementation
  strategy: '複用既有的 `_is_section_empty`（`0.1.0-W3-137` 分析確認其四層剝除能正確識別現有全部模板形態為空殼），缺口僅在編排層——`check_execution_log_filled` 不查 Problem Analysis、DOC 型整體豁免。


    **WARNING 不阻擋**。選填章節本就允許不填，攔截會讓 complete 更難通過而收益不明；本票的目的是讓「填了樣板等於沒填」這件事可見，不是強制填寫。


    **涵蓋範圍須隨程式碼落地，不只留在分析票**：`0.1.0-W3-137` 的 Solution 已列出七種情形（涵蓋三種／不涵蓋四種）。**該表須寫入程式碼註解或 WARNING 訊息本身**——否則下一個讀者看到「模板殘留檢查通過」會以為全部情形都查過了。這正是 `PC-GPD-009`〈守衛可能只實作規則的一個子集〉的防範點：檢查的名稱取自它想驗證的性質，偵測樣式取自實作，落差沒有機制會提醒。


    本檔為框架資產，經 sync 擴散至全部 consumer 專案，且為 complete 路徑上即時生效的 hook；修改時機依 `PC-GPD-010` 排序。'
acceptance:
- '[ ] acceptance-gate-hook 於 complete 時對選填章節的模板殘留輸出 WARNING，不阻擋'
- '[ ] 複用既有 _is_section_empty，未另建平行的空殼判定邏輯'
- '[ ] 涵蓋範圍明細（0.1.0-W3-137 Solution 的七種情形，涵蓋三種／不涵蓋四種）已寫入程式碼註解或 WARNING 訊息，讀者可知綠燈的範圍'
- '[ ] 既有 check_execution_log_filled 的必填章節檢查行為未變更，原有測試全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

新增 acceptance-gate-hook 的 per-section 選填章節模板殘留 WARNING

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-137 | chars: 808 -->

### Task Reference
- 0.1.0-W3-137 what: 評估 選填章節留存未填樣板的偵測形態與掛載點

### Rationale Chain
- 0.1.0-W3-137 why: `0.1.0-W3-087` 已讓 `dispatch-validate` 規則 4b 擋下 acceptance 的未填樣板，但**選填章節（Problem Analysis 等）留存樣板仍然完全無訊號**。

**實證**（記於 087 的 why 段）：`0.1.0-W3-082` 完成時 Problem Analysis 仍是未動的樣板，而執行代理人回報稱已在該處更正描述。兩者矛盾，但沒有任何機制會發聲——選填章節不受 acceptance-gate 檢查，樣板留存不產生訊號。

**「選填」的語意是「可以不填」，不是「可以留樣板」。**兩者現在無法區分：空白與樣板在 schema 檢查上同樣通過，但對讀者的意義完全相反——空白誠實地表示沒有內容，樣板則看起來像有內容。這與 087 修的 acceptance 樣板是同一個形態（`PC-GPD-008`：看起來像真的驗收條件，只有大括號洩漏），只是載體從逐項清單換成自由文字。

**與 087 的兩項差異，是本票獨立存在的理由**：

1. **檢查點不同**：acceptance 樣板擋在派發前（`dispatch-validate`，CLI）；選填章節樣板若要擋，時機在 complete 前（`acceptance-gate-hook`）。兩者是不同的執行時機與不同的承載體。
2. **判準難度不同**：acceptance 是逐項清單，可與建立端 `DEFAULT_ACCEPTANCE_CRITERIA` 逐字比對；選填章節是自由文字，樣板與真實內容的邊界模糊（部分填寫、改寫後保留樣板句式等中間態），逐字比對不足。

本票要評估的正是第 2 點——**判準是否存在、可靠度如何、誤報成本多少**，而非直接實作。

### Related Files
- .claude/pm-rules/ticket-body-schema.md  # from 0.1.0-W3-137

