---
id: 0.1.0-W3-335.6
title: 修補 元件庫規格範本缺引導與佔位文字技術中立化（G16–G20）
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy: []
relatedTo:
- 0.1.0-W3-335.7
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 修補 元件庫規格範本缺引導與佔位文字技術中立化（G16–G20）
when: 前驅票完成後
where:
  layer: 待定義
  files:
  - .claude/skills/doc/templates/component-library-spec-template.md
why: W3-332 審查與走查確認 agent 依此文件推導元件與測試時被迫猜測或讀出兩種做法；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
how:
  task_type: Documentation
  strategy: 依 chained-examples 規格與 Context Bundle 執行；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
acceptance:
- '[x] 每項補文字約束，引用 W3-332.4 草稿者標明採用或修改理由'
- '[x] 佔位文字中的框架名改為中性說法，框架對應改寫於範本指明的 L2 實作規範處（用戶裁示 2026-09-14）'
- '[x] 狀態矩陣 error 列示範同步策略落在範本三選項內'
- '[x] 回饋契約狀態邊示範與狀態矩陣狀態名一致'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T11:56:00'
completed_at: '2026-09-14T12:03:18'
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修補 元件庫規格範本缺引導與佔位文字技術中立化（G16–G20）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-14 11:55] readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## Context Bundle

### 修補票共同 Context（PM，2026-09-14）

**缺口清單在哪**：本票涵蓋的缺口編號見 `ticket track full 0.1.0-W3-332.13` Solution；每個缺口的位置錨點、兩位讀者的分歧模擬、子票草稿見其來源子票（W3-332.2–.10）Solution。**W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 優先於 .13 草稿**——草稿漏收、誤歸或數字不符處以 P 項為準。

**規格**：`.claude/skills/skill-design-guide/references/chained-examples.md`（三段鏈式、驗證段兩形態、缺範例與缺引導分界）。

**已定案的用戶裁示**：
- 語言專屬 skill 的違規／修正程式碼對照不受「禁程式語法」約束（例外條款由 W3-335.15 寫入規格）
- 元件庫規格範本佔位文字改為技術中立，框架名移到 L2 註記
- version-bootstrap 的 v02-walkthrough 依新規格重寫整篇

**矛盾類票的簽核流程**：先在 Solution 寫「矛盾位置｜兩方原文｜建議以哪方為準｜理由｜改後文字草稿」，**不改檔**，以 `ticket track set-exit-status` 設 needs_context 並停手回報；PM 取得用戶簽核寫回本票後再續做改文。

**通則**：
- 框架檔不得寫本專案識別符（票號、SPEC 編號、畫面名），範例一律去識別化
- 集合大小改引用權威名稱而非寫死數字；錨點用標題不用行號
- 改動 skill 須同步其 CHANGELOG 與 SKILL.md 版號
- 只改本票 where.files；發現範圍外缺口登記 spawn request，不就地修

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
**Completion Time**: 2026-09-14T12:10:00
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

修改檔案：`.claude/skills/doc/templates/component-library-spec-template.md`（唯一 where.files）

### 一、G16-G20 缺引導補齊（引用 W3-332.4 草稿，逐項標明採用/修改理由）

| 編號 | 位置 | W3-332.4 草稿 | 本票處置 | 理由 |
|------|------|--------------|---------|------|
| G16（#5） | §2 狀態綁定模式「最小重繪邊界」 | 「以『單一 store 欄位變化只影響讀取該欄位的元件』為判準，逐元件記錄其讀取的欄位清單」 | 修改採用：判準本體不變，改「store」為「單一資料來源」並補「框架特定訂閱/選擇寫法對應見 L2 框架實作規範」一句 | 草稿完成於 D02（佔位文字技術中立化）用戶裁示之前，原句仍含框架特定詞彙（store 為特定狀態管理框架慣用語），依 D02 裁示中性化並將框架對應導向 L2 |
| G17（#18） | §4 回饋契約引言「時間門檻由 UX 審查結論引用」 | 「UX 審查結論未產出時，時間門檻格填『待 UX 審查』，該元件票不得驗收」 | 採用（原樣語意），補一句「不得由規格撰寫者自行估計數值填入」 | 原句已完整回答缺口，補充句排除「先填估計值、日後再改」的隱性退路，避免時間門檻被規格撰寫者實質取代 UX 審查 |
| G18（#25） | §4 無障礙「狀態變化播報」 | 「資料態狀態改變一律播報；互動瞬態不播報，理由欄固定寫『輸入裝置已提供回饋』」 | 採用（原樣語意），補判準來源指向「見狀態矩陣『來源』欄」 | 播報判準的「資料態／互動瞬態」定義已存在於同元件的狀態矩陣「來源」欄，補指向避免讀者另外發明分類 |
| G19（#32） | §7 豁免清單條件 3「工具白名單」 | 「白名單登記於 {工具名} 的 {設定檔路徑}，本欄填登記鍵」（工具名待 PM 指定） | 修改採用：不指定具體工具名，改為條件式規則——有自動化靜態檢查工具則登記其登記鍵，無此類工具則本表自身即為權威登記 | 草稿要求的工具名屬未定案的 PM 裁決事項，本票 Context Bundle 與 W3-332 Problem Analysis P1-P9 均未指定；為避免引入未決定的跨票依賴，且與既有句「本清單為單一權威來源」自洽，改寫為不需預先指名工具的通用規則 |
| G20（#33） | §8 跨平台命名契約「權威端」 | 「權威端為最先實作該元件的端；並列時由設計交付物所在端擔任」 | 採用（原樣語意，文字微調以銜接原句） | 草稿已完整回答缺口，無修改必要 |

### 二、佔位文字技術中立化（D02，用戶裁示 2026-09-14）

範圍內共 3 處框架特定佔位文字，全數改為中性說法，框架對應改寫至範本已定義的 L2 實作規範層級（§0 層級定位所述「L2 框架實作規範」）：

| 位置 | 原文 | 改後 |
|------|------|------|
| §2 狀態綁定模式「狀態管理方案」 | `{如 Riverpod / Provider / Redux}` | `{狀態管理機制名稱，全元件庫一致；框架 API 對應寫入 L2 框架實作規範，本檔不列}` |
| §4 slot 契約 `{leading}` 接受型別 | `{Widget / Icon}` | `{視覺元件或圖示型別；框架型別名對應見 L2 框架實作規範}` |
| §5 容器元件引言「安全區」 | `SafeArea 或同等機制` | `安全區機制；框架 API 對應見 L2 框架實作規範` |

### 三、既有範例修正（acceptance 3、4）

- 狀態矩陣 error 列「同步策略」原填「回滾提示指向回饋契約 state-change 列」（非狀態矩陣引言定義的三選一之一，屬示範自相矛盾）；改為「樂觀更新後確認 / 等確認才顯示」，落在引言定義的三選一（本地即時／樂觀更新後確認／等確認才顯示）內。
- 回饋契約「對應狀態邊」示範原填 `{如 pending → success}`，`pending`／`success` 皆不在同元件狀態矩陣的狀態集（default／hover／pressed／active/focused／disabled／loading／error／empty）內；改為 `{如 loading → default，狀態名須取自上方狀態矩陣的狀態集}`，並補一句明文規則避免示範本身再度漂移。

### 四、驗證

- `mcp__zhtw-mcp__zhtw` 對全部新增文字掃描：0 errors / 0 warnings（1 條 info 級翻譯腔建議，不影響合規）。
- 全文 grep 確認 Riverpod／Provider／Redux／Widget／SafeArea／Icon 已無殘留（D02 範圍內三處全數處理，無第四處遺漏）。
- 本票僅修改 where.files 指定的單一檔案，未觸碰其他並行 Ticket（W3-335.1/.2/.4/.7 等）範圍。
