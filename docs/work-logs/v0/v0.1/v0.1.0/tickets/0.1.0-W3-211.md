---
id: 0.1.0-W3-211
title: 修正 framework-issue link 命令只認少數派的章節名，對多數 error-pattern 失效
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-181
dispatch_reason: ''
decision_tree_path:
  entry_point: 對 PC-GPD-009 執行 link 時被拒，實查發現工具支援的章節名為少數派
  final_decision: IMP 先判定建立端權威再改，不直接擴充搜尋清單
  rationale: 工具、既有實例、多數檔案三者落點不一致，擴充清單只修表面而不解消不一致
who:
  current: thyme-python-developer
  history: {}
what: 修正 framework-issue link 命令只認少數派的章節名，對多數 error-pattern 失效
when: 2026-09-09 對 PC-GPD-009 執行 link 時失敗
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/link_issue.py
why: '`framework-issue link <pattern> <issue-ref>` 對 `.claude/error-patterns/` 下多數檔案失效：它要求「## 分類資訊」表格，實查 48 檔有該章節、**175 檔用的是「## 基本資訊」**。


  **工具支援的是少數派**，因此正常路徑（建 issue 後把 ref stamp 回本地 error-pattern）在多數情況下會被拒絕，使用者只能手動編輯 frontmatter——而手動路徑不受工具約束，寫在哪裡、格式為何都可能各自不同，正是 `link` 命令本應消除的漂移。


  **三處不一致並存**（實查）：工具寫表格；既有已 link 的實例（`ARCH-APP-002`、`IMP-BAL-006`、`IMP-MON-001`）把 `canonical_issue` 放在 frontmatter；而多數檔案的表格章節名與工具找的那個不同。三者沒有一個是另外兩個的子集。


  本票不預設修法方向——先判定建立端規定 `canonical_issue` 落在哪裡，再決定是工具改對象（表格→frontmatter）還是擴充章節名清單。若權威是 frontmatter，兩種表格名的差異根本無關。'
how:
  task_type: Implementation
  strategy: '`link_issue.py` 現行找「## 分類資訊」表格寫入 `canonical_issue`，找不到即拒絕。實查該目錄下 48 檔用「## 分類資訊」、175 檔用「## 基本資訊」——**工具支援的是少數派**。


    而既有已 link 的實例（`ARCH-APP-002`、`IMP-BAL-006`、`IMP-MON-001`）其 `canonical_issue` 都在 **frontmatter**，不在表格內。三者不一致：工具寫表格、實例寫 frontmatter、多數檔案兩種表格名都不是工具認的那個。


    **先判定何者為權威再改**，不要直接加第二個章節名到工具的搜尋清單：查 error-pattern 的建立端（範本或產生器）規定 `canonical_issue` 落在哪裡。若 frontmatter 為權威，工具應改寫 frontmatter 而非表格，兩種表格名的差異即無關。


    修法後補測至少涵蓋兩種章節名與無表格三種檔案形態。'
acceptance:
- '[ ] 已查明 canonical_issue 的建立端權威落點（frontmatter 或表格），並記錄判定依據'
- '[ ] link 命令對 .claude/error-patterns/ 下的全部既有檔案形態皆可成功寫入'
- '[ ] 既有已 link 的三個實例（ARCH-APP-002／IMP-BAL-006／IMP-MON-001）的既有寫法未被破壞'
- '[ ] 補測涵蓋兩種表格章節名與無表格三種形態'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修正 framework-issue link 命令只認少數派的章節名，對多數 error-pattern 失效

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
