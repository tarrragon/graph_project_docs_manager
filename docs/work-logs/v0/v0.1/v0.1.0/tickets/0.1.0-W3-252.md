---
id: 0.1.0-W3-252
title: 重構 AppSnackBar 測試檔依 domain 拆三檔並抽出 lib 端兩處重複編碼
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-205
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-205
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-205 Phase 4a 判定五項重構，兩項應該級不屬守衛層修復
  final_decision: R4 與 R5 合為獨立票，於 Phase 4b 之後執行
  rationale: 兩項皆非守衛失效故不阻斷 205 收尾；R5 的白名單同步與 R4 的測試檔家族相關，同票處理較不易漏；與 4b 錯開避免同時改同一批檔案
who:
  current: cinnamon-refactor-owl
  history: {}
what: 重構 AppSnackBar 測試檔依 domain 拆三檔並抽出 lib 端兩處重複編碼
when: 0.1.0-W3-205 Phase 4b 完成後（避免與其守衛層修復同時改同一批檔案）
where:
  layer: Presentation
  files:
  - test/unit/components/app_snack_bar_test.dart
  - lib/components/app_snack_bar.dart
  - test/unit/components/app_snack_bar_source_contract_test.dart
why: '`0.1.0-W3-205` Phase 4a 判定的五項重構中，兩項「應該」級不屬守衛層修復，由本票承接（Phase 4b 只做守衛失效的兩項「必須」與相關修正）。


  **R4 — 測試檔體量與 domain 混合**：`test/unit/components/app_snack_bar_test.dart` 現為 **1451 行、承載 4 個 domain**。Phase 2 預估遷移後約 950 行，**實際超出 53%**。`cognitive-load` 明訂單一檔案承載 domain 數 > 2 須拆分。


  **R5 — `lib` 端兩處重複編碼**：`origin → 等級`的對應在 `app_snack_bar.dart` 出現兩處（`:154-157`／`:259`），未抽為單一函式；`show()` 現為 149 行。


  兩項合為一票的理由：皆為「應該」級、皆非守衛失效；且 R5 抽出私有成員後**須同批更新原始碼契約測試的白名單**，與 R4 所在的測試檔家族相關，同票處理較不易漏。'
how:
  task_type: Implementation
  strategy: '**R4：依 domain 拆三檔，切分線取自 Phase 4a 的判定**——`83-322`（版面與時間契約）／`323-777`（日誌投影，`0.1.0-W3-165`＋`176`）／`778-1451`（截斷裁決，G-B＋G-C）。行號為審查當時值，以群組標題語意定位不依行號硬套。


    **依 domain 不依票號**：G-B 與 G-C 同屬「截斷裁決」一個 domain，**按票號切會切錯**。各群組已自裝 log spy，屬機械搬移。


    **R5**：抽 `_levelForOrigin` 消除 `:154-157`／`:259` 兩處 `origin → 等級` 編碼；視情況抽 `_buildContent` 縮短 `show()`。**抽出的私有成員須同批加入 `app_snack_bar_source_contract_test.dart` 的白名單**，否則該守衛紅燈。


    **行為不變是硬要求**：完成後跑全套件，須維持 Phase 4b 落地後的通過數與 0 skipped；`fvm flutter analyze` 須 no issues。任一項不成立即回退該項並回報。


    **不得順手處置的兩件事**：D5 的 warning 誤判（另票）、Phase 4b 範疇內的五項（D1／R1／R2／R3／D5 前半）。'
acceptance:
- '[ ] 測試檔依 domain 拆為三檔，切分依群組語意非票號，G-B 與 G-C 未被切開'
- '[ ] lib 端 origin → 等級 的編碼收斂為單一函式，兩處呼叫皆改走它'
- '[ ] 抽出的私有成員已加入原始碼契約測試白名單，該守衛未紅燈'
- '[ ] 全套件通過數不低於 Phase 4b 落地後的基線且 0 skipped，analyze no issues'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

重構 AppSnackBar 測試檔依 domain 拆三檔並抽出 lib 端兩處重複編碼

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: cinnamon-refactor-owl
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-205 | chars: 1709 -->

### Task Reference
- 0.1.0-W3-205 what: 修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別
- 0.1.0-W3-205 what: 修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別

### Rationale Chain
- 0.1.0-W3-205 why: `AppSnackBar.show` 無條件呼叫 `hideCurrentSnackBar()`，因此**任何一則新訊息都會截斷正在顯示的那一則，不分級別**——一則 L3 完成通知會殺掉正在顯示的 L2 失敗訊息，違反 `0.1.0-W3-184` 截斷規則第三條（低級別不得截斷任何東西）。

**且干擾是跨路徑且不對稱的。** `hideCurrentSnackBar()` 作用於 `_snackBars.first`，**不分那一則是誰放進去的**；而 `lib/main.dart:219` 直接呼叫 `ScaffoldMessenger.of(context).showSnackBar(...)` 繞過元件層，走排隊語意。結果是元件路徑殺得掉原生路徑的訊息、反之不行，且被殺的那則**零日誌**（不經過元件層，`0.1.0-W3-165` 的四個事件對它全不適用）。

**使用者可觀察到的徵狀**：訊息閃一下就不見了，而沒有任何紀錄能回答哪一則被殺、被誰殺。
- 0.1.0-W3-205 why: `AppSnackBar.show` 無條件呼叫 `hideCurrentSnackBar()`，因此**任何一則新訊息都會截斷正在顯示的那一則，不分級別**——一則 L3 完成通知會殺掉正在顯示的 L2 失敗訊息，違反 `0.1.0-W3-184` 截斷規則第三條（低級別不得截斷任何東西）。

**且干擾是跨路徑且不對稱的。** `hideCurrentSnackBar()` 作用於 `_snackBars.first`，**不分那一則是誰放進去的**；而 `lib/main.dart:219` 直接呼叫 `ScaffoldMessenger.of(context).showSnackBar(...)` 繞過元件層，走排隊語意。結果是元件路徑殺得掉原生路徑的訊息、反之不行，且被殺的那則**零日誌**（不經過元件層，`0.1.0-W3-165` 的四個事件對它全不適用）。

**使用者可觀察到的徵狀**：訊息閃一下就不見了，而沒有任何紀錄能回答哪一則被殺、被誰殺。

### Related Files
- lib/main.dart  # from 0.1.0-W3-205
- lib/app/attention_level.dart  # from 0.1.0-W3-205
- lib/screens/gap_report/gap_report_screen.dart  # from 0.1.0-W3-205
- lib/screens/gap_report/scan_notification_controller.dart  # from 0.1.0-W3-205
- test/unit/app/home_page_workspace_test.dart  # from 0.1.0-W3-205

### Source Acceptance (reference)
- [ref] [ ] lib/main.dart 不再直接呼叫 ScaffoldMessenger.showSnackBar，全部提示經 AppSnackBar  # from 0.1.0-W3-205
- [ref] [ ] 被截斷時日誌可回答被截斷的是哪一則（識別資訊而非僅事件類型）  # from 0.1.0-W3-205
- [ref] [ ] 級別取值以標題文字引用 SPEC-003，元件內未另立一套級別定義  # from 0.1.0-W3-205
- [ref] [ ] 截斷條件綁在級別上：低級別不得 hide、同級別截斷並留痕、僅高級別可截斷，三者各有測試  # from 0.1.0-W3-205
- [ref] [ ] 新建 lib/app/attention_level.dart 承載級別列舉，且 AppSnackBar 與未來的仲裁器皆 import 該檔而非各自定義  # from 0.1.0-W3-205
  ... (truncated, see source ticket)
- [ref] [ ] lib/main.dart 不再直接呼叫 ScaffoldMessenger.showSnackBar，全部提示經 AppSnackBar  # from 0.1.0-W3-205
- [ref] [ ] 被截斷時日誌可回答被截斷的是哪一則（識別資訊而非僅事件類型）  # from 0.1.0-W3-205
- [ref] [ ] 級別取值以標題文字引用 SPEC-003，元件內未另立一套級別定義  # from 0.1.0-W3-205
- [ref] [ ] 截斷條件綁在級別上：低級別不得 hide、同級別截斷並留痕、僅高級別可截斷，三者各有測試  # from 0.1.0-W3-205
- [ref] [ ] 新建 lib/app/attention_level.dart 承載級別列舉，且 AppSnackBar 與未來的仲裁器皆 import 該檔而非各自定義  # from 0.1.0-W3-205
  ... (truncated, see source ticket)

