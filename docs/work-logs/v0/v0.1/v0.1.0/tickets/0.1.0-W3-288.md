---
id: 0.1.0-W3-288
title: 規劃 hook_logging project_root 契約的修法，供 issue 97 統整後才派實作
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-285
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-285 判定升級成立並 observe 至 tarrragon/claude#97；用戶裁示發現當下建票不等於當下決定修法
  final_decision: 在 issue 統整出修法之前不派任何實作；本票承接統整與方案規劃，0.1.0-W3-284 改為 blockedBy 本票
  rationale: 284 的修法形狀（四處接線 + 單一 logger 工廠）決定於 W3-273 的 Phase 4 發現當下，未經分析驗證；285 事後量得漏接率 163/164，該修法涵蓋 4 個呼叫點且抽象層級落在 consumer 側
who:
  current: framework-issue-curator
  history: {}
what: 規劃 hook_logging project_root 契約的修法，供 issue 97 統整後才派實作
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/lib/hook_logging.py
why: '**本票存在的理由是時序，不是內容**。`0.1.0-W3-284` 的修法形狀（四處接線 + 收斂為單一 logger 工廠）決定於 `0.1.0-W3-273` 的 Phase 4 發現當下；`0.1.0-W3-285` 事後實測的漏接率是 **163/164**（`setup_hook_logging`／`init_hook_logging` 的非測試呼叫點 164 個，顯式傳 `project_root` 者 1 個）。**修法被鎖定的時點早於知道問題規模的時點。**


  **四處接線修的是 163 之中的 4**，且工廠這個抽象落在 consumer 側（`.claude/skills/ticket/`），而 285 判定的根源在 `.claude/lib/hook_logging.py` 的契約——opt-in kwarg、預設即舊行為，此設計本身落在它要修的無訊號區內。**在 consumer 側加一層工廠，不改變其他 consumer 專案的同型漏接，也不改變下一個新增呼叫點的預設行為。**


  **不做則後果**：實作先於統整落地，則(1)四處接線與工廠需在上游契約變更後重做，其測試與抵達驗證亦重做一次；(2)`#97` 上會出現一個已實作但未被方案涵蓋的修法，後續讀者無從判斷它是方案的一部分還是繞道；(3)真正的癥結（預設值的方向）不會因為修好四處而變得更清楚。


  **為何 P1**：它阻擋 `0.1.0-W3-284`，且 `#97` 每多累積一則未被統整的觀測，統整成本就升高一次。'
how:
  task_type: Analysis
  strategy: '**先讀既有結論，不重新推導**：


    1. 通讀 `tarrragon/claude#97` 全部區段與觀測（含 `0.1.0-W3-285` 附加的 comment 5628938690），列出**已有結論**與**尚未回答的問題**兩張清單。已有結論不重做。

    2. **提出至少兩個修法候選**，各附利與弊，且**各自明示涵蓋的呼叫點比例**（以實際掃描數為準，非估計）。候選應至少涵蓋：契約層改預設或改必填、共用工廠置於 `lib/` 層、維持 opt-in 但加靜態守衛。

    3. **選定方案須寫明判準**，並**標出該結論的承重點**——哪一個事實若不成立，整個選擇就崩（`0.1.0-W3-285` 的自檢示範了此形態）。

    4. 方案寫入 `#97` 的自有區段（`update` 既有自有區段或 `observe`）。**實作票的範圍由該區段界定**，不由本票或 284 界定。

    5. **對 `0.1.0-W3-284` 的既有修法做明確處置**：保留、改寫、或否決，三者擇一並附理由寫入 Solution。不得略過不談。


    **本票不改任何程式碼**，不改 `.claude/` 通用資產本體，不跑 `sync-push`／`skill-sync push`；免徵詢授權的兩項生效條件同既往。'
acceptance:
- '[ ] #97 既有區段與觀測已通讀，已有結論與未答問題各成一張清單'
- '[ ] 至少兩個修法候選，各附利與弊，且各自涵蓋的呼叫點比例以實際掃描數記錄'
- '[ ] 選定方案的判準已寫明，且已標出該結論的承重點'
- '[ ] 方案已寫入 #97 的自有區段，實作範圍由該區段界定'
- '[ ] 0.1.0-W3-284 的既有修法已明確處置（保留／改寫／否決三擇一），理由寫入 Solution'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T11:38:55'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

規劃 hook_logging project_root 契約的修法，供 issue 97 統整後才派實作

---

## Problem Analysis
### 問題根因

`lib/hook_logging.py` 的日誌 root 與呼叫端的狀態 root 由兩個彼此獨立、優先序相反的解析器各自決定；對齊手段是呼叫端逐點顯式傳一個**預設即舊行為**的 kwarg。漏接無任何訊號：日誌照樣寫、內容照樣正確，只是落在查詢者不讀的路徑上。

本票不是重新判定該形態（`0.1.0-W3-285` 已判定升級成立並 observe 至 `tarrragon/claude#97`），而是**在派任何實作之前先統整修法**。

### 影響範圍

已驗證的事實（本票實測，`.claude/` 全樹、排除測試檔與定義處）：

- 日誌入口呼叫行 320（`setup_hook_logging` 165 + `run_hook_safely` 153 + `mark_hook_entry` 2），分佈 159 檔（`hooks/` 118、`skills/ticket/` 23、其他 skills 16、其餘 2）。
- 顯式傳 `project_root` 者 **4 行 / 3 檔**（更正 `0.1.0-W3-285` 記錄的「1」，成因見下節）。
- `save_check_log` 與 `get_hook_log_dir` **完全沒有 root 參數**，非測試呼叫點 11 處——契約本身有洞，這 11 處連「記得傳」都做不到。
- 錯配不只一類：類一（狀態走 `get_ticket_state_root`）真陽 6 檔（2 已修 / 4 未修）；類二（狀態走 `lib/git_utils.py::get_project_root`，環境變數優先，與日誌走的 `hook_base` worktree 優先方向相反）12 檔，既有討論未涵蓋。

仍未驗證的假設：159 檔中 66 檔的 root 來源未判定；是否存在刻意要日誌留在 worktree 的呼叫端；`CLAUDE_PROJECT_DIR` 在 linked worktree 內實際指向何處。

### 相關 Error Pattern

- `PC-162`（接手者須獨立重新驗證數量／範圍）：本票即為其實例——`0.1.0-W3-285` 的「1/164」經複驗更正為「4/320」。
- `test-assertion-design-rules` E2（守衛的測試必須含正向對照輸入）：選定方案含靜態守衛，該規則直接約束其驗收。
- `bash-tool-usage-rules` 規則二（輸出過濾方向）：本票的計數錯誤來自單行 grep 對跨行呼叫無鑑別力，同族。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-285 | chars: 897 -->

### Task Reference
- 0.1.0-W3-285 what: 判定 hook_logging 的 project_root 漏接形態是否升級為 framework issue（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-285 why: 平行 session 於 `0.1.0-W3-273` 收尾時交還的一項判斷，**目前只存在於一則跨 session 訊息——訊息不是載體**。

**已量到的成本**：`.claude/lib/hook_logging.py` 的 `project_root` 參數 docstring 早已逐字寫明這個失效模式，該參數即為此而加；兩個呼叫點（`track_dashboard.py::_get_handoff_gc_logger` / `track_dispatch_check.py::_get_prune_logger`）照樣漏接，造成 linked worktree 內清理動作寫主倉庫、日誌落 worktree 自身的 hook-logs 目錄，測試在隔離目錄找不到檔案。Phase 4 第二視角另查出四個 hook 今日處於同型錯配（經 `handoff_utils` / `worklog_appender` 傳遞），四處皆無測試斷言日誌落點、無紅燈無 stderr 差異，可無限期潛伏。

**為何需要獨立判定**：`0.1.0-W3-284` 承接的是本專案 ticket skill 四個呼叫點的接線，屬具體修復；本票判定的是「參數存在且 docstring 已寫明失效模式，呼叫點仍漏接」這個形態是否構成框架問題——兩者不同層，修掉 284 不會回答本題，兩票之間**無前置依賴**。

**不做則後果**：該形態的根源若確在會 sync 至所有 consumer 的共用資產，本地修會被下次 sync-pull 以上游舊版覆蓋且無 canonical 記錄；其他 consumer 專案的同型漏接無從被發現。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] dedup 已跑，關係判定（重複／切分／引用）有記錄  # from 0.1.0-W3-285（來源已完成，結論見 Solution）
- [ref] [x] 兩條 AND 判準已逐條套用並在票面留下記錄，非憑印象  # from 0.1.0-W3-285（來源已完成，結論見 Solution）
- [ref] [x] 升級與否的判定已落地：升級者票面附 issue 連結，不升級者 Solution 逐條附兩條 AND 判準不成立的理由  # from 0.1.0-W3-285（來源已完成，結論見 Solution）
