---
id: 0.1.0-W3-105
title: 設計 服務類型與等待期政策對照：回饋形式、輸入阻擋範圍、重複觸發防護
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-102
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 等待期的行為契約缺整體設計
  final_decision: ANA 設計服務類型分類與各類型的等待期政策，再依結論落地 spec 與測試
  rationale: 回饋形式、輸入阻擋、重複觸發防護三者都以服務類型為自變數，分開設計會得出互相矛盾的規則
who:
  current: saffron-system-analyst
  history: {}
what: 設計 服務類型與等待期政策對照：回饋形式、輸入阻擋範圍、重複觸發防護
when: 用戶於 2026-09-08 三層歸屬裁示後補充：不同類型的服務應提供哪些回饋提示、等待期是否阻擋輸入、按鈕防抖歸屬
where:
  layer: Domain
  files:
  - docs/spec/ui/SPEC-003-interaction-response.md::read
  - docs/spec/ui/SPEC-004-component-library.md::read
  - .claude/methodologies/component-library-bidirectional-constraint-methodology.md::read
  - lib/components::read
why: '現行回饋契約回答的是「怎麼告知使用者」，不回答「按下之後到結果之前這段時間，系統怎麼保護自己與使用者」。用戶於三層歸屬裁示後補充三項（2026-09-08），三項都以**服務類型**為自變數：


  1. **不同類型的服務應提供哪些回饋提示** —— 並明指這是**測試驗收的一個指標**，故須可機械檢查，不能只是敘述性建議。

  2. **等待期間是否需要阻擋使用者的其他操作、屏蔽畫面輸入** —— 這與提示是兩件事：提示是「告知」，阻擋是「防止」。目前規格兩者都沒有依服務類型的規則。

  3. **按鈕類互動元件的防抖** —— 且明確裁示**防抖由服務提供，不是元件提供**。


  第 3 點的歸屬與三層模型一致，且推出一條重要不變式：**元件在每一次按下都必須發出第一層回饋，即使服務因防抖而丟棄該次呼叫**。若防抖做在元件層，防抖窗口內的按下會沒有 ripple，使用者看到的是一個沒反應的按鈕——正是 `ARCH-GPD-001` 的形態，只是成因從「服務不可用」換成「服務刻意不執行」。元件永遠回饋，服務決定要不要真的執行。


  現況：SPEC-003 §2.1 只有 `Motion.searchDebounce`（300 ms，搜尋輸入用），沒有按鈕類的防抖規則，也沒有規定它實作在哪一層；「阻擋」一詞在 SPEC-003 全文只用於「阻擋狀態元件」（畫面狀態的一種），與「等待期阻擋輸入」不是同一件事，目前後者無任何條文。'
how:
  task_type: Analysis
  strategy: '先建立**服務類型分類**，再逐類型定政策——三項政策共用同一組自變數，分開設計會得出互相矛盾的規則。


    **第一步：服務類型分類。** 依「使用者感知的等待長度」與「失敗形態」兩軸切，不依技術實作切（同步／非同步是實作細節，使用者感知不到）。候選維度：立即完成（感知不到等待）／短時（可能需要等待指示）／長時（必然需要等待指示與取消）／跨程序或平台（可能永不回應）／串流或漸進（結果分批到達）。分類數不預設，以「每一類的政策確實不同」為切分依據——政策相同的兩類應合併。


    **第二步：逐類型定三項政策。**


    - **回饋提示形式**：第二層（服務受理）與第三層（event 效果）各該給什麼。須對應到既有元件（既有元件庫已有的就指名，沒有的標為缺件交 PM 核定，不自行設計新元件）。

    - **輸入阻擋範圍**：三選一並說明判準——不阻擋／只禁用觸發來源／阻擋整個畫面。長時操作阻擋整畫面時必須同時提供取消（否則使用者被困住），此約束須寫進條文。

    - **重複觸發防護**：防抖／節流／冪等／禁用觸發來源，擇一或組合。**承擔者一律是服務**（用戶裁示），元件不實作防抖。


    **第三步：不變式。** 明寫「元件在每一次互動都必須發出第一層回饋，即使服務因防抖、節流或冪等而丟棄該次呼叫」。這條是防止防抖被誤放到元件層的守門條件。


    **第四步：測試驗收指標。** 每一類型的政策各給可機械檢查的驗收形式——用戶明指這是測試驗收指標。與 `0.1.0-W3-100` 的角色化替身結論對接（該票已完成，第二層有 acknowledged／processing／neverAcknowledges 三角色），說明每項政策用哪個角色驗。時間相關的驗收須遵守 `test-assertion-design-rules` 規則 D1（走假時鐘，不量測真實耗時）。


    **現況盤點**：`Motion.searchDebounce` 現在實作在哪一層？若在元件層，即為本裁示的既有違例，記入結論供落地票處理。


    產出為分類表、政策對照表、不變式與驗收指標，不改任何檔案（where.files 全為 `::read`）。落地至 SPEC-003／SPEC-004 與測試設計文件依結論以 `add-spawn-request` 登記，complete 前逐項 resolve 為終態。'
acceptance:
- '[ ] 服務類型分類以使用者感知的等待長度與失敗形態為軸，每類附「政策確實不同」的切分理由'
- '[ ] 逐類型定回饋提示形式、輸入阻擋範圍、重複觸發防護三項政策；阻擋整畫面者必須同時提供取消'
- '[ ] 不變式已明寫：元件每一次互動都必須發出第一層回饋，即使服務因防抖／節流／冪等丟棄該次呼叫'
- '[ ] 每項政策附可機械檢查的驗收形式，並指明用 0.1.0-W3-100 的哪個替身角色驗；時間相關者遵守規則 D1'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

設計 服務類型與等待期政策對照：回饋形式、輸入阻擋範圍、重複觸發防護

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-08 16:03] readiness=2 validate=0


## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-102 | chars: 1096 -->

### Task Reference
- 0.1.0-W3-102 what: 修正 回饋責任的三層歸屬：服務受理層的承擔者是服務不是元件

### Rationale Chain
- 0.1.0-W3-102 why: `0.1.0-W3-098` 落地的〈掛載點〉條文寫「按下確認與等待指示的觸發點是互動事件本身與『已送出、尚未回應』這個狀態」——把**兩層都歸給元件**。用戶隨後的裁示（2026-09-08）給出三層，且中間那層的承擔者是服務不是元件：

| 層 | 內容 | 承擔者 |
|----|------|--------|
| 一、互動元件層 | 互動當下的回饋：震動／音效／變形／變色／光環 | **元件自身**，與背後是否掛服務無關 |
| 二、服務受理層 | command 發送 event 給服務後，服務自己的處理回饋：toast、loading 動畫 | **服務** |
| 三、event 效果層 | 服務處理完之後才觸發 event 應該觸發的效果，無論成功失敗或其他 | 事件處理路徑 |

現行條文與此的差距有兩處：

1. **歸屬錯置**：「等待指示」（第二層）被寫成元件的義務。實際上 command 送出之後，是**服務**要對「我收到了、正在處理」負責。把它歸給元件，會讓服務側完全沒有回饋義務——服務可以什麼都不做而條文仍被滿足。

2. **第一層的無條件性未寫明**：條文的判準問句「把服務拔掉還看得到反應嗎」隱含了第一層必須存在，但沒有明說**每個可互動元件都必須有互動當下的回饋，沒有例外**。PM 在派發存量盤點票時因此把「互動不觸發跨層呼叫」誤讀為「判準自動成立」——純本地狀態切換的元件若按下時無任何表現，只有狀態改變後的結果，一樣是缺陷，而該誤讀在現行條文下讀得通。

`ARCH-GPD-001` 同樣只寫了兩層（回饋 vs 服務回應），未涵蓋服務受理層的獨立歸屬，須一併補。

### Related Files
- .claude/methodologies/component-library-bidirectional-constraint-methodology.md  # from 0.1.0-W3-102
- .claude/error-patterns/architecture/ARCH-GPD-001-feedback-coupled-to-service-success-path.md  # from 0.1.0-W3-102

### Source Acceptance (reference)
- [ref] [ ] 〈掛載點〉子節含三層歸屬表，承擔者欄明寫服務受理層屬服務而非元件  # from 0.1.0-W3-102
- [ref] [ ] 明寫每個可互動元件都必須有互動當下的回饋，沒有例外；「純本地狀態切換故無需回饋」列為不接受的填法  # from 0.1.0-W3-102
- [ref] [ ] ARCH-GPD-001 的解決方案段補三層歸屬，補齊服務受理層的獨立義務  # from 0.1.0-W3-102
- [ref] [ ] 未更動 0.1.0-W3-098 落地的三個子節標題字串（0.1.0-W3-101 以其為跨檔錨點）  # from 0.1.0-W3-102
