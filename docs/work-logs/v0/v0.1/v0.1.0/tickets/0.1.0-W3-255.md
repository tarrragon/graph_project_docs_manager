---
id: 0.1.0-W3-255
title: 新增 ticket track 的血緣遞移走訪子命令（給定票遞迴列出全部後裔並標出未終結者）
type: IMP
status: closed
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.1.0-W3-236
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 同一 spawn request 在兩個 session 各自長出一張票，撞題於派發前掃描才發現
  final_decision: 建 CLI 子命令，不只靠人工約定
  rationale: 原判斷為「先讓約定跑、漏第二次再談」，經並行 session 實查後重估：缺的不是載體而是走訪，成本遠低於原估
who:
  current: thyme-python-developer
  history: {}
what: 新增 ticket track 的血緣遞移走訪子命令（給定票遞迴列出全部後裔並標出未終結者）
when: 建規格類票之前的同源查重；本票不阻塞任何票
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/commands/track.py
why: "實證：`0.1.0-W3-210` 與 `0.1.0-W3-236` 同源於 `0.1.0-W3-167` SR-3，兩個 session 各自建票、都合法、都正確引用來源，但建立當下誰也不知道對方存在。撞題是在派發前掃 where.files 時才發現，比建票晚了一天。\n\n**缺的不是欄位，是走訪。** 世界平面查證：167 的 spawned_tickets 同時列著 169 與 184（共 11 張），兩條鏈每一跳的 source_ticket 都完整——\n\n  210 <- 169 <- 167\n  236 <- 204 <- 184 <- 167\n\n從 167 往下遞移展開三層，兩條分支都在裡面，沒有任何一跳缺資料。spawned_tickets 單看只答「我生了誰」，遞移展開就答「這個來源總共長出了什麼」，而後者正是同源查重要問的問題。\n\n**替代查法已排除**：查「SR 的反向引用」不可行——SR-N 是票內序號，每張票都從 SR-1 重新編，不是全域識別符，單獨 grep 無鑑別力；要查得指定 `<ticket-id> SR-N`，而那要求先知道是哪張票的 SR，又回到走訪血緣鏈。兩條路殊途同歸，遞移走訪是唯一那條。\n\n**掃 where.files 擋不住這一類**：本次恰好兩張票共用 SPEC-003 才被掃到；同來源而落在不同檔案的兩張票，掃 where.files 一樣會漏。"
how:
  task_type: Implementation
  strategy: 以既有 spawned_tickets 欄位遞迴展開，不新增資料結構；輸出標出 pending／in_progress 的後裔，並偵測環
acceptance:
- '[ ] 子命令接受一個 ticket id，遞迴輸出其全部後裔（不限層數）'
- '[ ] 輸出標出每個後裔的 status，pending 與 in_progress 者可一眼辨識'
- '[ ] 遞迴具環偵測，spawned_tickets 若構成環不進入無窮迴圈'
- '[ ] 後裔票的 md 缺失或 spawned_tickets 欄位缺失時，該節點標為無法展開而非靜默略過'
- '[ ] SKILL.md 的子命令路由表與對應 references 已同步（skill-cli-sync-check 規則）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
closed_at: '2026-09-10T13:53:37'
closed_by: none
close_reason: not_executable_knowledge_captured
close_reason_note: 分析 context 已遷至 tarrragon/claude#55（區段：當前結論（graph-project-docs-manager：ticket CLI 契約與提交路徑））
---

# Execution Log

## Task Summary

新增 ticket track 的血緣遞移走訪子命令（給定票遞迴列出全部後裔並標出未終結者）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-236 | chars: 1109 -->

### Task Reference
- 0.1.0-W3-236 what: 實機量測 openedExternallyMessage 的 SnackBar 在外部應用取得前景後是否仍可被使用者觀察到，並據以裁定 167 SR-3

### Rationale Chain
- 0.1.0-W3-236 why: `SPEC-003` §2.13 對照表第 2 列標「判準判否、條文判是，以條文為準執行，待 PM 核定」，來源為 `0.1.0-W3-167` SR-3。該列自 2026-09-09 起掛在 PM 名下未決，`0.1.0-W3-204` 的 §2.14 列 5 誠實照抄了這個未決狀態。

**為何不直接裁**：判準判否的整段理由是一個**可觀測的事實主張**——「`/usr/bin/open` 回傳 `exitCode == 0` 即代表外部應用程式已被帶到前景，我方視窗因此不再是前景，SnackBar 在背景視窗上停留 `Motion.snackBar` 後自行消失，使用者切回來時已經沒有」。雙方目前都在預測，沒有人量過。實機驗證屬 PM 職責（CLAUDE.md §PM 可寫路徑與 pm-role 明訂）。

**裁決權限是不對稱的，這決定了本票的產出形態**：`SPEC-003` §2.13 明訂「PM 裁定保留者，須在本表該列補具名理由」——**保留在 PM 權限內**；而 SR-3 自身的 `why` 明訂「變更 §2.2 外部開啟契約的明文條文須經規格程序與用戶簽核」——**移除不在 PM 權限內**。因此本票的產出只有兩種合法形態：(a) PM 裁定保留並補具名理由，或 (b) 帶量測數據提請用戶簽核移除。**不得由本票直接改條文。**

**不裁的成本**：§2.13 自己已寫明——「判準的例外必須有具名理由，否則下一個事件會照抄這個例外而無人察覺它原本是例外」。目前該列的實質理由是「改它很貴」，那不是具名理由。

**本票不阻塞任何票**：`0.1.0-W3-205` 的範疇是截斷條件綁級別而非綁呼叫路徑，列 5 是否顯示 SnackBar 與該修法正交（`205` 的 acceptance 四條逐條核對後確認）；`232`／`207`／`233` 亦不依賴本裁決。

### Related Files
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W3-236

### Source Acceptance (reference)
- [ref] [ ] 判定準則已於量測前寫入〈實驗方法〉，且量測後未修改  # from 0.1.0-W3-236
- [ref] [ ] 四個開啟入口各至少三次取樣，每次的觀測值與環境已記錄  # from 0.1.0-W3-236
- [ref] [ ] 產出為三種合法形態之一，且未直接改動 SPEC-003 §2.2 條文  # from 0.1.0-W3-236
- [ref] [ ] 裁定保留者，§2.13 對照表第 2 列已補具名理由且該理由為量測結果  # from 0.1.0-W3-236
- [ref] [ ] 產出限於量測結果與判定準則的命中判定，未代 0.1.0-W3-210 做出條文裁決  # from 0.1.0-W3-236
