---
id: 0.1.0-W3-101
title: 同步 回饋契約掛載點條款至 component-contract-design 引導流程與 doc 範本
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-098
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-098
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 同步 回饋契約掛載點條款至 component-contract-design 引導流程與 doc 範本
when: 0.1.0-W3-098 定案方法論條文後（引導流程須反映其最終措辭）
where:
  layer: Framework Docs
  files:
  - .claude/skills/component-contract-design/SKILL.md
  - .claude/skills/component-contract-design/CHANGELOG.md
  - .claude/skills/component-contract-design/references/field-questions.md
  - .claude/skills/doc/SKILL.md
  - .claude/skills/doc/CHANGELOG.md
  - .claude/skills/doc/templates/component-library-spec-template.md
why: '`0.1.0-W3-098` 在方法論補的是**條文**（掛載點、失敗路徑、降級不解除回饋義務）。條文存在不等於設計者會被問到——用戶裁示（2026-09-08）明指：**這部分在元件庫 skill 引導互動元件設計的時候就要列進去**。


  skill 是引導層，職責是**在設計互動元件的當下把這件事問出來**，不是把條文再抄一遍。判準問句（「把這個元件背後的服務整個拔掉，使用者按下去還看得到反應嗎？」）須出現在逐欄填寫回饋契約的流程中，位置在「元件被判定為可互動」之後、「回饋契約欄開始填」之時。使用者走完流程而沒被問過掛載點問句，即為引導失效——這是本票的失敗判準。


  第二項：〈契約齊全的定義〉須同步。齊全檢查若不納入掛載點與失敗路徑兩項，會放行一份只寫成功路徑的回饋契約——而那正是 `ARCH-GPD-001` 記錄的缺陷形態。齊全定義是機械檢查的依據，漏一項即該項永不被攔。


  第三項：`doc` skill 的元件庫規格範本是產物形狀的權威。範本的回饋契約子節若不含失敗路徑欄位，規格撰寫者無處可填，條文與齊全定義都會落空。


  三者關係：方法論定「該怎麼判斷」（`0.1.0-W3-098`），skill 定「何時問出來」，範本定「填在哪裡」。缺任一環，另兩環都不會生效。'
how:
  task_type: Documentation
  strategy: '先讀 `0.1.0-W3-098` 落定後的方法論〈回饋契約〉節，以其最終措辭為準，不依本票 why 段的轉述。


    **skill 側**：在 `component-contract-design` 引導逐欄填寫回饋契約的流程中插入掛載點判準問句，位置在「元件被判定為可互動」之後、「回饋契約欄開始填」之時。問句形式為要求作答，不是提醒閱讀條文——引導層的價值在於它擋在流程上。〈契約齊全的定義〉的回饋契約欄判準納入掛載點與失敗路徑兩項。


    **範本側**：`doc` skill 的元件庫規格範本，回饋契約子節的欄位說明補失敗路徑（成功／失敗／逾時／服務不可用）與掛載點的填寫要求，使規格撰寫者有處可填。


    **不重述條文**：skill 與範本引用方法論的權威節，不複製條文內容——同一判準寫在兩處，修改時只改一處即漂移（此為既有慣例，見方法論〈與既有判準的關係〉段）。


    **不寫數字**：不以條數命名章節或作為跨檔引用錨點（`reference-stability-rules` 規則 10）。


    **成對檢查**：本票新增的跨檔引用，兩端同時開啟確認措辭互相對應（`DOC-GPD-002` 變體：修正單邊接線的修法本身只補一端）。'
acceptance:
- '[x] component-contract-design 的引導流程在「判定為可互動元件後、填回饋契約前」要求作答掛載點判準問句'
- '[x] 〈契約齊全的定義〉的回饋契約欄判準納入掛載點與失敗路徑兩項'
- '[x] doc 範本的回饋契約子節欄位說明含失敗路徑（成功／失敗／逾時／服務不可用）與掛載點填寫要求'
- '[x] skill 與範本引用方法論權威節而非複製條文；新增的跨檔引用已做成對檢查'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T15:44:23'
completed_at: '2026-09-08T15:56:24'
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

同步 回饋契約掛載點條款至 component-contract-design 引導流程與 doc 範本

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-08 15:43] readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-08T15:56:24
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-08
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

變更摘要：
- `.claude/skills/component-contract-design/SKILL.md`（v2.0.0→2.1.0）：〈契約齊全的定義〉第 5 條補「按下確認／等待與結果不得僅在服務回應之後才成立」與「等待與結果涵蓋成功／失敗／逾時／服務不可用四段」兩項機械可查判準，各引用方法論〈掛載點〉〈失敗路徑覆蓋〉。
- `.claude/skills/component-contract-design/references/field-questions.md`：〈元件契約欄位填寫問句〉回饋契約列的填寫問句改為要求作答的掛載點判準問句（逐字：「把這個元件背後的服務整個拔掉，使用者按下去還看得到反應嗎？」），位置在「元件判定為可互動或有資料態變更」之後、按下確認／等待與結果開始填之時；同列補失敗路徑覆蓋（四段皆須有內容）與降級不解除回饋義務的作答要求；「常見空格與來源」欄補兩條款常見漏答樣態。（此檔未列於 ticket where.files，但為 SKILL.md〈按需讀取〉表指定的「填契約」步驟實際承載檔，是本票 acceptance 1 的落地位置。）
- `.claude/skills/doc/templates/component-library-spec-template.md`：〈回饋契約〉子節說明段補三則（掛載點／失敗路徑覆蓋／降級不解除回饋義務），皆引用方法論子節；按下確認、等待與結果兩列的「反應」欄位佔位符補填寫提示；第 9 章驗收標準的回饋契約項同步補兩項要求，與子節說明保持成對。
- `.claude/skills/doc/SKILL.md`（v1.13.0→1.14.0）：version bump 呼應範本變更。
- 兩份 CHANGELOG.md（component-contract-design 2.1.0、doc 1.14.0）：記錄變更範圍與血緣（同批落成，互相指回對方版號）。

驗證：
- 三處新增的跨檔引用（掛載點／失敗路徑覆蓋／降級不解除回饋義務）已與方法論檔 `.claude/methodologies/component-library-bidirectional-constraint-methodology.md` 的 `#####` 子節標題逐字核對一致（行 96/108/118）。
- `skill-sync push component-contract-design < /dev/null` 預覽顯示僅 3 檔異動（CHANGELOG.md/SKILL.md/field-questions.md），零可攜性違規。
- `python3 .claude/skills/broken-link-check/scan_links.py .`：broken=8，皆為既有 wrap-decision project-integration 缺檔，與本次改動無關，未新增 broken link。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-098 | chars: 1699 -->

### Task Reference
- 0.1.0-W3-098 what: 補強 回饋契約：按下確認與等待指示的掛載點須獨立於服務回應，並涵蓋失敗路徑（來源已完成，結論見 Solution）
- 0.1.0-W3-098 what: 補強 回饋契約：按下確認與等待指示的掛載點須獨立於服務回應，並涵蓋失敗路徑（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-098 why: 實機驗證時使用者按下重新掃描按鈕，**畫面完全沒有反應**——沒有按下的視覺、沒有等待指示、沒有失敗訊息。背後同時有三件事失敗（平台 handler 未註冊、完成邏輯落在同一 microtask 使骨架不可見、例外被降級收斂為某個狀態值），但使用者側的觀察只有一件事：按了沒反應。

用戶據此下的裁示（2026-09-08）：**提示效果應該掛在互動元件上，跟背後服務能不能運作不相關。**

現行〈回饋契約〉節的六類事件表定義了觸發者與回答範圍，但**沒有規定回饋的掛載點**。實作者自然會把回饋寫在服務回應的處理路徑上——那是「有東西可以顯示」的地方——於是產生一條反向相關：服務正常時回饋齊全，服務不可用時回饋為零。使用者最需要知道發生什麼事的那一刻，系統最沉默。

第二個缺口：六類事件表只列了事件，**沒有要求回答涵蓋失敗路徑**。「等待與結果」列指向三層回饋模型，而該模型的「結果通知」在實務上被讀成成功結果。失敗、逾時、服務不可用不在同一張表上，因此沒有人為它們填欄位。

第三個缺口：**降級與靜默沒有被區分**。把例外收斂為已知狀態值是行為確定性的手段，但收斂後的狀態若無可見表現（或其表現另有前提條件而前提不成立），收斂等於銷毀證據。現行條文沒有要求指出降級目標狀態的可見表現。

完整分析見 `ARCH-GPD-001`（互動回饋掛在服務的成功路徑上，服務越不可用回饋越少）。（來源已完成，結論見 Solution）
- 0.1.0-W3-098 why: 實機驗證時使用者按下重新掃描按鈕，**畫面完全沒有反應**——沒有按下的視覺、沒有等待指示、沒有失敗訊息。背後同時有三件事失敗（平台 handler 未註冊、完成邏輯落在同一 microtask 使骨架不可見、例外被降級收斂為某個狀態值），但使用者側的觀察只有一件事：按了沒反應。

用戶據此下的裁示（2026-09-08）：**提示效果應該掛在互動元件上，跟背後服務能不能運作不相關。**

現行〈回饋契約〉節的六類事件表定義了觸發者與回答範圍，但**沒有規定回饋的掛載點**。實作者自然會把回饋寫在服務回應的處理路徑上——那是「有東西可以顯示」的地方——於是產生一條反向相關：服務正常時回饋齊全，服務不可用時回饋為零。使用者最需要知道發生什麼事的那一刻，系統最沉默。

第二個缺口：六類事件表只列了事件，**沒有要求回答涵蓋失敗路徑**。「等待與結果」列指向三層回饋模型，而該模型的「結果通知」在實務上被讀成成功結果。失敗、逾時、服務不可用不在同一張表上，因此沒有人為它們填欄位。

第三個缺口：**降級與靜默沒有被區分**。把例外收斂為已知狀態值是行為確定性的手段，但收斂後的狀態若無可見表現（或其表現另有前提條件而前提不成立），收斂等於銷毀證據。現行條文沒有要求指出降級目標狀態的可見表現。

完整分析見 `ARCH-GPD-001`（互動回饋掛在服務的成功路徑上，服務越不可用回饋越少）。（來源已完成，結論見 Solution）

### Related Files
- .claude/methodologies/component-library-bidirectional-constraint-methodology.md  # from 0.1.0-W3-098（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 方法論〈回饋契約〉節含掛載點條款與可在設計期回答的判準問句  # from 0.1.0-W3-098（來源已完成，結論見 Solution）
- [ref] [x] 「等待與結果」的回答範圍涵蓋成功／失敗／逾時／服務不可用，且不接受「不會發生」填法  # from 0.1.0-W3-098（來源已完成，結論見 Solution）
- [ref] [x] 降級不解除回饋義務條款已補，並明寫日誌不抵扣使用者回饋  # from 0.1.0-W3-098（來源已完成，結論見 Solution）
- [ref] [x] 條文為型別化描述，本案具體情節（掃描按鈕、MethodChannel）只作為 ARCH-GPD-001 的引用，不寫進正文  # from 0.1.0-W3-098（來源已完成，結論見 Solution）
- [ref] [x] 方法論〈回饋契約〉節含掛載點條款與可在設計期回答的判準問句  # from 0.1.0-W3-098（來源已完成，結論見 Solution）
- [ref] [x] 「等待與結果」的回答範圍涵蓋成功／失敗／逾時／服務不可用，且不接受「不會發生」填法  # from 0.1.0-W3-098（來源已完成，結論見 Solution）
- [ref] [x] 降級不解除回饋義務條款已補，並明寫日誌不抵扣使用者回饋  # from 0.1.0-W3-098（來源已完成，結論見 Solution）
- [ref] [x] 條文為型別化描述，本案具體情節（掃描按鈕、MethodChannel）只作為 ARCH-GPD-001 的引用，不寫進正文  # from 0.1.0-W3-098（來源已完成，結論見 Solution）
