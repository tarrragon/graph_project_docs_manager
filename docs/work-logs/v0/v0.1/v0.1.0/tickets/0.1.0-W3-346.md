---
id: 0.1.0-W3-346
title: 修改 tracking_schema：FlowStep 新增 traverses 必填欄位（0..n domain 清單）
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-341
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-341 裁決改定
  final_decision: 本專案直接修改 schema，交同專案另一 session 執行
  rationale: 用戶裁示不等上游；schema 修改為 traverses 清單 0..n
who:
  current: thyme-python-developer
  history: {}
what: 在 tracking_schema 新增 FlowStep.traverses 必填欄位（domain 名字串清單，0..n），同步 conformance 測試、doc CLI 的 UC flow 驗證、CHANGELOG 與 SKILL 版號
when: W3-347 完成後（已完成）；UC-01..06 traverses 已回填
where:
  layer: 待定義
  files:
  - .claude/skills/doc/doc_system/core/tracking_schema.py
  - .claude/skills/doc/tests/test_tracking_schema_conformance.py
  - .claude/skills/doc/CHANGELOG.md
  - .claude/skills/doc/SKILL.md
why: W3-341 裁決改定（WRAP 後用戶裁示 2026-09-14）：單一 domain 欄位改為 traverses 清單 0..n，對應 domain-map §2.5 貫穿；用戶指示不等上游、在本專案直接修改 schema。Domain 視圖的格、SPEC-001 §1/§2/§5 與 W3-340 依賴此欄位
how:
  task_type: Documentation
  strategy: 讀 W3-341 Solution〈裁決改定〉與 domain-map §2.5 → FLOWSTEP_REQUIRED_FIELDS 加 traverses 並在型別註解寫明語意（直接觸及的 domain 公開面、純畫面步驟為空清單、消費事件的步驟觸及發出方 domain）→ 找出所有讀取 FLOWSTEP_REQUIRED_FIELDS 的驗證路徑，補型別檢查（必須是字串清單，允許空）→ 補測試（含缺欄位、非清單的正向對照輸入須被攔下）→ 以 UC-01..06 實際跑一次驗證全數通過
acceptance:
- '[ ] FLOWSTEP_REQUIRED_FIELDS 含 traverses，schema 註解寫明語意（直接觸及的 domain 公開面、0..n、純畫面步驟為空清單）'
- '[ ] doc CLI 驗證 UC flow 時，缺 traverses 或 traverses 非字串清單即報錯；空清單合法。domain 名是否存在於 domain-map 不在本票檢查範圍'
- '[ ] 測試含正向對照：缺欄位、traverses 為字串（非清單）兩種輸入各斷言被攔下；另有空清單通過的斷言'
- '[ ] 以現行 docs/usecases/UC-01..06 跑 doc CLI 驗證全數通過，指令與輸出記錄於票面'
- '[ ] doc CHANGELOG 與 SKILL.md 版號同步（CHANGELOG 不含專案 ticket ID）；修改超出 where.files 時停手寫 NeedsContext 回報，不自行擴大範圍'
- '[ ] Solution 記錄本地修改會被 sync-pull 覆蓋的風險，並以 add-spawn-request 登記「是否回報框架 issue」交 PM 裁決（本票不執行 issue 操作）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T16:19:37'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

回報 FlowStep 缺 domain 欄位至框架 issue（查重後 observe 或 init）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-14 14:27] readiness=1 validate=0


### 票面改定（PM，2026-09-14）

本票原為「回報框架 issue」，W3-341 裁決改定後改為本專案直接修改 schema。Task Summary 段落為建票時舊文字，以 frontmatter 的 title／what／acceptance 為準。

- 欄位語意來源：W3-341 Solution〈裁決改定〉、`docs/domain-map.md` §2.5「貫穿」
- 已完成前置：W3-347（domain-map 缺口）；UC-01..06 的 traverses 已回填（`grep -n traverses docs/usecases/*.md` 可驗）
- 本票不改 docs/usecases；若驗證指出 UC 資料錯誤，停手寫 NeedsContext
## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext
### NC-1：doc CLI 不存在 FlowStep 欄位驗證路徑（2026-09-14，執行 session 盤點）

- **缺失項**：acceptance 第 2、4 項要求「doc CLI 驗證 UC flow 時」攔下缺 traverses／非字串清單，並以 UC-01..06 跑 doc CLI 驗證。實測 doc CLI 內沒有任何命令讀取 `FLOWSTEP_REQUIRED_FIELDS`。
- **觸發位置**：全庫消費者盤點（`grep -rln FLOWSTEP_REQUIRED_FIELDS .claude lib test`，排除 hook-logs）僅 2 處——`tracking_schema.py`（定義）與 `tests/test_tracking_schema_conformance.py` 的 `TestGraphTypeTablesRealFlowStepConformance`（且 fixture 只讀 UC-01，UC-02..06 未被檢查）。CLI 側唯一讀 flow 區塊的是 `doc_system/core/uc_registry.py` 的 `_extract_structured_flow_steps`，用途為 `uc context` 摘要，不做欄位驗證；`commands/validate.py` 只處理 SPEC。
- **影響**：在 where.files 四檔內只能完成 acceptance 1（常數＋註解）、3 的測試側、5 的版號；第 2、4 項須新建 CLI 驗證入口，必然修改 where.files 外的檔案（候選：`doc_system/core/uc_registry.py` 或新增驗證函式後接到 `doc_system/commands/uc.py` 的 `uc verify`，另 `cli.py` 若新增子命令）。另 `references/usecases.md` 與 `templates/usecase-template.md` 描述 FlowStep 欄位，加必填後會與 schema 不一致，亦在 where.files 外。
- **建議補料**（PM 擇一）：(a) 擴 where.files：驗證函式放 `tracking_schema.py`（純函式，JSON export 不受影響——FLOWSTEP_REQUIRED_FIELDS 本就不在 `tracking_schema.json`），掛到 `uc verify` 並補 `commands/uc.py`、`tests/test_uc*.py`、`references/usecases.md`、`templates/usecase-template.md`；(b) 縮 acceptance：第 2、4 項改為「conformance 測試對 UC-01..06 全數檢查 traverses 型別」，CLI 驗證入口另開票。
- **重派成本**：低。修改點已定位，補料後本 session 可直接續做；尚未改動任何檔案。

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-341 | chars: 536 -->

### Task Reference
- 0.1.0-W3-341 what: 定案 FlowStep 所屬 domain 的資料來源（CLAUDE.md §6 待決項）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-341 why: W3-335.19 回寫時確認 FlowStep 無 domain 欄位；SPEC-001 §2 步驟表 Domain 欄、事件流小表「步驟序號 · domain」、破洞類別 event-declaration-mismatch 判準皆依賴此資料，而 CLAUDE.md §6 將「Domain 視圖的列與格無來源、FlowStep 無 domain 欄位」列為待決、至今無承接票（來源已完成，結論見 Solution）

### Related Files
- docs/domain-map.md::read  # from 0.1.0-W3-341（來源已完成，結論見 Solution）
- docs/usecases/UC-01-open-project-and-reach-usable-state.md::read  # from 0.1.0-W3-341（來源已完成，結論見 Solution）
- .claude/skills/doc/doc_system/core/tracking_schema.py::read  # from 0.1.0-W3-341（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 候選來源逐項列出並各附本專案 39 個 FlowStep 的可推導率實測  # from 0.1.0-W3-341（來源已完成，結論見 Solution）
- [ref] [x] 標明每個方案是否需改上游 tracking_schema 並依框架問題判準分流  # from 0.1.0-W3-341（來源已完成，結論見 Solution）
- [ref] [x] 用戶裁決結果記錄並回寫 CLAUDE.md §6 待決清單  # from 0.1.0-W3-341（來源已完成，結論見 Solution）
- [ref] [x] 依賴此資料的規格位置與實作票已標 blockedBy 或指向本票結論  # from 0.1.0-W3-341（來源已完成，結論見 Solution）

### UC Context
#### UC-01: 開啟專案並抵達可用狀態
Spec 位置: docs/app-use-cases.md:22
主要流程:
  select-folder: 選擇資料夾
  load-schema: 載入型別表
  parse-nodes: 解析節點
  reach-domain-view: 抵達 Domain 視圖
PM 註記（2026-09-14）：用戶指示本票由另一個 session 執行（該 session 負責修改新增的框架 issue），本 session 不派發。執行時依據見 0.1.0-W3-341 Solution（候選方案表、39 步可推導率實測、兩條 AND 判準套用記錄、用戶裁決選項 A）；上游位置 tracking_schema.py 的 FlowStep 型別與 FLOWSTEP_REQUIRED_FIELDS。
【暫停，勿執行】PM 註記（2026-09-14）：W3-345 回填時發現 39 步中 22 步無法分配單一 domain（domain-map §2 畫面狀態為 L4 layer 非 domain；另有步驟同時觸及兩個 domain），W3-341 選項 A「必填單一 domain」的欄位形狀正在以 WRAP 重新評估。欄位形狀定案並回寫本票前，請勿 observe／init 框架 issue，以免上游依錯誤形狀修 schema。
### 【解除暫停，範圍改定】用戶裁示（2026-09-14）——以本節為準，取代前述「只回報 issue」

**用戶原話要旨**：現在不等上游；暫停其他開發，集中在本專案處理，框架修復派給另一個 session 直接做。

**欄位形狀（WRAP 後定案，取代 W3-341 選項 A 的「單一必填 domain」）**

| 項 | 定案 |
|----|------|
| 欄位名 | `traverses`（對應 domain-map §2.5 權威詞「貫穿（traverse）」，與 `emits`／`consumes` 同為動詞形欄位） |
| 型別 | domain 名清單，0..n |
| 必填性 | 欄位必須存在（納入 `FLOWSTEP_REQUIRED_FIELDS`）；空清單 `[]` 合法 |
| 語意 | 步驟**直接觸及**的 domain 公開面；經依賴邊間接到達者不列；純畫面步驟（domain-map 的 L4 畫面狀態層）為 `[]` |
| 值域 | 被觀察專案 domain-map 定義的 domain 名 |

**改定理由**：W3-345 回填時 39 步中 22 步無法分配單一 domain——畫面狀態在 domain-map §2 是 L4 layer 非 domain，且「貫穿」是步驟與 domain 的多對多關係而非歸屬。

**執行範圍**：`tracking_schema.py` 的 FlowStep 必填欄位與註解、conformance 測試（含正向對照：缺 `traverses` 必須被判不合規；`[]` 必須合法）、doc skill 版號與 CHANGELOG；另依 framework-issue 流程查重後 observe／init 記錄此變更（內容去識別化）。

**不在範圍**：本專案 UC 文件回填（W3-345）、domain-map 缺口（另票）。
