---
id: 0.1.0-W3-103
title: 盤點 頁面層對元件 callback 的接線：服務受理層與 event 效果層的承擔者
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-099
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 元件層已驗證，缺口在呼叫端
  final_decision: ANA 逐頁盤點接線，再依結論建 IMP 票
  rationale: 元件把二三層委派給呼叫端是設計上正確的，但委派不等於承接方有做；未驗證前無法判斷要修幾處、修什麼
who:
  current: saffron-system-analyst
  history: {}
what: 盤點 頁面層對元件 callback 的接線：服務受理層與 event 效果層的承擔者
when: 0.1.0-W3-099 已驗證元件層第一層齊備並將二三層判為範圍外（2026-09-08）
where:
  layer: Presentation
  files:
  - lib/screens::read
  - lib/app::read
  - docs/spec/ui/SPEC-001-screen-state-matrix.md::read
  - .claude/error-patterns/architecture::read
why: '`0.1.0-W3-099` 驗證了元件層：22 個可互動元件的第一層回饋（互動當下的表現）全部齊備，由 Material `InkWell` 的 ripple 提供，且該回饋不依賴任何服務回應。


  同時它指出元件把第二層（服務受理）與第三層（event 效果）**委派給呼叫端**——SPEC-004 的回饋契約明文寫「本元件承載三層中的點擊確認層；等待指示為畫面級／結果通知為狀態轉換本身」。委派本身是設計上正確的：元件只發 callback，不該知道服務存在。


  **但「已委派」不等於「承接方有做」**，而承接方從未被驗證。本次實機驗證撞到的正是這一層——按下重新掃描之後，第二層（服務受理：toast 或 loading）與第三層（event 效果：成功／失敗結果）都沒有出現，而元件自身的 ripple 是有的。缺陷完全落在呼叫端。


  因此 `ARCH-GPD-001` 在本專案的實際落點是頁面層，不是元件層。元件層盤完不代表問題盤完——那反而是最容易誤判「已檢查過」的地方，因為結論是全綠。'
how:
  task_type: Analysis
  strategy: '以「接線」為單位，不以頁面或元件為單位：一條接線是「某個元件的某個 callback，在某個頁面被接到某個動作」。逐條回答第二層與第三層的承擔者是誰、有沒有實作。


    分類：


    - **無跨層呼叫**：callback 只改本地狀態（切分頁、展開收合、選取）。第二、三層不適用，記錄後略過。

    - **有跨層呼叫且兩層皆有承擔者**：記錄承擔者與實作位置，作為其餘接線的參照樣板。

    - **有跨層呼叫但缺第二層**：使用者按下後到結果出現之間沒有任何「已受理」的表現。記錄缺口。

    - **有跨層呼叫但缺第三層**：結果（尤其失敗、逾時、服務不可用）沒有可見表現。記錄缺口，並註明失敗是否被降級收斂掉（收斂到無表現的狀態即為靜默失敗，見 `ARCH-GPD-001`〈降級不解除回饋義務〉）。


    **已知的一條**：破洞報告的重新掃描（`action-gaps-rescan`）——第二層與第三層皆缺，已由 `0.1.0-W3-096`（掃描完成時機）與 `0.1.0-W3-097`（MethodChannel 未註冊）從別的角度承接。本票須把它納入盤點以保持清單完整，但不重複那兩票的分析，只標註承接票。


    **判準的權威**是 `ARCH-GPD-001` 加 `0.1.0-W3-102` 定案的三層歸屬表（若 102 尚未完成，以 ARCH-GPD-001 加本票 why 段的三層描述為準，並於 Solution 註明）。


    產出為接線清單與缺口表，不改任何檔案（where.files 全為 `::read`，ANA 代理人無 Write/Edit）。缺口依修補形態分組建 IMP 票（同形態合為一票），以 `add-spawn-request` 登記，complete 前逐項 resolve 為終態。'
acceptance:
- '[ ] 接線逐條分類（無跨層呼叫／兩層皆備／缺第二層／缺第三層）並附承擔者與實作位置'
- '[ ] 缺第三層者註明失敗是否被降級收斂掉（收斂到無表現的狀態即為靜默失敗）'
- '[ ] 缺口依修補形態分組建 IMP 票並以 add-spawn-request 登記，complete 前逐項 resolve 為終態'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

盤點 頁面層對元件 callback 的接線：服務受理層與 event 效果層的承擔者

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-099 | chars: 1149 -->

### Task Reference
- 0.1.0-W3-099 what: 盤點 既有可互動元件的回饋掛載點，判定哪些需調整（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-099 why: `ARCH-GPD-001` 定案的判準——**回饋掛在互動元件上，與背後服務能否運作無關**——是新確立的，既有元件建立時沒有這條約束。用戶裁示（2026-09-08）：既有的所有互動元件都要檢討是否需調整。

盤點範圍：`lib/components/` 現有 43 個元件中，含 `onTap` / `onPressed` / `onChanged` / `onAction` / `InkWell` / `GestureDetector` 者共 25 個（PM 已 grep 確認）：`app_shell`、`app_button`、`app_snack_bar`、`app_table_row`、`list_row`、`expander_icon`、`filter_dropdown`、`blocked_state`、`load_prompt`、`matrix_grid`、`document_body`、`loading_state`、`issue_marker`、`nav_item`、`missing_source_state`、`matrix_cell`、`project_switcher_entry`、`recent_project_item`、`relation_item`、`search_field`、`segmented_control`、`switcher_overlay`、`swimlane_node`、`tree`、`table_column_header`。SPEC-004 的對應條目皆已有回饋契約子節，但那些子節是在舊判準下填的。

不逐件檢討的後果：新判準只約束新元件，存量元件維持「回饋掛在服務回應上」的形態，而存量正是使用者實際會碰到的部分。今天的實例（按下重新掃描完全沒反應）就出自存量元件，不是新設計的。

**這不是逐件重寫回饋契約**。多數元件的互動可能根本不呼叫服務（純本地狀態切換），對它們判準自動成立；需要調整的是那些互動會觸發跨層呼叫、而回饋定義依賴該呼叫回應的元件。先分類才知道實際工作量。（來源已完成，結論見 Solution）

### Related Files
- lib/components::read  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-004-component-library.md::read  # from 0.1.0-W3-099（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 25 個可互動元件逐件歸類（A 無需調整／B 已符合／C 需調整／D 承載者不明）並附理由  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
- [ref] [x] SPEC-004 對應條目的回饋契約子節與程式碼的一致性逐件記錄（規格對實作錯／實作對規格未寫／兩者皆未涵蓋分開列）  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
- [ref] [x] C 類與 D 類依調整形態分組並以 add-spawn-request 登記，complete 前逐項 resolve 為終態  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
