---
id: 0.1.0-W3-031
title: 建立 skill-sync push 的「內容變更但版號未變」閘門
type: IMP
status: completed
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-025
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-025 SR-1 待 PM 裁決
  final_decision: 成票（IMP/P2）
  rationale: 六支分歧的判定成本全部來自此缺口，屬結構性而非個案
who:
  current: thyme-python-developer
  history: {}
what: push 時偵測內容雜湊與 .skill-sync-base 不同、但 metadata.version 與遠端相同，於 preview 印出警告並要求確認
when: 下次修改 skill-sync CLI 時；或 W3-025 的六支分歧復發時
where:
  layer: framework
  files:
  - .claude/skills/skill-sync/skill_sync/cli.py::write
  - .claude/skills/skill-sync/tests/test_cli.py::write
why: W3-025 判定的六支分歧全數是同一形態：內容改了、版號沒動。版號相同時它不只無鑑別力，而是主動宣稱兩邊一致，使分歧不會被例行同步檢查發現，累積到需要一張票逐支讀 diff 才判得出方向。閘門放在 push 端最便宜——寫入者當下仍握有變更脈絡，事後比對端已經沒有
how:
  task_type: Implementation
  strategy: 先寫紅燈測試（內容變但版號同時應警告、內容變且版號變時應靜默）-> 於 push preview 路徑加判斷 -> 確認既有測試不受影響
acceptance:
- '[x] 內容雜湊與 .skill-sync-base 不同但版號與遠端相同時，push preview 印出警告並要求確認'
- '[x] 內容變更且版號已 bump 時不觸發警告（避免每次 push 都跳）'
- '[x] 新增測試覆蓋上述兩條，且既有 test_cli.py 全數通過'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-07T11:12:38'
completed_at: '2026-09-07T11:19:13'
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

push 時偵測內容雜湊與 .skill-sync-base 不同、但 metadata.version 與遠端相同，於 preview 印出警告並要求確認

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-07 11:12] 先寫紅燈測試再實作。注意 --force 在此 CLI 已有雙重語意（跳過確認 + 旁路 portability 閘門），新閘門的旁路方式請避免再疊到同一個旗標上。

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
新增純函式 `_stale_version_warning(source, local_ver, remote_ver)`（cli.py）：
版號與遠端相同（local_ver == remote_ver）且內容雜湊自 `.skill-sync-base` 已變更
（`compute_content_hash(source) != _read_sync_base(source)`）時回傳警告文字，
其餘情況（版號不同、無 base 記錄、內容未變、任一版號缺失）回傳 None。
搭配 `_print_stale_version_warning` 包裝列印，與既有 `_diverge_warning` /
`_print_divergence_warning` 同一設計對稱（純邏輯函式 + print 包裝，利於單元測試）。

於 `cmd_push` 的 `[Push Preview]` 區塊內、`_print_divergence_warning` 之後、
`print_diff_preview` 之前呼叫 `_print_stale_version_warning(source, local_ver,
remote_ver)`，warning 印至 stderr。

未疊加任何新旗標語意到 `--force`：本閘門只是在 preview 階段多印一行警告，
沒有引入額外的中止/略過機制；`--force` 對它的效果與既有「跳過互動確認」語意
一致（非 force 模式下警告會顯示在既有的 [y/N] 確認提示之前），因此沒有新增
第三種 `--force` 語意，符合派發 note 的提醒。

修改檔案：
- .claude/skills/skill-sync/skill_sync/cli.py（新增 `_stale_version_warning` /
  `_print_stale_version_warning`，`cmd_push` 接線一行）
- .claude/skills/skill-sync/tests/test_cli.py（新增 4 個純函式單元測試 +
  2 個 cmd_push 整合測試，import 增列 `_stale_version_warning`）

### 自檢結果（Layer 1）

**執行時機**: complete 前
**掃描手段**: （實際執行的指令或檢查項，逐項可複驗）
- `grep -n "數據\|代碼\|默認\|文檔\|軟件\|硬件\|信息\|視頻" cli.py test_cli.py` → 0 處命中
- `uv run --directory .claude/skills/skill-sync pytest tests/test_cli.py -v` → 164 passed
- 人工對照 ticket-body-schema.md IMP 必填章節清單（Solution / Test Results / Completion Info）
- 人工核對三明示：新函式 docstring 含 Why（分歧形態的成因）、Consequence（等到事後才判得出方向）、Action（閘門放 push 端的理由）

| 類別 | 發現項目 | 處理方式 |
|------|---------|---------|
| B1 | 禁用詞掃描 | 0 處命中，無需修正 |
| A1 | 新函式 docstring 三明示 | 已於首次撰寫時齊備 |
| （無其他發現）| — | — |

**自檢結論**: 已掃描範圍內通過（未涵蓋：Layer 2 委員審查，本票非規則/方法論類產出未觸發該層）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
執行指令：
  uv run --directory .claude/skills/skill-sync pytest tests/test_cli.py -v

紅燈確認（實作前）：新測試 import `_stale_version_warning` 時
ImportError: cannot import name '_stale_version_warning' from 'skill_sync.cli'
（collection error，6 個新測試全數無法收集）。

綠燈（實作後）：164 passed in 1.22s（含新增 4 個 `_stale_version_warning`
純函式單元測試 + 2 個 `cmd_push` 整合測試，既有 158 個測試維持全數通過）。

Phase 4（重構評估）：新函式與既有 `_diverge_warning`/`_print_divergence_warning`
同構（純邏輯 + print 包裝分離），無重複邏輯需抽取；`cmd_push` 接線僅一行呼叫，
認知負擔未增加。結論：無需重構，程式碼品質 A。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-07T11:19:13
**Executing Agent**: thyme-python-developer 或 general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-025 | chars: 475 -->

### Task Reference
- 0.1.0-W3-025 what: 逐支比對本地與 claude-skills.git 的差異，判定該 push 或 pull，並釐清為何改了內容卻未 bump 版號（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-025 why: 版號相同而內容不同代表至少一邊改了東西沒 bump 版號。版號在此情況下不只無鑑別力，而是誤導性的——它主動宣稱兩邊一致。sync-push 的措辭已明示內容雜湊只證明不同、不證明誰較新（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/ticket/SKILL.md::write  # from 0.1.0-W3-025（來源已完成，結論見 Solution）
- .claude/skills/ticket/CHANGELOG.md::write  # from 0.1.0-W3-025（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/SKILL.md::write  # from 0.1.0-W3-025（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/CHANGELOG.md::write  # from 0.1.0-W3-025（來源已完成，結論見 Solution）
- .claude/skills/tdd/SKILL.md::write  # from 0.1.0-W3-025（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] 六支各有明確方向判定與依據，非憑版號推斷  # from 0.1.0-W3-025（來源已完成，結論見 Solution）
- [ref] [x] 已執行對應的 skill-sync push 或 pull，再跑 sync-push 時該六支不再出現在分歧清單  # from 0.1.0-W3-025（來源已完成，結論見 Solution）
- [ref] [x] 改了內容未 bump 版號的一方已補上版號  # from 0.1.0-W3-025（來源已完成，結論見 Solution）
