---
id: 0.1.0-W1-002
title: 建立 六項導覽的路由殼
type: IMP
status: in_progress
version: 0.1.0
wave: 1
priority: P0
parent_id: null
children: []
blockedBy:
- 0.1.0-W1-001
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 七個畫面需要一個共用的導覽容器
  final_decision: 導覽列六項，專案切換不佔一項而收為側欄浮層
  rationale: PROP-004 §範圍界定已定案：專案切換不是獨立畫面，側欄頂端的專案名本身即切換入口，因此導覽列為六項而非七項
who:
  current: parsley-flutter-developer
  history: {}
what: 建立 六項導覽的路由殼
when: W1-001 的 ProviderScope 接線完成後
where:
  layer: Presentation
  files:
  - lib/app/router.dart
  - lib/app/shell.dart
  - lib/main.dart
  - integration_test/app_test.dart
why: SPEC-001 §1-7 界定六個畫面加一個浮層，共 29 個狀態。各畫面若自帶導覽，切換行為會分歧。本票交出可切換的殼，讓後續每張畫面票只需關心自己的內容。
how:
  task_type: Implementation
  strategy: 建立路由與 shell：左側導覽列六項（Domain 視圖、UC Flow、追溯、Ticket 清單、破洞報告、節點詳情），側欄頂端顯示專案名作為切換浮層入口。各路由先渲染標示畫面名的佔位頁。浮層本身的三個狀態由後續票實作，本票只留入口。
acceptance:
- '[x] 導覽列為六項，與 PROP-004 的畫面清單逐項對應'
- '[x] 六項皆可切換且各自渲染標示畫面名的佔位頁'
- '[x] 側欄頂端顯示專案名，點擊有回應（浮層內容可為空殼）'
- '[x] 四種視窗尺寸下導覽列與內容區皆不溢位'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-08-28T10:43:48'
completed_at: null
created: '2026-08-27'
updated: '2026-08-27'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

建立 六項導覽的路由殼

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
建立六項導覽的路由殼，新增 `lib/app/router.dart`（`AppDestination` 六值列舉、`selectedDestinationProvider`、`buildDestinationPage` 佔位頁工廠）與 `lib/app/shell.dart`（`AppShell`：左側 `_Sidebar`——頂端 `_ProjectSwitcherEntry` + 六個 `_NavItem`——與右側 `IndexedStack` 內容區）。`lib/main.dart` 的 `child: HomePage(...)` 改為 `child: const AppShell()`，`colorSchemeSeed` 裸色碼（屬 0.1.0-W1-024）與其餘結構未動。

PM 裁決採選項 1 後，`integration_test/app_test.dart` 三項調整：
1. `HomePage.pageKey` 全數改用 `kLandingPageKey`（= `AppDestination.domain.pageKey`），語意由「抵達 HomePage」改為「抵達導覽殼的預設落地畫面（Domain 視圖）」。
2. `_pumpAppAndCollectErrors` 新增 `ProviderScope` 包裹——`main()` 的 `runApp` 才有這層祖先，直接 `pumpWidget(DocsManagerApp(...))` 缺此層，`AppShell`（`ConsumerWidget`）的 `ref.watch` 會找不到 ProviderScope 而全數斷言落空（0/10 起始復現）。這是本票改動觸發的既有測試結構缺口，非新增邏輯，修在 where.files 範圍內。
3. 多語系測試（`以 zh/en 啟動時顯示對應語系文案`）斷言 `find.text('專案文件流'/'Docs Flow')`，原本由 `HomePage` 的 `AppBar` 提供；`AppShell` 補上同語意的 `AppBar(title: Text(l10n.appTitle))` 使其維持綠燈，未改動測試本身的斷言內容或案例數。

ARB 新增 8 個 key（`navDomain`/`navUcFlow`/`navTraceability`/`navTickets`/`navGaps`/`navNodeDetail`/`projectSwitcherEntryLabel`/`projectSwitcherPlaceholderTitle`），英中雙語系皆已補齊，已跑 `fvm flutter gen-l10n`（讀 `l10n.yaml` 設定）重新產生 `lib/l10n/app_localizations*.dart`。

側欄頂端專案切換入口點擊後開啟 `AlertDialog`（`showDialog`），標題為 `l10n.projectSwitcherPlaceholderTitle`，符合「浮層內容可為空殼」——三個狀態（近期專案／選擇資料夾／切換中）留給後續票。

驗收條目 4（四種視窗尺寸不溢位）依 Context Bundle 指引，透過既有整合測試的 4 個 viewport 案例驗證，未新增測試檔。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext
### 缺失項：main.dart 改動與既有整合測試的直接衝突

- **缺失項**：ticket 指示將 `lib/main.dart` 的 `child: HomePage(repository: repository)` 改為指向路由殼，但 `integration_test/app_test.dart`（不在本票 `where.files`）的全部 8 個測試案例都以 `find.byKey(HomePage.pageKey), findsOneWidget` 作為「App 已成功啟動」的斷言依據（涵蓋首頁煙霧測試、4 種視窗尺寸、2 種語系、3 種工作資料夾狀態）。若把 `home: child` 的 `child` 從 `HomePage` 換成六項導覽的路由殼，`HomePage` 將不再被渲染於根路徑，上述斷言會全數失敗。
- **觸發位置**：`lib/main.dart:63`（`child: HomePage(repository: repository)`）與 `integration_test/app_test.dart` 全檔（多處 `find.byKey(HomePage.pageKey)`）。
- **影響**：驗收條目 4「四種視窗尺寸下導覽列與內容區皆不溢位」——Context Bundle 明示「該檔已涵蓋那四種尺寸，跑既有測試即可驗證，不需新增測試檔」，但這個驗證路徑本身依賴 `HomePage.pageKey` 存在於渲染樹中，與本票要求的路由殼改動互斥。無法在不修改 `integration_test/app_test.dart`（不在 `where.files`）的前提下，同時滿足「main.dart 指向路由殼」與「既有整合測試維持綠燈」。
- **建議補料**：以下二擇一，需 PM 裁決並視需要調整 `where.files`：
  1. 將 `integration_test/app_test.dart` 納入本票 `where.files`，把 `HomePage.pageKey` 斷言改為導覽殼的等價錨點（例如「六個導覽項之一可見」或新增 shell 層級的 Key），本票一併修正該測試。
  2. 本票 `main.dart` 僅新增路由殼定義（`lib/app/router.dart`、`lib/app/shell.dart`）但暫不改動 `main.dart` 的進入點，待後續票（負責整合測試調整）再切換，acceptance 條目「app 進入點指向路由殼」延後。
- **重派成本**：低——路由殼本身（router.dart / shell.dart）可先完成且不受影響；僅 `main.dart` 一行改動與其連鎖的整合測試調整需要 PM 決定範圍後再執行，預估 <30 分鐘。

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-001 | chars: 464 -->

### Task Reference
- 0.1.0-W1-001 what: 接線 Riverpod 至既有的 ScreenUtilInit 巢狀結構（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W1-001 why: tech-decisions Stage 4 已定狀態管理為 Riverpod（理由為與框架的 dart-provider-architecture skill 及 parsley-flutter-developer 代理人生態一致），但 pubspec.yaml 至今無此依賴，lib/ 下只有 main.dart 與 workspace/。沒有骨架，七個畫面無處可掛。（來源已完成，結論見 Solution）

### Related Files
- pubspec.yaml  # from 0.1.0-W1-001（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] pubspec.yaml 含 flutter_riverpod 且 fvm flutter pub get 成功  # from 0.1.0-W1-001（來源已完成，結論見 Solution）
- [ref] [x] ProviderScope 包住 ScreenUtilInit，且 ScreenUtilInit 仍在 MaterialApp 之外  # from 0.1.0-W1-001（來源已完成，結論見 Solution）
- [ref] [x] 既有的 test/ 與 integration_test/ 全數維持綠燈  # from 0.1.0-W1-001（來源已完成，結論見 Solution）
- [ref] [x] 四種視窗尺寸下首頁仍不溢位（960x640 / 1280x800 / 1512x982 / 1920x1080）  # from 0.1.0-W1-001（來源已完成，結論見 Solution）
### 派發前更正：where.files 漏了進入點（2026-08-27，PM）

原 `where.files` 只有 `lib/app/router.dart` 與 `lib/app/shell.dart`。但 app 的進入點在 `lib/main.dart:63`：

    builder: (context, child) => MaterialApp(
      ...
      home: child,
    ),
    child: HomePage(repository: repository),

要讓路由殼成為 app 的起始畫面，必須改該行——**照原範圍執行會被自己的可寫範圍擋住**。已補 `lib/main.dart`。

`ticket track conflicts` 未攔截此項：它比對 `where.files` 欄位，而缺的正是該欄位本身沒宣告的檔案。**檢查器讀 A 欄位，矛盾住在「A 欄位少寫了一項」——欄位不完整時，以該欄位為輸入的檢查沒有任何鑑別力。** 同型缺陷本版已出現三次（W1-001 的 strategy 跨票撞車、W1-009 的驗收要求產出未在範圍內、本票）。

### 由此產生的序列約束

補上 `lib/main.dart` 後，本票與三張票同檔：

| 對方 | 交集 |
|------|------|
| `0.1.0-W1-024` | `lib/main.dart`（裸色碼改引用 token） |
| `0.1.0-W1-014` | `lib/main.dart`、`test/main_test.dart` |
| `0.1.0-W1-016` | `lib/main.dart`、`test/main_test.dart` |

四票不可並行，須序列。`0.1.0-W1-024` 的 `when` 已預先記為「W1-002 路由殼完成後」，順序不變。

### 對執行者的意思

`lib/main.dart` 的改動應**只限於把 `child:` 指向路由殼**。同檔的 `colorSchemeSeed` 裸色碼（第 59 行）屬 `0.1.0-W1-024`，不要順手改——那會使兩票的 diff 混淆歸屬。
### 派發環境（PM 於 2026-08-28 在 fresh worktree 實測）

本票以 `isolation: "worktree"` 派發，工作目錄是全新 checkout。三項已驗事實：

| 項目 | 量測 | 結果 |
|------|------|------|
| FVM 可用性 | `fvm flutter --version` | Flutter 3.47.1，不需重建 `.fvm/` symlink（該目錄 gitignored，`fvm` 讀入庫的 `.fvmrc` 即可解析） |
| 靜態分析 | `fvm flutter analyze` | **No issues found** |
| 測試 baseline | `fvm flutter test` | **All tests passed，8 項** |

`.dart_tool/` 不在版控，**動任何 build 前先跑 `fvm flutter pub get`**（本次實測需安裝，且會報 6 個套件有不相容的新版——那是既有狀態，不是你造成的）。

`lib/l10n/app_localizations*.dart` 是刻意入庫的生成產物，fresh worktree 已有。

**baseline 是綠的**。開工後若見既有測試紅燈或 analyze 有問題，那是本次改動造成的，不是環境噪音——不要歸因於並行資源耗盡（該誤判有前例，見 `IMP-APP-003`）。

### 派發指引

開工第一步執行 `git merge main`——`isolation: "worktree"` 的 base 取 `origin/main` 而非 local main HEAD。派發前 PM 已 push 對齊，本步為保險。

收尾用 `ticket track finish <id> --as parsley-flutter-developer`，**不要用 `complete`**——worktree isolation guard 對 argv 逐元素比對 basename，`complete` 命中 bash builtin 而被條件性阻擋。`finish` 是等價別名。

### 精準 staging 制式句（逐字取自 `ticket track dispatch` 骨架）

原應置於派發 prompt，但該骨架全長 39 行而 `agent-prompt-length-guard-hook.py` 的 `PROMPT_LINE_LIMIT = 30` 為無豁免硬計數，骨架過不了自己的 guard（已建 `0.1.0-W1-020`）。措辭逐字保留，載體改為票面。

```
Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.
```

`{exact files}` 取自本票 `where.files`，`<ticket-id>` 為本票票號。

### 並行狀況

本輪同時派發三張，`where.files` 互不相交（`lib/app/` 與 `lib/main.dart` 與 `lib/tokens/` 與 `lib/l10n/`）。**不要動不屬於本票的路徑**——另外兩位正在同時工作。
### 本票專屬：`main.dart` 的改動限於一行

app 的進入點在 `lib/main.dart` 的 `child: HomePage(repository: repository)`。要讓路由殼成為起始畫面，改的是那一處。

**同檔的 `colorSchemeSeed` 裸色碼屬 `0.1.0-W1-024`，不要順手改**——那會使兩票的 diff 混淆歸屬。`lib/tokens/AppColors` 已存在且該票已排定，不缺人做。

`ScreenUtilInit` 必須維持在 `MaterialApp` 之外（0.0.1 約束，`main.dart` 內有註解說明原因），`ProviderScope` 在最外層（W1-001 已接線）。三者的巢狀順序不可調換。

### 驗收第 4 條的驗證方式

「四種視窗尺寸下導覽列與內容區皆不溢位」——`integration_test/app_test.dart` 已涵蓋那四種尺寸（W1-001 執行時實測 11/11 通過），**跑既有測試即可驗證，不需新增測試檔**。該檔不在本票 `where.files`，若你判斷確實需要新增測試案例，停手以 NeedsContext 上報。

### PM 對 dispatch-readiness 警告的覆核

「acceptance 條目 4 > 2，建議拆分」——**不拆**。四條是同一件工作（交出可切換的殼）的四個驗收面向：導覽項數、切換與佔位頁、浮層入口、不溢位。`where.files` 3 個檔與 Context Bundle tokens 兩項硬閾值皆 PASS。
### PM 裁決（2026-08-28）：採選項 1，`where.files` 已擴充

`integration_test/app_test.dart` 已加入本票 `where.files`（現為四檔）。`ticket track conflicts` 重跑確認無其他票宣告該檔，不製造新交集。

**選項 2（本票先只交出路由殼定義、進入點改動延後）不採**，兩個理由：

1. **它破壞本票自己的驗收條目 2**——「六項皆可切換且各自渲染標示畫面名的佔位頁」無法在未掛載的情況下驗證。交出去的會是無法被否證的產物。
2. 路由殼未掛載即為 dead code，而它要等到「後續票」才活起來——那張票不存在，等於無 trigger 延後（違反 `decision-trigger-binding.md` 規則 1）。

### 為什麼這不算範疇擴張

該整合測試斷言的是 **app 的進入點**（`find.byKey(HomePage.pageKey)` 即「啟動後抵達首頁」）。本票的工作正是更換進入點。**斷言目標必須跟著進入點移動，那是同一個變更的兩半，不是兩件事。**

原 `where.files` 漏列該檔，是票面缺陷而非你越界。PM 派發前的檢查也沒抓到——那套檢查掃的是票面「提及的檔案路徑」，而該檔從未被票面提及（驗收第 4 條寫的是「四種視窗尺寸下不溢位」，以能力描述指稱，不以檔名）。**檢查器讀路徑，而依賴藏在能力描述裡。**

### 對你的具體要求

- **錨點移動而非刪除斷言**：把 `HomePage.pageKey` 換成路由殼的對應 key，保持「啟動後抵達某個確定畫面」這件事仍被斷言。刪掉斷言會讓測試變成只驗「沒有 exception」，那是降級。
- **四種視窗尺寸與三種工作資料夾狀態的覆蓋不得減少**：現況為 4 個 `testWidgets` 宣告（迴圈展開為 11 個執行案例），其中 3 個宣告使用該錨點。改完後執行案例數不應低於 11。
- **語系那個測試（第 71 行）不使用該錨點**，除非你的改動影響它，否則不要動。
- 跑 `fvm flutter test integration_test/app_test.dart -d macos` 確認，並把改前改後的案例數寫進 Test Results。

### 一項數字更正（供你自檢）

你的回報寫「共 8 個測試案例……全部以 `find.byKey(HomePage.pageKey)` 作為核心斷言」。PM 複驗：`testWidgets` 宣告 4 個、`HomePage.pageKey` 出現 3 次、迴圈展開後執行案例 11 個。

**「8」與「全部」兩者都不準**，但阻塞判定不受影響（3/4 個宣告會紅仍是阻塞）。停手上報是對的處置，這裡只更正數字——後續 Test Results 請以複驗後的數字為準。
