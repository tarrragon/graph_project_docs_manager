---
id: 0.1.0-W3-057
title: 同步 元件庫規格範本：互動反應升格為回饋契約、狀態矩陣加來源與同步欄、回饋通道子表
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children:
- 0.1.0-W3-057.1
blockedBy:
- 0.1.0-W3-056
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 同步 元件庫規格範本至方法論新版欄位表：互動反應子節升格為回饋契約、狀態矩陣加來源與同步欄、第 1 章回饋通道子表、第 2 章加兩列
when: 0.1.0-W3-056 與 056.1（方法論 1.14.0）已完成；本票可派
where:
  layer: Presentation
  files:
  - .claude/skills/doc/templates/component-library-spec-template.md
  - .claude/skills/doc/SKILL.md
  - .claude/skills/doc/CHANGELOG.md
why: 範本是契約的產物形狀；方法論升格第十二欄後，範本若無對應子節與欄位，skill 的填寫問句會問到範本沒有格子的東西。範本既有「互動反應」子節（互動／反應／動畫／時間門檻）標為額外承載，是升格的自然落點；狀態矩陣現缺 hover／pressed 列且無來源欄。
how:
  task_type: Documentation
  strategy: '依 0.1.0-W3-056 落成的方法論定義投影，不另設計。**計數處置（DOC-GPD-003）**：範本與 doc SKILL.md 所有「N 欄位契約」字面改為「元件契約欄位表的全部欄位」或直接列欄名，不寫數字。

    1. 檔頭對應表：改為逐欄對照方法論欄位表，新增「回饋契約 = 回饋契約」；「互動反應」自「額外承載」移除。

    2. 第 1 章「回饋通道」列改為子表：列 = 方法論回饋事件表的事件（affordance／focus／press-ack／locate／state-change／等待與結果），欄 = 每形態，格 = 通道或「不可用 + 替代」；表頭註明 CLI 為合法形態。

    3. 第 2 章加兩列：過渡提示觸發點、樂觀更新政策。

    4. 逐元件「狀態矩陣」加「來源」「同步策略」兩欄；預置列補 hover（僅指標形態）、pressed。

    5. 逐元件「互動反應」子節改名「回饋契約」，欄改為：事件／觸發者／反應／動畫 token／時間門檻／回饋通道（引用第 1 章，每形態一欄）／對應狀態邊／填寫者（UX 審查｜規格撰寫者）；預置每個事件各一列；純顯示且無資料態變更者填一列「不適用 + 理由」。

    6. 第 9 章驗收：互動反應項改為回饋契約各事件齊全、狀態矩陣來源與同步欄非空。

    7. doc SKILL.md 模板表描述去數字；doc CHANGELOG 新增 1.13.0 條目、SKILL.md metadata.version 同步。'
acceptance:
- '[x] 檔頭對應表逐欄對照方法論元件契約欄位表且含回饋契約，不寫欄位數；逐元件子節「回饋契約」存在，欄位含觸發者、回饋通道（每形態）、對應狀態邊、填寫者'
- '[x] 第 1 章回饋通道為事件 × 形態子表；第 2 章含過渡提示觸發點與樂觀更新政策兩列'
- '[x] 狀態矩陣模板含來源、同步策略兩欄與 hover、pressed 預置列'
- '[x] 第 9 章驗收項對應更新；範本全檔不再出現欄位計數字面'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T00:17:59'
completed_at: '2026-09-08T00:27:57'
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

同步 元件庫規格範本至十二欄位：互動反應子節升格為回饋契約、狀態矩陣加來源與同步欄、第 1 章回饋通道子表、第 2 章加兩列

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
**basil-writing-critic 二次審查結論**（依 Context Bundle 約束派發）：

審查標的：`.claude/skills/doc/templates/component-library-spec-template.md`（commit 0591f5ea9）本次變更範圍。

結果：通過，附 3 項 Warning 級建議修正（無 Critical）：
1. 「填寫者拆分（用戶裁決）」—— 兩岸用語，改「使用者裁決」
2. 〈回饋通道子表〉CLI 欄三處「終端」—— 臺灣慣用「終端機」，三處皆改
3. 檔頭對應表句「逐元件條目的子節與方法論元件契約欄位表的對應」—— 定語堆疊翻譯腔，拆句為「逐元件條目的子節，對應方法論的元件契約欄位表：」

三明示/資訊優先序（回饋通道子表、回饋契約節說明段落）、簡體字污染、DOC-GPD-003 欄位計數字面殘留（grep 確認無「十一欄」「N 欄」殘留）均無問題。

三項建議已於 commit e29454b2d 修正完畢。

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
**Completion Time**: 2026-09-07T16:23:16Z
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

**變更摘要**（`.claude/skills/doc/templates/component-library-spec-template.md`）：
1. 檔頭對應表：「互動反應」自額外承載移除，補「回饋契約 = 回饋契約」；「十一欄位」全檔改為「元件契約欄位表」（不寫欄位數，依 DOC-GPD-003 計數處置）
2. 第 1 章：「回饋通道」列改指向新增的〈回饋通道子表〉（事件 × 形態，格為通道或「不可用 + 替代」；CLI 明列為合法形態）
3. 第 2 章：新增「過渡提示觸發點」「樂觀更新政策」兩列
4. 第 4 章逐元件「狀態矩陣」：新增「來源」「同步策略」兩欄；預置列補 hover（僅指標形態）、pressed
5. 第 4 章逐元件「互動反應」子節改名「回饋契約」：欄位改為事件／觸發者／反應／動畫 token／時間門檻／回饋通道（每形態一欄）／對應狀態邊／填寫者（UX 審查／規格撰寫者），預置六類事件各一列，並註明純顯示元件填「不適用 + 理由」的唯一合法情境
6. 第 9 章驗收：狀態矩陣項補來源／同步欄與 hover／pressed 檢查；互動反應項改為回饋契約六類事件檢查
7. 全檔已 grep 確認無殘留「十一欄」「互動反應」字面

未動 `.claude/skills/doc/SKILL.md`、`.claude/skills/doc/CHANGELOG.md`——依派發指示由子票 0.1.0-W3-057.1 接續處理。

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-056 | chars: 656 -->

### Task Reference
- 0.1.0-W3-056 what: 擴充 元件庫雙向約束方法論：元件契約欄位表新增「回饋契約」欄（狀態集加來源與同步策略、回饋通道子表、L2 過渡提示觸發點）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-056 why: 現行契約把 hover／pressed／focused（來源：輸入裝置、本地瞬態）與 selected／loading／error（來源：store、有同步問題）混在同一張狀態矩陣，沒有「來源」與「同步策略」維度；回饋通道只定義「按下」一種事件；「系統主動把注意力帶到元件」（scroll-into-view 高亮、新項閃爍）與「資料態 X→Y 當下的提示」是狀態圖的邊而非節點，現行十一欄位無處承載。用戶要求元件庫同時考慮呈現狀態與處理狀態的同步、hover／點擊／定位／狀態變更四類提示、以及 web／app／CLI／桌面系統通知等通道差異，並與狀態管理系統銜接。方法論是判準權威，範本與 skill 皆自它推導，故先改此處。（來源已完成，結論見 Solution）

### Related Files
- .claude/methodologies/component-library-bidirectional-constraint-methodology.md  # from 0.1.0-W3-056（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 元件契約欄位表（不以欄位數命名）存在且含「回饋契約」列，六類事件、觸發者、每事件四項填法、缺失後果齊全  # from 0.1.0-W3-056（來源已完成，結論見 Solution）
- [ref] [x] 狀態集列含「來源」「同步策略」兩項須定義內容  # from 0.1.0-W3-056（來源已完成，結論見 Solution）
- [ref] [x] 全檔同步：不再出現以數字描述欄位表大小的字面（歷史版本紀錄行除外）；檢查清單與 Reference 同步；footer 1.8.0 條目說明動因與 DOC-GPD-003  # from 0.1.0-W3-056（來源已完成，結論見 Solution）
- [ref] [x] 經 basil-writing-critic 二次審查  # from 0.1.0-W3-056（來源已完成，結論見 Solution）
**需求摘要**: 把方法論（0.1.0-W3-056 與 056.1 落成後）的回饋契約欄投影到元件庫規格範本：互動反應子節升格為「回饋契約」、狀態矩陣加來源／同步欄與 hover／pressed 列、第 1 章回饋通道展開為子表、第 2 章加兩列、檔頭對應表與第 9 章驗收同步、doc SKILL.md 模板表描述去數字。how.strategy 已載七點，照投影不重新設計。

**相關檔案**:
- `.claude/skills/doc/templates/component-library-spec-template.md`——主要寫入檔；現有「互動反應」子節（互動／反應／動畫／時間門檻）即升格落點；狀態矩陣預置列現缺 hover／pressed
- `.claude/skills/doc/SKILL.md` 模板表「元件庫規格模板」列、`.claude/skills/doc/CHANGELOG.md`——描述與版本 1.13.0
- `.claude/methodologies/component-library-bidirectional-constraint-methodology.md`〈元件契約判準〉欄位表回饋契約列、〈形態因素先決〉回饋通道子表、〈L2 狀態綁定判準〉——只讀，為投影來源；讀 056／056.1 完成後的版本
- `.claude/error-patterns/documentation/DOC-GPD-003-variable-count-instantiated-in-prose.md`——計數處置

**約束**:
- 範本檔頭註解的對應表改為逐欄對照，不寫欄位數；「互動反應」自「額外承載」句移除
- 回饋契約子節的「填寫者」欄只有兩值：UX 審查（時間門檻、通知形式）／規格撰寫者（通道、狀態來源）
- 純顯示且無資料態變更的元件：回饋契約子節填一列「不適用 + 理由」，範本註解須寫明此為唯一合法不適用情境
- 框架檔不得寫入本專案 ticket ID（規則 8）；doc SKILL.md `metadata.version` 與 CHANGELOG 同步
- 完成後派 basil-writing-critic 二次審查，結論寫入 Solution

**驗收條件**: 見 acceptance。
