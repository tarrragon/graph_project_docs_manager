---
id: 0.1.0-W3-078
title: 實機驗證 MacosScanNotifier 四項平台事實並回填 SPEC-003
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-096
relatedTo: []
spawned_tickets:
- 0.1.0-W3-093
- 0.1.0-W3-094
- 0.1.0-W3-095
- 0.1.0-W3-096
- 0.1.0-W3-097
- 0.1.0-W3-098
- 0.1.0-W3-099
- 0.1.0-W3-100
source_ticket: 0.1.0-W3-074
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager
  history: {}
what: 實機驗證 MacosScanNotifier 四項平台事實並回填 SPEC-003
when: 待定義
where:
  layer: Service + Spec
  files:
  - docs/spec/ui/SPEC-003-interaction-response.md
  - lib/services/macos_scan_notifier.dart::read
  - macos/Runner/AppDelegate.swift::read
why: W3-064 acceptance 2、3 承接，平台事實只有實機能驗
how:
  task_type: Implementation
  strategy: 待定義
acceptance:
- '[ ] 實機測得 UNUserNotificationCenter 在沙盒關閉（PROP-001）+ Developer ID 簽章下的可用性，並記錄 debug build 未簽章時授權請求的實際回應（是否直接回錯）'
- '[ ] Flutter 端載體的選擇已定案並附實測依據（原生 channel 或第三方套件，二擇一）'
- '[x] 實機測得 macOS 上 AppLifecycleState 的 inactive / hidden 各對應何種視窗狀態（失焦／最小化）'
- '[ ] 實機測得撤回已送達通知的 API，以及它對「通知中心已收合」狀態的效果'
- '[ ] 四項實測結果已回填 SPEC-003 §2.2「實作票驗證」段（原文改為實測記錄，附量測環境與日期）'
- '[ ] 0.1.0-W3-064 的 acceptance 2、3 已可勾選或已說明為何仍不可勾'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-08T14:59:30'
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實機驗證 MacosScanNotifier 四項平台事實並回填 SPEC-003

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 實機驗證前置條件（`0.1.0-W3-097` 交接，2026-09-08）

`0.1.0-W3-097` 修復 MethodChannel 註冊後，本票第 1、2、4 項解除阻擋。其執行者交接兩個前置條件，兩者皆為該次多實例事件的直接教訓：

**一、授權狀態只有一次真值。** 第 1 項（未簽章 debug build 的授權請求實際回應）在 `requestAuthorization` 被呼叫的那一瞬間就消耗掉——之後整個 bundle 都停在該次結果上，重跑只會拿到殘留值。`0.1.0-W3-097` 全程未呼叫 `requestAuthorization`、未點過任何授權對話框，**該真值仍在**，但經不起第二次。

執行紀律：跑該項之前先確認起始狀態為 `notDetermined`；那一輪**只跑那一件事**，不與其他項混跑。

**二、通知識別碼是固定字串。** `scanCompleteNotificationIdentifier` = `"scan-complete-notification"`，而第 4 項（撤回已送達通知的 API 效果）要觀察的正是該識別碼的送達與移除。同時執行兩個實例時後發的會覆蓋前面的，觀測到的通知未必來自正在驗的那一個。

**單實例紀律在該項上不是整潔問題，是量測有效性問題。**

**三、觀測管道**（PM 補，來自本票稍早的失誤）：`developer.log` 不進 `flutter run` console，只進 Dart VM Service 的 Logging stream。本專案可觀測性全面採用 `developer.log`（CLAUDE.md §6），因此**驗證期間必須開 DevTools 的 Logging 分頁**，不可依 `flutter run` 的 console 輸出判斷「有沒有發生」。PM 曾據 console 零輸出誤判「通知路徑從未執行」，實際該路徑在 App 啟動當下即已執行並失敗。

**四、掃描時機**（`0.1.0-W3-096` 分析）：掃描掛在 `GapReportNotifier.build()`，首讀者是 App 啟動時的 `ScanNotificationController.start()`，非首次可見 `nav-page-gaps`。因此通知路徑在**啟動當下**即觸發，而非使用者按下重新掃描時。驗證時的觀測時機須以此為準，該缺陷由 `0.1.0-W3-122` 承接、本票不等它。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 實機觀測記錄（2026-09-08，PM 執行）

**量測環境**：macOS 25.5.0（arm64）、Flutter 3.47.1（FVM 釘選）、debug build 未簽章、
App Sandbox 關閉（PROP-001）。觀測管道為 Dart VM Service 的 DevTools Logging 分頁
（`developer.log` 不進 `flutter run` console，見下方「觀測管道」）。

#### 事實三（已測得）：macOS 的 AppLifecycleState 對應

實測轉換序列（時間為當次 session 的實際時戳）：

| 時間 | 轉換 | 對應操作 |
|------|------|---------|
| 15:12:51.371 | `resumed -> resumed` | App 啟動（自轉換） |
| 15:14:56.889 | `resumed -> inactive` | 視窗離開前景 |
| 15:14:57.420 | `inactive -> hidden` | （531 ms 後） |
| 15:18:13.031 | `hidden -> inactive` | 視窗返回 |
| 15:18:13.037 | `inactive -> resumed` | （6 ms 後） |
| 15:18:15.732 | `resumed -> inactive` | 再次離開 |
| 15:18:15.739 | `inactive -> hidden` | （7 ms 後） |
| 15:18:17.556 | `hidden -> inactive` | 返回 |
| 15:18:17.556 | `inactive -> resumed` | （同毫秒） |
| 15:18:23.648 | `resumed -> inactive` | 再次離開 |
| 15:18:24.167 | `inactive -> hidden` | （519 ms 後） |

**結論**：macOS 上 `inactive` **不是可停留狀態，而是過渡態**。離開前景（失焦或最小化）
一律走 `resumed -> inactive -> hidden`，返回一律走 `hidden -> inactive -> resumed`；
兩段間隔在 6 ms 到 531 ms 之間，取決於系統當下負載，不具語意。

**對 SPEC-003 §2.2 的影響**：觸發條件 (a) 寫「完成當下視窗非前景（`AppLifecycleState`
非 `resumed`）」。依本次實測：

1. 條件本身成立且可用——`hidden` 確實表示非前景。
2. 但**任何以 `inactive` 為判斷分支的邏輯在 macOS 上是死碼**，因為它從不停留於該值。
3. **失焦與最小化在此列上不可區分**——兩者終態皆為 `hidden`。若後續需要區分
   「視窗可見但失焦」與「已最小化」，`AppLifecycleState` 不提供該資訊，須另尋來源。

#### 事實一（測得的結果與預期不同）：授權查詢在平台邊界就失敗

原待驗項為「`UNUserNotificationCenter` 在沙盒關閉與 Developer ID 簽章下的可用性」。
實測顯示這一層**根本沒被觸達**——`MethodChannel` 原生端未註冊：

    15:12:51.441 [MacosScanNotifier] Level.WARNING (900)
    查詢授權狀態失敗，原生端未註冊 handler（視為 denied）：
    MissingPluginException(No implementation found for method authorizationStatus
    on channel graph_project_docs_manager/scan_notifier)

因此本次**未能**測得 `UNUserNotificationCenter` 的可用性，也未能測得未簽章 debug build
的授權請求回應——兩者都在 channel 修好之後才有意義。承接票 `0.1.0-W3-097`（P0）。

#### 事實二（部分可答）：Flutter 端載體

載體選擇為原生 `MethodChannel`（`macos/Runner/AppDelegate.swift` +
`lib/services/macos_scan_notifier.dart`），`pubspec.yaml` 無任何第三方通知套件。
此為原始碼事實，不需實機。但「該選擇是否可行」的實測依據仍待 `0.1.0-W3-097` 修復後補。

#### 事實四（未測得）：撤回 API 的效果

需先有已送達的通知才能測撤回，因此同樣阻擋於 `0.1.0-W3-097` 與 `0.1.0-W3-096`。

#### 觀測管道（本次驗證的方法學發現）

`flutter run` 的 console **不顯示 `developer.log` 的輸出**——該 API 寫入的是 Dart VM
Service 的 Logging stream，`flutter run` 只轉送 `print` / `debugPrint`。本專案的可觀測性
策略全面採用 `developer.log`（CLAUDE.md §6），因此**專案的全部 log 在標準開發迴圈中不可見**，
必須開 DevTools 的 Logging 分頁才看得到（本次實測共 244 個 event）。

PM 曾據 `flutter run` console 零輸出推論「通知路徑從未執行」，該推論錯誤：實際上啟動時
即已執行授權查詢並失敗。**以某一管道的輸出缺席推論世界狀態，前提是該管道確實承載該訊號**，
本次未先驗證此前提。此教訓應併入驗證配方（`.claude/skills/verify/` 尚未建立，
session 啟動每次警告「驗證環境未就緒」）。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
---

### 第一次實機驗證（2026-09-09 10:05–10:36，PM 執行）

**量測環境**：macOS 26.5（Darwin 25.5.0）／Flutter 3.47.1（FVM 釘選）／debug build 未簽章／App Sandbox 關閉（PROP-001）／bundle id `com.example.graphProjectDocsManager`。單實例紀律成立：啟動前 `ps` 確認零 App 實例，全程僅一個實例，驗證後關閉。

### 前置條件確認（依 Problem Analysis 的四項交接）

| 前置 | 確認方式 | 結果 |
|------|---------|------|
| 一次性真值仍在 | `plutil -convert xml1 ~/Library/Preferences/com.apple.ncprefs.plist \| grep -c graphProjectDocsManager` | **0 條目**——從未請求過，`notDetermined` 真值完好 |
| 單實例 | `ps aux \| grep graph_project_docs_manager.app` | 啟動前 0、驗證中 1、關閉後 0 |
| 觀測管道 | `flutter run` console 全程無任何 `developer.log` 輸出 | **票面說法成立**，Dart 側日誌只進 VM Service |
| 掃描時機 | 見下方結論 | 啟動當下觸發，但未抵達通知路徑 |

### 觀測結果

| 觀測 | 結果 |
|------|------|
| MethodChannel 註冊 | **成功**——console 於 10:05:11 出現 `[AppDelegate] graph_project_docs_manager/scan_notifier channel 已註冊`（Swift 的 `NSLog` 進 console，`0.1.0-W3-097` 的修復生效） |
| `requestAuthorization` 是否被呼叫 | **否**——`ncprefs` 啟動前後皆 0 條目 |
| 授權對話框 | **未出現**（`osascript` 查 frontmost process 為其他 App，畫面無對話框） |
| 一次性真值 | **未消耗** |

### 結論：通知路徑結構性不可達，已由實機證實

`0.1.0-W3-096` 的分析為真。`lib/screens/gap_report/scan_notification_controller.dart:110` 的觸發條件為 `previous is GapReportScanning && completed && !cancelled`——必須觀測到「從掃描中轉出」這個狀態轉換；而 `gap_report_provider.dart` 的 `_scheduleScan()` 為 `Future.microtask(_completeScan)`，掃描在同一幀的 microtask 內完成，監聽者的 `previous` 永不為 `GapReportScanning`。

**這是確認的否定結果，不是「未能驗證」**：App 確實啟動、channel 確實註冊、路徑確實未被觸達，三者互相佐證。

### 四項的狀態

| # | 項目 | 狀態 |
|---|------|------|
| 1 | 未簽章 debug build 的 `requestAuthorization` 回應 | **阻擋於 0.1.0-W3-096**——觸發條件不可達，呼叫不會發生 |
| 2 | Flutter 端載體選擇 | **部分實證**——原生 channel 已證實可註冊並存活至 runtime；是否足以結案待第二輪 |
| 3 | `AppLifecycleState` 對應 | 先前已完成 |
| 4 | 撤回已送達通知的 API 效果 | **阻擋於 0.1.0-W3-096**——需先有通知送達 |

### 第二輪驗證的執行約束（承接者必讀）

**一、`0.1.0-W3-096` 修好之後，下一次啟動 App 就是第 1 項的量測本身。** 屆時觸發條件成立，`requestAuthorization` 會在啟動當下被呼叫，一次性真值於該瞬間消耗。**不得在未準備好觀測的情況下啟動 App。**

**二、`0.1.0-W3-122` 必須維持押著，不可先於本票完成。** 122 未修時掃描在 App 啟動當下執行，而啟動時可見頁不是 `nav-page-gaps`，觸發條件因此成立——那正是本票唯一的受控觀測窗口。先修 122 會關掉它。此約束已寫入 `0.1.0-W3-096` 的派發。

**三、第 1 項不需要 Dart 日誌即可觀測**（本輪的方法改進）：未簽章若直接回錯，對話框不會出現且 `ncprefs` 保持 0 條目；若正常運作，對話框出現且 `ncprefs` 產生條目。兩者在畫面與檔案系統上皆可分辨，不依賴 DevTools。第 4 項（撤回效果）仍須人眼觀察通知中心。


---

**[PM 更正 2026-09-09] 第一次驗證的結論句推論過當，證據本身無誤。**

上方「結論」段寫：「通知路徑在 `previous is GapReportScanning && completed && !cancelled` 這道閘門即已擋下，**未抵達後續的 `authorizationStatus()` / `requestAuthorization()`**。若曾抵達，`ncprefs` 不可能維持 0。」

**該推論的前半無效。** `authorizationStatus()` 是唯讀查詢，**不會在 `ncprefs` 留下條目**；只有 `requestAuthorization()` 會。因此 `ncprefs` 條目數為 0 只證明 **`requestAuthorization()` 未被呼叫**，對 `authorizationStatus()` 是否被呼叫**零鑑別力**。

以一個只對事件 B 成立的副作用，同時否證了事件 A 與事件 B。

**量測值本身無誤，受影響的只有結論的範圍**：
- 仍然成立：`requestAuthorization()` 未被呼叫，一次性真值未消耗
- 不再成立：「未抵達 `authorizationStatus()`」——該事實目前**未知**

**這個未知正是第二輪要量的東西。** `0.1.0-W3-096` 落地時附的診斷測試（`test/unit/screens/gap_report_startup_notification_test.dart`，不 override `gapReportProvider`、走真實 `GapReportNotifier` 與真實 `ScanNotificationController`）綠燈，`authorizationStatusCalls == 1`，並給出一項與計時器種類無關的結構性論證：`ScanNotificationController.start()` 在 `AppShell.initState` 同步執行，其中 `listenManual(gapReportProvider, ...)` 觸發首次 `build()`；監聽註冊與該次 build 屬同一次同步呼叫，完成回呼必然在 `start()` 返回後才執行。**舊版 `Future.microtask` 同樣滿足此保證。**

於是形成一個明確的落差：**測試環境下舊版邏輯也應在啟動時呼叫 `authorizationStatus()`，而真機舊版沒有走到 `requestAuthorization()`。** 兩者可以並存（`authorizationStatus()` 可能被呼叫且回傳非 `notDetermined`、或拋錯、或平台通道回應與測試替身不同），但要分辨是哪一種**只能靠實機**——這正是本票第 1 項「未簽章 debug build 的授權請求實際回應」要回答的事。

**第二輪的觀測項因此增加一項**：除了對話框是否出現、`ncprefs` 是否產生條目之外，還要能分辨 `authorizationStatus()` 是否被呼叫及其回傳值。該事實**不留檔案系統副作用**，只在 `developer.log`——因此第二輪**需要 DevTools Logging 分頁**，或以其他能讀取 VM Service Logging stream 的方式捕捉。前一則「第 1 項不需要 Dart 日誌即可觀測」的方法改進**僅對「對話框是否出現」有效，不涵蓋本項**。

---

### 第二次實機驗證（2026-09-09 11:51，PM 執行，用戶在場觀測）

**量測環境**：同第一次，但 `0.1.0-W3-096` 已落地（`Future.microtask` → `Future<void>.delayed(Motion.spinnerMinVisible, ...)` + 世代號）。以 `flutter run -d macos --start-paused` 啟動，使 VM 停在 `main()` 之前，待 DevTools 連上 Logging stream 後才 resume——確保啟動期日誌零遺漏（第一次驗證因 DevTools 晚連而無 Dart 側日誌）。

### 決定性觀測：通知路徑有走到，平台回 `denied`

DevTools Logging 分頁（用戶提供原始 `logRecord`，`error` 與 `stackTrace` 皆為 `null`，確認為成功路徑而非例外收斂）：

| 時間 | logger | 訊息 |
|------|--------|------|
| 11:51:36.942 | `MacosScanNotifier` | 查詢授權狀態 |
| 11:51:37.171 | `MacosScanNotifier` | 授權狀態：`NotificationAuthorization.denied` |

**這推翻了第一次驗證的推論並解釋了它。** 第一次量到 `ncprefs` 條目數為 0，我推論為「通知路徑未抵達 `authorizationStatus()`」——**該推論已於前一則更正中撤回，本次證實路徑確實抵達了**。

`ncprefs` 為 0 的真正成因是：`authorizationStatus()` 回傳 `denied`，程式碼因此走 `denied` 分支，**結構上不會呼叫 `requestAuthorization()`**，故系統偏好中不產生任何條目。

### 平台事實（acceptance 第 1 項的答案，限未簽章 debug build）

**未簽章 debug build 下，`UNUserNotificationCenter.getNotificationSettings` 回報的 `authorizationStatus` 不是 `.notDetermined`，而是會被 `AppDelegate.authorizationString(for:)` 映射為 `"denied"` 的值。**

三項佐證使此結論不依賴推測：
1. 日誌來自成功路徑（`授權狀態：$status`），非三個例外分支（那些印「查詢授權狀態失敗（視為 denied）」），且 `logRecord.error` 為 `null`
2. `AppDelegate.swift` 的 `.notDetermined` 明確映射為 `"notDetermined"`，Dart 端 `_parseAuthorization` 對該字串亦有專屬分支——若平台回 `.notDetermined`，日誌會顯示 `notDetermined`
3. `~/Library/Preferences/com.apple.ncprefs.plist` 中本 App 條目數**驗證前後皆為 0**——系統沒有任何使用者層級的拒絕紀錄，卻回報等同拒絕的狀態

**因此在此組態下 `notDetermined` 結構性不可達。** 本票 Problem Analysis 交接的「授權狀態只有一次真值、經不起第二次」前提**在未簽章 build 上不成立**——該真值不是被消耗，是從未可達。真值仍完好（`ncprefs` 仍為 0），但要量測它必須改用 Developer ID 簽章的 build，那屬 `0.1.0-W1-015`（發布配方）的範圍。

### 附帶確認：`0.1.0-W3-096` 的修復在實機成立

通知路徑抵達 `authorizationStatus()` 這件事本身即證明 `previous is GapReportScanning && completed` 閘門通過——`state-gaps-scanning` 在實機上確實成為可觀測的狀態轉換。第一次驗證時該閘門未通過（`ncprefs` 為 0 且無任何 `MacosScanNotifier` 日誌）。

### 未觀測項與其成因

**`denied` fallback 的 SnackBar 是否出現：未觀測。** 用戶當時在看 DevTools 螢幕，SnackBar 會自動消失，事後無法回溯。

`appLifecycleStateProvider` 初始值為 `AppLifecycleState.resumed`（`lib/app/app_lifecycle.dart:16`），故 `_fallbackToSnackbar` 在 11:51:37 當下應走「立即顯示」分支而非延後分支。SnackBar 依規格應顯示 `scanCompleteSnackbarMessage` 並帶 `viewGapsAction` 按鈕。

**此項可重跑，不消耗任何一次性資源**——授權狀態已是 `denied`，重新啟動 App 會再走一次同一條 fallback 路徑。

### 獨立缺陷：`_showDeniedSnackbar` 零日誌

`lib/screens/gap_report/scan_notification_controller.dart` 的 `_showDeniedSnackbar()` **沒有任何 `developer.log`**，其第一行 `if (!context.mounted) return;` 的靜默早退亦不留痕跡。

後果是：SnackBar 出現與靜默失敗兩種情形**在日誌上完全同形**，唯一的分辨手段是人眼即時觀察，而 SnackBar 會自動消失。這使一個使用者可見的回饋動作本身不可觀測——與 `observability-rules` 規則 5、以及 `ARCH-GPD-001`「日誌不抵扣使用者回饋」的反面（使用者回饋不留日誌，故失效時無人知道）同源。本項獨立於 SnackBar 是否實際出現，兩種結果下都成立。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-08 17:16)
  - what: W3-078 拆解：第 3 項平台事實（AppLifecycleState inactive/hidden 對應視窗失焦與最小化）與掃描時機無關，可立即實機驗；第 1、2、4 項設 blockedBy 指向真實掃描串接票
  - why: W3-096 分析確認 W3-078 四項平台事實可驗性不同質：第 3 項只需啟動 App 後失焦／最小化，shell.dart 的 didChangeAppLifecycleState 已有 log；第 1、2、4 項需通知真的送出，繫於掃描具真實耗時。整張票一起卡住會使可驗的部分也停擺
  - suggested_type: ADJ
  - suggested_priority: P1
  - related_files: docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-078.md,docs/spec/ui/SPEC-003-interaction-response.md
  - context: 
  - status: dismissed（0.1.0-W3-096 建議拆解 078；PM 改採更輕的處置：第 3 項（AppLifecycleState 對應）已於 2026-09-08 實機測得並回填 Solution、acceptance 已勾，本票 blockedBy 收窄為 0.1.0-W3-097（MethodChannel 未註冊）單一項，不另拆票）


## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-074 | chars: 1213 -->

### Task Reference
- 0.1.0-W3-074 what: 為跨 app 邊界的功能補上測試分類（框架層：tdd 五層分工表缺測試形態軸；專案層：三個呼叫點的驗證形態與 W3-064／W1-036 待驗項承接），並評估改進成果的落點（改既有 skill 或新建）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-074 why: **框架層缺口**：`tdd/references/layered-test-strategy.md` 的五層分工表以**架構層級**為軸（UI／Application／UseCase／Interface／Domain），缺**測試形態**軸。跨 app 邊界的功能在表上落於 Layer 1（整合測試），但該格指定的方法做不到——`integration_test` 無頭執行，觸不到系統權限對話框、通知中心、原生檔案選擇器、真實 process。表上有位置、位置上的方法無效，且無任何條文說明此情形，讀者依表執行會得到「已覆蓋」的假象。

**專案層實例**：CLAUDE.md §6 定測試策略為雙層（`test/` 契約 + `integration_test/` 行為），本專案三個跨邊界呼叫點（`macos_scan_notifier.dart` 系統通知、`workspace_repository.dart` 資料夾存取、`gap_report_screen.dart` 外部開啟與 doc CLI process）**只有 fake 測試**，平台行為從未被驗證。SPEC-003 §2.2「實作票驗證」段明文要求四項平台事實由實機測得後回填，0.1.0-W3-064 的 acceptance 2、3 因此未勾；`ExternalOpener`（W1-036）同類。`.claude/skills/verify/` 不存在，session 啟動每次警告「驗證環境未就緒」而無人承接。

**為何是框架問題而非單票問題**：三個功能落在同一個縫隙，逐票各自決定會產生三種不一致的做法；且此缺口與載體無關（任何有平台互動的專案都會撞到），替換專案名稱與路徑後仍成立。（來源已完成，結論見 Solution）

### Related Files
- docs/tech-decisions.md  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- CLAUDE.md  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- integration_test::read  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- test::read  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- lib/workspace/workspace_repository.dart::read  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] 跨邊界判準已定義，並判定它與既有五層的關係（新的一層或正交屬性），該判定的依據寫入結論  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- [ref] [x] layered-test-strategy 表已補形態軸或等效結構；doc-handoff 的分工框架已檢查並同步或說明不需同步  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- [ref] [x] 落點評估兩案（改既有 skill／新建 skill）各附可執行切分與四項判準的比較，並給結論  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- [ref] [x] 本專案三個呼叫點逐項標驗證形態；決議寫入 tech-decisions，CLAUDE.md §6 同步或說明不需；W3-064 與 W1-036 的待驗項各有承接票或 spawn 落地紀錄  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
