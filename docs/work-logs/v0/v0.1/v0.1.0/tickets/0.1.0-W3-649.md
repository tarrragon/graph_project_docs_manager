---
id: 0.1.0-W3-649
title: 清倉 0.1.0 pending 池中 50 張可攜票對 canonical issue 的重複
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 閘門判準實測顯示 41% 的 pending 是可攜問題
  final_decision: 先分群再逐群查重，不逐張查
  rationale: 今日 5 張框架票全指向 4 張 issue，逐張查會重複命中同一批；分群同時解決用戶指出的另一個症狀——同一 BUG 的三四種變體各自成票、重複描述同一問題
who:
  current: saffron-system-analyst
  history: {}
what: 把 0.1.0 pending 池中 where.files 全數落在 .claude/ 下的 50 張票按根因分群，逐群對 canonical 查重，命中者關票並 observe、未命中者分群待裁
when: 收斂分析定案後立即
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets
why: '以 W3-648 的閘門判準實測：0.1.0 的 123 張 pending 中，50 張（41%）的 where.files 全數落在 .claude/ 之下，屬可攜的框架問題卻坐在專案版本的待辦池裡，且主題標籤多被歸為 0.1 App 地基。本 session 的小樣本顯示框架票的重複率極高——13 張框架票中至少 11 張在 canonical 已有 open issue（#102／#54／#105／#104），其中一張從零實作了 #102 早已完整分析過的修法。這 50 張累積得更久，而同期 canonical 的 issue 已增至 109 張，重複率預期更高。不先清倉，後續無論做主軸或做框架都是在一個已知含大量重複的池上排優先級'
how:
  task_type: Analysis
  strategy: 四段：(1) 以 where.files 全數落在 .claude/ 為判準重跑清單，逐張取 title 與 why 首句，按根因分群——今日實測同一根因會裂成三到六張票，逐張查重會重複命中同一批 issue，故先分群；(2) 每群跑一次 dedup，關鍵字取該群的根因詞而非票面標題字面；(3) 命中且判定為重複者，以 close --reason not_executable_knowledge_captured --resolved-by <issue ref> 關票，並把該群在本 consumer 的症狀與已有實證 observe 至該 issue；命中但屬切分或引用關係者依協定檔三種關係處置；(4) 未命中者按群輸出待裁清單，含建議的 issue 標題與該群票號，不自行 init。產出為一張分群對照表（群／票號／根因一句／查重結論／處置）
acceptance:
- '[ ] 50 張已按根因分群，每群有一句根因描述與票號清單'
- '[ ] 每群已跑 dedup 並記錄查重結論；命中且判定重複者已關票並 observe，關票的 resolved-by 指向該 issue'
- '[ ] 未命中的群已輸出待裁清單（建議 issue 標題 + 票號），未自行 init 新 issue'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-21'
updated: '2026-09-21'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

把 0.1.0 pending 池中 where.files 全數落在 .claude/ 下的 50 張票按根因分群，逐群對 canonical 查重，命中者關票並 observe、未命中者分群待裁

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending
