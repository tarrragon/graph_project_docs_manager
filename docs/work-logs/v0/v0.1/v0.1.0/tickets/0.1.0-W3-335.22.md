---
id: 0.1.0-W3-335.22
title: 凍結 閘門組複驗題目、餵入範圍與判定門檻（V1–V4）
type: ANA
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶裁示凍結題目
  final_decision: 建 ANA 子票出題，不讀修補後文件
  rationale: 出題者看過修補結果會無意識出容易過的題目
who:
  current: saffron-system-analyst
  history: {}
what: 凍結 閘門組複驗題目、餵入範圍與判定門檻（V1–V4）
when: W3-346 完成前（題目須在最後一次變更前固定）
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-332.2.md::read
why: 用戶裁示（2026-09-14）：現在凍結複驗題目與門檻。W3-302 方法要求題目在修補前逐字固定，本輪修補已先進行，V1–V3 只能以絕對門檻驗；W3-346 仍會變更 schema，須在它完成前寫死題目，避免出題者看到修補結果而調整題目
how:
  task_type: Analysis
  strategy: 只讀 W3-332.2～.10 票面（修補前的位置表、分歧模擬、猜測點表）與 W3-335 Context Bundle 的 V1–V4 設計，不讀任何修補後的 skill 或 spec 檔。V1 從各矛盾位置出題；V2、V3 從各 skill 位置表每張修補票抽 3 題；V4 沿用 W3-332.8／.9／.10 的走查題與允許輸入（新增 docs/usecases），逐點列為待判定。每題寫明餵入檔案範圍（以檔名與章節名指定，拆檔後依 SKILL.md 路由取得）、逐字題目、判定門檻
acceptance:
- '[x] V1 每個矛盾位置各一題，含會觸發兩條原規則的輸入與逐字問句'
- '[x] V2、V3 每張修補票各 3 題，題目取自修補前的分歧模擬，未參考修補後文件'
- '[x] V4 沿用三批走查的逐點題目，允許輸入含 UC 文件，並列出依賴 W3-352 路徑表的已知必猜點以免計為失敗'
- '[x] 每批寫明樣本數、判定門檻與「2/3 對 1/3 不下結論」的限制'
- '[x] 凍結時間與 commit 記錄於票面，之後不得修改題目'
tdd_phase: phase0
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T15:16:56'
completed_at: '2026-09-14T15:28:12'
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

凍結 閘門組複驗題目、餵入範圍與判定門檻（V1–V4）

---

## Problem Analysis
### 問題根因

本票為出題票，非問題修正。需從 W3-332.2~.10 票面（修補前的位置表、分歧模擬、猜測點表）提取素材，依 W3-335 Context Bundle 的 V1-V4 設計逐批凍結題目、餵入範圍與判定門檻。

### 影響範圍

已讀票面：W3-332.2（component-contract-design）、W3-332.3（test-assertion-design/tdd）、W3-332.4（doc spec templates）、W3-332.5（dart-style-guardian）、W3-332.6（ux-design-evaluation）、W3-332.7（foundation-design/version-bootstrap）、W3-332.8（先導走查 §4）、W3-332.9（走查 §1-§3）、W3-332.10（走查 §5-§8）。未讀任何修補後的 skill、spec、UC 或 domain-map 檔案。

### 相關 Error Pattern

無直接相關。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
### 矛盾位置識別方法

從 W3-332.2~.7 的位置表中，篩選分歧類型為「規則交界」且分歧模擬顯示兩條原規則文字明確互斥（非邊界模糊）的位置。判準：同一 skill 或同一模板內兩處條文對同一輸入各給一個答案，且兩答案不可同時成立。

---

### V1 凍結題目——矛盾統一（10 題）

**樣本數**：每位置 3 份獨立探針。
**判定門檻**：3/3 產出一致才算生效；任一不同即未生效。
**限制**：2/3 對 1/3 不下結論。

### V1-01（來源：W3-332.5 #14）

**餵入範圍**：dart-style-guardian SKILL.md〈Typography System〉`.rsp` 段 + references/typography-system.md〈Manual Responsive Text〉

**題目**：一個 Dart 檔案含 `fontSize: 14.rsp`。依本文件，這是正確寫法還是違規寫法？請引用依據句。

### V1-02（來源：W3-332.5 #11）

**餵入範圍**：dart-style-guardian references/i18n-guidelines.md〈Violation 2〉Fix 段 + dart-style-guardian SKILL.md〈Common Violations and Fixes〉Violation 6 Fix 段

**題目**：一個非 widget 層的 Dart 函式需要回傳錯誤訊息給使用者。依本文件，應以 `throw Exception(context.l10n!.genericError)` 直接攜帶已翻譯字串，還是改用 ErrorHandler 在呈現層才翻譯？請引用依據句。

### V1-03（來源：W3-332.5 #6）

**餵入範圍**：dart-style-guardian references/spacing-system.md〈Non-Standard Values〉 + 同檔〈Common Replacements〉

**題目**：一個元件使用 `SizedBox(height: 20)`。〈Common Replacements〉寫 `20 → md`。〈Non-Standard Values〉對 20 列出 md 與 lg 兩個候選。依本文件，應替換為 md 還是 lg？請引用依據句。

### V1-04（來源：W3-332.6 #17）

**餵入範圍**：ux-design-evaluation references/error-recovery.md〈錯誤訊息的兩個職責〉三層結構段 + 同檔〈Error loop 的逃生口〉

**題目**：一個輕微錯誤（如「無法載入頭像，使用預設圖」）。〈三層結構〉說輕微錯誤只需標題加一個行動（重試）。〈Error loop 的逃生口〉要求每個 error 畫面至少兩個選項。依本文件，此錯誤應放幾個行動按鈕？請引用依據句。

### V1-05（來源：W3-332.6 #24）

**餵入範圍**：ux-design-evaluation references/interaction-feedback.md〈三層回饋模型〉第 2 層段 + 同檔〈時間門檻與回饋策略〉表

**題目**：一個查詢操作 p90 延遲為 250ms。〈三層回饋模型〉第 2 層說 100ms-10s 為等待指示（spinner/loading）。〈時間門檻與回饋策略〉表說 100-400ms 只用動畫過渡。依本文件，此操作應掛 spinner 還是只用動畫過渡？請引用依據句。

### V1-06（來源：W3-332.7 F15）

**餵入範圍**：foundation-design references/dimension-product-notes.md〈非 SaaS 專案的形態轉換〉段 + foundation-design SKILL.md〈權威缺席時〉段

**題目**：權威存在但形態不符且無可用部分。dimension-product-notes 說「確認無可用部分時標無」。SKILL.md 說「判不出來一律待定」。依本文件，產物欄應填「無」還是「待定」？請引用依據句。

### V1-07（來源：W3-332.7 F19）

**餵入範圍**：foundation-design references/handoff-mode.md 四步程序第 2 步段 + foundation-design references/examples.md〈萃取不等於照抄〉段

**題目**：七種實測間距值為 4, 8, 12, 16, 20, 24, 32。handoff-mode 第 2 步說「逐一給名字，值不變」。examples.md〈萃取不等於照抄〉說歸納為離散階。依本文件，應保留七個原始值各給名字，還是歸納為少於七個的離散階？請引用依據句。

### V1-08（來源：W3-332.4 #13）

**餵入範圍**：doc skill templates/component-library-spec-template.md §4〈狀態矩陣〉error 列「同步策略」欄預填值 + §2「樂觀更新政策」同步策略三選一定義

**題目**：填寫元件狀態矩陣的 error 列「同步策略」欄。error 列預填值為「回滾提示指向回饋契約」。§2 同步策略定義為三選一（樂觀更新/悲觀等待/離線佇列）。預填值不在三選一之中。依本文件，error 列應填預填值還是從三選一中挑一個？請引用依據句。

### V1-09（來源：W3-332.4 #3）

**餵入範圍**：doc skill templates/component-library-spec-template.md §1 引言「同形態內不依寬度自動切版」 + §4〈尺寸契約〉「視窗尺寸 N 下的行為」選項

**題目**：桌面形態的小視窗（960px 寬）下，側欄元件需要收合。§1 禁依寬度自動切版。§4 尺寸契約的行為選項含「換版型」。依本文件，小視窗下側欄收合應填「換版型」（違反 §1）還是填「維持」並交由容器 collapse（§4 選項不含此值）？請引用依據句。

### V1-10（來源：W3-332.6 #3）

**餵入範圍**：ux-design-evaluation references/screen-state-matrix.md〈四欄定義〉退出路徑列 + ux-design-evaluation SKILL.md〈Core Pillars〉

**題目**：connecting 狀態會在連線成功後自動轉為 connected。〈四欄定義〉退出路徑列含「完成後自動轉場」。〈Core Pillars〉要求「使用者可自行觸發」。依本文件，connecting→connected 的自動轉場算有退出路徑還是死胡同？請引用依據句。

---

### V2 凍結題目——缺引導（18 題，6 組×3 題）

**樣本數**：每位置 3 份獨立探針。
**判定門檻**：3/3 產出一致且皆能指出依據句。
**限制**：2/3 對 1/3 不下結論。

### V2-A 組：component-contract-design（來源：W3-332.2 缺引導位置）

**V2-A1（位置 #3）**
餵入：component-contract-design references/step-1 全文
題目：一份畫布上，清單卡與篩選工具列在四個畫面共現、另兩個畫面只有清單卡。「畫布是主要來源」中的「主要」如何判定？依本文件應走路徑 A 還是 B？請引用依據句。

**V2-A2（位置 #11）**
餵入：component-contract-design references/step-2 路徑 C 步驟 1 全文
題目：一個按鈕的背景色來自全域主題的 primaryColor。三態標記為「已定義於程式碼」「隱含於呼叫端」「空白」。這個值不來自元件本身也不來自呼叫端，而是框架主題預設。依本文件應標哪一態？請引用依據句。

**V2-A3（位置 #24）**
餵入：component-contract-design references/step-5 全文
題目：一張元件票已有 3 個 commit 觸及元件 A 與元件 B 兩個契約。「已 claim 且實作中的票拆不動時改用整條目投影」。何謂「拆不動」？依本文件如何判定？請引用依據句。

### V2-B 組：test-assertion-design / tdd（來源：W3-332.3 缺引導位置）

**V2-B1（位置 #3）**
餵入：tdd references/doc-handoff.md〈Phase 1 輸入映射〉UC 替代/例外場景列
題目：一個替代場景的分岔點橫跨主場景步驟 3 與步驟 4。此替代場景應歸屬步驟 3 的種子、步驟 4 的種子，還是另立獨立種子？依本文件如何判定？請引用依據句。

**V2-B2（位置 #10）**
餵入：tdd references/doc-handoff.md〈檢查清單〉 + 〈UC 步驟→GWT 轉換規則〉全文
題目：一條 FR 寫「系統應在使用者閒置 30 分鐘後自動登出」，但此 FR 無對應的 UC 步驟。如何產生 GWT 種子？依本文件如何處理無 UC 步驟承接的 FR？請引用依據句。

**V2-B3（位置 #14）**
餵入：tdd references/test-object-catalogue.md〈二、來源類別〉+〈四〉C3 來源唯一
題目：「使用者點擊確認後系統儲存訂單」同時載於 UC 步驟 5 與畫面狀態矩陣的 confirmed→saved 轉換。C3 要求來源唯一。兩個來源取哪一條？依本文件如何判定？請引用依據句。

### V2-C 組：doc spec templates（來源：W3-332.4 缺引導位置）

**V2-C1（位置 #5）**
餵入：doc skill templates/component-library-spec-template.md §2 狀態綁定模式「最小重繪邊界」列
題目：一個購物車圖示需在商品數量變動時更新。「rebuild 邊界標準寫法」欄為佔位。依本文件，最小重繪邊界的判定依據是什麼？如何填寫此欄？請引用依據句。

**V2-C2（位置 #18）**
餵入：doc skill templates/component-library-spec-template.md §4〈回饋契約〉引言「填寫者」欄與時間門檻
題目：填寫元件回饋契約的時間門檻欄，但 UX 審查尚未產出。依本文件，應留空、暫填估計值、還是阻擋元件票直到 UX 審查完成？請引用依據句。

**V2-C3（位置 #33）**
餵入：doc skill templates/component-library-spec-template.md §8 跨平台命名契約「權威端」
題目：一個按鈕元件在 iOS 與 Android 同時實作，先實作的是 Android，但設計交付物的 Figma 檔以 iOS 為基準。依本文件，權威端應指定 iOS 還是 Android？請引用依據句。

### V2-D 組：dart-style-guardian（來源：W3-332.5 缺引導位置）

**V2-D1（位置 #1）**
餵入：dart-style-guardian SKILL.md〈與相鄰資產的交界〉component-contract-design 列 + component-contract-design SKILL.md 同名節
題目：一個 Dart 檔案直接使用 `Container()` 而非元件庫封裝版。兩處都宣稱本 skill 偵測「原生元件直用」。依本文件，何謂「原生元件」？哪些框架元件算原生？請引用依據句。

**V2-D2（位置 #3）**
餵入：dart-style-guardian SKILL.md〈與相鄰資產的交界〉version-bootstrap 列含「見〈Project Calibration〉的 baseline 說明」 + SKILL.md〈Project Calibration〉全節
題目：首次接入 dart-style-guardian hook 時，既有程式碼有 200 處違規。「見 baseline 說明」。依本文件，baseline 的設定方式與格式為何？請引用依據句。

**V2-D3（位置 #12）**
餵入：dart-style-guardian SKILL.md〈Internationalization〉全段 + references/i18n-guidelines.md 全文
題目：元件庫內一個空狀態元件的預設訊息「目前沒有項目」寫在元件內部。此文案的 ARB key 應由元件擁有還是由呼叫端傳入？依本文件如何判定？請引用依據句。

### V2-E 組：ux-design-evaluation（來源：W3-332.6 缺引導位置）

**V2-E1（位置 #6）**
餵入：ux-design-evaluation references/screen-state-matrix.md〈檢查清單〉段
題目：有三條路由只能從 deep link 進入，沒有 UI 入口。「明確標記為 deep link／程式內部專用」。依本文件，此標記應寫在 router 定義旁、矩陣新欄、還是規格附表？請引用依據句。

**V2-E2（位置 #20）**
餵入：ux-design-evaluation references/error-recovery.md〈Degraded mode〉進入與退出的防抖動段
題目：一個功能的單次請求逾時為 5 秒。進入 degraded mode 需「連續 N 秒失敗」。依本文件，N 的取值規則為何？請引用依據句。

**V2-E3（位置 #28）**
餵入：ux-design-evaluation references/interaction-feedback.md〈按鈕級回饋〉元件語意與版面檢查第 1 項
題目：一個按鈕在「收藏」與「已收藏」之間切換，切換時畫面其他結構不變。三種消歧做法並列（拆分、switch、動詞句）。依本文件，應選哪一種？請引用依據句。

### V2-F 組：foundation-design / version-bootstrap（來源：W3-332.7 缺引導位置）

**V2-F1（位置 F9）**
餵入：foundation-design SKILL.md〈工作流〉步驟 3「改寫產物」段 + 形態 2 段
題目：改寫產物後走形態 2，形態 2 又說走改寫產物。依本文件，改寫後產物的合格判準為何？何時停止循環？請引用依據句。

**V2-F2（位置 F22）**
餵入：foundation-design references/handoff-mode.md 末段「三種情形抽取不是純命名」
題目：一個共用實例被兩處 mutate（寫入不同欄位）。handoff-mode 列出此情形但未給處置。依本文件，應如何抽取此共用實例？請引用依據句。

**V2-F3（位置 B11）**
餵入：version-bootstrap references/version-shift-sop.md 盤點步驟 5 全文
題目：consumer 專案的 CLAUDE.md 不含「強制操作 2」，且專案無對應 blog。「依 CLAUDE.md 強制操作 2…讀 blog」。依本文件，此步驟應跳過還是阻擋？請引用依據句。

---

### V3 凍結題目——缺範例（18 題，6 組×3 題）

**樣本數**：每則 3 份獨立探針。
**判定門檻**：驗證段合規 3/3；新輸入產出一致 ≥ 2/3。
**限制**：2/3 對 1/3 不下結論。

### V3-A 組：component-contract-design（來源：W3-332.2 缺範例位置）

**V3-A1（位置 #5）**
餵入：component-contract-design references/step-2 路徑 A 步驟 2「元件候選」段
新輸入：六個畫面中五個出現「商品圖＋價格標籤」組合，一個畫面只有價格標籤。
題目 a：依本文件，「商品圖＋價格標籤」應判「總是一起出現→合一」還是「有時一起出現→slot」？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容（框架 API 名、特定語言語法）？

**V3-A2（位置 #10）**
餵入：component-contract-design references/step-2〈畫布漂移〉段
新輸入：一個畫布上 Tab 的排列順序為「首頁、搜尋、個人」，規格寫「首頁、個人、搜尋」。
題目 a：Tab 排列順序屬視覺值（取畫布）還是行為（取規格）？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-A3（位置 #17）**
餵入：component-contract-design references/step-3〈關卡一：掛載點判準〉段
新輸入：一個純本地的計算機元件，按下等號後顯示結果，不呼叫任何後端服務。
題目 a：「拔掉服務後按下是否仍看得到反應」——此元件無後端服務，此問句是否適用？若不適用，關卡一結果為「通過」還是「跳過」？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

### V3-B 組：test-assertion-design / tdd（來源：W3-332.3 缺範例位置）

**V3-B1（位置 #5）**
餵入：tdd references/doc-handoff.md〈UC 步驟→GWT 轉換規則〉轉換方法段
新輸入：UC 步驟 5 寫「系統驗證訂單並(a)扣除庫存(b)發送確認信」，含兩個預期結果。
題目 a：此步驟應轉為一個 GWT 種子（含兩個 Then）還是拆為兩個種子（5a、5b）？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-B2（位置 #8）**
餵入：tdd references/doc-handoff.md〈整合測試 vs 單元測試分工〉判斷原則段
新輸入：UC 步驟「使用者點擊儲存後系統寫入資料庫並更新畫面」。
題目 a：「存入後查得到」的正確性由自己、串接還是配置決定？應歸整合測試還是單元測試？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-B3（位置 #21）**
餵入：test-assertion-design SKILL.md〈判斷軸〉+〈斷言品質三問〉段
新輸入：一個非同步定時器的測試斷言 `expect(await timer.elapsed, lessThan(Duration(seconds: 2)))`。
題目 a：依三問歸確定性失效（環境類處置），還是依分族歸斷言設計（選「等最終狀態」）？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

### V3-C 組：doc spec templates（來源：W3-332.4 缺範例位置）

**V3-C1（位置 #7）**
餵入：doc skill templates/component-library-spec-template.md §3 元件清單總表引言（容器判別問句）
新輸入：一個卡片內含圖示與標題，圖示在左、標題在右，排列由卡片自身決定。
題目 a：圖示與標題的相對位置由「頁面本身決定」還是由「卡片」決定？需不需要另立「圖示標題列」容器？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-C2（位置 #12）**
餵入：doc skill templates/component-library-spec-template.md §4〈狀態矩陣〉「來源」欄
新輸入：一個「確認」按鈕在表單未填完時為 disabled。disabled 由表單驗證結果推導。
題目 a：此 disabled 屬互動瞬態（本地處理）還是資料態（store 欄位驅動）？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-C3（位置 #21）**
餵入：doc skill templates/component-library-spec-template.md §4〈內容政策〉「最長測試文案」
新輸入：一個按鈕文案有繁中「確認送出」與英文 "Confirm and Submit" 兩個語系。
題目 a：「最長測試文案」欄應填 i18n key、最長語系的實際字串、還是字元數？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

### V3-D 組：dart-style-guardian（來源：W3-332.5 缺範例位置）

**V3-D1（位置 #9）**
餵入：dart-style-guardian SKILL.md〈五類約束〉顏色列 + references/color-system.md 各 Usage 欄
新輸入：一個「刪除」按鈕的背景色。刪除是破壞性動作（歸 negative），但刪除成功後的「已刪除」提示呢？
題目 a：「已刪除」提示的色彩應歸 negative（破壞性動作結果）還是 positive（操作成功）？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-D2（位置 #10）**
餵入：dart-style-guardian references/i18n-guidelines.md〈Exceptions〉段
新輸入：程式碼含 `Text('ISBN: $isbn')`。「ISBN:」標籤後有空格與冒號。
題目 a：整句免 i18n（技術識別符例外），還是「ISBN:」標籤需翻譯（使用者可見文字，冒號全形半形因語系不同）？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-D3（位置 #8）**
餵入：dart-style-guardian references/color-system.md〈Best Practices〉4「Prefer theme colors」段 + SKILL.md 全檔
新輸入：一個 Dart 檔案的背景色寫 `Theme.of(context).colorScheme.surface`。
題目 a：依本文件，應保留 Theme.of 還是改為 token 類別（如 UIColors.surface）？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

### V3-E 組：ux-design-evaluation（來源：W3-332.6 缺範例位置）

**V3-E1（位置 #2）**
餵入：ux-design-evaluation references/screen-state-matrix.md〈填寫步驟〉步驟 1 段
新輸入：一個表單畫面有三種錯誤訊息（欄位格式錯誤、伺服器錯誤、網路逾時），但版面佈局與可用操作皆相同。
題目 a：應合為一列 error 還是分為三列？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-E2（位置 #14）**
餵入：ux-design-evaluation references/input-mechanism.md〈場景二：搜尋〉觸發模式表
新輸入：搜尋十萬筆本地資料庫（SQLite 全文搜尋）。
題目 a：「本地」判低成本即時觸發，還是「DB 全文搜尋十萬筆」判高成本按送出觸發？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-E3（位置 #25）**
餵入：ux-design-evaluation references/interaction-feedback.md〈時間門檻與回饋策略〉延遲是分布段
新輸入：一個操作平均 300ms、p90 800ms、p95 1.5s。
題目 a：對表取 p90（800ms，落 400ms-1s 帶）還是 p95（1.5s，落 1-10s 帶）？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

### V3-F 組：foundation-design / version-bootstrap（來源：W3-332.7 缺範例位置）

**V3-F1（位置 F3）**
餵入：foundation-design SKILL.md〈產物欄的五種合法答案〉段
新輸入：一個維度的權威存在但形態不符，無可用部分。
題目 a：產物欄應填「無」還是「待定」？依據為何？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-F2（位置 F17）**
餵入：foundation-design references/dimension-product-notes.md〈改寫產物是主路徑〉推導問句段
新輸入：一個 CLI 專案，UI 維度的「失效」是什麼？
題目 a：依推導問句「這個維度要防的失效在本專案長成什麼樣子」，CLI 專案的 UI 維度失效是什麼？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

**V3-F3（位置 B1）**
餵入：version-bootstrap SKILL.md〈Step 2〉UI 類提案判別段
新輸入：一條 FR 寫「系統於終端顯示進度條」。
題目 a：此 FR 含「顯示」但不含「畫面」「頁面」「元件」「介面」「UI」。「等」為開放列舉。依本文件，此 FR 算 UI 類還是非 UI 類？產出什麼並引用依據句。
題目 b：修補後新增的範例（若有），其驗證段是否只使用產出段已定義的欄位？是否含技術相依內容？

---

### V4 凍結題目——SPEC 回寫與稽核（4 題）

**樣本數**：每畫面 1 份。
**判定門檻**：原猜測點仍需猜者 0；新猜測點逐項歸因，屬 spec 缺描述者建追修票。
**限制**：2/3 對 1/3 不下結論。

**允許輸入**：修補後的 skill 文件（經 SKILL.md 路由取得拆檔後結構）+ SPEC-001 + SPEC-003 + docs/usecases/。**不給 SPEC-004**。

**已知必猜點（探針無法解決，不計為失敗）**：
- W3-332.9 G1（右欄寬度數值）：spec 未給數值
- W3-332.10 G1（無破洞 i18n key）：i18n key 由 SPEC-004 定義，不在探針餵入範圍
- W3-332.10 G6（專案項卡片欄位明細）：SPEC-004 自行設計，無 SPEC-001/003 上游

### V4-01 §4 Ticket 清單（來源：W3-332.8 猜測點 G1-G5）

餵入：修補後畫面鏈 skill + SPEC-001 §4 + SPEC-003 §3.4 + docs/usecases/。不給 SPEC-004。
題目：對 §4 的六個狀態逐狀態盲推導元件選件與排位、契約關鍵欄、Given/When/Then 驗證。逐項記錄被迫猜測之處。
原猜測點逐項判定：G1（N 值來源）、G2（預估耗時計算方式）、G3（載入提示卡元件形態歸屬）、G4（進度條形態，spec 層）、G5（取消按鈕位置，spec 層）。

### V4-02 §1 Domain 視圖（來源：W3-332.9 猜測點 G1-G5）

餵入：修補後畫面鏈 skill + SPEC-001 §1 + SPEC-003 §3.1 + docs/usecases/。不給 SPEC-004。
題目：對 §1 的九個狀態逐狀態盲推導。逐項記錄被迫猜測之處。
原猜測點逐項判定：G1（右欄寬度，**已知必猜點**）、G2（事件標籤排列）、G3（泳道列視覺，spec 層）、G4（BlockingState 版本值矛盾，spec 間）、G5（ExpandablePanel 內容，spec 層）。

### V4-03 §2 UC Flow + §3 追溯視圖（來源：W3-332.9 猜測點 G6-G12）

餵入：修補後畫面鏈 skill + SPEC-001 §2-§3 + SPEC-003 §3.2-§3.3 + docs/usecases/。不給 SPEC-004。
題目：對 §2 三狀態與 §3 三狀態逐狀態盲推導。逐項記錄被迫猜測之處。
原猜測點逐項判定：G6（UCBasicInfo 欄位，spec 層）、G7（檢視關聯動作在未結構化狀態是否渲染）、G8（StepTable 欄位排列，spec 層）、G9（UCSelector 是否存在，spec 層）、G10（TreeNode 展開預設，spec 層）、G11（四層連線視覺，spec 層）、G12（虛線框放置邏輯，spec 層）。

### V4-04 §5-§8（來源：W3-332.10 猜測點 G1-G7）

餵入：修補後畫面鏈 skill + SPEC-001 §5-§8 + SPEC-003 §3.5-§3.7 + §2.13 + docs/usecases/。不給 SPEC-004。
題目：對 §5 三狀態、§6 四狀態、§7 三狀態逐狀態盲推導，另對 §8 判準適用結果表逐列判定。逐項記錄被迫猜測之處。
原猜測點逐項判定：G1（無破洞 i18n key，**已知必猜點**）、G2（掃描範圍說明結構，spec 層）、G3（節點詳情工具列容器）、G4（消失狀態元件 vs 空狀態變體）、G5（cancelDeadline 超時行為，spec 層）、G6（專案項卡片欄位，**已知必猜點**）、G7（§8.2 事件窮盡性，spec 層）。

---

### 統計摘要

| 批 | 題數 | 樣本/題 | 判定門檻 |
|----|------|---------|---------|
| V1 | 10 | 3 | 3/3 一致 |
| V2 | 18（6組×3） | 3 | 3/3 一致且引用依據句 |
| V3 | 18（6組×3） | 3 | 驗證段合規 3/3；產出一致 ≥ 2/3 |
| V4 | 4（逐畫面） | 1 | 原猜測點仍猜 = 0；已知必猜點 3 項豁免 |

凍結時間：2026-09-14T15:17（本次寫入時間）。題目凍結後不得修改。

### 自檢結果

本票為出題票，產出為凍結題目寫入票面，無程式碼、無檔案修改、無 spawn 規劃。

- 命名規範：不適用（無程式碼）
- 架構一致性：不適用
- 依賴方向：不適用
- 測試：不適用
- 可觀測性：不適用

multi_view_status: n_a
reason: 本票為出題凍結，產出為逐字題目寫入自身票面，不改任何檔案，不產生設計決策，無需多視角審查

### 後續 Ticket 說明

本票產出為凍結題目，題目的執行（派探針）由父票 0.1.0-W3-335 統籌，不在本票建子票。本票無分析結論需轉化為修復或防護措施。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "V1-V4 四批題目已凍結寫入票面 Solution，含逐字題目、餵入範圍、樣本數與判定門檻"
confidence: 0.95
acceptance_met: [1, 2, 3, 4, 5]
acceptance_unmet: []
artifacts: []
context_dependencies:
  - "0.1.0-W3-332.2 至 .10 票面（位置表、分歧模擬、猜測點表）"
  - "0.1.0-W3-335 Context Bundle〈複驗探針設計〉"
estimated_recovery_effort: ""
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
**Completion Time**: 2026-09-14T15:28
**Executing Agent**: saffron-system-analyst
**Review Status**: pending
