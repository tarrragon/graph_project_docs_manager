---
id: 0.1.0-W3-258
title: 建立 既有階段產出於前置變更後的重新覆核檢查點
type: ANA
status: closed
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-207 接手時發現 Phase 1 型別定義段因前置票合併而靜默失效，並行 session 歸納為一般形態
  final_decision: 建 ANA 票分析檢查點落層，與 0.1.0-W3-257（撰寫層）分工並互引
  rationale: 兩個實例分屬型別定義段與驗收條件兩種載體、外部變更來源一為他人票合併一為自身拆分，單一實例不足以定位檢查點該放哪一層
who:
  current: 主線程
  history: {}
what: 建立 既有階段產出於前置變更後的重新覆核檢查點
when: 隨時；0.1.0-W3-257（撰寫層）不阻塞本票，兩者互引
where:
  layer: Infrastructure
  files:
  - .claude/pm-rules/tdd-flow.md::read
why: '**形態**：一份已完成階段的產出，其正確性依賴當時的外部狀態；該狀態之後改變，產出靜默失效，而**沒有任何檢查點會在下一階段開工前要求覆核**。失效不發出訊號——檔案還在、語法正確、grep 命中、測試不紅。


  **兩個實例，分屬不同載體，各自獨立發生於 2026-09-10**：


  | 載體 | 實例 | 外部變更來源 |

  |------|------|------------|

  | **型別定義段** | `0.1.0-W3-207` Phase 1 §2.2 以內嵌方式定義 `enum AttentionLevel`；其後 `0.1.0-W3-205` 把同一列舉落在 `lib/app/attention_level.dart` 並合併進 main。兩份成員名與宣告順序**逐字相同**（`discardable`／`mustLeaveTrace`／`undroppable`） | 前置票合併（他人的票） |

  | **驗收條件** | `0.1.0-W3-176` 的 acceptance 第 4 項要求「重現實驗 A 與 B 兩組情境、兩者日誌可區分」；PM 把 G2 拆出後該項變成**要求執行者做他做不到的事**（A 與 B 的唯一差異即 G2 的可見時長） | 本票自身被拆分（自己的動作） |


  **同名同值同順序比語意衝突難發現**：兩份一致時任何測試都不會紅，直到某天有人只改了其中一份。第二個實例則是由下游執行者（Phase 1 的 lavender）發現並上報，不是由任何檢查發現。


  **為何 TDD 流程接不住**：`tdd-flow` 的階段轉換表定義 Phase N → N+1 的移交要件，但**要件檢查的是本階段產出是否齊備，不檢查前階段產出所依賴的外部狀態是否仍成立**。Phase 1 的正確性在產出當下成立，之後由外部變更靜默失效。


  **與 `0.1.0-W3-257` 的分工**（並行 session 建立，`reference-stability-rules` 規則 12：量測與關係判定須帶時戳或失效條件）：`257` 在**撰寫層**——要求寫下的斷言自帶失效條件；本票在**流程層**——要求階段轉換時有人去檢查那些條件。兩者不重疊，互引。


  **該 session 補的一項判準本票採用**：這一族須分兩種——**原生斷言**（無可重算來源，如「本票不阻塞任何票」）與**衍生資料**（有權威來源可重算，如 Context Bundle，`claim` 會重抽、過期窗口有界）。**只有原生斷言需要規則，衍生資料自帶修復。**'
how:
  task_type: Analysis
  strategy: 'PM 前台。先做重現實驗（ANA 強制）：以本票 why 的兩個實例為對象，實查現行 `tdd-flow` 階段轉換表與 `ticket` CLI 的 `phase` / 拆票路徑，確認**確實不存在**任何要求覆核前階段產出的檢查點——不以「我沒看到」代替實查。


    分析三問：

    1. **檢查點該放在哪一層**？候選：階段轉換時（`ticket track phase` 的前置檢查）、派發時（`dispatch-readiness` 增一項）、或撰寫時（併入 `0.1.0-W3-257` 的規則層）。三者的偵測時機與漏網形態不同。

    2. **可機械判定的部分有多少**？「前置票已 completed 且其產出與本階段的型別定義段重疊」也許可由工具偵測（比對 `blockedBy` 票的 `where.files` 與本票 Solution 提及的識別符）；「acceptance 因拆票而失效」則可能只能靠人。**能機械化的部分不該留給人記得。**

    3. **原生斷言與衍生資料的界線在本專案的實例上如何落**：兩個實例分別屬哪一類，是否都需要規則。


    **本票不改任何框架資產**。裁定後依結論建 DOC／IMP 承接票；判定不需要新規則者，逐項說明理由。


    完成後依 `.claude/skills/framework-issue/SKILL.md` 評估是否 observe——兩條 AND 判準初判成立（抽象可攜：任何有階段移交的流程皆有此形態；根源在 `.claude/` 通用資產：`tdd-flow` 與 ticket CLI），**尚須跑 `dedup`**，並評估是否與 `0.1.0-W3-257` 併為同一則觀測。'
acceptance:
- '[ ] 已實查 tdd-flow 階段轉換表與 ticket CLI 的 phase／拆票路徑，確認檢查點不存在，非以未見代替實查'
- '[ ] 檢查點的落層已裁定，三個候選各附偵測時機與漏網形態的比較'
- '[ ] 可機械判定與只能靠人的部分已分開，能機械化者未被留給人記得'
- '[ ] 兩個實例各已歸入原生斷言或衍生資料，並說明是否需要規則'
- '[ ] 本票未改動任何框架資產，承接動作已建票或逐項標註無需建票的理由'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
closed_at: '2026-09-10T14:00:22'
closed_by: none
close_reason: not_executable_knowledge_captured
close_reason_note: 分析 context 已遷至 tarrragon/claude#56（區段：當前結論（graph-project-docs-manager：條文缺口與方法論驗證債））
---

# Execution Log

## Task Summary

建立 既有階段產出於前置變更後的重新覆核檢查點

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 兩例的可機械化程度差距，須分開下結論（並行 session 指出，PM 覆核成立）

原 how 第 2 問把兩例並列問「可機械判定的部分有多少」。**兩例的答案差距比原文暗示的大，合併下結論會讓難的那例拖住容易的那例。**

| 實例 | 觸發源 | 可機械化程度 |
|------|-------|------------|
| `0.1.0-W3-176` acceptance 因拆票失效 | **本票內部**（自己的拆分動作） | **高**——偵測點可綁在拆票這個動作上：拆票時把母票 acceptance 逐條標為待重評。不是「只能靠人」 |
| `0.1.0-W3-207` §2.2 因前置票合併失效 | **外部票**（他人的票合併） | **低**——原 how 提的比對法（`blockedBy` 票的 `where.files` 與本票 Solution 提及的識別符）**會漏掉此例**：`205` 的產出是**新建**一個檔案，`207` §2.2 是**內嵌宣告**同名列舉，識別符相同（`AttentionLevel`）但檔案路徑不同，`where.files` 交集為空。要抓到它得比對**識別符**而非路徑，而識別符須從 Solution 的程式碼區塊裡抽——已非 grep 層級 |

**因此本票的結論須分開下**：拆票路徑可綁動作、機械可行；前置票合併路徑先只落為派發前的人工檢查點，機械化另議。

### 第三個載體：同一張票內的欄位之間（2026-09-10 追加）

`0.1.0-W3-236` 的 `why` 寫「本票做裁定」，而今日追加的 Problem Analysis 寫「本票只產出證據、裁決歸 `210`」——**同一份票面並存兩套相反的分工敘述**。成因是 PM 收窄範疇時只追加了說明段，沒回頭改 `why` 與 `how`。

由下游讀者（`graph-project-docs-manager-86` 讀票準備調整 `210`）發現並直接來問，**不是任何檢查發現的**。

此例使本票的載體清單成為三種：**型別定義段**（跨票）、**驗收條件**（跨階段）、**同票欄位之間**（票內）。第三種的偵測條件與前兩種不同——它不需要跨票比對，兩段相反的文字就在同一個檔案裡。

### 偵測構想：同票欄位互相矛盾（來自 `graph-project-docs-manager-86`，2026-09-10）

本節應該票持有者之邀寫入（該 session 表示可直接寫、不需經其轉交）。**僅為構想，未實作、未驗證誤報率**，是否採用由持有者判斷。

**這一類的可偵測性優於另外兩類。** 型別定義段與驗收條件的失效需要語意比對；同票欄位矛盾有一個**純結構的訊號**：

> 票面出現了標題含「更正」「收窄」「撤回」「改寫」的追加小節，而 frontmatter 的 `why` 或 `how` 在該小節加入之後**未被修改過**。

判別不需讀懂內容，只需兩個時間戳：

```bash
# 更正小節首次出現的 commit
git log --diff-filter=A -S'### PM 更正' --format=%ct -- <ticket.md> | tail -1
# frontmatter how 區塊最後一次變動的 commit
git log -L '/^how:/,/^acceptance:/:<ticket.md>' --format=%ct | head -1
```

後者早於前者即命中。

**為何這個訊號有效**：追加更正小節的人，是在已經知道原文有問題的狀態下動筆的。他若同時改了 `why`／`how`，兩者就一致；他若只追加，就是把矛盾留在票裡。**時間順序直接編碼了「知道了但沒回頭改」這件事**，不需推測意圖。

**已知的誤報來源**（提出者未量測，僅列出）：

1. `why` 是建票當下的決策記錄，**本來就不該改寫**（`0.1.0-W3-236` 的持有者即以此為由保留 `why` 原文，只改 `how` 並在更正節註明以何者為準）。**這是正確處置卻會命中訊號**——除非把「更正節內含『以 X 為準』的優先序宣告」列為豁免。
2. 更正小節可能只涉及 Solution 或 Test Results，與 `why`／`how` 無關。
3. 標題用詞不在列舉內（「補記」「附記」「修訂」）則漏檢；擴充列舉會提高誤報。

**提出者的實例**：`0.1.0-W3-236` 的 `why` 稱本票做裁定、Problem Analysis 的今日更正稱裁決歸他票，兩段各自讀都成立、合起來相反。下游（本 session）依 `why` 收窄了自己的票，方向相反於持有者的意圖，直到直接詢問才對齊。**發現者是下游讀者，不是任何檢查**——與本票已記的另外兩個實例同形。

**與豁免 1 的關係值得先想清楚再實作**：若「保留 `why` 原文 + 更正節宣告優先序」是正確處置，那訊號要偵測的其實不是「未回頭改」，而是**「未回頭改且未宣告優先序」**。後者才是真正的缺陷形態，且同樣是純結構可判（更正節內是否含優先序宣告字串）。
## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending
