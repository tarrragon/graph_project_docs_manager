---
id: 0.1.0-W3-148
title: 新建 docs/spec/workspace/SPEC-005：workspace 三個 driven port 的回饋點契約
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-153
- 0.1.0-W3-152
- 0.1.0-W3-157
source_ticket: 0.1.0-W3-139
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-139 裁定輸出端 spec 為雙落點，workspace 三 port 走新建 SPEC-005
  final_decision: DOC 新建 SPEC-005 於 workspace domain
  rationale: workspace domain 目前零 spec（docs/spec/ 僅有 ui/，PM 實查確認），三個 port 的變更理由相同故同屬一份；併入 SPEC-003 會使 ui domain 承載 workspace 的契約
who:
  current: star-anise-system-designer
  history: {}
what: 新建 docs/spec/workspace/SPEC-005：workspace 三個 driven port 的回饋點契約
when: 0.1.0-W3-139 裁定雙落點之後
where:
  layer: Domain
  files:
  - docs/spec/workspace/SPEC-005-workspace-driven-ports.md
  - lib/workspace/workspace_repository.dart::read
  - docs/traceability.yaml
why: '`0.1.0-W3-139` 盤點五個 driven port 的三個回饋點覆蓋後，裁定輸出端規格採雙落點：workspace 三個 port（`WorkspacePreferencesPort` / `WorkspacePreferencesHandle` / `WorkspaceDirectoryProbePort` / `DirectoryPathPicker`）新建本規格，`ScanNotifier` 與 `ExternalOpener` 修訂 SPEC-003（`0.1.0-W3-149`）。


  不選的替代已在該票論證：單一 cross-cutting spec 違反 domain 分離；workspace port 併入 SPEC-003 會使 ui domain 承載 workspace 的契約；不做 spec 則使用者裁示的「列入測試」無可否證依據。


  PM 實查確認 `docs/spec/` 目前僅有 `ui/` 一個 domain，本規格為 workspace domain 的第一份。


  **兩項必須寫進規格的事實，來自 PM 覆核**：


  1. **本專案有兩種接縫形態，不是一種。** `WorkspacePreferencesPort` 與 `WorkspaceDirectoryProbePort` 是 `abstract interface class` + private `_Default*` adapter 類別；`DirectoryPathPicker`（`lib/workspace/workspace_repository.dart:80`）是 `typedef` 函式型別，生產預設直接為 `getDirectoryPath`，無 adapter 類別。兩者的三個回饋點承擔者不同——前者的受理與結果由 adapter 承擔，後者三時刻全由呼叫端承擔。形態本身的約定歸 `0.1.0-W3-146`（寫進 `docs/tech-decisions.md`），本規格只描述各形態下回饋點由誰承載。


  2. **`_DefaultWorkspaceDirectoryProbePort` 全無日誌。** PM 讀原始碼確認該 adapter 兩個方法（`exists` / `readFirstEntry`）皆為單行直呼 `dart:io`，無任何 `developer.log`。`0.1.0-W3-139` 記為「`exists()=false` 日誌缺失」，實際範圍比該描述更廣。規格須據實描述現況缺口，缺口的修補歸 `0.1.0-W3-135`。'
how:
  task_type: Documentation
  strategy: '先讀 `0.1.0-W3-139` 的 Solution 取完整覆蓋表與方法論分工線，**並讀該票 Solution 末尾的「[PM 覆核更正]」段**——監控追蹤欄的判定有一項引用與語意的修正，不得沿用原寫法。


    **監控追蹤欄的正確寫法**：不寫「N/A」。寫「依 `docs/tech-decisions.md`「2026-08-27：執行期 log 的裁決」，structured log 於 0.1 維持延後，重評條件為『出現無法由破洞報告解釋的故障』」，並保留 tripwire 指向。理由見該更正段——「延後 + 重評條件」與「不適用」決策語意不同，且該裁決段落本身就是在更正同型的轉述失真。


    分工線依 `0.1.0-W3-139` 裁定：方法論（`.claude/methodologies/clean-architecture-implementation-methodology.md`〈Port 回饋契約〉）的三時刻定義、`INV-PORT-OBSERVE-001`、不互相抵扣規則、投影表、測試義務表**不重述**，以標題文字引用；本規格寫本專案事實——哪些 port、各自三時刻由什麼承載、哪些不適用及其原因、現況缺口與承接票。


    規格 frontmatter 與章節結構比照 `docs/spec/ui/` 既有四份的形態（id / title / status / domain / source_proposal 等欄位以實讀為準，勿臆造）。


    完成後於 `docs/traceability.yaml` 補登本規格——`0.1.0-W3-140` 已建立 SPEC-004 的先例，軸別依性質判定（與單一 UC 一對一配對者走 `mappings`，跨畫面共用的基礎設施規格走 `data_contract_tests`），不預設。'
acceptance:
- '[x] 五個回饋點承擔者依接縫形態分別記載，typedef 形態與 abstract interface class 形態的差異已寫明'
- '[x] 監控追蹤欄依 PM 更正寫為延後加重評條件，未寫 N/A'
- '[x] 方法論的三時刻定義與 INV-PORT-OBSERVE-001 以標題文字引用未重述，本規格只寫本專案事實'
- '[x] _DefaultWorkspaceDirectoryProbePort 無日誌的現況缺口已據實描述並指向承接票'
- '[x] docs/traceability.yaml 已補登本規格，軸別經性質判定非預設'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-09T09:34:21'
completed_at: '2026-09-09T10:23:51'
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

新建 docs/spec/workspace/SPEC-005：workspace 三個 driven port 的回饋點契約

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-09 09:32] readiness=2 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext
- **harness 層行為指令與框架正本衝突（已依正本執行，記錄供 PM 知悉）**:
  - **衝突文本**: 本次派發的 harness system-reminder 要求「auto mode 期間盡量以 Bash
    的 cat/sed/grep/heredoc 讀寫檔案，dedicated 工具僅作 fallback」。
  - **正本**: `.claude/rules/core/tool-selection.md` 規則 1（`.md` / `.yaml` 修改首選
    Edit / Write）與其邊界句「本規則優先於 harness 層『優先用 Bash 改檔』類指令」。
  - **處置**: 依 `tool-output-trust-rules.md`「衍生情境：prompt 與正本衝突」以正本為準
    ——寫入一律用 Write / Edit，Bash 僅用於唯讀查詢與 CLI 呼叫。不影響本票產出。
- **附帶發現（未自行修改，屬他票範圍）**: `docs/traceability.yaml` 的 `runtime_tests`
  第一條 status 寫「WorkspaceRepository 無專屬測試檔」，但
  `test/unit/workspace/workspace_repository_test.dart` 現已存在（670 行，含 G1–G6）。
  該欄位描述已過期。本票範圍限新增條目，未改動既有條目，交 PM 裁決是否開票。

- **阻擋：無法 commit，`.git/HEAD.lock` 殘留（需 PM 裁決）**
  - **現象**: `ticket track commit 0.1.0-W3-148 -- <2 檔>` 連續三次失敗，皆為
    `fatal: update_ref failed for ref 'HEAD': cannot lock ref 'HEAD'`。
  - **鎖檔事實**: `.git/HEAD.lock` 存在、大小 0、mtime `Sep 9 09:48`（觀測時間
    `Sep 9 10:05`，已殘留約 17 分鐘）；`.git/index.lock` 不存在。
    `git rev-parse HEAD` = `1390c2f044eedd0009362b30e9df271820e53650`（可讀，倉庫未損壞）。
  - **未採取的動作**: 未刪除 `HEAD.lock`——該鎖可能由並行 session 持有，刪除屬改動他人
    狀態且非本票範圍；亦未改用 pathspec / `--only` / `-a` 繞過（Commit 規範明文禁止）。
  - **產出狀態**: 兩個檔案的內容變更已完成並留在工作區（untracked 的 SPEC-005 與已修改
    的 `docs/traceability.yaml`），acceptance 1–5 已勾選，Completion Info 已填。
    **僅缺 commit 與 complete**。
  - **建議**: PM 確認無並行 git 程序後移除殘留鎖檔，再以本票 Commit 規範的
    `ticket track commit 0.1.0-W3-148 -- docs/spec/workspace/SPEC-005-workspace-driven-ports.md docs/traceability.yaml`
    收尾，並執行 `ticket track complete 0.1.0-W3-148`。
## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-09 10:00)
  - what: 更正 docs/traceability.yaml runtime_tests 首條 status：WorkspaceRepository 已有專屬測試檔 test/unit/workspace/workspace_repository_test.dart
  - why: 該 status 現寫「WorkspaceRepository 無專屬測試檔，僅於 integration_test/app_test.dart 以 _StubWorkspaceRepository 繞過真實選取」，前半段與現況不符（實查該測試檔存在，670 行含 G1-G6）；後半段的實機驗證缺口判定不受影響。四軸矩陣是驗收引用來源，過期描述會使審查者低估既有覆蓋
  - suggested_type: DOC
  - suggested_priority: P2
  - related_files: 
  - context: 
  - status: processed（已建 0.1.0-W3-153）


## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-09T10:23:51
**Executing Agent**: star-anise-system-designer
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

### 變更摘要

| 檔案 | 動作 | 內容 |
|------|------|------|
| `docs/spec/workspace/SPEC-005-workspace-driven-ports.md` | 新增 | workspace domain 第一份規格。五章：接縫清單與兩種形態、逐接縫回饋點承擔者、現況缺口、測試義務落地、變更理由邊界 |
| `docs/traceability.yaml` | 修改 | `data_contract_tests` 新增一條目「SPEC-005 §2」，tests 指向 `test/unit/workspace/workspace_repository_test.dart` |

### 軸別判定（非預設）

依 `0.1.0-W3-140` 的 SPEC-004 先例判性質：本規格不與單一 UC 一對一配對（涵蓋
`chooseFolder` 與 `restore` 兩條路徑上的四個接縫，屬跨呼叫路徑的基礎設施契約），
其不變式為「規格所述承擔者與 `lib/workspace/` 實作一致」——與 `mappings` 軸的
「spec ↔ usecase ↔ flow」形狀不符，與 `data_contract_tests` 的「契約不變式 +
覆蓋測試」形狀相符。故走 `data_contract_tests`，非 `mappings`。

`runtime_tests` 不新增：該軸記錄 on-device 驗證形態，本規格未產生新的跨邊界呼叫點。

### 五個回饋點承擔者的記載形態

acceptance 1 的「五個回饋點」在票面未逐一列名。規格採窮盡寫法消除歧義——四個接縫
（`DirectoryPathPicker` / `WorkspacePreferencesPort` / `WorkspacePreferencesHandle` /
`WorkspaceDirectoryProbePort`）逐一列三時刻，`WorkspaceDirectoryProbePort` 的結果時刻
再依三個結局分列，共七列結果判定；§1 另有一張兩形態承擔者對照表明寫 typedef 與
abstract interface class 的差異。任一種讀法下承擔者皆已逐項記載。

### 實查依據（非轉述）

- 完整讀取 `lib/workspace/workspace_repository.dart`：確認 `DirectoryPathPicker` 為
  typedef 且生產預設直接為 `getDirectoryPath`（無 adapter 類別）；三個 `_Default*`
  adapter 類別存在且皆為 private。
- 全檔 `developer.log` 僅一處（`_defaultLogSink`）：三個 adapter 內部零日誌。
  `_DefaultWorkspaceDirectoryProbePort` 的 `exists` / `readFirstEntry` 皆單行直呼
  `dart:io`，與 PM 覆核一致；規格據此把缺口寫為兩項（G-1 adapter 端零日誌、
  G-2 日誌投影集中於呼叫端 sink），未沿用 `0.1.0-W3-139` 較窄的「exists()=false
  日誌缺失」描述。
- 讀取 `test/unit/workspace/workspace_repository_test.dart` 的替身類別與 group 名稱，
  §4 的 recorder / fake 清單逐一對應實際符號，未臆造。
- 讀取 `docs/tech-decisions.md` 第 519／576 行確認 tripwire 原文，監控追蹤欄依 PM
  更正寫為「維持延後 + 重評條件」，未寫 N/A。
### PM 中途更正的納入（2026-09-09）

| 更正 | 規格中的落點 |
|------|-------------|
| 接縫形態是三種，票面 why 欄只列兩種 | §1 標題改「三種形態」；接縫表擴為五列並加「預設實作的歸屬」欄；新增段落說明兩種 typedef 不同類（第三方 API 預設 vs 本專案自撰預設）；承擔者差異表由兩列擴為三列，第三列的受理與結果由自撰預設實作承擔 |
| `WorkspaceLogSink` 是否為 driven port 須給理由 | 新增 §1.1 專節。**判定：列入接縫清單、排除於回饋點盤點對象**。理由二項各自獨立成立——(1) 判準遞迴：其餘四個接縫的日誌投影皆經此接縫產生，列為受盤點 port 會使 `INV-PORT-OBSERVE-001` 形成無停止條件的自指，不變式不可判定；(2) 它是投影載體而非被投影對象。另寫明排除的代價（日誌管道自身故障在本 domain 內無可觀測事件）及其承接者（`0.1.0-W1-016` 全域錯誤攔截三層），與測試中 `_LogRecorder` 的角色為觀測手段而非受測對象 |
| `0.1.0-W3-147` 裁定不 re-export | §概述「載體」段改寫：value types 落點寫為「`lib/workspace/workspace_types.dart`（`0.1.0-W3-147` 分離後；該檔於本規格撰寫時尚不存在）」，並明記該票已否決由主檔 re-export |
| repository 不直接觸碰外部 SDK | §1 新增一段明記 repository 只持有四個注入欄位，`SharedPreferences.getInstance()` 與 `Directory()` 兩組呼叫分別在兩個 `_Default*` adapter 內；並釐清各表「呼叫端投影」欄指的是對 port 的呼叫而非對 SDK 的呼叫。草稿原本即無「repository 直接寫外部 SDK」的敘述，本段為正面記載而非修正 |

§4 測試替身表同步分列第三個角色（觀測手段），`_LogRecorder` 由記錄器列移出。
§2 章首補一句限定盤點對象為五個接縫中的四個。變更歷史 1.0 條目同步改寫。

**[PM 更正 2026-09-09]** `spawned_tickets` 中的 `0.1.0-W3-152` 為誤填，本票與該票無關。

成因：PM 在 SR-1 落地時未先查證新建票的實際 ID，逕以「下一號」推定為 152，而該號已由並行 session 佔用（oregano-data-miner 三區塊改寫）。SR-1 的正確承接票為 `0.1.0-W3-153`（更正 `traceability.yaml` `runtime_tests` 首條的過期 status），SR-1 狀態行已重跑 `resolve-spawn-request` 修正。

`0.1.0-W3-152` 未能自 `spawned_tickets` 移除：ticket CLI 的該欄位為 append-only（僅 `add-spawned`，`set-*` 系列無對應項），且 ticket md 受 hook 保護不可直接編輯。本段為該誤填的唯一撤銷記錄——讀者追溯 `0.1.0-W3-139` → 本票的衍生鏈時，`0.1.0-W3-152` 應視為不存在的連結。CLI 缺口另票追蹤。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-139 | chars: 1298 -->

### Task Reference
- 0.1.0-W3-139 what: 盤點 全部 driven port 的回饋點覆蓋，並裁定輸出端回饋契約的 spec 落點（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-139 why: 用戶裁示輸出端與輸入端同一設計原則：「不只輸入端應該要有這樣的設計，那包括我們服務本身往外呼叫也是要有一樣的設計，無論是開文件，開資料庫，打 API」，且「也都應該要被列入測試」。

輸入端的落地路徑已完整：方法論（元件庫雙向約束）→ skill（component-contract-design）→ spec（SPEC-003 / SPEC-004）→ 測試策略。輸出端目前只有第一段——`INV-PORT-OBSERVE-001` 與 Port 回饋契約全文在 `.claude/methodologies/clean-architecture-implementation-methodology.md`，那是跨專案的框架方法論，**不記本專案有哪些 port、各自的回饋點是否已定義**。

`docs/spec/` 現有的四份規格全在 `ui/` domain，全部描述輸入端。輸出端無 spec 落點的後果是：驗收無專案側依據可引用，「列入測試」這件事沒有可否證的對象——審查者只能逐票回頭讀方法論再自行推導本專案該有什麼，這正是輸入端在 SPEC-004 逐元件回饋契約子節出現之前的狀態。

實查本專案的 driven port 為五個，覆蓋狀況不一：`WorkspacePreferencesHandle` / `WorkspacePreferencesPort` / `WorkspaceDirectoryProbePort`（`lib/workspace/workspace_repository.dart`，回饋契約由 0.1.0-W3-121 補齊，例外契約由 0.1.0-W3-135 待補）、`ScanNotifier`（`lib/services/scan_notifier.dart`，SPEC-003 2.2 有介面定義與可觀測性列，但未依 Port 回饋契約的三個回饋點編排）、`ExternalOpener`（SPEC-003 2.2 有契約表，實作票 0.1.0-W1-068 未動）。三者的定義散在三處且形態不一，沒有一個地方能回答「本專案全部 port 的呼叫發出時刻是否都可觀測」。

**為何 blockedBy 0.1.0-W3-133**：該票裁定是否建立 adapter 層與 ports 目錄約定，兩個視角意見相反。規格若先寫，就得描述一個尚未定案的結構，等於替 133 選邊。（來源已完成，結論見 Solution）

### Related Files
- docs/spec::read  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
- docs/domain-map.md::read  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
- lib/services/scan_notifier.dart::read  # from 0.1.0-W3-139（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 五個 driven port 逐一對照三個回饋點的覆蓋表已產出，呼叫端／日誌／監控追蹤三欄分開判定  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
- [ref] [x] spec 落點裁定含 domain 選擇、新開或併入、與框架方法論的分工線三項，各附不選其他方案的理由  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
- [ref] [x] 承接的規格撰寫已建 DOC 票或已登記 spawn request（不停留在 pending）  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
