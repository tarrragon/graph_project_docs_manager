---
id: 0.1.0-W3-299
title: 測試 新條文在 issue 103 / 92 上的行為，產出條文缺口清單
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-293
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: '用戶裁示先用幾個 issue 測試；選定標的含 #103 / #92'
  final_decision: 兩個 issue 同票，因其共同構成「關係判定」這一個測試條件
  rationale: '#92 今天被兩張獨立的票各自判為引用，#103 帶含正向對照組的探針實測；兩者測的都是判準在關係與證據上的鑑別力'
who:
  current: framework-issue-curator
  history: {}
what: 測試 新條文在 issue 103 / 92 上的行為，產出條文缺口清單
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/references/escalation-flow.md
why: '**這一組測的是判準的鑑別力，不是流程順序。**


  **`#92` 今天被兩個獨立執行體各自判為「引用而非同源」**（`0.1.0-W3-180` 與 `0.1.0-W3-289`），且其中一次是在被明確告知「查到不同結論就推翻前者」的條件下仍維持原判。**兩次獨立判定一致是目前最強的一個正向訊號，但一致不等於正確**——兩者可能共用同一個過寬的判準。本組要查的是：條文對「重複／切分／引用」的三分是否給得出可推翻的依據，還是只要兩個 issue 主題不同就自動落到「引用」。


  **`#92` 另有一項自指性質**：平行 session 指出其 body 逐字引用三張來源票的 `how.strategy` 當作「建議修法」——**它本身就是新條文要治的那個形態的實例**。條文對「一個已經把未驗證修法寫進正本的 issue」該怎麼處置，若沒有說法，這裡會暴露。


  **`#103` 帶的是證據品質的測試**：平行 session 留了四項探針實測，含正向對照組（`sed -i` 會觸發而 `python3`／heredoc 不會）。**沒有那組對照，前三項的沉默與「hook 根本沒載入」不可區分。** 三問的第二問（反例已試）要求的正是這種對照——本組要查的是條文對「什麼算反例」是否給得出判準，還是留給執行者自由心證。'
how:
  task_type: Analysis
  strategy: '**這是條文的測試，不是 issue 的修復。產出是條文的缺口清單，不是修法。**


    **先讀條文**：`.claude/skills/framework-issue/references/escalation-flow.md`〈修法時點分離：發現票何時能寫定修法〉（`0.1.0-W3-293` 於 `51662bb2` 落地）。連同 `SKILL.md` 的路由與 `quality-baseline.md` 規則 5 的接縫註記一併讀。


    **執行方式**：對指定的每一個 issue，實際走一次現行流程（讀 body 與全部 comments、跑 `dedup`、套三問、套兩檔擴散面處置表），**逐步記錄條文在哪一步給不出答案、給了模稜兩可的答案、或給了你認為錯誤的答案**。


    **測試期間禁止寫入 canonical issue**：不執行 `init`／`add`／`update`／`observe`，不改任何 `.claude/` 資產，不跑 `sync-push`。**改為在 Solution 記錄「依條文我會貼什麼、貼到哪個區段、為什麼」**——那份記錄本身就是受測產出。唯讀操作（`dedup`／`check`／`show`／讀 comments）不受此限。


    **要記錄的三類發現，缺一不可**：


    1. **條文失效點**：具體指出條文的哪一句在哪個 issue 的哪一步失效，附該步驟的實際輸入。禁止「條文大致可用」這種無法被推翻的結論。

    2. **條文正確攔下的**：哪些地方條文確實給出了與你直覺不同、而且更對的答案。**沒有這一類就無法分辨條文有效還是條文無效**——全是缺口的報告與全是肯定的報告同樣不可信。

    3. **skill 引導層的缺口**：`SKILL.md` 的路由是否把你導到對的地方、`escalation-flow.md` 的章節順序是否符合實際執行順序、是否有該讀而沒被指到的檔案。


    **不做的事**：不修條文（統整票承接）、不建修復票、不對 issue 做任何寫入。發現屬本專案的問題則依規則 5 建票，但**票面不得寫定修法**——這本身是條文的自我適用測試。'
acceptance:
- '[ ] 兩個 issue 各自的條文失效點已分開記錄，未合併為單一結論'
- '[ ] 條文正確攔下的項目已記錄，非只列缺口'
- '[ ] SKILL.md 路由與 escalation-flow 章節順序的引導缺口已記錄'
- '[ ] 「依條文我會貼什麼、貼到哪個區段、為什麼」已寫入 Solution，且全程未對 canonical issue 寫入'
- '[ ] 條文對「重複／切分／引用」三分是否給得出可推翻的依據，已以 #92 為例判定'
- '[ ] 條文對「什麼算反例」是否給得出判準，已以 #103 的探針對照組為例判定'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T12:36:33'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

測試 新條文在 issue 103 / 92 上的行為，產出條文缺口清單

---

## Problem Analysis
### 問題根因

本票不修任何東西，測的是 `51662bb2` 落地的〈修法時點分離：發現票何時能寫定修法〉在真實 issue 上是否具鑑別力。受測對象三層：條文本身（三問、兩檔擴散面表、三個檢驗案例）、`SKILL.md` 的路由、`escalation-flow.md` 的章節順序。

直接原因是條文落地時只做過自我一致性檢查，未在任何既存 issue 上實跑。條文的三個檢驗案例全部是條文作者自選的構造例，其中唯一判「豁免」的那一個（測試檔對已棄用 API 的直接替換）是本地票，依同檔上游的〈介入判斷〉根本不會進入 `escalation-flow.md` 的射程——**條文在自己所在檔案的定義域內，沒有給出任何一個豁免例**。這一點只有實跑才會浮現。

### 影響範圍

受影響檔案（本票皆唯讀，未修改任一個）：

- `.claude/skills/framework-issue/references/escalation-flow.md`（受測條文主文）
- `.claude/skills/framework-issue/SKILL.md`（路由表）
- `.claude/skills/framework-issue/references/ticket-intake.md`（實測發現的接線缺口對側）
- `.claude/skills/framework-issue/references/comment-as-section-protocol.md`（三種關係處置，與條文衝突處）
- `.claude/rules/core/quality-baseline.md` 規則 5 接縫註記

**已驗證的事實**：條文與兩份 reference 的交叉引用狀態為機械查得（見重現實驗結果）；#103／#92 的 body 與全部 comment 為 `gh issue view` 實讀；`dedup`／`show`／`check` 為實跑。

**仍未驗證的假設**：本票只在兩張 issue 上實跑，缺口清單是下界不是全集；「消費端」一詞的兩種讀法是本票從條文自身三個案例反推的，未向條文作者查證其本意。

### 相關 Error Pattern

未新建。最接近的既有形態是 `PC-BAL-001`（建立端與下游驗證端清單各自演進）——本票發現的「條文與 `ticket-intake.md` 的列舉值無接線」屬同族但不同層：PC-BAL-001 是兩份清單內容漂移，本票是兩份條文從未互指，沒有可漂移的對應關係。canonical 側同構者為 `#105`（未經檢查者佔據「已確認」的位置），條文來源票 `0.1.0-W3-289` 的 Solution 已判為「引用（同構，異載體）」。

## 重現實驗結果
### 實驗方法

對 `#103` 與 `#92` 各走一次現行流程，逐步記錄條文在哪一步給不出答案。步驟固定為：讀 body 與全部 comment（`gh issue view --json body,comments`）→ 跑 `dedup` → 套〈介入判斷〉→ 套〈兩條路徑〉→ 套〈修法時點分離〉三問與兩檔擴散面表 → 套 `comment-as-section-protocol.md` 的三種關係處置 → 跑 `show`／`check`。

**全程對 canonical issue 零寫入**：只用 `gh issue view`、`dedup`、`show`、`check` 四個唯讀入口，未執行 `init`／`add`／`update`／`observe`，未動任何 `.claude/` 檔案，未跑 `sync-push`。

條文原文取自 `git show 51662bb2`，非取自可能已被後續改動的工作區副本。

### 實驗執行

**一、唯讀命令實跑輸出**

| 命令 | 結果 |
|------|------|
| `dedup --keywords "bash-edit-guard" "繞過 Edit 改檔" "直譯器內嵌腳本"` | 三組關鍵字分別 11／49／若干則命中；`#103` 三個位置全中，`#54`／`#92`／`#84`／`#90` 亦命中 |
| `show 103` | `索引缺失：body 無區段索引表`；區段 0 則、觀測流 1 則 |
| `check 103` | 主警訊：找不到「當前結論」區段；警訊 C 索引缺失 |
| `check 92` | 三則「當前結論」皆未觸發（最大落後 2d22h < 7 天）；索引一致 6 筆；comment 11 則 |

`check 92` 的落後時間經逐則 comment 時戳交叉驗證（最新觀測 `2026-09-10T05:58:08Z`），計算正確，非誤報。

**二、條文交叉引用的機械查證**

```
grep -rn "時點分離" .claude/skills/framework-issue/
  → 命中 4 檔位置：SKILL.md 路由列、CHANGELOG、escalation-flow 自身 3 處
grep -n "escalation-flow" .claude/skills/framework-issue/references/ticket-intake.md
  → 0 命中
grep -rn "escalation-flow|時點分離" .claude/agents/framework-issue-curator.md
  → 0 命中
```

即：**收束流程（`ticket-intake.md`）與 curator 代理人定義都不指向新條文**，而收束正是票面修法進入 canonical 正本的那一步。

**三、對 `#103` 的逐步走查**

- 〈介入判斷〉：抽象可攜（敘述不含專案名）＋根源在 `.claude/hooks/` → 框架問題。條文給得出答案。
- 〈兩條路徑〉：順手發現 → 路徑 A。本 consumer 已照做（分析票已 close、驗證票 `0.1.0-W3-180` 保留 pending）。條文給得出答案。
- 〈修法時點分離〉三問，對 body「待辦與來源」第一列（模式 A 擴充涵蓋六類手段）：
  - 反例已試＝**是**。`#103` 的觀測 comment 有四項探針，第四項 `sed -i` 是正向對照，證明 hook 掛載中且警告管道通暢，前三項的沉默因此是涵蓋缺口而非 hook 未載入。
  - 範圍已量＝**部分**。五項穿透手段中三項（`python3 -c`／heredoc／重導向）有實測，`awk -i inplace` 與 `ed`／`ex` 兩項只有讀原始碼推論，body 表格對五項一律標「否」，兩種證據強度在表面上不可區分。
  - 消費端已觀測＝**兩種讀法得出相反答案**（見實驗發現一）。
- 三種關係處置，對 `dedup` 命中的 `#54`：`#54` 徵狀第 3 項即「bash-edit-guard 的字面區段掃描未處理引號脈絡」，與 `#103` 同一支 hook。依三分表「同一領域、不同層級＝切分」須先判定層級，而條文未給層級的操作定義；真正可用的判別問法在 `#97` body 內（「把該 hook 的判定謂詞改對之後，這個徵狀還在不在」），不在任何 skill 條文裡。套該問法：`#103` 的徵狀（`python3 -c` 穿透）在謂詞改對後消失 → 屬 `#54` 那一層 → 兩者應判重複或切分，不是無關。

**四、對 `#92` 的逐步走查**

- `#92` body 三段「建議修法」逐字引用三張來源票的 `how.strategy`，三段皆無任何證據狀態欄位——即條文原則一所描述形態的現成實例，且已進入 canonical 正本。
- 套兩檔擴散面表：三項修法落點皆為 `.claude/` 通用資產 → 第二列「修法形狀移至 framework issue 區段決定，發現票降為 ANA」。但**修法形狀已經在 issue body 裡**，而 `comment-as-section-protocol.md` 明訂 body 於 `init` 後不再由工具改寫、curator 不得直接改 body。條文的處置指向一個協定禁止的動作（見實驗發現三）。
- 三種關係處置對「`#92` 與新條文主題是否同源」：兩個獨立執行體（`0.1.0-W3-180`／`0.1.0-W3-289`）皆判「引用非同源」。本票查的不是再投一票，而是條文能否給出可推翻的依據——實測結果見實驗發現二。

### 實驗發現

**已驗證的事實（每一項附可複驗的輸入）**

1. 「消費端」一詞在條文自身的三個檢驗案例中被用了兩種不相容的讀法。案例一寫「消費端＝那兩個測試檔自身」（讀法 A：這段程式碼的執行環境與觸發條件）；案例二寫「消費端＝其他 consumer 是否呼叫該工具」（讀法 B：這項資產的使用者群）。對 `#103` 套讀法 A 得「已觀測」（探針實測了 hook 的觸發條件），套讀法 B 得「未觀測」（未觀測其他 consumer 的 Bash 使用形態）。條文未給選擇規則，兩個相反結論都有條文支持。
2. `ticket-intake.md` 與 `framework-issue-curator.md` 對新條文的引用數為 0（grep 實測）。
3. `ticket-intake.md`〈步驟四〉已有狀態列舉含「待裁票／已裁票」，語意正是「修法是否已定案」；新條文未提及 `ticket-intake.md` 任何一次。
4. `#103` body 的「待辦與來源」表把四列分為「Hook」一列與「設計決策」三列——這正是條文要的「證據狀態隨修法一起寫」，但它在 issue 載體上自發完成，條文全篇只以「發現票的 `title` 與 `how.strategy`」為對象，未涵蓋 issue 載體。
5. 條文〈現況〉段寫「待 `#81` 區段討論後另行決定」，既無承擔者也無具體動作。

**仍未驗證的假設**

- 條文若被實際套用於建票期是否會降低 74 張同型票的比例（`0.1.0-W3-289` 亦記為無法量測）。
- 讀法 A／B 的二分是本票從條文案例反推，非條文作者自述。
- `#103` 與 `#54` 應判重複或切分，本票只證明「不是無關」，未定案該併入哪一張——定案需要 issue 端的判準，而那正是本票發現缺少的東西。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-293 | chars: 888 -->

### Task Reference
- 0.1.0-W3-293 what: 落條文 framework-issue 升級流程的時點分離判準，與品質基線規則 5 的接縫註記（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-293 why: **用戶裁示（2026-09-11）**：先調整 framework-issue skill 的說明與引導、以及相關 rule 的規範，再回頭評估目前是否已可開始實作、或仍須回頭檢查 issue 內容再做分析。

**條文內容已由 `0.1.0-W3-289` 產出，本票只做落地**，不重新設計。該票的機械量測：443 張票中 275 張為發現衍生，其中 108 張 IMP，其中 74 張來源票非 ANA；這 74 張建票當下 `how.strategy` 全部非空，僅 12 張（16%）帶待驗證／停手語彙——**62/74（84%）在發現當下寫定了一個沒有任何欄位記載其驗證狀態的修法**。

**289 原本的定位是「未定案，供討論」**，理由寫在該票：條文形狀是它自己的修法，其消費端（其他 consumer 的建票實務）未經觀測，故依自身判準不寫成結論。用戶裁示先落地，此裁示即為本票的依據；**`#81` 的區段討論不因此關閉**，後續若該處給出不同形狀，以其為準。

**不做則後果**：判準只存在於一張已完成的票與一則 issue 觀測裡。下一個 session 建發現票時不會讀到它，84% 的比例不會改變。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/framework-issue/SKILL.md  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/CHANGELOG.md  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- .claude/rules/core/quality-baseline.md  # from 0.1.0-W3-293（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三問與兩檔擴散面處置表已落於 escalation-flow.md，判準內容與 0.1.0-W3-289 一致  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- [ref] [x] 三個檢驗案例已收錄，且能分辨豁免與不豁免（非全面禁令）  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- [ref] [x] SKILL.md 有一行路由指向該節，未複製判準內容  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- [ref] [x] quality-baseline 規則 5 的接縫註記不超過一行且含路由，未在自動載入層展開三明示  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- [ref] [x] 單位是欄位不是票、豁免軸是證據狀態不是規模，兩項邊界已明寫  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
