---
id: 0.1.0-W3-205
title: 修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-204
- 0.1.0-W3-176
- 0.1.0-W3-239
relatedTo: []
spawned_tickets:
- 0.1.0-W3-245
source_ticket: 0.1.0-W3-184
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-184 Solution 第二節：現況違反截斷規則第三條
  final_decision: 改走元件層單一入口，並把截斷條件綁在級別上
  rationale: 干擾不對稱與零日誌同源於 main.dart 繞過元件層；不先收斂入口，級別判斷會有一條路徑管不到
who:
  current: lavender-interface-designer
  history: {}
what: 修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別
when: 0.1.0-W3-204 的 SPEC-003 級別對應落地之後
where:
  layer: Presentation
  files:
  - lib/main.dart
  - lib/components/app_snack_bar.dart
  - lib/app/attention_level.dart
  - lib/screens/gap_report/gap_report_screen.dart
  - lib/screens/gap_report/scan_notification_controller.dart
  - test/unit/components/app_snack_bar_test.dart
  - test/unit/app/home_page_workspace_test.dart
  - test/unit/components/app_snack_bar_source_contract_test.dart
why: '`AppSnackBar.show` 無條件呼叫 `hideCurrentSnackBar()`，因此**任何一則新訊息都會截斷正在顯示的那一則，不分級別**——一則 L3 完成通知會殺掉正在顯示的 L2 失敗訊息，違反 `0.1.0-W3-184` 截斷規則第三條（低級別不得截斷任何東西）。


  **且干擾是跨路徑且不對稱的。** `hideCurrentSnackBar()` 作用於 `_snackBars.first`，**不分那一則是誰放進去的**；而 `lib/main.dart:219` 直接呼叫 `ScaffoldMessenger.of(context).showSnackBar(...)` 繞過元件層，走排隊語意。結果是元件路徑殺得掉原生路徑的訊息、反之不行，且被殺的那則**零日誌**（不經過元件層，`0.1.0-W3-165` 的四個事件對它全不適用）。


  **使用者可觀察到的徵狀**：訊息閃一下就不見了，而沒有任何紀錄能回答哪一則被殺、被誰殺。'
how:
  task_type: Implementation
  strategy: '兩件事，順序不可調換。


    (1) 把 `lib/main.dart:219` 的直接呼叫改走 `AppSnackBar`——繞過元件層是干擾不對稱的成因，也是零日誌的成因，不先收斂入口則後續的級別判斷有一條路徑管不到。


    (2) 把截斷條件綁在級別上而非呼叫路徑上：`show` 接受級別參數，僅當新訊息級別**高於**當前顯示者時才 `hide`；**同級別截斷並留痕**；低級別不得 `hide`。級別取值與各訊息的歸屬以標題文字引用 `SPEC-003`（`0.1.0-W3-204` 的 §2.14 指派表、`0.1.0-W3-232` 的 §2.15 可驗收形態），不在元件內另立。日誌須能回答被截斷的是哪一則（現行 `AppSnackBarLogEvent` 的 `closed` 事件需帶被截斷者的識別，由 `0.1.0-W3-176` 提供欄位）。


    **當前顯示者的級別從仲裁器讀，元件內不另存一份**（`0.1.0-W3-207` Phase 1 SR-2，PM 已裁決）：`0.1.0-W3-207` 的仲裁器持有恰一筆持有者記錄，元件若自行比較級別即產生第二份同一事實的狀態，兩份必然漂移。本票的 `hide` 條件改為讀仲裁器的持有者記錄，或以仲裁決策回傳的被搶佔者欄位是否非空為準。**若 `207` 尚未合併，本票先以「呼叫端傳入當前持有者級別」實作並在 Solution 記錄該過渡形態**，不在元件內建立獨立的持有者狀態；`207` 合併後由該票收斂為單一來源。


    **「同級別截斷並留痕」是經裁決的具名例外，不是疏漏**（`0.1.0-W3-239`）：方法論讓步規則第二條字面要求同級別排隊或合併，本專案裁定**不引入任何形式的提示佇列**，改以留痕滿足其保護目標（「事件已發生的告知本身不得被無痕跡銷毀」）。三項依據見 `239` 的 Solution 與其 PM 驗收更正段。**實作不得自行改回排隊**；若實作過程認為留痕不足以滿足該保護目標，停手回報 PM，不自行推翻裁決。'
acceptance:
- '[ ] lib/main.dart 不再直接呼叫 ScaffoldMessenger.showSnackBar，全部提示經 AppSnackBar'
- '[ ] 被截斷時日誌可回答被截斷的是哪一則（識別資訊而非僅事件類型）'
- '[ ] 級別取值以標題文字引用 SPEC-003，元件內未另立一套級別定義'
- '[ ] 截斷條件綁在級別上：低級別不得 hide、同級別截斷並留痕、僅高級別可截斷，三者各有測試'
- '[ ] 新建 lib/app/attention_level.dart 承載級別列舉，且 AppSnackBar 與未來的仲裁器皆 import 該檔而非各自定義'
- '[ ] 新建 test/unit/components/app_snack_bar_source_contract_test.dart 承載原始碼契約測試（G-E），與行為測試分檔'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-10T01:03:23'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
current_phase: Phase 3b
assignee: parsley-flutter-developer
tdd_phase_source: manual
---

# Execution Log

## Task Summary

修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 由 `0.1.0-W3-176` Phase 4a 轉來的兩項觀察（PM 路由，2026-09-10）

兩項皆經審查判定**當前不可達、不建票**，但本票要改寫的正是那條路徑，改寫時須各看一眼。

**觀察 A：`reason == hide` 是承重前提，但無守衛。**
現行實作以 `SnackBarClosedReason.hide` 判定「這則是被後續一次 `show` 截斷的」。該等價關係成立的前提是 `hideCurrentSnackBar()` 全 repo **恰 2 處**（審查實測：`app_snack_bar.dart:173` 預設 `hide`、`:220` 明確 `action`；無 `removeCurrentSnackBar` / `clearSnackBars`）。若日後新增第三個呼叫點，`hide` 不再恆等於「被截斷」，warning 會開始標到另一類事件上，而 T-1～T-9 沒有任何一個會紅燈。本票既然重寫截斷條件，**須重新確立或以斷言鎖住這個前提**，不可預設它繼續成立。

**觀察 B：動作回呼的順序在理論上可產生未稽核的截斷。**
`app_snack_bar.dart:214-223` 的順序為 `log(actionPressed)` → `onAction()` → `hideCurrentSnackBar(reason: action)`。若某個 `onAction` **同步**呼叫 `AppSnackBar.show`，新的一則會先取代舊的（記 `hide`），隨後那句 `hideCurrentSnackBar(reason: action)` 會把**新的那一則**以 `action` 關掉——新訊息使用者一眼未見，日誌記為 `closed{reason=action, level=null}`，**正好是這條線要消滅的那種不可稽核截斷**。

當前不可達：唯一的 `withAction` 呼叫點（`gap_report_screen.dart:199`）其 `onAction` 回傳 Future 且未 await，同步段不顯示任何 SnackBar。且此順序繼承自 `0.1.0-W3-165`，非新引入。本票改寫該段時一併確認新設計是否仍有此形態。


### Phase 1 的輸入來源（PM 整理，2026-09-10）

本票的規格與判準已全數落地，Phase 1 不需重新推導任何判準，只需把它們對應到本元件：

| 來源 | 提供什麼 |
|------|---------|
| `SPEC-003`〈注意力通道的到達類別與級別指派表〉 | 本專案每則提示的級別、載體與標定點（13 列）。**級別取值以此為準，元件內不另立** |
| `SPEC-003`〈讓步規則的可驗收形態〉 | 三條規則對本專案事件集合的可驗收形態；跨級別（規則 1／3）已證時序互斥而空成立、同級別（規則 2）為經裁決的具名例外 |
| `SPEC-003`〈顯示位爭用時的約束與 0.1 的處置規則〉的〈重評結果〉 | **不引入任何形式的提示佇列**的裁決與三項具名理由（`0.1.0-W3-239`）。本票不得自行改回排隊 |
| `0.1.0-W3-176` 的產出 | `showId`（關聯識別）、`AppSnackBarOrigin`（等級判別輸入）、`_resolveClosedLevel` 私有純函式、四個事件的 `baseFields` 展開 |
| 本檔〈由 `0.1.0-W3-176` Phase 4a 轉來的兩項觀察〉 | 觀察 A（`reason == hide` 承重前提無守衛）、觀察 B（動作回呼順序理論上可產生未稽核截斷）——本票改寫的正是這兩處所在路徑 |

**一項須正面回答的設計問題**：`show()` 目前的級別資訊只有 `origin`（發起者），而讓步規則需要的是**級別**（不可棄／須留痕／可棄）。兩者不是同一件事——`origin` 決定日誌等級，級別決定誰能截斷誰。Phase 1 須定義級別如何進入 `show()`：新增參數、由 `origin` 推導、或由呼叫端查 §2.14 指派表後傳入。**若採推導，須說明 §2.14 的 13 列是否真能由 `origin` 唯一決定級別**（實查該表：可棄列橫跨自發型與等待型，不可棄列全為自發型——單看 `origin` 不足以決定級別）。

**範疇邊界**：不改 `SPEC-003`；不改 `0.1.0-W3-176` 已落地的四個事件值語意；G2／G3（`0.1.0-W3-241`／`242`）不在本票。

### PM 裁決：`where.files` 擴充與閾值 2 的例外（2026-09-10）

**採納 Phase 1 的必填 `level`，不取預設值方案。** 理由與 `0.1.0-W3-176` 對 `origin` 取預設值的裁決**不矛盾，因為誤判後果的形狀不同**：

| 參數 | 漏傳的後果 | 是否自我揭露 |
|------|-----------|------------|
| `origin`（`176`） | 日誌等級偏高 | **是**——噪音會被發現並追查 |
| `level`（本票） | 該則訊息被錯誤地截斷，或錯誤地截斷了別人 | **否**——使用者只是沒看到那則訊息 |

`origin` 的預設值站在會被發現的那一側；`level` 沒有這樣的一側。Phase 1 的論點成立：預設值使「確實可棄」與「忘了查 §2.14」在程式碼上不可區分。

**`where.files` 擴至 6 項，超過閾值 2（> 5 建議拆分），本票明示接受，不拆。** 理由是**語言語意上的原子性**：必填參數無法漸進導入——列舉檔、元件、全部呼叫端必須同一次改動才能編譯。不存在可切的接縫；強行拆分只會產生一個必然紅燈的中間狀態。認知負擔的實質未超標：6 檔中 4 檔（兩個呼叫端、`main.dart`、新列舉檔）各只有機械式的單行改動或十行列舉，功能職責數仍為 2。

**`lib/app/attention_level.dart` 為待建立的新檔**，已補 acceptance 使新建語意顯性化。列舉不住元件內的理由（`0.1.0-W3-207` 的 acceptance 明訂仲裁器不得 import `AppSnackBar`，住元件內必然產生兩份平行定義）成立。

**附帶發現一併吸收**：`gap_report_screen.dart` 兩處 `AppSnackBar.show` 未傳 `origin`、取預設 `background`，但兩者實為使用者按破洞項觸發——現況把使用者發起的截斷誤記為 warning。本票必填 `level` 已迫使該二處改動，同批修正邊際成本為零。**該二處原屬 `0.1.0-W3-245` 的範疇，本票吸收後 `245` 已重新界定為純 SPEC-004 票**（見該票）。

### PM 裁決：Phase 2 的三項回報（2026-09-10）

**一、`where.files` 再擴兩項測試端檔案——兩項皆核准，`where.files` 至 8 項。**

- `test/unit/app/home_page_workspace_test.dart`（既有）：核准。Phase 2 的理由成立——靜態零命中只證明舊呼叫消失，不證明新呼叫接對訊息；不加則 acceptance 1 的行為面失去驗證，只剩靜態面。
- `test/unit/components/app_snack_bar_source_contract_test.dart`（新增）：核准，且**不採「落入既有檔的獨立 group」的替代方案**。原始碼契約測試（掃描原始碼文字）與 widget 行為測試是**兩個不同的驗證對象**，不只是分量問題：前者在原始碼未編譯前就成立、後者需要 widget tree。`cognitive-load` 明訂單一檔案承載 domain 數 > 2 須拆分，而 Phase 2 自己也指出遷移後既有檔約 950 行。分檔是正確的形狀，不是為了避開行數。

閾值 2 的例外理由不變（必填參數在語言語意上無法漸進導入），測試檔須與其驗證的改動同批落地，同屬該原子性範圍。

**二、T-205-21(c)（測試套件讀 `SPEC-003` 並斷言五段章節標題文字存在）——核准，附一項要求。**

這是本專案今日反覆處理的那件事的**正向解**：我們刻意把引用從行號改為標題文字（`0.1.0-W3-207` 的 trigger、`0.1.0-W3-249` 的十三處），理由是行號會靜默漂移。**但標題文字同樣會被改名，只是沒有人在看**。這條斷言就是那個看的人。

「其他 DOC 票改章節名會觸發本票無關的紅燈」不是缺點，是**正確行為**——被程式碼引用的錨點若能無聲改名，錨點就不成其為錨點。

**附加要求**：該斷言失敗時的訊息必須指名補救動作（「`SPEC-003` 的章節標題已變更，請同步更新 `app_snack_bar.dart` 的 dartdoc 引用」），不得只印預期與實際值。紅燈出現在改名者的票上，而他不知道自己踩到什麼——訊息是他唯一的線索。

不違反 `test-assertion-design-rules`：該規則管的是「斷言結果依賴程式以外的**可變環境因素**」（機器負載、網路、時區），版控檔案是確定性輸入，不在其列。

**三、G7-4 的 `pumpAndSettle` 推論待 Phase 3b 實跑——無需 PM 動作。** M17 的處置與「禁止以放寬斷言掩蓋」的禁令已足。

### 附記：Phase 2 的正向對照值得沿用

T-205-21(b) 為剝除註解的靜態掃描加了**正向對照**，理由是剝除器若剝過頭會使多數計數斷言**變綠**——守衛失效且無聲。單靠計數上界擋不住這個方向。

**「失效方向是變綠」的守衛必須有正向對照**，這條在本專案已有多個同族實例（檢核器找不到節報成缺標定、hook 把禁令誤判為指示）。Phase 3b 與 Phase 4a 遇到任何新增的靜態守衛，比照辦理。

### PM 裁決：Phase 3a 的兩項回報（2026-09-10）

**一、5 對 7 的數字歧異——票面正本為準，7 項正確；派發 prompt 的「5 項」是我抄錯。**

實查 P2.6 遷移核對表第 1272 行，(E) 事件序列的枚舉為 **M3、M4、M5、M6、M8、M11、M14，共 7 項**；表末共同禁令段列同一組七個編號。

錯誤來源可追：Phase 2 的完成回報寫「**5 項（M3–M6／M8／M11／M14）**」——**計數寫 5，緊接著的列舉是 7 個**（`M3–M6` 本身即 4 個）。我把那個「5」原樣抄進派發 prompt。

這正是 `DOC-GPD-003` 的變體「計數與其列舉同時寫下卻不一致」，該則明訂讀者側處置：**列舉是權威，計數是衍生值**；兩者不一致時信列舉，並把計數當待修正的敘述，不據它推論列舉有缺漏。Phase 3a 依票面 7 項編列且不自行裁決，處置正確。

**Phase 3b 依 7 項執行。**

**二、ticket CLI 在 worktree 中把寫入落到共用 checkout——已處置，並記入 `0.1.0-W3-247`。**

`ticket track append-log` 與 `ticket track phase` 的寫入落在 `/Users/mac-eric/project/graph_project_docs_manager/docs/...`（共用 checkout），而非執行者所在的 worktree。Phase 3a 的處置正確：把結果檔複製進 worktree、逐位元組比對確認僅含自身增補與三行 frontmatter、精確 stage、核對 index、裸 commit。

PM 端已收攏：共用 checkout 的殘留與分支版本經 `diff -q` 確認**逐位元組一致**，先行提交（`23c15f22`）後合併分支，未產生內容分歧。

此為 `0.1.0-W3-247` 的**第三項**（前兩項：`ticket track commit` 拒收票檔、`ticket track phase` 不觸發 auto-commit）。三者共同的使用者可見形態是「worktree 隔離的代理人無法以正規路徑提交自己的票面產出」，但成因各異——本項是**寫入去向錯誤**，非提交被拒。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 功能規格（lavender-interface-designer，2026-09-10）

模式：Full（明確指定）。`/spec init` 依 frontmatter 會落 Lite（`how.task_type` 為
Implementation 非「新增」、`where.files` 3 項），但票面 acceptance 第三、四項要求
介面契約與跨模組裁決規則，Lite 骨架無 API Signatures 與 Error Handling 兩區段。
獨立檔案 `0.1.0-W3-205-phase1-design.md` 會被 `ticket-file-access-guard-hook` 阻擋，
依本專案既有慣例（`0.1.0-W3-176`、`0.1.0-W3-207` 的 Phase 1 皆然）落於本節。

引用約定：SPEC-003 以章節標題文字定位，不用行號、不重述判準本身；方法論判準一律
連同「它回答的問題」欄引用。本節全程唯讀 SPEC-003 與 SPEC-004。

#### 0. 前置防護（ARCH-010 框架內建機制與元件庫優先）

| 步驟 | 問題 | 結論 |
|------|------|------|
| 1 | 框架內建機制是否已解決？ | **部分存在，但已被裁決排除。** `ScaffoldMessengerState` 內建 FIFO 佇列——不呼叫 `hideCurrentSnackBar()` 即自動排隊，這確實滿足讓步規則第二條字面（同級別排隊）。但 `0.1.0-W3-239` 已裁定本專案**不引入任何形式的提示佇列**，Material 內建佇列亦屬「任何形式」之一。內建機制無級別概念，無法表達「僅高於當前顯示者才截斷」。故：不是沒有內建機制，是內建機制的兩種形態（無條件排隊／無條件取代）都不是要的那一種 |
| 2 | 語言標準庫是否已有解？ | 否。Dart 標準庫無「依級別裁決佔用」的原語 |
| 3 | 本地狀態是否足夠？ | 否，且被明文禁止。判斷需要「當前顯示者的級別」，該狀態屬畫面級——SPEC-003 §2〈頁面狀態保留〉列「畫面級狀態不得存於元件庫元件內部」；`0.1.0-W3-207` Phase 1 已在 L4 持有恰一筆持有者記錄，元件內另存即為第二份同一事實 |
| 4 | 外部狀態管理 | 採用既有的：級別由呼叫端在 SPEC-003 §2.14 標定點填妥後傳入；持有者狀態由 `0.1.0-W3-207` 的仲裁器持有。本票不新增任何狀態持有者 |

**元件庫優先**：不新增元件。`AppSnackBar` 已是 SPEC-004 §4.26 定義的元件庫元件，
本票只改其入口的行為參數與截斷條件，不動任何視覺、slot、token、無障礙契約。
`level` 與 `origin` 同屬行為參數而非內容 slot（`0.1.0-W3-176` 新增 `origin` 時亦未
動 SPEC-004 §4.26 slot 契約表），故 slot 契約不需改寫；但狀態矩陣退出路徑「新的
SnackBar 取代」與回饋契約「重複觸發防護：冪等——新的 `show` 取代前一則」兩處在
讓步路徑上不再無條件成立，須由 DOC 票同步（見第 10 節，已登記）。

#### 1. Purpose

**問題**：`AppSnackBar.show` 在顯示前無條件 `hideCurrentSnackBar()`，截斷的唯一依據
是「這次呼叫發生了」——即 SPEC-003〈鑑別「綁級別」與「綁呼叫路徑」兩種實作〉判為
「綁呼叫路徑」的那一種。同時 `lib/main.dart` 的 `ChooseFolderUnavailable` 分支直接
呼叫 `ScaffoldMessenger.showSnackBar` 繞過元件層，使干擾不對稱（元件路徑殺得掉原生
路徑的訊息、反之不行）且被殺的那則零日誌。

**目標**：兩件事，順序不可調換。

1. 收斂入口：全部提示經 `AppSnackBar`，使「哪些訊息在競爭同一個顯示位」是一個可以
   被完整回答的問題。
2. 把截斷條件綁在級別上：`show` 接受級別參數，截斷與否的唯一輸入是「新請求的級別」
   與「當前持有者的級別」，判定落在單一純函式內，靜態可讀出。

**可觀察的改善**：截斷發生時，日誌能回答被截斷的是哪一則（`0.1.0-W3-176` 的
`showId` 已使被截斷者自報身分，本票再補「誰截斷了誰」的關聯）；讓步發生時（低級別
遇高級別持有者），被丟棄的那一則不再無聲——它從未呈現，但留下一筆帶級別與訊息的
事件。

**非目標**：

- 不引入任何形式的提示佇列（`0.1.0-W3-239`），含 Material 內建 FIFO。讓步的處置是
  **不呈現**，不是排隊等待。
- 不在元件內持有「當前顯示者」狀態（見第 0 節步驟 3）。
- 不改 SPEC-003 任一條文；不改 `0.1.0-W3-176` 已落地四個事件值的語意（本票只新增
  兩個事件值）。
- 不實作仲裁器（`0.1.0-W3-207`）。本票只實作載體側，且介面形狀對齊該票 Solution
  第 2.6 節的 C1／C3／C5／C6 四項載體側契約。
- G2（實際可見時長）／G3（未顯示的決定）不在本票（`0.1.0-W3-241`／`242`）。

#### 2. API Signatures

##### 2.0 須正面回答的設計問題：級別如何進入 `show()`

票面要求正面回答，三個候選逐一裁決：

| 候選 | 裁決 | 理由 |
|------|------|------|
| 由 `origin`（`AppSnackBarOrigin`）推導 | **否決** | 兩者不同軸且非單射。`origin` 的值域是 {使用者發起, 背景發起}，它回答「截斷這一則要不要記 warning」（SPEC-003〈截斷事件的日誌等級〉明訂以發起者為鍵）；級別回答「誰能截斷誰」。實查 §2.14 指派表：13 列中可棄組（列 1、3、5–10）橫跨自發型與等待型、亦橫跨背景發起（列 3）與使用者發起（列 5–10），不可棄組（列 11–13）全為自發型且不走 SnackBar。以 `origin` 推導在 0.1 只能得到常數函式（全部映射到可棄）；一旦 §2.14 新增一則背景發起的須留痕 SnackBar，其 `origin` 與列 3 完全相同而級別不同，推導函式無從區分。更根本的後果是耦合：合併後任一條規則的修改會污染另一條 |
| 由 `variant`（`plain` / `withAction`）推導 | **否決** | SPEC-003〈截斷事件的日誌等級〉已明寫「以變體為鍵會同時誤判兩邊」，該論證對級別同樣成立且更強——變體是版面決策（T4 的產物），與級別無因果 |
| 由呼叫端查 §2.14 指派表後以顯式參數傳入 | **採用** | 級別是請求的屬性、在標定點標定（SPEC-003 §2.14〈級別的標定依據〉，回答「級別是靜態標註還是動態推導？」——答案是靜態標註）。呼叫端即標定點所在處，是唯一持有該資訊的位置。參數**必填**（不給預設值）：預設值會使「確實是可棄」與「呼叫端忘了查表」在型別與日誌上都不可區分，而後者正是本票要消除的那一類無聲降級 |

##### 2.1 級別型別的落點

| 項目 | 規格 |
|------|------|
| 型別 | `enum AttentionLevel { discardable, mustLeaveTrace, undroppable }`，列舉順序即優先序，`index` 越大級別越高 |
| 對應 | 與 SPEC-003 §2.14 級別欄主名一一對應：可棄／須留痕／不可棄。0.1 無須留痕實例，仍為合法值（`0.1.0-W3-207` Phase 1 同此裁決） |
| 檔案 | **`lib/app/attention_level.dart`（新增，僅型別無行為）** |
| 為何不放元件內 | 票面 acceptance 第三項要求「元件內未另立一套級別定義」。若定義在 `app_snack_bar.dart`，`0.1.0-W3-207` 的仲裁器需要同一組值，而該票 acceptance 明訂仲裁器不得 import `AppSnackBar`——結果必然是兩份平行定義，即 §2.15 警告的兩份同一事實。放 L4 的獨立型別檔，兩者各自 import 同一份 |
| 與 `0.1.0-W3-207` 的關係 | 該票 Phase 1 的 2.2 節把此列舉寫在 `lib/app/attention_arbiter.dart` 內。名稱、值、順序完全相同，差別只在檔案。本票先落地，故由本票建立該檔；207 落地時改為 import。已登記為 spawn request 請 PM 對齊（第 10 節） |

##### 2.2 `show()` 簽名（粗體為本票新增）

```dart
static void show(
  BuildContext context, {
  required String message,
  required AttentionLevel level,            // 新增，必填
  AppSnackBarVariant variant = AppSnackBarVariant.plain,
  String? actionLabel,
  VoidCallback? onAction,
  Key? actionTestKey,
  AppSnackBarOrigin origin = AppSnackBarOrigin.background,
  AttentionHolderHint? currentHolder,       // 新增，過渡形態（見 2.7）
});
```

```dart
/// 呼叫端已知的當前通道持有者快照。
///
/// 過渡形態：`0.1.0-W3-207` 的仲裁器合併後，本型別與 `currentHolder` 參數一併
/// 由 `AttentionAccepted.preempted` 取代（收斂由該票承擔，見 2.7）。
final class AttentionHolderHint {
  const AttentionHolderHint({required this.showId, required this.level});
  final int showId;          // 被截斷者的識別（與 0.1.0-W3-176 的 showId 同一空間）
  final AttentionLevel level;
}
```

`currentHolder == null` 的語意是**未知**，不是「通道為空」。過渡期呼叫端無資訊來源，
一律傳 null；此時行為與現況一致（截斷後呈現），但事件必帶 `holderUnknown: true`，
使「這個判斷還沒有輸入」本身可被搜尋、可稽核，而非靜默退化為舊行為。

##### 2.3 截斷裁決（唯一的裁決表）

判定落在單一私有純函式，輸入恰為兩項，不接受 `variant`／`origin`／`message`／
`context`——函式體因此**不可能**引用它們（結構性可讀出，形態沿用
`0.1.0-W3-176` 的 `_resolveClosedLevel`）：

```dart
enum _Preemption { holderUnknown, preemptLower, replaceSameLevel, yieldToHolder }

static _Preemption _resolvePreemption(
  AttentionLevel incoming,
  AttentionHolderHint? holder,
);
```

| 持有者 | 新請求級別 | 結果 | 行為 | 依據 |
|--------|-----------|------|------|------|
| `null`（未知） | 任意 | `holderUnknown` | `hideCurrentSnackBar()` 後呈現；`preempted` 事件帶 `holderUnknown: true`、`preemptedShowId: null` | 過渡形態（2.7）。不得改為「不 hide 直接 show」——那會落入 Material 內建 FIFO，即 `0.1.0-W3-239` 禁止的佇列 |
| 級別 < 新請求 | 任意 | `preemptLower` | 截斷後呈現；`preempted` 事件 `sameLevelReplace: false` | 讓步規則 1「高級別抵達時可搶佔低級別」 |
| 級別 == 新請求 | 任意 | `replaceSameLevel` | 截斷後呈現；`preempted` 事件 `sameLevelReplace: true`（留痕） | `0.1.0-W3-239` 具名例外：規則 2 在本通道以截斷並留痕取代排隊或合併。**實作不得改回排隊** |
| 級別 > 新請求 | 任意 | `yieldToHolder` | **不呼叫 `hideCurrentSnackBar`、不呼叫 `showSnackBar`**，記 `yielded` 事件後返回 | 讓步規則 3「低級別不得搶佔任何請求」。規則 3 字面為「排隊等待」，但 `0.1.0-W3-239` 裁定不引入佇列，故本專案的形態是不呈現並留痕——與 SPEC-003〈顯示位爭用時的約束與 0.1 的處置規則〉「留痕是唯一不吸收症狀的滿足方式」同一理由，且對應 `0.1.0-W3-207` 裁決表的 `Skipped`／`Rejected` 兩格 |

比較鍵恆為 `AttentionLevel.index`；元件內無以載體型別、變體或呼叫路徑為鍵的分支。

**不變式 INV-SNACKBAR-NOQUEUE**：`ScaffoldMessengerState.showSnackBar` 的呼叫在
`AppSnackBar` 內恰一處，且該處必然緊接於同一函式的 `hideCurrentSnackBar()` 之後。
任何「不 hide 就 show」的路徑都會使 Material 內建 FIFO 生效，構成 `0.1.0-W3-239`
禁止的提示佇列，並使 SPEC-003 §2.16／§2.17「佔用量恆為 0 或 1」的既有結論失效。

##### 2.4 日誌事件（新增兩值，既有四值語意不變）

```dart
enum AppSnackBarLogEvent { shown, skippedUnmounted, actionPressed, closed,
                           preempted, yielded }
```

| 事件 | 何時 | `fields`（於 `baseFields` 之外） | 等級 |
|------|------|--------------------------------|------|
| `preempted` | 截斷發生的當下，由**截斷者**記 | `level`、`preemptedShowId`（`int?`）、`preemptedLevel`（`AttentionLevel?`）、`sameLevelReplace`（`bool`）、`holderUnknown`（`bool`） | 無（info）。**被截斷那一則的嚴重度不由本事件承擔**——SPEC-003〈截斷事件的日誌等級〉的鍵是被截斷者的發起者，截斷者不知道它，該判定仍由被截斷者自己的 `closed{reason: hide}` 經 `_resolveClosedLevel` 承擔，本票不改該路徑 |
| `yielded` | 低級別讓步、決定不呈現時 | `level`、`holderShowId`、`holderLevel`、`variant`、`message` | 依 `origin`：背景發起 warning（900）、使用者發起 info。**鍵與 §2.13 一致**——此處被丟棄的告知就是新來的這一則，其發起者即 `origin` |

`preempted` 與被截斷者的 `closed{showId, reason: hide}` 兩筆合起來回答票面 acceptance
第二項：前者給出「誰截斷了誰」的關聯，後者給出被截斷者的身分與嚴重度。過渡期
`preemptedShowId` 為 null 時，關聯由 `closed{reason: hide}` 單獨承擔（即 `0.1.0-W3-176`
的現況能力），且 `holderUnknown: true` 明記關聯缺口的成因。

##### 2.5 動作回呼的順序（處置轉來的觀察 B）

現行順序 `log(actionPressed) → onAction() → hideCurrentSnackBar(reason: action)` 改為：

```text
log(actionPressed) → hideCurrentSnackBar(reason: action) → onAction()
```

**為何不用 `controller.close()`**：`ScaffoldFeatureController.close()` 在 Flutter 內部
`assert(_snackBars.first == controller)` 且以預設 `reason: hide` 關閉——既會在被取代後
觸發 assert，又會把使用者按下動作的結局錯記為 `hide`（`_resolveClosedLevel` 據此判
warning），兩者都比現況差。改順序是最小且語意正確的修法：使用者按下動作，這一則就
結束了，`onAction` 的後續效果屬另一次事件。

##### 2.6 呼叫端對應表（級別取值以 SPEC-003 §2.14 為權威）

| 呼叫點 | 訊息 | §2.14 列 | `level` | `origin` |
|--------|------|---------|---------|---------|
| `main.dart` `ChooseFolderUnavailable` | `chooseFolderUnavailableMessage` | **未登記**（`0.1.0-W3-248` 補寫中） | `discardable`（過渡值，見 2.7） | `userInitiated` |
| `gap_report_screen.dart:199` | `sourceFileNotFoundSnackbarMessage` | 列 7 | `discardable` | `userInitiated` |
| `gap_report_screen.dart:218` | `openedExternallyMessage` / `externalOpenFailedMessage` | 列 5／列 6 | `discardable` | `userInitiated` |
| `scan_notification_controller.dart:202` | `scanCompleteSnackbarMessage` / `scanCompleteNoGapsSnackbarMessage` | 列 3 | `discardable` | `background` |

**附帶發現（本票必須順手修正的既有缺口）**：`gap_report_screen.dart` 兩處均未傳
`origin`，取預設值 `background`，但兩者都是使用者按下破洞項觸發——依 SPEC-003
〈截斷事件的日誌等級〉應為 `userInitiated`。現況會把使用者發起的截斷誤記為 warning。
必填 `level` 已迫使這兩處改動，同批修正 `origin` 的邊際成本為零。此二檔不在票面
`where.files`，見第 10 節。
##### 2.7 過渡形態與收斂路徑

`0.1.0-W3-207`（仲裁器）現為進行中、實作尚未合併（實查 `lib/` 無仲裁器檔案），
故本票依票面 `how` 的過渡條款實作，並在此明記形態與收斂 trigger：

| 期別 | `currentHolder` 的來源 | `hide` 條件 | 級別比較發生在 |
|------|----------------------|------------|--------------|
| 過渡（本票落地後至 `0.1.0-W3-207` 合併前） | 呼叫端傳入；0.1 呼叫端無來源，一律 null（未知） | `_resolvePreemption` 回傳非 `yieldToHolder` | 元件內單一純函式 `_resolvePreemption`。**元件不持有任何持有者狀態**——資訊逐次由參數帶入，呼叫結束即消失 |
| 收斂（`0.1.0-W3-207` 合併後，由該票執行） | 仲裁決策 `AttentionAccepted.preempted`（該票 Solution 2.6 C1） | `preempted` 非空 | 仲裁器（L4），元件不再比較。`AttentionHolderHint` 與 `currentHolder` 參數一併刪除 |

**收斂 trigger**：`0.1.0-W3-207` 合併（狀態 (b) 明確 trigger 延後，
`decision-trigger-binding` 規則 2 合法形式）。過渡期的可稽核標記為每筆 `preempted`
事件的 `holderUnknown: true`——收斂完成時該欄位應在正式路徑上不再出現，可作為
收斂是否落地的機械判據。

**過渡期為何仍要建立完整的三條裁決路徑**：SPEC-003〈鑑別「綁級別」與「綁呼叫路徑」
兩種實作〉明訂「驗收形態必須為未來新增事件預先準備鑑別能力，而非只驗證現況」，且
其行為測試要求「注入一個目前佔用者為高級別的測試替身狀態」——`currentHolder` 參數
即該注入點。過渡期正式路徑恆走 `holderUnknown`，另三條路徑由元件層單元測試以直接
傳入快照驗證，這正是該節指定的驗收方式。

**過渡期的誠實邊界**：正式路徑上跨級別的讓步規則 1／3 不會被執行到（`holderUnknown`
無條件截斷）。這不構成規格違反——SPEC-003〈跨級別：規則 1／3 對列 1–10 與列 11–13
的驗收〉已以時序互斥證明兩規則對本專案**空成立**，且 0.1 經 `AppSnackBar` 的訊息
全為可棄一級（2.6 表逐列），跨級別情形在正式路徑上不存在。

#### 3. GWT Scenarios

級別縮寫：可棄＝D、須留痕＝T、不可棄＝U。「持有者」一律指 `currentHolder` 參數。

正常流程：

- **場景 1（過渡期正式路徑）**：Given `currentHolder == null`；When 以 `level: D` 呼叫
  `show`；Then 呼叫 `hideCurrentSnackBar()` 後呈現，發出 `preempted` 事件且
  `holderUnknown == true`、`preemptedShowId == null`，隨後發出 `shown`。
- **場景 2（高級別搶佔低級別）**：Given `currentHolder = (showId: 7, level: D)`；When 以
  `level: U` 呼叫；Then 截斷後呈現，`preempted` 事件 `preemptedShowId == 7`、
  `preemptedLevel == D`、`sameLevelReplace == false`、`holderUnknown == false`。
- **場景 3（同級別截斷並留痕）**：Given `currentHolder = (showId: 7, level: D)`；When 以
  `level: D` 呼叫；Then 截斷後呈現，`preempted` 事件 `sameLevelReplace == true`。
- **場景 4（低級別讓步）**：Given `currentHolder = (showId: 7, level: U)`；When 以
  `level: D`、`origin: background` 呼叫；Then **不發出 `shown`**、畫面上原持有者未被
  清除，發出 `yielded` 事件（`holderShowId == 7`、`holderLevel == U`、`level == D`、
  帶 `message`），等級為 900。
- **場景 5（讓步的等級鍵）**：同場景 4 但 `origin: userInitiated`；Then `yielded` 事件
  等級為 info（無 900）。
- **場景 6（動作按下的順序）**：Given `withAction` 已呈現；When 按下動作；Then 事件序為
  `actionPressed` 在前，`closed{reason: action}` 隨後，且 `onAction` 於 `hide` 之後被
  呼叫恰一次。
- **場景 7（`main.dart` 收斂）**：Given `chooseFolder` 回傳 `ChooseFolderUnavailable`；
  When 分支執行；Then 經 `AppSnackBar.show`（`plain`、`origin: userInitiated`、
  `level: D`）呈現 `chooseFolderUnavailableMessage`，且 `main.dart` 內
  `ScaffoldMessenger.of(context).showSnackBar` 零命中。

邊界條件：

- **場景 8（讓步不排隊）**：Given 場景 4 的讓步已發生；When 等待超過
  `Motion.snackBarWithAction`；Then 被讓步的那一則**不會**在稍後自行出現（無延遲呈現、
  無佇列），全程只有一筆 `yielded`。
- **場景 9（動作回呼同步再顯示，觀察 B）**：Given `withAction` 已呈現，其 `onAction`
  同步呼叫 `AppSnackBar.show`；When 按下動作；Then 新的一則不被 `reason: action` 關閉
  ——新 `showId` 的 `closed` 事件不得帶 `reason: action`；使用者可見到新的一則。
- **場景 10（`hideCurrentSnackBar` 呼叫點守衛，觀察 A）**：靜態掃描
  `lib/components/app_snack_bar.dart` 原始碼：`hideCurrentSnackBar` 恰 2 處（一處預設
  `hide`、一處明確 `reason: action`），`removeCurrentSnackBar` 與 `clearSnackBars` 皆 0；
  全 `lib/` 範圍內該三個識別符在本元件之外零命中。
- **場景 11（裁決函式不接受無關輸入）**：靜態檢查 `_resolvePreemption` 的參數恰為
  `(AttentionLevel, AttentionHolderHint?)`，其函式體不含 `variant`／`origin`／
  `message`／`context`／`Type` 判斷（SPEC-003〈鑑別「綁級別」與「綁呼叫路徑」〉靜態
  檢查列）。
- **場景 12（級別定義唯一）**：靜態檢查 `AttentionLevel` 在 `lib/` 內恰一處宣告，且
  三個值與 SPEC-003 §2.14 級別欄主名一一對應；`app_snack_bar.dart` 內無第二套級別
  列舉或級別常數。
- **場景 13（`showSnackBar` 呼叫點唯一且必接於 hide 之後）**：靜態檢查
  INV-SNACKBAR-NOQUEUE（2.3）。

異常流程：

- **場景 14（context 已卸載）**：Given `context.mounted == false`；When 呼叫 `show`；
  Then 行為與現況一致——發出 `skippedUnmounted`（warning）後返回，且**不呼叫**
  `hideCurrentSnackBar`（不得因為新增裁決而在早退前先截斷）。
- **場景 15（`withAction` 缺 slot）**：Given `variant: withAction` 而 `actionLabel` 或
  `onAction` 為 null；When 呼叫；Then 觸發既有 assert（行為不變）。
- **場景 16（持有者快照已失效）**：Given `currentHolder` 指向一則已自然過期消失的
  SnackBar；When 以較低級別呼叫；Then 元件仍依級別讓步（不驗證快照新鮮度）。**理由**：
  快照新鮮度屬持有者狀態，元件不持有亦不查詢；收斂後由 `0.1.0-W3-207` 的逾期懶檢查
  （該票場景 11）承擔。過渡期呼叫端一律傳 null，此路徑不會在正式路徑發生。

#### 4. Error Handling

| 錯誤情境 | 處理策略 | 回傳／拋出 | 需要回滾？ |
|---------|---------|-----------|-----------|
| `context` 已卸載 | 早退，不觸碰 messenger | `skippedUnmounted`（warning），無截斷 | 否 |
| `withAction` 缺 `actionLabel` / `onAction` | 開發期 assert（既有） | assert 訊息不變 | 否 |
| 呼叫端未傳 `level` | 編譯失敗 | 必填參數，無執行期分支 | 否 |
| `currentHolder` 為 null（資訊缺席） | 不猜測、不代填，退回無條件截斷並標記 | `preempted{holderUnknown: true}` | 否 |
| `currentHolder` 快照已失效 | 依級別裁決，不驗證新鮮度（場景 16） | 對應決策事件 | 否 |
| 讓步後呼叫端仍期待訊息出現 | 不補救、不排隊 | `yielded` 事件即該請求的終局；等級依 `origin` 使背景發起者可被 warning 發現 | 否 |
| `onAction` 內拋出例外 | 不攔截（既有行為） | 由全域錯誤攔截承接 | 否 |

#### 5. Dependencies

| 依賴 | 類型 | 用途 | 不可用時 |
|------|------|------|---------|
| SPEC-003 §2.14 指派表 | 規格 | 呼叫端 `level` 取值的唯一權威 | 缺列者以過渡值 + trigger 處理（`chooseFolderUnavailableMessage` → `0.1.0-W3-248`） |
| SPEC-003〈讓步規則的可驗收形態〉 | 規格 | 2.3 裁決表三條規則的依據與驗收形態 | 無替代 |
| SPEC-003〈顯示位爭用時的約束與 0.1 的處置規則〉〈重評結果〉 | 規格 | 不引入佇列、同級截斷並留痕的裁決（`0.1.0-W3-239`） | 無替代；實作不得自行推翻 |
| `0.1.0-W3-176` 產出 | 已完成 | `showId`、`origin`、`_resolveClosedLevel`、四事件 `baseFields` | 已在庫 |
| `0.1.0-W3-207` Solution 2.6（C1／C3／C5／C6） | 進行中 | 載體側契約形狀，本票介面對齊之 | 未合併時走過渡形態（2.7） |
| `0.1.0-W3-248` | 待處理 | `chooseFolderUnavailableMessage` 的 §2.14 登記 | 未完成時以過渡值 `discardable` 實作並於本節記錄 |
| Material `ScaffoldMessenger` | 框架 | 呈現與 `closed` future | 無替代 |
#### 6. Acceptance（展開票面四項）

票面 1（`lib/main.dart` 不再直接呼叫 `ScaffoldMessenger.showSnackBar`，全部提示經 `AppSnackBar`）：

- [ ] `lib/main.dart` 內 `ScaffoldMessenger` 與 `showSnackBar` 兩個識別符零命中（靜態）
- [ ] `ChooseFolderUnavailable` 分支經 `AppSnackBar.show` 呈現同一 i18n key，`origin: userInitiated`（場景 7）
- [ ] `lib/` 內 `ScaffoldMessenger.of(...).showSnackBar` 的呼叫點恰一處，且位於 `lib/components/app_snack_bar.dart`（靜態）

票面 2（被截斷時日誌可回答被截斷的是哪一則）：

- [ ] 截斷發生時發出 `preempted` 事件，`fields` 含 `preemptedShowId`、`preemptedLevel`、`sameLevelReplace`、`holderUnknown` 四鍵（場景 1／2／3）
- [ ] 被截斷者自身的 `closed{reason: hide}` 仍帶其 `showId` 與依 `origin` 判定的等級（`0.1.0-W3-176` 既有能力不回退）
- [ ] 讓步（未呈現）時發出 `yielded` 事件並帶 `message`、`level`、`holderShowId`、`holderLevel`（場景 4）

票面 3（級別取值以標題文字引用 SPEC-003，元件內未另立一套級別定義）：

- [ ] `AttentionLevel` 在 `lib/` 內恰一處宣告，位於 `lib/app/attention_level.dart`（場景 12）
- [ ] 三個列舉值與 SPEC-003 §2.14 級別欄主名一一對應，dartdoc 以章節標題文字引用該表
- [ ] `app_snack_bar.dart` 內無第二套級別列舉、級別常數或級別推導函式（不由 `origin`／`variant` 推導，2.0）

票面 4（截斷條件綁在級別上：低級別不得 hide、同級別截斷並留痕、僅高級別可截斷，三者各有測試）：

- [ ] 行為測試三條：場景 4（低級別讓步，持有者未被清除）、場景 3（同級別截斷且 `sameLevelReplace: true`）、場景 2（高級別截斷）
- [ ] 靜態檢查：`_resolvePreemption` 參數恰為 `(AttentionLevel, AttentionHolderHint?)`，函式體不含 `variant`／`origin`／`message`／`context`（場景 11）
- [ ] **靜態與行為兩者皆通過方算通過**——SPEC-003〈鑑別「綁級別」與「綁呼叫路徑」兩種實作〉明訂「只通過行為測試不算通過本節」

不引入佇列（`0.1.0-W3-239` 裁決的守衛）：

- [ ] INV-SNACKBAR-NOQUEUE：`showSnackBar` 在元件內恰一處且緊接於 `hideCurrentSnackBar()` 之後（場景 13）
- [ ] 讓步路徑不呼叫 `hideCurrentSnackBar` 亦不呼叫 `showSnackBar`，被讓步的訊息不會延後出現（場景 8）
- [ ] 元件內無任何集合型欄位持有待顯訊息（`List`／`Queue`／`Map`）；元件內無「當前顯示者」狀態欄位

轉來的兩項觀察（第 7 節）：

- [ ] 場景 10 的守衛測試存在且綠燈（觀察 A）
- [ ] 場景 9 的順序測試存在且綠燈（觀察 B）

#### 7. 由 `0.1.0-W3-176` Phase 4a 轉來的兩項觀察：本票的處置

**觀察 A（`reason == hide` 是承重前提但無守衛）——處置：降低承重 + 加守衛，兩者並行。**

| 項目 | 內容 |
|------|------|
| 承重降低 | 「被截斷的是哪一則」不再單靠 `reason == hide` 反推：截斷方在截斷當下主動發出 `preempted` 事件，關聯由該事件直接給出（2.4）。`reason == hide` 的剩餘承重僅剩 `_resolveClosedLevel` 的等級判定 |
| 守衛 | 場景 10 的靜態掃描測試：`hideCurrentSnackBar` 恰 2 處、`removeCurrentSnackBar` 與 `clearSnackBars` 為 0、且三者在 `lib/` 內不出現於本元件之外。新增第三個呼叫點時該測試紅燈，前提破壞不再無聲 |
| 為何不改為完全不依賴 `reason` | `_resolveClosedLevel` 的輸入來自 Material 的 `closed` future，改為自建判定需在元件內追蹤「這一則是否被自己截斷」，即持有者狀態的變體，與第 0 節步驟 3 衝突 |

**觀察 B（動作回呼順序在理論上可產生未稽核截斷）——處置：改順序，並以測試鎖住。**

新設計仍有該形態的**成立條件**（`onAction` 同步呼叫 `show`），故不能只依賴「當前不可
達」。修法見 2.5（`hide(action)` 移到 `onAction()` 之前），驗收見場景 9。改後即使
`onAction` 同步顯示新的一則，`reason: action` 已作用於舊的那一則，新的不會被「使用者
一眼未見即以 action 關掉」。

#### 8. 行為單元清單（供 Phase 2）

| # | 行為單元 | 對應場景 | 型別 | 獨立可測 |
|---|---------|---------|------|---------|
| B1 | 未知持有者時截斷並標記 | 1 | 行為（widget test + log spy） | 是 |
| B2 | 高級別搶佔低級別 | 2 | 行為 | 是 |
| B3 | 同級別截斷並留痕 | 3 | 行為 | 是 |
| B4 | 低級別讓步不呈現 | 4、8 | 行為 | 是 |
| B5 | 讓步事件等級依 `origin` | 5 | 行為 | 是 |
| B6 | 動作按下的事件序與 `hide` 先於 `onAction` | 6 | 行為 | 是 |
| B7 | `onAction` 同步再顯示不被誤殺 | 9 | 行為 | 是 |
| B8 | `main.dart` 分支經元件層 | 7 | 行為（widget test） | 是 |
| B9 | `hideCurrentSnackBar` 呼叫點守衛 | 10 | 靜態（原始碼掃描） | 是 |
| B10 | 裁決函式輸入面 | 11 | 靜態 | 是 |
| B11 | 級別定義唯一且對應 §2.14 主名 | 12 | 靜態 | 是 |
| B12 | INV-SNACKBAR-NOQUEUE | 13 | 靜態 | 是 |
| B13 | context 已卸載時不截斷 | 14 | 行為 | 是 |
| B14 | `withAction` slot assert 不變 | 15 | 行為 | 是 |

B9／B10／B11／B12 四項為靜態檢查（讀原始碼字串），落 `test/unit/components/`；
其餘為行為測試。靜態與行為並行是 SPEC-003〈鑑別「綁級別」與「綁呼叫路徑」〉的
明文要求，Phase 2 不得只設計行為測試。
#### 9. 需求完善度驗證（`.claude/skills/spec/SKILL.md` validate 流程）

**Layer 1 結構檢查（Full 模式 6 區段）**：通過。

| 檢查項 | 結果 |
|--------|------|
| 6 區段存在且非空 | Purpose（1）／API Signatures（2）／GWT Scenarios（3）／Error Handling（4）／Dependencies（5）／Acceptance（6）皆存在 |
| GWT 完整三元組 | 16 個場景，正常 7 / 邊界 6 / 異常 3；靜態檢查型場景（10–13）以「靜態檢查」取代 When-Then 敘述，判定為合格變體（驗收對象為原始碼而非執行行為） |
| Acceptance 可驗證性 | 全部以 `- [ ]` 起首 |
| Purpose 簡潔性 | 未超過 500 字 |
| API surface（HTTP） | 不適用：本功能無 HTTP endpoint |
| domain-map 覆蓋 | 不適用：ticket 級 feature-spec，非規劃波 domain spec |

**Layer 2 語義推演：第一輪 10 個未回答問題，逐一回答如下；第二輪掃描無新問題。**

維度 1（邊界完整性）：

| # | 問題 | 回答 |
|---|------|------|
| Q1 | `currentHolder.showId` 與本次呼叫產生的 `showId` 指向同一請求（自我截斷）時的行為？ | 元件不處理。過渡期呼叫端恆傳 null，此輸入不可達；收斂後「持有者再請求」由 `0.1.0-W3-207` 裁決表最後一列以 `Rejected` + `heldAndRequested` 在仲裁層擋下，元件收不到 `Accepted`。元件若自行判定「自我」需比對持有者身分，即持有者狀態的變體，與第 0 節步驟 3 衝突 |
| Q2 | `message` 為空字串或超長時？ | 既有行為不變。文案成分與長度由 SPEC-004 §4.0.12 與 §4.26 內容政策管轄，本票不新增約束、不改既有截斷處置 |
| Q3 | 未來 `AttentionLevel` 新增第四值時，2.3 裁決表是否仍完整？ | 是。比較鍵為 `index` 的三分（小於／等於／大於），對任意有序列舉皆為完整劃分，新增值不需改裁決函式。新增值需要的是 §2.14 補列與該值的卸載動作裁決，屬規格層 |

維度 2（錯誤路徑）：

| # | 問題 | 回答 |
|---|------|------|
| Q4 | 讓步（未呈現）時，`show` 是否應回傳決策讓呼叫端得知？ | **不改，維持 `void`。** 0.1 呼叫端對「未呈現」沒有任何合法補救動作——重試或暫存即為呼叫端自建佇列，違反 `0.1.0-W3-239`。給回傳值會誘發那條路徑。留痕由 `yielded` 事件承擔，其等級依 `origin` 使背景發起的丟棄以 warning 可被發現 |
| Q5 | `logSink` 自身拋出例外時？ | 既有行為不變（`logSink` 為 `@visibleForTesting` 注入點，生產路徑固定為 `_defaultLogSink`）。不在本票範疇 |
| Q6 | 讓步後被丟棄的訊息，使用者完全不會知道——是否需要 SPEC-003〈因判準而不顯示時的日誌承接〉的四欄位事件？ | 不需要。該節的觸發條件是「判準 T 判定不使用暫態提示且三個承接皆不可行」，發生在請求方、載體呼叫之前；本票的讓步發生在載體內、判準 T 已判定要用 SnackBar 之後，兩者是不同觀測點。`yielded` 事件是載體側的對應物，欄位依 `0.1.0-W3-176` `baseFields` 形態設計，不套用該節的四欄位 schema |

維度 3a（狀態轉換完整性）：

| # | 問題 | 回答 |
|---|------|------|
| Q7 | 讓步是否引入新的元件狀態？ | 否。被讓步的請求從未進入 `shown`，SPEC-004 §4.26 狀態矩陣不新增節點 |
| Q8 | §4.26 狀態矩陣「新的 SnackBar 取代 → dismissed」在讓步路徑上不成立，規格是否需同步？ | 需要，但不在本票 `where.files`（本票全程唯讀規格）。已登記 spawn request 請 PM 開 DOC 票，於該列與回饋契約「重複觸發防護：冪等」加級別條件 |

維度 3b（約束條件違反行為）：

| # | 問題 | 回答 |
|---|------|------|
| Q9 | 約束「元件不持有持有者狀態」被違反時如何偵測？ | 第 6 節 acceptance「元件內無任何集合型欄位持有待顯訊息；元件內無當前顯示者狀態欄位」為靜態檢查項，B12 承接 |
| Q10 | 約束「不引入任何形式的佇列」被違反時如何偵測？ | INV-SNACKBAR-NOQUEUE（2.3）+ 場景 13 + B12。違反的具體形態是「不 hide 直接 show」，該形態使 Material 內建 FIFO 生效——它不像自建 `Queue` 那樣顯眼，故需要明寫的不變式而非只靠程式碼審閱 |

維度 4（教學一致性）：**skipped**——專案 CLAUDE.md 無「教學模組對應表」章節（實查零命中），依 SKILL.md 降級條款不計入未回答問題數。

維度 5（資源競爭，訊號詞命中「提示／通知／事件／背景／佇列」）：

| 三問 | 回答 |
|------|------|
| 到達類別與級別是否已標定？ | 是。2.6 表逐呼叫點對應 SPEC-003 §2.14 的列與級別；唯一未登記者（`chooseFolderUnavailableMessage`）以過渡值 + trigger `0.1.0-W3-248` 處理 |
| 與哪些既有事件流共用同一通道？ | §2.14 全 13 列共用單一通道「使用者呈現焦點」（`docs/domain-map.md` §2.6.1）。經 `AppSnackBar` 者為列 3、5–8、10 及待登記的資料夾不可用一則 |
| 通道進入卸載時本功能的行為？ | 本通道無佔用量觸發的卸載狀態（SPEC-003 §2.16 已證，佔用量恆為 0 或 1）。事件級的讓步行為已於 2.3 定義（讓步＝不呈現並留痕，對應 `0.1.0-W3-207` 的 `Skipped`／`Rejected`） |
| 附帶：逐筆日誌本身是否構成通道負載？ | 否。沿用 `0.1.0-W3-207` Phase 1 2.5 的既有結論——請求率受單一使用者的互動速率上界，逐筆記錄不構成負載，計數退化形態不啟用 |

**迭代結論**：第二輪掃描（維度 1、2、3a、3b、5）無新增未回答問題，validate 通過，
Phase 1 產出物就緒。
#### 10. 範疇缺口與已登記事項

**票面 `where.files` 不足以承載本設計。** 必填 `level` 參數迫使全部呼叫端改動，
清單如下（lib 端 5 檔，未超 `cognitive-load` 的「修改檔案數 > 5 須拆分」門檻；
功能職責數為 2——收斂入口、綁級別截斷，符合 `<= 2`）：

| 檔案 | 票面已列 | 改動 |
|------|---------|------|
| `lib/main.dart` | 是 | `ChooseFolderUnavailable` 分支改走 `AppSnackBar.show` |
| `lib/components/app_snack_bar.dart` | 是 | 簽名、裁決函式、兩個新事件、動作回呼順序 |
| `lib/app/attention_level.dart` | **否（新增檔）** | `AttentionLevel` 列舉（理由見 2.1） |
| `lib/screens/gap_report/gap_report_screen.dart` | **否** | 兩處呼叫補 `level` 與 `origin: userInitiated`（附帶發現，見 2.6） |
| `lib/screens/gap_report/scan_notification_controller.dart` | **否** | 一處呼叫補 `level` |
| `test/unit/components/app_snack_bar_test.dart` | 是 | 既有 T-1～T-9 需補參數；新增 B1–B14 |

替代方案（給 `level` 預設值 `discardable`，只改 `main.dart` 與元件）**經評估不採用**，
理由見 2.0——預設值會使「確實可棄」與「忘了查 §2.14」不可區分。已列入 NeedsContext
請 PM 裁定。

**已登記的 spawn request**：

| # | 內容 | 類型 |
|---|------|------|
| SR-1 | 對齊 `0.1.0-W3-207`：`AttentionLevel` 改 import `lib/app/attention_level.dart` | IMP |
| SR-2 | SPEC-004 §4.26 狀態矩陣與冪等取代條文補級別條件 | DOC |

**未另開票的發現**（依規則 5 逐項標註）：

| 發現 | 處置 |
|------|------|
| `chooseFolderUnavailableMessage` 不在 SPEC-003 §2.14 | 無需建 ticket：同一發現已由 `0.1.0-W3-207` SR-1 登記並建 `0.1.0-W3-248`，本票以過渡值 `discardable` + trigger 引用該票，不重複開票 |
| `gap_report_screen.dart` 兩處 `origin` 誤取預設 `background` | 無需建 ticket：本票必填 `level` 已迫使該二處改動，同批修正，邊際成本為零；已列入上表與 NeedsContext 的 `where.files` 擴充請求 |
| 動作回呼同步再顯示時 Material 內部佇列深度瞬時為 2 | 無需建 ticket：屬 Material `hideCurrentSnackBar` 的反向動畫期間既有行為（`0.1.0-W3-165` 之前即如此），非本票引入；且 2.5 改序後該路徑上被關閉的是舊的那一則，SPEC-003 §2.16「佔用量恆為 0 或 1」的架構層結論不受影響 |
### Phase 2 測試設計（sage-test-architect，2026-09-10）

本節只設計測試，不寫任何實作或測試程式碼。全程唯讀 `SPEC-003`／`SPEC-004`。
Phase 1 規格（本 Solution 第 0–10 節）為唯一輸入，本節不重新推導任何判準，
亦不改動 Phase 1 的任何裁決；發現的規格層缺口一律記於第 9 節交 PM，不自行選邊。

引用約定沿用 Phase 1：`SPEC-003` 以章節標題文字定位，不用行號。

#### P2.0 測試策略與分層決策

| 決策項 | 結論 | 依據 |
|-------|------|------|
| 分層 | Layer 1（UI／元件庫元件）關鍵流程 → widget test；原始碼契約 → 純 `test()` 讀檔斷言 | `hybrid-testing-strategy-methodology` 分層決策樹。本票無 Layer 3 UseCase、無 Layer 5 Domain，故無 BDD 分層與 domain 不變式軸的消費對象 |
| 靜態檢查的必要性 | **強制，不可只做行為測試** | `SPEC-003`〈鑑別「綁級別」與「綁呼叫路徑」兩種實作〉驗收列：「靜態檢查與行為測試兩者皆須通過；只通過行為測試不算通過本節」 |
| 靜態檢查的既有形態 | 讀原始碼字串 + 正規表示式斷言，`test/window_size_contract_test.dart` 為本專案既有先例（跨語言常數對齊），本票沿用同一形態 | 專案既有慣例，不新立測試機制 |
| 時間推進 | 一律用 `flutter_test` fake clock（既有 `_pumpBy` 輔助函式）+ `Motion.*` token | `test-assertion-design-rules` 規則 D1：主套件禁 `Stopwatch` + `lessThan` 作 pass-fail 斷言 |

**Mock 策略（Sociable Unit Tests 原則）**：

| 依賴 | 是否替身 | 理由 |
|------|---------|------|
| `AppSnackBar.logSink` | **是**（既有 `@visibleForTesting` 注入點） | 日誌投影是本票的主要驗收面；替身為既有機制，本票不新增注入點 |
| `ScaffoldMessenger` / Material `SnackBar` | **否**，用真實 Material | 真實 Material 的 FIFO 行為正是 INV-SNACKBAR-NOQUEUE 要防的東西。替換掉它，違反不變式的實作會全綠通過——守衛失效且無聲 |
| `AttentionLevel` / `AttentionHolderHint` | **否**，直接構造真值 | 兩者皆為無行為的值型別（Phase 1 §2.1／§2.2），屬內層物件，`behavior-first-tdd-methodology` 明訂不 mock |
| `BuildContext` | **否**，用 widget tree 真實 context | `context.mounted` 的真實語意是場景 14 的驗收對象 |

**測試耦合目標**：全部斷言以「事件值 + `fields` 結構化欄位 + 畫面可見性」為對象，
不比對日誌散文字面（沿用 `0.1.0-W3-165` 既有規範：`0.1.0-W3-132` 尚未把日誌訊息改為
事件識別碼，比對字面會與其目標衝突）；不斷言私有函式的呼叫次數。唯一例外是第 P2.5 節
的原始碼契約測試，其驗收對象**本來就是**原始碼文字，非執行期行為。
#### P2.1 測試檔案配置與群組劃分（Phase 3b 拆分友善）

Phase 1 §10 認定本票功能職責數為 2（收斂入口、綁級別截斷）。測試群組依此對齊，
使 Phase 3b 可按職責分派而不需跨群組讀取。

| 群組 | 功能職責歸屬 | 檔案 | 內容 | 跨群組依賴 |
|------|------------|------|------|-----------|
| G-A 入口收斂 | 職責 1 | `test/unit/app/home_page_workspace_test.dart`（既有，增修一個測試） | T-205-21 | 無 |
| G-B 截斷裁決 | 職責 2 | `test/unit/components/app_snack_bar_test.dart`（既有，新增群組） | T-205-1 ～ T-205-9 | 共用 log spy fixture（見下） |
| G-C 動作回呼順序 | 職責 2 | 同上（新增群組） | T-205-10 ～ T-205-12 | 同上 |
| G-D 既有行為不回歸 | 職責 2 | 同上（既有群組遷移） | 見 P2.6 遷移表 | 同上 |
| G-E 原始碼契約 | 職責 1 + 2 | `test/unit/components/app_snack_bar_source_contract_test.dart`（**新增檔**） | T-205-13 ～ T-205-20 | **無**（不 import 任何 lib 符號，只讀檔） |

**共用 fixture 的處理**：G-B／G-C／G-D 共用同一組 log spy（`setUp` 裝上、`tearDown`
還原、`records` 清空）。既有測試檔已有兩份幾乎相同的 spy（`診斷日誌（0.1.0-W3-165）`
群組的 `events`／`levels` 版、`關聯識別與截斷等級判別（0.1.0-W3-176）` 群組的 `records`
版）。**Phase 3b 應把 `_SnackBarLogRecord` 版抽為檔案級輔助函式，供三個群組各自在自己的
`setUp` 內呼叫**，不使用跨群組共享的可變狀態——各群組獨立呼叫同一個建構函式，任一群組
可單獨執行。不改寫 `0.1.0-W3-165` 群組既有的 `events`／`levels` 兩個清單（P2.6 只補值，
不換機制），避免一次改動同時觸及機制與斷言。

**G-E 為新增檔的四項理由**（本項需 PM 裁定，見 P2.9）：

1. 既有 `app_snack_bar_test.dart` 為 703 行，加入 G-B／G-C／G-D 後逾 1100 行，
   遠超 `cognitive-load` 的「目標檔案行數 > 300 行 → 分析 domain 邊界，考慮拆分」。
2. G-E 的驗收對象是原始碼文字，G-B～G-D 的驗收對象是執行期行為——兩者的 domain 不同，
   合併即單一檔案承載 2 個 domain。
3. G-E 不需要 widget harness、不需要 log spy、不 import 任何 `lib/` 符號，是本票中唯一
   可完全獨立分派的群組。
4. 本專案已有同形先例：`test/window_size_contract_test.dart` 即獨立的原始碼契約測試檔。

若 PM 裁定不新增檔案，G-E 全部內容原樣落入 `app_snack_bar_test.dart` 的獨立 `group`，
測試規格本身不需改寫（G-E 對其他群組零依賴，搬移為機械動作）。

#### P2.2 級別縮寫與共用術語

| 縮寫 | `AttentionLevel` 值 | SPEC-003 級別主名 |
|------|-------------------|-----------------|
| D | `discardable` | 可棄 |
| T | `mustLeaveTrace` | 須留痕 |
| U | `undroppable` | 不可棄 |

「持有者」一律指傳入 `show()` 的 `currentHolder` 參數（Phase 1 §2.2），不是元件內部狀態
——元件不持有任何持有者狀態（Phase 1 §0 步驟 3）。
#### P2.3 裁決表窮舉矩陣（G-B 的骨幹，T-205-1）

Phase 1 §2.3 的裁決表以「持有者級別 vs 新請求級別」的三分（小於／等於／大於）加上
「持有者未知」共四種結果。本節把它展開為 12 格參數化窮舉，每格一個測試案例。

**為何窮舉而非只測三條路徑**：`SPEC-003`〈鑑別「綁級別」與「綁呼叫路徑」兩種實作〉的
觸發時機列明訂「§2.14 新增一列，其級別與該列共用載體上既有列的級別不同」——驗收形態
必須為未來新增事件預先準備鑑別能力，而非只驗證現況。0.1 現況只有可棄一級，只測三條
代表路徑無法在新增「須留痕」列時給出紅燈。12 格窮舉是這項要求對本元件的最小完整形態。

| 持有者 | 新請求 D | 新請求 T | 新請求 U |
|-------|---------|---------|---------|
| `null`（未知） | `holderUnknown` | `holderUnknown` | `holderUnknown` |
| D | `replaceSameLevel` | `preemptLower` | `preemptLower` |
| T | `yieldToHolder` | `replaceSameLevel` | `preemptLower` |
| U | `yieldToHolder` | `yieldToHolder` | `replaceSameLevel` |

**每一種結果的斷言集合**（測試依格別套用對應集合）：

| 結果 | 畫面 | 事件 | `fields` |
|------|------|------|---------|
| `holderUnknown` | 新訊息可見 | `preempted` 先於 `shown`，無 `yielded` | `holderUnknown == true`、`preemptedShowId == null`、`preemptedLevel == null`、`sameLevelReplace == false`、`level == ` 新請求級別 |
| `preemptLower` | 新訊息可見、舊訊息不可見 | `preempted` 先於 `shown`；持有者產生 `closed{reason: hide}` | `holderUnknown == false`、`preemptedShowId ==` 持有者 `showId`、`preemptedLevel ==` 持有者級別、`sameLevelReplace == false` |
| `replaceSameLevel` | 同上 | 同上 | 同上，但 `sameLevelReplace == true` |
| `yieldToHolder` | **舊訊息仍可見、新訊息不可見** | 只有 `yielded`；**無 `preempted`、無 `shown`**；持有者**無** `closed{reason: hide}` | `holderShowId ==` 持有者 `showId`、`holderLevel ==` 持有者級別、`level ==` 新請求級別，且帶 `variant`、`message` |

**`sameLevelReplace` 與 `preemptedLevel == level` 在語意上重複，此為刻意**：
`0.1.0-W3-239` 的具名例外（同級別以截斷並留痕取代排隊）需要一個可直接搜尋的欄位，
不需讀者自行推導兩個級別欄位是否相等。測試對兩者**皆**斷言，使實作把 `sameLevelReplace`
接成常數 `false` 或誤接成 `preemptedLevel != null` 時會紅燈。

**測試骨架（每格共用，行為鏈式）**：

| 步驟 | 動作 | 前置條件斷言（Given 的驗證，不可省） |
|------|------|--------------------------------|
| 0 | 掛載 harness、裝上 log spy | `records` 為空、`find.byType(SnackBar)` 為 `findsNothing`、anchor `context.mounted` 為 `true` |
| 1（持有者非 `null` 的 9 格） | 以持有者級別 `show` 第一則（不傳 `currentHolder`），pump 過進場動畫 | 第一則可見；恰一筆 `shown`；由該筆取得 `holderShowId`（**不得寫死數字**，見 P2.7） |
| 2 | 清空 `records`，以新請求級別 + `currentHolder` 呼叫 `show` | 清空前先確認步驟 1 的事件已齊（`shown` 存在），避免把步驟 1 的殘留誤判為步驟 2 的產物 |
| 3 | pump `_materialTransition` 兩次（**不得** pump 超過 `Motion.snackBar`） | 逾時退場會產生 `closed{reason: timeout}`，與 `yieldToHolder` 要驗的「持有者未被截斷」互相污染 |
| 4 | 套用上表對應結果的斷言集合 | — |

**步驟 1 的持有者是真實顯示中的 SnackBar，不是憑空構造的快照**：`currentHolder` 的
`showId` 取自步驟 1 實際發出的 `shown` 事件。這使 `yieldToHolder` 的「持有者未被清除」
是可被畫面觀察的事實，而非僅由事件推論。
#### P2.4 GWT 規格逐條

**編號更正（本節為權威）**：P2.1 表格的編號區間為草擬值，最終編號以本節為準——
G-B = T-205-1～9、G-C = T-205-10～13（群組名更正為「動作 slot 與回呼順序」）、
G-E = T-205-14～21、G-A = T-205-22。P2.1 的群組劃分、檔案配置與依賴關係不變。

每一條的 Given 都必須有對應的前置驗證斷言（P2.3 骨架步驟 0／1），
Phase 3b 不得以「顯然成立」為由省略。

##### G-B 截斷裁決（`app_snack_bar_test.dart` 新增群組）

| # | 對應 | 維度 | Given | When | Then |
|---|------|------|-------|------|------|
| T-205-1 | B1–B4、場景 1／2／3／4 | 正常 | P2.3 骨架的 12 格參數化，每格的持有者為步驟 1 實際顯示的那一則 | 以該格的新請求級別與 `currentHolder` 呼叫 `show` | 套用 P2.3 對應結果的斷言集合（畫面 + 事件 + `fields` 三面皆驗） |
| T-205-2 | B5、場景 5 | 正常 | 持有者為 U 級別且顯示中；新請求為 D 級別 | 分別以 `origin: background` 與 `origin: userInitiated` 呼叫 | `yielded` 的 `level` 引數：背景發起為 `900`、使用者發起為 `800`。**鍵是 `origin` 不是 `variant`**——與 `SPEC-003`〈截斷事件的日誌等級〉同一判準 |
| T-205-3 | B4、場景 8 | 邊界 | T-205-2 的讓步已發生，`records` 已含恰一筆 `yielded` | 續 pump `Motion.snackBarWithAction + 2 × _materialTransition` | 被讓步那一則的訊息文字**全程**未出現；`records` 中 `shown` 事件不含該則的 `message`；`yielded` 仍恰一筆（不重送、不延後呈現、不排隊） |
| T-205-4 | 綁級別的行為面佐證 | 正常 | 持有者為 U 級別且顯示中 | 以 D 級別、`variant` 取 `plain` 與 `withAction` 各呼叫一次 | 兩者皆讓步（`yielded`、無 `shown`）。證明裁決對 `variant` 不敏感，與 T-205-16 的靜態檢查構成 `SPEC-003`〈鑑別「綁級別」與「綁呼叫路徑」〉要求的雙重確認 |
| T-205-5 | 票面 acceptance 2 | 正常 | 同 T-205-4 的 `plain` 分支 | 讓步發生 | `yielded` 的 `fields` 含 `level`、`holderShowId`、`holderLevel`、`variant`、`message` 五鍵且皆非 `null`；`message` 等於呼叫時傳入的字串。**被丟棄的告知不得只留事件類型而無內容** |
| T-205-6 | 過渡缺口可搜尋（PM 指定） | 邊界 | 通道空、log spy 已裝 | 不傳 `currentHolder` 呼叫 `show`（過渡期正式路徑） | `preempted` 的 `fields` **含** `holderUnknown` 鍵且值為 `true`。另對 T-205-1 的 9 個非 `null` 格斷言該鍵為 `false`——鍵恆存在、值二分，使「這個判斷還沒有輸入」可用單一欄位搜尋，收斂完成時該值應在正式路徑上不再為 `true` |
| T-205-7 | 票面 acceptance 2、B1 | 正常 | 第一則以 D 顯示中，其 `showId` 已由 `shown` 事件取得 | 以 U + `currentHolder` 截斷 | `preempted.preemptedShowId` 等於被截斷者的 `showId`，且被截斷者自身產生 `closed{reason: hide}` 帶同一 `showId`——兩筆可由同一把鑰匙拼接。`0.1.0-W3-176` 既有的 `_resolveClosedLevel` 等級判定不回退（`closed` 的 `level` 仍依被截斷者自己的 `origin`） |
| T-205-8 | B13、場景 14 | 異常 | `context.mounted == false`（以整棵替換卸載，前置斷言 `capturedContext.mounted` 為 `false`） | 以任意級別與任意 `currentHolder` 呼叫 `show` | 恰一筆 `skippedUnmounted`（`level` 為 `900`）；**無 `preempted`、無 `yielded`、無 `shown`**；`find.byType(SnackBar)` 為 `findsNothing`。**早退必須發生在裁決之前**——這一條同時鎖住實作的語句順序 |
| T-205-9 | 場景 16 | 邊界 | 第一則以 U 顯示後，pump 超過 `Motion.snackBar` 使其自然逾時消失（前置斷言 `findsNothing`） | 以 D + 指向那一則（已失效）的 `currentHolder` 呼叫 | 仍讓步（`yielded`、無 `shown`）。元件不驗證快照新鮮度，亦不查詢通道實況——新鮮度屬持有者狀態，收斂後由 `0.1.0-W3-207` 的逾期懶檢查承擔 |

##### G-C 動作 slot 與回呼順序（`app_snack_bar_test.dart` 新增群組）

| # | 對應 | 維度 | Given | When | Then |
|---|------|------|-------|------|------|
| T-205-10 | B6、場景 6 | 正常 | `withAction` 已呈現且動作錨點可見（前置斷言 `find.byKey(actionTestKey)` 為 `findsOneWidget`）；`onAction` 為記錄呼叫時序的探針 | 點按動作 | 事件序為 `actionPressed` → `closed{reason: action}`；`onAction` 被呼叫恰一次，且其呼叫時點在 `hideCurrentSnackBar(reason: action)` **之後**——以「`onAction` 執行當下 `records` 已含 `actionPressed`，且 SnackBar 已進入退場」佐證 |
| T-205-11 | B7、場景 9 | 中斷 | `withAction` 已呈現，其 `onAction` **同步**呼叫 `AppSnackBar.show`（新訊息文字與舊者不同） | 點按動作，續 pump `2 × _materialTransition` | 舊 `showId` 的 `closed` 帶 `reason: action`；**新 `showId` 的 `closed` 不得帶 `reason: action`**；新訊息文字在退場動畫走完後可見。改序前的形態（新的一則被 `reason: action` 無聲關掉）即為紅燈 |
| T-205-12 | 觀察 B 的現況鎖定 | 中斷 | `withAction` 已呈現，其 `onAction` 回傳未 `await` 的 `Future`（現行 `gap_report_screen.dart:199` 形態），同步段不顯示任何 SnackBar | 點按動作 | 只有一筆 `closed{reason: action}`，`records` 中無第二個 `showId`。此測試把觀察 B「當前不可達」的前提從口頭陳述變為會失敗的斷言——呼叫端改為同步顯示時會紅燈並被導向 T-205-11 |
| T-205-13 | B14、場景 15 | 異常 | `variant: withAction` 而 `actionLabel` 或 `onAction` 為 `null` | 呼叫 `show` | 觸發既有 assert（`AssertionError`），訊息不變。**必填 `level` 的加入不得改變本斷言的觸發條件**——`level` 為編譯期必填，不產生執行期分支 |
##### G-E 原始碼契約（`app_snack_bar_source_contract_test.dart`，純 `test()`）

**共同前置：註解剝除（不可省，否則守衛恆綠或恆紅）**

實測（2026-09-10，`grep` 全 `lib/`）：`ScaffoldMessenger` 與 `showSnackBar`
在 `lib/app/shell.dart` 的說明註解中各出現，`app_snack_bar.dart` 的 dartdoc 亦多處提及。
**未剝除註解的整行掃描會把註解算成呼叫點，使場景 10／13 的守衛恆紅**；只用行首
`//` 過濾則放過行尾註解。故 G-E 全體先經同一個剝除函式處理，其契約為：

| 剝除對象 | 規則 |
|---------|------|
| `///` 與 `//` | 自該記號至行尾（含記號） |
| `/* ... */` | 整段（可跨行） |
| 字串字面量內的 `//` | **已知限制**：本剝除器不解析字串字面量。`lib/` 現況無此形態；T-205-21 以正向對照鎖住剝除器行為，形態出現時由該測試給出紅燈 |

剝除後的行號不需保留（G-E 全部斷言為計數與相對位置，不引用行號）。

| # | 對應 | Given（掃描對象） | Then |
|---|------|------------------|------|
| T-205-14 | B9、場景 10 前半 | 剝除註解後的 `lib/components/app_snack_bar.dart` | `hideCurrentSnackBar` 恰 2 次（一次無 `reason` 引數、一次帶 `reason: SnackBarClosedReason.action`）；`removeCurrentSnackBar` 與 `clearSnackBars` 各 0 次。**觀察 A 的承重前提由此鎖住**——新增第三個呼叫點即紅燈，`reason == hide` 不再恆等於「被截斷」的變化不再無聲 |
| T-205-15 | 場景 10 後半 | 剝除註解後的全部 `lib/**/*.dart` | `hideCurrentSnackBar`、`removeCurrentSnackBar`、`clearSnackBars` 三個識別符在 `lib/components/app_snack_bar.dart` 之外命中數為 0 |
| T-205-16 | B12、場景 13、INV-SNACKBAR-NOQUEUE | 剝除註解後的 `app_snack_bar.dart` | 五項全部成立：(a) `showSnackBar` 恰 1 次；(b) 無 `reason` 引數的 `hideCurrentSnackBar()` 恰 1 次；(c) 該 `hide` 在來源順序上早於該 `showSnackBar`；(d) 兩者之間無 `return` 語句；(e) 兩者的大括號巢狀深度相等——(e) 排除「`hide` 被包進條件分支而 `show` 在分支外」這一種「不 hide 就 show」的形態，該形態會使 Material 內建 FIFO 生效，構成 `0.1.0-W3-239` 禁止的提示佇列，且不像自建 `Queue` 那樣顯眼 |
| T-205-17 | B10、場景 11 | 剝除註解後的 `app_snack_bar.dart` 中 `_resolvePreemption` 的簽名與函式體（以簽名後第一個 `{` 起大括號配對取出） | 四項：(a) 參數恰 2 個，型別依序為 `AttentionLevel` 與 `AttentionHolderHint?`（識別符名稱不限）；(b) 函式體不含 `variant`／`origin`／`message`／`context`／`runtimeType`／` is `；(c) 函式體不含 `_` 開頭識別符後接 `(` 的私有函式呼叫；(d) 函式體不含 `AppSnackBar.` 靜態成員存取。**(a) 是真正的結構性保證**——`static` 方法且參數不含那些值，語言上即無從引用；(b)(c)(d) 封住剩餘的兩條逃逸路徑（讀元件靜態欄位、轉呼叫另一個私有函式） |
| T-205-18 | 票面 acceptance 4 的靜態面、Phase 1 §0 步驟 3 | 剝除註解後的 `app_snack_bar.dart` | 兩張白名單：(a) 檔內宣告的 `static` 成員名稱集合恰為 `_actionSlotAssertMessage`、`_tag`、`_levelInfo`、`_levelWarning`、`_lastShowId`、`logSink`、`_nextShowId`、`_resolveClosedLevel`、`_resolvePreemption`、`_defaultLogSink`、`show`；(b) 檔內宣告的 `enum` 名稱集合恰為 `AppSnackBarVariant`、`AppSnackBarOrigin`、`AppSnackBarLogEvent`、`_Preemption`。**白名單而非型別黑名單**——在 `abstract final class` 中新增持有者狀態或待顯訊息集合的唯一途徑就是新增 `static` 欄位，白名單使任何新增都必須是刻意的一次改動，而 `List`／`Queue`／`Map` 型別黑名單擋不住 `static int? _currentHolderLevel` 這種形態 |
| T-205-19 | B11、場景 12、票面 acceptance 3 | 剝除註解後的全部 `lib/**/*.dart`，以及 `lib/app/attention_level.dart` 原始檔（含 dartdoc） | 四項：(a) `enum AttentionLevel` 在 `lib/` 內宣告恰 1 次，位於 `lib/app/attention_level.dart`；(b) 三個值依序為 `discardable`、`mustLeaveTrace`、`undroppable`（順序即優先序，`index` 越大級別越高）；(c) 各值的 dartdoc 分別含主名「可棄」「須留痕」「不可棄」；(d) 檔內含 `SPEC-003` 章節標題文字「注意力通道的到達類別與級別指派表」。**(c) 的權威來源是《事件流負載仲裁方法論》〈級別〉表的三個主名**，`SPEC-003` §2.14 只出現「可棄」「不可棄」兩者（0.1 無須留痕實例），故不對 §2.14 表格做剖析比對 |
| T-205-20 | 票面 acceptance 1 的靜態面 | 剝除註解後的 `lib/main.dart` | `ScaffoldMessenger` 與 `showSnackBar` 各 0 次；且含 `AppSnackBar.show` 至少 1 次 |
| T-205-21 | 守衛自身的守衛 | 剝除器 + `lib/app/shell.dart` + `SPEC-003` | 三項：(a) 剝除器對含 `///`、行尾 `//`、跨行 `/* */`、以及字串內 `//` 四種形態的 fixture 產出預期結果（字串內 `//` 一項為**已知會被誤剝**，以 `skip` 標註並附本節限制說明，使限制可見而非隱形）；(b) 正向對照：`lib/app/shell.dart` 剝除後 `ScaffoldMessenger` 命中數為 0（剝除前為 2，2026-09-10 實測）——若剝除器失效變成空操作，此斷言先紅；(c) 引用穩定性：`docs/spec/ui/SPEC-003-interaction-response.md` 含「注意力通道的到達類別與級別指派表」「讓步規則的可驗收形態」「鑑別「綁級別」與「綁呼叫路徑」兩種實作」「截斷事件的日誌等級」「顯示位爭用時的約束與 0.1 的處置規則」五段標題文字 |

**T-205-21(b) 的必要性**：T-205-14～20 全部建立在剝除器正確之上。剝除器若有 bug 而
變成近似空操作，多數計數斷言會紅（可被發現）；但若它剝過頭（例如把程式碼也剝掉），
計數會下降而多數斷言反而**變綠**——守衛失效且無聲。(b) 的正向對照與各測試的「恰 N 次」
下界共同封住兩個方向。

##### G-A 入口收斂（`home_page_workspace_test.dart`）

| # | 對應 | 維度 | Given | When | Then |
|---|------|------|-------|------|------|
| T-205-22 | B8、場景 7 | 正常 | 既有 `G7-4 ChooseFolderUnavailable` 測試的 harness（替身回傳 `ChooseFolderUnavailable`），前置斷言初始文案 `folderAccessRationale` 可見 | 點按選擇資料夾 | 既有斷言保留（`SnackBar` 為 `findsOneWidget`、狀態不變）；**新增**斷言 `find.text(l10n.chooseFolderUnavailableMessage)` 為 `findsOneWidget`——原測試只驗型別，改走 `AppSnackBar` 後需驗同一 i18n key 確實被呈現，否則「接錯訊息」不會紅燈 |
#### P2.5 原始碼契約測試的執行環境規範（補充 G-E）

| 項目 | 規範 | 依據 |
|------|------|------|
| 工作目錄 | `flutter test` 的工作目錄為專案根，故一律以專案相對路徑開檔（`File('lib/components/app_snack_bar.dart')`） | `test/window_size_contract_test.dart` 既有先例（`File('macos/Runner/MainFlutterWindow.swift')`） |
| `lib/` 全掃描的列舉方式 | `Directory('lib').listSync(recursive: true)` 過濾 `.dart` 結尾，**不硬編碼檔案清單** | 硬編碼清單使新增檔案自動逃出守衛，而新增檔案正是要防的變化 |
| 檔案不存在時 | 以 `fail()` 附明確訊息，不得靜默跳過 | 檔案被改名時守衛應紅燈，不應變綠 |
| 測試型別 | 純 `test()`，**不使用** `testWidgets` | 不需 widget binding；純 `test()` 使 G-E 可獨立於 Flutter widget 環境執行 |
| 斷言失敗訊息 | 每個 `expect` 附 `reason:`，內含本票號 `0.1.0-W3-205` 與被違反的不變式名稱 | 守衛紅燈時，讀者需要知道違反了什麼、去哪裡讀規格，而非只看到一個數字不符 |

#### P2.6 既有測試遷移逐項核對表（PM 指定：逐項核對，不得因本次改動而紅燈）

行號為改動前（2026-09-10）的 `test/unit/components/app_snack_bar_test.dart` 快照，
改動過程中會位移；**識別以測試名稱與斷言文字為準**。

**遷移原因分三類**：
- **(C) 編譯**：`level` 改必填，全部 `AppSnackBar.show` 呼叫端須補參數。傳 `discardable`
  （對應生產呼叫點現況，`SPEC-003` §2.14 全為可棄），不傳 `currentHolder`（過渡期正式路徑）。
- **(E) 事件序**：過渡期每一次 `show` 都先發 `preempted{holderUnknown: true}` 再發 `shown`
  （Phase 1 §2.3 第一列 + 場景 1），既有的「事件清單完全相等」斷言須補入該值。
- **(S) 規格驅動**：測試本身在鎖定會被本票刻意改變的形狀，須同步更新。

| # | 位置 | 原斷言 | 類別 | 遷移動作 | 遷移後是否仍成立 |
|---|------|-------|------|---------|----------------|
| M1 | `_triggerHarness`（L43-73） | — | C | 新增 `AttentionLevel level = AttentionLevel.discardable` 與 `AttentionHolderHint? currentHolder` 兩個具名參數並轉傳。**預設值只存在於測試輔助函式，不得回流到 `AppSnackBar.show`**（Phase 1 §2.0 已否決預設值） | 是 |
| M2 | 全檔 13 處裸呼 `AppSnackBar.show`（L199／216／225／401／453／478／479／505／506／531／541／586／611／616／678） | — | C | 逐一補 `level: AttentionLevel.discardable` | 是 |
| M3 | L344-345（165：plain 自然逾時） | `expect(events, [shown])`、`expect(levels, [null])` | E | 改為 `[preempted, shown]` 與 `[null, null]` | 是 |
| M4 | L353 | `expect(events, [shown, closed])` | E | 改為 `[preempted, shown, closed]` | 是 |
| M5 | L372（165：withAction 按下動作） | `expect(events, [shown])` | E | 改為 `[preempted, shown]` | 是 |
| M6 | L378-382 | `expect(events, [shown, actionPressed, closed])` | E | 改為 `[preempted, shown, actionPressed, closed]` | 是 |
| M7 | L403-404（165：unmounted） | `expect(events, [skippedUnmounted])`、`expect(levels, [900])` | — | **不改**。早退發生在裁決之前（場景 14），此斷言正是該順序的守衛 | 是 |
| M8 | L461-468（T-1） | `records.map(event) == [shown, closed]`、`showIdOf(records[0]) == showIdOf(records[1])`、`records[1].fields['reason']`、`records[1].level`、`records[0].level` | E | 事件清單改為 `[preempted, shown, closed]`；三處索引由 `[0]/[1]` 位移為 `[1]/[2]`。**位移後須確認 `showIdOf(records[1]) == showIdOf(records[2])` 比較的仍是 `shown` 與 `closed`**，不是 `preempted` 與 `shown` | 是 |
| M9 | L471-499（T-2） | 以 `.where(event == shown)` 與 `closedWithReason` 選取 | — | **不改**。選取條件不依索引，新增事件不影響 | 是 |
| M10 | L501-525（T-3） | `indexWhere` 比較相對順序 | — | **不改**。同 M9 | 是 |
| M11 | L539（T-4） | `final firstShowId = showIdOf(records.single)` | E | `records` 此時含 `preempted` 與 `shown` 兩筆，`records.single` 會拋例外。改為選取 `event == shown` 的那一筆。**不得改成 `records.last`**——依位置選取會在事件順序調整時無聲取到錯的一筆 | 是 |
| M12 | L562-605（T-5 矩陣） | `closedWithReason(hide)` + `originOf` + `level` | — | **不改**。`closedWithReason` 為條件選取 | 是 |
| M13 | L607-627（T-6 origin 預設值） | 同 M12 | — | **不改**。`origin` 的預設值本票未改動（Phase 1 §2.2 簽名保留 `origin` 預設 `background`） | 是 |
| M14 | L650-660（T-7） | `records.map(event) == [shown, actionPressed, closed]`、`records[0]/[1]/[2]` | E | 事件清單改為 `[preempted, shown, actionPressed, closed]`；三處索引位移 `+1` | 是 |
| M15 | L680-691（T-8） | `records hasLength(1)` 兩處 | — | **不改**。早退路徑不產生 `preempted`（場景 14）。此斷言與 M7 共同構成「裁決不得前移到 mounted 檢查之前」的雙重守衛 | 是 |
| M16 | L694-701（T-9 事件列舉結構） | `AppSnackBarLogEvent.values == [shown, skippedUnmounted, actionPressed, closed]` | **S** | 改為六值，**既有四值維持原順序在前，新增 `preempted`、`yielded` 接於其後**。此為本票唯一一項「既有測試必然改動」的項目——它的職責就是在事件集合改變時紅燈，紅燈即其正確運作，不是回歸 | 是（更新後） |
| M17 | `test/unit/app/home_page_workspace_test.dart` L85-107（G7-4） | `find.byType(SnackBar) findsOneWidget` | — | 既有斷言保留並依 T-205-22 加強。**風險項**：該測試用 `pumpAndSettle()`；`AppSnackBar` 的 `SnackBar` 停留為 `Motion.snackBar`（4 秒），與原生呼叫的 Material 預設同量級，`pumpAndSettle` 在進場動畫結束即停止，不會推進到逾時。Phase 3b 須實跑確認；若逾時或提前消失，改用 `await tester.pump()` + `_pumpBy` 明確推進，**不得改用放寬的斷言（如 `findsAtLeastNWidgets`）掩蓋** | 待實跑確認 |
| M18 | `test/unit/screens/gap_report_test.dart` L167-183 | `find.byType(SnackBar) findsOneWidget` | — | **不改**。該呼叫點本就經 `AppSnackBar`，補 `level`／`origin` 屬 `lib/` 端改動，行為不變（過渡期恆走 `holderUnknown`，等同現行無條件截斷） | 是 |
| M19 | `test/unit/screens/scan_notification_controller_test.dart`（5 處 `find.byType(SnackBar)`） | 同 M18 | — | **不改**。同 M18；該檔無點按動作錨點的測試，P2.4 T-205-10 的順序改動不影響 | 是 |
| M20 | `test/unit/components/app_button_test.dart` L166、`loading_state_test.dart` L212 | `find.byType(SnackBar) findsNothing` | — | **不改**。兩者為「不應出現 SnackBar」的負向斷言，本票不新增任何顯示路徑 | 是 |

**M3～M6、M8、M11、M14 的共同禁令**：不得為了少改動而把「清單完全相等」的斷言
放寬為 `contains` / `containsAllInOrder`。完全相等是這批日誌測試的價值來源——
它使「多出一個未預期的事件」也會紅燈，而放寬後多出的事件將不再被發現。
#### P2.7 測試獨立性與禁止事項

| # | 約束 | 理由 |
|---|------|------|
| 1 | **禁止斷言 `showId` 的絕對值**。一律以同一測試內取得的值比較（相等、`greaterThan`） | `AppSnackBar._lastShowId` 是 process 內單調遞增且不重置的靜態計數器，跨測試共用。寫死數字會使測試結果依賴執行順序——單獨跑綠、整檔跑紅，且紅燈與程式碼無關 |
| 2 | **禁止 `Stopwatch` + `lessThan` / `greaterThan` 作 pass-fail 斷言**。時間一律以 `_pumpBy` 推進 fake clock，門檻取 `Motion.*` token | `test-assertion-design-rules` 規則 D1。T-205-3（讓步不排隊）斷言的是「pump 完 `Motion.snackBarWithAction` 後仍未出現」，量的是 fake clock 的確定性推進，不是真實耗時 |
| 3 | **禁止 `DateTime.now()` 進入斷言** | 規則 D3。本票無時間戳斷言需求，此條為預防 |
| 4 | **G-B／G-C 的 pump 總量不得超過 `Motion.snackBar`**，除非該測試的目的就是驗證逾時 | 逾時會產生 `closed{reason: timeout}`，與「持有者未被截斷」「新訊息未出現」等斷言互相污染，且污染方向是讓測試變綠 |
| 5 | **log spy 一律 `setUp` 裝上、`tearDown` 還原原值**，各群組自裝自己的 spy | 沿用 `0.1.0-W3-176` 既有慣例（acceptance A8：新群組自裝替身，既有群組不被觸及）。跨群組共享可變 spy 會使群組無法獨立執行，違反 Phase 3b 拆分友善要求 |
| 6 | **禁止比對日誌散文字面**，斷言一律針對事件列舉值與 `fields` 鍵值 | `0.1.0-W3-165` 既有規範：`0.1.0-W3-132` 尚未把日誌訊息改為事件識別碼，比對字面會與其目標衝突 |
| 7 | **禁止在測試中重新定義級別語意**（如自建 `int` 常數比大小） | 票面 acceptance 3：級別定義唯一。測試若另立一套，T-205-19 的靜態守衛就只守住 `lib/` 而測試端已漏 |
| 8 | **禁止以 `skip` 繞過紅燈**，唯一例外是 T-205-21(a) 的剝除器已知限制，且該 `skip` 須附本節限制說明的引用 | `skip` 使失敗不可見，與本票消除無聲降級的目標直接相反 |

#### P2.8 Phase 2 退出與移交 Phase 3a

**Phase 2 產出物**：本節 P2.0～P2.9，含 22 條 GWT 規格（T-205-1～22）、12 格裁決窮舉矩陣、
20 項既有測試遷移核對、8 項獨立性約束。無測試程式碼（Phase 2 職責邊界）。

**行為單元覆蓋核對（Phase 1 §8 的 B1–B14 逐項）**：

| 行為單元 | 承接的 GWT | 行為單元 | 承接的 GWT |
|---------|-----------|---------|-----------|
| B1 未知持有者截斷並標記 | T-205-1（3 格）、T-205-6 | B8 `main.dart` 經元件層 | T-205-22、T-205-20 |
| B2 高級別搶佔低級別 | T-205-1（3 格）、T-205-7 | B9 `hide` 呼叫點守衛 | T-205-14、T-205-15 |
| B3 同級別截斷並留痕 | T-205-1（3 格） | B10 裁決函式輸入面 | T-205-17（+ T-205-4 行為佐證） |
| B4 低級別讓步不呈現 | T-205-1（3 格）、T-205-3 | B11 級別定義唯一 | T-205-19 |
| B5 讓步事件等級依 `origin` | T-205-2 | B12 INV-SNACKBAR-NOQUEUE | T-205-16、T-205-18 |
| B6 動作事件序 | T-205-10 | B13 卸載時不截斷 | T-205-8（+ M7／M15 既有守衛） |
| B7 同步再顯示不被誤殺 | T-205-11（+ T-205-12 現況鎖定） | B14 slot assert 不變 | T-205-13 |

14 項行為單元全部有承接，另加 T-205-5（`yielded` 欄位齊全）、T-205-9（快照失效）、
T-205-21（守衛的守衛）三條 Phase 1 行為單元清單未列但票面 acceptance 或守衛完整性所需者。

**四維度窮舉核對**：正常 7 條（T-205-1／2／4／5／7／10／22）、邊界 4 條（T-205-3／6／9／
以及 T-205-1 的 `null` 持有者 3 格）、異常 2 條（T-205-8／13）、中斷 2 條（T-205-11／12），
靜態 8 條（T-205-14～21）。**無法再提出新的行為分支**：裁決函式的輸入空間為
（3 級別 × 4 持有者狀態）＝ 12，已窮舉；`show` 的其餘輸入（`variant`／`origin`／`message`）
經 T-205-4（行為）與 T-205-17（靜態）證明不進入裁決；早退路徑恰 1 條（`!context.mounted`）。

**移交 pepper-test-implementer（Phase 3a）的注意事項**：

1. **群組職責歸屬與跨群組依賴已在 P2.1 標註**，G-E 對其他群組零依賴，可單獨分派。
2. Phase 3a 轉語言無關策略時，**12 格矩陣應維持參數化生成而非展開為 12 段複製**——
   展開後新增第四個級別時會有 12 段各自遺漏的風險。
3. **T-205-16 的大括號配對與 T-205-17 的函式體抽取是本批最易寫錯的兩處**，
   Phase 3a 的虛擬碼須明寫「以配對計數而非最近的 `}`」。
4. 既有測試遷移（P2.6 的 M1～M20）**與新增測試分開提交或分開檢視**——遷移是機械動作，
   新增是設計落地，混在一起會使 review 無法分辨哪些紅燈是遷移錯誤、哪些是實作缺陷。
#### P2.9 範疇缺口、新耦合與發現追蹤

**`where.files` 需再擴充兩項（測試端，請 PM 裁定；已同步記於 NeedsContext）**：

| 檔案 | 狀態 | 改動 | 不擴充的後果 |
|------|------|------|------------|
| `test/unit/components/app_snack_bar_source_contract_test.dart` | **新增檔** | G-E 八條原始碼契約測試（T-205-14～21） | 落回 `app_snack_bar_test.dart` 使該檔逾 1100 行且承載 2 個 domain（原始碼文字／執行期行為）。**測試規格本身不變**，此項只影響檔案配置 |
| `test/unit/app/home_page_workspace_test.dart` | 既有 | G7-4 測試增修（T-205-22） | 場景 7「`main.dart` 分支經 `AppSnackBar` 呈現同一 i18n key」失去行為驗證，只剩 T-205-20 的靜態零命中——靜態只證明舊呼叫消失，不證明新呼叫接對了訊息 |

擴充後 `where.files` 為 8 項（lib 端 5、test 端 3）。**功能職責數仍為 2**（P2.1 已按職責
對齊群組），認知負擔的實質未因測試端拆檔而上升——G-E 對其他群組零依賴。

**本票引入的一項新耦合，請 PM 知悉**：T-205-21(c) 使測試套件讀取
`docs/spec/ui/SPEC-003-interaction-response.md` 並斷言五段章節標題文字存在。
**取捨**：`SPEC-003` 章節改名時測試紅燈（可能由其他 DOC 票觸發，紅燈與該票無關）；
不加則 `attention_level.dart` 與元件 dartdoc 的章節標題引用會靜默 rot，
而票面 acceptance 3 明訂「以標題文字引用 SPEC-003」——引用不可驗證等同該條 acceptance
無守衛。判斷是加的價值較高，但這是跨產物耦合，記於此交 PM 確認。

**未另開票的發現（依 quality-baseline 規則 5 逐項標註）**：

| 發現 | 處置 |
|------|------|
| 過渡期每一次 `show` 都發出一筆 `preempted{holderUnknown: true}`，含通道原本為空時——語意上「搶佔了不存在的持有者」 | 無需建 ticket：此為 Phase 1 §2.2 的明文設計（`null` 語意是**未知**不是「通道為空」，元件無從分辨），且該筆事件正是 PM 指定的「缺口可搜尋」載體。收斂 trigger 已由 Phase 1 §2.7 綁定 `0.1.0-W3-207` 合併，屬 `decision-trigger-binding` 規則 1 狀態 (b)。Phase 2 不推翻 Phase 1 裁決 |
| 註解剝除器不解析字串字面量，字串內含 `//` 時會誤剝 | 無需建 ticket：`lib/` 現況無此形態（2026-09-10 實測），且 T-205-21(a) 已把限制寫成標註 `skip` 的測試使其可見。形態出現時該測試即為發現點 |
| `SPEC-003` §2.14 指派表只出現「可棄」「不可棄」兩個級別主名，無「須留痕」（0.1 無該級別實例） | 無需建 ticket：非缺陷。三個主名的權威來源是《事件流負載仲裁方法論》〈級別〉表，§2.14 只登記本專案已發生的實例，方法論明訂該表「不窮舉」。T-205-19(c) 因此以方法論主名為斷言對象，不剖析 §2.14 表格 |
| 遷移後 `app_snack_bar_test.dart` 將達約 950 行（G-E 拆出後），仍遠超 `cognitive-load` 的 300 行門檻 | 無需建 ticket：本票 `tdd_stage` 含 `phase4`，Phase 4a 重構評估為必經階段，該階段即此項的處置點。另建票會產生一張與 Phase 4a 職責重疊的票。**Phase 4a 執行者須把「G-B／G-C／G-D 是否應再拆檔」列為明示評估項**，不得以「無需重構」一句帶過 |
| `home_page_workspace_test.dart` G7-4 使用 `pumpAndSettle()`，改走 `AppSnackBar` 後的行為需實跑確認（P2.6 M17） | 無需建 ticket：屬本票 Phase 3b 的實作內驗證項，已寫入 M17 並附「不得以放寬斷言掩蓋」的禁令 |

**本 Phase 2 未產生任何 spawn request**——上表五項發現的處置皆落在本票既有階段或既有裁決內。
### Phase 3a 實作策略（pepper-test-implementer，2026-09-10）

本節產出語言無關的實作策略與虛擬碼，**不含任何 Dart 實作**。輸入為 Phase 1（第 0–10 節）
與 Phase 2（P2.0～P2.9），兩者的裁決一律沿用，本節不重新推導、不推翻。全程唯讀
`SPEC-003`／`SPEC-004`。術語沿用 P2.2（D／T／U）。

「元件」＝ `AppSnackBar`；「載體框架」＝ Material `ScaffoldMessenger`；「錨點」＝ 呼叫端
傳入的 build context；「呈現」＝ 框架的 `showSnackBar`；「清除」＝ 框架的
`hideCurrentSnackBar`。以下虛擬碼一律用這組中性詞。

#### P3a.1 實作策略設計

##### P3a.1.1 `show` 的控制流骨架（同時滿足 INV-SNACKBAR-NOQUEUE 與 T-205-16(c)(d)(e)）

```text
FUNCTION show(錨點, message, level, variant, actionLabel, onAction, actionTestKey,
              origin, currentHolder)

    ASSERT 動作 slot 契約                       # 既有，不動（T-205-13／M-無）
    showId  <- 下一個識別                       # 既有：在錨點檢查之前（skippedUnmounted 亦有識別）
    base    <- { showId, origin }

    # ── 早退關卡 1：錨點已卸載（必須位於裁決之前）────────────────
    IF NOT 錨點.已掛載 THEN
        log(skippedUnmounted, base + {variant, message}, 等級=WARNING)
        RETURN                                  # 出口 1
    END IF

    # ── 裁決（唯一裁決點，輸入恰兩項）────────────────────────
    decision <- resolvePreemption(level, currentHolder)

    # ── 早退關卡 2：讓步（不清除、不呈現）──────────────────────
    IF decision == yieldToHolder THEN
        log(yielded,
            base + { level,
                     holderShowId  : currentHolder.showId,
                     holderLevel   : currentHolder.level,
                     variant, message },
            等級 = (origin == 背景發起 ? WARNING : INFO))
        RETURN                                  # 出口 2
    END IF

    # ── 以下為唯一的呈現路徑；本區塊內不得再出現任何 RETURN ──────
    載體 <- 取載體(錨點)
    載體.清除()                                  # (A) 全檔唯一的「無引數清除」
    log(preempted,
        base + { level,
                 preemptedShowId : currentHolder?.showId,      # 未知時為空
                 preemptedLevel  : currentHolder?.level,       # 未知時為空
                 sameLevelReplace: (decision == replaceSameLevel),
                 holderUnknown   : (decision == holderUnknown) })
    log(shown, base + { variant, message, duration, actionLabel? })   # 既有欄位不變
    控制器 <- 載體.呈現( 內容組裝(message, variant, actionLabel, actionTestKey, onAction) )   # (B)
    控制器.結束時(reason -> log(closed, base + {reason},
                               等級 = resolveClosedLevel(reason, origin)))   # 既有，不動
END FUNCTION
```

**INV-SNACKBAR-NOQUEUE 如何被滿足，而非被繞過**：不變式要求 (A) 與 (B) 在同一函式內、
(A) 在前、其間無 `return`、且**大括號巢狀深度相等**（T-205-16 的 (b)～(e) 五項）。本骨架
以**兩道前置護衛（guard clause）**達成——兩個 `RETURN` 全部發生在 (A) **之前**，(A) 與 (B)
之間只剩三個 log 呼叫（不引入區塊、不引入 return）。兩者皆是函式主體的直屬語句，
巢狀深度同為「類別 → 方法主體」，(e) 因此**結構性成立**，不需任何額外處理。

**與之相對的錯誤形態（明文禁止）**：把讓步寫成 `IF decision != yieldToHolder THEN { 清除(); ... 呈現(); }`。
該形態行為上與本骨架等價、行為測試全綠，但 (A)(B) 被包進條件區塊、深度較函式主體深一層，
T-205-16(e) 紅燈。**Phase 3b 若看到 (e) 紅燈，正確處置是把條件改寫為前置護衛，不是放寬該斷言**——
(e) 存在的理由是「`清除` 被包進分支而 `呈現` 在分支外」這條路徑會使框架內建 FIFO 生效，
構成 `0.1.0-W3-239` 禁止的提示佇列，且不像自建佇列那樣顯眼。

**兩個出口的順序不可對調**：出口 1（錨點卸載）必須早於裁決，這是 T-205-8 與既有 M7／M15
共同鎖住的順序；出口 2（讓步）必須早於 (A)，否則讓步路徑會清除持有者，直接違反讓步規則 3。

##### P3a.1.2 裁決純函式（唯一裁決點）

```text
FUNCTION resolvePreemption(incoming: 級別, holder: 持有者快照?) -> 裁決
    IF holder 為空          THEN RETURN holderUnknown
    IF holder.級別序 < incoming.級別序 THEN RETURN preemptLower
    IF holder.級別序 = incoming.級別序 THEN RETURN replaceSameLevel
    RETURN yieldToHolder                       # holder.級別序 > incoming.級別序
END FUNCTION
```

四項實作約束（對應 T-205-17 的 (a)～(d)）：

| 約束 | 內容 |
|------|------|
| 輸入面 | 參數恰兩項，型別依序為級別與可空的持有者快照。**不接受** variant／origin／message／錨點——語言上即無從引用（(a) 的結構性保證） |
| 函式體 | 只讀 `holder.級別`（不讀 `holder.showId`——識別欄位的組裝屬呼叫端職責，見 P3a.1.1）；不含型別判斷、不含私有函式轉呼、不含元件靜態成員存取（(b)(c)(d)） |
| 比較鍵 | 恆為級別的序（列舉宣告順序），**不得**以載體型別、變體或呼叫路徑為鍵 |
| 完整性 | 三分（小於／等於／大於）＋ 空持有者，對任意有序級別集合皆為完整劃分；新增第四級別不需改本函式 |

**`sameLevelReplace` 必須由 `decision` 推導，不得由 `preemptedLevel == level` 推導**：
P2.3 明訂測試對兩者皆斷言，正是為了讓「接成常數」或「誤接成 `preemptedLevel != null`」紅燈。
語意重複是刻意的，不是可合併的冗餘。

##### P3a.1.3 動作回呼的順序（Phase 1 §2.5、觀察 B）

```text
動作被按下 ->
    log(actionPressed, base + {actionLabel})
    載體.清除(原因 = 動作)          # 移到 onAction 之前（本票唯一的順序改動）
    onAction()
```

改序的效果：即使 `onAction` **同步**再顯示一則，`原因 = 動作` 已作用於舊的那一則，
新的一則不會被「使用者一眼未見即以動作原因關掉」（T-205-11）。**不得改用控制器的
`close()`**——理由見 Phase 1 §2.5（會觸發框架內部斷言，且把結局錯記為 `hide`）。
此處的帶引數清除位於呈現內容的回呼閉包內，深度較深，**不計入** T-205-16 的
「無引數清除恰 1 次」；T-205-14 的「恰 2 次」則同時涵蓋這一處。

#### P3a.2 資料結構選擇

| 結構 | 形態 | 選擇理由 |
|------|------|---------|
| 級別 | 有序列舉，三值（可棄／須留痕／不可棄），宣告順序即優先序 | 比較退化為序的整數比較，無需比較器；新增值不改裁決函式。獨立型別檔、無行為——仲裁器（`0.1.0-W3-207`）與元件各自引用同一份，避免兩份同一事實 |
| 持有者快照 | 不可變值物件（識別 + 級別），可空 | 值物件無身分、無生命週期，不構成「元件持有狀態」；可空性承載「未知」語意 |
| 裁決結果 | 私有列舉，四值 | 把「要不要清除」與「事件欄位怎麼填」由同一個值決定，避免兩處各自判斷而漂移。私有＝不進入公開面 |
| 元件內狀態 | **無新增** | T-205-18 以 `static` 成員白名單鎖住。任何持有者狀態或待顯集合都必須是新增靜態成員，白名單使之無法悄悄發生 |

**明確不採用的結構**：任何集合型（清單／佇列／映射）承載待顯訊息。讓步的處置是
**不呈現**，被丟棄的請求不進入任何容器（`0.1.0-W3-239`）。

#### P3a.3 資料流程圖

```text
呼叫端（標定點）
  │  message、variant、origin ── 既有
  │  level        ── 新增，必填，取自 SPEC-003〈注意力通道的到達類別與級別指派表〉
  │  currentHolder ── 新增，過渡期恆為空
  ▼
show()
  ├─► 識別產生 ──────────────► base{showId, origin} ──┐
  ├─► 錨點檢查 ──(未掛載)────► log(skippedUnmounted)  │  出口 1
  ├─► resolvePreemption(level, currentHolder)          │
  │        │                                            │
  │        ├─(yieldToHolder)─► log(yielded)  ───────────┤  出口 2（不呈現）
  │        │                                            │
  │        └─(其餘三值)─► 清除 ─► log(preempted) ─► log(shown) ─► 呈現
  │                                                              │
  └───────────────────────────────────────────► 控制器.結束 ─► log(closed)
```

**「誰截斷了誰」的拼接鍵**：`preempted.preemptedShowId`（截斷者側記錄）與被截斷者自身
`closed{reason: hide}` 的 `showId` 為同一把鑰匙（T-205-7）。過渡期前者為空，關聯由後者
單獨承擔（即 `0.1.0-W3-176` 的現況能力），且 `holderUnknown: true` 明記缺口成因。

#### P3a.4 控制流程圖

```text
              ┌──────────────┐
              │  show() 進入 │
              └──────┬───────┘
                     ▼
            [錨點已掛載？] ──否──► log(skippedUnmounted, WARNING) ──► 返回
                     │是
                     ▼
            [裁決 = f(級別, 持有者)]
                     │
   ┌─────────┬───────┴────────┬──────────────┐
   ▼         ▼                ▼              ▼
未知持有者  持有者較低      持有者同級      持有者較高
   │         │                │              │
   └────┬────┴────────────────┘              ▼
        ▼                              log(yielded, 等級依 origin) ──► 返回
   清除（唯一無引數）                    （不清除、不呈現、不排隊）
        ▼
   log(preempted)
        ▼
   log(shown)
        ▼
   呈現（唯一）
        ▼
   結束時 log(closed)
```

**三條路徑在過渡期的可達性**：正式路徑恆走最左（未知持有者）；其餘三條由元件層測試
直接傳入快照觸發。這正是 `SPEC-003`〈鑑別「綁級別」與「綁呼叫路徑」兩種實作〉指定的
驗收方式（「注入一個目前佔用者為高級別的測試替身狀態」），不是為了測試而開的後門。
#### P3a.5 靜態守衛的實作策略（G-E，T-205-14～21）

##### P3a.5.1 註解剝除器的形態：單趟狀態機，不是逐行正規表示式

Phase 2 已確立剝除契約（`///`／`//` 至行尾、`/* */` 整段跨行、字串字面量不解析為已知限制）。
Phase 3a 指定其**形態**：

```text
FUNCTION 剝除註解(原始文字) -> 文字
    狀態 <- 程式碼
    輸出 <- 空
    逐字元掃描 c（可前瞻一個字元）：
        CASE 狀態 = 程式碼:
            IF c 與下一字元為 "/" "/" THEN 狀態 <- 行註解；跳過兩字元
            ELSE IF c 與下一字元為 "/" "*" THEN 狀態 <- 區塊註解；跳過兩字元
            ELSE 輸出 += c
        CASE 狀態 = 行註解:
            IF c 為換行 THEN 狀態 <- 程式碼；輸出 += c      # 換行必須保留
            ELSE 丟棄
        CASE 狀態 = 區塊註解:
            IF c 與下一字元為 "*" "/" THEN 狀態 <- 程式碼；跳過兩字元
            ELSE IF c 為換行 THEN 輸出 += c                 # 換行必須保留
            ELSE 丟棄
    RETURN 輸出
END FUNCTION
```

三項形態要求，逐項有理由：

| 要求 | 理由 |
|------|------|
| **單趟字元狀態機，不得用「逐行套正規表示式」** | 逐行處理無法表達跨行的 `/* */`（它需要跨行狀態）；而「先用多行正規表示式吃掉 `/* */`、再逐行砍 `//`」是兩趟且兩趟的邊界互相干擾（區塊註解內的 `//`、行註解內的 `/*`）。單趟狀態機是唯一不需要處理這組交互的形態 |
| **換行一律保留（行數守恆）** | 過剝的最極端形態是把整檔吞掉；行數守恆使該形態立即可測（見 P3a.5.3 的不變式 3）。且保留換行後，剝除前後的行結構仍可對照，除錯成本低 |
| **`///` 不需另設分支** | `///` 的前兩字元即 `//`，行註解規則已完整涵蓋。另設分支只會多一條可寫錯的路徑 |

**已知限制照 Phase 2 契約保留，不擴充**：本剝除器不解析字串字面量。Phase 3a 已複驗
（2026-09-10，`grep -rn '://' --include='*.dart' lib`，命中數 0），`lib/` 現況無此形態；
限制的可見化由 T-205-21(a) 標註 `skip` 的 fixture 承擔。**Phase 3b 不得順手「修好」它**——
修好會使該 `skip` 測試的預期值失效，而那條測試的職責是讓限制可見，不是讓限制消失。

##### P3a.5.2 剝除器的正向對照（本票守衛的守衛）

失效方向是**變綠**的守衛必須有正向對照（Problem Analysis 附記）。剝除器的兩個失效方向
與各自的對照如下：

| 失效方向 | 徵狀 | 攔截者 |
|---------|------|-------|
| **剝太少**（近似空操作） | 註解被算成呼叫點，計數**上升** | T-205-14 的「恰 2 次」、T-205-16(a) 的「恰 1 次」等上界；以及 T-205-21(b) 的正向對照（`shell.dart` 剝除後 `ScaffoldMessenger` 命中 0）先紅 |
| **剝過頭**（把程式碼也吃掉） | 計數**下降**，多數斷言反而變綠 | 各斷言的「恰 N 次」下界；T-205-20 的「`AppSnackBar.show` 至少 1 次」；P3a.5.3 的三項剝除器不變式 |

**T-205-21(b) 的基準值已由 Phase 3a 獨立複驗**：`lib/app/shell.dart` 剝除前
`ScaffoldMessenger` 命中 2 次（第 92、94 行，兩處皆在說明註解內；第 94 行者為
`ScaffoldMessengerState`，子字串命中），剝除後應為 0。此基準與 Phase 2 的 2026-09-10
實測一致。**若 Phase 3b 實跑時剝除前不是 2**，代表 `shell.dart` 的註解在此期間被改動，
處置是更新該基準值並在 Solution 記錄，**不是**改用不等式放寬。

##### P3a.5.3 剝除器 fixture 的三項不變式（落在 T-205-21(a) 內，非新增測試）

T-205-21(a) 的職責是「對四種形態的 fixture 產出預期結果」。Phase 3a 指定其中三項應同時
被斷言，因為它們各自封住一個獨立的失效方向：

1. **形態正確性**：`///`、行尾 `//`、跨行 `/* */` 三種形態各自被剝乾淨，且註解之前的程式碼
   原樣保留（不只斷言「註解消失」，也要斷言「程式碼還在」）。
2. **無註解輸入的恆等性**：不含任何註解的 fixture 剝除後與輸入**逐字相等**。這條直接排除
   「剝除器對正常程式碼也有動作」這一整類過剝。
3. **行數守恆**：任意 fixture 剝除前後行數相等。

字串內 `//` 一項依 Phase 2 標註 `skip` 並引用 P2.4 的限制說明。

##### P3a.5.4 大括號深度與函式體抽取：配對計數，且與剝除器分離

Phase 2 移交注意事項第 3 點明訂「以配對計數而非最近的 `}`」。Phase 3a 指定形態：

```text
FUNCTION 深度掃描(已剝除文字) -> 每個位置的深度
    深度 <- 0；狀態 <- 程式碼
    逐字元：
        CASE 狀態 = 程式碼:
            IF c 為單引號或雙引號 THEN 狀態 <- 字串（記住引號種類）
            ELSE IF c = "{" THEN 深度 += 1
            ELSE IF c = "}" THEN 深度 -= 1
            記錄本位置的深度（"{" 記其後、"}" 記其前，以本文式為準並在測試內註明）
        CASE 狀態 = 字串:
            IF c 為跳脫字元 THEN 跳過下一字元
            ELSE IF c 為同種引號 THEN 狀態 <- 程式碼
    RETURN 深度表
END FUNCTION

FUNCTION 取函式體(已剝除文字, 簽名樣式) -> 文字
    起點 <- 簽名樣式之後第一個 "{"
    以深度掃描自起點向後走，直到深度回到起點前的值
    RETURN 兩者之間的文字
END FUNCTION
```

**為何深度掃描要另外處理引號，而剝除器不處理**：兩者的失效後果不同。剝除器不處理字串
只在「字串內含 `//`」時出錯（`lib/` 現況零命中，已標為已知限制）；**深度掃描不處理字串
則在現況即出錯**——`app_snack_bar.dart` 內已存在含大括號的字串插值（日誌組句處的
`'${...}=${...}'` 形態）。該處的大括號雖然配對、深度會回正，但若日後出現不配對的形態
（如字串內單一 `}`），深度表會整段偏移而 T-205-16(e)／T-205-17 **靜默給出錯誤結論**。
在同一個掃描器內多一個字串狀態的成本是數行，換掉一整類無聲失效。

**兩者是兩個函式，不是一個**：剝除器的輸出是文字（供計數用），深度掃描的輸出是深度表
（供位置比較用）。合併會使剝除器背上它不需要的引號狀態，也使 Phase 2 已凍結的剝除契約
變得難以逐條驗證。

##### P3a.5.5 靜態守衛的訊息要求

| 測試 | 訊息要求 |
|------|---------|
| 全體 G-E | 每個斷言附理由，含票號 `0.1.0-W3-205` 與被違反的不變式名稱（P2.5 既有規範） |
| T-205-16 | 訊息須指名 INV-SNACKBAR-NOQUEUE，並說明「不清除就呈現會使框架內建 FIFO 生效」，使紅燈者知道為何不能只把斷言改寬 |
| T-205-18 | 白名單不符時，訊息須列出「多出的成員名稱」，並說明新增靜態成員需先確認不是在引入持有者狀態或待顯集合 |
| **T-205-21(c)**（PM 附加要求） | 失敗訊息**必須指名補救動作**：「`SPEC-003` 的章節標題已變更，請同步更新 `lib/app/attention_level.dart` 與 `lib/components/app_snack_bar.dart` 的 dartdoc 章節引用（本斷言為該引用的唯一守衛）」。**不得只印預期與實際值**——紅燈出現在改名者的票上，而他不知道自己踩到什麼，訊息是他唯一的線索 |
#### P3a.6 測試面策略

##### P3a.6.1 12 格矩陣：迴圈驅動，期望值以獨立資料表宣告

Phase 2 移交注意事項第 2 點要求「維持參數化生成而非展開為 12 段複製」。直接的做法有一個
陷阱：若期望值也由「比較級別序」生成，測試就與被測邏輯同構——實作寫錯時期望值同步寫錯，
12 格全綠。策略如下：

| 層 | 形態 | 理由 |
|----|------|------|
| 期望值 | **顯式常數表**，每列為（持有者級別或空、新請求級別、預期裁決） | 期望值必須是獨立宣告的事實，不得由被測規則推導 |
| 測試主體 | **單一工廠函式**，對常數表逐列產生一個測試案例（沿用 P2.3 骨架四步驟） | 不複製 12 段；新增列只需加一行資料 |
| 完整性守衛 | 一條 meta 斷言：常數表列數 == 級別數 ×（級別數 + 1） | 新增第四級別時該斷言紅燈並指名「請補齊裁決矩陣的新增列」，避免矩陣悄悄退化為部分覆蓋 |

meta 斷言的訊息須指名補列動作，理由同 P3a.5.5（紅燈者是新增級別的人，不是本票的人）。

##### P3a.6.2 既有測試遷移（M1～M20）

**共同禁令（沿用 P2.6 表末，Phase 3b 不得例外）**：不得把「清單完全相等」的斷言放寬為
`contains` / `containsAllInOrder`。完全相等是這批日誌測試的價值來源——它使「多出一個未預期
的事件」也會紅燈，而放寬後多出的事件將不再被發現。**M17 亦不得以放寬斷言（如
`findsAtLeastNWidgets`）掩蓋**。

依 P2.6 的類別，遷移動作分三種形態，處置策略各異：

| 類別 | 項目 | 策略 |
|------|------|------|
| (C) 編譯驅動 | M1、M2 | 機械補參數。**預設值只存在於測試輔助函式**，不得回流到元件的 `show`（Phase 1 §2.0 已否決）。M2 的 13 處逐一補，補完以編譯通過為完成判準 |
| (E) 事件序 | M3、M4、M5、M6、M8、M11、M14 | 過渡期每次 `show` 先發 `preempted{holderUnknown: true}` 再發 `shown`，既有的完全相等清單須補入該值。**M8／M14 的索引位移後，須逐一確認比較的仍是原本那兩筆事件**（M8 明訂：位移後比較的必須是 `shown` 與 `closed`，不是 `preempted` 與 `shown`）。**M11 不得改成依位置選取**（`records.last`），一律改為依事件值條件選取 |
| (S) 規格驅動 | M16 | 事件列舉擴為六值，**既有四值維持原順序在前**，新增兩值接於其後。此項紅燈即其正確運作 |
| 不改 | M7、M9、M10、M12、M13、M15、M18、M19、M20 | **原樣保留**。其中 M7／M15 是「裁決不得前移到錨點檢查之前」的雙重守衛，若因新增裁決而紅燈，代表 P3a.1.1 的出口 1／出口 2 順序被寫反，處置是改實作不是改測試 |
| 待實跑 | M17 | Phase 3b 實跑 `pumpAndSettle()`；若逾時或提前消失，改用明確推進（`pump` + 既有的時間推進輔助函式），禁止放寬斷言 |

**依位置選取的通則**：本批遷移中凡是「以索引取事件」的斷言，位移後都應優先改為
**依事件值條件選取**（既有測試已有此形態：M9／M10／M12 正因如此而不需改動）。索引選取
在下一次事件集合變動時會無聲取到錯的一筆，而條件選取不會。M8／M14 若在改動成本可接受
的前提下一併改為條件選取，屬正向改善；但**清單完全相等的那一條斷言必須保留為索引無關
的完整比較**，不可一併改寫。

##### P3a.6.3 提交切分（Phase 2 移交注意事項第 4 點）

「遷移與新增分開檢視」的可執行形態，受一項語言約束：`level` 為必填參數，**元件簽名改動與
既有測試遷移必須同批**（否則測試檔無法編譯）。故切分為兩批而非三批：

| 批次 | 內容 | 完成判準 |
|------|------|---------|
| 批 1（遷移） | 級別列舉新檔 + 元件簽名／裁決／兩個新事件／動作回呼順序 + 四個呼叫端補參數 + M1～M20 遷移 | Phase 2 記錄的五檔基線（59 passed）**回到全綠**，且無任何斷言被放寬 |
| 批 2（新增） | T-205-1～22 全部新增測試（含新增的原始碼契約測試檔） | 22 條全綠；批 1 的既有測試不因批 2 而改動 |

**批 1 全綠是批 2 的前置**：批 1 若有紅燈，先依 P2.6 對照 M 編號判讀（標「不改」者紅燈＝
實作缺陷、標 (C)/(E)/(S) 者先查遷移動作），紅燈未清不進批 2。混在一起會使 review
無法分辨哪些紅燈是遷移錯誤、哪些是實作缺陷。

#### P3a.7 關鍵實作指引

**第一階段目標（批 1）**，依相依序：

1. 建立級別列舉檔（僅型別、無行為；dartdoc 引用 `SPEC-003` 章節標題文字與三個級別主名）。
2. 元件：新增持有者快照值型別、私有裁決列舉、裁決純函式（P3a.1.2）。
3. 元件：`show` 依 P3a.1.1 骨架改寫——**先寫兩道前置護衛，再寫呈現路徑**。順序反過來寫
   （先改條件再抽出護衛）容易留下把清除包進分支的形態。
4. 元件：事件列舉補兩值（接於既有四值之後）、動作回呼改序（P3a.1.3）。
5. 四個呼叫端補 `level`（全部 `discardable`）；`gap_report_screen.dart` 兩處同批補
   `origin: userInitiated`（Phase 1 §2.6 附帶發現）；`main.dart` 分支改走元件層。
6. M1～M20 遷移（P3a.6.2）。

**第二階段目標（批 2）**：G-E（原始碼契約，零依賴，可最先或並行）→ G-B（12 格矩陣 + 其餘）
→ G-C → G-A。G-E 不需要 widget 環境，是本票唯一可完全獨立分派的群組。

**Phase 3b 遇紅燈的判讀順序**：(1) 對照 P2.6 找 M 編號；(2) 對照 P3a.1.1 確認兩個出口的
位置；(3) 靜態守衛紅燈先跑 T-205-21(b) 的正向對照，確認不是剝除器自身失效；(4) 以上皆非
才視為實作缺陷。**任何情況下不得以放寬斷言或 `skip` 消紅燈**（唯一例外是 T-205-21(a)
既有的已知限制標註）。

#### P3a.8 權宜方案與技術債務

| 項目 | 形態 | 收斂／處置 | 可稽核標記 |
|------|------|-----------|-----------|
| 持有者資訊由呼叫端傳入 | 過渡形態（Phase 1 §2.7）。0.1 呼叫端無資訊來源，一律傳空；正式路徑恆走「未知持有者」分支，行為與現況一致（清除後呈現） | **trigger：`0.1.0-W3-207` 合併**（`decision-trigger-binding` 規則 1 狀態 (b)）。收斂時刪除持有者快照型別與該參數，改由仲裁決策的被搶佔者欄位判定 | 每筆 `preempted` 事件的 `holderUnknown` 鍵：**鍵恆存在、值二分**；收斂完成時該值應在正式路徑上不再為真，可作為收斂是否落地的機械判據（T-205-6 同時鎖住兩側） |
| 跨級別的讓步規則在正式路徑不被執行 | 誠實邊界（Phase 1 §2.7）。0.1 經元件的訊息全為可棄一級，跨級別情形在正式路徑不存在 | 非缺陷：`SPEC-003` 已以時序互斥證明兩規則對本專案空成立。鑑別能力由 12 格矩陣預先建立 | 12 格矩陣的 9 個非空持有者格 |
| 剝除器不解析字串字面量 | 已知限制（Phase 2 契約）。Phase 3a 複驗 `lib/` 現況零命中 | 形態出現時由 T-205-21(a) 的 `skip` fixture 作為發現點；**不在本票修復** | 該 `skip` 測試本身 |
| `chooseFolderUnavailableMessage` 未登記於指派表 | 以過渡值 `discardable` 實作 | trigger：`0.1.0-W3-248` 補寫該列 | Phase 1 §2.6 表格該列已標「未登記」 |
| 既有元件測試檔遷移後約 950 行 | 超過認知負擔門檻 | **Phase 4a 明示評估項**（Phase 2 P2.9 已定），須逐項回答「G-B／G-C／G-D 是否應再拆檔」，不得以「無需重構」一句帶過 | — |
| 測試套件讀取規格文件 | 跨產物耦合（T-205-21(c)，PM 已核准） | 不收斂：這是刻意建立的引用守衛 | 失敗訊息須指名補救動作（P3a.5.5） |

**Phase 4a 應另外檢視的兩點**（非債務，屬本票新引入而值得復核者）：

1. 事件欄位組裝（P3a.1.1 的 `preempted` 五欄）目前內聯於 `show`；若 Phase 3b 為可讀性
   抽為私有輔助函式，須確認該函式**不在**裁決純函式的呼叫鏈上（T-205-17(c) 只約束裁決
   函式，但兩者混淆會使該守衛的意圖被稀釋）。
2. 元件的靜態成員白名單（T-205-18）在本票後成為十一項；每次新增靜態成員都須通過該白名單，
   Phase 4a 應確認白名單本身沒有因重構而被批次放寬。

#### P3a.9 語言特定實作注意事項（Dart／Flutter）

供 Phase 3b（parsley-flutter-developer）參考，非本階段的實作決定：

| 主題 | 注意事項 |
|------|---------|
| 級別序 | 列舉的宣告順序即序值，比較直接用序值；**不要另立整數常數**（票面 acceptance 3 與 T-205-19 皆會紅） |
| 前置護衛 | 讓步分支必須是 `if (...) { log; return; }` 形態；不可寫成包住清除與呈現的 `if/else`（T-205-16(e)） |
| 閉包捕獲 | 結束回呼的欄位沿用既有做法，捕獲本次呼叫的區域繫結，不讀模組級現值（識別計數器可能已被後續呼叫遞增） |
| 早退與載體查找 | 載體查找必須維持在錨點檢查之後；讓步路徑**不得**先查載體（查找本身無副作用，但會使「讓步不觸碰載體」這件事無法由讀碼確認） |
| 原始碼契約測試 | 純 `test()`、不用 widget 測試入口；以專案相對路徑開檔；全掃描用目錄遞迴列舉而非硬編碼清單；檔案不存在時明確失敗不靜默跳過（P2.5 既有規範） |
| 時間推進 | 一律用既有的假時鐘推進輔助函式與動作時間 token，禁止碼錶類斷言（`test-assertion-design-rules` 規則 D1；P2.7 約束 2） |
| 識別值 | 禁止斷言識別值的絕對數字，一律以同測試內取得的值比較（P2.7 約束 1） |

#### P3a.10 交接 Phase 3b 檢查清單

- [ ] `show` 的兩個出口皆在唯一的清除之前，清除與呈現為函式主體直屬語句、其間無 `return`
- [ ] 裁決純函式輸入恰兩項，函式體只讀持有者級別
- [ ] `sameLevelReplace` 由裁決結果推導，非由兩個級別欄位相等推導
- [ ] 動作回呼順序為「記錄 → 帶原因清除 → 回呼」
- [ ] 剝除器為單趟狀態機、保留換行；深度掃描為另一函式且處理引號
- [ ] T-205-21(b) 的基準（`shell.dart` 剝除前 2 → 剝除後 0）實跑確認
- [ ] T-205-21(c) 與 12 格 meta 斷言的失敗訊息皆指名補救動作
- [ ] 批 1 完成時五檔基線回到全綠且無斷言被放寬；批 2 與批 1 分開檢視
- [ ] M17 的 `pumpAndSettle` 已實跑確認，未以放寬斷言掩蓋
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 2 改動前基線（sage-test-architect，2026-09-10）

Phase 2 不寫程式碼，本節記錄的是**改動前**的綠燈基線，供 Phase 3b 判斷紅燈是新引入
還是既有問題。非本票的完成記錄。

```
fvm flutter test test/unit/components/app_snack_bar_test.dart \
                 test/unit/app/home_page_workspace_test.dart \
                 test/unit/screens/gap_report_test.dart \
                 test/unit/screens/scan_notification_controller_test.dart \
                 test/window_size_contract_test.dart
```

結果：**59 passed, 0 failed**（`All tests passed!`）。

五個檔案的選取依據為 Solution P2.6 遷移核對表——M1～M16 落在
`app_snack_bar_test.dart`，M17 落在 `home_page_workspace_test.dart`，
M18／M19 落在兩個畫面測試檔；`window_size_contract_test.dart` 為 G-E 原始碼契約測試
的形態先例，一併確認其現行綠燈。

**Phase 3b 的紅燈判讀規則**：改動後若上述任一檔出現紅燈，先對照 P2.6 找出對應的
M 編號——標「不改」者紅燈即為實作缺陷（不是遷移遺漏），標 (C)／(E)／(S) 者
先確認遷移動作是否已依表執行。**不得以放寬斷言消紅燈**（P2.6 表末禁令）。

### Phase 3a 記錄（pepper-test-implementer，2026-09-10）

Phase 3a 產出為策略與虛擬碼，**無程式碼改動、未執行測試套件**。Phase 2 記錄的改動前
基線（五檔 59 passed）仍為 Phase 3b 的判讀依據，本階段未使其失效。

本階段執行的唯讀查證（供 Phase 3b 免於重跑）：

| 查證 | 方式 | 結果 |
|------|------|------|
| T-205-21(b) 正向對照的基準值 | 以 grep 檢視 lib/app/shell.dart 的 ScaffoldMessenger 與 showSnackBar 命中 | ScaffoldMessenger 命中 2（第 92、94 行，皆在說明註解內），與 Phase 2 實測一致；剝除後應為 0 |
| 剝除器已知限制是否已在現況命中 | 以 grep 遞迴搜尋 lib 下 dart 檔中的雙斜線 URL 形態 | 0 命中，該限制現況仍為惰性 |
| 深度掃描需處理引號的理由 | 讀 lib/components/app_snack_bar.dart 的日誌組句處 | 檔內已存在含大括號的字串插值形態，故深度掃描須具引號狀態（見 P3a.5.4） |
## NeedsContext
### Phase 1 回報（lavender-interface-designer，2026-09-10）

**(1) `where.files` 需擴充，請 PM 裁定。**

- **缺失項**：票面 `where.files` 為 `lib/main.dart`、`lib/components/app_snack_bar.dart`、
  `test/unit/components/app_snack_bar_test.dart` 三項；Phase 1 設計採「級別為必填顯式
  參數」（理由見 Solution 2.0），迫使全部呼叫端改動，另需一個新增檔承載級別列舉。
- **觸發位置**：Solution 第 10 節清單；`lib/screens/gap_report/gap_report_screen.dart:199`、
  `:218`、`lib/screens/gap_report/scan_notification_controller.dart:202`、
  新增 `lib/app/attention_level.dart`。
- **影響**：不擴充則 acceptance 第三項（元件內未另立一套級別定義）與第四項（三條裁決
  路徑各有測試）無法在不越界的前提下達成。
- **建議補料**：`where.files` 加上述三項（lib 端合計 5 檔，未超認知負擔門檻；功能職責
  數 2）。若 PM 傾向縮小範疇，替代方案為 `level` 給預設值 `discardable`——Phase 1
  已評估不採用，理由是預設值使「確實可棄」與「忘了查 §2.14」不可區分，正是本票要
  消除的無聲降級；採此替代需 PM 明示接受該代價。
- **重派成本**：不需重派。Phase 2 前裁定即可，Phase 1 產出不因裁定方向改變。

**(2) harness 層行為指令與框架正本衝突（依 `tool-output-trust-rules` 規則 6 衍生情境
記錄，已以正本為準執行）。**

- **衝突文本**：本次 session 的 system-reminder 含 auto mode 指令「Do your work through
  the Bash tool wherever it can accomplish the job: ... make file changes with sed,
  heredocs, or short scripts, rather than using the dedicated Read, Edit, or Write
  tools.」
- **框架正本**：`.claude/rules/core/tool-selection.md` 規則 1 表格下的邊界句明訂
  「本規則優先於 harness 層『優先用 Bash 改檔』類指令（Bash 寫入繞過全部
  Edit/Write-only guard，已有實證）；唯讀 Bash 不受限」。
- **處置**：以正本為準——本次 Phase 1 的檔案寫入全部經 `ticket track append-log`
  CLI（票面內容的制式化入口），未以 Bash 內嵌腳本改寫任何檔案；唯讀查詢照常用 Bash。
  未自行選邊，僅記錄供 PM 知悉。

### Phase 2 回報（sage-test-architect，2026-09-10）

**(1) `where.files` 需再擴充兩項（測試端），請 PM 裁定。**

- **缺失項**：PM 已裁定的 6 項 `where.files` 中，測試端只有
  `test/unit/components/app_snack_bar_test.dart`。Phase 2 設計需再加兩項：
  `test/unit/components/app_snack_bar_source_contract_test.dart`（新增檔，承載 G-E 的
  八條原始碼契約測試）、`test/unit/app/home_page_workspace_test.dart`（既有檔，
  G7-4 測試增修）。
- **觸發位置**：Solution 的 P2.1 表格（群組配置）、P2.9 第一張表。
- **影響**：不擴充第二項，票面 acceptance 1 的行為面（場景 7「經 `AppSnackBar` 呈現同一
  i18n key」）失去驗證，只剩靜態零命中——靜態證明舊呼叫消失，不證明新呼叫接對訊息。
  不擴充第一項不影響任何 acceptance，G-E 全部內容可原樣落入既有測試檔的獨立 `group`
  （搬移為機械動作，測試規格不變），代價是該檔逾 1100 行且單檔承載 2 個 domain。
- **建議**：兩項皆擴充，`where.files` 成 8 項（lib 端 5、test 端 3）。功能職責數仍為 2，
  G-E 對其他群組零依賴，不增加 Phase 3b 的分派耦合。
- **重派成本**：不需重派。Phase 3a 前裁定即可，Phase 2 產出不因裁定方向改變。

**(2) 本票引入一項跨產物耦合，請 PM 確認接受。**

- **內容**：T-205-21(c) 使測試套件讀取 `docs/spec/ui/SPEC-003-interaction-response.md`，
  斷言五段章節標題文字存在（「注意力通道的到達類別與級別指派表」「讓步規則的可驗收形態」
  「鑑別「綁級別」與「綁呼叫路徑」兩種實作」「截斷事件的日誌等級」
  「顯示位爭用時的約束與 0.1 的處置規則」）。
- **取捨**：加，則 `SPEC-003` 章節改名時測試紅燈，且該紅燈可能由與本票無關的 DOC 票觸發；
  不加，則 `attention_level.dart` 與元件 dartdoc 的章節標題引用會靜默 rot，而票面
  acceptance 3 明訂「級別取值以標題文字引用 SPEC-003」——引用不可驗證等同該條無守衛。
- **Phase 2 判斷**：加的價值較高，已寫入設計；但這是測試套件對 `docs/` 的新依賴方向，
  超出單純的元件測試範疇，故提請確認。PM 若不接受，刪去 T-205-21(c) 一項即可，
  其餘 21 條規格不受影響。

**(3) 一項需 Phase 3b 實跑確認的風險（不需 PM 動作，記錄供追蹤）。**

`test/unit/app/home_page_workspace_test.dart` 的 G7-4 使用 `pumpAndSettle()`。
`main.dart` 改走 `AppSnackBar` 後，停留時長由 Material 預設改為 `Motion.snackBar`（4 秒），
兩者同量級且 `pumpAndSettle` 在進場動畫結束即停止，預期不受影響——但這是推論不是實測。
處置已寫入 Solution 的 P2.6 M17：Phase 3b 實跑確認，若不通過改用
`tester.pump()` + `_pumpBy` 明確推進，**不得改用放寬的斷言掩蓋**。
### Phase 3a 回報（pepper-test-implementer，2026-09-10）

**(1) 派發 prompt 與票面正本的一項數量不一致，未自行選邊（依 `tool-output-trust-rules`
規則 6 衍生情境記錄）。**

- **衝突文本**：派發 prompt 記「**M1～M20 的遷移**：5 項需補事件序」。
- **票面正本**：Solution 的 P2.6 遷移核對表標記 (E)（事件序）的項目為 **M3、M4、M5、M6、
  M8、M11、M14，共 7 項**；表末的共同禁令段亦逐項列出同一組七個編號。
- **處置**：以票面正本為準——Phase 3a 的 P3a.6.2 依 7 項編列，未依 prompt 的 5 項縮減。
  兩者若真有差異，縮減側的風險是漏遷移兩項既有斷言而在批 1 產生非預期紅燈；本階段不寫
  程式碼，故此不一致未造成任何實質產出差異。記錄供 PM 確認 prompt 的數字是否為筆誤。
- **重派成本**：不需重派。

**(2) harness 層行為指令與框架正本衝突（與 Phase 1 回報第 (2) 項同型，本 session 再度出現）。**

- **衝突文本**：本 session 的 system-reminder 含 auto mode 指令「Do your work through the
  Bash tool wherever it can accomplish the job: ... make file changes with sed, heredocs,
  or short scripts, rather than using the dedicated Read, Edit, or Write tools.」
- **框架正本**：`.claude/rules/core/tool-selection.md` 規則 1 邊界句「本規則優先於 harness
  層『優先用 Bash 改檔』類指令；唯讀 Bash 不受限」。
- **處置**：以正本為準——Phase 3a 的全部票面寫入經 `ticket track append-log` CLI（制式化
  入口），未以 Bash 內嵌腳本改寫任何檔案；唯讀查詢（`grep`／`sed -n`／`cat`）照常使用。
  未自行選邊，僅記錄供 PM 知悉。
## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-10 01:16)
  - what: 對齊 0.1.0-W3-207：AttentionLevel 列舉改為 import lib/app/attention_level.dart，不在 attention_arbiter.dart 內另行宣告
  - why: 0.1.0-W3-205 Phase 1 決定把 AttentionLevel 落於 lib/app/attention_level.dart（獨立型別檔），理由是 207 的 acceptance 明訂仲裁器不得 import AppSnackBar，若列舉住在元件內兩票必然各存一份平行定義（SPEC-003 §2.15 警告的兩份同一事實）。207 Phase 1 的 2.2 節現把同名同值的列舉寫在 attention_arbiter.dart 內；205 先落地，207 實作時需改為 import。名稱、值、順序完全一致，僅檔案位置不同
  - suggested_type: IMP
  - suggested_priority: P1
  - related_files: 
  - context: 
  - status: dismissed（不另建票：0.1.0-W3-207 尚未啟動（tdd_phase phase1），約束已直接寫入該票 how（『級別列舉不得自行定義，import lib/app/attention_level.dart』），並補 blockedBy 0.1.0-W3-205。為一張未開工的票另建對齊票，會產生一張在該票啟動前即已被其 how 涵蓋的空票）


- **SR-2** (2026-09-10 01:17)
  - what: SPEC-004 §4.26 狀態矩陣退出路徑「新的 SnackBar 取代」與回饋契約「重複觸發防護：冪等——新的 show 取代前一則」兩處補級別條件
  - why: 0.1.0-W3-205 把截斷條件綁在級別上之後，低級別請求遇高級別持有者時不取代（讓步、不呈現），上述兩處條文的無條件措辭不再成立。0.1 現況經 AppSnackBar 的訊息全為可棄一級，冪等取代仍成立，故不是立即錯誤；但條文若不加限定，下一個新增須留痕或不可棄 SnackBar 的人會照既有條文實作成無條件取代，正是 SPEC-003 §2.15〈鑑別綁級別與綁呼叫路徑〉要防的形態。205 全程唯讀規格，SPEC-004 不在其 where.files
  - suggested_type: DOC
  - suggested_priority: P2
  - related_files: docs/spec/ui/SPEC-004-component-library.md
  - context: 
  - status: processed（已建 0.1.0-W3-245）

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-176,0.1.0-W3-184,0.1.0-W3-204 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-184 what: 定義 SPEC 設計時的前台背景分配與事件流跨 domain 協調要求（來源已完成，結論見 Solution）
- 0.1.0-W3-204 what: 補寫 SPEC-003 的注意力級別與載體指派表（來源已完成，結論見 Solution）
- 0.1.0-W3-176 what: 補齊 AppSnackBar 元件層日誌的三項可稽核欄位（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-184 why: **用戶更正（2026-09-09）**，起因是 PM 把提示密度問題框成「先問這些提示時刻是否都必要」：

> 這樣也不完全正確。如果需要提示使用者的訊息，表示有他的重要性，那**有重要性的動作，不應該同時大量執行**，或者真正有重要問題發生的時候，**應該要暫停背景的行為**，所以我才提到有的提示訊息要擋住使用者的操作。
>
> 那重點是當我們設計 spec 的時候，也要評估**有哪些行為會在背景執行，哪些動作會在前台跟使用者互動**，是不是有些動作要**避免同時請求使用者的關注或者指令**，或者會造成互動影響的 SPEC 在設計的時候，**整個事件流就應該包含跟其他 domain 確認過再執行，或者應該加入延遲的動作**。

**PM 原本的框架把提示當成雜訊，這是錯的。** 提示需求本身是重要性訊號——需要提示使用者，表示該事件有重要性。因此提示密度過高不是「提示太多」，是**同時執行了太多重要的事**。

**這使 `0.1.0-W3-167` 的處置範圍必須收窄。** 在提示層做排隊、優先級或載體分化，是幫一個排程問題做視覺化緩衝——症狀被吸收，成因原封不動，而且緩衝做得越好，成因越不可能被發現。167 處理「留下來的提示怎麼呈現」，本票處理「為什麼會有這麼多同時發生的重要事件」。

**本專案已有具體實例。** 破洞掃描掛在 `GapReportNotifier.build()`，首讀者是 App 啟動時的 `ScanNotificationController.start()`（`0.1.0-W3-122` 的主題）——使用者尚未看過破洞報告頁，掃描就已在背景完成並試圖請求通知授權。這正是「背景行為在前台注意力尚未就緒時請求使用者關注」，而現行 SPEC-003 §2.2 的觸發條件只判斷「可見頁是不是 gaps」，不判斷「現在是不是請求使用者注意的適當時機」。

編輯能力（`0.1.0-W3-073`）落地後前台互動會大幅增加，此時若背景仍有掃描、載入、git 操作各自獨立觸發，注意力競爭會從偶發變成常態。**現在定義比事後補容易。**（來源已完成，結論見 Solution）
- 0.1.0-W3-204 why: `0.1.0-W3-184` 定義了級別與規則，但**沒有指定本專案的每一種提示走哪個級別、哪個載體**。規格層不指定，實作只能各自判斷，而各自判斷的結果不會一致——這是 `0.1.0-W3-168` 為元件契約解決過的同型問題。

**已知須被規格涵蓋的一項現況違規**：`AppSnackBar.show` 無條件 `hideCurrentSnackBar()`，使 L3 完成通知可截斷正在顯示的 L2 失敗訊息（違反截斷規則第三條）。修法屬實作，但**「哪個級別可截斷哪個級別」必須先是規格**，否則實作票沒有可依循的判準。

**不寫則後果**：`0.1.0-W3-184` 的規則停在方法論層，本專案無任何可驗收的落點——規則存在但沒有任何測試會因違反它而變紅。（來源已完成，結論見 Solution）
- 0.1.0-W3-176 why: `0.1.0-W3-167` 的確定性重現實驗（四組，`fvm flutter test`）證實 `0.1.0-W3-165` 落地後仍有三項可稽核缺口，使「程式主動決定不告知使用者」這件事無法被事後追究：

| 缺口 | 現況 | 後果 |
|------|--（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Related Files
- docs/domain-map.md::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/usecases::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- .claude/methodologies::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md::write  # from 0.1.0-W3-204（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 注意力競爭的避免規則已定義，含「同時」的操作性含義與依重要性的優先序來源  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 事件流的跨 domain 協調要求已定義，含哪些事件屬會造成互動影響、確認對象、確認形態、延遲判準  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 重要問題發生時暫停背景行為的規則已定義，含何謂重要問題、暫停範圍、恢復條件  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 產出落點經裁定，跨專案通則與本專案 domain 對應關係分開落位  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 與 0.1.0-W3-167 的分工已明寫；若本票結論使 167 的某些處置不再需要，已通知其執行者  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
- [ref] [x] 本專案現有每一則提示訊息已逐一指定級別與載體，無遺漏且無同一則落兩個級別  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
- [ref] [x] 級別定義與截斷規則未重述，以標題文字引用方法論  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
- [ref] [x] 每則提示的標定點已寫明（級別在邊界或發起點標定一次，之後傳播單調不升）  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
- [ref] [x] SPEC-003 既有 §2.x 編號與既有時間 token／狀態／錨點類別集合皆未變動  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
