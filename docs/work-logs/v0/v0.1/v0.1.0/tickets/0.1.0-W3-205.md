---
id: 0.1.0-W3-205
title: 修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-204
- 0.1.0-W3-176
- 0.1.0-W3-239
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-184
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-184 Solution 第二節：現況違反截斷規則第三條
  final_decision: 改走元件層單一入口，並把截斷條件綁在級別上
  rationale: 干擾不對稱與零日誌同源於 main.dart 繞過元件層；不先收斂入口，級別判斷會有一條路徑管不到
who:
  current: lavender-interface-designer
  history: {}
what: 修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別
when: 0.1.0-W3-204 的 SPEC-003 級別對應落地之後
where:
  layer: Presentation
  files:
  - lib/main.dart
  - lib/components/app_snack_bar.dart
  - test/unit/components/app_snack_bar_test.dart
why: '`AppSnackBar.show` 無條件呼叫 `hideCurrentSnackBar()`，因此**任何一則新訊息都會截斷正在顯示的那一則，不分級別**——一則 L3 完成通知會殺掉正在顯示的 L2 失敗訊息，違反 `0.1.0-W3-184` 截斷規則第三條（低級別不得截斷任何東西）。


  **且干擾是跨路徑且不對稱的。** `hideCurrentSnackBar()` 作用於 `_snackBars.first`，**不分那一則是誰放進去的**；而 `lib/main.dart:219` 直接呼叫 `ScaffoldMessenger.of(context).showSnackBar(...)` 繞過元件層，走排隊語意。結果是元件路徑殺得掉原生路徑的訊息、反之不行，且被殺的那則**零日誌**（不經過元件層，`0.1.0-W3-165` 的四個事件對它全不適用）。


  **使用者可觀察到的徵狀**：訊息閃一下就不見了，而沒有任何紀錄能回答哪一則被殺、被誰殺。'
how:
  task_type: Implementation
  strategy: '兩件事，順序不可調換。


    (1) 把 `lib/main.dart:219` 的直接呼叫改走 `AppSnackBar`——繞過元件層是干擾不對稱的成因，也是零日誌的成因，不先收斂入口則後續的級別判斷有一條路徑管不到。


    (2) 把截斷條件綁在級別上而非呼叫路徑上：`show` 接受級別參數，僅當新訊息級別**高於**當前顯示者時才 `hide`；**同級別截斷並留痕**；低級別不得 `hide`。級別取值與各訊息的歸屬以標題文字引用 `SPEC-003`（`0.1.0-W3-204` 的 §2.14 指派表、`0.1.0-W3-232` 的 §2.15 可驗收形態），不在元件內另立。日誌須能回答被截斷的是哪一則（現行 `AppSnackBarLogEvent` 的 `closed` 事件需帶被截斷者的識別，由 `0.1.0-W3-176` 提供欄位）。


    **當前顯示者的級別從仲裁器讀，元件內不另存一份**（`0.1.0-W3-207` Phase 1 SR-2，PM 已裁決）：`0.1.0-W3-207` 的仲裁器持有恰一筆持有者記錄，元件若自行比較級別即產生第二份同一事實的狀態，兩份必然漂移。本票的 `hide` 條件改為讀仲裁器的持有者記錄，或以仲裁決策回傳的被搶佔者欄位是否非空為準。**若 `207` 尚未合併，本票先以「呼叫端傳入當前持有者級別」實作並在 Solution 記錄該過渡形態**，不在元件內建立獨立的持有者狀態；`207` 合併後由該票收斂為單一來源。


    **「同級別截斷並留痕」是經裁決的具名例外，不是疏漏**（`0.1.0-W3-239`）：方法論讓步規則第二條字面要求同級別排隊或合併，本專案裁定**不引入任何形式的提示佇列**，改以留痕滿足其保護目標（「事件已發生的告知本身不得被無痕跡銷毀」）。三項依據見 `239` 的 Solution 與其 PM 驗收更正段。**實作不得自行改回排隊**；若實作過程認為留痕不足以滿足該保護目標，停手回報 PM，不自行推翻裁決。'
acceptance:
- '[ ] lib/main.dart 不再直接呼叫 ScaffoldMessenger.showSnackBar，全部提示經 AppSnackBar'
- '[ ] 被截斷時日誌可回答被截斷的是哪一則（識別資訊而非僅事件類型）'
- '[ ] 級別取值以標題文字引用 SPEC-003，元件內未另立一套級別定義'
- '[ ] 截斷條件綁在級別上：低級別不得 hide、同級別截斷並留痕、僅高級別可截斷，三者各有測試'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-10T01:03:23'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 由 `0.1.0-W3-176` Phase 4a 轉來的兩項觀察（PM 路由，2026-09-10）

兩項皆經審查判定**當前不可達、不建票**，但本票要改寫的正是那條路徑，改寫時須各看一眼。

**觀察 A：`reason == hide` 是承重前提，但無守衛。**
現行實作以 `SnackBarClosedReason.hide` 判定「這則是被後續一次 `show` 截斷的」。該等價關係成立的前提是 `hideCurrentSnackBar()` 全 repo **恰 2 處**（審查實測：`app_snack_bar.dart:173` 預設 `hide`、`:220` 明確 `action`；無 `removeCurrentSnackBar` / `clearSnackBars`）。若日後新增第三個呼叫點，`hide` 不再恆等於「被截斷」，warning 會開始標到另一類事件上，而 T-1～T-9 沒有任何一個會紅燈。本票既然重寫截斷條件，**須重新確立或以斷言鎖住這個前提**，不可預設它繼續成立。

**觀察 B：動作回呼的順序在理論上可產生未稽核的截斷。**
`app_snack_bar.dart:214-223` 的順序為 `log(actionPressed)` → `onAction()` → `hideCurrentSnackBar(reason: action)`。若某個 `onAction` **同步**呼叫 `AppSnackBar.show`，新的一則會先取代舊的（記 `hide`），隨後那句 `hideCurrentSnackBar(reason: action)` 會把**新的那一則**以 `action` 關掉——新訊息使用者一眼未見，日誌記為 `closed{reason=action, level=null}`，**正好是這條線要消滅的那種不可稽核截斷**。

當前不可達：唯一的 `withAction` 呼叫點（`gap_report_screen.dart:199`）其 `onAction` 回傳 Future 且未 await，同步段不顯示任何 SnackBar。且此順序繼承自 `0.1.0-W3-165`，非新引入。本票改寫該段時一併確認新設計是否仍有此形態。


### Phase 1 的輸入來源（PM 整理，2026-09-10）

本票的規格與判準已全數落地，Phase 1 不需重新推導任何判準，只需把它們對應到本元件：

| 來源 | 提供什麼 |
|------|---------|
| `SPEC-003`〈注意力通道的到達類別與級別指派表〉 | 本專案每則提示的級別、載體與標定點（13 列）。**級別取值以此為準，元件內不另立** |
| `SPEC-003`〈讓步規則的可驗收形態〉 | 三條規則對本專案事件集合的可驗收形態；跨級別（規則 1／3）已證時序互斥而空成立、同級別（規則 2）為經裁決的具名例外 |
| `SPEC-003`〈顯示位爭用時的約束與 0.1 的處置規則〉的〈重評結果〉 | **不引入任何形式的提示佇列**的裁決與三項具名理由（`0.1.0-W3-239`）。本票不得自行改回排隊 |
| `0.1.0-W3-176` 的產出 | `showId`（關聯識別）、`AppSnackBarOrigin`（等級判別輸入）、`_resolveClosedLevel` 私有純函式、四個事件的 `baseFields` 展開 |
| 本檔〈由 `0.1.0-W3-176` Phase 4a 轉來的兩項觀察〉 | 觀察 A（`reason == hide` 承重前提無守衛）、觀察 B（動作回呼順序理論上可產生未稽核截斷）——本票改寫的正是這兩處所在路徑 |

**一項須正面回答的設計問題**：`show()` 目前的級別資訊只有 `origin`（發起者），而讓步規則需要的是**級別**（不可棄／須留痕／可棄）。兩者不是同一件事——`origin` 決定日誌等級，級別決定誰能截斷誰。Phase 1 須定義級別如何進入 `show()`：新增參數、由 `origin` 推導、或由呼叫端查 §2.14 指派表後傳入。**若採推導，須說明 §2.14 的 13 列是否真能由 `origin` 唯一決定級別**（實查該表：可棄列橫跨自發型與等待型，不可棄列全為自發型——單看 `origin` 不足以決定級別）。

**範疇邊界**：不改 `SPEC-003`；不改 `0.1.0-W3-176` 已落地的四個事件值語意；G2／G3（`0.1.0-W3-241`／`242`）不在本票。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 功能規格（lavender-interface-designer，2026-09-10）

模式：Full（明確指定）。`/spec init` 依 frontmatter 會落 Lite（`how.task_type` 為
Implementation 非「新增」、`where.files` 3 項），但票面 acceptance 第三、四項要求
介面契約與跨模組裁決規則，Lite 骨架無 API Signatures 與 Error Handling 兩區段。
獨立檔案 `0.1.0-W3-205-phase1-design.md` 會被 `ticket-file-access-guard-hook` 阻擋，
依本專案既有慣例（`0.1.0-W3-176`、`0.1.0-W3-207` 的 Phase 1 皆然）落於本節。

引用約定：SPEC-003 以章節標題文字定位，不用行號、不重述判準本身；方法論判準一律
連同「它回答的問題」欄引用。本節全程唯讀 SPEC-003 與 SPEC-004。

#### 0. 前置防護（ARCH-010 框架內建機制與元件庫優先）

| 步驟 | 問題 | 結論 |
|------|------|------|
| 1 | 框架內建機制是否已解決？ | **部分存在，但已被裁決排除。** `ScaffoldMessengerState` 內建 FIFO 佇列——不呼叫 `hideCurrentSnackBar()` 即自動排隊，這確實滿足讓步規則第二條字面（同級別排隊）。但 `0.1.0-W3-239` 已裁定本專案**不引入任何形式的提示佇列**，Material 內建佇列亦屬「任何形式」之一。內建機制無級別概念，無法表達「僅高於當前顯示者才截斷」。故：不是沒有內建機制，是內建機制的兩種形態（無條件排隊／無條件取代）都不是要的那一種 |
| 2 | 語言標準庫是否已有解？ | 否。Dart 標準庫無「依級別裁決佔用」的原語 |
| 3 | 本地狀態是否足夠？ | 否，且被明文禁止。判斷需要「當前顯示者的級別」，該狀態屬畫面級——SPEC-003 §2〈頁面狀態保留〉列「畫面級狀態不得存於元件庫元件內部」；`0.1.0-W3-207` Phase 1 已在 L4 持有恰一筆持有者記錄，元件內另存即為第二份同一事實 |
| 4 | 外部狀態管理 | 採用既有的：級別由呼叫端在 SPEC-003 §2.14 標定點填妥後傳入；持有者狀態由 `0.1.0-W3-207` 的仲裁器持有。本票不新增任何狀態持有者 |

**元件庫優先**：不新增元件。`AppSnackBar` 已是 SPEC-004 §4.26 定義的元件庫元件，
本票只改其入口的行為參數與截斷條件，不動任何視覺、slot、token、無障礙契約。
`level` 與 `origin` 同屬行為參數而非內容 slot（`0.1.0-W3-176` 新增 `origin` 時亦未
動 SPEC-004 §4.26 slot 契約表），故 slot 契約不需改寫；但狀態矩陣退出路徑「新的
SnackBar 取代」與回饋契約「重複觸發防護：冪等——新的 `show` 取代前一則」兩處在
讓步路徑上不再無條件成立，須由 DOC 票同步（見第 10 節，已登記）。

#### 1. Purpose

**問題**：`AppSnackBar.show` 在顯示前無條件 `hideCurrentSnackBar()`，截斷的唯一依據
是「這次呼叫發生了」——即 SPEC-003〈鑑別「綁級別」與「綁呼叫路徑」兩種實作〉判為
「綁呼叫路徑」的那一種。同時 `lib/main.dart` 的 `ChooseFolderUnavailable` 分支直接
呼叫 `ScaffoldMessenger.showSnackBar` 繞過元件層，使干擾不對稱（元件路徑殺得掉原生
路徑的訊息、反之不行）且被殺的那則零日誌。

**目標**：兩件事，順序不可調換。

1. 收斂入口：全部提示經 `AppSnackBar`，使「哪些訊息在競爭同一個顯示位」是一個可以
   被完整回答的問題。
2. 把截斷條件綁在級別上：`show` 接受級別參數，截斷與否的唯一輸入是「新請求的級別」
   與「當前持有者的級別」，判定落在單一純函式內，靜態可讀出。

**可觀察的改善**：截斷發生時，日誌能回答被截斷的是哪一則（`0.1.0-W3-176` 的
`showId` 已使被截斷者自報身分，本票再補「誰截斷了誰」的關聯）；讓步發生時（低級別
遇高級別持有者），被丟棄的那一則不再無聲——它從未呈現，但留下一筆帶級別與訊息的
事件。

**非目標**：

- 不引入任何形式的提示佇列（`0.1.0-W3-239`），含 Material 內建 FIFO。讓步的處置是
  **不呈現**，不是排隊等待。
- 不在元件內持有「當前顯示者」狀態（見第 0 節步驟 3）。
- 不改 SPEC-003 任一條文；不改 `0.1.0-W3-176` 已落地四個事件值的語意（本票只新增
  兩個事件值）。
- 不實作仲裁器（`0.1.0-W3-207`）。本票只實作載體側，且介面形狀對齊該票 Solution
  第 2.6 節的 C1／C3／C5／C6 四項載體側契約。
- G2（實際可見時長）／G3（未顯示的決定）不在本票（`0.1.0-W3-241`／`242`）。

#### 2. API Signatures

##### 2.0 須正面回答的設計問題：級別如何進入 `show()`

票面要求正面回答，三個候選逐一裁決：

| 候選 | 裁決 | 理由 |
|------|------|------|
| 由 `origin`（`AppSnackBarOrigin`）推導 | **否決** | 兩者不同軸且非單射。`origin` 的值域是 {使用者發起, 背景發起}，它回答「截斷這一則要不要記 warning」（SPEC-003〈截斷事件的日誌等級〉明訂以發起者為鍵）；級別回答「誰能截斷誰」。實查 §2.14 指派表：13 列中可棄組（列 1、3、5–10）橫跨自發型與等待型、亦橫跨背景發起（列 3）與使用者發起（列 5–10），不可棄組（列 11–13）全為自發型且不走 SnackBar。以 `origin` 推導在 0.1 只能得到常數函式（全部映射到可棄）；一旦 §2.14 新增一則背景發起的須留痕 SnackBar，其 `origin` 與列 3 完全相同而級別不同，推導函式無從區分。更根本的後果是耦合：合併後任一條規則的修改會污染另一條 |
| 由 `variant`（`plain` / `withAction`）推導 | **否決** | SPEC-003〈截斷事件的日誌等級〉已明寫「以變體為鍵會同時誤判兩邊」，該論證對級別同樣成立且更強——變體是版面決策（T4 的產物），與級別無因果 |
| 由呼叫端查 §2.14 指派表後以顯式參數傳入 | **採用** | 級別是請求的屬性、在標定點標定（SPEC-003 §2.14〈級別的標定依據〉，回答「級別是靜態標註還是動態推導？」——答案是靜態標註）。呼叫端即標定點所在處，是唯一持有該資訊的位置。參數**必填**（不給預設值）：預設值會使「確實是可棄」與「呼叫端忘了查表」在型別與日誌上都不可區分，而後者正是本票要消除的那一類無聲降級 |

##### 2.1 級別型別的落點

| 項目 | 規格 |
|------|------|
| 型別 | `enum AttentionLevel { discardable, mustLeaveTrace, undroppable }`，列舉順序即優先序，`index` 越大級別越高 |
| 對應 | 與 SPEC-003 §2.14 級別欄主名一一對應：可棄／須留痕／不可棄。0.1 無須留痕實例，仍為合法值（`0.1.0-W3-207` Phase 1 同此裁決） |
| 檔案 | **`lib/app/attention_level.dart`（新增，僅型別無行為）** |
| 為何不放元件內 | 票面 acceptance 第三項要求「元件內未另立一套級別定義」。若定義在 `app_snack_bar.dart`，`0.1.0-W3-207` 的仲裁器需要同一組值，而該票 acceptance 明訂仲裁器不得 import `AppSnackBar`——結果必然是兩份平行定義，即 §2.15 警告的兩份同一事實。放 L4 的獨立型別檔，兩者各自 import 同一份 |
| 與 `0.1.0-W3-207` 的關係 | 該票 Phase 1 的 2.2 節把此列舉寫在 `lib/app/attention_arbiter.dart` 內。名稱、值、順序完全相同，差別只在檔案。本票先落地，故由本票建立該檔；207 落地時改為 import。已登記為 spawn request 請 PM 對齊（第 10 節） |

##### 2.2 `show()` 簽名（粗體為本票新增）

```dart
static void show(
  BuildContext context, {
  required String message,
  required AttentionLevel level,            // 新增，必填
  AppSnackBarVariant variant = AppSnackBarVariant.plain,
  String? actionLabel,
  VoidCallback? onAction,
  Key? actionTestKey,
  AppSnackBarOrigin origin = AppSnackBarOrigin.background,
  AttentionHolderHint? currentHolder,       // 新增，過渡形態（見 2.7）
});
```

```dart
/// 呼叫端已知的當前通道持有者快照。
///
/// 過渡形態：`0.1.0-W3-207` 的仲裁器合併後，本型別與 `currentHolder` 參數一併
/// 由 `AttentionAccepted.preempted` 取代（收斂由該票承擔，見 2.7）。
final class AttentionHolderHint {
  const AttentionHolderHint({required this.showId, required this.level});
  final int showId;          // 被截斷者的識別（與 0.1.0-W3-176 的 showId 同一空間）
  final AttentionLevel level;
}
```

`currentHolder == null` 的語意是**未知**，不是「通道為空」。過渡期呼叫端無資訊來源，
一律傳 null；此時行為與現況一致（截斷後呈現），但事件必帶 `holderUnknown: true`，
使「這個判斷還沒有輸入」本身可被搜尋、可稽核，而非靜默退化為舊行為。

##### 2.3 截斷裁決（唯一的裁決表）

判定落在單一私有純函式，輸入恰為兩項，不接受 `variant`／`origin`／`message`／
`context`——函式體因此**不可能**引用它們（結構性可讀出，形態沿用
`0.1.0-W3-176` 的 `_resolveClosedLevel`）：

```dart
enum _Preemption { holderUnknown, preemptLower, replaceSameLevel, yieldToHolder }

static _Preemption _resolvePreemption(
  AttentionLevel incoming,
  AttentionHolderHint? holder,
);
```

| 持有者 | 新請求級別 | 結果 | 行為 | 依據 |
|--------|-----------|------|------|------|
| `null`（未知） | 任意 | `holderUnknown` | `hideCurrentSnackBar()` 後呈現；`preempted` 事件帶 `holderUnknown: true`、`preemptedShowId: null` | 過渡形態（2.7）。不得改為「不 hide 直接 show」——那會落入 Material 內建 FIFO，即 `0.1.0-W3-239` 禁止的佇列 |
| 級別 < 新請求 | 任意 | `preemptLower` | 截斷後呈現；`preempted` 事件 `sameLevelReplace: false` | 讓步規則 1「高級別抵達時可搶佔低級別」 |
| 級別 == 新請求 | 任意 | `replaceSameLevel` | 截斷後呈現；`preempted` 事件 `sameLevelReplace: true`（留痕） | `0.1.0-W3-239` 具名例外：規則 2 在本通道以截斷並留痕取代排隊或合併。**實作不得改回排隊** |
| 級別 > 新請求 | 任意 | `yieldToHolder` | **不呼叫 `hideCurrentSnackBar`、不呼叫 `showSnackBar`**，記 `yielded` 事件後返回 | 讓步規則 3「低級別不得搶佔任何請求」。規則 3 字面為「排隊等待」，但 `0.1.0-W3-239` 裁定不引入佇列，故本專案的形態是不呈現並留痕——與 SPEC-003〈顯示位爭用時的約束與 0.1 的處置規則〉「留痕是唯一不吸收症狀的滿足方式」同一理由，且對應 `0.1.0-W3-207` 裁決表的 `Skipped`／`Rejected` 兩格 |

比較鍵恆為 `AttentionLevel.index`；元件內無以載體型別、變體或呼叫路徑為鍵的分支。

**不變式 INV-SNACKBAR-NOQUEUE**：`ScaffoldMessengerState.showSnackBar` 的呼叫在
`AppSnackBar` 內恰一處，且該處必然緊接於同一函式的 `hideCurrentSnackBar()` 之後。
任何「不 hide 就 show」的路徑都會使 Material 內建 FIFO 生效，構成 `0.1.0-W3-239`
禁止的提示佇列，並使 SPEC-003 §2.16／§2.17「佔用量恆為 0 或 1」的既有結論失效。

##### 2.4 日誌事件（新增兩值，既有四值語意不變）

```dart
enum AppSnackBarLogEvent { shown, skippedUnmounted, actionPressed, closed,
                           preempted, yielded }
```

| 事件 | 何時 | `fields`（於 `baseFields` 之外） | 等級 |
|------|------|--------------------------------|------|
| `preempted` | 截斷發生的當下，由**截斷者**記 | `level`、`preemptedShowId`（`int?`）、`preemptedLevel`（`AttentionLevel?`）、`sameLevelReplace`（`bool`）、`holderUnknown`（`bool`） | 無（info）。**被截斷那一則的嚴重度不由本事件承擔**——SPEC-003〈截斷事件的日誌等級〉的鍵是被截斷者的發起者，截斷者不知道它，該判定仍由被截斷者自己的 `closed{reason: hide}` 經 `_resolveClosedLevel` 承擔，本票不改該路徑 |
| `yielded` | 低級別讓步、決定不呈現時 | `level`、`holderShowId`、`holderLevel`、`variant`、`message` | 依 `origin`：背景發起 warning（900）、使用者發起 info。**鍵與 §2.13 一致**——此處被丟棄的告知就是新來的這一則，其發起者即 `origin` |

`preempted` 與被截斷者的 `closed{showId, reason: hide}` 兩筆合起來回答票面 acceptance
第二項：前者給出「誰截斷了誰」的關聯，後者給出被截斷者的身分與嚴重度。過渡期
`preemptedShowId` 為 null 時，關聯由 `closed{reason: hide}` 單獨承擔（即 `0.1.0-W3-176`
的現況能力），且 `holderUnknown: true` 明記關聯缺口的成因。

##### 2.5 動作回呼的順序（處置轉來的觀察 B）

現行順序 `log(actionPressed) → onAction() → hideCurrentSnackBar(reason: action)` 改為：

```text
log(actionPressed) → hideCurrentSnackBar(reason: action) → onAction()
```

**為何不用 `controller.close()`**：`ScaffoldFeatureController.close()` 在 Flutter 內部
`assert(_snackBars.first == controller)` 且以預設 `reason: hide` 關閉——既會在被取代後
觸發 assert，又會把使用者按下動作的結局錯記為 `hide`（`_resolveClosedLevel` 據此判
warning），兩者都比現況差。改順序是最小且語意正確的修法：使用者按下動作，這一則就
結束了，`onAction` 的後續效果屬另一次事件。

##### 2.6 呼叫端對應表（級別取值以 SPEC-003 §2.14 為權威）

| 呼叫點 | 訊息 | §2.14 列 | `level` | `origin` |
|--------|------|---------|---------|---------|
| `main.dart` `ChooseFolderUnavailable` | `chooseFolderUnavailableMessage` | **未登記**（`0.1.0-W3-248` 補寫中） | `discardable`（過渡值，見 2.7） | `userInitiated` |
| `gap_report_screen.dart:199` | `sourceFileNotFoundSnackbarMessage` | 列 7 | `discardable` | `userInitiated` |
| `gap_report_screen.dart:218` | `openedExternallyMessage` / `externalOpenFailedMessage` | 列 5／列 6 | `discardable` | `userInitiated` |
| `scan_notification_controller.dart:202` | `scanCompleteSnackbarMessage` / `scanCompleteNoGapsSnackbarMessage` | 列 3 | `discardable` | `background` |

**附帶發現（本票必須順手修正的既有缺口）**：`gap_report_screen.dart` 兩處均未傳
`origin`，取預設值 `background`，但兩者都是使用者按下破洞項觸發——依 SPEC-003
〈截斷事件的日誌等級〉應為 `userInitiated`。現況會把使用者發起的截斷誤記為 warning。
必填 `level` 已迫使這兩處改動，同批修正 `origin` 的邊際成本為零。此二檔不在票面
`where.files`，見第 10 節。
##### 2.7 過渡形態與收斂路徑

`0.1.0-W3-207`（仲裁器）現為進行中、實作尚未合併（實查 `lib/` 無仲裁器檔案），
故本票依票面 `how` 的過渡條款實作，並在此明記形態與收斂 trigger：

| 期別 | `currentHolder` 的來源 | `hide` 條件 | 級別比較發生在 |
|------|----------------------|------------|--------------|
| 過渡（本票落地後至 `0.1.0-W3-207` 合併前） | 呼叫端傳入；0.1 呼叫端無來源，一律 null（未知） | `_resolvePreemption` 回傳非 `yieldToHolder` | 元件內單一純函式 `_resolvePreemption`。**元件不持有任何持有者狀態**——資訊逐次由參數帶入，呼叫結束即消失 |
| 收斂（`0.1.0-W3-207` 合併後，由該票執行） | 仲裁決策 `AttentionAccepted.preempted`（該票 Solution 2.6 C1） | `preempted` 非空 | 仲裁器（L4），元件不再比較。`AttentionHolderHint` 與 `currentHolder` 參數一併刪除 |

**收斂 trigger**：`0.1.0-W3-207` 合併（狀態 (b) 明確 trigger 延後，
`decision-trigger-binding` 規則 2 合法形式）。過渡期的可稽核標記為每筆 `preempted`
事件的 `holderUnknown: true`——收斂完成時該欄位應在正式路徑上不再出現，可作為
收斂是否落地的機械判據。

**過渡期為何仍要建立完整的三條裁決路徑**：SPEC-003〈鑑別「綁級別」與「綁呼叫路徑」
兩種實作〉明訂「驗收形態必須為未來新增事件預先準備鑑別能力，而非只驗證現況」，且
其行為測試要求「注入一個目前佔用者為高級別的測試替身狀態」——`currentHolder` 參數
即該注入點。過渡期正式路徑恆走 `holderUnknown`，另三條路徑由元件層單元測試以直接
傳入快照驗證，這正是該節指定的驗收方式。

**過渡期的誠實邊界**：正式路徑上跨級別的讓步規則 1／3 不會被執行到（`holderUnknown`
無條件截斷）。這不構成規格違反——SPEC-003〈跨級別：規則 1／3 對列 1–10 與列 11–13
的驗收〉已以時序互斥證明兩規則對本專案**空成立**，且 0.1 經 `AppSnackBar` 的訊息
全為可棄一級（2.6 表逐列），跨級別情形在正式路徑上不存在。

#### 3. GWT Scenarios

級別縮寫：可棄＝D、須留痕＝T、不可棄＝U。「持有者」一律指 `currentHolder` 參數。

正常流程：

- **場景 1（過渡期正式路徑）**：Given `currentHolder == null`；When 以 `level: D` 呼叫
  `show`；Then 呼叫 `hideCurrentSnackBar()` 後呈現，發出 `preempted` 事件且
  `holderUnknown == true`、`preemptedShowId == null`，隨後發出 `shown`。
- **場景 2（高級別搶佔低級別）**：Given `currentHolder = (showId: 7, level: D)`；When 以
  `level: U` 呼叫；Then 截斷後呈現，`preempted` 事件 `preemptedShowId == 7`、
  `preemptedLevel == D`、`sameLevelReplace == false`、`holderUnknown == false`。
- **場景 3（同級別截斷並留痕）**：Given `currentHolder = (showId: 7, level: D)`；When 以
  `level: D` 呼叫；Then 截斷後呈現，`preempted` 事件 `sameLevelReplace == true`。
- **場景 4（低級別讓步）**：Given `currentHolder = (showId: 7, level: U)`；When 以
  `level: D`、`origin: background` 呼叫；Then **不發出 `shown`**、畫面上原持有者未被
  清除，發出 `yielded` 事件（`holderShowId == 7`、`holderLevel == U`、`level == D`、
  帶 `message`），等級為 900。
- **場景 5（讓步的等級鍵）**：同場景 4 但 `origin: userInitiated`；Then `yielded` 事件
  等級為 info（無 900）。
- **場景 6（動作按下的順序）**：Given `withAction` 已呈現；When 按下動作；Then 事件序為
  `actionPressed` 在前，`closed{reason: action}` 隨後，且 `onAction` 於 `hide` 之後被
  呼叫恰一次。
- **場景 7（`main.dart` 收斂）**：Given `chooseFolder` 回傳 `ChooseFolderUnavailable`；
  When 分支執行；Then 經 `AppSnackBar.show`（`plain`、`origin: userInitiated`、
  `level: D`）呈現 `chooseFolderUnavailableMessage`，且 `main.dart` 內
  `ScaffoldMessenger.of(context).showSnackBar` 零命中。

邊界條件：

- **場景 8（讓步不排隊）**：Given 場景 4 的讓步已發生；When 等待超過
  `Motion.snackBarWithAction`；Then 被讓步的那一則**不會**在稍後自行出現（無延遲呈現、
  無佇列），全程只有一筆 `yielded`。
- **場景 9（動作回呼同步再顯示，觀察 B）**：Given `withAction` 已呈現，其 `onAction`
  同步呼叫 `AppSnackBar.show`；When 按下動作；Then 新的一則不被 `reason: action` 關閉
  ——新 `showId` 的 `closed` 事件不得帶 `reason: action`；使用者可見到新的一則。
- **場景 10（`hideCurrentSnackBar` 呼叫點守衛，觀察 A）**：靜態掃描
  `lib/components/app_snack_bar.dart` 原始碼：`hideCurrentSnackBar` 恰 2 處（一處預設
  `hide`、一處明確 `reason: action`），`removeCurrentSnackBar` 與 `clearSnackBars` 皆 0；
  全 `lib/` 範圍內該三個識別符在本元件之外零命中。
- **場景 11（裁決函式不接受無關輸入）**：靜態檢查 `_resolvePreemption` 的參數恰為
  `(AttentionLevel, AttentionHolderHint?)`，其函式體不含 `variant`／`origin`／
  `message`／`context`／`Type` 判斷（SPEC-003〈鑑別「綁級別」與「綁呼叫路徑」〉靜態
  檢查列）。
- **場景 12（級別定義唯一）**：靜態檢查 `AttentionLevel` 在 `lib/` 內恰一處宣告，且
  三個值與 SPEC-003 §2.14 級別欄主名一一對應；`app_snack_bar.dart` 內無第二套級別
  列舉或級別常數。
- **場景 13（`showSnackBar` 呼叫點唯一且必接於 hide 之後）**：靜態檢查
  INV-SNACKBAR-NOQUEUE（2.3）。

異常流程：

- **場景 14（context 已卸載）**：Given `context.mounted == false`；When 呼叫 `show`；
  Then 行為與現況一致——發出 `skippedUnmounted`（warning）後返回，且**不呼叫**
  `hideCurrentSnackBar`（不得因為新增裁決而在早退前先截斷）。
- **場景 15（`withAction` 缺 slot）**：Given `variant: withAction` 而 `actionLabel` 或
  `onAction` 為 null；When 呼叫；Then 觸發既有 assert（行為不變）。
- **場景 16（持有者快照已失效）**：Given `currentHolder` 指向一則已自然過期消失的
  SnackBar；When 以較低級別呼叫；Then 元件仍依級別讓步（不驗證快照新鮮度）。**理由**：
  快照新鮮度屬持有者狀態，元件不持有亦不查詢；收斂後由 `0.1.0-W3-207` 的逾期懶檢查
  （該票場景 11）承擔。過渡期呼叫端一律傳 null，此路徑不會在正式路徑發生。

#### 4. Error Handling

| 錯誤情境 | 處理策略 | 回傳／拋出 | 需要回滾？ |
|---------|---------|-----------|-----------|
| `context` 已卸載 | 早退，不觸碰 messenger | `skippedUnmounted`（warning），無截斷 | 否 |
| `withAction` 缺 `actionLabel` / `onAction` | 開發期 assert（既有） | assert 訊息不變 | 否 |
| 呼叫端未傳 `level` | 編譯失敗 | 必填參數，無執行期分支 | 否 |
| `currentHolder` 為 null（資訊缺席） | 不猜測、不代填，退回無條件截斷並標記 | `preempted{holderUnknown: true}` | 否 |
| `currentHolder` 快照已失效 | 依級別裁決，不驗證新鮮度（場景 16） | 對應決策事件 | 否 |
| 讓步後呼叫端仍期待訊息出現 | 不補救、不排隊 | `yielded` 事件即該請求的終局；等級依 `origin` 使背景發起者可被 warning 發現 | 否 |
| `onAction` 內拋出例外 | 不攔截（既有行為） | 由全域錯誤攔截承接 | 否 |

#### 5. Dependencies

| 依賴 | 類型 | 用途 | 不可用時 |
|------|------|------|---------|
| SPEC-003 §2.14 指派表 | 規格 | 呼叫端 `level` 取值的唯一權威 | 缺列者以過渡值 + trigger 處理（`chooseFolderUnavailableMessage` → `0.1.0-W3-248`） |
| SPEC-003〈讓步規則的可驗收形態〉 | 規格 | 2.3 裁決表三條規則的依據與驗收形態 | 無替代 |
| SPEC-003〈顯示位爭用時的約束與 0.1 的處置規則〉〈重評結果〉 | 規格 | 不引入佇列、同級截斷並留痕的裁決（`0.1.0-W3-239`） | 無替代；實作不得自行推翻 |
| `0.1.0-W3-176` 產出 | 已完成 | `showId`、`origin`、`_resolveClosedLevel`、四事件 `baseFields` | 已在庫 |
| `0.1.0-W3-207` Solution 2.6（C1／C3／C5／C6） | 進行中 | 載體側契約形狀，本票介面對齊之 | 未合併時走過渡形態（2.7） |
| `0.1.0-W3-248` | 待處理 | `chooseFolderUnavailableMessage` 的 §2.14 登記 | 未完成時以過渡值 `discardable` 實作並於本節記錄 |
| Material `ScaffoldMessenger` | 框架 | 呈現與 `closed` future | 無替代 |
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-176,0.1.0-W3-184,0.1.0-W3-204 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-184 what: 定義 SPEC 設計時的前台背景分配與事件流跨 domain 協調要求（來源已完成，結論見 Solution）
- 0.1.0-W3-204 what: 補寫 SPEC-003 的注意力級別與載體指派表（來源已完成，結論見 Solution）
- 0.1.0-W3-176 what: 補齊 AppSnackBar 元件層日誌的三項可稽核欄位（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-184 why: **用戶更正（2026-09-09）**，起因是 PM 把提示密度問題框成「先問這些提示時刻是否都必要」：

> 這樣也不完全正確。如果需要提示使用者的訊息，表示有他的重要性，那**有重要性的動作，不應該同時大量執行**，或者真正有重要問題發生的時候，**應該要暫停背景的行為**，所以我才提到有的提示訊息要擋住使用者的操作。
>
> 那重點是當我們設計 spec 的時候，也要評估**有哪些行為會在背景執行，哪些動作會在前台跟使用者互動**，是不是有些動作要**避免同時請求使用者的關注或者指令**，或者會造成互動影響的 SPEC 在設計的時候，**整個事件流就應該包含跟其他 domain 確認過再執行，或者應該加入延遲的動作**。

**PM 原本的框架把提示當成雜訊，這是錯的。** 提示需求本身是重要性訊號——需要提示使用者，表示該事件有重要性。因此提示密度過高不是「提示太多」，是**同時執行了太多重要的事**。

**這使 `0.1.0-W3-167` 的處置範圍必須收窄。** 在提示層做排隊、優先級或載體分化，是幫一個排程問題做視覺化緩衝——症狀被吸收，成因原封不動，而且緩衝做得越好，成因越不可能被發現。167 處理「留下來的提示怎麼呈現」，本票處理「為什麼會有這麼多同時發生的重要事件」。

**本專案已有具體實例。** 破洞掃描掛在 `GapReportNotifier.build()`，首讀者是 App 啟動時的 `ScanNotificationController.start()`（`0.1.0-W3-122` 的主題）——使用者尚未看過破洞報告頁，掃描就已在背景完成並試圖請求通知授權。這正是「背景行為在前台注意力尚未就緒時請求使用者關注」，而現行 SPEC-003 §2.2 的觸發條件只判斷「可見頁是不是 gaps」，不判斷「現在是不是請求使用者注意的適當時機」。

編輯能力（`0.1.0-W3-073`）落地後前台互動會大幅增加，此時若背景仍有掃描、載入、git 操作各自獨立觸發，注意力競爭會從偶發變成常態。**現在定義比事後補容易。**（來源已完成，結論見 Solution）
- 0.1.0-W3-204 why: `0.1.0-W3-184` 定義了級別與規則，但**沒有指定本專案的每一種提示走哪個級別、哪個載體**。規格層不指定，實作只能各自判斷，而各自判斷的結果不會一致——這是 `0.1.0-W3-168` 為元件契約解決過的同型問題。

**已知須被規格涵蓋的一項現況違規**：`AppSnackBar.show` 無條件 `hideCurrentSnackBar()`，使 L3 完成通知可截斷正在顯示的 L2 失敗訊息（違反截斷規則第三條）。修法屬實作，但**「哪個級別可截斷哪個級別」必須先是規格**，否則實作票沒有可依循的判準。

**不寫則後果**：`0.1.0-W3-184` 的規則停在方法論層，本專案無任何可驗收的落點——規則存在但沒有任何測試會因違反它而變紅。（來源已完成，結論見 Solution）
- 0.1.0-W3-176 why: `0.1.0-W3-167` 的確定性重現實驗（四組，`fvm flutter test`）證實 `0.1.0-W3-165` 落地後仍有三項可稽核缺口，使「程式主動決定不告知使用者」這件事無法被事後追究：

| 缺口 | 現況 | 後果 |
|------|--（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Related Files
- docs/domain-map.md::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/usecases::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- .claude/methodologies::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md::write  # from 0.1.0-W3-204（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 注意力競爭的避免規則已定義，含「同時」的操作性含義與依重要性的優先序來源  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 事件流的跨 domain 協調要求已定義，含哪些事件屬會造成互動影響、確認對象、確認形態、延遲判準  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 重要問題發生時暫停背景行為的規則已定義，含何謂重要問題、暫停範圍、恢復條件  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 產出落點經裁定，跨專案通則與本專案 domain 對應關係分開落位  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 與 0.1.0-W3-167 的分工已明寫；若本票結論使 167 的某些處置不再需要，已通知其執行者  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
- [ref] [x] 本專案現有每一則提示訊息已逐一指定級別與載體，無遺漏且無同一則落兩個級別  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
- [ref] [x] 級別定義與截斷規則未重述，以標題文字引用方法論  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
- [ref] [x] 每則提示的標定點已寫明（級別在邊界或發起點標定一次，之後傳播單調不升）  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
- [ref] [x] SPEC-003 既有 §2.x 編號與既有時間 token／狀態／錨點類別集合皆未變動  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
