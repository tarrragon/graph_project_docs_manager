---
id: 0.1.0-W3-171
title: 修復 Context Bundle 抽取在同一票 ID 同時出現於兩個來源欄時重複抽取，且冪等主鍵無法揭露
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-166
dispatch_reason: ''
decision_tree_path:
  entry_point: 派發 0.1.0-W3-166 後 claim 觸發重抽，觀測到 Context Bundle 內容整段重複
  final_decision: IMP 修去重，並補一條不以 sources 主鍵為斷言對象的測試
  rationale: 重複成因已在程式碼層確認（_collect_source_ids 跨 kind 不去重），非推論；去重層級有兩個候選且影響面不同，故 acceptance 要求記錄選擇理由而非指定作法
who:
  current: thyme-python-developer
  history: {}
what: 修復 Context Bundle 抽取在同一票 ID 同時出現於兩個來源欄時重複抽取，且冪等主鍵無法揭露
when: 2026-09-09 派發 0.1.0-W3-166 後，代理人 claim 觸發重抽時觀測到 Context Bundle 內容整段重複
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/lib/context_bundle_extractor.py
why: '`_collect_source_ids()`（`context_bundle_extractor.py:389`）依 `SOURCE_PRIORITY` 逐一走 `source_ticket` / `blocked_by` / `related_to` 三種 kind，把 `(id, kind)` append 進 `collected`，**跨 kind 不比對 id**。同一票 ID 同時出現在兩個來源欄時，該 ID 進入 `source_list` 兩次，主迴圈因此把它的欄位抽取兩份。


  **實證（`0.1.0-W3-166`）**：`source_ticket: 0.1.0-W3-165`，`relatedTo` 亦含 `0.1.0-W3-165`。建票當下 `relatedTo` 尚為空，抽取結果單份（標記 `chars: 1058`）；`relatedTo` 於 `e64ea721` 補上後，代理人 claim 觸發重抽，Task Reference 與 Rationale Chain 各出現兩份完全相同的內容，標記變為 `chars: 1944`。


  **本缺陷的代價不在重複本身，在它不可見**：合併邏輯的冪等主鍵是 `sources` 字串（`test_s12` 明示「chars 不同但 sources 相同」即判為 `no_change_idempotent`），而該字串對來源清單去重——165 出現兩次仍只印一個名字。**主鍵量的是來源集合，內容重複發生在來源清單的重數上，兩者不是同一個軸**，故標記在重複前後外觀一致，既有冪等測試對此恆綠。機制同族見 `PC-GPD-009`（驗證器量的軸與缺陷不同）。


  **觸發條件是機械的，不是偶發的**：任何票只要同一 ID 同時落在 `source_ticket` 與 `relatedTo`（或 `blockedBy`），必然命中；而「來源票同時列為 relatedTo」在本專案是常見寫法（`0.1.0-W3-166` 即由補關聯的維護動作造成）。'
how:
  task_type: Implementation
  strategy: '先決定去重層級再改：候選一為 `_collect_source_ids()` 收集後依 src_id 去重（保留 SOURCE_PRIORITY 較前的 kind）；候選二為把各欄位規則的 `dedup` 旗標由預設 False 改為 True。兩者影響面不同——前者改變 sources_declared 計數，後者只改內容合併行為，且 Related Files 規則已 dedup=True，代表部分去重是既有設計而非疏漏。實作前須讀 `dedup` 旗標預設為 False 的原始意圖（是否有欄位刻意允許重複），不得直接翻轉全部旗標。


    測試須涵蓋主鍵盲區：同一 ID 同時位於 source_ticket 與 relatedTo 時，`sources:` 標記字串維持單一名稱，故既有以 sources 為主鍵的冪等測試（test_s12）恆綠、對本缺陷零鑑別力。新測試須斷言抽取內容本身不含重複段落，不可只斷言標記。


    本檔為框架資產，經 sync 擴散至全部 consumer 專案，且為多個並行 session 即時呼叫中的 CLI；修改時機依 `PC-GPD-010` 排序（危險窗口是「還沒改完」而非「改壞了」）。'
acceptance:
- '[ ] 已判定去重層級並記錄選擇理由（source_list 層 vs 欄位規則 dedup 旗標層），未直接翻轉全部旗標'
- '[ ] 同一 ID 同時位於 source_ticket 與 relatedTo 時，抽取結果不再含重複段落'
- '[ ] 新增測試斷言抽取內容本身無重複，不以 sources 標記字串為斷言對象（該主鍵對本缺陷零鑑別力）'
- '[ ] 既有 merge 冪等測試（test_s12 等）仍全綠，未因去重改動而破壞既有語意'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修復 Context Bundle 抽取在同一票 ID 同時出現於兩個來源欄時重複抽取，且冪等主鍵無法揭露

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
