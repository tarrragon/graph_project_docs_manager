---
id: 0.1.0-W3-177
title: 處置 main.dart HomePage 直呼原生 SnackBar 的元件庫違規與日誌盲點
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-167
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-167 複驗一實查：以「會產生一則 SnackBar 的位置」為口徑重新盤點，發現第四個呼叫點繞過 AppSnackBar
  final_decision: 建 IMP 票處置；二擇一（改用 AppSnackBar 或移除死碼）由執行者依判準裁定
  rationale: 此處是 0.1.0-W3-165「元件內部加 log 結構上不可能漏」論證的唯一反例；不處置則該論證在文件上成立而在程式碼中不成立
who:
  current: parsley-flutter-developer
  history: {}
what: 處置 main.dart HomePage 直呼原生 SnackBar 的元件庫違規與日誌盲點
when: 0.1.0-W3-167 完成後（該票實查發現本違規）
where:
  layer: Presentation
  files:
  - lib/main.dart
  - test/unit/app/home_page_workspace_test.dart
why: '`0.1.0-W3-167` 複驗一實查發現：`lib/main.dart:219` 以

  `ScaffoldMessenger.of(context).showSnackBar(SnackBar(...))` 直呼顯示

  `chooseFolderUnavailableMessage`，未經 `AppSnackBar`。兩個獨立問題：


  1. **違反 SPEC-004 §6 原生元件禁用對照表**——該表明列「`ScaffoldMessenger.showSnackBar(SnackBar(...))` 直呼 → 改用 `AppSnackBar`」，適用範圍為 `lib/components/` 之外的所有頁面層程式碼。

  2. **它是 `0.1.0-W3-165` 涵蓋範圍論證的唯一反例**。165 的立論是「元件內部加 log → 結構上不可能漏」，該論證的前提是全部呼叫點都經過元件。此處不經過，故不留任何日誌；且停留時間未取 `Motion` token。


  **可達性現況**：`HomePage` 在正式樹上不可達——`DocsManagerApp.home` 為

  `FatalErrorGate(child: AppShell())`，全 repo 對 `HomePage` 的引用只有 `lib/main.dart`

  的定義本身與 `test/unit/app/home_page_workspace_test.dart`。故目前是測試可達、正式不可達的殘留碼。


  這不使問題消失：它仍是上述論證的反例，且日後若被重新接線即成為使用者可達的無日誌路徑——而「漏掉的那一個正好不會被發現，因為它沒有 log」正是 165 要消除的形態。'
how:
  task_type: Implementation
  strategy: '二擇一，由執行者依下列判準裁定並於 Solution 記錄理由：


    | 選項 | 何時選 | 代價 |

    |------|-------|------|

    | (a) 改用 `AppSnackBar.show` | `HomePage` 的工作資料夾選取流程仍有未來用途（例如日後接為設定入口） | 需確認 `chooseFolderUnavailableMessage` 的變體（依 `0.1.0-W3-167` 判準應為 `plain`：畫面可見、狀態未變、無附隨動作） |

    | (b) 移除 `HomePage` 與其單元測試 | 該流程已由 `AppShell` 的專案切換取代且無回收計畫 | 需確認 `WorkspaceRepository` 的四 variant 覆蓋不因刪測試而失守——若失守，覆蓋須改掛在現行可達路徑上，不可淨損失 |


    **(b) 的前置檢查是硬要求**：`test/unit/app/home_page_workspace_test.dart` 目前是

    `ChooseFolderResult` 四個 variant（含 `ChooseFolderNotRemembered` 的裁決 A 呼叫端義務）的唯一覆蓋處；直接刪除會使該義務無測試把關。


    本票不改 SPEC 條文；若裁定 (b)，SPEC-004 §6 該列不受影響（規則本身仍成立，只是少一個違規實例）。'
acceptance:
- '[ ] lib/ 中不再有 lib/components/ 之外的 ScaffoldMessenger.showSnackBar 直呼（以 rg 驗證計數為 0）'
- '[ ] 選項裁定理由已記錄於 Solution，含未採選項的排除理由'
- '[ ] 若採 (b)：ChooseFolderResult 四個 variant 的測試覆蓋未淨損失，已指明改掛位置'
- '[ ] 若採 (a)：訊息變體依 0.1.0-W3-167 判準判定並記錄判定過程'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

處置 main.dart HomePage 直呼原生 SnackBar 的元件庫違規與日誌盲點

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 來自 0.1.0-W3-167 的追加要求：G4 跨路徑干擾歸本票承接

`0.1.0-W3-167` 補記二第一節新增缺口 G4，歸本票（不歸 `0.1.0-W3-176`——該票補的是
元件內欄位，對不經元件的路徑無效）。

**不對稱干擾**：`AppSnackBar.show` 先 `hideCurrentSnackBar()` 再 show（取代語意），
而 `main.dart:219` 原生直呼不 hide（排隊語意）。`hideCurrentSnackBar` 作用於
`_snackBars.first`，**不分是誰放進去的**——故元件路徑會截斷原生路徑的訊息，反之不會。

**兩側皆無痕跡**：原生那則從未產生 `shown` 事件（不經元件），被截斷時也不產生
`closed`；元件那則的 `closed{reason=hide}` 記的是「它自己被誰關掉」，不是「它關掉
了誰」。補欄位解決不了此缺口，只能靠讓所有路徑都經過元件——即本票。

**時機論證**：`HomePage` 目前在正式樹上不可達，故跨路徑干擾只在測試環境可達。
這不降低本票必要性，反而指出最佳時機——趁該路徑尚未被重新接線時消除，成本最低；
一旦接線，G4 即成為使用者可達且雙側無痕的失效。

**對本票 acceptance 的影響**：既有第 1 項（`lib/` 中不再有 `lib/components/` 之外
的 `ScaffoldMessenger.showSnackBar` 直呼，以 rg 驗證計數為 0）已足以涵蓋 G4——
無論採選項 (a) 改用元件或 (b) 移除死碼，跨路徑干擾皆消除。不需新增 acceptance。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-167 | chars: 1792 -->

### Task Reference
- 0.1.0-W3-167 what: 設計 提示元件的使用時機、重疊處置、類型分化，與不可見時的日誌補位

### Rationale Chain
- 0.1.0-W3-167 why: **用戶裁示（2026-09-09）**：「也要額外評估一個問題，哪些時機要呼叫 snackbar 或者 bottoast 這類提示元件，**如果大量呼叫提示元件，反而會重疊遮蔽**，那有可能需要依照不同類型的提示需求，設計使用不同的提示元件，以及**如果提示元件看不到，我們要在哪些事件或者流程上要補 log**。」

`0.1.0-W3-166` 固化「有顯示就要有紀錄」，但不回答「何時該顯示」。兩者一起才完整：只有前者會導向「凡事都跳提示」，而提示互相爭用同一個顯示位時，**資訊反而比不提示更少**——後出現的取代前一則，使用者兩則都沒讀完。

**這不是假設性風險。** 本專案目前 `AppSnackBar.show(` 的呼叫點實查為 **3 處、2 個檔案**（`gap_report_screen.dart:199`、`:218`、`scan_notification_controller.dart:202`；`shell.dart` 只有註解提及，無呼叫）。而 `0.1.0-W3-113`（`ScanNotificationController.show` 失敗的 SnackBar fallback）與 `0.1.0-W3-165` 都會再增加觸發時機，編輯能力（`0.1.0-W3-073`）落地後還會更多——**呼叫點只會增加，所以此數為量測當下的值，接手者須自行複驗現值**（`reference-stability-rules` 規則 9）。

> **建票時的數字更正（2026-09-09）**：本段原寫「6 個呼叫點，分佈於三個檔案」。該值來自 `grep 'AppSnackBar.show\|AppSnackBar\.'`——第二個分支匹配任何成員存取（型別引用、列舉、註解），第一個分支的 `.` 為未跳脫的正則萬用字元，故量到的是「提及 `AppSnackBar` 的行數」而非呼叫點數。現值由兩個 session 各自以 `AppSnackBar\.show\(` 獨立實查並互相對上。另有一種口徑是「訊息結局」（同一呼叫點的分支各算一則）為 5 則，可並列參考，但 acceptance 第 2 項的對照對象是**呼叫點**。

**失效形態的更正見 Problem Analysis 的〈PM 前置實查〉第二節**：本專案的 `AppSnackBar.show` 在顯示前無條件呼叫 `hideCurrentSnackBar()`，且 `ScaffoldMessenger` 一次只呈現一則，故版面上的「重疊」不會發生，實際形態是**替換截斷**。用戶關切的後果成立且更嚴重（重疊時兩則都在畫面上，替換是後者把前者抹掉），但處置方向不同——分化元件解決的是「同時要看到兩則」，解決不了「被靜默丟棄」。

**第三個子問題是前兩者的必然對偶**：一旦決定某些時機不顯示提示，那些事件就失去了使用者可見的回饋，此時可觀測性必須由日誌承接——否則等於用「安靜」換掉了「有紀錄」，兩頭皆空。`0.1.0-W3-078` 的實機驗證正是撞上這個：路徑走到了、程式做了對的事，但沒有任何一側留下使用者層級的證據。

### Related Files
- docs/spec/ui/SPEC-004-component-library.md::read  # from 0.1.0-W3-167
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-167
- .claude/skills/ux-design-evaluation/references/interaction-feedback.md::read  # from 0.1.0-W3-167

### Source Acceptance (reference)
- [ref] [ ] 何時使用暫態提示的判準已建立且可機械套用，輸入為事件性質而非逐案列舉  # from 0.1.0-W3-167
- [ref] [ ] 若結論為需要新提示元件，已說明與 AppSnackBar 的分界並走 component-contract-design 流程  # from 0.1.0-W3-167
- [ref] [ ] 因判準或遮蔽處置而不顯示的事件集合已逐類指定日誌承接點與等級，含被丟棄的提示  # from 0.1.0-W3-167
- [ref] [ ] 承接的規格撰寫已建 DOC 票或已登記 spawn request  # from 0.1.0-W3-167
- [ref] [ ] 全部 AppSnackBar.show( 呼叫點逐一對照該判準，不符者列出並給處置（派發前實查為 3 處／2 檔，接手者須自行複驗現值）  # from 0.1.0-W3-167
  ... (truncated, see source ticket)
