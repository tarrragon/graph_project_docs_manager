---
id: 0.1.0-W3-113
title: 補上 ScanNotificationController.show 失敗時的 SnackBar fallback
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-097
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-103
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層回饋在服務失敗時消失
  final_decision: IMP 補 show 失敗的 fallback 路徑
  rationale: SPEC-003 §2.2 已為 denied 定義 SnackBar fallback，但發送失敗（非授權問題）未定義，屬規格缺口的實作面
who:
  current: parsley-flutter-developer
  history: {}
what: 補上 ScanNotificationController.show 失敗時的 SnackBar fallback
when: 0.1.0-W3-097 修好 MethodChannel 註冊後（在 channel 未註冊時修 fallback 無從驗證）
where:
  layer: Presentation
  files:
  - lib/screens/gap_report/scan_notification_controller.dart
  - test/unit/screens
why: '`0.1.0-W3-103` 盤點頁面層接線時的附帶發現：`ScanNotificationController.show()` 失敗時**沒有 SnackBar fallback**——`MacosScanNotifier` 內部 catch 之後靜默回傳，controller 不知道發送失敗，使用者在非 `nav-page-gaps` 頁面因此不會收到掃描完成的任何通知。


  SPEC-003 §2.2 為權限 `denied` 定義了 fallback（`AppSnackBar.withAction` + `viewGapsAction`，非前景時延後至 `resumed`），但那是**授權被拒**的路徑。**發送本身失敗**（授權已 granted 但 `show` 拋例外或平台回錯）沒有對應條文，實作也沒有處置。


  這是 `ARCH-GPD-001` 的第三層（event 效果）在失敗時消失的實例：成功時有系統通知，失敗時什麼都沒有——回饋與服務可用性正相關。且失敗被 `MacosScanNotifier` 的 catch 收斂為靜默回傳，符合〈降級不解除回饋義務〉所述「收斂到一個沒有表現的狀態，等同銷毀證據」。


  **blockedBy `0.1.0-W3-097`** 的理由：目前 `MethodChannel` 原生端未註冊，`show` 必然失敗。在那個狀態下修 fallback 無法區分「fallback 生效」與「fallback 沒生效但反正也沒通知」——修好註冊之後才有可驗證的成功基準線。'
how:
  task_type: Implementation
  strategy: '先確認規格立場再改碼：SPEC-003 §2.2 對「授權已 granted 但發送失敗」沒有條文。屬規格缺口，**不自行決定行為**——於 NeedsContext 提出並停手，或依既有 `denied` fallback 的形態提案後交 PM 核定，二擇一。


    實作面須先讓失敗**可被 controller 知道**：`MacosScanNotifier.show()` 目前 catch 後靜默回傳，controller 拿不到失敗訊號。介面是否改為回傳結果值（比照 `ExternalOpener` 的 opened / notFound / failed 三結果形態）由本票判斷並論證；改介面會影響 fake 與既有測試，須一併處理。


    fallback 的形態沿用 `denied` 路徑的既有機制（`AppSnackBar.withAction` + `viewGapsAction`，非前景時延後至 `resumed`），不新增元件。


    `0.1.0-W3-105` 定案本專案的服務分類中，通知發送屬「短暫非同步」類（政策為只需第三層 SnackBar、不阻擋、冪等），本票的處置須與該政策一致。'
acceptance:
- '[ ] show 失敗時 controller 能取得失敗訊號（不再靜默回傳）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補上 ScanNotificationController.show 失敗時的 SnackBar fallback

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-097,0.1.0-W3-103 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-103 what: 盤點 頁面層對元件 callback 的接線：服務受理層與 event 效果層的承擔者（來源已完成，結論見 Solution）
- 0.1.0-W3-097 what: 修復 scan_notifier MethodChannel 原生端未註冊，實機拋 MissingPluginException

### Rationale Chain
- 0.1.0-W3-103 why: `0.1.0-W3-099` 驗證了元件層：22 個可互動元件的第一層回饋（互動當下的表現）全部齊備，由 Material `InkWell` 的 ripple 提供，且該回饋不依賴任何服務回應。

同時它指出元件把第二層（服務受理）與第三層（event 效果）**委派給呼叫端**——SPEC-004 的回饋契約明文寫「本元件承載三層中的點擊確認層；等待指示為畫面級／結果通知為狀態轉換本身」。委派本身是設計上正確的：元件只發 callback，不該知道服務存在。

**但「已委派」不等於「承接方有做」**，而承接方從未被驗證。本次實機驗證撞到的正是這一層——按下重新掃描之後，第二層（服務受理：toast 或 loading）與第三層（event 效果：成功／失敗結果）都沒有出現，而元件自身的 ripple 是有的。缺陷完全落在呼叫端。

因此 `ARCH-GPD-001` 在本專案的實際落點是頁面層，不是元件層。元件層盤完不代表問題盤完——那反而是最容易誤判「已檢查過」的地方，因為結論是全綠。（來源已完成，結論見 Solution）
- 0.1.0-W3-097 why: 實機執行時，`MacosScanNotifier` 的第一次授權查詢即失敗。DevTools Logging 實測記錄（2026-09-08 15:12:51.441，`Level.WARNING (900)`）：

    查詢授權狀態失敗，原生端未註冊 handler（視為 denied）：
    MissingPluginException(No implementation found for method authorizationStatus
    on channel graph_project_docs_manager/scan_notifier)

**`macos/Runner/AppDelegate.swift` 的 `MethodChannel` 從未被註冊。** 該檔在 `applicationDidFinishLaunching` 內以

    guard let controller = mainFlutterWindow?.contentViewController as? FlutterViewController else { return }

取得 controller 後才建立 channel——guard 失敗時**靜默 return**，channel 不建立，且不留任何訊號。實測結果顯示這個 guard 確實失敗了（需確認 `mainFlutterWindow` 在該時點是否已由 NIB 載入完成，屬 macOS 啟動時序問題）。

後果：`0.1.0-W3-064` 實作的整套系統通知功能——授權查詢、授權請求、發送、撤回、點擊回傳——**在實機上零可用**。全部四個方法都會拋 `MissingPluginException`；Dart 端依 SPEC-003 §2.2 的「其他結果視為 `denied`」把它收斂掉，因此 App 不崩潰、畫面正常，只是通知永遠不會出現。

**為何 1159 個測試全綠**：`test/` 用 `FakeScanNotifier` 替換整個 `ScanNotifier`，從不經過真實 `MethodChannel`。這正是 `0.1.0-W3-074` 判定「跨邊界功能只有 fake 覆蓋、平台端事實零覆蓋」所指的缺口，本案是它的第一個實例化證據。

Dart 端的降級處理（收斂為 `denied` 並記 WARNING）設計正確——沒有它，這個缺陷會是崩潰而非靜默。但也因為降級太乾淨，缺陷在實機上唯一的痕跡就是那一行 log，而該 log 不出現在 `flutter run` console（`developer.log` 只進 VM service 的 Logging stream），必須開 DevTool
  ... (truncated, see source ticket)

### Related Files
- lib/screens::read  # from 0.1.0-W3-103（來源已完成，結論見 Solution）
- lib/app::read  # from 0.1.0-W3-103（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-001-screen-state-matrix.md::read  # from 0.1.0-W3-103（來源已完成，結論見 Solution）
- .claude/error-patterns/architecture::read  # from 0.1.0-W3-103（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 接線逐條分類（無跨層呼叫／兩層皆備／缺第二層／缺第三層）並附承擔者與實作位置  # from 0.1.0-W3-103（來源已完成，結論見 Solution）
- [ref] [x] 缺第三層者註明失敗是否被降級收斂掉（收斂到無表現的狀態即為靜默失敗）  # from 0.1.0-W3-103（來源已完成，結論見 Solution）
- [ref] [x] 缺口依修補形態分組建 IMP 票並以 add-spawn-request 登記，complete 前逐項 resolve 為終態  # from 0.1.0-W3-103（來源已完成，結論見 Solution）

