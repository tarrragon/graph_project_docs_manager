---
id: 0.1.0-W3-466.1
title: 評估 W3-466 四則草稿（TDD-04a、06a、08b、11b）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-466
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 閘門範例補依據
  final_decision: 新判準交 WRAP
  rationale: 用戶常駐裁示
who:
  current: general-purpose
  history: {}
what: 評估 W3-466 四則草稿（TDD-04a、06a、08b、11b）
when: tdd 失敗題重跑前
where:
  layer: 待定義
  files:
  - .claude/skills/tdd/SKILL.md
why: 新判準須經 WRAP；W3-465.1 經驗顯示撰寫者常漏查同義詞與他 skill 出處
how:
  task_type: Analysis
  strategy: wrap-decision 逐則：先以同義詞、他 skill、同 skill 產出清單查既有出處；確為新判準者給逐詞可推改文與判否反例；用戶偏好明標
acceptance:
- '[ ] 四則判定與建議改文；用戶偏好選項'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-16T03:18:30'
completed_at: null
created: '2026-09-16'
updated: '2026-09-16'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

評估 W3-466 四則草稿（TDD-04a、06a、08b、11b）

---

## Problem Analysis
### 問題根因

W3-466 對四題以 `git grep -F` 查條文字面（「狀態旗標」「app 自有」「一條斷言」「進入點條目的斷言」「單一訊號」），查無非範例出處，判為新判準寫成草稿。與 W3-465.1 同型：沒有查同義詞（「僅描述」「業務語言」「各有斷言形態」「實際輸出記錄成預期值」）、沒有查同 skill 其他 references 的定義句（`layered-test-strategy.md`、`bdd-behavior-testing.md`、`phase2-test-design.md`）、沒有查同段落自身已有的正向判準。重查後四則都有既有出處，缺的是判準位置或射程標示；另發現 11b 的讀歪風險被 W3-466 已落檔的 11a 補句放大。

### 影響範圍

- 已驗證：`.claude/skills/tdd/references/test-object-catalogue.md`〈二、來源類別〉〈六、派發語言〉、`test-object-catalogue-starting-points.md` 起點 C、`bdd-behavior-testing.md`〈BDD→單元拆分標準〉；出處檔 `layered-test-strategy.md`、`phase2-test-design.md`、`skill-design-guide/references/chained-examples.md`。
- 已驗證：commit `3afc5eec1` 在訊號表「Given 設置過長」列補入的後置條件沒有帶「多訊號同時成立」的射程。
- 未驗證：建議改文經探針重跑是否 3/3；本票不跑探針。

### 相關 Error Pattern

`DOC-GPD-007-example-read-as-exhaustive-contract`：04a（禁止項列舉被讀成封閉清單，旗標落在列舉外即放行）屬同型。

## 重現實驗結果
### 實驗方法

1. 出處重查：`git grep -F`（範圍 `.claude/skills`、`.claude/methodologies`、`.claude/rules`），關鍵字含同義詞與他檔定義句：「內部狀態」「實作細節」「業務語言」「可觀察結果」「可觀測的事實」「觸發動作」「一條斷言」「一個斷言」「合併」「各有斷言形態」「過度集中」「characterization」「觸發它的進入點」「多訊號同時成立時」；另讀 commit `3afc5eec1` 對 bdd-behavior-testing.md 的 diff。
2. 逐詞推導：把草稿與建議改文放回條文語境，對 `probes/q/W3-417-b4-tdd-1.md` 四題題幹重推判定，並與 W3-417〈b4 tdd-1 判定〉失敗歸因所列讀歪路徑比對。
3. 判否反例：每則構造一個與題幹不同構、依改文應判「否」的輸入。

### 實驗執行

出處命中（非範例，引文皆 `git grep -F` 命中 1 處以上）：
- 04a：`test-object-catalogue.md`〈六〉書寫邊界句本身「When 與 Then 僅描述規格宣告的觸發條件與可觀察結果」與句型佔位符「When {來源規格條目描述的觸發動作}」；`phase2-test-design.md`〈場景設計原則〉「用業務語言描述，不用技術術語」（`phase2/rules.md` 同句）；`bdd-behavior-testing.md`〈行為 vs 實作判別〉「使用者能否觀察到這個結果？」（射程為斷言）。
- 06a：`layered-test-strategy.md`〈終端回饋訊息的三類驗收〉表列三類斷言對象與主要形態各不相同（內容「單元測試或靜態讀檔；不比對散文字面」、可讀性「widget 測試，含窄幕尺寸」），段末「驗收時三類並行檢查」，檢查清單「各有斷言形態，且未以其中一類的綠燈抵扣另一類」。〈二〉已路由到該節，但路由句緊接「不得拆成三行」，層級混在一起。
- 08b：`layered-test-strategy.md`〈分層之外的補位形態〉characterization test 定義「先對現有程式跑一次、把實際輸出記錄成預期值」；C2 本身「每個可呼叫的公開進入點一條」。
- 11b：〈多訊號同時成立時〉段標籤本身即射程條件；訊號表「Given 設置過長」列原動作「Module 過大，考慮拆分 Module 而非加測試」。`3afc5eec1` 把多訊號段的後置條件「套用不移動邊界動作後……才考慮拆分 Module」直接接在該格，未帶射程。

逐詞推導：
- 04a 草稿：「判準一律以是否為使用者可觀測的事實為準」套到 When 時，答案鍵的正解「儲存請求回傳成功」與〈六〉範例一的「前置資料解析完成」都不是使用者可觀測的事實，會被判不合格；草稿把 Then 的判準誤套到 When。例示「僅供程式內部判斷用的布林旗標」與題幹 isSaved 同構。
- 06a 草稿：「斷言層級三類須各自對應不同測試方法」可推出正解，但沒有指向出處，理由句「合併會使某一類驗收失去對應的測試方法」為已刪範例原句，不是條文。
- 08b 草稿：理由「驗收面向屬進入點條目的斷言範圍」可擋 r2，但無出處；characterization 定義句可逐詞推出同一結論。
- 11b 草稿：範圍限定句可擋 r2，但只加在段落前時，讀者只讀表格那一格（11a 的讀歪路徑）仍看到無射程的後置條件；r3 的「若不過大則接受現設計」分支兩稿都沒有處理。

### 實驗發現

- 已驗證：四則都有非範例出處，屬重排或補射程，不是新判準。
- 已驗證：04a 草稿的判準方向與答案鍵衝突，不可落檔；11b 的主要讀歪源在 `3afc5eec1` 補入的表格格內句。
- 未驗證：建議改文經探針重跑是否 3/3。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending
