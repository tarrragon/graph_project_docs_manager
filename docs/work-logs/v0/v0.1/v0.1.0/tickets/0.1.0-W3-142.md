---
id: 0.1.0-W3-142
title: 補正 add-acceptance 自動補 checkbox 前綴但 help 未說明，導致雙前綴
type: ADJ
status: pending
version: 0.1.0
wave: 3
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-141
dispatch_reason: ''
decision_tree_path:
  entry_point: CLI 引導缺口，使用者依既有字面操作反而出錯
  final_decision: 補 help 說明；是否改為冪等處理由執行者評估
  rationale: help 補正成本極低且立即正確；冪等處理需判斷是否有人刻意想要字面前綴
who:
  current: thyme-python-developer
  history: {}
what: 補正 add-acceptance 自動補 checkbox 前綴但 help 未說明，導致雙前綴
when: 2026-09-08 並行 session 實際命中並自行 remove+add 修復
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/commands/track.py
why: '`ticket track add-acceptance` 會自動為傳入的文字補上 `[ ]` checkbox 前綴，但 `--help` 只寫「驗收條件文字」，**沒有說明這件事**。


  使用者若照票面既有的字面（票面存的是 `[ ] 條件內容`）複製並傳入，結果會變成 `[ ] [ ] 條件內容`。並行 session 於 2026-09-08 實際命中，自行 `remove` + `add` 修復。


  **這是引導缺口而非使用者錯誤**：票面顯示的字面帶前綴，複製票面字面是最自然的操作，而 help 沒有任何訊息說明前綴會被自動加上。**使用者做了看起來最合理的事，得到錯誤結果，且該結果不會報錯**——雙前綴是合法字串，CLI 照收。


  **與 `--acceptance` 的不一致同源**：`ticket create --acceptance` 不以逗號分隔（整串當一條），`--where` 則吃逗號分隔，而兩者的 help 描述看起來同構。三者合起來是同一個問題的三個實例——**help 描述參數「是什麼」，不描述參數「怎麼被處理」**。'
how:
  task_type: Implementation
  strategy: 第一步（必做，立即正確）：help 文字說明前綴會被自動補上，傳入時不需自帶。第二步（評估）：是否改為冪等——偵測到既有 checkbox 前綴時不重複補。冪等處理看似更好，但須先確認沒有人刻意想要字面的方括號內容（例如條件文字本身以 [ ] 開頭的正常情況），若無此需求則冪等是安全的。順帶檢視同一 CLI 內其他自動處理輸入的參數，help 是否同樣未說明——本票的價值在於它是一類問題而非單點。
acceptance:
- '[ ] add-acceptance 的 help 說明 checkbox 前綴會被自動補上，傳入時不需自帶'
- '[ ] 冪等處理（偵測既有前綴不重複補）已評估，結論與理由寫入 Solution'
- '[ ] 同一 CLI 內其他自動處理輸入的參數已檢視，help 未說明者一併補正或列出清單'
- '[ ] 分隔符不一致（create --where 用逗號、set-related-to 用空格）已給出結論：統一／雙向相容／維持現狀三選一，附不選另兩者的理由'
tdd_phase: phase2
tdd_stage:
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補正 add-acceptance 自動補 checkbox 前綴但 help 未說明，導致雙前綴

---

## Problem Analysis

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Context Bundle

**需求摘要**: 讓 `add-acceptance` 的 help 說出它會對輸入做什麼，而不只說輸入是什麼。

**現況（已驗證：`ticket track add-acceptance --help`）**

```
positional arguments:
  ticket_id          Ticket ID
  value              驗收條件文字
```

`value` 的描述是「驗收條件文字」——沒有任何訊息說明 `[ ]` 前綴會被自動補上。而票面存的字面是 `- '[ ] 條件內容'`，複製票面字面是最自然的操作。

**PM 未受影響的原因（供執行者理解影響面）**：本 session 今日全部使用 `set-acceptance --add`（可一次傳多條）而非 `add-acceptance`，且傳入時未自帶前綴，故無雙前綴產生。實查全部 ticket md，`[ ] [ ]` 零命中。**這代表問題的觸發需要「用 `add-acceptance` 且自帶前綴」兩個條件同時成立**，不是普遍發生——但一旦發生不會報錯。

**這是一類問題的第三個實例，本票的價值在此**

| 參數 | help 描述 | 實際處理 |
|------|----------|---------|
| `add-acceptance` 的 `value` | 「驗收條件文字」 | 自動補 `[ ]` 前綴 |
| `create --acceptance` | 「驗收條件」 | **不**以逗號分隔，整串當一條 |
| `create --where` | 「影響檔案（逗號分隔）」 | 以逗號分隔 |

前兩者的 help 描述看起來同構，實際處理相反——使用者從介面上得不到區分依據。**共同形態是 help 描述參數「是什麼」，不描述參數「怎麼被處理」。**

acceptance 第 3 條要求檢視同一 CLI 內其他自動處理輸入的參數，即為此——**只修一個等於承認前兩個實例不算數**。

**約束**

- 目標檔在 `.claude/` 之下，命中派發豁免，**不要加 `isolation: "worktree"`**。
- 這支 CLI 是多個 session 共用且執行者自己會呼叫，改完立刻跑測試（`PC-GPD-010`）。
- 第二步（冪等處理）是評估項不是必做項。**不做也是合法結論**，但須說明理由——冪等看似更安全，仍須先確認沒有人刻意需要字面的方括號開頭內容。

---


---

**PM 補充：第四個實例是分隔符不一致，不是 help 缺漏（2026-09-09，實查）**

並行 session 回報 `set-related-to` 與 `append-log --section` 為新實例。**實查後兩者的 help 都有寫**，本票不應把它們列為 help 缺漏：

- `ticket track set-related-to --help` 的 epilog 明寫：「value 是單一位置參數：多個 ID 須引號包成一個字串，字串內以空格分隔；不加引號會被 shell 拆成多個參數，argparse 報 unrecognized arguments。」並附兩則範例。
- `ticket track append-log --help` 的 usage 行為 `[-h] --section SECTION ...`——`--section` 未加方括號，argparse 慣例即表示必填。

**但真實的問題還在，只是位置不同：同一支 CLI 內兩個清單型參數用相反的分隔符。**

| 參數 | 分隔符 | 是否有 help 說明 |
|------|--------|----------------|
| `create --where` | **逗號** | 有（「逗號分隔」） |
| `set-related-to` 的 value | **空格**（且須引號包成單一字串） | 有（epilog） |

兩者都有文件，**問題在於使用者不會為每個參數重讀 help**——剛用完 `create --where` 的逗號，下一個命令換成空格，慣性會直接沿用。這與本票原本三個實例的機制**不同**：那三個是 help 沒說，這個是 help 說了但介面本身違反一致性預期。

**對本票範圍的意義**：兩類問題的處方不同。help 缺漏補文字即可；分隔符不一致補文字**不夠**——因為文件已經存在而人仍會踩。要嘛統一分隔符（破壞性變更，須評估既有呼叫端），要嘛讓兩者都接受兩種分隔符（相容性做法）。

執行者請於 Solution 明確區分這兩類，並就分隔符不一致給出結論：**統一／雙向相容／維持現狀並僅記錄**三選一，附不選另兩者的理由。若判定屬破壞性變更超出本票（ADJ／P3）範圍，登記 spawn request 交 PM，不自行擴大。

**一項方法教訓（並行 session 提供，值得寫進 Solution）**：回報缺陷前先實查該處的 help 全文——包含 argparse 的 epilog 段，它不在 `--help` 輸出的前幾行。本次兩則回報都是在踩到錯誤後憑印象歸類為「help 沒說」，而實際上錯誤訊息與 epilog 都寫了。**踩到與沒有文件是兩件事。**
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
