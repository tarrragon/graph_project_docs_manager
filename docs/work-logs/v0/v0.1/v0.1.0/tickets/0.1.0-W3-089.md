---
id: 0.1.0-W3-089
title: 補強 隔離索引 CAS 配方：基準必須釘選為變數，用 HEAD 符號會同時廢掉 parent 與 CAS 舊值
type: DOC
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-081
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 補強 隔離索引 CAS 配方：基準必須釘選為變數，用 HEAD 符號會同時廢掉 parent 與 CAS 舊值
when: 2026-09-08 PM 執行隔離索引提交時實測命中，並由並行代理人以正向 commit f7bd7fa0 修復
where:
  layer: Framework Docs
  files:
  - .claude/references/bash-tool-usage-details.md
  - .claude/rules/core/bash-tool-usage-rules.md
why: '`bash-tool-usage-details.md` 的隔離索引 CAS 配方寫得對——步驟 1 明訂 `OLD_HEAD=$(git rev-parse HEAD)` 釘選基準，步驟 3 `read-tree "$OLD_HEAD"`、步驟 6 `commit-tree -p "$OLD_HEAD"`、步驟 8 `update-ref HEAD "$COMMIT_SHA" "$OLD_HEAD"` 全部用該變數。但配方沒有說明**不釘選會怎樣**，而「把 `$OLD_HEAD` 換成 `HEAD`」是一個看起來完全等價的替換。


  實測命中（commit `4bd0e2ac`，PM 執行）：`read-tree HEAD` 與 `commit-tree -p HEAD` 分屬兩次 Bash 呼叫，其間並行 session 提交了 `eab601b2`。結果是 tree 取自舊 HEAD、parent 指向新 HEAD——**該 commit 靜默回滾了 `eab601b2` 對 `0.1.0-W3-088.md` 的變更**（`blockedBy` 被清回 `[]`）。


  三層防護同時失效，而每一層單看都「通過」：


  | 防護 | 為何沒擋住 |

  |------|-----------|

  | 要件 2（`GIT_INDEX_FILE`） | 確實生效——共用 index 全程未被觸碰。它保護的是寫入端，不保護基準的新鮮度 |

  | 要件 3（tree 層級自檢） | 用 `$OLD_HEAD` 比對時必然通過：tree 相對它就是只有預期的檔案。要察覺得比對**新** HEAD |

  | 步驟 8 的 CAS | 舊值寫 `HEAD` 時自我解析為當前 ref，比較必然相等——CAS 的全部保護力來自釘選舊值，不釘選等於沒有 |


  現有的「完整性三要件」章節涵蓋不到這個模式：三要件談的是清單來源、寫入隔離、範圍自檢，全都是**空間維度**（提交了哪些檔案）；本案是**時間維度**（基準有多新），與規則七既有的「版本邊界（過期 index 快照）」同源，但發生在隔離索引路徑上——而該路徑正是被推薦用來取代共用 index 的做法，讀者不會預期它有同型風險。'
how:
  task_type: Documentation
  strategy: '在「隔離索引提交的完整性三要件」之後新增一項要件（不改既有三項的編號與內容），主張：基準必須釘選為變數並全程使用，禁止在配方任一步驟寫 `HEAD` 符號。含三點說明：


    1. **失效機制**：`read-tree` 與 `commit-tree` 之間若 HEAD 被並行推進，tree 與 parent 分屬不同基準，差集即成靜默回滾。跨 Bash 呼叫執行時窗口尤其大。

    2. **為何三要件擋不住**：三要件是空間維度（提交哪些檔案），本項是時間維度（基準多新）。要件 3 以 `$OLD_HEAD` 自檢時必然通過。

    3. **CAS 舊值是唯一的偵測點**：`update-ref` 的舊值寫 `HEAD` 會自我解析而恆相等；寫 `$OLD_HEAD` 則在 HEAD 被推進時**失敗而非覆蓋**，把靜默回滾轉成明顯錯誤。這是配方裡唯一能察覺本情形的環節。


    另補一句可執行建議：配方步驟 1-8 盡量在單一 Bash 呼叫內完成，縮短窗口；跨呼叫時基準變數需以檔案或明確重述傳遞，不可依賴 shell 變數存活（Bash 工具的 shell 狀態不跨呼叫保留）。


    `bash-tool-usage-rules.md` 規則七後既有的「版本邊界（過期 index 快照）」邊界段補一句交叉引用，指出隔離索引路徑有同型風險與其防線（CAS 舊值釘選），避免讀者以為換用隔離索引即可免疫。'
acceptance:
- '[ ] details.md 的三要件章節後新增一項要件，含失效機制、為何三要件擋不住、CAS 舊值為唯一偵測點三點'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補強 隔離索引 CAS 配方：基準必須釘選為變數，用 HEAD 符號會同時廢掉 parent 與 CAS 舊值

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
