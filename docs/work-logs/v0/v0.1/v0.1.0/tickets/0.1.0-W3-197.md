---
id: 0.1.0-W3-197
title: 驗證 framework issue 54 的 dispatch 讀取路徑與守衛登記表兩項修復是否抵達
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-193 與 0.1.0-W3-188 依升級流程路徑 A 收束至 tarrragon/claude#54，需驗證票承接修復抵達後的確認
  final_decision: 建單一 IMP 驗證票涵蓋兩則觀測，trigger 為 sync-pull 帶回含 fix-version 的版本
  rationale: 兩則由同一次 sync-pull 帶回且驗證動作同屬讀 hook 行為，分開會製造兩張 trigger 相同的票；但驗收項分列，任一則未達成即不可 complete
who:
  current: thyme-python-developer
  history: {}
what: 驗證 framework issue 54 的 dispatch 讀取路徑與守衛登記表兩項修復是否抵達
when: sync-pull 帶回含 fix-version 的框架版本後
where:
  layer: Infrastructure
  files:
  - .claude/lib/dispatch_tracker.py
  - .claude/hooks/main-thread-edit-restriction-hook.py
  - .claude/hooks/canonical-schema-consistency-check-hook.py
  - .claude/references/agent-self-check-template.md
why: 本專案已於 tarrragon/claude#54 附加兩則觀測：一是 dispatch_tracker 的 get_active_dispatches 不套用既有的過期判定，使 is_file_under_dispatch 對已派發過的檔案恆真報警（觀測 comment 5597251317）；二是 canonical 集合守衛的下游登記表不完整，未登記者漂移時無任何訊號（觀測 comment 5597262183）。原分析票 0.1.0-W3-193 與 0.1.0-W3-188 依 framework-issue 升級流程路徑 A 已 close，本票是該路徑要求的驗證票——issue ref 與 sync-pull 都不是合法 trigger，合法 trigger 只有 ticket，故於 close 當下建立，不等修復發生。兩則併為一票的理由是它們會由同一次 sync-pull 帶回、且驗證動作同屬讀 hook 行為，分開會製造兩張 trigger 相同的票。
how:
  task_type: Implementation
  strategy: '第一則的驗證：以一筆 turn_ended_at 超過 TURN_ENDED_MAX_AGE_HOURS 的記錄確認 is_file_under_dispatch 不再命中；同時確認 workspace-wipe-guard 的並行派發計數行為完全不變、其 docstring 的保守存活語意宣告仍成立、session-registry-start-hook 的 idle 候選集合大小不變含最舊那批。這三項是最容易被順手破壞的，須逐項實測而非讀原始碼推斷。


    第二則的驗證：確認 agent-self-check-template 的合法 H2 清單已與 ticket CLI body 模板全量一致（不只補當初命中的那一項），且該檔已登記進守衛的下游表；再以機械手段獨立盤點該 canonical 集合的下游總數，與修復後的登記表比對——不以修復後的登記表為準，那正是原本被質疑的對象。


    任一則未涵蓋或行為與觀測不符時，以 observe 附回該 issue 記錄實測結果，不在本專案就地補實作。'
acceptance:
- '[ ] 以一筆逾期記錄實測 is_file_under_dispatch 不再命中，且 wipe-guard 計數與 idle 候選集合大小皆不變'
- '[ ] agent-self-check-template 的合法 H2 清單已與 ticket CLI body 模板全量一致，且該檔已登記進守衛的下游表'
- '[ ] 以機械手段獨立盤點該 canonical 集合的下游總數並與修復後的登記表比對，未以該登記表為盤點依據'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

驗證 framework issue 54 的 dispatch 讀取路徑與守衛登記表兩項修復是否抵達

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 2026-09-10 抵達驗證：兩項皆未抵達

**上游狀態**：`tarrragon/claude#54` 仍為 OPEN（updatedAt 2026-09-10T05:44:46Z），無 fix-version。

**觀測一（dispatch 讀取路徑，comment 5597251317）**：未修復。
`.claude/lib/dispatch_tracker.py:622` 的 `get_active_dispatches` 仍為 `return state["dispatches"]`，直接回傳全量陣列，未套用同檔 649 行起的 `_is_dispatch_expired`；`is_file_under_dispatch`（632 行）以該函式為唯一資料來源，故對已派發過的檔案仍恆真報警。過期判定目前只在 `cleanup_expired` 路徑被呼叫（`active-dispatch-tracker-hook.py:185`），讀取路徑不經過它。

**觀測二（守衛登記表，comment 5597262183）**：未修復，且以機械盤點取得一則具體缺項。

盤點手段（不以登記表為依據）：`grep -rln "EMOJI_RANGES" .claude/ --include='*.py'`，得 10 個檔案；扣除守衛自身、測試檔、canonical 定義檔（`constants.py`）後，實質下游為 4 個。

| 下游檔案 | 持有形式 | 是否在 `EMOJI_RANGES_TARGETS` |
|---|---|---|
| `hooks/language-guard-hook.py` | 副本 | 是 |
| `hooks/askuserquestion-charset-guard-hook.py` | 副本 | 是 |
| `skills/ticket/ticket_system/lib/parser.py:350` | import（`_enum_constants.EMOJI_RANGES`），不會漂移 | 否，且不需登記 |
| `scripts/charset-pollution-detector.py:115` | **自有字面副本**（`EMOJI_RANGES: list[tuple[int, int]] = [...]`，全檔無 import canonical） | **否 —— 缺項** |

`charset-pollution-detector.py` 持有第三份字面副本卻未登記，canonical 增減 range 時該檔漂移無任何訊號，正是觀測二描述的形態。

acceptance 第 2 項另指 `agent-self-check-template`：`.claude/references/agent-self-check-template.md` 存在，但 `canonical-schema-consistency-check-hook.py` 全檔無該檔名，未登記進守衛下游表。

**處置**：票保留於 pending，不 close。三條 acceptance 皆以「修復後」為前提，現況無修復可驗。上述盤點結果可在修復抵達後直接作為 acceptance 第 3 項的獨立盤點基準。

### 2026-09-10 觀測已上報上游

盤點結果已附加至 `tarrragon/claude#54`（observe comment `5616822369`），延伸既有觀測 `5597262183`。

**查重與關係判定**：`dedup --keywords "EMOJI_RANGES 下游登記表" "canonical 守衛 登記表 漂移"` 共 10 則命中，僅 `#54` 命中位置為 comments，其餘為標題層粗篩誤命中。`#53`（hook 註冊清單三份不同步）主題相鄰但對象不同——該則治 hook 註冊清單，本則治 canonical 常數的下游副本登記，不合併。關係判定為**延伸既有觀測**，非新問題、非切分，故走 observe 不走 init。

**上報內容**：具名缺項 `scripts/charset-pollution-detector.py:115`（第三份字面副本未登記）、`references/agent-self-check-template.md`（`CANONICAL_BODY_SECTIONS` 下游未登記），及「判準應為是否存在獨立賦值，而非是否引用該識別符」一項。

本票狀態不變，仍保留於 pending。

### 2026-09-11 CANONICAL_BODY_SECTIONS／EMOJI_RANGES 下游登記表機械盤點（延伸 2026-09-10 記錄）

**盤點手段**（不以修復後的登記表為依據）：

1. `.py` 引用者：`grep -rln "CANONICAL_BODY_SECTIONS" .claude/ --include="*.py"`、`grep -rln "_SCHEMA_SECTION_NAMES" .claude/ --include="*.py"`、`grep -rln "SCHEMA_H2_SECTIONS" .claude/ --include="*.py"`，逐檔開啟人工判定是否為獨立賦值（副本，會漂移）或 import（不會漂移）。
2. `.md` 文件不引用識別符本身，改用內容比對：以 Python 掃描 `.claude/**/*.md`，鎖定「Task Summary」出現位置後 400 字元窗內，統計 10 個 canonical 章節名稱同時出現的個數，`>= 6` 者判定為疑似完整清單副本，逐一人工確認。
3. `EMOJI_RANGES` 同法：`grep -rln "EMOJI_RANGES" .claude/ --include="*.py"`，逐檔判定賦值形式。

判準：是否存在該集合的**獨立賦值**（副本才需登記），非是否引用識別符——import 不計入缺口。

**CANONICAL_BODY_SECTIONS 完整盤點結果**：

| 下游持有者 | 持有形式 | 是否登記進 `canonical-schema-consistency-check-hook.py` |
|---|---|---|
| `hooks/acceptance_checkers/custom_h2_checker.py`（`_SCHEMA_SECTION_NAMES`） | 獨立賦值（副本） | 是（`VALIDATOR_TARGETS`） |
| `hooks/acceptance_checkers/execution_log_checker.py`（`_SCHEMA_SECTION_NAMES`） | 獨立賦值（副本） | 是（`VALIDATOR_TARGETS`） |
| `skills/ticket/ticket_system/lib/ticket_validator.py`（`_SCHEMA_SECTION_NAMES`） | 獨立賦值（副本） | 是（`VALIDATOR_TARGETS`） |
| `pm-rules/ticket-body-schema.md`（Schema 對照表第一欄） | 副本（文件表格） | 是（`TICKET_BODY_SCHEMA_MD`） |
| `references/agent-definition-standard-details.md`（Schema 章節清單行） | 副本（文件行內清單） | 是（`AGENT_DEFINITION_STANDARD_MD`） |
| `references/agent-self-check-template.md`（「Schema 合法 H2 清單」行） | 副本（文件行內清單，**缺 `Spawn Requests`，已漂移**） | 否 — 缺項（2026-09-10 已記錄，本次覆核確認） |
| `skills/ticket/references/track-command.md:341-343`（`append-log --section` 說明註解區塊） | 副本（文件程式碼註解區塊，含全部 10 項，**目前未漂移**） | 否 — 缺項（本次新發現） |
| `skills/ticket/ticket_system/lib/ticket_builder.py`（`SCHEMA_H2_SECTIONS`） | import 別名（`= CANONICAL_BODY_SECTIONS`，語言層級恆等） | 不需登記（hook docstring 已明式排除，恆為一致） |
| `skills/ticket/ticket_system/lib/command_tracking_messages.py`（`VALID_SECTIONS`） | import（`from ticket_system.constants import CANONICAL_BODY_SECTIONS`） | 不需登記（非副本） |
| `skills/ticket/ticket_system/commands/track_acceptance.py`／`track_artifacts.py`／`track_dispatch.py` | 僅註解提及識別符，無獨立賦值 | 不適用 |
| `hooks/tests/test_canonical_schema_consistency_check_hook.py` | 測試 fixture 字面副本（測本 hook 擷取邏輯本身的測試替身） | 不需登記（非生產下游） |
| `.claude/tdd/contracts.yaml` | 依 TDD phase 各自列出部分章節存在斷言，非完整清單副本 | 不適用（非本集合的副本） |

**新發現缺項**：`skills/ticket/references/track-command.md` 的 `append-log --section` 說明區塊持有完整 10 項字面副本，未登記進守衛下游表。與既有 `agent-self-check-template.md` 缺項同型——差別在於本檔目前未漂移（仍含 `Spawn Requests`），現況不觸發任何警告；但因未登記，canonical 集合未來再變動時，此檔同樣會無聲落後而無任何訊號。

**EMOJI_RANGES 完整盤點結果**（覆核既有記錄，無新增）：

| 下游持有者 | 持有形式 | 是否登記進 `EMOJI_RANGES_TARGETS` |
|---|---|---|
| `hooks/language-guard-hook.py` | 獨立賦值（副本） | 是 |
| `hooks/askuserquestion-charset-guard-hook.py` | 獨立賦值（副本） | 是 |
| `scripts/charset-pollution-detector.py:115` | 獨立賦值（副本，`list[tuple[int, int]]` 字面清單） | 否 — 缺項（2026-09-10 已記錄，本次覆核確認） |
| `skills/ticket/ticket_system/lib/parser.py:350` | import（`_enum_constants.EMOJI_RANGES`） | 不需登記（非副本） |
| `hooks/tests/test_language_guard.py`／`test_askuserquestion_charset_guard.py` | import（`hook_module.EMOJI_RANGES` 別名） | 不需登記（非副本） |
| `hooks/tests/test_canonical_schema_consistency_check_hook.py` | 測試 fixture 字面副本 | 不需登記（測試替身，非生產下游） |
| `skills/ticket/ticket_system/tests/test_charset_gate.py` | 僅註解提及，無賦值 | 不適用 |

**是否推翻既有判定**：未推翻。2026-09-10 記錄的兩項缺口（`charset-pollution-detector.py`、`agent-self-check-template.md`）經本次機械盤點覆核，判定一致。本次延伸新增一項先前未發現的未登記缺口：`skills/ticket/references/track-command.md`（CANONICAL_BODY_SECTIONS 副本，目前未漂移但未受保護）。

**處置**：依派發指示不修改 `agent-self-check-template.md`／`canonical-schema-consistency-check-hook.py`／`track-command.md`（均屬會被 `sync-pull` 覆蓋的框架資產，本地修改無 canonical 記錄）。三項缺口（`agent-self-check-template.md`、`track-command.md`、`charset-pollution-detector.py`）已完整列出，供上游修復抵達後作為 acceptance 第 2、3 項的獨立盤點基準；未上報新的上游 issue observe（`track-command.md` 屬同一既有觀測 `5597262183` 的延伸範圍，非新問題）。票狀態不變，仍保留於 pending，acceptance 三項皆未勾選。
### 2026-09-11 第二則觀測已上報上游

`track-command.md` 具名缺項、以及「已漂移／未漂移但未登記」兩種形態的區分，已附加至 `tarrragon/claude#54`（observe comment `5629090213`），延伸既有的 `5597262183` 與 `5616822369`。

**本則新增的判斷**（先前兩則未涵蓋）：若修復方式為「掃描出當前不一致的檔案並補進登記表」，第二形態（內容正確但未登記）全數抓不到——它們今天完全正確、掃不出差異，卻在 canonical 下次增減成員時無聲落後。入表判準必須是「是否持有獨立副本」，不能是「是否已經不一致」。

**三項缺口中僅第 1 項為實際不一致**：`agent-self-check-template.md` 的「Schema 合法 H2 清單」只列 9 項、缺 `Spawn Requests`。該檔為會 sync 的框架資產，本地修正會被下次 `sync-pull` 以上游版本覆蓋且無 canonical 記錄，故不在本專案修，處置隨上游修復一併抵達。

本票狀態不變，仍保留於 pending。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
