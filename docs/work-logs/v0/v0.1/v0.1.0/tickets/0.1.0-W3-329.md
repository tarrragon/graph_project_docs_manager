---
id: 0.1.0-W3-329
title: 實作 待裁列的 ack-based delta 推送，含成本壓不下來時的停手條件
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-326
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-326 裁定形態為 ack-based delta、主通道 SessionStart，並量得 todo --json 需 13.63 秒
  final_decision: 實作票帶停手條件：成本壓不下來則停手回報，不得出貨慢 hook
  rationale: 326 自述該秒數不可原樣帶入；SessionStart 已註冊 44 支 hook，一支 13 秒的 hook 會使整個啟動序列的成本結構改變，屆時正確做法是回頭重裁通道而非硬上
who:
  current: basil-hook-architect
  history: {}
what: 實作 待裁列的 ack-based delta 推送，含成本壓不下來時的停手條件
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/section_comment.py
  - .claude/hooks/session-start-issue-check-hook.py
  - .claude/settings.json
why: '**`0.1.0-W3-326` 已裁定三項，本票不重裁**：形態為 delta 而非清單；ack-based 而非 seen-based（後者 at-most-once，印過未處理即永久沉默）；主通道 SessionStart、輔以寫入端回印、條文層不承擔。


  **通道的依據是讀者身分不對稱**：寫入者是 curator，裁決者是 PM，而 PM 的固定入口只有 session 啟動。


  **delta 的 predicate 已驗**：逐列身分 `(issue, owner, 正規化後的來源票)` 在 109 列上唯一、零碰撞。**時間軸必須由本地快照製造**——`todo` 逐列無任何日期欄位。


  **本票的主要風險是成本，而該風險可能否決 326 選的通道**：`0.1.0-W3-326` 實測 `todo --json` 預設範圍 **real 13.63 秒**，SessionStart **已註冊 44 支 hook**。326 自述該值不可原樣帶入。**一支 13 秒的 hook 會改變整個啟動序列的成本結構**——屆時正確做法是回頭重裁通道，不是出貨一支慢 hook。


  **四項未決（`0.1.0-W3-326` 明列，本票承接）**：ack 訊號的取得方式與載體、成本控制、推送範圍、狀態遺失後的退化行為與誤報定義。**四項皆須在票面留下量測依據，不得憑設計直覺定案。**'
how:
  task_type: Implementation
  strategy: '**先解成本，再談其餘三項**——成本不成立則通道不成立，其餘三項的設計全部作廢。


    1. **成本控制優先**：不得在 SessionStart 路徑上呼叫 `todo --json`。可評估的方向至少三個（本地快照僅在寫入端更新、登記檔快速路徑、僅掃本 consumer 擁有的 issue），各自量測實際秒數寫入票面。**停手條件：若無任何方向能使 SessionStart 的新增成本低於一個先寫明的預算，停手回報，不得出貨**——此時應回頭重裁通道（326 的輔通道或 PM 的固定入口另尋），那不是本票的權限。

    2. **ack 訊號的載體**：需在跨 session 間持久且不得寫入 canonical issue（那是 curator 的權限面）。本地狀態檔屬既有形態（`.claude/hook-state/` 等），但**狀態遺失後的退化行為必須明訂**——遺失後全列重印（吵）或全列沉默（漏）是兩個相反的失效方向，選哪個要說明。

    3. **誤報定義**：什麼情況下印了但其實不該印。**無誤報定義的推送無法被評估**，日後只會被關掉。

    4. **可沿用 `session-start-issue-check-hook.py` 的基礎設施**（登記檔快速路徑、`SOURCES_TO_RUN`、subagent 略過、fail-open、per-issue 逾時），**但不得沿用其訊號模型**——`0.1.0-W3-326` 判定該模型的資料端無時間軸且失效方向相反。


    **測試要求**：ack 後不再印、新增列後會印、狀態檔遺失時走已明訂的退化路徑、subagent 環境下略過。**正向對照輸入為硬要求**：須有一個「應該印而確實印了」與一個「不該印而確實沒印」的成對斷言。


    hook 失敗須可見（`quality-baseline` 規則 4：stderr + 檔案日誌雙通道）。'
acceptance:
- '[x] SessionStart 的新增成本已量測並低於票面先寫明的預算；若無方向達標，已停手回報而未出貨'
- '[x] ack 訊號載體已定，且未寫入 canonical issue'
- '[x] 狀態遺失後的退化行為已明訂，兩個相反失效方向的取捨有說明'
- '[x] 誤報定義已寫明'
- '[x] 測試含「應印而印」與「不該印而沒印」成對斷言，並涵蓋狀態遺失與 subagent 略過'
- '[x] hook 異常走 stderr + 檔案日誌雙通道'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-12T20:44:10'
completed_at: null
created: '2026-09-12'
updated: '2026-09-12'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實作 待裁列的 ack-based delta 推送，含成本壓不下來時的停手條件

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: basil-hook-architect
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-326 | chars: 877 -->

### Task Reference
- 0.1.0-W3-326 what: 裁決 todo 待裁列的推送形態，使寫入待裁列的內容會被讀到（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-326 why: **`0.1.0-W3-318` 判定 C1 群（條文層須裁決的 15 項）應寫入待裁列，但同時指出其可執行性實質依賴 `todo` 的推送——而 `todo` 目前零 hook。**

**後果**：待裁列寫了也沒有人會讀到。15 項待裁事項會從「只存在於 318 票面」移到「只存在於 issue 的待裁列」，**兩者都不會在任何 session 啟動時出現在任何人眼前**。這是同一個載體問題換一個位置，不是解決。

**已有前例可評估但未經判定**：`check` 有 `session-start-issue-check-hook.py`（SessionStart 觸發，會輸出 issue 警訊）；`todo` 沒有任何 hook。**前例存在不等於前例適用**——`check` 推送的是「區段落後於觀測流」這種有時間軸的訊號，而待裁列是一份靜態清單，兩者的推送頻率、觸發時機、以及「推送什麼才不變成噪音」都可能不同。

**已實測的一個反面教訓**（`0.1.0-W3-318`）：`check` 的 `--stale-days` **早就是可調參數**（`DEFAULT_STALE_DAYS = 7`），CLI 參數化這條路已走完而缺口未動——**缺的是「以什麼為訊號」的裁決，不是參數**。todo 推送若只做「把清單印出來」，很可能落入同一形態。

**不做則後果**：C1 群寫入後無法驗證抵達；或反覆在 session 啟動時輸出一份沒人處理的清單，使該通道的訊號價值下降到零（`check` 主警訊七天內不觸發的實測即為此類）。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 「什麼情況下不該印」已寫明，答不出者該形態已判為不可採  # from 0.1.0-W3-326（來源已完成，結論見 Solution）
- [ref] [x] session-start-issue-check-hook 作為前例的適用性已判定，非沿用  # from 0.1.0-W3-326（來源已完成，結論見 Solution）
- [ref] [x] 推送承擔者已裁定，三個候選的漏讀失效方向各自說明  # from 0.1.0-W3-326（來源已完成，結論見 Solution）
- [ref] [x] 318 標為 M2 的其餘四項是否共用同一通道已判定  # from 0.1.0-W3-326（來源已完成，結論見 Solution）
- [ref] [x] Solution 未寫入實作細節，範圍界定交後續票  # from 0.1.0-W3-326（來源已完成，結論見 Solution）
