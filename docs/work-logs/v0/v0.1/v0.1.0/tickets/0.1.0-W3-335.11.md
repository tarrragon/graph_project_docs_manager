---
id: 0.1.0-W3-335.11
title: 修補 tdd 的 spec/UC 轉測試缺引導（G10–G13）
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 修補 tdd 的 spec/UC 轉測試缺引導（G10–G13）
when: 前驅票完成後
where:
  layer: 待定義
  files:
  - .claude/skills/tdd/references/doc-handoff.md
  - .claude/skills/tdd/references/test-object-catalogue.md
  - .claude/skills/tdd/CHANGELOG.md
  - .claude/skills/tdd/SKILL.md
why: W3-332 審查與走查確認 agent 依此文件推導元件與測試時被迫猜測或讀出兩種做法；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
how:
  task_type: Documentation
  strategy: 依 chained-examples 規格與 Context Bundle 執行；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
acceptance:
- '[x] 替代與例外場景歸屬、無 UC 步驟的 FR、模組群組切法、多來源取最具體四項補文字約束'
- '[x] 引用 W3-332.3 草稿者標明採用或修改理由'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T11:56:06'
completed_at: '2026-09-14T12:07:04'
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修補 tdd 的 spec/UC 轉測試缺引導（G10–G13）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-14 11:55] readiness=0 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
### 四項缺引導修補內容與 W3-332.3 草稿採用/修改理由

| 缺口編號 | 修補位置 | 對 W3-332.3 草稿的處置 | 理由 |
|---------|---------|---------------------|------|
| G10 替代／例外場景歸屬 | `doc-handoff.md`〈轉換注意事項〉新增條款 | 修改（語意不變，改寫用詞） | 草稿原文「替代／例外場景歸屬『分岔點所在的主場景步驟』產生的種子」對 zhtw 定語堆疊檢查（ZY5）判定為翻譯腔警告（30 字無逗點、3 個「的」）；改寫為兩句式（「替代／例外場景是主場景的分岔路徑，歸屬分岔點所在的步驟；該步驟轉出的 GWT 種子，即為邊界條件的掛載點」），語意與判斷規則（含跨步驟分岔另立獨立種子、Given 寫到分岔前累積狀態）逐字保留 |
| G11 無 UC 步驟的 FR | `doc-handoff.md`〈轉換方法〉後新增〈無 UC 步驟的 FR → GWT 種子〉節 | 修改（映射語意保留，改為表格呈現＋修正欄位對應書寫） | 草稿原句「以 FR 的觸發條件作 When、前提作 Given、宣告結果作 Then」的映射方向（觸發條件→When、前提→Given）予以保留，正文採與既有〈轉換方法〉表格一致的三欄格式（FR 元素／→GWT 元素／規則）取代散文句，避免散文版本在 zhtw 檢查中出現「作作」重複觸發詞與翻譯腔警告；同步將〈TDD 輸入種子包格式〉種子表「來源 UC 步驟」欄泛化為「來源」以容納 `{SPEC-ID} FR-{編號}` 格式，此為草稿未觸及但由本節新增規則直接導致的既有表格不一致，一併修正避免自我矛盾 |
| G12 模組群組切法 | `doc-handoff.md`〈新專案起手模式〉新增〈模組群組切法〉節 | 採用（逐字實質不變） | 草稿「模組群組以 spec 的 domain 為單位；單一 domain 的紅燈票超出任務拆分閾值時，再依架構層切分」語意完整且無 zhtw 命中，僅將「任務拆分閾值」補上具體出處引用（`.claude/rules/core/cognitive-load.md` 任務拆分閾值表），避免讀者不知道閾值定義在何處 |
| G13 多來源取最具體 | `test-object-catalogue.md`「四、齊全判準」C1-C4 表後新增段落 | 採用（逐字實質不變） | 草稿「同一行為同時載於多個來源時，取最具體的條目作來源（畫面狀態矩陣列優先於 UC 步驟優先於 FR），其餘來源寫入該行附註，不另立行」語意完整且無 zhtw 命中；因契約欄位表無獨立「附註」欄，將「附註」具體化為「以括號附註於『來源規格條目』欄位值之後」並附格式範例，使規則可直接操作 |

### 版本同步

`.claude/skills/tdd/references/doc-handoff.md` 1.1.5→1.1.6、`.claude/skills/tdd/references/test-object-catalogue.md` 1.2.0→1.2.1、`.claude/skills/tdd/SKILL.md` metadata.version 2.3.0→2.4.0、`.claude/skills/tdd/CHANGELOG.md` 新增 2.4.0 條目，四處版號同步完成。

### 自檢結果

- 四則新增文字皆以 `mcp__zhtw-mcp__zhtw`（relaxed=true, content_type=markdown）逐段驗證，最終版本 warnings=0 / errors=0
- 未使用票號、SPEC 編號等專案層級識別符（`.claude/references/reference-stability-rules.md` 規則 8）
- 未寫死行號引用，路由改用標題錨點與檔案路徑（規則 11）

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## Context Bundle

### 修補票共同 Context（PM，2026-09-14）

**缺口清單在哪**：本票涵蓋的缺口編號見 `ticket track full 0.1.0-W3-332.13` Solution；每個缺口的位置錨點、兩位讀者的分歧模擬、子票草稿見其來源子票（W3-332.2–.10）Solution。**W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 優先於 .13 草稿**——草稿漏收、誤歸或數字不符處以 P 項為準。

**規格**：`.claude/skills/skill-design-guide/references/chained-examples.md`（三段鏈式、驗證段兩形態、缺範例與缺引導分界）。

**已定案的用戶裁示**：
- 語言專屬 skill 的違規／修正程式碼對照不受「禁程式語法」約束（例外條款由 W3-335.15 寫入規格）
- 元件庫規格範本佔位文字改為技術中立，框架名移到 L2 註記
- version-bootstrap 的 v02-walkthrough 依新規格重寫整篇

**矛盾類票的簽核流程**：先在 Solution 寫「矛盾位置｜兩方原文｜建議以哪方為準｜理由｜改後文字草稿」，**不改檔**，以 `ticket track set-exit-status` 設 needs_context 並停手回報；PM 取得用戶簽核寫回本票後再續做改文。

**通則**：
- 框架檔不得寫本專案識別符（票號、SPEC 編號、畫面名），範例一律去識別化
- 集合大小改引用權威名稱而非寫死數字；錨點用標題不用行號
- 改動 skill 須同步其 CHANGELOG 與 SKILL.md 版號
- 只改本票 where.files；發現範圍外缺口登記 spawn request，不就地修

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-14T12:07:04
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
**Completion Time**: 2026-09-14T12:05
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

**變更摘要**：
- `.claude/skills/tdd/references/doc-handoff.md`（1.1.5→1.1.6）：新增〈無 UC 步驟的 FR → GWT 種子〉節（G11）、〈轉換注意事項〉新增「替代／例外場景歸屬」條款（G10）、〈新專案起手模式〉新增〈模組群組切法〉節（G12）、同步泛化〈TDD 輸入種子包格式〉GWT 種子表「來源」欄以容納 FR 來源
- `.claude/skills/tdd/references/test-object-catalogue.md`（1.2.0→1.2.1）：「四、齊全判準」C1-C4 表後新增「多來源取最具體（C3 的判斷依據）」段（G13）
- `.claude/skills/tdd/CHANGELOG.md` 新增 2.4.0 條目；`.claude/skills/tdd/SKILL.md` metadata.version 同步 2.3.0→2.4.0
- 四項對 W3-332.3 草稿的採用/修改理由詳見 Solution 章節對照表
