---
id: 0.1.0-W1-044.1
title: 建立 SPEC-004 骨架與元件目錄：畫布拆解進規格推導至容器候選
type: DOC
status: completed
version: 0.1.0
wave: 1
priority: P0
parent_id: 0.1.0-W1-044
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: dispatch-readiness：acceptance 6 > 4 強制拆分
  final_decision: 依 skill 步驟切兩段：目錄與骨架 / 逐元件契約
  rationale: 元件清單定案是填九欄位的前提，兩段的產出可各自驗收
who:
  current: lavender-interface-designer
  history: {}
what: 建立 SPEC-004 骨架與元件目錄：畫布拆解進規格推導至容器候選
when: W1-044 拆分後立即；本票是 W1-044.2 的前置
where:
  layer: Presentation
  files:
  - docs/spec/ui/SPEC-004-component-library.md
  - docs/spec/ui/SPEC-002-design-tokens-and-component-library.md
why: W1-044 六條 acceptance 逾派發閾值，依 component-contract-design skill 的步驟切為兩段：本票做模式 B 畫布拆解四步與模式 A 的存在必要性、容器盤點、同型歸併（步驟 3-5），產出 SPEC-004 前三章與定案的元件清單（含容器），讓後續逐元件填契約有固定的清單可填。
how:
  task_type: Documentation
  strategy: 依 component-contract-design skill：(1) 前置確認 token 層（lib/tokens/）與形態因素矩陣（依 CLAUDE.md §6 與 PROP-004 填 SPEC-004 第 1 章作提案，標待核定）；(2) 模式 B：逐 artboard 視覺單元盤點 → 語意問句 → 同型歸併 → 排列關係盤點；(3) 模式 A 步驟 2-5：與 SPEC-001 30 列狀態表的顯示欄、可用操作欄、畫面清單合併候選，存在必要性檢視（合併結論標提案），容器盤點（含框架層，一組 = 一個佈局結構的全部子件），同型歸併（先子件類型 + 方向）；(4) 用 doc skill 的 component-library-spec 範本建立 SPEC-004，填第 1-3 章，第 3 章總表含種類欄（元件 / 容器）；(5) SPEC-002 §元件庫的範圍 改為指向 SPEC-004。九欄位與排列不變式留給 W1-044.2。
acceptance:
- '[x] SPEC-004 依範本建立，第 1 章形態因素矩陣（標待核定）與第 2 章狀態綁定模式（Riverpod）已填'
- '[x] 第 3 章元件清單總表含全部元件與容器候選，每列有種類、用途、出現畫面、變體；SPEC-002 八類與 SPEC-001 30 列顯示欄的單元皆有歸屬'
- '[x] 存在必要性檢視的合併與不合併結論逐項附理由，標「提案」待 PM 核定'
- '[x] SPEC-002 §元件庫的範圍 改為指向 SPEC-004'
tdd_phase: phase1
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-02T17:14:04'
completed_at: '2026-09-02T17:37:31'
created: '2026-09-02'
updated: '2026-09-02'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

建立 SPEC-004 骨架與元件目錄：畫布拆解進規格推導至容器候選

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### WRAP 三問（認領 checkpoint）

- **W（其他做法）**：(a) 依 skill 模式 B → A 逐 artboard 盤點再歸併（採用）；(b) 直接把 SPEC-002 八類擴寫成總表、不做畫布拆解。否決 (b)：八類是依出現次數推定的清單，「節點卡」一名命不出共同目的，不經視覺單元盤點與語意問句無法發現它其實是四種內容角色。
- **A（機會成本）**：本票產出的候選數（元件 25、容器 17）直接決定 W1-044.2 九欄位契約的工作量；歸併不足會讓 044.2 填契約時膨脹，歸併過頭會讓 slot 子件清單退化為「任意元件」。
- **P（最可能失敗）**：容器數超過 skill 十個門檻被判為歸併鍵混入畫面名。防護：3.5 逐條記錄已歸併與未歸併的理由，未歸併者皆因子件類型或空間不足策略不同，非因畫面不同。

### 推導摘要

- 起點判定：repo 有路由殼與 token、頁面尚未呼叫任何元件（`lib/app/shell.dart` 的 `_NavItem` / `_ProjectSwitcherEntry` 為私有雛形，非元件庫）→ 模式 B 畫布拆解四步再進模式 A 步驟 3-5。
- 輸入實走：`design/` 九份 artboard 以 scratchpad 腳本抽成 DOM 樹（去 svg、只留佈局屬性與文字）逐一盤點；SPEC-001 30 列顯示欄與可用操作欄；SPEC-003 §1.1 十個捲動處、§2.6–2.7、§2.11、§3.1–3.7 作為行為來源；`lib/tokens/` 三檔作為 token 名來源；`lib/main.dart` 的 `kDesignSize` / `kMinWindowSize` 作為視窗尺寸名。
- 未列入元件的視覺單元：泳道底部箭頭列（`SwimlaneGrid` 內部裝飾）、矩陣列（併入格線）、側欄堆疊（併入 `AppShell`）。

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
### 自檢結果

DOC 型 Solution 依 schema 免填；本子節依 W17-064 Layer 1 自檢可觀測性要求補列，內容與 Completion Info「自檢結果」表同源：裸值 0 命中、禁用詞 0 命中、無 emoji、SPEC-002 八類 8/8 與 SPEC-001 30 列 30/30 皆有歸屬、存在必要性 20 項皆附理由並標提案、形態因素矩陣標待核定、未改其他票、第 4-9 章僅骨架。

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext
### 1. 佈局尺寸 token 缺料（044.2 尺寸契約的前置）

- **缺失項**：`lib/tokens/` 只有顏色、間距、圓角、字級四類；畫布上的固定佈局尺寸無具名常數：側欄寬、頁首高、導覽項高、表格列高（票列／步驟列／樹列／節首各一）、主副雙欄右欄寬（畫布兩處不同：矩陣詳情卡與節點詳情右欄）、浮層寬、矩陣首欄寬與小計欄寬、泳道名欄寬與泳道列高、圖示尺寸階（畫布出現三種）。
- **觸發位置**：SPEC-004 §3.4 排列關係表「列高固定（token 缺）」諸列；§3.5 漂移表側欄寬與右欄寬兩列。
- **影響**：本票不受影響（第 3 章只記錄排列關係與間距 token 名）；044.2 的尺寸契約與排列不變式公式需要這些具名值，缺料時該欄依 skill 標「待決」，元件不得被畫面票引用。
- **建議補料**：依 skill〈前置〉建 token 前置票（走 `foundation-design` skill UI 維度，產出 `lib/tokens/layout.dart` 或同等檔），設為 044.2 的 blockedBy。實測值（量測環境：`design/*.dc.html` 內聯樣式，設計基準無縮放）：側欄 172、頁首高 52、標題列高 36、導覽項 padding 7/10、票列高 34、主題票行高 29、樹列高 31、節首高 28、矩陣列高 32、泳道列高 52、矩陣首欄 132、小計欄 46、右欄 236（矩陣）/ 216（詳情）、浮層寬 262、泳道名欄 106、圖示 13/15/17。
- **重派成本**：無需重派本票；044.2 派發前補齊即可。

### 2. `lib/app/shell.dart` 與畫布的漂移（W1-005 應改內容，本票不改）

- `_kSidebarWidth` 的值與畫布側欄寬不一致（程式碼較寬）；畫布為準（SPEC-002 設計約束），待第 1 項 token 落地後 shell 改引用該 token。
- `_NavItem` 無 leading 圖示，畫布六項皆有圖示；`NavItem` 契約含 icon slot，shell 對齊屬 W1-005。
- 標題列：shell 用 `AppBar` + `AppFontSize.title`，畫布標題列為 `body` 級置中小字、`AppColors.surfaceChip` 底、`AppColors.borderStrong` 底邊；視覺值以畫布為準，`AppShell` 契約（044.2）依畫布填。
- `_NavItem` / `_ProjectSwitcherEntry` 為 shell 私有 widget，不在統一匯出入口；W1-005 建元件庫時應改為引用 `NavItem` / `ProjectSwitcherEntry` 並刪私有版本。

### 3. `Motion` token 尚未存在

- SPEC-003 通篇引用 `Motion.transition` / `Motion.overlay` / `Motion.snackBar` / `Motion.snackBarWithAction` / `Motion.progressTick` / `Motion.skeletonCycle` / `Motion.spinnerMinVisible` / `Motion.cancelDeadline` 等，`lib/tokens/` 無對應檔。
- **影響**：044.2 逐元件「互動反應」與「使用 design token → 動畫」子節需引用；缺則標待決。
- **建議補料**：確認 W1-030～039 是否已含 `Motion` token 產出；未含則併入第 1 項的 token 前置票。

### 4. `two_dimensional_scrollables` 未在 `pubspec.yaml`

- `docs/tech-decisions.md` 定案矩陣渲染委派該套件，SPEC-003 §1.1 亦以此為前提；`pubspec.yaml` 目前無此依賴。`MatrixGrid` 契約依賴它；建議 W1-005 或矩陣元件票的 acceptance 含加入依賴。

### 5. SPEC-001 §1 與畫布的行為類漂移（跨檔，待 PM 裁決）

- 畫布 `Main.dc.html` 矩陣模式右側有「格詳情卡」（Corpus × UC-02：標題、說明、編號步驟、事件標籤），SPEC-001 §1 正常·矩陣顯示欄只寫「domain × UC 交叉表」，SPEC-003 §3.1 亦無「選格後右欄顯示詳情」的反應。
- 若納入 0.1：SPEC-001 §1 顯示欄與 SPEC-003 §3.1 須補；`TwoColumnLayout` 於 §1 出現。若不納入：`TwoColumnLayout` 只出現於 §6，`ListRow.numbered` 變體可刪。SPEC-004 §3.5 已標待核定，本票不改 SPEC-001 / SPEC-003。

### 6. 派發 prompt 與 ticket body schema 衝突

- 派發訊息要求「填 Solution（含自檢結果子節）與 Test Results」；`ticket-body-schema.md` DOC 型 Solution / Test Results 為免填。依 `tool-output-trust-rules` 規則 6 衍生情境以正本為準：兩節保留免填 placeholder，自檢結果與變更摘要寫入 Completion Info（DOC 必填節）。

### 7. 待核定項彙整（供 PM 於本票驗收與 044.2 派發前裁決）

| # | 項目 | 位置 |
|---|------|------|
| 1 | 形態因素矩陣全表（版型策略屬用戶簽核） | SPEC-004 §1 |
| 2 | 文字縮放適應（提案：0.1 不承諾動態字級） | §1 |
| 3 | 是否加入第三種「大於設計基準」測試尺寸 | §1 |
| 4 | 第 2 章「傳值 + callback」與資料視圖容器例外 | §2 |
| 5 | 存在必要性檢視 20 項提案（含節點卡拆分、專案切換浮層拆分、損壞標記更名、ListRow / TableRow / Section / SplitRow 合併、LoadPrompt 獨立、EmptyState 四變體） | §3.3 |
| 6 | 非資料視圖容器 13 個超過 skill 十個門檻，是否接受 | §3.1 末段、§3.5 |
| 7 | `Badge` chip / inline 外觀是否為第二變體軸 | §3.3 第 8 項 |
| 8 | `StepNumber` 圓／方統一；`BadgeRow.legend` 間距取一；`Panel` 內距是否分兩階 | §3.5 |
| 9 | 損壞計數可點錨點位置（摘要底列或工具列）；票列主題模式欄序 | §3.5 |
| 10 | `DocumentBody`、`AppSnackBar` 是否屬元件庫或列第 7 章豁免 | §3.3 第 15、16 項 |
| 11 | `AppButton` 變體集（L2） | §3.1 |
| 12 | 矩陣格詳情卡是否納入 0.1（第 5 項） | §3.5 |
| 13 | 無最近專案態 `EmptyState.compact` 是否渲染訊息；節點詳情返回／開啟原始檔按鈕位置；破洞報告重新掃描按鈕位置 | §3.6 |

### 8. 044.2 派發建議

- 第 3 章核定（去「提案」標）後再填第 4-5 章，否則九欄位會對著可能被拆併的清單填。
- 最長測試文案來源：狀態文案類 key 已在 `lib/l10n/app_zh.arb` / `app_en.arb`（兩語系，少於三種 → 依 skill 另加一條人工長文案）；資料文字（domain 名、UC 名、ticket 標題、節點路徑）非 i18n key，最長值須取自 SPEC-001 設計約束的假資料快照（flutter_balance 1313 張 ticket）——建議 044.2 派發 prompt 指定該來源，或於假資料票落地前該欄標待決。
- 044.2 若依 skill 粒度規則一票一元件，元件 25 + 容器 17 = 42 張元件票；建議以 parent 傘票分 wave，原子元件（`Divider`、`AppIcon`、`AppText`）依 skill 得合併為一票。


### 補記：第 3 項的承接票

complete 時 CLI 提示 where.files 與 `0.1.0-W1-033`（建立時間 token 與其裸值檢查）有交集：該票即 `Motion` token 的產出票，尚 pending。第 3 項「建議補料」改為：W1-033 設為 044.2 的 blockedBy（互動反應與動畫 token 子節），不另建票。已以 relatedTo 連結。
## Exit Status
```yaml
exit_status: success
reason: "SPEC-004 第 1-3 章與 SPEC-002 指向已完成；第 3 章結論全數標提案待核定，待核定項彙整於 NeedsContext 第 7 項"
confidence: 0.85
acceptance_met: [0, 1, 2, 3]
acceptance_unmet: []
artifacts:
  - docs/spec/ui/SPEC-004-component-library.md
  - docs/spec/ui/SPEC-002-design-tokens-and-component-library.md
context_dependencies:
  - .claude/skills/component-contract-design/SKILL.md
  - .claude/skills/doc/templates/component-library-spec-template.md
  - docs/spec/ui/SPEC-001-screen-state-matrix.md
  - docs/spec/ui/SPEC-003-interaction-response.md
  - design/*.dc.html
  - lib/tokens/*.dart
  - lib/main.dart（kDesignSize / kMinWindowSize）
  - docs/tech-decisions.md（Riverpod 定案）
estimated_recovery_effort: ""
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-02T17:37:31
**Executing Agent**: lavender-interface-designer
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

### 變更摘要

| 檔案 | 變更 |
|------|------|
| `docs/spec/ui/SPEC-004-component-library.md` | 新建。第 1 章形態因素矩陣（九維度，全表提案標待核定）；第 2 章狀態綁定模式（Riverpod，傳值 + callback 為提案，含資料視圖容器例外與頁面狀態保留條款）；第 3 章元件清單總表（元件 25、容器 17，含種類／層級／用途／出現畫面／變體）與推導記錄 3.2 視覺單元盤點、3.3 存在必要性檢視 20 項、3.4 排列關係盤點、3.5 同型歸併與畫布漂移、3.6 SPEC-002 八類與 SPEC-001 30 列歸屬對照；第 4-9 章保留範本骨架（尺寸契約列改引用 `kMinWindowSize` / `kDesignSize`，第 8 章標不適用） |
| `docs/spec/ui/SPEC-002-design-tokens-and-component-library.md` | §元件庫的範圍 清單改為指向 SPEC-004 §3；狀態數 29 → 30；保留「空狀態與阻擋狀態必須是兩個元件」為 SPEC-004 前提；frontmatter `related_specs` 補 SPEC-004、版本 1.1、變更歷史一列 |

### 自檢結果

| 檢查 | 結果 |
|------|------|
| 裸值：`#RRGGBB` / `0x` 色碼、`px`、`N×N` 尺寸 | SPEC-004 grep 0 命中（視窗尺寸以 `kDesignSize` / `kMinWindowSize` 引用；間距／圓角／字級／顏色皆為 `lib/tokens/` 常數名） |
| 禁用詞（智能／文檔／數據／默認／代碼／信息／優化等） | 0 命中 |
| Emoji | 無 |
| SPEC-002 八類歸屬 | 8 / 8（§3.6 第一表） |
| SPEC-001 30 列顯示欄歸屬 | 30 / 30（§3.6 第二表，含 §4 疊加態與 §7 三列） |
| 合併／不合併結論附理由並標提案 | 20 / 20（§3.3） |
| 形態因素矩陣標待核定 | 是（§1 節首與版型策略列） |
| 未修改 W1-005 或其他票 | 是；應改內容記於 NeedsContext 第 2、4 項 |
| 未寫第 4-9 章內容 | 是（僅範本骨架與節狀態註記） |
