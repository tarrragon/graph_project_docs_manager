---
id: 0.1.0-W2-014
title: 接線 降級旗標的寫入端
type: IMP
status: completed
version: 0.1.0
wave: 2
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W2-009
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W2-009 SR-1：容器已就位但寫入端在其授權範圍外
  final_decision: 獨立成票接寫入端
  rationale: W2-009 派發時 domain_view 與 ticket_list 有其他代理人在改，範圍限制是當時的並行紀律，不是設計切分
who:
  current: parsley-flutter-developer
  history: {}
what: domain_view 觸發降級時把 degradedSchemaProvider 設真，切換專案時重置，使 badge-<screen>-degraded-schema 在真實執行中實際出現
when: W2-009 交出容器後
where:
  layer: Presentation
  files:
  - lib/app/degraded_schema.dart
  - lib/components/app_shell.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_en.dart
  - lib/l10n/app_localizations_zh.dart
  - lib/l10n/app_zh.arb
  - lib/screens/domain_view/domain_view_screen.dart
  - lib/screens/domain_view/domain_view_state.dart
  - lib/screens/project_switcher/project_switcher_overlay.dart
  - lib/screens/project_switcher/project_switcher_providers.dart
  - test/unit/components/app_shell_test.dart
  - test/unit/screens/domain_view_test.dart
  - test/unit/screens/project_switcher_test.dart
why: W2-009 已交出返回列容器與 app 層的 degradedSchemaProvider，並以 provider override 驗證容器行為。但寫入端未接——真實 App 執行中該旗標永遠為 false，六畫面的降級徽章不會出現。W1-035 裁定的型別表缺席時降級而非拒絕，其可見證據鏈到此仍未接通：W1-074 交出元件 slot、W2-011 接了 domain_view 的降級出口、W2-012 讓門檻讀真實資產、W2-009 建了徽章容器，四張票之後使用者仍看不到任何降級標示。本票是該鏈的最後一環
how:
  task_type: Implementation
  strategy: domain_view 的降級出口觸發時（W2-011 已實作的 onDegradedView callback 落地處）同步把 degradedSchemaProvider 設真；project_switcher 選定新專案時重置為假（SPEC-004 4.25 狀態矩陣同步策略欄的切換專案重置，含下次可見且圖已建立時的但書）。徽章文字的兩個 placeholder（builtinVersion／projectVersion）取自 W2-012 建立的 builtinSchemaVersionProvider 與掃描到的專案 VERSION
acceptance:
- '[x] domain_view 觸發降級檢視後 degradedSchemaProvider 為真，切換專案後重置為假'
- '[x] 真實執行路徑下（非 provider override）六畫面任一狀態皆可見 badge-<screen>-degraded-schema，且徽章文字含內建型別表版本與專案 VERSION 兩個值'
- '[x] 測試斷言涵蓋設真與重置兩個方向，並含一組會因寫入端未接而翻紅的案例'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-22T17:39:42'
completed_at: '2026-09-22T17:48:54'
created: '2026-09-21'
updated: '2026-09-21'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

domain_view 觸發降級時把 degradedSchemaProvider 設真，切換專案時重置，使 badge-<screen>-degraded-schema 在真實執行中實際出現

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-22 17:39] Compare how with SPEC-001 §1 降級型別表段落和 SPEC-004 §4.27 before implementing. 在 domain_view onDegradedView callback 處同步寫入 degradedSchemaProvider=true；project_switcher 切換專案時重置為 false。AC3 要求含一組因寫入端未接而翻紅的案例。｜readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
寫入端：`lib/screens/domain_view/domain_view_screen.dart` 的
`_SchemaUnconsumableView.onDegradedView`（觸發 `action-domain-degraded-view`
時）同步寫入 `degradedSchemaProvider=true` 與
`degradedSchemaVersionsProvider`（`DegradedSchemaVersions(builtinVersion,
projectVersion)`，前者取自 `builtinSchemaVersionProvider` 已載入的值，
後者為 `DomainSchemaUnconsumable.version`）。

重置端：`lib/screens/project_switcher/project_switcher_overlay.dart` 的
`_selectProject`（切換至任一最近專案時）重置兩個 provider 為
`false`/`null`（SPEC-001 §1「切換專案時旗標重置」）。

徽章文字：`lib/components/app_shell.dart` 的 `_MainArea` 改watch
`degradedSchemaVersionsProvider`，`l10n.degradedSchemaBadgeLabel` 新增
`builtinVersion`/`projectVersion` 兩個 placeholder（`app_zh.arb` /
`app_en.arb` 同步更新，`fvm flutter gen-l10n` 重新生成
`app_localizations*.dart`）。`degradedVersions` 為 null 時徽章版本文字
退回空字串（防禦性處理，非預期路徑——寫入端與旗標本身同步寫入）。

Compare with SPEC-001 §1〈降級型別表〉與 SPEC-004 §4.27〈實作註記〉：
兩者對「觸發降級寫真、切換專案重置、返回列常駐徽章」的敘述與本次接線
一致，無需求缺口。

### 自檢結果

- 類別 A（三明示/資訊優先序/負向錨點）：Solution/Test Results 為事實
  記述（做了什麼、驗證結果），非規則性論述，不適用三明示結構；已確認
  無「禁止 X 無理由」型單向敘述。
- 類別 B（禁用字/emoji/簡體字）：掃描 Solution/Test Results 全文，無
  數據/代碼/默認/文檔/軟件/硬件/信息/視頻、無 emoji、無簡體字形。
- 類別 C（Schema 結構）：所有 H2 為 Schema 定義章節（Solution / Test
  Results），Problem Analysis 保留選填佔位符（IMP 型 Problem Analysis
  為選填，未強制填寫）。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
- `fvm flutter test test/unit/screens/domain_view_test.dart --reporter compact`：42/42 通過
- `fvm flutter test test/unit/screens/project_switcher_test.dart test/unit/components/app_shell_test.dart --reporter compact`：53/53 通過
- `fvm flutter test --reporter compact`（全量）：All tests passed
- `fvm dart analyze lib/ test/`：No issues found
- commit: 92caf115e601c74790a4f2e0f5964a770dc5a6c4

AC3 對照：`domain_view_test.dart`「VERSION 不高於內建型別表版本 → ...」
測試新增對 `degradedSchemaProvider`/`degradedSchemaVersionsProvider` 的
斷言（寫入端未接線時，兩者維持預設值 false/null，此斷言會翻紅）；
另新增「降級旗標寫入端真實執行路徑」群組（真實 App，非 override
degradedSchemaProvider，走真實觸發→徽章可見路徑）；
`project_switcher_test.dart` 新增「選擇項目後降級旗標重置為假」測試
（切換前設真，寫入端未接線時切換後仍為真，斷言翻紅）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-22T17:48:54
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W2-009 | chars: 448 -->

### Task Reference
- 0.1.0-W2-009 what: 在 shell.dart 建立 SPEC-001 §1〈降級型別表〉所述的「返回列」容器，供 badge-domain-degraded-schema 於六畫面任一狀態常駐渲染（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W2-009 why: SPEC-001 §1〈降級型別表〉要求降級徽章「由頁面框架於返回列同一列常駐渲染（六畫面任一狀態）」；shell.dart 目前只有 SplitRow.header（W2-001 已追加條件式 trailing 掛點），無返回列概念。即使 DomainReady.isDegraded 為 true，六畫面皆無法顯示該徽章，W1-035 定案的常駐降級徽章在 UI 上無落點（來源已完成，結論見 Solution）

### Related Files
- lib/app/shell.dart  # from 0.1.0-W2-009（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] shell.dart 提供返回列容器，六個目的地皆可於該列渲染常駐徽章  # from 0.1.0-W2-009（來源已完成，結論見 Solution）
- [ref] [x] 降級旗標為真時 badge-domain-degraded-schema 於六畫面任一狀態可見  # from 0.1.0-W2-009（來源已完成，結論見 Solution）
- [ref] [x] 與 0.1.0-W2-008 的 SplitRow.header trailing 掛點並存不互相覆蓋，既有測試維持綠燈  # from 0.1.0-W2-009（來源已完成，結論見 Solution）
