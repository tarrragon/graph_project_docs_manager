---
id: 0.1.0-W3-335.10
title: 修補 test-assertion-design 缺範例與元件執行期行為驗證範例（W3-332.3）
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy:
- 0.1.0-W3-335.9
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 修補 test-assertion-design 缺範例與元件執行期行為驗證範例（W3-332.3）
when: 前驅票完成後
where:
  layer: 待定義
  files:
  - .claude/skills/test-assertion-design/SKILL.md
  - .claude/skills/test-assertion-design/CHANGELOG.md
  - .claude/skills/test-assertion-design/references/app-testing-context.md
  - .claude/skills/test-assertion-design/references/web-testing-context.md
why: W3-332 審查與走查確認 agent 依此文件推導元件與測試時被迫猜測或讀出兩種做法；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
how:
  task_type: Documentation
  strategy: 依 chained-examples 規格與 Context Bundle 執行；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
acceptance:
- '[x] W3-332.3 範圍內 test-assertion-design 缺範例位置補三段鏈式範例'
- '[x] 補至少一則「元件狀態集與內容政策如何轉為 Given/When/Then 驗證」的範例（W3-332 審查指出測試端無執行期行為範例）'
- '[x] 範例技術中立且不含本專案識別符'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T12:17:07'
completed_at: '2026-09-14T12:26:22'
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修補 test-assertion-design 缺範例與元件執行期行為驗證範例（W3-332.3）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-14 12:16] readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-14T12:26:22
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-14
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

**變更摘要**：

`SKILL.md`（1.4.0 -> 1.5.0）新增〈判讀範例〉章節（置於〈9 類型問題斷言分類決策表〉之後、〈不在範圍聲明〉之前），依 chained-examples 三段鏈式格式補五則範例：

1. 聚焦性：行為面向如何計數（斷言品質三問）—— 設計產出物，檢驗問句
2. 類型 9：斷言數量與複雜度的比例判準 —— 設計產出物，檢驗問句
3. 類型 8：快照粒度是否匹配驗證意圖 —— 設計產出物，檢驗問句
4. 類型 6：一般隨機與密碼學安全隨機的策略分歧 —— 設計產出物，檢驗問句（不修改 backend-testing-context.md，該檔不在本票 where.files，判準已於 SKILL.md 本體給出）
5. 元件執行期行為：狀態集與內容政策如何轉為驗證 —— 執行期行為，Given/When/Then（對應本票 acceptance 第 2 項）

五則範例對應 W3-332.3 Solution〈需範例位置表〉第 20、24、23、25、（新增）列；範例輸入/產出/驗證三段皆為語言與框架中立描述，不含本專案識別符。

**第 21 列複驗（不需另補範例）**：W3-332.3 第 21 列（三問確定性 vs 判斷軸分族對類型 5、6 的處置分歧）之根本矛盾已於 0.1.0-W3-335.8（C11）改文解決——〈斷言品質三問〉表格後與〈判斷軸〉節皆已新增明確路由句「讀者判斷處置時以分族為準」。複驗〈何處需要範例〉判準：模擬兩位讀者依現行文字（皆含路由句）推導類型 5/6 的處置，因路由規則為封閉指向（一律依分族查表，不套三問症狀分組的處置），兩人推導結果必然一致。結論：未命中，不需另補範例。

`CHANGELOG.md` 新增 1.5.0 版紀錄，摘要同上（已避免專案層級 ticket ID 引用，依 reference-stability-rule8-guard-hook 提示改自足描述）。

`app-testing-context.md`／`web-testing-context.md` 本票未變更——W3-332.3 範圍內無命中此二檔的缺範例位置，維持現況。

自檢：zhtw-mcp 掃描新增內容（compact 模式）無 error；warning 集中於「渲染」「對象」「斷點」三項 cross_strait/confusable 命中，經比對均為本 skill／同源 skill-design-guide 既有慣用詞（`chained-examples.md` 已用「畫面渲染完成」、SKILL.md 原文已用「被驗證對象」、「斷點」為「判斷點」子字串誤判），依 language-constraints 規則 2 邊界（與既有用語一致者判定專案 canonical）不列違規；translationese 警告源自掃描時將表格攤平為連續散文的人工產物，實際檔案為表格/條列格式，不構成定語堆疊。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-335.9 | chars: 227 -->

### Task Reference
- 0.1.0-W3-335.9 what: 修補 test-assertion-design 缺引導（G14、G15）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-335.9 why: W3-332 審查與走查確認 agent 依此文件推導元件與測試時被迫猜測或讀出兩種做法；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 類型 3 命中率與參考等價的二選一判準補齊  # from 0.1.0-W3-335.9（來源已完成，結論見 Solution）
- [ref] [x] 斷言預期值取自 spec 條文的約束補齊  # from 0.1.0-W3-335.9（來源已完成，結論見 Solution）
- [ref] [x] 新約束逐條套〈何處需要範例〉判準並記錄  # from 0.1.0-W3-335.9（來源已完成，結論見 Solution）
### 修補票共同 Context（PM，2026-09-14）

**缺口清單在哪**：本票涵蓋的缺口編號見 `ticket track full 0.1.0-W3-332.13` Solution；每個缺口的位置錨點、兩位讀者的分歧模擬、子票草稿見其來源子票（W3-332.2–.10）Solution。**W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 優先於 .13 草稿**——草稿漏收、誤歸或數字不符處以 P 項為準。

**規格**：`.claude/skills/skill-design-guide/references/chained-examples.md`（三段鏈式、驗證段兩形態、缺範例與缺引導分界）。

**已定案的用戶裁示**：
- 語言專屬 skill 的違規／修正程式碼對照不受「禁程式語法」約束（例外條款由 W3-335.15 寫入規格）
- 元件庫規格範本佔位文字改為技術中立，框架名移到 L2 註記
- version-bootstrap 的 v02-walkthrough 依新規格重寫整篇

**矛盾類票的簽核流程**：先在 Solution 寫「矛盾位置｜兩方原文｜建議以哪方為準｜理由｜改後文字草稿」，**不改檔**，以 `ticket track set-exit-status` 設 needs_context 並停手回報；PM 取得用戶簽核寫回本票後再續做改文。

**通則**：
- 框架檔不得寫本專案識別符（票號、SPEC 編號、畫面名），範例一律去識別化
- 集合大小改引用權威名稱而非寫死數字；錨點用標題不用行號
- 改動 skill 須同步其 CHANGELOG 與 SKILL.md 版號
- 只改本票 where.files；發現範圍外缺口登記 spawn request，不就地修
