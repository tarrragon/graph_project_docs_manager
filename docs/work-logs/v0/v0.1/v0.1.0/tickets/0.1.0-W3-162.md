---
id: 0.1.0-W3-162
title: 改用 additionalContext 輸出 agent-dispatch-validation-hook 的廣域 staging 警告
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-156
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-156 執行中查證確認同一 hook 的既有 PC-092 警告有相同的通道缺陷，登記 SR-4
  final_decision: 另開 IMP，不在 0.1.0-W3-156 一併修
  rationale: 兩處防護標的不同（PC-092 廣域 staging vs worktree complete 誤判），混改會使兩者的驗證互相污染；通道修法已由 156 建立範例，本票只是套用
who:
  current: basil-hook-architect
  history: {}
what: 改用 additionalContext 輸出 agent-dispatch-validation-hook 的廣域 staging 警告
when: 0.1.0-W3-156 落地 additionalContext 通道之後
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/hooks/agent-dispatch-validation-hook.py
why: '`_emit_wide_staging_warning_if_parallel`（PC-092 廣域 git staging 防護）目前以

  `print(..., file=sys.stderr)` 輸出。`main()` 在多數放行路徑（review 模式、僅主 repo

  `.claude/`、worktree 隔離）皆以 exit 0 回傳，而 PreToolUse hook 於 exit 0 時 stderr

  不注入 PM 的 context——該警告在未觸發阻擋的並行派發中不可見。


  此結論由 `0.1.0-W3-156` 執行中實查確認（登記為 SR-4），非推測。同一份查證亦說明了為何

  過去看得到這條警告：只在同一次 hook 呼叫稍後以 exit 2 阻擋時，stderr 才隨阻擋一併投遞，

  屬巧合的可見性。


  **後果不是少一條提示，是防護等於不存在**：PC-092 的防護對象是並行代理人的 git index

  競爭與跨票吸收，那正是「沒有人看見就不會有人改」的一類問題——警告不可見時，廣域 staging

  照樣送出，事後從 commit 內容也看不出曾被警告過。


  `0.1.0-W3-156` 已在同一檔案內建立正確通道的實作範例

  （`_emit_worktree_complete_warning` 以 `hookSpecificOutput.additionalContext` 輸出），

  本票是把該範例套用到既有那一處。


  與 `tarrragon/claude#54` 第 2 項（`canonical-schema-consistency-check` 的 WARNING 走

  stderr 未注入 session context）屬同一機制族，hook 事件類型不同。'
how:
  task_type: Implementation
  strategy: 比照同檔案內 _emit_worktree_complete_warning 的寫法改 _emit_wide_staging_warning_if_parallel 的輸出通道。注意兩者呼叫位置不同：本處可能在稍後仍會 exit 2 的路徑上被呼叫，需確認同一次呼叫既輸出 additionalContext JSON 到 stdout、又走阻擋路徑時，兩者不衝突（若衝突，阻擋路徑維持 stderr 即可，因 exit 2 時 stderr 本就會投遞）。測試須涵蓋放行路徑（exit 0）命中時 additionalContext 有輸出，這是既有測試未涵蓋的缺口。
acceptance:
- '[ ] 並行 + 廣域 staging 且走 exit 0 放行路徑時，警告經 hookSpecificOutput.additionalContext 輸出'
- '[ ] 同時觸發廣域 staging 警告與 exit 2 阻擋時，兩條輸出路徑不互相破壞（以測試證明）'
- '[ ] 既有 PC-092 相關測試全數通過，且新增放行路徑可見性測試'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

改用 additionalContext 輸出 agent-dispatch-validation-hook 的廣域 staging 警告

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: basil-hook-architect
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-156 | chars: 1649 -->

### Task Reference
- 0.1.0-W3-156 what: 補強 agent-dispatch-validation-hook：worktree 派發 prompt 用 complete 而非 finish 時提出警告（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-156 why: `.claude/references/agent-dispatch-template.md:746` 與 `.claude/pm-rules/parallel-dispatch.md`
已明訂：worktree 隔離派發的收尾指令須用 `ticket track finish`（`complete` 的等價別名），
避開 CC runtime worktree isolation guard 對 argv basename 誤判 bash builtin `complete`
而條件性阻擋。規範存在，但沒有任何自動層在派發當下檢查。

`0.1.0-W3-147` 實際命中：PM 的派發 prompt 寫了 `ticket track complete`，代理人照做，
所有實質工作（提取、analyze 0 issues、test 1207/1207、commit `6c941dff`、ticket body
補齊、acceptance 4/4）全部完成後，卡在最後一步被 harness 拒絕，於 NeedsContext 停手
回報，由 PM 在主倉庫代跑收尾。代理人的處置正確（`tool-selection.md` 規則 2：非專案來源
的拒絕應停手回報而非繞道），成本落在 PM 的接手與一次完整的往返。

`agent-dispatch-validation-hook.py` 已在同一個派發事件上檢查兩件事——廣域 git staging
（PC-092）與實作代理人是否用 worktree 隔離——兩者都在同一個 prompt 字串上做 regex。
本項所需的判定是同一形態且更簡單：isolation 為 worktree 且 prompt 含 `track complete`
即命中。攔截點已存在，缺的只是這一條規則。

規範層有、強制層無，是這類條文最典型的失效形態：規範寫在按需讀取的 references 裡，
PM 為控制 prompt 體量憑記憶起草時不會展開它，於是規範對實際派發沒有影響力。（來源已完成，結論見 Solution）

### Related Files
- .claude/hooks/tests/test_agent_dispatch_validation_hook.py  # from 0.1.0-W3-156（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] hook 在 isolation=worktree 且 prompt 含 track complete 時輸出警告，訊息含 finish 替代指令與 template 路由  # from 0.1.0-W3-156（來源已完成，結論見 Solution）
- [ref] [x] 以 0.1.0-W3-147 的實際派發 prompt 為輸入驗證命中；以含 track finish 的 prompt 驗證不誤報  # from 0.1.0-W3-156（來源已完成，結論見 Solution）
- [ref] [x] 警告不阻擋派發（exit 0），且經 hookSpecificOutput.additionalContext 輸出——不得只走 stderr，PreToolUse hook 於 exit 0 時 stderr 不注入 PM context（Problem Analysis 第三點）  # from 0.1.0-W3-156（來源已完成，結論見 Solution）
- [ref] [x] 已確認 _WIDE_STAGING_WARNING 在未觸發阻擋時是否同樣不可見；若成立已登記 spawn request 交 PM，未在本票一併修改該處  # from 0.1.0-W3-156（來源已完成，結論見 Solution）
- [ref] [x] 本 session 實地觸發確認：已於本 session 以 settings.json 登記的實際指令（uv run --quiet .claude/skills/ticket/hooks/agent-dispatch-validation-hook.py）餵入三組真實 stdin JSON payload 執行，確認 worktree+track complete 命中輸出 additionalContext、worktree+track finish 不誤報、exit code 皆為 0  # from 0.1.0-W3-156（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

