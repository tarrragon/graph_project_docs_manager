---
id: 0.1.0-W3-295
title: 重評 framework-issue 條文與 rule 設計的 sync-push 時機，並在此期間守住本地改動
type: ADJ
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-293
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-293
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶裁示（2026-09-11）：先用幾個 issue 做測試以改進 framework-issue 與 rule 設計，暫不 sync-push
  final_decision: 建本票承接延後並綁定 trigger，不以口頭記錄取代
  rationale: decision-trigger-binding 規則 1：無 trigger 的延後在以後與永不之間沒有可驗證邊界；且延後期間 sync-pull 會無聲覆蓋本地改動
who:
  current: 主線程
  history: {}
what: 重評 framework-issue 條文與 rule 設計的 sync-push 時機，並在此期間守住本地改動
when: 用戶指定的 issue 測試迭代結束、條文形狀不再變動時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/
  - .claude/rules/core/quality-baseline.md
why: '**用戶裁示（2026-09-11）**：先用幾個 issue 作為測試案例，藉以改進 `framework-issue` skill 與 rule 設計，使其符合「發現時建票但不當下定修法」的做法；**在此之前不執行 `sync-push`**。


  **延後本身是合法決策，但它產生一個沒有訊號的風險**：`.claude/` 的條文改動在推送前只存在於本地。下一次 `sync-pull` 以上游版本做三方合併，這段期間的本地改動可能被覆蓋——而**覆蓋成功與「上游本來就長這樣」在結果上不可區分**，不會有任何錯誤訊息或紅燈。觸發者是一個常規維護動作，任何 session 都可能順手執行。


  **為何不用口頭記錄**：`decision-trigger-binding.md` 規則 1——延後必須是「等 ticket X 完成後執行 Y」，無 trigger 的延後必累積為死議題。本票即該 trigger 的載體。


  **不做則後果**：測試迭代結束後無人記得要推；或迭代期間某個 session 執行 `sync-pull`，條文改動消失且無人察覺，`0.1.0-W3-293` 的產出歸零而票面仍顯示 completed。'
how:
  task_type: Review
  strategy: '**兩段工作。第一段的強度已依用戶裁示下修（見下）。**


    **一、延後期間的守護（強度：記錄，非禁令）**


    **用戶裁示（2026-09-11）**：此時點無其他人在修改框架資產。該事實使覆蓋風險不成立——`sync-pull` 的三方合併中，上游未前進時合併基準等於上游，本地改動不會被覆蓋。**故不設 `sync-pull` 禁令。**


    **該事實是時點觀測，不是恆常條件**，故保留一項零成本的記錄：


    - `0.1.0-W3-293` 落地後，將其 commit SHA 與受改檔案清單記入本票 Solution。

    - **執行 `sync-pull` 前先確認上游是否已前進**（`git -C <canonical clone> log` 或 pull 後檢視合併範圍）。上游未前進則無需任何額外動作；已前進則以該 SHA 為基準，用 `git log --oneline <sha>..HEAD -- <那些檔案>` 確認本地改動是否仍在。


    SHA 記錄本身在 trigger 階段的重評也用得到（acceptance 第 1 項），非專為此風險而設。


    **二、trigger 成立後的重評（不預設結論）**


    trigger 為「用戶指定的 issue 測試迭代結束、條文形狀不再變動」。屆時評估三件，逐項寫入 Solution：


    1. 測試迭代是否改變了 `0.1.0-W3-293` 落地的條文形狀？改了哪些、依據是什麼。

    2. 候選 B（建票期 CLI 提示）是否已有結論？`0.1.0-W3-289` 將其定位為「待 `#81` 區段討論後決定」，若測試迭代已回答提示強度與誤報處置，應一併納入推送範圍。

    3. 推送範圍是否僅限 `.claude/`？`sync-push` 影響全部 consumer 專案，範圍須逐檔確認而非整目錄推。


    **推送本身仍須用戶當次確認**——`sync-push` 為跨專案動作，不在本專案的常駐授權內，本票不預先取得該授權。'
acceptance:
- '[ ] 測試迭代是否改變條文形狀已判定，改動項與依據逐條記錄'
- '[ ] 候選 B 的結論狀態已確認（已決定／仍待議），並判定是否納入推送範圍'
- '[ ] 推送範圍已逐檔確認，非整目錄推'
- '[ ] 推送前已取得用戶當次確認，確認記錄寫入 Solution'
- '[ ] 0.1.0-W3-293 的 commit SHA 與受改檔案清單已記錄於本票 Solution'
tdd_phase: phase2
tdd_stage:
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

重評 framework-issue 條文與 rule 設計的 sync-push 時機，並在此期間守住本地改動

---

## Problem Analysis

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-293 | chars: 867 -->

### Task Reference
- 0.1.0-W3-293 what: 落條文 framework-issue 升級流程的時點分離判準，與品質基線規則 5 的接縫註記

### Rationale Chain
- 0.1.0-W3-293 why: **用戶裁示（2026-09-11）**：先調整 framework-issue skill 的說明與引導、以及相關 rule 的規範，再回頭評估目前是否已可開始實作、或仍須回頭檢查 issue 內容再做分析。

**條文內容已由 `0.1.0-W3-289` 產出，本票只做落地**，不重新設計。該票的機械量測：443 張票中 275 張為發現衍生，其中 108 張 IMP，其中 74 張來源票非 ANA；這 74 張建票當下 `how.strategy` 全部非空，僅 12 張（16%）帶待驗證／停手語彙——**62/74（84%）在發現當下寫定了一個沒有任何欄位記載其驗證狀態的修法**。

**289 原本的定位是「未定案，供討論」**，理由寫在該票：條文形狀是它自己的修法，其消費端（其他 consumer 的建票實務）未經觀測，故依自身判準不寫成結論。用戶裁示先落地，此裁示即為本票的依據；**`#81` 的區段討論不因此關閉**，後續若該處給出不同形狀，以其為準。

**不做則後果**：判準只存在於一張已完成的票與一則 issue 觀測裡。下一個 session 建發現票時不會讀到它，84% 的比例不會改變。

### Related Files
- .claude/skills/framework-issue/references/escalation-flow.md  # from 0.1.0-W3-293
- .claude/skills/framework-issue/SKILL.md  # from 0.1.0-W3-293

### Source Acceptance (reference)
- [ref] [ ] 三問與兩檔擴散面處置表已落於 escalation-flow.md，判準內容與 0.1.0-W3-289 一致  # from 0.1.0-W3-293
- [ref] [ ] 三個檢驗案例已收錄，且能分辨豁免與不豁免（非全面禁令）  # from 0.1.0-W3-293
- [ref] [ ] SKILL.md 有一行路由指向該節，未複製判準內容  # from 0.1.0-W3-293
- [ref] [ ] quality-baseline 規則 5 的接縫註記不超過一行且含路由，未在自動載入層展開三明示  # from 0.1.0-W3-293
- [ref] [ ] 單位是欄位不是票、豁免軸是證據狀態不是規模，兩項邊界已明寫  # from 0.1.0-W3-293
  ... (truncated, see source ticket)
