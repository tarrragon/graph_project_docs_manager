---
id: 0.1.0-W3-335.37
title: 裁決 V4 歸因的 12 項 spec 間矛盾（以 WRAP 評估取代用戶逐項裁決）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy: []
relatedTo:
- 0.1.0-W3-335.36
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-335.36 矛盾清單
  final_decision: WRAP 評估取代逐項簽核
  rationale: 用戶裁示
who:
  current: general-purpose
  history: {}
what: 裁決 V4 歸因的 12 項 spec 間矛盾（以 WRAP 評估取代用戶逐項裁決）
when: W3-335.36 完成後
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.37.md
why: 用戶裁示（2026-09-14）：簽核改由 WRAP 評估，除非牽涉用戶寫程式碼的風格偏好。W3-335.36 列出 12 項需裁決的 spec 間矛盾
how:
  task_type: Analysis
  strategy: 逐項讀 .36 Solution〈spec 間矛盾清單〉兩處原文 → 以 WRAP 完整模式評估（錨點：UC 使用者目標與 PROP 決策；W 須含反向框架；R 以 UC 驗收與既有 SPEC-004 設計為事證）→ 寫出裁決、被放棄選項的代價、要改的 spec 位置與改法
acceptance:
- '[ ] 12 項逐項 WRAP 評估：候選選項（含反向框架）、事證、裁決、被放棄選項優點、改動位置（檔名＋章節）與改法'
- '[ ] 牽涉用戶寫程式碼風格偏好者標「風格偏好，交用戶」，不代為裁決'
- '[ ] Solution 含依 spec 檔分組的追修清單（連同 .36 不需裁決的 8 項同步），供建追修票；本票不改 spec'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T17:05:47'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

裁決 V4 歸因的 12 項 spec 間矛盾（以 WRAP 評估取代用戶逐項裁決）

---

## Problem Analysis
### 問題根因

W3-335.36 歸因出 20 項 spec 間矛盾，其中 12 項（.36 矛盾清單 #2、#3、#5、#9、#10、#11、#13、#16、#17、#18、#19、#20）標為需裁決。成因分四型：

| 型 | 說明 | 項 |
|----|------|----|
| 同檔兩處各自演進 | 通則段與逐畫面表格分次回寫，後寫的一方未回頭同步 | #3、#5、#10、#11、#13、#18 |
| 前提假設失效 | 條文以「0.1 假資料此路徑不出現」或「一律 jump」為前提，後續新增狀態使前提不成立 | #17、#18、#20 |
| 上游文件未隨規格收斂 | UC 描述先於規格，規格定案後 UC 未改 | #16、#19 |
| 冗餘被誤讀為矛盾 | 兩處皆顯示同一資料，實為重複而非衝突 | #9 |

（#18 同屬兩型，列兩次。）

### 影響範圍

- 已驗證：SPEC-001 §1、§2、§4、§5、§8.1、FR-08；SPEC-003 §2.3、§2.7、§3.1、§3.2、§3.4、§3.5、§4；SPEC-004 §3.6、§3.7、4.21、4.23；UC-04、UC-06（逐項原文已讀）
- 已驗證：受影響錨點中僅 `action-domain-schema-detail`（`lib/components/blocked_state.dart`）與 i18n key `backToDomainAction` 已入程式碼；Domain／UC Flow／Ticket 清單畫面尚未實作，改規格不牽動既有畫面程式碼
- 未驗證：SPEC-002 是否引用受影響條文（本票未讀 SPEC-002）

### 相關 Error Pattern

無直接對應。#17、#18 屬「條文前提假設失效但無偵測點」型，與 decision-trigger-binding 規則 2.5 的條件式條文同源，不另立 pattern。

## 重現實驗結果
### 實驗方法

本票為規格裁決，「重現」＝逐項讀兩端原文，確認矛盾真實存在且範圍與 .36 描述一致，再列候選。讀取來源：SPEC-001 全文；SPEC-003 §2.3–§2.8、§3.1–§3.7、§4、§5；SPEC-004 §3.6、§3.7、4.21、4.22、4.23；UC-01、UC-02、UC-04、UC-05、UC-06；EVT-DIAGNOSTICS-001、EVT-CORPUS-003 負載欄位；`lib/l10n/app_zh.arb` 的 `schemaIncompatibleMessage`；`grep` 受影響錨點於 `lib/`、`test/`。

### 實驗執行

逐項對照結果：

| .36 # | 矛盾是否成立 | 範圍修正 |
|------|------------|---------|
| 2 | 成立 | 另見 UC-05 替代場景〈列表模式篩選〉明寫篩選屬列表模式 |
| 3 | 成立 | 範圍大於 .36 所述：SPEC-003 §2.3 另有「§1、§3、§5 不渲染返回」例外段，其理由只引 SPEC-001 退出欄未列（循環引用） |
| 5 | 成立 | 僅 SPEC-003 §3.1〈導航跳轉與退出〉載入中列漏列；同檔〈動畫提示〉與 §4 #2 已含三個阻擋狀態 |
| 9 | 不成立為矛盾，為冗餘 | `schemaIncompatibleMessage` 已代入 `{appVersion}`、`{projectVersion}`，面板再列兩值 |
| 10 | 成立 | 選格必寫入選定 UC，故 `uc-unset` 分支不可達，實際為兩分支 |
| 11 | 成立 | 衝突點：泳道模式下選另一 domain 後切回矩陣，選中格保留而列高亮已移走 |
| 13 | 成立 | §2.3 首段把「§2 flow 未結構化的返回」列為需來源記錄者，§3.2 定為固定目標不讀 returnTo |
| 16 | 成立 | UC-04 驗收條件已自標「前提未滿足」 |
| 17 | 成立 | 範圍大於 .36 所述：圖未建立的可達路徑有三條（未選專案、Domain 載入中經 L1 切頁、三個阻擋狀態），非僅阻擋狀態 |
| 18 | 成立 | 範圍大於 .36 所述：SPEC-004 §3.6 §5 無破洞列（EmptyState 動作 slot）與同檔 §3.7 第 18 項（header）互相矛盾；SPEC-003 §2.7 覆蓋列另含「無最近專案」，與 SPEC-004 §3.7 第 3 項不用 EmptyState 矛盾 |
| 19 | 成立 | UC-06 驗收只要求「解析失敗時的行號」；EVT-CORPUS-003 `line: int?` 可空 |
| 20 | 成立 | FR-08(b) 斷言形式對 SPEC-001 §8.2 已列的事件類 SnackBar 無豁免 |

### 實驗發現

- 12 項中 11 項為真矛盾，#9 為冗餘（仍給裁決以結案）
- 附帶發現（不在 12 項內，列入追修清單）：SPEC-004 4.23 `BlockedState` 用途段與反例表仍寫「0.1 不渲染以純檔案模式檢視，待 `0.1.0-W1-035`」，`plain` 變體無 `action-domain-degraded-view`，與 SPEC-001 v1.5 §1、SPEC-003 §3.1 已定案的降級檢視動作不一致
- 用戶寫程式碼風格偏好：12 項皆為產品行為或版面語意，無一牽涉，0 項交用戶

## Solution
### 評估框架

WRAP 完整模式，逐項走 錨點 → W → R → A → P。

| 階段 | 本票的落地 |
|------|-----------|
| 錨點 | UC 的使用者目標與成功保證；PROP-004 版型決策；SPEC-001 FR-01（每狀態可退出）與 FR-03（空狀態有前進動作） |
| Step 0 | 資料來源為 spec／UC／EVT 原文，非個人化決策，判定充足 |
| W 三類涵蓋 | 改造既有（改寫既有條文或元件用法）／零新增（刪除、不做、改讀法）／新增（新狀態、新操作、新元件）；每項另列反向框架 |
| R | 事證限原文可查者（UC 步驟與驗收、SPEC 條文、EVT 負載、已入庫程式碼）；各項寫明假設根因 |
| A | 以「規格改動牽動的下游處數」與「對 UC 目標的損益」比較，不以估時比較 |
| P | 每項列一個最可能的失敗與可觀測訊號 |

選項編號規則：每項 O1 起算，「兩處原文二選一」若在候選中則明標，且至少另有一個非二選一選項。

### 裁決總表

| R# | .36 # | 議題 | 裁決（一句話） | 交用戶 |
|----|------|------|--------------|-------|
| R1 | 2 | 主題態是否受搜尋／篩選作用 | 主題態不套用搜尋詞與篩選（值保留供切回列表），帶目標跳入在主題態只處理收合節 | 否 |
| R2 | 3 | 載入完成後帶 returnTo 是否渲染返回 | 返回改為頁面框架通則：六畫面任一狀態只要 `returnTo` 非 null 即渲染，刪除 §1／§3／§5 例外段 | 否 |
| R3 | 5 | Domain 導航表載入中列缺三阻擋落點 | 導航表補列三個阻擋狀態錨點，與同檔 §4 #2 對齊 | 否 |
| R4 | 9 | schema 不相容本體與面板是否都顯示兩值 | 兩處都顯示，定為有意冗餘：本體為句子、面板為帶標籤的單行值；補註記即結案 | 否 |
| R5 | 10 | 在泳道中檢視的落點 | 按鈕先以該格 UC 寫入選定 UC，再依「切至泳道」列的分支落 `swimlane` 或 `swimlane-unstructured` | 否 |
| R6 | 11 | 列高亮唯一 vs 切模式保留選中格 | 選中 domain 為兩模式共用的單一值；任一模式選了與選中格不同列的 domain 即清除選中格 | 否 |
| R7 | 13 | UC Flow「返回 Domain 視圖」固定目標 vs §2.3 | 刪除 `action-ucFlow-back-to-domain`，flow 未結構化改由選擇 UC、R2 通用返回、導覽列承擔退出 | 否 |
| R8 | 16 | UC-04 選定提案／反向追溯 | UC-04 步驟 1 改寫為「展開一個 PROP」；反向追溯保留於 UC-04 並標 0.1 規格未涵蓋（前提未滿足），不加入規格 | 否 |
| R9 | 17 | 圖未建立時經導覽列抵達其他畫面 | 五個非 Domain 畫面各增「專案未就緒」狀態（共用一則定義），前進動作 jump 至 Domain 視圖；刪除「此路徑不出現」 | 否 |
| R10 | 18 | 無破洞的重新掃描位置與 §2.7「一律 jump」 | 重新掃描在無破洞與有破洞一律置於 header；無破洞的 EmptyState 不帶動作；§2.7 動作觸發後改為「jump 或同畫面轉換」並移除「無最近專案」 | 否 |
| R11 | 19 | 事件類破洞項是否帶行號 | 行號可空：有行號的項（解析失敗）顯示，其餘只顯示檔案路徑，不顯示佔位 | 否 |
| R12 | 20 | FR-08(b) vs 帶目標跳入 SnackBar；§8.1 順序基準 | (b) 改為「進入轉換本身不觸發 SnackBar，SPEC-001 §8.2 所列事件的 SnackBar 不計」；§8.1 順序明定為 §1–§7 各表列序 | 否 |

交用戶項目：0（12 項皆不牽涉用戶寫程式碼的風格偏好）。

### 逐項評估（R1–R4）

#### R1（.36 #2）主題態是否受搜尋／篩選作用

**兩端原文**：SPEC-003 §3.4〈排序欄首的元件級契約〉S6「排序與篩選只於 `state-tickets-list` 可觸發」；同檔〈帶目標跳入〉「已載入但目標被搜尋詞或篩選隱藏」列「主題模式下展開該票所在主題節」（隱含主題態受篩選作用）。
**假設根因**：S6 只定「可觸發處」，未定「作用範圍」；帶目標跳入撰寫時把作用範圍讀成兩模式共用。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | 主題態不套用搜尋與篩選，值保留供切回列表（S6 字面延伸） |
| O2 | 改造既有（原文二選一的另一端） | 主題態套用列表設定的搜尋與篩選，但不提供修改控制 |
| O3 | 新增 | 主題態也渲染 `Toolbar`，搜尋與篩選兩模式皆可操作（排序仍限列表） |
| O4 | 反向框架 | 搜尋與篩選改為各模式各自一組值 |

**R 事證**：UC-05 替代場景〈列表模式篩選〉「使用者停留在列表模式，以搜尋與篩選定位特定票」，把篩選綁在列表模式；UC-05 步驟 3–4 主題模式的目的是看主題進度分佈與阻擋，全量才能呈現分佈；SPEC-004 §3.6 §4 正常 · 主題列無 `Toolbar`；SPEC-001 §4 正常 · 主題可用操作無搜尋篩選。O2 使主題態顯示子集卻無可見控制與提示，使用者無從得知票為何缺席。
**裁決**：O1。
**被放棄選項優點**：O3 讓大型專案可在主題分組內縮小範圍；O4 避免切模式時的語意混淆。
**選定方案缺點**：(1) 使用者在列表設了篩選後切主題，看到的票數與列表不同，需靠 S6 的保留語意理解；(2) 主題態無法縮小範圍；(3) 帶目標跳入在兩模式行為分岔，測試需分開。
**P 失敗訊號**：畫面票實作時為主題態加上篩選指示或清除按鈕（表示使用者需要看到篩選狀態）→ 重評 O3。
**改動位置與改法**：
- SPEC-003 §3.4〈排序欄首的元件級契約〉S6：補「主題模式不套用搜尋詞與篩選，一律呈現全部已載入票；值保留，切回列表時照原值套用」
- SPEC-003 §3.4〈帶目標跳入〉「已載入但目標被搜尋詞或篩選隱藏」列：限定為列表模式；刪「主題模式下展開該票所在主題節（收合時）」子句（主題態由既有「目標僅因主題節收合而不可見」列承擔）
- SPEC-001 §4 正常 · 主題：註記補一句「不套用搜尋與篩選」

#### R2（.36 #3）載入完成後帶 returnTo 是否渲染返回

**兩端原文**：SPEC-003 §2.3 規則 4「`returnTo` 為 `null` 時返回按鈕不渲染」（非 null 即渲染的通則）；同檔 §3.4〈導航跳轉與退出〉只在未載入列列出 `action-tickets-back`；同檔 §2.3「並非所有 jump 目標畫面都渲染規則 4 的返回錨點」段排除 §1、§3、§5。
**假設根因**：返回鍵的渲染被寫成「逐狀態列舉」，但 §2.4 已定為頁面框架單一渲染；兩種模型並存。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增（原文二選一一端） | 只在 SPEC-001 退出欄列有返回的狀態渲染（Ticket 清單僅未載入） |
| O2 | 改造既有（原文二選一另一端，限縮） | §4、§6 兩畫面的全部狀態，`returnTo` 非 null 即渲染 |
| O3 | 改造既有（反向框架：取消逐畫面例外） | 六畫面全部狀態，`returnTo` 非 null 即渲染；刪 §2.3 例外段 |
| O4 | 零新增（反向框架：取消返回） | 全面移除返回鍵，一律靠導覽列 |

**R 事證**：UC-05 替代場景〈自破洞報告切入〉與 SPEC-003 §3.5 破洞項三種 jump（Ticket 清單、節點詳情、UC Flow）——使用者逐項處理破洞時需回到破洞報告，現行只有節點詳情有返回；O1 使「自破洞跳入 → 自動載入 → 載入完成」過程中返回鍵先出現後消失，控制在無使用者操作下消失；§2.3 例外段的唯一理由是「SPEC-001 退出欄未列」，屬循環引用；§2.4 與 SPEC-004 §3.7 第 17 項已定返回鍵由頁面框架在 `SplitRow.header` 單一渲染，O3 使框架只需一條規則；O4 違反 SPEC-001 §6「返回來源畫面」。
**裁決**：O3。
**被放棄選項優點**：O2 改動處少、不動 Domain／追溯／破洞三畫面；O1 零改動。
**選定方案缺點**：(1) Domain 視圖 header 已有 `PageTitle` 與 `SegmentedControl`，再加返回鍵使 `kMinWindowSize` 下擁擠；(2) SPEC-003 §4 對照表多列需補返回；(3) 自追溯視圖跳到節點詳情再點「前往追溯視圖」這類往返會出現意義薄弱的返回。
**P 失敗訊號**：SPEC-004 `SplitRow.header` 在 `kMinWindowSize` 下的排列不變式無法同時容納三件 → 回退為 O2。
**改動位置與改法**：
- SPEC-003 §2.3：刪「並非所有 jump 目標畫面都渲染規則 4 的返回錨點」段，改寫為「返回鍵為頁面框架通則：六畫面任一狀態，`returnTo` 非 null 即渲染 `action-<screen>-back`」；首段「四個退出路徑需要回到來源」改為引用本通則
- SPEC-003 §3.1～§3.6 各〈導航跳轉與退出〉表與 §4 對照表：不逐列補返回，於 §4 表頭註記「各列另含通則返回（`returnTo` 非 null 時）」
- SPEC-001 概述或 §7 之後：補一段「返回為殼層通則，不逐列列出；§4 未載入、§6 各列的返回為該通則的既有列舉」
- SPEC-004 4.28 `PageColumn` 或 4.29 `SplitRow.header`：確認返回鍵 slot 對六畫面皆存在（追修票核對）

#### R3（.36 #5）Domain 導航表載入中列缺三個阻擋落點

**兩端原文**：SPEC-003 §3.1〈導航跳轉與退出〉載入中列「解析完成 → `state-domain-matrix` 或 `state-domain-empty`」；同檔 §4 #2 與 SPEC-001 §1 載入中退出欄含三個阻擋狀態。
**假設根因**：§3.1 表寫於阻擋狀態併入載入中落點之前，SPEC-001 v1.3 補齊時只同步了 §4。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | §3.1 載入中列補三個阻擋錨點 |
| O2 | 零新增（反向框架：不在兩處列舉） | §3.1 該列改為引用 §4 #2，不重複列舉落點 |
| O3 | 改造既有（原文二選一另一端） | 自 §4 與 SPEC-001 移除阻擋落點 |

**R 事證**：SPEC-001 §1 註記「載入中的退出路徑涵蓋三個阻擋狀態」為定案；同檔〈動畫提示〉已列「載入中 → 正常 / 空圖 / 三個阻擋狀態」；O3 與上游定案衝突。§3.1 表的其他列皆直接列錨點，測試撰寫者以該表取錨點。
**裁決**：O1。
**被放棄選項優點**：O2 消除同檔兩處列舉的漂移來源。
**選定方案缺點**：(1) 同一落點集合在同檔列兩次，下次新增落點仍可能漏改；(2) 表格列變長；(3) 不處理降級旗標疊加的表示。
**P 失敗訊號**：SPEC-001 §1 載入中落點再增減時，§3.1 與 §4 #2 列數不一致 → 改 O2。
**改動位置與改法**：SPEC-003 §3.1〈導航跳轉與退出〉載入中列改為「解析完成 → `state-domain-matrix`、`state-domain-empty`、`state-domain-not-framework`、`state-domain-schema-unconsumable` 或 `state-domain-schema-incompatible`」。

#### R4（.36 #9）schema 不相容本體與面板是否都顯示兩值

**兩端原文**：SPEC-001 §1 schema 不相容顯示欄「版本不符說明 + 兩個版本值」且可用操作含「檢視詳情」；SPEC-003 §3.1「檢視 schema 詳情」列面板「恰為兩列 標籤 + 版本值」；SPEC-004 4.23 內容政策 `message` 為 `schemaIncompatibleMessage`（代入 appVersion／projectVersion）。
**假設根因**：非矛盾。`app_zh.arb` 的 `schemaIncompatibleMessage` 為「App 支援的 schema 版本為 {appVersion}，此專案為 {projectVersion}，版本不相容」，本體已含兩值，面板重複。探針因看不到 arb 而無法判斷。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | 兩處皆顯示，註記面板用途（帶標籤、單行 mono、便於辨識與轉述） |
| O2 | 改造既有 | 本體訊息不含版本值，兩值只在面板 |
| O3 | 零新增（反向框架） | 刪除檢視詳情與 `withDetail` 變體，本體已足 |
| O4 | 新增 | 面板改放本體沒有的資訊（App 已知版本範圍、`.claude/VERSION` 路徑） |

**R 事證**：SPEC-001 FR-04 驗收「顯示兩個版本值與說明」——O2 使收合時看不到版本值，違反 FR-04；O4 的「App 已知範圍」屬 CLAUDE.md §6 五項空殼判準之一，無定義可寫；O3 須刪已入庫的 `lib/components/blocked_state.dart` `withDetail` 與測試，並改 SPEC-001／003／004 三檔。
**裁決**：O1。
**被放棄選項優點**：O3 移除一個資訊量為零的互動；O4 讓面板有獨立價值。
**選定方案缺點**：(1) 同一資料顯示兩次；(2) 「檢視詳情」按鈕承諾了詳情但內容無新增；(3) 版本值文字若改格式需兩處同步。
**P 失敗訊號**：「App 已知範圍」判準定案時 → 以 O4 重評面板內容（該判準定案票即 trigger，由 PM 綁定）。
**改動位置與改法**：SPEC-004 4.23 變體表 `withDetail` 列補註「本體訊息已代入兩值；面板為帶標籤的單行值，兩者為有意冗餘」；SPEC-001、SPEC-003 不改。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-335.36 | chars: 411 -->

### Task Reference
- 0.1.0-W3-335.36 what: 歸因 V4 走查 143 個新猜測點與 5 項仍猜原猜測點，對照 SPEC-004 分出真缺口並提出 spec 追修草案（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-335.36 why: W3-335.30 判定 V4 未通過（原猜測點仍猜 5 項）；凍結門檻要求新猜測點逐項歸因、spec 缺描述者建追修票。探針禁讀 SPEC-004，多數「spec 未描述」可能在 SPEC-004 已有答案，需對照後才知道真缺口（來源已完成，結論見 Solution）

### Related Files
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.36.md  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 143 個新猜測點與 5 項仍猜點逐項歸類表（題號＋猜測點編號、歸類、答案所在檔名＋章節或缺口說明）  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）
- [ref] [x] 各歸類計數；spec 間矛盾逐項列出兩處原文位置  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）
- [ref] [x] spec 間矛盾與真缺口的追修草案，依 spec 檔分組，每項標明是否需用戶裁決；不建票、不改檔  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）
- [ref] [x] skill 缺引導／缺範例者列出與 W3-335.31～.35 已涵蓋項次的重疊關係  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）
