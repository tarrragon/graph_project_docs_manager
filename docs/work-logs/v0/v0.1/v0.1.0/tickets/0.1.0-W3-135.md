---
id: 0.1.0-W3-135
title: 宣告 WorkspaceDirectoryProbePort 的例外契約，使可替換性不只在簽章層成立
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-121
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: Coupling 視角發現介面宣稱可替換但例外語意綁死 dart:io
  final_decision: IMP 把結果時刻的錯誤分類移到 port 邊界
  rationale: 違反契約的替換實作會靜默改變降級行為且編譯期零警訊
who:
  current: parsley-flutter-developer
  history: {}
what: 宣告 WorkspaceDirectoryProbePort 的例外契約，使可替換性不只在簽章層成立
when: 0.1.0-W3-121 Phase 4a 完成後（2026-09-08）
where:
  layer: Infrastructure
  files:
  - lib/workspace/workspace_repository.dart
  - test/unit/workspace/workspace_repository_test.dart
why: 'Coupling 視角發現：`WorkspaceDirectoryProbePort.readFirstEntry(String path)` 的簽章回傳 `Future<void>`，**不宣告任何例外型別**；但 `_inspect()` 用 `on FileSystemException catch (e)` 與 `on StateError` 分類結局。


  也就是說介面帶著一條**隱性契約**：實作必須擲出 `dart:io.FileSystemException` 表示不可讀、`StateError` 表示空資料夾。這條契約只存在於 `_inspect()` 的實作細節與測試替身的行為約定裡，沒有被介面聲明固定。


  後果：若替換 `WorkspaceDirectoryProbePort` 的實作（例如包裝非本機檔案系統來源），只要擲出的例外型別不符，`_inspect()` 的降級分支就失效——例外穿透到全域錯誤攔截層，行為從「顯示資料夾不可用」變成「整個畫面被 `BlockedState.plain` 取代」。**這個差異在編譯期沒有任何警訊**，只能靠執行期觀察或補寫測試發現。


  這與〈Port 回饋契約〉的精神有落差：契約應包含「結果」時刻的錯誤分類方式，目前分類邏輯（FileSystemException vs StateError vs 其他）留在呼叫端而非 port 邊界，使 port 的可替換性宣告與實際約束不一致。


  `abstract interface class` 這個宣告方式本身就是在對讀者承諾可替換。承諾與約束不一致時，承諾會被相信。'
how:
  task_type: Implementation
  strategy: '把結果時刻的錯誤分類從呼叫端移到 port 邊界。兩個候選形態，擇一並在 Solution 寫明理由：


    - **A：port 回傳分類結果**。`readFirstEntry` 改回傳一個表達「可讀／不可讀／空資料夾」的結局型別，例外由各實作自行吸收並轉換。`_inspect()` 不再做型別比對。優點是契約在簽章上顯性；代價是 port 承擔了分類語意。

    - **B：port 定義自己的例外型別**。宣告 `WorkspaceProbeException` 等，實作負責把平台例外轉為它。`_inspect()` 改捕捉 port 自己的型別。優點是保留例外流；代價是每個實作多一層轉換。


    無論選哪個，須補一條測試：注入一個擲出**非 `FileSystemException` 也非 `StateError`** 例外的探測替身，斷言行為仍為降級而非穿透。現行 `_FakeDirectoryProbe`（`test/unit/workspace/workspace_repository_test.dart:626-643`）的 `exists()` 只回布林、無錯誤注入點，`readFirstEntryError` 也只在測試中注入過那兩個會被吞掉的型別——**現有測試結構上到不了這條路徑**，補測試時須同時擴充替身的注入面。


    本票與 0.1.0-W3-121 Phase 4b 的 `_inspect` 移出 try 有交集：兩者都動 `_inspect` 的例外處理。派發前確認 Phase 4b 已完成，避免衝突。'
acceptance:
- '[ ] 錯誤分類移至 port 邊界（回傳分類結果或 port 自有例外型別），_inspect 不再直接比對 dart:io 型別'
- '[ ] _FakeDirectoryProbe 的 exists 具備錯誤注入點；補一條注入非 FileSystemException／非 StateError 例外的測試，斷言行為為降級而非穿透'
- '[ ] 兩個候選形態的選擇理由寫入 Solution'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

宣告 WorkspaceDirectoryProbePort 的例外契約，使可替換性不只在簽章層成立

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-121 | chars: 1363 -->

### Task Reference
- 0.1.0-W3-121 what: 補齊 WorkspaceRepository 三個 driven port 呼叫的回饋契約

### Rationale Chain
- 0.1.0-W3-121 why: `lib/workspace/workspace_repository.dart` 有四個 driven port 呼叫點，依 `clean-architecture-implementation-methodology.md`〈Port 回饋契約〉的 INV-PORT-OBSERVE-001 全數不合規（PM 逐行實查）：

1. `chooseFolder()` 的 `getDirectoryPath()`（file_selector 平台通道）：無 try/catch、無日誌。MissingPluginException 或使用者環境異常時直接往上拋，呼叫端拿到的是未分類例外。
2. `chooseFolder()` 的 `SharedPreferences.getInstance()` / `setString(_pathKey, path)`：**回傳值完全未檢查**。`setString` 回傳 `Future<bool>`，寫入失敗時函式照樣往下走並回傳 `_inspect(path)`——使用者看到資料夾已選定，但下次 `restore()` 讀不到值會回 `WorkspaceUnset`。這是方法論「典型違反」的加重變體：連日誌都沒有，呼叫端與日誌兩個獨立義務同時缺席。
3. `restore()` 的 `getInstance()` / `getString()`：無失敗路徑。Phase 1 已修正本條的初始描述——`getString()` 是純記憶體查表無 I/O 失敗路徑，真正的讀取失敗集中在會拋例外的 `getInstance()`，兩者不可混為一談，詳見 Solution 裁決 B。
4. `_inspect()` 的 `Directory.exists()` 與 `dir.list().first`（dart:io 檔案系統）：現況只有 `FileSystemException` 分支有日誌，屬結果時刻的失敗分支；`exists()` 完全無記錄，兩處皆缺「呼叫發出」事件。本條由 Phase 1 的 SR-1 補入——PM 建票時盤點遺漏，非後續擴張。

四處皆缺「呼叫發出」事件，判準問句「把外部系統整個拔掉，呼叫端能否得知呼叫已發出但無回應」目前答案為否。

測試覆蓋現況：`test/` 下無 workspace_repository 的單元測試檔，僅 `integration_test/app_test.dart` 間接引用。

範圍界線：本票只處理 App 端邏輯軌的回饋契約合規。平台端事實軌（實機驗證 file_selector 與真實檔案系統行為）由 `0.1.0-W3-088` 評估，兩者不重疊。`gap_report_screen._openItem` 的同類缺口由 `0.1.0-W3-112` 承接，不在本票範圍。

### Related Files
- lib/main.dart  # from 0.1.0-W3-121
- lib/l10n/app_zh.arb  # from 0.1.0-W3-121
- lib/l10n/app_en.arb  # from 0.1.0-W3-121
- lib/l10n/app_localizations.dart  # from 0.1.0-W3-121
- lib/l10n/app_localizations_zh.dart  # from 0.1.0-W3-121
  ... (truncated, see source ticket)

