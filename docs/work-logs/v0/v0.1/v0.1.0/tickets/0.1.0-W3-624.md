---
id: 0.1.0-W3-624
title: 修規格並驗收 受測步驟行外移、brief 與計分欄對帳、7.2(3-2)② 的無格區（第六輪，Step 1-3）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-623
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-623 三項開票級發現（兩項規格層、一項條文層）
  final_decision: 開第六輪，規格層兩項與條文層一項一次處理
  rationale: 三項皆通過收納判準第一列（使一道本該不通過的題被判通過）；批次改文一次的形態下同輪處理，避免逐項開票復發 W3-417 的發散
who:
  current: general-purpose
  history: {}
what: 修規格並驗收 受測步驟行外移、brief 與計分欄對帳、7.2(3-2)② 的無格區（第六輪，Step 1-3）
when: 0.1.0-W3-623 收尾後
where:
  layer: 待定義
  files:
  - docs/probes/probe-authoring-spec.md
  - .claude/skills/ux-design-evaluation/references/interaction-feedback-component-semantics.md
why: 0.1.0-W3-623（本線首個零改動輪）產生三項依收納判準第一列須開票的發現。(1) 規格 1.1 要求「受測判定步驟一行」寫在題目檔，而 5.2 把題目檔路徑直接交給探針，兩條合起來使受測款次編號與受測動作隨題目檔送到探針眼前——世界平面查證：0.1.0-W3-621 的 q/Q01.md 首行即該行、prompt-r1.txt 第一項即該檔路徑。這是 §0 第二種成因（字面對應）的直接通路，會使一道本該不通過的題被判通過。W3-623 的處置是把該行外移到出題者側獨立檔並記為刻意偏離，規格層須裁決該行的歸屬。(2) 判定者 brief 的「什麼不算問題」清單（為 L13 而設）與計分欄的記通過條件可互相矛盾，規格未要求兩者對帳：W3-623 的豁免第 1 條（多引與判定相容的其他款次不算問題）與計分欄 C3、規格 5.5「補述與條文牴觸者記不通過」撞車，兩名判定者對同一輪（R1）給出相反計分，寬讀即放行。(3) 條文層：7.2(3-2)② 的三格對「某項兩端都整項未寫」沒有格——略去不比限兩端都寫「看不出」，無從比對限只有一端寫出內容；完整條文下由 ①乙 的漏寫條款在前面堵住，但 ② 的「整項未寫」支同時與 ①乙 的「不得進 ②」互斥，提供一條指向相反答案的替代路徑。W3-623 的 1.7 遮蔽推導經裁決力條款取判定者側的反向紅，依規格屬條文缺陷須回 Step 1 改條文；同一缺口實測已使一輪作答（R1）在補述中誤把兩端皆未寫歸入「無從比對」而失分。
how:
  task_type: Analysis
  strategy: Step 1 規格層兩項（受測步驟行的歸屬、brief 豁免清單與計分欄的對帳要求）與條文層一項（7.2(3-2)② 補「兩端都整項未寫」的處理，並解消 ② 的「整項未寫」支與 ①乙 的互斥）；Step 2 出題涵蓋條文層改動；Step 3 三輪兩判定者，產出覆蓋比與布林
acceptance:
- '[ ] 受測判定步驟一行的歸屬已在規格裁決（寫進題目檔或外移到出題者側），並說明它不會送到探針眼前'
- '[ ] brief 的豁免清單與計分欄的對帳要求已寫進規格，並以 W3-623 的 R1 分歧為對照輸入驗證新規則會判出同一結果'
- '[ ] 7.2(3-2)② 對「某項兩端都整項未寫」已有處理，且該處理與 ①乙 的「不得進 ②」不互斥；附遮蔽推導驗證'
- '[ ] Step 3 產出已依規格給出（覆蓋比；布林或下界成因）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-18T01:26:15'
completed_at: null
created: '2026-09-18'
updated: '2026-09-18'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修規格並驗收 受測步驟行外移、brief 與計分欄對帳、7.2(3-2)② 的無格區（第六輪，Step 1-3）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### PM 裁決：發現 1 是洩題，須先判定它污染了哪些既有讀數

W3-623 的三項發現中，**發現 1 的性質與另兩項不同，優先處理**：

> 規格 1.1 要求「受測判定步驟一行」寫在題目檔，而 5.2 的 prompt 把題目檔路徑直接交給探針——**受測款次編號與受測動作因此送到探針眼前**。世界平面已查證：`r621/q/Q01.md` 首行即該行、`r621/prompt-r1.txt` 第一項即該檔路徑。

這不是可用性問題，是**洩題**。探針被告知要測哪一款、要做什麼動作，然後我們用「引用率」與「套用率」當作條文生效的證據。

**本票須先回答的問題**（在處理另兩項之前）：

1. **這個洩漏從哪一輪開始存在？** 規格 1.1 自 1.0.0 即有（W3-612 落檔），故推定 W3-616／618／620／621 四輪全數受影響，請以各輪的 `q/` 與 prompt 實體檔案逐輪查證，不要以推定代替查證。
2. **它污染了哪些讀數？** 逐項判定：引用率、套用率、計分（判定值）、1.7 遮蔽推導的判紅判綠。三者受影響的程度不同——引用率幾乎必然被抬高，計分未必。
3. **哪些既有結論須降級？** 本線用「依據句引條文」與「引用／套用分計」當作條文生效的核心證據（W3-550 的降級、W3-618 的通過、W3-621 的覆蓋比 1/1 皆依賴它）。若這些讀數被洩題抬高，須逐項寫明降為何種強度，**不得只登記缺陷而不動結論**。
4. **W3-623 自己也受影響嗎？** 該輪已把該行外移至 `key/Q01-tested-step.md` 並記為刻意偏離——即本輪是唯一未洩題的一輪。請確認此判斷。

處置（改規格）須同時滿足：受測判定步驟一行仍須事先定稿可複核（那是 1.7 遮蔽推導的輸入），但不得進入探針可見範圍。

### 另兩項

2. **brief 豁免清單與計分欄可互相矛盾，規格未要求對帳**——W3-623 的 R1 分歧即實例（判定者甲依 brief 豁免判通過、乙依計分欄 C3 判不通過，裁決取不通過）。
3. **條文層**：7.2(3-2)② 的三格對「某項兩端都整項未寫」**沒有格**（略去不比限兩端寫「看不出」、無從比對限只有一端寫出內容），且 ② 的「整項未寫」支與 ①乙 的「不得進 ②」互斥。完整條文下由 ①乙 堵住，遮掉即露出，而**同一缺口在真實作答中已造成失分**（R1 的補述）。

## 重現實驗結果
### 實驗方法

逐輪以實體檔案查證洩題通道是否存在，不以推定代替。查證對象三組：

1. 各輪工作目錄的 `q/` 題目檔——是否含「受測款次編號 ＋ 該款要求讀者執行的動作」的指定行（規格 1.1 所要求者）。
2. 各輪的派發 prompt 檔——第一項是否為該題目檔的路徑（規格 5.2）。
3. 規格前的 `probes/q/` 全部 140 份舊題目檔（`0.1.0-W3-417` 系列，含 `0.1.0-W3-550` 重測所用的 A3c／A2c）——同一判準。

工作目錄前綴 `<scratchpad>` ＝ `/private/tmp/claude-501/-Users-mac-eric-project-graph-project-docs-manager/2ccf6d72-30b6-455c-96ea-2023fad2d11e/scratchpad`。

### 實驗執行

| 輪 | 票 | 題目檔含受測指定行 | prompt 交出題目檔路徑 | 判定 |
|---|---|---|---|---|
| r616 | `0.1.0-W3-616` | **17／17 題**（每檔兩處命中：`## 受測判定步驟` 標題與其下的內容行） | 是（r616 未留 prompt 檔；票面逐字記載「prompt 只給四項（單題檔路徑、節錄目錄、作答格式、輸出路徑）」，且 51 次派發皆為 `Agent`） | **洩題** |
| r618 | `0.1.0-W3-618` | **3／3 題**（`> **受測判定步驟**：…`，置於題目檔第 3 行） | 是（`prompts/R2-Q01-r1.txt` 第一行即 `題目檔：<r618>/q/R2-Q01.md`） | **洩題** |
| r620 | `0.1.0-W3-620` | **1／1 題**（`**受測**：7.2(3-2)② ——依乙那一目要求寫的各項比完之後的結果，合成本款給出的判定。`） | 是（`prompt-r1.txt` 第一行 `單題檔路徑：<r620>/q/Q01.md`） | **洩題** |
| r621 | `0.1.0-W3-621` | **1／1 題**（`**受測**：7.2(3-2)② ——依乙那一目要求寫的各項的書寫狀態與擺在一起看的結果，合成本款給出的判定。`） | 是 | **洩題** |
| r623 | `0.1.0-W3-623` | **0／1 題**（`grep -c 受測 q/Q01.md` ＝ 0；該行外移至 `key/Q01-tested-step.md`） | 是 | **未洩題** |
| W3-417 系列 | 含 `0.1.0-W3-550` | **0／140 份**（140 份題目檔中 6 份出現「受測」字串，逐則檢視皆為材料用語「受測 Module」，屬 tdd 題組的被測對象名稱，非受測款次指定行） | 該系列 prompt 為整份題目檔內嵌，非路徑 | **未洩題** |

r616 的十七行逐字（節錄三則，全部十七則的款次與動詞皆已核對）：

- Q01「受測：7.2(3-2)①——對一份已寫出兩端名稱的比對，逐項執行該款要求的檢查並寫出檢查結果，再判定這份比對有沒有完成該款的這一步。」
- Q10「受測：8.2(3)⑤——判定一項歸屬所引的那一句界定句算不算數。」
- Q15「受測：「不得另定間距」射程判定表的第二列與第三列——判定某段距離的管轄該填在哪裡。」

**第二項世界平面事實（決定處置範圍，非推測）**：r620 與 r621 的**子問**逐字為

> 依 7.2(3-2)②，左圖與右圖這一組，本款給出的判定是什麼？

受測款次的完整識別符**不只出現在該指定行**，也出現在子問。只把 1.1 那一行外移，這兩輪的通路仍開著——處置必須及於題目檔全文，不能只及於那一行。

**第三項世界平面事實**：機械檢查確實看過那一行。`r621/check.py` 的題幹取法為
`stem = open(q/Q01.md).read(); stem = stem.split("## 作答格式")[0]`，即**包含受測指定行**。該行仍通過 §7 第 11 項（class (iii) 命中 0），成因有二：受測動作是**改寫過的措辭**，與條文原句無 5 字以上逐字共用子字串，4.2 的位元組比對抓不到；受測款次編號屬 4.6 第 (i) 類「指涉性字串——用來指出受測款次的位置，不提供答案」，依現行條文判**綠**。

### 實驗發現

**已驗證的事實**：

1. 洩題存在於 `0.1.0-W3-616`、`618`、`620`、`621` 四輪，**全部題目、全部輪次**，共 17 ＋ 3 ＋ 1 ＋ 1 ＝ 22 題、66 份作答。
2. `0.1.0-W3-623` 是本線**唯一未洩題的一輪**，票面所記的「刻意偏離」與實體檔案一致（PM 第 4 問確認成立）。
3. 規格前的 `0.1.0-W3-417` 系列（含 `0.1.0-W3-550` 的重測）**不經由本通路**，該線的「依據句引條文」讀數不因本案降級。
4. 洩題**不是決定性的**：r616 的 Q02 在題目檔已寫明「受測：7.2(3-2)①乙——判定一份以乙寫成的兩端敘述有沒有完成本步」的情況下，仍有一輪（Q02-r3）判定欄正確而**受測款次零套用**，讀數為引用 2/3、套用 2/3。被告知款次與動作之後仍未套用者存在，故低讀數在洩題下仍成立。
5. 1.7 遮蔽推導**不經由本通路**被污染：執行該關的判定者，其 brief 本來就由派發者夾帶受測款次與三個登載項（規格 1.7 brief 必備項第 1 條明文要求，實體佐證 `r621/judge/brief-a.md`「受測款次：7.2(3-2)②」）。該關被遮的是條文**正文**，不是款次識別符；題目檔多寫一行不改變該關的輸入。

**仍未驗證的假設**：

- 洩題對 3/3 這類高讀數的抬高幅度不可量測。量它需要同題的未洩題對照輪，而拋棄式題庫不保存題目、不做跨輪比較（規格附錄已捨棄歷輪對照）。本票因此不量幅度，改以方向論證處理降級（見 Solution 第二節）。
- 「洩題與未洩題各一輪」的唯一可比點是 r623（未洩題、計分 2/3）與 r616～621（洩題、除 Q02／Q15 外皆 3/3），樣本數 1，不構成對照組，不得據以推論幅度。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-623 | chars: 365 -->

### Task Reference
- 0.1.0-W3-623 what: 補測 7.2(3-2)①乙 窮盡要求句的讀數（以它為受測款次另起一輪）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-623 why: 7.2(3-2)①乙 的窮盡要求句自 0.1.0-W3-620 的 C1 落地後讀數零觀測，已連續兩輪：W3-620 的覆蓋題受測 ②，W3-621 的覆蓋題同樣受測 ②。W3-621 不補測的成因是規格 2.2（同輪題目題材零交集）——要測 ①乙 的題必然與該輪 Q01 共用乙寫法欄位的題材詞，同批出即違反 2.2；且 ①乙 本輪無改動，無覆蓋題身分。依 L4，零觀測不得記為沒發生，而每多測一批都會讓人更相信它不存在。拋棄式題庫下，以 ①乙 為受測款次另起一輪即無題材衝突。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] ①乙 窮盡要求句的三輪讀數已產出（引用與套用兩個數字分開計）  # from 0.1.0-W3-623（來源已完成，結論見 Solution）
- [ref] [x] 該讀數是否支持「C1' 落地後 ①乙 仍被套用」已明寫；不支持時依 1.7 判讀決定是否回 Step 1  # from 0.1.0-W3-623（來源已完成，結論見 Solution）
