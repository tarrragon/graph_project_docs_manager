---
id: 0.1.0-W1-016
title: 建立 全域錯誤攔截三層與單一錯誤出口
type: IMP
status: pending
version: 0.1.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W1-013
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 實作類任務
  final_decision: 裁決已定產物形態，建 IMP
  rationale: W1-013 已完成分析與載體決定，本票只做接線
who:
  current: parsley-flutter-developer
  history: {}
what: 建立 全域錯誤攔截三層與錯誤轉畫面態的接線
when: W1-013 裁決後、七張畫面票開工前
where:
  layer: 待定義
  files:
  - lib/main.dart
  - lib/workspace/workspace_repository.dart
why: 'W1-013 裁決：structured log 維持延後，但全域錯誤處理三層不在延後範圍，須立即建立。

  實測 lib/ 五個檔案中 FlutterError.onError、PlatformDispatcher.instance.onError、

  runZonedGuarded 三者皆 0 命中——全域攔截完全缺席。

  難在缺席時的失敗形態沒有徵兆：未捕捉的異常會讓畫面停在半渲染狀態，不崩潰、不報錯、

  不留痕跡，使用者只看到一個不動的畫面。本 App 要執行他人專案的 doc CLI、解析他人的

  markdown、存取任意目錄，這三處都會拋例外且都發生在使用者的機器上。

  載體已定為畫面而非檔案（見裁決三）：攔截到的異常轉為 SPEC-001 既有的「阻擋狀態」或

  「損壞標記」元件，不寫 log 檔。因此本票的產物是接線，不是日誌系統。'
how:
  task_type: Implementation
  strategy: 三層各自接上：FlutterError.onError 接框架層、PlatformDispatcher.instance.onError 接平台層、runZonedGuarded 包 runApp 接非同步層。三者匯流至同一個錯誤呈現入口，該入口決定顯示阻擋狀態或損壞標記。既有的 1 個 catch 一併檢視
acceptance:
- '[ ] 三層攔截皆已接線且各有一個測試以人為拋出的例外驗證其被攔截並轉為畫面錯誤態；lib/ 下無空 catch，靜默者附註解說明原因'
- '[ ] [ ] 單一錯誤出口的介面已定義（接收什麼、暴露什麼），但不含任何畫面呈現實作——呈現屬各畫面票'
- '[ ] [ ] lib/ 下無空 catch；靜默者附註解說明原因'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-08-27'
updated: '2026-08-27'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

建立 全域錯誤攔截三層與錯誤轉畫面態的接線

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-013 | chars: 541 -->

### Task Reference
- 0.1.0-W1-013 what: 裁決 執行期 log 的寫與不寫

### Rationale Chain
- 0.1.0-W1-013 why: foundation-design 五維度盤點判定可觀測性為「待定：無判準」——不是「無」，兩者不可混用。
衝突實況：.claude/rules/core/observability-rules.md 規則 4 要求關鍵流程有啟動／錯誤／關閉三階段
log，規則 2 要求統一 log 入口（本專案指定 dart:developer 的 developer.log）；而 docs/tech-decisions.md
Stage 5 明示不寫執行期 log。兩份皆為現行有效文件，無第三份裁決，lib/ 下實測零 log。
難在這不是選一邊就好：本 App 要執行專案內的 doc CLI、要解析他人 repo 的 markdown，
失敗形態多且都發生在使用者的機器上；但它又是單機桌面工具，沒有伺服器可收 log。
「寫到哪裡、誰會看」必須連同「寫不寫」一起裁決，否則會產出無人讀取的檔案。

### Related Files
- docs/tech-decisions.md  # from 0.1.0-W1-013
- CLAUDE.md  # from 0.1.0-W1-013

### Source Acceptance (reference)
- [ref] [ ] tech-decisions 有一段裁決記錄含載體決定；CLAUDE.md §6 待決清單移除該項；決定為寫 log 時 observability-rules 的三階段要求有對應實作票  # from 0.1.0-W1-013
