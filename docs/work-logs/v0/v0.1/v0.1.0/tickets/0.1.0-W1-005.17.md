---
id: 0.1.0-W1-005.17
title: 建立 ProjectSwitcherEntry（SPEC-004 4.8）
type: IMP
status: in_progress
version: 0.1.0
wave: 1
priority: P0
parent_id: 0.1.0-W1-005
children: []
blockedBy:
- 0.1.0-W1-005.14
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W1-005 傘票 Wave C
  final_decision: 一票一元件／容器
  rationale: acceptance 投影單位即元件；wave 為排程單位
who:
  current: parsley-flutter-developer
  history: {}
what: 建立元件 ProjectSwitcherEntry（SPEC-004 4.8）
when: Wave C；前置見 blockedBy
where:
  layer: Presentation
  files:
  - lib/components/
  - test/unit/components/
why: SPEC-004 條目 4.8 的契約已齊（十一欄位），依 component-contract-design skill〈派發語言〉一票一元件；實作只依契約不臨場設計。
how:
  task_type: Implementation
  strategy: 讀 SPEC-004 §4.0 通用約定與條目 4.8（容器另讀 ）；依契約實作於 lib/components/ 並自 barrel 匯出；測試依測試契約子節；契約值實測不成立時停手改契約條目（記變更歷史）再改實作；Phase 4 結論寫 Solution。
acceptance:
- '[x] 符合 SPEC-004 條目 4.8 ProjectSwitcherEntry 的十一欄位'
- '[x] 測試契約：一支 widget test 逐一渲染全部變體與狀態，kMinWindowSize 與 kDesignSize 不溢位，最長測試文案依 §4.0.4 TestCopy'
- '[x] token 與 i18n key 引用非硬編碼，無障礙朗讀標籤與焦點路徑有測試'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-02T22:08:16'
completed_at: null
created: '2026-09-02'
updated: '2026-09-02'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

建立元件 ProjectSwitcherEntry（SPEC-004 4.8）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因
無問題根因，本票為新元件建立（IMP，SPEC-004 條目 4.8 契約已齊）。

### 影響範圍
- `lib/components/project_switcher_entry.dart`（新增）
- `lib/components/components.dart`（新增一行 export）
- `test/unit/components/project_switcher_entry_test.dart`（新增）

### 相關 Error Pattern
無相關已知錯誤模式（component-contract-design skill 已涵蓋一票一元件流程）。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
依 SPEC-004 4.8 十一欄位契約實作 `ProjectSwitcherEntry`（StatefulWidget，持有 FocusNode 以承載 §4.0.1 焦點裝飾）：資料夾圖示（`AppColors.accent`）+ 專案名（`AppFontSize.body` 半粗、`textTitle`，單行截斷）+ 展開箭頭（`textDisabled`，純裝飾 `ExcludeSemantics`，展開態由 `Semantics.expanded` 承載）。`projectName` 為 `null` 時顯示元件預設 key `projectSwitcherEntryLabel`。鍵盤 Enter/Space 走 `Shortcuts`+`Actions`（沿用 `ExpanderIcon` 既有模式），點選走 `InkWell`。高固定為 `LayoutSize.hitTargetMin`，圓角 `Radius.md`，內距/圖示間距 `Space.sm`，皆為 token 引用、零裸值。barrel `lib/components/components.dart` 依既有慣例插入一行 `export`，未重排既有行。

契約無需改動——十一欄位、狀態矩陣（collapsed/expanded，無 disabled）、尺寸契約、無障礙要求皆可直接依契約落地，未發現契約與 Flutter 實作衝突之處。

### 自檢結果
- A1 三明示：本節理由/做法/驗證方式齊備。
- A2 資訊優先序：先述實作核心（元件結構），後述細節（token 引用），符合順序。
- A3 負向對比：無新增禁令，無需正向錨點。
- 禁用字/簡體字：掃描本節內容無命中。
- Schema 結構：Problem Analysis / Solution / Test Results 三節皆已填寫，Solution 含本自檢子章節。

### Phase 4 結論
重構需要：否。技術債務：無發現。程式碼品質：A（遵循既有 `AppButton`/`ExpanderIcon` 的焦點裝飾與鍵盤觸發模式，token 零裸值，`dart analyze` 對新檔 0 issue）。結論：本元件僅 87 行、單一職責（顯示 + 回呼），與既有元件檔複雜度相當，無需重構。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
- `fvm flutter test test/unit/components/` → `All tests passed!`（519 tests，含新增 23 則於 `project_switcher_entry_test.dart`）
- `fvm dart analyze lib test` → 3 issues（皆為 `expander_icon_test.dart` 既有 `hasFlag` deprecated info，不在本票範圍；本票新檔 0 issue）

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-005.14 | chars: 314 -->

### Task Reference
- 0.1.0-W1-005.14 what: 建立容器 Section（SPEC-004 4.41、5.15）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W1-005.14 why: SPEC-004 條目 4.41 的契約已齊（十一欄位），依 component-contract-design skill〈派發語言〉一票一元件；實作只依契約不臨場設計。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 符合 SPEC-004 條目 4.41 Section 的十一欄位  # from 0.1.0-W1-005.14（來源已完成，結論見 Solution）
- [ref] [x] 符合 SPEC-004 條目 5.15 排列不變式三項（不重疊／最小間距／空間不足策略），各有對應測試斷言  # from 0.1.0-W1-005.14（來源已完成，結論見 Solution）
- [ref] [x] 測試契約：一支 widget test 逐一渲染全部變體與狀態並含子件數量上限情境，kMinWindowSize 與 kDesignSize 不溢位，最長測試文案依 §4.0.4 TestCopy  # from 0.1.0-W1-005.14（來源已完成，結論見 Solution）
