---
id: 0.1.0-W3-583
title: 出題並量測 doc 線第五列分支判斷的四格批次（DOC-19d 與 19c 同批、含 19a/19b/20a）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-588
- 0.1.0-W3-589
- 0.1.0-W3-590
- 0.1.0-W3-591
- 0.1.0-W3-592
source_ticket: 0.1.0-W3-574
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-533 改文、W3-534/539 改門檻與答案鍵、W3-545 判定 19c(a) 歸零、W3-538 備料 19d、W3-574 落檔完成
  final_decision: 開 ANA 票跑 doc 下一批四格量測
  rationale: doc 線五張前置票已全部完成，且 19c(a) 單題已無鑑別力，不重測則第五列改動的效果無從判斷
who:
  current: w3-583-docb21
  history: {}
what: 出題並量測 doc 線第五列分支判斷的四格批次（DOC-19d 與 19c 同批、含 19a/19b/20a）
when: W3-574 落檔後、W3-417 閘門重新確認前
where:
  layer: 待定義
  files:
  - .claude/methodologies/component-library-bidirectional-constraint-methodology.md
why: W3-533 改第五列為條件—值兩分支後，doc 線尚未重測。19c(a) 已判定鑑別力歸零（僅測得「把命中那一支的值輸出完整」），分支判斷改由 W3-538 備料的 DOC-19d 與 19c(a) 併看四格承擔。W3-574 已把同構比對表、四格判讀表、題序與計分紀律落檔於 w574-doc-manifest.md，須逐字併入本批 manifest。
how:
  task_type: Analysis
  strategy: 由新 HEAD 重建節錄（W3-550 實測：不重建會量到改檔前條文而外觀像改了沒用）→ 逐字併入 w574-doc-manifest.md 四節 → 19d 置於 19c 之前且中間隔一題非方法論題 → 每題三輪 haiku → 兩名獨立判定者先由條文推導再比對 key → 四格判讀與逐輪帶入檢查
acceptance:
- '[ ] 節錄由新 HEAD 重建並以 grep 佐證含第五列兩分支措辭'
- '[ ] w574-doc-manifest.md 的同構比對／四格／題序／計分紀律四節已逐字併入本批 manifest'
- '[ ] 19d 與 19c(a) 的四格落點已判定，並據此回答讀者是否在做分支判斷'
- '[ ] 各題逐輪依據句原文與所引列次已記錄，計分以依據句落在哪一列為準而非結論句'
- '[ ] DOC-27a 續任零變數對照組的位移已記錄，並據以給出本批幅度'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-17T16:17:04'
completed_at: null
created: '2026-09-17'
updated: '2026-09-17'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

出題並量測 doc 線第五列分支判斷的四格批次（DOC-19d 與 19c 同批、含 19a/19b/20a）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### WRAP 四階段

- **W（Widen）**：批次代號有兩個候選。候選一沿用 `b16` 標籤重建節錄檔——否決，會覆寫 b16 的節錄檔本體，使 b16 的歷輪讀數不可複驗，且 `0.1.0-W3-582` 正在追蹤的 check1 現象會被抹掉。候選二配發新代號 `b21`（doc 線上一批為 b16，b17 至 b20 由 ux 線佔用，b21 未被任何檔案使用，已 `grep` 確認）——採用。題目組成亦有兩個候選：只出 19d + 19c 兩題，或連同 19a／19b／20a 與零變數對照 27a 同批。前者省成本但無幅度量尺、且 19d 與 19c 之間無非方法論題可隔——依 `w574-doc-manifest.md`〈題序與帶入方向〉的間隔要求即不可行，故採後者。
- **R（Reality Test）**：本票的重現實驗即量測本身，記於〈重現實驗結果〉。第一步先驗證節錄檔確由新 HEAD 重建（`0.1.0-W3-550` 實測：不重建會量到改檔前條文，失敗方向是「看起來改了沒用」），佐證為 `grep -cF` 對第五列兩分支措辭各命中 1，並附原文行。
- **A（Attain distance）**：機會成本是 `0.1.0-W3-417` 閘門重新確認順延一次。但 19c(a) 的鑑別維度已降級、19d 尚無讀數，不跑本批則第五列改寫的效果無任何資料可判。
- **P（Prepare to be wrong）**：最可能的失敗是**四格讀數在帶入檢查後全數作廢，本批等於白跑**。防護：題序依既有處置（19d 在前、中隔非方法論題 27a），並要求兩位判定者逐輪引出兩題原文而非只填格。**此預想部分成真**——三輪中兩輪被判帶入，且兩位判定者對「哪一輪沒被帶入」判定相反；讀數因此不足以支撐分支判別的任一方向結論，但逐輪原文仍留下可用的形態讀數（見 Solution）。

### 問題根因

`0.1.0-W3-533` 把射程判定表第五列管轄欄由單值改為條件—值兩分支後，doc 線未曾重測。`0.1.0-W3-545` 判定 DOC-19c(a) 對「逐列排除」與「自行判定有無容器」兩維度的鑑別力皆已歸零（題幹可純字面對上第五列並抄出分支二三成分），故分支判斷的讀數必須改由 `0.1.0-W3-538` 備料的 DOC-19d（分支一）與 19c(a)（分支二）併看四格承擔，且兩題須同批出題、餵入同一份節錄檔。

### 影響範圍

| 類別 | 項目 |
|---|---|
| 新增（皆在 scratchpad，untracked） | `tools/specs-b21-doc.json`、`excerpt/*b21-*.md`（5 份）、`q/W3-417-b21-doc-1.md`、`key/W3-417-b21-doc-1.md`、`b21-doc-manifest.md`、`a/W3-417-b21-doc-1-r{1,2,3}.md`、`judge-b21-doc-j{1,2}.md` 與其 `-derivation.md`、`tools/b21_doc_build_q.py`／`b21_doc_key_copy.py`／`b21_doc_manifest.py`／`header_b21.md`／`footer_b21.md`／`addenda_b21.json`／`manifest_head_b21.md`／`manifest_tail_b21.md`／`probe_answer_format_b21.md`／`judge_brief_b21.md` |
| 未動 | 條文（`.claude/`）、既有題目檔與答案鍵（b16／b8／w538 三份來源）、既有節錄檔、`tools/specs-b16-doc.json` 等既有規格——本批只新增 |
| 已驗證的事實 | 節錄檔由新 HEAD 重建且含第五列兩分支措辭；六題題幹對來源檔 sha256 全等；47 條引文對 b21 節錄檔與 HEAD 兩道比對皆 0 失敗；四節逐字併入的 sha256 兩側相同 |
| 仍未驗證的假設 | 無。本票不推論條文是否有效，只產出讀數與形態 |

### 相關 Error Pattern

`PC-165`（測試綠燈不等於 runtime 正確）在本批的對應形態為「題目全綠不等於讀者在做分支判斷」——19d 三輪 3/3 但逐列排除 0/3 寫出，計分與所測維度分離。`test-assertion-design-rules` 規則 E1（明示旗標必須有對照測試）是 `0.1.0-W3-538` 增設 19d 的依據；本批是該對照配對的第一個讀數，且讀數顯示**配對本身尚不足以產生結論**（帶入與四格描述兩個缺口，已各自成票）。

## 重現實驗結果
### 實驗方法

六題探針批次（`b21-doc-1`），每題三輪 haiku 作答，兩名獨立判定者各自先由條文推導再與答案鍵比對、分別產判定書。題序 DOC-19d → DOC-27a → DOC-19c → DOC-19a → DOC-19b → DOC-20a（19d 在 19c 之前，中隔非方法論題 27a）。

### 實驗執行

**第一步（先做且佐證）：節錄檔由新 HEAD 重建。** `python3 tools/build_excerpts.py specs-b21-doc.json`，RC=0，五份節錄檔全部產出（kept 26／16／12／35／35）。對 `excerpt/methodologies--component-library-bidirectional-constraint-methodology--b21-容器亦為元件.md` 的機械佐證：

| 檢查字串 | `grep -cF` |
|---|---|
| `共同上層是一個容器元件者` | **1** |
| `找不到這樣的容器元件` | **1** |

節錄檔內第五列原文（逐字）：

> `| 皆非上列四種組合（如兩個容器之間） | 共同上層是一個容器元件者，由該層承載；找不到這樣的容器元件（兩端直接放在頁面上）者，該處缺一個容器元件，須先補上該容器元件，由它承載。兩端各自的組合規則均不得另定這段間距。 | 本節判別問句段 |`

兩分支措辭在 b16 節錄檔內零命中、在 b21 節錄檔內各命中一次，故本批量到的是改寫後的條文。節錄檔檔頭記 HEAD `29a8d64b`，其後 HEAD 前進至 `84a85dcb`（並行 session 的票面提交），`git log 29a8d64b..HEAD -- <方法論檔> <範本檔>` 為**空集**。

**第二步：四節逐字併入。** `python3 tools/b21_doc_manifest.py`，RC=0，`carried sections=4 mismatches=0`，四節兩側 sha256 逐節相同（同構比對 `ee9cfc753d5eab94`／併看四格 `a40352a43c9651c6`／題序與帶入方向 `111758930b5b8334`／計分紀律 `c22e81c6381ff0c5`）。程式複製而非重打，是「逐字」可被核對的原因。

**第三步：題目與答案鍵。** `b21_doc_build_q.py` RC=0（`carried stems=6 intended_edits=0 unexpected_mismatches=0`，切點 A／B sha256 對來源檔全等）；`b21_doc_key_copy.py` RC=0（`carried blocks=6 total_quotes=47 quote_mismatches=0`，本批新增條目不含任何引文，故引文集合與來源批完全相同）。

**第四步：器材檢查。** `selfcheck.py b21-doc-1` RC=0（`questions=6 quotes=47 failures=0`）；`verify_head.py b21-doc-1` RC=0（`quotes=47`、HEAD `84a85dcb`、`failures=0`）。

**第五步：三輪 + 兩判定者。** 三輪作答與兩份判定書＋兩份 derivation 全數產出。

### 實驗發現（已驗證的事實）

**FAIL 以差集判讀，兩類既有漂移逐一對位：**

| 漂移類型 | 來源票 | 本批是否命中 |
|---|---|---|
| 超前型（key 引文對自己批次節錄檔超前，check1 方向） | `0.1.0-W3-582` | **零命中**（本批 47 條引文全過） |
| 落後型（key 引文對 HEAD 落後，verify_head 方向） | `0.1.0-W3-557` | **零命中**（`failures=0`） |
| check2 單一 `[HIT]` | `0.1.0-W3-557` 範圍 | 命中（既有），與 `selfcheck-b11/b12/b13/b16/b19-ux/w538.log` 逐字相同，不計入 `failures` |

**b16 的兩則 check1 FAIL 仍在，且不應消失**：`selfcheck.py` 以批次代號定位節錄檔，b16 的 key 比對 `b16-` 標籤節錄檔，本批重建的是 `b21-` 標籤。**同一組字串在 b16 節錄檔 FAIL、在 b21 節錄檔 PASS，本身即節錄檔確由新 HEAD 重建的第二道佐證。** `w574-doc-manifest.md`〈自然消解時點〉要求下批「確認消失」，該預期建立在「下批沿用同一標籤重建」的隱含前提上，與既有的新代號慣例衝突——已成票 `0.1.0-W3-591`。

**計分（兩位判定者逐題一致）**：19d 3/3、27a 3/3、19c 0/3、19a 3/3、19b 3/3、20a 2/3。兩位的自行推導與答案鍵全數一致（j1 明寫十二子問零分歧），故計分差異不來自判定者對條文的理解。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 分析結論

### 四格落點與據以回答「讀者是否在做分支判斷」

| 輪 | j1 | j2 |
|---|---|---|
| r1 | **第三格**（19d 通過 + 19c(a) 不通過） | **不可判讀**（帶入） |
| r2 | **不可判讀**（帶入） | **不可判讀**（帶入） |
| r3 | **不可判讀**（帶入） | **第三格**（但明寫該格判讀語在 r3 不成立） |

**結論：本批不得宣稱測到分支判別成立，亦不得宣稱分支判別未發生。** 三項依據：

1. **第一格（兩題皆通過）零命中**——19c(a) 三輪全不通過。
2. **可判讀的輪至多一輪，且兩位判定者指的不是同一輪**（j1 取 r1、j2 取 r3），另兩輪作廢。兩人算出的有效樣本完全不重疊。
3. **唯一可判讀的那一輪落第三格，而該格的判讀語在實況上不成立**——j2 逐字說明：r3 對兩題輸出的是相反分支（19d 分支一、19c 分支二的核心斷言「該處缺一個容器元件」），不是該格所述的單一輸出模板；其失分在承載者選言含「頁面根堆疊」。

### 各題逐輪依據句原文與所引列次（核心讀數）

完整逐輪表在 `judge-b21-doc-j1.md` 第三章與 `judge-b21-doc-j2.md` 第三章。摘要：

| 題號 | 依據句落點 | 第一列命中 |
|---|---|---|
| DOC-19d | 三輪皆**第五列**（r1 僅截分支一，r2／r3 取全行）＋表外判別問句段 | **0/3** |
| DOC-19c | 三輪 (a) 皆**第五列**；(b) 落表外綜上段／排列不變式表後句，r3 (b) 另取第三列 | **0/3**（引排列不變式表後句不等於引第一列，沿 b13 j1 的區分） |
| DOC-19a | (a)(b) 表外範本引言＋**第二列**；(c) r2／r3 取**第一列**、r1 僅範本引言 | — |
| DOC-19b | (a) 三輪皆**第四列**；(b) 三輪皆**第三列** | 0/3 |
| DOC-20a | (a) 三輪表外範本引言；(b) r1／r3 **第二列**、r2 誤取**第三列** | — |
| DOC-27a | 餵入不含射程表，全部表外；計數規則取用位置 **甲 3／乙 0／丙 0** | 不適用 |

### DOC-27a 續任零變數對照組的位移與本批幅度

| 項目 | 值 |
|---|---|
| 題幹凍結鏈 | b10 → b11 → b12 → b13 → b16 → b21，sha256 `0c2c3e8fe0958a28` 六批逐字全等 |
| 範本檔變動 | `f44a42ab..HEAD` 為空集（本批複驗），續任成立 |
| b16 讀數 | j1 1/3、j2 1/3（取自 `judge-b16-doc-j1.md`／`judge-b16-doc-j2.md`，未取答案鍵〈歷輪對照〉欄） |
| b21 讀數 | j1 3/3、j2 3/3 |
| **位移** | **+2** |
| **本批幅度** | **2 輪**（兩位判定者一致） |

**限制（兩位判定者皆明寫）**：本批零變數對照只有 27a 一題，**只有主量尺、無合議值可交叉**（b16 用來取合議的 07c／28b／28a 不在本批）。此幅度無第二個獨立來源可核對。19a／19b 位移 0 在幅度之內且受上界截斷，**不足以支撐任何關於條文的推論**；19c 與 20a 不可比。

### 帶入檢查結果

| 輪 | j1 | j2 |
|---|---|---|
| r1 | **否**（截取範圍隨各自判定而異，判別問句只在 19d） | **是**，19d → 19c(a)（同一句式模板；兩題都附加依據句中未被引用的同一補語「排列不變式『最小間距』」） |
| r2 | **是**，19d → 19c(a)（引句逐字全等含 19d 用不到的末句，屬整行照搬） | **是**，19d → 19c(a) |
| r3 | **是**，19d → 19c(a)（引句逐字全等且截點相同） | **否**（引句重疊屬結構性，兩題輸出分支相反，無模板沿用） |

判準「依據句是否互相複製（引句組合或推理措辭）」把兩個獨立訊號併在一句裡且未給門檻，在同一組資料上產生相反判定——已成票 `0.1.0-W3-590`。

### 逐列排除是否實際發生（兩位判定者一致）

| 觀測點 | 讀數 |
|---|---|
| 19d 寫出「排除第一列那一句」 | **0/3** |
| 19d 依據句誤命中第一列 | **0/3** |
| 19d 寫出排除第二至四列的理由 | **0/3** |
| 19c(a) 寫出任何逐列排除的理由 | **0/3** |

三輪 19d 皆直接命中第五列並落分支一，**未誤命中第一列，也未寫出排除它的理由**。`0.1.0-W3-545` 指派給 19d 的「逐列排除」維度，本批**未觀測到它實際發生**——三輪的通過皆由直接命中第五列產生。19d 題幹不含任何在五列中只出現於一列的實詞，讀者無字面通路可用，故此形態即答案鍵第 5 項所稱值得記錄者，三輪全中。

### DOC-19c 0/3 的兩種失分形態

| 形態 | 輪 | 實況 |
|---|---|---|
| (i) 存在性事實被退回為條件子句 | r1、r2 | 首要結論落分支一，補容器僅以「若缺少此容器」「若兩容器直接放在畫面上」附掛，三成分中的「該處缺一個容器元件」未被斷言 |
| (ii) 頁面根堆疊被當作合法承載者 | r3 | 已斷言「該處缺一個容器元件」，三成分字面大致齊備；失分在牴觸性補述——把條文指名為「缺一個容器元件」的那個狀態當成承載者 |

形態 (ii) 與 `0.1.0-W3-532` 診斷的舊形態同形，而該票正是 `0.1.0-W3-533` 改寫的動因；改寫後仍復發一次，且這次讀者**已引用判別問句**。已成票 `0.1.0-W3-588`。

### 候選方案（下一步該怎麼走）

| 案 | 內容 | 利 | 弊 |
|---|---|---|---|
| **A（採用）** | 先修量測器材（四格表描述 `0.1.0-W3-589`、帶入判準 `0.1.0-W3-590`），再依 `0.1.0-W3-588` 分析條文處置，最後重測 | 本批的四格讀數作廢主因是器材而非條文；不先修，下一批仍會得到不可判讀的樣本 | 條文缺口的修復順延一批 |
| B | 直接依 19c 的 0/3 改條文，不修器材 | 快 | 0/3 的兩種形態尚未歸因（`0.1.0-W3-588` 未做），且四格無有效樣本，改了無從驗證 |
| C | 只重測、不修器材也不改條文 | 成本最低 | 帶入判準不變則有效樣本仍不可重現，等於重跑一次同樣的不可判讀 |

**採 A。** 理由：本批已證明配對本身可執行（生效條件兩項皆滿足、計分兩位一致、器材四道檢查全過），失效的是判讀層——四格表的格描述與帶入判準。這兩項是 `0.1.0-W3-589`／`0.1.0-W3-590` 的標的，且與條文分析 `0.1.0-W3-588` 可並行。

### Spawn 規劃與落地確認

| # | 發現 | 落地 |
|---|---|---|
| 1 | 第五列改寫後 19c(a) 仍 0/3，兩種失分形態（存在性事實條件子句化／頁面根堆疊當承載者），後者與 `0.1.0-W3-532` 同形復發 | **`0.1.0-W3-588`**（ANA，P0） |
| 2 | 四格判讀表第三格的括號描述與判讀語無法涵蓋 r3 的實況（兩題輸出相反分支、對照題因牴觸性補述失分） | **`0.1.0-W3-589`**（DOC，P1） |
| 3 | 帶入檢查判準不可操作：兩位判定者對同一組資料判出的「未被帶入」輪次完全不重疊 | **`0.1.0-W3-590`**（ANA，P1） |
| 4 | `w574-doc-manifest.md`〈自然消解時點〉的消解條件失準：配發新批次代號時舊批 check1 FAIL 恆存且不是訊號 | **`0.1.0-W3-591`**（DOC，P2） |
| 5 | DOC-20a (b) 第二／三列兩端混同在 b8 後首次復發，同輪 (a)(b) 自相矛盾，且非第五列改寫的外溢 | **`0.1.0-W3-592`**（ANA，P2） |

五項發現全數已建票，`spawned_tickets` 已由 CLI 回填雙向血緣。**無以「進階」「後續再說」省略的項目。**

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-574 | chars: 689 -->

### Task Reference
- 0.1.0-W3-574 what: 下批 doc 出題前：(1) 在答案鍵 DOC-19c 與下批 manifest 同構比對節落檔 W3-545 的擴充迴避清單與逐題命中表；(2) 把 (a) 的鑑別維度由逐列排除降級記為把命中那一支的值輸出完整，並記明 (a) 對逐列排除與自行判定有無容器兩維度皆已無鑑別力；(3) 為 W3-538 的分支一對照子問加一條題幹用字約束：不得逐字使用「共同上層」，須以「同放在一個 X 容器元件之內」一類措辭，讓讀者自行對映到分支一的條件（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-574 why: W3-545 複驗結論：W3-533 把「容器元件」帶進第五列後，該詞成為射程判定表內第五列的唯一字面，與 19c 前言「兩個容器元件」及 (a)「畫面上沒有其他容器元件包住它們」逐字共用；疊加既有的「兩個容器」對「（如兩個容器之間）」通路，(a) 可純靠字面匹配命中第五列並抄出分支二三成分而完全不走逐列排除，b16 的 r2 已用自己的話寫出這條路。題幹改不掉（分支條件是存在性陳述，任何陳述該事實的措辭都會用到容器類詞；且前言與括號例示的通路不在 (a) 那一句內），故不改題幹改記載。W3-538 若以「共同上層是一個容器元件」入題，其子問亦逐字對上分支一，配對仍不測分支判斷，E1 缺口以字面形態復發（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 下批 manifest 同構比對節已含 W3-545 的擴充迴避清單與 19a/19b/19c/20a 逐題命中表  # from 0.1.0-W3-574（來源已完成，結論見 Solution）
- [ref] [x] 答案鍵 DOC-19c 已記明 (a) 的鑑別維度降級與已知字面通路，且不再宣稱 (a) 測逐列排除  # from 0.1.0-W3-574（來源已完成，結論見 Solution）
- [ref] [x] W3-538 的分支一對照子問題幹逐字不含「共同上層」，並經機械驗證記入 manifest  # from 0.1.0-W3-574（來源已完成，結論見 Solution）
