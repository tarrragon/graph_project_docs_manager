---
id: 0.1.0-W3-501.2
title: 改切 DOC-28a 探針題的解耦來源並保留原題為對照
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.1.0-W3-501
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: general-purpose
  history: {}
what: 下批 doc 探針新增 DOC-28a 改切版：值的解耦來源由「使用者於其他畫面設定」改為背景流程或另一元件寫入，題幹不出現「使用者」與「設定」二詞；原題同批保留為對照。判定時分別記錄兩版的依據句，比對讀者是取用步驟 1 (1) 的排除子句語意，還是字面匹配題幹用詞
when: 下一批 doc 探針出題時
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-417.md
why: 步驟 1 排除子句「可由使用者或另一流程另行設定」與現行 28a 題幹字面必然重疊，該子句在新條文中成為必經路徑，命中機率由 b11 的 0/3 顯著上升；不改切則無法分辨理解與字面匹配（j1 §五.2、j2 §8.1 同向要求）
how:
  task_type: Analysis
  strategy: 改切版與原題同批施測，兩版依據句對照
acceptance:
- '[ ] 改切版題幹經 grep 確認不含「使用者」與「設定」'
- '[ ] 改切版與原題同批施測且各 3 輪作答'
- '[ ] 判定書逐輪記錄兩版依據句並給出「語意取用 vs 字面匹配」的分離結論'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-16T12:58:04'
completed_at: null
created: '2026-09-16'
updated: '2026-09-16'
protocol_version: '2.0'
chain: {}
---

# Execution Log

## Task Summary

下批 doc 探針新增 DOC-28a 改切版：值的解耦來源由「使用者於其他畫面設定」改為背景流程或另一元件寫入，題幹不出現「使用者」與「設定」二詞；原題同批保留為對照。判定時分別記錄兩版的依據句，比對讀者是取用步驟 1 (1) 的排除子句語意，還是字面匹配題幹用詞

---

## Problem Analysis
### 問題根因

DOC-28a 的題幹（b10 起凍結）以「使用者於其他畫面把這支影片設為看完」承載解耦訊號；
W3-501.1（`f44a42ab`）把反向指引「可由使用者或另一流程另行設定」由主判準之後的獨立句
降為步驟 1 必要條件 (1) 的排除子句，該子句因此成為**必經路徑**。兩者共用「使用者」與
「設」二字，重疊由潛在（b11 實測 0/3 未觸發）轉為必然。後果：DOC-28a 本輪若翻綠，
無法分辨讀者是理解「值的來源不同」還是字面對上題幹用詞——該題的讀數失去歸因能力。

### 影響範圍

**已驗證的事實**：

- 題幹用詞機械命中（對 `q/W3-417-b12-doc-1.md` 題幹字串逐詞 count）：DOC-28a「使用者」1、
  「設」1；DOC-28b「使用者」0、「設定」0、「設」0、「流程」0、「背景」0。
- 現行條文 (1) 的排除子句逐字為「任一部分可由使用者或另一流程另行設定，或雖隨該量變化而值
  本身另有來源者，即非由同一個量換算得出。」（節錄檔 `doc--…--b12-4條目標頭與內容角色.md`）
- W3-501.1 的三處條文變動使 b11 答案鍵 37 條引文中失效 12 處／7 條，本批逐條更換。

**仍未驗證的假設**：DOC-28b 對讀者的實際效力。本票只產出題目與答案鍵，未施測；
28b 能否分離「語意取用」與「字面匹配」，要到探針作答與判定書出來才存在讀數。

### 相關 Error Pattern

查詢範圍內未命中既有 PC 編號。本題的形態是「探針題幹與受測條文共用字面，使通過不可歸因」，
屬 `test-assertion-design-rules` 規則 E1（明示旗標必須有對照測試）的同族——單邊斷言沒有
比較對象。本票的處置（同批保留原題為對照、新增零字面重疊的分離題）即該規則的套用，
不新增 error-pattern。

## 重現實驗結果
### 實驗方法

本票的重現對象不是程式缺陷，是**出題的機械可驗證性**。三條驗證線，全部以整數固定值判讀，
不讀輸出外觀：

1. **題幹凍結**：以 `**題目**：` 標籤之後起算至題區塊結束的整段文字，`strip()` 後不做其他
   正規化，逐字元比對並取 sha256，跨 b8／b10／b11／b12 四批比對。
2. **引文有效性**：`tools/selfcheck.py b12-doc-1`（引文逐字存在於該題餵入的節錄檔）與
   `tools/verify_head.py b12-doc-1`（引文逐字存在於 HEAD 原始檔），兩者忽略空白與換行。
3. **同構比對**：對題幹字串逐詞 `count`，分欄 A（條文例示）與欄 B（新增／改寫判準句的
   觸發條件），並另對 DOC-28b 跑禁用詞與三題領域名詞的重疊掃描。

節錄檔由 HEAD `bf1e9ea2` 以 `tools/build_excerpts.py specs-b12-doc.json` 重產，範例區塊剝除。

### 實驗執行

**題幹凍結（manifest 切點，標籤之後起算）**：

| 題號 | b8 | b10 | b11 | b12 | 全等 | sha256（前 16 碼） |
|---|---|---|---|---|---|---|
| DOC-07c | 有 | 有 | 有 | 有 | 是 | `366a33cfb7ef2174` |
| DOC-19c | 有 | 有 | 有 | 有 | 是 | `4a44a464029b5666` |
| DOC-27a | — | 有 | 有 | 有 | 是 | `0c2c3e8fe0958a28` |
| DOC-28a | — | 有 | 有 | 有 | 是 | `df62865c6c06b560` |
| DOC-28b | — | — | — | 有 | 新題 | `a56511e248644570` |

四個 sha256 與 `b10-doc-manifest.md`／`b11-doc-manifest.md` 登記值逐字相符。產生腳本
`tools/b12_doc_build_q.py` 另以含標籤的切點內建同一比對並在任一題不全等時 `sys.exit(1)`，
輸出 `carried stems=4 mismatches=0`，四組 hash 亦與 b11 manifest 登記值相符。

**自檢**：

```
$ python3 tools/selfcheck.py b12-doc-1
check1 b12-doc-1: questions=5 quotes=50
check2: excerpts=175 removed_lines_checked=1568
questions total=5 failures=0        （exit code 0）

$ python3 tools/verify_head.py b12-doc-1
head b12-doc-1: quotes=50
HEAD bf1e9ea2: quotes total=50 failures=0   （exit code 0）
```

selfcheck 另印一行 `[HIT]`，指向一個與本批無關的舊 tdd 節錄檔的表頭列
（`場景 | Given | When | Then`）；該行不計入 failures，且 `selfcheck-b11.log` 有逐字相同的
一行，屬既有狀態而非本批引入。

**DOC-28b 禁用詞掃描**（題幹字串逐詞 count）：

| 詞 | 28b | 28a（對照） |
|---|---|---|
| 使用者 | 0 | 1 |
| 設定 | 0 | 0 |
| 設 | 0 | 1 |
| 流程 | 0 | 0 |
| 背景 | 0 | 0 |

**DOC-28b 對既有三題的領域名詞掃描**：對 07c（書籍／評分／星／顆／平均評分／3.5）、
27a（捐款／專案卡／偏鄉／午餐／金額／人數／募得）、28a（影片／播放／環形／圓弧／看完／進度）
逐詞 grep，命中皆為 0；唯一共用的一般名詞為「填滿」，該詞在 07c 與 28a 的凍結題幹中本即
共用，且在節錄檔命中為 0，不構成字面路徑。

**引文失效掃描**：b11 答案鍵 37 條引文對 b12 節錄檔逐條比對，失效 12 處／7 條，逐條更換
（明細見 `b12-doc-manifest.md`〈答案鍵引文的更換〉與答案鍵〈出題者附記〉）。

### 實驗發現

**已驗證的事實**：

1. 四題凍結題的題幹在 b12 與前批逐字全等，07c／19c 的凍結鏈 b8→b10→b11→b12 完整，
   27a／28a 為 b10→b11→b12。本批對四題而言唯一的變數是條文（DOC-19c 連條文都未變，
   來源檔 sha256 與 b11 相同，為零變數對照組）。
2. DOC-28b 題幹不含「使用者」與「設定」，亦不含「設」「流程」「背景」，與 (1)(2) 兩條
   必要條件的措辭零複述；與 07c／27a／28a 的領域名詞零重疊。
3. 答案鍵 50 條引文全數逐字存在於餵入節錄檔與 HEAD 原始檔，兩項自檢 failures=0。

**仍未驗證的假設**：

- DOC-28b 能否真正分離「語意取用」與「字面匹配」。該題與 28a 的正解相同，若讀者對兩題
  皆答對，仍需依據句欄才能區分機制；答案鍵已把該區分寫成必記四格，但四格要有內容，
  要等探針作答與判定書。
- 題序安排（28b 置於 28a 之前）能否抑制帶入。該安排把殘餘帶入風險移到對照組 28a，
  但未消除；判定者須檢查 28a 依據句是否與 28b 雷同且未觸及「使用者」。

## Solution
### 分析結論

本票為**出題端**產出：b12-doc 批次（5 題）已產出並通過兩項機械自檢，尚未施測。

**DOC-28b（分離題）題幹**：

> 某「倉位卡」元件本身不可點擊，由兩個部分組成：左側一條水平長條，長條填滿的長度隨這個
> 倉位目前存放的箱數增加；右側並列「可供貨」，在夜間的批次作業依供應商回報的交期寫入之後
> 改為「缺貨」。以下兩問各自獨立判定。
> (a) 這個元件的「內容角色」應填幾項？各是什麼？
> (b) 撰寫者主張：「這兩個部分講的都是這個倉位的存貨狀況，所以只算一項。」這個主張是否成立？
> 請引用依據句。

**正解**：(a) 2 項：數值 + 標籤。(b) 不成立。與 DOC-28a 的正解與判定路徑完全相同，
唯一差異是解耦訊號的承載者——28a 為使用者於他處設定，28b 為夜間批次作業依供應商交期寫入。

**設計選擇：解耦訊號只換承載者，句型全同**。28b 沿用 28a 的句型骨架（「元件本身不可點擊，
由兩個部分組成：左側…；右側並列『X』，在…之後改為『Y』」），僅換題材與觸發者。理由：
兩題若在句型上不平行，讀數差異將無法歸因於解耦訊號本身，而可能來自句型難度。唯二與條文
共用的詞（「講的」「並列」）皆為 28a 凍結題幹既有用詞，沿用是對照設計的必要代價。

**被放棄選項**：
- (a) 只重出 28a、不設分離題——b11 已證明該路徑的讀數不可歸因（j1 §五.2、j2 §8.1 同向要求
  改切），續行等於再花一批次得到同樣不可用的讀數。
- (b) 改切 28a 本身、不保留原題——會斷掉 b10→b11→b12 的凍結鏈，且失去「同一讀者在兩種
  承載者下的對照」這個唯一能分離字面匹配的設計。
- (c) 題幹加註「此值不隨左側變動」一類的明示提示——該提示等於把 (1) 的排除子句抄進題幹，
  正是本票要消除的字面路徑，且 28a 無對應提示，破壞兩題的平行性。

**題序**：07c → 19c → 27a → **28b** → 28a。28b 置於 28a 之前，使殘餘的類比帶入風險落在
對照組而非受測題（若順序相反，讀者可能以字面路徑答對 28a 再類比帶入 28b，虛抬其讀數）。
代價是 28a 由第 4 題移至第 5 題，這是本批對凍結題唯一的環境變動，已於 manifest 登記，
且答案鍵要求判定者檢查 28a 依據句是否與 28b 雷同且未觸及「使用者」。

### 判定者必記觀察項（已寫入答案鍵）

| # | 觀察項 | 寫在哪 |
|---|---|---|
| 1 | **DOC-07c 首次必經 (2)**：逐輪記錄是否取用 (2)、受詞是否正確（滑回「它自己」即記受詞滑動）、對 (2) 的判定結論 | 答案鍵 DOC-07c 必記第 1 項 |
| 2 | **計數規則前移是否生效**：逐輪記錄是否取用「本欄填的是類別，同一類別只寫一次」，及取自哪一行（甲逐字引佔位符行／乙其他位置的改述／丙未取用） | 答案鍵 DOC-07c 必記第 2 項、DOC-27a 必記第 2 項第二記錄點（兩題各記一次不合併） |
| 3 | **28b／28a 的語意取用 vs 字面匹配分離**：逐輪填四格並引出兩題各自的依據句原文 | 答案鍵 DOC-28b 必記第 5 項、DOC-28a 對照側 |
| 4 | 19c 甲乙丙三格續記（零變數對照組，位移須歸因於批次環境） | 答案鍵 DOC-19c 必記第 4 項 |
| 5 | 另一邊誤學的單獨記錄（答 2 項但理由是「凡兩部分並置皆不合併」） | 答案鍵 DOC-28a 必記第 6 項，明文同樣適用 28b |
| 6 | 27a 是否回到 b10 基線（2/3）；另記作答是否仍引用已刪除的讓步子句措辭 | 答案鍵 DOC-27a 歷輪對照 |

第 1、2 項為團隊指定的首要與另一觀察項；第 3 項為本票驗收條件的載體。

### 產出

| 檔案 | 說明 |
|---|---|
| `q/W3-417-b12-doc-1.md` | 題目檔（5 題） |
| `key/W3-417-b12-doc-1.md` | 答案鍵（50 條引文、6 項必記觀察） |
| `excerpt/doc--component-library-spec-template--b12-4條目標頭與內容角色.md` | 節錄檔（HEAD bf1e9ea2 重產） |
| `excerpt/methodologies--component-library-bidirectional-constraint-methodology--b12-容器亦為元件.md` | 節錄檔（同上，本體與 b11 逐字相同） |
| `tools/specs-b12-doc.json`、`tools/b12_doc_build_q.py` | 節錄檔規格與題目檔產生腳本 |
| `selfcheck-b12.log`、`verify-head-b12.log` | 自檢日誌 |
| `b12-doc-manifest.md` | 批次 manifest |

全部位於 `/private/tmp/claude-501/-Users-mac-eric-project-graph-project-docs-manager/2ccf6d72-30b6-455c-96ea-2023fad2d11e/scratchpad/probes/`；依派發指示不 commit 到 git。

### Spawn 落地確認

本票不新增 spawn request，逐項宣告如下：

- **施測與判定（acceptance 2、3 的執行端）**：無需建 ticket——該工作屬 `0.1.0-W3-417` 主線
  既有的批次流程（每批 3 探針 × 2 判定者），由 PM 依既有節奏派發 `b12-doc-probe-r1..r3` 與
  `b12-doc-judge-1/2`，不需另立票。本票已把兩題的分離判準寫入答案鍵，派發時無額外 context 需求。
- **error-pattern**：無需建立——「探針題幹與受測條文共用字面使通過不可歸因」的形態，
  在既有 `test-assertion-design-rules` 規則 E1 的射程內（單邊斷言無比較對象），本票的處置
  即該規則的套用，另立 error-pattern 會重複既有條文。
- **範本條文的後續修文**：無需建 ticket——本批尚未施測，無讀數可據以提修法；提前建票會違反
  `decision-trigger-binding` 規則 2（trigger 必須是已存在的 ticket 而非預期中的結果）。

## Test Results
兩項機械自檢，均 `failures=0`、exit code 0（日誌：`selfcheck-b12.log`、`verify-head-b12.log`）：

```
$ python3 tools/selfcheck.py b12-doc-1
check1 b12-doc-1: questions=5 quotes=50
[HIT] check2 removed line found in tdd--doc-handoff-uc-to-gwt-conversion--種子與行為單元分工邊界.md: 場景 | Given        | When        | Then
check2: excerpts=175 removed_lines_checked=1568
questions total=5 failures=0

$ python3 tools/verify_head.py b12-doc-1
head b12-doc-1: quotes=50
HEAD bf1e9ea2: quotes total=50 failures=0
```

`[HIT]` 一行指向與本批無關的舊 tdd 節錄檔表頭列，不計入 failures，`selfcheck-b11.log`
有逐字相同的一行。

題幹全等比對（`tools/b12_doc_build_q.py` 內建，不全等即 `sys.exit(1)`）：

```
| DOC-07c | 106 | 106 | True | 1b9fa571df4ecc74 / 1b9fa571df4ecc74 |
| DOC-19c | 240 | 240 | True | bcec929e16465683 / bcec929e16465683 |
| DOC-27a | 223 | 223 | True | 4e35d9bc28710fbe / 4e35d9bc28710fbe |
| DOC-28a | 189 | 189 | True | cd6c4ac6901e9dd6 / cd6c4ac6901e9dd6 |
| DOC-28b | (new) | 192 | n/a | b4650bb43c51fbf9 |
carried stems=4 mismatches=0
```

**探針作答與判定書不在本票範圍**（派發指示：出題，不作答不判定），故無 3 輪讀數。

## NeedsContext
- **缺失項**：驗收條件 2、3 的執行端（探針施測 3 輪 × 2 版、判定書逐輪記錄）不在本票的
  派發範圍內。派發訊息明寫「本票任務（出題，不作答不判定）」，而票面 acceptance 第 2、3 項
  描述的是施測與判定的結果。兩者的射程不一致。
- **觸發位置**：票面 acceptance `[ ] 改切版與原題同批施測且各 3 輪作答` 與
  `[ ] 判定書逐輪記錄兩版依據句並給出「語意取用 vs 字面匹配」的分離結論`。
- **影響**：出題端能做到的部分已全部完成並機械驗證——兩版同批（同一題目檔，28b 為第 4 題、
  28a 為第 5 題），且分離判準已逐條寫入答案鍵成為判定者的必記項。但「各 3 輪作答」與
  「判定書給出結論」需要 probe 與 judge 代理人執行，本票無法自行滿足。故本票只勾選
  acceptance 第 1 項，第 2、3 項留白。
- **建議補料**：無需補 context——依 `0.1.0-W3-417` 主線既有節奏派發
  `b12-doc-probe-r1..r3` 與 `b12-doc-judge-1/2` 即可；答案鍵已含判定者所需的全部判準，
  無額外 context 需求。PM 可在判定書產出後回頭勾選本票 acceptance 2、3，或另立收尾票承接。
- **重派成本**：零——本票的產出（題目檔、答案鍵、節錄檔、manifest）即為該派發的完整輸入。

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-501 | chars: 373 -->

### Task Reference
- 0.1.0-W3-501 what: 評估 doc 範本〈條目標頭與內容角色〉的計數規則、可操作驗證句與辨識法反向指引的落點與形態（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-501 why: 兩位獨立判定者一致，且 j2 產出逐句取用率表：四處修文中表後綜上段 3/3 正向、驗證句補受詞 2/3 正向但不穩、計數單位句補半 2/3 取用卻淨負向（DOC-27a 由 2/3 退步 1/3）、辨識法反向指引目標題 0/3 且鄰題誤用一次（負向）。四題題幹 sha256 全等，變動全歸因於條文。同一節已連續四次補句且報酬遞減轉負；判定者明言留在引言第三段已證明無效、修法須換軸（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/doc/templates/component-library-spec-template.md  # from 0.1.0-W3-501（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三案各附涵蓋範圍變化與誤傷實驗逐題結果  # from 0.1.0-W3-501（來源已完成，結論見 Solution）
- [ref] [x] 明確回答計數單位句補半是否應回退  # from 0.1.0-W3-501（來源已完成，結論見 Solution）
- [ref] [x] 草稿措辭逐句比對既有探針題材，重疊者標明受影響題號  # from 0.1.0-W3-501（來源已完成，結論見 Solution）
