---
id: 0.1.0-W3-017
title: 審查 重構後的 component-contract-design 六檔（多輪審查）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶指示對重構後的 skill 作多輪審查
  final_decision: 建 ANA 票承載 Context Bundle，reviewer prompt 收於 30 行內
  rationale: agent-prompt-length-guard 硬擋 30 行（PC-040）；共用背景由多個 reviewer 消費，置於票面為單一權威來源避免各 prompt 複製漂移
who:
  current: rosemary-project-manager
  history: {}
what: 審查 重構後的 component-contract-design 六檔（多輪審查）
when: 拆分重構 commit fe01a7e 與計數修正 d9c0280 之後
where:
  layer: 待定義
  files:
  - .claude/skills/component-contract-design::read
why: component-contract-design 由單一 SKILL.md（245 行、15015 字元）拆為 SKILL.md + references 五份。拆分經驗證為純位元搬移（245/245 行覆蓋、5/5 檔位元相同），但位元正確不等於拆分後可用：原本同檔內以章節名互指的段落現在跨檔，讀者的導航路徑因此改變，而純搬移的驗證方法對此完全無鑑別力。需依 multi-round-review 跑至少三輪不同 frame 的審查。
how:
  task_type: Analysis
  strategy: 依 multi-round-review 三輪硬底線：Round 1 規範/案例/跨檔一致與路由承接/下游任務/斷言支撐；Round 2 讀者旅程/冷讀/可執行走查/低階探針/題目範圍/體例定位；Round 3 自審/steelman/outbound/persona/搜尋落點/誤用梯度/共同前提/個案實跑。共用 context 置於本票 Context Bundle
acceptance:
- '[ ] 三輪皆執行且每輪 frame 互不重疊'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-04T15:03:33'
completed_at: null
created: '2026-09-04'
updated: '2026-09-04'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

審查 重構後的 component-contract-design 六檔（多輪審查）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### Context Bundle

**審查對象（六檔，全部唯讀）**

`.claude/skills/component-contract-design/SKILL.md` 與同目錄 `references/` 下五檔：
`modes-a-b.md`、`mode-c.md`、`field-questions.md`、`dispatch-language.md`、`examples-troubleshooting.md`。

**這次改動是什麼**

原為單一 SKILL.md（245 行、15,015 字元，為 skill body 預算 6,500 字元的 2.31 倍）。拆分為純位元搬移：每一行不是留在 SKILL.md 就是原封搬入某個 reference，已驗證 245/245 行覆蓋無重複無遺漏、五份 reference 正文與原檔對應區間位元相同。SKILL.md 現為 3,985 字元。

**本次唯一新寫的內容（其餘一字未改）**

1. SKILL.md 的〈按需讀取〉章節：標題、一句說明、五列路由表
2. 五個 reference 各自的檔頭三行：`# 標題`、`> 自 SKILL.md〈X〉逐字搬移…`、`> 同目錄：…`

因此 finding 請分兩組回報：**A 組**為本次新寫內容的問題（可立即修）、**B 組**為搬移前既有內容的問題（屬原檔既有品質，另行評估，仍須報）。

**切分依據**

「一次工作會不會同時用到」。三種模式互斥，判定起點後只走一條，故 `modes-a-b.md`（模式 B 末步驟接回模式 A）與 `mode-c.md` 分立；`field-questions.md` 為三模式共用的查表；`dispatch-language.md` 在契約齊全後寫票才用；`examples-troubleshooting.md` 為卡住時查詢。SKILL.md 留路由與判準：三種起點判定表、〈契約齊全的定義〉、〈分工邊界〉。

**已知的風險面**

拆分前這些章節同處一檔，互相以〈章節名〉指涉（如模式 A 步驟 6 寫「依〈十一欄位填寫問句〉逐欄填」）。現在那些章節分散在不同檔案，「本節」「見上」「見下」這類位置性指涉在搬移後可能失效。

**定位（frame 判斷標準的輸入）**

這是 **agent 指令 / 規範**，不是人類教材。讀者是照著執行的執行者（PM 或設計類代理人），讀完之後的動作落在世界上：複製 spec 範本、逐元件填十一欄位、建立元件票與畫面票。體例應為操作手冊——步驟編號是執行的位置，判錯代價要寫出來。

**共通產出契約（所有 reviewer 適用）**

- 每個 finding 欄位：位置（檔名 + 段落語意標題，不用行號）/ 問題 / 組別（A 新寫 / B 既有）/ 嚴重度（嚴重必修 / 建議可改）/ 建議修法
- 每個 finding 須註明**它那一類的全部命中位置**；抽樣者寫明抽了幾處、怎麼選的
- 某一維零命中須明寫「這一維跑過、零命中」，不可省略
- 報告 1500 字內，最後給整體評估分級
- 不修改任何檔案；不再往下派 agent
- 完成後以 `SendMessage({to: "main"})` 主動送出報告本體——任務結束通知不會把報告帶回主線程；預期超長時按維度切分並標號 1/N

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### Round 1 — 主 session 自行執行的窮盡列舉（不外包項）

multi-round-review 規定「需要窮盡列舉的維度」由主 session 執行（列舉在主 context 便宜、派出去容易失敗）。以下為機械列舉結果，與 Round 1-C reviewer 的報告互為對照。

**指涉總量**：六檔共 44 處 `〈章節名〉` 指涉。分布：modes-a-b 14、dispatch-language 8、field-questions 8、SKILL 6、mode-c 6、examples-troubleshooting 2。

**跨檔指涉的路由覆蓋檢查**（以檔頭同目錄行比對指涉目標的實際住址）：44 處中屬 skill 內部且跨檔者，僅 2 種未被來源檔的路由涵蓋，兩者皆在 SKILL.md。

**自查 finding 1（A 組，本次引入）**：SKILL.md〈三種起點〉段有一句「三種模式共用〈十一欄位填寫問句〉與〈派發語言〉兩節」，而 SKILL.md 身為入口檔沒有檔頭同目錄行，〈按需讀取〉路由表的五列又以「何時讀」描述條件、未把章節名對映到檔名。讀者在此處看到兩個章節名，需自行推斷它們分別是哪一份 reference。修法方向：路由表增一欄「涵蓋章節」，或該句直接帶檔名。

**自查 finding 2（B 組既存、經本次搬移惡化）**：modes-a-b.md 步驟 6 寫「問句見〈十一欄位填寫問句〉末段」。「末段」在拆分前已不精確——它指的是原 L143 的排列不變式三項，而該節實際結束於 L155 的範本章節表。拆分後兩層惡化：其一「末段」成為跨檔的位置指涉；其二多形態專案段被搬入 field-questions.md 並置於全檔最後（L43），使該檔真正的末段與被指涉的段落（L29）距離再拉開。修法方向：「末段」改為具名指涉（如「〈十一欄位填寫問句〉的容器排列不變式三問」）。

**位置性指涉總量**：六檔共 4 處（本節 2、上列 1、末段 1）。SKILL.md 的「上列五項」「本節五條」同段自指、拆分後仍成立；dispatch-language.md 的「本節」位於該檔第二節內、緊接標題後可讀但該檔含兩節故略減精確；modes-a-b.md 的「末段」即 finding 2。

### Round 1-D 報告（reviewer: r1de-claim-support，1/2）

六項 finding，五項 B 組（搬移前既有）、一項 A 組。主 session 已抽驗 D6 的路徑宣稱，`.claude/skills/doc/templates/component-library-spec-template.md` 確實存在，宣稱屬實。

| 編號 | 嚴重度 | 組別 | 位置 | 問題 |
|------|--------|------|------|------|
| D1 | 嚴重必修 | B | modes-a-b 模式 B 步驟 1/2/4 + SKILL.md 起點表 B 列 | 模式 B 的輸入通道整段未定義：三步皆以「看畫布」為輸入，但畫布如何進到執行者手上未交代。三種模式中只有 B 的第一手輸入非檔案系統可及物，而全檔未給出口 |
| D2 | 嚴重必修 | B | SKILL.md〈三種起點〉判定段 | 起點判錯的代價未寫。這是全程第一個分岔且成本不可逆（走 A 而該走 C 會產出與存量不符的契約、事故不被對應、遷移票整批缺失） |
| D3 | 嚴重必修 | B | field-questions 佔位值段 | 「不適用」誤填的代價未寫，而它是齊全檢查唯一的假陽性入口。相對地「待決」的代價寫得很清楚——兩個佔位值一寫代價一不寫，危險的是不寫的那個 |
| D4 | 嚴重必修 | B | modes-a-b 步驟 2、步驟 4、模式 B 步驟 4；mode-c 步驟 1（4 處） | 四種中間產出物只說要做未說長什麼樣（無欄位定義、無落點）。連帶使步驟 7 的齊全判準 2 失去分母形態，該判準的機械執行性落空 |
| D5 | 建議可改 | B | dispatch-language 三張票表格 | 票欄位句型不對稱：畫面票缺 what、元件票缺 how、三票共缺 title/type/priority/why |
| D6 | 建議可改 | A | SKILL.md〈產出檔案〉 | 範本無路徑無指令（實測路徑經主 session 複驗屬實） |

**零命中聲明**：「步驟停在 WHAT 層而完全無 HOW」這一維跑過——三模式共 20 步，除 D1 涵蓋的模式 B 3 步外其餘 17 步皆下沉到「開哪個檔／查哪個表／填哪個欄」。此維非普遍缺陷，僅模式 B 局部失效。

**主 session 判讀**：D1-D4 四項嚴重全屬 B 組，與本次拆分無關；但 D4 與拆分有間接關聯——步驟 7 引用「步驟 4 的候選表」作為判準 2 的分母，而步驟 4 與步驟 7 現同在 modes-a-b.md 內，拆分未加劇此問題。D6 屬 A 組但成因是原文即無路徑，本次未新增該缺陷、亦未修補。
### Round 1-C 報告（reviewer: r1c-cross-file）+ 主 session 複驗

10 項 finding（3 嚴重、7 建議）。主 session 對兩項可查證宣稱做了獨立複驗，皆屬實：

**F3 複驗屬實**：`PC-GPD-004-dispatch-tool-check-covers-bash-not-write.md:49` 含路徑級引用
`` `.claude/skills/component-contract-design/SKILL.md`〈執行者與簽核者〉 ``，該章節已移入
`references/dispatch-language.md`，此引用因本次拆分而失效。

**F10 複驗屬實**：以 `git show fe01a7e^` 取原檔，`^## ` 清單共 11 個章節，無「多形態專案」；
原檔第 33 行為 `**多形態專案**：` 粗體段，位於〈三種起點〉（17-36 行）內。
`field-questions.md` 檔頭宣稱「自〈十一欄位填寫問句〉與〈多形態專案〉逐字搬移」——
虛構了一個不存在的章節，並漏報真實來源章節〈三種起點〉。

**主 session 先前結論的更正**：fe01a7e 的 commit 訊息寫「外部引用檢查：12 處外部引用全部
指名 skill 名或章節名，無一處使用 SKILL.md 檔案路徑，拆分不破壞任何 consumer」。**此結論錯誤。**
成因是當時的 grep 管線末端加了 `head -15`，而實際含引用的檔案有 81 個，PC-GPD-004 落在
截斷之外。這是 bash-tool-usage-rules 規則二「輸出過濾方向」的實例——選擇性過濾更容易濾掉
與正常輸出措辭不同的那一行，而我據截斷後的清單下了全稱結論（「無一處」）。
正確結論為：外部引用中至少一處為路徑級且已因拆分失效。

**A 組（本次新寫內容引入）**

| 編號 | 嚴重度 | 位置 | 問題 |
|------|--------|------|------|
| F1 | 嚴重 | SKILL.md〈按需讀取〉引言 | 引言「只讀對應的一份——三種模式互斥，一次工作只會走其中一條」與其下列 3-5 自相矛盾；列 3-5 明文要求另讀三份。照字面執行者不會開 field-questions.md，模式 A/C 步驟 6「填契約」即無問句可依，程序斷在中段 |
| F2 | 嚴重 | SKILL.md〈按需讀取〉表 + SKILL.md 用詞段 | 索引軸不一致：路由表以「情境 → 檔名」索引，全套文件卻以「章節名」互指，表中五列無一列出現章節名。外部另有 77 處 ticket 以章節名引用（〈派發語言〉69、〈十一欄位填寫問句〉7、〈執行者與簽核者〉1），這些執行者在 SKILL.md 站不到落點 |
| F10 | 建議 | field-questions.md 檔頭 | 虛構章節名（見上複驗） |
| F4 | 建議 | mode-c.md 檔頭同目錄行 | 未列 examples-troubleshooting.md，而本檔步驟 4 明文指向「Troubleshooting 的計數門檻」 |
| F7 | 建議 | mode-c.md 檔頭 | 「元件目錄／元件清單／元件庫」三詞定義只在 SKILL.md，消費點在 mode-c 步驟 1；直接開模式 C 者失去定義 |
| F8 | 建議 | SKILL.md 路由表列 4 | 前綴「契約齊全後」對兩處不成立：mode-c 步驟 7 缺件建票與遷移票發生於契約未齊時；〈執行者與簽核者〉是派發前就要查的 |
| F9 | 建議 | skill-design-guide/SKILL.md:52 | 引用的實測值（245 行／15,015 字元／最長單行 548 字）描述的是拆分前狀態，現讀似描述現況 |

**F3 為外部檔案的失效引用（嚴重）**。

**B 組內容、經 A 改動激活**

| 編號 | 位置 | 問題 |
|------|------|------|
| F5 | modes-a-b.md 步驟 6 | 「〈十一欄位填寫問句〉末段」——「末段」的容器單位由章節變成檔案，而該檔檔尾是多形態專案段（原本不在該章節內），照字面跳檔尾者落在無關段落 |
| F6 | modes-a-b.md 兩處、mode-c.md 一處 | 〈派發語言〉同名雙義（本 skill 有此章節、方法論亦有）；同檔時期靠鄰近性消歧，跨檔後失去該線索 |

**零命中／判定 OK 的部分**：方法論外部指名 12 處全數以「方法論」限定詞前置、不受拆分影響；
位置性指涉 10 處中 9 處為檔內自指、搬移後仍指同一段；五份檔頭四份宣稱全對。

**與主 session 自查的交叉**：自查 finding 1 = F2、自查 finding 2 = F5，兩者獨立命中同一位置。
自查未發現 F1、F3、F10（F3 因自查 grep 截斷、F1 與 F10 因自查只比對指涉落點未讀路由表語意與檔頭事實）。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
