---
id: 0.1.0-W1-008
title: 審查 兩個新 skill 的多輪審查
type: ANA
status: in_progress
version: 0.1.0
wave: 1
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W1-011
- 0.1.0-W1-012
- 0.1.0-W1-013
- 0.1.0-W1-014
- 0.1.0-W1-015
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 兩個新 skill 只跑了單輪寫作審查即推上 canonical 庫，未依 multi-round-review 跑滿三輪
  final_decision: 補跑 Round 1-3（1-A 已由 basil 完成），finding 修正後重推 canonical
  rationale: version-sequencing 自身第一步明文規定規格文件須經 multi-round-review 至少三輪，該規則未套用於寫下它的文件本身；且 Round 3 找的是沒寫的東西，對要被他人照做的 skill 最關鍵
who:
  current: rosemary-project-manager
  history: {}
what: 審查 兩個新 skill 的多輪審查
when: 兩個 skill 已推上 canonical 庫之後（審查應在 push 之前，此為補做）
where:
  layer: Infrastructure
  files:
  - .claude/skills/foundation-design/SKILL.md
  - .claude/skills/version-sequencing/SKILL.md
why: 兩份 skill 是跨專案 canonical 資產，其他專案會照著執行。單輪單 frame（寫作規範）的覆蓋面不足以支撐這個角色：Round 1 尚缺事實核對、跨 skill 一致性、可執行性、斷言支撐；Round 2、3 完全未跑。且兩份 skill 宣稱跨專案適用卻只從一個專案歸納（Flutter 桌面、無資料庫、無部署），需個案實跑驗證。
how:
  task_type: Implementation
  strategy: 依 multi-round-review 跑三輪，frame 切換且跨輪 finding 不重疊。Round 1 補 1-B 事實核對、1-C 跨 skill 一致性、1-D 可執行性、1-E 斷言支撐。Round 2 冷讀、低階探針、情境可想像性。Round 3 self-application（兩 skill 各自套用於自己）、steelman、誤用梯度、個案實跑（不同形態專案）。每輪修完再進下一輪。
acceptance:
- '[ ] Round 1-3 皆完成且 frame 判定表記錄不跑的 frame 與理由'
- '[ ] 跨輪 finding 不重疊，重疊即代表 frame 未切換須重跑'
- '[ ] 3-A self-application 已對兩個 skill 各自執行：foundation-design 的三問套用於自身、version-sequencing 的六步套用於自身'
- '[ ] 3-H 個案實跑涵蓋至少三種不同形態專案（含有資料庫者、無 UI 者、缺文件的老專案）'
- '[ ] finding 修正後兩個 skill 重推 canonical 庫與框架 repo'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-08-27T11:04:24'
completed_at: null
created: '2026-08-27'
updated: '2026-08-27'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

審查 兩個新 skill 的多輪審查

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### Round 3 修正掃描清單（動手前建立，防「只修被指出的那一處」）

統一修法：**把「要求動作」的條文改寫為「要求產物攜帶只有真做過才交得出的證據」**。
此即不變量 4 的動作，缺的是它對散文型產物的落地形態（R3-selfapply 第 6 項）。
兩份文件的 Examples 已示範三次（實測 i18n 0 次 / seed 0 次 / depends_on 零筆 exit=0），
但躺在 Examples 未提升為規則。

#### A 類：我在 Round 2 的修正造成或未套完（優先）

| # | 位置 | 缺陷 | 修法 |
|---|------|------|------|
| A1 | FD 協作表第四列 | 「以上皆非」首個條件「無 version-bootstrap」吞掉所有老專案，把它旁邊的第三列（skill 自稱無可替代）吃掉 | 第三列優先於第四列，明訂判定順序 |
| A2 | FD 工作流步驟 3 | 步驟 2 加了「無權威」三態，步驟 3 只寫「進入的維度：讀其權威來源」，無權威無分支，流程終止在步驟 2 | 步驟 3 補無權威分支 |
| A3 | FD UI 列 N/A 條件 | 同一格內自相矛盾：前半句「無人眼可見輸出」與括號「（純 library／純 daemon）」對 CLI 給相反答案 | 擇一表述 |
| A4 | VS 步驟 3 斷言票 | 與同檔交接契約矛盾（契約：該版啟動時寫入，非預先登記；斷言票：步驟 3 全部預建）。預建後「還沒輪到」與「卡住」在 runqueue 同形 | 改為該版啟動時建 |
| A5 | VS 交接契約表 | 步驟 6 的「誰在何時檢查」欄未套用至此表。本專案實測履行 1/3 且無人察覺 | 補第三欄 |

#### B 類：實跑證實會造成損害（最高嚴重度）

| # | 位置 | 實證 | 修法 |
|---|------|------|------|
| B1 | FD 可觀測性列「從不 N/A」 | cc-statusline 的 stdout 即產品，照文字建 log 接線點會毀掉產品。現有的「錯誤畫進 statusline、失敗靜默降級」是正確設計卻逐條違反權威 | 補第四態「進入但權威對本形態有害」，並授權維度內部分 N/A |
| B2 | FD 形態 2 授權範圍 | 「只取與形態無關的部分」只授權裁剪權威內容，不及於產物清單；判定粒度是維度，無「維度內部分 N/A」 | 授權擴及產物清單 |
| B3 | FD UI 四塊 | cc-statusline 照文字要建 i18n/design-system/UX 審查/元件庫四票，四塊對終端機產品全為負收益或零投影 | 同 B2 |

#### C 類：缺格與缺態

| # | 缺什麼 |
|---|--------|
| C1 | 情境四選一無「規劃波已完成、正在實作中」（monitor 實測兩條路各自壞掉） |
| C2 | 三態缺「進入且已完成」（monitor 可觀測性已全面 slog，照文字建一張沒事可做的票） |
| C3 | 〈權威缺席時〉不涵蓋「工作流自身引用的 skill 缺席」（monitor 缺 version-sequencing） |
| C4 | 移植前置 checklist 無「缺一項」中間狀態；且提問比 skill 自己的載體定義窄（問「有 CI？」但自訂載體含 hooks，本專案 133 個 hook 卻被判缺項） |
| C5 | DevOps 的 N/A 條件只接受「不需要」，不接受「不值得」（編譯式語言幾乎必然強制進入） |

#### D 類：宣告自身完整性時不指名範圍（FD 的集中失效處）

| # | 位置 |
|---|------|
| D1 | Common Issues「本 skill 的路由表即為查重結果」——該表自己在 Examples 承認漏過三次 |
| D2 | Common Issues「五維度都填了」隱含「五維度＝地基全部」，全文無處宣告此窮盡性 |
| D3 | FD frontmatter「Do NOT use for 環境安裝」內文零次展開，而 scaffold 是它自己的觸發詞、與環境安裝實務高度混同 |

#### E 類：其餘要求動作缺保證者（R3-misuse 11 條中未列於上）

VS 小版本「必須有一條測試涵蓋該變體」／產出契約「清單需有持久載體」／
移植前置「該風險由使用者承擔並應記錄」——三條同樣無保證者。

#### 待補
Layer 2 文字審查（R3-writing）結果未到，到齊後併入本清單再動手。

#### 證據基礎的限制（記錄，非修正項）
本機 23 個 repo 全部同一作者，「老專案接手」在整個工作區零命中；
Flutter 佔 10/23。兩份 skill 的歸納來源形態偏窄，此限制應如實記錄而非以文字掩蓋。

### 掃描清單增補（R3-writing）與修法定案

**定案（用戶裁示）**：換判定對象，維度 → 產物。維度恆進入，判的是「這個維度在本專案的
產物是什麼」，答案可為「無」。權威降為預設產物。此舉一次收掉 B1/B2/B3、C2、C5、A3，
以及「三態不夠用」的整串補丁需求。

#### F 類：跨檔一致性（R3-writing Critical，全數落在 Round 2 新增段）

| # | 缺陷 | 修法 |
|---|------|------|
| F1 | 交接步驟號三處互斥：VS L77 指步驟 3、VS L114 指步驟 5、FD L106 指步驟 5。步驟 5「首版開票」無「決定排進哪一版」職能 | 三處統一，改語意錨取代步驟號 |
| F2 | VS 層名約定寫 FD「定進入條件」，FD 第一句「本欄是 N/A 條件，不是進入條件」 | 對齊用語 |
| F3 | 「已有規格的老專案」同時滿足第三列與第四列（與 R3-realprojects 獨立收斂） | 明訂分界軸與優先序 |
| F4 | L53「候選軸至少三個」使 L63「候選達 3 個以上」恆真；同段末「選任一即可」推翻剛給的條件 | 條件收斂為單一有效判準 |

#### G 類：寫作規範（R3-writing W 級）

| # | 缺陷 |
|---|------|
| G1 | W-7/W-8 沿革敘事佔據 Why 位置，違反 spec 與 process record 不可混寫 |
| G2 | W-18 「症狀欄寫承受者看得到的東西」宣告後第四行即違反（「尚未搜過」只有旁觀者知道） |
| G3 | W-3 「五個權威來源」失準，路由表實列 8 個權威檔案 |
| G4 | W-2 降級路徑矛盾：「前三步」含固化，固化需機械檢查，而 CI 正是缺的三項之一 |
| G5 | W-1 「記為缺口」缺 Action，屬無 trigger 延後 |
| G6 | W-12~16 五處位置編號（第二列/第三列/第三欄）應改語意錨——編號錯位會成功解析到錯的東西 |
| G7 | W-17 「出產出物契約」斷詞失敗；W-20 模板→範本；W-21 內聯→行內 |
| G8 | W-5 中版本未定義即使用；W-6 「該 skill」指涉不閉合；W-11 三項枚舉結構非對稱 |
| G9 | W-19 「另外兩個」內嵌數量；W-22 「自然結果」隱含詞；W-23 「通常是地基版」條件模糊 |
| G10 | W-24 破折號 cadence 異常（FD dash density 3.09 / ai_signature 0.70；VS 4.44 / cv 0.51） |
| G11 | 評價語「誠實的宣告」「真正無可替代」；「把他導回去」人稱；觸發詞「腳手架」建議並列「鷹架」 |

#### 通過項（不動）
禁用詞 0 命中、字元集污染 0、翻譯腔 signature 0.0、12 條禁令全數帶正向錨點。

#### 停止判讀
七軸已覆蓋；finding 未退化為表層（4 條 Critical + 一個實跑證實有害）；
但關鍵訊號是**每一輪的修正是下一輪 Critical 的主要來源**（R1 修正 → R2 的 C4/C5；
R2 修正 → R3 的 F1-F4、A1-A5、G1、G2）。故不跑 Round 4 審同一文字，改為結構重整後驗證。
### 兩輪實跑驗證的合併修正清單（v4.2.0 → v5.0.0）

實跑對象：cc-statusline / monitor（V-rerun）、c2c_website / book_overview_v1（V-unverified）、
unichat_desktop / unimall_shop（V-handover）。六個 repo 涵蓋四種原列為未驗證的形態。

**統一病灶**：四條判準寫成「這個東西存不存在」，該問的是「它對本專案有沒有作用」。
同 PC-BAL-010（驗證管道對待驗現象不敏感）——存在性檢查對有效性零鑑別力。

#### H 類：存在性→有效性（統一病灶的四個實例）

| # | 現行問法 | 改為 | 實證 |
|---|---------|------|------|
| H1 | 情境第一列「無規格文件、無設計記錄」 | 「既有文件能否回答維度表任一格的產物」 | unimall_shop 的 changelog 532 行 + .cursorrules 278 行對五維度零回答，卻使判準跳離第一列，連帶失去老專案模式 |
| H2 | 「monorepo 按子專案分列」 | 「repo 內有多個子樹各持獨立工具鏈或部署單元時分列」 | c2c_website 無 workspaces/lerna/nx，但三套工具鏈必須分列 |
| H3 | 移植前置「有執法載體？」 | 「有執法載體，且掃描範圍涵蓋本專案實際存在的檔案？」 | c2c_website 的 style-guardian-hook 只認 /lib/presentation/*.dart，該 repo .dart 檔 0 個，結構上永不觸發 |
| H4 | 形態 3「框架內確實無承接者」單問 | 拆判準層與產物層兩問 | c2c_website 的 seed 有實作有 SeederTest 覆蓋，照條文卻要填「待定：缺口」 |

#### I 類：內容缺漏

| # | 缺什麼 | 實證 |
|---|-------|------|
| I1 | 資料庫列預設產物缺「備份還原驗證」 | c2c_website 真 PostgreSQL + docker volume + 零備份痕跡，是該 repo 最高價值缺口，靠 reviewer 讀權威〈防護底線〉第 2 條才撈到 |
| I2 | 情境第三列「誰驅動＝無」與「本 skill 的角色＝補判」互斥 | 兩個活躍 repo 都落此列，字面像收手，實際是最需要跑維度表的狀態 |
| I3 | 步驟 3「已存在→不建票」與步驟 4「產物落為地基票」無銜接 | book_overview_v1 幾乎全格已存在，跑完產出盤點表零張票，未定義算不算完成 |
| I4 | 移植前置的自指 | 缺執法載體時處置是「建票補齊」，但補它正是 DevOps 維度的產物，同一件事兩張票 |
| I5 | 命名前提無規模閘門 | fallback 是補特徵測試，在 34k/110k 行零覆蓋專案上比命名本身大一量級，且無「補不起來時怎麼辦」 |
| I6 | 盤點措辭把假陽性當通過 | 「grep 既有測試」——void main() {} 會命中成測試檔，保護力為零 |
| I7 | 萃取前提缺第四種「決策者不可得」 | 交接情境常態；V-handover 已給處置形狀（保留原值原名、疑義落決策文件、不在地基波內改） |
| I8 | 情形 3「已知過期」未給判斷方法，且「直接排除」過度 | unimall_shop 的 changelog 落後一個 minor 但覆蓋範圍內與程式碼一致，是全 repo 唯一能說明 0.01 是手續費率的來源。應為「排除其對現況的宣稱效力，保留其作為語意線索」 |
| I9 | 判準的可查證對象未指名 tracked 或工作目錄 | .cursorrules 在 .gitignore，「有無設計記錄」變成工作目錄屬性，同一 repo 換台機器結論不同 |
| I10 | 移植前置缺「他人在途工作」一項 | unimall_shop 五作者、最後 commit 距今 8 天仍在途，收斂裸值會與他人在途工作衝突 |

#### J 類：正面發現，應寫進條文

| # | 內容 |
|---|------|
| J1 | 交接情境下既有測試是比文件更可靠的意圖載體。unimall_shop 的 test/unit_test.dart 僅 22 行，是全 repo 唯一記下「AppRoutes 前五順序承載語意」的地方，程式碼與 532 行 changelog 皆無 |
| J2 | 非 Flutter GUI 四塊全部成立，可移出未驗證清單。book_overview_v1 的 design-system 超出權威（多出 component-rules.js 這個 CSS 層特有的第五類），可反向回饋方法論 |

#### 仍判不出來（記錄，非本次修正範圍）
無 UI 框架（vanilla DOM）形態下的元件庫產物定義。三個 repo 皆卡在此，且非權威缺席，
是權威對該形態無可投影的產物定義。推導問句答得出前半（防什麼）答不出後半（產物是什麼）。
## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### Spawn 落地確認

| 發現 | 落地 |
|------|------|
| 框架地基權威三處缺口（seed 無承接者／非 SaaS 三維度權威失效／wrap-decision 與 design-decision-framework 零引用） | 建票 **0.1.0-W1-011**（ANA） |
| 背景 reviewer 報告不會自動回主線程，idle 通知與完成通知同形 | 建票 **0.1.0-W1-012**（DOC） |
| PC-BAL-010 新實例（依賴檢查腳本在欄位零筆時回報 [OK]） | 無需建 ticket：已直接併入既有 error-pattern 的恆真案例表並新增〈空集合子群的辨識法〉，commit bbed371 |
| component-library 方法論檔內「三者」與「四塊」不一致 | 無需建 ticket：單句修正已完成，commit bbed371 |
| 兩份 skill 的 portable 誤宣告 | 無需建 ticket：已更正為框架綁定，commit c012c68，並經推送實測驗證 |
| 證據基礎限制（單一作者專案群、Flutter 佔近半、老專案接手零實例） | 無需建 ticket：屬如實記錄而非待修缺陷，已寫入 foundation-design〈已知的證據基礎限制〉節 |
| 本票 acceptance 前三條 oracle 在執行者手上（R3-selfapply 第 6 項） | 無需建 ticket：執行中不改 AC（PC-055 漂移守衛）；判準已提升為 version-sequencing〈不變量 4 對散文型產物的落地形態〉，後續票沿用該形態 |
| `.claude/hooks/` 下兩個 session 本地 symlink | 無需建 ticket：收尾時已移除，實體檔在 skills/ticket/hooks/ 且 settings.json 只指向新位置 |

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
