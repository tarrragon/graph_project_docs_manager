---
id: 0.1.0-W3-045.2
title: 遷移 comment-qa-hook 至新建的 dart-comment-traceability skill
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: 0.1.0-W3-045
children: []
blockedBy:
- 0.1.0-W3-045.1
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 父票 acceptance 5 > 4 被閾值 1 硬擋
  final_decision: 依 domain 邊界拆為兩支 skill 各一子票
  rationale: 兩支各自成家本就是父票的定案，拆分與該決定一致
who:
  current: thyme-python-developer
  history: {}
what: 新建 dart-comment-traceability skill 承接 comment-qa-hook.py，架構文件引用改為可省略，並更新 settings.json 對應註冊
when: 同層子票 0.1.0-W3-045.1 完成後（兩者皆改 settings.json）
where:
  layer: framework
  files:
  - .claude/skills/dart-comment-traceability/hooks/comment-qa-hook.py::write
  - .claude/skills/dart-comment-traceability/SKILL.md::write
  - .claude/skills/dart-comment-traceability/CHANGELOG.md::write
  - .claude/skills/compositional-writing/hooks/comment-qa-hook.py::write
  - .claude/settings.json::write
why: 該 hook 檢查 Dart 事件處理函式與 UseCase Domain 層的註解追溯，主題為特定架構風格下的註解規範，不屬 compositional-writing。它規範內指名的 docs event-driven-architecture-design.md 在本專案與 flutter_balance 皆不存在，故該引用應可省略而非只是可設定。skill 名依既有 dart-style-guardian 與 dart-test-async-guardian 的前綴慣例
how:
  task_type: Implementation
  strategy: 取 flutter_balance 較新副本（其 PROJECT_ROOT 已改 get_project_root，並保留 .claude/lib 的選用性降級）-> 建 skill 骨架與 frontmatter -> 架構文件引用改為可省略 -> 改 settings.json 該 hook 的註冊 -> 刪 compositional-writing 側原檔 -> 實測觸發正常
acceptance:
- '[x] 檔案落在 .claude/skills/dart-comment-traceability/hooks/comment-qa-hook.py（hook 檔名維持原名，skill 名與 hook 名不一致是刻意的，下游票以此路徑為 claim 前提）'
- '[x] 架構文件引用可省略，缺該文件時 hook 不報錯且其餘檢查照常執行'
- '[x] settings.json 該 hook 的註冊已指向新位置，compositional-writing 側原檔已刪且該目錄清空'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-07T13:13:33'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

新建 dart-comment-traceability skill 承接 comment-qa-hook.py，架構文件引用改為可省略，並更新 settings.json 對應註冊

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-07 13:12] hook 檔名維持 comment-qa-hook.py 不可改——skill 名與 hook 名不一致是刻意的，下游跨專案 ticket 以 dart-comment-traceability/hooks/comment-qa-hook.py 這個逐字路徑為 claim 前提。同層票 W3-045.1 剛改過 settings.json，動它之前先讀現況。架構文件引用改為可省略（兩個 consumer 都沒有那份文件）。

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: basil-hook-architect 或 thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-045.1 | chars: 936 -->

### Task Reference
- 0.1.0-W3-045.1 what: 把 worklog-format-check.py 移入 doc-flow/hooks/，作用域由寫死 docs/work-logs/ 改為可設定，並更新 settings.json 對應註冊（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-045.1 why: 該 hook 緩解表格單元格 emoji 造成的 Claude Code CLI crash，主題為 worklog 的工具相容性。doc-flow 的 description 明寫管 worklog 與 documentation consistency checks 且目前無 hooks 目錄，主題相符。作用域寫死 docs/work-logs 是它在廢除方環境被判不適用的唯一原因，改為可設定後三個 consumer 各指自己的路徑（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/doc-flow/hooks/worklog-format-check.py::write  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- .claude/skills/doc-flow/SKILL.md::write  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- .claude/skills/doc-flow/CHANGELOG.md::write  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- .claude/skills/compositional-writing/hooks/worklog-format-check.py::write  # from 0.1.0-W3-045.1  [路徑已不存在]（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 檔案落在 .claude/skills/doc-flow/hooks/worklog-format-check.py（檔名不可改，下游票以此為 claim 前提）  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- [ref] [x] 作用域可設定且預設值為 docs/work-logs/，hook 實測觸發正常  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- [ref] [x] settings.json 該 hook 的註冊已指向新位置，compositional-writing 側原檔已刪  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- [ref] [x] 本 session 已實地觸發 doc-flow/hooks/worklog-format-check.py 並以 hook-liveness 確認落檔（本 session c1525670 內共 677 筆記錄，最近一筆於遷移完成後產生）  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- [ref] [x] liveness 驗證方式：ticket track hook-liveness <hook 路徑> 讀取 hook 自身 logger 持久化記錄並依 session 聚合  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
