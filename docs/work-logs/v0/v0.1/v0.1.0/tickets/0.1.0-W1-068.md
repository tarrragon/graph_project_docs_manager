---
id: 0.1.0-W1-068
title: 建立 ExternalOpener 抽象與 macOS open 實作（SPEC-003 §2.2 外部開啟契約）
type: IMP
status: in_progress
version: 0.1.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.1-W1-002
source_ticket: 0.1.0-W1-036
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W1-036 裁決落地
  final_decision: 先建共用抽象再派畫面票
  rationale: 四處共用一個抽象，避免逐畫面實作分歧
who:
  current: parsley-flutter-developer
  history: {}
what: 建立 ExternalOpener 抽象與 macOS open 實作（SPEC-003 §2.2 外部開啟契約）
when: W2 畫面票（§1 空圖、§2 flow 未結構化、§5 破洞、§6 節點詳情）實作前
where:
  layer: Infrastructure
  files:
  - lib/workspace/external_opener.dart
  - test/unit/workspace/external_opener_test.dart
why: SPEC-003 §2.2「外部開啟契約」（v1.7）定案 0.1 落地外部開啟；四處畫面共用同一可注入抽象，需先於畫面票建立，否則四張畫面票各自實作會分歧且無法以 fake 斷言。
how:
  task_type: Implementation
  strategy: '介面 ExternalOpener.open(path) 回傳 opened/notFound/failed；前置 FileSystemEntity.type 檢查，notFound 不呼叫外部程序；實作 Process.run(''/usr/bin/open'',[path])，exit 0 → opened 否則 failed 並 developer.log warning 記錄 stderr；提供 FakeExternalOpener 記錄 calls 並可指定回傳值；Riverpod provider 供畫面注入。


    補充（2026-09-08，依 0.1.0-W3-114 落地的〈Port 回饋契約〉INV-PORT-OBSERVE-001 回填）——上述設計已覆蓋「結果」時刻（三值回傳可區分成功與失敗、failed 分支有日誌），另兩個時刻須補：


    1. **呼叫發出時刻缺失**：原設計只在 failed 分支記日誌，opened 與 notFound 兩條路徑無任何記錄。INV-PORT-OBSERVE-001 要求每一次 port 呼叫都產生「呼叫已發出」事件，且明文涵蓋「即使呼叫最終失敗、被防抖丟棄或立即拋出例外」。notFound 由前置檢查擋下、不 spawn 外部程序，但 port 方法本身已被呼叫，依判準（呼叫端調用 port 方法的那一刻即為事件，不依賴外部系統是否回應）仍屬呼叫發出時刻，須有事件。實作：open(path) 進入點即記錄一次，位置在前置檢查之前。


    2. **外部系統受理時刻未定義**：Process.run 無 acknowledged 狀態也無 progress callback，且原設計未提逾時。以方法論判準問句檢驗——把外部系統整個拔掉，呼叫端能否得知「呼叫已發出但無回應」——目前答案為否：Process.run 若 hang 則在它自行返回前無任何記錄。須擇一並在 Solution 寫明理由：(a) 加逾時，逾時視為獨立結局並擴充回傳值；(b) 判定此 port 無受理概念、以呼叫發出與結果兩時刻為完整契約。選 (b) 亦須明寫，不得略過。


    3. **不互相抵扣**：日誌不抵扣呼叫端回饋。failed 分支目前兩者皆有；補上的呼叫發出事件屬日誌側，不因此免除任何回傳值義務。'
acceptance:
- '[x] 契約測試：fake 記錄呼叫路徑與次數；notFound 路徑不 spawn 外部程序'
- '[x] failed 分支有 developer.log warning 含路徑與 stderr'
- '[x] open(path) 進入點有「呼叫發出」日誌事件，opened / notFound / failed 三條路徑皆涵蓋（INV-PORT-OBSERVE-001）'
- '[x] 外部系統受理時刻的處置已擇一（加逾時並擴充回傳值 / 判定此 port 無受理概念）並在 Solution 寫明理由'
- '[ ] 實機驗證：在沙盒關閉的 debug .app 內，經既有開啟入口對一個檔案與一個目錄各開啟一次，記錄 exitCode 與是否丟 ProcessException（簽章 .app 驗證由 0.1.1-W1-002 承接，用戶裁示 2026-09-23）'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-23T10:48:52'
completed_at: null
created: '2026-09-03'
updated: '2026-09-03'
protocol_version: '2.0'
scope_blocker: 0.1.0 契約：SPEC-003 §2.2 外部開啟契約，SPEC-001 §1/§2/§5/§6 四處開啟動作依賴此抽象
tdd_phase_source: auto
---

# Execution Log

## Task Summary

建立 ExternalOpener 抽象與 macOS open 實作（SPEC-003 §2.2 外部開啟契約）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-23 10:43] compare how with SPEC before implementing：how 欄凍結於建票時，SPEC 之後仍有演進；以 SPEC 現行版為準，差異寫入 Problem Analysis。收尾用 finish 不用 complete。｜readiness=2 validate=1
- [2026-09-23 10:45] compare how with SPEC before implementing：how 欄凍結於建票時，SPEC 之後仍有演進；以 SPEC 現行版為準，差異寫入 Problem Analysis。收尾用 finish 不用 complete。 AC2（Developer ID 簽章 .app 內實機驗證）屬 PM 職責，代理人不做、不勾，finish 前以 NeedsContext 標明留給 PM。｜readiness=2 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

### AC2 改寫（2026-09-23，用戶裁示）

原 AC2 要求在 Developer ID 簽章 .app 內實機驗證。本機 `security find-identity -v -p codesigning` 為 0 個有效身分，用戶裁示「修改 AC 條件，現在不需要做簽章身分相關的設定」。AC2 改為沙盒關閉的 debug .app 內驗證；簽章 .app 的驗證需求未消失，延後由 `0.1.1-W1-002` 承接。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
建立 `lib/workspace/external_opener.dart`：`ExternalOpenResult` 三值 enum、
`ExternalOpener` 抽象介面、`MacosExternalOpener`（`/usr/bin/open` 實作）、
`FakeExternalOpener`（記錄 calls，`resultFor`／`defaultResult` 可配置）、
`externalOpenerProvider`（Riverpod `Provider<ExternalOpener>`）。

how 與 SPEC 現行版比對（compare how with SPEC before implementing）：
建票時 how 未涵蓋 SPEC-003 v1.37 §2.2〈兩個 port 的三時刻覆蓋〉小節
（`0.1.0-W3-139` 裁定，較 how 凍結時間晚）新增的「呼叫發出日誌」義務。
本實作依 SPEC 現行版補上：`open(path)` 方法體第一語句即
`developer.log(path, name: _tag)`，位於 `FileSystemEntity.type` 前置檢查
之前，故 opened／notFound／failed 三條路徑皆留下入口記錄
（INV-PORT-OBSERVE-001）。ticket how 欄本身描述的介面／前置檢查／
macOS 實作／失敗日誌／不新增依賴等主體設計與 SPEC 一致，無其他差異。

AC5（外部系統受理時刻擇一並寫理由）：採 SPEC 現行版已定案的選項——
SPEC-003 §2.2「外部開啟契約」表〈逾時〉列明文「0.1 不設逾時」，且
〈兩個 port 的三時刻覆蓋〉小節將 `ExternalOpener` 的「受理」時刻列為
「不適用：`Process.run` 為 fire-and-wait，外部程序不回中間 acknowledge」。
本票採用此既有裁決，不另加逾時機制；可觀測性由〈呼叫發出日誌〉承載
（見上段），符合「不互相抵扣」原則（日誌不抵扣呼叫端回饋，呼叫端已有
`ExternalOpenResult` 三值回傳）。

日誌內容：notFound／failed 為 warning（`level: 900`），opened 為 info
（預設 level）；failed 分支含路徑與 `result.stderr`。三則診斷字串標記
`i18n-exempt`（開發者診斷字串，非使用者可見文字，與 `MacosScanNotifier`
既有慣例一致）。

測試（`test/unit/workspace/external_opener_test.dart`，5 個）：
G1 FakeExternalOpener 契約（calls 記錄、defaultResult、resultFor 覆寫）；
G2 MacosExternalOpener notFound 路徑不 spawn 外部程序（以不存在的暫存
路徑驗證，安全、不啟動真實應用程式）；G3 呼叫發出日誌的靜態結構驗證
（依 SPEC-003 §2.2〈呼叫發出日誌〉表「驗收」列「靜態可讀出，不需執行」
的既定驗證形態，以原始碼文字定位驗證語句順序）。

`opened`／`failed` 分支涉及實際喚起系統應用程式，依 SPEC-003 §2.2
「測試斷言」列（整合測試注入 fake）不在本單元測試中以真實 Process.run
驗證，避免測試執行期間意外開啟應用程式；四處畫面票的整合測試將依
FakeExternalOpener 驗證 opened／failed／notFound 三分支的畫面反應。

AC2（Developer ID 簽章 .app 內實機驗證）留給 PM，見 NeedsContext。

追加修正（PM 補派）：MacosExternalOpener.open 原未處理 Process.run 丟出的
ProcessException（`/usr/bin/open` 無法被 spawn 時，如權限／沙盒限制——
即 AC2 在簽章 app 內要驗的情況，Process.run 拋例外而非回傳非零 exitCode，
會繞過三值契約直接傳到呼叫端）。

修正：`Process.run` 呼叫改用 `try/on ProcessException`，catch 時記
developer.log warning（level 900，含 path 與 e.message），回傳
ExternalOpenResult.failed；其餘例外不攔截、照樣往上拋。同時將執行檔路徑
改為建構子參數（`_executable`，預設 '/usr/bin/open'），供測試傳入不存在
的執行檔路徑觸發此分支。

新增測試 G2-2：以不存在的執行檔路徑呼叫 open，斷言回傳 failed。
G3-1 靜態結構驗證同步改比對新的 `Process.run(_executable, [path])` 呼叫式。
測試數由 5 增至 6，全數通過；`fvm flutter analyze` 0 issues。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
fvm flutter analyze lib/workspace/external_opener.dart test/unit/workspace/external_opener_test.dart
→ No issues found!

fvm flutter test test/unit/workspace/external_opener_test.dart --reporter compact
→ All tests passed! (+5)

## NeedsContext
- 缺失項：AC2「Developer ID 簽章、沙盒關閉的 .app 內實機驗證」——本票只
  在裸 dart 測試程序驗證（notFound 路徑），未在簽章 .app 內對一個檔案與
  一個目錄各執行一次 open 並記錄 exitCode。
- 觸發位置：ticket acceptance index 2。
- 影響：`opened`／`failed` 分支的真實 Process.run 行為在簽章 .app 沙盒
  關閉環境下的 exitCode 仍為未驗證假設（SPEC-003 §2.2「實測依據」段落
  現有紀錄來自 `0.1.0-W1-036` 的裸 dart 環境）。
- 建議補料：PM 執行簽章打包後的 `.app` 手動驗證（開一個檔案、開一個
  目錄），記錄 exitCode 並回填 SPEC-003 §2.2「實測依據」段落。
- 重派成本：屬 PM 職責範圍內動作，非重派代理人。

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-036 | chars: 584 -->

### Task Reference
- 0.1.0-W1-036 what: 決定 0.1 是否落地外部開啟能力；不落地則須定義 disabled 呈現與常駐說明文案（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W1-036 why: SPEC-001 §1、§2、§5、§6 共四處有「開啟原始檔」或「開啟 docs 目錄」動作，SPEC-003 §3 已為四處各自定義了成功、檔案不存在兩條反應路徑。

App Sandbox 已關閉（PROP-001 的能力決策），技術上可行，但 0.1 是否實際落地未見任何決策記錄。

依 SPEC-003 FR-06，未接線動作只有兩種合法形態：不渲染，或 enabled 為 false 加同列常駐說明文字。無論落地與否都需要一個決定——若落地，需確認 macOS 開啟外部程序的實作路徑與失敗處理；若不落地，需定義四處各自的 disabled 呈現與說明文案，而該文案又回頭需要 i18n key。決定懸空時，W1-005 的元件無法確定要不要承載這個動作的兩種形態。（來源已完成，結論見 Solution）

### Related Files
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W1-036（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 已實測 macOS 下開啟外部檔案與目錄的可行性，記錄實測環境與結果  # from 0.1.0-W1-036（來源已完成，結論見 Solution）
- [ref] [x] 落地或不落地擇一定案；不落地時四處的 disabled 呈現與說明文案皆已定義  # from 0.1.0-W1-036（來源已完成，結論見 Solution）
- [ref] [x] SPEC-003 §3.1／§3.2／§3.5／§3.6 對應列已回填定案結果  # from 0.1.0-W1-036（來源已完成，結論見 Solution）
- [ref] [x] 若定案產生新文案需求，已建票或併入 i18n 補齊票  # from 0.1.0-W1-036（來源已完成，結論見 Solution）
