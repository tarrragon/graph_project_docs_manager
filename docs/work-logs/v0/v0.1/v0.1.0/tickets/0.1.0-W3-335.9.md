---
id: 0.1.0-W3-335.9
title: 修補 test-assertion-design 缺引導（G14、G15）
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy:
- 0.1.0-W3-335.8
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 修補 test-assertion-design 缺引導（G14、G15）
when: 前驅票完成後
where:
  layer: 待定義
  files:
  - .claude/skills/test-assertion-design/SKILL.md
  - .claude/skills/test-assertion-design/CHANGELOG.md
why: W3-332 審查與走查確認 agent 依此文件推導元件與測試時被迫猜測或讀出兩種做法；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
how:
  task_type: Documentation
  strategy: 依 chained-examples 規格與 Context Bundle 執行；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
acceptance:
- '[x] 類型 3 命中率與參考等價的二選一判準補齊'
- '[x] 斷言預期值取自 spec 條文的約束補齊'
- '[x] 新約束逐條套〈何處需要範例〉判準並記錄'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T12:10:12'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修補 test-assertion-design 缺引導（G14、G15）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-14 12:09] readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
### G14/G15 缺引導修補與〈何處需要範例〉複驗

**G14**（TAD〈問題類型表〉類型 3 相對計時比較）：原設計原則欄只寫「改用快取命中率或物件參考等價驗證」，無二選一判準。補入約束：「被驗證對象對外承諾『重複取得同一實例』時用參考等價，只承諾『不重算』（結果值相同、不保證同一實例）時用命中率」。

複驗〈何處需要範例〉判準：模擬兩位讀者面對同一份輸入（一段已明確陳述被驗證對象公開承諾內容的快取測試情境），依新約束各自推導。約束為封閉二元映射——承諾詞彙落在「同一實例」或「不重算」其中一類，對應處置唯一。兩位讀者的推導結果必然一致。**結論：未命中，不需要範例**。

**G15**（TAD〈確定性斷言的基礎形態〉功能正確性）：原「說明」欄只寫「輸出值直接比對固定預期值」，未說明預期值來源，且〈不在範圍聲明〉未將其排除。補入約束：「預期值必須取自 spec 條文或 UC 預期結果，並依 test-object-catalogue〈三、契約欄位表〉斷言來源欄格式記錄出處；取自現有實作執行結果者屬 characterization test，依同檔〈五、三種起點的推導程序〉起點 B 標記」。

複驗〈何處需要範例〉判準：模擬兩位讀者面對同一份輸入（一則已知預期值來源的功能正確性斷言），依新約束判定歸類。約束為封閉二分（spec/UC 來源 vs 現有實作來源），對應處置與登記格式各自固定、不重疊。兩位讀者的推導結果必然一致。**結論：未命中，不需要範例**。

兩處新約束皆未再命中〈何處需要範例〉判準，本票範圍內（僅缺引導）已完整處置，未發現需另立缺範例修補的殘留缺口。

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-14 12:15
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

變更摘要：
- `.claude/skills/test-assertion-design/SKILL.md`：〈問題類型表〉類型 3「相對計時比較」設計原則欄補二選一判準（承諾同一實例用參考等價，只承諾不重算用命中率，G14）；〈確定性斷言的基礎形態〉表格後補一句功能正確性預期值出處約束（須取自 spec 條文或 UC 預期結果並依 test-object-catalogue 斷言來源欄格式記錄，取自現有實作執行結果者改標 characterization test 並依起點 B 標記，G15）；`metadata.version` 由 1.3.0 → 1.4.0
- `.claude/skills/test-assertion-design/CHANGELOG.md`：新增 1.4.0 條目對應上述兩項修補
- 兩項新約束已依〈何處需要範例〉判準複驗，均為封閉二元映射，判定不需另補範例（詳見 Solution 章節）
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-335.8 | chars: 244 -->

### Task Reference
- 0.1.0-W3-335.8 what: 修補 test-assertion-design 原文矛盾裁決與過期描述更新（C11、S08）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-335.8 why: W3-332 審查與走查確認 agent 依此文件推導元件與測試時被迫猜測或讀出兩種做法；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 斷言品質三問與 9 類型分族對類型 5、6 的歸類提出統一建議並經用戶簽核後改文  # from 0.1.0-W3-335.8（來源已完成，結論見 Solution）
- [ref] [x] rules 層描述更新為實際涵蓋（含 Dart 與跨語言規則）  # from 0.1.0-W3-335.8（來源已完成，結論見 Solution）
### 修補票共同 Context（PM，2026-09-14）

**缺口清單在哪**：本票涵蓋的缺口編號見 `ticket track full 0.1.0-W3-332.13` Solution；每個缺口的位置錨點、兩位讀者的分歧模擬、子票草稿見其來源子票（W3-332.2–.10）Solution。**W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 優先於 .13 草稿**——草稿漏收、誤歸或數字不符處以 P 項為準。

**規格**：`.claude/skills/skill-design-guide/references/chained-examples.md`（三段鏈式、驗證段兩形態、缺範例與缺引導分界）。

**已定案的用戶裁示**：
- 語言專屬 skill 的違規／修正程式碼對照不受「禁程式語法」約束（例外條款由 W3-335.15 寫入規格）
- 元件庫規格範本佔位文字改為技術中立，框架名移到 L2 註記
- version-bootstrap 的 v02-walkthrough 依新規格重寫整篇

**矛盾類票的簽核流程**：先在 Solution 寫「矛盾位置｜兩方原文｜建議以哪方為準｜理由｜改後文字草稿」，**不改檔**，以 `ticket track set-exit-status` 設 needs_context 並停手回報；PM 取得用戶簽核寫回本票後再續做改文。

**通則**：
- 框架檔不得寫本專案識別符（票號、SPEC 編號、畫面名），範例一律去識別化
- 集合大小改引用權威名稱而非寫死數字；錨點用標題不用行號
- 改動 skill 須同步其 CHANGELOG 與 SKILL.md 版號
- 只改本票 where.files；發現範圍外缺口登記 spawn request，不就地修
### PM 更正：W3-335.14 稽核結論的票號指向（2026-09-14）

W3-335.14 Solution 寫「回寫併入 0.1.0-W3-335.9（R06）與 0.1.0-W3-335.10（R09）」，**票號錯誤**——該處沿用了彙整草稿的 R 編號對應，未對照實際建票。實際承接：

| 項目 | 承接票 |
|------|-------|
| SPEC-001/003 回寫（含 §3.7 無上游出處 11 條、G6/G7/G8 篩選排序欄位） | **0.1.0-W3-335.13** |
| S09 SPEC-004 缺 i18n key | **0.1.0-W3-335.13**（建票時已併入，見其 acceptance） |

W3-335.9／.10 是 test-assertion-design 的引導與範例票，與 SPEC 回寫無關。
