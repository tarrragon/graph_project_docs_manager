---
id: 0.1.0-W3-332.10
title: 走查 SPEC-001 §5 破洞報告、§6 節點詳情、§7 專案切換浮層、§8 狀態轉換提示的元件與測試推導
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-332
children: []
blockedBy:
- 0.1.0-W3-332.8
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: saffron-system-analyst
  history: {}
what: 走查 SPEC-001 §5 破洞報告、§6 節點詳情、§7 專案切換浮層、§8 狀態轉換提示的元件與測試推導
when: 先導走查完成且方法回饋已吸收後
where:
  layer: 待定義
  files:
  - docs/spec/ui/SPEC-001-screen-state-matrix.md::read
  - docs/spec/ui/SPEC-003-interaction-response.md::read
  - docs/spec/ui/SPEC-004-component-library.md::read
why: 走查讓 agent 只持 skill 與規格去推導元件契約與測試情境，被迫猜測之處即缺口；猜測點是區分「skill 缺引導」與「spec 缺描述」的量測依據
how:
  task_type: Analysis
  strategy: 盲推導：只讀畫面鏈 skill 與 SPEC-001／SPEC-003 對應章節，不讀 SPEC-004；逐狀態產出三段鏈式範例（元件選件與排位、契約關鍵欄、Given/When/Then 驗證）；每個猜測點記錄位置錨點、猜了什麼、依據缺在 skill 或 spec。推導完成後才與 SPEC-004 對照，分歧列為猜測誤差或 SPEC-004 自身缺陷。只產表不改檔
acceptance:
- '[x] 範圍內每個狀態皆產出三段鏈式推導結果'
- '[ ] 猜測點表每列含位置錨點、猜測內容、初判缺口來源（skill／spec／兩者）'
- '[ ] 與 SPEC-004 的對照分歧逐項列出並初判歸因'
- '[ ] 走查方法本身的問題（規格不足以操作處）另列一節回饋範例規格子票'
tdd_phase: phase0
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T11:06:55'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

走查 SPEC-001 §5 破洞報告、§6 節點詳情、§7 專案切換浮層、§8 狀態轉換提示的元件與測試推導

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

本票為盲推導實驗，非問題修正。SPEC-001 §5-§8 的元件與測試推導尚未從「skill + SPEC-001/003」視角執行過，無法確認 skill 引導的缺口與 spec 描述的缺口在這四個章節的分布。

### 影響範圍

- SPEC-001 §5 破洞報告（3 狀態）
- SPEC-001 §6 節點詳情（4 狀態）
- SPEC-001 §7 專案切換浮層（3 狀態）
- SPEC-001 §8 狀態轉換提示（判準適用結果表，無獨立畫面狀態）
- SPEC-003 §3.5、§3.6、§3.7（對應互動反應）
- SPEC-003 §2.13（暫態提示判準 T）

### 相關 Error Pattern

無直接相關。本票為方法驗證實驗。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 盲推導結果

### §5 破洞報告（3 狀態）

#### 狀態 1：掃描中（`state-gaps-scanning`）

【輸入】SPEC-001 §5「掃描中」列：顯示＝骨架＋進度，可用操作＝取消，退出路徑＝取消→返回；完成→有／無破洞。SPEC-003 §3.5 生命週期：首次可見且圖已建立→自動進入掃描中。SPEC-003 §2.5 取消契約。SPEC-003 §3.5 動畫提示：shimmer `Motion.skeletonCycle`。

【產出】`頁面框架[導覽側欄, 主區[骨架佔位(shimmer), 取消按鈕]]`

關鍵契約欄：
- 骨架佔位：shimmer 動畫 `Motion.skeletonCycle`；`disableAnimations` 時靜態
- 取消按鈕：依 §2.5 取消契約，目標態為 `returnTo` 指定畫面；`returnTo` 為 `null` 時為 `nav-page-domain`

【驗證】
- Given 使用者進入破洞報告且圖已建立 When 畫面首次可見 Then 自動進入掃描中，顯示骨架佔位帶 shimmer 動畫與取消按鈕
- Given 掃描中 When 使用者點擊取消 Then 切至 `returnTo` 指定畫面（`null` 時為 Domain 視圖）
- Given 掃描中 When 掃描完成且破洞數為 0 Then 轉為無破洞狀態（cross-fade）
- Given 掃描中 When 掃描完成且破洞數 > 0 Then 轉為有破洞狀態（cross-fade）

猜測點：無。SPEC-001 §5 + SPEC-003 §3.5 + §2.5 已覆蓋所有欄位。

---

#### 狀態 2：無破洞（`state-gaps-none`）

【輸入】SPEC-001 §5「無破洞」列：顯示＝「未偵測到破洞」＋掃描範圍說明，可用操作＝重新掃描、導覽。SPEC-003 §3.5 互動反應：重新掃描 `action-gaps-rescan`。SPEC-003 §3.5 生命週期：再次可見不重新掃描。

【產出】`頁面框架[導覽側欄, 主區[結果訊息(「未偵測到破洞」), 掃描範圍說明, 重新掃描按鈕]]`

關鍵契約欄：
- 結果訊息：靜態文字，i18n key **G1 猜測：未知**
- 掃描範圍說明：**G2 猜測：形態未知——是路徑列表、數量統計、或文字描述？** SPEC-001 寫「掃描範圍說明」但未定義其內容結構
- 重新掃描按鈕：點擊→`state-gaps-scanning` 出現

【驗證】
- Given 無破洞狀態 When 畫面渲染完成 Then 顯示「未偵測到破洞」訊息與掃描範圍說明
- Given 無破洞狀態 When 使用者點擊重新掃描 Then 現有結果消失，掃描中骨架出現（SPEC-003 §3.5 動畫：舊結果立即被骨架取代）

---

#### 狀態 3：有破洞（`state-gaps-found`）

【輸入】SPEC-001 §5「有破洞」列：顯示＝依類別分節，各項帶檔案與行號，可用操作＝點項→開啟原始檔並定位、重新掃描。SPEC-003 §3.5 互動反應：破洞項 `card-gaps-<itemId>` 點擊→外部開啟；分節收合 `expander-gaps-<category>`；分節捲動 `scroll-gaps-sections`。SPEC-003 §3.5 系統通知 `ScanNotifier.activated` 與 SnackBar fallback `viewGapsAction`。

【產出】`頁面框架[導覽側欄, 主區[重新掃描按鈕, 可捲動區域[分節收合容器[破洞卡片*N]]]]`

關鍵契約欄：
- 分節收合容器：每個類別一個，錨點 `expander-gaps-<category>`，點擊→項目出現或消失，高度變化 `Motion.transition`
- 破洞卡片：顯示檔案路徑與行號；點擊→外部開啟（§2.2 外部開啟契約）；三種結果：`opened`→SnackBar `openedExternallyMessage`、`notFound`→SnackBar `sourceFileNotFoundSnackbarMessage` 帶「重新掃描」動作、`failed`→SnackBar `externalOpenFailedMessage`
- 系統通知：掃描完成時視窗非前景→macOS 系統通知（權限 denied 時 SnackBar fallback `viewGapsAction`）

【驗證】
- Given 有破洞狀態 When 畫面渲染 Then 依類別分節顯示，各項帶檔案路徑與行號
- Given 有破洞 When 點擊破洞項且檔案存在 Then 以系統預設方式開啟，SnackBar 顯示 `openedExternallyMessage`
- Given 有破洞 When 點擊破洞項且檔案不存在 Then SnackBar 顯示 `sourceFileNotFoundSnackbarMessage` 帶「重新掃描」動作
- Given 有破洞 When 點擊分節 header Then 該類別展開或收合（`Motion.transition`）
- Given 掃描完成時使用者不在破洞報告頁 When 掃描完成 Then 發送 macOS 系統通知（權限 denied 時改 SnackBar fallback）

猜測點：無。SPEC-001 §5 + SPEC-003 §3.5 + §2.2 已覆蓋。

---

### §5 猜測點

| 編號 | 位置錨點 | 猜了什麼 | 替代選項 | 缺口類別 |
|------|---------|---------|---------|---------|
| G1 | SPEC-001 §5「無破洞」列「顯示」欄 | 「未偵測到破洞」的 i18n key 未知 | — | spec 未描述（SPEC-001 不定 i18n key，可能由 SPEC-004 承載） |
| G2 | SPEC-001 §5「無破洞」列「顯示」欄 | 「掃描範圍說明」的內容結構未知——路徑列表？數量統計？文字描述？ | 三種皆可能 | spec 未描述（SPEC-001 僅寫名詞，未定義結構） |

---

### §6 節點詳情（4 狀態）

#### 狀態 1：未選節點（`state-nodeDetail-unset`）

【輸入】SPEC-001 §6「未選節點」列：顯示＝空狀態元件＋「尚未選取節點」，可用操作＝前往追溯視圖。SPEC-003 §3.6：前進動作 `action-nodeDetail-goto-traceability`，返回鍵不渲染。

【產出】`頁面框架[導覽側欄, 主區[空狀態元件(訊息=「尚未選取節點」, 前進動作=前往追溯視圖)]]`

關鍵契約欄：
- 空狀態元件：跨畫面共用元件（§5 無破洞也可能用同型，見先導走查 G13），訊息為 i18n key，前進動作錨點 `action-nodeDetail-goto-traceability`→jump 至追溯視圖
- 返回鍵：`action-nodeDetail-back` 不渲染（`returnTo` 為 `null`）

【驗證】
- Given 經導覽列進入節點詳情且無選定節點 When 畫面渲染 Then 顯示空狀態元件帶「尚未選取節點」訊息與前往追溯視圖按鈕
- Given 未選節點 When 點擊前往追溯視圖 Then jump 至追溯視圖

猜測點：無。

---

#### 狀態 2：正常（`state-nodeDetail-normal`）

【輸入】SPEC-001 §6「正常」列：顯示＝全頁內容＋關聯右欄，可用操作＝開啟原始檔、點關聯→跳轉。SPEC-003 §3.6 互動反應：返回 `action-nodeDetail-back`→`returnTo`；開啟原始檔 `action-nodeDetail-open-source`→外部開啟；關聯項 `card-nodeDetail-relation-<nodeId>`→主欄內容替換（`returnTo` 不變）；主欄捲動 `scroll-nodeDetail-content`；右欄捲動 `scroll-nodeDetail-relations`。

【產出】`頁面框架[導覽側欄, 主區[工具列[返回按鈕, 開啟原始檔按鈕], 雙欄佈局[主欄(可捲動, 節點內容), 右欄(可捲動, 關聯項卡片*N)]]]`

關鍵契約欄：
- 雙欄佈局：主欄與右欄各自獨立捲動；關聯項點擊→主欄內容替換（cross-fade `Motion.transition`，主欄 offset 歸零，右欄 offset 保留）
- 返回按鈕：`action-nodeDetail-back`→`returnTo`；`returnTo` 為 `null` 時不渲染
- 開啟原始檔按鈕：外部開啟契約（§2.2），三種結果同 §5
- 關聯項卡片：`card-nodeDetail-relation-<nodeId>`，點擊→同畫面替換
- **G3 猜測：工具列是否為獨立容器元件？** skill step-2 路徑 A 步驟 4 要求逐畫面列出佈局結構含框架層（工具列／內容），但 SPEC-001/003 未明寫節點詳情有工具列——我從「可用操作＝開啟原始檔」與「返回」推導出需要承載控制項的容器

【驗證】
- Given 正常狀態 When 畫面渲染 Then 顯示全頁內容（主欄）與關聯列表（右欄）
- Given 正常 When 點擊關聯項 Then 主欄內容替換為該節點（cross-fade），主欄 offset 歸零，右欄 offset 保留，`returnTo` 不變
- Given 正常 When 點擊開啟原始檔且檔案存在 Then 外部開啟，SnackBar `openedExternallyMessage`
- Given 正常 When 點擊開啟原始檔且檔案不存在 Then 狀態轉為 `state-nodeDetail-missing`（不出現 SnackBar）
- Given 正常 When 點擊返回 Then 切至 `returnTo`

---

#### 狀態 3：部分損壞（`state-nodeDetail-partial`）

【輸入】SPEC-001 §6「部分損壞」列：顯示＝可讀欄位＋損失欄位標示「因檔案損壞而無法讀取」，可用操作＝同正常＋跳轉破洞報告。

【產出】`頁面框架[導覽側欄, 主區[工具列[返回按鈕, 開啟原始檔按鈕, 跳轉破洞報告按鈕], 雙欄佈局[主欄(可捲動, 節點內容含損壞標示), 右欄(可捲動, 關聯項卡片*N)]]]`

關鍵契約欄：
- 損壞標示：損失欄位以「因檔案損壞而無法讀取」替代，靜態不閃爍（§3.6 動畫提示）
- 跳轉破洞報告：`action-nodeDetail-goto-gaps`→jump 至 `nav-page-gaps`，`returnTo` 設為 nodeDetail

【驗證】
- Given 部分損壞 When 畫面渲染 Then 可讀欄位正常顯示，損失欄位標示「因檔案損壞而無法讀取」（靜態）
- Given 部分損壞 When 點擊跳轉破洞報告 Then jump 至破洞報告

猜測點：無。

---

#### 狀態 4：原始檔已消失（`state-nodeDetail-missing`）

【輸入】SPEC-001 §6「原始檔已消失」列：顯示＝「原始檔已不存在」＋最後已知路徑，可用操作＝重新整理、返回。SPEC-003 §3.6 重新整理三分支。

【產出】`頁面框架[導覽側欄, 主區[工具列[返回按鈕], 消失狀態元件(訊息=「原始檔已不存在」, 路徑=最後已知路徑, 重新整理按鈕)]]`

關鍵契約欄：
- 消失狀態元件：**G4 猜測：是否為獨立元件還是空狀態元件的變體？** SPEC-001 的描述（訊息＋路徑＋重新整理按鈕）與空狀態元件（訊息＋前進動作）結構相似但語意不同——空狀態的退出路徑是「前往其他畫面」，消失狀態的退出路徑是「重新整理（可能恢復）或返回」。依 skill step-2 存在必要性的判準（退出路徑不同→不歸併），應為獨立元件
- 重新整理三分支（§3.6）：檔案仍不存在→維持 missing + SnackBar；存在且完整→轉 normal；存在但有斷點→轉 partial。三分支皆須在 `Motion.cancelDeadline` 內抵達
- 返回按鈕：`action-nodeDetail-back`→`returnTo`

【驗證】
- Given 原始檔已消失 When 畫面渲染 Then 顯示「原始檔已不存在」與最後已知路徑
- Given 原始檔已消失 When 點擊重新整理且檔案仍不存在 Then 維持 missing，SnackBar 告知
- Given 原始檔已消失 When 點擊重新整理且檔案存在且完整 Then 轉為正常狀態
- Given 原始檔已消失 When 點擊重新整理且檔案存在但有斷點 Then 轉為部分損壞狀態
- Given 重新整理 When 超過 `Motion.cancelDeadline` Then **G5 猜測：超時行為未定義**——SPEC-003 §3.6 寫「三分支皆須在 `Motion.cancelDeadline` 內抵達」但未寫超時後的行為

---

### §6 猜測點

| 編號 | 位置錨點 | 猜了什麼 | 替代選項 | 缺口類別 |
|------|---------|---------|---------|---------|
| G3 | SPEC-001 §6「正常」列「可用操作」欄 + skill step-2 路徑 A 步驟 4（容器盤點） | 節點詳情需要一個工具列容器承載返回按鈕與開啟原始檔按鈕 | 控制項直接嵌在主欄頂部而非獨立工具列 | skill 缺引導（step-2 要求列出佈局結構含框架層，但 SPEC-001/003 未明寫此畫面的框架層結構——「全頁內容＋關聯右欄」暗示雙欄，未提工具列） |
| G4 | SPEC-001 §6「原始檔已消失」列「顯示」欄 + skill step-2 共用規則一（存在必要性） | 消失狀態元件應為獨立元件而非空狀態元件變體（退出路徑不同） | 歸為空狀態元件的 `missing` 變體 | skill 缺引導（step-2 存在必要性判準是退出路徑，但 SPEC-001 的描述結構相似使判斷不確定——skill 沒有「結構相似但退出路徑不同」的範例） |
| G5 | SPEC-003 §3.6 重新整理三分支 | `Motion.cancelDeadline` 超時後行為未定義 | 超時→顯示錯誤 SnackBar / 超時→維持 missing | spec 未描述（§3.6 寫 deadline 約束但未寫超時後的結果） |

---

### §7 專案切換浮層（3 狀態）

#### 狀態 1：收合（`state-switcher-collapsed`）

【輸入】SPEC-001 §7「收合」列：顯示＝側欄頂端顯示目前專案名，可用操作＝點擊展開。SPEC-003 §3.7：入口 `project-switcher-entry` 點擊→展開。

【產出】`側欄[專案切換入口(顯示=目前專案名)]`（入口嵌在導覽側欄頂端）

關鍵契約欄：
- 專案切換入口：錨點 `project-switcher-entry`，顯示目前專案名，點擊→浮層展開

【驗證】
- Given 浮層收合 When 畫面渲染 Then 側欄頂端顯示目前專案名
- Given 收合 When 點擊入口 Then 浮層展開（淡入＋自入口向下展開，`Motion.overlay`）

猜測點：無。

---

#### 狀態 2：展開（`state-switcher-expanded`）

【輸入】SPEC-001 §7「展開」列：顯示＝最近專案清單＋健康徽章＋選擇其他，可用操作＝選專案、選資料夾、點外部收合。SPEC-003 §3.7：最近專案項 `card-switcher-recent-<index>`；不可用項 `enabled=false` 帶常駐文字；選擇其他 `action-switcher-choose-folder`；焦點限制於浮層內（§2.10）；背景導覽項不可點。

【產出】`浮層覆蓋[可捲動區域[專案項卡片*N(含健康徽章)], 選擇其他按鈕]`

關鍵契約欄：
- 專案項卡片：`card-switcher-recent-<index>`，點擊→浮層收合＋全域重置＋`state-domain-loading`；不可用項 `enabled=false` 帶常駐文字說明原因（不用 tooltip）
- 健康徽章：`badge-switcher-health-<index>`，非互動；點擊不產生反應且不呈現按鈕形態（§2.2 可點性辨識）
- 選擇其他按鈕：`action-switcher-choose-folder`→開啟系統資料夾選擇器；選定→收合並載入；取消→浮層維持展開
- 收合觸發：Esc→收合＋焦點回 `project-switcher-entry`；點浮層外→收合
- 探測逾時處理（§3.7）：逾時項 `enabled=false`，常駐文字含說明；逾時期間浮層仍可操作

【驗證】
- Given 浮層展開 When 畫面渲染 Then 顯示最近專案清單帶健康徽章與「選擇其他」按鈕
- Given 展開 When 點擊可用專案項 Then 浮層收合（反向 `Motion.overlay`），六頁狀態全部重置，Domain 視圖進入載入中
- Given 展開 When 點擊不可用專案項 Then 無反應（`enabled=false`）
- Given 展開 When 按 Esc Then 浮層收合，焦點回到入口
- Given 展開 When 點浮層外 Then 浮層收合，不改變當前專案
- Given 展開 When 點擊「選擇其他」Then 開啟系統資料夾選擇器
- Given 展開 When 健康徽章存在 Then 不呈現按鈕形態，點擊無反應

猜測點：

| 編號 | 位置錨點 | 猜了什麼 | 替代選項 | 缺口類別 |
|------|---------|---------|---------|---------|
| G6 | SPEC-001 §7「展開」列「顯示」欄 | 專案項卡片的欄位結構——顯示專案名、路徑、健康徽章，是否還有其他欄位（最後開啟時間？版本？） | 可能含更多欄位 | spec 未描述（SPEC-001 僅寫「最近專案清單＋健康徽章」，未列卡片欄位明細） |

---

#### 狀態 3：無最近專案（`state-switcher-no-recent`）

【輸入】SPEC-001 §7「無最近專案」列：顯示＝僅「選擇資料夾…」，可用操作＝選擇資料夾。SPEC-003 §3.7：展開時機僅由入口或阻擋狀態出口觸發。

【產出】`浮層覆蓋[選擇資料夾按鈕]`

關鍵契約欄：
- 選擇資料夾按鈕：同展開狀態的「選擇其他」

【驗證】
- Given 無最近專案 When 浮層展開 Then 僅顯示「選擇資料夾…」
- Given 無最近專案 When 點擊選擇資料夾 Then 開啟系統資料夾選擇器；選定→收合並載入
- Given 無最近專案 When 按 Esc Then 浮層收合

猜測點：無。

---

### §8 狀態轉換提示（無獨立畫面狀態）

§8 不含獨立畫面，是 §1-§7 每個狀態轉換的提示判定結果表。以三段鏈式推導 §8 的方式是：驗證判準 T 的適用結論是否與 §5-§7 的互動反應一致。

【輸入】SPEC-001 §8.1 逐狀態表中 §5-§7 的列：
- §5 掃描中→否（T2）；無破洞→條件式（T1）；有破洞→條件式（T1）
- §6 未選節點→否（T2）；正常→否（T2）；部分損壞→否（T2）；原始檔已消失→否（T2）
- §7 收合→否（T2）；展開→否（T2）；無最近專案→否（T2）

SPEC-003 §2.13 判準 T：T1（使用者是否在觸發畫面且視窗前景）→T2（是否有可見狀態轉換）→T3（是否需保留資訊）→T4（是否附帶動作）

【產出】我的盲推導判定：

| 節 | 狀態 | 我的判定 | 推理 |
|----|------|---------|------|
| §5 | 掃描中 | 否 | 使用者主動進入掃描，骨架即回饋（T2 是） |
| §5 | 無破洞 | 條件式 | 使用者可能不在此頁（§2.8 L1 掃描跨頁繼續）；在此頁→否（T2：狀態轉換可見）；不在→升級通道（§2.2 系統通知） |
| §5 | 有破洞 | 條件式 | 同上 |
| §6 | 未選節點 | 否 | 進入畫面即渲染（T2 是） |
| §6 | 正常 | 否 | jump 進入即渲染（T2 是） |
| §6 | 部分損壞 | 否 | 損失欄位標示即結果（T2 是） |
| §6 | 原始檔已消失 | 否 | 狀態轉換即回饋，SPEC-003 §3.6 明寫不出現 SnackBar（T2 是） |
| §7 | 收合 | 否 | 靜止態（T2 是，無轉換可告知） |
| §7 | 展開 | 否 | 點擊即展開（T2 是） |
| §7 | 無最近專案 | 否 | 同展開（T2 是） |

【驗證】（檢驗問句形態）
- 檢驗：§5 無破洞/有破洞的「條件式」判定是否與 SPEC-003 §2.2 系統層通知一致？→是，§2.2 明確定義了掃描完成時視窗非前景的系統通知路徑
- 檢驗：§6 原始檔已消失的「否」判定是否與 §3.6 互動反應表一致？→是，§3.6 明寫「不出現 SnackBar」
- 檢驗：§8.2 的三則「不改變狀態但需要提示的事件」是否在我的推導中被覆蓋？→**G7 猜測**：§8.2 列出三則事件（外部開啟成功/失敗、重新整理仍不存在），我的 §5/§6 推導已涵蓋這些的 SnackBar 回饋，但我是否遺漏了其他不改變狀態的事件？

猜測點：我的判定與 §8.1 完全一致。唯一猜測在於 §8.2。

| 編號 | 位置錨點 | 猜了什麼 | 替代選項 | 缺口類別 |
|------|---------|---------|---------|---------|
| G7 | SPEC-001 §8.2 | §8.2 的三則事件是否窮盡了 §5-§7 範圍內「不改變狀態但需要提示」的全部事件 | 可能遺漏專案切換浮層中的某些事件 | spec 未描述（§8.2 明寫「本表只作路由」而非窮盡清單，無法從本節判斷是否完整） |

---

### 猜測點彙整表

| 編號 | 位置錨點 | 猜了什麼 | 替代選項 | 缺口類別 |
|------|---------|---------|---------|---------|
| G1 | SPEC-001 §5「無破洞」列「顯示」欄 | i18n key 未知 | — | spec 未描述 |
| G2 | SPEC-001 §5「無破洞」列「顯示」欄 | 「掃描範圍說明」內容結構未知 | 路徑列表/數量統計/文字描述 | spec 未描述 |
| G3 | SPEC-001 §6「正常」列 + skill step-2 路徑 A 步驟 4 | 節點詳情需要工具列容器 | 控制項嵌在主欄頂部 | skill 缺引導（step-2 §容器盤點要求列框架層，但 spec 未明寫此畫面框架層結構） |
| G4 | SPEC-001 §6「原始檔已消失」列 + skill step-2 §存在必要性 | 消失狀態是獨立元件而非空狀態元件變體 | 歸為空狀態元件 `missing` 變體 | skill 缺範例（step-2 存在必要性判準是退出路徑，appendix-examples §一的正例是「空清單 vs 阻擋」但無「消失/遺失」情境，結構相似但退出路徑不同時讀出兩種做法） |
| G5 | SPEC-003 §3.6「重新整理三分支」 | `Motion.cancelDeadline` 超時後行為未定義 | 超時→錯誤 SnackBar / 超時→維持 missing | spec 未描述 |
| G6 | SPEC-001 §7「展開」列「顯示」欄 | 專案項卡片欄位結構不完整 | 可能含更多欄位 | spec 未描述 |
| G7 | SPEC-001 §8.2 | §8.2 三則事件是否窮盡 §5-§7 全部「不改變狀態但需要提示」的事件 | 可能有遺漏 | spec 未描述（§8.2 自述「只作路由」） |

### 與 SPEC-004 的對照分歧

推導完成後讀 SPEC-004 §3.6 歸屬對照（§5-§7 列）、§3.7 核定記錄、§4.22 MissingSourceState、§4.9 RecentProjectItem、§4.42 SwitcherOverlay、§4.32 Toolbar。

| 猜測點 | 盲推導猜測 | SPEC-004 實際 | 分歧？ | 上游出處 | 歸因 |
|--------|-----------|--------------|--------|---------|------|
| G1 | i18n key 未知 | §4.0.6 i18n 表有 `noGapsFoundMessage` / `scanScopeDescription` | **有分歧** | SPEC-004 §4.0.6 為本檔自行編排的 i18n key 總表，SPEC-001/003 未列 i18n key | SPEC-001/003 無 i18n key 屬預期分工（i18n key 由 SPEC-004 定義），但上游出處即 SPEC-004 §4.0.6 本身，有出處 |
| G2 | 「掃描範圍說明」結構未知 | §3.6 歸屬：`EmptyState.page`（說明 slot）——掃描範圍說明為 `EmptyState` 的說明 slot，靜態文字 | **有分歧** | SPEC-004 §3.6 §5 無破洞列，元件為 `EmptyState.page` 含說明 slot | 盲推導未預期「無破洞」狀態會使用 `EmptyState`——「無破洞」有結果（未偵測到破洞）而非「空」；SPEC-004 §3.7 第 3 項核定理由為「退出路徑跳轉離開畫面」，與空狀態判準一致。**漏讀**：SPEC-003 §3.5 生命週期「再次可見→不重新掃描，顯示既有結果；要重掃須按 action-gaps-rescan」——重新掃描按鈕即前進動作，與空狀態元件的「訊息＋前進動作」一致 |
| G3 | 節點詳情需要工具列容器 | §3.7 第 17 項核定：返回與開啟原始檔按鈕位於 `SplitRow.header` 右側 `ButtonRow` | **有分歧** | SPEC-004 §3.7 第 17 項：「與 §5 重新掃描一致，頁首右側為頁面級動作區」；SPEC-003 §2.4 規則 4：返回鍵由頁面框架統一渲染 | 我猜「工具列容器」，SPEC-004 用 `SplitRow.header` + `ButtonRow`，不用 `Toolbar`。上游出處為 SPEC-003 §2.4（返回鍵統一渲染）+ SPEC-004 §3.7 第 17/18 項（頁首右側為動作區）。**漏讀**：SPEC-003 §2.4 規則 4 有明確條文但我推導時未查該節 |
| G4 | 消失狀態應為獨立元件（退出路徑不同） | §3.7 第 3 項核定：`missing` 改為獨立元件 `MissingSourceState`；理由：退出路徑差異指離開的層級——`missing` 的重新整理留在畫面內，`page` 跳轉離開畫面 | **無分歧** | SPEC-004 §3.7 第 3 項核定，引用 skill 明文 | 盲推導正確。skill step-2 存在必要性的退出路徑判準已足夠決定，但 skill 缺「結構相似但退出路徑不同」的範例使判斷過程不確定——SPEC-004 核定記錄回溯到同一判準並給了明確理由 |
| G5 | `Motion.cancelDeadline` 超時後行為未定義 | §4.22 MissingSourceState 回饋契約「等待與結果」列：「`Motion.cancelDeadline` 保證三分支之一必於期限內抵達，不逾時停滯；服務不可用：同失敗段（檔案系統層級不可用亦歸入「仍不存在」分支處理，無獨立表現）」 | **有分歧** | SPEC-004 §4.22「等待與結果」列的逾時子段，引用 SPEC-003 §3.6 | SPEC-004 明確聲明不逾時停滯（三分支保證之一必達），而非定義超時後行為——即「超時」這個狀態不存在。SPEC-003 §3.6 的 deadline 約束是品質保證（必在 deadline 內返回結果）而非超時處理規格。**漏讀**：我將 deadline 約束讀成超時觸發條件，但 SPEC-004 的解讀為「保證不超時」 |
| G6 | 專案項卡片欄位不完整 | §4.9 RecentProjectItem：圖示 + 名稱 `textTitle` + 摘要（節點數·票數）+ 健康徽章 slot + 不可用原因常駐文字 + selected/disabled 狀態 | **有分歧** | SPEC-004 §4.9 用途段，欄位為 SPEC-004 自行設計 | 盲推導猜「專案名、路徑、健康徽章」，SPEC-004 實際為「圖示＋名稱＋摘要（節點數·票數）＋健康徽章」——無路徑但有摘要統計。上游出處：SPEC-001 §7 僅寫「最近專案清單＋健康徽章」，欄位明細為 SPEC-004 自行設計，無 SPEC-001/003 上游。**SPEC-004 無上游出處**——這些欄位（圖示、摘要統計）不在 SPEC-001/003 的任何描述中 |
| G7 | §8.2 三則事件是否窮盡 | SPEC-004 未額外提及 §5-§7 範圍內其他「不改變狀態但需提示」的事件 | **無分歧** | SPEC-001 §8.2 + SPEC-003 §3.5/§3.6/§3.7 | §8.2 的三則事件在 SPEC-003 逐畫面規格中皆有對應條文，未發現遺漏 |

### 分歧統計

| 類型 | 數量 | 猜測點 |
|------|------|--------|
| 無分歧 | 2 | G4, G7 |
| 有分歧 | 5 | G1, G2, G3, G5, G6 |

### 分歧歸因分析

| 歸因類別 | 猜測點 | 說明 |
|---------|--------|------|
| 漏讀（允許輸入內有答案） | G2, G3, G5 | G2：SPEC-003 §3.5 生命週期段的「要重掃須按 action-gaps-rescan」暗示重新掃描即前進動作，與空狀態元件一致。G3：SPEC-003 §2.4 規則 4 明文返回鍵統一渲染。G5：deadline 約束是品質保證而非超時觸發條件 |
| 預期分工（SPEC-004 職責，上游有出處） | G1 | i18n key 由 SPEC-004 定義 |
| SPEC-004 無上游出處 | G6 | 專案項卡片欄位（圖示、摘要統計）不在 SPEC-001/003 的任何描述中，為 SPEC-004 撰寫者自行設計 |

### 走查方法本身的問題（回饋範例規格子票）

1. **SPEC-003 §2.4（退出路徑的四種導航反應）在推導「控制項承載容器」時是關鍵輸入但容易被跳過**：G3 的分歧源於我推導時只讀了 §3.6 互動反應表而未回查 §2 通用機制。skill step-2 路徑 A 步驟 4（容器盤點）要求「逐畫面列出佈局結構含框架層」，但未引導讀者回查通用機制中的統一渲染條文。建議：step-2 容器盤點步驟補充「頁面級控制項（返回、全頁動作）先查 spec 通用機制是否有統一承載規則」。

2. **「空狀態元件」的語意判準比外觀判準更寬**：G2 的分歧暴露盲推導傾向依字面理解——「無破洞」有結果不像「空」，但 SPEC-004 核定以退出路徑（跳轉離開）判定為空狀態元件。skill 的存在必要性判準以退出路徑為準，此處一致；但「空」的直覺語意容易誤導推導者跳過判準。建議：appendix-examples 補一例「看起來不空但退出路徑屬空狀態」。

3. **deadline 約束的解讀需要服務類型分類的支撐**：G5 的分歧源於我將 `Motion.cancelDeadline` 讀成超時觸發條件而非品質保證。SPEC-003 §2.12 的服務類型分類（短暫非同步 vs 長時操作）決定了 deadline 的語意——短暫非同步保證不逾時。skill 未引導讀者在遇到 deadline 相關條文時查閱服務類型分類。建議：step-3 回饋契約的「等待與結果」欄提示「先判定服務類型再填逾時行為」。

### Spawn 落地確認

本票為純分析票，Solution 無 spawn 規劃。7 個猜測點的分歧全部記錄於本票票面，不衍生實作票（衍生決策由父票 0.1.0-W3-332 彙整）。

### 結論

走查完成 §5 破洞報告（3 狀態）、§6 節點詳情（4 狀態）、§7 專案切換浮層（3 狀態）、§8 狀態轉換提示（10 列判定）的盲推導。7 個猜測點中 2 個與 SPEC-004 一致、5 個有分歧。分歧中 3 個為漏讀（允許輸入內有答案），1 個為預期分工（i18n key），1 個為 SPEC-004 無上游出處（專案項卡片欄位）。

與先導走查（332.8）比較：本批漏讀率 3/7（43%）高於先導的 1/16（6%），主因是 SPEC-003 §2 通用機制章節的交叉引用密度在 §5-§7 比 §4 更高——§4 的互動反應較自足，§5-§7 的元件承載與退出路徑大量依賴 §2.2/§2.4/§2.12/§2.13 的通用規則。方法修正第 1 項（「先窮盡允許輸入內的交叉引用」）確實改善了對漏讀的識別能力（先導的 G2 被錯記為 spec 缺口，本批的 G2/G3/G5 正確歸為漏讀），但走查者仍需更系統性地回查 §2 通用機制。

### ANA 後續 Ticket 說明

本票為父票 0.1.0-W3-332 的走查子票，分析結論（7 個猜測點、5 個分歧、3 項方法回饋）全部回饋至父票彙整。本票不獨立衍生後續 ticket，理由同 332.8：分歧中 1 個屬預期分工、3 個為漏讀（方法改善而非規格修正）、1 個為 SPEC-004 無上游出處（由父票決定是否建修正票）。方法改進項屬執行調整由父票派發時納入。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-332.8 | chars: 276 -->

### Task Reference
- 0.1.0-W3-332.8 what: 走查 SPEC-001 §4 Ticket 清單的元件與測試推導（先導畫面）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-332.8 why: 走查讓 agent 只持 skill 與規格去推導元件契約與測試情境，被迫猜測之處即缺口；猜測點是區分「skill 缺引導」與「spec 缺描述」的量測依據（先導：六狀態含疊加態，走查方法的缺陷在此先暴露再調整後續批次）（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 範圍內每個狀態皆產出三段鏈式推導結果  # from 0.1.0-W3-332.8（來源已完成，結論見 Solution）
- [ref] [x] 猜測點表每列含位置錨點、猜測內容、初判缺口來源（skill／spec／兩者）  # from 0.1.0-W3-332.8（來源已完成，結論見 Solution）
- [ref] [x] 與 SPEC-004 的對照分歧逐項列出並初判歸因  # from 0.1.0-W3-332.8（來源已完成，結論見 Solution）
- [ref] [x] 走查方法本身的問題（規格不足以操作處）另列一節回饋範例規格子票  # from 0.1.0-W3-332.8（來源已完成，結論見 Solution）
### 先導走查（W3-332.8）後的方法修正（PM，2026-09-14）——本節優先於上節

PM 審查先導結果，發現三個會使歸因失真的方法缺陷，本批必須照下列修正執行：

**1. 記猜測點前，先窮盡允許輸入內的交叉引用。** 先導的 G2（預估耗時）被記為 spec 缺口，但 SPEC-003 §3.4 同節內即有「未載入態不顯示預估耗時」的覆寫條文，走查者讀了該節卻漏看。允許輸入已回答的問題不是缺口，記為「漏讀」。

**2. 猜測點表的「缺口類別」改為五選一（取代原「初判缺口來源」）**

| 類別 | 判定 |
|------|------|
| spec 未描述 | 允許輸入全數查過仍無答案 |
| spec 間矛盾 | 允許輸入內兩處說法不同且無覆寫條文 |
| skill 缺引導 | 須指名查過的 skill 步驟／章節錨點，並說明它為何沒決定此事；無錨點不收 |
| skill 缺範例 | 同上須錨點；該處有文字約束但你讀出兩種以上做法 |
| 漏讀 | 允許輸入內其實有答案（對照 SPEC-004 時才發現亦歸此類，並補記答案所在錨點） |

**3. SPEC-004 對照時加「上游出處」欄。** 分歧處 SPEC-004 有值時，追查該值能否在 SPEC-001／SPEC-003／UC／domain-map／proposals／tech-decisions 或 SPEC-004 自身的核定記錄找到出處。有出處 → 你漏讀或 skill 沒引導你去找；**無出處 → 標「SPEC-004 無上游出處」**，不得歸為「預期分工缺口、不需修正」。先導把篩選維度、排序欄、卡片欄位歸為預期分工缺口，但未查這些值從何而來；若無上游，代表前一位規格撰寫者同樣是猜的。

**4.【產出】段的選件與排位可用組合樹記法**：`容器[子件, 子件]`，名稱用 spec 內的中性元件名。此記法屬 ASCII 線框的一種，與 SPEC-004 對照成本較低。

**5. 讀取範圍**：畫面鏈 skill 除 SKILL.md 外，須讀到做判斷時實際依據的 references 章節，並在 skill 類猜測點寫出錨點。
