---
id: 0.1.0-W3-074
title: 決策 跨 app 邊界功能的驗證層：雙層測試策略是否加第三類，及各功能的驗證形態
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-064
dispatch_reason: ''
decision_tree_path:
  entry_point: 測試策略缺口（實作後發現既有策略不涵蓋）
  final_decision: ANA 決策：盤點跨邊界功能、評估驗證形態、定案後寫入 tech-decisions 與 CLAUDE.md §6
  rationale: 缺口為策略層而非單票問題；已有三個功能落在縫隙（系統通知、外部開啟、資料夾選擇），逐票各自決定會產生三種不一致做法
who:
  current: saffron-system-analyst
  history: {}
what: 決策 跨 app 邊界功能的驗證層：雙層測試策略是否加第三類，及各功能的驗證形態
when: 0.1.0-W3-064 完成後（該票的 acceptance 2、3 待本決策定案才知道怎麼執行）；早於下一個跨邊界功能實作
where:
  layer: Domain
  files:
  - docs/tech-decisions.md
  - CLAUDE.md
  - integration_test::read
  - test::read
  - lib/services/macos_scan_notifier.dart::read
  - lib/workspace/workspace_repository.dart::read
  - lib/screens/gap_report/gap_report_screen.dart::read
why: 'CLAUDE.md §6 定測試策略為雙層（`test/` 契約 + `integration_test/` 行為），該策略隱含假設「行為層可用 integration_test 無頭驗證」。跨出 app 邊界的功能不在此假設內——系統通知需回應權限對話框並觀察通知中心、外部開啟需觀察系統預設程式、資料夾選擇需操作原生對話框、doc CLI 呼叫需真實 process。這三類目前**只有 fake 測試**，平台行為從未被驗證。


  具體卡住的是 0.1.0-W3-064：SPEC-003 §2.2「實作票驗證」段明文要求四項平台事實由實機測得後回填（`UNUserNotificationCenter` 在沙盒關閉與 Developer ID 簽章下的可用性、debug build 未簽章時授權請求是否直接回錯、macOS `AppLifecycleState` 的 inactive／hidden 對應視窗失焦與最小化的實際行為、撤回已送達通知的 API 效果），該票 acceptance 2、3 因此未勾。同類的 `ExternalOpener`（W1-036）亦然。


  `.claude/skills/verify/` 不存在，session 啟動每次警告「驗證環境未就緒：找不到驗證配方」，但無人承接。PROP-005 的 0.1 整合測試定義為「七個畫面與浮層的全部狀態皆能渲染、不溢位」，斷言來源 SPEC-001，不涵蓋跨邊界功能——它們是 0.1 規劃後才加入的。'
how:
  task_type: Analysis
  strategy: '1. **現況盤點**：列出目前跨 app 邊界的呼叫點（已初步定位三檔：`lib/services/macos_scan_notifier.dart` 通知、`lib/workspace/workspace_repository.dart` 資料夾存取、`lib/screens/gap_report/gap_report_screen.dart` 外部開啟與 doc CLI process），逐項標「現有測試形態」與「未被驗證的平台事實」。

    2. **選項評估**（至少三案，附各自可自動化的比例與殘留手動項）：(a) 建 `/verify` 配方——把手動步驟寫成可重複執行的文件，以 checklist 形式驗收；(b) 擴充 `integration_test/`——能自動化者入測（如 `AppLifecycleState` 轉換、process 呼叫的存在性），不能者另列；(c) 混合——自動化打底 + 配方補殘留；(d) 維持現狀，每票各自手動並在 Solution 記錄。

    3. **判準**：可重複性（下次改動能否照做）、涵蓋率（多少平台事實被驗到）、建置成本、以及「驗證結果如何回寫規格」的路徑是否明確。

    4. **定案後**：寫入 `tech-decisions.md` 補記段（append-only 新增一節）；若決議改變雙層策略的表述，同步 CLAUDE.md §6 該列；為 W3-064 與 W1-036 的待驗項各建執行票（或於本票 spawn 規劃逐項落地，依規則 5）。

    5. 不在本票做的：實際執行任何一次實機驗證（那是定案後的執行票）；建 `/verify` skill 本體（若決議採 (a)，另建實作票）。'
acceptance:
- '[ ] 跨 app 邊界呼叫點盤點表完成，逐項標現有測試形態與未驗證的平台事實'
- '[ ] 至少三個選項各附可自動化比例與殘留手動項，依四項判準評估'
- '[ ] 決議寫入 tech-decisions 補記段；若改變雙層策略表述則 CLAUDE.md §6 同步'
- '[ ] W3-064 與 W1-036 的待驗項各有承接票或 spawn 落地紀錄'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

決策 跨 app 邊界功能的驗證層：雙層測試策略是否加第三類，及各功能的驗證形態

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending
