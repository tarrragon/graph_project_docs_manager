---
id: 0.1.0-W3-074
title: 跨邊界功能的測試分類：補 tdd 五層分工表的形態軸，評估落點，並定案本專案三個呼叫點的驗證形態
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-064
dispatch_reason: ''
decision_tree_path:
  entry_point: 測試策略缺口（實作後發現既有策略不涵蓋）
  final_decision: ANA 決策：盤點跨邊界功能、評估驗證形態、定案後寫入 tech-decisions 與 CLAUDE.md §6
  rationale: 缺口為策略層而非單票問題；已有三個功能落在縫隙（系統通知、外部開啟、資料夾選擇），逐票各自決定會產生三種不一致做法
who:
  current: saffron-system-analyst
  history: {}
what: 為跨 app 邊界的功能補上測試分類（框架層：tdd 五層分工表缺測試形態軸；專案層：三個呼叫點的驗證形態與 W3-064／W1-036 待驗項承接），並評估改進成果的落點（改既有 skill 或新建）
when: 0.1.0-W3-064 完成後（該票的 acceptance 2、3 待本決策定案才知道怎麼執行）；早於下一個跨邊界功能實作
where:
  layer: .claude/skills/tdd/references/layered-test-strategy.md,.claude/skills/tdd/references/doc-handoff.md,docs/tech-decisions.md,CLAUDE.md,.claude/skills/test-effectiveness/SKILL.md::read,.claude/skills/test-assertion-design/SKILL.md::read,integration_test::read,lib/services/macos_scan_notifier.dart::read,lib/workspace/workspace_repository.dart::read,lib/screens/gap_report/gap_report_screen.dart::read
  files:
  - docs/tech-decisions.md
  - CLAUDE.md
  - integration_test::read
  - test::read
  - lib/services/macos_scan_notifier.dart::read
  - lib/workspace/workspace_repository.dart::read
  - lib/screens/gap_report/gap_report_screen.dart::read
why: '**框架層缺口**：`tdd/references/layered-test-strategy.md` 的五層分工表以**架構層級**為軸（UI／Application／UseCase／Interface／Domain），缺**測試形態**軸。跨 app 邊界的功能在表上落於 Layer 1（整合測試），但該格指定的方法做不到——`integration_test` 無頭執行，觸不到系統權限對話框、通知中心、原生檔案選擇器、真實 process。表上有位置、位置上的方法無效，且無任何條文說明此情形，讀者依表執行會得到「已覆蓋」的假象。


  **專案層實例**：CLAUDE.md §6 定測試策略為雙層（`test/` 契約 + `integration_test/` 行為），本專案三個跨邊界呼叫點（`macos_scan_notifier.dart` 系統通知、`workspace_repository.dart` 資料夾存取、`gap_report_screen.dart` 外部開啟與 doc CLI process）**只有 fake 測試**，平台行為從未被驗證。SPEC-003 §2.2「實作票驗證」段明文要求四項平台事實由實機測得後回填，0.1.0-W3-064 的 acceptance 2、3 因此未勾；`ExternalOpener`（W1-036）同類。`.claude/skills/verify/` 不存在，session 啟動每次警告「驗證環境未就緒」而無人承接。


  **為何是框架問題而非單票問題**：三個功能落在同一個縫隙，逐票各自決定會產生三種不一致的做法；且此缺口與載體無關（任何有平台互動的專案都會撞到），替換專案名稱與路徑後仍成立。'
how:
  task_type: Analysis
  strategy: '分三段，框架層與專案層一併處理，落點由本票評估後建議而非預設。


    **A. 框架層：補測試形態軸**

    1. 盤點五層分工表的現行軸與其隱含假設（每格指定的方法是否對該層全部功能有效）。

    2. 定義跨邊界的判準：什麼樣的功能屬此類（呼叫離開 app process、需人工回應系統 UI、依賴平台授權狀態、依賴外部程序），以及它與既有五層的關係（是新的一層，或是每層都可能命中的正交屬性——此判定影響表的結構）。

    3. 為該類定驗證形態與其產出（可自動化的部分、殘留手動項、驗證結果如何回寫規格）。

    4. 檢查 `doc-handoff.md` 的整合 vs 單元分工框架是否需同步——該框架的判斷原則「正確性由自己決定或由串接決定」對跨邊界功能給不出答案（兩者皆非，是由平台決定）。


    **B. 落點評估（用戶裁示由本票建議）**

    比較兩案並給結論：(a) 改既有 skill（`tdd` 的兩份 reference + 必要時 `test-effectiveness` 補 references）；(b) 新建一支測試契約 skill 承載推導程序與目錄，`tdd` 只保留流程編排。判準：與既有內容的實際重疊度、`skill-design-guide` 的體量門檻（改後 `layered-test-strategy.md` 是否超標）、讀者觸發路徑（現有觸發詞能否帶到新內容）、可攜性（本專案外的 consumer 是否需要）。**不預設答案**，兩案各給可執行的切分。


    **C. 專案層落地**

    5. 三個呼叫點逐項標「現有測試形態」與「未被驗證的平台事實」，依 A 的分類指定驗證形態。

    6. 決議寫入 `tech-decisions.md` 補記段（append-only 新增一節）；若改變雙層策略表述則同步 CLAUDE.md §6 該列。

    7. 為 W3-064 與 W1-036 的待驗項各建執行票，或於本票 spawn 規劃逐項落地（規則 5）。


    **不在本票**：實際執行任何一次實機驗證；建 `/verify` skill 本體（若 B 結論採此路徑，另建實作票）；測試對象目錄與 traceability 兩個相鄰缺口（已各建票，blockedBy 本票——它們的形狀取決於本票的分類結論）。'
acceptance:
- '[ ] 跨邊界判準已定義，並判定它與既有五層的關係（新的一層或正交屬性），該判定的依據寫入結論'
- '[ ] layered-test-strategy 表已補形態軸或等效結構；doc-handoff 的分工框架已檢查並同步或說明不需同步'
- '[ ] 落點評估兩案（改既有 skill／新建 skill）各附可執行切分與四項判準的比較，並給結論'
- '[ ] 本專案三個呼叫點逐項標驗證形態；決議寫入 tech-decisions，CLAUDE.md §6 同步或說明不需；W3-064 與 W1-036 的待驗項各有承接票或 spawn 落地紀錄'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

決策 跨 app 邊界功能的驗證層：雙層測試策略是否加第三類，及各功能的驗證形態

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending
