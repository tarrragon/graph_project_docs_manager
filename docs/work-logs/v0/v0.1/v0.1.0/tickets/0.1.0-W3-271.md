---
id: 0.1.0-W3-271
title: 修復 commit_files_isolated 的 ref lock 重試恆為 False，且工具無法區分崩潰殘骸與並行活鎖
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-267
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-267 收束期間該失敗路徑實地命中，七個 curator 的全部提交路徑同時停擺 39 分鐘
  final_decision: '裁 #55 的 229 列為實作票，並併入群 G／群 A 於收束期間補出的鎖齡與歸屬延伸'
  rationale: 本票動 ticket CLI，而並行 session 正在呼叫它，故派進 worktree，合併前不影響主 checkout
who:
  current: thyme-python-developer
  history: {}
what: 修復 commit_files_isolated 的 ref lock 重試恆為 False，且工具無法區分崩潰殘骸與並行活鎖
when: 0.1.0-W3-267 收束後裁票；本缺陷於 2026-09-10 造成全域提交停擺 39 分鐘
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/lib/git_ops.py
why: '知識住址：`tarrragon/claude#55`，區段「當前結論（graph-project-docs-manager：ticket CLI 契約與提交路徑）」與「待辦與來源（同後綴）」的 `0.1.0-W3-229` 列；原票 `0.1.0-W3-229` 已 closed（`not_executable_knowledge_captured`）。


  **原缺陷**：`_run_git_with_lock_retry` 僅在錯誤字串含 `index.lock` 才重試，而 `commit_files_isolated` 的 CAS 步驟 `update-ref` 撞鎖時的錯誤文字是 `cannot lock ref ''HEAD'': ... refs/heads/main.lock`（或 `HEAD.lock`），不含該子字串，**重試條件恆為 False**。撞鎖瞬間即失敗且 `commit_sha` 遺失，逼迫執行者手動繞出工具外完成 CAS，進而跳過收尾同步，造成共用 index 過期 entry 並被後續裸 commit 靜默回滾他票內容。


  **2026-09-10 實地命中，且暴露了原票未記錄的兩項後果**：


  一、**CAS 失敗會同時留下兩個鎖**——`refs/heads/main.lock`（41 bytes，內含已寫好的新 ref 值）與 `HEAD.lock`（0 bytes），而 ref 從未更新。commit 物件與鎖殘骸同時存在，全部 session 的全部提交路徑（工具路徑與手動 CAS）撞同一把鎖，換命令不繞過。實測全域停擺 39 分鐘、`HEAD` 一次都沒推進。


  二、**工具無鎖齡判讀，故即使修好重試條件仍會對殘骸無限重試**。殘骸與並行活鎖的錯誤輸出**完全同形**，而兩者的正確處置相反：活鎖約十秒自行消失（判別是消失的同時 HEAD 前進到新 commit），重試即可、不得移除；殘骸不會自行消失（判別是內容可溯源為某次已知崩潰操作的產物且 mtime 停在該時刻），須由原執行者移除。修好重試而不加鎖齡判讀，等於把「立刻失敗」換成「永遠重試」。


  三、**`update-ref` 失敗時應一併指出自己是否留下鎖殘骸**（群 A 由群 F 的溯源判準推得）。否則清理責任無法歸屬——合法的移除判準是「可溯源為自己崩潰操作的殘骸」，而執行者若不知道自己留下了什麼，就沒有人有資格清理。「舊鎖就刪」不是合法判準：它在多數情況結論正確，但對「這個鎖是誰留下的」零回答，結論若錯也無從發現錯在哪一步。


  **實測數據**（群 G 單一觀測者橫跨兩種鎖，`#55` observe `5614476090`）：第 1 次 `HEAD.lock` 17 分鐘 0 bytes 期間零 commit（殘骸）；第 2 次 `HEAD.lock` 失敗後立即 `stat` 已不存在（活鎖）；第 3 次 `main.lock` 41 bytes 時間戳同為 14:01:39（殘骸的另一半）。該則另記錄一條誤判路徑：**剛清完同名鎖的人看到它再現，會傾向推論「有東西在重建它」而去查工具，而正確動作只是重試**——誤判的觸發條件不是鎖的屬性，是觀測者剛做過什麼。'
how:
  task_type: Implementation
  strategy: 三段。(1) 重試條件由 index.lock 子字串比對改為涵蓋 refs/heads/*.lock 與 HEAD.lock。(2) 加鎖齡判讀：重試前讀鎖的 mtime，超過閾值即停止重試並輸出診斷（mtime／大小／內容），不把殘骸當競爭無限重試。(3) update-ref 失敗路徑補兩項輸出：已建立的 commit-tree SHA，以及本次是否留下鎖殘骸。測試須含一個已知該紅的輸入（持久殘骸鎖），斷言工具放棄並診斷而非持續重試——只斷言「正常情況重試會成功」的測試在鎖齡判讀完全失效時照樣全綠。
acceptance:
- '[ ] 重試邏輯對 refs/heads/*.lock 與 HEAD.lock 兩種錯誤文字皆能觸發重試，附測試'
- '[ ] update-ref 最終失敗時，錯誤輸出包含已建立的 commit-tree SHA'
- '[ ] 新增或更新單元測試模擬 ref lock 競爭，驗證重試行為（背景執行緒持有 refs/heads/main.lock）'
- '[ ] 重試前判讀鎖齡：鎖存在時間超過閾值即停止重試並輸出鎖的 mtime、大小與內容，不無限重試殘骸'
- '[ ] update-ref 失敗時輸出本次是否留下鎖殘骸，使清理責任可歸屬到唯一有資格清理的執行者'
- '[ ] 測試含一個已知該紅的輸入：以持久殘骸鎖為輸入，斷言工具在有限次重試後放棄並輸出殘骸診斷，而非持續重試'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修復 commit_files_isolated 的 ref lock 重試恆為 False，且工具無法區分崩潰殘骸與並行活鎖

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-267 | chars: 1779 -->

### Task Reference
- 0.1.0-W3-267 what: 收束 44 張框架票為 framework issue 區段，分七個主題群並行派發 curator（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-267 why: `0.1.0-W3-254` 把十三張框架資產待派票 observe 到七則既有 issue，但**停在 observe 沒有 close**——知識已上去、票還在本地，處於中間態。另有約 30 張框架票尚未升級。合計 44 張。

**為何要收束而非放著**：ticket 池單調增長而增長不對應框架品質提升。分析 context 記在單一 consumer 的 ticket 時，其他 consumer 不可見，同一問題會被重複發現、已解的問題後續票不知情。

**分群不採主題中央清單**：配方步驟一指定以該清單分群，但實測已漂移——`框架 skill 撰寫` 群含 `0.1.0-W1-014`（shared_preferences）、`0.1.0-W1-015`（notarization）等純專案票；`0.1 App 地基` 群含 `0.1.0-W3-162`／`163`／`164`／`212`（派發 hook 與 worktree skill）等純框架票。漂移機制為 S1 依 `source_ticket` 繼承主題而不看內容，且 `--no-topic` 這唯一的人工出口在推導成功時是空操作（`0.1.0-W3-266`）。故本次分群逐張套兩條 AND 判準，主題標籤只作候選來源。

**同儕分工已對齊**（世界平面查證，非採信回報）：`graph-project-docs-manager-59` 回覆其建立的 `237`／`246`／`247`／`258`／`260` 全未 claim、可逕行收束，其 claim 中的 `207`／`236` 不在範圍；`graph-project-docs-manager-92` 回覆 claim 中僅 `251`，不在範圍。全庫 in_progress 四張（`019`／`207`／`236`／`251`）皆不在範圍內。

**同儕要求的保留項**：`0.1.0-W3-250` 與 `257` 為同一條設計鏈且仍在收斂（`257 blockedBy 250`，其驗收在一小時前才改寫），本次不收；`0.1.0-W3-206` 的原修正方向（`0.1.0-W3-184` 的 L1/L2/L3）已被《事件流負載仲裁方法論》取代，鍵由「可補性」換成「丟棄後果由誰承擔、可否回復」，須連同該則前提重評一起收，不得把已知錯誤的方向寫進 issue。

**工具側已知缺陷，curator 必須帶著走**：`section_comment.py` 的 `init`／`add`／`update`／`observe` 四支皆無內容長度檢查，空內容照樣 POST、exit 0、印出成功 URL，其中 `update` 空內容會清空既有區段（`0.1.0-W3-264`）。故每則寫入後必須以 `gh api --jq '.body | length'` 回讀，不得只看 exit code。（來源已完成，結論見 Solution）

### Related Files
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-117.md::write  # from 0.1.0-W3-267（來源已完成，結論見 Solution）
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-118.md::write  # from 0.1.0-W3-267（來源已完成，結論見 Solution）
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-171.md::write  # from 0.1.0-W3-267（來源已完成，結論見 Solution）
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-190.md::write  # from 0.1.0-W3-267（來源已完成，結論見 Solution）
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-191.md::write  # from 0.1.0-W3-267（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] 七個主題群各有一份來源票對照表寫回本票 Solution，含票 ID／型別／處置／落點區段  # from 0.1.0-W3-267（來源已完成，結論見 Solution）
- [ref] [x] 每個 curator 的 show 顯示當前結論為第一則、check 三項未命中，且每則寫入皆有回讀字元數為證  # from 0.1.0-W3-267（來源已完成，結論見 Solution）
- [ref] [x] 範圍票依步驟五處置完畢，reason-note 含 issue ref；250／257／206 依同儕保留要求處理  # from 0.1.0-W3-267（來源已完成，結論見 Solution）
- [ref] [x] 分群歸屬逐張以兩條 AND 判準確認過，未採信主題中央清單標籤  # from 0.1.0-W3-267（來源已完成，結論見 Solution）

