---
id: 0.1.0-W3-332.1
title: 定義 三段鏈式範例規格與「何處需要範例」判準
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-332
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 定義 三段鏈式範例規格與「何處需要範例」判準
when: 所有審查與走查子票派發前
where:
  layer: 待定義
  files:
  - .claude/skills/skill-design-guide/references/chained-examples.md
  - .claude/skills/skill-design-guide/SKILL.md
  - .claude/skills/skill-design-guide/CHANGELOG.md
  - .claude/skills/compositional-writing/SKILL.md::read
  - .claude/skills/component-contract-design/references/appendix-examples.md::read
  - .claude/skills/skill-design-guide/references/writing-the-body.md::read
  - .claude/skills/skill-design-guide/references/seeing-like-an-agent.md::read
why: 範例形態若不先定，各審查者各自發明格式，產出無法彙整；判準若不先定，「需要範例」會退化為審查者的個人品味
how:
  task_type: Documentation
  strategy: 規格三段：輸入（spec 片段，語言中立）→ 產出物（契約表／ASCII 線框／決策表）→ 驗證（Given/When/Then，不含任何框架 API）；判準以「讀者只讀文字時可能產出兩種以上不同結果」為需要範例的訊號；附一則去識別化完整示範；產出落在 .claude/references 或 skill-design-guide references，路徑於 Solution 定案
acceptance:
- '[x] 規格明定三段各自的允許與禁止內容（禁程式語法與框架 API 名）'
- '[x] 「何處需要範例」判準可機械套用並附正反各一例'
- '[x] 附一則完整三段鏈式示範且通過去識別化（無本專案識別符）'
- '[x] 記錄既有 appendix-examples 與新規格的相容或遷移判定'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T10:30:51'
completed_at: '2026-09-14T10:39:58'
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

定義 三段鏈式範例規格與「何處需要範例」判準

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-14 10:30] readiness=1 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## Context Bundle

### 用戶裁示（2026-09-14，AskUserQuestion）

- **範例形態**：三段鏈式範例。每則固定三段：【輸入】spec 片段 →【產出】產出物（契約表／ASCII 線框／決策表）→【驗證】語言中立的 Given/When/Then
- **目的**：範例不示範程式語法（skill 跨 html+css、Flutter 等任何能建構畫面的技術使用），而是讓 agent 以範例校準文字約束的意義，從 spec 推導出元件設計與測試驗證方式，不靠猜測生成
- **下游**：本票產出是 W3-332.2–.10 共九張審查／走查票的共同格式與判準，產出不可含本專案識別符（規則 8，skill 為 portable）

### 用戶選定時所見的示範（作為起點，非定稿）

```
【輸入】SPEC 狀態表一列：
  空清單 | 顯示提示 | 可操作：新增
【產出】
  元件：空清單提示 variant=default
  +--------------------------+
  |  (icon)                  |
  |  尚無項目                |
  |  [ 新增第一筆 ]          |
  +--------------------------+
  內容政策：訊息 可換行=是 最大2行
【驗證】
  Given 清單來源回傳 0 筆
  When  畫面渲染完成
  Then  主區只含空清單提示，不含清單容器
  And   最長文案下按鈕不被擠出容器
```

### PM 定案

- **產出位置**：`.claude/skills/skill-design-guide/references/chained-examples.md`（範例屬 skill 撰寫慣例，與 writing-the-body 並列），並在 `skill-design-guide/SKILL.md` 路由表補一列指向；SKILL.md 其餘內容不改
- **「何處需要範例」判準起點**：讀者只讀文字約束時可能產出兩種以上不同結果 → 需要範例。本票須把它寫成可機械套用的判準（含正例與反例各一）
- **既有範例的處置判定**：`component-contract-design/references/appendix-examples.md` 已有 Examples／正例／Troubleshooting 三類，本票判定其與新規格相容、需遷移或並存，只記判定不改該檔
- **不做**：不審查任何 skill 的需範例位置（那是 .2–.7）；不改 component-contract-design

### 已知風險

驗證段容易滑向框架 API（例如「find.byType」「getByRole」）。規格須明訂驗證段只能以使用者可觀測事實與元件契約欄位措辭。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
**Completion Time**: 2026-09-14 10:38
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
**Summary**: 新增 .claude/skills/skill-design-guide/references/chained-examples.md：定義三段鏈式範例格式（【輸入】【產出】【驗證】）、各段允許/禁止內容表（禁程式語法與框架 API 名）、「何處需要範例」機械判準（讀者只讀文字約束時是否可能產出兩種以上不同結果，附正反例）、一則去識別化完整示範（含逐段核對表）、與 component-contract-design skill 既有範例附錄的相容判定（相容並存、不遷移，範疇不重疊）。.claude/skills/skill-design-guide/SKILL.md：〈按需讀取〉路由表新增一列指向 chained-examples.md（涵蓋章節五個 ## 標題已雙向驗證），frontmatter metadata.version 由 1.12.4 升至 1.13.0。CHANGELOG.md 新增 1.13.0 版本紀錄。已執行機械檢查：SKILL.md 全檔 4303 tokens / 193 行（皆通過）；chained-examples.md 單檔 2414 tokens（通過選段查閱門檻）；一跳可達 comm 檢查無新孤兒節；外部路徑檢查無裸 .claude 路徑命中；禁用詞與本專案識別符掃描皆無命中。
