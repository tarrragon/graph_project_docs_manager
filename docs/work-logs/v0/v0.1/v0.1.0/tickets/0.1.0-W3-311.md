---
id: 0.1.0-W3-311
title: 判定 owner 驗證器與本專案 session 命名不相容的影響範圍與處置層級
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-315
source_ticket: 0.1.0-W3-306
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-306 回歸測試的承重發現 N1；PM 以 regex 實測四個真實 session 名複核，範圍比回報更廣
  final_decision: 建票記錄現象與證據，修法形狀不在本票決定；優先級 P0
  rationale: 本專案無任何 session 可成為區段擁有者，使 framework-issue 的區段機制對本 consumer 結構性不可用；且 0.1.0-W3-304 已把一個失效值寫進範本
who:
  current: framework-issue-curator
  history: {}
what: 判定 owner 驗證器與本專案 session 命名不相容的影響範圍與處置層級
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/section_comment.py
why: '**現象**：`section_comment.py` 的 `OWNER_FORMAT_RE = ^[a-z0-9]+(-[a-z0-9]+)*-[0-9]+$`（行 140，消費於行 212）要求 owner 識別的尾碼為**純數字**。本專案的 session 序號為十六進位樣式。


  **PM 實測（非採信轉述，範圍比 `0.1.0-W3-306` 回報的更廣）**：


  | 值 | 通過 | 來源 |

  |---|---|---|

  | `graph-project-docs-manager-5d` | 否 | 本 session |

  | `graph-project-docs-manager-8c` | 否 | 平行 session |

  | `graph-project-docs-manager-2f` | 否 | 既有觀測的 owner 欄 |

  | `flutter-balance-77` | 是 | 他 consumer |


  **本專案至今無任何 session 可通過該驗證**。這解釋了一個此前被歸因於「PM 漏填」的模式：本專案歷次 curator 派發全數降級為 `observe`，而 `#97` 的 6 個區段全屬 `flutter-balance-77`——**那個 consumer 的 session 尾碼恰好是數字**。


  **漏填只是症狀**：即使正確填入真實 session 名，`add`／`init` 仍會以 exit 3 失敗。因此「結論落在觀測流而非區段」對本 consumer 是**結構性**的，不是紀律問題。


  **`0.1.0-W3-304` 已把一個失效值寫進範本**：`ticket-intake.md` 第 160 行的範例格式為 `graph-project-docs-manager-5d`（由 PM 於派發時提供）。該段加入的目的正是修「owner 未提供」，**而它給的範例讓照做的人命中同一後果**，成因從「漏填」換成「照範本填」。


  **為何 P0**：它使本 consumer 無法使用 framework-issue 的核心機制（區段），且範本現正引導後續 session 重複失敗。'
how:
  task_type: Analysis
  strategy: '**本票不決定修法形狀**。可能的方向至少三個且互斥（改 regex／改 session 命名取值方式／範本改用合法範例並說明取值規則），**選哪一個取決於下列未知項**：


    1. **`OWNER_FORMAT_RE` 的純數字尾碼是刻意或巧合**：查該常數的引入脈絡與 `section_comment.py` 對 owner 的實際用途（是否有依賴數字尾碼的排序、比較或解析）。**若有依賴，改 regex 會破壞它。**

    2. **其他 consumer 的 session 命名樣式**：`flutter-balance-77` 通過是普遍情形或特例？若多數 consumer 的尾碼含字母，則此為普遍缺陷而非本專案特有。

    3. **owner 識別的取值來源是否唯一**：條文寫「取自 `ListAgents`」，而 `ListAgents` 給的是 session 名。是否存在另一個合法取值管道（例如自訂而非沿用 session 名），若有則問題可能在條文的取值指示而非驗證器。


    **一併判定 `0.1.0-W3-304` 寫入範本的失效範例該如何處置**——但同樣不在本票決定修法，記錄其為待處置項。


    **升級判定**：依 `framework-issue/SKILL.md` 流程，先跑 `dedup`，兩條 AND 判準逐條套用並留痕。**注意本票自身可能無法 `add` 區段**（即本票主題），降級 `observe` 時記錄該事實。


    不改 `.claude/` 資產、不跑 `sync-push`。'
acceptance:
- '[x] OWNER_FORMAT_RE 純數字尾碼是刻意或巧合已判定，附引入脈絡與用途查證'
- '[x] 其他 consumer 的 session 命名樣式已抽樣，判定此為普遍缺陷或本專案特有'
- '[x] owner 識別是否有 ListAgents 以外的合法取值管道已查證'
- '[x] ticket-intake 第 160 行失效範例的處置已列為待處置項並記錄'
- '[ ] dedup 已跑，兩條 AND 判準已逐條套用並留痕；Solution 未寫入修法形狀'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T16:16:44'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

判定 owner 驗證器與本專案 session 命名不相容的影響範圍與處置層級

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

`section_comment.py` 的 `validate_owner`（`OWNER_FORMAT_RE = ^[a-z0-9]+(-[a-z0-9]+)*-[0-9]+$`，`init`／`add`／`transfer-owner` 共用）要求 owner 尾碼為純數字；而 owner 值的唯一供應端是 session 短名（`ListAgents` 首行名稱，等同跨 session 訊息的 `from-name`），其尾碼實測為兩碼十六進位，字元集含 a-f。寫入端（純數字尾碼）與讀取端（三個消費點皆為 `startswith(f"{prefix}-")` 前綴比對）採兩套判準，尾碼寫入後從未被任何讀取端解讀——此為候選根因表述。

### 影響範圍

**已驗證的事實**：

1. 受影響檔案：`.claude/skills/framework-issue/scripts/section_comment.py`（`OWNER_FORMAT_RE`／`validate_owner`）、`references/comment-as-section-protocol.md` 的 owner 識別格式段、`references/ticket-intake.md` 的 owner 識別行段。
2. 不是本專案特有：13 個真實 session 名跨三個 consumer，4 個被擋，其中 `blog-cd` 屬第三個 consumer（非本專案、非 flutter-balance）。
3. 不是本專案結構性不可用：`#92` 有 4 則、`#95` 有 3 則區段的 owner 為本專案 session（`-59`／`-66`），為世界平面查證（`show 92`／`show 95`）。**票面 why 段「本專案至今無任何 session 可通過該驗證」「歷次 curator 派發全數降級 observe」兩句與此矛盾，以本節實測為準**。
4. 影響形態為逐 session 擲骰：能否擁有區段取決於該次 session 尾碼是否恰為兩碼數字。若尾碼為均勻兩碼十六進位，不通過比例為 156/256。
5. 降級為 `observe` 有兩個獨立成因：(a) 尾碼含字母被 exit 3 擋；(b) 子代理人無取值管道（`ListAgents` 對 subagent 不可得；`pm-registry.json` 的 session 條目欄位為 `name`／`project`／`registered_at`／`heartbeat_ts`／`tickets`／`files`，無 session 序號欄，且 `name` 為底線形式專案名，本身亦不通過驗證）。本次派發即為 (b) 的實例。

**仍未驗證的假設**：

- session 短名尾碼的產生規則未見於任何本機可讀來源；「兩碼十六進位」為 13 個觀測值的歸納，非規格。本 session 的 uuid 前綴與其短名尾碼無可見對應關係。
- `flutter-balance` 本次抽到 4 個全通過。無管道判定該 consumer 的尾碼字元集是否與本專案同形，據實記為未知，不推測。

### 相關 Error Pattern

`ARCH-BAL-023`（識別欄位無共用契約，消費端靜默漏檢）——與本現象同批由上游帶入（見 `.claude` sync 至 framework-issue 2.0.0 的 commit 訊息），描述的正是寫入端與消費端各自自洽而兩者之間缺約定的形態，與本票候選根因表述同構。

## 重現實驗結果
### 實驗方法

1. **取真實 session 名，不自行構造**：`grep -rho 'from-name="[^"]*"' .claude/hook-logs`（跨 session 訊息的寄件者名，即 `ListAgents` 短名）；再取 `tarrragon/claude` #80–#107 全部 comment 首行的 `<!-- section: ... owner: X -->` 與 `<!-- observation: ... by X -->` 標記（`gh api repos/tarrragon/claude/issues/N/comments`，150 則標記）。
2. **以 `validate_owner` 本體判定**，不以人工比對 regex：`sys.path` 加入 scripts 目錄後 `import section_comment`，逐值呼叫 `validate_owner`，捕捉 `ValueError` 記 FAIL。
3. **消費端查證**：`grep` 全 `.claude` 的 owner 讀取點；`show 92`／`show 95` 取世界平面的現有區段 owner。
4. **取值管道查證**：讀 `$(git rev-parse --git-common-dir)/pm-registry.json` 的 session 條目欄位；確認本次 subagent 工具清單無 `ListAgents`。

### 實驗執行

`validate_owner` 實跑 14 值（13 個真實 session 名 + 代理人角色名對照）：

| 值 | 結果 | 來源 |
|---|---|---|
| `graph-project-docs-manager-59` | PASS | from-name（78 次）、#92／#95 區段 owner |
| `graph-project-docs-manager-66` | PASS | from-name（47 次）、#92／#95 區段 owner |
| `graph-project-docs-manager-92` | PASS | from-name（25 次） |
| `graph-project-docs-manager-86` | PASS | from-name（13 次）、#92 觀測 |
| `graph-project-docs-manager-73` | PASS | from-name（2 次） |
| `graph-project-docs-manager-2f` | FAIL | from-name（24 次）、#92／#95 觀測 |
| `graph-project-docs-manager-5d` | FAIL | from-name（9 次） |
| `graph-project-docs-manager-8c` | FAIL | from-name（3 次）、觀測 |
| `flutter-balance-99` | PASS | from-name、區段 owner |
| `flutter-balance-94` | PASS | from-name |
| `flutter-balance-61` | PASS | from-name、區段 owner |
| `flutter-balance-77` | PASS | from-name、區段 owner（34 則） |
| `blog-cd` | FAIL | from-name（16 次，第三個 consumer） |
| `framework-issue-curator` | FAIL | 代理人角色名對照 |

消費端 `grep` 結果：owner 值的讀取點三處，皆為 `startswith(f"{prefix}-")`——`section_comment.py` 的 `_discover_owned_issue_numbers_by_prefix`、`todo --consumer` 的列篩選、`session-start-issue-check-hook.py` 的擁有檢查。該檔內 `int(...)` 兩處作用於 `issue_ref`，不在 owner 上。`_project_owner_prefix()` 實跑回傳 `graph-project-docs-manager`（git common dir 目錄名 kebab 化）。

`pm-registry.json` 實讀：2 個 session 條目，key 為 uuid，欄位 `name`（值 `graph_project_docs_manager`，底線形式）／`project`／`registered_at`／`heartbeat_ts`／`tickets`／`files`；無 session 序號欄。

`show 92`：6 則區段，owner 為 `graph-project-docs-manager-59`（1）、`-66`（3）、`flutter-balance-77`（2）。`show 95`：6 則區段，`flutter-balance-77`（3）、`graph-project-docs-manager-66`（3）。

觀測流中出現而區段中不出現的值：`-2f`、`-8c`、`-dcead428`、`flutter_balance-pm`、`17e1cd76`、`framework-issue-curator`、ticket ID 形態數則（`observe --session` 無格式驗證）。存量中另有一則區段 owner 為 `thyme-python-developer-e2e`（不通過現行驗證），為驗證器加入前的既有資料。

### 實驗發現

**已驗證的事實**：

1. 純數字尾碼要求與任何讀取端無關（三個消費點皆前綴比對），為寫入端單方面檢查。
2. 缺陷跨 consumer：`blog-cd` 被擋，證明非本專案特有。
3. 本專案可通過與不可通過的 session 同時存在，且通過者已實際擁有 7 則區段，票面「結構性不可用」的表述不成立；正確表述為逐 session 命中率問題。
4. 子代理人無任何管道取得合法 owner 值：`ListAgents` 不可得、`pm-registry.json` 無該欄位。本次派發刻意不提供 owner，`add` 因此無值可用，依派發指示降級 `observe`（已附加於 `#95`，見 Solution）。
5. 數字尾碼同時兼任「排除代理人角色名」的過濾器：`framework-issue-curator` 正是被尾碼規則擋下。該值曾被用為區段 owner——上游 sync commit 訊息記載其後以 `transfer-owner` 將 #87–#93 的 owner 由代理人型別名改為 session 識別，現況 #92／#95 owner 已為 session 識別，與該記載一致（此為對平行 session 所述誤用實例的獨立複核路徑：本人未見該批區段的歷史版本，僅以 commit 訊息與現況一致性佐證）。

**仍未驗證的假設**：

- 各 consumer 的 session 短名尾碼字元集是否一致：`flutter-balance` 抽樣 4 個全為數字，無管道判定其尾碼是否可能含字母，記為未知。
- 「兩碼十六進位」的產生規則無來源可查，為觀測歸納。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 分析結論（四項）

**一、現象**：`validate_owner` 要求 owner 尾碼純數字；owner 的供應端是 session 短名，尾碼實測為兩碼十六進位。尾碼含字母的 session 對 `init`／`add`／`transfer-owner` 一律 exit 3。

**二、證據**：見「重現實驗結果」。三項承重數字——13 個真實 session 名實測 4 個被擋；owner 讀取點 3 處全為前綴比對，尾碼零讀取；`pm-registry.json` 無 session 序號欄。

**三、影響範圍**：**跨 consumer 的普遍缺陷，非本專案特有**（`blog-cd` 屬第三個 consumer 亦被擋）；**非結構性不可用**（本專案 session `-59`／`-66` 現持有 #92 四則、#95 三則區段）。實際形態是逐 session 命中率，加上一條與尾碼無關的獨立阻斷：子代理人無取值管道，只能靠派發者代填。兩者疊加才是「本專案歷次降級 `observe`」的成因，**票面原歸因（結構性不可用）與世界平面矛盾，已於 Problem Analysis 更正**。

**四、處置層級**（本節只判層級，不判修法形狀）：

| 候選層級 | 內容 | 支持證據 | 代價／風險 |
|---|---|---|---|
| A. 驗證器層（`section_comment.py`） | 判準錯在寫入端，與三個讀取端不一致 | 尾碼零讀取；缺陷跨 consumer；`blog-cd` 被擋 | 純數字尾碼現正兼任「排除代理人角色名」的過濾器，動它須同時保住該過濾能力 |
| B. 供應端層（取值管道） | 沒有任何欄位存放 session 序號，subagent 取不到值 | `pm-registry.json` 欄位實讀；`ListAgents` 對 subagent 不可得 | 只治 subagent 那條阻斷，對尾碼含字母的 PM session 無效 |
| C. 文件層（協定檔／派發範本） | 範本範例本身是失效值，照做必失敗 | `ticket-intake.md` owner 識別行範例尾碼含字母 | 只治「填錯」，驗證器仍擋掉六成 session |

**三者不等價也不互補**：A 治命中率、B 治 subagent 阻斷、C 治範本誤導。**修法形狀（含是否同時動多層）不在本票決定**，已建 `0.1.0-W3-315` 承接裁決。

### 升級判定留痕（framework-issue 兩條 AND 判準）

| 判準 | 逐條套用 |
|---|---|
| 抽象可攜性 | 成立。敘述替換掉專案名後仍成立：「區段 owner 驗證器要求純數字尾碼，而 session 短名尾碼字元集含字母，使部分 session 無法成為區段擁有者」。`blog-cd` 為非本專案實例。 |
| 資產範圍 | 成立。根源在 `.claude/skills/framework-issue/scripts/section_comment.py` 與同 skill 兩份 references，屬通用資產，非 `lib/`／`docs/` 專案產物。 |

### 查重（`dedup`）與關係判定

`section_comment.py dedup --keywords "owner 格式" "OWNER_FORMAT_RE" "session 序號" "識別欄位 契約"` 已執行。命中清單多為「契約」「識別欄位」泛詞命中（#61／#62／#63／#66／#91／#92／#94／#96／#98／#99／#105），逐一檢視後判為**無關**（命中位置為 body 中的一般用詞，主題各異）。關係判定如下：

| issue | 關係 | 依據 |
|---|---|---|
| #95 framework-issue skill 2.0.0（`add`／`transfer-owner` 與 owner 格式） | **落點（同一主題）** | `OWNER_FORMAT_RE` 正是 2.0.0 隨 `add`／`transfer-owner` 一併引入；本現象屬該批變更的缺陷 |
| #92 PM 派發判準與代理人自檢三項缺口 | **引用** | 只提供本專案既有區段 owner 的現況證據，主題不同 |
| 其餘命中 | 無關 | 泛詞命中 |

**未新開 issue**：主題已有 open issue（#95），依規則附加不新開。

### 落點與降級記錄

落點為 `#95`，但**未 `add` 區段，降級為 `observe`**：本次派發刻意不提供 owner 識別（該缺席本身即受測現象），而 subagent 無任何管道自行取得合法值（`ListAgents` 不可得、`pm-registry.json` 無該欄位），亦不得沿用範本失效範例或自行捏造合法值。降級結果：

- 觀測連結：https://github.com/tarrragon/claude/issues/95#issuecomment-5631640314
- 後果記錄：結論落在觀測流，不受 `check` 主警訊涵蓋；#95 上本專案既有的 3 則區段 owner 為 `-66`（已結束的 session），本次結論無法併入該區段。

### 待處置項（不決定處置方式）

1. **`ticket-intake.md` owner 識別行的範例值失效**：該範例尾碼含字母，照填必然 exit 3；該段加入目的正是修「漏填 owner」，成因因此由「漏填」換成「照範本填」。已掛 `0.1.0-W3-315`。
2. **存量中不合現行驗證的區段 owner**：`thyme-python-developer-e2e` 仍為某則區段的 owner（驗證器加入前的既有資料），現行 `check` 未涵蓋此類存量。已於 `0.1.0-W3-315` 的證據引用中可見，是否處置由該票裁決。

### Spawn 落地確認

- `0.1.0-W3-315`（ANA，P1，`--source-ticket 0.1.0-W3-311`，`blockedBy` 已設）：承接修法方向裁決與上述待處置項 1。已建立，`spawned_tickets` 由 CLI 回填。
- 待處置項 2（存量 owner）：**無需另建 ticket**——其處置方式完全取決於 W3-315 選定的修法方向（放寬 regex 則存量自動合法，其餘方向才需遷移），提前建票會建出一張內容未定的票。已於 W3-315 的證據中登記。

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-306 | chars: 734 -->

### Task Reference
- 0.1.0-W3-306 what: 回歸測試 改版條文對第一輪六項缺口的實際效果：消失、位移或換形態（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-306 why: **改版的效果至今全是推論**。`0.1.0-W3-300` 自述「本票不落條文，因此全部改進候選的效果皆為推論」。`0.1.0-W3-304` 落了地，但它驗的是「有沒有照設計改」，不是「改了之後缺口還在不在」。

**缺口有三種去向，外觀相同**：真的消失、位移到別處、換一種形態留在原處。**只有逐項複測分得出來**，而第一輪留下的六項失效點（`297` F1–F6）、四項（`298` G1–G4）、七項（`299`）是現成的對照基線。

**特別需要複測的兩項**：

- 第二檔處置已由「降為分析票」改寫為「待辦與來源／問題與方案」配對。**新形態有沒有載體、有沒有觸發器、非擁有者能不能執行**——這三問正是舊形態失效的原因，不會因為換了名字就自動解決。
- 「消費端」已拆為執行條件／使用者群並加綜合規則。**拆分後是否仍讀得出兩義**是條文自己寫下的 fallback 條件（讀得出兩義則降為提示句移出豁免閘門），該條件成立與否須實測。

**不做則後果**：帶著一份效果未經觀測的條文進入下一階段，而它的前一版正是因為同樣的理由才需要測。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/framework-issue/references/escalation-flow.md  # from 0.1.0-W3-306（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 第一輪每一項既有缺口皆有四選一判定（消失／位移／換形態／未變），位移者指出新位置  # from 0.1.0-W3-306（來源已完成，結論見 Solution）
- [ref] [x] 未實走的項目標記為未測，未以推論標為已解  # from 0.1.0-W3-306（來源已完成，結論見 Solution）
- [ref] [x] 第二檔新處置的載體、觸發器、非擁有者可執行性三問已實測  # from 0.1.0-W3-306（來源已完成，結論見 Solution）
- [ref] [x] 「消費端」拆分後是否仍讀得出兩義已實測，並對照條文自訂的 fallback 條件  # from 0.1.0-W3-306（來源已完成，結論見 Solution）
- [ref] [x] 改版引入的新缺口與既有缺口分開列  # from 0.1.0-W3-306（來源已完成，結論見 Solution）
