---
id: 0.1.0-W3-207
title: 實作 注意力仲裁點：單一持有者依級別裁決跨 domain 的提示請求
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-203
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-184
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-184 SR-1：仲裁點的實作落點與範疇需 PM 裁決
  final_decision: 本票只做我方發起事件的仲裁，不含第三類的接收緩衝
  rationale: 第三類為空類別且有明確 trigger；現在一併做只能憑猜測定義緩衝的容量、逐出策略與持久性，屆時再建才有需求形狀
who:
  current: parsley-flutter-developer
  history: {}
what: 實作 注意力仲裁點：單一持有者依級別裁決跨 domain 的提示請求
when: 0.1.0-W3-203 指定仲裁點落層之後
where:
  layer: Presentation
  files:
  - lib/components::write
why: '`0.1.0-W3-184` 第三節裁定協調形態為**單一仲裁點而非分散式協商**：各 domain 不互相確認，改為向注意力通道的持有者請求，由後者依級別規則裁決。


  **為何不能讓 domain 互相確認**：注意力競爭發生在任何兩個會產生注意力請求的 domain 之間、不分層級（協調圖與 `docs/domain-map.md` §2 的依賴圖正交），因此互相確認需要 N² 條協調邊，且迫使 domain 互相知道彼此存在——直接違反 §3 Bundle 界定表的公開面設計。仲裁只需 N 條，且 domain 之間維持互不知情。


  **不做則後果**：`0.1.0-W3-204` 的級別規則會停在單一載體內（`AppSnackBar`），跨 domain 的請求仍各自直達載體，「現在是不是請求使用者注意的適當時機」沒有任何地方在回答。'
how:
  task_type: Implementation
  strategy: '落層依 0.1.0-W3-203 的指定（不得落入任何 domain 內部）。介面形態：domain 側只表達「我要發出這個級別的請求」，不表達載體與時機；仲裁側依 SPEC-003（0.1.0-W3-204）的級別規則決定立即呈現、延後至當前請求被消費、或不呈現。

    判準以標題文字引用「事件流負載仲裁方法論」的〈通道與仲裁器〉〈讓步與卸載順序〉，引用時連同「它回答的問題」欄。該方法論對本票新增兩項實作約束：(a) **禁止持有並等待**——同一請求對同一通道的多次佔用須在首次進入時一次申請（持有並等待是佔用量計不到的狀態）；(b) 仲裁器與通道處於同一 failure domain 時不新增單點，若實作為通道之外的獨立物件則須明寫它是新增的可用性單點。

    **本票範疇不含第三類（推送型／外部驅動）的接收緩衝**——該類在本專案為空類別，其 trigger 為 SPEC-003:761 的裁定被推翻時；0.1.0-W3-184 第八節已明寫該情形下緩衝是實作前置，屆時另票承接。與 0.1.0-W3-205 的分界：205 修單一載體內的截斷規則，本票建跨 domain 的請求入口。原引用對象 0.1.0-W3-202 已 close（superseded_by 0.1.0-W3-215），對帳見 0.1.0-W3-213。'
acceptance:
- '[ ] domain 側介面只表達級別，不表達載體與時機'
- '[ ] 仲裁決策依 SPEC-003 的級別規則，未在仲裁點另立一套'
- '[ ] 仲裁點未落入任何 domain 內部，落層符合 0.1.0-W3-203 的指定'
- '[ ] 本票未實作第三類接收緩衝，且在 Solution 明記該範疇歸屬與 trigger'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實作 注意力仲裁點：單一持有者依級別裁決跨 domain 的提示請求

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-184,0.1.0-W3-203 | chars: 1619 -->

### Task Reference
- 0.1.0-W3-184 what: 定義 SPEC 設計時的前台背景分配與事件流跨 domain 協調要求
- 0.1.0-W3-203 what: 補寫 domain-map 的注意力軸：哪些 domain 產生注意力請求與仲裁點落層

### Rationale Chain
- 0.1.0-W3-184 why: **用戶更正（2026-09-09）**，起因是 PM 把提示密度問題框成「先問這些提示時刻是否都必要」：

> 這樣也不完全正確。如果需要提示使用者的訊息，表示有他的重要性，那**有重要性的動作，不應該同時大量執行**，或者真正有重要問題發生的時候，**應該要暫停背景的行為**，所以我才提到有的提示訊息要擋住使用者的操作。
>
> 那重點是當我們設計 spec 的時候，也要評估**有哪些行為會在背景執行，哪些動作會在前台跟使用者互動**，是不是有些動作要**避免同時請求使用者的關注或者指令**，或者會造成互動影響的 SPEC 在設計的時候，**整個事件流就應該包含跟其他 domain 確認過再執行，或者應該加入延遲的動作**。

**PM 原本的框架把提示當成雜訊，這是錯的。** 提示需求本身是重要性訊號——需要提示使用者，表示該事件有重要性。因此提示密度過高不是「提示太多」，是**同時執行了太多重要的事**。

**這使 `0.1.0-W3-167` 的處置範圍必須收窄。** 在提示層做排隊、優先級或載體分化，是幫一個排程問題做視覺化緩衝——症狀被吸收，成因原封不動，而且緩衝做得越好，成因越不可能被發現。167 處理「留下來的提示怎麼呈現」，本票處理「為什麼會有這麼多同時發生的重要事件」。

**本專案已有具體實例。** 破洞掃描掛在 `GapReportNotifier.build()`，首讀者是 App 啟動時的 `ScanNotificationController.start()`（`0.1.0-W3-122` 的主題）——使用者尚未看過破洞報告頁，掃描就已在背景完成並試圖請求通知授權。這正是「背景行為在前台注意力尚未就緒時請求使用者關注」，而現行 SPEC-003 §2.2 的觸發條件只判斷「可見頁是不是 gaps」，不判斷「現在是不是請求使用者注意的適當時機」。

編輯能力（`0.1.0-W3-073`）落地後前台互動會大幅增加，此時若背景仍有掃描、載入、git 操作各自獨立觸發，注意力競爭會從偶發變成常態。**現在定義比事後補容易。**
- 0.1.0-W3-203 why: `0.1.0-W3-184` 第三節推得：**協調圖不是依賴圖**。`docs/domain-map.md` §2 的 8 條依賴邊描述資料方向且單向向下；注意力競爭發生在任何兩個會產生注意力請求的 domain 之間，**不分層級**。

**具體反例已存在**：`Diagnostics`（L2）與 `Workspace`（L0）之間無依賴邊，但兩者都會產生注意力請求（破洞掃描完成、資料夾不可用），因此在注意力軸上相鄰——**這條關係在現行 domain-map 上完全查不到**。

**不寫則後果**：讀者會用依賴圖去推導協調關係，而依賴圖對此無鑑別力。設計新 SPEC 時會漏掉不相鄰層之間的注意力競爭，且漏掉的方式沒有訊號——依賴圖查起來是「無關係」，看起來像已確認過。

### Related Files
- docs/domain-map.md::read  # from 0.1.0-W3-184
- docs/usecases::read  # from 0.1.0-W3-184
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-184
- .claude/methodologies::read  # from 0.1.0-W3-184
- docs/domain-map.md::write  # from 0.1.0-W3-203

### Source Acceptance (reference)
- [ref] [ ] 8 個 domain 逐一判定是否產生注意力請求，判準引用方法論而非另立  # from 0.1.0-W3-203
- [ref] [ ] 注意力軸相鄰對已列出，且明寫與 §2 依賴圖正交（含 Diagnostics/Workspace 這組跨層實例）  # from 0.1.0-W3-203
- [ref] [ ] 仲裁點落層已指定，且說明為何不得落入任何 domain 內部  # from 0.1.0-W3-203
- [ref] [ ] 級別定義與截斷規則未重述，以標題文字引用方法論  # from 0.1.0-W3-203
