---
id: 0.1.0-W3-207
title: 實作 注意力仲裁點：單一持有者依級別裁決跨 domain 的提示請求
type: IMP
status: completed
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-203
- 0.1.0-W3-205
relatedTo: []
spawned_tickets:
- 0.1.0-W3-243
- 0.1.0-W3-248
- 0.1.0-W3-249
- 0.1.0-W3-262
- 0.1.0-W3-263
- 0.1.0-W3-270
source_ticket: 0.1.0-W3-184
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-184 SR-1：仲裁點的實作落點與範疇需 PM 裁決
  final_decision: 本票只做我方發起事件的仲裁，不含第三類的接收緩衝
  rationale: 第三類為空類別且有明確 trigger；現在一併做只能憑猜測定義緩衝的容量、逐出策略與持久性，屆時再建才有需求形狀
who:
  current: cinnamon-refactor-owl
  history: {}
what: 實作 注意力仲裁點：單一持有者依級別裁決跨 domain 的提示請求
when: 0.1.0-W3-203 指定仲裁點落層之後
where:
  layer: Presentation
  files:
  - lib/app/attention_arbiter.dart
  - test/unit/app/attention_arbiter_test.dart
why: '`0.1.0-W3-184` 第三節裁定協調形態為**單一仲裁點而非分散式協商**：各 domain 不互相確認，改為向注意力通道的持有者請求，由後者依級別規則裁決。


  **為何不能讓 domain 互相確認**：注意力競爭發生在任何兩個會產生注意力請求的 domain 之間、不分層級（協調圖與 `docs/domain-map.md` §2 的依賴圖正交），因此互相確認需要 N² 條協調邊，且迫使 domain 互相知道彼此存在——直接違反 §3 Bundle 界定表的公開面設計。仲裁只需 N 條，且 domain 之間維持互不知情。


  **不做則後果**：`0.1.0-W3-204` 的級別規則會停在單一載體內（`AppSnackBar`），跨 domain 的請求仍各自直達載體，「現在是不是請求使用者注意的適當時機」沒有任何地方在回答。'
how:
  task_type: Implementation
  strategy: '落層依 0.1.0-W3-203 的指定（不得落入任何 domain 內部）。介面形態：domain 側只表達「我要發出這個級別的請求」，不表達載體與時機；仲裁側依 SPEC-003 的級別規則決定立即呈現、延後至當前請求被消費、或不呈現。


    **級別列舉不得自行定義**：0.1.0-W3-205 已新建 `lib/app/attention_level.dart` 承載級別列舉，本票 import 該檔。本票 acceptance 明訂仲裁器不得 import `AppSnackBar`——列舉住在元件內會使兩者必然產生兩份平行定義，共用列舉檔即為此而設。


    判準以標題文字引用《事件流負載仲裁方法論》的〈通道與仲裁器〉〈讓步與卸載順序〉，引用時連同「它回答的問題」欄。規格輸入為 SPEC-003 的〈注意力通道的到達類別與級別指派表〉〈讓步規則的可驗收形態〉〈暫停與恢復的可驗收形態〉〈仲裁決策事件的定義與測試義務對照〉四節（依序由 0.1.0-W3-204／232／232／243 落地）。


    **測試義務不自行擬定**：取自〈仲裁決策事件的定義與測試義務對照〉的七項逐項裁決表——該表已標明各項在本專案為適用／空成立／不適用並附證據，本票逐項承接，空成立者仍須在 Solution 記錄其空成立依據，不得靜默略過。


    **仲裁決策事件與 AppSnackBarLogSink 的關聯機制由本票 Phase 1 設計**：上述第四節已裁定兩者是同一請求生命週期的兩個觀測點（非另立事件線），並登記了「以請求識別關聯」的要求，但未指定如何把仲裁層的請求識別與 AppSnackBar 的 showId 拼接——那是本票的設計工作。0.1.0-W3-205 已在元件側定義 `currentHolder`／`holderUnknown` 的過渡形態，其收斂 trigger 即綁定本票合併，Phase 1 須一併給出收斂後的形態。


    方法論對本票的兩項實作約束：(a) **禁止持有並等待**——同一請求對同一通道的多次佔用須在首次進入時一次申請；(b) 仲裁器與通道處於同一 failure domain 時不新增單點，若實作為通道之外的獨立物件則須明寫它是新增的可用性單點。


    **本票範疇不含第三類（推送型／外部驅動）的接收緩衝**——該類在本專案為空類別。其 trigger 為 SPEC-003 §3.1〈Domain 視圖〉互動反應表「開啟 docs 目錄」列所載「目錄消失屬外部變更，由下一次重新載入承接，**本畫面不設偵測點**」這項裁定被推翻時。**以該引號內的文字定位，不以行號或章節號定位**——本檔為多 session 併發編輯，該裁定的行號今日已漂移多次，行號引用一律視為過期。0.1.0-W3-184 第八節已明寫該情形下緩衝是實作前置，屆時另票承接。


    與 0.1.0-W3-205 的分界：205 修單一載體內的截斷規則，本票建跨 domain 的請求入口。'
acceptance:
- '[x] domain 側介面只表達級別，不表達載體與時機'
- '[x] 仲裁決策依 SPEC-003 的級別規則，未在仲裁點另立一套'
- '[x] 仲裁點未落入任何 domain 內部，落層符合 0.1.0-W3-203 的指定'
- '[x] 本票未實作第三類接收緩衝，且在 Solution 明記該範疇歸屬與 trigger'
tdd_phase: phase4
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-09T23:56:21'
completed_at: '2026-09-10T14:52:30'
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
current_phase: Phase 4
assignee: parsley-flutter-developer
tdd_phase_source: manual
---

# Execution Log

## Task Summary

實作 注意力仲裁點：單一持有者依級別裁決跨 domain 的提示請求

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 前置實查與 WRAP 三問（lavender-interface-designer，2026-09-10）

**前提驗證**：

| 項目 | 實查 |
|------|------|
| blockedBy `0.1.0-W3-203` | 已完成（2026-09-09），落層寫於 `docs/domain-map.md` §2.6.1：L4 或其下的 presentation 包裝，不得落入 Workspace／Schema／Diagnostics 內部 |
| SPEC-003 §2.14 指派表 | 13 列，級別主名「不可棄／須留痕／可棄」，0.1 無須留痕實例；§2.15 已證跨級別（列 1–10 與列 11–13）時序互斥；§2.16 已證本通道無佔用量觸發的卸載狀態 |
| `0.1.0-W3-239` | 裁定 0.1 不引入任何形式的提示佇列，讓步規則 2 在本通道記為具名例外（截斷並留痕），規則 3 空成立 |
| `0.1.0-W3-205` | pending，載體級別介面尚不存在；本票 Phase 1 只寫「仲裁側需要載體提供什麼」 |
| 現行呼叫點 | `AppSnackBar.show(`：`gap_report_screen.dart:199`、`:218`、`scan_notification_controller.dart:202`；原生 `ScaffoldMessenger.showSnackBar`：`main.dart:219`（`chooseFolderUnavailableMessage`）。三個阻擋狀態尚未實作，現行唯一關卡載體為 `main.dart` 的 `FatalErrorGate` |
| 票面 trigger 引用 `SPEC-003:761` | 該行內容（「本畫面不設偵測點」）現位於 `SPEC-003-interaction-response.md:1186`，屬 §3.1〈Domain 視圖〉「開啟 docs 目錄」列；Solution 改以標題文字定位 |

**發現一**：`main.dart:219` 的 `chooseFolderUnavailableMessage`（Workspace「資料夾不可用」，`docs/domain-map.md` §2.6.2 Workspace 列明寫為注意力請求）不在 SPEC-003 §2.14 的 13 列內，亦不在其〈明確排除〉表。204 acceptance 第一項「無遺漏」對此列未成立。已登記 spawn request。

**發現二**：`0.1.0-W3-205` how (2) 要求 `AppSnackBar.show` 在元件內比較「新訊息級別 vs 當前顯示者級別」；本票仲裁器同樣讀持有者級別做比較。兩處各存一份持有者級別即為 SPEC-003 §2.15 警告的「綁呼叫路徑」變體（兩份記錄可分歧）。已登記 spawn request 請 PM 對齊 205 的 how：載體讀 `holder`，`hide` 條件為 `Accepted.preempted` 非空。

**WRAP**：

- W（其他做法）：(a) 仲裁邏輯放進 `AppSnackBar.show`（載體內）——只管一種載體，關卡與系統通知管不到，且違反 203 的落層；(b) 各 domain 互相確認——184 已否決（N² 邊）；(c) 採用：L4 獨立仲裁器，恰一筆持有者記錄，無佇列。
- A（機會成本）：本票 Phase 1 不擠壓其他票；Phase 3b 需在 205 之前或並行落地，否則 205 會在元件內另存級別記錄。
- P（最可能失敗）：載體側漏回報 `release` 使持有者記錄卡住。已在 Solution 第 8 節列為失效模式並綁防線（懶檢查逾期 + warning 日誌 + 關卡退出路徑靜態檢查）。


### PM 接手記錄（2026-09-10 09:2x）

**本票的 Phase 1／2 與〈PM 裁決：Phase 2 第 11 節〉由另一 session 產出**（最後異動 09-10 01:02）。該 session 於今日 09:2x 明示停手並交接，理由是其手上有只有 PM 能做且卡住兩張票的實機量測工作。本 session 接手 Phase 3a 之後。

**接手前的世界平面查證**（不採信交接訊息本身）：`6f0db318` 確實存在且為該票的 Solution 追加；`tdd_phase` 為 `phase3a`；`blockedBy` 的 `0.1.0-W3-203` 與 `0.1.0-W3-205` 皆已 completed，`205` 的產出已在 main（`AttentionLevel` 共用列舉檔、`AppSnackBar` 的必填 `level`，全套件 1264 passed / 0 skipped）。

**交接方指出並已修進 Solution 的一項，PM 覆核成立**：Phase 1 §2.2 原以內嵌方式定義 `enum AttentionLevel`，而 `0.1.0-W3-205` 已把同一列舉落在 `lib/app/attention_level.dart` 並合併進 main——**兩份成員名與宣告順序逐字相同**（`discardable` / `mustLeaveTrace` / `undroppable`）。

**同名同值同順序比語意衝突難發現**：兩份一致時任何測試都不會紅，直到某天有人只改了其中一份。Phase 3a 前的三件已寫進 Solution：§2.2 改為 import 共用列舉檔並註明權威落點、`AttentionArrival` 與 `AttentionSource` 不受影響（`205` 未建此二者，仍由本票新建）、Phase 2 若有對宣告位置的斷言一併改指向。

**一般形態（交接方歸納，PM 認同）**：Phase 1 產出後、Phase 3a 派發前，**其所依賴的前置票若已合併，Phase 1 的型別定義段必須重新覆核**。TDD 階段轉換表沒有這個檢查點——Phase 1 的正確性在產出當下成立，之後由外部變更靜默失效，且失效不發出任何訊號。本 session 今日在 `0.1.0-W3-176` 撞過同族形態（acceptance 在拆票後變成要求執行者做他做不到的事）。此形態的固化另議，不在本票範疇。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 功能規格（lavender-interface-designer，2026-09-10）

模式：Full（明確指定）。`/spec init` 依 frontmatter 會落 Lite（`how.task_type` 非「新增」、`where.files` 僅 1 項），但 acceptance 第一、二項要求介面契約與跨模組決策規則，Lite 骨架無 API Signatures 與 Error Handling 區段。獨立檔案 `0.1.0-W3-207-phase1-design.md` 被 `ticket-file-access-guard-hook` 阻擋，依本專案既有慣例（`0.1.0-W3-176` Solution）落於本節。

引用約定：引用《事件流負載仲裁方法論》（`.claude/methodologies/event-flow-load-arbitration-methodology.md`）的判準時一律以章節標題文字定位並連同「它回答的問題」欄一起引用，本節不重述判準本身；引用 SPEC-003 以章節標題文字定位。

#### 0. 前置防護（ARCH-010 與元件庫優先）

| 步驟 | 問題 | 結論 |
|------|------|------|
| 1 | 框架內建機制是否已解決？ | 否。`ScaffoldMessenger` 只管 SnackBar 一種載體、只有 FIFO 佇列、無級別概念；macOS 系統通知與顯式關卡不經它。跨載體、依級別的裁決沒有內建機制 |
| 2 | 語言標準庫是否已有解？ | 否。Dart 標準庫無「單一持有者依級別裁決」的原語 |
| 3 | 本地狀態是否足夠？ | 否。請求方分屬三個 domain（`docs/domain-map.md` §2.6.2 判「是」的 Workspace、Schema、Diagnostics），互不知情；本地狀態無法跨 domain 共享持有者記錄 |
| 4 | 外部狀態管理 | 採用。專案已定案 Riverpod（`docs/tech-decisions.md` 補記段「2026-08-26：Stage 4 技術維度定案」），仲裁器以單一 `Provider` 暴露，狀態有界（恰一筆持有者記錄） |

元件庫優先：本功能無任何視覺呈現，不新增、不引用元件庫元件；SnackBar、系統通知、顯式關卡三種既有載體維持原樣，本節只定義它們與仲裁器之間的契約。

#### 1. Purpose

**背景**：`0.1.0-W3-184` 第三節裁定跨 domain 的注意力協調形態為單一仲裁點而非分散式協商。`docs/domain-map.md` §2.6.1 已指定仲裁器落層（L4 畫面狀態層或其下的 presentation 包裝，不得落入任一涉及 domain 內部）；SPEC-003 §2.14 已逐則指定每一則提示的到達類別、級別、標定點；§2.15／§2.16 已把讓步規則與暫停恢復寫成可驗收形態。三者都是規則，沒有任何一處程式碼在執行它們：現行三處 `AppSnackBar.show` 呼叫點（`gap_report_screen.dart:199`、`:218`、`scan_notification_controller.dart:202`）與 `main.dart:219` 的原生呼叫各自直達載體，「現在是不是請求使用者注意的適當時機」沒有任何地方在回答。

**目標**：建立一個仲裁器。各 domain 的事件在其標定點只表達「我要發出這個級別、這個到達類別的請求」，仲裁器依 SPEC-003 §2.15 的讓步規則與持有者狀態回覆一個決策（接受、跳過、拒絕、延後），並在持有者被消費時廣播「已消費」訊號，供被延後的自發型工作重驗前提後再請求。可觀察的改善：每一則進入或離開通道的請求都留下攜帶七項欄位的決策事件；跨級別的截斷只在仲裁器一處讀取持有者級別。

**非目標**：

- 不實作第三類（推送型）接收緩衝，範疇歸屬與 trigger 見第 7 節。
- 不修改任何載體的截斷行為（`AppSnackBar` 的 `hideCurrentSnackBar` 屬 `0.1.0-W3-205`）。
- 不選擇載體。載體由 SPEC-003 §2.13〈判準 T〉與 §2.14 指派表決定，發生在請求方；仲裁器不知道載體是什麼。
- 不引入提示佇列（`0.1.0-W3-239` 裁定），仲裁器不持有任何等待中的請求。
- 不暫停任何背景工作。仲裁器只裁決不做工（方法論〈核心概念〉），暫停與恢復由工作擁有者依 SPEC-003 §2.16 執行，仲裁器只提供「已消費」訊號。

#### 2. API Signatures

**2.1 落點與生命週期**

| 項目 | 規格 |
|------|------|
| 層級 | L4 畫面狀態層（`docs/domain-map.md` §2 定為 layer 而非 domain），與 `lib/app/router.dart`、`lib/app/app_lifecycle.dart` 同層 |
| 暴露方式 | `attentionArbiterProvider = Provider<AttentionArbiter>`，App 生命週期內單一實例；測試以 `overrideWithValue` 注入替身 |
| 建議檔案 | `lib/app/attention_arbiter.dart`。不得放在 `lib/screens/gap_report/`（Diagnostics）、`lib/workspace/`（Workspace）或 Schema 相關路徑；亦不建議放 `lib/components/`——SPEC-003 §2〈頁面狀態保留〉列「畫面級狀態不得存於元件庫元件內部」，持有者記錄正是畫面級狀態。票面 `where.files` 現為 `lib/components::write`，見 NeedsContext |
| 執行緒 | 與通道（Flutter UI isolate）同一 isolate，所有方法同步、無鎖。方法論〈跨執行緒與 isolate〉「仲裁器與它的通道住在同一個執行緒或 isolate」回答「多執行緒下仲裁器放哪？」 |
| 狀態上界 | 恰一筆持有者記錄或空。無佇列、無等待清單。方法論同節「仲裁器的決策為常數時間，狀態有界」回答「仲裁器自己過載怎麼辦？」——本專案無佇列，以「狀態恰為零或一筆」退化滿足 |

**2.2 型別定義**

```dart
/// 級別（SPEC-003 §2.14 主名；L1–L3 僅慣用名）。列舉順序即優先序，index 越大越高。
enum AttentionLevel {
  discardable,     // 可棄（L3 可補查）
  mustLeaveTrace,  // 須留痕（L2 必須知悉）；0.1 無實例，仍為合法值
  undroppable,     // 不可棄（L1 必須決策）
}

/// 到達類別（SPEC-003 §2.14 到達類別欄）。
/// 推送型刻意不存在：0.1 無接收緩衝，該類別在型別層即不可表達（第 7 節）。
enum AttentionArrival {
  waiting,      // 等待型：能回溯到一個正在等待我方回覆的外部發起者
  spontaneous,  // 自發型：開始的決定點在我方程式碼內
}

/// 來源 domain。僅列 docs/domain-map.md §2.6.2 判「是」的三者；
/// 新增來源須先在該表改判，不得在此擴充。
enum AttentionSource { workspace, schema, diagnostics }

/// 一次請求。全部欄位在標定點（SPEC-003 §2.14「標定點」欄）由請求方填妥，
/// 仲裁器只讀取、不推導。
final class AttentionRequest {
  const AttentionRequest({
    required this.id,
    required this.level,
    required this.arrival,
    required this.source,
    this.parentLevel,
    this.deadline,
  });

  /// 請求識別。與 0.1.0-W3-176 的 shown／closed 配對識別同源，
  /// 使仲裁事件與載體事件可跨層配對。
  final String id;
  final AttentionLevel level;
  final AttentionArrival arrival;
  final AttentionSource source;

  /// 子呼叫時填父請求的級別（SPEC-003 §2.14「傳播規則」欄非空的列）。
  final AttentionLevel? parentLevel;

  /// 等待型由發起者攜帶的剩餘預算；自發型為下一次排程時刻前的餘量。
  /// 缺席時仲裁器不代填，只在事件記「指派」（見 2.5）。
  final Duration? deadline;
}

/// 接受後交給請求方與載體的持有憑證；載體以它回報 presented／release。
final class AttentionHandle {
  final String requestId;
  final AttentionLevel level;
  final AttentionArrival arrival;
  final AttentionSource source;
  final DateTime acceptedAt;
}

/// 仲裁決策。動作集取自方法論〈讓步與卸載順序〉卸載順序表，本專案只用到四種。
sealed class AttentionDecision {}

/// 接受：立即呈現。preempted 非空時載體必須先截斷該持有者再呈現。
final class AttentionAccepted extends AttentionDecision {
  final AttentionHandle handle;
  final AttentionHandle? preempted;
  /// 同級別取代（0.1.0-W3-239 具名例外）時為 true，載體依 SPEC-003 §2.13
  /// 〈截斷事件的日誌等級〉留痕；跨級別搶佔時為 false。
  final bool sameLevelReplace;
}

/// 跳過：自發型／可棄在通道被高級別持有時的動作（「跳過，不補跑」）。
final class AttentionSkipped extends AttentionDecision {
  final AttentionHandle blockedBy;
}

/// 拒絕：等待型在通道被高級別持有時的動作，附可重試資訊。
final class AttentionRejected extends AttentionDecision {
  final AttentionHandle blockedBy;
  /// 可重試資訊：訂閱後於持有者被消費時收到一次通知。
  final Stream<AttentionConsumed> retryAfter;
}

/// 延後：自發型／須留痕在通道被更高級別持有時的動作。
/// 仲裁器不保存此請求；請求方訂閱 signal，收到後重驗前提再重新 request。
final class AttentionDeferred extends AttentionDecision {
  final AttentionHandle blockedBy;
  final Stream<AttentionConsumed> signal;
}

/// 持有者離開通道的原因（消費三形態 + 非消費的三種離開）。
enum AttentionReleaseReason {
  responded,     // 使用者回應（按下動作、關卡決策、授權對話框回答）
  dismissed,     // 使用者主動關閉
  expired,       // 載體自然過期（SnackBar timeout）；或仲裁器懶檢查判定逾期
  preempted,     // 被高級別搶佔、或同級別取代而截斷（由仲裁器於接受當下記錄）
  notPresented,  // 接受後載體未呈現（如 context 已卸載）
  withdrawn,     // 我方撤回（ScanNotifier.withdraw）
}

/// 「已消費」訊號的內容。
final class AttentionConsumed {
  final AttentionHandle handle;
  final AttentionReleaseReason reason;
  final DateTime at;
}

/// 目前持有者的快照（讀取用）。
final class AttentionHolder {
  final AttentionHandle handle;
  final DateTime? presentedAt;      // presented 尚未回報時為 null
  final Duration? naturalLifespan;  // 載體回報；關卡類為 null（無自然存活期）
  Duration ageAt(DateTime now);      // 最老未消費請求年齡（本專案恆等於持有者年齡）
}
```

**2.3 仲裁器介面**

```dart
abstract class AttentionArbiter {
  /// 請求佔用通道。同步、常數時間、不阻塞、不保存請求。
  AttentionDecision request(AttentionRequest request);

  /// 載體開始呈現時回報；naturalLifespan 為該載體的自然存活期
  /// （SnackBar 取 Motion.snackBar／Motion.snackBarWithAction；關卡與授權對話框傳 null）。
  void presented(AttentionHandle handle, {required Duration? naturalLifespan});

  /// 持有者離開通道。非當前持有者的 handle 只留痕不改狀態。
  void release(AttentionHandle handle, AttentionReleaseReason reason);

  /// 目前持有者；空通道為 null。
  AttentionHolder? get holder;

  /// 「已消費」廣播。每次當前持有者離開即發出一則；無訂閱者時不緩衝。
  Stream<AttentionConsumed> get consumed;
}
```

| 成員 | 輸入約束 | 回傳／副作用 |
|------|---------|-------------|
| `request` | `request.id` 非空；`parentLevel` 非空時 `level` 不得高於它 | 回傳四種決策之一；`AttentionAccepted` 時持有者記錄立即改為本請求（不等 `presented`）；`preempted` 非空時舊持有者記錄已被移除並已發出 `consumed(reason: preempted)` |
| `presented` | `handle` 為當前持有者；`naturalLifespan` 為零或負值視同 `null` 並留痕原值 | 記錄 `presentedAt` 與 `naturalLifespan`；非當前持有者時只留痕 |
| `release` | 任意 handle | 當前持有者：清空記錄、發出 `consumed`；其他：留痕 `releasedNonHolder`，狀態不變 |
| `holder` | 無 | 快照；讀取不觸發懶檢查 |
| `consumed` | 無 | 廣播 stream；`AttentionRejected.retryAfter` 與 `AttentionDeferred.signal` 皆為同一 stream 過濾為 `blockedBy` 那一筆的視圖 |

**2.4 決策規則（唯一的裁決表）**

判準引用方法論〈讓步與卸載順序〉三條讓步規則（回答「高負載時先停哪些請求？」）與卸載順序表（到達類別 × 級別）；級別比較鍵引用〈級別〉「級別是請求的屬性，不是端點的屬性」（回答「級別是靜態標註還是動態推導？」）。本表不另立判準，只把兩者對本專案「恰一筆持有者」的狀態機實例化。

先做懶檢查：持有者已 `presented` 且 `naturalLifespan` 非空且 `now - presentedAt > naturalLifespan` → 先以 `expired` 釋放並留痕，再依空通道處理。

| 持有者 H | 請求 R 的級別與類別 | 決策 | 依據 |
|----------|-------------------|------|------|
| 空 | 任意 | `Accepted(preempted: null)` | 無競爭（184 §二「競爭 = 第二個抵達時第一個尚未被消費」） |
| 級別 < R | 任意 | `Accepted(preempted: H, sameLevelReplace: false)` | 讓步規則 1「高級別抵達時可搶佔低級別」 |
| 級別 == R | 任意 | `Accepted(preempted: H, sameLevelReplace: true)` | `0.1.0-W3-239` 具名例外：規則 2 在本通道以截斷並留痕取代排隊或合併；跨載體時載體無法截斷，`preempted` 仍記為舊持有者以維持恰一筆記錄，留痕由仲裁事件承接 |
| 級別 > R | 自發型／可棄 | `Skipped(blockedBy: H)` | 卸載順序表自發型／可棄格「跳過，不補跑」；規則 3「低級別不得搶佔任何請求」 |
| 級別 > R | 自發型／須留痕 | `Deferred(blockedBy: H)` | 卸載順序表自發型／須留痕格「延後至離開卸載狀態」。本專案「離開卸載狀態」= 持有者被消費（184 §三「延遲的終點是當前注意力請求被消費」） |
| 級別 > R | 自發型／不可棄 | 不可達 | 不可棄已是最高級別，不存在更高的 H |
| 級別 > R | 等待型／可棄 | `Rejected(blockedBy: H)` | 卸載順序表等待型／可棄格「拒絕＋可重試資訊」 |
| 級別 > R | 等待型／須留痕 | `Rejected(blockedBy: H)` 且仲裁事件逐筆留痕 | 等待型／須留痕格「拒絕＋可重試資訊＋留痕」 |
| 級別 > R | 等待型／不可棄 | 不可達 | 同上；卸載順序表明寫等待型／不可棄「不由仲裁器卸載」，本表以型別層不可達滿足 |
| `H.requestId == R.id`（持有者再請求） | 任意 | `Rejected(blockedBy: H)` 並留痕 `heldAndRequested` | 方法論〈通道與仲裁器〉「禁止的是並行持有：持有一份的同時申請第二份」（回答「一個請求可不可以在持有通道時再要一份？」）。序列重借（先 `release` 再 `request`）不受此限 |

**級別傳播單調不升**：`parentLevel` 非空且 `level > parentLevel` 時，仲裁器以 `parentLevel` 裁決並留痕 `levelClamped`。這是執行既有標定而非推導級別——方法論〈級別〉「呼叫方可為子呼叫指派不高於自身的級別，不可升」（回答「一個不可棄請求發出的子呼叫算什麼級別？」）。

**與 SPEC-003 §2.13〈判準 T〉的先後**：T1–T4 在請求方執行，決定要不要暫態提示、用哪個載體、是否延後至 `resumed`；仲裁在「即將呈現」的那一刻執行，決定此刻能不能。順序為 T → 仲裁 → 載體；(b2) 延後顯示的項目在 `resumed` 當下才 `request`。

**與 `0.1.0-W3-122` 的分工**：122 把掃描觸發改為消費 `firstVisibleProvider`，解的是「掃描為何在錯誤時機啟動」；本票解的是「掃描完成後的注意力請求此刻能否呈現」。兩者正交，122 落地與否不改變本表。

**2.5 可觀測事件**

不變式引用方法論〈可觀測性〉「每一個請求離開通道都是一個可觀測事件」（回答「被丟棄的請求怎麼追溯？」）。以與 `AppSnackBarLogSink` 同形的接縫實作：

```dart
enum AttentionArbiterLogEvent {
  accepted, preempted, sameLevelReplaced, skipped, rejected, deferred,
  released, expired, releasedNonHolder, heldAndRequested, levelClamped,
}

typedef AttentionArbiterLogSink = void Function(
  AttentionArbiterLogEvent event,
  Map<String, Object?> fields, {
  int? level,
});
```

每則事件的 `fields` 至少含七項：`requestId`、`arrival`、`level`、`channel`（0.1 固定為 `userFocus`）、`decision` 或 `reason`、`occupancy`（`{inFlight: 0|1, queueDepth: 0, oldestAge: Duration?}`，三項分列不相加）、`deadlineRemaining`（為 `null` 時另附 `deadlineAssigned: true`）。

留痕粒度：0.1 三個級別皆逐筆記錄。方法論〈可觀測性〉「可棄只計數加取樣樣本」的前提是留痕本身可能成為負載；本通道的請求率受人類互動速率上界（單一使用者桌面應用），逐筆記錄不構成負載，計數退化形態不啟用。留痕載體為 `developer.log`（`name: 'AttentionArbiter'`），與被保護的通道（畫面）不共用。方法論〈旁路的存在與可靠性〉「允許有損者降級為同進程的計數」（回答「沒有旁路的系統怎麼辦？」）——本專案無 structured log 基礎設施，此為退化形態，明列不隱藏。

日誌等級：`skipped`／`rejected`／`deferred`／`expired` 對背景發起者取 warning（900），對使用者發起者取 info，鍵為發起者而非級別，沿用 SPEC-003 §2.13〈截斷事件的日誌等級以「事件的發起者」為鍵〉（發起者由 `arrival` 判定：`spontaneous` 為背景發起）；`heldAndRequested`、`levelClamped`、`releasedNonHolder` 恆為 warning（呼叫方契約違反）。

**2.6 仲裁側需要載體提供什麼（載體側契約，本票不實作）**

`0.1.0-W3-205` 尚未執行，載體級別介面不存在。本節只寫仲裁側的需求，供 205 與後續載體票承接：

| 需求 | 內容 | 0.1 對應載體 |
|------|------|-------------|
| C1 執行搶佔 | 收到 `Accepted(preempted: H)` 時，先截斷 H 再呈現新的一則。被搶佔者的離開已由仲裁器在接受當下記錄並發出 `consumed(preempted)`，載體對 H **不再** `release`（否則落 `releasedNonHolder`） | `AppSnackBar`：205 的 `hide` 條件即「`preempted` 非空」；「當前顯示者的級別」須讀自 `holder`，不在元件內另存一份 |
| C2 回報呈現 | 呈現開始即 `presented(handle, naturalLifespan)` | SnackBar：`Motion.snackBar`／`Motion.snackBarWithAction`；系統通知見 C4；授權對話框、關卡：`null` |
| C3 回報消費 | 三種消費形態各對應 `release` 一次：使用者回應 → `responded`；主動關閉 → `dismissed`；自然過期 → `expired` | SnackBar：`closed` future 的 `SnackBarClosedReason` 映射（action → responded；dismiss／swipe → dismissed；timeout → expired；hide／remove 由呼叫端指定 `withdrawn`，被搶佔者依 C1 不回報）；關卡：狀態機退出即 `responded` |
| C4 系統通知的消費定義 | 0.1 裁定：送達即消費。`ScanNotifier.show` 的 `Future` 完成即 `release(handle, responded)`；通知在通知中心的後續存活不計入通道 | 理由：App 邊界外的存活無法觀測，若計入則持有者記錄無上界且無自然存活期可懶檢查；使用者點擊通知（`activated`）另行導向，不經仲裁 |
| C5 不呈現路徑 | 接受後任何未呈現即返回的路徑（如 `context.mounted == false`）必須 `release(handle, notPresented)` | 對應 `AppSnackBarLogEvent.skippedUnmounted` |
| C6 配對識別 | 載體事件（`shown`／`closed`）攜帶與 `AttentionRequest.id` 同源的識別 | `0.1.0-W3-176` G1 欄位 |

#### 3. GWT Scenarios

級別縮寫：可棄＝D、須留痕＝T、不可棄＝U。持有者以「載體／級別／類別」描述，皆為 SPEC-003 §2.14 的列。

正常流程：

- **場景 1：空通道接受等待型**。Given `holder == null`；When Diagnostics 以 `waiting`／D 請求（§2.14 列 5）；Then 回傳 `Accepted(preempted: null)`，`holder.handle.requestId` 等於請求 id，發出 `accepted` 事件含七欄位。
- **場景 2：空通道接受自發型**。Given `holder == null`；When Diagnostics 以 `spontaneous`／D 請求（列 3，於 `resumed` 當下）；Then 回傳 `Accepted`，`holder` 為該請求。
- **場景 3：呈現、消費、訊號**。Given 場景 1 的持有者已 `presented(naturalLifespan: Motion.snackBar)`；When 載體 `release(handle, expired)`；Then `holder == null`，`consumed` 發出一則 `(handle, expired)`，發出 `released` 事件。
- **場景 4：序列重借合法**。Given 授權對話框請求（列 4，`spontaneous`／D）已被接受並以 `responded` 釋放；When 同一流程以新 id 請求系統通知（列 2，`parentLevel: D`）；Then 回傳 `Accepted`，無 `heldAndRequested` 事件。

讓步規則：

- **場景 5：高級別搶佔低級別**。Given 持有者為 SnackBar／D／`waiting`；When Schema 以 `spontaneous`／U 請求（列 11）；Then 回傳 `Accepted(preempted: <SnackBar handle>, sameLevelReplace: false)`，`holder` 為關卡請求，發出 `preempted` 事件（`requestId` 為被搶佔者），`consumed` 發出 `(舊 handle, preempted)`。
- **場景 6：同級別取代（具名例外）**。Given 持有者為 SnackBar／D／`waiting`（列 6）；When Diagnostics 以 `spontaneous`／D 請求（列 3）；Then 回傳 `Accepted(preempted: <舊 handle>, sameLevelReplace: true)`，發出 `sameLevelReplaced` 事件，`holder` 為新請求。
- **場景 7：低級別自發型遇高級別持有者被跳過**。Given 持有者為關卡／U／`spontaneous`；When Diagnostics 以 `spontaneous`／D 請求；Then 回傳 `Skipped(blockedBy: <關卡 handle>)`，`holder` 不變，發出 `skipped` 事件（level 900）。
- **場景 8：低級別等待型遇高級別持有者被拒絕並可重試**。Given 持有者為關卡／U；When Workspace 以 `waiting`／D 請求；Then 回傳 `Rejected(blockedBy, retryAfter)`，`holder` 不變；關卡以 `responded` 釋放時 `retryAfter` 發出一則且 `handle` 為關卡 handle。
- **場景 9：須留痕自發型被延後、訊號後重新請求**。Given 持有者為關卡／U，請求方為測試替身的 `spontaneous`／T；When 請求得 `Deferred(signal)`，訂閱 `signal`，關卡釋放，請求方重驗前提為真後再次 `request`；Then 第一次回傳 `Deferred`，訊號恰發出一則，第二次回傳 `Accepted`，釋放後再請求前 `holder == null`（仲裁器未持有該請求）。

邊界條件：

- **場景 10：持有者再請求被拒（禁止持有並等待）**。Given 持有者 `requestId == 'r1'` 尚未釋放；When 以 `id: 'r1'` 再次 `request`；Then 回傳 `Rejected`，發出 `heldAndRequested`（level 900），`holder` 不變。
- **場景 11：逾期懶檢查**。Given 持有者已 `presented(naturalLifespan: 4s)`，注入時鐘前進 4.001s，載體未回報；When 任意新請求到達；Then 先發出 `expired` 事件並 `consumed(expired)`，再依空通道接受新請求。
- **場景 12：非持有者的 release 不改狀態**。Given 持有者為 `r2`；When `release(<r1 handle>, dismissed)`；Then `holder` 仍為 `r2`，發出 `releasedNonHolder`，`consumed` 不發出。
- **場景 13：子呼叫級別高於父級別被夾住**。Given `holder == null`；When 請求 `level: U, parentLevel: D`；Then 回傳 `Accepted` 且 `handle.level == D`，發出 `levelClamped`。
- **場景 14：無 deadline 的請求標「指派」**。When 請求 `deadline: null`；Then 對應事件 `fields['deadlineRemaining'] == null` 且 `fields['deadlineAssigned'] == true`。
- **場景 15：關卡無自然存活期不逾期**。Given 持有者為關卡／U，`presented(naturalLifespan: null)`，時鐘前進任意長；When 新請求到達；Then 不發出 `expired`，依 2.4 以關卡為持有者裁決。
- **場景 16：`naturalLifespan` 為零或負值**。When `presented(handle, naturalLifespan: Duration.zero)`；Then 視同 `null`，`presented` 事件 `fields` 附原值；之後不觸發懶檢查逾期。

異常流程：

- **場景 17：接受後未呈現**。Given 請求已被接受，載體因 `context` 已卸載未呈現；When `release(handle, notPresented)`；Then `holder == null`，發出 `released`（`reason == notPresented`），`consumed` 發出一則。
- **場景 18：推送型不可表達**。靜態檢查：`AttentionArrival` 恰有 `waiting`、`spontaneous` 兩值，無第三值。

#### 4. Error Handling

| 錯誤情境 | 處理策略 | 回傳／拋出 | 需要回滾？ |
|---------|---------|-----------|-----------|
| `request.id` 為空字串 | 拒絕進入裁決 | 開發期 `assert`；生產期回傳 `Rejected` 並留痕 `heldAndRequested` 以外的獨立欄位 `reason: emptyId` | 否 |
| 持有者再請求 | 拒絕，不改狀態 | `Rejected` + `heldAndRequested` warning | 否 |
| `level > parentLevel` | 夾至 `parentLevel` 後照常裁決 | 對應決策 + `levelClamped` warning | 否 |
| `presented` 的 handle 非當前持有者 | 忽略，留痕 | `releasedNonHolder` warning（`fields['op'] = 'presented'`） | 否 |
| `release` 的 handle 非當前持有者 | 忽略，留痕 | `releasedNonHolder` | 否 |
| 載體從未回報 `release`（契約違反） | 有自然存活期者由懶檢查以 `expired` 清除；無自然存活期者（關卡）無法自癒 | `expired` warning；關卡類停留為持有者直到狀態機退出回報 | 否 |
| `consumed` 無訂閱者時發出 | 不緩衝、不重播 | 無 | 否 |
| 請求方在 `Deferred` 後未重新請求 | 不屬仲裁器責任；自發型 deadline 為「下一次排程時刻」，於下一輪自行再請求 | 無 | 否 |
| 同一 tick 內兩次 `request` | `oldestAge` 由注入時鐘計算得零；裁決不用年齡，無影響 | 無 | 否 |

方法論〈跨執行緒與 isolate〉「仲裁器佇列深度是它自身的佔用量指標；溢出時對等待型的申請回覆拒絕」（回答「仲裁器溢出時的預設是什麼？」）：本專案無佇列，溢出分支不存在，此處明列避免讀者以為遺漏。

#### 5. Dependencies

| 依賴 | 類型 | 用途 | 不可用時 |
|------|------|------|---------|
| `flutter_riverpod` `Provider` | 套件 | 暴露單一實例 | 專案定案依賴，無降級 |
| `dart:developer` `developer.log` | 標準庫 | 留痕載體 | 專案統一日誌工具，無降級 |
| 注入時鐘 `DateTime Function() now` | 建構參數 | 逾期懶檢查與年齡計算；測試注入固定時鐘（`test-assertion-design-rules` D3） | 生產預設 `DateTime.now` |
| SPEC-003 §2.14 指派表 | 規格 | 請求方填 `level`／`arrival` 的權威來源 | 缺列的事件不得請求；見 Spawn Requests SR 對 `chooseFolderUnavailableMessage` 的登記 |
| `0.1.0-W3-205` | 載體側前置票 | 執行 C1 搶佔、C2／C3 回報 | 未落地前仲裁器可獨立測試（替身載體）；正式路徑上 `Accepted(preempted)` 的截斷不會發生，仲裁事件仍完整 |
| `0.1.0-W3-176` | 載體側前置票 | C6 配對識別 | 未落地前仲裁事件與載體事件無法跨層配對，各自仍完整 |
| `0.1.0-W3-203` | 已完成 | 落層指定 | 不適用 |

#### 6. Acceptance（展開票面四項）

票面 1（domain 側介面只表達級別，不表達載體與時機）：

- [ ] `AttentionRequest` 無任何欄位指涉載體（無 variant、message、context、widget 型別），靜態可讀出
- [ ] `AttentionRequest` 無任何欄位指涉呈現時機（無 delay、showAt、schedule 型別）；`arrival` 表達「這一次的到達時機由誰決定」，屬 SPEC-003 §2.14 標定點填妥的請求屬性，不是呈現時機的指示
- [ ] `AttentionDecision` 四個子型別涵蓋 2.4 全部可達格，且每格恰對應一個子型別

票面 2（仲裁決策依 SPEC-003 的級別規則，未另立一套）：

- [ ] 場景 5、6、7、8、9 各有測試，斷言結果與 2.4 表逐格一致
- [ ] 仲裁器原始碼中級別比較只出現在一處函式，比較鍵為 `AttentionLevel.index`，無以載體型別、呼叫路徑或 `AttentionSource` 為鍵的分支（SPEC-003 §2.15〈鑑別「綁級別」與「綁呼叫路徑」〉靜態檢查）
- [ ] 級別列舉值與 SPEC-003 §2.14 主名一一對應，元件內無第二套級別定義

票面 3（未落入任何 domain 內部，落層符合 203）：

- [ ] 仲裁器檔案位於 L4（`lib/app/`）或經 PM 裁定的 presentation 包裝路徑，不在 `lib/screens/gap_report/`、`lib/workspace/` 或任何 Schema 路徑
- [ ] 仲裁器不 import 任何 domain 型別（`GapReportState`、`WorkspaceState`、schema 型別）；三個來源只以 `AttentionSource` 列舉出現
- [ ] 仲裁器不呼叫任何載體（不 import `AppSnackBar`、`ScanNotifier`、`BlockedState`）

票面 4（未實作第三類接收緩衝，Solution 明記歸屬與 trigger）：

- [ ] `AttentionArrival` 恰兩值（場景 18）
- [ ] 仲裁器無任何集合型欄位（`List`、`Queue`、`Map` 持有請求），狀態恰為 `AttentionHolder?` 一筆
- [ ] 本 Solution 第 7 節存在

方法論兩項實作約束：

- [ ] 禁止持有並等待：場景 10 有測試；仲裁器無任何回傳 `Future` 的方法（靜態檢查；`Stream` 是訂閱點不是等待點，仲裁器自身不 `await`）
- [ ] 可用性單點：仲裁器原始碼 library doc 明寫「本物件是通道之外的獨立物件，為新增的可用性單點」並列出失效模式與防線（第 8 節）

可觀測性：

- [ ] 每一種 `AttentionDecision` 與每一種 `AttentionReleaseReason` 各對應至少一則事件，事件 `fields` 含 2.5 七項（以記錄器替身斷言欄位鍵集合）
- [ ] 日誌等級依發起者：場景 7 事件 level 900；等待型被拒絕的事件無 level 或為 info

#### 7. 第三類（推送型）接收緩衝的範疇歸屬與 trigger

| 項目 | 內容 |
|------|------|
| 歸屬 | 不在本票。`0.1.0-W3-184` 第八節：推送型的協調點在「接收後、呈現前」，需要接收緩衝作為實作前置；本票只建「發起前」的協調點 |
| 現況 | 本專案推送型為空類別（184 Problem Analysis 更正段） |
| trigger | SPEC-003 §3.1〈Domain 視圖〉互動反應表「開啟 docs 目錄」列「目錄消失屬外部變更，由下一次重新載入承接，本畫面不設偵測點」的裁定被推翻時。票面原以行號 `SPEC-003:761` 指涉，該行內容現已移至 §3.1（實查於 `SPEC-003-interaction-response.md:1186`），此處改以標題文字定位 |
| 屆時形狀 | `AttentionArrival` 新增第三值；仲裁器新增每（類別 × 級別）各一條有界 FIFO 的接收緩衝（方法論〈跨執行緒與 isolate〉）；可棄溢出丟最舊、須留痕溢出先留痕、不可棄落持久佇列（〈讓步與卸載順序〉卸載順序表推送型列）。本票不預留任何欄位或空實作 |

#### 8. 可用性單點的明寫

方法論〈通道與仲裁器〉「仲裁器與通道處於同一 failure domain 時，仲裁器不新增單點；仲裁器若是通道之外的獨立行程，它就是新增的可用性單點」回答「仲裁器會不會成為新的單點？」。

本仲裁器與通道同在 UI isolate，非獨立行程；但它是通道之外的獨立物件——持有者記錄與 `ScaffoldMessenger` 內部佇列、系統通知送達狀態、關卡狀態機三者皆不共享記憶體。因此存在「仲裁器狀態壞而通道還能動」的失效路徑，票面要求明寫：

| 失效模式 | 表現 | 防線 |
|---------|------|------|
| 持有者記錄未釋放（載體漏回報） | 之後所有低級別請求被跳過或拒絕，畫面本身正常 | 有自然存活期者：懶檢查逾期（場景 11）；`releasedNonHolder`／`expired` warning 使漏回報可被日誌發現 |
| 關卡類持有者記錄未釋放 | 同上且無法自癒 | 無上界。消費機制：Phase 3b acceptance 須含「關卡狀態機每一條退出路徑皆有 `release`」的靜態檢查（方法論〈登記類條款的消費要件〉「登記只有在連接消費機制時才算完成」回答「寫下待量測之後呢？」） |
| 訊號無人訂閱時發出 | 被延後者永不重新請求 | 第 4 節：自發型工作於下一次排程時刻自行再請求，訊號只縮短等待、不是唯一恢復路徑 |
| 對等待型 fail-closed | 使用者操作的回饋被拒 | 只在持有者為更高級別時拒絕，且拒絕附 `retryAfter`；空通道與同級別一律接受 |

#### 9. 行為單元清單

| 行為單元 | Given-When-Then | 邊界條件 | 獨立可測？ |
|---------|----------------|---------|-----------|
| BU-1 空通道接受 | 場景 1、2 | 場景 14（無 deadline） | 是（無替身） |
| BU-2 呈現與消費 | 場景 3 | 場景 12（非持有者）、16（零存活期）、17（未呈現） | 是（無替身） |
| BU-3 跨級別搶佔 | 場景 5 | 場景 13（夾級別） | 是（注入持有者狀態） |
| BU-4 同級別取代 | 場景 6 | 跨載體時載體不截斷（仲裁側行為相同） | 是（注入持有者狀態） |
| BU-5 低級別讓步 | 場景 7、8、9 | 場景 15（關卡不逾期） | 是（注入持有者狀態） |
| BU-6 已消費訊號 | 場景 8 後半、9 | 無訂閱者不緩衝 | 是（stream 記錄器） |
| BU-7 禁止持有並等待 | 場景 10 | 序列重借合法（場景 4） | 是（無替身） |
| BU-8 逾期懶檢查 | 場景 11 | 場景 15、16 | 是（注入時鐘） |
| BU-9 可觀測事件 | 各場景的事件斷言 | 七欄位齊全、等級依發起者 | 是（記錄器替身） |
| BU-10 型別層排除推送型 | 場景 18 | 無 | 是（靜態） |

方法論〈測試義務〉對照：仲裁決策事件、級別傳播單調不升兩項不需替身；三條讓步規則、卸載順序格需注入可控的持有者狀態（經正常 `request` + `presented` 建立，不開後門）；仲裁器自身溢出在本專案不可達，以「無集合型欄位」的靜態檢查替代；進入與離開卸載的閾值滯後在本通道無落點（SPEC-003 §2.16 已證），不設測試。

#### 10. Decision Questions 與 SOLID

| 問題 | 回答 |
|------|------|
| Q1 跨模組相似邏輯 | 級別比較邏輯在 `0.1.0-W3-205` 的 `AppSnackBar.show` 與本仲裁器會重複。裁定：只在仲裁器一處比較，205 的 `hide` 條件改為執行 `Accepted.preempted`，載體不自存「當前顯示者的級別」（2.6 C1）。已登記 spawn request 交 PM 對齊 205 的 how |
| Q2 共用或各自實作 | 共用（相似度 100%、變更必同步；各自實作即 SPEC-003 §2.15 警告的「無人察覺下違反」路徑） |
| Q3／Q4 ID 設計 | `AttentionRequest.id` 由請求方提供、與 176 配對識別同源；仲裁器不生成 ID，無時序碰撞問題 |
| Q5 出錯時操作者如何知道 | 2.5 事件與等級表；契約違反類恆 warning |
| Q6a 最慢操作 | 無 I/O；所有方法常數時間 |
| Q6b 寫入中途失敗 | 無多步驟寫入；`request` 對持有者記錄的替換為單一賦值 |

SOLID：SRP 單一職責（裁決與留痕；留痕經接縫注入，不在裁決路徑做 I/O）；OCP 新增級別或來源只改列舉與 2.4 表；DIP 請求方依賴 `AttentionArbiter` 抽象與 provider。不需拆分。

#### 11. /spec validate 結果

第一輪。結構檢查：通過（六區段皆存在且非空；18 個完整 GWT；Acceptance 皆以 `- [ ]` 開頭；Purpose 未超過 500 字）。`check_api_surface.py` 不適用（無 HTTP API）；`check_domain_coverage.py` 適用對象為 domain spec，非 ticket 級規格。語義推演：

| 維度 | 問題 | 處置 |
|------|------|------|
| 1 邊界完整性 | `naturalLifespan` 為零或負值？ | 已補：視同 `null` 並留痕原值（2.3 表、場景 16） |
| 1 邊界完整性 | 同一 tick 內兩次 `request` 的年齡？ | 已補：第 4 節末列 |
| 2 錯誤路徑 | 載體對被搶佔者再 `release` 會落 `releasedNonHolder`？ | 已修：C1 改為載體對被搶佔者不回報，仲裁器於接受當下記錄並發訊號（場景 5 同步更正） |
| 3a 狀態轉換 | `Accepted` 但 `presented` 未回報前即被搶佔？ | 已涵蓋：持有者記錄不依賴 `presented`；`presentedAt == null` 時懶檢查不觸發 |
| 3b 約束違反 | 「無回傳 `Future` 的方法」與 `Stream` 欄位是否衝突？ | 不衝突，acceptance 加註說明 |
| 5 資源競爭 | 仲裁事件日誌本身的到達類別與級別？ | 日誌屬留痕旁路非通道請求，不標定；「留痕的載體不得共用被保護的通道」已滿足 |
| 4 教學一致性 | skipped：無教學模組對應表 | — |

第二輪（套用修正後）：結構檢查通過；語義推演無新問題。迭代結束（Full 上限 3 次）。

#### 12. Phase 2 交接摘要

- 測試對象為純 Dart 類別，建議 `test/unit/app/attention_arbiter_test.dart`；不需 widget 測試。
- 需要的替身：記錄器（`AttentionArbiterLogSink`）、固定時鐘、預先注入的持有者狀態（經正常 `request` + `presented` 建立）。
- 不需替身：型別層檢查（場景 18）、級別夾住（場景 13）、七欄位齊全（場景 14 與各事件）。
- 載體側（205／176）未落地不阻擋本票 Phase 2–3b；與載體的整合測試留待 205 完成後另票。

### Phase 2 測試設計（sage-test-architect，2026-09-10）

範圍：Phase 1 第 3 節場景 1–18、第 4 節錯誤處理表、第 6 節 acceptance 展開項、第 9 節 BU-1～BU-10。落點：`test/unit/app/attention_arbiter_test.dart`（`where.files` 已由 PM 更正，NeedsContext 有記錄）。測試依 Phase 1 規格設計；Phase 1 與 SPEC-003 §2.17 的兩處分歧與 Phase 1 自身的三處缺口見第 11 節，測試對其採中立形態，不代為裁決。

四項硬條件的承接方式：(1) 恰一筆持有者、無佇列——無任何測試假設排隊或依序呈現，V-3 靜態鎖住無集合型欄位；(2) 持有者再請求即拒絕——T-14；(3) 推送型型別層排除——T-26，不設任何推送型案例；(4) 載體側未實作——測試本身依 C1–C6 契約扮演載體呼叫 `presented`／`release`，不引用 `AppSnackBar`、`ScanNotifier`、關卡。

#### 1. 測試策略決策（分層與隔離邊界）

| 決策 | 選擇 | 依據 |
|------|------|------|
| 測試層級 | 純 Dart 單元測試（`test`，非 `testWidgets`），置於 `test/unit/app/` | 被測物為 L4 純類別：無 widget、無 `Timer`（逾期只在 `request` 時懶檢查，時間由注入時鐘決定）。與 `router_test.dart` 同層同形 |
| 分層決策樹 | Layer 5 等價（複雜裁決邏輯）→ 單元測試；不需 BDD 整合層 | 仲裁器無 runtime surface（無畫面、無 CLI），依 Phase 2 規則「外圈紅燈豁免」判準豁免外圈 |
| 隔離邊界（Sociable Unit Test） | 只隔離兩個外部世界：`developer.log`（經記錄器替身）與牆上時鐘（經注入時鐘） | 其餘皆為被測行為本身 |
| 載體 | 不存在於測試中。測試依 C1–C6 契約直接呼叫 `presented`／`release`，即「測試扮演載體」 | `0.1.0-W3-205` 未落地；仲裁側行為與載體實作無關 |
| 持有者狀態的建立 | 一律經正常 `request` → 斷言 `Accepted` → `presented` 建立，不開後門、不加測試用 setter | Phase 1 第 9 節「不開後門」；後門會使測試耦合到內部結構 |
| Stream 斷言 | 訂閱在 When 之前完成；斷言前 `await pumpEventQueue()`（`flutter_test` 提供）排空 microtask | 不假設 `StreamController` 為 `sync: true`，實作改變投遞時機時測試不受影響 |
| Handle 比對 | 以 `requestId` 相等判定，不用物件同一性 | 不綁定 `AttentionHandle` 是否覆寫 `==` |

Mock 判斷（Sociable Unit Tests 原則）：

| 依賴 | 策略 | 理由 |
|------|------|------|
| `AttentionArbiterLogSink` | 記錄器替身 | 外部世界；`developer.log` 輸出無法斷言 |
| `DateTime Function() now` | 可控假時鐘 | `test-assertion-design-rules` D3；逾期判定須可重現 |
| `AttentionRequest`／`AttentionHandle`／`AttentionDecision` 子型別／`AttentionConsumed`／`AttentionHolder` | 真實 | 內層值型別 |
| `consumed` Stream 與 `retryAfter`／`signal` 視圖 | 真實，以收集器訂閱 | 被測行為 |
| `attentionArbiterProvider` | 真實 `ProviderContainer`（僅 T-30） | 只驗單例契約，不驗 Riverpod 本身 |
| `Motion.snackBar`／`Motion.snackBarWithAction` | 真實常數，符號引用 | 契約 token；寫死毫秒違反 D1 精神 |

#### 2. 測試基礎設施（測試檔內私有，不進 `lib/`）

**注入點的形態要求**（具體形態由 Phase 3a 定）：每個測試能安裝自己的記錄器與時鐘，且測試間不洩漏。建構子注入（`AttentionArbiterImpl({required DateTime Function() now, required AttentionArbiterLogSink logSink})`）為預設建議；若 Phase 3a 選擇與 `AppSnackBar.logSink` 同形的靜態欄位，測試必須在 `setUp` 安裝、`tearDown` 還原。

**假時鐘**：

    class _FakeClock { DateTime now = DateTime(2026, 1, 1); void advance(Duration d) => now = now.add(d); }
    arbiter = _makeArbiter(now: () => clock.now, logSink: recorder.sink);

**記錄器**：記錄三元組（事件、欄位快照、等級），`Map.of(fields)` 取快照（理由同 `0.1.0-W3-176` Phase 2 第 2 節：實作若重用同一 map，持有參考會使先前記錄被回填而不紅燈）。

    records: List<({AttentionArbiterLogEvent event, Map<String, Object?> fields, int? level})>

**訊號收集器**：`consumedLog = <AttentionConsumed>[]`，於 `setUp` 訂閱 `arbiter.consumed`；`retryAfter`／`signal` 的收集器於取得決策後立即訂閱。

**請求工廠**：`_req(String id, {AttentionLevel level = discardable, AttentionArrival arrival = waiting, AttentionSource source = diagnostics, AttentionLevel? parentLevel, Duration? deadline})`。每個測試內的 id 互異（`'r1'`、`'r2'`、`'gate'`），不重用。

**持有者建立輔助**：`_hold(arbiter, request, {Duration? naturalLifespan})`：`request` → `expect(decision, isA<AttentionAccepted>())` → `presented(handle, naturalLifespan: ...)` → 回傳 handle。輔助內含前置斷言，任何 Given 建立失敗都在 When 之前紅燈。

**查詢輔助**：`recordsOf(event)`、`singleRecordOf(event)`（恰一筆，否則失敗）、`expectSevenFields(record)`（見第 4 節 G-D 的鍵集合定義）、`consumedFor(requestId)`。

**索引使用限制**：`records` 的索引只用於斷言「事件出現順序」（僅 T-9、T-17 各一處），不用於配對事件與請求；配對一律以 `fields['requestId']`。

#### 3. 行為鏈推演與四維度分支

行為鏈（單一請求的完整生命週期）：

    request 進入 → 懶檢查（持有者逾期則 expired 釋放）→ 持有者再請求判定 → 級別夾住（parentLevel）→ 裁決表 2.4
      → Accepted：持有者記錄替換（preempted 者發 consumed(preempted)）→ presented（記 presentedAt／naturalLifespan）→ release（清空、發 consumed）
      → Skipped：終止，持有者不變
      → Rejected／Deferred：請求方訂閱視圖 → 持有者被消費 → 視圖發出一則 → 請求方重驗前提後以新一次 request 進入

三個前置判定的先後順序是可被改壞的實作細節，各有專屬鑑別測試：懶檢查先於裁決（T-17）、再請求判定先於級別比較（T-14 第 2 列）、夾住先於裁決（T-15 第 4 列）。

四個維度的分支窮舉：

| 維度 | 分支 | 承接測試 |
|------|------|---------|
| 正常 | 空通道接受（等待型／自發型）；呈現→消費→訊號；序列重借；高搶低；同級取代；低級別讓步三形態；被延後／被拒者於訊號後重新請求 | T-1、T-2、T-3、T-4、T-9、T-10、T-11、T-12、T-13 |
| 異常 | 持有者再請求；非持有者的 `presented`／`release`；空 id；接受後未呈現 | T-14、T-6、T-16、T-5 |
| 邊界 | `naturalLifespan` 零／負；逾期恰等於存活期；無自然存活期；尚未 `presented`；無 deadline；級別夾住的四種組合；同一 tick 年齡為零；訊號無訂閱者 | T-7、T-18、T-19、T-20、T-8、T-15、T-24、T-31 |
| 中斷 | 被搶佔者的離開（訊號、事件、不再回報）；被搶佔者的訊號視圖不受後續持有者影響；`holder` 讀取不觸發懶檢查 | T-9、T-32、T-21 |

**前置條件驗證強制表**（每個 When 之前必須先斷言 Given 成立）：

| Given 形態 | 前置斷言 |
|-----------|---------|
| 空通道 | `expect(arbiter.holder, isNull)`；`expect(records, isEmpty)`；`expect(consumedLog, isEmpty)` |
| 持有者為 X（經 `_hold` 建立） | `expect(arbiter.holder!.handle.requestId, X.id)`；`expect(arbiter.holder!.presentedAt, clock.now)`（於 `presented` 當下讀）；`records` 恰一筆 `accepted`（若採第 11 節建議新增 `presented` 事件則為兩筆） |
| 持有者已被接受但未 `presented` | `expect(arbiter.holder!.presentedAt, isNull)` |
| 時鐘已前進 Δ | `expect(arbiter.holder!.ageAt(clock.now), Δ)`（T-20 的主斷言，其他測試作前置） |
| 已取得 `Rejected`／`Deferred` 且視圖已訂閱 | `expect(viewLog, isEmpty)` |

無前置斷言的 When 使紅燈語意不明：分不出「裁決錯」與「持有者根本沒建立起來」。

#### 4. 測試規格（Given-When-Then）

級別縮寫：D＝`discardable`、T＝`mustLeaveTrace`、U＝`undroppable`。`Δ` 為假時鐘推進量，一律以符號量（`Motion.snackBar`、`Duration(days: 1)` 等）表達。

##### G-A 空通道與持有者生命週期（BU-1、BU-2、BU-7）

**T-1 空通道接受等待型**（場景 1；A-1、A-15）

    Given: 空通道（前置斷言如上表）
    When:  request(_req('r1', level: D, arrival: waiting, source: diagnostics, deadline: Motion.snackBar))
    Then:  決策 isA<AttentionAccepted>，preempted 為 null，sameLevelReplace 為 false
           handle 的 requestId／level／arrival／source 逐一等於請求值，acceptedAt == clock.now
           arbiter.holder!.handle.requestId == 'r1'，presentedAt 為 null，naturalLifespan 為 null
           records 恰一筆 accepted；其 fields：requestId == 'r1'、arrival == waiting、level == D、
           channel == 'userFocus'、decision == 'accepted'、occupancy == {inFlight: 0, queueDepth: 0, oldestAge: null}、
           deadlineRemaining == Motion.snackBar，且無 deadlineAssigned 鍵（或為 false）
           consumedLog 為空

`occupancy` 的語意定為**裁決前快照**（空通道接受時 `inFlight == 0`；持有者存在時的 skipped／rejected／deferred／preempted 為 `inFlight == 1`）。Phase 1 未明寫快照時點，本設計選裁決前並以斷言顯性化；Phase 3a 若採裁決後，須回頭改本節與 T-24，不得靜默調整斷言。

**T-2 空通道接受自發型**（場景 2）

    Given: 空通道
    When:  request(_req('r1', level: D, arrival: spontaneous))
    Then:  Accepted(preempted: null)；holder 為 r1；accepted 事件 fields['arrival'] == spontaneous、level 為 null 或非 900

**T-3 呈現、消費、訊號**（場景 3；BU-2、BU-6）

    Given: r1 經 _hold(naturalLifespan: Motion.snackBar) 為持有者；holder.presentedAt == clock.now、naturalLifespan == Motion.snackBar
    When:  clock.advance(Motion.feedback)；release(handle, expired)；await pumpEventQueue()
    Then:  holder 為 null
           consumedLog 恰一筆：handle.requestId == 'r1'、reason == expired、at == clock.now（推進後的值）
           records 含恰一筆 released，fields['requestId'] == 'r1'、fields['reason'] == 'expired'
           expectSevenFields 對 released 成立

**T-4 序列重借合法**（場景 4；BU-7 反面）

    Given: r1（列 4 授權對話框，spontaneous／D）經 _hold(naturalLifespan: null) 為持有者，隨後 release(r1, responded)；前置斷言 holder 為 null
    When:  request(_req('r2', level: D, arrival: spontaneous, parentLevel: D))
    Then:  Accepted(preempted: null)；records 無 heldAndRequested；holder 為 r2

**T-5 接受後未呈現**（場景 17；C5）

    Given: r1 已 Accepted、未 presented；前置斷言 holder.presentedAt 為 null
    When:  release(handle, notPresented)；await pumpEventQueue()
    Then:  holder 為 null；consumedLog 恰一筆 reason == notPresented；released 事件 fields['reason'] == 'notPresented'

**T-6 非持有者的回報只留痕不改狀態**（場景 12；錯誤表第 4、5 列）

表格驅動兩列，各為獨立 `test`：

| 列 | When | 期望事件 |
|----|------|---------|
| 1 | `release(<r1 的 handle>, dismissed)`，此時持有者為 r2 | `releasedNonHolder`，fields['requestId'] == 'r1'、fields['op'] == 'release'、level == 900 |
| 2 | `presented(<r1 的 handle>, naturalLifespan: Motion.snackBar)`，此時持有者為 r2 | `releasedNonHolder`，fields['op'] == 'presented'、level == 900 |

    Given: r1 先被 _hold 再 release(responded)（取得一個已離開的 handle）；r2 經 _hold 為持有者；前置斷言 holder 為 r2
    Then（兩列共通）: holder 仍為 r2 且 presentedAt／naturalLifespan 不變；consumedLog 在 When 前後筆數相同（不發出）

列 1 的 handle 來自「曾是持有者、已釋放」而非憑空建構，理由：憑空建構的 handle 若因 `acceptedAt` 等欄位無法構造而需要開放建構子，會為測試增加公開面。

**T-7 `naturalLifespan` 零或負值視同 null**（場景 16）

表格驅動兩列：`Duration.zero`、`Duration(seconds: -1)`。

    Given: r1 已 Accepted（D／waiting）
    When:  presented(handle, naturalLifespan: <該列值>)；clock.advance(Duration(days: 1))；request(_req('r2', level: D, arrival: spontaneous))
    Then:  holder.naturalLifespan 為 null（presented 當下讀）
           留痕記錄存在且 fields 含原值（鍵名建議 naturalLifespanRaw，值為該列 Duration；承載事件見第 11 節缺口 3）
           records 無 expired；r2 的決策為 Accepted(preempted: r1, sameLevelReplace: true)（證明持有者仍在，未被逾期清除）

**T-8 deadline 的兩種形態**（場景 14；A-15）

| 列 | 請求的 deadline | 期望 accepted 事件的 fields |
|----|----------------|---------------------------|
| 1 | `null` | `deadlineRemaining` 鍵存在且值為 null；`deadlineAssigned == true` |
| 2 | `Motion.snackBarWithAction` | `deadlineRemaining == Motion.snackBarWithAction`；無 `deadlineAssigned` 鍵或其值為 false |

列 2 是列 1 的鑑別對照：若實作對所有請求都附 `deadlineAssigned: true`，列 2 紅燈。

##### G-B 裁決表 2.4（BU-3、BU-4、BU-5、BU-7）

**T-9 高級別搶佔低級別**（場景 5；讓步規則 1）

表格驅動三列（持有者級別 < 請求級別的全部組合），各為獨立 `test`：

| 列 | 持有者（載體／級別／類別） | 請求（級別／類別／來源） |
|----|-------------------------|----------------------|
| 1 | SnackBar／D／waiting（列 6） | U／spontaneous／schema（列 11） |
| 2 | SnackBar／D／waiting | T／spontaneous／diagnostics |
| 3 | 測試替身／T／spontaneous | U／waiting／workspace |

    Given: 持有者經 _hold(naturalLifespan: Motion.snackBar) 建立，id 'low'；訂閱 consumed
    When:  request(_req('high', ...))；await pumpEventQueue()
    Then:  Accepted，preempted!.requestId == 'low'，sameLevelReplace == false，handle.requestId == 'high'
           holder 為 'high'，presentedAt 為 null（新持有者尚未呈現）
           records 含 preempted（fields['requestId'] == 'low'、fields['reason'] == 'preempted'、occupancy.inFlight == 1）
           與 accepted（fields['requestId'] == 'high'）；preempted 的索引小於 accepted 的索引（舊者離開先於新者進入，唯一一處順序斷言）
           records 無 sameLevelReplaced
           consumedLog 恰一筆：requestId == 'low'、reason == preempted

列 3 的持有者為自發型、請求為等待型，與列 1 相反，證明搶佔只看級別不看到達類別。

**T-10 同級別取代（`0.1.0-W3-239` 具名例外）**（場景 6；讓步規則 2 的專案形態）

表格驅動三列：D／D（waiting → spontaneous，即場景 6）、T／T（spontaneous → waiting）、U／U（spontaneous → spontaneous）。

    Given: 持有者 'old' 經 _hold 建立；訂閱 consumed
    When:  request(_req('new', level: <同級>, ...))；await pumpEventQueue()
    Then:  Accepted，preempted!.requestId == 'old'，sameLevelReplace == true
           holder 為 'new'
           records 含 sameLevelReplaced（fields['requestId'] == 'old'）與 accepted（'new'）；無 preempted 事件
           consumedLog 恰一筆：requestId == 'old'、reason == preempted

`sameLevelReplaced` 與 `preempted` 互斥出現，是「同級別取代」與「跨級別搶佔」在觀測層可區分的證據；載體依此決定留痕形態（SPEC-003 §2.13〈截斷事件的日誌等級〉）。

**T-11 低級別自發型被跳過**（場景 7；卸載順序表自發型／可棄格；讓步規則 3）

表格驅動兩列：持有者 U（關卡，`naturalLifespan: null`）、持有者 T（替身，`naturalLifespan: null`）；請求皆為 D／spontaneous／diagnostics。

    Given: 持有者 'gate' 經 _hold 建立；訂閱 consumed
    When:  request(_req('bg', level: D, arrival: spontaneous))；await pumpEventQueue()
    Then:  決策 isA<AttentionSkipped>，blockedBy.requestId == 'gate'
           holder 仍為 'gate'（requestId 與 presentedAt 皆不變）
           records 恰一筆 skipped：fields['requestId'] == 'bg'、level == 900、occupancy == {inFlight: 1, queueDepth: 0, oldestAge: Duration.zero}
           consumedLog 為空

**T-12 自發型須留痕被延後、訊號後重新請求**（場景 9；卸載順序表自發型／須留痕格；BU-6）

    Given: 持有者 'gate'（U／spontaneous，naturalLifespan: null）；訂閱 consumed
    When:  d1 = request(_req('trace', level: T, arrival: spontaneous))
           前置斷言 d1 isA<AttentionDeferred>、blockedBy.requestId == 'gate'；訂閱 d1.signal 至 signalLog；前置斷言 signalLog 為空
           release(gateHandle, responded)；await pumpEventQueue()
           前置斷言 holder 為 null（仲裁器未保存 'trace'）
           d2 = request(_req('trace-2', level: T, arrival: spontaneous))
    Then:  signalLog 恰一筆：handle.requestId == 'gate'、reason == responded
           d2 isA<AttentionAccepted>，preempted 為 null
           records 含恰一筆 deferred（fields['requestId'] == 'trace'、level == 900）
           records 無任何 fields['requestId'] == 'trace' 的 accepted（第一次請求從未進入通道）

「釋放後、再請求前 `holder == null`」是硬條件 (1) 的行為證據：若實作偷偷保存被延後者並在釋放時自動接受，此斷言紅燈。

**T-13 等待型被拒絕並可重試**（場景 8；卸載順序表等待型／可棄、等待型／須留痕格）

表格驅動三列：

| 列 | 持有者 | 請求 | 附加期望 |
|----|-------|------|---------|
| 1 | U（關卡） | D／waiting／workspace | — |
| 2 | T（替身） | D／waiting／diagnostics | — |
| 3 | U（關卡） | T／waiting／workspace | rejected 事件存在（須留痕格「＋留痕」；本專案三級別皆逐筆記錄，此列鎖住的是它不因級別為 T 而被省略） |

    Given: 持有者 'gate' 經 _hold 建立；訂閱 consumed
    When:  d = request(_req('user', ...))
           前置斷言 d isA<AttentionRejected>、blockedBy.requestId == 'gate'；訂閱 d.retryAfter 至 retryLog；前置斷言 retryLog 為空
           release(gateHandle, responded)；await pumpEventQueue()
    Then:  holder 為 null
           retryLog 恰一筆：handle.requestId == 'gate'、reason == responded
           records 恰一筆 rejected：fields['requestId'] == 'user'、fields['decision'] == 'rejected'；level 為 null 或 800（使用者發起，非 900）
           在 release 之前 holder 仍為 'gate'（拒絕不改狀態）

**T-14 持有者再請求被拒（禁止持有並等待）**（場景 10；BU-7）

表格驅動兩列：

| 列 | 持有者 | 再請求 | 鎖住的性質 |
|----|-------|-------|-----------|
| 1 | 'r1'／D／waiting | `_req('r1', level: D, arrival: waiting)` | 場景 10 原型 |
| 2 | 'r1'／D／waiting | `_req('r1', level: U, arrival: spontaneous)` | 同 id 判定先於級別比較——若順序顛倒，U > D 會走搶佔路徑得到 Accepted |

    Given: 'r1' 經 _hold 為持有者；訂閱 consumed
    When:  request(<該列再請求>)；await pumpEventQueue()
    Then:  決策 isA<AttentionRejected>，blockedBy.requestId == 'r1'
           holder 仍為 'r1'，presentedAt 不變
           records 恰一筆 heldAndRequested：fields['requestId'] == 'r1'、level == 900
           records 無 preempted、無 sameLevelReplaced、無 accepted（除建立持有者那一筆）
           consumedLog 為空

**T-15 級別傳播單調不升**（場景 13；方法論〈級別〉；測試義務第 7 項）

表格驅動四列，各為獨立 `test`：

| 列 | 持有者 | 請求 level／parentLevel | 期望決策 | 期望 handle.level | 期望 levelClamped 事件 |
|----|-------|------------------------|---------|------------------|----------------------|
| 1 | 空 | U／D | Accepted | D | 有，level == 900，fields 含 requestedLevel == U 與 parentLevel == D |
| 2 | 空 | D／D | Accepted | D | 無 |
| 3 | 空 | D／U | Accepted | D | 無（降不夾） |
| 4 | T（替身，spontaneous） | U／D（spontaneous） | **Skipped** | — | 有 |

列 4 證明夾住發生在裁決之前：未夾住時 U > T 會得到 Accepted(preempted)；夾成 D 後 D < T 且自發型，落跳過格。列 2、3 為列 1 的鑑別對照（Q9：實作若把 `parentLevel` 誤讀為 `level`，列 3 的 handle.level 會是 U 而紅燈）。

**T-16 空 id 被拒**（錯誤表第 1 列）

    Given: 空通道
    When:  request(_req(''))
    Then:  決策 isA<AttentionRejected>；records 恰一筆 rejected 且 fields['reason'] == 'emptyId'、level == 900；holder 仍為 null

本測試依第 11 節缺口 1 的建議（單一行為：一律 `Rejected` 留痕，不設開發期 `assert`）設計。`flutter test` 恆啟用 assert，Phase 1 的雙行為在測試環境只能觀測到 `AssertionError`；若 PM 裁定維持 assert，本測試改為 `expect(() => request(...), throwsA(isA<AssertionError>()))`，生產期 `Rejected` 路徑記為不可測並在 Test Results 明記。

##### G-C 逾期懶檢查（BU-8）

**T-17 逾期懶檢查**（場景 11）

    Given: 'old'（D／waiting）經 _hold(naturalLifespan: Motion.snackBar) 為持有者；訂閱 consumed
           clock.advance(Motion.snackBar + Motion.feedback)（嚴格大於存活期，推進量為符號量）
           前置斷言 holder 仍為 'old'（讀取不觸發懶檢查，見 T-21）
    When:  request(_req('new', level: D, arrival: waiting))；await pumpEventQueue()
    Then:  records 含 expired（fields['requestId'] == 'old'、fields['reason'] == 'expired'）
           決策為 Accepted 且 preempted 為 null、sameLevelReplace 為 false（以空通道處理，非同級取代）
           records 無 sameLevelReplaced；expired 的索引小於 'new' 的 accepted 的索引
           consumedLog 恰一筆：requestId == 'old'、reason == expired
           holder 為 'new'

`preempted == null` 是本測試的核心鑑別：實作若漏做懶檢查，同級別 D 會走取代路徑得到 `preempted == old`，測試紅燈。

**T-18 恰等於存活期不逾期**（邊界：`>` 非 `>=`）

    Given: 同 T-17，但 clock.advance(Motion.snackBar)（恰等於）
    When:  request(_req('new', level: D, arrival: waiting))
    Then:  records 無 expired；決策為 Accepted(preempted: 'old', sameLevelReplace: true)

**T-19 無自然存活期不逾期**（場景 15）

    Given: 'gate'（U／spontaneous）經 _hold(naturalLifespan: null)；clock.advance(Duration(days: 1))
    When:  request(_req('bg', level: D, arrival: spontaneous))
    Then:  records 無 expired；決策 isA<AttentionSkipped>；holder 仍為 'gate'

**T-20 尚未 `presented` 不觸發懶檢查，且年齡自接受起算**（Phase 1 §11 語義推演 3a）

    Given: 'r1' 已 Accepted、未 presented；clock.advance(Motion.snackBarWithAction)
    Then（前置兼主斷言）: holder!.ageAt(clock.now) == Motion.snackBarWithAction；holder!.presentedAt 為 null
    When:  request(_req('r2', level: D, arrival: spontaneous))
    Then:  records 無 expired；決策為 Accepted(preempted: 'r1', sameLevelReplace: true)
           該 accepted／sameLevelReplaced 事件的 occupancy.oldestAge == Motion.snackBarWithAction

**T-21 `holder` 讀取不觸發懶檢查**

    Given: 'old' 經 _hold(naturalLifespan: Motion.snackBar)；clock.advance(Motion.snackBar + Motion.feedback)
    When:  連續讀取 arbiter.holder 兩次
    Then:  兩次皆非 null 且 requestId == 'old'；records 無 expired；consumedLog 為空
           隨後 request(_req('new')) 才產生 expired（與 T-17 銜接）

##### G-D 可觀測事件（BU-9）

**七欄位鍵集合定義**（`expectSevenFields`）：`fields.keys` 必須包含 `requestId`、`arrival`、`level`、`channel`、`occupancy`、`deadlineRemaining` 六鍵，且包含 `decision` 或 `reason` 至少一鍵；`occupancy` 為 Map 且鍵恰為 `inFlight`、`queueDepth`、`oldestAge`；`channel == 'userFocus'`；`queueDepth == 0`。存在性檢查（Q_new3）之外，各測試另有值斷言。

**T-22 每種事件、每種決策、每種離開原因各至少一則**（A-13；測試義務第 1 項）

單一腳本測試，依序執行下列步驟後對 `records` 做集合斷言（各步驟間 `await pumpEventQueue()`）：

| 步 | 動作 | 產生 |
|----|------|------|
| 1 | request 'r1'（D／waiting） | accepted |
| 2 | request 'r1' 再次 | heldAndRequested |
| 3 | presented('r1', Motion.snackBar)；release(<'r1' 之前另建立並釋放的 'r0' handle>, dismissed) | releasedNonHolder |
| 4 | request 'r2'（U／spontaneous，parentLevel: D） | levelClamped、sameLevelReplaced、accepted |
| 5 | request 'r3'（U／spontaneous） | preempted、accepted |
| 6 | request 'r4'（D／spontaneous） | skipped |
| 7 | request 'r5'（T／spontaneous） | deferred |
| 8 | request 'r6'（D／waiting） | rejected |
| 9 | release('r3', responded) | released（responded） |
| 10 | request 'r7'（D／waiting）；presented('r7', Motion.snackBar)；advance(Motion.snackBar + Motion.feedback)；request 'r8'（D／waiting） | expired、accepted |
| 11 | release('r8', dismissed) | released（dismissed） |
| 12 | request 'r9'；release('r9', notPresented) | released（notPresented） |
| 13 | request 'r10'；presented('r10', Motion.snackBar)；release('r10', withdrawn) | released（withdrawn） |

    Then:  AttentionArbiterLogEvent.values 每一值在 records 中至少出現一次
           AttentionReleaseReason.values 每一值至少出現於一筆 released／expired／preempted／sameLevelReplaced 事件的 fields['reason']
           四種 AttentionDecision 子型別各至少對應一筆 accepted／skipped／rejected／deferred 事件
           records 每一筆皆通過 expectSevenFields

步 3 的 `r0` handle 取得方式同 T-6。

**T-23 日誌等級依發起者，契約違反類恆為 warning**

表格驅動，各列獨立 `test`：

| 列 | 事件 | 情境 | 期望 level |
|----|------|------|-----------|
| 1 | skipped | 請求 spontaneous | 900 |
| 2 | deferred | 請求 spontaneous | 900 |
| 3 | rejected | 請求 waiting | null 或 800 |
| 4 | expired | 逾期持有者為 spontaneous | 900 |
| 5 | expired | 逾期持有者為 waiting | null 或 800 |
| 6 | heldAndRequested | 持有者為 waiting（鑑別「恆 900」與「依 arrival」） | 900 |
| 7 | levelClamped | 請求 waiting | 900 |
| 8 | releasedNonHolder | 對應 handle 為 waiting | 900 |

列 6～8 刻意取 `waiting`：若實作把契約違反類也套用發起者規則，會得到 info 而紅燈。

**T-24 occupancy 三項分列與年齡**

| 列 | 情境 | 期望 occupancy |
|----|------|---------------|
| 1 | 空通道接受 | `{inFlight: 0, queueDepth: 0, oldestAge: null}` |
| 2 | 持有者存在、同一 tick 內請求（未推進時鐘）被跳過 | `{inFlight: 1, queueDepth: 0, oldestAge: Duration.zero}` |
| 3 | 持有者存在、推進 Δ = Motion.snackBarWithAction 後被拒絕（持有者無存活期） | `{inFlight: 1, queueDepth: 0, oldestAge: Δ}` |

`oldestAge` 為 `Duration` 型別而非毫秒整數（型別斷言）；三項為獨立鍵，不得合併為單一數字。

**T-25 channel 固定值**：T-1 的斷言已涵蓋，另於 T-22 對全部 records 斷言 `fields['channel'] == 'userFocus'`。

##### G-E 結構、單例與訊號語意（BU-10 + acceptance 靜態項）

**T-26 推送型不可表達**（場景 18）：`expect(AttentionArrival.values, [waiting, spontaneous])`（逐項相等，鎖名稱與順序）。

**T-27 級別列舉順序即優先序**：`expect(AttentionLevel.values, [discardable, mustLeaveTrace, undroppable])`；`expect(undroppable.index > mustLeaveTrace.index && mustLeaveTrace.index > discardable.index, isTrue)`。

**T-28 來源列舉恰三值**：`expect(AttentionSource.values, [workspace, schema, diagnostics])`。

**T-29 事件列舉**：逐項等於 Phase 1 2.5 列出的 11 值（若採第 11 節缺口 3 新增 `presented`，為 12 值；以 Phase 3a 定案為準，本測試鎖住定案後的集合）。

**T-30 provider 單例**：`ProviderContainer` 讀 `attentionArbiterProvider` 兩次，`identical` 為 true 且 `isA<AttentionArbiter>`；`container.dispose()` 於 `addTearDown`。

**T-31 `consumed` 無訂閱者不緩衝、不重播**

    Given: 空通道、尚未訂閱 consumed
    When:  _hold('r1')；release('r1', responded)；await pumpEventQueue()；此時才訂閱 consumed 至 lateLog；await pumpEventQueue()
    Then:  lateLog 為空
    When:  _hold('r2')；release('r2', responded)；await pumpEventQueue()
    Then:  lateLog 恰一筆且 requestId == 'r2'

**T-32 `retryAfter`／`signal` 為 blockedBy 那一筆的過濾視圖**

    Given: 'g1'（U／spontaneous）為持有者；d = request(_req('user', level: D, arrival: waiting)) 得 Rejected；訂閱 d.retryAfter 至 viewLog；同時訂閱 consumed 至 consumedLog
    When:  request(_req('g2', level: U, arrival: spontaneous))（同級取代 g1）；await pumpEventQueue()
           release('g2' handle, responded)；await pumpEventQueue()
    Then:  consumedLog 兩筆（g1／preempted、g2／responded）
           viewLog 恰一筆：handle.requestId == 'g1'、reason == preempted
           （g2 的離開不出現於視圖）

同形的 `Deferred.signal` 以 T-12 的正向路徑覆蓋，過濾性由本測試代表兩者（Phase 1 2.3 表明寫兩視圖為同一 stream 的同一過濾）。

#### 5. 非測試驗證項（靜態）

| 編號 | 驗收項 | 驗證方式 | 承擔階段 |
|------|-------|---------|---------|
| V-1 | 仲裁器不 import 任何 domain 型別（`GapReportState`、`WorkspaceState`、schema 型別） | 讀 `lib/app/attention_arbiter.dart` 的 import 區塊；`grep -n "^import" lib/app/attention_arbiter.dart` 列出供逐一核對 | Phase 3b 自檢 + Phase 4 |
| V-2 | 仲裁器不 import 任何載體（`AppSnackBar`、`ScanNotifier`、`BlockedState`）與 `package:flutter/material.dart` | 同上 | 同上 |
| V-3 | 無集合型欄位持有請求：類別欄位中無 `List`／`Queue`／`Map`／`Set` 型別以 `AttentionRequest`／`AttentionHandle` 為元素；狀態恰為 `AttentionHolder?` 一筆 | 人工讀類別欄位宣告 | 同上 |
| V-4 | 級別比較只在一處函式、比較鍵為 `AttentionLevel.index`，無以 `AttentionSource`、載體或呼叫路徑為鍵的分支 | `grep -n "\.index\|AttentionSource\." lib/app/attention_arbiter.dart` 列出後逐一核對；`AttentionSource` 只應出現於事件 fields 的投影 | 同上 |
| V-5 | 無回傳 `Future` 的方法；類別內無 `await` | `grep -n "Future\|await" lib/app/attention_arbiter.dart` 預期只命中 doc comment 或零命中 | 同上 |
| V-6 | library doc 明寫「本物件是通道之外的獨立物件，為新增的可用性單點」並列出 Phase 1 第 8 節四項失效模式與防線 | 人工讀 library doc | 同上 |
| V-7 | 檔案位於 `lib/app/`，測試位於 `test/unit/app/` | `where.files` 即為此；`ticket track commit` 寫入子集強制 | Phase 3b |
| V-8 | `AttentionRequest` 無載體欄位（variant／message／context／widget）與時機欄位（delay／showAt／schedule） | 人工讀 2.2 型別定義的實作 | 同上 |
| V-9 | `AttentionDecision` 恰四個子型別且 sealed | `switch` 窮舉編譯期檢查：測試檔內一個對 decision 的 `switch` 不含 `default` 分支，缺子型別或多子型別皆編譯失敗 | Phase 3b（編譯即驗） |

**不在本票的驗證**：Phase 1 第 8 節「關卡狀態機每一條退出路徑皆有 `release`」——關卡狀態機尚未實作（三個阻擋狀態皆未落地，Phase 1 前置實查表），該靜態檢查落在承接關卡載體的票，本票 Phase 3b 不承擔。

#### 6. 七項測試義務逐項承接（`how.strategy` 要求，空成立者附依據）

| # | 判準 | SPEC-003 §2.17 裁決 | 本票承接 |
|---|------|-------------------|---------|
| 1 | 仲裁決策事件 | 適用 | T-22（每事件七欄位）、T-1／T-24（值）、T-8（deadline 兩形態） |
| 2 | 三條讓步規則 | 規則 1／3 空成立；規則 2 不適用（具名例外） | 仲裁器為通用機制，以注入持有者狀態測其裁決：規則 1 → T-9（三列）；規則 3 → T-11、T-13、T-15 列 4；規則 2 的專案形態（截斷並留痕）→ T-10（三列，`sameLevelReplaced` 逐筆）。§2.15 證明 0.1 的 13 列事件域內不會出現這些輸入，不改變仲裁器須正確處理它們的義務；空成立依據以標題文字引用 §2.15〈跨級別：規則 1／3 對列 1–10 與列 11–13 的驗收〉，本節不重述 |
| 3 | 卸載順序九格 | 3 格適用、6 格空成立 | 自發型／可棄 → T-11；等待型／可棄 → T-13 列 1、2；自發型／不可棄 → 無更高持有者，格內動作為「接受」，由 T-9 列 1、3 與 T-1 承接（2.4 表標為不可達的是「被更高級別卸載」那一支，非該格本身）。另兩個 T 格（自發型／須留痕 → T-12；等待型／須留痕 → T-13 列 3）雖為 0.1 空集合，`mustLeaveTrace` 為合法列舉值，仲裁器行為必須定義，故仍設測試。推送型三格：型別層不可達，T-26 承接，不設行為測試 |
| 4 | 進入與離開卸載的閾值滯後 | 不適用 | 不設測試。依據以標題文字引用 §2.16〈佔用量觸發型暫停（滯後）在注意力通道的形態〉 |
| 5 | 仲裁器自身溢出 | §2.17 記「適用」 | Phase 1 第 9 節裁定：本專案無佇列，溢出分支不存在，以 V-3（無集合型欄位）靜態承接；T-14 覆蓋「等待型申請收到明確拒絕而非靜默丟棄」在本專案唯一可達的變體（持有者再請求）。§2.17 與 Phase 1 的措辭差異登記於第 11 節，交 PM |
| 6 | 可棄推送型丟最舊 | 空成立 | 不設測試；T-26 鎖住「推送型不存在」使空成立可被靜態驗證 |
| 7 | 級別傳播單調不升 | 適用 | T-15（四列，含夾住先於裁決） |

#### 7. Decision Questions（Q7–Q14）

| 問題 | 回答 |
|------|------|
| Q7 中間步驟 | `request` 內部有四個中間步驟（懶檢查、再請求判定、夾住、裁決表），各有專屬測試且三個先後順序各有鑑別列（第 3 節） |
| Q8 私有方法 | 級別比較若抽為私有純函式，由 T-9～T-15 的矩陣經公開入口間接覆蓋；V-4 補靜態 |
| Q9 碰巧通過 | 每測試內 id 互異；T-15 列 3 鑑別 `level` 與 `parentLevel` 誤讀；T-9 列 3 鑑別「只看級別」；T-17 以 `preempted == null` 鑑別逾期與取代；T-23 列 6～8 鑑別「恆 900」與「依 arrival」 |
| Q10／Q_new4 異常路徑對照 | 見下表 |
| Q_new3 值 vs 存在性 | `expectSevenFields` 為存在性；T-1、T-8、T-11、T-24 補值斷言；handle 的四欄位逐一值比對（T-1） |
| Q11 資料純淨性 | 無 schema 遷移；工廠只含 Phase 1 2.2 的六個欄位，無殘留 |
| Q12 防護目標 | 每條測試的「鎖住的性質」已寫於各測試說明；綠燈後補的防護型測試（T-14 列 2、T-15 列 4、T-17、T-21、T-23 列 6～8）Phase 3b 須各做一次故意破壞驗紅 |
| Q13 flaky | 時間全走假時鐘；stream 全走 `pumpEventQueue`；記錄順序只在 T-9、T-17 各斷言一處且該順序為規格明寫；若記錄器為靜態欄位須 `tearDown` 還原；`ProviderContainer` 於 `addTearDown` dispose |
| Q14 資料代表性 | 輸入為列舉組合，可窮舉；矩陣測試已窮舉 H<R 三組、H==R 三組、H>R 的可達五格 |

**異常路徑對照（Q_new4）**：

| Phase 1 §4 錯誤路徑 | 對應測試 | 覆蓋 |
|--------------------|---------|------|
| `request.id` 為空 | T-16 | 已覆蓋（形態待第 11 節缺口 1 裁定） |
| 持有者再請求 | T-14 | 已覆蓋 |
| `level > parentLevel` | T-15 | 已覆蓋 |
| `presented` 非當前持有者 | T-6 列 2 | 已覆蓋 |
| `release` 非當前持有者 | T-6 列 1 | 已覆蓋 |
| 載體從未回報 `release`（有存活期） | T-17 | 已覆蓋 |
| 載體從未回報 `release`（無存活期，關卡） | T-19（證明不自癒、持有者留存） | 已覆蓋 |
| `consumed` 無訂閱者 | T-31 | 已覆蓋 |
| `Deferred` 後未重新請求 | 不屬仲裁器責任；T-12 已證仲裁器不保存 | 不設測試（無仲裁器行為可驗） |
| 同一 tick 兩次 `request` | T-24 列 2 | 已覆蓋 |

#### 8. Acceptance 對照表（Phase 1 第 6 節展開項）

| 驗收項 | 承接 | 形態 |
|-------|------|------|
| 票面 1-a `AttentionRequest` 無載體欄位 | V-8 | 靜態 |
| 票面 1-b 無時機欄位 | V-8 | 靜態 |
| 票面 1-c 四子型別涵蓋 2.4 全部可達格 | V-9 + T-9～T-15 | 編譯期 + 行為 |
| 票面 2-a 場景 5～9 逐格一致 | T-9、T-10、T-11、T-13、T-12 | 行為 |
| 票面 2-b 級別比較一處、鍵為 index | V-4 + T-9 列 3 | 靜態 + 行為 |
| 票面 2-c 級別列舉對應主名、無第二套 | T-27 + V-4 | 結構 + 靜態 |
| 票面 3-a 檔案位於 `lib/app/` | V-7 | 路徑 |
| 票面 3-b 不 import domain 型別 | V-1 + T-28 | 靜態 + 結構 |
| 票面 3-c 不呼叫載體 | V-2 | 靜態 |
| 票面 4-a `AttentionArrival` 恰兩值 | T-26 | 結構 |
| 票面 4-b 無集合型欄位 | V-3 | 靜態 |
| 票面 4-c Solution 第 7 節存在 | 已存在 | 文件 |
| 禁止持有並等待 | T-14（兩列）+ V-5 | 行為 + 靜態 |
| 可用性單點明寫 | V-6 | 靜態 |
| 每種決策／離開原因至少一則事件、七欄位 | T-22 | 行為 |
| 日誌等級依發起者 | T-23 | 行為 |

十六項全數有承接，無懸空項。

#### 9. 拆分友善性（Phase 3b 可拆分派發）

| 群組 | 功能職責 | 測試 | 跨群組依賴 |
|------|---------|------|-----------|
| G-A 生命週期 | 接受、呈現、釋放、訊號 | T-1～T-8 | 只共用第 2 節基礎設施（各測試於 `setUp` 自建 arbiter、時鐘、記錄器，無 mutable 狀態跨測試） |
| G-B 裁決表 | 2.4 表的九個可達格與兩個前置判定 | T-9～T-16 | 同上；依賴 G-A 的 `_hold` 輔助（純函式，無狀態） |
| G-C 懶檢查 | 逾期判定與時鐘 | T-17～T-21 | 同上 |
| G-D 可觀測 | 事件、欄位、等級 | T-22～T-25 | 同上；T-22 腳本橫跨全部行為，宜最後實作 |
| G-E 結構 | 列舉、provider、stream 語意 | T-26～T-32 | 無 |

五群組皆改同一測試檔與同一實作檔，**若並行派發須注意同檔衝突**；建議序列執行，順序 G-E（結構先紅，驅動型別骨架）→ G-A → G-B → G-C → G-D。

#### 10. 測試手段限制（D1／D3 合規）

| 限制 | 內容 |
|------|------|
| 假時鐘 | 全部時間推進走 `_FakeClock.advance`；不得出現 `Stopwatch`、`DateTime.now()`、`Future.delayed`、未固定 seed 的 `Random` |
| 符號時間量 | 推進量一律由 `Motion.snackBar`／`Motion.snackBarWithAction`／`Motion.feedback`／`Duration(days: 1)` 組成，不寫字面毫秒；「嚴格大於」以 `+ Motion.feedback` 表達 |
| 無計時斷言 | 無任何以耗時為對象的 `lessThan`／`greaterThan`；`ageAt` 與 `oldestAge` 的斷言為假時鐘推進量的相等比較，不是計時 |
| 不比對散文 | 斷言依列舉與結構化 fields，不比對 `developer.log` 訊息字面 |
| 不新增讀取用公開 API | 內部狀態一律經 `holder` 快照與 `logSink` fields 觀測；不得為了測試在 `lib/` 增設 getter 或重置方法 |
| 不開後門建立持有者 | 一律經 `request` + `presented` |

#### 11. 回報 PM 的規格分歧與缺口（測試已採中立形態，不代為裁決）

| 編號 | 內容 | 測試的處置 | 建議 |
|------|------|-----------|------|
| 分歧 A | SPEC-003 §2.17「七項欄位」表定請求識別為「仲裁點受理當下產生的 process 內單調遞增整數」；Phase 1 2.2 定為請求方提供的 `String id`（與 176 配對識別同源），仲裁器不生成 | 依 Phase 1（§2.17 已明寫關聯機制屬 207 Phase 1 範疇） | 登記 spawn request：DOC 對齊 §2.17 欄位表措辭 |
| 分歧 B | §2.17 決策欄五值（立即呈現／升級呈現／延後呈現／狀態承載不另發／不顯示且留痕）並明寫「拒絕」本專案不使用；Phase 1 決策集為 Accepted／Skipped／Rejected／Deferred，直接取自方法論卸載順序表 | 依 Phase 1 | 併入同一 spawn request：§2.17 決策欄與 Phase 1 決策集的對應關係需明寫（例如 §2.17 五值屬請求方 T 判準的結局，Phase 1 四值屬仲裁層動作，兩者為不同觀測點），或修訂其一 |
| 缺口 1 | 空 id：Phase 1 §4 定為「開發期 `assert`；生產期 `Rejected`」雙行為。`flutter test` 恆啟用 assert，生產期路徑在測試中不可達；且 `AttentionRejected.blockedBy` 為非空型別，空通道下無 handle 可填 | T-16 依「單一行為：一律 `Rejected` + `rejected(reason: emptyId)`」設計，並不斷言 `blockedBy` | 建議去除 assert 改單一行為（可觀測性規則 4：兩種行為使 debug 與 release 表現不同）；`blockedBy` 改為可空或另立 `AttentionInvalid` 子型別——後者會使 V-9 的四子型別變五，需 PM 裁定 |
| 缺口 2 | `occupancy` 快照時點（裁決前／後）Phase 1 未明寫 | 本設計定為裁決前（T-1、T-11、T-24 斷言顯性化） | Phase 3a 採納或改本節，不得靜默調整斷言 |
| 缺口 3 | 場景 16 要求「`presented` 事件 fields 附原值」，但 2.5 事件列舉無 `presented` 值 | T-7 斷言「存在一筆留痕含原值」，不指定事件名；T-29 依 Phase 3a 定案的集合 | 建議新增 `presented` 事件（同時承接 C2 回報的可觀測性，使「已接受但從未呈現」在日誌上可分辨），列舉為 12 值 |
| 流程 | `ticket track claim --as sage-test-architect` 回「已被接手」，`who.current` 仍為 lavender-interface-designer；`assignee` 與 `current_phase` 已為 Phase 2／sage | 未用 `set-who` 繞過 | PM 確認是否需修正 `who.current` |

#### 12. 移交 Phase 3a（pepper-test-implementer）

| 項目 | 狀態 |
|------|------|
| 三十二個測試規格（T-1～T-32），每個含前置條件斷言與鎖住的性質 | 第 4 節 |
| 四維度分支窮舉與行為鏈 | 第 3 節 |
| 測試基礎設施（假時鐘、記錄器、收集器、工廠、`_hold`、查詢輔助、索引限制） | 第 2 節 |
| Mock 策略與隔離邊界 | 第 1 節 |
| 靜態驗證項 V-1～V-9 | 第 5 節 |
| 七項測試義務逐項承接 | 第 6 節 |
| Decision Questions 與異常路徑對照 | 第 7 節 |
| Acceptance 十六項對照，無懸空項 | 第 8 節 |
| 拆分友善性與建議實作順序 | 第 9 節 |
| D1／D3 合規限制 | 第 10 節 |
| 待 PM 裁定的三項缺口與兩項分歧（測試已採中立形態） | 第 11 節；缺口 1、3 影響 T-16、T-7、T-29 的最終形態，Phase 3a 須先讀 PM 裁決 |
### PM 裁決：Phase 2 第 11 節的兩項分歧與三項缺口（2026-09-10）

**分歧的性質先講清楚**：`0.1.0-W3-207` Phase 1 於 00:14:25 提交，`0.1.0-W3-243` 寫入 SPEC-003 §2.17 於 00:16:19，相差不到兩分鐘。**兩者是平行發明，不是副本漂移**——雙方都沒讀到對方。修法因此不是同步兩套（那會產出兩邊都不像的第三套），而是選單一載體並寫出對應關係。

| 項 | 裁決 | 理由 |
|----|------|------|
| **分歧 A：請求識別的產生方** | **依 Phase 1**：請求方提供 `String id`，仲裁器不生成。SPEC-003 §2.17 欄位表的「仲裁點受理當下產生的 process 內單調遞增整數」須改 | 被仲裁器拒絕或跳過的請求從未抵達載體，它的可追溯性只能靠請求方自己持有的識別。若識別由仲裁器生成，請求方必須捕捉回傳的決策才知道自己那一筆的下場，而「決策回傳被忽略」正是無痕的來源之一。請求方提供則它能直接把自己的 domain 日誌與仲裁結果對起來，不需往返 |
| **分歧 B：決策值域** | **兩者皆保留，但須明寫是不同觀測點**：§2.17 的五值是**請求方視角的結局**（我這則訊息最後怎麼了），Phase 1 的四值是**仲裁層的動作**。§2.17「拒絕本專案不使用」的範圍限定為格式正確的請求——Phase 1 的 `Rejected` 只用於誤用路徑（持有者再請求、空 id），不出現在正常訊息流，兩者不衝突 | 五值與四值不是同一個集合的兩種切法，是兩個層次。強制其一去對齊另一個會讓其中一層失去它自己要回答的問題。§2.17 須補一段對應表 |
| **缺口 1：空 id 的雙行為** | **採納建議的單一行為**：一律 `Rejected` 加留痕，去除開發期 `assert`。`blockedBy` 改為可空，**不新增第五個子型別** | 兩種行為使 debug 與 release 表現不同，違反可觀測性規則 4；且測試環境恆啟用 assert，生產路徑不可測——不可測的行為不是可規格化的行為。不加子型別的理由是它會使決策集從四個變五個而動到 V-9 與分歧 B 的對應表，代價大於把一個欄位改成可空 |
| **缺口 2：`occupancy` 快照時點** | **採納：裁決前**，並維持以斷言顯性化 | 快照要回答的是「這個決策是基於什麼狀態做的」，裁決後的狀態已含本次決策的效果，回答不了那個問題 |
| **缺口 3：新增 `presented` 事件** | **採納，列舉為 12 值** | 沒有它，「已接受但從未呈現」與「已接受且已呈現」在日誌上同形——那正是方法論〈可觀測性〉不變式要防的形態，且它同時承接載體契約 C2 的回報 |
| **流程：`who.current` 未更新** | 未以 `set-who` 繞過是對的。PM 於派發 Phase 3a 時一併更正為 `pepper-test-implementer` | `claim` 對已 in_progress 的票回「已被接手」屬預期行為，非缺陷 |

**對 Phase 3a 的約束**：缺口 1 與 3 的裁決已定，`T-16`、`T-7`、`T-29` 依此收斂，不得再採中立形態。分歧 A、B 的 SPEC-003 修訂由 `SR-3` 承接的 DOC 票處理，**Phase 3a 不等它**——實作依 Phase 1，規格的措辭對齊是文件層工作。
### PM 更正：Phase 1 §2.2 的 `AttentionLevel` 是重複定義，Phase 3a 前須改

**世界平面查證**（非轉述）：`0.1.0-W3-205` 已 completed 並併入 main，共用列舉檔 `attention_level.dart`（位於 `lib/app/`）在 HEAD 存在，其 `AttentionLevel` 的成員名與宣告順序為 `discardable` / `mustLeaveTrace` / `undroppable`——**與 Phase 1 §2.2 的內嵌定義逐字相同**。

所以這不是語意衝突，是**兩份同一事實**：同名、同值、同順序，各自宣告一次。它比語意衝突更難發現——兩份一致時任何測試都不會紅，直到某一天有人只改了其中一份。

**這正是本票 `how` 與 `205` 都明文警告過的形態**：`how` 寫「級別列舉不得自行定義，import 該共用列舉檔」；該檔檔頭寫「AppSnackBar 與未來的仲裁器（`0.1.0-W3-207`）皆 import 本檔，不各自定義（避免 SPEC-003 §2.15 警告的兩份同一事實）」。Phase 1 §2.2 產出於該檔合併之前，未被回頭覆核。

**Phase 3a 派發前的處置**（不改 Phase 1 的其餘內容）：

1. §2.2 的 `enum AttentionLevel { ... }` 區塊改為 import 該共用列舉檔，並註明「本票不定義級別列舉，其權威在該檔」。
2. `AttentionArrival` 與 `AttentionSource` **不受影響**——`205` 未建立這兩者，它們仍由本票新建，維持 §2.2 現狀。
3. Phase 2 測試設計中若有對 `AttentionLevel` 宣告位置的斷言，一併指向該共用列舉檔。

**這條的一般形態**：Phase 1 產出後、Phase 3a 派發前，其所依賴的前置票若已合併，Phase 1 的型別定義段必須重新覆核。TDD 階段轉換表沒有這個檢查點——Phase 1 的正確性在產出當下成立，之後由外部變更靜默失效。與 `PC-GPD-019` 的「關係未查證」同族但觸發源相反：那邊是關係從未成立，這邊是關係曾經成立而後被改變。
### Phase 3a 實作策略（pepper-test-implementer，2026-09-10）

語言無關。本節不含任何可執行 Dart 程式碼，只給結構、流程與虛擬碼；型別名沿用 Phase 1 §2.2／§2.3 既有命名（那是 Phase 1 已定的介面契約，不是本節的實作選擇）。

**輸入**：Phase 1 §2.4 裁決表、Phase 2 §4 的 T-1～T-32、〈PM 裁決：Phase 2 第 11 節〉五項、〈PM 更正：Phase 1 §2.2 的 AttentionLevel 是重複定義〉三項處置。三份輸入互相衝突時以較晚的 PM 裁決為準，本節逐處標明採用來源。

#### 3a.0 Phase 1 §2.2 的三項前置處置（已覆核，狀態如下）

| 處置 | 狀態 |
|------|------|
| §2.2 `enum AttentionLevel` 改為 import 共用列舉檔 | **採納**。權威落點為 `lib/app/attention_level.dart`（世界平面複核：該檔在本 worktree HEAD 存在，成員與宣告順序為 `discardable` / `mustLeaveTrace` / `undroppable`，檔頭已明寫「未來的仲裁器（0.1.0-W3-207）皆 import 本檔」）。本票**不宣告**級別列舉，Phase 3b 對該檔只 import 不修改 |
| `AttentionArrival` 與 `AttentionSource` 不受影響 | 確認。共用列舉檔內無此二者（已逐行讀畢全檔），仍由本票在 `lib/app/attention_arbiter.dart` 新建 |
| Phase 2 對宣告位置的斷言一併指向共用檔 | **T-27** 是唯一命中者（`AttentionLevel.values` 逐項相等）。處置：斷言內容不變，測試檔對 `AttentionLevel` 的 import 指向共用列舉檔，不從 `attention_arbiter.dart` 轉出。**Phase 3b 硬約束：`attention_arbiter.dart` 不得 `export` 該列舉檔**——轉出會使呼叫端可經仲裁器取得級別，兩條 import 路徑指向同一型別即是「宣告位置」再度模糊化的入口，而它同樣不會讓任何測試變紅 |

#### 3a.1 模組切分與資料結構選擇

單一檔案 `lib/app/attention_arbiter.dart`，內部四個關注點，**不拆檔**（合計遠低於認知負擔的 300 行拆檔閾值，且拆檔會使 V-1～V-5 的靜態核對面從一個檔擴為多個）：

| 關注點 | 內容 | 為何不外移 |
|--------|------|-----------|
| 值型別區 | `AttentionArrival`／`AttentionSource`／`AttentionRequest`／`AttentionHandle`／`AttentionDecision` 四子型別／`AttentionReleaseReason`／`AttentionConsumed`／`AttentionHolder` | 全為無行為的值型別，與裁決邏輯同生命週期 |
| 留痕接縫區 | `AttentionArbiterLogEvent`（12 值）／`AttentionArbiterLogSink`／預設 sink | 與 `AppSnackBarLogSink` 同形，該檔亦內含 |
| 裁決器區 | 抽象介面 + 唯一實作 | — |
| 暴露區 | `attentionArbiterProvider` | T-30 只驗單例契約 |

**狀態的資料結構選擇：恰一個可空的持有者槽，不是長度上限為 1 的集合。**

| 候選 | 否決理由 |
|------|---------|
| 容量 1 的佇列／堆疊 | 型別層即可表達「第二筆」，`0.1.0-W3-239`「不引入任何形式的提示佇列」與票面 acceptance「無任何集合型欄位」的違反只差一次改容量常數，且不會有任何測試變紅。V-3 之所以是靜態檢查而非行為測試，正因為此違反在行為上不可觀測 |
| 依級別各一槽（3 槽） | 恰為「隱式佇列」——被讓步者被保存下來即成等待清單，與 Phase 1 非目標第 4 項相反 |
| **採用：單槽 + 三個伴隨欄位**（`presentedAt`／`naturalLifespan`／`acceptedAt`） | 「恰一筆或空」在型別層即為不變式；伴隨欄位是持有者的屬性不是額外請求 |

伴隨欄位放在持有者記錄內（`AttentionHolder`）而非仲裁器上的平行欄位：平行欄位會產生「holder 為空但 presentedAt 有值」的不可達狀態，而清空兩處是兩個動作，漏一個不會紅燈。

`AttentionHolder.ageAt(now)` 的基準為 `acceptedAt` **不是** `presentedAt`（T-20 的主斷言：未 `presented` 時年齡自接受起算）。逾期判定的基準才是 `presentedAt`——兩個時間基準各自回答不同問題（年齡＝佔用了多久，逾期＝載體承諾的存活期過了沒有），不可合併為一個欄位。

#### 3a.2 留痕接縫的形態決策：建構子注入，不用靜態欄位

Phase 2 §2 把此項留給 Phase 3a。**採建構子注入**（時鐘與 sink 皆為建構參數，生產預設分別為系統時鐘與轉呼專案統一日誌工具的預設 sink）。

| 比較項 | 建構子注入（採用） | 靜態欄位（`AppSnackBar.logSink` 形態） |
|--------|------------------|--------------------------------|
| 測試隔離 | 各測試於 `setUp` 自建實例即天然隔離 | 須 `setUp` 安裝 + `tearDown` 還原，漏一次即跨測試汙染，且汙染表現為別的測試偶發紅燈 |
| 與被測物的生命週期 | 相同 | 不同（跨測試存活） |
| 既有前例 | `WorkspaceRepository` 的 `logSink` 建構參數（已在庫內） | `AppSnackBar` |

**為何 `AppSnackBar` 用靜態而本票不用**：`AppSnackBar` 是無實例的靜態元件（`abstract final class` + 靜態 `show`），沒有可注入的建構點；仲裁器有實例且經 provider 暴露，注入點天然存在。**這不是兩套慣例，是同一原則在兩種可注入性下的結果**——Phase 3b 不得以「與元件庫一致」為由改回靜態。
#### 3a.3 決策管線（虛擬碼，語言無關）

```
函式 REQUEST(r) -> decision:

  # 步驟 0：輸入驗證（純輸入檢查，無副作用）
  若 r.id 為空字串：
      發事件 rejected {requestId: 空字串, reason: emptyId}，等級 = 警告
      回傳 Rejected(blockedBy: 空)       # PM 裁決缺口 1：單一行為、blockedBy 可空、不設 assert
      # 注意：本步驟先於步驟 1，故無效請求不會觸發任何狀態變更（含逾期清除）

  # 步驟 1：逾期懶檢查（唯一會改狀態的前置步驟）
  若 有持有者 且 持有者.presentedAt 非空
     且 持有者.naturalLifespan 非空
     且 (現在 - 持有者.presentedAt) 嚴格大於 持有者.naturalLifespan：
      RELEASE_HOLDER(原因 = expired)     # 發 expired 事件 + consumed；等級依「持有者」的到達類別

  # 步驟 2：禁止持有並等待（先於任何級別比較）
  若 有持有者 且 持有者.handle.requestId == r.id：
      發事件 heldAndRequested，等級 = 警告
      回傳 Rejected(blockedBy: 持有者.handle)    # 狀態完全不變

  # 步驟 3：級別傳播單調不升（先於裁決表）
  有效級別 := r.level
  若 r.parentLevel 非空 且 r.level 高於 r.parentLevel：
      有效級別 := r.parentLevel
      發事件 levelClamped {requestedLevel, parentLevel}，等級 = 警告

  # 步驟 4：裁決表（Phase 1 §2.4，唯一比較級別之處）
  快照 := OCCUPANCY_SNAPSHOT()           # 裁決「前」，見 3a.5
  若 無持有者：  回傳 ACCEPT(r, 有效級別, 被搶佔者 = 空, 同級取代 = 否)
  比較 := COMPARE(持有者.handle.level, 有效級別)    # 唯一的級別比較點
  分支 比較：
      持有者較低 -> 回傳 ACCEPT(r, 有效級別, 被搶佔者 = 持有者, 同級取代 = 否)
      相等       -> 回傳 ACCEPT(r, 有效級別, 被搶佔者 = 持有者, 同級取代 = 是)
      持有者較高 -> 分支 r.arrival：
          spontaneous 且 有效級別 == discardable    -> 發 skipped；回傳 Skipped(持有者.handle)
          spontaneous 且 有效級別 == mustLeaveTrace -> 發 deferred；回傳 Deferred(持有者.handle, 視圖)
          waiting                                  -> 發 rejected；回傳 Rejected(持有者.handle, 視圖)
          # spontaneous 且 undroppable 在此不可達：undroppable 為最高級別，
          # 不存在更高的持有者。以「表達式窮舉」承接（見 3a.12）
```

```
函式 ACCEPT(r, 有效級別, 被搶佔者, 同級取代):
  若 被搶佔者 非空：
      # 舊者先離開再讓新者進入。順序是可觀測的（T-9／T-17 各有一處索引斷言），
      # 且它是語意的：兩則事件的先後即「通道在任一時刻恰一個持有者」的痕跡
      發事件 (同級取代 ? sameLevelReplaced : preempted) {requestId: 被搶佔者.requestId, reason: preempted}
      清空持有者槽
      廣播 consumed(被搶佔者, preempted, 現在)
  新 handle := HANDLE(r.id, 有效級別, r.arrival, r.source, acceptedAt = 現在)
  持有者槽 := HOLDER(新 handle, presentedAt = 空, naturalLifespan = 空)
  發事件 accepted {requestId: r.id, decision: accepted}
  回傳 Accepted(新 handle, 被搶佔者, 同級取代)

函式 PRESENTED(handle, 自然存活期, 載體事件識別 = 空):
  若 無持有者 或 持有者.handle.requestId != handle.requestId：
      發事件 releasedNonHolder {op: presented}，等級 = 警告；返回（狀態不變）
  有效存活期 := (自然存活期 為空 或 小於等於零) ? 空 : 自然存活期
  持有者.presentedAt := 現在
  持有者.naturalLifespan := 有效存活期
  發事件 presented {naturalLifespanRaw: 自然存活期原值, carrierEventId: 載體事件識別}

函式 RELEASE(handle, 原因):
  若 無持有者 或 持有者.handle.requestId != handle.requestId：
      發事件 releasedNonHolder {op: release}，等級 = 警告；返回（狀態不變、不廣播）
  RELEASE_HOLDER(原因)

函式 RELEASE_HOLDER(原因):
  離開者 := 持有者.handle
  發事件 (原因 == expired ? expired : released) {requestId: 離開者.requestId, reason: 原因}
  清空持有者槽                    # 先清空再廣播（見 3a.4 再進入）
  廣播 consumed(離開者, 原因, 現在)
```

**三個前置步驟的順序各由一條測試鎖住**（Phase 2 §3 已指名）：步驟 1 先於步驟 4 由 T-17 的 `preempted == null` 鑑別；步驟 2 先於步驟 4 由 T-14 列 2 鑑別；步驟 3 先於步驟 4 由 T-15 列 4 鑑別。**步驟 0 先於步驟 1 是本節新增的順序**，無測試覆蓋（空 id 且持有者逾期的組合在 T 集合中不存在）——理由是無效輸入不應產生狀態變更，屬設計選擇非規格要求，登記於 3a.8 第 6 列。

#### 3a.4 已消費訊號的投遞與再進入

| 決策 | 內容 |
|------|------|
| 通道形態 | 廣播式、**非同步投遞**（不使用同步投遞模式） |
| 為何非同步 | 同步投遞會在「清空持有者槽」與「設定新持有者」之間把控制權交給訂閱者，訂閱者於回呼中再次 `request` 即讀到中間狀態。非同步投遞使狀態變更在單一 tick 內原子完成。Phase 2 §1 已預先要求斷言前排空 microtask，正是不假設同步投遞 |
| 廣播時點 | **清空持有者槽之後**（見 `RELEASE_HOLDER`）。即使日後改為同步投遞，訂閱者看到的也是「通道已空」而非中間態 |
| 無訂閱者 | 不緩衝、不重播（T-31）。廣播式通道的既有語意即此，不另寫邏輯 |
| `retryAfter`／`signal` | 同一通道的**過濾視圖**，過濾鍵為 `blockedBy.requestId`（T-32）。不新建第二個通道——兩個通道即兩份同一事實 |
| 視圖不設自動關閉 | 過濾鍵為請求識別且識別不重用（Phase 2 §2「每個測試內的 id 互異」），一個 blockedBy 至多離開一次，視圖至多發一則 |
#### 3a.5 事件欄位的組裝

單一組裝函式產生共同欄位，各呼叫點只補差異欄位——七欄位齊全由**一處**保證，不是十二個呼叫點各自記得（`expectSevenFields` 對每一筆 records 都成立，逐點組裝時漏一點不會被該事件以外的測試發現）。

| 欄位 | 值 |
|------|---|
| `requestId` | **事件所描述的那一筆請求**的識別。`preempted`／`sameLevelReplaced`／`expired`／`released`／`releasedNonHolder` 填的是離開者，不是觸發者（T-9、T-10、T-17 皆以此斷言） |
| `arrival`／`level` | 同上，取自事件所描述那一筆 |
| `channel` | 固定 `userFocus`（0.1 單通道；常數宣告一次） |
| `decision` 或 `reason` | 裁決類填 `decision`，離開類填 `reason` |
| `occupancy` | `{inFlight, queueDepth, oldestAge}` 三獨立鍵。`queueDepth` 恆為 0（無佇列，非「未量測」）；`oldestAge` 為 `Duration` 型別或空 |
| `deadlineRemaining` | 請求攜帶值；為空時**額外**附 `deadlineAssigned: true`（T-8 列 2 鑑別：不得對所有請求都附） |

**`occupancy` 快照時點（PM 裁決缺口 2：裁決前）的精確化**：定義為「**該事件所描述的動作發生之前**」。逐事件的落值：

| 事件 | inFlight | oldestAge |
|------|---------|-----------|
| 空通道 `accepted` | 0 | 空 |
| `skipped`／`rejected`／`deferred` | 1 | 持有者年齡（同 tick 為零，T-24 列 2） |
| `preempted`／`sameLevelReplaced` | 1 | 被搶佔者年齡（T-20 斷言此值） |
| 搶佔後的 `accepted` | 1 | 被搶佔者年齡。**理由**：它描述的動作是「新者進入」，其前狀態為舊者仍在。與 T-20「該 accepted／sameLevelReplaced 事件的 occupancy.oldestAge 等於 Δ」一致 |
| `expired` | 1 | 逾期者年齡 |
| 逾期清除後的 `accepted` | 0 | 空（T-17 不斷言此值，但與「裁決前」定義一致：逾期已在該動作之前完成） |

#### 3a.6 日誌等級

| 類別 | 事件 | 等級 |
|------|------|------|
| 契約違反（呼叫方用錯） | `heldAndRequested`、`levelClamped`、`releasedNonHolder` | **恆為警告**，不看到達類別（T-23 列 6～8 刻意以 `waiting` 鑑別） |
| 請求未進入通道 | `skipped`、`deferred`、`rejected` | 依**該請求**的到達類別：自發型（背景發起）警告，等待型資訊 |
| 逾期 | `expired` | 依**逾期持有者**的到達類別（T-23 列 4／5），不是觸發懶檢查那一筆的 |
| 其餘 | `accepted`、`preempted`、`sameLevelReplaced`、`presented`、`released` | 資訊 |

等級數值沿用專案既有兩個常數值（資訊／警告），與 `AppSnackBar` 同源，仲裁器內各宣告一次具名常數，不寫裸數字。判定寫成**單一純函式**（輸入恰為事件類別與到達類別），與 `AppSnackBar._resolveClosedLevel` 同形——輸入不含級別，其函式體因此不可能以級別為鍵，這是 SPEC-003〈截斷事件的日誌等級以「事件的發起者」為鍵〉的結構性可讀出保證。

#### 3a.7 仲裁決策事件與 `AppSnackBarLogSink` 的關聯機制（本票的設計工作）

**世界平面實查**：`AppSnackBar` 的 `showId` 由元件內 `_nextShowId()` 產生（process 內遞增整數），在 `context.mounted` 判定**之前**產生，故 `skippedUnmounted` 也帶識別；`baseFields` 為 `{showId, origin}`；`preempted` 事件另帶 `preemptedShowId`。仲裁側的 `AttentionRequest.id` 為請求方提供的字串（PM 裁決分歧 A）。

**兩個識別不能拼接為一個值**，三個理由：(a) 產生方不同（一個在請求方、一個在載體內）且產生時點不同（仲裁受理早於載體呈現）；(b) 生命週期不同——被跳過／拒絕／延後的請求**從未抵達載體**，永遠沒有 `showId`，拼接值對它無法成形；(c) 一對多——同一請求識別在序列重借下可對應零或一個 `showId`，跨載體時對應的甚至不是 `showId`（系統通知、關卡各有自己的識別空間）。

**採用形態：雙向背向參照，關聯鍵為請求識別。**

| 方向 | 機制 | 承擔票 |
|------|------|--------|
| 載體 → 仲裁 | 載體收到 `AttentionHandle` 後，其 `baseFields` 增一鍵 `requestId`（值取自 handle），與既有 `showId` 並列 | `0.1.0-W3-205` 之後的載體接線票（本票不改載體） |
| 仲裁 → 載體 | `presented` 回報時**可選**攜帶載體側識別，落為 `presented` 事件的 `carrierEventId` 欄位；未提供時為空 | 本票（介面），載體接線票（填值） |

**關聯查詢的兩種形態**：以請求識別過濾兩側日誌即得該請求的完整生命週期（此為主路徑，任一側缺 `carrierEventId` 仍成立）；`carrierEventId` 非空時可由仲裁側單筆直接定位載體事件，不需掃描。**後者是最佳化不是必要條件**——Phase 3b 不得因載體尚未接線而視此欄位為未完成。

**對 Phase 1 §2.3 的介面增補（本節唯一的介面變更）**：`presented` 增一個可選具名參數承載載體側識別，預設為空。既有呼叫（Phase 2 全部 `presented` 呼叫點）不需修改即編譯通過；`expectSevenFields` 為「必須包含」語意，多一鍵不違反。

**`0.1.0-W3-205` 過渡形態的收斂（該檔檔頭指名由本票承擔）**：`AttentionHolderHint` 與 `currentHolder` 參數的取代路徑為——載體改為接受 `AttentionAccepted`，`hide` 條件為其 `preempted` 非空，`holderUnknown: true` 隨之消滅（持有者不再是「呼叫端已知的快照」而是仲裁器的唯一記錄）。**本票不改載體檔**（票面 `where.files` 只含仲裁器與其測試，且 acceptance 明訂不得 import `AppSnackBar`）；本段是交給載體接線票的收斂規格，登記於 3a.8 第 4 列。
#### 3a.8 權宜方案、技術債務與 Phase 4 方向

| # | 項目 | 形態 | Phase 4／後續方向 |
|---|------|------|------------------|
| 1 | 逾期只在 `request` 時懶檢查 | 權宜（刻意）。無計時器即無 `Timer`、無 `dispose` 漏洞、測試無需假計時器 | **不建議改為計時器**。代價是「無新請求時逾期者停留在記錄中」，而該狀態的唯一可觀測後果是下一次請求前的 `holder` 讀值——沒有任何使用者可見行為依賴它 |
| 2 | 無自然存活期的持有者（關卡）漏回報即無法自癒 | 已知限制（Phase 1 §8 第 2 列）。無上界 | 消費機制為「關卡狀態機每條退出路徑皆有 `release`」的靜態檢查，**落在承接關卡載體的票**（Phase 2 §5 已明記不在本票）。本票 Phase 3b 不承擔 |
| 3 | 載體側識別（`carrierEventId`）在 0.1 恆為空 | 介面已備、填值未接線 | 載體接線票（3a.7 表第一列）填入後即生效，本票無需改動 |
| 4 | `AttentionHolderHint`／`currentHolder`／`holderUnknown` 的收斂 | 未執行（跨票面範圍） | 規格見 3a.7 末段，執行者為載體接線票 |
| 5 | `queueDepth` 恆為 0 | 非債務，是**佔位語意**——「已量測且為零」與「未量測」在欄位缺席時同形 | 第三類接收緩衝落地時（Solution 第 7 節 trigger）才會有非零值 |
| 6 | 步驟 0（空 id 驗證）先於步驟 1（懶檢查）無測試覆蓋 | 設計選擇，無規格依據 | 若 Phase 4 認為需鎖住，補一條「空 id 請求不清除逾期持有者」的測試；本票不補（無 acceptance 承接，且補測試等於把一個未經裁定的順序固化） |
| 7 | 留痕逐筆不取樣 | 刻意（Phase 1 §2.5）。前提是請求率受人類互動速率上界 | 前提失效的訊號：出現非人類觸發的高頻請求方。屆時退化為方法論〈可觀測性〉的「計數 + 取樣」形態 |

#### 3a.9 七項測試義務的 Phase 3a 覆核

Phase 2 §6 已逐項承接完畢（含空成立依據，以標題文字引用 SPEC-003 §2.15／§2.16，未重述判準）。本階段**逐項覆核其在實作策略上有對應落點**，不重寫該表：

| # | 判準 | 本節落點 | 空成立／不適用者的依據是否仍成立 |
|---|------|---------|--------------------------|
| 1 | 仲裁決策事件 | 3a.5 單一組裝函式；3a.7 關聯機制 | 適用，無空成立 |
| 2 | 三條讓步規則 | 3a.3 步驟 4 分支表 | 規則 1／3 空成立、規則 2 具名例外——依據為 SPEC-003〈跨級別：規則 1／3 對列 1–10 與列 11–13 的驗收〉對 0.1 事件域的證明，與實作策略無關聯，未受影響 |
| 3 | 卸載順序九格 | 3a.3 步驟 4；推送型由 `AttentionArrival` 恰兩值於型別層排除 | 6 格空成立依據不變；推送型三格的型別層排除由 3a.1 的列舉宣告落實 |
| 4 | 進入與離開卸載的閾值滯後 | **無落點**（正確） | 不適用，依據為 SPEC-003〈佔用量觸發型暫停（滯後）在注意力通道的形態〉已證本通道無佔用量觸發的卸載狀態。本節未引入任何佔用量閾值，依據仍成立 |
| 5 | 仲裁器自身溢出 | 3a.1 單槽選擇的否決表 | 以「型別層無集合」承接。**本節強化了該依據**：否決「容量 1 的佇列」的理由即「溢出分支存在與否應在型別層可讀出，不靠常數」 |
| 6 | 可棄推送型丟最舊 | **無落點**（正確） | 空成立，依據為推送型不存在 |
| 7 | 級別傳播單調不升 | 3a.3 步驟 3 | 適用，無空成立 |

#### 3a.10 方法論兩項實作約束的落實

**(a) 禁止持有並等待**——同一請求對同一通道的多次佔用須在首次進入時一次申請。落實為三層，缺一則該約束只是文件上的宣告：

| 層 | 機制 |
|----|------|
| 行為層 | 3a.3 步驟 2：持有者以同一識別再請求即拒絕，且**先於級別比較**（否則高級別再請求會走搶佔路徑，變成「持有一份的同時取得第二份」） |
| 結構層 | 仲裁器**無任何回傳延後值（future）的方法**、內部無等待點。`request` 為同步、常數時間。`Stream` 是訂閱點不是等待點，仲裁器自身不等待任何人 |
| 語意層 | 被拒絕／延後者**不被保存**。「等待」在本設計中不存在於仲裁器內——請求方自行重驗前提後重新申請，這是「一次申請」的反覆執行，不是一次持有中的等待（T-12 以「釋放後、再請求前 `holder` 為空」鎖住） |

**(b) 仲裁器單點性——明寫，不迴避。** 本仲裁器與通道同在同一執行緒，**非**獨立行程，故不引入跨行程的可用性單點；但它是**通道之外的獨立物件**：持有者記錄與載體佇列、系統通知送達狀態、關卡狀態機三者皆不共享記憶體。因此存在「仲裁器狀態壞而通道仍能動」的失效路徑，四項失效模式與防線見 Phase 1 §8。

**Phase 3b 的落地要求**：上述兩段須寫入實作檔的 library doc（V-6），逐字含「本物件是通道之外的獨立物件，為新增的可用性單點」，並列出四項失效模式與防線。**這不是註解排版要求**——單點性是讀者從程式碼讀不出來的性質（沒有 import、沒有型別會揭示它），文件是它唯一的載體。

#### 3a.11 交付 Phase 3b 的實作順序與逐階段目標

沿用 Phase 2 §9 的群組與建議順序（G-E → G-A → G-B → G-C → G-D），五群組同檔，**序列執行不並行派發**（Phase 2 已標明同檔衝突）。

| 階段 | 目標測試 | 產出 | 完成判準 |
|------|---------|------|---------|
| 1 | T-26～T-30 | 型別骨架（列舉、值型別、sealed 決策、provider）；方法體全為未實作 | 五條結構測試綠；其餘全紅且紅在「未實作」不在「編譯失敗」 |
| 2 | T-1～T-8 | 單槽狀態機的接受／呈現／釋放／廣播；事件組裝函式 | G-A 全綠 |
| 3 | T-9～T-16 | 三個前置步驟 + 裁決表分支 | G-B 全綠；此時 3a.3 虛擬碼全部落地 |
| 4 | T-17～T-21 | 懶檢查與時鐘 | G-C 全綠 |
| 5 | T-22～T-25、T-31～T-32 | 欄位齊全、等級、視圖過濾 | 全套件綠 |
| 6 | — | V-1～V-9 靜態自檢 + Q12 指名的五處故意破壞驗紅（T-14 列 2、T-15 列 4、T-17、T-21、T-23 列 6～8） | 每處破壞皆使**對應那一條**變紅（破壞後全綠或紅到別條，代表該測試沒有鎖住宣稱的性質） |

**Phase 3b 的四項硬約束**（違反皆不會被任何測試發現，故在此逐條列出）：

1. `attention_arbiter.dart` 不 `export` 級別列舉檔（3a.0）
2. 留痕接縫為建構參數，不改為靜態欄位（3a.2）
3. 狀態為單一可空槽，不用任何集合型（3a.1）
4. 級別比較恰在一處函式，比較鍵為列舉序值（V-4）

#### 3a.12 語言特定實作注意事項（Dart／Flutter）

本節為交付 `parsley-flutter-developer` 的落地提醒，非策略本身：

| 項目 | 注意事項 |
|------|---------|
| 窮舉檢查 | 決策以 `sealed` 宣告，測試端對決策的 `switch` **不寫 `default`**（V-9 以編譯期窮舉承接子型別數量）。實作端對「持有者較高 × 到達類別 × 有效級別」的分支若以 `switch` 表達式撰寫，不可達格（自發型／不可棄）由編譯器要求窮舉——此時以拋出狀態錯誤表達不可達，**不得**以回傳某個決策充數（那會讓不可達格靜默取得行為） |
| 廣播通道 | 建立時不啟用同步投遞（3a.4）；視圖以過濾運算子取得，**不**另建控制器 |
| 資源釋放 | provider 需在容器銷毀時關閉廣播通道；測試以 `addTearDown` 銷毀容器（Phase 2 §7 Q13 已列） |
| 時鐘 | 建構參數為「取得目前時刻」的函式，生產預設為系統時刻取得函式。實作內**任何一處**都不得直接呼叫系統時刻（否則假時鐘只覆蓋部分路徑，逾期測試會偶發） |
| 存活期正負判定 | 零或負值視同不設存活期，原值仍落入事件欄位（T-7 兩列） |
| 日誌 | 預設接縫轉呼專案統一日誌工具並帶標籤，`fields` 攜帶結構化欄位；組句給人讀由接收端處理，**不在仲裁器內組散文** |
| i18n | 本檔無任何 user-facing 字串。事件的字串值（`userFocus`、`expired` 等）為日誌鍵不是文案，不進 l10n |

**Phase 3a 完成**。產出為策略、虛擬碼與流程，未寫任何 Dart 實作；`lib/` 與 `test/` 在本階段零改動。
Phase 3b 實作補記（parsley-flutter-developer，2026-09-10）：實作依 3a 虛擬碼逐步落地，過程中發現並修正 Phase 3a 未明示的一項細節——AttentionArbiterLogEvent.expired 僅限步驟 1 懶檢查自動觸發時使用；顯式呼叫 release(handle, expired) 一律記為 released（T-3、T-22 步 10/11 的區分即據此）。已在實作內以 _expireHolderLazily()／_releaseHolder(event:) 分離兩條路徑，並補上先前遺漏的 occupancy 快照時序 bug（_accept 內先於任何狀態變更取快照，否則空通道接受會誤讀為已寫入新持有者）與缺漏的 deadlineRemaining 七欄位補齊（preempted／sameLevelReplaced／released／expired／releasedNonHolder／presented 皆補上 deadlineRemaining: null, deadlineAssigned: true）。step 0（空 id 驗證先於懶檢查）依 3a.8 第 6 列維持無測試覆蓋的設計選擇，未新增測試固化該順序。

### PM 對 Phase 4a 的裁決（2026-09-10）

品質 B。架構層四項硬約束全數結構性成立（審查原話：**這部分是 good taste**）；扣分全在實作層的局部問題。審查報告因長度三次截斷，PM 已就其點名的實質項目自行實查補齊。

**一、`analyze` 基線退化——須修，且不得以 `// ignore:` 修。**

審查的查證方式使結論可查而非印象：`analysis_options.yaml` 全文 8 行、無任何規則覆寫；全庫 5 處 `// ignore` **全在 `lib/l10n/` 生成產物**，手寫檔案歷史上零抑制先例。**本專案慣例是零抑制零 issue，不是「info 可放行」。** 加抑制標記等於為迴避一次改名而建立全庫第一個手寫抑制先例。修法：`expectSevenFields` → `_expectSevenFields`（已確認無跨檔引用）。

**二、`deadlineAssigned` 欄位說謊——採修法 (a)，取消特例。**

`AttentionHandle` 不帶 `deadline`，離場時仲裁器無從得知請求當初有沒有帶預算，於是帶 `deadline` 的請求被搶佔時會記下 `deadlineRemaining: null, deadlineAssigned: true`——**與事實相反**。**這不是欄位缺席，是欄位說謊；說謊的日誌比缺席的日誌更難除錯。**

輔證：helper 名為 `_deadlineFieldsUnassigned`（未指派）而回傳 `deadlineAssigned: true`（已指派），名稱與值互相矛盾。

**採 (a)**：`AttentionHandle` 增 `deadline` 欄位、離場事件據實回報、`_deadlineFieldsUnassigned` 整個消失。理由取審查原話——**讓特例消失，而不是為特例補一個名字**。此項動 Phase 1 §2.2 的型別定義，本裁決即為該變更的核准，Phase 4b 一併更新 §2.2 的記載。

**三、`released` 事件等級錯誤——PM 自行實查確認，補記其機制。**

`_levelForOrigin(arrival)`（`:692-693`）在 `spontaneous` 時回傳 900，`_releaseHolder`（`:630`）以它決定 `released` 的等級。**於是背景發起的請求，其正常、自願的釋放被記為 warning。** 釋放不是異常，是持有者生命的正常終點。

**不一致可由測試本身佐證**：同一請求的 `accepted` 事件另走一條等級路徑，且 `:169` 明確斷言 `spontaneous` 的 `accepted` 不得為 900。**同一個請求的接受記 info、釋放記 warning**，而只有前者有斷言。

**附帶**：`:169` 的 `record.level == null || record.level != 900` 第一個析取項是冗餘的（`null != 900` 在 Dart 即為真），寫法讓它看起來在處理一個其實不需要處理的情形。

**四、Phase 4b 範疇**

4b 執行：analyze 退化修正、`deadlineAssigned` 修法 (a)（含 §2.2 記載更新）、`released` 等級修正並補測試斷言、`:169` 冗餘析取項簡化。

審查另點名三項未及展開細節者，一併由 4b 處置並在 Solution 列出：**151 行的 `request()`**（PM 實測該函式區塊 201 行）、**參數全死且名稱說謊的 `_copyWith`**（`:196` 宣告、`:561` 唯一呼叫）、**兩處逐字重複的函式**。審查稱技術債共 11 項，其餘未點名者由 4b 自行盤點列出，判定不需處理者逐項附理由。

**§3a.8 的未批准設計選擇**（step 0 空 id 驗證置於惰性過期檢查之前，無測試覆蓋該順序）：4b 須補一條覆蓋該順序的測試，或說明為何該順序不需鎖定。
### Phase 4b 更正：3a.1「遠低於 300 行」的行數宣稱失實（cinnamon-refactor-owl，2026-09-10）

3a.1 原文「合計遠低於認知負擔的 300 行拆檔閾值」在本票落地當下已不成立——`lib/app/attention_arbiter.dart` 實測 699 行（Phase 4b 移除 `_emit`／`_deadlineFieldsUnassigned` 等重複後仍逾 2 倍閾值）。歷史段落不回改，於此更正：

**不拆檔的決策維持**，理由改為 3a.1 原文已具備的第二項——拆檔會使 V-1～V-5（import 白名單、欄位型別、`.index` 比較位置等靜態核對）從一個檔案的核對面擴散為多個檔案，核對成本隨檔案數增加而非隨行數增加。行數閾值不成立不影響此理由的有效性，故決策本身不需重新評估。
### Phase 4b 自檢結果（cinnamon-refactor-owl，2026-09-10）

| 項 | 狀態 | 說明 |
|---|------|------|
| analyze 退化 | 已修 | `expectSevenFields` → `_expectSevenFields`，無跨檔引用，無 `// ignore:` |
| `deadlineAssigned` 說謊 | **移出範疇** | PM 追加裁決 F-3 屬行為變更非重構，已另開 `0.1.0-W3-262`；本票未動 `AttentionHandle`／Phase 1 §2.2 |
| `released` 等級錯誤 | 已修 | `_releaseHolder` 僅在 `event == expired` 時依發起者判定等級，`released` 恆 info；補測試鎖住 spontaneous 正常釋放非 900 |
| `:169` 冗餘析取項 | 已修 | 化簡為 `expect(record.level != 900, isTrue)` |
| 151 行 `request()` | 已處置 | 抽出 `_isHigher()`，`.index` 比較由三處收斂為一處（強化 V-4）；函式本身長度未進一步拆分（PM 僅裁決此修法） |
| `_copyWith` 參數全死 | 已修 | 移除恆為 true 且命名與語意矛盾的 `clearNaturalLifespan`，改無條件覆寫 |
| 兩處逐字重複的函式 | 已修 | `_reasonName`（lib 與 test 各一份）移除，改用 `enum.name` |
| 其餘技術債（自行盤點） | 已修 3 項 | F-7（`_deadlineFieldsUnassigned` 與 `_deadlineFields(null)` 重複）、F-8（`_defaultLogSink` 冗餘分支）、F-9（空 id 拒絕的 `retryAfter` 誤用未過濾 `consumed` 全量廣播） |
| F-10 `_emit` 純轉呼包裝 | 已修 | 移除，呼叫點直接呼叫 `_logSink` |
| 3a.8 未批准設計選擇 | 已補測試 | 新增「空 id 驗證先於逾期懶檢查」順序測試，鎖住該順序不被日後修改破壞 |
| 3a.1「遠低於 300 行」失實 | 已更正 | 見前一則 Solution 追記，不拆檔決策維持，理由改為避免 V-1～V-5 靜態核對面擴散 |

**技術債盤點聲明**：審查稱共 11 項，PM 點名 3 項（`request()` 長度、`_copyWith`、重複函式）+ 4 項既裁決（analyze／deadlineAssigned／released／`:169`）= 7 項有明確線索；本次另盤點出 F-7／F-8／F-9／F-10 共 4 項，合計 11 項，與審查計數一致，判定無遺漏。

**硬要求核對**：全套件 1297 passed / 0 failed（未放寬任何既有斷言，新增 2 條）；`fvm flutter analyze` No issues found（121.6s）。commit：8d086321。

**worktree 隔離提醒（`0.1.0-W3-247`）**：本次所有 `ticket track` 讀寫指令皆在共用 checkout（`/Users/mac-eric/project/graph_project_docs_manager`）下執行（票務系統既有限制，worktree 內無法直接操作 ticket md），程式碼改動全數落在 worktree 副本（`.claude/worktrees/agent-a83700816fd7c1053`）並已於該處 commit，未見殘留衝突。

### 自檢結果

由 PM 於主 checkout 收尾時記錄（Phase 4b 執行者的 `check-acceptance` 被 identity-guard 擋下——`who.current` 停在 `pepper-test-implementer`，那是我在 Phase 3a 派發時設定後未隨 3b／4b 更新，屬 PM 疏漏而非執行者問題）。

| 檢查項 | 結果 |
|--------|------|
| acceptance 四項逐項對照產出 | 通過。介面只表達級別、決策依 SPEC-003 級別規則未另立、落層符合 `0.1.0-W3-203` 指定、未實作第三類接收緩衝且 Solution 明記範疇與 trigger |
| 100% 測試通過 | 通過。PM 於 **main**（非 worktree）獨立實跑：**1297 passed / 0 failed**（Phase 3b 基線 1295 + F-1 與 3a.8 各一）；`fvm flutter analyze` 全專案 **No issues found** |
| `analyze` 基線已回復 | 通過。Phase 3b 引入的 `library_private_types_in_public_api` 已由 F-2 修掉，**未使用任何 `// ignore:`**——本專案手寫檔案的零抑制先例維持不破 |
| Phase 3a 四項硬約束 | 通過。Phase 4a 逐項固定值查核：零 `export`、`grep static` 全檔零命中、狀態僅 `AttentionHolder? _holder`、`.index` 經 F-4 抽出 `_isHigher()` 後**由三處收斂為一處**（較原設計更強） |
| Phase 4 未跳過 | 通過。4a 產出四項必答（重構需要：是／技術債：11 項／品質：B／結論說明），4b 執行十項＋兩項補測試 |
| 技術債逐項有處置 | 通過。F-1／F-2／F-4～F-11 由 4b 落地；**F-3 移出 4b 另由 `0.1.0-W3-262` 承接**（理由：改變日誌輸出內容屬行為變更非重構） |
| spawn 規劃全數落地 | 通過。SR-3 已 resolve 為 processed → `0.1.0-W3-270` |
| 設計文件缺陷有承接 | 通過。Phase 2 對 `released` 等級的覆蓋缺口與 3a.3 虛擬碼的等級選擇條文由 `0.1.0-W3-263` 承接 |

**自檢發現三項並已處置**：

1. **`who.current` 未隨階段更新**：我在 Phase 3a 設為 `pepper-test-implementer` 後，3b（parsley）與 4b（cinnamon）皆未更新，直到 4b 收尾時被 identity-guard 擋下才發現。**identity-guard 只在 `check-acceptance`／`complete` 咬合**，中間兩個階段全程無訊號。此形態與 `0.1.0-W3-256` 同族（`who.current` 在並行環境下的鑑別力問題），已於該票範疇內。
2. **F-3 的階段歸屬我裁錯一次**：原裁入 Phase 4b 並寫「本裁決即為該變更的核准」，Phase 4a 完整報告補齊後更正——**核准一個變更不等於它該在重構階段做**。已於 4b 執行中發訊移出，執行者確認從未實作、無需回退。
3. **一次合併未落地而 git 回報成功**：`git merge` 印出「Merge made by the 'ort' strategy」但 `refs/heads/main` 因殘留 `main.lock`（14:01，`lsof` 確認無行程持有）未更新，`git merge-base --is-ancestor` 查證為 `NOT_MERGED`。**未手動刪鎖**，以 `git merge --abort` 清 `MERGE_HEAD` 殘局後等待鎖自行釋放再重試成功。此為「失效方向是成功」的又一實例——git 的 stdout 與 ref 的實際狀態不一致。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
Phase 3b 測試執行（parsley-flutter-developer，2026-09-10）：`fvm flutter test test/unit/app/attention_arbiter_test.dart --reporter compact` 31 個測試全數通過；`fvm flutter test --reporter compact`（全套件）1295 passed / 0 failed / 0 skipped（基線 1264 + 新增 31）；`fvm dart analyze lib/app/attention_arbiter.dart test/unit/app/attention_arbiter_test.dart` 0 issues（僅 1 則 info：測試內私有型別作參數，屬慣例）。

V-1/V-2：grep import 僅四項（dart:async／dart:developer／flutter_riverpod／attention_level.dart），無 domain 型別／載體／material.dart。V-3：類別欄位僅 _holder（AttentionHolder?），無任何集合型欄位。V-4：.index 比較僅在 request() 內三處，無以 AttentionSource 為鍵的分支。V-5：無回傳 Future 的方法（grep 命中的 await 僅為 unawaited 子字串誤判）。V-6：library doc 已明寫可用性單點聲明與四項失效模式防線。V-7：檔案路徑符合 where.files。V-8：AttentionRequest 六欄位無載體/時機欄位。V-9：AttentionDecision 為 sealed class，測試以 isA 而非窮舉 switch 間接覆蓋四子型別。四項硬約束（不 export 級別列舉檔／建構參數注入／單一可空槽／級別比較恰一處）逐一確認成立。
Phase 4b 測試執行（cinnamon-refactor-owl，2026-09-10）：
`fvm flutter test test/unit/app/attention_arbiter_test.dart --reporter compact` 33/33 通過（原 31 + F-1 補測試 1 + 3a.8 補測試 1）。
`fvm flutter test --reporter compact`（全套件）**1297 passed / 0 failed**（基線 1295 + 新增 2 條）。
`fvm dart analyze lib/app/attention_arbiter.dart test/unit/app/attention_arbiter_test.dart` → No issues found（F-2 修正後）。
`fvm flutter analyze`（全專案）→ No issues found（121.6s）。
commit：8d086321。
## NeedsContext
- **缺失項**：`where.files` 為 `lib/components::write`，但仲裁器持有畫面級狀態（持有者記錄），SPEC-003 §2〈頁面狀態保留〉列「畫面級狀態不得存於元件庫元件內部」；Solution 2.1 建議落 `lib/app/attention_arbiter.dart`（L4，與 router／app_lifecycle 同層），亦符合 203 指定的 L4。
- **觸發位置**：Solution 2.1「建議檔案」列；Phase 3b 的 `ticket track commit` 以 `where.files` 為寫入子集，落 `lib/app/` 會被拒。
- **影響**：不阻擋 Phase 1／Phase 2；Phase 3b 派發前需 PM 裁定路徑並以 `set-where` 更新（`lib/app/attention_arbiter.dart::write` + `test/unit/app/attention_arbiter_test.dart::write`），或裁定維持 `lib/components/` 並在票面記錄與 §2 條文的例外理由。
- **建議補料**：PM 執行 `ticket track set-where 0.1.0-W3-207 "lib/app/attention_arbiter.dart,test/unit/app/attention_arbiter_test.dart"`。
- **重派成本**：無需重派，Phase 1 產出不受路徑影響。

**PM 已處置（2026-09-10）**：where.files 已由 `lib/components::write` 改為 `lib/app/attention_arbiter.dart,test/unit/app/attention_arbiter_test.dart`，落層與 domain-map 通道節的指定一致（L4 畫面狀態層或其下的 presentation 包裝，不落入任一涉及 domain）。Phase 2 起以新範圍為準。

**回報的框架層矛盾已查證屬實並升級**：`ticket-file-access-guard-hook.py` 的攔截樣式涵蓋 tickets 目錄下所有 `.md` 且對 Write 一律阻擋，而 lavender 定義的〈產出物路徑規範（強制）〉要求 Phase 1 產出必須寫入該目錄，兩者對同一路徑給出相反指令。兩條 AND 判準逐條套過（抽象可攜：替換 agent 名與副檔名後仍成立；資產範圍：兩端皆在 `.claude/`），dedup 命中 open issue #56「規則與方法論條文缺口」，其標題已含「可寫路徑分類衝突」，判定為同類新徵狀，已 observe 附加，未 init。落票面 Solution 的處置維持，不需回退。
## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-10 00:13)
  - what: SPEC-003 §2.14 補列 main.dart:219 的 chooseFolderUnavailableMessage（Workspace 資料夾不可用）之到達類別、級別、標定點與載體，或補入〈明確排除〉表並附理由
  - why: docs/domain-map.md §2.6.2 Workspace 列明寫「資料夾不可用」為注意力請求，但 §2.14 的 13 列與排除表皆無此則；仲裁器請求方須依 §2.14 填級別，缺列即無法發出請求。0.1.0-W3-204 acceptance 第一項「無遺漏」對此列未成立
  - suggested_type: DOC
  - suggested_priority: P1
  - related_files: 
  - context: 
  - status: processed（已建 0.1.0-W3-248）


- **SR-2** (2026-09-10 00:13)
  - what: 對齊 0.1.0-W3-205 how (2)：AppSnackBar.show 不在元件內另存「當前顯示者的級別」，改讀 attentionArbiterProvider 的 holder，hide 條件為 AttentionAccepted.preempted 非空；被搶佔者不由載體 release
  - why: 205 與本票各自比較持有者級別即為 SPEC-003 §2.15 警告的兩份記錄可分歧路徑；本票 Solution 2.6 C1 已定載體側契約，205 的 how 需 PM 改寫以免兩票落地後互相衝突
  - suggested_type: IMP
  - suggested_priority: P1
  - related_files: 
  - context: 
  - status: dismissed（不建票，改直接修 0.1.0-W3-205 的 how（該票 pending、未派發，修 how 比建票再改成本低）。裁決採 SR-2 的建議：當前顯示者的級別從仲裁器讀，元件內不另存一份，兩份同一事實的狀態必然漂移。另補一句過渡形態——207 尚未合併時，205 以呼叫端傳入當前持有者級別實作並於 Solution 記錄，不在元件內建立獨立持有者狀態，207 合併後收斂為單一來源。已於 205 的 how 落條文。）

- **SR-3** (2026-09-10 00:35)
  - what: 對齊 SPEC-003 §2.17 與 0.1.0-W3-207 Phase 1 的兩處分歧：(A) 請求識別的產生方——§2.17 欄位表定為仲裁點受理當下產生的 process 內單調遞增整數，Phase 1 2.2 定為請求方提供的 String id（與 176 配對識別同源，仲裁器不生成）；(B) 決策欄值域——§2.17 五值且明寫「拒絕」不使用，Phase 1 決策集為 Accepted／Skipped／Rejected／Deferred（取自方法論卸載順序表）。需明寫兩者為不同觀測點的對應關係（§2.17 五值屬請求方判準 T 的結局，Phase 1 四值屬仲裁層動作）或修訂其一
  - why: Phase 2 測試設計逐條核對 §2.17 七項欄位表與測試義務裁決表時發現；§2.17 已明寫關聯機制屬 207 Phase 1 範疇，故測試依 Phase 1 設計，但 §2.17 欄位表措辭未隨 Phase 1 定案更新，後續讀者會以 §2.17 為準誤判 Phase 1 違規。另 §2.17 第 5 項（仲裁器自身溢出）記適用，Phase 1 第 9 節以無佇列改為靜態檢查承接，措辭差異一併對齊
  - suggested_type: DOC
  - suggested_priority: P2
  - related_files: docs/spec/ui/SPEC-003-interaction-response.md
  - context: Phase 2 Solution 第 11 節分歧 A、B；第 6 節第 5 項
  - status: processed（已建 0.1.0-W3-270）

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-10T14:52:30
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-184,0.1.0-W3-203 | chars: 1844 -->

### Task Reference
- 0.1.0-W3-184 what: 定義 SPEC 設計時的前台背景分配與事件流跨 domain 協調要求（來源已完成，結論見 Solution）
- 0.1.0-W3-203 what: 補寫 domain-map 的注意力軸：哪些 domain 產生注意力請求與仲裁點落層（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-184 why: **用戶更正（2026-09-09）**，起因是 PM 把提示密度問題框成「先問這些提示時刻是否都必要」：

> 這樣也不完全正確。如果需要提示使用者的訊息，表示有他的重要性，那**有重要性的動作，不應該同時大量執行**，或者真正有重要問題發生的時候，**應該要暫停背景的行為**，所以我才提到有的提示訊息要擋住使用者的操作。
>
> 那重點是當我們設計 spec 的時候，也要評估**有哪些行為會在背景執行，哪些動作會在前台跟使用者互動**，是不是有些動作要**避免同時請求使用者的關注或者指令**，或者會造成互動影響的 SPEC 在設計的時候，**整個事件流就應該包含跟其他 domain 確認過再執行，或者應該加入延遲的動作**。

**PM 原本的框架把提示當成雜訊，這是錯的。** 提示需求本身是重要性訊號——需要提示使用者，表示該事件有重要性。因此提示密度過高不是「提示太多」，是**同時執行了太多重要的事**。

**這使 `0.1.0-W3-167` 的處置範圍必須收窄。** 在提示層做排隊、優先級或載體分化，是幫一個排程問題做視覺化緩衝——症狀被吸收，成因原封不動，而且緩衝做得越好，成因越不可能被發現。167 處理「留下來的提示怎麼呈現」，本票處理「為什麼會有這麼多同時發生的重要事件」。

**本專案已有具體實例。** 破洞掃描掛在 `GapReportNotifier.build()`，首讀者是 App 啟動時的 `ScanNotificationController.start()`（`0.1.0-W3-122` 的主題）——使用者尚未看過破洞報告頁，掃描就已在背景完成並試圖請求通知授權。這正是「背景行為在前台注意力尚未就緒時請求使用者關注」，而現行 SPEC-003 §2.2 的觸發條件只判斷「可見頁是不是 gaps」，不判斷「現在是不是請求使用者注意的適當時機」。

編輯能力（`0.1.0-W3-073`）落地後前台互動會大幅增加，此時若背景仍有掃描、載入、git 操作各自獨立觸發，注意力競爭會從偶發變成常態。**現在定義比事後補容易。**（來源已完成，結論見 Solution）
- 0.1.0-W3-203 why: `0.1.0-W3-184` 第三節推得：**協調圖不是依賴圖**。`docs/domain-map.md` §2 的 8 條依賴邊描述資料方向且單向向下；注意力競爭發生在任何兩個會產生注意力請求的 domain 之間，**不分層級**。

**具體反例已存在**：`Diagnostics`（L2）與 `Workspace`（L0）之間無依賴邊，但兩者都會產生注意力請求（破洞掃描完成、資料夾不可用），因此在注意力軸上相鄰——**這條關係在現行 domain-map 上完全查不到**。

**不寫則後果**：讀者會用依賴圖去推導協調關係，而依賴圖對此無鑑別力。設計新 SPEC 時會漏掉不相鄰層之間的注意力競爭，且漏掉的方式沒有訊號——依賴圖查起來是「無關係」，看起來像已確認過。（來源已完成，結論見 Solution）

### Related Files
- docs/domain-map.md::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/usecases::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- .claude/methodologies::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/domain-map.md::write  # from 0.1.0-W3-203（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 注意力競爭的避免規則已定義，含「同時」的操作性含義與依重要性的優先序來源  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 事件流的跨 domain 協調要求已定義，含哪些事件屬會造成互動影響、確認對象、確認形態、延遲判準  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 重要問題發生時暫停背景行為的規則已定義，含何謂重要問題、暫停範圍、恢復條件  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 產出落點經裁定，跨專案通則與本專案 domain 對應關係分開落位  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 與 0.1.0-W3-167 的分工已明寫；若本票結論使 167 的某些處置不再需要，已通知其執行者  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
- [ref] [x] 8 個 domain 逐一判定是否產生注意力請求，判準引用方法論而非另立  # from 0.1.0-W3-203（來源已完成，結論見 Solution）
- [ref] [x] 注意力軸相鄰對已列出，且明寫與 §2 依賴圖正交（含 Diagnostics/Workspace 這組跨層實例）  # from 0.1.0-W3-203（來源已完成，結論見 Solution）
- [ref] [x] 仲裁點落層已指定，且說明為何不得落入任何 domain 內部  # from 0.1.0-W3-203（來源已完成，結論見 Solution）
- [ref] [x] 級別定義與截斷規則未重述，以標題文字引用方法論  # from 0.1.0-W3-203（來源已完成，結論見 Solution）
