---
id: 0.1.0-W3-097
title: 修復 scan_notifier MethodChannel 原生端未註冊，實機拋 MissingPluginException
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-078
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 實機平台呼叫全面失敗
  final_decision: IMP 修原生端 channel 註冊時機
  rationale: channel 未註冊使 W3-064 整個系統通知功能在實機零可用；fake 測試不經過真實 channel 故無訊號
who:
  current: parsley-flutter-developer
  history: {}
what: 修復 scan_notifier MethodChannel 原生端未註冊，實機拋 MissingPluginException
when: 2026-09-08 實機驗證由 DevTools Logging 觀測到（W3-078 執行期間）
where:
  layer: Infrastructure
  files:
  - macos/Runner/AppDelegate.swift
  - macos/Runner/MainFlutterWindow.swift
why: "實機執行時，`MacosScanNotifier` 的第一次授權查詢即失敗。DevTools Logging 實測記錄（2026-09-08 15:12:51.441，`Level.WARNING (900)`）：\n\n    查詢授權狀態失敗，原生端未註冊 handler（視為 denied）：\n    MissingPluginException(No implementation found for method authorizationStatus\n    on channel graph_project_docs_manager/scan_notifier)\n\n**`macos/Runner/AppDelegate.swift` 的 `MethodChannel` 從未被註冊。** 該檔在 `applicationDidFinishLaunching` 內以\n\n    guard let controller = mainFlutterWindow?.contentViewController as? FlutterViewController else { return }\n\n取得 controller 後才建立 channel——guard 失敗時**靜默 return**，channel 不建立，且不留任何訊號。實測結果顯示這個 guard 確實失敗了（需確認 `mainFlutterWindow` 在該時點是否已由 NIB 載入完成，屬 macOS 啟動時序問題）。\n\n後果：`0.1.0-W3-064` 實作的整套系統通知功能——授權查詢、授權請求、發送、撤回、點擊回傳——**在實機上零可用**。全部四個方法都會拋 `MissingPluginException`；Dart 端依 SPEC-003 §2.2 的「其他結果視為 `denied`」把它收斂掉，因此 App 不崩潰、畫面正常，只是通知永遠不會出現。\n\n**為何 1159 個測試全綠**：`test/` 用 `FakeScanNotifier` 替換整個 `ScanNotifier`，從不經過真實 `MethodChannel`。這正是 `0.1.0-W3-074` 判定「跨邊界功能只有 fake 覆蓋、平台端事實零覆蓋」所指的缺口，本案是它的第一個實例化證據。\n\
  \nDart 端的降級處理（收斂為 `denied` 並記 WARNING）設計正確——沒有它，這個缺陷會是崩潰而非靜默。但也因為降級太乾淨，缺陷在實機上唯一的痕跡就是那一行 log，而該 log 不出現在 `flutter run` console（`developer.log` 只進 VM service 的 Logging stream），必須開 DevTools 才看得到。"
how:
  task_type: Implementation
  strategy: '先確認 guard 失敗的實際原因，不要直接改成強制解包：以 log 或中斷點確認 `applicationDidFinishLaunching` 執行當下 `mainFlutterWindow` 與其 `contentViewController` 的狀態。


    修復方向（擇一並論證）：把 channel 註冊移到 window 確實就緒的時點（例如 `MainFlutterWindow.awakeFromNib` 之後，或 `applicationDidFinishLaunching` 內以 window 就緒為條件延後執行）。


    **guard 的靜默 return 必須一併處理**：現行寫法在失敗時什麼都不做，缺陷因此只能由 Dart 端的 `MissingPluginException` 間接暴露。依 quality-baseline 規則 4（Hook 失敗必須可見）的同一精神，原生端取不到 controller 時應記錄可見的錯誤，不得靜默返回。


    補一個能攔截本形態的驗證：`test/` 的 fake 路徑無法覆蓋真實 channel，需要 `integration_test` 或啟動時自檢（例如 App 啟動後主動查詢一次授權狀態，收到 `MissingPluginException` 時以可見方式回報而非降級為 `denied`）。降級與缺陷偵測的邊界須在 Solution 論證：SPEC-003 §2.2 要求「其他結果視為 denied」是為了畫面層的行為確定性，不等於「接線錯誤也該靜默」。


    修好後回頭讓 `0.1.0-W3-078` 的四項平台事實可被實測。'
acceptance:
- '[ ] 實機執行時 authorizationStatus 不再拋 MissingPluginException，DevTools Logging 可見真實授權結果'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修復 scan_notifier MethodChannel 原生端未註冊，實機拋 MissingPluginException

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-078 | chars: 524 -->

### Task Reference
- 0.1.0-W3-078 what: 實機驗證 MacosScanNotifier 四項平台事實並回填 SPEC-003

### Rationale Chain
- 0.1.0-W3-078 why: W3-064 acceptance 2、3 承接，平台事實只有實機能驗

### Related Files
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W3-078
- lib/services/macos_scan_notifier.dart::read  # from 0.1.0-W3-078
- macos/Runner/AppDelegate.swift::read  # from 0.1.0-W3-078

### Source Acceptance (reference)
- [ref] [ ] 實機測得 UNUserNotificationCenter 在沙盒關閉（PROP-001）+ Developer ID 簽章下的可用性，並記錄 debug build 未簽章時授權請求的實際回應（是否直接回錯）  # from 0.1.0-W3-078
- [ref] [ ] Flutter 端載體的選擇已定案並附實測依據（原生 channel 或第三方套件，二擇一）  # from 0.1.0-W3-078
- [ref] [ ] 實機測得 macOS 上 AppLifecycleState 的 inactive / hidden 各對應何種視窗狀態（失焦／最小化）  # from 0.1.0-W3-078
- [ref] [ ] 實機測得撤回已送達通知的 API，以及它對「通知中心已收合」狀態的效果  # from 0.1.0-W3-078
- [ref] [ ] 四項實測結果已回填 SPEC-003 §2.2「實作票驗證」段（原文改為實測記錄，附量測環境與日期）  # from 0.1.0-W3-078
  ... (truncated, see source ticket)

