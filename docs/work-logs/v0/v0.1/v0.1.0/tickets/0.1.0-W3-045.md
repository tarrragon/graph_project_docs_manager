---
id: 0.1.0-W3-045
title: 建立 承接 worklog-format-check 與 comment-qa-hook 的專案規範 skill
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children:
- 0.1.0-W3-045.1
- 0.1.0-W3-045.2
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-041
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-041 三方定案為不恢復、另立
  final_decision: 成 IMP 票，由本方持有版號
  rationale: 本專案與 flutter_balance 是實際執行者，版號歸屬應與執行者一致
who:
  current: main
  history: {}
what: 把兩個 hook 自 compositional-writing 搬出，另立由本專案持有版號的 skill，並將路徑改為可設定、架構文件引用改為可省略
when: 本票建立後可執行；完成後通知 flutter_balance 與廢除方
where:
  layer: framework
  files:
  - .claude/skills/doc-flow/hooks/worklog-format-check.py::write
  - .claude/skills/doc-flow/SKILL.md::write
  - .claude/skills/doc-flow/CHANGELOG.md::write
  - .claude/skills/dart-comment-traceability/hooks/comment-qa-hook.py::write
  - .claude/skills/dart-comment-traceability/SKILL.md::write
  - .claude/skills/dart-comment-traceability/CHANGELOG.md::write
  - .claude/skills/compositional-writing/hooks/comment-qa-hook.py::write
  - .claude/skills/compositional-writing/hooks/worklog-format-check.py::write
  - .claude/settings.json::write
why: 三方定案（W3-041）：兩個 hook 的主題不屬於 compositional-writing。查證發佈庫七支帶 hooks 的 skill，每一支的 hook 都是該 skill 自己的主題，唯一例外正是被刪掉的這一支。決定性依據是版號由不跑這兩個 hook 的一方持有，恢復等於固化兩個作者一個版號——而那正是本次落差的產生方式。搬離後共用價值完全保住而版號歸屬清楚，寫它的人也是跑它的人
how:
  task_type: Implementation
  strategy: 決定形態（兩支各自成家或合為一支專案規範 skill）-> 取本專案與 flutter_balance 兩份副本中較新者 -> 建 skill 目錄與 SKILL.md -> 路徑改為可設定、架構文件引用改為可省略 -> 更新 settings.json 四處註冊指向新位置 -> 驗證兩個 hook 仍正常執行 -> skill-sync push
acceptance:
- '[x] 兩個 hook 已遷至新 skill，settings.json 四處註冊指向新位置且執行正常'
- '[x] worklog-format-check 的作用域可設定，comment-qa-hook 對架構文件的引用可省略（兩個 consumer 皆無該文件）'
- '[x] 新 skill 已推發佈庫，且 compositional-writing 不再持有這兩個檔'
- '[x] 已通知 flutter_balance 與廢除方新 skill 的名稱與位置'
- '[x] 兩個 hook 落在逐字指定的路徑：doc-flow/hooks/worklog-format-check.py 與 dart-comment-traceability/hooks/comment-qa-hook.py（hook 檔名維持原名，改名會使下游票的 claim 前提落空）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-07T13:32:45'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

把兩個 hook 自 compositional-writing 搬出，另立由本專案持有版號的 skill，並將路徑改為可設定、架構文件引用改為可省略

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 形態與命名定案（2026-09-07）

**形態：兩支各自成家，不合併。** 採納 flutter_balance 的理由——「兩支各一個 hook 顯得零碎」
不是成本，**零碎正是那個不變式的形狀**（發佈庫七支帶 hooks 的 skill，每支的 hook 都只服務
自己的主題）。合成一支「專案規範 skill」會再造一個承載兩個主題的 skill，與被刪的那支同構。

| 檔 | 落點 | 依據 |
|----|------|------|
| `worklog-format-check.py` | **併入既有 `doc-flow`**，成為其 `hooks/` 的第一支 | 本側查證：`doc-flow` 的 description 明寫管 worklog 與 documentation consistency checks，正文提及 work-log 四次，且目前**無 `hooks/` 目錄**。主題相符，滿足七比零不變式 |
| `comment-qa-hook.py` | **獨立成家，命名 `dart-comment-traceability`** | 依既有 `dart-style-guardian`／`dart-test-async-guardian` 的 `dart-` 前綴慣例 |

**不選 `doc` skill**：該 skill 的主題是需求追蹤文件（PROP／SPEC／UC），與 worklog 格式無關。
本側同時存在 `doc` 與 `doc-flow` 兩支，前者管需求文件、後者管專案文件系統（CHANGELOG／
worklog／ticket／error-pattern／todolist），worklog 屬後者。

**`dart-` 前綴的邊界（記錄以免日後被七比零判為違規）**：該 hook 支援 `.dart` 與 `.js`／`.ts`，
但實質偏重 Dart（該檔內 dart 出現 26 次、typescript／javascript 合計 7 次，全檔 889 行），
且兩個 consumer 皆為 Flutter 專案。**若日後 JS/TS 分支成為實質負擔，該分支應拆出而非改名**——
改名會讓 `dart-` 家族失去一致性，而拆分符合同一個不變式。

**檔案來源：取 flutter_balance 的副本。** 其 `W3-1198` 把 `PROJECT_ROOT` 由 cwd-relative 改為
`get_project_root()`，屬 IMP-BAL-019 那族（shim 的 cwd 是 skill 目錄）的修復；本側版本在
shim 下 hook-logs 會寫錯位置。**取用前兩項確認**：本側該檔無未提交的本地改動（先 diff）；
該側 `W3-699.1` 把 `.claude/lib` 的 import 改為選用性降級，搬入新 skill 時該段須保留。

**下游待辦**：新 skill 落地後通知 flutter_balance，該側會 spawn 一張 IMP 把其 settings.json
四處註冊改指向；本側四處註冊亦須同步改。

### 落點路徑逐字鎖定（下游票的 claim 前提，不可更動）

flutter_balance 的 `0.2.1-W3-1254`（改指向）把 claim 前提寫成兩項 AND：本方通知落地
canonical、該側 sync-pull 後兩檔存在於**逐字指定的路徑**。

```
.claude/skills/doc-flow/hooks/worklog-format-check.py
.claude/skills/dart-comment-traceability/hooks/comment-qa-hook.py
```

**兩點須留意**：

1. **hook 檔名維持原名不改。** 新 skill 名為 `dart-comment-traceability` 而其中的 hook 仍是
   `comment-qa-hook.py`——skill 名與 hook 名不一致是刻意的，目的是讓兩側 `settings.json`
   的改動僅限路徑前綴，不動檔名。**改檔名會使該側的 claim 前提落空。**
2. **`doc-flow/hooks/` 目前不存在**，須新建該目錄（該側已核實，本側亦查證過）。

### 落地通知的協議

該側要求**通知時附 canonical commit hash**，他們以 hash 核對後才 pull。本票完成後的通知
須包含：新 skill 名稱與位置、兩個逐字路徑、**canonical 的 commit hash**。

該側現況（其核實值）：`doc-flow/` 無 `hooks/`；`dart-*` 家族現有 4 支而
`dart-comment-traceability` 尚不存在；其 `settings.json` 四處註冊仍指向
`compositional-writing/hooks/`（L681／L690／L745／L759）。

**本側的四處註冊亦須在本票內同步改指向**——該側只改自己那四處。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: basil-hook-architect 或 thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-041,0.1.0-W3-045.1 | chars: 1307 -->

### Task Reference
- 0.1.0-W3-041 what: 裁決 comment-qa-hook.py 與 worklog-format-check.py 在本地保留、還是同步廢除；並確認 push compositional-writing 時不會把上游已刪的檔案回吐（來源已完成，結論見 Solution）
- 0.1.0-W3-045.1 what: 把 worklog-format-check.py 移入 doc-flow/hooks/，作用域由寫死 docs/work-logs/ 改為可設定，並更新 settings.json 對應註冊（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-041 why: W3-033 反向盤點發現：發佈庫已於 2026-08-27（commit 563e9fc，訊息為刪掉已廢除的兩個 hook）移除這兩檔，canonical 與本地仍保留。本地 settings.json 有 4 處註冊且 hook-logs 有 2026-09-03/04 的執行記錄，對本專案並未廢除。分歧本身無害，但下一次從本專案 push compositional-writing 會把兩檔重新送回發佈庫，覆蓋另一 consumer 的移除決定，且該覆蓋不會有任何提示（來源已完成，結論見 Solution）
- 0.1.0-W3-045.1 why: 該 hook 緩解表格單元格 emoji 造成的 Claude Code CLI crash，主題為 worklog 的工具相容性。doc-flow 的 description 明寫管 worklog 與 documentation consistency checks 且目前無 hooks 目錄，主題相符。作用域寫死 docs/work-logs 是它在廢除方環境被判不適用的唯一原因，改為可設定後三個 consumer 各指自己的路徑（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/compositional-writing/hooks/comment-qa-hook.py  # from 0.1.0-W3-041  [路徑已不存在]（來源已完成，結論見 Solution）
- .claude/skills/compositional-writing/hooks/worklog-format-check.py  # from 0.1.0-W3-041  [路徑已不存在]（來源已完成，結論見 Solution）
- .claude/settings.json  # from 0.1.0-W3-041（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 兩個 hook 的去留有明確裁決與依據，非憑另一 consumer 的廢除訊息推斷  # from 0.1.0-W3-041（來源已完成，結論見 Solution）
- [ref] [x] 若裁決保留，已確認下次 push compositional-writing 時的處置方式  # from 0.1.0-W3-041（來源已完成，結論見 Solution）
- [ref] [x] 檔案落在 .claude/skills/doc-flow/hooks/worklog-format-check.py（檔名不可改，下游票以此為 claim 前提）  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- [ref] [x] 作用域可設定且預設值為 docs/work-logs/，hook 實測觸發正常  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- [ref] [x] settings.json 該 hook 的註冊已指向新位置，compositional-writing 側原檔已刪  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- [ref] [x] 本 session 已實地觸發 doc-flow/hooks/worklog-format-check.py 並以 hook-liveness 確認落檔（本 session c1525670 內共 677 筆記錄，最近一筆於遷移完成後產生）  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
- [ref] [x] liveness 驗證方式：ticket track hook-liveness <hook 路徑> 讀取 hook 自身 logger 持久化記錄並依 session 聚合  # from 0.1.0-W3-045.1（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
