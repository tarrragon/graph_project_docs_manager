---
id: 0.1.0-W1-009
title: 確認 i18n 資源覆蓋 0.1 所需文案 key
type: IMP
status: pending
version: 0.1.0
wave: 1
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 地基波四塊的第一塊；元件庫的文字須經 i18n key 傳入
  final_decision: 本專案 0.0.1 已建 ARB 機制，本票只補齊 0.1 所需的 key，不重建機制
  rationale: component-library 方法論規定四塊依序完成才進測試設計，i18n 為第一塊；機制已存在時本塊退化為覆蓋率確認，但不可略過——元件庫若先建、文字寫死字面，之後每個元件都要回頭改
who:
  current: parsley-flutter-developer
  history: {}
what: 確認 i18n 資源覆蓋 0.1 所需文案 key
when: 與 W1-004 token 萃取可並行，皆為元件庫的前置
where:
  layer: Presentation
  files:
  - lib/l10n/app_zh.arb
  - lib/l10n/app_en.arb
why: component-library-bidirectional-constraint-methodology 的地基波 build 順序規定 i18n 為第一塊，元件庫的文字須經 i18n key 傳入而非寫死字面。SPEC-001 的 29 個狀態含大量文案（空狀態訊息、阻擋說明、載入提示），這些 key 目前不存在。方法論另明訂四塊未完成不得開啟測試設計 Ticket。
how:
  task_type: Implementation
  strategy: 自 SPEC-001 §1-7 逐狀態盤點所需文案，於 app_zh.arb 與 app_en.arb 補齊 key 並執行 gen-l10n。命名依既有慣例。不改動 ARB 機制本身。
acceptance:
- '[ ] SPEC-001 §1-7 每個狀態的顯示欄文案皆有對應 i18n key（zh 與 en 皆備）'
- '[ ] fvm flutter gen-l10n 成功且 lib/l10n/app_localizations*.dart 已更新入庫'
- '[ ] 既有 test/ 與 integration_test/ 維持綠燈'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-08-27'
updated: '2026-08-27'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

確認 i18n 資源覆蓋 0.1 所需文案 key

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## Context Bundle

### 派發環境（PM 於 2026-08-27 在 fresh worktree 實測，非推論）

本票以 `isolation: "worktree"` 派發，工作目錄是全新 checkout。三項已驗事實：

| 項目 | 量測 | 結果 |
|------|------|------|
| FVM 可用性 | `cd <worktree> && fvm flutter --version` | Flutter 3.47.1 / Dart 3.13.1，exit 0 |
| 依賴安裝 | `fvm flutter pub get` | Changed 71 dependencies，成功 |
| 測試 baseline | `fvm flutter test` | **All tests passed，8 項** |

`.fvm/` 是 gitignored 的 symlink，fresh worktree 沒有它——**不需要補**，`fvm` 讀入庫的 `.fvmrc`（釘 3.47.1）即可解析。這一點已實測，不要花時間重建 symlink 或改用系統 flutter。

`.dart_tool/` 同樣不在版控，**動任何 build 前先跑 `fvm flutter pub get`**。

`lib/l10n/app_localizations*.dart` 是刻意入庫的生成產物，fresh worktree 已有，不需先跑 gen-l10n 才能編譯。

**baseline 是綠的**。開工後若見既有測試紅燈，那是本次改動造成的，不是環境噪音——不要歸因於並行資源耗盡（該誤判有前例，見 `IMP-APP-003`）。

### PM 對 dispatch-readiness 兩項警告的覆核

| 警告 | 裁決 |
|------|------|
| 閾值 1：acceptance 條目 > 2，建議拆分 | **不拆**。該閾值以 acceptance 條目數近似「功能職責數」，本票的條目是同一件工作的多個驗收面向，非多個職責。`where.files` 與 Context Bundle 兩項硬閾值皆 PASS |
| 檢查 4：acceptance 提測試但 `where.files` 無測試路徑（僅 W1-001 / W1-009） | **啟發式誤報**。「既有測試維持綠燈」是回歸防護，不是要求新增測試。若你判斷本票確實需要新增測試檔，**停手並以 `ticket track append-log <id> "..." --section NeedsContext` 上報**，不要逕自寫到 `where.files` 之外 |

### 收尾注意（worktree 專屬）

收尾用 `ticket track finish <id> --as parsley-flutter-developer`，**不要用 `complete`**——worktree isolation guard 對 argv 逐元素比對 basename，`complete` 命中 bash builtin 而被條件性阻擋。`finish` 是等價別名。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending
