---
id: 0.1.0-W3-180
title: 驗證 framework issue 103 的 bash-edit-guard 涵蓋缺口修復是否抵達
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: framework issue 103 依升級流程路徑 A 建立，需要驗證票承接修復抵達後的確認
  final_decision: 建 IMP 驗證票，trigger 為 sync-pull 帶回含 fix-version 的版本
  rationale: 本 hook 的修復含誤判成本取捨，抵達後必須同時驗證擋住了該擋的與沒誤傷合法用途，讀原始碼不足以確認
who:
  current: thyme-python-developer
  history: {}
what: 驗證 framework issue 103 的 bash-edit-guard 涵蓋缺口修復是否抵達
when: sync-pull 帶回含 fix-version 的框架版本後
where:
  layer: Infrastructure
  files:
  - .claude/hooks/bash-edit-guard-hook.py
why: 本專案已於 tarrragon/claude#103 記錄 bash-edit-guard 模式 A 只偵測 sed -i 與 perl -pi 四種樣式，直譯器內嵌腳本、heredoc 寫檔、重導向覆寫全數穿透，且命中後只警告不阻擋。實證為同一工作日內兩個代理人收到同一則 harness 改檔指令，一個遵守框架規則、一個以 python3 heredoc 改寫 markdown，後者手段正落在缺口內。原分析票依 framework-issue 升級流程路徑 A 已 close，本票是該路徑要求的驗證票——issue ref 與 sync-pull 都不是合法 trigger，合法 trigger 只有 ticket。
how:
  task_type: Implementation
  strategy: 以 sync-pull 帶回的版本實測缺口是否封閉：對受管路徑各發一次 python3 內嵌腳本寫檔、heredoc 寫檔、重導向覆寫，確認 hook 有反應且反應等級符合 issue 的裁定結果（警告或阻擋）。同時實測合法用途未被誤傷：產生器腳本輸出至暫存路徑、唯讀 Bash 應全數放行。若修復採路徑範圍化，須各測受管與非受管路徑一次。實測結果不論通過與否都以 observe 附回 issue。
acceptance:
- '[ ] python3 內嵌腳本寫入受管路徑時 hook 有反應，且反應等級與 issue 裁定一致'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

驗證 framework issue 103 的 bash-edit-guard 涵蓋缺口修復是否抵達

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 2026-09-10 抵達驗證：未抵達

**上游狀態**：`tarrragon/claude#103` 仍為 OPEN（updatedAt 2026-09-09T04:54:46Z），無 fix-version。

**本地事實**（`.claude/hooks/bash-edit-guard-hook.py`）：

| issue 記錄的缺口 | 本地查證（行號） | 結論 |
|---|---|---|
| 模式 A 只認 sed -i 與 perl -pi | 檔頭說明 15-17 行與實作 84-89 行仍為兩條 pattern；57 行明寫「原始 6 個 pattern 中保留 2 個高風險原地編輯模式」 | 未修復 |
| 直譯器內嵌腳本穿透 | 全檔無 `python3 -c` / `python -c` / `node -e` 類 pattern | 未修復 |
| heredoc 寫檔與重導向覆寫穿透 | heredoc 相關程式碼（106、211 行）用途為裸 cd 的偽陽性抑制，非偵測 heredoc 寫檔 | 未修復 |
| 命中僅警告不阻擋 | 29 行仍為「模式 A ... 輸出警告訊息（permission_decision=allow），允許繼續執行」 | 未修復 |

**附帶佐證**：本 session 於同日觸發該 hook 的裸 cd 分支（模式 B），確認 hook 本身在執行中，故上述缺口非「hook 未載入」所致。

**處置**：票保留於 pending，不 close。acceptance（python3 內嵌腳本寫入受管路徑時 hook 有反應）在無對應 pattern 時必然不滿足，不需另跑實測。trigger 不變，下次 sync-pull 後重驗。


### 2026-09-11 復核（thyme-python-developer）：同意 PM 判定，並以實測正向對照探針補強

**復核結論**：同意 2026-09-10 判定（未抵達）。PM 原判定依讀原始碼推論「無對應 pattern 故必然
不觸發」；本次以實際 Bash 呼叫做正向對照探針補強為實測結論（`test-assertion-design-rules`
規則 E2：放行與「輸入本來就沒問題」在輸出上同形，僅實測正向對照輸入可分辨）。

**探針設計**：全數寫入自建暫存檔（scratchpad，非既有專案檔案），不觸碰任何專案內容。

| 探針 | 命令形態 | hook 反應 |
|---|---|---|
| 1. python3 內嵌腳本 | `python3 -c "open('...','w').write(...)"` | 無 `additional_context`；`bash-edit-guard-20260911.log` 記錄「允許: 正常 Bash 命令」 |
| 2. heredoc 寫檔 | `cat > file <<'EOF' ... EOF` | 同上，無反應 |
| 3. 一般重導向覆寫 | `echo ... > file`（連續兩次） | 同上，無反應（**此為 hook docstring 明載的刻意降級決策**，見 `bash-edit-guard-hook.py:57-69`「移除模式」——3 天 1662 次 Action 實測不可逆風險 ~0% 後主動移除，非本 issue 所指的意外缺口） |
| 4. 正向對照組 | `sed -i '' 's/.../.../' file` | **有反應**：`additional_context` 正確送達（「[Bash Edit Guard] 警告: 偵測到使用 Bash 進行檔案編輯操作」），證明 hook 掛載中且非全域失效 |

探針 1、2、4 的結果與 PM 原判定完全一致：hook 機制本身持續執行（探針 4 證實），但對 python3
內嵌腳本與 heredoc 寫檔兩種手法**無任何 pattern 涵蓋**，屬選擇性偵測缺口而非掛載失敗。

**本地可做工作（不依賴上游）**：
- 找到本專案既有 `.claude/hooks/bash-write-boundary-guard-hook.py` 已實作
  `_OPEN_CALL_PATTERN = re.compile(r"open\(\s*(['\"])([^'\"]+)\1")`，可偵測 python 內嵌腳本／
  heredoc 中 `open('path', ...)` 呼叫的技術，同倉庫已有可運作先例。惟其涵蓋範圍窄化於「主線程
  對 `test/`、`lib/`、`*.dart` 的職責邊界」（見該檔頭 8-19 行），且明確排除 subagent 環境
  （`is_subagent_environment()` 命中即跳過），不涵蓋 issue #103 描述的一般受管路徑（如
  `.claude/` 本身、ticket `.md`）與 subagent 執行者，故不能視為 issue #103 已被此檔間接涵蓋。
- 由於 (a) `bash-edit-guard-hook.py` 與 `bash-write-boundary-guard-hook.py` 皆屬 `.claude/`
  框架同步資產、本地修改會被下次 sync-pull 覆蓋，(b) 票面約束不對上游倉庫寫入，本票不將此
  技術佐證提交至 issue，僅記錄於票面供 PM 後續決定是否併入下次 observe。
- 確認 `.claude/settings.local.json` 為 gitignored 且未受版控（`git ls-files` 無回傳），理論上
  可承載一份僅供本機生效的補充 hook 註冊；但因僅單機生效、不隨版控傳播給其他 session/機器，
  屬個人暫時性繞道而非團隊可靠的專案級防護，未在本票逕行建立，留待 PM 裁決是否值得作為過渡
  措施。
- 未發現其他獨立於 `.claude/hooks/` 之外、能攔截 Bash PreToolUse 事件的專案級掛載點
  （hook 註冊集中於 `.claude/settings.json`，同屬框架同步範圍）。

**查證指令**：
```
python3 -c "open('<scratchpad>/probe-w3-180.txt','w').write('probe test line 1\n')"
cat > <scratchpad>/probe-heredoc.txt <<'EOF' ... EOF
echo ... > <scratchpad>/probe-redirect.txt
sed -i '' 's/sanity/PROBE/' <scratchpad>/probe-sedi.txt   # 正向對照
tail -n 20 .claude/hook-logs/bash-edit-guard/bash-edit-guard-20260911.log
```
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
