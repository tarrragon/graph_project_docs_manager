---
id: 0.1.0-W3-077
title: 修正 引用穩定性規則 8 的識別符類型表判準
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 跨 consumer 協作中對方指出本方規則同源缺口，本方查證屬實
  final_decision: 成 DOC 票 P2
  rationale: 屬框架規則判準錯置非單次疏失，修法涉及 references 層依 PC-053 建票
who:
  current: rosemary-project-manager
  history: {}
what: 規則 8 的識別符類型表以識別符類別判定可否在框架文件引用，改為以是否已到達共用通道判定；並補 consumer 分域編號這一類
when: 本票建立後可執行
where:
  layer: framework
  files:
  - .claude/references/reference-stability-rules.md
why: 該表列「框架 error-pattern ID -> 是（框架內部分類）」，判準是識別符的類別而非它是否已在共用通道上。表中範例全為無 consumer 後綴的舊編號，該表早於 consumer 分域編號而未隨之更新。後果：一個本專案自建、尚未推上共用通道的分域編號，形式上就是框架 error-pattern ID，照此表會被判為可引；寫進會隨同步擴散的框架檔案後，落到其他 consumer 手上即為懸空引用，而寫的人查證過該編號真實存在，不會覺得漏了什麼。缺口在跨 consumer 協作中由對方指出，本專案自行查證屬實。正確判準見 PC-GPD-006 的反方向小節：跨 consumer 引用的合法性取決於被引物是否已到達共用通道，不取決於它是否真實存在；且送達與否才是判準，歸屬哪一層不是——共用層已建但未推的編號同樣不可引。
how:
  task_type: Documentation
  strategy: 在識別符類型表的 error-pattern 列加上通道條件（已到達共用通道者可引，尚未到達者僅本地票面可引）-> 補一列涵蓋 consumer 分域編號並給範例 -> 於規則 8 內文加一句說明判準是送達而非歸屬層，含已建未推的時間窗 -> 與 PC-GPD-006 的三態表交叉引用，避免兩處判準分歧
acceptance:
- '[x] 識別符類型表的 error-pattern 列判準改為通道條件，非僅類別'
- '[x] 表中補 consumer 分域編號一類並給範例，涵蓋已建未推的情形'
- '[x] 規則 8 內文明寫判準為送達與否而非歸屬哪一層'
- '[x] 與 PC-GPD-006 反方向小節的三態表交叉引用且兩處判準一致'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T13:58:53'
completed_at: '2026-09-08T14:00:52'
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

規則 8 的識別符類型表以識別符類別判定可否在框架文件引用，改為以是否已到達共用通道判定；並補 consumer 分域編號這一類

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-08T14:00:52
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
