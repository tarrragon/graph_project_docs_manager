---
id: 0.1.0-W3-106
title: 更新 SPEC-004 全部元件條目的回饋契約格式：補掛載點、失敗路徑與等待期政策
type: DOC
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children:
- 0.1.0-W3-106.1
- 0.1.0-W3-106.2
- 0.1.0-W3-106.3
- 0.1.0-W3-106.4
blockedBy:
- 0.1.0-W3-108
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-099
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 更新 SPEC-004 全部元件條目的回饋契約格式：補掛載點、失敗路徑與等待期政策
when: 0.1.0-W3-105 定案服務類型與等待期政策後（與掛載點、失敗路徑合為一次更新，避免 42 條目改兩遍）
where:
  layer: Presentation
  files:
  - docs/spec/ui/SPEC-004-component-library.md
why: '`0.1.0-W3-099` 逐件盤點時發現：SPEC-004 §4 既有元件條目的回饋契約**語意上正確、格式上落後**。


  正確的部分：各條目的「等待與結果」列均寫「本元件承載三層中的點擊確認層；等待指示為畫面級／結果通知為狀態轉換本身」——這與用戶的三層裁示一致，比方法論當時的條文還準確（方法論的歸屬錯誤已由 `0.1.0-W3-102` 修正）。


  落後的部分有兩項：


  1. **無顯式「掛載點」欄位**。三層歸屬目前藏在「等待與結果」列的敘述裡，讀者要讀完整句才知道該元件承載哪一層；`0.1.0-W3-101` 已在 `doc` 範本的回饋契約子節加了掛載點填寫要求，既有條目與新範本因此形狀不一致。

  2. **「等待與結果」列未明列失敗路徑**。`0.1.0-W3-098` 定案的失敗路徑覆蓋要求四段（成功／失敗／逾時／服務不可用）同處呈現，既有條目只寫成功結局。


  兩項都是格式差異而非語意錯誤，因此不緊急；但格式不一致會使 `0.1.0-W3-101` 加進〈契約齊全的定義〉的機械可查判準對既有條目全數判為不齊全——判準與存量不一致時，執行者要嘛忽略判準（防護失效），要嘛逐次手動豁免（成本累積），兩者都不可持續。


  **本票 blockedBy `0.1.0-W3-105`** 的理由：該票正在設計服務類型與等待期政策（回饋形式／輸入阻擋範圍／重複觸發防護），其結論同樣要落進 SPEC-004 的元件條目。兩批合為一次更新，避免全部元件條目改兩遍。'
how:
  task_type: Documentation
  strategy: '先讀三份定案來源，再動 SPEC-004：方法論〈掛載點〉三層歸屬表（`0.1.0-W3-102`，commit `38e92dab`）、`doc` 範本的回饋契約子節（`0.1.0-W3-101`）、`0.1.0-W3-105` 的服務類型與等待期政策結論。三者不一致時停手上報，不自行選邊。


    逐條目更新回饋契約子節：


    1. **掛載點顯式化**：把現在藏在敘述裡的三層歸屬提為明確欄位或固定句首，使讀者不必讀完整句即知該元件承載哪一層。既有敘述語意正確，是改呈現形式不是改結論——**原判斷不得被改寫**，若某條目的既有敘述與三層歸屬表衝突，記入 Solution 交 PM，不自行改判。

    2. **失敗路徑四段**：「等待與結果」列改為成功／失敗／逾時／服務不可用四段呈現。多數元件把後三層委派給呼叫端，四段內容可能重複——依方法論〈失敗路徑覆蓋〉的規定寫「同 X」，不得留空或省略該段。

    3. **等待期政策**：依 `0.1.0-W3-105` 的結論補對應欄位。該票若判定政策屬畫面層而非元件層，本項改為在元件條目標明委派對象，不強塞進元件契約。


    **體量提醒**：SPEC-004 的元件條目數量可觀，逐條目更新易在中途走樣。建議分批進行並在 Completion Info 記錄批次邊界；單次 session 內做不完時，於 Solution 明記已完成到哪一條，不要留下半更新狀態而不標示。


    **不寫數字**：變更歷史與敘述不以條目數描述進度（`reference-stability-rules` 規則 10），以條目名或章節範圍描述。'
acceptance:
- '[ ] 全部元件條目的回饋契約子節有顯式掛載點呈現，讀者不需讀完整句即知承載哪一層'
- '[ ] 「等待與結果」列為成功／失敗／逾時／服務不可用四段；重複者寫「同 X」不留空'
- '[ ] 等待期政策依 0.1.0-W3-105 結論補入：逐元件標註所屬服務類型、輸入阻擋範圍與重複觸發防護（承接已關閉的 0.1.0-W3-109）'
- '[ ] 既有敘述的語意判斷未被改寫；發現與三層歸屬表衝突者記入 Solution 交 PM，未自行改判'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

更新 SPEC-004 全部元件條目的回饋契約格式：補掛載點、失敗路徑與等待期政策

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-099,0.1.0-W3-105 | chars: 2068 -->

### Task Reference
- 0.1.0-W3-099 what: 盤點 既有可互動元件的回饋掛載點，判定哪些需調整（來源已完成，結論見 Solution）
- 0.1.0-W3-105 what: 設計 服務類型與等待期政策對照：回饋形式、輸入阻擋範圍、重複觸發防護

### Rationale Chain
- 0.1.0-W3-099 why: `ARCH-GPD-001` 定案的判準——**回饋掛在互動元件上，與背後服務能否運作無關**——是新確立的，既有元件建立時沒有這條約束。用戶裁示（2026-09-08）：既有的所有互動元件都要檢討是否需調整。

盤點範圍：`lib/components/` 現有 43 個元件中，含 `onTap` / `onPressed` / `onChanged` / `onAction` / `InkWell` / `GestureDetector` 者共 25 個（PM 已 grep 確認）：`app_shell`、`app_button`、`app_snack_bar`、`app_table_row`、`list_row`、`expander_icon`、`filter_dropdown`、`blocked_state`、`load_prompt`、`matrix_grid`、`document_body`、`loading_state`、`issue_marker`、`nav_item`、`missing_source_state`、`matrix_cell`、`project_switcher_entry`、`recent_project_item`、`relation_item`、`search_field`、`segmented_control`、`switcher_overlay`、`swimlane_node`、`tree`、`table_column_header`。SPEC-004 的對應條目皆已有回饋契約子節，但那些子節是在舊判準下填的。

不逐件檢討的後果：新判準只約束新元件，存量元件維持「回饋掛在服務回應上」的形態，而存量正是使用者實際會碰到的部分。今天的實例（按下重新掃描完全沒反應）就出自存量元件，不是新設計的。

**這不是逐件重寫回饋契約**。多數元件的互動可能根本不呼叫服務（純本地狀態切換），對它們判準自動成立；需要調整的是那些互動會觸發跨層呼叫、而回饋定義依賴該呼叫回應的元件。先分類才知道實際工作量。（來源已完成，結論見 Solution）
- 0.1.0-W3-105 why: 現行回饋契約回答的是「怎麼告知使用者」，不回答「按下之後到結果之前這段時間，系統怎麼保護自己與使用者」。用戶於三層歸屬裁示後補充三項（2026-09-08），三項都以**服務類型**為自變數：

1. **不同類型的服務應提供哪些回饋提示** —— 並明指這是**測試驗收的一個指標**，故須可機械檢查，不能只是敘述性建議。
2. **等待期間是否需要阻擋使用者的其他操作、屏蔽畫面輸入** —— 這與提示是兩件事：提示是「告知」，阻擋是「防止」。目前規格兩者都沒有依服務類型的規則。
3. **按鈕類互動元件的防抖** —— 且明確裁示**防抖由服務提供，不是元件提供**。

第 3 點的歸屬與三層模型一致，且推出一條重要不變式：**元件在每一次按下都必須發出第一層回饋，即使服務因防抖而丟棄該次呼叫**。若防抖做在元件層，防抖窗口內的按下會沒有 ripple，使用者看到的是一個沒反應的按鈕——正是 `ARCH-GPD-001` 的形態，只是成因從「服務不可用」換成「服務刻意不執行」。元件永遠回饋，服務決定要不要真的執行。

現況：SPEC-003 §2.1 只有 `Motion.searchDebounce`（300 ms，搜尋輸入用），沒有按鈕類的防抖規則，也沒有規定它實作在哪一層；「阻擋」一詞在 SPEC-003 全文只用於「阻擋狀態元件」（畫面狀態的一種），與「等待期阻擋輸入」不是同一件事，目前後者無任何條文。

### Related Files
- lib/components::read  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-004-component-library.md::read  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
- .claude/error-patterns/architecture::read  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-105
- .claude/methodologies/component-library-bidirectional-constraint-methodology.md::read  # from 0.1.0-W3-105

### Source Acceptance (reference)
- [ref] [x] 25 個可互動元件逐件歸類（A 無需調整／B 已符合／C 需調整／D 承載者不明）並附理由  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
- [ref] [x] SPEC-004 對應條目的回饋契約子節與程式碼的一致性逐件記錄（規格對實作錯／實作對規格未寫／兩者皆未涵蓋分開列）  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
- [ref] [x] C 類與 D 類依調整形態分組並以 add-spawn-request 登記，complete 前逐項 resolve 為終態  # from 0.1.0-W3-099（來源已完成，結論見 Solution）
- [ref] [ ] 服務類型分類以使用者感知的等待長度與失敗形態為軸，每類附「政策確實不同」的切分理由  # from 0.1.0-W3-105
- [ref] [ ] 逐類型定回饋提示形式、輸入阻擋範圍、重複觸發防護三項政策；阻擋整畫面者必須同時提供取消  # from 0.1.0-W3-105
  ... (truncated, see source ticket)
**PM 補充（阻塞對象已改，2026-09-08）**

**`blockedBy` 由 `0.1.0-W3-105` 改為 `0.1.0-W3-108`。**

105 已完成，形式上本票已解除阻塞——但直接依 105 的 ticket Solution 更新 SPEC-004 會製造規格層的分歧風險。105 是分析票，其結論要先經 `0.1.0-W3-108` 寫進 SPEC-003 才成為規格。若本票先跑，SPEC-004 的元件條目會沿用 105 ticket 內的類別名稱，而 108 之後在寫 SPEC-003 條文時可能收斂或改名——兩份規格於是各自持有一組類別名，且兩邊各自讀起來都合理，沒有人會發現不一致（`DOC-005` 跨文件原則失步的形態）。

**規格層的單一來源是 SPEC-003，不是 105 的 ticket。**本票 acceptance 第 3 條的「依 0.1.0-W3-105 結論補入」請讀作「依 108 落進 SPEC-003 的條文補入」——結論同源，但引用的載體是規格不是票。

**`how.strategy` 的「先讀三份定案來源」相應更新為**：方法論〈掛載點〉三層歸屬表、`doc` 範本的回饋契約子節、**SPEC-003 的服務類型與等待期政策條文**（原寫「0.1.0-W3-105 的結論」）。三者不一致時停手上報的要求不變。

**〈掛載點〉已於今日新增一行**（commit `1e708bf2`）：明示該節三層是輸入端（driving port）特化，輸出端（driven port）有結構平行、消費者不同的對應形態。對本票的影響有限——SPEC-004 的元件多屬輸入端——但若盤點時遇到元件條目描述的是往外呼叫的行為，其三層歸屬應查 `.claude/methodologies/clean-architecture-implementation-methodology.md` 的〈Port 回饋契約〉而非〈掛載點〉。以標題文字引用，不寫 URL fragment。

**類別名稱視為跨檔錨點**：本票會逐字沿用 SPEC-003 定的類別名。選名時避開衍生值（`DOC-V1-001` 第三實例），並且不要把類別數量寫進條目或標題（`reference-stability-rules.md` 規則 10）——本票要改的元件條目數量可觀，一旦數量寫死，日後增減元件會讓全部條目的敘述同時失效。

---

**PM 補充二：類別名稱的錨點形態已由 0.1.0-W3-108 落地確認，不再是 PM 傾向（2026-09-09）**

上一則補充寫「類別名稱視為跨檔錨點來選……選名時避開衍生值」，當時標的是 PM 傾向。**108 已落地（commit `da8fa720`，SPEC-003 v1.10 → v1.11），其執行者獨立做出同一判斷並寫進規格**，本項因此升格為已確認的事實，執行 106 時不需重新判斷。

**108 的兩項結構決策，皆為本票的直接前提**：

1. **類別名為語意名，不含羅馬數字序號。** 執行者拒絕沿用 `0.1.0-W3-105` 的 `I. 本地即時` ～ `IV. 長時操作` 形式，理由是序號屬位置衍生值、類別增減即失效，直接援引 `reference-stability-rules.md` 規則 10。**本票逐字沿用的錨點因此是語意名，不是序號。**
2. **新章編為 SPEC-003 §2.12 而非插入 §2.3。** 理由是整體後移會使既有的 `SPEC-003 §2.x` 形式跨檔引用全數失效（並行 session 實查：§2.10 單獨 102 處，全部 §2.x 共 335 處）。**本票在 SPEC-004 引用等待期政策時，指向的是 §2.12。**

**執行時仍須實查而非採信本節行號**：SPEC-003 是活文件，§ 編號與類別名以動手當下的實際內容為準。本節記的是**形態決策**（語意名、不整體後移），那部分已定案不會變；具體的編號與名稱字面請以 `rg` 實查。

**與 acceptance 第 3 條的關係**：該條寫「依 `0.1.0-W3-105` 結論補入」，上一則補充已更正為「依 108 落進 SPEC-003 的條文補入」。此處再補一層——**105 的類別名（帶羅馬數字）已被 108 明確否決，若沿用會與規格不一致。**
