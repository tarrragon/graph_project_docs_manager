---
id: 0.1.0-W3-317
title: 審查 元件庫相鄰四 skill 的正文可讀性與互指對稱性
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-302
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶裁示 component-contract-design 的相鄰四 skill 須做同樣的探針審查與可讀性調整，必要時補連結
  final_decision: 四 skill 各跑探針批次；三項硬違規（超標零 references、兩處 description 超長、孤兒 reference）不等探針先處置
  rationale: 互指矩陣已靜態量出三處單向斷裂與一個零入站 reference，問題不是假設；只修 component-contract-design 一端等於在交界處重新製造同一形態
who:
  current: rosemary-project-manager
  history: {}
what: 審查 元件庫相鄰四 skill 的正文可讀性與互指對稱性
when: 隨時
where:
  layer: Presentation
  files:
  - .claude/skills/foundation-design/SKILL.md
  - .claude/skills/ux-design-evaluation/SKILL.md
  - .claude/skills/dart-style-guardian/SKILL.md
  - .claude/skills/version-bootstrap/SKILL.md
why: '`component-contract-design` 已於 `0.1.0-W3-296`／`0.1.0-W3-302` 走完探針審查與依步驟重切，其〈與相鄰資產的交界〉表指名四個相鄰 skill。用戶裁示：**這四個相鄰 skill 要做同樣的審查，確保同樣的脈絡與可讀性，必要時補連結。**


  **派發前的靜態量測已顯示問題不是假設**：


  互指矩陣（各 SKILL.md 內指名對方的次數）嚴重不對稱——`component-contract-design` 指向其餘四者（3／3／1／2），但 `ux-design-evaluation`→foundation／dart／version 為 0／0／0，`dart-style-guardian`→foundation／ux／version 為 0／0／0，`version-bootstrap`→foundation／ux／dart 為 0／0／0。其中兩處是明確的單向斷裂：`foundation-design` 五處寫「交回 `version-bootstrap`」而後者零處提及前者；`foundation-design` 的載體表列「執法工具」一類、工作流第 5 步要求「接入執法載體」，卻從未指名 `dart-style-guardian`。


  體量硬違規兩處：`version-bootstrap` SKILL.md 5132 tokens **超過 L2 的 5000 上限且零 references**；`ux-design-evaluation` description 989 字元，為 250 字元上限的四倍（`dart-style-guardian` 260 字元亦略超）。


  孤兒 reference 一處：`dart-style-guardian/references/i18n-guidelines.md`（1507 tokens）在該 skill 的 SKILL.md 內入站連結數為 **0**，無任何管道把讀者導向它。


  **不做則後果**：這四個 skill 是元件庫程序的上下游。讀者從 `component-contract-design` 被導過去之後，落在一份沒有〈按需讀取〉路由、沒有交界節、且指不回來的正文裡——`component-contract-design` 這次修好的正是這個形態，只修一端等於在交界處重新製造它。`version-bootstrap` 的超標尤其不能用搬運處理：它零 references 且正文含三件不同觸發條件的事（6 步 pipeline、移版硬耦合盤點 SOP、與早期手動流程對照），依 SRP 應先判斷是否該拆而非硬搬。


  **為何 P1**：`component-contract-design` 已推送在即（尚未 push，且需帶 `--prune`），交界處的不對稱會隨它一起擴散到其他 consumer 專案。'
how:
  task_type: Analysis
  strategy: '沿用 `multi-round-review` 的 **2-B⁗ 低階 model 讀者探針**，四個 skill 各跑一批，逐條依既定紀律：


    1. **派發前寫死門檻與切換條件**，不得事後訂（事後訂會被「這次寫得更清楚了」蓋過去）。

    2. **重點涵蓋度差集的作者意圖清單須在探針回報前登記**，否則分母被回溯填補、差集塌成空集。

    3. 同批內指令**逐字相同**、無審查框架、明令禁止臆測填補；判準取**分歧的分布**而非任一份報告。

    4. 規則類文件加問「照這一節工作，我的第一個具體動作是什麼」。

    5. **歸因先於處置**：判「讀者讀錯」之前先 grep 原文對應字串，分三類（文章自己這樣寫／表述有歧義／模型的問題，第三類須舉出排除材料）。

    6. **一份讀錯就是判決**；收斂度只決定證據強度，不決定是否處置。

    7. 改檔前等該批全部回報；連續兩輪 2/3 以上命中即處置。


    **已知的三項硬違規不必等探針**，它們是量測出的事實不是可讀性判斷：`version-bootstrap` 超標且零 references（先依 SRP 判斷拆 skill 或拆檔，不得為達標而刪判斷條件）、兩處 description 超 250 字元、`i18n-guidelines.md` 零入站連結。


    **互指補連結以矩陣為驗收**：修完後重跑互指矩陣，單向斷裂歸零；但**不得為對稱而對稱**——每一條新增的互指都要說明它讓讀者在哪個決策點少走一步，說不出來的不補。


    本票**不改 `component-contract-design` 本體**（它已完成處置，另有 `0.1.0-W3-302.1` 的第四輪複驗未跑），不跑 `sync-push` 或 `skill-sync push`。'
acceptance:
- '[ ] 四個 skill 各完成一批探針量測，門檻與作者意圖清單於派發前登記'
- '[ ] 每項處置皆先做歸因三分類，判為模型問題者已舉出排除材料'
- '[ ] version-bootstrap 超標已依 SRP 判定為拆 skill 或拆檔並落地，未以刪判斷條件達標'
- '[ ] 兩處超長 description 已收斂至 250 字元內且觸發詞未流失'
- '[ ] i18n-guidelines.md 已有入站路由，或已判定不需要並記錄理由'
- '[ ] 互指矩陣重跑，單向斷裂歸零，且每條新增互指皆附「讓讀者在哪個決策點少走一步」'
- '[ ] 處置後複驗，連續兩輪 2/3 以上命中的項目皆已處置'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T16:58:21'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

審查 元件庫相鄰四 skill 的正文可讀性與互指對稱性

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

`component-contract-design` 這次修好的是「內容拆分造成的閱讀割裂」：正文跳躍、references 無閱讀引導、讀某一節時不知道還有另一節。它的〈與相鄰資產的交界〉表指名四個相鄰 skill——若只修一端，讀者被導過去之後落回同一形態，等於在交界處重新製造它。

### 影響範圍（派發探針前的靜態量測）

**互指矩陣**（各 SKILL.md 內指名對方的次數）：

| 從＼到 | component | foundation | ux-design | dart-style | version-boot |
|--------|-----------|-----------|-----------|-----------|-------------|
| component-contract-design | — | 3 | 3 | 1 | 2 |
| foundation-design | 1 | — | 1 | **0** | 5 |
| ux-design-evaluation | 1 | **0** | — | **0** | **0** |
| dart-style-guardian | 1 | **0** | **0** | — | **0** |
| version-bootstrap | 1 | **0** | **0** | **0** | — |

兩處是明確的單向斷裂，不是「剛好沒提到」：

- `foundation-design` 五處寫「交回 `version-bootstrap`」「本 skill 的產物交給 `version-sequencing` 排版本」，`version-bootstrap` 零處提及 `foundation-design`。下游不知道有上游會把東西交過來。
- `foundation-design` 的載體表列「執法工具」一類、工作流第 5 步要求「機械檢查接入執法載體」、Common Issues 有一列專講「執法載體裝了卻從沒擋下違規」——全程未指名 `dart-style-guardian`。讀者被要求接一個沒有名字的東西。

**體量硬違規**：

| 對象 | 實測 | 上限 | 情況 |
|------|------|------|------|
| `version-bootstrap/SKILL.md` | 5132 tokens | 5000 | 超標，且該 skill **零 references** |
| `ux-design-evaluation` description | 989 字元 | 250 | 超標近四倍 |
| `dart-style-guardian` description | 260 字元 | 250 | 略超 |

**孤兒 reference**：`dart-style-guardian/references/i18n-guidelines.md`（1507 tokens）在該 skill SKILL.md 內入站連結數為 **0**。同 skill 的另三份 reference 各有 1 條。它不是「連結寫得不好」，是沒有任何管道把讀者導向它。

**超 5k 的 reference**：`ux-design-evaluation/references/interaction-feedback.md` 6087 tokens。

### 相關 Error Pattern

`DOC-GPD-009`（對表述歧義加警告而非改句子結構）——本票處置時的直接風險：補一句「另見 X」比改結構便宜，但歧義出在成分層、警告出在段落層，修改的可見度會替代修改的有效性。

`PC-GPD-024`（變更記錄照修改計畫寫而非照 diff 寫）——本票四個 skill 同批修改，改善不得歸給批次裡的任一條處置。


### 更正：description 超標不是「未被發現」，是已凍結的既有債

上文把兩處 description 超標列為硬違規，語氣暗示它們未被察覺。查證後更正：

`.claude/hooks/skill-description-baseline.json`（產生於 2026-09-08）已凍結 **22 個 skill** 的 description 超標值，`ux-design-evaluation: 989` 與 `dart-style-guardian: 260` 都在其中。`skill-description-length-check-hook.py` 的 `WARNING_THRESHOLD = 250` 只對 baseline 以外的新增違規告警——這正是 `foundation-design` 自己工作流第 5 步寫的做法（「首次接入執法載體必然大量失敗：設 baseline 凍結既有違規、只擋新增，再逐批收斂」）。

**這改變兩件事**：

1. **事實層**：這兩項是 22 項待收斂清單中的兩項，不是漏網。原本的寫法會讓讀者以為執法層失效，實際上執法層正照設計運作。
2. **處置層**：本票只收斂這兩項（它們落在本次審查範圍內），**修完必須同步移除 baseline 中的對應條目**，否則 baseline 留著一個比實際值大的凍結值，等於替未來的回退預先簽名。其餘 20 項不在本票射程，不順手改。

`i18n-guidelines.md` 零入站連結與 `version-bootstrap` 超標零 references 兩項未在任何 baseline 內，仍為未被任何機制發現的缺口。
## 重現實驗結果
### 實驗方法

**本節於派發探針之前寫定，內容不得於探針回報後修改。** 事後訂門檻會被「這次寫得更清楚了」蓋過去；事後補作者意圖清單會使分母被回溯填補、重點涵蓋度差集塌成空集。

**批次形態（2-B⁗）**：每個 skill 一批、每批 3 個低階 model 實例，同批內指令逐字相同，不含任何審查框架（不告訴它們這是在找問題），明令禁止臆測填補（不知道就寫不知道）。判準取分歧的分布，不取任一份報告。

**共同提問（五問，逐字相同）**：

1. 照這份文件工作，我的第一個具體動作是什麼？
2. 這份文件要我產出什麼？
3. 有哪些情況這份文件叫我去別的地方？各是哪裡？
4. 讀完之後，我還不知道但需要知道的是什麼？
5. 這份文件與哪些其他 skill 有分工關係？各自的界線在哪？

第 1 問對應規則類文件的「第一個具體動作」要求；第 5 問是本票特有，直接量測互指斷裂在讀者端的後果。

### 門檻（派發前寫定）

| 命中率 | 處置 |
|--------|------|
| 3/3 答不出或答錯 | 必處置 |
| 2/3 | 處置 |
| 1/3 | 記錄，複驗再現（連續兩輪 2/3 以上）才處置 |
| 重點涵蓋度：作者意圖清單中 0/3 提及者 | 必處置 |

**歸因先於處置**：判「讀者讀錯」之前先 grep 原文對應字串，分三類——(a) 文章自己這樣寫的、(b) 表述有歧義、(c) 模型的問題。判為 (c) 者須舉出排除 (a)(b) 的材料。

**一份讀錯就是判決**；收斂度只決定證據強度，不決定是否處置。

### 作者意圖清單（派發前登記，供重點涵蓋度差集用）

**foundation-design**（8 項）
1. 這是路由層與單一入口，不重新定義任何維度的判準
2. 逐維度回答「本專案的產物是什麼」，權威只提供預設產物
3. 產物欄四種合法答案：照預設／改寫產物／已存在於 X／無
4. 權威缺席時填「待定」，**不得填「無」**
5. 判準問作用不問存在，且查證對象是版控內容不是工作目錄
6. 接手模式的核心程序：盤點 → 命名 → 固化 → 補文件
7. 四種情境由上而下先命中者適用；「規劃波進行中」要交回 `version-bootstrap`
8. 三份 references 各自在什麼時候讀

**ux-design-evaluation**（5 項）
1. 四個核心支柱（每狀態有退出／每關卡有替代／回饋即時誠實／設計決策成品化）
2. 事前六步流程；事後依症狀路由
3. 六份 reference 各對應哪一類症狀
4. 只到畫面級狀態，元件級契約由 `component-contract-design` 承接
5. 每份 reference 自包含，可不讀 SKILL.md 獨立套用

**dart-style-guardian**（6 項）
1. 它只抓「不該怎麼做」，正面做法由 `component-contract-design` 定義
2. 五類約束：色／間距／字級／圓角／i18n
3. 六種常見違規與各自修法
4. 偵測腳本怎麼跑、以及 hook 整合
5. Project Calibration 在什麼情況下使用
6. 四份 references 各自在什麼時候讀

**version-bootstrap**（6 項）
1. 六步 pipeline 的順序與各步 checkpoint
2. 每步是 PM 確認才前進，不是全自動
3. UI 版本要跑 Step 4.5 地基波四塊，其中含 3.5 元件契約
4. Step 2 的 UI 前置三項檢查（design token 層／L3 元件庫章節／design-system spec）
5. 移版硬耦合盤點 SOP 的觸發條件
6. Step 2.5 domain map 與 Step 2.6 資料契約各被哪一步消費

### 不必等探針的三項

以下是量測出的事實不是可讀性判斷，先處置：

1. `version-bootstrap` 5132 tokens 超標且零 references——**先依 SRP 判定拆 skill 或拆檔，不得為達標而刪判斷條件**。
2. 兩處 description 超 250 字元（989／260）——收斂時觸發詞不得流失。
3. `i18n-guidelines.md` 零入站連結——補路由或判定不需要並記錄理由。

### 實驗執行

（探針派發後填寫）

### 實驗發現

（探針全數回報後填寫）


---

## 實驗執行：第一輪（12 個探針，4 批 × 3）

`dart-style-guardian` 與 `version-bootstrap` 兩批已滿 3/3，以下為這兩批的判定。`foundation-design` 與 `ux-design-evaluation` 兩批各有回報在傳輸中截斷，已要求原樣重貼，判定待補。

### dart-style-guardian（3/3）

| 問題 | 三份的答案 | 命中 | 歸因 |
|------|-----------|------|------|
| Q1 第一個具體動作 | A：讀 `.claude/config/dart-style-guardian.json` 校準檔／B：「文件沒有明確指定第一個動作」，猜 scan 但「順序不確定」／C：執行 `style_checker.py scan lib/` | **3/3 不一致** | **(a) 文章自己這樣寫的** |
| Q2 產出什麼 | A：「文件沒有明確說」／B：報告與摘要，但「沒有規定格式」／C：「報告」，但「文件沒有寫格式」 | **3/3 答不出** | (a) |
| Q5 與其他 skill 的分工 | A：「只明確提到 1 個」／B：「文件沒有明確提及與其他 skill 的分工」／C：「只有一個明確的分工關係」 | **3/3 只找到 `component-contract-design`** | (a) |
| 重點涵蓋度：四份 references 何時讀 | 三份**全部只列出 color／spacing／typography** | **i18n-guidelines.md 0/3** | (a) |

**Q1 的歸因查證**：對該 SKILL.md grep `第一|起手|first|開始前|步驟 ?1|workflow`，**零命中**。三個讀者給出三個不同的第一動作，是因為文件沒有指定任何一個——這是 (a) 不是 (c)，排除模型問題的材料就是這個零命中。

**孤兒 reference 的交叉驗證**：靜態量測（SKILL.md 內入站連結 0）與探針量測（3/3 未提及）在同一項上一致，且全 repo 掃描確認 `i18n-guidelines.md` 除本次工作的 hook log 外**無任何入站引用**，對照組 color／spacing／typography 各有 3 處。1507 tokens 的內容無管道可達。

**一項意外**：i18n 在正文中**確實有一節**（`## Internationalization (i18n)`，與 Color／Spacing／Typography／Border Radius 並列為約束類別），但〈Reference Files (in this SKILL)〉的清單只列三份。所以缺的不是規則、是路由——正文講了 i18n 的規則，卻沒有任何一處把讀者導向那份寫得更完整的 reference。

### version-bootstrap（3/3）

| 問題 | 三份的答案 | 命中 | 歸因 |
|------|-----------|------|------|
| Q5 與其他 skill 的分工 | 三份都列 `doc`／`spec validate`／`tdd`／`ticket`／`component-contract-design` + 方法論 | **`foundation-design`／`ux-design-evaluation`／`dart-style-guardian`／`version-sequencing` 0/3** | (a)，與互指矩陣量測一致 |
| 重點涵蓋度：移版硬耦合盤點 SOP 的觸發條件 | A 明問「何時執行移版硬耦合盤點 SOP（只有 Step 1 檢查出矛盾時，還是任何改版都要過）」／B、C **完全未提及該 SOP 存在** | **0/3 正確掌握** | (a) |
| 「每步 PM 確認才前進，不是全自動」 | A 明問「執行後的互動方式（自動進行或手動逐步推進）」與「『全自動』vs『半自動』的具體含義」／C 問「Checkpoint 的確切觸發方式」／B 未提 | **2/3 讀不出** | **(b) 表述有歧義** |
| Step 4.5 四塊含 3.5 元件契約 | A、B 列出元件契約／C 列「i18n／design-system／UX 反應規格／元件庫實作」**漏 3.5** | 1/3 漏 | 記錄，待複驗 |

**「全自動」歧義的歸因查證**：同一份文件內——

- 行 32：「每步有 checkpoint（PM 確認後才進下一步），**不是全自動 pipeline**」
- 行 38：「### Step 1：列出提案清單（**全自動**）」

相隔六行，同一個詞指兩件事：前者指「整條 pipeline 不會自己跑完」，後者指「這一步不需要人填內容」。其餘七步標「半自動」，而文件從未定義這兩個標籤。這是 (b) 成分層的歧義，不是讀者的問題——**處置必須改詞或加定義，加一句警告不會解消它**（`DOC-GPD-009`）。

**移版 SOP 的位置問題**：該 SOP 是一份 50 行、觸發條件與 6 步 pipeline 完全不同的獨立程序，卻放在 Step 6 之後、與〈反應式工作〉並列的位置，正文中沒有任何一處在「什麼時候會用到它」的脈絡下指向它。三個讀者裡兩個根本沒看見它存在——這與作者意圖清單第 5 項的差集是 0/3。
## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-302 | chars: 440 -->

### Task Reference
- 0.1.0-W3-302 what: 複驗 component-contract-design 第二輪之後的三項修改是否生效（F7 第二次修法、判定起點補回、方法論改行內指路）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-302 why: 三項修改皆在第二輪探針之後才做，零外部量測。F7 尤其關鍵——該處已連續兩輪被命中，第一次修法（加粗體警告）看起來像修好了但複驗 3/3 仍命中，同一個自我感覺良好的路徑在第二次修法上完全可能重演（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/component-contract-design/SKILL.md  # from 0.1.0-W3-302（來源已完成，結論見 Solution）
- .claude/skills/component-contract-design/references/step-2-extract-components.md  # from 0.1.0-W3-302（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] F7 在 A′ 批的命中數已與第二輪（3/3）比對並下判定  # from 0.1.0-W3-302（來源已完成，結論見 Solution）
- [ref] [x] 判定起點節在 B′ 批疲勞點的命中數已與第二輪（3/3）比對並下判定  # from 0.1.0-W3-302（來源已完成，結論見 Solution）
- [ref] [x] 方法論在 C′ 批缺料清單的命中數已與第二輪（3/3）比對並下判定  # from 0.1.0-W3-302（來源已完成，結論見 Solution）
- [ref] [x] 第三輪新產出的 finding 已完成歸因並分為處置與不處置兩類  # from 0.1.0-W3-302（來源已完成，結論見 Solution）
