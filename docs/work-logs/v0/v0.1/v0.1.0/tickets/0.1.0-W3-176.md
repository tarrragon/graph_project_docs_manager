---
id: 0.1.0-W3-176
title: 補齊 AppSnackBar 元件層日誌的三項可稽核欄位
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-165
relatedTo: []
spawned_tickets:
- 0.1.0-W3-241
- 0.1.0-W3-242
source_ticket: 0.1.0-W3-167
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-167 第三問（不可見時的日誌補位）的實驗發現 F3／F4：165 落地後截斷仍留有三項不可稽核缺口
  final_decision: 以 IMP 票在既有 AppSnackBarLogSink 之上增補欄位，不改既有事件語意
  rationale: G1／G2 是 0.1.0-W3-167 規劃之頻率量測協定的前置——缺欄位則指標二（被截斷者是否為 withAction）與指標三（實際可見時長）皆取不到；G3 的觸發條件屬規格層，故本票只提供載體，條件留 0.1.0-W3-169
who:
  current: parsley-flutter-developer
  history: {}
what: 補齊 AppSnackBar 元件層日誌的三項可稽核欄位
when: 0.1.0-W3-165 合併後（本票在其產出的 AppSnackBarLogSink 之上增補欄位）
where:
  layer: Presentation
  files:
  - lib/components/app_snack_bar.dart
  - test/unit/components/app_snack_bar_test.dart
why: '`0.1.0-W3-167` 的確定性重現實驗（四組，`fvm flutter test`）證實 `0.1.0-W3-165` 落地後仍有三項可稽核缺口，使「程式主動決定不告知使用者」這件事無法被事後追究：


  | 缺口 | 現況 | 後果 |

  |------|------|------|

  | G1 訊息識別 | `closed` 事件的 `fields` 只有 `reason`，無 message 或關聯識別 | 無法回答「被截斷的是哪一則」。只能靠 `shown`／`closed` 的 FIFO 順序推斷，與其他 tag 的日誌交錯時不可靠 |

  | G2 實際可見時長 | `closed` 不帶「這則實際被看到多久」 | 實驗 F4：「一幀都沒顯示」與「顯示滿 1 秒後被截斷」的日誌逐字相同，而兩者對使用者的嚴重度不同 |

  | G3 未顯示的決定 | 因判準而不顯示的事件完全無事件 | 程式決定不告知使用者時零痕跡，該決定不可稽核 |


  實驗同時確立判別式：全 repo 呼叫 `hideCurrentSnackBar` 只有兩處（`app_snack_bar.dart:121` 取預設 `reason: hide`、`:166` 明確 `reason: action`），故 `closed{reason=hide}` 恆等於「被後續一次 `show` 截斷」。G1／G2 補齊後，此判別式才足以支撐 `0.1.0-W3-167` 規劃的頻率量測協定——否則量測拿不到「被截斷的是不是 withAction」與「使用者實際看到多久」兩項指標。'
how:
  task_type: Implementation
  strategy: '在 `0.1.0-W3-165` 既有的 `AppSnackBarLogEvent` / `AppSnackBarLogSink` 之上增補，不改既有事件語意，不改既有測試斷言形態（依列舉與結構化欄位，不比對散文字面）。


    1. G1：`shown` 與 `closed` 攜帶同一個關聯識別（每次 `show` 產生一個遞增序號或 token），使兩者可配對而不依賴 FIFO 假設。

    2. G2：`closed` 增加該則實際可見時長欄位。不得以真實時鐘量測作 pass-fail 斷言（test-assertion-design-rules 規則 D1）；測試以 `tester.pump` 假時鐘推進驗證欄位存在與相對關係，不驗絕對毫秒數。

    3. G3：新增一個「因判準而未顯示」的事件值，供呼叫端在決定不顯示時主動記錄。該事件的觸發條件由 `0.1.0-W3-169` 的規格條文定義，本票只提供事件載體與欄位；規格未定案前不得由本票自行決定哪些時機該記。


    順序限制：本票不得先於 `0.1.0-W3-165` 合併執行（會與其產出衝突）。'
acceptance:
- '[ ] shown 與 closed 事件攜帶可配對的關聯識別，配對不依賴 FIFO 假設'
- '[ ] 既有四個事件值的語意與既有測試斷言形態未被改動'
- '[ ] 截斷相關日誌的等級判別依據為事件發起者（使用者發起／背景發起），未以 variant 近似替代；元件內判定不到時由呼叫端傳參並於 Solution 記錄該選擇'
- '[ ] 重現 0.1.0-W3-167 實驗 A 與 B 兩組情境，兩者的日誌在補關聯識別後可區分'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補齊 AppSnackBar 元件層日誌的三項可稽核欄位

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 來自 0.1.0-W3-167 的更正：G3 事件的日誌等級不得以 variant 為鍵

`0.1.0-W3-167` 補記四第三節的更正，直接影響本票 G3 的實作。

**原本的判別依據已失效**。167 的 C1–C8 表曾把「`withAction` 被截斷」定為 warning，
理由是「損失的是操作能力（唯一入口消滅）」。PM 覆核實查後該理由不成立——
`rescanAction` 有 `action-gaps-rescan` 按鈕（`gap_report_screen.dart:87-92`，呼叫
同一個 `rescan()`，`app_localizations.dart:467` 註解明寫兩者共用文案）、
`viewGapsAction` 的目的地在導覽列（`router.dart:42`）。**動作入口皆有替代。**

**正確的判別依據是事件的發起者**：

| 發起者 | 損失可否被使用者自行察覺 | 等級 |
|-------|----------------------|------|
| 使用者發起（點破洞項 → `sourceFileNotFound` / `openedExternally` / `externalOpenFailed`） | 可——他剛做了動作，沒反應會注意到 | info |
| 背景發起（掃描完成 → `scanComplete*`） | 否——他沒做任何動作，截斷後無任何線索指出曾發生過什麼 | **warning** |

**以 `variant` 為鍵會同時誤判兩邊**：`sourceFileNotFoundSnackbarMessage` 是
`withAction` 但屬使用者發起（不需 warning）；日後若有背景發起的 `plain` 提示，
以 `variant` 為鍵會漏記。

**對本票的具體要求**：G3（「因判準而未顯示」事件）與截斷相關日誌的等級決策，
輸入須為**事件發起者**而非 `variant`。發起者可機械取得——呼叫來自使用者互動回呼
（`onTap` / `onPressed` 路徑）或背景 listener（`ref.listenManual` 路徑）。
本票若無法在元件內判定發起者，改由呼叫端以參數傳入，並於 Solution 記錄該選擇；
**不得以 `variant` 近似替代**。

G3 的觸發條件仍屬 `0.1.0-W3-169` 規格層（本票只提供載體），此更正只約束等級的
判別輸入，不改變該分工。


### 範疇拆分（PM，2026-09-09）

`dispatch-readiness` 閾值 1 判定強制拆分（acceptance 6 > 4）。依「`0.1.0-W3-205` 是否需要」與「是否需要不同的測試基礎設施」拆為三票：

| 缺口 | 承接票 | 理由 |
|------|-------|------|
| **G1 關聯識別** + 截斷日誌的等級判別依據 | **本票** | 兩者都是「讓截斷日誌回答『哪一則』與『多嚴重』」，同一條日誌線；G1 是 `0.1.0-W3-205` 的直接前置 |
| G2 實際可見時長 | `0.1.0-W3-241`（P2，blockedBy 本票） | `205` 不需要它；且需假時鐘測試基礎設施（規則 D1 禁絕對計時 pass-fail 斷言），與純欄位增補性質不同 |
| G3 因判準而不顯示的事件值 | `0.1.0-W3-242`（P3，blockedBy 本票） | 新增事件值而非既有事件的欄位；`SPEC-003` §2.13 明寫 0.1 落 (c) 的集合為**空集合**——是預留載體，不是修復當前可觀察的缺陷 |

三票皆改 `lib/components/app_snack_bar.dart`，故以 `blockedBy` 串成序列而非並行，避免同時改同一事件類別。**本票落地後 `0.1.0-W3-205` 即可解除阻塞**（其 `blockedBy` 中的 `0.1.0-W3-239` 已完成）。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-165,0.1.0-W3-167 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-167 what: 設計 提示元件的使用時機、重疊處置、類型分化，與不可見時的日誌補位
- 0.1.0-W3-165 what: 在 AppSnackBar 元件內部加入顯示日誌，使全部呼叫點的使用者可見回饋自動可觀測（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-167 why: **用戶裁示（2026-09-09）**：「也要額外評估一個問題，哪些時機要呼叫 snackbar 或者 bottoast 這類提示元件，**如果大量呼叫提示元件，反而會重疊遮蔽**，那有可能需要依照不同類型的提示需求，設計使用不同的提示元件，以及**如果提示元件看不到，我們要在哪些事件或者流程上要補 log**。」

`0.1.0-W3-166` 固化「有顯示就要有紀錄」，但不回答「何時該顯示」。兩者一起才完整：只有前者會導向「凡事都跳提示」，而提示互相爭用同一個顯示位時，**資訊反而比不提示更少**——後出現的取代前一則，使用者兩則都沒讀完。

**這不是假設性風險。** 本專案目前 `AppSnackBar.show(` 的呼叫點實查為 **3 處、2 個檔案**（`gap_report_screen.dart:199`、`:218`、`scan_notification_controller.dart:202`；`shell.dart` 只有註解提及，無呼叫）。而 `0.1.0-W3-113`（`ScanNotificationController.show` 失敗的 SnackBar fallback）與 `0.1.0-W3-165` 都會再增加觸發時機，編輯能力（`0.1.0-W3-073`）落地後還會更多——**呼叫點只會增加，所以此數為量測當下的值，接手者須自行複驗現值**（`reference-stability-rules` 規則 9）。

> **建票時的數字更正（2026-09-09）**：本段原寫「6 個呼叫點，分佈於三個檔案」。該值來自 `grep 'AppSnackBar.show\|AppSnackBar\.'`——第二個分支匹配任何成員存取（型別引用、列舉、註解），第一個分支的 `.` 為未跳脫的正則萬用字元，故量到的是「提及 `AppSnackBar` 的行數」而非呼叫點數。現值由兩個 session 各自以 `AppSnackBar\.show\(` 獨立實查並互相對上。另有一種口徑是「訊息結局」（同一呼叫點的分支各算一則）為 5 則，可並列參考，但 acceptance 第 2 項的對照對象是**呼叫點**。

**失效形態的更正見 Problem Analysis 的〈PM 前置實查〉第二節**：本專案的 `AppSnackBar.show` 在顯示前無條件呼叫 `hideCurrentSnackBar()`，且 `ScaffoldMessenger` 一次只呈現一則，故版面上的「重疊」不會發生，實際形態是**替換截斷**。用戶關切的後果成立且更嚴重（重疊時兩則都在畫面上，替換是後者把前者抹掉），但處置方向不同——分化元件解決的是「同時要看到兩則」，解決不了「被靜默丟棄」。

**第三個子問題是前兩者的必然對偶**：一旦決定某些時機不顯示提示，那些事件就失去了使用者可見的回饋，此時可觀測性必須由日誌承接——否則等於用「安靜」換掉了「有紀錄」，兩頭皆空。`0.1.0-W3-078` 的實機驗證正是撞上這個：路徑走到了、程式做了對的事，但沒有任何一側留下使用者層級的證據。
- 0.1.0-W3-165 why: `0.1.0-W3-078` 第二次實機驗證需觀測 `denied` fallback 的 SnackBar 是否出現，發現該路徑零日誌——SnackBar 出現與靜默失敗在日誌上完全同形，唯一分辨手段是人眼即時觀察，而 SnackBar 會自動消失、事後無法回溯。該次驗證因此未取得此項。

**用戶裁示（2026-09-09（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Related Files
- docs/spec/ui/SPEC-004-component-library.md::read  # from 0.1.0-W3-167
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-167
- .claude/skills/ux-design-evaluation/references/interaction-feedback.md::read  # from 0.1.0-W3-167

### Source Acceptance (reference)
- [ref] [ ] 何時使用暫態提示的判準已建立且可機械套用，輸入為事件性質而非逐案列舉  # from 0.1.0-W3-167
- [ref] [ ] 若結論為需要新提示元件，已說明與 AppSnackBar 的分界並走 component-contract-design 流程  # from 0.1.0-W3-167
- [ref] [ ] 因判準或遮蔽處置而不顯示的事件集合已逐類指定日誌承接點與等級，含被丟棄的提示  # from 0.1.0-W3-167
- [ref] [ ] 承接的規格撰寫已建 DOC 票或已登記 spawn request  # from 0.1.0-W3-167
- [ref] [ ] 全部 AppSnackBar.show( 呼叫點逐一對照該判準，不符者列出並給處置（派發前實查為 3 處／2 檔，接手者須自行複驗現值）  # from 0.1.0-W3-167
  ... (truncated, see source ticket)
