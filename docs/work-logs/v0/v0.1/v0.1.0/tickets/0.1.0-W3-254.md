---
id: 0.1.0-W3-254
title: 升級 十三張框架資產待派票至 framework issue
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 十三張待派票全為 .claude/ 框架資產，用戶裁示全部先過升級流程再決定本地是否實作
  final_decision: 按機制分六組逐組處理；每張逐條套兩條 AND 判準 + dedup + 關係判定後 observe 或 init
  rationale: 13 個概念超過認知負擔上限須分組；標題層比對已得候選但屬形狀相符，逐一驗證候選 issue 內容前不作結論（PC-GPD-019）
who:
  current: 主線程
  history: {}
what: 升級 十三張框架資產待派票至 framework issue
when: 用戶裁示全部先升級一張都不派後
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/
  - .claude/hooks/
  - .claude/agents/
  - .claude/references/framework-asset-separation.md
  - .claude/skills/framework-issue/scripts/link_issue.py
why: '十三張待派票全為 .claude/ 框架資產：190、191、192、194、195、198、199、171、150、152、141、142、211。


  用戶 2026-09-09 裁示「全部先升級，一張都不派」。依據是 09-08 的流程指正——框架問題應先查既有 issue 或建 issue，再開始修。該指正的成因是一次實際損失：一張本地票在未查重的情況下修了一個上游已知缺陷的第二條軌，本地修完後會被 sync-pull 以上游舊版覆蓋，且 canonical 端無任何記錄。


  兩條 AND 判準對十三張整體成立（逐張複核仍屬本票工作）：

  1. 抽象可攜——十三張的標的分別在 ticket CLI、hook 層、agent 定義、框架 references 與 framework-issue 腳本，替換專案名稱後論述皆成立

  2. 根源在 .claude/ 通用資產——十三張的 where.files 全數落在 .claude/ 下，隨 sync 分發至全部 consumer


  已取得全部 75 則 open issue 的標題索引，標題層比對顯示十三張各有候選承接 issue、零張需新建。但標題相符不等於該 issue 涵蓋該缺陷——此判定屬 PC-GPD-019「形狀相符終止查證」的典型觸發點，故候選須逐一讀 body 驗證後才可定案。'
how:
  task_type: Analysis
  strategy: '按機制分六組（13 個概念超過認知負擔拆分閾值）：


    | 組 | 票 | 機制 | 標題層候選（待驗證） |

    |----|----|------|--------------------|

    | G1 | 190、191 | ticket CLI 鎖與 dry-run 時序 | #15（*.lock 誤傷）、#55 |

    | G2 | 192、142、171 | ticket CLI 欄位與 body 處理 | #55（含 Context Bundle 品質區段）、#102 |

    | G3 | 194、198、141 | hook 層 | #37／#33／#70、#53、#92 |

    | G4 | 150、152 | agent 定義 | #73、#71、#72、#74 |

    | G5 | 199、211 | 框架 references 與 framework-issue 腳本 | #21（sync-pull 權限還原）、#95 |

    | G6 | 195 | 佔位符形態評估 | #33、#37 |


    每組流程固定四步：逐張套兩條 AND 判準 -> 讀候選 issue body 驗證涵蓋範圍 -> 判關係（重複／切分／引用）-> observe 或 init。


    驗證步驟不可省：標題相符只證明主題相鄰，不證明該 issue 的問題陳述涵蓋本票的缺陷。判定「重複」前必須讀到 issue 內實際描述同一機制的文字，讀不到就是「切分」或「引用」。


    本票不決定任何一張的本地實作與否——那取決於上游回應。十三張維持 pending，狀態不動。'
acceptance:
- '[ ] 十三張逐張記錄兩條 AND 判準的套用結果，非整體概括'
- '[ ] 每張的候選 issue 已讀 body 驗證涵蓋範圍，關係判定（重複／切分／引用）附引用依據'
- '[ ] 判定為重複者已 observe，判定需新建者已 init，兩者皆記錄 URL'
- '[ ] 十三張的 status 全部維持 pending，本票未變更任何一張'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-10T08:08:48'
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

升級 十三張框架資產待派票至 framework issue

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

十三張待派票積壓，成因不是排程，是它們全為 `.claude/` 框架資產而尚未過升級流程。用戶 09-08 的流程指正來自一次實際損失：一張本地票在未查重的情況下修了上游已知缺陷的第二條軌，本地修完會被 sync-pull 以上游舊版覆蓋，canonical 端無記錄。

十三張若照原樣派發，同一損失可能重演十三次。

### 影響範圍

十三張的標的分佈：ticket CLI（`ticket_system/` 五張）、hook 層（三張）、agent 定義（兩張）、框架 references（一張）、framework-issue 腳本（一張）、佔位符形態評估（一張）。全數隨 sync 分發至所有 consumer。

### 相關 Error Pattern

`PC-GPD-019`（形狀相符終止查證）。本票的方法設計直接受其約束：標題層比對顯示十三張各有候選承接 issue，但標題相符只證明主題相鄰。本票因此把「讀候選 issue 的 body 驗證涵蓋範圍」列為不可省的步驟，而該步驟當場推翻了一組候選（見 Solution 的 `#15` 段）。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending
