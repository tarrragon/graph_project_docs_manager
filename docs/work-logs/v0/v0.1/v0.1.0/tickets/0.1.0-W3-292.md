---
id: 0.1.0-W3-292
title: 分類 159 個日誌呼叫檔中 66 檔未判定的 root 來源
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-288
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-288 統整 issue 97 修法時，實測出承重點的風險面為 66 個 root 來源未判定的呼叫檔
  final_decision: 先分類再改 lib：本票只做判類，不改任何程式碼；分類結果決定選定方案是否需修形
  rationale: 同一形態的判準已失敗兩次（273 漏 4、288 偽陽 8/14），兩次皆是判準先於查證且錯誤與正確同形
who:
  current: framework-issue-curator
  history: {}
what: 分類 159 個日誌呼叫檔中 66 檔未判定的 root 來源
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/lib/hook_logging.py
  - .claude/lib/hook_base.py
  - .claude/lib/git_utils.py
why: '`0.1.0-W3-288` 選定的修法（把 root 綁進日誌入口，使 import 語句表達選擇，方案全文見 tarrragon/claude#97 comment 5629167716）有一個單一承重點：**每個會寫狀態的呼叫端，其 state root 來源是模組級的選擇，而非執行期才決定的值**。若某類呼叫端依 payload 內容決定操作哪個 root，import 綁定表達不出那個選擇，選定方案對該類退化為只剩靜態守衛。


  該承重點目前部分未驗證。實測 159 個日誌呼叫檔中，root 來源可指名者 93 檔（檔內有 `get_project_root()` 呼叫、走 `git_utils` 版、或經 ticket 狀態 root，三類去重後）；**其餘 66 檔未判定**。這 66 檔是承重點唯一的風險面。


  **不做則後果**：直接改 `lib/` 等於把方案押在一個未查的假設上。此形態本 wave 已發生兩次——`0.1.0-W3-273` 以「檔案是否直接 import 狀態 root 解析器」為判準，漏掉 4 處；`0.1.0-W3-288` 以「檔案是否提及觸及狀態 root 的模組」為判準，14 個候選中偽陽 8。兩次皆是判準先於查證，且錯誤在輸出上與正確完全同形。


  **為何 P1**：它是日誌 root 契約實作的前置，且結論可能改變選定方案的形狀。'
how:
  task_type: Analysis
  strategy: '逐檔判定 66 個未判定呼叫檔的 state root 來源，**不推論、以讀碼為準**。


    1. 取回清單：以 `0.1.0-W3-288` 重現實驗結果記載的掃描條件重跑（日誌入口呼叫點全量減去 root 來源可指名的 93 檔）。掃描須跨行（單行 grep 對折行呼叫無鑑別力，該缺陷已在 288 造成一次計數錯誤）。

    2. 每檔判為四類之一：(a) 無狀態寫入（純判定型，無 root 語意）；(b) 模組級可指名的 root 來源；(c) 執行期才決定 root；(d) 讀碼後仍無法判定。

    3. 只要出現任一 (c)，即回報承重點不成立，並於 `tarrragon/claude#97` 附觀測說明選定方案須修形；(d) 須逐檔列出理由，不併入 (a)。

    4. 結論寫入 Solution，並以 observe 附至 #97（不建立區段，除非派發提供 owner 識別）。


    本票不改任何程式碼、不改 `.claude/` 資產本體、不跑 `sync-push` 或 `skill-sync push`。'
acceptance:
- '[ ] 66 檔逐一判類，四類齊備且每檔有依據'
- '[ ] 出現執行期決定 root 的呼叫端時，已回報承重點不成立並附至 #97'
- '[ ] 判定結果以 observe 附至 tarrragon/claude#97，且掃描條件與 0.1.0-W3-288 可對接'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

分類 159 個日誌呼叫檔中 66 檔未判定的 root 來源

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-288 | chars: 896 -->

### Task Reference
- 0.1.0-W3-288 what: 規劃 hook_logging project_root 契約的修法，供 issue 97 統整後才派實作

### Rationale Chain
- 0.1.0-W3-288 why: **本票存在的理由是時序，不是內容**。`0.1.0-W3-284` 的修法形狀（四處接線 + 收斂為單一 logger 工廠）決定於 `0.1.0-W3-273` 的 Phase 4 發現當下；`0.1.0-W3-285` 事後實測的漏接率是 **163/164**（`setup_hook_logging`／`init_hook_logging` 的非測試呼叫點 164 個，顯式傳 `project_root` 者 1 個）。**修法被鎖定的時點早於知道問題規模的時點。**

**四處接線修的是 163 之中的 4**，且工廠這個抽象落在 consumer 側（`.claude/skills/ticket/`），而 285 判定的根源在 `.claude/lib/hook_logging.py` 的契約——opt-in kwarg、預設即舊行為，此設計本身落在它要修的無訊號區內。**在 consumer 側加一層工廠，不改變其他 consumer 專案的同型漏接，也不改變下一個新增呼叫點的預設行為。**

**不做則後果**：實作先於統整落地，則(1)四處接線與工廠需在上游契約變更後重做，其測試與抵達驗證亦重做一次；(2)`#97` 上會出現一個已實作但未被方案涵蓋的修法，後續讀者無從判斷它是方案的一部分還是繞道；(3)真正的癥結（預設值的方向）不會因為修好四處而變得更清楚。

**為何 P1**：它阻擋 `0.1.0-W3-284`，且 `#97` 每多累積一則未被統整的觀測，統整成本就升高一次。

### Source Acceptance (reference)
- [ref] [ ] #97 既有區段與觀測已通讀，已有結論與未答問題各成一張清單  # from 0.1.0-W3-288
- [ref] [ ] 至少兩個修法候選，各附利與弊，且各自涵蓋的呼叫點比例以實際掃描數記錄  # from 0.1.0-W3-288
- [ref] [ ] 選定方案的判準已寫明，且已標出該結論的承重點  # from 0.1.0-W3-288
- [ref] [ ] 方案已寫入 #97 的自有區段，實作範圍由該區段界定  # from 0.1.0-W3-288
- [ref] [ ] 0.1.0-W3-284 的既有修法已明確處置（保留／改寫／否決三擇一），理由寫入 Solution  # from 0.1.0-W3-288

