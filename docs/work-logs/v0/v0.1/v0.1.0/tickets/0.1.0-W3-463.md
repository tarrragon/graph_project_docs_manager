---
id: 0.1.0-W3-463
title: 補依據 tdd 第 4 批出題標記：TDD-02b／32b 依據只在範例＋TDD-21b／24a／29b／19b／36b 條文衝突或射程風險
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children:
- 0.1.0-W3-463.1
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-417
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 閘門範例補依據
  final_decision: 出題標記轉修補票
  rationale: 閘門第二條件；規則 5
who:
  current: thyme-documentation-integrator
  history: {}
what: 補依據 tdd 第 4 批出題標記：TDD-02b／32b 依據只在範例＋TDD-21b／24a／29b／19b／36b 條文衝突或射程風險
when: W2 開工閘門前
where:
  layer: 待定義
  files:
  - .claude/skills/tdd/SKILL.md
  - .claude/skills/tdd/CHANGELOG.md
  - .claude/skills/tdd/references/doc-handoff-uc-to-gwt-conversion.md
  - .claude/skills/tdd/references/phase2/rules.md
  - .claude/skills/tdd/references/task-granularity-rules.md
  - .claude/skills/tdd/references/phase1-split-methodology.md
  - .claude/skills/tdd/references/phase1/rules.md
why: W2 閘門第二條件（範例零依據只在範例內）；出題者標記的條文衝突依規則 5 追蹤
how:
  task_type: Documentation
  strategy: 逐項先判真衝突或讀法風險；階段 1 補句／路由；需新判準者草稿交 WRAP；Solution 附新增句出處表；例示不照搬題目輸入
acceptance:
- '[x] 7 項逐項處置；出處表每句有出處或轉草稿；tdd 升版；已推送'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-16T01:58:41'
completed_at: null
created: '2026-09-16'
updated: '2026-09-16'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補依據 tdd 第 4 批出題標記：TDD-02b／32b 依據只在範例＋TDD-21b／24a／29b／19b／36b 條文衝突或射程風險

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### 派發 context（PM 寫入，來源：W3-417 第 4 批出題者）

| 項 | 題 | 問題 |
|---|---|---|
| 1 | TDD-02b | 「反例要寫明為何誤判」只在被刪範例的驗證段 |
| 2 | TDD-32b | 「單次委派只測最終結果」只在被刪的 Q7 範例 |
| 3 | TDD-21b | 批次分配表與架構順序表給出不同批次，未寫優先序 |
| 4 | TDD-24a | 「每 1～2 個操作一張實作票」對上「單元與票 1:1」 |
| 5 | TDD-29b | phase1 Q5 靜默條件對上 .claude/rules/core/observability-rules.md 規則 1「catch 後 return 預設值必須記 warning」 |
| 6 | TDD-19b | 多結果拆分句位於「FR 無 UC 步驟」節，主詞卻是情境步驟 |
| 7 | TDD-36b | 條文只寫存在性驗證何時不足，未寫何時足夠 |

原文與附記見答案鍵：/private/tmp/claude-501/-Users-mac-eric-project-graph-project-docs-manager/2ccf6d72-30b6-455c-96ea-2023fad2d11e/scratchpad/probes/key/W3-417-b4-tdd-{1,2,3}.md 各題的判定者註。

**注意**
- 項 5 若確認為真衝突，要判斷改 tdd 或改 observability 規則；改 .claude/rules 屬另一資產，應先寫草稿交 PM。
- 第 4 批探針讀的是節錄檔快照，可與本票並行。


### 逐項判斷與處置

| 項 | 題 | 分類 | 判斷 | 處置 |
|---|---|---|---|---|
| 1 | TDD-02b | 依據只在範例內，不影響判定 | 〈三、契約欄位表〉反例列問句 + 範例引言句「應填具體誤判情境」已足以推出合格判定；「須另寫誤判理由（指出兩者驗證對象差異）」只在【驗證】檢驗問句，屬示範層級加值說明，非強制判準（與 2.8.0 對同範例的既有判定一致：緊鄰引言句視為條文，範例內部細節不視為條文） | 維持現狀不改檔 |
| 2 | TDD-32b | 依據只在範例內，不影響判定 | phase2/rules.md 組 1〈觸發條件〉「入口方法內部呼叫 2+ 個子方法」（非範例、正文）已可直接推出「只呼叫 1 個子方法 -> 不觸發 -> 直接測最終結果」；「直接測最終結果即可」的明文只在可能被移除的反面範例，不影響判定 | 維持現狀不改檔 |
| 3 | TDD-21b | 條文衝突 | phase1-split-methodology.md〈分配原則〉表（依賴關係決定批次）與〈架構層級執行順序〉表（依架構層固定批次）對同一任務給出不同批次時，條文全域查無何者優先的判準（grep 確認 phase1/rules.md 僅有「SOLID vs 行為單元衝突時以行為單元為準」一類優先序，未涵蓋本衝突） | 真衝突，需新判準；草稿寫入 NeedsContext，未落檔 |
| 4 | TDD-24a | 讀法風險 | task-granularity-rules.md〈禁止的模式〉表「每 1-2 個操作一個實作 Ticket」與同檔「1:1 映射」（27 行、136-154 行示範）、phase1/rules.md「每個行為單元 = 1 個測試 Ticket = 1 個實作 Ticket」（144 行）、phase3/rules.md 同句（三處既有正文重申嚴格 1:1）之間的關係未寫明，探針正確判定為嚴格 1:1（3 單元 -> 3 票） | 已有權威可依據（1:1 規則三處重申，數量與具體性皆高於單一行的「1-2 個操作」表列），補澄清句 |
| 5 | TDD-29b | 條文衝突（跨資產） | phase1/rules.md Q5 靜默三條件（AND 成立可選靜默）與 `.claude/rules/core/observability-rules.md` 規則 1「catch 後 return 預設值必須記錄 warning」在「三條件同時成立且 catch 回傳預設值」的交集情境，優先序條文全域查無 | 真衝突，需新判準，且改動涉及 `.claude/rules/core/`（另一資產）；依票面注意事項，草稿寫入 NeedsContext，不逕改任一規則檔 |
| 6 | TDD-19b | 讀法風險 | doc-handoff-uc-to-gwt-conversion.md「一步驟多結果的拆分」句位於〈無 UC 步驟的 FR → GWT 種子〉節內，但主詞為「一個場景步驟」，與檔頭「每個 UC 場景步驟轉換為...」用語一致，射程應涵蓋 UC 場景步驟 | 已有權威可依據（主詞本身），補澄清句釐清射程不受章節位置限制 |
| 7 | TDD-36b | 讀法風險 | phase2/rules.md Q_new3 只寫「存在性驗證何時不足」，未寫「何時足夠」；`.claude/rules/core/test-assertion-design-rules.md`〈鑑別力規則〉鑑別力定義（目標功能失效時該斷言會翻紅的能力）已蘊含足夠條件的邏輯反面 | 已有權威可依據（鑑別力定義），補足夠條件的顯式陳述 |

判斷依據：依「這是真衝突、讀法風險、或依據只在範例內」三分類逐項核對現行 `.claude/skills/tdd/references/` 條文（`git grep -F` 驗證，commit 基準與答案鍵一致：5d994e10）。
## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
### 新增句出處表

| 新增句 | 出處（`git grep -F` 命中，非範例） | 讀法是否改變 |
|---|---|---|
| doc-handoff-uc-to-gwt-conversion.md〈無 UC 步驟的 FR → GWT 種子〉「本段主詞為『一個場景步驟』，不限於本節（無 UC 步驟的 FR）——UC 場景步驟（見上文〈轉換方法〉）出現同樣的多結果情形時同樣適用。」 | 同段緊鄰句「一個場景步驟宣告多個預期結果...」本身（非範例，正文），`git grep -F "一個場景步驟宣告多個預期結果"` 命中該正文與另一則範例檢驗問句共 2 處，出處取正文位置 | 否——僅將該句主詞已蘊含的射程明文化 |
| task-granularity-rules.md〈禁止的模式〉表後「上表『每 1-2 個操作一個實作 Ticket』不牴觸〈從測試到實作的 1:1 映射〉...不得將 2 個行為單元合併進同一張實作 Ticket。」 | `phase1/rules.md`「每個行為單元 = 1 個測試 Ticket = 1 個實作 Ticket」（`git grep -F` 命中 `phase1/rules.md` 與 `phase3/rules.md` 兩處正文，皆非範例） | 否——依既有 1:1 正文（三處重申）澄清單一表列的關係，未新增判準 |
| phase2/rules.md Q_new3〈行動路徑〉「反之，目標功能失效時該存在性驗證會翻紅，即為足夠，不強制改驗值本身。」 | `.claude/rules/core/test-assertion-design-rules.md`「鑑別力 = 目標功能失效時，該斷言會翻紅的能力。」（`git grep -F` 命中，非範例，定義句） | 否——為既有「不足」條件的邏輯反面，未新增判準 |

TDD-02b／TDD-32b：查明既有正文（〈三、契約欄位表〉反例列問句；phase2/rules.md 組 1 觸發條件句）已足以推出探針正確判定，兩則明文雖只在可能被移除的範例區塊內，維持現狀不改檔，無新增句。

TDD-21b／TDD-29b：無可通過 `git grep -F` 驗證且非範例的既有出處可直接支持優先序判準，屬需新增判準，草稿寫入 NeedsContext，未落檔（TDD-29b 另涉修改 `.claude/rules/core/` 資產，依票面注意事項不逕改）。

### 版本與提交

tdd SKILL.md metadata.version：2.10.1 -> 2.10.2；CHANGELOG.md 新增對應條目；`Last Updated` 同步為 2026-09-16。


### PM 裁定後追加：TDD-21b／TDD-29b 落檔（依 0.1.0-W3-463.1 WRAP 評估）

| 新增句 | 出處 | 讀法是否改變 |
|---|---|---|
| `phase1-split-methodology.md`／`phase1/rules.md`〈版本號分配規則〉同落句：「架構層級執行順序表是本節『情況 / 版本/批次分配』表在『說明』欄所列依賴成立時算出的批次，不是另一套判準...」+ 判「否」反例（`StarRatingBar`） | 0.1.0-W3-463.1 WRAP 評估（Solution〈TDD-21b〉建議改文，逐字採用；WRAP 自身出處為兩檔既有正文「確保依賴關係正確」引言句與「情況／版本批次分配」表，經 `git grep -F` 驗證非範例） | 否——路由句消解表格並存造成的優先序疑義，未變更既有兩表任一格值 |
| `phase1/rules.md` Q5〈行動路徑〉取代句：三項成立時 catch 回傳預設值同樣可選靜默，warning 子句限定於「任一項不成立」分支，路由改指規則 1 表「catch 後 return 預設值」列 + 新判「否」反例（清除本機過期推播紀錄） | 0.1.0-W3-463.1 WRAP 評估（Solution〈TDD-29b〉建議改文，逐字採用；WRAP 自身出處為 `.claude/rules/core/observability-rules.md` 規則 1 表首句「否則需附註說明原因」與 quality-baseline 規則 4「只做 return/pass 時在註解說明」，經 `git grep -F` 驗證非範例） | 否——與 2.10.1 既有裁定（允許靜默）一致，僅修正 warning 子句射程與路由指錯列，未變更「三項成立可選靜默」的既有結論 |

`.claude/rules/core/observability-rules.md` 本輪未改動（U2：維持 2.10.1 裁定，允許靜默；方案比較見 W3-463.1 Solution〈A 兩側成本比較〉，tdd 側收窄射程即可消解表面衝突，rules 側加例外會與規則 1 表首句重複）。

原 NeedsContext 兩則草稿（含「以較晚者為準」句）依 WRAP 評估結果不採用，已由本次改文取代；U1／U3 依 PM 裁定執行（不另訂晝面票排序規則、探針未觸發仍視為條文缺陷本輪修補）。

tdd 版本：2.10.2 -> 2.10.3；commit cb288c921a17e54d78004176ff4e49dbc0250cf8。
## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext
### 草稿 1：TDD-21b — 分配原則表與架構層級執行順序表的優先序（Stage 2，未落檔）

**缺失項**：`.claude/skills/tdd/references/phase1-split-methodology.md`〈分配原則〉表（依賴關係決定批次：無依賴同批／依賴前一批下一批／依賴多批次取最後依賴的下一批）與〈架構層級執行順序〉表（Domain 第一批／Application、Infrastructure 第二批／Presentation 第三批，固定映射）對同一任務可能給出不同批次（例：某 Infrastructure 任務依賴另一個第二批任務，依分配原則應排第三批，但架構層級表固定 Infrastructure = 第二批），條文全域未寫明衝突時何者優先。

**觸發位置**：`phase1-split-methodology.md`〈架構層級執行順序〉表下方（第 71 行後）。

**影響**：讀者依架構層級表（較顯眼、表格在後）誤判批次，可能派發出違反依賴關係的並行任務（該任務依賴的前批尚未完成即被排入同批）。

**建議補料（草稿方向，待 PM/WRAP 確認後方可落檔）**：
- 方向 A（依賴關係優先）：〈架構層級執行順序〉表僅為「無特殊依賴時」的預設批次；具體任務若依〈分配原則〉判定的批次晚於架構層級表，以較晚者（分配原則）為準——因為批次分配的實質約束是 blockedBy，架構層級只是常見情形的捷徑對照。
- 方向 B（架構層級優先，分配原則表僅適用於同架構層內部）：分配原則表的「依賴前一批」限定在同一架構層內比較，跨架構層一律依架構層級表決定批次，不因額外依賴而順延。
- 兩方向皆有支持推論但條文未表態；本題探針判定採方向 A（TDD-21b (a) 答第三批），可作為傾向參考，但非既有條文明文，不可逕自落檔為既有規則的補句。

**重派成本**：低。僅需在兩表之間補一句優先序聲明，一旦裁定即可依 `git grep -F` 驗證後直接補句，預估 5 分鐘內可完成落檔。

---

### 草稿 2：TDD-29b — Q5 靜默三條件與 observability-rules.md 規則 1 的交集適用關係（Stage 2，未落檔，涉修改 `.claude/rules/core/` 資產）

**缺失項**：`.claude/skills/tdd/references/phase1/rules.md` Q5〈靜默判準〉三項條件（(a) 不寫入持久狀態、(b) 結果列在規格正常流程、(c) 呼叫端有處理分支）同時成立時可選靜默，但緊接其後的「catch 後回傳預設值者記 warning」一句，是否也內含「即使三條件成立、只要 catch 回傳預設值就必須記 warning」的例外，條文未寫明；`.claude/rules/core/observability-rules.md` 規則 1 表「catch 後 return 預設值：必須記錄 warning 日誌」為無條件表述，未提及與 Q5 三條件的關係。

**觸發位置**：`phase1/rules.md` Q5〈行動路徑〉段落末句；`.claude/rules/core/observability-rules.md` 規則 1 表「catch 後 return 預設值」列。

**影響**：兩條文並存時，讀者若嚴格套用 observability-rules 規則 1（無條件必須記 warning），會與 Q5 的三條件靜默例外衝突，導致「三條件皆成立仍被要求記 warning」或「Q5 例外被誤用到不該靜默的情境」兩種誤判皆可能發生（本題探針 TDD-29b 判定：三條件成立時可靜默，優先於規則 1 的無條件表述）。

**建議補料（草稿方向，待 PM 裁決，方向確定前不逕改任一規則檔）**：
- 方向 A（tdd 側收斂）：於 `phase1/rules.md` Q5 明確加註：「本三條件成立時的靜默例外，優先於 `.claude/rules/core/observability-rules.md` 規則 1『catch 後 return 預設值必須記錄 warning』的一般表述——三條件是規則 1 表首句『否則需附註說明原因』的具體化條件」。
- 方向 B（observability-rules 側收斂）：於 `.claude/rules/core/observability-rules.md` 規則 1 表「catch 後 return 預設值」列後加註例外：「三條件（不寫入持久狀態／列在正常流程／呼叫端有處理分支）同時成立時，可依 tdd Q5 改採靜默 + 註解說明，不強制 warning」。
- 兩方向皆需改動至少一份非 tdd-skill 的規則資產（observability-rules.md 屬 `.claude/rules/core/`），依 CLAUDE.md 開場行為原則與本 ticket 注意事項，thyme 不逕自決定改動方向，僅記錄兩案供 PM 裁決。

**重派成本**：低至中。方向確定後，補句本身簡短（1-2 句），但需 PM 或 WRAP 複核以避免與 observability-rules.md 其他消費方（parsley 等實作代理人）既有理解衝突。


### 定位補正（rule 11：行號不作為引用定位符）

草稿 1 的「觸發位置」改以標題錨點定位：`phase1-split-methodology.md`〈架構層級執行順序〉表結尾處（緊接在該表之後、下一個標題〈拆分報告範本〉之前）。草稿 2 的「觸發位置」改為：`phase1/rules.md` Q5 標題〈行動路徑〉段落內「catch 後回傳預設值者記 warning」句；`.claude/rules/core/observability-rules.md`〈規則 1：catch 區塊必須有日誌〉表「catch 後 return 預設值」列。上述兩則草稿原文的行號用語（「第 71 行後」）僅供撰寫當下參照，正式定位以本段標題錨點為準。
## Exit Status
```yaml
exit_status: needs_context
reason: "7 項逐項處置完成：2 項無需修改（依據只在範例內但不影響判定）、3 項已補句落檔（出處經 git grep -F 驗證非範例）、2 項為真衝突需 PM 裁決（TDD-21b 批次分配優先序；TDD-29b 涉修改 .claude/rules/core/observability-rules.md，跨資產不逕改），草稿已寫入 NeedsContext。tdd 已升版 2.10.1->2.10.2，待 commit+push。"
confidence: 0.8
acceptance_met: [0]
acceptance_unmet: []
artifacts: []
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）


**變更摘要**：
- `.claude/skills/tdd/references/doc-handoff-uc-to-gwt-conversion.md`：〈無 UC 步驟的 FR → GWT 種子〉補一句釐清「一步驟多結果的拆分」射程涵蓋 UC 場景步驟（TDD-19b）。
- `.claude/skills/tdd/references/task-granularity-rules.md`：〈禁止的模式〉表後補一句釐清「每 1-2 個操作一個實作 Ticket」與嚴格 1:1 映射的關係（TDD-24a）。
- `.claude/skills/tdd/references/phase2/rules.md`：Q_new3〈行動路徑〉補存在性驗證「足夠」的判定句（TDD-36b）。
- `.claude/skills/tdd/SKILL.md`：metadata.version 2.10.1 -> 2.10.2，Last Updated -> 2026-09-16。
- `.claude/skills/tdd/CHANGELOG.md`：新增 2.10.2 條目。
- 未落檔（草稿於 NeedsContext）：TDD-21b（分配原則表 vs 架構層級執行順序表優先序）、TDD-29b（tdd Q5 靜默三條件 vs `.claude/rules/core/observability-rules.md` 規則 1，跨資產待 PM 裁決）。
- 無需修改：TDD-02b、TDD-32b（既有正文已足以支持探針判定，範例內細節不影響判定）。

本票因含 2 則待 PM 裁決的草稿，exit_status 設為 needs_context，不執行 complete。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-417 | chars: 494 -->

### Task Reference
- 0.1.0-W3-417 what: 依用戶裁示以讀者探針為範例審查閘門跑第一輪實證：涵蓋本輪補過範例的 skill（component-contract-design、ux-design-evaluation、foundation-design、test-assertion-design、tdd、doc、version-bootstrap、dart-style-guardian、skill-design-guide），每則含判定的範例出題並含判準相反分支的對照版，題目凍結後各 3 探針作答，判定者先從條文寫出正確答案與依據句再比對；門檻 3/3；判定者同時標出依據只在範例內的判準

### Rationale Chain
- 0.1.0-W3-417 why: 用戶裁示：W2 開工條件為探針 3/3 通過且全部範例零無依據句；chained-examples 待本輪實證後再改

### Related Files
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-336.md  # from 0.1.0-W3-417

### Source Acceptance (reference)
- [ref] [ ] 凍結題組覆蓋每則含判定範例且含相反分支  # from 0.1.0-W3-417
- [ref] [ ] 逐題判定表（3/3 生效或未生效）與依據只在範例內清單寫入 Solution  # from 0.1.0-W3-417
- [ref] [ ] 未通過與無依據句逐項建修補票或記錄豁免理由  # from 0.1.0-W3-417
- [ref] [ ] W2 開工閘門達成與否的結論  # from 0.1.0-W3-417
