---
id: 0.1.0-W2-001
title: 實作 Domain 視圖的九個狀態（含已選格疊加態）
type: IMP
status: pending
version: 0.1.0
wave: 2
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W1-002
- 0.1.0-W1-005
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: SPEC-001 §1 的八個狀態需要實作
  final_decision: 泳道只做靜態版型，布局演算法後推
  rationale: 0.1 的斷言是畫得出來不溢位能捲能拖，不是排得好看；真正的排序與邊繞線需要真實 flow 資料才判斷得出好壞，在無資料可看時設計很可能設完又改，而泳道是 domain-map §6 判定的產品差異化本身，改的代價高
who:
  current: parsley-flutter-developer
  history: {}
what: 實作 Domain 視圖（SPEC-001 §1）九個狀態：未選專案／載入中／正常·矩陣／已選格／正常·泳道／空圖／不是框架專案／無可消費的型別表／schema 不相容，以 W1-003 fixture 驅動，不串真實資料
when: 路由殼完成後
where:
  layer: Presentation
  files:
  - lib/screens/domain_view/
why: Domain 視圖是本 App 的存在理由，也是 SPEC-001 中狀態最多的畫面（八個，含 0.1 新增的不是框架專案與無可消費的型別表）。
how:
  task_type: Implementation
  strategy: '依 SPEC-004 §3.6 逐狀態組合（狀態來源 SPEC-001 §1；頁面檔只從 lib/components/components.dart 匯入）：

    未選專案：`EmptyState.page`，動作格放 `AppButton.primary`（選擇資料夾）。

    載入中：`LoadingState.skeleton`（版位 `matrix`）。

    正常·矩陣：`PageColumn`[header 格 `SplitRow.header`[`PageTitle`, `SegmentedControl`], content 格 `TwoColumnLayout`[main `Panel`[`MatrixGrid`, `BadgeRow.legend`], detail `Panel.scrollable`[`EmptyState.section`（`panel-domain-cell-detail-empty`）]]]。

    已選格（疊加於正常·矩陣）：底層同上；detail 改 `Panel.scrollable`[`SplitRow.header`[`AppText.subtitle`, `AppButton.text`（關閉，`action-domain-cell-clear`）], `AppText.caption`（關係種類）, `AppText.body`（說明，可缺）, `ListRow.numbered` × N（可缺）, `BadgeRow`（事件標籤）, `AppButton.secondary`（在泳道中檢視）]；細節依 §3.6 該行。

    正常·泳道：`Panel`[`AppText.subtitle`, `SwimlaneGrid`（靜態版型，寫死座標 fixture）, `BadgeRow.legend`]。

    空圖：`EmptyState.page`（`ButtonRow`[`AppButton` 開啟 docs（目錄存在時；行為依 W1-036 定案）, `AppButton` 前往破洞報告]）。

    不是框架專案：`BlockedState.plain`。無可消費的型別表：`BlockedState.plain`（版本值 slot；降級與否依 W1-035 定案）。schema 不相容：`BlockedState.withDetail`（面板 `Section.static`[`AppText.caption`, `AppText.mono` × 2]）。

    退出路徑依 SPEC-003 §3.1 接 router.dart 既有 provider。寫不成上述句型的狀態即為缺件訊號：停手建元件票並回報，不就地自製。'
acceptance: []
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-08-27'
updated: '2026-08-27'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實作 Domain 視圖的八個狀態

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-002 | chars: 234 -->

### Task Reference
- 0.1.0-W1-002 what: 建立 六項導覽的路由殼

### Rationale Chain
- 0.1.0-W1-002 why: SPEC-001 §1-7 界定六個畫面加一個浮層，共 29 個狀態。各畫面若自帶導覽，切換行為會分歧。本票交出可切換的殼，讓後續每張畫面票只需關心自己的內容。

### Related Files
- lib/app/router.dart  # from 0.1.0-W1-002
- lib/app/shell.dart  # from 0.1.0-W1-002

### Source Acceptance (reference)
- [ref] [ ] 導覽列為六項，與 PROP-004 的畫面清單逐項對應  # from 0.1.0-W1-002
- [ref] [ ] 六項皆可切換且各自渲染標示畫面名的佔位頁  # from 0.1.0-W1-002
- [ref] [ ] 側欄頂端顯示專案名，點擊有回應（浮層內容可為空殼）  # from 0.1.0-W1-002
- [ref] [ ] 四種視窗尺寸下導覽列與內容區皆不溢位  # from 0.1.0-W1-002
