---
id: 0.1.0-W3-315
title: 裁決 owner 驗證器修法方向並處置 ticket-intake 失效範例
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-311
relatedTo: []
spawned_tickets:
- 0.1.0-W3-319
- 0.1.0-W3-320
source_ticket: 0.1.0-W3-311
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-311 判定完成，修法形狀依派發範圍不在該票決定
  final_decision: 另立裁決票，由 PM 依 W3-311 證據擇一修法方向並一併處置範本失效範例
  rationale: 三個方向互斥且各有代價；證據已齊不需重查，缺的是裁決
who:
  current: framework-issue-curator
  history: {}
what: 依 0.1.0-W3-311 的證據裁決 owner 驗證器的修法方向，並處置 ticket-intake owner 識別行的失效範例
when: 0.1.0-W3-311 完成後
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/section_comment.py
  - .claude/skills/framework-issue/references/ticket-intake.md
  - .claude/skills/framework-issue/references/comment-as-section-protocol.md
why: 0.1.0-W3-311 已判定：純數字尾碼與三個 owner 讀取端無關（皆前綴比對）、缺陷跨 consumer（blog-cd 亦被擋）、子代理人無合法取值管道（pm-registry.json 無 session 序號欄）。三個修法方向互斥且各有代價（放寬 regex 會連代理人角色名一併放行；改取值管道需新供應端；範本改合法範例只治派發不治驗證器），W3-311 依派發範圍不決定其一。ticket-intake owner 識別行的範例值尾碼含字母，照該範例填寫必然 exit 3，處置方式隨修法方向決定，故一併掛此票。
how:
  task_type: Implementation
  strategy: 讀 0.1.0-W3-311 的重現實驗結果與 Solution（證據已齊，不重跑）；裁決修法方向後依 framework-issue 流程把結論寫回 tarrragon/claude#95；修 .claude/ 資產前確認派發者提供合法 owner 識別。
acceptance:
- '[ ] 三個修法方向已擇一並記錄不選其他兩者的理由'
- '[ ] ticket-intake owner 識別行失效範例的處置已隨修法方向定案'
- '[ ] 結論已回寫 tarrragon/claude#95（區段或觀測，降級時記錄原因）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T17:02:39'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

依 0.1.0-W3-311 的證據裁決 owner 驗證器的修法方向，並處置 ticket-intake owner 識別行的失效範例

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

`section_comment.py` 的 `validate_owner`（`OWNER_FORMAT_RE`，`init`／`add`／`transfer-owner` 共用）要求 owner 尾碼為純數字，而 owner 值的供應端是 session 短名，尾碼實測為兩碼十六進位。**但這只是三個獨立缺陷之一**——本票的核心發現是票面「三方向互斥、擇一」的框定不成立：

| 票面方向 | 實際治的是什麼 | 是否被其他方向涵蓋 |
|---|---|---|
| A 驗證器（regex） | 逐 session 命中率 | 否，B 與 C 皆不治 |
| B 取值管道 | 子代理人取不到任何 owner 值 | 否，A 完全不觸及 |
| C 範本失效範例 | 照範本填必然 exit 3 | C 的內容由 A 決定，不是 A 的替代方案 |

三者的交集只在症狀（都降級為 `observe`），成因互不重疊。

### 影響範圍

**已驗證的事實**（承接 `0.1.0-W3-311`，本票另行複核）：

1. owner 的三個讀取端全為 `startswith(f"{prefix}-")`，無解析無排序——`section_comment.py` 的 `_discover_owned_issue_numbers_by_prefix`、`todo --consumer` 的列篩選、`session-start-issue-check-hook.py` 的擁有檢查。本票以 grep 獨立複核，與派發者提供的證據一致。
2. `validate_owner` 的三個呼叫點為 `cmd_init`／`cmd_add`／`cmd_transfer_owner`；`cmd_update` 不驗證 owner（沿用既有 comment 的 owner）。
3. `_project_owner_prefix()` 在 `section_comment.py` 與 `session-start-issue-check-hook.py` 各有一份同 heuristic 實作（git common dir 目錄名 kebab 化），推導失敗時回退 `Path.cwd().name`。
4. 失效範例不只 `ticket-intake.md` 一處：`comment-as-section-protocol.md` 的 owner 識別格式段與 `validate_owner` 的錯誤訊息，皆以某一個 consumer 的字面值作範例。

**仍未驗證的假設**：

- session 短名尾碼的產生規則無任何本機可讀來源，「兩碼十六進位」為觀測歸納。本票的修法選擇刻意不依賴此規則（見 Solution 對「收成十六進位」的否決理由）。
- 各 consumer 的 git checkout 目錄名是否皆等於其慣用 owner 前綴，無管道判定。此為錨定變體的主要風險，已列入實作票的要件。

### 相關 Error Pattern

`ARCH-BAL-023`（識別欄位無共用契約，消費端靜默漏檢）：寫入端與讀取端各自自洽、兩者之間缺約定，與本現象同構。本票的修法選擇正是消除該形態——令寫入端改用讀取端已在用的同一個判準函式。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-311 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-311 what: 判定 owner 驗證器與本專案 session 命名不相容的影響範圍與處置層級（來源已完成，結論見 Solution）
- 0.1.0-W3-311 what: 判定 owner 驗證器與本專案 session 命名不相容的影響範圍與處置層級（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-311 why: **現象**：`section_comment.py` 的 `OWNER_FORMAT_RE = ^[a-z0-9]+(-[a-z0-9]+)*-[0-9]+$`（行 140，消費於行 212）要求 owner 識別的尾碼為**純數字**。本專案的 session 序號為十六進位樣式。

**PM 實測（非採信轉述，範圍比 `0.1.0-W3-306` 回報的更廣）**：

| 值 | 通過 | 來源 |
|---|---|---|
| `graph-project-docs-manager-5d` | 否 | 本 session |
| `graph-project-docs-manager-8c` | 否 | 平行 session |
| `graph-project-docs-manager-2f` | 否 | 既有觀測的 owner 欄 |
| `flutter-balance-77` | 是 | 他 consumer |

**本專案至今無任何 session 可通過該驗證**。這解釋了一個此前被歸因於「PM 漏填」的模式：本專案歷次 curator 派發全數降級為 `observe`，而 `#97` 的 6 個區段全屬 `flutter-balance-77`——**那個 consumer 的 session 尾碼恰好是數字**。

**漏填只是症狀**：即使正確填入真實 session 名，`add`／`init` 仍會以 exit 3 失敗。因此「結論落在觀測流而非區段」對本 consumer 是**結構性**的，不是紀律問題。

**`0.1.0-W3-304` 已把一個失效值寫進範本**：`ticket-intake.md` 第 160 行的範例格式為 `graph-project-docs-manager-5d`（由 PM 於派發時提供）。該段加入的目的正是修「owner 未提供」，**而它給的範例讓照做的人命中同一後果**，成因從「漏填」換成「照範本填」。

**為何 P0**：它使本 consumer 無法使用 framework-issue 的核心機制（區段），且範本現正引導後續 session 重複失敗。（來源已完成，結論見 Solution）
- 0.1.0-W3-311 why: **現象**：`section_comment.py` 的 `OWNER_FORMAT_RE = ^[a-z0-9]+(-[a-z0-9]+)*-[0-9]+$`（行 140，消費於行 212）要求 owner 識別的尾碼為**純數字**。本專案的 session 序號為十六進位樣式。

**PM 實測（非採信轉述，範圍比 `0.1.0-W3-306` 回報的更廣）**：

| 值 | 通過 | 來源 |
|---|---|---|
| `graph-project-docs-manager-5d` | 否 | 本 session |
| `graph-project-docs-manager-8c` | 否 | 平行 session |
| `graph-project-docs-manager-2f` | 否 | 既有觀測的 owner 欄 |
| `flutter-balance-77` | 是 | 他 consumer |

**本專案至今無任何 session 可通過該驗證**。這解釋了一個此前被歸因於「PM 漏填」的模式：本專案歷次 curator 派發全數降級為 `observe`，而 `#97` 的 6 個區段全屬 `flutter-balance-77`——**那個 consumer 的 session 尾碼恰好是數字**。

**漏填只是症狀**：即使正確填入真實 session 名，`add`／`init` 仍會以 exit 3 失敗。因此「結論落在觀測流而非區段」對本 consumer 是**結構性**的，不是紀律問題。

**`0.1.0-W3-304` 已把一個失效值寫進範本**：`ticket-intake.md` 第 160 行的範例格式為 `graph-project-do（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] OWNER_FORMAT_RE 純數字尾碼是刻意或巧合已判定，附引入脈絡與用途查證  # from 0.1.0-W3-311（來源已完成，結論見 Solution）
- [ref] [x] 其他 consumer 的 session 命名樣式已抽樣，判定此為普遍缺陷或本專案特有  # from 0.1.0-W3-311（來源已完成，結論見 Solution）
- [ref] [x] owner 識別是否有 ListAgents 以外的合法取值管道已查證  # from 0.1.0-W3-311（來源已完成，結論見 Solution）
- [ref] [x] ticket-intake 第 160 行失效範例的處置已列為待處置項並記錄  # from 0.1.0-W3-311（來源已完成，結論見 Solution）
- [ref] [x] dedup 已跑，兩條 AND 判準已逐條套用並留痕；Solution 未寫入修法形狀  # from 0.1.0-W3-311（來源已完成，結論見 Solution）
