---
id: 0.1.0-W3-212
title: 分析 acceptance-gate Step 2.5.2 對 DOC 型 ticket 的 spawn 落地無執法點
type: ANA
status: closed
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-184
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 169 的 SR-10（P1）與 SR-11（P2）在 finish 時仍為 pending，票仍順利完成
  final_decision: 先分析執法點缺口的形狀與影響面，不當下修 .claude/ 通用資產
  rationale: 根源在 .claude/hooks/ 通用資產且敘述可攜，屬框架問題，須先跑升級判準與 dedup
who:
  current: 主線程
  history: {}
what: 分析 acceptance-gate Step 2.5.2 對 DOC 型 ticket 的 spawn 落地無執法點
when: 並行 session 於 169 驗收時發現後（2026-09-09）
where:
  layer: Infrastructure
  files:
  - .claude/config::read
why: '`quality-baseline` 規則 5 明寫「停在 pending 不算落地」，強制層是 acceptance-gate Step 2.5.2——比對 Solution 的 spawn 規劃數與已落地數，缺漏即阻擋 complete。


  **但 DOC 型 ticket 的 Solution 是免填的**，因此閘門拿到 0 對 0，比對通過。實例：`0.1.0-W3-169` 的 `SR-10`（P1）與 `SR-11`（P2）在 `finish` 時皆為 `status: pending`，票仍順利完成，無任何警告。


  **後果不是漏一次，是規則 5 對整個 DOC 類別無執法點**——而 DOC 票正是文件同步、規格補寫這類最容易長出後續發現的類型。本專案兩個並行 session 今日建立的 DOC 票數量可觀，此盲區在雙方都會重現。


  **須先判定是否為框架問題**：根源在 `.claude/hooks/`（通用資產），敘述「強制層比對的來源章節在某些 ticket 類型免填，使該類型無執法點」替換專案名後成立——兩條 AND 判準初判皆符合，故本票不當下修，先完成升級判準與 `dedup`。'
how:
  task_type: Analysis
  strategy: 三步。(1) 實查 acceptance-gate Step 2.5.2 的實作，確認 spawn 規劃數的來源是否只有 Solution 章節，以及 Spawn Requests 章節（結構化、有 status 欄位）為何未被納入比對——後者對全部 ticket 類型都存在且可機械讀取，可能是比 Solution 更好的來源。(2) 逐一判定四個 ticket 類型（IMP/ADJ/ANA/DOC）的 Solution 是否免填，得出盲區的實際涵蓋範圍，不以 DOC 單一類型推論。(3) 逐條套用框架問題兩條 AND 判準並記錄，跑 dedup 查重（涵蓋 comment 內文），依關係判定決定 observe 至既有 issue 或新建。本票不改任何 .claude/ 檔案。
acceptance:
- '[ ] Step 2.5.2 的 spawn 規劃數來源已實查確認，且判定 Spawn Requests 章節可否作為替代來源'
- '[ ] 四個 ticket 類型的 Solution 免填狀況逐一判定，盲區涵蓋範圍非由 DOC 單一類型推論'
- '[ ] 框架問題兩條 AND 判準逐條套過並記錄，非憑印象'
- '[ ] 已跑 dedup 且關係判定（重複／切分／引用）有記錄'
- '[ ] 本票未修改任何 .claude/ 檔案'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
closed_at: '2026-09-10T13:43:00'
closed_by: none
close_reason: not_executable_knowledge_captured
close_reason_note: 分析 context 已遷至 tarrragon/claude#33（區段：當前結論（graph-project-docs-manager：執法點與模板殘留）第二條軸；兩案取捨在同組問題與方案區段）
---

# Execution Log

## Task Summary

分析 acceptance-gate Step 2.5.2 對 DOC 型 ticket 的 spawn 落地無執法點

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 為何「換來源」優於「補類型分支」（並行 session 補充，PM 採用）

本票 `how` 的第一步要求判定 `Spawn Requests` 章節可否作為 Step 2.5.2 的替代來源。補一句該判定的依據：

> **補分支的話，每新增一個 ticket 類型都要有人記得回來補一次——而那個「有人記得」正是這個 bug 的成因。**

`Spawn Requests` 是結構化章節、有 `status` 欄位、對全部 ticket 類型都存在，讀它**不需要類型知識**；讀 Solution 則需要知道哪些類型免填，而那份知識沒有任何機制保證與 ticket 類型的增減同步。

**這不預先判定結論**：換來源可能有它自己的缺口（例如規劃只寫在 Solution 散文而未登記為 spawn request 的情形），本票仍須實查兩者的涵蓋差集，不得以本段為由略過第一步。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-184 | chars: 1273 -->

### Task Reference
- 0.1.0-W3-184 what: 定義 SPEC 設計時的前台背景分配與事件流跨 domain 協調要求（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-184 why: **用戶更正（2026-09-09）**，起因是 PM 把提示密度問題框成「先問這些提示時刻是否都必要」：

> 這樣也不完全正確。如果需要提示使用者的訊息，表示有他的重要性，那**有重要性的動作，不應該同時大量執行**，或者真正有重要問題發生的時候，**應該要暫停背景的行為**，所以我才提到有的提示訊息要擋住使用者的操作。
>
> 那重點是當我們設計 spec 的時候，也要評估**有哪些行為會在背景執行，哪些動作會在前台跟使用者互動**，是不是有些動作要**避免同時請求使用者的關注或者指令**，或者會造成互動影響的 SPEC 在設計的時候，**整個事件流就應該包含跟其他 domain 確認過再執行，或者應該加入延遲的動作**。

**PM 原本的框架把提示當成雜訊，這是錯的。** 提示需求本身是重要性訊號——需要提示使用者，表示該事件有重要性。因此提示密度過高不是「提示太多」，是**同時執行了太多重要的事**。

**這使 `0.1.0-W3-167` 的處置範圍必須收窄。** 在提示層做排隊、優先級或載體分化，是幫一個排程問題做視覺化緩衝——症狀被吸收，成因原封不動，而且緩衝做得越好，成因越不可能被發現。167 處理「留下來的提示怎麼呈現」，本票處理「為什麼會有這麼多同時發生的重要事件」。

**本專案已有具體實例。** 破洞掃描掛在 `GapReportNotifier.build()`，首讀者是 App 啟動時的 `ScanNotificationController.start()`（`0.1.0-W3-122` 的主題）——使用者尚未看過破洞報告頁，掃描就已在背景完成並試圖請求通知授權。這正是「背景行為在前台注意力尚未就緒時請求使用者關注」，而現行 SPEC-003 §2.2 的觸發條件只判斷「可見頁是不是 gaps」，不判斷「現在是不是請求使用者注意的適當時機」。

編輯能力（`0.1.0-W3-073`）落地後前台互動會大幅增加，此時若背景仍有掃描、載入、git 操作各自獨立觸發，注意力競爭會從偶發變成常態。**現在定義比事後補容易。**（來源已完成，結論見 Solution）

### Related Files
- docs/domain-map.md::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/usecases::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- .claude/methodologies::read  # from 0.1.0-W3-184（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 注意力競爭的避免規則已定義，含「同時」的操作性含義與依重要性的優先序來源  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 事件流的跨 domain 協調要求已定義，含哪些事件屬會造成互動影響、確認對象、確認形態、延遲判準  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 重要問題發生時暫停背景行為的規則已定義，含何謂重要問題、暫停範圍、恢復條件  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 產出落點經裁定，跨專案通則與本專案 domain 對應關係分開落位  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
- [ref] [x] 與 0.1.0-W3-167 的分工已明寫；若本票結論使 167 的某些處置不再需要，已通知其執行者  # from 0.1.0-W3-184（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
