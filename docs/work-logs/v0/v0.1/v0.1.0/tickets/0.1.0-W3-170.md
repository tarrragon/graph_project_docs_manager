---
id: 0.1.0-W3-170
title: 補寫 提示元件與提示訊息的測試驗收形態
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-169
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-167
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶指示：先改元件庫 skill、再檢查所有 SPEC、然後是補測試的部分
  final_decision: DOC 於測試策略補提示元件的驗收形態
  rationale: 規格寫了提示要求但測試層無對應驗收形態時，該要求無法被否證；前一輪回饋契約已有同型承接票（W3-100/104/111）
who:
  current: sage-test-architect
  history: {}
what: 補寫 提示元件與提示訊息的測試驗收形態
when: 0.1.0-W3-169 完成 SPEC 補件之後（測試驗收的對象須先有規格）
where:
  layer: Presentation
  files:
  - .claude/skills/tdd/references/layered-test-strategy.md
  - .claude/skills/tdd/references/test-object-catalogue.md
  - docs/spec/ui/SPEC-004-component-library.md
why: '**用戶裁示（2026-09-09）**：「然後是補測試的部分」，且指出核心理由——「這些元件庫的回饋可以成為測試的判定依據，尤其這是最終使用者真正看到的資訊，**如果我們只驗邏輯，整個資訊鏈還是有可能中斷**」。


  該裁示有當日的實證。`0.1.0-W3-078` 第二次實機驗證中，`0.1.0-W3-096` 附帶的診斷測試綠燈（`authorizationStatusCalls == 1`，程式邏輯正確），但**沒有任何測試能回答「使用者看到了什麼」**——`denied` fallback 的 SnackBar 是否顯示、顯示了什麼文字，測試層完全不涵蓋。測試驗到了「程式做了對的事」，沒驗到「使用者收到了對的資訊」。


  前一輪回饋契約的落地有同型承接票（`0.1.0-W3-100` 測試義務與替身角色、`104` 角色化替身形態、`111` `INV-FEEDBACK-001` 的驗證形態），本票是本輪的對應。


  **規格寫了要求而測試層無對應驗收形態時，該要求無法被否證**——它會停留在描述性文字，違反時沒有任何機制會報出來。`0.1.0-W3-169` 會把提示元件需求與提示訊息設計寫進三份 SPEC，本票補其驗收形態。'
how:
  task_type: Documentation
  strategy: '先讀 `0.1.0-W3-169` 落地的規格條文——**測試形態的對象是那些條文，不是重新設計要求**。


    至少涵蓋三類，各自的驗收手段不同：


    1. **提示是否出現**：斷言對象為 `0.1.0-W3-166` 條款要求的元件層日誌，或元件樹中的實際節點。**兩者不可互相取代**——日誌證明字串被送出，元件樹證明它進入了版面。哪一種適用於哪一類斷言須寫明。

    2. **提示的內容**：文字等於指定 i18n key 帶入參數後的值、placeholder 型別正確、失敗與成功的措辭區分成立。**斷言不得比對中文散文字面**（`0.1.0-W3-132` 尚未完成，比對字面會與其衝突）。

    3. **提示的可讀性**：版面保障（關鍵資訊未被截斷為刪節號）。這一類**無法以日誌斷言**——日誌記的是送出的字串，不是渲染後的版面。須以 widget 測試斷言實際佈局，並在窄幕條件下驗收。


    **明寫三者的不可互相抵扣**：日誌有記不代表元件在版面上、元件在版面上不代表文字未被截斷。這是 `0.1.0-W3-166` 那條「日誌不取代版面可讀性的行為斷言」邊界在測試層的展開。


    **重疊遮蔽的驗收**：若 `0.1.0-W3-167` 裁定需要佇列、優先級或合併策略，其正確性須有對應斷言形態（例如同時觸發 N 則時最終顯示的是哪一則、被丟棄的是否留下日誌）。若 167 裁定不需要，本項寫明「現階段不適用」及其判定來源，不留空白。


    沿用 `layered-test-strategy.md` 既有的〈角色化替身〉與〈分層之外的補位形態〉結構，以標題文字引用不寫章節號。`test-object-catalogue.md` 補對應的測試對象條目。'
acceptance:
- '[ ] 三類驗收形態（是否出現、內容、可讀性）各有可執行的斷言形態'
- '[ ] 三者的不可互相抵扣已明寫：日誌不證明在版面上，在版面上不證明未被截斷'
- '[ ] 內容類斷言以 i18n key 帶入值或結構化欄位為準，未比對中文散文字面'
- '[ ] 可讀性類以 widget 測試斷言實際佈局並含窄幕條件，未以日誌代替'
- '[ ] 重疊遮蔽的驗收形態已依 0.1.0-W3-167 的裁定寫明，裁定為不需要時亦寫明判定來源不留空白'
- '[ ] 沿用 layered-test-strategy 既有結構，以標題文字引用未寫章節號'
- '[ ] SPEC-004 4.26 測試點的「五個 key」改為引用 i18n 子節列出的全部 key，不寫死數字（0.1.0-W3-169 SR-11）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-09T15:19:29'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補寫 提示元件與提示訊息的測試驗收形態

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### PM 派發前實查（2026-09-09）

**一、本票的對象是 `0.1.0-W3-169` 已落地的條文，逐一讀**（commit `6863829f`）：

| 檔案 | 節 |
|------|-----|
| `docs/spec/ui/SPEC-003-interaction-response.md` | §2.13（判準 T 四問、T1 判否的四個承接、顯示位爭用的約束與 0.1 處置）、FR-16、FR-17 |
| `docs/spec/ui/SPEC-001-screen-state-matrix.md` | §8（8.1 逐狀態標註、8.2 事件路由表）、FR-08 |
| `docs/spec/ui/SPEC-004-component-library.md` | §4.0.12 提示訊息設計要求 |

**二、重疊遮蔽的驗收對象是「留痕」不是「重排」。** `0.1.0-W3-167` 裁定 A 經其
補記一收窄後，機制層（佇列／優先級／provider 仲裁）**撤回為待裁定**，前提由
`0.1.0-W3-184` gate；169 §2.13 據此裁定三者皆不採用，0.1 的規則是「不重排，
但必須留痕」。**故本票不寫佇列或優先級的正確性斷言**——那些機制在 0.1 不存在。

**三、SR-11 的具體位置**：SPEC-004 §4.26 測試點子節有一列寫「zh / en 五個 key
皆不溢位」，而該條目 i18n 子節現列六個（`viewGapsAction` 由 `0.1.0-W3-063` 新增後
未同步）。改為引用該子節列出的全部 key，不寫死數字（`document-format-rules`
規則 10）。**只改這一列**，4.26 其他內容不動。

**四、範圍協調（已與並行 session 對過）**：`0.1.0-W3-204` 正在改 SPEC-003，本票
`where.files` 不含 SPEC-003，兩者交集為空。**不要動 SPEC-003 與 SPEC-001。**

**五、接手者請獨立重驗上述行號與計數（PC-162）。**

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: sage-test-architect
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-09
**Executing Agent**: sage-test-architect
**Review Status**: pending

**變更摘要**（三檔）：

1. `.claude/skills/tdd/references/layered-test-strategy.md`（1.6.0 -> 1.7.0）
   - 「分層之外的補位形態」節內、〈不變式 INV-FEEDBACK-001 的驗證形態〉之後新增
     「終端回饋訊息的三類驗收（是否出現／內容／可讀性）」段（不新增 H2，既有補位形態
     各段零改動，故無編號插入問題——該節本身不編號）
   - 三類總表（問句／斷言對象／主要形態）+ 逐類展開：
     - **是否出現**：日誌與元件樹各有專屬適用面。日誌適用於元件樹上無對象可斷言的
       兩類路徑（不渲染該元件的層級要斷言「這條路徑確實請求了顯示」、規格判定不顯示
       或被後一則取代的事件）；元件樹適用於斷言使用者可見（節點存在／停留期間持續存在／
       時間到消失／動作節點可觸發）。不可互相取代的機制寫明為「日誌寫在顯示入口、
       節點產生在其後」，兩個方向的漏檢形態各給一則
     - **內容**：斷言值與被斷言值取自同一查表機制，測試檔內不出現該語言散文字面；
       日誌側比對 key 名／變體／停留時長／是否帶動作等結構化欄位；資源檔靜態屬性
       （placeholder 數量、型別、句首位置、各語系一致、零值另立 key）讀檔即可判定
     - **成敗措辭區分**單獨成段：三類中唯一觸及文字內容本身者，斷言限「句首標記集合的
       成員資格」，標記集合為具名 fixture；未維護該集合時降為人工核對並填目錄跳過欄，
       不得以斷言整句字面代替
     - **可讀性**：只能由渲染驗收（全部語系最長值 × 最小視窗尺寸與設計尺寸兩條件、
       行數與末行省略記號、競爭同列寬度者的讓步順序）；時間相關斷言遵守規則 D1
   - **三類不可互相抵扣**：四列反例表（日誌有記錄 !-> 在版面上、節點在版面上 !-> 未截斷、
     來源語系未截斷 !-> 全部語系未截斷、內容正確 !-> 使用者看得到）
   - **顯示位爭用的驗收對象依規格裁定而定**：採重排者驗重排結果；明文不採重排者
     重排斷言不適用（機制不存在，為它寫測試等於為不存在的行為建立預期），驗收對象
     只有留痕（等級以事件發起者為鍵、不以變體為輸入、靜態可讀出）。兩種情形都須在
     測試對象目錄留下判定紀錄，不適用者填「跳過條件與理由」欄不留空白
   - 檢查清單新增三列

2. `.claude/skills/tdd/references/test-object-catalogue.md`（1.0.0 -> 1.1.0）
   - 「二、來源類別」表追加「終端回饋訊息」一列（既有六列未動，章節編號未動）
   - 表後補三段：與元件契約類別的分界（對象是每一則訊息而非元件，增長來源不同，
     合併會使新增訊息無列可站而 C1 對它失效）、「測試形態」欄為組合值且不得拆成三行
     （拆行會共用同一條來源規格條目、違反 C3）、規格裁定不需要者填跳過欄不留空白、
     「反例」欄常見填法（持續性標示與阻擋狀態文字屬元件契約類別）
   - 「六、派發語言」追加三類驗收的句型範例（既有兩例未動）；檢查清單與相關文件各補一項

3. `docs/spec/ui/SPEC-004-component-library.md`（1.33 -> 1.34，SR-11）
   - 4.26 測試點子節**單列**修正：「zh / en 五個 key 皆不溢位」改為引用該元件 i18n
     子節列出的全部 key，並接上 §4.0.12 驗收段「不以該子節列舉的 key 數為上界」
     （`document-format-rules` 規則 10）。**4.26 其餘子節與其他內容零改動**
   - frontmatter version、正文粗體版號、變更歷史 1.34 列同步

**框架層與專案層的分工**：三類驗收形態的主體落在 `.claude/skills/tdd/references/`
兩檔，內容為跨專案可移植形態——依 `reference-stability-rules` 規則 8，框架檔案不引用
專案 ticket ID、SPEC ID 或專案元件名，故本專案的具體條文以標題文字與抽象描述引用
（「互動規格顯示位爭用節」「終端回饋元件」「〈元件層回饋日誌（觀測對偶）〉」）。
提交前以 grep 驗證兩檔無 ticket ID / SPEC-00N / PROP-00N / 專案元件名 / worklog 路徑。

**未做**（依票面明確排除）：未動 `lib/`、`test/`、ARB、SPEC-001、SPEC-003；未執行
`flutter run` 或啟動 App；未在 SPEC-004 新增 4.26 以外的測試點條目。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-167,0.1.0-W3-169 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-167 what: 設計 提示元件的使用時機、重疊處置、類型分化，與不可見時的日誌補位（來源已完成，結論見 Solution）
- 0.1.0-W3-169 what: 盤點 全部 SPEC 的狀態提示設計，補提示元件需求與提示訊息設計（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-167 why: **用戶裁示（2026-09-09）**：「也要額外評估一個問題，哪些時機要呼叫 snackbar 或者 bottoast 這類提示元件，**如果大量呼叫提示元件，反而會重疊遮蔽**，那有可能需要依照不同類型的提示需求，設計使用不同的提示元件，以及**如果提示元件看不到，我們要在哪些事件或者流程上要補 log**。」

`0.1.0-W3-166` 固化「有顯示就要有紀錄」，但不回答「何時該顯示」。兩者一起才完整：只有前者會導向「凡事都跳提示」，而提示互相爭用同一個顯示位時，**資訊反而比不提示更少**——後出現的取代前一則，使用者兩則都沒讀完。

**這不是假設性風險。** 本專案目前 `AppSnackBar.show(` 的呼叫點實查為 **3 處、2 個檔案**（`gap_report_screen.dart:199`、`:218`、`scan_notification_controller.dart:202`；`shell.dart` 只有註解提及，無呼叫）。而 `0.1.0-W3-113`（`ScanNotificationController.show` 失敗的 SnackBar fallback）與 `0.1.0-W3-165` 都會再增加觸發時機，編輯能力（`0.1.0-W3-073`）落地後還會更多——**呼叫點只會增加，所以此數為量測當下的值，接手者須自行複驗現值**（`reference-stability-rules` 規則 9）。

> **建票時的數字更正（2026-09-09）**：本段原寫「6 個呼叫點，分佈於三個檔案」。該值來自 `grep 'AppSnackBar.show\|AppSnackBar\.'`——第二個分支匹配任何成員存取（型別引用、列舉、註解），第一個分支的 `.` 為未跳脫的正則萬用字元，故量到的是「提及 `AppSnackBar` 的行數」而非呼叫點數。現值由兩個 session 各自以 `AppSnackBar\.show\(` 獨立實查並互相對上。另有一種口徑是「訊息結局」（同一呼叫點的分支各算一則）為 5 則，可並列參考，但 acceptance 第 2 項的對照對象是**呼叫點**。

**失效形態的更正見 Problem Analysis 的〈PM 前置實查〉第二節**：本專案的 `AppSnackBar.show` 在顯示前無條件呼叫 `hideCurrentSnackBar()`，且 `ScaffoldMessenger` 一次只呈現一則，故版面上的「重疊」不會發生，實際形態是**替換截斷**。用戶關切的後果成立且更嚴重（重疊時兩則都在畫面上，替換是後者把前者抹掉），但處置方向不同——分化元件解決的是「同時要看到兩則」，解決不了「被靜默丟棄」。

**第三個子問題是前兩者的必然對偶**：一旦決定某些時機不顯示提示，那些事件就失去了使用者可見的回饋，此時可觀測性必須由日誌承接——否則等於用「安靜」換掉了「有紀錄」，兩頭皆空。`0.1.0-W3-078` 的實機驗證正是撞上這個：路徑走到了、程式做了對的事，但沒有任何一側留下使用者層級的證據。（來源已完成，結論見 Solution）
- 0.1.0-W3-169 why: **用戶裁示（2026-09-09）**：「檢查所有 SPEC 當中對於狀態提示的設計，加入相關提示元件的需求以及提示訊息的設計。」

狀態提示的設計目前**散在三份 SPEC**，各自回答不同的一半：

| SPEC | 目前承載 | 缺的 |
|------|---------|------|
| SPEC-001 畫面狀態矩陣 | 有哪些狀態（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Related Files
- docs/spec/ui/SPEC-004-component-library.md::read  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- .claude/skills/ux-design-evaluation/references/interaction-feedback.md::read  # from 0.1.0-W3-167（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 何時使用暫態提示的判準已建立且可機械套用，輸入為事件性質而非逐案列舉  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- [ref] [x] 若結論為需要新提示元件，已說明與 AppSnackBar 的分界並走 component-contract-design 流程  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- [ref] [x] 因判準或遮蔽處置而不顯示的事件集合已逐類指定日誌承接點與等級，含被丟棄的提示  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- [ref] [x] 承接的規格撰寫已建 DOC 票或已登記 spawn request  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- [ref] [x] 全部 AppSnackBar.show( 呼叫點逐一對照該判準，不符者列出並給處置（派發前實查為 3 處／2 檔，接手者須自行複驗現值）  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
