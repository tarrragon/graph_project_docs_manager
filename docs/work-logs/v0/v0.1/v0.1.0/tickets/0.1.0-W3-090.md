---
id: 0.1.0-W3-090
title: 修復 doc test-map 不消費 traceability.yaml，且測試目錄與副檔名硬編為非 Flutter 形態
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-116
source_ticket: 0.1.0-W3-076
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 追溯查詢對本專案恆回傳零筆
  final_decision: IMP 修 test-map 的資料來源與掃描條件
  rationale: 資料已建立卻查不到，等於矩陣白建；三個成因（不讀 yaml／目錄硬編／副檔名不含 dart）任一未修都仍是零筆
who:
  current: thyme-python-developer
  history: {}
what: 修復 doc test-map 不消費 traceability.yaml，且測試目錄與副檔名硬編為非 Flutter 形態
when: 0.1.0-W3-076 完成後（2026-09-08），traceability.yaml 已存在但無消費者
where:
  layer: Infrastructure
  files:
  - .claude/skills/doc/doc_system/commands/test_map.py
  - .claude/skills/doc/doc_system/core/constants.py
  - .claude/skills/doc/tests/test_test_map.py
why: '`doc test-map` 有三個各自獨立、任一未修都足以使查詢對本專案恆回傳零筆的問題（PM 已逐項實查原始碼確認）：


  1. **完全不消費 `docs/traceability.yaml`**。全 `doc_system/` 中引用 traceability 的檔案為 `cli.py`、`core/tracking_schema.py`、`commands/batch_init.py`——`commands/test_map.py` 不在其中。它改以掃描檔案內容找 UC ID 字串。

  2. **測試目錄硬編為 `tests`**（`test_map.py:73`）。本專案是 Flutter，目錄為 `test/` 與 `integration_test/`。

  3. **副檔名過濾不含 `.dart`**（兩個掃描函式皆為 js/ts/py 系列）。


  後果直接落在剛完成的 `0.1.0-W3-076` 上：該票的驗收條件之一寫「traceability.yaml 建立且 doc skill 的 test-map 查詢可得結果」，而後半段**在本票修好之前無法由任何工作滿足**。矩陣已建立、資料是真的，但唯一的查詢入口讀不到它，也掃不到本專案的測試檔。


  追溯矩陣的價值在於「脫節能被看見」。查詢恆回傳零筆時，脫節與健全在輸出上不可區分——比沒有矩陣更糟，因為它看起來像已經有防護。'
how:
  task_type: Implementation
  strategy: '三項成因分別處置，並論證彼此的關係（是否應合併為單一資料來源）：


    1. **資料來源**：`test-map` 應以 `docs/traceability.yaml` 為主要來源（該檔的 `mappings` 軸即 UC↔測試對應），檔案內容掃描降為輔助或移除。若判定兩者並存，須說明何者優先、衝突時如何呈現。

    2. **測試目錄**：不再硬編。改由專案設定推導或接受參數；本專案需同時涵蓋 `test/` 與 `integration_test/`。

    3. **副檔名**：加入 `.dart`；同時檢視此清單是否也該由設定推導，避免下一個語言再撞一次。


    修完後以本專案實跑驗證：對 traceability.yaml 已登載的 UC 查詢應回傳非零且正確的測試檔清單。並回頭確認 0.1.0-W3-076 該條 acceptance 此時可被滿足——本票是它的實際承接者。


    注意 doc skill 的測試目錄目前有既有失敗（EVT 相關 1 failed + 4 errors，由另一張票承接）；本票只需確保自己新增或修改的測試通過，不需修那批。'
acceptance:
- '[x] test-map 以 docs/traceability.yaml 為資料來源，或明訂它與檔案掃描的優先關係'
- '[x] 測試目錄不再硬編，涵蓋本專案的 test/ 與 integration_test/'
- '[x] 掃描副檔名含 .dart；並評估該清單是否應由設定推導'
- '[x] 以本專案實跑驗證：對 traceability.yaml 已登載的 UC 查詢回傳非零且正確的測試檔清單'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-08T16:28:18'
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

修復 doc test-map 不消費 traceability.yaml，且測試目錄與副檔名硬編為非 Flutter 形態

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-08 16:26] readiness=2 validate=0
- [2026-09-08 16:27] readiness=2 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
三項成因逐一修復：

1. **資料來源優先序**：新增 `_load_traceability_tests()` 讀取 `docs/traceability.yaml` 的
   `mappings` 軸，以 `usecase -> tests` 建索引；`tests` 非空的條目視為已人工回填、優先採用。
   `tests` 為空（尚未回填）時退回既有的檔案內容掃描（`_search_in_index`）作為輔助。
   兩者不合併去重、不並列顯示——採「yaml 優先，否則掃描」的單一結果，避免同一 UC 出現
   兩套矛盾清單。

2. **測試目錄不再硬編**：新增 `constants.TEST_SCAN_DIRS = ["test", "integration_test"]`，
   `_build_test_content_index()` 改為遍歷此清單而非寫死的 `"tests"`。涵蓋本專案的
   `test/` 與 `integration_test/` 兩個目錄。

3. **副檔名含 .dart**：新增 `constants.TEST_FILE_EXTENSIONS`（含 `.dart`），
   `_scan_test_files` 與 `_build_test_content_index` 皆改用此常數，掃描函式與副檔名清單
   一併移入 constants.py 供設定推導，避免下個語言再撞一次。

修改檔案：
- `.claude/skills/doc/doc_system/commands/test_map.py`（重寫資料來源與掃描邏輯）
- `.claude/skills/doc/doc_system/core/constants.py`（新增 TEST_SCAN_DIRS、TEST_FILE_EXTENSIONS）
- `.claude/skills/doc/tests/test_test_map.py`（fixture 改用 test/ 佈局 + .dart，
  新增 integration_test 掃描測試、traceability.yaml 優先序測試、_load_traceability_tests
  單元測試）

實跑驗證（本專案 docs/traceability.yaml 已回填 UC-04、UC-06）：
- `test-map UC-04` -> 1 筆，`test/unit/screens/traceability_test.dart`（來自 yaml）
- `test-map UC-06` -> 1 筆，`test/unit/screens/gap_report_test.dart`（來自 yaml）
- `test-map UC-01` -> 2 筆，`matrix_cell_test.dart` / `table_column_header_test.dart`
  （yaml tests 為空，退回檔案掃描，證實 fallback 路徑亦生效）

附帶發現（非本票範圍，未修）：`doc_system/cli.py` 缺 `if __name__ == "__main__": main()`，
以 `python3 -m doc_system.cli` 直接執行無任何輸出且 exit 0（main() 從未被呼叫）；
以 `python3 -c "...; main()"` 或透過 skill 既有入口點呼叫皆正常。此為既有問題，與本票
三項成因無關，不影響本票驗收方式（本票驗證改用 execute()/main() 直接呼叫）。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
指令：(cd .claude/skills/doc && uv run pytest tests/test_test_map.py -v)
結果：8 passed in 0.33s（含新增的 test_scans_integration_test_dir、
test_traceability_yaml_takes_priority_over_scan、TestLoadTraceabilityTests 兩則單元測試）

實跑驗證（見 Solution 段落）：UC-04/UC-06 由 traceability.yaml 取得非零正確結果，
UC-01 由檔案掃描 fallback 取得非零正確結果。0.1.0-W3-076 該條 acceptance
（「traceability.yaml 建立且 doc skill 的 test-map 查詢可得結果」）此時可被滿足。

範圍確認：未觸碰 tests/test_tracking_schema_conformance.py（0.1.0-W3-091 承接），
未修既有 EVT 相關 1 failed + 4 errors。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-08 16:35)
  - what: doc_system/cli.py 缺 __main__ guard，python3 -m doc_system.cli 直接執行無任何輸出且 exit 0
  - why: main() 從未被呼叫，透過 -m 方式呼叫該 CLI 會靜默失效，可能誤導使用者以為指令無結果
  - suggested_type: IMP
  - suggested_priority: P3
  - related_files: 
  - context: 
  - status: processed（已建 0.1.0-W3-116）


## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-076 | chars: 579 -->

### Task Reference
- 0.1.0-W3-076 what: 建立 docs/traceability.yaml 四軸追溯矩陣，使測試與規格脫節可被偵測（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-076 why: CLAUDE.md §7 記 `docs/traceability.yaml`「尚未建立」；`doc` skill 已有 test-map 與 traceability 查詢功能但無資料可查。現況是 SPEC-004 每個元件的「測試契約」欄與 PROP-005 的每版整合測試為兩份互不相認的清單，UC↔測試無對應表——測試與規格脫節時沒有任何機制會發聲。這是「設計出與專案無關的測試」在偵測面的缺口（用戶 2026-09-08 指出），與 0.1.0-W3-075 的推導面互補：075 管「怎麼推導出該測什麼」，本票管「推導完之後脫節能不能被看見」。（來源已完成，結論見 Solution）

### Related Files
- docs/traceability.yaml  # from 0.1.0-W3-076（來源已完成，結論見 Solution）
- CLAUDE.md  # from 0.1.0-W3-076（來源已完成，結論見 Solution）
- docs/app-use-cases.md::read  # from 0.1.0-W3-076（來源已完成，結論見 Solution）
- docs/spec/ui::read  # from 0.1.0-W3-076（來源已完成，結論見 Solution）
- .claude/skills/doc/SKILL.md::read  # from 0.1.0-W3-076（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] docs/traceability.yaml 建立且 doc skill 的 test-map 查詢可得結果  # from 0.1.0-W3-076（來源已完成，結論見 Solution）
- [ref] [x] 現有資產已回填；「無對應」項目成表並列於 Solution（規格有而測試無、測試有而規格無兩類分開）  # from 0.1.0-W3-076（來源已完成，結論見 Solution）
- [ref] [x] 專案入口文件（根目錄）的文件索引列自「尚未建立」更新為實際狀態  # from 0.1.0-W3-076（來源已完成，結論見 Solution）
