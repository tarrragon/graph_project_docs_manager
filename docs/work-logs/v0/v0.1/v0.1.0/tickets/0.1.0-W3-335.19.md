---
id: 0.1.0-W3-335.19
title: 回寫 泳道 flow 未結構化狀態、UC Flow 事件流小表與孤立事件破洞、破洞項點擊跳票與票列 blockedBy
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-335.18 spawn requests
  final_decision: 三項合為一張 spec 回寫票
  rationale: 三項改同兩份規格且皆需同步完成矩陣與狀態總數，拆開會互相覆寫計數
who:
  current: star-anise-system-designer
  history: {}
what: 回寫 泳道 flow 未結構化狀態、UC Flow 事件流小表與孤立事件破洞、破洞項點擊跳票與票列 blockedBy
when: W3-335.18 登記的 SR-2／SR-3／SR-4 經用戶裁示後
where:
  layer: 待定義
  files:
  - docs/spec/ui/SPEC-001-screen-state-matrix.md
  - docs/spec/ui/SPEC-003-interaction-response.md
  - docs/usecases/UC-05-find-blocked-work.md
why: 用戶裁示（2026-09-14）：SR-2 比照 UC Flow 新增泳道 flow 未結構化狀態；SR-3 經 WRAP 判定事件是 domain 間連線而非步驟屬性，採事件流小表並將孤立事件列入破洞報告；SR-4 以 UC-05 為準改破洞項點擊為跳票、外部開啟降為次要操作，票列加 blockedBy
how:
  task_type: Documentation
  strategy: 依 Context Bundle〈裁示內容〉逐項回寫，內容已具體給定，未給定處停手回報
acceptance:
- '[x] SPEC-001 §1 新增「泳道 · flow 未結構化」列四欄齊全並計入狀態總數，SPEC-003 同步錨點與完成矩陣'
- '[x] SPEC-001 §2 正常態顯示欄含事件流小表，SPEC-003 §3.2 定義小表錨點、欄位與「本 UC 外」判定'
- '[x] SPEC-001 §5 與 SPEC-003 §3.5 新增孤立事件破洞類別與判準'
- '[x] SPEC-003 §3.5 破洞項主操作改為跳票並定位、外部開啟為次要操作；SPEC-001 §4 票列含 blockedBy 欄'
- '[x] UC-05 步驟 1 移除預估耗時與 SPEC-003 §3.4 一致'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T13:35:35'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

回寫 泳道 flow 未結構化狀態、UC Flow 事件流小表與孤立事件破洞、破洞項點擊跳票與票列 blockedBy

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-14 13:35] readiness=2 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## Context Bundle

### 修補票共同 Context（PM，2026-09-14）

**缺口清單在哪**：本票涵蓋的缺口編號見 `ticket track full 0.1.0-W3-332.13` Solution；每個缺口的位置錨點、兩位讀者的分歧模擬、子票草稿見其來源子票（W3-332.2–.10）Solution。**W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 優先於 .13 草稿**——草稿漏收、誤歸或數字不符處以 P 項為準。

**規格**：`.claude/skills/skill-design-guide/references/chained-examples.md`（三段鏈式、驗證段兩形態、缺範例與缺引導分界）。

**已定案的用戶裁示**：
- 語言專屬 skill 的違規／修正程式碼對照不受「禁程式語法」約束（例外條款由 W3-335.15 寫入規格）
- 元件庫規格範本佔位文字改為技術中立，框架名移到 L2 註記
- version-bootstrap 的 v02-walkthrough 依新規格重寫整篇

**矛盾類票的簽核流程**：先在 Solution 寫「矛盾位置｜兩方原文｜建議以哪方為準｜理由｜改後文字草稿」，**不改檔**，以 `ticket track set-exit-status` 設 needs_context 並停手回報；PM 取得用戶簽核寫回本票後再續做改文。

**通則**：
- 框架檔不得寫本專案識別符（票號、SPEC 編號、畫面名），範例一律去識別化
- 集合大小改引用權威名稱而非寫死數字；錨點用標題不用行號
- 改動 skill 須同步其 CHANGELOG 與 SKILL.md 版號
- 只改本票 where.files；發現範圍外缺口登記 spawn request，不就地修

---

### 裁示內容（用戶簽核 2026-09-14）——內容已具體給定，逐項照寫；表中未涵蓋的決策停手回報

**SR-2 泳道 · flow 未結構化**（SPEC-001 §1，比照 §2「flow 未結構化」）

| 欄 | 內容 |
|----|------|
| 顯示 | 泳道面板標題為選定 UC；主區顯示「此 UC 尚未填寫結構化 flow」，不渲染泳道列 |
| 可用操作 | 開啟原始檔、切回矩陣 |
| 進入條件 | 泳道模式，且選定 UC 無 FlowStep |
| 退出路徑 | 切回矩陣、導覽、切換專案 |
| 計數 | 計入狀態總數 |

**SR-3 事件流小表**（SPEC-001 §2 正常態、SPEC-003 §3.2）
- 位置：步驟表下方，步驟表本身不變
- 欄位：事件（EVT ID）｜發出（步驟序號 · domain）｜消費（步驟序號 · domain）；一個事件一列，同一事件多個消費步驟時消費欄列出全部
- 資料來源：本 UC 的 FlowStep `emits`／`consumes`
- 「本 UC 外」：事件在本 UC 內只有發出或只有消費時，缺的一側顯示「本 UC 外」，並附 EVT 節點 frontmatter `consumers`（或 `producers`）所列 domain
- 本 UC 無任何事件時不渲染小表

**SR-3 孤立事件破洞**（SPEC-001 §5、SPEC-003 §3.5）
- 類別名：孤立事件
- 判準（跨全專案所有 UC 的 FlowStep 比對）：
  - 有發出無消費：某 EVT 被至少一個 FlowStep `emits`，全專案無任何 FlowStep `consumes`，**且** EVT 節點 `consumers` 為空
  - 有消費無發出：某 EVT 被至少一個 FlowStep `consumes`，全專案無任何 FlowStep `emits`，**且** EVT 節點 `producers` 為空
- EVT 節點宣告了 consumers／producers 但全專案 FlowStep 無對應者：**未裁示**，遇到時停手回報，不自行歸類
- 畫面標示：事件流小表中符合判準的列以問題標記呈現（元件由 SPEC-004 承接）

**SR-4 破洞項點擊與票列 blockedBy**（SPEC-003 §3.5、SPEC-001 §4、UC-05）
- 破洞項主操作（點擊）：切至 Ticket 清單並定位該票（沿用 SPEC-003 定位提示）；**僅適用破洞項能對應到一張 ticket 者**
- 外部開啟：改為破洞項的次要操作，opened／notFound／failed 三結果與 SnackBar 行為不變
- 無法對應到 ticket 的破洞項（非 ticket 類破洞）主操作為何：**未裁示**，停手回報
- 票列新增 blockedBy 欄：值為被阻擋的 ticket ID 清單，無則「—」
- UC-05 主要成功場景步驟 1 與驗收清單移除「預估耗時」，與 SPEC-003 §3.4 一致
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: star-anise-system-designer
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
