---
id: 0.1.0-W3-179
title: 量測 提示截斷在真實使用中的發生頻率與被截斷者的變體分佈
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-176
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-167
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-167 第二問：是否需要依提示類型分化元件，取決於爭用頻率；該頻率本票期間取不到（不得啟動 App）
  final_decision: 把頻率量測獨立為一票，不由可達性推導頻率
  rationale: 0.1.0-W3-167 已裁定不依賴頻率的部分（withAction 不得被靜默截斷，依後果分級）；剩餘的分化元件決策確實依賴頻率，故停在待量測並以本票為 trigger（decision-trigger-binding 規則 2：合法 trigger 限 ticket ID）
who:
  current: parsley-flutter-developer
  history: {}
what: 量測 提示截斷在真實使用中的發生頻率與被截斷者的變體分佈
when: 0.1.0-W3-176 補齊日誌欄位之後，且可取得一次實機執行時窗時
where:
  layer: Presentation
  files:
  - lib/components/app_snack_bar.dart
why: '`0.1.0-W3-167` 把「爭用是否可能發生」與「實際發生多頻繁」分開：前者已由該票的確定性重現實驗證實（四組 widget 實驗 + Flutter SDK 控制流），後者需要真實操作驅動的執行期資料，該票期間受並行約束不得啟動 App。


  **這是分化提示元件與否的唯一未決依據。** `0.1.0-W3-167` 已裁定不依賴頻率的部分——`withAction` 不得被後續 `show` 靜默截斷（實驗 F5：被截斷時動作按鈕消失且從未被呼叫）。剩下「是否需要新的提示載體以承接不同類型的提示」確實依賴頻率：若實測顯示爭用極少發生，優先級規則即足夠，新增元件為過度設計。


  > **PM 覆核更正（2026-09-09）**：本段原寫該截斷「等同銷毀該功能在 0.1 的唯一入口」。**該理由不成立，結論仍成立。** 實查兩個動作皆有替代入口——`rescanAction` 對應 `lib/screens/gap_report/gap_report_screen.dart:88-90` 的 `action-gaps-rescan` 按鈕（呼叫同一個 `rescan()`，`lib/l10n/app_localizations.dart:467` 註解明寫兩者共用文案）；`viewGapsAction` 的目的地 `AppDestination.gaps` 在導覽列中（`lib/app/router.dart:42`）。

  >

  > **真正無替代的是通知本身，不是動作。** 系統層通知在 `denied` 分支已不可用（SnackBar fallback 存在的理由即是它），此時該則是「掃描已完成」的唯一訊號，截斷即無管道得知；但前往破洞頁與重新掃描這兩個動作的入口都還在。

  >

  > **本票的量測設計不因此改變**，但嚴重度的敘述必須用更正後的版本：損失是「使用者不知道事件已發生」，不是「使用者無法執行該動作」。兩者導出的處置方向不同——前者指向留痕與可回顧（`0.1.0-W3-176` / `0.1.0-W3-187`），後者會指向阻擋或必須確認的載體（`0.1.0-W3-185`）。引用本票結論的下游若採後者，屬建立在已更正的前提上。

  >

  > 本則為 `PC-GPD-014`（正確結論配錯誤理由，當下無症狀）的實例。


  `0.1.0-W3-063` SR-2 已有 PM 裁決（0.1 不新增 Banner 與 NavItem 徽章，重評綁 `0.1.0-W3-062`）。**本票不翻案該裁決**，只提供翻案與否所需的量測事實。'
how:
  task_type: Analysis
  strategy: '**判別式**（`0.1.0-W3-167` 已實查其唯一性）：全 repo 呼叫 `hideCurrentSnackBar` 只有兩處，`app_snack_bar.dart:121`（`show` 內無條件呼叫，取預設 `reason: hide`）與 `:166`（動作按下，明確 `reason: action`）。故 `closed{reason=hide}` 恆等於「被後續一次 `show` 截斷」，不與 `timeout` / `action` / `dismiss` 混淆。**不需人眼即時觀察**。


    **三項指標**：


    | 指標 | 定義 | 前置 |

    |------|------|------|

    | 一 爭用比例 | `closed{reason=hide}` 佔全部 `closed` 事件的比例 | 165 已足 |

    | 二 嚴重度分佈 | 每次 `hide` 對應的 `shown` 是否為 `withAction`（實驗 F5 的實際命中率） | 需 `0.1.0-W3-176` 的 G1 關聯識別 |

    | 三 可見時長分佈 | `shown` 至 `closed{hide}` 的實際可見時長；區分「一幀未顯示」與「顯示中被截斷」 | 需 `0.1.0-W3-176` 的 G2 時長欄位 |


    **取樣要求**：不得以單次執行下結論。爭用屬時序敏感事件，`test-assertion-design-rules` 規則 1 邊界（少量綠燈不等於 always 綠燈）同理適用於「少量未觀察到不等於不發生」——須記錄取樣次數與操作腳本，並在結論中標明取樣規模。


    **操作腳本**（至少涵蓋 `0.1.0-W3-167` 判定為共時可達的兩個獨立觸發源）：使用者驅動的破洞項點選（`_openItem`）與背景掃描驅動的完成通知（`_showDeniedSnackbar`，含 `_pendingDeniedNotification` 於 `resumed` 時的延後 flush）。


    **實機約束**：`0.1.0-W3-078` 的通知授權狀態具一次性量測價值。本票的實機執行須與該票持有者協調時窗，不得各自啟動 App 消耗該狀態。'
acceptance:
- '[ ] 三項指標各有量測值，且標明取樣規模與操作腳本'
- '[ ] 指標二區分被截斷者為 plain 或 withAction，withAction 命中數單獨列出'
- '[ ] 指標三區分一幀未顯示與顯示中被截斷兩種形態'
- '[ ] 依量測結果對「是否需要分化提示元件」給出結論；結論為需要時，說明與 AppSnackBar 的分界並走 component-contract-design 流程，不就地發明'
- '[ ] 未推翻 0.1.0-W3-063 SR-2 的既有 PM 裁決；若量測支持翻案，以 spawn request 交 PM 而不自行裁定'
- '[ ] 實機執行時窗已與 0.1.0-W3-078 持有者協調，未獨自消耗通知授權狀態'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

量測 提示截斷在真實使用中的發生頻率與被截斷者的變體分佈

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 前提重評（2026-09-10，因方法論與 spec 設計更新）

**本票的立論基礎與《事件流負載仲裁方法論》直接衝突，範疇需改。**

本票的 `why` 寫「剩下『是否需要新的提示載體』確實依賴頻率：若實測顯示爭用極少發生，優先級規則即足夠」。方法論〈競爭與佔用量〉明訂：**卸載的判準是佔用量的三項，不是到達率**；並給了理由——到達率是預測量（尖峰會持續多久未知），佔用量是狀態量。同節另訂到達率的合法用途只有兩項：入口配額與自發型的啟動時機。

「爭用多頻繁」是到達率量測。依方法論，它不能決定卸載規則是否足夠。

**這不表示本票該關**，而是它量到的東西要換一個用途：

| 原用途 | 是否仍成立 |
|-------|-----------|
| 決定要不要新增提示載體 | **不成立**——載體需求由級別決定（見 `0.1.0-W3-185`／`187` 的重評），不由頻率決定 |
| 決定既有規則是否足夠 | **不成立**——同上 |
| 作為佔用量三項中「最老未消費請求年齡」的代理量校準 | **成立**——注意力通道讀不到佇列深度，方法論〈競爭與佔用量〉允許以代理量作輸入，本票的量測正可用於此 |
| 作為自發型提示的啟動時機依據（已知尖峰前不啟動） | **成立**——方法論明列此為到達率的合法用途之一 |

**建議處置**：本票改為「量測注意力通道的代理量並校準其閾值」，`why` 段刪除「頻率決定是否需要新載體」的推論鏈。實機量測本身目前受用戶延後指示約束，不啟動。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-167,0.1.0-W3-176 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-167 what: 設計 提示元件的使用時機、重疊處置、類型分化，與不可見時的日誌補位
- 0.1.0-W3-176 what: 補齊 AppSnackBar 元件層日誌的三項可稽核欄位

### Rationale Chain
- 0.1.0-W3-167 why: **用戶裁示（2026-09-09）**：「也要額外評估一個問題，哪些時機要呼叫 snackbar 或者 bottoast 這類提示元件，**如果大量呼叫提示元件，反而會重疊遮蔽**，那有可能需要依照不同類型的提示需求，設計使用不同的提示元件，以及**如果提示元件看不到，我們要在哪些事件或者流程上要補 log**。」

`0.1.0-W3-166` 固化「有顯示就要有紀錄」，但不回答「何時該顯示」。兩者一起才完整：只有前者會導向「凡事都跳提示」，而提示互相爭用同一個顯示位時，**資訊反而比不提示更少**——後出現的取代前一則，使用者兩則都沒讀完。

**這不是假設性風險。** 本專案目前 `AppSnackBar.show(` 的呼叫點實查為 **3 處、2 個檔案**（`gap_report_screen.dart:199`、`:218`、`scan_notification_controller.dart:202`；`shell.dart` 只有註解提及，無呼叫）。而 `0.1.0-W3-113`（`ScanNotificationController.show` 失敗的 SnackBar fallback）與 `0.1.0-W3-165` 都會再增加觸發時機，編輯能力（`0.1.0-W3-073`）落地後還會更多——**呼叫點只會增加，所以此數為量測當下的值，接手者須自行複驗現值**（`reference-stability-rules` 規則 9）。

> **建票時的數字更正（2026-09-09）**：本段原寫「6 個呼叫點，分佈於三個檔案」。該值來自 `grep 'AppSnackBar.show\|AppSnackBar\.'`——第二個分支匹配任何成員存取（型別引用、列舉、註解），第一個分支的 `.` 為未跳脫的正則萬用字元，故量到的是「提及 `AppSnackBar` 的行數」而非呼叫點數。現值由兩個 session 各自以 `AppSnackBar\.show\(` 獨立實查並互相對上。另有一種口徑是「訊息結局」（同一呼叫點的分支各算一則）為 5 則，可並列參考，但 acceptance 第 2 項的對照對象是**呼叫點**。

**失效形態的更正見 Problem Analysis 的〈PM 前置實查〉第二節**：本專案的 `AppSnackBar.show` 在顯示前無條件呼叫 `hideCurrentSnackBar()`，且 `ScaffoldMessenger` 一次只呈現一則，故版面上的「重疊」不會發生，實際形態是**替換截斷**。用戶關切的後果成立且更嚴重（重疊時兩則都在畫面上，替換是後者把前者抹掉），但處置方向不同——分化元件解決的是「同時要看到兩則」，解決不了「被靜默丟棄」。

**第三個子問題是前兩者的必然對偶**：一旦決定某些時機不顯示提示，那些事件就失去了使用者可見的回饋，此時可觀測性必須由日誌承接——否則等於用「安靜」換掉了「有紀錄」，兩頭皆空。`0.1.0-W3-078` 的實機驗證正是撞上這個：路徑走到了、程式做了對的事，但沒有任何一側留下使用者層級的證據。
- 0.1.0-W3-176 why: `0.1.0-W3-167` 的確定性重現實驗（四組，`fvm flutter test`）證實 `0.1.0-W3-165` 落地後仍有三項可稽核缺口，使「程式主動決定不告知使用者」這件事無法被事後追究：

| 缺口 | 現況 | 後果 |
|------|------|------|
| G1 訊息識別 | `closed` 事件的 `fields` 只
  ... (truncated, see source ticket)

### Related Files
- docs/spec/ui/SPEC-004-component-library.md::read  # from 0.1.0-W3-167
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-167
- .claude/skills/ux-design-evaluation/references/interaction-feedback.md::read  # from 0.1.0-W3-167

### Source Acceptance (reference)
- [ref] [ ] 何時使用暫態提示的判準已建立且可機械套用，輸入為事件性質而非逐案列舉  # from 0.1.0-W3-167
- [ref] [ ] 若結論為需要新提示元件，已說明與 AppSnackBar 的分界並走 component-contract-design 流程  # from 0.1.0-W3-167
- [ref] [ ] 因判準或遮蔽處置而不顯示的事件集合已逐類指定日誌承接點與等級，含被丟棄的提示  # from 0.1.0-W3-167
- [ref] [ ] 承接的規格撰寫已建 DOC 票或已登記 spawn request  # from 0.1.0-W3-167
- [ref] [ ] 全部 AppSnackBar.show( 呼叫點逐一對照該判準，不符者列出並給處置（派發前實查為 3 處／2 檔，接手者須自行複驗現值）  # from 0.1.0-W3-167
  ... (truncated, see source ticket)
