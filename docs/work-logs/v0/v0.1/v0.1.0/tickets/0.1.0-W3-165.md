---
id: 0.1.0-W3-165
title: AppSnackBar 元件內建顯示日誌，使終端回饋可觀測
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-078
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-078 第二次實機驗證需觀測 denied fallback 的 SnackBar 是否出現，但該路徑零日誌，只能靠人眼即時觀察
  final_decision: IMP 補齊該路徑的日誌，使觀測機械化
  rationale: 用戶裁示：可重複的量測不該交給肉眼。補 log 同時解決 078 的觀測需求與該路徑本身的可觀測性缺陷，一次處理兩件事
who:
  current: parsley-flutter-developer
  history: {}
what: 在 AppSnackBar 元件內部加入顯示日誌，使全部呼叫點的使用者可見回饋自動可觀測
when: 0.1.0-W3-078 第二次實機驗證發現（2026-09-09）
where:
  layer: Presentation
  files:
  - lib/components/app_snack_bar.dart
  - test/unit/components/app_snack_bar_test.dart
why: '`0.1.0-W3-078` 第二次實機驗證（2026-09-09 11:51）確認通知路徑抵達 `authorizationStatus()` 且平台回 `denied`，程式碼因此走 `_fallbackToSnackbar()` 分支。但**該分支之後的全部路徑零日誌**，導致：


  **SnackBar 出現與靜默失敗在日誌上完全同形。** `_showDeniedSnackbar()` 沒有任何 `developer.log`，其第一行 `if (!context.mounted) return;` 的靜默早退亦不留痕跡。唯一的分辨手段是人眼即時觀察，而 SnackBar 會自動消失、事後無法回溯。該次驗證因此未能取得此項——用戶當時在看 DevTools 螢幕。


  **這是一個使用者可見的回饋動作本身不可觀測。** 與 `ARCH-GPD-001`「日誌不抵扣使用者回饋」互為反面：該則講日誌有記不代表使用者收到回饋；本案是使用者回饋不留日誌，故它失效時無人知道。`observability-rules` 規則 5 要求平台互動的每個決策點自帶日誌，本路徑是其未涵蓋的一類——它不呼叫平台 API，但它是平台回應之後唯一的使用者出口。


  **用戶裁示（2026-09-09）**：「這部份可以加入 log，就不用肉眼確認。」可重複的量測不應依賴人眼即時觀察——SnackBar 的顯示是可重複事件（授權已為 `denied`，每次啟動都會再走一次），把它機械化之後，`0.1.0-W3-078` 的該項觀測不再需要人在場，且此後任何回歸都會留下痕跡。'
how:
  task_type: Implementation
  strategy: '在 `lib/screens/gap_report/scan_notification_controller.dart` 的 denied fallback 路徑補日誌，涵蓋**每個會改變結果的決策點**，不只成功那一條：


    1. `_fallbackToSnackbar()`：記錄走的是「立即顯示」或「延後至 resumed」分支，含當下的 `appLifecycleStateProvider` 值——兩者導致使用者體驗完全不同，日誌須能分辨

    2. `_showDeniedSnackbar()` 入口：記錄即將顯示，含 `gapCount` 與選用的 i18n key（`scanCompleteSnackbarMessage` 或 `scanCompleteNoGapsSnackbarMessage`）

    3. `_showDeniedSnackbar()` 的 `if (!context.mounted) return;` 早退：**必須記錄**（warning 等級）——這是目前唯一完全無痕跡的失敗路徑

    4. `_onLifecycleChange()` 顯示延後的通知時：記錄實際顯示的時點


    日誌一律用 `developer.log(..., name: _tag)`，等級依 `observability-rules` 規則 1（正常流程 info、靜默早退 warning）。**沿用該檔既有的 `i18n-exempt: 開發者診斷 log` 註解慣例**（見 `macos_scan_notifier.dart` 的既有寫法）。


    測試須斷言日誌行為而非僅斷言 SnackBar 出現——後者既有測試已涵蓋，本票要補的是「失效時留下痕跡」。`0.1.0-W3-132`（改為日誌事件識別碼使斷言脫離中文散文字面）尚未完成，本票的日誌文字暫用中文散文，但**測試斷言不得比對散文字面**，改以呼叫次數或結構化欄位斷言，避免與該票衝突。


    **範圍限本檔的通知路徑。** 其他 `AppSnackBar.show` 呼叫點是否同樣缺日誌，屬同型問題但範圍更大——依規則 5 登記 spawn request 交 PM，不在本票擴張。'
acceptance:
- '[ ] denied fallback 的四個決策點皆有日誌：立即或延後分支、顯示入口、context 未 mounted 的早退、延後後的實際顯示'
- '[ ] context 未 mounted 的早退為 warning 等級，且日誌內容足以分辨它與正常顯示'
- '[ ] 測試斷言以呼叫次數或結構化欄位為準，未比對中文散文字面（避免與 0.1.0-W3-132 衝突）'
- '[ ] 其他 AppSnackBar.show 呼叫點的同型問題已登記 spawn request，未在本票擴張範圍'
- '[ ] 全量 flutter test 通過，指令與通過數記入 Test Results'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補上 掃描完成通知路徑的使用者回饋動作日誌

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-078 | chars: 500 -->

### Task Reference
- 0.1.0-W3-078 what: 實機驗證 MacosScanNotifier 四項平台事實並回填 SPEC-003

### Rationale Chain
- 0.1.0-W3-078 why: W3-064 acceptance 2、3 承接，平台事實只有實機能驗

### Related Files
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W3-078
- lib/services/macos_scan_notifier.dart::read  # from 0.1.0-W3-078
- macos/Runner/AppDelegate.swift::read  # from 0.1.0-W3-078

### Source Acceptance (reference)
- [ref] [ ] 實機測得 UNUserNotificationCenter 在沙盒關閉（PROP-001）+ Developer ID 簽章下的可用性，並記錄 debug build 未簽章時授權請求的實際回應（是否直接回錯）  # from 0.1.0-W3-078
- [ref] [ ] Flutter 端載體的選擇已定案並附實測依據（原生 channel 或第三方套件，二擇一）  # from 0.1.0-W3-078
- [ref] [ ] 實機測得撤回已送達通知的 API，以及它對「通知中心已收合」狀態的效果  # from 0.1.0-W3-078
- [ref] [ ] 四項實測結果已回填 SPEC-003 §2.2「實作票驗證」段（原文改為實測記錄，附量測環境與日期）  # from 0.1.0-W3-078
- [ref] [ ] 0.1.0-W3-064 的 acceptance 2、3 已可勾選或已說明為何仍不可勾  # from 0.1.0-W3-078
