---
id: 0.1.0-W1-001
title: 接線 Riverpod 至既有的 ScreenUtilInit 巢狀結構
type: IMP
status: in_progress
version: 0.1.0
wave: 1
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1 展示殼開工：SPEC-001 的 29 個狀態需要一個能承載它們的容器
  final_decision: 只做依賴接線與 Provider 範圍，不含路由（路由拆為 W1-002）
  rationale: 七個畫面若各自帶自己的骨架，前幾張票會重複建同樣的地基；且 ScreenUtilInit 與 MaterialApp 的巢狀順序是 0.0.1 已確立的約束，集中在一張票裡處理比分散在七張票裡各自維護安全
who:
  current: parsley-flutter-developer
  history: {}
what: 接線 Riverpod 至既有的 ScreenUtilInit 巢狀結構
when: 0.1 展示殼的第一張票
where:
  layer: Presentation
  files:
  - pubspec.yaml
  - lib/main.dart
why: tech-decisions Stage 4 已定狀態管理為 Riverpod（理由為與框架的 dart-provider-architecture skill 及 parsley-flutter-developer 代理人生態一致），但 pubspec.yaml 至今無此依賴，lib/ 下只有 main.dart 與 workspace/。沒有骨架，七個畫面無處可掛。
how:
  task_type: Implementation
  strategy: 加入 flutter_riverpod 依賴並以 ProviderScope 包住既有的 ScreenUtilInit + MaterialApp。ScreenUtilInit 必須維持在 MaterialApp 之外才拿得到 MediaQuery，此約束於 0.0.1 確立，不可調換順序。本票只交出 ProviderScope 接線，不建立任何新檔——原策略句要求建立 lib/app/router.dart 與 shell.dart，但那兩個檔是 W1-002 的 where.files，由該票負責（2026-08-27 派發前 PM 更正，理由見 Context Bundle）。
acceptance:
- '[x] pubspec.yaml 含 flutter_riverpod 且 fvm flutter pub get 成功'
- '[x] ProviderScope 包住 ScreenUtilInit，且 ScreenUtilInit 仍在 MaterialApp 之外'
- '[ ] 既有的 test/ 與 integration_test/ 全數維持綠燈'
- '[ ] 四種視窗尺寸下首頁仍不溢位（960x640 / 1280x800 / 1512x982 / 1920x1080）'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-08-27T16:48:49'
completed_at: null
created: '2026-08-27'
updated: '2026-08-27'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

接線 Riverpod 與應用程式骨架

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## Context Bundle

### 派發環境（PM 於 2026-08-27 在 fresh worktree 實測，非推論）

本票以 `isolation: "worktree"` 派發，工作目錄是全新 checkout。三項已驗事實：

| 項目 | 量測 | 結果 |
|------|------|------|
| FVM 可用性 | `cd <worktree> && fvm flutter --version` | Flutter 3.47.1 / Dart 3.13.1，exit 0 |
| 依賴安裝 | `fvm flutter pub get` | Changed 71 dependencies，成功 |
| 測試 baseline | `fvm flutter test` | **All tests passed，8 項** |

`.fvm/` 是 gitignored 的 symlink，fresh worktree 沒有它——**不需要補**，`fvm` 讀入庫的 `.fvmrc`（釘 3.47.1）即可解析。這一點已實測，不要花時間重建 symlink 或改用系統 flutter。

`.dart_tool/` 同樣不在版控，**動任何 build 前先跑 `fvm flutter pub get`**。

`lib/l10n/app_localizations*.dart` 是刻意入庫的生成產物，fresh worktree 已有，不需先跑 gen-l10n 才能編譯。

**baseline 是綠的**。開工後若見既有測試紅燈，那是本次改動造成的，不是環境噪音——不要歸因於並行資源耗盡（該誤判有前例，見 `IMP-APP-003`）。

### PM 對 dispatch-readiness 兩項警告的覆核

| 警告 | 裁決 |
|------|------|
| 閾值 1：acceptance 條目 > 2，建議拆分 | **不拆**。該閾值以 acceptance 條目數近似「功能職責數」，本票的條目是同一件工作的多個驗收面向，非多個職責。`where.files` 與 Context Bundle 兩項硬閾值皆 PASS |
| 檢查 4：acceptance 提測試但 `where.files` 無測試路徑（僅 W1-001 / W1-009） | **啟發式誤報**。「既有測試維持綠燈」是回歸防護，不是要求新增測試。若你判斷本票確實需要新增測試檔，**停手並以 `ticket track append-log <id> "..." --section NeedsContext` 上報**，不要逕自寫到 `where.files` 之外 |

### 收尾注意（worktree 專屬）

收尾用 `ticket track finish <id> --as parsley-flutter-developer`，**不要用 `complete`**——worktree isolation guard 對 argv 逐元素比對 basename，`complete` 命中 bash builtin 而被條件性阻擋。`finish` 是等價別名。

---

### 派發前更正：strategy 的跨票撞車（2026-08-27，PM）

原 strategy 末句要求「建立 lib/app/router.dart 與 shell.dart 放路由與導覽骨架」。該兩檔是 **W1-002 的 `where.files`**，W1-002 的任務正是「建立六項導覽的路由殼」並有自己的驗收（六個導覽項、佔位頁、切換浮層入口）。兩票對同一組檔案各有主張。

`ticket track conflicts` 未攔截此項——它比對 `where.files` 欄位，而本票的 `where.files` 只有 `pubspec.yaml` 與 `lib/main.dart`，撞車藏在 `how.strategy` 的散文裡，那個欄位檢查器不讀。

**處置**：strategy 已更正為「本票只交出 ProviderScope 接線，不建立任何新檔」。`acceptance` 未動（PC-055：驗收條件不因執行期發現而回改），四條驗收本來就沒有一條提到 router 或 shell。

**對你的意思**：`where.files` 就是完整的可寫範圍，`pubspec.yaml` 與 `lib/main.dart` 兩個檔。若實作過程判斷確實需要新檔才能交出 ProviderScope 接線，停手以 NeedsContext 上報，不要逕自建立。
### 派發指引（PM，2026-08-27）

開工第一步執行 `git merge main`——`isolation: "worktree"` 的 base 取 `origin/main` 而非 local main HEAD。派發前 PM 已 push 對齊，本步為保險。

收尾用 `ticket track finish <id> --as parsley-flutter-developer`，**不要用 `complete`**。worktree isolation guard 對 argv 逐元素比對 basename，`complete` 命中 bash builtin `complete` 而被條件性阻擋（同一操作五次派發兩擋三過）。`finish` 是完全等價的別名。

派發 prompt 的制式段中 `{exact files}` 與 `<ticket-id>` 是刻意保留的佔位符，實際值取自本票 `where.files` 與票號——prompt 不重述票面已載欄位。
### 精準 staging 制式句（逐字取自 `ticket track dispatch` 骨架）

原應置於派發 prompt，但該骨架全長 39 行，而 `agent-prompt-length-guard-hook.py` 的 `PROMPT_LINE_LIMIT = 30` 為無豁免硬計數（模板關鍵字只影響 Layer 2 軟提示，不減行數），骨架過不了自己的 guard。措辭逐字保留，載體改為票面。

```
Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.
```

`{exact files}` 取自本票 `where.files`，`<ticket-id>` 為本票票號。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending
