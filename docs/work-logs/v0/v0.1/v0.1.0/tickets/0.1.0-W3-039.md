---
id: 0.1.0-W3-039
title: 修復 skill-sync 內容雜湊的平台相依與 clone 的 autocrlf 繼承
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-038
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三方專案回饋 Windows hash bug，本專案查證屬實
  final_decision: 成 IMP 票 P1，排在 W3-038 之後（同檔）
  rationale: 兩缺陷在本地 cli.py 逐行查證存在；P1 因它使 Windows consumer 的方向報告發出錯誤指令
who:
  current: thyme-python-developer
  history: {}
what: compute_content_hash 的 rel 改用 as_posix()，三個 clone 呼叫點加 -c core.autocrlf=false，並補不需 Windows runner 的回歸測試
when: W3-038 完成讓出 cli.py 之後；本票阻擋任何 Windows consumer 的正常同步
where:
  layer: framework
  files:
  - .claude/skills/skill-sync/skill_sync/cli.py::write
  - .claude/skills/skill-sync/tests/test_cli.py::write
why: 兩個獨立缺陷使 Windows 上的內容雜湊與 POSIX 不同。其一 compute_content_hash 把 str(Path) 的原生分隔符餵進 hasher（本地 cli.py:529 -> :537），其二三個 clone 呼叫點繼承 Git for Windows 系統層的 core.autocrlf=true 使暫存 checkout 變 CRLF（本地 :1083 :1147 :1514）。量測：兩者皆未修 0/66 命中、只修 autocrlf 13/66、只修分隔符 0/66、兩者皆修 66/66；那 13 個恰為無子目錄的 skill，全 66 筆零例外。更嚴重的是取得 .skill-sync-base 之後三點判定不再回報 unknown 而發出自信的錯誤 SHOULD PULL 指令，照做會重新引入 CRLF。同檔 cli.py:331 已是正確寫法 as_posix()，同一檔內兩處同義操作寫法不同。診斷與重現由一個 Windows consumer 專案提供，本專案在 macOS 查證兩缺陷存在。framework issue 為 86
how:
  task_type: Implementation
  strategy: 先寫紅燈測試（含子目錄的 fixture skill + 硬編碼 digest 斷言，分隔符相依實作在任何平台皆失敗）-> 改 :529 為 as_posix() -> 三個 clone 加 -c core.autocrlf=false -> 確認 POSIX 上既有雜湊不變（該修法在 POSIX 為 no-op）-> 全套件回歸
acceptance:
- '[x] compute_content_hash 使用 as_posix()，且新增的硬編碼 digest 測試在本機通過'
- '[x] 三個 clone 呼叫點皆帶 -c core.autocrlf=false（含 push 讀取的那個暫存 clone）'
- '[x] 修法在 POSIX 上不改變任何既有雜湊，既有 test_cli.py 全數通過'
- '[x] 全檔 str(...relative_to...) 命中歸零，as_posix() 為 5 處（compute_content_hash / compute_diff 兩處 / prune / 既有那一處）'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-07T11:43:55'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

compute_content_hash 的 rel 改用 as_posix()，三個 clone 呼叫點加 -c core.autocrlf=false，並補不需 Windows runner 的回歸測試

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 範圍擴充：全檔統一，不只修 compute_content_hash（flutter_balance 建議，本側查證後採納）

該側指出除了 `compute_content_hash`，`compute_diff` 與 prune 也用 `str(f.relative_to(...))`
作路徑鍵。**它們不影響 manifest**（同機兩側一致），但若日後有人拿 diff 鍵去對 manifest
就會踩到同一顆。本側全檔盤點後採納此建議，並補上該側未列的第四處。

**全檔盤點（2026-09-07，指令見下）**：

| 寫法 | 命中 | 所在 |
|------|------|------|
| `str(...relative_to...)` | **4** | `compute_content_hash`（影響 manifest）／`compute_diff` 的 src 與 dst 兩處／prune 的 `directory.relative_to(dst)` |
| `.as_posix()` | **1** | 另一處計算 rel 的位置 |

**一比四**——正確寫法是全檔的少數。這使「疏漏而非設計」的判斷更確定。

```bash
C=.claude/skills/skill-sync/skill_sync/cli.py
grep -n "str(.*relative_to" "$C"   # 應歸零
grep -n "as_posix()" "$C"          # 應為 5 處
```

**票面刻意不寫死行號**：盤點當下 `compute_content_hash` 的 rel 在 :553，而
framework issue 86 記的是 :529——兩者相差 24 行是因為 `0.1.0-W3-038` 的代理人正在改
同一個檔案。行號是會漂的內嵌計數，執行時請以上述 grep 重新定位，不要沿用任何一份
文件裡的數字。

**修法對 manifest 的影響仍為零**：四處在 POSIX 上 `str()` 與 `.as_posix()` 輸出相同，
既有 `versions.json` 不受影響。這一點對四處都成立，不只對 `compute_content_hash`。

### 派發日誌

- [2026-09-07 11:43] 票面 Problem Analysis 有全檔盤點與 grep 指令——不要沿用任何文件裡的行號，issue 86 記的 :529 已經漂到 :553 又被 W3-038 再推移。grep pattern 不可綁變數名（prune 那處變數是 directory 不是 f）。同檔的 _stale_version_warning 與 _format_violation_lines 是前兩張票的產物，不要捲進來。

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-038 | chars: 548 -->

### Task Reference
- 0.1.0-W3-038 what: 把「跳過互動確認」與「旁路 portability 閘門」拆為兩個旗標，或至少在旁路閘門時於 stdout 明示（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-038 why: --help 只寫 Apply changes without confirmation，而 SKILL.md 另載明 --force 同時旁路 portability 閘門（違規清單寫入 hook-logs 的 jsonl）。一次實測中代理人為跳過互動確認而加 --force，未造成損害純粹因為目標 skill 未宣告 metadata.portable true——未宣告者本就不會 abort，是條件湊巧不成立而非旗標安全。同一指令換一支已宣告 portable 的 skill，我只是想跳過確認會靜默變成我旁路了違規檢查，而唯一的痕跡在使用者不會去看的 jsonl。經 flutter_balance 專案交叉檢視確認該側無對應票，本項由本側持有（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/skill-sync/SKILL.md::write  # from 0.1.0-W3-038（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 旁路 portability 閘門時，違規摘要出現在 stdout 而非僅寫入 jsonl  # from 0.1.0-W3-038（來源已完成，結論見 Solution）
- [ref] [x] --help 文字涵蓋該旗標的全部語意，不只跳過確認  # from 0.1.0-W3-038（來源已完成，結論見 Solution）
- [ref] [x] 既有 test_cli.py 全數通過，新增測試覆蓋旁路時的 stdout 輸出  # from 0.1.0-W3-038（來源已完成，結論見 Solution）
