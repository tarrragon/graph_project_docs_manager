---
id: 0.1.0-W3-274
title: 掃描並修復 存量 issue 的孤兒區段索引標題
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-269
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-269
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-269 只修此後不再產生，未處理已產生的存量
  final_decision: 另開一票做一次跨 issue 掃描與修復
  rationale: 存量落在 GitHub issue 而非本 repo，且既有 show／check 對此零覆蓋，不掃描即永不被發現
who:
  current: 主線程
  history: {}
what: 掃描並修復 存量 issue 的孤兒區段索引標題
when: 0.1.0-W3-269 落地後
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/section_comment.py
why: 0.1.0-W3-269 修的是 add／init 此後不再產生孤兒標題，未處理已產生的存量。tarrragon/claude#82 已查證屬實；其他曾以 add／init 併入手寫索引的 open issue 可能留有同型孤兒標題——宣稱是入口的區段底下沒有表格，而 show 與 check 對此皆通過（兩者只驗工具索引與區段的一致性），存量不會被任何既有檢查發現。範圍未知，需一次跨 issue 掃描確認。修法後執行任一次 add／init 不會自動修復存量：_strip_raw_index_rows 在 body 已含 section-index 標記時提前返回，孤兒標題不在其射程內。
how:
  task_type: Implementation
  strategy: 以 gh 列出 tarrragon/claude 的 open issue，逐張抓 body，判定條件為『## 區段索引 出現次數 > 1』或『該標題底下至今無表格列』。命中者人工核對後移除孤兒標題與其導言（導言若含入口宣稱則併入工具索引區塊）。若命中數多，評估是否值得為存量修復加一個一次性子命令而非逐張手改。
acceptance:
- '[ ] 已列出 tarrragon/claude 全部 open issue 並逐張判定是否含孤兒區段索引標題，命中清單記入本票'
- '[ ] 命中的 issue 已修復：body 內區段索引標題只出現一次，導言（若有）位於工具索引區塊內'
- '[ ] 若判定不修（例如 issue 已 close 或無命中），於本票逐項寫明理由'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

掃描並修復 存量 issue 的孤兒區段索引標題

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-269 | chars: 1955 -->

### Task Reference
- 0.1.0-W3-269 what: 修正 add 併入手寫索引表的列卻遺留其標題與導言，使宣稱為入口的區塊底下沒有表格
- 0.1.0-W3-269 what: 修正 add 併入手寫索引表的列卻遺留其標題與導言，使宣稱為入口的區塊底下沒有表格

### Rationale Chain
- 0.1.0-W3-269 why: `add` 對含手寫索引的 issue 只做了一半：**表格列正確併入工具索引，但手寫索引的標題與導言留在原處**，於是 body 出現兩個「## 區段索引」標題，而第一個底下沒有表格。

**已在 `tarrragon/claude#82` 實際發生並經查證**：該 issue 第 40 行的手寫標題底下寫的是「**入口從此處進，不從 comment 列表找**」，接著是導言與一段 `gh api` 重生指令；真正的索引表在 23 行之後的工具標記區塊內。讀者照著那句明示的入口宣稱進來，看不到任何索引。對照組 `#56`（原本就有工具標記）行為正確，故缺陷限於「手寫索引無工具標記」這一情境。

**配方對此情境的敘述射程過寬**：`ticket-intake.md` 步驟二第三列寫「`add`／`init` 現已正確併入手寫表既有列，**不再產生第二張索引表**」。列確實沒有重複——修法處理了表格層；但標題重複了，而讀者從那句話會判定整個情境已被覆蓋，不會去檢查標題。**修復的射程與敘述的射程不一致，且敘述較寬**。

**為何是 P1 而非低優先**：受損的不是外觀而是導航。手寫導言那句話是一個明示的承諾（從這裡進），併入操作使該承諾變成假的，而**執行併入的人看不到這個後果**——`show` 與 `check` 都只驗工具索引與區段的一致性，兩者在此情境下全部通過。這與本 skill 已記錄的其他缺陷同族：操作成功、驗證通過、而被承諾的那件事沒有成立。

**存量影響未知**：本次只查證 `#82` 一例。其他曾以 `add`／`init` 併入手寫索引的 issue 可能有同樣的孤兒標題，需要一次掃描確認範圍，該掃描不在本票，是本票落地後的後續。
- 0.1.0-W3-269 why: `add` 對含手寫索引的 issue 只做了一半：**表格列正確併入工具索引，但手寫索引的標題與導言留在原處**，於是 body 出現兩個「## 區段索引」標題，而第一個底下沒有表格。

**已在 `tarrragon/claude#82` 實際發生並經查證**：該 issue 第 40 行的手寫標題底下寫的是「**入口從此處進，不從 comment 列表找**」，接著是導言與一段 `gh api` 重生指令；真正的索引表在 23 行之後的工具標記區塊內。讀者照著那句明示的入口宣稱進來，看不到任何索引。對照組 `#56`（原本就有工具標記）行為正確，故缺陷限於「手寫索引無工具標記」這一情境。

**配方對此情境的敘述射程過寬**：`ticket-intake.md` 步驟二第三列寫「`add`／`init` 現已正確併入手寫表既有列，**不再產生第二張索引表**」。列確實沒有重複——修法處理了表格層；但標題重複了，而讀者從那句話會判定整個情境已被覆蓋，不會去檢查標題。**修復的射程與敘述的射程不一致，且敘述較寬**。

**為何是 P1 而非低優先**：受損的不是外觀而是導航。手寫導言那句話是一個明示的承諾（從這裡進），併入操作使該承諾變成假的，而**執行併入的人看不到這個後果**——`show` 與 `check` 都只驗工具索引與區段的一致性，兩者在此情境下全部通過。這與本 skill 已記錄的其他缺陷同族：操作成功、驗證通過、而被承諾的那件事沒有成立。

**存量影響未知**：本次只查證 `#82` 一例。其他曾以 `add`／`init` 併入手寫索引的 issue 可能有同樣的孤兒標題，需要一次掃描確認範圍，該掃描不在本票，是本票落地後的後續。

### Related Files
- .claude/skills/framework-issue/references/ticket-intake.md  # from 0.1.0-W3-269

### Source Acceptance (reference)
- [ref] [ ] 對含手寫索引（標題＋導言＋表格、無工具標記）的 issue 執行 add／init 後，body 中「區段索引」標題只出現一次  # from 0.1.0-W3-269
- [ref] [ ] 手寫索引的導言若含入口宣稱，併入時一併遷移至工具索引區塊，不留在失去表格的舊標題下  # from 0.1.0-W3-269
- [ref] [ ] 新增測試案例：輸入為含手寫索引的 body，斷言輸出的標題出現次數為 1（現行實作下此斷言為紅）  # from 0.1.0-W3-269
- [ref] [ ] ticket-intake.md 步驟二該列的「不再產生第二張索引表」修正為涵蓋標題層，或明寫其射程只到表格列  # from 0.1.0-W3-269
- [ref] [ ] 對含手寫索引（標題＋導言＋表格、無工具標記）的 issue 執行 add／init 後，body 中「區段索引」標題只出現一次  # from 0.1.0-W3-269
- [ref] [ ] 手寫索引的導言若含入口宣稱，併入時一併遷移至工具索引區塊，不留在失去表格的舊標題下  # from 0.1.0-W3-269
  ... (truncated, see source ticket)

