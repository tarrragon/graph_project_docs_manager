---
id: 0.1.0-W3-026
title: 修復 component-contract-design 的 portability 違規使其可發佈
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.1.0-W3-025
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: skill-sync push 被 portability 閘門阻擋
  final_decision: 建 DOC 票修違規，不以 --force 旁路
  rationale: 閘門擋的是真違規；--force 會把違規內容發佈到共用庫
who:
  current: thyme-documentation-integrator
  history: {}
what: 移除或改寫 references/addresses.md 第 15、16 行指名本專案檔案的兩處路徑，使 skill-sync push 通過 portability 閘門
when: 下次要發佈 component-contract-design 前
where:
  layer: framework
  files:
  - .claude/skills/component-contract-design/references/addresses.md::write
why: 該 skill 宣告 metadata.portable true 卻指名本專案的 .claude/methodologies/ 與 .claude/skills/doc/templates/ 兩個檔案路徑。閘門因此擋下 push，本地 1.5.2 無法發佈而遠端停在 1.0.0。強推等於發佈一支指著別人專案檔案路徑的 skill，正是該閘門存在的理由
how:
  task_type: Documentation
  strategy: 讀 addresses.md 第 15-16 行 -> 二擇一：保留句意刪去路徑，或整段移入 references/project-integration/（該目錄排除於 sync）-> 重跑 skill-sync push 確認閘門通過
acceptance:
- '[x] skill-sync push component-contract-design 不再被 portability 閘門擋下'
- '[x] 發佈庫的 component-contract-design 版號由 1.0.0 更新至本地版號'
- '[x] 改寫後該段句意仍完整，不因刪路徑而失去可執行性'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-07T10:22:37'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

移除或改寫 references/addresses.md 第 15、16 行指名本專案檔案的兩處路徑，使 skill-sync push 通過 portability 閘門

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-07 10:21] 閘門原文已在票的 Why 欄。二擇一：保留句意刪路徑，或整段移入 references/project-integration/（該目錄排除於 sync）。禁止以 --force 旁路閘門。

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
- 根因：`references/addresses.md` 第 15、16 列（方法論、元件庫規格範本）指名本專案的 `.claude/methodologies/component-library-bidirectional-constraint-methodology.md` 與 `.claude/skills/doc/templates/component-library-spec-template.md` 兩個路徑，與 SKILL.md frontmatter 宣告的 `metadata.portable: true` 矛盾，被 skill-sync 的 portability 閘門擋下（`check_portability` 的 `_CONSUMER_PATH_RE` 命中 `.claude/...`）。
- 採用選項二（整段移入 `references/project-integration/`，該目錄依 skill-sync 的 `EXCLUDE_DIRS` 排除於 sync）：新增 `references/project-integration/README.md`，比照本專案既有的 `wrap-decision` skill 慣例（同名目錄、單向依賴方向：通用 references 不得引用該目錄，該目錄可引用通用 references 的簡稱作上游），承接方法論與範本在本專案的實際路徑。
- `addresses.md` 兩列的地址欄改為指向 `references/project-integration/README.md`，句意保留完整（是什麼欄未動），只是路徑耦合搬到排除同步的目錄。
- 過程中發現 CHANGELOG.md 新增條目本身若寫入字面 `.claude/...` 路徑也會觸發同一閘門（`.py` 之外 `.md` 全文皆掃），已改寫為不含字面路徑的描述，`check_portability` 對整個 skill 目錄回傳空清單後才執行 push。
- 版號依專案雙住址規則同步：SKILL.md frontmatter `1.5.2 -> 1.5.3`，CHANGELOG.md 新增對應條目。
- 已執行 `skill-sync push component-contract-design`（無 --force），實際推送成功；已用 `curl` 驗證遠端 SKILL.md 版號為 1.5.3、`references/project-integration/README.md` 回 404（確認排除同步生效）、遠端 addresses.md 全文 grep 無 `.claude/` 殘留。

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程或 thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-07T10:29
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

變更摘要：
- `.claude/skills/component-contract-design/references/addresses.md` — 第 15、16 列（方法論、元件庫規格範本）地址欄由字面 `.claude/...` 路徑改為指向新增的 `references/project-integration/README.md`
- `.claude/skills/component-contract-design/references/project-integration/README.md`（新增）— 承接方法論與範本在本專案的實際路徑，排除於 skill-sync push
- `.claude/skills/component-contract-design/SKILL.md` — frontmatter `metadata.version` 由 1.5.2 更新為 1.5.3
- `.claude/skills/component-contract-design/CHANGELOG.md` — 新增 1.5.3 條目說明修法與理由
- 已執行 `skill-sync push component-contract-design`，發佈庫版號由 1.0.0 更新至 1.5.3，`project-integration/` 確認未同步
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-025 | chars: 419 -->

### Task Reference
- 0.1.0-W3-025 what: 逐支比對本地與 claude-skills.git 的差異，判定該 push 或 pull，並釐清為何改了內容卻未 bump 版號

### Rationale Chain
- 0.1.0-W3-025 why: 版號相同而內容不同代表至少一邊改了東西沒 bump 版號。版號在此情況下不只無鑑別力，而是誤導性的——它主動宣稱兩邊一致。sync-push 的措辭已明示內容雜湊只證明不同、不證明誰較新

### Related Files
- .claude/skills/ticket::read  # from 0.1.0-W3-025
- .claude/skills/framework-issue::read  # from 0.1.0-W3-025
- .claude/skills/tdd::read  # from 0.1.0-W3-025
- .claude/skills/wrap-decision::read  # from 0.1.0-W3-025
- .claude/skills/skill-sync::read  # from 0.1.0-W3-025
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [ ] 六支各有明確方向判定與依據，非憑版號推斷  # from 0.1.0-W3-025
- [ref] [ ] 已執行對應的 skill-sync push 或 pull，再跑 sync-push 時該六支不再出現在分歧清單  # from 0.1.0-W3-025
- [ref] [ ] 改了內容未 bump 版號的一方已補上版號  # from 0.1.0-W3-025
