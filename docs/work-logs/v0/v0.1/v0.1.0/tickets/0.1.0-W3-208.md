---
id: 0.1.0-W3-208
title: 修復 set-acceptance --add 重複加上核取方塊前綴，並更正 0.1.0-W3-169 既有兩列
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-169
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: PM 驗收 0.1.0-W3-169 時發現自建的兩列 acceptance 帶雙重前綴
  final_decision: 建 IMP 票修工具與資料，不對已完成票逕自動用逃生閥
  rationale: 用戶裁示開新票處理不回頭修；且根因在 CLI 沉默接受重複前綴，只改資料下次仍會復發
who:
  current: thyme-python-developer
  history: {}
what: 修復 set-acceptance --add 重複加上核取方塊前綴，並更正 0.1.0-W3-169 既有兩列
when: 隨時，不阻擋任何進行中的票
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-169.md
why: '`ticket track set-acceptance --add` 的參數語意是「條目文字」，CLI 自行補 `[ ]` 前綴。

  但介面沒有任何地方阻止呼叫者把 `[ ]` 也寫進文字，寫了就得到雙重前綴。


  實例：`0.1.0-W3-169` 的第 8、9 兩列現為 `''[x] [ ] 0.1.0-W3-167 裁定 A ...''`

  （PM 於 2026-09-09 以 `--add` 建立時帶入字面 `[ ]`）。


  **後果不是排版醜。** `check-acceptance` 認前綴故判定正確，票面顯示也正常——

  唯一的受害者是「以 `grep ''\[ \]''` 跨票掃未完成 acceptance」這個動作，它會在一張

  已完成的票上得到兩個誤命中，且該票 9/9 全勾、狀態 completed，掃描者沒有任何

  線索可判斷這是假陽性。錯誤形態與 `IMP-BAL-013`（locale 使 `uniq` 靜默合併

  相異 CJK 字串）同族：總數守恆、狀態正確、唯獨分組錯誤，且無警告。


  **根因在工具不在使用者。** 依 `opinionated-default-design` 主張 1，文件寫「別把

  `[ ]` 寫進 `--add` 的文字」不會贏過工具的預設行為——工具沉默接受就會一直有人寫。'
how:
  task_type: Implementation
  strategy: '兩件事，順序不可反：先修工具，再修資料。反過來會讓下一次 `--add` 重新製造同型資料。


    **一、CLI 層**：`set-acceptance` 的 `--add` 與 `--edit` 在寫入前正規化條目文字——

    剝除開頭的核取方塊字面（`[ ]` / `[x] ` / `[X]`，含其後空白），剝除後再由既有邏輯

    補正規前綴。**不要用拒絕（exit 非零）**：呼叫者的意圖明確無歧義，拒絕只是把工作

    推回給人；靜默正規化才是此處的正確預設。同一支檔案內若 `--edit` 走的是另一條

    寫入路徑，兩條都要過同一個正規化函式，不可只修一條。


    測試須含：`--add` 帶 `[ ]` 前綴、帶 `[x] ` 前綴、不帶前綴（回歸）、文字中段含

    `[ ]` 但不在開頭（**不得被剝除**，那是內容不是前綴）。


    **二、資料層**：更正 `0.1.0-W3-169` 第 8、9 兩列為單一前綴，勾選狀態維持 `[x]`。

    該票 status 為 completed，`set-acceptance` 會擋，需 `--force`——**本票即為該

    逃生閥的說明理由**，於 Completion Info 註明「依本票授權，非繞過驗收」。

    只改這兩列的前綴，條目文字與勾選狀態一字不動。


    **三、掃描確認**：改完後 `grep -rn "\[x\] \[ \]\|\[ \] \[ \]" docs/work-logs/`

    確認全 repo 無同型殘留；若命中其他票，逐一列出交 PM，不自行擴大修改範圍。'
acceptance:
- '[ ] set-acceptance 的 --add 與 --edit 皆會剝除開頭的核取方塊字面後再補正規前綴,文字中段的 [ ] 不被剝除且有對應測試,不帶前綴的既有呼叫行為不變（回歸測試）,0.1.0-W3-169 第 8 與第 9 列已更正為單一前綴且勾選狀態與條目文字未變,--force 的使用理由已記入該票 Completion Info,全 repo 掃描確認無其他同型殘留或已列出交 PM'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-09T15:19:30'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修復 set-acceptance --add 重複加上核取方塊前綴，並更正 0.1.0-W3-169 既有兩列

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

`set-acceptance` 的 `--add` / `--edit` 只負責「補正規前綴」，沒有在寫入前剝除
呼叫者可能誤帶入的既有核取方塊字面。呼叫者若複製票面既有字面（本身已帶
`[ ]`/`[x]`）傳入，得到雙重前綴。

### 影響範圍

- `.claude/skills/ticket/ticket_system/commands/track_set_acceptance.py`：
  `_apply_add`、`_apply_edit` 兩條寫入路徑皆受影響。
- `0.1.0-W3-169` 第 8、9 兩列既有資料帶雙重前綴（已更正，見 Solution）。

### 相關 Error Pattern

與 `IMP-BAL-013`（locale 使 `uniq` 靜默合併相異 CJK 字串）同族：總數守恆、
狀態正確、唯獨分組錯誤，且無警告。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-169 | chars: 1182 -->

### Task Reference
- 0.1.0-W3-169 what: 盤點 全部 SPEC 的狀態提示設計，補提示元件需求與提示訊息設計（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-169 why: **用戶裁示（2026-09-09）**：「檢查所有 SPEC 當中對於狀態提示的設計，加入相關提示元件的需求以及提示訊息的設計。」

狀態提示的設計目前**散在三份 SPEC**，各自回答不同的一半：

| SPEC | 目前承載 | 缺的 |
|------|---------|------|
| SPEC-001 畫面狀態矩陣 | 有哪些狀態、各狀態的退出路徑 | 狀態**轉換**時是否需要提示、需要哪種 |
| SPEC-003 互動反應 | §2.2 三層回饋、§2.12 服務類型與等待期政策、§2.6 等待指示形式 | 提示載體的選擇判準（何時用暫態提示而非狀態轉換本身） |
| SPEC-004 元件庫 | §1 回饋通道子表、§4.26 `AppSnackBar` 條目、逐元件回饋契約 | 提示訊息的設計要求（文案結構、i18n key 命名、可讀性保障） |

**只改其中一份的後果是三者對同一件事給出不同答案。** 本專案已有先例：`0.1.0-W3-108` 補 SPEC-003 §2.12 時發現 §2.2 既有的三層表是形式軸、新增的是歸屬軸，兩軸並存而不對接會使讀者無法判斷承擔者——當時靠 PM 在 acceptance 補了一條才接上。三份 SPEC 之間的落差比一份之內更難察覺。

**「提示訊息的設計」是目前完全沒有規格的一塊。** SPEC-003 §2.2 有零星的 i18n key 清單（`scanCompleteSnackbarMessage` 等），但沒有任何條文規範提示文案本身該長什麼樣——該含哪些成分、多長、如何處理 placeholder、失敗訊息與成功訊息的措辭差異。`ux-design-evaluation` 有「關鍵回饋文字要有版面保障」（flex 寬度競爭把關鍵資訊壓成刪節號，state 正確而使用者零資訊），那是跨專案判準，本專案未落地。（來源已完成，結論見 Solution）

### Related Files
- docs/spec/ui/SPEC-001-screen-state-matrix.md  # from 0.1.0-W3-169（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W3-169（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-004-component-library.md  # from 0.1.0-W3-169（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三份 SPEC 的既有狀態提示條文已逐一盤點，標出各自回答哪一半與彼此是否一致  # from 0.1.0-W3-169（來源已完成，結論見 Solution）
- [ref] [x] 不一致者列為發現記入 Solution 交 PM，未自行調和  # from 0.1.0-W3-169（來源已完成，結論見 Solution）
- [ref] [x] SPEC-001 依判準逐狀態標註是否需要提示，未為每個狀態都加  # from 0.1.0-W3-169（來源已完成，結論見 Solution）
- [ref] [x] SPEC-003 的載體選擇條文與既有 2.2 三層回饋、2.12 服務類型政策對接而非並列  # from 0.1.0-W3-169（來源已完成，結論見 Solution）
- [ref] [x] SPEC-004 的提示訊息設計要求落在既有回饋契約子節結構內，未新增平行清單  # from 0.1.0-W3-169（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
