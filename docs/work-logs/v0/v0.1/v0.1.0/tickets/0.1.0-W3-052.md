---
id: 0.1.0-W3-052
title: 修復 PM claim 後代理人無法 complete 的身份死結
type: DOC
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-python-developer
  history: {}
what: claim 時 who.current 停在 PM，代理人 complete 兩條路徑皆被擋，須給出合法出口
when: 本 session 三個代理人各自撞上後
where:
  layer: framework
  files:
  - .claude/skills/ticket/ticket_system/
  - .claude/hooks/
why: PM 依 pm-role 流程先 claim 再派發時，ticket 的 who.current 為 PM（本專案為 rosemary-project-manager）。代理人收尾時兩條路都封死：帶 --as <自己的 agent 名> 被 identity-guard 以身份不符拒絕（PC-V1-002），不帶 --as 則被要求必須提供。本 session 三個代理人（W3-003、W3-004、W3-018）各自獨立撞上同一狀態，其中一個以 ticket track set-who --current 自行轉移後通過，兩個停下來回報 PM 並等待——後者是正確處置，因為 who 欄位是權責歸屬，不該由執行者自行改。封死它的兩條規則各自正確：identity-guard 防甲代理人替乙代理人收尾，--as 必填防不具名收尾；缺口在兩者的交集無合法出口，且不會有任何一方報錯指出自己有問題，只會表現為執行者反覆卡住。三次同型阻擋且無人自曝，屬結構缺口非偶發。
how:
  task_type: Implementation
  strategy: 先判定意圖：PM claim 的語意是「這張票由我調度」還是「這張票由我執行」，兩者若同形則問題在 claim 本身。可能方向三選一或組合——claim 時支援 --for <agent> 直接把 who.current 設為預定執行者；或 complete 在 who.current 為 PM 角色時放行任一具名 agent 並記錄實際執行者；或把 set-who 明訂為派發流程的一步並寫進 agent-dispatch-template。無論採哪個，錯誤訊息要指出出口——現行兩則訊息各自正確卻都不說另一條路，是使用者卡住的直接原因。
acceptance:
- '[ ] PM claim 後派發的票，代理人能以自己的身份完成 complete，不需 PM 代跑也不需自行改 who'
- '[ ] 被擋時的錯誤訊息含合法出口的具體指令，不只說明為何被擋'
- '[ ] 三個實例（W3-003 自行 set-who、W3-004 與 W3-018 停下回報）在票面留存為判準來源，使後續讀者能判斷自己的情況是否落在同一形態'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

claim 時 who.current 停在 PM，代理人 complete 兩條路徑皆被擋，須給出合法出口

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
