---
id: 0.1.0-W3-248
title: 補寫 SPEC-003 指派表遺漏的 chooseFolderUnavailableMessage
type: DOC
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-207
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-207 Phase 1 的 SR-1
  final_decision: 建 DOC 票補齊，並要求回頭做一次來源盤點
  rationale: 只補一列防不了下一次；成因是逐一指定時缺少來源清單作對照，補盤點才是修因
who:
  current: thyme-documentation-integrator
  history: {}
what: 補寫 SPEC-003 指派表遺漏的 chooseFolderUnavailableMessage
when: 0.1.0-W3-207 Phase 1 盤點仲裁器需承接的提示來源時發現
where:
  layer: Presentation
  files:
  - docs/spec/ui/SPEC-003-interaction-response.md
why: lib/main.dart:220 呼叫 l10n.chooseFolderUnavailableMessage 顯示一則 SnackBar，而該訊息既不在 SPEC-003 §2.14 指派表的既有列中，也不在該表的排除清單裡（實查：SPEC-003 全文對該識別符與「資料夾不可用」皆零命中）。0.1.0-W3-204 的 how 把「資料夾不可用」列為須逐一指定的訊息之一，該項未落地，故 204 的「逐一指定本專案現有的每一則提示訊息」在此處未成立。缺漏的後果不只是表少一列：0.1.0-W3-205 要把截斷條件綁在級別上，級別取值以該表為權威；表中沒有的訊息在實作時無級別可取，只能由實作者當場判斷，正是該表要消除的東西。
how:
  task_type: Documentation
  strategy: 在 SPEC-003 §2.14 指派表補該訊息一列，逐欄填到達類別、級別、標定點、傳播規則、載體，判準以標題文字引用事件流負載仲裁方法論的〈到達類別〉〈級別〉並連同其「它回答的問題」欄。另回頭核對一次：以 grep 列出 lib/ 內全部 SnackBar 與系統通知的呼叫點，逐一比對指派表與排除清單，把差集補齊或補進排除清單並附排除理由——本票的成因是逐一指定時漏了來源盤點，只補一列不防下一次。若差集不只這一則，逐則補齊仍在本票範圍。
acceptance:
- '[ ] chooseFolderUnavailableMessage 已在指派表中有一列，五欄齊備'
- '[ ] lib/ 內全部提示呼叫點已列出並逐一比對指派表與排除清單，差集為零或已補齊'
- '[ ] 排除清單中的每一則附排除理由'
- '[ ] ticket track phase 的 frontmatter 變更不再靜默停在工作區（自動提交或明確提示二選一，且已查證原設計是刻意排除還是遺漏）'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補寫 SPEC-003 指派表遺漏的 chooseFolderUnavailableMessage

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### 併入的第二項：`ticket track phase` 的 frontmatter 變更不被 auto-commit 涵蓋（2026-09-10）

由 `0.1.0-W3-176` Phase 3a 的 worktree 代理人回報並寫入該票 NeedsContext，PM 已查證。

**三個限制疊起來形成靜默漏失**：

1. `ticket track phase` 改動票 frontmatter（`tdd_phase`），但**不觸發 ticket md auto-commit hook**。查證：`0.1.0-W3-176` 的 Phase 2 轉 Phase 3a 由 commit `ac475e87` 落地，其 message 為「docs(0.1.0-W3-176): Phase 2 測試設計完成，轉 Phase 3a」——是該代理人**手動**提交的，非 auto-commit 產物。
2. worktree 隔離的代理人無法以 `git -C <主 checkout>` 補提交（隔離的用意即在此）。
3. `ticket track commit` 因 `where.files` 範圍檢查拒收票檔——**即本票主文**。

**後果**：若某階段的最後一個動作是 `phase`，該 frontmatter 變更會**靜默停在工作區**——下一階段的代理人讀到的票仍是舊 phase，而全程零錯誤訊號。`0.1.0-W3-176` 本次未發生，僅因 `phase` 之後還有一筆 `append-log` 把它順帶掃入；換句話說，**它這次是被偶然救回來的，不是被機制保證的**。

**與本票主文的關係**：主文修好第 3 點（`ticket track commit` 接受自身 md）即給了代理人一條合法補救路徑，漏失不再無解；但**第 1 點是獨立缺陷**——即使有補救路徑，仍依賴代理人記得補提交。兩項一併修較完整，故併入本票而非另開。

**第二項的修法方向**：使 `phase` 與其他會改動票 md 的子命令走同一條 auto-commit 路徑。**先查證**目前哪些子命令有 auto-commit、`phase` 是刻意排除還是遺漏——若為刻意（例如避免階段轉換與內容變更混在同一 commit），則改為在 `phase` 完成後輸出明確提示要求提交，而非靜默。

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-207 | chars: 632 -->

### Task Reference
- 0.1.0-W3-207 what: 實作 注意力仲裁點：單一持有者依級別裁決跨 domain 的提示請求

### Rationale Chain
- 0.1.0-W3-207 why: `0.1.0-W3-184` 第三節裁定協調形態為**單一仲裁點而非分散式協商**：各 domain 不互相確認，改為向注意力通道的持有者請求，由後者依級別規則裁決。

**為何不能讓 domain 互相確認**：注意力競爭發生在任何兩個會產生注意力請求的 domain 之間、不分層級（協調圖與 `docs/domain-map.md` §2 的依賴圖正交），因此互相確認需要 N² 條協調邊，且迫使 domain 互相知道彼此存在——直接違反 §3 Bundle 界定表的公開面設計。仲裁只需 N 條，且 domain 之間維持互不知情。

**不做則後果**：`0.1.0-W3-204` 的級別規則會停在單一載體內（`AppSnackBar`），跨 domain 的請求仍各自直達載體，「現在是不是請求使用者注意的適當時機」沒有任何地方在回答。

### Related Files
- lib/app/attention_arbiter.dart  # from 0.1.0-W3-207  [路徑已不存在]
- test/unit/app/attention_arbiter_test.dart  # from 0.1.0-W3-207  [路徑已不存在]

### Source Acceptance (reference)
- [ref] [ ] domain 側介面只表達級別，不表達載體與時機  # from 0.1.0-W3-207
- [ref] [ ] 仲裁決策依 SPEC-003 的級別規則，未在仲裁點另立一套  # from 0.1.0-W3-207
- [ref] [ ] 仲裁點未落入任何 domain 內部，落層符合 0.1.0-W3-203 的指定  # from 0.1.0-W3-207
- [ref] [ ] 本票未實作第三類接收緩衝，且在 Solution 明記該範疇歸屬與 trigger  # from 0.1.0-W3-207
