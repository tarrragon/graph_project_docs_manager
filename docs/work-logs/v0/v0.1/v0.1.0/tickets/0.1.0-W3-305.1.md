---
id: 0.1.0-W3-305.1
title: 複驗 skill-design-guide 1.12.0 的改動是否生效（正文分工節、超標診斷、讀者可用性組、splitting reference 四處）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 前一票的改動零量測，且該票自己新增的條文要求修完再跑
  final_decision: 建 pending 票綁定 trigger，不以口頭延後結案
  rationale: decision-trigger-binding 規則 1 禁止無 trigger 延後；投入複驗屬成本判斷應由用戶決定，但該判斷要有可追蹤載體
who:
  current: PM（派探針、彙整）
  history: {}
what: 複驗 skill-design-guide 1.12.0 的改動是否生效（正文分工節、超標診斷、讀者可用性組、splitting reference 四處）
when: 本票為 pending。trigger：用戶決定投入，或本 skill 下次因其他原因被修改時一併跑
where:
  layer: 待定義
  files:
  - .claude/skills/skill-design-guide/SKILL.md
  - .claude/skills/skill-design-guide/references/splitting-an-existing-skill.md
why: 1.12.0 自己新增的〈讀者可用性〉組第五項即「修完再跑一次」，而該項對 1.12.0 本身尚未執行。前一批的教訓明確：某處連續兩輪被命中、兩次修法各失敗一次且方式不同，第一次改段落沒改結構、第二次改對結構但陳述範圍大於事實——不宜以「這次看起來對了」代替量測
how:
  task_type: Analysis
  strategy: 沿用 0.1.0-W3-305 的三批逐字相同指令與餵入範圍，派發前先寫死門檻。重點驗四項：正文與 references 的分工是否不再被回「文件沒說」（基準 2/3）、無 hook 執法與外移判準是否脫離差集全漏（基準各 3/3 全漏）、走完之後是否不再被回「文件沒說」（基準 4/4）、路由表定義是否不再被判不清楚（基準 2/4）
acceptance:
- '[ ] 四項基準各有命中數對照與判定'
- '[ ] 1.12.0 新增條文引入的新 finding 已完成歸因並分處置與不處置兩類'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T14:43:11'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
chain: {}
---

# Execution Log

## Task Summary

複驗 skill-design-guide 1.12.0 的改動是否生效（正文分工節、超標診斷、讀者可用性組、splitting reference 四處）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 派發前登記：五項待驗命題與門檻（2026-09-11，改動落於 34713f76 之後）

門檻先寫死。前一批的教訓：沒有事先登記門檻時，「改善了一半」會被寫成「已改善」。

| # | 命題 | 量測位置 | 1.12.0 之前的基準 | 判定門檻 |
|---|------|---------|-----------------|---------|
| V1 | 正文與 references 的分工不再被回「文件沒說」 | A 批第 4 題最後一項 | **2/3 回「文件沒說」** | 0/3 回「沒說」即生效；1/3 以上仍回即未生效 |
| V2a | 「第 2 層無 hook 執法、完全依賴自查」脫離差集全漏 | B 批第 2 題重點清單 | **3/3 全漏** | 0-1/3 漏即生效 |
| V2b | 「外移的判準與分界」脫離差集全漏 | B 批第 2 題重點清單 | **3/3 全漏** | 0-1/3 漏即生效 |
| V3 | 走完 splitting reference 最後一步之後有交棒 | C 批第 5 題 | **4/4 回「文件沒說」** | 0/3 回「沒說」即生效 |
| V4 | 「路由表」不再被判為無定義 | C 批第 4 題 | **2/4 判不清楚，另 2/4 判清楚卻各指不同位置** | 0-1/3 判不清楚，且判清楚者指向一致，才算生效 |

**附帶量測（無門檻，只記錄）**

- 「只能記住三件事」是否有任一份帶走「怎麼寫一個 skill」而非只有量測（基準 **0/3**）。這一項不設門檻——它取決於三選一的壓縮，樣本量支持不了判決，但方向有意義
- 疲勞點是否仍落在分段估算論證段（基準 3/3 一致）；1.12.0 已把該段壓縮為主張句
- A 批「第一個具體動作」是否仍 2/3 答「跑量測指令」（基準 2/3 量測、1/3 沒說）。1.12.0 在開場補了「先認你在做哪一件事」
- 1.12.0 新增的三節（正文分工、超標診斷、讀者可用性組）引入的新 finding

**派發參數**：三批指令與餵入範圍與 `0.1.0-W3-305` **逐字相同**——A 3 份讀 `SKILL.md`（六題）、B 3 份讀 `SKILL.md`（五題）、C 3 份讀 `references/splitting-an-existing-skill.md`（七題）。

**已知的比對限制**：受測文本在兩批之間變動（那正是被驗的東西），故這是同一儀器對前後兩版的量測，不是同一實驗的重複。三份的樣本量只支持全中／全無這類強訊號，2/3 對 1/3 不下結論。C 批前一輪為 4 份（延遲回報者與重派者皆到齊），本輪 3 份，比例換算時以比率而非份數對照。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: PM（派探針、彙整）
**Review Status**: pending
