---
id: 0.1.0-W3-047
title: 泛化 PC-APP-006 使其涵蓋所有被忽略卻需被追蹤的檔案
type: DOC
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-046
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-046 完成時發現重新推導了既有 pattern
  final_decision: 成 DOC 票泛化該 pattern，不新建重複 pattern
  rationale: 機制相同，問題在 scoping 使其查不到，泛化比新增第二份有效
who:
  current: 主線程或 thyme-documentation-integrator
  history: {}
what: 標題與辨識訊號由測試 fixture 擴為被忽略卻需被追蹤的檔案，補鎖檔案例為第二實例
when: 本票建立後可執行
where:
  layer: framework
  files:
  - .claude/error-patterns/process-compliance/PC-APP-006-gitignore-excludes-test-fixture.md::write
why: 該 pattern 的機制與測試 fixture 無關：症狀是 sync-push --clean 報告孤兒檔、診斷是 git check-ignore 加 git ls-files、核心區分是需追蹤者與 runtime 產物。但標題與辨識訊號都限定在測試 fixture（訊號表逐字寫「sync-push 報告孤兒檔在 tests/fixtures/」）。實測後果：本專案的 uv.lock 命中完全相同的機制，而查 uv.lock 或孤兒或 --clean 都不會撞到一個叫測試 fixture 的 pattern，於是花了三個根因版本重新推導出同一件事。error-pattern 衝突檢查 hook 確實列出了它，但該檢查在 complete 時觸發（工作已做完）且一次列 44 個未排序——它證實 pattern 在庫裡，不證實它在需要時找得到
how:
  task_type: Documentation
  strategy: 標題改為涵蓋機制而非單一領域 -> 辨識訊號表移除 tests/fixtures/ 的路徑限定 -> 補 uv.lock 與 --clean 誤刪為第二實例 -> 預防措施補「規則寫下時全庫掃一次」（既有已追蹤檔不受 gitignore 影響，故錯誤規則的命中率取決於寫下的時間點）
acceptance:
- '[ ] 標題與辨識訊號不再限定於測試 fixture，以查 uv.lock 或孤兒或 --clean 等關鍵字可命中'
- '[ ] 含兩個實例（測試 fixture 與依賴鎖定檔），且第二實例載明 --clean 誤刪的後果'
- '[ ] 預防措施含「規則寫下時全庫掃一次」及其理由'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

標題與辨識訊號由測試 fixture 擴為被忽略卻需被追蹤的檔案，補鎖檔案例為第二實例

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程或 thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-046 | chars: 501 -->

### Task Reference
- 0.1.0-W3-046 what: 把該 pattern 由副檔名分類改為用途分類，使 uv.lock 納入版控而執行期鎖檔仍被忽略

### Rationale Chain
- 0.1.0-W3-046 why: 第 99 行的 .claude/**/*.lock 註解寫「框架執行期鎖檔的類別式涵蓋」，用意是 dispatch-active.lock 與 scheduled_tasks.lock 那類執行期鎖檔。但 uv.lock 是 uv 的依賴鎖定檔，性質相反、應納版控。該 pattern 以副檔名分類而非用途分類，把兩種語意相反的檔案歸為一類。實測後果：本專案的 .claude/scripts/uv.lock 存在於磁碟且與 canonical 同為 40004 bytes，卻因被忽略而不進 git archive HEAD，使 sync-push 的孤兒偵測判定本專案沒有該檔並建議清除——執行後該檔自 canonical 消失。與 framework issue 57「uv.lock 未納版控」為同一件事，本票查出其機制

### Related Files
- .gitignore::write  # from 0.1.0-W3-046
- .claude/skills/ticket/ticket_system/tests/test_save_ticket_race.py::read  # from 0.1.0-W3-046

