---
id: 0.1.0-W3-550
title: 重測 8.2(3) 界定款落地後的 A3 軸
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-541
relatedTo: []
spawned_tickets:
- 0.1.0-W3-551
- 0.1.0-W3-552
source_ticket: 0.1.0-W3-541
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-541 的 AC3（改文後重測）被派發指示排除在該票之外
  final_decision: 另開 ANA 票承接重測，不在改文票內混跑
  rationale: 改文與量測同票會使歸因失據：同一 session 既寫條文又判讀數，無法排除出題方借用自己剛寫的款次；W3-530 的判定者獨立性設計即為此
who:
  current: general-purpose
  history: {}
what: 0.1.0-W3-541 已於 8.2(3) 清單句後增列五款（形狀／位置／文字內容三項內涵界定、靜態總則、預設值）。改文為純增列、既有句逐字未動（verify_head 261 條引文 0 failures），但條文效力尚未實測。本票重跑 A3 軸：以 A3c 題幹逐字不動重出一版（節錄檔須由新 HEAD 重建，才含新款次），跑三輪讀者探針與兩名互不可見的判定者，取得改文後的命中數，與 W3-542 定案的 baseline 1/3 對照。
when: 0.1.0-W3-541 落地後
where:
  layer: 待定義
  files:
  - .claude/skills/ux-design-evaluation/references/interaction-feedback-component-semantics.md
why: W3-541 的四款補法是否有效，只有重測能回答。PM 於 W3-541 裁決驗收問句不是通過率而是「否決誤指派的那幾輪，依據句引不引得出新增的款次」；W3-541 已完成九輪失守依據的逐句靜態對照（五輪失守句與 ① 逐字互斥、四輪否決句的自備前提均已被 ① 條文化），但靜態對照不是讀數。引用 baseline 1/3 時須附 W3-542 的但書：條文零次獨力擋下誤指派。
how:
  task_type: Analysis
  strategy: 由新 HEAD 重建餵入節錄（新款次須在節錄範圍內）；A3c 題幹逐字不動重出；三輪探針＋兩名獨立判定者；讀數與 1/3 對照寫入 Solution，並逐輪記載否決句有沒有引到 ①～⑤
acceptance:
- '[ ] 餵入節錄經確認含 8.2(3) 的五款新增條文（非由舊 HEAD 重建）'
- '[ ] 三輪讀數與命中數寫入 Solution，並與 W3-542 定案的 1/3 對照'
- '[ ] 逐輪記載否決句是否引用 ①～⑤ 之一，回答 PM 的驗收問句'
- '[ ] 引用 baseline 時已附 W3-542 的但書與 b19 題材共用「尖角」的隔離缺口'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-17T11:26:28'
completed_at: null
created: '2026-09-17'
updated: '2026-09-17'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

0.1.0-W3-541 已於 8.2(3) 清單句後增列五款（形狀／位置／文字內容三項內涵界定、靜態總則、預設值）。改文為純增列、既有句逐字未動（verify_head 261 條引文 0 failures），但條文效力尚未實測。本票重跑 A3 軸：以 A3c 題幹逐字不動重出一版（節錄檔須由新 HEAD 重建，才含新款次），跑三輪讀者探針與兩名互不可見的判定者，取得改文後的命中數，與 W3-542 定案的 baseline 1/3 對照。

---

## Problem Analysis
### 問題根因

`0.1.0-W3-541` 於 8.2(3) 清單句後增列五款（① 形狀／② 位置／③ 文字內容三項內涵界定、④ 靜態總則、⑤ 預設值）＋一句導引＋一句射程限制，但只完成了**靜態對照**（九輪失守／否決依據句與新款次的逐句互斥比對），未取得改文後的讀數。條文效力只有重測能回答。

`0.1.0-W3-542` 定案的對照基準 A3 軸 **1/3** 附有但書：該 1 不是條文擋下來的——A3 軸三版九輪，**條文零次獨力擋下誤指派**，四次否決全數倚賴讀者自備前提（第一版另有兩輪倚賴題幹事實），`8.3(2)` 九輪合計零引用。

### 影響範圍

- 受測條文：`.claude/skills/ux-design-evaluation/references/interaction-feedback-component-semantics.md` 第 8 項（8.1～8.5）
- 器材：`probes/` 下 b17 三軸題目、節錄、答案鍵與判定報告（scratchpad，未入庫）
- 本票不改任何條文，只取讀數

**已驗證的事實**：改文已在 HEAD（`7c45fbbd`）內，節錄重建後五款皆在餵入範圍（見「重現實驗結果」）。
**仍未驗證的假設**（本票開始時）：五款會不會被讀者實際引用、會不會使誤指派被條文獨力擋下。

### 相關 Error Pattern

`PC-165`（測試綠燈不等於 Runtime 正確）同型：靜態對照全數互斥不等於讀者實際會引。本票即為該同型風險的實測補位——`0.1.0-W3-541` 自己也把 AC3 拆出、不在改文票內自判效力。

## 重現實驗結果
### 實驗方法

1. **由新 HEAD 重建餵入節錄**（最易漏的一步，先做）：`probes/tools/build_excerpts.py` 以單一 spec（`specs-b17d-ux.json`，只含 `八項判準` 一個切片）重建 `excerpt/ux-design-evaluation--interaction-feedback-component-semantics--八項判準.md`；重建前整份 `excerpt/` 備份至 `excerpt-bak-b17d/`。
2. 驗證節錄確實含五款（逐款 `LC_ALL=C grep -cF`），再跑 `selfcheck.py` 與 `verify_head.py`。
3. 題幹、子問、作答規則**逐字凍結**，只換節錄：由 `A3c`／`A2c` 產生 `A3c-post`／`A2c-post`，唯一變更為檔頭「條文基準 commit」字串。
4. 各跑三輪 haiku 讀者探針（共六輪），作答寫入 `a/…-post.md`。
5. 兩名互不可見的判定者各自**先由條文推導答案**（`judge-b17d-ux-j{1,2}-derivation.md`）再讀 key 與作答，分別產報告。

### 實驗執行

**節錄重建（AC1）**

| 項目 | 重建前 | 重建後 |
| --- | --- | --- |
| 節錄檔頭 HEAD | `09012547` | `7c45fbbd` |
| `grep -cF "① 形狀"` | 0 | 1 |

重建後逐款計數（`LC_ALL=C grep -cF`，六款＋兩句各 1）：`① 形狀`=1、`② 位置`=1、`③ 文字內容`=1、`④ 靜態總則`=1、`⑤ 預設值`=1、導引句 `不得只憑該項名稱自行認定`=1、射程限制句 `不改變第 7 項對「同一圖形」的判定`=1。

節錄內原文佐證（第 148 行，逐字）：

> - ① 形狀：指這個元素是哪一個圖形，由輪廓本身決定，與這個圖形怎麼擺無關。同一個圖形旋轉、鏡射、翻面之後仍是同一個形狀；兩個元素之間只差朝向、擺放方位、哪一條邊或哪一個角朝上時，不算形狀差異，不得據以說第一點不成立。判別法：把其中一個轉一轉、翻一翻能與另一個重合，兩者就是同一個形狀。

**工具復驗**（`selfcheck-b17d-ux.log`／`verify-head-b17d-ux.log`，兩者 exit code 皆 0）

| 工具 | 輸出 |
| --- | --- |
| `selfcheck.py b17-ux-A3c b17-ux-A2c` | `check1 b17-ux-A3c: quotes=18`／`check1 b17-ux-A2c: quotes=17`／`questions total=2 failures=0` |
| `verify_head.py b17-ux-A3c b17-ux-A2c` | `HEAD 7c45fbbd: quotes total=35 failures=0` |

`check2` 有一則既有 `[HIT]`，落在無關的 tdd 節錄（`種子與行為單元分工邊界`），改文前批次亦同，`failures=0` 不受影響。`-post` 題目檔以同一組 key 復跑兩項工具，結果相同。

**題幹凍結的證據**

`A3c-post` 對 `A3c`、`A2c-post` 對 `A2c` 的 unified diff 各 **2 行**，唯一變更為檔頭第 3 行：

```
-條文基準 commit：c77115d5（節錄檔由此 commit 產生）。題數：1。
+條文基準 commit：7c45fbbd（節錄檔由此 commit 產生）。題數：1。
```

題幹、四個子問、五條作答規則與餵入欄逐字未動。改動該行的理由：它是對節錄檔來源的事實陳述，不是題幹、子問或作答規則；留舊值會讓題目檔與節錄檔頭的 HEAD 互相矛盾。

### 實驗發現

**已驗證的事實**：五款已在餵入範圍內（逐款計數＋原文佐證＋兩項工具 0 failures）；六輪讀數與兩份獨立判定報告已取得，讀數與逐輪依據句見 Solution。

**仍未驗證的假設**：③ 與 ⑤ 兩款本批零被引，其效力無讀數支撐（承接票 `0.1.0-W3-552`）；A2c-post r1 的 8.2(2) 極性反轉是否可重現，單輪不足以定性（承接票 `0.1.0-W3-551`）。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-541 | chars: 1316 -->

### Task Reference
- 0.1.0-W3-541 what: W3-530 九輪量測證否 W3-524 案一（A3）的未驗證假設：A3-r3 正面主張「朝向差異在換色後仍然存在，屬於 8.2(3) 清單中的『形狀』」並據此判第一點不成立、整題不違反，條文中無一句可駁——8.2(3) 的逐項封閉句只封閉清單成員數、未界定任一項的內涵，8.3(2) 的「換形狀是 8.2(3) 的一種」是充分條件陳述且實測九輪零引用。另有 3 輪把射程列為兩種合法讀法之一、3 輪自補條文沒有的界定（位置指靜止座標）才得以推進，1 輪因無法裁決而停在無結論。須於 8.2(3) 清單句之後增列界定款：形狀（同一圖形的旋轉與擺放方位不算）、位置（隨時間往復移動不算）、文字內容（字形、字重、字距不算），以及靜態總則與「無法判定時視為不在清單內」的預設值款。兩位判定者獨立收斂於同一組建議，且皆指出預設值款單獨即可擋下 A3-r3。（來源已完成，結論見 Solution）
- 0.1.0-W3-541 what: W3-530 九輪量測證否 W3-524 案一（A3）的未驗證假設：A3-r3 正面主張「朝向差異在換色後仍然存在，屬於 8.2(3) 清單中的『形狀』」並據此判第一點不成立、整題不違反，條文中無一句可駁——8.2(3) 的逐項封閉句只封閉清單成員數、未界定任一項的內涵，8.3(2) 的「換形狀是 8.2(3) 的一種」是充分條件陳述且實測九輪零引用。另有 3 輪把射程列為兩種合法讀法之一、3 輪自補條文沒有的界定（位置指靜止座標）才得以推進，1 輪因無法裁決而停在無結論。須於 8.2(3) 清單句之後增列界定款：形狀（同一圖形的旋轉與擺放方位不算）、位置（隨時間往復移動不算）、文字內容（字形、字重、字距不算），以及靜態總則與「無法判定時視為不在清單內」的預設值款。兩位判定者獨立收斂於同一組建議，且皆指出預設值款單獨即可擋下 A3-r3。（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-541 why: 0.1.0-W3-524 案一明示「讀者若仍正面主張某項清單外差異就是清單裡的某一項，A3 不阻止」為未驗證假設；b15 的 0 命中已由 W3-530 判定為題幹事實槓桿所致，W3-530 移除槓桿後取得正面命中實例，該假設證否為真實風險（來源已完成，結論見 Solution）
- 0.1.0-W3-541 why: 0.1.0-W3-524 案一明示「讀者若仍正面主張某項清單外差異就是清單裡的某一項，A3 不阻止」為未驗證假設；b15 的 0 命中已由 W3-530 判定為題幹事實槓桿所致，W3-530 移除槓桿後取得正面命中實例，該假設證否為真實風險（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 8.2(3) 已增列形狀／位置／文字內容三項的內涵界定  # from 0.1.0-W3-541（來源已完成，結論見 Solution）
- [ref] [x] 8.2(3) 已增列靜態總則與「無法判定視為不在清單內」的預設值款  # from 0.1.0-W3-541（來源已完成，結論見 Solution）
- [ref] [x] 既有題組誤傷實驗結果已記錄（含引文失效清單）  # from 0.1.0-W3-541（來源已完成，結論見 Solution）
- [ref] [x] 改文後重測已委由 0.1.0-W3-550 承接（blockedBy 本票），本票 Solution 已載明重測的對照基準與驗收問句  # from 0.1.0-W3-541（來源已完成，結論見 Solution）
- [ref] [x] 8.2(3) 已增列形狀／位置／文字內容三項的內涵界定  # from 0.1.0-W3-541（來源已完成，結論見 Solution）
- [ref] [x] 8.2(3) 已增列靜態總則與「無法判定視為不在清單內」的預設值款  # from 0.1.0-W3-541（來源已完成，結論見 Solution）
- [ref] [x] 既有題組誤傷實驗結果已記錄（含引文失效清單）  # from 0.1.0-W3-541（來源已完成，結論見 Solution）
- [ref] [x] 改文後重測已委由 0.1.0-W3-550 承接（blockedBy 本票），本票 Solution 已載明重測的對照基準與驗收問句  # from 0.1.0-W3-541（來源已完成，結論見 Solution）
