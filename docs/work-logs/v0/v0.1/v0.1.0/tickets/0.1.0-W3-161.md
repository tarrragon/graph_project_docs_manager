---
id: 0.1.0-W3-161
title: 修復 worktree create 對大型 monorepo 的 10s git 子行程 timeout 過短，導致部分檢出
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-096
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-096 執行期間第二次撞到 worktree create 逾時導致部分檢出
  final_decision: IMP 排入本 wave 待處理技術債（P3）
  rationale: 框架資產缺陷影響範圍廣（所有 agent 使用此 skill），但非阻擋性（有手動繞過方式），故列低優先級而非立即修復
who:
  current: 待派發
  history: {}
what: worktree_manager.py 的 run_git_command 預設 timeout=10 秒用於 git worktree add，本 repo 規模下該指令實測需時遠超過 10 秒；CLI 逾時回報失敗，但底層 git 行程仍在背景繼續執行，導致 worktree 呈現部分檢出的破損狀態（曾見 4237 個檔案顯示為 D），且分支已建立，重試會撞到『分支已存在』
when: 第二次撞到即符合『重複發生』門檻（第一次見 0.1.0-W3-094 worklog 備註，未成票）
where:
  layer: Infrastructure
  files:
  - .claude/skills/worktree/scripts/worktree_manager.py
why: 0.1.0-W3-094 與 0.1.0-W3-096 兩票皆撞到此問題，符合『重複發生』的追蹤門檻；本框架資產（.claude/skills/worktree/）影響所有後續在本 repo 使用 /worktree create 的 agent，逾時失敗訊息未提示『底層命令可能仍在執行』，容易誤導使用者重複嘗試或誤判環境損毀
how:
  task_type: Implementation
  strategy: 為 git worktree add 呼叫提供可設定或依 repo 規模調整的更長 timeout（如 60-120 秒），或改為非阻塞輪詢；至少應在逾時訊息中提示『底層命令可能仍在背景執行，稍候重跑 status 確認』，並提供清理殘留 worktree/branch 的一鍵指令
acceptance:
- '[ ] 大型 repo（本專案規模）下 worktree create 不再逾時失敗，或逾時訊息明確指出正確復原步驟'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

worktree_manager.py 的 run_git_command 預設 timeout=10 秒用於 git worktree add，本 repo 規模下該指令實測需時遠超過 10 秒；CLI 逾時回報失敗，但底層 git 行程仍在背景繼續執行，導致 worktree 呈現部分檢出的破損狀態（曾見 4237 個檔案顯示為 D），且分支已建立，重試會撞到『分支已存在』

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending
