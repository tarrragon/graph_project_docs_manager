---
id: 0.1.0-W3-076
title: 建立 docs/traceability.yaml 四軸追溯矩陣，使測試與規格脫節可被偵測
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-075
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-074
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 建立 docs/traceability.yaml 四軸追溯矩陣，使測試與規格脫節可被偵測
when: 0.1.0-W3-075 的測試對象單位與欄位定案後（矩陣的列即測試對象）
where:
  layer: docs/traceability.yaml,CLAUDE.md,docs/app-use-cases.md::read,docs/spec/ui::read,.claude/skills/doc/SKILL.md::read
  files:
  - docs/traceability.yaml
  - CLAUDE.md
  - docs/app-use-cases.md::read
  - docs/spec/ui::read
  - .claude/skills/doc/SKILL.md::read
why: CLAUDE.md §7 記 `docs/traceability.yaml`「尚未建立」；`doc` skill 已有 test-map 與 traceability 查詢功能但無資料可查。現況是 SPEC-004 每個元件的「測試契約」欄與 PROP-005 的每版整合測試為兩份互不相認的清單，UC↔測試無對應表——測試與規格脫節時沒有任何機制會發聲。這是「設計出與專案無關的測試」在偵測面的缺口（用戶 2026-09-08 指出），與 0.1.0-W3-075 的推導面互補：075 管「怎麼推導出該測什麼」，本票管「推導完之後脫節能不能被看見」。
how:
  task_type: Documentation
  strategy: '依 `doc` skill 的 traceability schema 建立 `docs/traceability.yaml`。列的單位與欄位取自 0.1.0-W3-075 的定案（該票 Solution 全文，commit 7ff11af4）：


    **單位為規格條目**（非 UC 場景、非元件）。075 的論證：UC 場景覆蓋不全（只有 6 個 UC／39 個 FlowStep，涵蓋不到元件層、token 層、平台契約）；元件會與 SPEC-004 的測試契約欄完全重複。


    **六個來源類別**：畫面狀態轉換（SPEC-001 每行）／UC 行為場景（UC-01~06 FlowStep 與替代例外）／互動規則（SPEC-003 各節）／元件契約（SPEC-004 §4）／token 不變式（SPEC-002 與 `lib/tokens/`）／平台契約（CLAUDE.md §6 與 tech-decisions）。


    **每列九欄**：對象、來源規格條目、來源類別、測試形態、驗證形態、斷言來源、fixture 需求、跳過條件與理由、反例。各欄的問句與缺失後果見 075 Solution 第二節。「驗證形態」欄消費 0.1.0-W3-074 的正交屬性定義（fake／實機／兩者）。


    **與 SPEC-004 不重複**：本檔對元件只記「來源 → 測試」的映射行，不記逐欄契約值；SPEC-004 測元件渲染正確性，本檔測行為正確性。


    先以現有資產回填：SPEC-001 §1–§7 畫面狀態、SPEC-004 各元件測試契約欄、PROP-005 各版整合測試定義、既有 `test/` 與 `integration_test/` 實際案例。落差處標「無對應」而非略過——**無對應的項目本身是本票的主要產出**，須分兩類列出：規格有而測試無（缺測試）、測試有而規格無（測試對應不到任何規格條目，即用戶指的「與專案無關的測試」）。


    齊全判準四條（075 第三節 C1–C4：覆蓋性／欄位完整／來源唯一／跳過合法）為本檔的驗收依據，與既有覆蓋率數字是補充關係不是取代。


    完成後 `doc uc verify` 與 test-map 查詢應可得結果；專案入口文件的文件索引列自「尚未建立」更新。


    本票承接已關閉的 0.1.0-W3-084（duplicate）的專案層填寫範圍；框架層定義文件由 0.1.0-W3-083 另行落地，本票不寫該檔。'
acceptance:
- '[ ] docs/traceability.yaml 建立且 doc skill 的 test-map 查詢可得結果'
- '[ ] 現有資產已回填；「無對應」項目成表並列於 Solution（規格有而測試無、測試有而規格無兩類分開）'
- '[ ] 專案入口文件（根目錄）的文件索引列自「尚未建立」更新為實際狀態'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T14:32:31'
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

建立 docs/traceability.yaml 四軸追溯矩陣，使測試與規格脫節可被偵測

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-08 14:31] readiness=1 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-08 14:45)
  - what: 修復 doc skill test_map.py：tests_dir 硬編為 "tests"（非 Flutter 慣例 test/）且掃描副檔名不含 .dart，導致本專案 doc test-map 對任何 UC 永遠回傳 0 筆，且該指令完全不消費 docs/traceability.yaml
  - why: 0.1.0-W3-076 驗證發現：直接呼叫 test_map.execute() 對 6 個 UC 皆回傳 0（tests_dir=project_root/tests 不存在，本專案為 test/ 與 integration_test/；掃描副檔名僅 .js/.ts/.py，不含 .dart）。建立 docs/traceability.yaml 不影響此結果——該指令是獨立的即時 grep 掃描，不讀取 traceability.yaml。導致 W3-076 acceptance『test-map 查詢可得結果』無法藉由本票工作滿足
  - suggested_type: IMP
  - suggested_priority: P2
  - related_files: .claude/skills/doc/doc_system/commands/test_map.py
  - context: 
  - status: pending


- **SR-2** (2026-09-08 14:46)
  - what: 修復 .claude/skills/doc/tests/test_tracking_schema_conformance.py 的 TestGraphTypeTablesRealEvtConformance 與 test_emits_and_consumes_reference_existing_evt_ids：硬編 EVT_BALANCE_DIR=docs/events/balance，本專案的 EVT 落在 docs/events/{corpus,diagnostics,graph,layout,schema,workspace}/，無 balance 子目錄
  - why: 0.1.0-W3-076 執行期間以 uv run pytest 驗證 traceability.yaml 是否符合 TRACEABILITY_SCHEMA 時，附帶跑出同檔案內 4 個 ERROR + 1 個 FAILED（EVT_BALANCE_DIR.is_dir() 為 False）。與本票的 traceability.yaml 改動無關（純讀 UC-01 flow 區塊與硬編目錄名），為既有測試套件的預先存在缺陷——測試檔案沿用了範本專案的 domain 名稱 balance，未依本專案實際 domain 命名調整。違反 quality-baseline 規則 1（測試通過率 100%）
  - suggested_type: IMP
  - suggested_priority: P2
  - related_files: .claude/skills/doc/tests/test_tracking_schema_conformance.py
  - context: 
  - status: pending

- **SR-3** (2026-09-08 14:46)
  - what: 評估是否擴充 TRACEABILITY_SCHEMA（.claude/skills/doc/doc_system/core/tracking_schema.py）以承載 0.1.0-W3-075/083 定案的『規格條目為單位、六來源類別、九欄契約』測試對象目錄——目前該設計只能落在 test-object-catalogue.md（方法論文件）與各票 Solution 的分析表，docs/traceability.yaml 的四軸結構（mappings/domain_bundle_tests/data_contract_tests/runtime_tests）無法承載 SPEC-001 畫面狀態、SPEC-003 互動規則、SPEC-004 元件契約、SPEC-002 token 不變式這四類來源的逐條規格條目
  - why: 0.1.0-W3-076 建立 docs/traceability.yaml 時發現：ticket how.strategy 要求依 W3-075 定案（規格條目單位、九欄）回填，但該檔的真正 SSOT（TRACEABILITY_SCHEMA，有 pytest conformance 測試強制頂層鍵封閉）是四軸結構，兩者形狀不同構。本票已依現有四軸盡力回填真實資料，六來源類別中僅『UC 行為場景』（經 mappings 軸）與『token 不變式＋平台契約』（經 data_contract_tests 軸）有容身之處；『畫面狀態轉換』『互動規則』『元件契約』三類目前只能停留在 Solution 的分析表，不進入 yaml。是否值得擴充 schema 需 PM 裁定（涉及修改 .claude/ 框架 .py 檔案，超出 DOC 票範圍）
  - suggested_type: ANA
  - suggested_priority: P3
  - related_files: .claude/skills/doc/doc_system/core/tracking_schema.py,docs/traceability.yaml
  - context: 
  - status: pending

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-075 | chars: 781 -->

### Task Reference
- 0.1.0-W3-075 what: 設計 測試對象目錄與測試契約欄位：從規格推導要測什麼、每項定義到什麼程度（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-075 why: `component-contract-design` 的核心產出是元件目錄加逐元件契約欄位，畫面票因此只剩「選件與排位」。測試側無對應物：`doc-handoff.md` 有規格→測試的映射與 GWT 轉換，`layered-test-strategy.md` 有「哪一層要測」，但沒有「**要測哪些對象**」的目錄，也沒有「**每個測試對象要定義到什麼程度**」的欄位表。

三處具體落差（對照元件庫 skill）：起點只有「有規格」一條，缺接手模式（既有測試如何盤點、補契約）；完成判準只有覆蓋率數字（行為場景 100%、分支 100%），那是量的判準，沒有質的可機械檢查；無派發語言，測試票仍要實作者自行決定測什麼。

後果：測試設計品質依賴 Phase 2 代理人當下的判斷，不同票之間無一致性可言，且「這個測試對應規格的哪一條」無處記載——這正是「設計出與專案無關的測試」的結構成因（用戶 2026-09-08 指出）。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/tdd/references/doc-handoff.md::read  # from 0.1.0-W3-075（來源已完成，結論見 Solution）
- .claude/skills/tdd/references/layered-test-strategy.md::read  # from 0.1.0-W3-075（來源已完成，結論見 Solution）
- .claude/skills/component-contract-design::read  # from 0.1.0-W3-075（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-004-component-library.md::read  # from 0.1.0-W3-075（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 測試對象的單位已判定並附理由（與既有 spec 不重複的論證）  # from 0.1.0-W3-075（來源已完成，結論見 Solution）
- [ref] [x] 契約欄位表草案完成，每欄附缺失後果  # from 0.1.0-W3-075（來源已完成，結論見 Solution）
- [ref] [x] 齊全定義為可機械檢查的形式；與既有覆蓋率判準的關係已釐清  # from 0.1.0-W3-075（來源已完成，結論見 Solution）
- [ref] [x] 三種起點的推導程序各有步驟；派發語言給出測試票 how 的目標句型  # from 0.1.0-W3-075（來源已完成，結論見 Solution）
**前置票 0.1.0-W3-074 的 A2 結論（2026-09-08）**：跨邊界判定為**正交屬性**而非新的一層（依據見 074 Solution）。**對本票的影響**：追溯矩陣不需為跨邊界另立區塊，加一個「驗證形態」欄即可，該欄的值域由 074 定案的分類提供。
