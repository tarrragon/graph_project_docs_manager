---
id: 0.1.0-W3-245
title: 補 SPEC-004 的 origin 欄位判定與狀態矩陣的級別條件
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-176
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-176
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-176 Phase 1 登記 SR-2：三個呼叫點標註 origin 與 SPEC-004 欄位，範疇待 PM 裁決
  final_decision: 另立獨立票，不併入 0.1.0-W3-205
  rationale: 205 的職責是截斷條件綁級別；併入三個畫面層呼叫點與一份 SPEC-004 條文會使該票同時承擔元件行為、呼叫點標註與規格三種變更理由
who:
  current: parsley-flutter-developer
  history: {}
what: 補 SPEC-004 的 origin 欄位判定與狀態矩陣的級別條件
when: 0.1.0-W3-176 的 origin 參數落地後（本票是其呼叫端的補完）
where:
  layer: Presentation
  files:
  - docs/spec/ui/SPEC-004-navigation-contract.md
why: '`0.1.0-W3-176` 的 SR-2，PM 裁定另立獨立票（不併入 `0.1.0-W3-205`——該票的職責是把截斷條件綁在級別上，再併入三個畫面層呼叫點與一份 SPEC-004 條文會使它同時承擔三種變更理由）。


  `176` 的 Phase 1 規格新增 `AppSnackBarOrigin { userInitiated, background }`，具名**選填**、預設 `background`。選填而非必填的理由是三個呼叫點不在 `176` 的 `where.files`，必填會使它們立即無法編譯。


  **未標註前的代價（已由 Phase 1 判斷並接受為過渡）**：`gap_report_screen.dart:199` 與 `:218` 兩處實為使用者發起，未傳 `origin` 時落入預設 `background`，其截斷會被記為偏高的等級。偏高側是自我揭露（噪音會被發現），偏低側是自我隱藏（該被看見的截斷被靜靜降級）——過渡期站在會被發現的那一側是對的，但**過渡不應無限期**，本票即其終點。'
how:
  task_type: Implementation
  strategy: '兩部分。


    (1) **三個呼叫點標註 `origin`**（依 `0.1.0-W3-176` Phase 1 規格已判定的歸屬，本票不重新判定）：

    - `lib/screens/gap_report/gap_report_screen.dart:199`、`:218` → `userInitiated`

    - `lib/screens/gap_report/scan_notification_controller.dart:202` → `background`


    行號依 `176` Phase 1 當時的實查；實作前以呼叫點的語意重新定位，不依行號硬套（`DOC-V1-001` 位置編號當錨點的形態）。


    (2) **SPEC-004 §4.0.11 是否補列新欄位**：先判定該節的性質是否涵蓋元件層日誌欄位。**若判定不需補列，於 Solution 寫明理由即為完成該項**，不強行增補——`176` 的 SR-2 只問「是否」，未預設答案。


    **邊界**：不改 `lib/components/app_snack_bar.dart`（`176` 的範疇）；不改 `SPEC-003`。標註後既有測試若因等級改變而紅燈，那是預期的行為變更，須更新斷言並在 Solution 記錄哪些斷言變了、為何。'
acceptance:
- '[ ] 三個呼叫點皆已標註 origin，歸屬與 0.1.0-W3-176 Phase 1 規格一致'
- '[ ] 呼叫點以語意定位而非依行號硬套'
- '[ ] SPEC-004 §4.0.11 是否補列新欄位已判定，不補列者於 Solution 附理由'
- '[ ] 因等級改變而需更新的既有測試斷言已於 Solution 逐項記錄'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

標註 三個 AppSnackBar 呼叫點的 origin 並補 SPEC-004 對應欄位

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-176 | chars: 1935 -->

### Task Reference
- 0.1.0-W3-176 what: 補齊 AppSnackBar 元件層日誌的三項可稽核欄位
- 0.1.0-W3-176 what: 補齊 AppSnackBar 元件層日誌的三項可稽核欄位

### Rationale Chain
- 0.1.0-W3-176 why: `0.1.0-W3-167` 的確定性重現實驗（四組，`fvm flutter test`）證實 `0.1.0-W3-165` 落地後仍有三項可稽核缺口，使「程式主動決定不告知使用者」這件事無法被事後追究：

| 缺口 | 現況 | 後果 |
|------|------|------|
| G1 訊息識別 | `closed` 事件的 `fields` 只有 `reason`，無 message 或關聯識別 | 無法回答「被截斷的是哪一則」。只能靠 `shown`／`closed` 的 FIFO 順序推斷，與其他 tag 的日誌交錯時不可靠 |
| G2 實際可見時長 | `closed` 不帶「這則實際被看到多久」 | 實驗 F4：「一幀都沒顯示」與「顯示滿 1 秒後被截斷」的日誌逐字相同，而兩者對使用者的嚴重度不同 |
| G3 未顯示的決定 | 因判準而不顯示的事件完全無事件 | 程式決定不告知使用者時零痕跡，該決定不可稽核 |

實驗同時確立判別式：全 repo 呼叫 `hideCurrentSnackBar` 只有兩處（`app_snack_bar.dart:121` 取預設 `reason: hide`、`:166` 明確 `reason: action`），故 `closed{reason=hide}` 恆等於「被後續一次 `show` 截斷」。G1／G2 補齊後，此判別式才足以支撐 `0.1.0-W3-167` 規劃的頻率量測協定——否則量測拿不到「被截斷的是不是 withAction」與「使用者實際看到多久」兩項指標。
- 0.1.0-W3-176 why: `0.1.0-W3-167` 的確定性重現實驗（四組，`fvm flutter test`）證實 `0.1.0-W3-165` 落地後仍有三項可稽核缺口，使「程式主動決定不告知使用者」這件事無法被事後追究：

| 缺口 | 現況 | 後果 |
|------|------|------|
| G1 訊息識別 | `closed` 事件的 `fields` 只有 `reason`，無 message 或關聯識別 | 無法回答「被截斷的是哪一則」。只能靠 `shown`／`closed` 的 FIFO 順序推斷，與其他 tag 的日誌交錯時不可靠 |
| G2 實際可見時長 | `closed` 不帶「這則實際被看到多久」 | 實驗 F4：「一幀都沒顯示」與「顯示滿 1 秒後被截斷」的日誌逐字相同，而兩者對使用者的嚴重度不同 |
| G3 未顯示的決定 | 因判準而不顯示的事件完全無事件 | 程式決定不告知使用者時零痕跡，該決定不可稽核 |

實驗同時確立判別式：全 repo 呼叫 `hideCurrentSnackBar` 只有兩處（`app_snack_bar.dart:121` 取預設 `reason: hide`、`:166` 明確 `reason: action`），故 `closed{reason=hide}` 恆等於「被後續一次 `show` 截斷」。G1／G2 補齊後，此判別式才足以支撐 `0.1.0-W3-167` 規劃的頻率量測協定——否則量測拿不到「被截斷的是不是 withAction」與「使用者實際看到多久」兩項指標。

### Related Files
- lib/components/app_snack_bar.dart  # from 0.1.0-W3-176
- test/unit/components/app_snack_bar_test.dart  # from 0.1.0-W3-176

### Source Acceptance (reference)
- [ref] [ ] shown 與 closed 事件攜帶可配對的關聯識別，配對不依賴 FIFO 假設  # from 0.1.0-W3-176
- [ref] [ ] 既有四個事件值的語意與既有測試斷言形態未被改動  # from 0.1.0-W3-176
- [ref] [ ] 截斷相關日誌的等級判別依據為事件發起者（使用者發起／背景發起），未以 variant 近似替代；元件內判定不到時由呼叫端傳參並於 Solution 記錄該選擇  # from 0.1.0-W3-176
- [ref] [ ] 重現 0.1.0-W3-167 實驗 A 的情境：同一同步區塊內連續兩次 show，兩則訊息在日誌上可配對且可辨識何者被截斷  # from 0.1.0-W3-176
- [ref] [ ] shown 與 closed 事件攜帶可配對的關聯識別，配對不依賴 FIFO 假設  # from 0.1.0-W3-176
- [ref] [ ] 既有四個事件值的語意與既有測試斷言形態未被改動  # from 0.1.0-W3-176
- [ref] [ ] 截斷相關日誌的等級判別依據為事件發起者（使用者發起／背景發起），未以 variant 近似替代；元件內判定不到時由呼叫端傳參並於 Solution 記錄該選擇  # from 0.1.0-W3-176
- [ref] [ ] 重現 0.1.0-W3-167 實驗 A 的情境：同一同步區塊內連續兩次 show，兩則訊息在日誌上可配對且可辨識何者被截斷  # from 0.1.0-W3-176
