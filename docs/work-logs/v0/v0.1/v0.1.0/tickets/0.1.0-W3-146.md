---
id: 0.1.0-W3-146
title: 追加 docs/tech-decisions.md 的 driven port 接縫形態約定
type: DOC
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-133
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-133 裁定為 A，其 SR-2 要求以約定文件取代 adapter 層
  final_decision: DOC 追加約定一節，並補裁定未涵蓋的第二種接縫形態
  rationale: 約定的目的是防止三張下游票各自發明接縫；PM 覆核時實查發現現有程式碼已有兩種形態，裁定只描述其一，約定若照抄會留下同一個缺口
who:
  current: thyme-documentation-integrator
  history: {}
what: 追加 docs/tech-decisions.md 的 driven port 接縫形態約定
when: 0.1.0-W3-133 裁定結論 A（維持現狀 + 接縫約定文件）之後
where:
  layer: Domain
  files:
  - docs/tech-decisions.md
  - lib/workspace/workspace_repository.dart::read
why: '`0.1.0-W3-133` 裁定為結論 A（維持現狀），其成立條件是「補一份接縫形態約定文件」——`0.1.0-W1-068`（ExternalOpener）、`0.1.0-W3-112`、`0.1.0-W1-014` 三個 driven port 都會照該形態實作，無約定則各自發明。


  **PM 覆核裁定時實查程式碼，發現現有檔案已有兩種接縫形態，而裁定只描述其一**：


  | 接縫 | 形態 |

  |------|------|

  | `WorkspacePreferencesPort` | `abstract interface class` + private `_DefaultWorkspacePreferencesPort` adapter 類別 |

  | `WorkspaceDirectoryProbePort` | 同上（`_DefaultWorkspaceDirectoryProbePort`） |

  | `DirectoryPathPicker` | **`typedef` 函式型別**（`lib/workspace/workspace_repository.dart:80`），生產預設為 `pickDirectoryPath ?? getDirectoryPath`（第 179 行），直接是 `file_selector` 的 API，**無 `_Default*` adapter 類別** |


  裁定的 Q3 結論寫「port 介面 + private default adapter 同檔案」，並以此形態指導三張下游票。該描述涵蓋前兩者，不涵蓋第三者。


  第二種形態未必是錯的——單方法、無受理步驟的 port 用函式 typedef 是合理的簡化。但**約定若只寫一種形態，下游票遇到單方法 port 時仍須自行判斷該用哪種，「防止各自發明接縫」這個目的就沒有達成**。這正是本約定存在的理由，缺口不能留在它自己身上。


  裁定同時確立了另一件事實，須一併寫入：`WorkspaceRepository` **不直接呼叫外部 SDK**——`SharedPreferences.getInstance()` 在 `_DefaultWorkspacePreferencesPort` 內、`Directory(path).exists()` 與 `.list()` 在 `_DefaultWorkspaceDirectoryProbePort` 內（PM 實查確認）。`0.1.0-W3-133` why 段記載的相反描述來自 Phase 4a 的 linux 視角，該前提與程式碼不符，約定文件不得沿用。'
how:
  task_type: Documentation
  strategy: '先讀 `0.1.0-W3-133` 的 Solution 取三問的完整論證，再讀 `lib/workspace/workspace_repository.dart` 確認三個接縫的實際形態（勿僅依本票 why 段的行號，該檔可能已被 spawn 出的另一張票改動）。


    約定至少涵蓋四項：


    1. **adapter 是角色標籤不是類別要求**——方法論〈Port 回饋契約〉的「adapter 實作」指實作 port 介面並與外部系統交互的程式碼，不要求獨立類別、獨立檔案或特定目錄。此為 `0.1.0-W3-133` Q1 的裁定，須引方法論原文而非轉述。

    2. **兩種接縫形態各自的適用判準**——何時用 `abstract interface class` + private `_Default*` adapter，何時用 `typedef` 函式型別。判準須可機械套用，不是「視情況而定」。可考慮的軸：port 的方法數、是否有受理（acknowledge）步驟、是否需要在測試中扮演多種角色。

    3. **兩步 API 的使用時機**——`WorkspacePreferencesHandle` 的 `open()` / `readString` 兩步形態，依裁定僅限「外部系統有 acknowledge 步驟」時使用，不得一律套用。

    4. **測試替身紀律**——各形態對應的替身寫法，與 `.claude/skills/tdd/references/layered-test-strategy.md` 的〈角色化替身〉一致，以標題文字引用不寫 fragment。


    `docs/tech-decisions.md` 為 append-only、以最後的補記為準，依既有補記格式新增一節，不改寫既有內容。'
acceptance:
- '[ ] 約定含 adapter 為角色標籤而非類別要求，並引方法論原文非轉述'
- '[ ] 兩種接縫形態各有適用判準，判準可機械套用而非視情況而定'
- '[ ] 兩步 API 的適用條件已寫明限於外部系統有 acknowledge 步驟'
- '[ ] 三個接縫的現況形態逐一列出並與判準對照，含 DirectoryPathPicker 的 typedef 形態'
- '[ ] 未沿用 0.1.0-W3-133 why 段「repository 直接寫外部 SDK」的描述（該前提與程式碼不符）'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

追加 docs/tech-decisions.md 的 driven port 接縫形態約定

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### PM 派發前實查（HEAD b499faab，`lib/workspace/workspace_repository.dart` 共 309 行）

acceptance 第 5 條的事實基礎已覆核，與票面一致：`SharedPreferences.getInstance()`
在第 125 行、位於 `_DefaultWorkspacePreferencesPort.open()` 內；`Directory(path).exists()`
第 135 行、`Directory(path).list()` 第 139 行，位於 `_DefaultWorkspaceDirectoryProbePort`
內。`WorkspaceRepository` 本體（第 173 行起）只持有 `_pickDirectoryPath`／`_preferencesPort`／
`_directoryProbe`／`_log` 四個注入欄位，無任何外部 SDK 直呼。

**PM 覆核時另發現第四個接縫，票面 why 段只列三個**：`WorkspaceLogSink`（第 101 行，
`typedef` 函式型別）＋ `_defaultLogSink`（第 142 行，**private 頂層函式**，轉呼
`developer.log`）。它與 `DirectoryPathPicker` 同為 typedef，但不同於後者——後者的生產
預設直接是 `file_selector` 的 `getDirectoryPath`（第 179 行構造式的 `??` 右運算元），
本專案未寫任何預設實作；前者則有自己撰寫的 private 頂層函式作為預設。

因此檔案內實際是三種形態而非兩種：

| 接縫 | 介面形態 | 生產預設 |
|------|---------|---------|
| `WorkspacePreferencesPort`（含 `Handle`）／`WorkspaceDirectoryProbePort` | `abstract interface class` | private `_Default*` **類別** |
| `DirectoryPathPicker` | `typedef` | 第三方 API 函式，無自撰預設 |
| `WorkspaceLogSink` | `typedef` | private 頂層**函式** `_defaultLogSink` |

判準若只切「class 形態 vs typedef 形態」兩分，讀者遇到 `WorkspaceLogSink` 仍無法從約定
推出該不該自寫預設實作——這與票面指出的缺口同型，只是多一層。執行者須先實查確認本段
（行號會因 `0.1.0-W3-147` 的 value types 提取而位移，以符號名為準），再決定第四個接縫
是併入判準的第三列、或明寫排除理由（例如日誌投影不屬 driven port）。兩者皆可，但不得
無聲略過。

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-133 | chars: 1567 -->

### Task Reference
- 0.1.0-W3-133 what: 裁定 是否為 driven port 建立 adapter 層與 ports 目錄約定（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-133 why: Phase 4a 兩個視角對同一問題給出方向相反的意見，需要裁定。

**linux（Acceptable 偏低）主張補 adapter 層**：`WorkspaceRepository` 同時是 repository 又是三個 adapter。方法論〈Port 回饋契約〉自己寫了承擔者分工——呼叫發出歸呼叫端，受理與結果歸 **adapter**——但本專案沒有 adapter 層，repository 直接寫 `SharedPreferences.getInstance()`、`Directory(path).list()`。於是要讓 adapter 內部的「受理」時刻可觀測，唯一手段是往 repository 身上鑿洞：`WorkspacePreferencesHandle` 的存在理由只是 `getInstance()` 是兩段式 API，那是 SharedPreferences 的實作細節，卻被提升成 repository 的公開介面。它主張三個真 adapter 類別（各自獨立檔案）可讓接縫從 5 降到 3，可觀測性一條不減。

**Coupling 視角的相反證據**：實查 `WorkspacePreferencesPort`／`Handle` 與 `DirectoryPathPicker` 兩處**未發現型別洩漏**——`SharedPreferences` 與 `file_selector` 的型別沒有出現在任何介面簽章或呼叫端的具體型別比對中，呼叫端一律 generic `catch (e)`。以隔離效果衡量，這兩個接縫是乾淨的。

**Complexity 視角的第三種意見**：量測到單檔 19 個頂層宣告、267 行，但明確標註信心 55、未達回報門檻、**不建議據此拆檔**——理由是 `cognitive-load.md` 沒有「單檔型別數上限」條文，把任務拆分閾值（需追蹤概念數 > 7）類比套用到檔案內型別數缺乏明文依據；且五個 port 加四個 default adapter 是同一個 ports-adapters 圖式的必然展開，全檔僅一個 domain，未命中「單一檔案承載 domain 數 > 2」。

三者不是簡單的對錯：linux 談的是結構歸屬（實作細節該不該出現在 repository 介面上），Coupling 談的是洩漏事實（型別有沒有跑出來），Complexity 談的是有無明文閾值依據。**本票要裁定的是該用哪個標準。**

決定的外溢範圍：`0.1.0-W1-068`（ExternalOpener）、`0.1.0-W3-112`、`0.1.0-W1-014` 三個 driven port 都會照本形態實作。linux 的推估是複製後約 4 個 port × 8 個測試替身 ≈ 30 個 bespoke 類別，每個 port 各自發明接縫形態。（來源已完成，結論見 Solution）

### Related Files
- docs/tech-decisions.md::read  # from 0.1.0-W3-133（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三問各有論證且引方法論原文，不憑印象  # from 0.1.0-W3-133（來源已完成，結論見 Solution）
- [ref] [x] 三個候選結論（維持現狀／建 adapter 層／部分）各有論證並擇一  # from 0.1.0-W3-133（來源已完成，結論見 Solution）
- [ref] [x] 裁定對 W1-068、W3-112、W1-014 三票的影響已明寫；若結論為 B 或 C，已登記 spawn request 交 PM  # from 0.1.0-W3-133（來源已完成，結論見 Solution）
- [ref] [x] workspace_repository.dart 已達 309 行（超過 cognitive-load.md 的 300 行門檻，0.1.0-W3-121 Phase 4b 記錄）——若本票結論為「維持現狀」，須一併裁定該檔行數的處置或明寫豁免理由，不得因結論 A 而使此項無承接者  # from 0.1.0-W3-133（來源已完成，結論見 Solution）
