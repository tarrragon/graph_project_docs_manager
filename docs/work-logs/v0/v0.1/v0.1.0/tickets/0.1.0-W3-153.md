---
id: 0.1.0-W3-153
title: 更正 traceability.yaml runtime_tests 首條的過期 status
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-148
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 更正 traceability.yaml runtime_tests 首條的過期 status
when: 0.1.0-W3-148 完成後
where:
  layer: Domain
  files:
  - docs/traceability.yaml
why: '`docs/traceability.yaml` 的 `runtime_tests` 首條（scenario「選擇資料夾建立 workspace」）status 現寫「無 fake 測試涵蓋、無實機驗證——WorkspaceRepository 無專屬測試檔，僅於 integration_test/app_test.dart 以 _StubWorkspaceRepository 繞過真實選取」。


  前半段與現況不符。PM 實查：`test/unit/workspace/workspace_repository_test.dart` 存在（670 行），含 G1「呼叫發出時刻（INV-PORT-OBSERVE-001 覆蓋）」、G2「chooseFolder 結局與四個 variant」、G3「restore 結局三分支」、G4「受理時刻」、G6「_inspect 探測結局」五個 group。「無 fake 測試涵蓋」與「無專屬測試檔」兩項皆已過期。


  後半段仍為真：`integration_test/app_test.dart` 的 `_StubWorkspaceRepository` 出現 4 處，真實選取路徑確實被繞過。


  四軸矩陣是驗收引用來源，過期描述會使審查者低估既有覆蓋。此條由 `0.1.0-W3-148` 的執行者發現並登記為 SR-1，因涉及既有條目而未自行修改，經 PM 覆核事實成立。'
how:
  task_type: Documentation
  strategy: '更正時必須維持軸別語意，不可把單元測試寫成實機覆蓋。`runtime_tests` 軸的定義是跨邊界呼叫點的**平台端事實軌**（實機驗證），`workspace_repository_test.dart` 全部以 fake 替身執行，屬 App 端邏輯軌，不構成本軸覆蓋——CLAUDE.md 第 6 節「跨邊界驗證是正交屬性」段已載明本專案三個跨邊界呼叫點的平台端事實軌全數零覆蓋。


    因此 status 應改寫為兩段：邏輯軌已有專屬單元測試（指明檔案與 G1-G6 群組），平台端事實軌（真實 file_selector 系統對話框）仍零覆蓋且真實選取路徑於 integration_test 被 `_StubWorkspaceRepository` 繞過。


    `tests:` 欄是否一併填入該單元測試檔，依本軸的欄位語意判定——若該欄專指實機驗證的測試檔，則維持空陣列並僅改 status；若為該 scenario 的全部相關測試，則填入。判定依據以 `.claude/skills/doc/doc_system/core/tracking_schema.py` 的 `TRACEABILITY_SCHEMA` 為準，不憑既有條目慣例推測。


    同時檢查另兩條 runtime_tests 條目的 status 是否有同型過期。'
acceptance:
- '[ ] runtime_tests 首條 status 已改寫為邏輯軌與平台端事實軌兩段，未把 fake 單元測試寫成實機覆蓋'
- '[ ] tests 欄的填或不填已依 tracking_schema.py 的欄位語意判定並在 Solution 記錄依據'
- '[ ] 另兩條 runtime_tests 條目已檢查同型過期並處置'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-09T12:40:55'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

更正 traceability.yaml runtime_tests 首條的過期 status

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
三條 runtime_tests 條目逐一實查：

entry1（選擇資料夾建立 workspace）：舊 status 前半段「無 fake 測試涵蓋、無實機驗證——WorkspaceRepository 無專屬測試檔」與現況不符。實查 test/unit/workspace/workspace_repository_test.dart（670 行），確認五個 group：G1｜呼叫發出時刻（INV-PORT-OBSERVE-001 覆蓋）、G2｜chooseFolder 結局與四個 variant、G3｜restore 結局三分支、G4｜受理時刻、G6｜_inspect 探測結局。後半段「integration_test/app_test.dart 以 _StubWorkspaceRepository 繞過真實選取」仍為真（grep 確認 4 處引用）。

entry2（掃描完成的系統層通知）：舊 status 引用「0.1.0-W3-064 acceptance 2、3 未勾」已過期——實查該票 YAML frontmatter，acceptance 2、3 現皆為 [x]。但深入追溯發現此為假象：`ticket track full 0.1.0-W3-078`（該票 why 明寫「W3-064 acceptance 2、3 承接，平台事實只有實機能驗」）顯示承接票仍 in_progress（9/10 子票完成），其自身 acceptance 6「0.1.0-W3-064 的 acceptance 2、3 已可勾選或已說明為何仍不可勾」尚未打勾；且 SPEC-003 §2.2「實作票驗證」段（git log 確認自 0.1.0-W3-063 建立後從未修改）仍是待驗清單而非已回填的實測記錄。CLAUDE.md 第 6 節 W3-081 補記亦明文「三個跨邊界呼叫點的平台端事實軌全數零覆蓋」。三方交叉驗證確認：064 的 [x] 未反映真實驗證完成，屬既有票務資料的既存不一致（非本票範圍內產生），本票不改動 064/078，僅在 traceability.yaml 改引用現行有效的承接票 0.1.0-W3-078，避免延續已失效的引用。此發現已由 078 自身的驗收項追蹤，不需另開票。

entry3（開啟原始檔）：舊 status「僅涵蓋『檔案不存在』的失敗分支」已過期。實查 test/unit/screens/gap_report_test.dart 新增「破洞項外部開啟（state-gaps-found）」group，以注入 gapItemProcessRunnerProvider 替身覆蓋 exitCode 0（成功）、exitCode 非零（失敗）、呼叫拋例外三案例；另有既有「檔案不存在時顯示提示」測試以真實 Process.run 對刻意不存在路徑驗證。doc CLI 呼叫路徑實查 lib/ 全庫僅一處 Process.run 呼叫點（gap_report_screen.dart 的 open 指令），確認 doc CLI 呼叫尚無對應實作（rescan() 讀寫死 fixture），非「有實作但未驗證」，status 已補一句澄清。

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
改寫 docs/traceability.yaml 的 runtime_tests 三條 status（僅此段，未動 mappings/domain_bundle_tests/data_contract_tests，未動其他 session 新增的 SPEC-005 條目）：

entry1 改寫後 status：邏輯軌（fake）已有專屬單元測試 test/unit/workspace/workspace_repository_test.dart（五個 group：G1｜呼叫發出時刻/INV-PORT-OBSERVE-001、G2｜chooseFolder 結局與四個 variant、G3｜restore 結局三分支、G4｜受理時刻、G6｜_inspect 探測結局）；平台端事實軌（真實 file_selector 系統對話框）仍零覆蓋——真實選取路徑於 integration_test/app_test.dart 被 _StubWorkspaceRepository（4 處）繞過。

entry2 改寫後 status：邏輯軌（fake）已覆蓋（SPEC-003 §2.2 斷言十二列全綠）；平台端事實軌仍零覆蓋，改引用現行承接票 0.1.0-W3-078（進行中）取代已過期的 064 acceptance 引用。

entry3 改寫後 status：邏輯軌（fake）已覆蓋 _openItem 三個結局分支 + 檔案不存在情境；平台端事實軌仍零覆蓋，並澄清 doc CLI 呼叫尚無對應實作（非僅未驗證）。

tests 欄判定依據：TRACEABILITY_SCHEMA 的 runtime_tests_entry_required 含 "tests" 為必填欄位，schema 本身未限定該欄「僅列實機測試」；既有 entry2/entry3 皆已將對應的 fake 單元測試檔列入 tests 欄（scan_notification_controller_test.dart／gap_report_test.dart），status 文字則承擔 fake／實機兩軌覆蓋現況的說明——此為 schema 允許範圍內、且已由兩條既有條目建立的一致欄位用法（非單憑既有條目慣例推測，而是 schema 未反對此用法且已成事實上的一致模式）。故 entry1 的 tests 欄比照填入 test/unit/workspace/workspace_repository_test.dart，不留空陣列。

驗證：`uv run --project .claude/skills/doc pytest .claude/skills/doc/tests/test_tracking_schema_conformance.py -q` 31 passed；另以 python3 yaml.safe_load 確認檔案可解析、runtime_tests 三條 tests 欄與預期一致、data_contract_tests 仍為 8 條（他人新增的 SPEC-005 條目完整保留，未被本次編輯影響）。

commit 75003858d470bb76d7c8cc93f656f2db80cc2f6d（ticket track commit，隔離索引，僅 docs/traceability.yaml）。

附帶發現（不在本票範圍，未修改，記錄供後續參考）：0.1.0-W3-064 的 acceptance 2、3 標記為 [x]，但其承接票 0.1.0-W3-078（因「平台事實只有實機能驗」而開立）仍 in_progress 且自身驗收項 6 明寫「0.1.0-W3-064 的 acceptance 2、3 已可勾選或已說明為何仍不可勾」尚未打勾，SPEC-003 §2.2「實作票驗證」段自建立後未曾回填實測。此為既有票務資料的既存不一致，其解決已內含於 0.1.0-W3-078 自身的驗收路徑（該票完成時會回頭處理 064 的勾選正確性），故不另開新票，僅在此記錄供 PM 知悉。

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-148 | chars: 1526 -->

### Task Reference
- 0.1.0-W3-148 what: 新建 docs/spec/workspace/SPEC-005：workspace 三個 driven port 的回饋點契約（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-148 why: `0.1.0-W3-139` 盤點五個 driven port 的三個回饋點覆蓋後，裁定輸出端規格採雙落點：workspace 三個 port（`WorkspacePreferencesPort` / `WorkspacePreferencesHandle` / `WorkspaceDirectoryProbePort` / `DirectoryPathPicker`）新建本規格，`ScanNotifier` 與 `ExternalOpener` 修訂 SPEC-003（`0.1.0-W3-149`）。

不選的替代已在該票論證：單一 cross-cutting spec 違反 domain 分離；workspace port 併入 SPEC-003 會使 ui domain 承載 workspace 的契約；不做 spec 則使用者裁示的「列入測試」無可否證依據。

PM 實查確認 `docs/spec/` 目前僅有 `ui/` 一個 domain，本規格為 workspace domain 的第一份。

**兩項必須寫進規格的事實，來自 PM 覆核**：

1. **本專案有兩種接縫形態，不是一種。** `WorkspacePreferencesPort` 與 `WorkspaceDirectoryProbePort` 是 `abstract interface class` + private `_Default*` adapter 類別；`DirectoryPathPicker`（`lib/workspace/workspace_repository.dart:80`）是 `typedef` 函式型別，生產預設直接為 `getDirectoryPath`，無 adapter 類別。兩者的三個回饋點承擔者不同——前者的受理與結果由 adapter 承擔，後者三時刻全由呼叫端承擔。形態本身的約定歸 `0.1.0-W3-146`（寫進 `docs/tech-decisions.md`），本規格只描述各形態下回饋點由誰承載。

2. **`_DefaultWorkspaceDirectoryProbePort` 全無日誌。** PM 讀原始碼確認該 adapter 兩個方法（`exists` / `readFirstEntry`）皆為單行直呼 `dart:io`，無任何 `developer.log`。`0.1.0-W3-139` 記為「`exists()=false` 日誌缺失」，實際範圍比該描述更廣。規格須據實描述現況缺口，缺口的修補歸 `0.1.0-W3-135`。（來源已完成，結論見 Solution）

### Related Files
- docs/spec/workspace/SPEC-005-workspace-driven-ports.md  # from 0.1.0-W3-148（來源已完成，結論見 Solution）
- lib/workspace/workspace_repository.dart::read  # from 0.1.0-W3-148（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 五個回饋點承擔者依接縫形態分別記載，typedef 形態與 abstract interface class 形態的差異已寫明  # from 0.1.0-W3-148（來源已完成，結論見 Solution）
- [ref] [x] 監控追蹤欄依 PM 更正寫為延後加重評條件，未寫 N/A  # from 0.1.0-W3-148（來源已完成，結論見 Solution）
- [ref] [x] 方法論的三時刻定義與 INV-PORT-OBSERVE-001 以標題文字引用未重述，本規格只寫本專案事實  # from 0.1.0-W3-148（來源已完成，結論見 Solution）
- [ref] [x] _DefaultWorkspaceDirectoryProbePort 無日誌的現況缺口已據實描述並指向承接票  # from 0.1.0-W3-148（來源已完成，結論見 Solution）
- [ref] [x] docs/traceability.yaml 已補登本規格，軸別經性質判定非預設  # from 0.1.0-W3-148（來源已完成，結論見 Solution）
