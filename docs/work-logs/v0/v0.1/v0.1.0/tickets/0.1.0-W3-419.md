---
id: 0.1.0-W3-419
title: 補寫 ux-design-evaluation 範例依據只在範例內的判定補條文（W3-417 盤點）
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-417
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-417 用戶裁示
  final_decision: 每 skill 一張修補票
  rationale: 避免跨 skill 同檔衝突
who:
  current: thyme-documentation-integrator
  history: {}
what: 依 W3-417 Problem Analysis 盤點表，為 ux-design-evaluation 所有條文查無與部分依據的判定補條文依據，兩階段處置（已有權威則補路由或補句；需新判準則草稿停手交 WRAP）
when: 立即
where:
  layer: 待定義
  files:
  - .claude/skills/ux-design-evaluation/SKILL.md
why: 用戶裁示 W2 開工閘門：範例零無依據句，63 個判定全部先補條文再出探針題
how:
  task_type: Documentation
  strategy: 逐項判定階段，階段 1 落檔並 git grep -F 查證，階段 2 草稿停手
acceptance:
- '[ ] 盤點表中本 skill 的條文查無與部分依據判定逐項處置並記錄'
- '[ ] 階段 2 項目草稿已寫入 Problem Analysis 並停手'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-15T13:22:34'
completed_at: null
created: '2026-09-15'
updated: '2026-09-15'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

依 W3-417 Problem Analysis 盤點表，為 ux-design-evaluation 所有條文查無與部分依據的判定補條文依據，兩階段處置（已有權威則補路由或補句；需新判準則草稿停手交 WRAP）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### 階段 2 草稿（11 則，需新增判準或會改變既有判定，本輪不落檔）

以下每則含：條文落點、草稿文字、會改變哪些判定。全部維持草稿狀態，等 WRAP／PM 裁決後才可落檔，不屬用戶程式碼風格偏好。

**B-03b + B-04b（input-mechanism.md〈四個決策維度〉Keyboard type 小節，同一缺口，兩則範例共用）**
草稿：「決策表填入的值須為可跨框架轉譯的中性描述（如「隱碼型鍵盤」「email 型鍵盤」），不得直接寫特定 UI 框架的鍵盤型別列舉名稱；框架對應可另立旁註欄，不進決策表本體。」
會改變哪些判定：目前條文完全未限制決策表用詞形式；補上後，不限 Keyboard type，Submit model／IME policy 等其他維度的決策表寫法都會被要求比照辦理，屬新增的通用書寫規範。

**B-05（input-mechanism.md〈場景一：表單〉驗證時機段落）**
草稿：「判準是規則能否在使用者裝置本地、不需伺服器參與就算出結果：能→格式驗證（on blur）；需要查詢或核對伺服器狀態→業務驗證（on submit）。」
會改變哪些判定：目前條文僅以「格式明確／格式不明確」的例子帶過，沒有可機械執行的測試；補上「能否本地算出」後，格式正確但仍須查重複（如 email 唯一性）等邊界案例的既有分類需要重新核對是否仍落在原本歸類。

**B-12b（screen-state-matrix.md〈四欄定義〉或檢查清單「error 類狀態」項）**
草稿：「error 類狀態涵蓋所有代表『功能因失敗或中斷而無法正常使用』的狀態，包含連線類流程的 disconnected；兩者都須同時提供『重試』與『返回』兩個選項。」
會改變哪些判定：「error 類狀態」一詞目前外延未定義，只在範例中把 disconnected 一併處理；明文定義後，timeout／expired／revoked 等語意相近的既有狀態是否也算 error 類會被連帶追問，可能牽動其他規格已寫定的狀態分類。

**B-13（interaction-feedback.md〈按鈕級回饋〉timeout 兜底句）**
草稿：「緩衝量沒有固定公式，由規格撰寫者依該操作的重要性與使用者等待耐性決定，只要求『略大於後端逾時值』。」
會改變哪些判定：目前條文只給「略大於」的方向，沒交代決定權與依據；補上後等於明文承認本節不再提供精確公式，未來若要追加「重要操作應如何量化緩衝秒數」的細則，須先修改本句。

**B-14c（screen-state-matrix.md〈填寫步驟〉步驟 2 或步驟 3；目前規則僅存在於〈填寫範例〉步驟 3 的敘述中，屬範例內文字不算條文）**
草稿：「可用操作欄中會導致離開本狀態的項目，一律以相同名稱出現在退出路徑欄，不換一個名稱。」
會改變哪些判定：此規則目前只被〈填寫範例〉一個範例遵守；升格為〈填寫步驟〉正式步驟後，既有規格的矩陣都要重新檢查「可用操作」與「退出路徑」兩欄用詞是否一致，可能揭露既有矩陣的用詞不一致案例。

**B-15（screen-state-matrix.md〈容易漏列的狀態：initializing〉段落）**
草稿：「一般的單次請求（發出、等待、回應到達即結束）不算獨立生命週期——其存續完全依附本次畫面互動，不需要 initializing 狀態；只有查詢對象在本次互動之外仍持續存在（常駐服務、另一 process）才需要。」
會改變哪些判定：目前條文只 positive 定義「有生命週期時」要加 initializing，沒有排除「單次請求」；補上後，既有規格中把「等待中」誤標為 initializing 的畫面會被要求改判為一般 loading。

**B-16（screen-state-matrix.md〈反模式〉「進入條件不可逆」段落）**
草稿：「判斷可逆性只看是否存在能讓狀態轉回進入前那一刻的操作；單純的畫面導覽返回（不改變狀態本身）不算反向操作。」
會改變哪些判定：目前條文只舉一個「連線成功／斷開連線」的例子，沒有「導覽返回不算」的排除規則；補上後，既有規格中把「返回上一頁」當成已滿足〈檢查清單〉「反向操作」要求的既有判定都要重新檢查。

**B-19b（error-recovery.md〈重試機制〉間隔策略段落）**
草稿：「固定間隔場景（前景瞬斷）與指數退避場景一致，都須提供『立即重試』按鈕，讓使用者可以跳過等待；重試中的 UI 三要件（正在重試／已重試次數／下次時間）兩種間隔策略皆適用。」
會改變哪些判定：目前「立即重試按鈕」只明確綁定在「伺服器過載訊號…指數退避」子句，固定間隔子句沒有這個要求；補上後，只為指數退避場景設計立即重試按鈕、固定間隔場景略過的既有畫面會被判定為缺口。

**B-24（navigation-patterns.md〈iOS HIG vs Material Design 慣例差異〉選擇策略表後）**
草稿：「未列在〈面向〉表中的元件或手勢，逐一問『是否為使用者對該平台既有系統行為的預期（如手勢慣例、系統鍵語意）』：是→系統行為類差異，必適配；否（僅涉及畫面上的視覺呈現）→視覺風格類差異，可視適配成本決定是否統一。」
會改變哪些判定：目前只給表中列舉的三個面向（Back/Tab/Modal）當例子，沒有給讀者可套用到未列舉元件的通用測試；補上後，先前依直覺判定的元件（手勢返回列表、下拉刷新等）都要重新依此判準覆核，可能推翻原本「憑列表比對」的既有結論。

**B-29（gate-fallback.md〈破壞性操作確認 gate〉段落）**
草稿：「落差大小只影響確認文案的具體程度（落差越大越需要具體描述後果），不影響是否需要確認本身：任何不可逆、覆蓋或清空既有資料的操作，即使名稱誠實、落差小（如「刪除」），仍須攔一道確認。」
會改變哪些判定：目前條文的觸發條件寫成「破壞半徑大於使用者直覺預期時」，字面上暗示「落差不存在」時可能落在條件之外；補上「落差小仍須確認」後，須反過來覆核既有規格中「名稱誠實、直覺一致」的不可逆操作是否已補上確認機制。

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-417 | chars: 494 -->

### Task Reference
- 0.1.0-W3-417 what: 依用戶裁示以讀者探針為範例審查閘門跑第一輪實證：涵蓋本輪補過範例的 skill（component-contract-design、ux-design-evaluation、foundation-design、test-assertion-design、tdd、doc、version-bootstrap、dart-style-guardian、skill-design-guide），每則含判定的範例出題並含判準相反分支的對照版，題目凍結後各 3 探針作答，判定者先從條文寫出正確答案與依據句再比對；門檻 3/3；判定者同時標出依據只在範例內的判準

### Rationale Chain
- 0.1.0-W3-417 why: 用戶裁示：W2 開工條件為探針 3/3 通過且全部範例零無依據句；chained-examples 待本輪實證後再改

### Related Files
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-336.md  # from 0.1.0-W3-417

### Source Acceptance (reference)
- [ref] [ ] 凍結題組覆蓋每則含判定範例且含相反分支  # from 0.1.0-W3-417
- [ref] [ ] 逐題判定表（3/3 生效或未生效）與依據只在範例內清單寫入 Solution  # from 0.1.0-W3-417
- [ref] [ ] 未通過與無依據句逐項建修補票或記錄豁免理由  # from 0.1.0-W3-417
- [ref] [ ] W2 開工閘門達成與否的結論  # from 0.1.0-W3-417
