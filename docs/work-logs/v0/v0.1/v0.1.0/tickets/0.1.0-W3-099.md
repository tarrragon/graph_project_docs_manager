---
id: 0.1.0-W3-099
title: 盤點 既有可互動元件的回饋掛載點，判定哪些需調整
type: ANA
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.1.0-W3-098
spawned_tickets:
- 0.1.0-W3-103
source_ticket: 0.1.0-W3-078
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 新判準確立後的存量檢討
  final_decision: ANA 先逐件判定，再依結論建 IMP/DOC 票
  rationale: 25 個可互動元件不會全部需要調整；直接建 25 張實作票會有多數無事可做，且調整形態可能分數類、值得先歸類再派
who:
  current: saffron-system-analyst
  history: {}
what: 盤點 既有可互動元件的回饋掛載點，判定哪些需調整
when: 0.1.0-W3-098 定案回饋契約條文與齊全判準後（記錄格式須依其定義）
where:
  layer: Presentation
  files:
  - lib/components::read
  - docs/spec/ui/SPEC-004-component-library.md::read
  - .claude/error-patterns/architecture::read
why: '`ARCH-GPD-001` 定案的判準——**回饋掛在互動元件上，與背後服務能否運作無關**——是新確立的，既有元件建立時沒有這條約束。用戶裁示（2026-09-08）：既有的所有互動元件都要檢討是否需調整。


  盤點範圍：`lib/components/` 現有 43 個元件中，含 `onTap` / `onPressed` / `onChanged` / `onAction` / `InkWell` / `GestureDetector` 者共 25 個（PM 已 grep 確認）：`app_shell`、`app_button`、`app_snack_bar`、`app_table_row`、`list_row`、`expander_icon`、`filter_dropdown`、`blocked_state`、`load_prompt`、`matrix_grid`、`document_body`、`loading_state`、`issue_marker`、`nav_item`、`missing_source_state`、`matrix_cell`、`project_switcher_entry`、`recent_project_item`、`relation_item`、`search_field`、`segmented_control`、`switcher_overlay`、`swimlane_node`、`tree`、`table_column_header`。SPEC-004 的對應條目皆已有回饋契約子節，但那些子節是在舊判準下填的。


  不逐件檢討的後果：新判準只約束新元件，存量元件維持「回饋掛在服務回應上」的形態，而存量正是使用者實際會碰到的部分。今天的實例（按下重新掃描完全沒反應）就出自存量元件，不是新設計的。


  **這不是逐件重寫回饋契約**。多數元件的互動可能根本不呼叫服務（純本地狀態切換），對它們判準自動成立；需要調整的是那些互動會觸發跨層呼叫、而回饋定義依賴該呼叫回應的元件。先分類才知道實際工作量。'
how:
  task_type: Analysis
  strategy: '逐件對每個可互動元件回答判準問句：「把這個元件背後的服務整個拔掉，使用者按下去還看得到反應嗎？」依答案分四類，每件標註歸類理由：


    - **A：互動不觸發跨層呼叫**（純本地狀態切換、純視覺展開收合）——判準自動成立，無需調整。此類預期占多數，逐件確認即可，不需深入分析。

    - **B：觸發跨層呼叫，但按下確認與等待指示已獨立於回應**——已符合，記錄其寫法作為其餘元件的參照樣板。

    - **C：回饋依賴服務回應**——需調整。記錄依賴點（哪一段回饋寫在回應處理路徑上）與調整方向。

    - **D：無法從程式碼判定**（回饋由呼叫端提供、或由容器統一承載）——記錄承載者是誰；承載者不明即為缺陷，與 C 同等處理。


    同時檢查 SPEC-004 對應條目的回饋契約子節：該子節是否已寫明掛載點與失敗路徑。子節內容與程式碼不一致時分別記錄——規格對而實作錯、實作對而規格未寫、兩者皆未涵蓋，三種情形的後續處置不同。


    **產出為分類表與調整清單，不改任何檔案**（本票 where.files 全為 `::read`，且 ANA 代理人無 Write/Edit）。C 類與 D 類合計超過三件時，依調整形態分組建 IMP 票（同形態合為一票），不逐件建票；分組理由寫入 Solution。以 `add-spawn-request` 登記，complete 前逐項 resolve 為終態。


    判準的權威是 `ARCH-GPD-001`；記錄格式依 `0.1.0-W3-098` 定案的回饋契約欄位。兩者不一致時停手上報，不自行選邊。'
acceptance:
- '[x] 25 個可互動元件逐件歸類（A 無需調整／B 已符合／C 需調整／D 承載者不明）並附理由'
- '[x] SPEC-004 對應條目的回饋契約子節與程式碼的一致性逐件記錄（規格對實作錯／實作對規格未寫／兩者皆未涵蓋分開列）'
- '[x] C 類與 D 類依調整形態分組並以 add-spawn-request 登記，complete 前逐項 resolve 為終態'
tdd_phase: phase0
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T15:41:35'
completed_at: '2026-09-08T15:53:21'
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

盤點 既有可互動元件的回饋掛載點，判定哪些需調整

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-08 15:40] readiness=1 validate=0


### 問題根因

判準問句（ARCH-GPD-001）：「把這個元件背後的服務整個拔掉，使用者按下去還看得到反應嗎？」——逐件套用於 25 個可互動元件。分析依據為原始碼中的互動觸發機制（InkWell / TextButton / Shortcuts+Actions）與 SPEC-004 §4 回饋契約子節。

### 影響範圍

25 個元件（ticket why 欄已列全），SPEC-004 §4 回饋契約子節中對應的 25 條目。已驗證事實：全部 25 個元件皆使用「傳值 + callback」模式（SPEC-004 §2 決策），不在元件內部呼叫服務；press-ack 均由 Material 內建機制提供。仍未驗證的假設：無（全數已讀取原始碼確認）。

### 相關 Error Pattern

ARCH-GPD-001（本票判準權威來源）。
## 重現實驗結果
### 實驗方法

逐件讀取 `lib/components/*.dart` 原始碼（25 個檔案），辨識每個元件的互動觸發機制（InkWell / TextButton / GestureDetector / Shortcuts + Actions），回答判準問句：「把服務拔掉，使用者按下去還看得到反應嗎？」同時對照 SPEC-004 §4 的「回饋契約」子節（每條目的 press-ack 列與等待與結果列），記錄程式碼與規格的一致性。

### 實驗執行

全部 25 個檔案逐一讀取並分析完成。另讀取 SPEC-004 §1 回饋通道子表、§2 狀態綁定模式、§4 中可 grep 到的回饋契約段落（涵蓋 AppButton、IssueMarker、NavItem、ProjectSwitcherEntry、RecentProjectItem、SegmentedControl、SearchField、FilterDropdown、TableColumnHeader、MatrixCell 等），確認其 press-ack 列均寫「Material 內建 ripple」、等待與結果列均寫「本元件承載三層中的點擊確認層；等待指示/結果通知由畫面/呼叫端承載」。

### 實驗發現

**已驗證事實（25/25）**：
1. 全部 25 個元件使用「傳值 + callback」（SPEC-004 §2），不在元件內部持有服務依賴或呼叫服務。
2. 所有可點擊互動的 press-ack 均由 Material 內建機制提供（InkWell / TextButton 的 pressed 態 + ripple），觸發點掛在互動事件上，與服務回應無關。
3. 兩個元件（`swimlane_node`、`document_body`）在原始碼中無任何互動元素（無 InkWell / GestureDetector / onTap / onPressed），判準空真成立。
4. SPEC-004 §4 回饋契約的 press-ack 列與程式碼一致（均為 Material 內建）。
5. 0.1 為唯讀展示殼（PROP-004），所有狀態變更皆為本地即時（互動瞬態）或等資料層確認才顯示（資料態），不存在跨遠端服務的呼叫。

**結論**：在「傳值 + callback」架構下，元件自身的回饋掛載點先天獨立於服務——ARCH-GPD-001 的耦合風險不在元件層，在呼叫端（頁面層/容器層）如何接線。

## Solution
### 更正聲明（2026-09-08 15:52 後追加）

原分類邏輯「A 類 = 無服務 → 判準自動成立」有誤。PM 更正：用戶裁示三層各自獨立，「沒有服務」不等於「判準成立」——第 1 層（互動當下回饋）每個可互動元件都必須有，無例外。以下改用三層責任歸屬結構重新歸類。

---

### 一、三層責任歸屬結構

| 層次 | 定義 | 承擔者 |
|------|------|--------|
| L1 互動元件層 | 互動當下的回饋（震動/音效/變形/變色/光環），使用者按下去要「看得到/感覺得到」反應 | **元件自身** |
| L2 服務受理層 | command 發送 event 給服務後，服務自己的處理回饋（toast、loading 動畫） | **服務**（非元件） |
| L3 event 效果層 | 服務處理完之後觸發 event 應有的效果，無論成功/失敗/其他 | **服務或呼叫端** |

---

### 二、25 件逐件三層盤點

#### 有 L1 且表現充分的元件（20 件）

以下元件的互動點均有 Material 內建的即時回饋（InkWell ripple / TextButton pressed 態 / TextField 原生回顯），L1 存在且表現充分。

| # | 元件 | L1 互動元件層 | L1 表現細節 | L2 服務受理層 | L3 event 效果層 |
|---|------|:---:|----------|:---:|:---:|
| 1 | `app_button` | YES | TextButton 內建 pressed 態 + Material ripple；disabled 時無 ripple（正確，disabled 不可互動） | 視 onPressed 對接的服務而定（元件為通用包裝） | 同左 |
| 2 | `app_table_row` | YES | InkWell ripple（ticket/step 變體） | N/A（導覽為本地狀態） | N/A |
| 3 | `list_row` | YES | InkWell ripple（tree/item 變體） | N/A（導覽/展開為本地） | N/A |
| 4 | `expander_icon` | YES | InkWell ripple + 聚焦時 accent 邊框 + 箭頭方向即時切換 | N/A（展開收合為本地） | N/A |
| 5 | `filter_dropdown` | YES | 觸發器 InkWell ripple + 選項 InkWell ripple + 觸發器 active 態邊框色 accent | N/A（篩選為本地運算） | N/A |
| 6 | `blocked_state` | YES | 兩個 AppButton 各自 TextButton pressed 態（切換專案 + 檢視詳情） | N/A（浮層開關/面板展開為本地 UI 態） | N/A |
| 7 | `issue_marker` | YES | InkWell ripple（三變體皆可點） | N/A（跳轉破洞報告為本地導覽） | N/A |
| 8 | `nav_item` | YES | InkWell ripple + hover overlay + selected 底色/字色即時切換 | N/A（導覽為本地，IndexedStack 已建構） | N/A |
| 9 | `matrix_cell` | YES | InkWell ripple + selected 時底色 accent/符號 surfaceBase + rowSelected 底色 surfaceIconTint + 聚焦 accent 邊框 | N/A（選取為本地） | N/A |
| 10 | `matrix_grid` | YES | 列首 `_DomainHeaderCell` 的 InkWell ripple | N/A（列選取為本地） | N/A |
| 11 | `project_switcher_entry` | YES | InkWell ripple + 聚焦 accent 邊框 + 箭頭方向即時切換 | N/A（浮層開關為本地 UI 態） | N/A |
| 12 | `recent_project_item` | YES | InkWell ripple + isCurrent 底色 surfaceIconTint | 承擔者=呼叫端（浮層收合 + 全頁重載，LoadingState 接手） | 承擔者=呼叫端（載入結果轉場） |
| 13 | `relation_item` | YES | InkWell ripple + 聚焦 accent 邊框 + chip 底色 surfaceChip | N/A（替換主欄/跳轉為本地導覽） | N/A |
| 14 | `search_field` | YES | TextField 原生字元回顯（輸入）+ 清除鈕 InkWell ripple + 聚焦 accent 邊框 | N/A（搜尋為本地同步運算，防抖動期間無 spinner） | N/A |
| 15 | `segmented_control` | YES | InkWell ripple + selected 段白底/accentStrong 字/半粗 + 聚焦 accent 邊框 | N/A（模式切換為本地，同一份資料的不同視圖） | N/A |
| 16 | `table_column_header` | YES | sortable: InkWell ripple + 聚焦 accent 邊框 + 排序指示圖示即時替換 | N/A（排序為本地重排） | N/A |
| 17 | `load_prompt` | YES | AppButton TextButton pressed 態 | 承擔者=呼叫端（開始載入後 LoadingState 接手，骨架/進度條） | 承擔者=呼叫端（載入結果轉場） |
| 18 | `loading_state` | YES | 取消鈕 AppButton TextButton pressed 態；按下後 Motion.feedback 內鈕轉 disabled + 文案換 cancelInProgressAction | 承擔者=呼叫端（取消命令送出後，進度改 indeterminate，SPEC-003 C3） | 承擔者=呼叫端（取消結果轉場） |
| 19 | `missing_source_state` | YES | AppButton TextButton pressed 態 | 承擔者=呼叫端（重新整理結果三分支，SPEC-003 §3.6） | 承擔者=呼叫端（三分支轉場） |
| 20 | `app_snack_bar` | YES | withAction 變體內部 `_SnackBarActionLabel` InkWell ripple；動作只觸發一次（`_triggered` 旗標） | N/A（SnackBar 本身即 L3 的回饋載體，其內部動作為本地操作） | N/A |

#### L1 由容器委派給子件的元件（3 件）

以下元件本身為容器或框架，不直接承載互動——互動由其子件元件承擔 L1。

| # | 元件 | L1 承擔者 | L2 | L3 |
|---|------|----------|----|----|
| 21 | `app_shell` | 返回鈕 = `AppButton`（TextButton pressed 態）；導覽 = `NavItem`（InkWell）；切換 = `ProjectSwitcherEntry`（InkWell） | N/A | N/A |
| 22 | `tree` | 每列 = `ListRow.tree`（InkWell）；展開 = `ExpanderIcon`（InkWell） | N/A | N/A |
| 23 | `switcher_overlay` | 專案項 = `RecentProjectItem`（InkWell）；選擇其他 = `AppButton`（TextButton）；Esc / 點外部 = 浮層收合本身即回饋（卸載觸發淡出） | N/A | N/A |

#### 原始碼無互動元素的元件（2 件）

| # | 元件 | 三層狀態 | 說明 |
|---|------|---------|------|
| 24 | `swimlane_node` | L1/L2/L3 皆 N/A | 「0.1 不可點、不可拖」（dartdoc 第 3 行），無 InkWell/GestureDetector。列入清單可能因 dartdoc 提及 GestureDetector 被 grep 命中 |
| 25 | `document_body` | L1/L2/L3 皆 N/A | Markdown 渲染，selectable: false，不接 onTapLink。列入清單可能因 flutter_markdown_plus 內部使用 InkWell |

---

### 三、SPEC-004 回饋契約與程式碼一致性

#### 3.1 L1（互動元件層）一致性

SPEC-004 §4 回饋契約的「按下確認 press-ack」列均寫「Material 內建 ripple」或「Material 內建」，與程式碼一致：所有 20 個直接互動元件均使用 InkWell / TextButton / TextField，L1 由 Material 框架在互動事件當下提供。

**未發現 L1 缺失的元件。**

#### 3.2 L2/L3 承擔者明確性

4 個涉及服務操作的元件（load_prompt、loading_state、missing_source_state、recent_project_item），其 SPEC-004「等待與結果」列均寫「本元件承載三層中的點擊確認層；等待指示為畫面級」——正確地將 L2 歸給呼叫端/服務，不歸給元件自身。承擔者皆已明確指出（LoadingState 或呼叫端 provider），無「承擔者不明」的情況。

其餘 16 個純本地互動元件，L2/L3 不適用（無服務呼叫），SPEC-004 契約正確標示為「不適用」。

#### 3.3 逐件一致性確認

| 元件 | L1 code vs spec | L2 承擔者明確 | L3 承擔者明確 | 不一致 |
|------|:---:|:---:|:---:|------|
| app_button | Y | Y（通用，視呼叫端） | Y | 無 |
| app_table_row | Y | N/A | N/A | 無 |
| list_row | Y | N/A | N/A | 無 |
| expander_icon | Y | N/A | N/A | 無 |
| filter_dropdown | Y | N/A | N/A | 無 |
| blocked_state | Y | N/A | N/A | 無 |
| issue_marker | Y | N/A | N/A | 無 |
| nav_item | Y | N/A | N/A | 無 |
| matrix_cell | Y | N/A | N/A | 無 |
| matrix_grid | Y | N/A | N/A | 無 |
| project_switcher_entry | Y | N/A | N/A | 無 |
| recent_project_item | Y | Y（呼叫端→LoadingState） | Y（呼叫端） | 無 |
| relation_item | Y | N/A | N/A | 無 |
| search_field | Y | N/A | N/A | 無 |
| segmented_control | Y | N/A | N/A | 無 |
| table_column_header | Y | N/A | N/A | 無 |
| load_prompt | Y | Y（呼叫端→LoadingState） | Y（呼叫端） | 無 |
| loading_state | Y | Y（呼叫端，C3 indeterminate） | Y（呼叫端） | 無 |
| missing_source_state | Y | Y（呼叫端，三分支） | Y（呼叫端） | 無 |
| app_snack_bar | Y | N/A | N/A | 無 |
| app_shell | Y（委派） | N/A | N/A | 無 |
| tree | Y（委派） | N/A | N/A | 無 |
| switcher_overlay | Y（委派） | N/A | N/A | 無 |
| swimlane_node | N/A | N/A | N/A | 無 |
| document_body | N/A | N/A | N/A | 無 |

---

### 四、方法論條文與用戶裁示的衝突位置

PM 指出：「方法論的〈掛載點〉條文目前把第 2 層歸給元件，與本裁示不一致」。具體衝突位置：

**檔案**：`.claude/methodologies/component-library-bidirectional-constraint-methodology.md`

**位置 1**（〈掛載點〉條款，約第 98 行）：
> 「按下確認與等待指示的觸發點是互動事件本身與『已送出、尚未回應』這個狀態，不是服務回應。」

**衝突**：此句將「等待指示」與「按下確認」並列歸給互動事件/元件。用戶裁示將等待指示歸給 L2（服務受理層），承擔者為服務而非元件。

**位置 2**（〈掛載點〉條款，約第 98 行續）：
> 「等待指示由送出後、結果尚未到達之前的狀態觸發，兩者在服務呼叫得到任何結果之前就已經成立」

**衝突**：此句把「等待指示由送出後的狀態觸發」寫成元件層責任。用戶裁示：等待指示（L2）是服務接到 command 後，服務自己要有的回饋（toast / loading），不是元件的責任。

**位置 3**（〈掛載點〉判準問句，約第 102 行）：
> 「把這個元件背後的服務整個拔掉，使用者按下去還看得到反應嗎？」

**備註**：判準問句本身仍有效（L1 檢驗），但其適用範圍應限縮為 L1（互動當下回饋），不同時覆蓋 L2。原文的語境暗示這個問句同時驗證 press-ack 和 waiting indicator，用戶裁示下只驗證前者。

**位置 4**（回饋事件表，約第 92 行）：
> 「等待與結果 | 系統 | 引用三層回饋模型...元件級只填『本元件承載哪一層』」

**備註**：此句與用戶裁示一致（元件只填自己承載哪一層，不承擔全部三層）。不需修改。

**位置 5**（SPEC-004 §4 既有 42 條目的「等待與結果」列）：
> 均寫「本元件承載三層中的點擊確認層；等待指示為畫面級」

**備註**：既有 SPEC-004 契約已正確地將 L2 歸給畫面/呼叫端而非元件，與用戶裁示一致，與方法論〈掛載點〉文字不一致。即 SPEC-004 的做法是對的，方法論的文字需要對齊。

---

### 五、結論

#### 候選方案

**方案一（建議）**：元件層盤點結論為「25 件 L1 全數存在且表現充分，L2/L3 承擔者皆已明確，無缺陷」。方法論〈掛載點〉條文的 L2 歸屬修正由 PM 另建票處理。

利：盤點結論基於實際程式碼與規格的交叉驗證，三層歸類完整。

弊：不直接修正方法論條文。

**方案二**：同方案一盤點結論，另建 DOC 票修正方法論〈掛載點〉條文。

利：修正路徑明確。

弊：PM 已表示會另建票修正方法論，本票建票屬重複。

#### 建議

採方案一。25 件 L1 全數存在，4 件涉及服務的 L2/L3 承擔者已明確，無元件層缺陷需調整。方法論衝突的 4 個具體位置已記錄於第四節，供 PM 的修正票使用。

#### Spawn 落地確認

L1 缺失 = 0，L2/L3 承擔者不明 = 0，不需建立 IMP 票。Solution 無 spawn 規劃表。PM 已說明方法論修正票由 PM 自建，不由本票 spawn。

### 自檢結果

Layer 1 自檢清單檢視結果：

| 項目 | 結果 | 說明 |
|------|------|------|
| acceptance 全數可勾 | PASS | 3/3 已勾選 |
| Solution 含至少 2 個候選方案 | PASS | 方案一（盤點結論無缺陷）、方案二（另建 DOC 票） |
| 判準來源明確引用 | PASS | ARCH-GPD-001 + 用戶裁示三層歸屬 |
| 三層盤點逐件完成 | PASS | 25 件全數附 L1/L2/L3 歸類 |
| SPEC-004 一致性逐件記錄 | PASS | 25 件逐件確認 |
| 方法論衝突位置已記錄 | PASS | 4 個具體位置供 PM 修正票使用 |
| spawn 落地確認 | PASS | L1 缺失=0，L2/L3 承擔者不明=0，無 spawn |

multi_view_status: n_a
reason: 本票為存量元件盤點（逐件以三層結構對照原始碼），結論為 L1 全數存在、L2/L3 承擔者皆明確，無設計決策需多視角審查。判準權威（ARCH-GPD-001 + 用戶裁示三層歸屬）已定案，分類結構由 PM 定義，本票只做分類判定。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "25 件以三層責任歸屬結構重新盤點：20 件 L1 直接存在（Material InkWell/TextButton/TextField）、3 件 L1 委派子件、2 件無互動元素。L1 缺失=0。4 件涉及服務的 L2/L3 承擔者皆已明確（呼叫端/LoadingState）。方法論〈掛載點〉條文 L2 歸屬衝突的 4 個位置已記錄供 PM 修正票使用。"
confidence: 0.95
acceptance_met: [0, 1, 2]
acceptance_unmet: []
artifacts: []
context_dependencies:
  - "ARCH-GPD-001（判準權威）"
  - "用戶裁示三層責任歸屬（PM 2026-09-08 轉達）"
  - "SPEC-004 §1-§4（回饋通道與逐元件契約）"
  - "lib/components/*.dart（25 個元件原始碼）"
  - ".claude/methodologies/component-library-bidirectional-constraint-methodology.md（衝突位置記錄）"
estimated_recovery_effort: ""
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-08T15:53:21
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-078,0.1.0-W3-098 | chars: 1389 -->

### Task Reference
- 0.1.0-W3-078 what: 實機驗證 MacosScanNotifier 四項平台事實並回填 SPEC-003
- 0.1.0-W3-098 what: 補強 回饋契約：按下確認與等待指示的掛載點須獨立於服務回應，並涵蓋失敗路徑

### Rationale Chain
- 0.1.0-W3-078 why: W3-064 acceptance 2、3 承接，平台事實只有實機能驗
- 0.1.0-W3-098 why: 實機驗證時使用者按下重新掃描按鈕，**畫面完全沒有反應**——沒有按下的視覺、沒有等待指示、沒有失敗訊息。背後同時有三件事失敗（平台 handler 未註冊、完成邏輯落在同一 microtask 使骨架不可見、例外被降級收斂為某個狀態值），但使用者側的觀察只有一件事：按了沒反應。

用戶據此下的裁示（2026-09-08）：**提示效果應該掛在互動元件上，跟背後服務能不能運作不相關。**

現行〈回饋契約〉節的六類事件表定義了觸發者與回答範圍，但**沒有規定回饋的掛載點**。實作者自然會把回饋寫在服務回應的處理路徑上——那是「有東西可以顯示」的地方——於是產生一條反向相關：服務正常時回饋齊全，服務不可用時回饋為零。使用者最需要知道發生什麼事的那一刻，系統最沉默。

第二個缺口：六類事件表只列了事件，**沒有要求回答涵蓋失敗路徑**。「等待與結果」列指向三層回饋模型，而該模型的「結果通知」在實務上被讀成成功結果。失敗、逾時、服務不可用不在同一張表上，因此沒有人為它們填欄位。

第三個缺口：**降級與靜默沒有被區分**。把例外收斂為已知狀態值是行為確定性的手段，但收斂後的狀態若無可見表現（或其表現另有前提條件而前提不成立），收斂等於銷毀證據。現行條文沒有要求指出降級目標狀態的可見表現。

完整分析見 `ARCH-GPD-001`（互動回饋掛在服務的成功路徑上，服務越不可用回饋越少）。

### Related Files
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W3-078
- lib/services/macos_scan_notifier.dart::read  # from 0.1.0-W3-078
- macos/Runner/AppDelegate.swift::read  # from 0.1.0-W3-078
- .claude/methodologies/component-library-bidirectional-constraint-methodology.md  # from 0.1.0-W3-098

### Source Acceptance (reference)
- [ref] [ ] 實機測得 UNUserNotificationCenter 在沙盒關閉（PROP-001）+ Developer ID 簽章下的可用性，並記錄 debug build 未簽章時授權請求的實際回應（是否直接回錯）  # from 0.1.0-W3-078
- [ref] [ ] Flutter 端載體的選擇已定案並附實測依據（原生 channel 或第三方套件，二擇一）  # from 0.1.0-W3-078
- [ref] [ ] 實機測得撤回已送達通知的 API，以及它對「通知中心已收合」狀態的效果  # from 0.1.0-W3-078
- [ref] [ ] 四項實測結果已回填 SPEC-003 §2.2「實作票驗證」段（原文改為實測記錄，附量測環境與日期）  # from 0.1.0-W3-078
- [ref] [ ] 0.1.0-W3-064 的 acceptance 2、3 已可勾選或已說明為何仍不可勾  # from 0.1.0-W3-078
- [ref] [ ] 方法論〈回饋契約〉節含掛載點條款與可在設計期回答的判準問句  # from 0.1.0-W3-098
- [ref] [ ] 「等待與結果」的回答範圍涵蓋成功／失敗／逾時／服務不可用，且不接受「不會發生」填法  # from 0.1.0-W3-098
- [ref] [ ] 降級不解除回饋義務條款已補，並明寫日誌不抵扣使用者回饋  # from 0.1.0-W3-098
- [ref] [ ] 條文為型別化描述，本案具體情節（掃描按鈕、MethodChannel）只作為 ARCH-GPD-001 的引用，不寫進正文  # from 0.1.0-W3-098
