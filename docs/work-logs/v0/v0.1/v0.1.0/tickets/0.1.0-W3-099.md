---
id: 0.1.0-W3-099
title: 盤點 既有可互動元件的回饋掛載點，判定哪些需調整
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-098
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-078
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 新判準確立後的存量檢討
  final_decision: ANA 先逐件判定，再依結論建 IMP/DOC 票
  rationale: 25 個可互動元件不會全部需要調整；直接建 25 張實作票會有多數無事可做，且調整形態可能分數類、值得先歸類再派
who:
  current: saffron-system-analyst
  history: {}
what: 盤點 既有可互動元件的回饋掛載點，判定哪些需調整
when: 0.1.0-W3-098 定案回饋契約條文與齊全判準後（記錄格式須依其定義）
where:
  layer: Presentation
  files:
  - lib/components::read
  - docs/spec/ui/SPEC-004-component-library.md::read
  - .claude/error-patterns/architecture::read
why: '`ARCH-GPD-001` 定案的判準——**回饋掛在互動元件上，與背後服務能否運作無關**——是新確立的，既有元件建立時沒有這條約束。用戶裁示（2026-09-08）：既有的所有互動元件都要檢討是否需調整。


  盤點範圍：`lib/components/` 現有 43 個元件中，含 `onTap` / `onPressed` / `onChanged` / `onAction` / `InkWell` / `GestureDetector` 者共 25 個（PM 已 grep 確認）：`app_shell`、`app_button`、`app_snack_bar`、`app_table_row`、`list_row`、`expander_icon`、`filter_dropdown`、`blocked_state`、`load_prompt`、`matrix_grid`、`document_body`、`loading_state`、`issue_marker`、`nav_item`、`missing_source_state`、`matrix_cell`、`project_switcher_entry`、`recent_project_item`、`relation_item`、`search_field`、`segmented_control`、`switcher_overlay`、`swimlane_node`、`tree`、`table_column_header`。SPEC-004 的對應條目皆已有回饋契約子節，但那些子節是在舊判準下填的。


  不逐件檢討的後果：新判準只約束新元件，存量元件維持「回饋掛在服務回應上」的形態，而存量正是使用者實際會碰到的部分。今天的實例（按下重新掃描完全沒反應）就出自存量元件，不是新設計的。


  **這不是逐件重寫回饋契約**。多數元件的互動可能根本不呼叫服務（純本地狀態切換），對它們判準自動成立；需要調整的是那些互動會觸發跨層呼叫、而回饋定義依賴該呼叫回應的元件。先分類才知道實際工作量。'
how:
  task_type: Analysis
  strategy: '逐件對每個可互動元件回答判準問句：「把這個元件背後的服務整個拔掉，使用者按下去還看得到反應嗎？」依答案分四類，每件標註歸類理由：


    - **A：互動不觸發跨層呼叫**（純本地狀態切換、純視覺展開收合）——判準自動成立，無需調整。此類預期占多數，逐件確認即可，不需深入分析。

    - **B：觸發跨層呼叫，但按下確認與等待指示已獨立於回應**——已符合，記錄其寫法作為其餘元件的參照樣板。

    - **C：回饋依賴服務回應**——需調整。記錄依賴點（哪一段回饋寫在回應處理路徑上）與調整方向。

    - **D：無法從程式碼判定**（回饋由呼叫端提供、或由容器統一承載）——記錄承載者是誰；承載者不明即為缺陷，與 C 同等處理。


    同時檢查 SPEC-004 對應條目的回饋契約子節：該子節是否已寫明掛載點與失敗路徑。子節內容與程式碼不一致時分別記錄——規格對而實作錯、實作對而規格未寫、兩者皆未涵蓋，三種情形的後續處置不同。


    **產出為分類表與調整清單，不改任何檔案**（本票 where.files 全為 `::read`，且 ANA 代理人無 Write/Edit）。C 類與 D 類合計超過三件時，依調整形態分組建 IMP 票（同形態合為一票），不逐件建票；分組理由寫入 Solution。以 `add-spawn-request` 登記，complete 前逐項 resolve 為終態。


    判準的權威是 `ARCH-GPD-001`；記錄格式依 `0.1.0-W3-098` 定案的回饋契約欄位。兩者不一致時停手上報，不自行選邊。'
acceptance:
- '[ ] 25 個可互動元件逐件歸類（A 無需調整／B 已符合／C 需調整／D 承載者不明）並附理由'
- '[ ] SPEC-004 對應條目的回饋契約子節與程式碼的一致性逐件記錄（規格對實作錯／實作對規格未寫／兩者皆未涵蓋分開列）'
- '[ ] C 類與 D 類依調整形態分組並以 add-spawn-request 登記，complete 前逐項 resolve 為終態'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

盤點 既有可互動元件的回饋掛載點，判定哪些需調整

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-078,0.1.0-W3-098 | chars: 1580 -->

### Task Reference
- 0.1.0-W3-078 what: 實機驗證 MacosScanNotifier 四項平台事實並回填 SPEC-003
- 0.1.0-W3-098 what: 補強 回饋契約：按下確認與等待指示的掛載點須獨立於服務回應，並涵蓋失敗路徑

### Rationale Chain
- 0.1.0-W3-078 why: W3-064 acceptance 2、3 承接，平台事實只有實機能驗
- 0.1.0-W3-098 why: 實機驗證時使用者按下重新掃描按鈕，**畫面完全沒有反應**——沒有按下的視覺、沒有等待指示、沒有失敗訊息。背後同時有三件事失敗（平台 handler 未註冊、完成邏輯落在同一 microtask 使骨架不可見、例外被降級收斂為某個狀態值），但使用者側的觀察只有一件事：按了沒反應。

用戶據此下的裁示（2026-09-08）：**提示效果應該掛在互動元件上，跟背後服務能不能運作不相關。**

現行〈回饋契約〉節的六類事件表定義了觸發者與回答範圍，但**沒有規定回饋的掛載點**。實作者自然會把回饋寫在服務回應的處理路徑上——那是「有東西可以顯示」的地方——於是產生一條反向相關：服務正常時回饋齊全，服務不可用時回饋為零。使用者最需要知道發生什麼事的那一刻，系統最沉默。

第二個缺口：六類事件表只列了事件，**沒有要求回答涵蓋失敗路徑**。「等待與結果」列指向三層回饋模型，而該模型的「結果通知」在實務上被讀成成功結果。失敗、逾時、服務不可用不在同一張表上，因此沒有人為它們填欄位。

第三個缺口：**降級與靜默沒有被區分**。把例外收斂為已知狀態值是行為確定性的手段，但收斂後的狀態若無可見表現（或其表現另有前提條件而前提不成立），收斂等於銷毀證據。現行條文沒有要求指出降級目標狀態的可見表現。

完整分析見 `ARCH-GPD-001`（互動回饋掛在服務的成功路徑上，服務越不可用回饋越少）。

### Related Files
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W3-078
- lib/services/macos_scan_notifier.dart::read  # from 0.1.0-W3-078
- macos/Runner/AppDelegate.swift::read  # from 0.1.0-W3-078
- .claude/methodologies/component-library-bidirectional-constraint-methodology.md  # from 0.1.0-W3-098
- .claude/skills/doc/templates/component-library-spec-template.md  # from 0.1.0-W3-098
- .claude/skills/component-contract-design/SKILL.md  # from 0.1.0-W3-098

### Source Acceptance (reference)
- [ref] [ ] 實機測得 UNUserNotificationCenter 在沙盒關閉（PROP-001）+ Developer ID 簽章下的可用性，並記錄 debug build 未簽章時授權請求的實際回應（是否直接回錯）  # from 0.1.0-W3-078
- [ref] [ ] Flutter 端載體的選擇已定案並附實測依據（原生 channel 或第三方套件，二擇一）  # from 0.1.0-W3-078
- [ref] [ ] 實機測得撤回已送達通知的 API，以及它對「通知中心已收合」狀態的效果  # from 0.1.0-W3-078
- [ref] [ ] 四項實測結果已回填 SPEC-003 §2.2「實作票驗證」段（原文改為實測記錄，附量測環境與日期）  # from 0.1.0-W3-078
- [ref] [ ] 0.1.0-W3-064 的 acceptance 2、3 已可勾選或已說明為何仍不可勾  # from 0.1.0-W3-078
- [ref] [ ] 方法論〈回饋契約〉節含掛載點條款與可在設計期回答的判準問句  # from 0.1.0-W3-098
- [ref] [ ] 「等待與結果」的回答範圍涵蓋成功／失敗／逾時／服務不可用，且不接受「不會發生」填法  # from 0.1.0-W3-098
- [ref] [ ] 降級不解除回饋義務條款已補，並明寫日誌不抵扣使用者回饋  # from 0.1.0-W3-098
- [ref] [ ] 下游範本與 component-contract-design skill 已同步；條文為型別化描述，具體情節只引用 ARCH-GPD-001  # from 0.1.0-W3-098
- [ref] [ ] component-contract-design skill 的引導流程在「判定為可互動元件後、填回饋契約前」問出掛載點判準問句  # from 0.1.0-W3-098
  ... (truncated, see source ticket)
