---
id: 0.1.0-W3-134
title: 補強 測試斷言鑑別力：升級無鑑別力斷言並評估變異測試
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-121
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: Phase 4a 查出缺陷能溜過 20 個紅燈的載體是無鑑別力斷言
  final_decision: IMP 升級現存無鑑別力斷言，並評估是否引入變異測試
  rationale: 斷言退化目前無任何自動化偵測；此為防護機制本身的缺口，不是單一 bug
who:
  current: parsley-flutter-developer
  history: {}
what: 補強 測試斷言鑑別力：升級無鑑別力斷言並評估變異測試
when: 0.1.0-W3-121 Phase 4a 完成後（2026-09-08）
where:
  layer: Infrastructure
  files:
  - test/unit/workspace/workspace_repository_test.dart
  - pubspec.yaml
why: 'Phase 4a 對「防護類產出」的四問查出兩層問題。


  **第一層：斷言退化無人察覺。** 實查 `pubspec.yaml` 的 dev_dependencies 僅 `flutter_test`、`integration_test`、`flutter_lints`，**無變異測試（mutation testing）工具，亦無 coverage 門檻強制機制**。若某條斷言退化成恆真式，1205 個測試仍全綠、CI 仍通過，沒有任何自動化機制會察覺。唯一路徑是人工審查——抽樣、事後、依賴審查者記憶，不是可重複執行的防線。


  **第二層：現存無鑑別力斷言清單。** 3b-C 引入的 `reason: ''$e''` 缺陷之所以能通過 RED 批 20 個紅燈與 acceptance 全綠，載體是 `workspace_repository_test.dart:303` 的 `expect(result.reason, isNotEmpty)`——固定文案與例外字串化結果皆非空，此斷言必然通過。同型斷言尚存四處：


  | 位置 | 斷言 | 缺口 |

  |------|------|------|

  | `:180`（G2-2） | `expect(notRemembered.reason, isNotEmpty)` | 無法區分固定文案與例外原文 |

  | `:261`（G2-6） | `expect((result as ChooseFolderUnavailable).reason, isNotEmpty)` | 同上 |

  | `:305-308`（G3-2） | `log.entries.any((e) => e.level == 900)` | 只驗「存在某條 level 900」，未比對訊息內容；日誌錯置到不相關事件仍會通過 |

  | `:434-437`（G6-2） | `log.entries.any((e) => e.level == 900)` | 同上 |


  Redundancy 視角另指出 `level == 900` 樣板全檔重複 6 次，**其中只有 `:184` 檢查了訊息內容**，其餘五處都是弱斷言。


  **第三層：死欄位使斷言少了一個獨立證據面。** `_Call.args`（`:478`）被三個 Recorder 寫入、全檔零讀取；`_LogEntry.error`（`:560`）被寫入、零斷言。前者的後果具體：G1-4（`:62-75`）現在只用日誌子字串驗 `workspace.path` 與路徑值，而 `_HandleRecorder` 已把 `writeString` 的 key/value 記進 `calls`——用它斷言才是「不靠日誌」的獨立第二證據，正合 G5「不互相抵扣」的精神。目前那條斷言被寫成日誌側，args 因此是死的。


  **第四層：G4-1 漏了存在性檢查。** `:368-372` 是順序斷言的手寫展開版，缺 G2-1／G3-1 都有的 `everyElement(greaterThanOrEqualTo(0))`。三個 `indexOfSubstring` 若全回 `-1`，`-1 > -1` 為 false 會紅燈；但若只有第一個回 `-1`，`acceptedIndex > -1` 反而成立——漏掉的事件不會被抓到。'
how:
  task_type: Implementation
  strategy: '1. 四處 `isNotEmpty` 與 `level == 900` 斷言升級為含內容檢查：`reason` 類比照 G3-2b（`:314-328`）的形態斷言不含例外型別名；日誌類改為 `hasErrorContaining(<訊息>)`，驗證是「該分支自己記了錯誤」而非「某處有錯誤」。

    2. 補 `_Call.args` 與 `_LogEntry.error` 的斷言，使其成為活欄位——**方向是補斷言不是刪欄位**。G1-4 改用 `preferences.calls` 的 args 斷言 key/value，取得不依賴日誌的第二證據。

    3. 順序斷言抽 `_expectLoggedInOrder(log, messages)` helper，三處共用（`:154-164`、`:281-289`、`:368-372`），順手補上 G4-1 缺的存在性檢查。此抽取不衝突 G5——G5 約束的是「回傳型別斷言 + 日誌斷言」不得包成單一 helper，順序斷言不在其內。

    4. G2-4 的 arrange 逐字複製 G2-1／G2-2（`:211-220` vs `:140-145`／`:169-174`），抽 `_repoWriting(bool result, {_LogRecorder? log})` 工廠。此項不只減行數：G2-4 的立論是「兩者輸入除 writeString 回傳外完全相同」，目前兩份 arrange 各自獨立，日後有人改其中一份，跨案例等價斷言會靜默失去比較基準。

    5. 評估是否引入變異測試工具（Dart 生態的可用選項、CI 成本、是否值得）。**評估結論寫入 Solution，不引入也要寫理由**——這是本票要回答的問題，不是可略過的加分項。


    順序：先做 1-4（確定有價值），5 為評估。'
acceptance:
- '[ ] 四處無鑑別力斷言（:180 / :261 / :305-308 / :434-437）升級為含內容檢查'
- '[ ] _Call.args 與 _LogEntry.error 成為活欄位；G1-4 取得不依賴日誌的第二證據'
- '[ ] 順序斷言抽為共用 helper，G4-1 補上存在性檢查'
- '[ ] G2-4 的 arrange 改用工廠，跨案例等價斷言的比較基準不再依賴兩份獨立 arrange 保持同步'
- '[ ] 變異測試引入與否已評估並在 Solution 寫明理由（不引入亦須寫）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補強 測試斷言鑑別力：升級無鑑別力斷言並評估變異測試

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-121 | chars: 1368 -->

### Task Reference
- 0.1.0-W3-121 what: 補齊 WorkspaceRepository 三個 driven port 呼叫的回饋契約

### Rationale Chain
- 0.1.0-W3-121 why: `lib/workspace/workspace_repository.dart` 有四個 driven port 呼叫點，依 `clean-architecture-implementation-methodology.md`〈Port 回饋契約〉的 INV-PORT-OBSERVE-001 全數不合規（PM 逐行實查）：

1. `chooseFolder()` 的 `getDirectoryPath()`（file_selector 平台通道）：無 try/catch、無日誌。MissingPluginException 或使用者環境異常時直接往上拋，呼叫端拿到的是未分類例外。
2. `chooseFolder()` 的 `SharedPreferences.getInstance()` / `setString(_pathKey, path)`：**回傳值完全未檢查**。`setString` 回傳 `Future<bool>`，寫入失敗時函式照樣往下走並回傳 `_inspect(path)`——使用者看到資料夾已選定，但下次 `restore()` 讀不到值會回 `WorkspaceUnset`。這是方法論「典型違反」的加重變體：連日誌都沒有，呼叫端與日誌兩個獨立義務同時缺席。
3. `restore()` 的 `getInstance()` / `getString()`：無失敗路徑。Phase 1 已修正本條的初始描述——`getString()` 是純記憶體查表無 I/O 失敗路徑，真正的讀取失敗集中在會拋例外的 `getInstance()`，兩者不可混為一談，詳見 Solution 裁決 B。
4. `_inspect()` 的 `Directory.exists()` 與 `dir.list().first`（dart:io 檔案系統）：現況只有 `FileSystemException` 分支有日誌，屬結果時刻的失敗分支；`exists()` 完全無記錄，兩處皆缺「呼叫發出」事件。本條由 Phase 1 的 SR-1 補入——PM 建票時盤點遺漏，非後續擴張。

四處皆缺「呼叫發出」事件，判準問句「把外部系統整個拔掉，呼叫端能否得知呼叫已發出但無回應」目前答案為否。

測試覆蓋現況：`test/` 下無 workspace_repository 的單元測試檔，僅 `integration_test/app_test.dart` 間接引用。

範圍界線：本票只處理 App 端邏輯軌的回饋契約合規。平台端事實軌（實機驗證 file_selector 與真實檔案系統行為）由 `0.1.0-W3-088` 評估，兩者不重疊。`gap_report_screen._openItem` 的同類缺口由 `0.1.0-W3-112` 承接，不在本票範圍。

### Related Files
- lib/workspace/workspace_repository.dart  # from 0.1.0-W3-121
- lib/main.dart  # from 0.1.0-W3-121
- lib/l10n/app_zh.arb  # from 0.1.0-W3-121
- lib/l10n/app_en.arb  # from 0.1.0-W3-121
- lib/l10n/app_localizations.dart  # from 0.1.0-W3-121
  ... (truncated, see source ticket)

