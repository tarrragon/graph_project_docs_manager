---
id: 0.1.0-W3-437.3
title: 評估 F-16 元件自身資料態驅動外觀的建模慣例（狀態集 vs 變體參數）
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-437
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 閘門範例補依據
  final_decision: 新判準交獨立 WRAP 評估者
  rationale: 用戶常駐裁示：草稿與矛盾交 WRAP
who:
  current: general-purpose
  history: {}
what: 評估 F-16 元件自身資料態驅動外觀的建模慣例（狀態集 vs 變體參數）
when: W2 開工閘門前
where:
  layer: 待定義
  files:
  - .claude/skills/component-contract-design/references/step-3-fill-contract.md
why: 用戶要求以 WRAP 評估 F-16 修補方向（W3-437.1 SR-2）
how:
  task_type: Analysis
  strategy: wrap-decision 完整模式評估；草稿與建議寫入 Solution；屬用戶偏好者明標
acceptance:
- '[ ] Solution 含 WRAP 判定、建議改文與是否屬用戶偏好'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-15'
updated: '2026-09-15'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

評估 F-16 元件自身資料態驅動外觀的建模慣例（狀態集 vs 變體參數）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 評估對象（PM 寫入的派發 context）

- 問題：元件外觀隨元件自身資料態改變（如封存卡片變暗），要建模為狀態集的一個狀態，還是變體參數。
- 兩個選項：
  - (A) 歸狀態集：沿用 step-3 狀態集來源（含資料態）；〈共用規則一〉「外觀不同：降為變體」與 path-b 步驟 3 補射程子句（只管兩個候選元件之間的外觀差）；正例一補 archived 狀態。
  - (B) 變體參數：改 step-3 狀態集來源刪資料態；正例一改變體寫法；〈共用規則一〉原文不動。
- 先讀：0.1.0-W3-437.1 Solution（F-16 判定與六處改文草稿）、step-3-fill-contract.md 狀態集列、step-2-extract-components.md〈共用規則一〉、step-2-path-b-canvas-decomposition.md 步驟 3、appendix-examples.md 正例一。
- 用戶未直接裁示，要求以 WRAP 評估；W 階段不限於 A/B。R 階段須查業界元件庫（Material、設計系統文件）對資料態驅動外觀的慣例作基本率，並查本專案 SPEC／docs 內既有元件庫規格是否已隱含採用某一方。
- 產出：判定 + 建議選項 + 若非 A 則附改文草稿；不改 skill 檔。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending
