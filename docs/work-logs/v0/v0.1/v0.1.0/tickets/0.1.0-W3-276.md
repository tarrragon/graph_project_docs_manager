---
id: 0.1.0-W3-276
title: 擴充 rule8 守衛的偵測範圍至 consumer 分域編號與符號名，並盤點自動載入層存量
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-268
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 守衛放行了規則 8 明文禁止的引用形態，違規靠人工審查才被發現
  final_decision: 擴充守衛偵測範圍並盤點存量，另評估是否升級為框架 issue
  rationale: PC-BAL-024 的新實例（規則射程寬於守衛偵測條件），根源在 .claude/ 通用資產且敘述可攜
who:
  current: 主線程
  history: {}
what: 擴充 rule8 守衛的偵測範圍至 consumer 分域編號與符號名，並盤點自動載入層存量
when: 0.1.0-W3-268 的 Layer 2 審查發現自動載入層新增內容含 consumer 分域 error-pattern 編號時
where:
  layer: Infrastructure
  files:
  - .claude/hooks/reference-stability-rule8-guard-hook.py
  - .claude/references/reference-stability-rules.md
why: '規則 8 的識別符類型表明列『框架 error-pattern ID，尚未到達共用通道（含 consumer 分域形式 PC-<CONSUMER>-0NN）』為禁止引用，理由是它在其他 consumer 手上是懸空引用。但 rule8 守衛只偵測專案 ticket ID 樣式與專案目錄開頭的路徑，對分域 error-pattern 編號與程式碼符號名（測試函式名、參數名）零偵測。


  **實測命中**：本批於 .claude/rules/core/test-assertion-design-rules.md（自動載入層，隨 sync 擴散至全部 consumer）新增內容含一個分域 error-pattern 編號三處、測試函式名與參數名各一處，守衛全程未出聲；同一批對 .claude/references/reference-stability-rules.md 寫入票號時守衛正確 exit 2 阻擋。**同一條規則在同一批被遵守一次、違反一次，差別只在守衛有沒有出聲**——合規實際追隨的是守衛射程而非規則射程。


  **這是 PC-BAL-024 的新實例**（規則收緊／射程寬於守衛偵測條件，落差被規則文件寫下卻未修補），不是新形態，故不另建 error-pattern；本票處理的是該落差在規則 8 上的具體修補。


  **存量已知非零**：Layer 2 審查者指出本檔既有 sibling 已有同型引用，本批使其增生而非首創。存量範圍未盤點。


  **邊界**：符號名（測試函式名、變數名）是否一律禁止需先裁定——框架文件示範程式碼時本就會出現符號名，全禁會產生大量誤報。可能的收窄是只管『作為實證指涉的具名符號』，判別條件需設計。'
how:
  task_type: Implementation
  strategy: 三步。(1) 盤點存量：掃 .claude/rules/core/ 與其他隨 sync 擴散的路徑，找 consumer 分域 error-pattern 編號（PC-<CODE>-0NN 形式，CODE 取自 _project-registry.yaml）與具名符號指涉，命中清單記入本票。(2) 擴充守衛：分域編號可機械判定（比對 registry 的 code 清單），優先做這一項；符號名的判別條件先裁定再決定是否納入，未裁定前不做以免誤報。(3) 依 framework-issue 兩條 AND 判準判定是否升級為框架 issue。新增偵測條件須配一個正向對照輸入（test-assertion-design-rules 規則 E2）：測試含一個已知該被攔下的分域編號並斷言其確實被攔。
acceptance:
- '[ ] 存量盤點完成，命中清單（檔案 + 引用形態）記入本票'
- '[ ] 守衛已能偵測 consumer 分域 error-pattern 編號，且測試含一個已知該紅的輸入並斷言其為紅（E2）'
- '[ ] 符號名是否納入偵測已裁定，理由記入 Solution；判定不納入則寫明規則 8 該處由何者保證'
- '[ ] 已依 framework-issue 兩條 AND 判準判定是否升級為框架 issue，判定結果記入本票'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

擴充 rule8 守衛的偵測範圍至 consumer 分域編號與符號名，並盤點自動載入層存量

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-268 | chars: 1248 -->

### Task Reference
- 0.1.0-W3-268 what: 建立 測試鑑別力的兩條設計要求：明示旗標的對照測試與守衛的正向對照輸入（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-268 why: `PC-GPD-020`（明示的意圖與明示的失敗，在可觀測面與預設或成功不可區分）歸納出七個實例。該 error-pattern 記錄的是**知識**，本票要落地的是它衍生出的**兩條可機械檢查的設計要求**——兩者載體不同，前者按需讀取、後者須進測試設計規則才會在寫測試時被想起。

**查證結果：兩條在 `.claude/rules/`、`.claude/references/`、`.claude/methodologies/` 全域零命中。**

**第一條，明示旗標的對照測試。** 只斷言「傳了旗標之後產物正確」的測試，在旗標完全無效時照樣全綠——因為它沒有比較對象。實例：`test_no_topic_writes_no_assignment` 以 `_make_args(no_topic=True)` 呼叫，fixture 不帶 `source_ticket`，走不到會產生差異的 S1 繼承路徑；該路徑上 `no_topic` 為真或為假結果相同，於是測試覆蓋的正好是旗標不改變任何結果的情境。修法是同批各跑一次傳與不傳，斷言**產物不同**。

**第二條，守衛的正向對照輸入。** 守衛的失效方向是放行，而放行與「本來就沒問題」在輸出上同形。只給正常輸入的測試無法分辨守衛在工作還是守衛已死。實例：靜態守衛的註解剝除器剝過頭，使多數計數斷言變綠——守衛失效且無聲。修法是測試必須含一個**已知該紅的輸入**並斷言它確實紅。

**為何這兩條值得從 pattern 升格為規則**：`PC-GPD-020` 的七個實例中，沒有一個是靠它自己的測試被發現的——三個靠執行者花整輪定位、兩個靠 Phase 4a 專門去查、兩個靠並行 session 實測。這不是巧合而是定義使然：失效方向為成功的機制，其自身測試若只走正向路徑就必然全綠。**要發現它只能從外部給一個已知該紅的輸入**，而這件事現在完全依賴審查者的個人警覺，沒有任何條文要求。

**邊界**：既有規則 D1～D5 治的是「斷言結果依賴程式以外的可變因素」（機器負載、網路、時區），本二條治的是「斷言對目標功能無鑑別力」。兩者不同軸，一個斷言可以完全穩定（永不 flaky）而同時完全沒有鑑別力。（來源已完成，結論見 Solution）

### Related Files
- .claude/rules/core/test-assertion-design-rules.md  # from 0.1.0-W3-268（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 新增規則：新增或修改旗標時，測試須同批各跑一次傳與不傳並斷言產物不同，僅斷言「傳了之後產物正確」不合格  # from 0.1.0-W3-268（來源已完成，結論見 Solution）
- [ref] [x] 新增規則：守衛／檢核器的測試須含一個已知該紅的輸入，斷言它確實紅；僅斷言正常輸入通過不合格  # from 0.1.0-W3-268（來源已完成，結論見 Solution）
- [ref] [x] 兩條各附一個已發生的實例（--no-topic 空操作而測試全綠；註解剝除器剝過頭使計數斷言變綠）  # from 0.1.0-W3-268（來源已完成，結論見 Solution）
- [ref] [x] 與既有規則 D1~D5 的邊界寫明：D1~D5 治「斷言結果依賴程式以外因素」，本二條治「斷言對目標功能無鑑別力」，兩者不同軸  # from 0.1.0-W3-268（來源已完成，結論見 Solution）

