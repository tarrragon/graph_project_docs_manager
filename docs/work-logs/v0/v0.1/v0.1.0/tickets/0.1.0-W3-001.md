---
id: 0.1.0-W3-001
title: 執行 框架 sync-pull 取得最新版並解衝突（W3 全部框架票的前置）
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-009
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶裁示：框架庫未取得最新版，先建票不直接改
  final_decision: 建 W3-001 為前置，其餘框架票全部 blockedBy 本票
  rationale: 先拉再改才能避免雙向衝突；獨立 wave 便於辨認整批框架工作
who:
  current: rosemary-project-manager
  history: {}
what: 執行 框架 sync-pull 取得最新版並解衝突（W3 全部框架票的前置）
when: 任何 .claude/ 修改之前
where:
  layer: Framework
  files:
  - .claude/
why: 本地框架庫自 2026-08-27 起未 sync-pull，期間本專案已在 .claude/ 新增 component-contract-design skill、方法論擴章、範本擴充與 PC-GPD-004；若不先拉最新版就直接改框架檔，之後 sync-pull 會與 canonical 的異動衝突，且本專案的框架回饋也無法乾淨推回。
how:
  task_type: Implementation
  strategy: 依 sync skill 流程 sync-pull；三方合併衝突逐檔檢視（優先保留本專案 W1-041／042／043／050／051／053 的新增內容）；拉完後跑 SessionStart 各 hook 健檢確認 skill 註冊、hook 完整性無退化；記錄拉到的 canonical 版本與衝突清單於 Solution。
acceptance:
- '[x] sync-pull 完成，Solution 記錄 canonical 版本與衝突處理清單'
- '[x] SessionStart 健檢（skill 註冊、hook 完整性、禁用詞掃描）全部通過'
- '[x] 本專案既有框架資產（component-contract-design skill、方法論〈元件契約判準〉、範本擴充、PC-GPD-004）內容無遺失'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-03T11:45:51'
completed_at: null
created: '2026-09-03'
updated: '2026-09-03'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

執行 框架 sync-pull 取得最新版並解衝突（W3 全部框架票的前置）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### WRAP 三問（認領 checkpoint）

**W（有其他做法嗎）**：候選一為「先推後拉」，直接 push 四個本地改過的 skill 再處理落差；候選二為「先拉後推」，先取回 canonical 的異動、逐檔合併、再推。採候選二——canonical 端在本地未同步期間改過 foundation-design，先推會覆蓋該端改動。

**A（機會成本）**：本次投入排擠的是 W2 畫面票的派發。可接受——W3 其餘七張框架票全部 blockedBy 本票，不先做則整批框架回饋無法落地，且落差只會隨時間擴大。

**P（最可能失敗的原因）**：誤判合併方向，把 canonical 的新內容當成本地舊內容覆蓋掉。防護是不依賴 skill-sync 的方向報告，改以 `git log dad9610..HEAD` 界定本地實際改過的範圍，並 clone canonical 逐檔 diff 後才決定取捨。

### 落差範圍界定

本地最後一次框架同步為 `dad9610`（2026-08-27）。以 `git log dad9610..HEAD -- .claude/skills/` 界定，此後本地只動過四個 skill：component-contract-design（新建）、doc、foundation-design、version-bootstrap。其餘 54 個標記為 DIVERGED 的 skill 本地未動，差異來自 canonical 端，不在本次推送範圍。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### canonical 版本

推送前 canonical HEAD 為 `aa0f6d1` 之前；推送後為 `5f3fe22`（含本次四筆內容 commit 與各自的 versions.json 更新）。

### 拉取（本地未動、canonical 有更新）

| skill | 內容 |
|-------|------|
| skill-design-guide | +CHANGELOG.md，SKILL.md +3/-4 |
| version-sequencing | +CHANGELOG.md，SKILL.md +1/-2 |

### 衝突處理

canonical 期間改採新慣例：版本紀錄由 SKILL.md 檔尾 inline 外移至同目錄 `CHANGELOG.md`，frontmatter 保留 `metadata.version` 作為第二住址。本地三個既有 skill 仍為舊慣例，逐一改採新慣例並保留本地內容改動。

| skill | canonical 端改動 | 本地端改動 | 處置 |
|-------|-----------------|-----------|------|
| foundation-design | 版本紀錄外移 CHANGELOG（真衝突，兩側皆動） | UI 列補 component-contract-design 反向路由 | 取 canonical 的 CHANGELOG，把本地 6.1.0 一則併入其頂部，SKILL.md 檔尾改指向 CHANGELOG，frontmatter 升 6.1.0 |
| doc | 版本紀錄外移 CHANGELOG + frontmatter 補 metadata.version 1.11.0 | 元件庫規格模板列、cp 命令、模板擴為十一欄位 | 同上形態；新增 1.12.0 一則，frontmatter 升 1.12.0 |
| version-bootstrap | 同 doc（metadata.version 1.4.0） | Step 2 檢查項對齊、Step 4.5 插入 3.5 元件契約 | 同上形態；新增 1.5.0 一則，frontmatter 升 1.5.0 |

### 推送

| skill | 變更 |
|-------|------|
| component-contract-design | 新增（canonical 原本無此 skill）：SKILL.md + CHANGELOG.md |
| doc | CHANGELOG +2、SKILL.md +5/-3、templates/component-library-spec-template.md +163/-32；其餘 60 檔未變 |
| foundation-design | CHANGELOG +2、SKILL.md +2/-2 |
| version-bootstrap | CHANGELOG +1、SKILL.md +6/-5；其餘 3 檔未變 |

component-contract-design 已宣告 `metadata.portable: true`，可攜性閘門零違規（無專案票號、無 `.claude/` 路徑、無代理人名稱或專案名稱）。另三個未宣告 portable，閘門只報數不阻擋，其命中全部落在既有的測試檔與舊 SKILL.md 行，非本次新增內容。

### 復驗

推送後重新 clone canonical，`diff -rq` 逐檔比對四個 skill，本地與 canonical 內容完全一致（排除 egg-info、__pycache__、.venv、project-integration、.skill-sync-base）。

`skill-sync pull` 的報告在推送後仍顯示這三個 skill 為 `[SHOULD PULL]` 且 remote hash 未變。該報告讀的是遠端 `versions.json` manifest，內容落後於實際 git 樹（raw 端快取），屬「輸出真實但過期」，非推送失敗——依 tool-output-trust-rules 規則 6 改查建立端（實際 clone 的檔案內容）確認。

### 健檢

| 項目 | 結果 | 與 session 啟動基線 |
|------|------|-------------------|
| skill 註冊完整性 | 已註冊 58、未註冊 0 | 一致 |
| hook 完整性 | 已註冊 110、未註冊 0；skill hooks 37/38 | 一致（未註冊的 ticket/hooks/handoff_session_mgmt.py 為既有狀態） |
| skill 禁用詞掃描 | 掃 471 檔、命中 0 | 一致（檔數增加來自新拉的 CHANGELOG） |
| skill 殘留 | 無 blocking 級 | 一致 |

### 自檢結果

- `skill-sync pull`（報告）：SHOULD PULL 2 已拉、CONFLICT 1 已手動合併、DIVERGED 54 未動（非本次範圍）
- `diff -rq canonical/<skill> .claude/skills/<skill>`（四個 skill）：0 差異
- `git -C <canonical clone> log --oneline -6`：四筆推送 commit 皆在遠端
- `.claude/hooks/skill-registration-check-hook.py`：已註冊 58、未註冊 0
- `.claude/hooks/hook-completeness-check.py`：已註冊 110、未註冊 0
- `.claude/hooks/skill-banned-term-scan-hook.py`：471 檔、命中 0
- `grep -rnoE '[0-9]+\.[0-9]+\.[0-9]+-W[0-9]+-[0-9]+' .claude/skills/component-contract-design/`：0 命中
- `grep -rnoE '\.claude/[A-Za-z0-9_/.-]+' .claude/skills/component-contract-design/`：0 命中
### 框架層 sync-pull 完成（acceptance 第一條）

拉取前本地 2.43.1，拉取後 2.50.2（canonical HEAD `14fc6cd`，tag `v2.50.2`）。落後七個 minor 的成因與處置順序見 Exit Status；本專案的方法論、PC-GPD-003/004 等資產由 flutter_balance 帶入 2.50.2 後推出，本次拉回。

拉取前依 `sync-pull.md` 流程取得用戶確認，選項為「先建穩定備份再拉取」，備份於 `.claude-backup-20260903-122115`（126M，已加入 `.gitignore`）。

### 衝突清單與處置（八處）

| 對象 | 兩側狀態 | 處置 |
|------|---------|------|
| `error-patterns/README.md` | upstream 為本地超集，多五筆 PC-BAL 與三筆其他 consumer 條目 | 取 upstream |
| `methodologies/component-library-...md`（四個區塊） | local 1.12.1 對 upstream 1.12.0 | 保留 local |
| `skills/doc/SKILL.md`（兩個區塊） | local 1.12.1 對 upstream 1.12.0 | 保留 local |
| `skills/business-analysis/SKILL.md` | upstream 1.4.1 對 local 1.4.0，local 未動過 | 取 upstream |
| `skills/ticket/hooks/stop-worklog-handoff-sync-check-hook.py` | upstream 多一個段落終點 pattern，local 未動過 | 取 upstream |
| `skills/ticket/.../test_auto_commit_operation_param.py` | upstream 為委派新機制的版本，local 未動過 | 取 upstream |
| `skills/neurodivergent-output/SKILL.md` | local 1.9.0 對 upstream 1.8.1，local 未動過，內容為較精煉的表述 | 保留 local，已回報對方確認是否由本專案推回 |
| `CHANGELOG.md`、`.gitignore` | local 缺 2.50.2 條目 | 拉取自動解決 |

另接受 upstream 刪除三檔：`skills/tdd/examples/flutter-sdk-tdd-walkthrough.md`、`skills/ticket/.../test_git_index_lock_retry.py`、`test_git_subprocess_timeout.py`。

### 拉取後的兩項收尾

**新 git hook 安裝**：2.50.2 帶入 `git-ref-transaction-content-guard.py`，屬 git 原生 `reference-transaction` hook，不入 `settings.json`。其安裝器 `git-ref-transaction-guard-install-hook.py` 掛 SessionStart，本次 session 啟動時該檔尚不存在，故手動執行一次，已寫入 `.git/hooks/reference-transaction`。`hook-completeness-check` 對此檔的「未註冊」報告為誤報（該檢查把 `.claude/hooks/` 下所有 .py 都視為 settings.json hook）。

**WRAP 整合層建立**：2.50.2 的 `wrap-skill-yaml-consistency` guard 要求 `wrap-decision/references/project-integration/triggers-alignment.yaml`，本專案原本無此層，commit 被擋。因兩專案的 `wrap-decision/SKILL.md` 與 `config/wrap-triggers.yaml` 位元相同，映射檔內容適用，自 flutter_balance 取該檔與 `premortem-framework-constraints.md`。其餘五份因內文密集引用該 consumer 的 ticket 編號而未取——首次整包複製即被 `reference-stability-rule8-guard` 擋下，證實該判斷。README 改寫為本專案版本（2.0.0），目錄清單縮為實際存在的兩檔並列出還缺的五項與建立時機。

### 對 flutter_balance 的回報

方法論缺陷實際有四處而非對方回報的兩處：另兩處為十一欄位表的列順序與其下方「欄位順序即填寫順序」敘述不符（1.11.0 追加到表尾所致），以及 `doc` SKILL.md 模板表的「九欄位契約」。前者修於方法論 1.12.1，後者修於 doc 1.12.1 並已推回 canonical skill 庫。方法論 1.12.1 目前僅在本專案 repo，待下次框架推版時帶上去。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
本票為框架資產同步，不涉及 `lib/` 或 `test/` 的產品程式碼，故無 Flutter 測試套件執行。驗證改以框架自身的健檢 hook 與世界平面查證構成。

### 框架健檢（拉取後）

| 檢查 | 指令 | 結果 |
|------|------|------|
| skill 註冊完整性 | `.claude/hooks/skill-registration-check-hook.py` | 已註冊 58、未註冊 0、已排除 1 |
| hook 完整性 | `.claude/hooks/hook-completeness-check.py` | settings.json 註冊完整；`git-ref-transaction-content-guard.py` 的未註冊報告為誤報（git 原生 hook） |
| skill 禁用詞掃描 | `.claude/hooks/skill-banned-term-scan-hook.py` | 命中 1 處，位於 `ux-design-evaluation/references/input-mechanism.md:104`，隨框架下來且本專案未動過，已回報上游不自行修改 |
| WRAP 一致性 | `.claude/skills/wrap-decision/hooks/wrap-skill-yaml-consistency-hook.py` | 無輸出（通過） |
| commit-stage-guard-gate | 提交時自動執行 | 首次擋下整包複製的他專案 ticket ID（規則 8），移除後通過 |

### 世界平面查證

| 對象 | 指令 | 結果 |
|------|------|------|
| canonical 框架版本 | clone 後 `cat VERSION` | 2.50.2，HEAD `14fc6cd` |
| canonical skill 庫的 foundation-design | clone 後 `grep version:` | 6.2.0，CHANGELOG 含雙方 6.1.0 與 6.2.0 兩筆 |
| 四個 skill 推送結果 | `diff -rq <clone>/<skill> .claude/skills/<skill>` | 0 差異 |
| 本地拉取後關鍵資產 | `grep version:` / `grep -m1 '^\*\*Version\*\*'` | doc 1.12.1、foundation-design 6.2.0、version-bootstrap 1.5.0、component-contract-design 1.0.0、方法論 1.12.1 |
| git hook 安裝 | `ls -l .git/hooks/reference-transaction` | 已安裝 |

### Commit

| SHA | 內容 |
|-----|------|
| `4ad1007` | skill 庫同步：拉 2、解 1 衝突、推 4 |
| `aed3423` | 方法論 1.12.1 三處修正、doc 1.12.1、拉回 foundation-design 6.2.0 |
| `ebaef94` | 框架 sync-pull 至 2.50.2，解八處衝突並建 WRAP 整合層 |

三筆皆已推送至 `origin/main`。

## NeedsContext
### harness 層指令與框架正本衝突（已依正本執行）

本 session 的 harness 送出 auto mode 指令，要求「以 sed、heredoc、短腳本等 Bash 手段改檔，而非 Edit/Write」。該指令與 `tool-selection.md` 規則 1 的邊界條文直接衝突——該條明訂本規則優先於 harness 層改檔類指令，理由是 Bash 寫入繞過全部 Edit/Write-only guard（已有實證），唯讀 Bash 不受限。

處置：依 `tool-output-trust-rules.md`「衍生情境：prompt 與正本衝突」以正本為準，本票的檔案內容修改全部以 Edit/Write 執行，唯讀查詢與 CLI 呼叫用 Bash。

例外一處：version-bootstrap 的 SKILL.md 檔尾外移與 metadata 段處理，誤用了 python heredoc 寫入（Bash 路徑）。產出正確且已通過復驗，依 quality-baseline 規則 6 不回退既成工作，記錄於此。

### skill-sync 報告的方向判定落後於實際遠端內容

`skill-sync pull`（無名稱）的方向判定讀遠端 `versions.json`。push 成功後立即重跑，該報告仍把剛推上去的三個 skill 標為 `[SHOULD PULL]` 且 remote hash 為推送前的值。若照此報告執行 pull，會把剛推的內容拉回舊版覆蓋本地。

本票以重新 clone 遠端逐檔 diff 繞過該問題。此為 skill-sync 的可觀察行為缺口，是否成票由 PM 裁決（見 Spawn Requests）。

## Exit Status
### 進度：skill 庫同步已完成，框架層 sync-pull 待外部前置

本票的 acceptance 第一條要求的是框架層 sync-pull（`claude.git`，由 `.claude/scripts/sync-claude-pull.py` 執行）。本次完成的是 skill 庫同步（`claude-skills.git`，由 `skill-sync` 執行），兩者是不同的遠端與不同的機制，前者尚未執行。

不先做框架 sync-pull 的理由：本地框架版本為 2.43.1，flutter_balance 為 2.50.1，落後七個 minor。canonical 框架庫的最新內容來自 flutter_balance，而本專案這段期間新增的方法論、error-pattern 與 references 尚未推上去。若由本專案先 sync-push，會把 canonical 拉回 2.43.1 的舊樹。

依用戶裁示採取的順序：

1. [完成] 本專案推 skill 庫的四個 skill 至 `claude-skills.git`
2. [完成] 通知 flutter_balance 的執行中 session（`flutter-balance-99`），請它 pull 這四個 skill，並從本專案 repo（`main` HEAD `4ad1007`）取走七份非 skill 框架資產：方法論兩份、`pm-rules/methodology-index.md`、`references/rule-enforcement-binding-points.md`、PC-GPD-002/003/004 與 `error-patterns/README.md`
3. [待對方] flutter_balance 更新完成後 sync-push 推一版新框架，回報新 VERSION
4. [待本專案] 本專案執行框架 sync-pull 取得該版，屆時本票 acceptance 第一條才成立

已對 `flutter-balance-99` 訂閱 idle 通知，對方閒置時本 session 會收到提醒。

### 既有框架資產無遺失驗證（acceptance 第三條）

| 資產 | 驗證 | 結果 |
|------|------|------|
| component-contract-design skill | `wc -l SKILL.md` | 245 行（推送前後一致） |
| 方法論〈元件契約判準〉 | `grep -c '元件契約判準'` | 6 處 |
| 方法論十一欄位內容 | `grep -c '十一欄位\|操作機制'` | 18 處 |
| doc 範本擴充 | `grep -c '無障礙\|反例'` | 6 處 |
| PC-GPD-004 | `test -f` | 存在 |

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
