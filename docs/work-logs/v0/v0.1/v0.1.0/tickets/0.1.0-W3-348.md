---
id: 0.1.0-W3-348
title: 重切 component-contract-design step-2 與 step-3 依讀者任務拆檔
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-343
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-343 Spawn 規劃
  final_decision: 每張單一 skill 建重切實作票
  rationale: 跨檔指標更新限同一 skill 內，避免並行衝突
who:
  current: thyme-documentation-integrator
  history: {}
what: 重切 component-contract-design step-2 與 step-3 依讀者任務拆檔
when: W3-343 完成後
where:
  layer: 待定義
  files:
  - .claude/skills/component-contract-design/references/step-2-extract-components.md
  - .claude/skills/component-contract-design/references/step-2-path-a-spec-derivation.md
  - .claude/skills/component-contract-design/references/step-2-path-b-canvas-decomposition.md
  - .claude/skills/component-contract-design/references/step-2-path-c-code-extraction.md
  - .claude/skills/component-contract-design/references/step-3-fill-contract.md
  - .claude/skills/component-contract-design/references/step-3-fill-contract-conditional-cases.md
  - .claude/skills/component-contract-design/references/addresses.md
  - .claude/skills/component-contract-design/SKILL.md
  - .claude/skills/component-contract-design/CHANGELOG.md
why: 用戶裁示（2026-09-14）：補範例後超標應依讀者任務重切章節與單篇，不刪範例、不壓縮規則；W3-343 已逐檔以讀者情境三問產出重切方案
how:
  task_type: Documentation
  strategy: 依 W3-343 Solution 第 1、2 案搬移，遵守 splitting-an-existing-skill 的節界保留與 H1 規則；更新 SKILL.md〈全程〉與跨檔指標；搬移後以同一量測指令複驗
acceptance:
- '[x] 依 W3-343 第 1、2 案完成拆檔，範例與其所服務的規則同檔'
- '[x] 全部新檔與入口檔以量測指令複驗通過門檻並記錄數值'
- '[x] 搬移前後節界以排除 fenced block 的計數比對一致'
- '[x] SKILL.md 與各檔指向新檔的引用全數更新，grep 無失效錨點'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T14:43:02'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

重切 component-contract-design step-2 與 step-3 依讀者任務拆檔

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-14 14:42] readiness=1 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
依 0.1.0-W3-343 Solution 第 1、2 案完成拆檔，不刪範例、不壓縮規則文字。

**案 1（step-2-extract-components.md，超標 45%）**：三條路徑各自獨立成檔——
`step-2-path-a-spec-derivation.md`（路徑 A 規格推導）、
`step-2-path-b-canvas-decomposition.md`（路徑 B 畫布拆解）、
`step-2-path-c-code-extraction.md`（路徑 C 程式碼萃取）。入口檔（原檔名保留）
留下三路徑共用內容：〈這一步在解什麼〉（補「在哪份檔」欄作路由）、
〈中間產出物的形態與落點〉、〈共用規則一：存在必要性檢視〉、〈共用規則二：同型歸併〉。

**案 2（step-3-fill-contract.md，超標 14%，W3-342 範圍）**：〈路徑 C 專屬：證據順序與
漂移判定〉〈多形態專案〉兩節外移至新檔 `step-3-fill-contract-conditional-cases.md`
（前者僅路徑 C 讀者需要，後者僅多形態專案需要，兩者互不相依）。入口檔（原檔名保留）
保留全體讀者共用的〈元件契約欄位填寫問句〉〈回饋契約的填寫順序〉〈容器元件另答排列
不變式三項〉〈範本中元件契約欄位表以外的章節〉。W3-342 範圍併入本票結論處置。

**H1 規則套用**：路徑 A/B/C 三檔皆屬「單一 `##` 連同全部 `###` 子節整體外移」
（三路徑本身無 `###` 子節），H1 直接沿用原 `##` 字串，原 `##` 刪除；
`step-3-fill-contract-conditional-cases.md` 屬「並列的多個 `##` 併入同一新檔」，
H1 取複合名，原兩個 `##`（路徑 C 專屬、多形態專案）全部保留。

**節界驗證**（`awk '/^```/{f=!f;next} !f && /^## /{n++} END{print n+0}'`，排除 fenced block）：
- step-2 組：入口 H2=4（含 H3=1，策略不一致小節）、路徑 A/B/C 各 H2=0（內容全升 H1）
- step-3 組：入口 H2=7（含 H3=6，回饋契約五關卡＋漏答樣態）、conditional-cases H2=2
- 對照原檔：step-2-extract-components.md 拆前 7 個 H2（3 個變 H1），
  step-3-fill-contract.md 拆前 9 個 H2（皆保留，2 個移至新檔）——計數一致

**跨檔指涉更新**：
- `step-2-extract-components.md`〈共用規則一／二〉在路徑 A/B/C 內原用「本檔」「本節」
  指涉，改具名 `` `step-2-extract-components.md`〈共用規則N〉``
- 路徑 C「走完之後」原指「路徑 C 專屬的證據順序與漂移判定」（無檔名），
  改具名 `` `step-3-fill-contract-conditional-cases.md`〈路徑 C 專屬：證據順序與漂移判定〉``
- `addresses.md` 三列（統一匯出入口、事故對照、特徵測試）原指
  `step-2-extract-components.md` 路徑 C 步驟，改指 `step-2-path-c-code-extraction.md`
- 六份新／入口檔皆補「同目錄」段列出直接相關的兄弟檔（step-2 組四檔互列、
  step-3 組兩檔互列、跨組經路徑 C／conditional-cases 單向連結）

**範例保留驗證**：`grep -c '^\*\*範例'` 原檔 step-2=6、step-3=2，合計 8；
新六檔合計仍為 8（step-2 組 1+2+1+2=6，step-3 組 1+1=2），無範例遺失或新增。

SKILL.md metadata.version 由 3.4.0 升至 3.5.0，CHANGELOG.md 新增對應版本紀錄
（未於框架檔案內嵌本專案 ticket ID，依規則 8）。

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-343 | chars: 537 -->

### Task Reference
- 0.1.0-W3-343 what: 評估 補範例後的 skill 章節重切：依讀者任務重新拆分章節與單篇內容（閘門組修補範圍）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-343 why: 用戶裁示（2026-09-14）：補範例後超出體量門檻是正常現象——原寫法為符合格式而過度精簡，未顧及讀者理解。門檻的目的是避免認知超載，因此補齊範例後應重新評估內容是否過度複雜，依章節內容需求重切章節與單篇，而非壓縮範例回到門檻內（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/component-contract-design/SKILL.md::read  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
- .claude/skills/ux-design-evaluation/SKILL.md::read  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
- .claude/skills/test-assertion-design/SKILL.md::read  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
- .claude/skills/tdd/SKILL.md::read  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
- .claude/skills/doc/SKILL.md::read  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] 閘門組修補過的 skill 檔全數以量測指令實測並記錄指令與數值  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
- [ref] [x] 超標與接近門檻者逐節標出服務的讀者任務與步驟  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
- [ref] [x] 重切方案以讀者任務為切分依據，並寫明不以刪減範例達標  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
- [ref] [x] W3-322 與 W3-342 的範圍併入本票結論並記錄處置  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
- [ref] [x] 依方案列出重切實作票規劃（每張單一 skill）  # from 0.1.0-W3-343（來源已完成，結論見 Solution）
