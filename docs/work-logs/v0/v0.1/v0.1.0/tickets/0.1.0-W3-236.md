---
id: 0.1.0-W3-236
title: 實機量測 openedExternallyMessage 的 SnackBar 在外部應用取得前景後是否仍可被使用者觀察到，並據以裁定 167 SR-3
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-204
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: PM 驗收 0.1.0-W3-204 時發現 §2.14 列 5 照抄了 167 SR-3 的未決狀態
  final_decision: 不由論證裁決，改為實機量測其事實前提；量測結果決定走 PM 裁定或提請用戶簽核
  rationale: 判準判否的整段理由是可觀測的事實主張而雙方都未量過；且裁決權限不對稱（保留屬 PM、移除須用戶簽核），憑論證裁只可能產出保留，等於以成本凍結一個未經證實的例外
who:
  current: 主線程
  history: {}
what: 實機量測 openedExternallyMessage 的 SnackBar 在外部應用取得前景後是否仍可被使用者觀察到，並據以裁定 167 SR-3
when: 0.1.0-W3-232 派發後的 PM 前台空檔（本票不阻塞任何票，亦不被任何票阻塞）
where:
  layer: Presentation
  files:
  - docs/spec/ui/SPEC-003-interaction-response.md
why: '`SPEC-003` §2.13 對照表第 2 列標「判準判否、條文判是，以條文為準執行，待 PM 核定」，來源為 `0.1.0-W3-167` SR-3。該列自 2026-09-09 起掛在 PM 名下未決，`0.1.0-W3-204` 的 §2.14 列 5 誠實照抄了這個未決狀態。


  **為何不直接裁**：判準判否的整段理由是一個**可觀測的事實主張**——「`/usr/bin/open` 回傳 `exitCode == 0` 即代表外部應用程式已被帶到前景，我方視窗因此不再是前景，SnackBar 在背景視窗上停留 `Motion.snackBar` 後自行消失，使用者切回來時已經沒有」。雙方目前都在預測，沒有人量過。實機驗證屬 PM 職責（CLAUDE.md §PM 可寫路徑與 pm-role 明訂）。


  **裁決權限是不對稱的，這決定了本票的產出形態**：`SPEC-003` §2.13 明訂「PM 裁定保留者，須在本表該列補具名理由」——**保留在 PM 權限內**；而 SR-3 自身的 `why` 明訂「變更 §2.2 外部開啟契約的明文條文須經規格程序與用戶簽核」——**移除不在 PM 權限內**。因此本票的產出只有兩種合法形態：(a) PM 裁定保留並補具名理由，或 (b) 帶量測數據提請用戶簽核移除。**不得由本票直接改條文。**


  **不裁的成本**：§2.13 自己已寫明——「判準的例外必須有具名理由，否則下一個事件會照抄這個例外而無人察覺它原本是例外」。目前該列的實質理由是「改它很貴」，那不是具名理由。


  **本票不阻塞任何票**：`0.1.0-W3-205` 的範疇是截斷條件綁級別而非綁呼叫路徑，列 5 是否顯示 SnackBar 與該修法正交（`205` 的 acceptance 四條逐條核對後確認）；`232`／`207`／`233` 亦不依賴本裁決。'
how:
  task_type: Analysis
  strategy: 'PM 前台執行，不派代理人（實機驗證屬 PM 職責，且需觀察 runtime 行為）。


    **本票只產出證據，不做裁決。** 裁決歸 `0.1.0-W3-210`（該票已標 `blockedBy` 本票）。此為 2026-09-10 的範疇收窄，理由見 Problem Analysis〈PM 更正：本票與 `0.1.0-W3-210` 的分工〉——本票建立時未查重，而 `210` 早已存在且處理同一主題。


    **量測前先寫死判定準則，寫入本票〈實驗方法〉後才開始量**（避免看到數據再定判準），三項準則已於 2026-09-09 寫定並推上 `origin/main`，不得事後修改。


    **量測方法**：`/run` 啟動 app（本專案驗證配方尚未建立，`.claude/skills/verify/SKILL.md` 不存在，故不走 `/verify`），對 `SPEC-003` §3.1／§3.2／§3.5／§3.6 四個開啟入口各觸發一次 `opened` 路徑，觀察 SnackBar 是否在我方視窗可見。至少三次取樣，記錄每次的觀測值與環境（目標應用是否已開啟、是否同一 Space）。


    **產出三選一，三者皆為證據不是裁決**：

    - 命中「支持保留」的觀測條件 → 記錄命中的是 (a) 或 (b) 分支與其觀測值。

    - 命中「支持移除」的觀測條件 → 記錄四個入口 × 三次取樣皆無例外的完整數據。

    - 三次取樣結果不一致 → 記錄為「不可重現」並附各次差異與環境。


    **退場條件（強制）**：三次量測仍無法得到穩定結果即停手走第三種產出，不再追加量測。


    **本票不得做的事**（皆已移交 `210`）：裁定保留或移除、改動 `SPEC-003` §2.13 對照表第 2 列或任何條文、提請用戶簽核。**「移除不在 PM 權限內、須經規格程序與用戶簽核」這條邊界隨裁決權一併移交 `210`**（來源：`0.1.0-W3-167` SR-3 的 `why`）。


    **本票不量測的事**：不量測 SnackBar 的絕對顯示毫秒數作為 pass-fail 依據；判定依據是「可否被觀察到」這個二元事實，不是時長數值。'
acceptance:
- '[ ] 判定準則已於量測前寫入〈實驗方法〉，且量測後未修改'
- '[ ] 四個開啟入口各至少三次取樣，每次的觀測值與環境已記錄'
- '[ ] 產出為三種合法形態之一，且未直接改動 SPEC-003 §2.2 條文'
- '[ ] 裁定保留者，§2.13 對照表第 2 列已補具名理由且該理由為量測結果'
- '[ ] 產出限於量測結果與判定準則的命中判定，未代 0.1.0-W3-210 做出條文裁決'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-09T23:48:53'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實機量測 openedExternallyMessage 的 SnackBar 在外部應用取得前景後是否仍可被使用者觀察到，並據以裁定 167 SR-3

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### PM 更正：本票與 `0.1.0-W3-210` 的分工（2026-09-10）

**我建立本票時未查重，而 `0.1.0-W3-210`（裁決 `openedExternallyMessage` 判準判否而條文判是的四處衝突，`star-anise-system-designer`）已先存在且處理同一主題。** 成因與本 session 稍早的誤植同源——建票前未搜尋既有票。

**兩票不合併，改為明確分工**——它們是證據與裁決兩件事：

| 票 | 職責 |
|----|------|
| **本票（`236`）** | **只產出證據**：實機量測 `open` 回傳後我方視窗是否一律立即失去前景、SnackBar 是否全程不可被觀察。判定準則已於量測前寫死（見〈實驗方法〉），不得事後修改 |
| `0.1.0-W3-210` | **裁決**四處條文衝突（§2.2 外部開啟契約、§3.5 互動反應表、§4.26 slot 契約與 i18n、§2.12 短暫非同步列），已補 `blockedBy` 本票 |

**因此本票的產出形態收窄**：原 how 列的三種產出中，「PM 裁定保留並補具名理由」與「提請用戶簽核移除」兩項**移交 `210`**；本票只輸出量測結果與其對三項判定準則的命中判定。「不可重現」的情形同樣只回報，不由本票裁定後續。

**本票的退場條件不變**：三次量測仍無法得到穩定結果即停手，回報「不可重現」，不追加量測。

### 更正：「本票不阻塞任何票」已失效，本票現為兩張票的前置

建票當下的判定（`205`／`232`／`207`／`233` 皆不依賴本裁決）對那四張仍成立，但 `why` 段那句「**本票不阻塞任何票**」現在是錯的，不可再被讀者採信：

| 票 | 現況 | 依賴什麼 |
|----|------|---------|
| `0.1.0-W3-248` | pending，`blockedBy: [0.1.0-W3-236]` | 等本票的裁決結果 |
| `0.1.0-W3-210` | pending，`blockedBy: [0.1.0-W3-236]` | 等本票的裁決結果（由 210 的擁有者於派發前掃描 `where.files` 時發現撞題，主動讓出裁決權） |

**這使本票的位置改變，處置也跟著改變**：原本它是「PM 前台空檔可做」的自選項，現在它是兩張票的唯一解鎖點，且那兩張都在 pending 佇列裡等著。實機量測不再是有空再做，是排程項。

**兩張票的分工已釐清，本票的產出形態不因此擴大**：`210` 收窄為「等本票裁決後，把結果落為四處條文的同步，並列出 `0.1.0-W1-068` 需調整的測試斷言交 PM」，不裁決、不決定移除與否。本票仍只產出 (a) 保留並補具名理由，或 (b) 帶量測數據提請用戶簽核移除——**不得由本票或 210 直接改 §2.2 明文條文**。`210` 原 acceptance 第 2 條「裁定為 (a) 時四處條文皆已同步」照原樣執行會越權，其擁有者已認並將於本票完成後重寫該票條目。

### 同一個 SR 在兩個 session 各自長出一張票

本票與 `0.1.0-W3-210` 同源於 `0.1.0-W3-167` SR-3：本票直接引 SR-3，`210` 經 `0.1.0-W3-169` SR-10 轉一手。兩張都合法、都正確引用來源，但建立當下**誰也不知道對方存在**——`spawned_tickets` 只記錄「我生了誰」，不回答「這個來源還被誰生過」。撞題是在派發前掃 `where.files` 時才發現，比建票晚了一天。

**這是關係未查證的又一實例，形態見 `PC-GPD-019`**：「SR-3 需要有人處理」是真的（兩邊觀測皆正確），「還沒有人在處理」這個關係兩邊都沒查。與該檔第四例（兩個 session 為同一教訓各自建 error-pattern）同形。

`210` 的擁有者已把約定往前挪：建規格類的票之前，先掃一次同檔案的 pending／in_progress 票，不只在派發前掃。本票同步採用。

### 票面自相矛盾的更正（PM，2026-09-10）

**本票曾在同一份票面上並存兩套相反的分工敘述，已於本次修掉。** 由 `graph-project-docs-manager-86` 讀票時發現並直接來問，非經轉述。

| 位置 | 原文主張 |
|------|---------|
| `why`（建票時寫） | 「本票的產出只有兩種合法形態：(a) PM 裁定保留並補具名理由，或 (b) 帶量測數據提請用戶簽核移除」——**本票做裁定** |
| Problem Analysis〈PM 更正：本票與 `0.1.0-W3-210` 的分工〉（今日追加） | 「本票只產出證據……裁決歸 `210`」——**本票不做裁定** |

**成因**：今日發現 `210` 早於本票存在且處理同一主題後，我在 Problem Analysis 追加收窄說明，**但沒有回頭改 `why` 與 `how`**。兩段各自讀都成立，合起來相反。

**這正是 `0.1.0-W3-258` 要處理的形態在本票上的實例**：一份產出（`why`）在寫下當刻正確，之後被我自己的另一個動作（收窄）弄失效，而**失效不發出任何訊號**——`why` 還在、語法正確、讀起來完整。差別在這次的發現者是下游讀者，不是任何檢查。

**已處置**：`how` 全文改寫為只產出證據；`why` 的該段以本節更正（`why` 為建票時的決策記錄，保留原文並在此註明其已被收窄，不回頭改寫歷史）。**以本節與現行 `how` 為準。**

**確認給 `210` 的答案**：`236` 完成時**不會**給出「保留」或「移除」的結論，只給量測數據與三項判定準則的命中判定。`210` 需要保有裁決權，且「移除不在 PM 權限內、須經規格程序與用戶簽核」這條邊界應落在 `210` 的 acceptance。
## 重現實驗結果
### 實驗方法

**本節於量測前寫定，量測後不得修改**（本票 acceptance 第一條）。撰寫時點：尚未啟動 app、尚未觀察任何一次 `opened` 路徑。

**待證的事實主張**（`SPEC-003` §2.13 判準判否的整段理由）：

> `/usr/bin/open` 回傳 `exitCode == 0` 即代表外部應用程式已被帶到前景，我方視窗因此不再是前景，SnackBar 在背景視窗上停留 `Motion.snackBar` 後自行消失，使用者切回來時已經沒有。

`Motion.snackBar` 實查為 `Duration(seconds: 4)`（`lib/tokens/motion.dart:47`）。

**三項判定準則**（先寫死，不看數據再定）：

| 判定 | 觀測條件 |
|------|---------|
| **支持保留**（現行條文為準，PM 可裁） | 出現下列任一：(a) SnackBar 在我方視窗仍為前景時可見達可辨識的時間；(b) 外部應用未必搶前景（目標已開啟、位於其他 Space、開啟失敗回退），使該則提示成為唯一回饋 |
| **支持移除**（須用戶簽核，PM 不可逕裁） | `open` 回傳後我方視窗**一律立即**失去前景，且 SnackBar 全程不可被觀察——四個入口、每個入口三次取樣皆如此，無任何一次例外 |
| **不可重現** | 三次取樣結果不一致（同一入口有時可見、有時不可見） |

**量測程序**：

1. 以 `/run` 啟動 app（本專案驗證配方未建立，`.claude/skills/verify/SKILL.md` 不存在，故不走 `/verify`）。
2. 對 `SPEC-003` §3.1（Domain 開啟 docs 目錄）、§3.2（UC Flow 開啟原始檔）、§3.5（破洞項）、§3.6（節點詳情）四個入口各觸發 `opened` 路徑至少三次。
3. 每次記錄：SnackBar 是否在我方視窗可見、可見約多久、目標應用當時是否已開啟、是否位於同一 Space。
4. **退場條件（強制）**：三次取樣仍無法得到穩定結果即停手，走「不可重現」分支，不再追加量測。

**本票不量測的事**：不量測 SnackBar 的絕對顯示毫秒數作為 pass-fail 依據（`test-assertion-design-rules` 規則 D1 的精神——環境相依的計時值不作判定依據）；判定依據是「可否被觀察到」這個二元事實，不是時長數值。

### 實驗執行

（待執行：啟動 app 後填入四個入口各三次的觀測值與環境）

### 實驗發現

（待實驗執行完成後填寫）

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-204 | chars: 567 -->

### Task Reference
- 0.1.0-W3-204 what: 補寫 SPEC-003 的注意力級別與載體指派表（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-204 why: `0.1.0-W3-184` 定義了級別與規則，但**沒有指定本專案的每一種提示走哪個級別、哪個載體**。規格層不指定，實作只能各自判斷，而各自判斷的結果不會一致——這是 `0.1.0-W3-168` 為元件契約解決過的同型問題。

**已知須被規格涵蓋的一項現況違規**：`AppSnackBar.show` 無條件 `hideCurrentSnackBar()`，使 L3 完成通知可截斷正在顯示的 L2 失敗訊息（違反截斷規則第三條）。修法屬實作，但**「哪個級別可截斷哪個級別」必須先是規格**，否則實作票沒有可依循的判準。

**不寫則後果**：`0.1.0-W3-184` 的規則停在方法論層，本專案無任何可驗收的落點——規則存在但沒有任何測試會因違反它而變紅。（來源已完成，結論見 Solution）

### Related Files
- docs/spec/ui/SPEC-003-interaction-response.md::write  # from 0.1.0-W3-204（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 本專案現有每一則提示訊息已逐一指定級別與載體，無遺漏且無同一則落兩個級別  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
- [ref] [x] 級別定義與截斷規則未重述，以標題文字引用方法論  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
- [ref] [x] 每則提示的標定點已寫明（級別在邊界或發起點標定一次，之後傳播單調不升）  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
- [ref] [x] SPEC-003 既有 §2.x 編號與既有時間 token／狀態／錨點類別集合皆未變動  # from 0.1.0-W3-204（來源已完成，結論見 Solution）
