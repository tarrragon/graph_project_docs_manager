---
id: 0.1.0-W3-284
title: 修復 ticket skill 四個 handoff hook 的日誌 root 錯配，並收斂為單一 logger 工廠
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-273
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-273 Phase 4 補做審查（linux）實測四處同型錯配，與該票範圍判準的結論相反
  final_decision: 獨立成票，不回頭改 W3-273——該票修法正確且全套件已綠，屬範圍不足非修法錯誤
  rationale: 根源在 .claude/ 通用資產（hook_logging 的 project_root 契約）且敘述可攜，具框架問題形態；但四處接線與工廠收斂是本專案 ticket skill 的具體修復，先在本專案落地，框架層契約問題另行判定是否升級
who:
  current: thyme-python-developer
  history: {}
what: 修復 ticket skill 四個 handoff hook 的日誌 root 錯配，並收斂為單一 logger 工廠
when: 0.1.0-W3-273 的 Phase 4 補做審查發現（2026-09-11）
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/lib/paths.py
  - .claude/skills/ticket/hooks/handoff-auto-resume-stop-hook.py
  - .claude/skills/ticket/hooks/handoff-prompt-reminder-hook.py
  - .claude/skills/ticket/hooks/handoff-reminder-hook.py
  - .claude/skills/ticket/hooks/stop-worklog-handoff-sync-check-hook.py
  - .claude/skills/ticket/ticket_system/commands/track_dashboard.py
  - .claude/skills/ticket/ticket_system/commands/track_dispatch_check.py
why: '0.1.0-W3-273 修掉兩個 CLI 呼叫點的日誌 root 錯配（狀態讀寫走 get_ticket_state_root、日誌落回 hook_base.get_project_root，linked worktree 內解析方向相反）。該票的範圍盤點判準是「檔案內是否直接 import get_ticket_state_root」，該判準只抓直接 import，漏掉經 lib 模組傳遞的解析。


  Phase 4 補做審查實測四個 hook 今天處於同一錯配：handoff-auto-resume-stop-hook / handoff-prompt-reminder-hook / handoff-reminder-hook 三者經 handoff_utils（paths.py 的 get_ticket_state_root）讀寫狀態，stop-worklog-handoff-sync-check-hook 經 worklog_appender，四者的 setup_hook_logging 呼叫點皆未傳 project_root。


  後果與 W3-273 修掉的同一類無聲：動作寫主倉庫、日誌落 worktree 自己的 hook-logs，在主倉庫查不到。偵測難度更高——那四處沒有任何測試斷言日誌落點，無斷言、無紅燈、無 stderr 差異，可無限期潛伏。W3-273 之所以被發現，純粹因為剛好有三個測試斷言了落點。


  觸發條件是 hook 在 linked worktree 內被觸發，而本專案 worktree 派發是常態。


  根本問題是正確性押在呼叫者記性上，且該路徑已失敗過一次：hook_logging.py 的 project_root 參數 docstring 早已逐字記載此失效模式，參數為此而加、文件寫得清楚，兩個呼叫點照樣漏接。故本票除接線四處外，另要求把配對固定在函式邊界（單一 logger 工廠），使未來新模組不需記得傳參數。


  否決的兩個方案（Phase 4 審查已評估，不重新評估）：合併兩個 root 函式會犧牲其中一個已驗證的修復；讓 project_root 改必填需編輯框架全域 166 個非測試呼叫點且破壞所有 consumer 的 hook。'
how:
  task_type: Implementation
  strategy: 先建 logger 工廠再接線，不逐處補 kwarg。工廠置於 ticket_system/lib/，內部固定 setup_hook_logging(name, project_root=get_ticket_state_root())；兩個 CLI 呼叫點與四個 hook 改走工廠。盤點手段須可追傳遞路徑（例如以 get_ticket_state_root 為起點反查 lib 消費者再反查 hook），並在票面記錄手段本身，不以直接 grep 為依據。
acceptance:
- '[ ] 四個 hook 的日誌 root 與其經 lib 取得的狀態 root 對齊，以 linked worktree 實測日誌落點而非僅斷言函式呼叫'
- '[ ] ticket skill 內新增單一 logger 工廠（狀態 root 與日誌 root 的配對在函式邊界固定），兩個 CLI 呼叫點與四個 hook 皆改走該工廠'
- '[ ] 盤點判準改為可追傳遞路徑者：不以檔案內是否直接 import get_ticket_state_root 為判準，須涵蓋經 lib 模組取得狀態 root 的情形，並記錄盤點手段'
- '[ ] track_dashboard.py 的 local import 與 track_dispatch_check.py 的模組層 import 風格統一，使 monkeypatch 在兩處行為一致'
- '[ ] 三項日誌落點斷言改為計算式（log_dir 由狀態 root 推導），不再依賴 conftest 讓 CLAUDE_PROJECT_DIR 與 tmp_path 相異這一巧合'
- '[ ] 真正的 worktree 分歧路徑取得覆蓋，或明確記錄不覆蓋的理由：現行測試經 TICKET_SYSTEM_TEST_ISOLATION 逃生艙繞過 _linked_worktree_root()，驗的是「兩個 root 對齊」而非「worktree 下對齊」，原始 bug 的那條路徑零覆蓋，改計算式斷言不會補上這一項'
- '[ ] 收斂後的註解須寫進 hook_logging.py 的 project_root docstring 早已描述此失效模式這項事實（使下一位讀者去改結構而非再抄一份說明），且不以日期標記變更來源，改標票號（本專案票號慣例；日期在 append-only 註解內會累積且不可追溯）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修復 ticket skill 四個 handoff hook 的日誌 root 錯配，並收斂為單一 logger 工廠

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-273 | chars: 1424 -->

### Task Reference
- 0.1.0-W3-273 what: 修復 三項既存紅燈：hook-logs 檔案未產生使 dashboard GC 與 dispatch-check 的日誌斷言失敗（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-273 why: `ticket` skill 全套件現況 `1721 passed / 1 skipped / 3 failed`，違反品質基線規則 1（測試通過率 100%）。

**三項**：`test_dashboard_auto_gc_logging.py` 兩項、`test_track_dispatch_check.py::TestPruneFlag::test_prune_writes_hook_log_via_real_logger`。共同徵狀為 `.claude/hook-logs/` 下的預期檔案未產生。

**已確認為既存而非新引入**：`0.1.0-W3-271` 與 `0.1.0-W3-272` 兩個實作代理人在各自隔離的 worktree 中獨立撞到同三項，且各自以還原變更後重跑（一方用修改前的檔案版本、另一方用 `git stash`）確認 baseline 同樣紅。兩次獨立驗證，非單一觀測。

**為何獨立成票**：三項與那兩張票的改動範圍無交集（`git_ops.py`／`topic_inference.py`／`create.py`），把它們併進任一張會使該票的驗收混入無關的紅燈，且兩張票的代理人都正確地沒有擅自修範圍外的檔案。

**成因未判定，不可假設三項同源**：「檔案未產生」有三種處置相反的成因——功能真的沒寫入、測試斷言的路徑與實際落點不同、或測試假設了一個非必然的副作用。三項徵狀相同不表示成因相同；本票要求逐項確認實際落點再判斷。

**附帶一項環境落差**：`uv run --project .claude/hooks pytest` 在該 venv 缺 `filelock`，collection 即 ImportError（兩個代理人皆撞到，其中一個因此 64 個 collection error）。`filelock>=3.12` 只在 ticket skill 自身的 pyproject 宣告。派發 prompt 若沿用前者會使代理人先撞一次環境錯誤，須在 SKILL.md 明寫測試指令應用哪個 project。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/ticket/ticket_system/tests/test_dashboard_auto_gc_logging.py  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- .claude/skills/ticket/ticket_system/tests/test_track_dispatch_check.py  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- .claude/skills/ticket/references/architecture.md  # from 0.1.0-W3-273（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三項紅燈的成因逐項判定：測試假設錯誤、日誌路徑漂移、或功能實際失效，三者處置不同不可混為一談  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- [ref] [x] 若為功能失效則修功能；若為測試假設錯誤則修測試並在測試內註明日誌落點的來源  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- [ref] [x] ticket skill 全套件回到 0 failed（現況 1721 passed / 1 skipped / 3 failed）  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- [ref] [x] 查證 uv run --project .claude/hooks 缺 filelock 是否為預期（ticket skill 自身 pyproject 才宣告），並在 SKILL.md 明寫測試指令應用哪個 project  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
