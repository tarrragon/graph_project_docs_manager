---
id: 0.1.0-W3-335.53
title: 裁決 V4 第三輪仍猜 2 項、約 12 項矛盾與 3 處同步缺漏（WRAP）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy: []
relatedTo:
- 0.1.0-W3-335.52
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-335.52 第三輪未通過
  final_decision: WRAP 裁決後追修、同步稽核、第四輪
  rationale: 用戶裁示
who:
  current: general-purpose
  history: {}
what: 裁決 V4 第三輪仍猜 2 項、約 12 項矛盾與 3 處同步缺漏（WRAP）
when: W3-335.52 判定完成後
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.53.md
why: W3-335.52 判定第三輪未通過（仍猜 V4-01 G3 未載入元件類別、V4-02 G4 無可消費的型別表版本值）；用戶裁示加同步稽核再跑第四輪，簽核改由 WRAP（風格偏好除外）
how:
  task_type: Analysis
  strategy: 讀 .52 Solution〈本輪矛盾與同步缺漏〉與 V4R3 探針表 → 逐項核對 spec 現文 → WRAP 完整模式裁決（與 .37／.47 衝突時說明）→ 依檔分組追修清單與同步點
acceptance:
- '[ ] 仍猜 2 項與全部列出矛盾、同步缺漏逐項：原文位置、WRAP 候選（含反向框架）、裁決、改動位置與改法'
- '[ ] 每項列出須同步位置（grep 結果）'
- '[ ] 牽涉用戶寫程式碼風格偏好者標「風格偏好，交用戶」；依檔分組追修清單，本票不改 spec、不建票'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T22:07:46'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

裁決 V4 第三輪仍猜 2 項、約 12 項矛盾與 3 處同步缺漏（WRAP）

---

## Problem Analysis
### 問題根因

V4 第三輪（`0.1.0-W3-335.52`）未通過：原猜測點仍猜 2 項（V4-01 G3、V4-02 G4），另列 spec 間矛盾與同步缺漏。共同根因兩類：(1) 答案只在 SPEC-004（探針輸入範圍外），SPEC-001／SPEC-003 未回寫（E1、E9、E10、E15）；(2) 新增共用狀態、SnackBar 或通則後，逐畫面生命週期、動畫表與彙總表未同步（E5、E11、E13、E14）。其餘為同表進入條件重疊或條文內在衝突（E4、E6、E7、E12）與顯示值未具名（E2、E3、E8）。

### 影響範圍

- 已驗證：docs/spec/ui/SPEC-001、SPEC-003、SPEC-004；docs/usecases/UC-02、UC-05（同步點核對）；lib/components/blocked_state.dart（E9 實作錨點已存在）
- 未驗證：arb 訊息 key 與 slot 是否重複顯示（E2，列為追修核對項）

### 相關 Error Pattern

PC-GPD-025（前輪猜中不視為已解決，以本輪為準）；同步缺漏同型觀察交 `0.1.0-W3-335.54` 窮舉。

## 重現實驗結果
### 實驗方法

本票為規格裁決，「重現」= 逐項以原文核對探針卡點是否仍成立。探針：scratchpad `probes/a/V4R3-01.md`～`V4R3-04.md` 各項「初判」表列。原文核對以 Read（SPEC-001 全檔、SPEC-003 §2.3–§2.13 與 §3–§5、SPEC-004 §3.6／4.6／4.23／4.25）與 grep。可重跑 grep 腳本：scratchpad `w3-335-53/g.sh`（固定字串比對 docs/spec/ui 與 docs/usecases，排除變更歷史列）。

### 實驗執行

- 逐項比對探針引文與現文：15 個裁決單位全數原文仍在（E4 合併兩探針的 C3 議題）
- E12 子問題「三態根錨點名未知」：SPEC-003 §4 #12–#14 已有 `state-traceability-normal／broken／empty`，屬漏讀，不裁決
- E9：`grep -rn switch-project lib test` 命中 `lib/components/blocked_state.dart` 與 `test/unit/components/blocked_state_test.dart` 的 `action-domain-switch-project`
- E14：`0.1.0-W3-210`（`openedExternallyMessage` 裁決）status pending，確認為 §2.13 #2 的承接票

### 實驗發現

- 已驗證：15 單位皆為現存矛盾或缺描述；E1、E9、E10、E15 答案已在 SPEC-004 或實作，SPEC-001／003 未回寫
- 計數差異：`.52` 摘要稱「約 12 項矛盾」（3／6／1／2），其清單實列 14 項（3／6／2／3）；「3 處同步缺漏」即清單內 G-37、V4R3-03 G-29、V4R3-04 G-23，與矛盾重疊不另計
- 未驗證：E2 arb `schemaUnconsumableMessage` 已代入 `{version}` 與 `BlockedState.version` slot 是否雙重顯示

## Solution
### 評估框架

WRAP 完整模式，與 `.47` 同構：錨點 → W（類別欄：改造既有／零新增／新增；含反向框架）→ R（事證限原文可查，檔名＋章節標題）→ A（規格改動牽動處數與對 UC 目標的損益，不以估時比較）→ P（最可能失敗與可觀測訊號）；並列被放棄選項優點與選定方案三個缺點。

| 階段 | 本票的落地 |
|------|-----------|
| 錨點 | UC-02／UC-05 使用者目標；SPEC-001 FR-01／FR-04／FR-07／FR-08；SPEC-003 §2.3 規則 1、§2.5 C1–C8、§2.11 三處同一；`0.1.0-W1-035` 降級定案（用戶層）；前輪 `.37` R1–R12、`.38` S 系列、`.47` D1–D16（不重複、不無理由推翻） |
| Step 0 | 資料來源為 spec／UC／實作原文，非個人化決策，判定充足；未驗證假設見〈重現實驗結果〉 |
| 探針範圍 | V4 探針不讀 SPEC-004；答案只在 SPEC-004 者仍判為須回寫 SPEC-001／003（同 `.47` D3 判讀） |
| 同步點 | 每項以 grep（腳本 scratchpad `w3-335-53/g.sh`）列本項直接相關位置，標「改」或「已一致，不改」；窮舉由 `0.1.0-W3-335.54` 執行 |

計數：仍猜 2 項 + 清單列 14 項矛盾（含 3 處同步缺漏）＝ 16 條，E4 合併 V4R3-01 G-14 與 V4R3-02 G-09（同為 SPEC-003 §2.5 C3），得 15 個裁決單位。

### 裁決總表

| E# | 來源 | 議題 | 裁決（一句話） | 交用戶 | 與前輪關係 |
|----|------|------|--------------|-------|-----------|
| E1 | V4-01 G3（V4R3-01 G-08） | 「未載入」由哪類元件承載 | 第三類「載入提示」（SPEC-004 4.25 `LoadPrompt`），SPEC-003 §2.7 前導與 §2.11 補列 | 否 | 與 SPEC-004 §3.7 核定（§3.3 第 4 項不合併）一致 |
| E2 | V4-02 G4（V4R3-02 G-29） | 無可消費的型別表顯示哪個版本值 | 版本值＝專案 `.claude/VERSION` 一值；vX＝內建表產生版本，只出現於降級說明，降級說明與動作同條件渲染 | 否 | 細化 `.47` D5（顯示與否不變） |
| E3 | V4R3-01 G-05 | 降級徽章位置與逐列缺席 | 頁面框架通則：與返回列同由殼層渲染，旗標生效時六畫面任一狀態皆存在，不逐列列出 | 否 | 落實 `0.1.0-W1-035`；比照 `.37` R2 通則形態 |
| E4 | V4R3-01 G-14、V4R3-02 G-09 | C3「骨架」vs Ticket 進度條；C3 逐幀終點 vs C4 | C3 改「§2.6 所列形式與版位不變」；逐幀區間終點改為目標態錨點出現，其後 cross-fade 期兩錨點至少一個存在 | 否 | 無衝突 |
| E5 | V4R3-01 G-37 | §3.4 切換專案「重置為未載入」vs 專案未就緒 | 重置為初始，下次可見依「首次可見但圖未建立／首次可見」判定；§3.5 同改 | 否 | 落實 `.37` R9 漏同步 |
| E6 | V4R3-02 G-07 | 無可消費的型別表與 schema 不相容進入條件重疊 | 先判 JSON 存在性：JSON 不存在一律無可消費；JSON 存在才判版本範圍 | 否 | 無衝突（不依賴 `0.1.0-W3-364` 範圍判準） |
| E7 | V4R3-02 G-18 | 同事件兼 emits／consumes 的標籤 | 每個（事件, 方向）一個標籤，兼有時兩個、`emits` 在前 | 否 | 無衝突 |
| E8 | V4R3-02 G-21 | 泳道面板標題格式 | `<UC id> <UC 標題>`；格詳情卡標題維持 `<domain 名> × <UC id>` | 否 | 與 UC-02 v1.2（.36 #6「選定 UC 標題」）一致 |
| E9 | V4R3-02 G-27 | 阻擋狀態本體是否另有切換專案按鈕 | 有，錨點 `action-domain-switch-project`（實作已存在），與 `project-switcher-entry` 同結果；刪「唯一出口」 | 否 | 無衝突 |
| E10 | V4R3-02 G-28 | 阻擋狀態有無說明 slot | 有，§2.11 類別名補「說明（可缺）」 | 否 | 與 `.47` D5 類別名改法同處補字 |
| E11 | V4R3-03 G-29 | §3.3「三個狀態之間」vs 四態；專案未就緒無動畫列 | 通則一句：專案未就緒進出一律整體 cross-fade；§3.3、§3.6 計數詞改列狀態名 | 否 | 與 `.38` S-25 形式一致 |
| E12 | V4R3-03 G-20 | 正常與鏈路斷裂進入條件重疊（根錨點名為漏讀，不裁決） | 互斥：正常改「無缺下游的父節點」、斷裂改「至少一個」 | 否 | 與 `.38` S-26／S-28 一致 |
| E13 | V4R3-04 G-09 | §3.6「`returnTo` 保留」vs §2.3 規則 1 | 依規則 1：rail 離開即設 `null`，§3.6 改寫 | 否 | 落實 `.37` R2「規則 1 對任一畫面一律」 |
| E14 | V4R3-04 G-23 | §8.2 窮盡清單漏 §3.6 `opened`；§3.5 漏檔案不存在 | 依現行條文補列；`0.1.0-W3-210` 若裁定移除成功回饋，§8.2 各「成功」一併刪 | 否 | 綁 `0.1.0-W3-210`（§2.13 #2 待裁決） |
| E15 | V4R3-04 G-13 | 部分損壞跳轉：欄位級標記 vs 獨立按鈕 | 欄位級標記即跳轉入口（`action-nodeDetail-goto-gaps` 多實例），不另設按鈕 | 否 | 與 `.38` V4-01 G37「三變體皆可點」一致 |

交用戶項目：0（15 項皆為產品行為、顯示值或規格措辭；E8 標題組字屬 UI 文案結構、E15 錨點命名屬 SPEC-003 §2.9 規範，皆非用戶寫程式碼風格偏好）。
推翻前輪裁決：0。修正前輪落檔措辭 1 處（E13：§3.6 生命週期列與 `.37` R2 通則不一致，R2 結論不變）。

### 逐項評估（E1–E5）

#### E1（V4-01 G3，仍猜；V4R3-01 G-08）「未載入」的承載元件

**原文位置**：SPEC-001 §4 未載入列；SPEC-003 §2.7 對照表「覆蓋狀態」兩欄（空狀態、阻擋狀態皆未列未載入）；SPEC-003 §2.11 元件庫對應（載入態只承擔 C1–C8）；SPEC-004 §3.6 §4 未載入列 `LoadPrompt`、4.25 `LoadPrompt`、§3.3 第 4 項「不合併，立 `LoadPrompt`」。
**現文核對**：仍需猜（SPEC-003 內無第三類）。
**假設根因**：SPEC-004 §3.7 核定新增 `LoadPrompt` 時未回寫 SPEC-003 §2.7／§2.11。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | SPEC-003 認第三類「載入提示」：§2.7 前導補「未載入不屬兩者」、§2.11 補一列指向 SPEC-004 4.25 |
| O2 | 改造既有（探針候選） | 歸空狀態 `page` 變體，前進動作為開始載入 |
| O3 | 改造既有（探針候選） | 歸載入態元件的 idle 狀態 |
| O4 | 零新增（反向框架：讓未載入態不存在） | 首次可見即自動載入，刪未載入 |

**R 事證**：SPEC-004 §3.3 第 4 項核定理由「空狀態說沒有內容，未載入說有 N 筆內容尚未載入；含數值角色 N」，4.21〈何時不用〉明列 `LoadPrompt`，O2 推翻核定；SPEC-003 §2.5 C1「取消錨點恆可用」與 4.24 `LoadingState` 單一承擔 10 條，idle 態無取消可言，O3 使 C1 對 idle 不成立；O4 違反 UC-05〈驗收條件〉「首次進入不自動載入」與 SPEC-003 §2.8〈斷言形式（首次可見）〉。
**裁決**：O1。
**被放棄選項優點**：O2 元件類別少一個；O4 省一次點擊。
**選定方案缺點**：(1) SPEC-002 八類之外多一類，SPEC-003 §2.11 與 SPEC-002 對照需讀者知悉；(2) §2.7 表仍為兩欄，第三類只以前導句與 §2.11 承載；(3) 再有「待觸發」型狀態時需判定是否沿用本類。
**P 失敗訊號**：第四輪探針仍將未載入歸空狀態 → 改為 §2.7 表增第三欄。
**改動位置與改法**：
- SPEC-003 §2.7 表前導句「本規格定其行為差異」後補「；SPEC-001 §4 未載入（有 N 筆內容尚未載入）不屬兩者，由載入提示元件承載，見 §2.11」
- SPEC-003 §2.11 表於「阻擋狀態」列後新增列「載入提示（訊息 + 票數 N + 開始載入；SPEC-004 4.25 `LoadPrompt`）｜§3.4 未載入與開始載入列、§2.6 分母 N、§2.3 返回由頁面框架承載」
**同步點**（`g.sh LoadPrompt 載入提示 state-tickets-unloaded`）：SPEC-004 §1 總表 `LoadPrompt` 列「+ 返回」、§3.6 §4 未載入列 `ButtonRow`[`AppButton.primary`, `AppButton.secondary`]、4.34 `ButtonRow` 用途「開始載入 + 返回」三處與 4.25（返回由頁面框架承載）不一致（改：刪返回）；SPEC-004 4.25、4.21〈何時不用〉、UC-05 主要流程與驗收（已一致，不改）；SPEC-001 §4（不改）。

#### E2（V4-02 G4，仍猜；V4R3-02 G-29）無可消費的型別表的版本值

**原文位置**：SPEC-001 §1 無可消費的型別表顯示欄「版本值 + … + 降級說明『可改用 App 內建型別表（vX）檢視』」、可用操作欄（`VERSION` 高於內建表產生版本時不渲染動作）；FR-07「顯示框架版本值」；SPEC-003 §2.7「是否顯示版本值」列；SPEC-004 4.23 slot `version`；`lib/l10n/app_zh.arb` `schemaUnconsumableMessage`「此專案的框架版本（{version}）早於…」。
**現文核對**：仍需猜（「版本值」單數未具名，vX 另存）。另發現：降級說明在動作不渲染時仍照字面顯示。
**假設根因**：`0.1.0-W1-035` 回寫降級說明時未區分專案版本與內建表版本，也未綁動作的渲染條件。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 版本值＝專案 `.claude/VERSION`；vX＝內建表產生版本，只在降級說明內；降級說明與動作同條件渲染 |
| O2 | 改造既有（探針候選） | 兩值並列（專案 + 內建表） |
| O3 | 改造既有 | 只顯示內建表產生版本 |
| O4 | 零新增（反向框架） | 不顯示版本值 |

**R 事證**：FR-07 理由段「四個個案指向同一個缺席變數（被觀察專案的框架版本）」；arb 訊息已代入專案版本 `{version}`；SPEC-001 §1 降級註記 (4) 徽章「含內建表產生版本與專案 `.claude/VERSION` 兩個值」屬進入降級後的標示，O2 在阻擋頁提前重複且在動作不渲染時顯示無可用的 vX；O3 丟失 FR-07 要求的值；O4 違反 FR-07 與 `.47` D5。
**裁決**：O1。
**被放棄選項優點**：O2 使用者可在阻擋頁直接比較兩版本；O4 版面最簡。
**選定方案缺點**：(1) 阻擋頁看不到內建表版本，需讀降級說明；(2) 降級說明變為條件式，測試分兩組；(3) 需新 i18n key（含內建表版本 placeholder）。
**P 失敗訊號**：追修核對發現訊息 `{version}` 與 `version` slot 雙重顯示同值 → 比照 `.37` R4 以「有意冗餘」註記或刪 slot，交追修票判定。
**改動位置與改法**：
- SPEC-001 §1 無可消費的型別表顯示欄改「專案框架版本值（`.claude/VERSION`）+「此專案的框架版本早於圖譜型別表的機器可讀匯出」+ 降級說明「可改用 App 內建型別表（v<內建表產生版本>）檢視，B 層邊不渲染」（降級說明與「以 App 內建型別表檢視」同條件渲染，不渲染該動作時亦不顯示）」
- SPEC-001 FR-07 驗收「並顯示框架版本值」改「並顯示專案 `.claude/VERSION` 值」
- SPEC-003 §2.7「是否顯示版本值」阻擋欄「是」後補「（無可消費的型別表：專案 `.claude/VERSION`；schema 不相容：App 支援版本與專案版本兩值）」
- SPEC-004 4.23 slot `version` 文字來源改「呼叫端（專案 `.claude/VERSION`）」；slot `explanation` 補「無可消費的型別表且 `onDegradedView` 非 null 時為降級說明（新 key，placeholder `builtinVersion`）」；i18n 表與 §4.0.6 新 key 總表各補一列；§3.6 §1 無可消費的型別表列顯示欄單元補「+ 降級說明（條件）」
**同步點**（`g.sh 版本值 vX 產生版本 useBuiltinSchemaAction action-domain-degraded-view`）：SPEC-001 §1 降級註記 (4)、§3.1 導航表無可消費列、§4 #7、§5 判讀列（已一致，不改）；SPEC-004 §1 總表 `BlockedState` 列（已一致，不改）；arb `schemaUnconsumableMessage`（核對雙重顯示，見 P）。

#### E3（V4R3-01 G-05）降級徽章的位置與逐列缺席

**原文位置**：SPEC-001 §1 降級註記 (4)「每個畫面的 header 常駐 `badge-<screen>-degraded-schema`」；SPEC-001 §4 各列未列；SPEC-004 §3.6 表後註記「`SplitRow.header` 僅渲染於 §1 五列」；SPEC-004 4.27 `AppShell` 無此徽章。
**現文核對**：矛盾仍在（通則 vs 各表未列；且多數狀態無 header 可放）。
**假設根因**：`0.1.0-W1-035` 以「header」概稱，未指定承載者；元件庫未回寫。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 殼層通則：頁面框架於返回列同一列渲染徽章，旗標生效時六畫面任一狀態皆存在，不逐列列出 |
| O2 | 改造既有 | 逐列列入 SPEC-001 §1–§6 顯示欄與 SPEC-004 §3.6 |
| O3 | 零新增 | 只在正常態的 `SplitRow.header` 顯示 |
| O4 | 零新增（反向框架） | 只在 Domain 視圖與浮層項標示 |

**R 事證**：CLAUDE.md §6 記 `0.1.0-W1-035`「每畫面常駐降級徽章」為定案，O3／O4 推翻；SPEC-001 §8.1 無可消費列以徽章常駐為不需暫態提示的理由，O3 使非正常態無承載；`.37` R2 與 SPEC-004 4.27 實作註記「返回列疊於 `IndexedStack` 內容之上、不分畫面」為既有殼層通則先例；SPEC-001〈降級旗標不落此狀態〉保證旗標生效時圖已建立，專案未就緒與徽章不並存；O2 需改 30 列以上並形成第二份窮舉。
**裁決**：O1。
**被放棄選項優點**：O2 單讀 SPEC-001 各列即知；O3 版面最乾淨。
**選定方案缺點**：(1) 返回列原本只在 `returnTo` 非 null 時出現，現需在旗標生效時常駐；(2) 讀者需跨節讀通則；(3) 殼層多一個資料依賴（降級旗標）。
**P 失敗訊號**：畫面票實作時殼層列在 `kMinWindowSize` 下遮蔽 Domain 視圖 `SegmentedControl` → 改為併入 `PageTitle` 副標 slot。
**改動位置與改法**：
- SPEC-001 §1 降級註記 (4)「每個畫面的 header 常駐」改「由頁面框架於返回列同一列常駐渲染（六畫面任一狀態；同〈返回為殼層通則〉，各節不逐列列出）」；〈返回為殼層通則〉註記末補「降級徽章同屬殼層通則（§1 降級註記）」
- SPEC-003 §2.4〈渲染位置統一〉段末補「降級徽章 `badge-<screen>-degraded-schema` 由同一頁面框架渲染：降級旗標生效期間六畫面任一狀態皆存在，切換專案後不存在（斷言：旗標生效時 `findsOneWidget`，L2 後 `findsNothing`）」
- SPEC-004 4.27 `AppShell` 狀態變更提示列與實作註記補「依降級旗標渲染 `badge-<screen>-degraded-schema`（`Badge.tag`，文字含內建表產生版本與專案 `.claude/VERSION`）」；§4.0.6 新 key 總表補徽章文字 key
**同步點**（`g.sh degraded-schema`）：SPEC-001 §8.1 無可消費列、SPEC-003 §3.1 導航表無可消費列、§4 #7、§5 判讀列「六畫面皆常駐」（已一致，不改）；SPEC-004 4.9 `RecentProjectItem` `isDegraded`（浮層項，不改）。

#### E4（V4R3-01 G-14；V4R3-02 G-09）取消契約 C3

**原文位置**：SPEC-003 §2.5 C3「畫面維持載入態版面（骨架與版位不變）」、〈C3 的斷言方式〉「逐幀推進至 `Motion.cancelDeadline`；每一幀皆斷言載入態骨架根錨點存在」；C4「`Motion.cancelDeadline` 內目標狀態錨點存在，載入態錨點不存在」；§2.6 `state-tickets-loading` 形式為進度條。
**現文核對**：兩處矛盾仍在（Ticket 無骨架；C3 逐幀終點與 C4 同一時點結論相反）。
**假設根因**：C3 寫於三處皆假設骨架時；逐幀終點取 C4 上限而未取目標態抵達時點。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | C3「§2.6 所列形式與版位不變」；區間終點改為目標態錨點出現；其後至 C4 期間兩錨點至少一個存在 |
| O2 | 改造既有 | C3 只適用骨架兩處，Ticket 另立條文 |
| O3 | 改造既有 | C4 放寬為「目標態存在」，允許載入態 cross-fade 殘留 |
| O4 | 零新增（反向框架） | 取消後不 cross-fade，瞬間切換 |

**R 事證**：SPEC-003 §2.11「三處…C1–C8 與 L1–L2 共 10 條完全相同」，O2 破壞；SPEC-004 4.24 `cancelling` 列已寫「版面不變」不限骨架；§3.1／§3.4 動畫提示轉換皆 cross-fade，O4 與之不一致；O3 使「載入態錨點不存在」失去時限，C6「不存在半渲染」無可斷言時點。
**裁決**：O1。
**被放棄選項優點**：O3 實作最寬鬆；O4 斷言最簡。
**選定方案缺點**：(1) 斷言由單一錨點變為「至少一個」組合條件；(2) 實作須在 `Motion.cancelDeadline` 內完成 cross-fade，壓縮批次檢查點預算；(3) C3 條件欄與 C4 條件欄時點需讀者對照。
**P 失敗訊號**：元件測試中取消後目標態出現時點加 `Motion.transition` 超過 `Motion.cancelDeadline` → 檢查 C4 實作約束的批次大小。
**改動位置與改法**：
- SPEC-003 §2.5 C3 條件欄改「按下取消後至目標態錨點出現之前」；可觀察結果「（骨架與版位不變）」改「（§2.6 所列形式與版位不變：Domain 與破洞為骨架，Ticket 為進度條）」
- 〈C3 的斷言方式〉改「逐幀推進直到目標態錨點出現，上限 `Motion.cancelDeadline`；目標態錨點出現前每一幀載入態根錨點 `findsOneWidget`；出現後至 `Motion.cancelDeadline` 的 cross-fade 期間，每一幀載入態與目標態錨點至少一個存在；全區間 `SnackBar`／`Dialog` 為 `findsNothing`」；「載入態骨架根錨點」改「載入態根錨點」
**同步點**（`g.sh 骨架與版位 骨架根錨點`）：SPEC-004 4.24 `cancelling` 列（已一致，不改）、4.24 狀態變更提示列（核對「骨架」用語）；FR-02（不改）。

#### E5（V4R3-01 G-37）切換專案後 Ticket 清單落點

**原文位置**：SPEC-003 §3.4〈生命週期〉切換專案「重置為 `state-tickets-unloaded`」；同表「首次可見但圖未建立」列；SPEC-001〈專案未就緒〉共用定義「取代該畫面原本依圖判定的其他狀態」；SPEC-003 §2.8 L2 可見頁切至 Domain。
**現文核對**：矛盾仍在；§3.5 切換專案「下次可見時重新自動掃描」同型。
**假設根因**：`.37` R9 新增專案未就緒時未回改 §3.4／§3.5 切換專案列（§3.2、§3.3 已寫「重新判定」）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 重置為初始，下次可見依「首次可見但圖未建立／首次可見」判定；§3.5 同改 |
| O2 | 改造既有（原文字面） | 照字面落未載入，圖未建立時允許開始載入 |
| O3 | 改造既有 | 切換專案當下即落專案未就緒 |
| O4 | 零新增（反向框架） | 切換專案保留舊結果 |

**R 事證**：L2「六頁狀態全部重置為各自初始狀態」且可見頁為 Domain，Ticket 下次可見時 Domain 可能已完成載入，O3 此時錯落；O2 違反 R9 共用定義；O4 違反 L2；§3.2「重置為初始（依新專案的圖判定狀態）」為同型先例。
**裁決**：O1。
**被放棄選項優點**：O2 條文最短；O3 狀態值在切換當下即確定。
**選定方案缺點**：(1) 切換專案列不再寫明具體落點，需讀首次可見兩列；(2) 「重置」語意由「設為某值」變為「回到未判定」；(3) SPEC-004 4.25 狀態變更提示的觸發點描述需補條件。
**P 失敗訊號**：畫面測試於 L2 後 Domain 載入中點 `nav-item-tickets` 出現 `state-tickets-unloaded` → 檢查 provider 是否以圖建立為前置。
**改動位置與改法**：
- SPEC-003 §3.4〈生命週期〉切換專案改「中止載入；頁面狀態重置為初始，下次可見時依『首次可見但圖未建立』與『首次可見』兩列判定；搜尋詞與篩選清空；全部欄首 `order` 重置為 `none`」
- SPEC-003 §3.5〈生命週期〉切換專案「下次可見時重新自動掃描」改「下次可見時依『首次可見但圖未建立』與『首次可見且圖已建立』兩列判定」
- SPEC-004 4.25 狀態變更提示列「切換專案重置」補「（下次可見且圖已建立時）」
**同步點**（`g.sh state-tickets-unloaded`，另 `grep -n "切換專案 |" SPEC-003`）：SPEC-003 §3.1、§3.2、§3.3、§3.6 切換專案列（已一致，不改）；SPEC-003 §4 #15（不改）；SPEC-004 4.25 狀態矩陣同步策略欄「切換專案重置」（改，同上補條件）。
### 逐項評估（E6–E10）

#### E6（V4R3-02 G-07）無可消費的型別表與 schema 不相容的進入條件重疊

**原文位置**：SPEC-001 §1 無可消費的型別表進入條件「`.claude/VERSION` 存在，但 JSON 檔案不存在」、可用操作欄「`VERSION` 高於內建表產生版本時不渲染該動作」；schema 不相容進入條件「框架版本超出 App 已知範圍」；〈Gate 三問對照〉schema 版本相容列。
**現文核對**：矛盾仍在（`VERSION` 超出範圍且 JSON 不存在時兩列皆成立，未定序）。
**假設根因**：schema 不相容寫於 v1.2 以前單一上界時代，JSON 缺席一路（v1.2 新增）未回改其條件。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 先判 JSON 存在性：不存在一律無可消費；存在才判版本範圍；schema 不相容條件補「JSON 存在」 |
| O2 | 改造既有（另一端） | 先判版本：`VERSION` 超出範圍即不相容，不論 JSON |
| O3 | 零新增 | 兩列並列，只在〈Gate 三問對照〉補優先序註記 |
| O4 | 新增（反向框架） | 重疊時合併為「無可消費且版本超出」新狀態 |

**R 事證**：無可消費列自身的「`VERSION` 高於內建表產生版本」分支只在 O1 下可達，O2 使該分支成為死條文須刪；schema 不相容的「檢視詳情」顯示 App 支援版本（SPEC-003 §3.1），對無 JSON 可消費的專案無「不相容對象」；O4 增加狀態數（SPEC-001〈狀態總數〉與 SPEC-003 §4、SPEC-004 §3.6 三處同步）且無 UC 出處；O3 不改進入條件欄，SPEC-001 §1 表內仍重疊。
**裁決**：O1。
**被放棄選項優點**：O2 版本過新者一律拒絕，較保守；O3 改動最少。
**選定方案缺點**：(1) 版本過新且無 JSON 的專案看到「早於」措辭的訊息，語意不符；(2) 判定順序散在進入條件欄與 Gate 註記兩處；(3) 「App 已知範圍」判準定案時需複核順序。
**P 失敗訊號**：`0.1.0-W3-364` 定案「已知範圍」以 `VERSION` 為準且要求對無 JSON 專案亦拒絕 → 以 O2 重評；或語料出現 `VERSION` 高於內建表且無 JSON 的專案 → 檢視 (1) 的訊息措辭。
**改動位置與改法**：
- SPEC-001 §1 schema 不相容進入條件改「`tracking_schema.json` 存在，且框架版本超出 App 已知範圍」
- SPEC-001〈Gate 三問對照〉表後註記補「**判定順序**：`.claude/VERSION` 與 JSON 皆缺 → 不是框架專案；JSON 不存在 → 無可消費的型別表（不論 `VERSION` 值）；JSON 存在 → 依版本範圍分正常／schema 不相容」
**同步點**（`g.sh 已知範圍`）：SPEC-001 FR-04、FR-07 理由段、R4 重評註記（已一致，不改）；UC-01〈schema 版本超出已知範圍〉（不改）；SPEC-003 §3.1 導航表載入中列、§4 #2（不涉條件，不改）。

#### E7（V4R3-02 G-18）同事件兼發出與消費的事件標籤

**原文位置**：SPEC-003 §3.1〈格詳情卡的內容契約〉事件標籤列「該格步驟的 `emits`／`consumes` 聯集非空｜每個事件一個標籤，前綴 `emits` 或 `consumes`」；SPEC-004 4.5 `Badge` 變體 `event`「文字（含 `emits`／`consumes` 前綴）」。
**現文核對**：條文內在衝突仍在（格內步驟 A emits E、步驟 B consumes E 時）。
**假設根因**：「每個事件一個」與「前綴二選一」寫作時假設同格內同事件只有一個方向。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 每個（事件, 方向）一個標籤；兼有時兩個，`emits` 在前 |
| O2 | 改造既有 | 一個標籤雙前綴 |
| O3 | 零新增 | `emits` 優先，只顯示一個 |
| O4 | 零新增（反向框架） | 不帶前綴，只列事件 ID |

**R 事證**：SPEC-004 4.5 `event` 文字契約與 `TestCopy.eventLabel`「consumes EVT-…」為單一前綴，O2 須改元件契約與測試文案；SPEC-001 §2〈事件流小表〉分「發出」「消費」兩側，方向是資訊本體；O3 丟失消費側，UC-02 評估影響需知道誰消費；O4 違反 4.5 契約。
**裁決**：O1。
**被放棄選項優點**：O2 標籤數等於事件數；O3 最短。
**選定方案缺點**：(1) 同 ID 出現兩次，讀者可能誤判重複；(2) 標籤數上限變為事件數兩倍，`BadgeRow` 換行增加；(3) 需補「`emits` 在前」的排序約定。
**P 失敗訊號**：假資料格內事件數多使 `scroll-domain-cell-detail` 首屏看不到步驟 → 檢視 `BadgeRow` 位置。
**改動位置與改法**：SPEC-003 §3.1〈格詳情卡的內容契約〉事件標籤列可觀察結果改「每個（事件, 方向）一個標籤，前綴 `emits` 或 `consumes`；同一事件在該格步驟中兼有發出與消費時渲染兩個標籤，`emits` 在前」。
**同步點**（`g.sh 前綴`）：SPEC-004 4.5 `event` 變體、`TestCopy.eventLabel`、§3.6 §1 已選格列 `Badge.event × N`（已一致，不改）；SPEC-001 §1 已選格顯示欄「事件標籤」（不改）。

#### E8（V4R3-02 G-21）泳道面板標題格式

**原文位置**：SPEC-001 §1 正常 · 泳道「面板標題為選定 UC」、泳道 · flow 未結構化「泳道面板標題為選定 UC」；SPEC-003 §3.1 切至泳道列、§2.8〈斷言形式（共用值）〉「面板標題為該 UC」；UC-02〈驗收條件〉「顯示選定 UC 標題」；SPEC-004 §3.2 DomainSwimlane 畫布「UC-02 檢視穿透」、〈UC 選擇入口〉主文字「UC ID + 標題」、§3.6 §1 兩列 `AppText.subtitle`（選定 UC）。
**現文核對**：仍需猜（ID／標題／兩者未定）。
**假設根因**：SPEC-001 以「選定 UC」指值，未定顯示組字。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | `<UC id> <UC 標題>` |
| O2 | 改造既有 | 只 UC id（與格詳情卡標題一致） |
| O3 | 改造既有 | 只 UC 標題 |
| O4 | 零新增（反向框架） | 不顯示面板標題 |

**R 事證**：UC-02 v1.2（`.37` 追修 .36 #6）驗收要求「選定 UC 標題」，O2 違反；畫布與〈UC 選擇入口〉皆為 ID + 標題；矩陣欄首與格詳情卡標題使用 UC id，O3 使泳道與矩陣之間失去對應鍵；O4 使泳道 · 尚未選定 UC 以「無面板標題的 UC 名」區分兩態的依據消失（SPEC-001 §1）。
**裁決**：O1。
**被放棄選項優點**：O2 與格詳情卡一致、短；O3 可讀性高。
**選定方案缺點**：(1) 格詳情卡與泳道面板兩處 UC 呈現不同；(2) 長標題在 `AppText.subtitle` 單行下截斷；(3) 需一個含兩 placeholder 的 i18n key。
**P 失敗訊號**：`kMinWindowSize` 下標題截斷使 UC id 不可見 → 改為 id 不截斷、標題截斷的兩段組字。
**改動位置與改法**：
- SPEC-001 §1 正常 · 泳道、泳道 · flow 未結構化兩列「面板標題為選定 UC」後補「（`<UC id> <UC 標題>`）」
- SPEC-003 §3.1 切至泳道列「泳道面板標題為選定 UC」與 §2.8〈斷言形式（共用值）〉「面板標題為該 UC」改「面板標題文字為 `<UC id> <UC 標題>`」
- SPEC-004 §3.6 §1 正常 · 泳道、泳道 · flow 未結構化兩列 `AppText.subtitle` 補文字組成；§4.0.6 新 key 總表補 `swimlanePanelTitle`（placeholder `ucId`、`ucTitle`）
**同步點**（`g.sh 面板標題`）：SPEC-001 §1 泳道 · 尚未選定 UC「無面板標題的 UC 名」（不改）；SPEC-003 §3.1〈格詳情卡的內容契約〉標題列 `<domain 名> × <UC id>`（不改）；SPEC-004 §3.2 畫布盤點（不改）；UC-02 驗收（已一致，不改）。

#### E9（V4R3-02 G-27）阻擋狀態本體的切換專案出口

**原文位置**：SPEC-003 §2.7 阻擋狀態「必備動作：至少一個出口（切換專案）」「動作觸發後：開啟專案切換浮層」；§2.11「訊息 + 版本值（可缺）+ 出口」；§3.1〈導航跳轉與退出〉不是框架專案「`project-switcher-entry` → 浮層（唯一出口，恆可用）」；§3.7〈生命週期〉展開時機「僅由 `project-switcher-entry` 或阻擋狀態的出口觸發」；SPEC-004 4.23 `plain` `ButtonRow`[切換專案 `primary`]、slot `onSwitchProject` 必填。
**現文核對**：矛盾仍在（SPEC-003 §3.1 稱唯一出口為殼層入口，§3.7 與 SPEC-004 另有本體出口，但無錨點）。
**世界平面事實**：`lib/components/blocked_state.dart` 與 `test/unit/components/blocked_state_test.dart` 使用 `ValueKey('action-domain-switch-project')`。
**假設根因**：SPEC-004 核定本體按鈕與元件票實作後，未回寫 SPEC-003 錨點表。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 本體按鈕具名 `action-domain-switch-project`，與 `project-switcher-entry` 同結果；刪「唯一出口」 |
| O2 | 零新增（原文一端：§3.1） | 刪本體按鈕，只靠殼層入口 |
| O3 | 改造既有 | 本體按鈕只把焦點移至殼層入口 |
| O4 | 零新增（反向框架） | 進入阻擋狀態自動展開浮層 |

**R 事證**：實作與元件測試已採 O1 錨點；O2 須改 SPEC-004 4.23 變體、slot、回饋契約與實作，且全頁阻擋內容無任何動作；O3 多一步且無 UC 出處；O4 違反 SPEC-003 §3.7「不自動展開」；SPEC-001 FR-07「皆須提供切換專案的出口」不限承載位置，O1／O2 皆滿足。
**裁決**：O1。
**被放棄選項優點**：O2 單一入口、無重複；O4 省一次點擊。
**選定方案缺點**：(1) 同一動作兩個入口，測試需各驗一次；(2) §2.7 浮層可用性斷言與本體按鈕斷言並存；(3) §4 三列導航反應欄變長。
**P 失敗訊號**：畫面測試以 `project-switcher-entry` 與 `action-domain-switch-project` 開啟浮層後焦點歸位不同 → 補 §2.10 浮層收合焦點回歸目標。
**改動位置與改法**：
- SPEC-003 §3.1〈互動反應〉「切換專案」列後新增列「阻擋狀態切換專案｜`action-domain-switch-project`｜點擊（僅於三個阻擋狀態渲染）｜浮層展開（§3.7），不改變 `IndexedStack` 索引；結果同 `project-switcher-entry`」
- SPEC-003 §3.1〈導航跳轉與退出〉三個阻擋狀態列補「`action-domain-switch-project` → 浮層」，不是框架專案列「（唯一出口，恆可用）」改「（兩入口同結果，恆可用）」
- SPEC-003 §4 #6、#7、#8 覆蓋層補 `action-domain-switch-project`；§2.7 必備動作阻擋欄補錨點名；§3.7〈生命週期〉展開時機「阻擋狀態的出口」補「（`action-domain-switch-project`）」
- SPEC-004 4.23 slot 契約 `testKey` 列補切換專案按鈕 `action-domain-switch-project`
**同步點**（`g.sh projectSwitcherEntryKey 唯一出口`，`grep -rn switch-project lib test`）：SPEC-003 §2.7〈阻擋狀態的浮層可用性斷言〉（不改，殼層入口仍須恆可用）；SPEC-004 4.23 無障礙列「切換專案入口同時恆可用」（已一致，不改）；SPEC-001 §1 三列（不改）。

#### E10（V4R3-02 G-28）阻擋狀態的說明 slot

**原文位置**：SPEC-001 §1 不是框架專案顯示欄「+ 說明本 App 需要什麼」、FR-07 驗收；SPEC-003 §2.11 阻擋狀態組成「訊息 + 版本值（可缺）+ 出口」；SPEC-004 4.23 slot `explanation`（`notFrameworkProjectExplanation`）。
**現文核對**：輕微矛盾仍在（SPEC-003 類別名缺說明）。
**假設根因**：`.47` D5 改類別名時只補版本值可缺。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | §2.11 類別名補「說明（可缺）」 |
| O2 | 零新增 | 訊息 slot 容多段文字 |
| O3 | 零新增（反向框架） | 刪 SPEC-001「說明本 App 需要什麼」 |

**R 事證**：SPEC-004 4.23 已有 `explanation` slot 與內容政策（4 行）；`message` 內容政策 3 行截斷，O2 使說明被截；O3 違反 FR-07 驗收；E2 的降級說明亦使用同一 slot。
**裁決**：O1。
**被放棄選項優點**：O2 slot 少一個；O3 版面最短。
**選定方案缺點**：(1) 類別名再加長；(2) SPEC-002 類別名與 SPEC-003 差異擴大；(3) 說明 slot 依狀態有無，測試組合增加。
**P 失敗訊號**：第四輪探針仍問說明位置 → 改在 §2.7 表增「說明」列。
**改動位置與改法**：SPEC-003 §2.11 阻擋狀態列類別名改「訊息 + 說明（可缺）+ 版本值（可缺）+ 出口」。
**同步點**（`g.sh 說明本 App 損壞標記（兩級）`）：SPEC-004 §1 總表 `BlockedState` 列、4.23〈用途〉（已含說明，不改）；SPEC-004 §3.6 SPEC-002 對照表阻擋狀態列（引用 SPEC-002 類別名，不改，同 `.47` D5 判讀）。
### 逐項評估（E11–E15）

#### E11（V4R3-03 G-29）動畫表的狀態計數詞與專案未就緒轉場

**原文位置**：SPEC-003 §3.3〈動畫提示〉「三個狀態之間｜cross-fade」（§3 現為四態）；§3.6〈動畫提示〉「三個狀態之間」（§6 現為五態）；§3.2〈動畫提示〉「無 UC 與其他三態之間」（未含專案未就緒）；§3.4、§3.5 動畫表皆未含專案未就緒。
**現文核對**：同步缺漏仍在（`.37` R9 新增專案未就緒、`v1.3` 新增未選節點後未回改）。
**假設根因**：動畫表以計數詞指涉狀態集合，集合增列時靜默失效（`reference-stability-rules` 規則 10 同型）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | §3.2–§3.6 各補一列「專案未就緒與其他狀態之間」；§3.3、§3.6 計數詞改列狀態名 |
| O2 | 改造既有 | §2.1 立通則「專案未就緒進出一律整體 cross-fade，各畫面動畫表不逐列」；§3.3、§3.6 計數詞改列狀態名 |
| O3 | 零新增（反向框架） | 專案未就緒進出為瞬間切換，不定義轉場 |

**R 事證**：§3.2「無 UC 與其他三態之間：整體 cross-fade」（`.38` S-25）與 §3.1「載入中 → 三個阻擋狀態：cross-fade」為同型先例；§2.1 `Motion.transition` 用途「狀態之間的 cross-fade」；專案未就緒五畫面共用一則定義（SPEC-001），O1 在五處重複同一列，下次再增共用狀態時仍需五處同步（`.47` D3 P 同型風險）；O3 與全檔轉換手法不一致。
**裁決**：O2。
**被放棄選項優點**：O1 單讀一畫面即知全部轉場；O3 無需斷言。
**選定方案缺點**：(1) 讀者需跨節讀 §2.1；(2) 通則與各表並存，兩者衝突時需優先序（通則只管專案未就緒）；(3) §3.3、§3.6 列狀態名使列變長。
**P 失敗訊號**：新增第二個五畫面共用狀態時未併入通則 → 將通則改為「共用狀態定義一律」。
**改動位置與改法**：
- SPEC-003 §2.1〈減少動態效果〉段後新增段「**專案未就緒進出的轉場**：五個非 Domain 畫面的 `state-<screen>-project-unready` 與該畫面其他任一狀態之間，一律整體 cross-fade `Motion.transition`；各畫面〈動畫提示〉不逐列列出（SPEC-001 共用定義，`0.1.0-W3-335.37` R9）」
- SPEC-003 §3.3〈動畫提示〉「三個狀態之間」改「正常、鏈路斷裂、無提案之間」
- SPEC-003 §3.6〈動畫提示〉「三個狀態之間」改「未選節點、正常、部分損壞、原始檔已消失之間」
**同步點**（`grep -n "狀態之間" SPEC-003`）：§3.2「尚未選定 UC / flow 未結構化 / 正常 三者之間」「無 UC 與其他三態之間」（列名式，不改；「其他三態」不含專案未就緒，由通則補足）；§3.1、§3.4、§3.5 動畫表（不改）；SPEC-004 各狀態元件狀態變更提示列引用 §3.x 動畫提示（不改）。

#### E12（V4R3-03 G-20）追溯視圖正常與鏈路斷裂的進入條件

**原文位置**：SPEC-001 §3 正常進入條件「至少一個 PROP 節點」、鏈路斷裂「某層無下游節點」、〈初始展開與缺口標示單位〉「缺下游的父節點」、〈父節點層級〉、〈Ticket 層的資料來源〉；SPEC-003 §4 #12–#14 三個根錨點；SPEC-004 §3.6 §3 鏈路斷裂列「同上 + 缺口列」。
**現文核對**：子問題「三態根錨點名未知」已消解（SPEC-003 §4 #12–#14 `state-traceability-normal／broken／empty`），標漏讀不裁決；進入條件字面重疊仍在。
**假設根因**：鏈路斷裂列寫於缺口單位定案前，未改以「缺下游的父節點」表述並排除正常。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 互斥：正常「至少一個 PROP 且無缺下游的父節點」、斷裂「至少一個 PROP 且至少一個缺下游的父節點」 |
| O2 | 改造既有 | 斷裂改為疊加於正常的修飾（比照含損壞），根錨點共用 |
| O3 | 零新增 | 只補優先序註記「斷裂優先」，不改進入條件欄 |
| O4 | 零新增（反向框架） | 刪鏈路斷裂狀態，缺口標示為正常態內容 |

**R 事證**：SPEC-003 §4 #13 以獨立根錨點 `state-traceability-broken` 列為互斥狀態；O2／O4 改變 SPEC-001〈狀態總數〉的互斥語意或列數，牽動 SPEC-003 §4、SPEC-004 §3.6、SPEC-001 §8.1；O3 使進入條件欄仍重疊，探針讀表仍需推論；缺口單位（用戶簽核 2026-09-14）與父節點層級（`.38` S-26）已定，O1 只是改用既有術語。
**裁決**：O1。
**被放棄選項優點**：O2 與含損壞同一慣例，Ticket 載入完成時不需狀態轉換；O3 改動最小。
**選定方案缺點**：(1) Ticket 載入完成可使狀態由正常轉鏈路斷裂，多一條轉換需斷言；(2) 兩列條件互為否定，任一列改寫需同步另一列；(3) SPEC-004「同上 +」的組成描述與互斥語意需讀者自行分辨。
**P 失敗訊號**：畫面測試於 Ticket 載入後回到追溯視圖時 `state-traceability-normal` 與 `state-traceability-broken` 同時存在 → 檢查 cross-fade 斷言時點。
**改動位置與改法**：
- SPEC-001 §3 正常進入條件改「至少一個 PROP 節點，且無缺下游的父節點」；鏈路斷裂進入條件「某層無下游節點」改「至少一個 PROP 節點，且至少一個缺下游的父節點（UC 層判定受 Ticket 載入狀態限制，見〈Ticket 層的資料來源〉）」
- SPEC-003 §3.3〈生命週期〉「Ticket 清單載入完成後再次可見」列補「新增缺口使狀態可由 `state-traceability-normal` 轉為 `state-traceability-broken`」
**同步點**（`g.sh 鏈路斷裂 某層無下游`）：UC-04〈驗收條件〉「某層無下游節點時以虛線框標示」（使用者語言，不改）；SPEC-003 §4 #12–#14、§3.3 導航表（不改）；SPEC-001 §8.1（不改）；SPEC-004 §3.6 §3 兩列（不改）。

#### E13（V4R3-04 G-09）節點詳情切換導覽項時的 `returnTo`

**原文位置**：SPEC-003 §3.6〈生命週期〉切至其他導覽項「`returnTo` 保留」；§2.3 規則 1「`rail` 切換 → `returnTo` 設為 `null`」與 R2 段「規則 1 對任一畫面的下一次 rail 切換一律將 `returnTo` 設為 `null`」；§2.4 導覽切換列「`returnTo` 為 `null`」。
**現文核對**：矛盾仍在（`returnToProvider` 為 App 層單槽，無「本頁保留」的可能）。
**假設根因**：§3.6 生命週期列寫於單槽模型定案前。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有（原文一端：§2.3） | §3.6 改依規則 1，rail 離開即 `null` |
| O2 | 改造既有（原文另一端：§3.6） | 規則 1 對 nodeDetail 例外 |
| O3 | 新增 | 每畫面各自一槽 `returnTo` |
| O4 | 零新增（反向框架） | rail 一律不清 `returnTo` |

**R 事證**：§2.3 採單槽的理由（導覽項恆可見、單值可斷言）；`.37` R2 以規則 1 為通則消費者；SPEC-004 4.27 locate 列「`returnToProvider` 設為 `null`（§2.3 規則 1）」；O2／O3 推翻單槽模型與 R2；O4 使 rail 回到某頁後返回鍵指向與使用者當前路徑無關的畫面。
**裁決**：O1。
**被放棄選項優點**：O2 自節點詳情暫離後回來仍可返回來源；O3 各畫面返回互不干擾。
**選定方案缺點**：(1) 使用者自破洞報告 jump 至節點詳情、rail 查看他頁再回來時返回鍵消失；(2) 需以導覽列回到來源；(3) §3.6 生命週期列語意由保留變清除，讀者需對照 §2.3。
**P 失敗訊號**：實機驗證使用者回報「回到節點詳情後返回鍵不見」→ 以 O3 重評並列入 `returnTo` 模型變更。
**改動位置與改法**：SPEC-003 §3.6〈生命週期〉切至其他導覽項改「選定節點、兩欄 offset 保留；`returnTo` 依 §2.3 規則 1 設為 `null`（回到本畫面時 `action-nodeDetail-back` 不渲染）」。
**同步點**（`grep -n "returnTo.\{0,30\}保留" docs/spec/ui/*.md`）：僅 SPEC-003 §3.6 一處命中；SPEC-003 §2.4 導覽切換列、SPEC-004 4.27 locate 列（已一致，不改）。

#### E14（V4R3-04 G-23）SPEC-001 §8.2 窮盡清單漏列

**原文位置**：SPEC-001 §8.2「本表對 0.1 為窮盡清單：判別方式是 SPEC-003 §3 逐畫面互動反應表中，可觀察結果欄含 SnackBar 且畫面狀態不變的列」、「節點詳情開啟原始檔失敗」列、「破洞項開啟原始檔成功／失敗」列；SPEC-003 §3.6 開啟原始檔 `opened` → SnackBar；§3.5 `notFound` → SnackBar 帶 `rescanAction`；§2.13 對照表 #2 `openedExternallyMessage`「判準判否、條文判是，以條文為準執行」待裁決。
**現文核對**：同步缺漏仍在（§3.6 `opened` 符合判別方式卻未列；§3.5 列未寫檔案不存在）。
**假設根因**：`0.1.0-W1-036` 補外部開啟成功 SnackBar 後，§8.2 節點詳情列只記失敗分支。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 依現行條文補：節點詳情列改「成功／失敗」、破洞項列改「成功／檔案不存在／失敗」 |
| O2 | 零新增（探針候選） | §3.6 `opened` 視為依 §2.13 #2 判準排除，不列 |
| O3 | 零新增（反向框架） | 刪「窮盡清單」宣稱，改為路由範例 |

**R 事證**：§2.13 #2 明寫「以條文為準執行」直到裁決，O2 提前套用未定裁決；§8.2 泳道、UC Flow、docs 目錄三列已列「成功」，同型先例；O3 使 FR-08(b)「§8.2 所列事件的 SnackBar 不計」失去封閉集合，(b) 無可斷言範圍。承接票：`0.1.0-W3-210`（pending）。
**裁決**：O1；`0.1.0-W3-210` 若裁定移除成功回饋，§8.2 各列「成功」一併刪（列為該票同步點）。
**被放棄選項優點**：O2 若 W3-210 判移除則少改一次；O3 免維護窮盡性。
**選定方案缺點**：(1) W3-210 裁定移除時需回改 §8.2 五列；(2) 表列文字加長；(3) 窮盡性仍靠人工同步。
**P 失敗訊號**：`0.1.0-W3-335.54` 窮舉仍找到 §8.2 漏列 → 改為由 SPEC-003 §2.13 對照表產生 §8.2。
**改動位置與改法**：SPEC-001 §8.2「節點詳情開啟原始檔失敗（§6 正常／部分損壞）」改「節點詳情開啟原始檔成功／失敗（§6 正常／部分損壞；檔案不存在轉 `missing`，不屬本表）」；「破洞項開啟原始檔成功／失敗」改「破洞項開啟原始檔成功／檔案不存在／失敗」。
**同步點**（`g.sh 窮盡清單`；`g.sh "SnackBar \`openedExternallyMessage\`"`）：SPEC-003 §3.1 docs 目錄與泳道、§3.2 三列（§8.2 已列，不改）；SPEC-003 §2.13 #2（不改，屬 W3-210）；SPEC-001 FR-08(b)（不改）。

#### E15（V4R3-04 G-13）部分損壞的跳轉入口

**原文位置**：SPEC-003 §3.6〈互動反應〉「跳轉破洞報告｜`action-nodeDetail-goto-gaps`」（未說明元素形態）；§2.11「損壞標記（兩級）｜§3.4 含損壞、§3.6 部分損壞的跳轉行為」；SPEC-004 4.6 `damagedDetail` 行為差異「節點詳情欄位級 `action-nodeDetail-goto-gaps`」、slot `testKey` 同；SPEC-004 §3.6 §6 部分損壞「同上 + 欄位級 `IssueMarker.damagedDetail`」；UC-05〈含損壞票〉「欄位級（含說明文字）逐欄標示」。
**現文核對**：矛盾仍在（SPEC-003 §3.6 讀作獨立按鈕，§2.11 與 SPEC-004 為欄位級標記）。
**假設根因**：SPEC-003 §3.6 列寫於 `IssueMarker` 合併（SPEC-004 §3.3 第 6 項）前，元素名停在「跳轉破洞報告」。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 欄位級標記即入口，沿用 `action-nodeDetail-goto-gaps`（每個損失欄位一個實例），不另設按鈕 |
| O2 | 改造既有 | 欄位級標記改名 `badge-nodeDetail-corrupted-<field>`（比照 `.47` D2 列末標記） |
| O3 | 新增（探針候選） | 頁首獨立按鈕，欄位標記不可點 |
| O4 | 新增（反向框架） | 兩者並存 |

**R 事證**：`.38` V4-01 G37 已判 `IssueMarker` 三變體皆可點，O3 推翻；SPEC-004 4.6 與 §3.6 已採 O1，實作僅 `lib/components/issue_marker.dart` 文件註解提及此錨點；O2 命名與 §2.9 徽章類一致，但需定 `<field>` 值域並改 SPEC-003 三處、SPEC-004 兩處；O4 同一行為兩入口且 SPEC-004 `ButtonRow` 無此按鈕。
**裁決**：O1。
**被放棄選項優點**：O2 命名與其他標記一致、可單獨定位某欄；O3 單一入口易發現。
**選定方案缺點**：(1) `action-` 前綴用於標記，與 §2.9 徽章類 `badge-` 慣例不一致；(2) 同錨點多實例，斷言需祖先限定 finder；(3) 無法以錨點區分是哪個欄位。
**P 失敗訊號**：畫面票測試需指定某一欄位的標記 → 以 O2 重評。
**改動位置與改法**：
- SPEC-003 §3.6〈互動反應〉「跳轉破洞報告」列元素改「欄位級損壞標記（`IssueMarker.damagedDetail`，每個損失欄位一個）」，觸發欄改「點擊（僅於 `state-nodeDetail-partial` 渲染；同一錨點多實例，斷言以祖先限定 finder，同 §3.3〈多父節點〉）」；列後補「不另設頁首跳轉按鈕」
- SPEC-003 §3.6〈導航跳轉與退出〉部分損壞列、§4 #25：`action-nodeDetail-goto-gaps` 前補「欄位級標記」
- SPEC-001 §6 部分損壞可用操作「跳轉破洞報告」補「（欄位級損壞標記）」
**同步點**（`g.sh 損壞標記（兩級）`；`grep -rn nodeDetail-goto-gaps lib test docs/spec`）：SPEC-003 §2.11（已一致，不改）；SPEC-004 4.6 變體表與 slot、§3.6 §6 部分損壞列（已一致，不改）；UC-05〈含損壞票〉（已一致，不改）；SPEC-001 FR-05（已一致，不改）。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-335.52,0.1.0-W3-335.54 | chars: 719 -->

### Task Reference
- 0.1.0-W3-335.52 what: 重驗 V4 走查第三輪（同一份凍結題目）並依 W3-335.22 門檻判定（來源已完成，結論見 Solution）
- 0.1.0-W3-335.54 what: 稽核 SPEC-001／003／004 與 UC 的集合同步（狀態、SnackBar、錨點、計數、生命週期敘述）窮舉式核對

### Rationale Chain
- 0.1.0-W3-335.52 why: 用戶裁示修完整輪重跑 V4：第二輪 .46 未通過（仍猜 2 項），.47 WRAP 裁決 16 項矛盾並經 .48～.51 落檔（SPEC-001 v1.13、SPEC-003 v1.33、SPEC-004 v1.40、UC-01 v1.1、UC-05 v1.3）（來源已完成，結論見 Solution）
- 0.1.0-W3-335.54 why: 用戶裁示（2026-09-14）加同步稽核再跑第四輪：三輪 V4 顯示每輪修補都引入同步缺漏（§2.7 覆蓋清單、§2.13 SnackBar 表、§3.3 狀態計數、§3.4 生命週期、§8.2 窮盡清單），探針抽樣無法窮舉；同步缺漏屬機械性，應以窮舉核對清除

### Related Files
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.52.md  # from 0.1.0-W3-335.52（來源已完成，結論見 Solution）
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.54.md  # from 0.1.0-W3-335.54

### Source Acceptance (reference)
- [ref] [x] 原猜測點逐項判定表（仍猜／已解決／漏讀），已知必猜點另列不計  # from 0.1.0-W3-335.52（來源已完成，結論見 Solution）
- [ref] [x] 依門檻（原猜測點仍猜 = 0）給出通過或未通過結論  # from 0.1.0-W3-335.52（來源已完成，結論見 Solution）
- [ref] [x] 新猜測點數與 spec 間矛盾數三輪比較；已裁決未落實者逐項列出  # from 0.1.0-W3-335.52（來源已完成，結論見 Solution）
- [ref] [ ] 集合清單與各自權威位置（檔名＋章節）  # from 0.1.0-W3-335.54
- [ref] [ ] 成員 × 位置矩陣（可放 scratchpad，票面放摘要與路徑），缺格逐項列出  # from 0.1.0-W3-335.54
- [ref] [ ] 所有計數敘述（狀態總數、N 個狀態、三個狀態之間等）與權威表列數比對結果  # from 0.1.0-W3-335.54
- [ref] [ ] 缺漏依檔分組的改法清單；grep 指令全數記錄可重跑；本票不改 spec  # from 0.1.0-W3-335.54
