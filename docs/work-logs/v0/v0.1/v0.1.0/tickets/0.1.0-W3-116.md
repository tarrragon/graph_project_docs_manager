---
id: 0.1.0-W3-116
title: doc_system/cli.py 補 __main__ guard，修正 python3 -m 呼叫靜默失效
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-090
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-090 驗證時附帶發現
  final_decision: IMP 補 __main__ guard
  rationale: 低優先級技術債，獨立成票追蹤
who:
  current: 待派發
  history: {}
what: '為 .claude/skills/doc/doc_system/cli.py 補上 if __name__ == ''__main__'': main() guard'
when: 0.1.0-W3-090 驗證時發現
where:
  layer: Infrastructure
  files:
  - .claude/skills/doc/doc_system/cli.py
why: python3 -m doc_system.cli 直接執行時 main() 從未被呼叫，靜默 exit 0 且無輸出，會誤導使用者以為指令無結果（0.1.0-W3-090 驗證時發現的附帶問題）
how:
  task_type: Implementation
  strategy: 補 __main__ guard 並確認既有呼叫入口（skill 既有腳本／entry point）不受影響
acceptance:
- '[ ] python3 -m doc_system.cli test-map 有輸出'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

為 .claude/skills/doc/doc_system/cli.py 補上 if __name__ == '__main__': main() guard

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-090 | chars: 802 -->

### Task Reference
- 0.1.0-W3-090 what: 修復 doc test-map 不消費 traceability.yaml，且測試目錄與副檔名硬編為非 Flutter 形態

### Rationale Chain
- 0.1.0-W3-090 why: `doc test-map` 有三個各自獨立、任一未修都足以使查詢對本專案恆回傳零筆的問題（PM 已逐項實查原始碼確認）：

1. **完全不消費 `docs/traceability.yaml`**。全 `doc_system/` 中引用 traceability 的檔案為 `cli.py`、`core/tracking_schema.py`、`commands/batch_init.py`——`commands/test_map.py` 不在其中。它改以掃描檔案內容找 UC ID 字串。
2. **測試目錄硬編為 `tests`**（`test_map.py:73`）。本專案是 Flutter，目錄為 `test/` 與 `integration_test/`。
3. **副檔名過濾不含 `.dart`**（兩個掃描函式皆為 js/ts/py 系列）。

後果直接落在剛完成的 `0.1.0-W3-076` 上：該票的驗收條件之一寫「traceability.yaml 建立且 doc skill 的 test-map 查詢可得結果」，而後半段**在本票修好之前無法由任何工作滿足**。矩陣已建立、資料是真的，但唯一的查詢入口讀不到它，也掃不到本專案的測試檔。

追溯矩陣的價值在於「脫節能被看見」。查詢恆回傳零筆時，脫節與健全在輸出上不可區分——比沒有矩陣更糟，因為它看起來像已經有防護。

### Related Files
- .claude/skills/doc/doc_system/commands/test_map.py  # from 0.1.0-W3-090
- .claude/skills/doc/doc_system/core/constants.py  # from 0.1.0-W3-090
- .claude/skills/doc/tests/test_test_map.py  # from 0.1.0-W3-090

