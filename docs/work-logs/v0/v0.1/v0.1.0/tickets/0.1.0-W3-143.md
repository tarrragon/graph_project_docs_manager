---
id: 0.1.0-W3-143
title: 裁定 七個無有效寫入能力的 agent 定義，其 description 與 tools 各該修哪一邊
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.1.0-W3-141
- 0.1.0-W3-144
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-141 指出 saffron 的 description 明寫「撰寫」但 tools 無 Write／Edit，並明示該矛盾的修法屬 agent 定義範疇、交 PM
  final_decision: ANA 全量盤點同型矛盾並逐一裁定修法方向，實際改動另經核定
  rationale: PM 實查後確認不只 saffron：七個 agent 無有效寫入能力，其中 oregano 更連 Bash 都沒有。單修 saffron 會留下同型缺陷，且修 tools 未必是正確方向——審查型 agent 取得寫入權可能違反設計意圖
who:
  current: thyme-documentation-integrator
  history: {}
what: 裁定 七個無有效寫入能力的 agent 定義，其 description 與 tools 各該修哪一邊
when: 0.1.0-W3-141 建立後（該票只加派發前偵測，明示根因修法交 PM）
where:
  layer: Infrastructure
  files:
  - .claude/agents/saffron-system-analyst.md
  - .claude/agents/oregano-data-miner.md
  - .claude/agents/sassafras-data-administrator.md
  - .claude/agents/sumac-system-engineer.md
why: '`0.1.0-W3-141` 找到的根因是 `saffron-system-analyst` 的 frontmatter 自相矛盾——`description` 明寫「檢視/**撰寫**需求文件」，`tools` 為 `Read, Grep, Glob, LS, Bash, mcp__serena__*` 不含 Write／Edit。建票者依 description 指派完全合理，卻必然卡住：serena 的寫入工具依 `tool-selection` 規則 1 禁用於 `.md`。該票只加派發前偵測，明示修法屬 agent 定義範疇、交 PM。


  PM 實查 `.claude/agents/` 全部定義後確認**不只 saffron**。無有效寫入能力者七個，分兩種形態：


  - **A 類（完全無寫入工具）**：`basil-writing-critic`、`incident-responder`、`linux`、`oregano-data-miner`、`sumac-system-engineer`

  - **B 類（僅 `mcp__serena__*` 命名空間萬用字元，對 `.md` 依規則 1 禁用）**：`saffron-system-analyst`、`sassafras-data-administrator`


  逐一對照 description 後，三個一致、兩個矛盾、兩個疑似：


  | agent | description 承諾的產出 | 判定 |

  |-------|---------------------|------|

  | `basil-writing-critic` | 「產出審查報告，不修改文件」 | 一致——明示不寫 |

  | `incident-responder` | 「開錯誤處理 Ticket」 | 一致——有 Bash 可呼叫 ticket CLI |

  | `linux` | 架構決策與技術審查 | 一致——回報型 |

  | `oregano-data-miner` | 「為執行代理人提供詳細的實作指引」 | **矛盾，且最嚴重**——無 Write 亦無 Bash，既不能寫檔也不能填 ticket body 或建票。依 quality-baseline 規則 5 情境表，這類 agent 在派發階段即不應被指派 ANA 型 ticket，但「策略規劃者」的定位本質上就是 ANA 型工作 |

  | `saffron-system-analyst` | 「檢視/撰寫需求文件」 | **矛盾**——W3-141 的來源實例 |

  | `sassafras-data-administrator` | 「設計資料儲存方式、設計資料遷移方案」 | 疑似——產出若須落為文件則同 saffron |

  | `sumac-system-engineer` | 「規劃及建置系統執行環境、SERVER 安裝及設定」 | 疑似——有 Bash 可安裝，但改設定檔需 Edit |


  **為何不能一律補 Write／Edit**：`saffron` 是 TDD 前置審查專家，賦予寫入權可能違反「審查者不修改被審對象」的設計意圖——`basil-writing-critic` 正是以「不修改文件」為明示約束的同型 agent，兩者的正確修法可能相反（一個改 description、一個維持現狀）。每個 agent 須分別判斷，不套用單一答案。


  **擴散範圍**：`.claude/agents/` 是框架資產，經 sync 擴散至全部 consumer 專案。改動影響的不只本專案。'
how:
  task_type: Analysis
  strategy: '對上表四個矛盾／疑似項逐一裁定，每項給出三選一的方向與理由：


    (a) 改 `description`——移除該 agent 實際做不到的產出承諾，並指出該產出改由誰承接

    (b) 改 `tools`——補 Write／Edit（或 Bash），須論證此舉不違反該 agent 的設計意圖

    (c) 維持現狀——論證該承諾實際可由既有工具達成（如僅需口頭回報或經 ticket CLI 落地）


    判斷依據為該 agent 主文的「允許產出／禁止行為／適用情境」三區塊（`agent-definition-standard` 強制結構）——frontmatter 的 description 應是三區塊的摘要，兩者不一致時以三區塊為準，不以 description 為準。


    `oregano-data-miner` 另有一項獨立問題須一併裁定：無 Bash 使其無法呼叫 ticket CLI，因此無法填 ticket body、無法建票、無法回報 spawn request。這不是 description 與 tools 的措辭問題，是該 agent 在現行 ticket 流程中不可用。裁定其應補 Bash、或改為不指派 ticket 的純諮詢型。


    **本票不改 `.claude/agents/`**：agent 定義是框架資產，改動經 sync 擴散至全部 consumer 專案。裁定產出後依規則 5 建 DOC 票承接，或登記 spawn request 交 PM 核定後才動。'
acceptance:
- '[ ] 裁定依據為各 agent 主文三區塊而非 frontmatter description，且三區塊與 description 的不一致處逐項列出'
- '[ ] 實際改動已建 DOC 票或已登記 spawn request（本票不直接改 .claude/agents/）'
- '[ ] why 段矛盾／疑似表所列的每一項（oregano 已移出，見 0.1.0-W3-144）逐一裁定為改 description、改 tools 或維持現狀，各附不選其他兩者的理由'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

裁定 七個無有效寫入能力的 agent 定義，其 description 與 tools 各該修哪一邊

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
[PM 範圍收窄 2026-09-09] oregano-data-miner 移出本票，改由 0.1.0-W3-144 承接。

理由為結構性，非工作量：

1. 本票的仲裁方法（三區塊與 description 不一致時以三區塊為準）對 oregano 失效——它的矛盾在三區塊內部。「允許產出」同一區塊的「檔案類別」欄承諾產出 .md 至 docs/，「操作類型」欄卻寫「純唯讀工具」，仲裁者本身不一致。
2. oregano 的合理結論包含「退役此 agent」，不在本票 acceptance 規定的三選一選單內。
3. 其「禁止行為」第 6 條要求「必須記錄在 Ticket 執行日誌」，而它無 Edit 亦無 Bash——規範要求了一件結構上做不到的事，屬可用性缺陷而非措辭不一致。

本票餘下範圍：saffron-system-analyst（矛盾）、sassafras-data-administrator（疑似）、sumac-system-engineer（疑似）。why 段的對照表未改動，仍列全部判定以保留脈絡；oregano 那一列請讀作已移交，不在本票裁定範圍。

acceptance 原有一條寫「四個矛盾／疑似項」，該計數已隨移出失效，改為引用 why 段對照表（reference-stability-rules 規則 10：可變計數不實例化）。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-141 | chars: 1263 -->

### Task Reference
- 0.1.0-W3-141 what: 新增 派發前偵測 who 指派的代理人是否具備該票所需的工具

### Rationale Chain
- 0.1.0-W3-141 why: 建票時指派的 `who`，**沒有任何機制對照該代理人實際具備的工具**。指派一個沒有 Write/Edit 的代理人去寫檔案，要到派發後它卡住才會被發現。

**一日三次命中（2026-09-08，跨兩個 session）**：三張票的 `who.current` 都是 `saffron-system-analyst`，而該代理人的 frontmatter `tools` 為 `Read, Grep, Glob, LS, Bash, mcp__serena__*`——不含 Write／Edit。三張票的任務分別是改寫測試設計文件、撰寫 SPEC-003 條文、撰寫 SPEC-004 條文，**全部必須寫檔**。三次都由 PM 在派發前逐票覆核時發現並改派。

**為何會反覆發生**：`saffron-system-analyst` 的定位是「TDD 前置審查專家，檢視/撰寫需求文件」——**description 明寫「撰寫」**，建票者依 description 指派完全合理，但該代理人的工具清單不支援它。**description 與 tools 之間沒有一致性檢查**，兩者可以各自演進。

**現有機制為何擋不住**：`dispatch-validate` 的五條規則檢查 Context Bundle、檔案存在、acceptance 項數與內容、UC 對齊——**沒有一條看 `who`**。派發後才失敗的成本是一次無效派發加一次改派，而若 PM 未逐票覆核，代理人會在 claim 之後才卡住，ticket 停在 `in_progress` 需要 release。

**判準是可機械檢查的**：從 `.claude/agents/<who>.md` 的 frontmatter 讀 `tools`，與票的產出性質比對。產出性質的來源可用 `where.files`（有檔案清單即需寫入能力）或 `type`（DOC／IMP 需寫入，ANA 可能只讀）。

### Related Files
- .claude/skills/ticket/ticket_system/commands/track_dispatch_validate.py  # from 0.1.0-W3-141
- .claude/skills/ticket/ticket_system/tests/test_track_dispatch.py  # from 0.1.0-W3-141

### Source Acceptance (reference)
- [ref] [ ] who 指派的代理人缺少該票所需工具時，dispatch-validate 發出可見訊號並指出缺哪一項工具  # from 0.1.0-W3-141
- [ref] [ ] 所需能力的判定來源已於 Solution 論證（where.files／type／組合），並說明未選其他來源的理由  # from 0.1.0-W3-141
- [ref] [ ] 三種邊界已處理：agent 定義不存在、tools 為萬用字元或 All tools、唯讀型票  # from 0.1.0-W3-141
- [ref] [ ] 嚴格度選擇（FAIL 或 WARN）已論證，判準為誤報成本高於漏報  # from 0.1.0-W3-141
- [ref] [ ] 測試涵蓋：缺工具被偵測、工具齊備放行、萬用字元 tools 不誤擋、agent 定義缺失不崩潰  # from 0.1.0-W3-141
