---
id: 0.1.0-W3-121
title: 補齊 WorkspaceRepository 三個 driven port 呼叫的回饋契約
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-114
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 新落地的 Port 回饋契約方法論與既有 driven port 實作的合規落差
  final_decision: IMP 補齊 WorkspaceRepository 三個呼叫點的三時刻回饋與對應測試
  rationale: 缺口是程式碼既成事實而非待裁決設計問題；setString 回傳值未檢查會讓使用者以為資料夾已記住，屬可觀測的行為錯誤，不需先開 ANA
who:
  current: parsley-flutter-developer
  history: {}
what: 補齊 WorkspaceRepository 三個 driven port 呼叫的回饋契約
when: 0.1.0-W3-114 落地 Port 回饋契約後（2026-09-08）
where:
  layer: Infrastructure
  files:
  - lib/workspace/workspace_repository.dart
  - lib/main.dart
  - lib/l10n/app_zh.arb
  - lib/l10n/app_en.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_zh.dart
  - lib/l10n/app_localizations_en.dart
  - test/unit/workspace/workspace_repository_test.dart
  - test/unit/app/home_page_workspace_test.dart
  - integration_test/app_test.dart
why: '`lib/workspace/workspace_repository.dart` 有四個 driven port 呼叫點，依 `clean-architecture-implementation-methodology.md`〈Port 回饋契約〉的 INV-PORT-OBSERVE-001 全數不合規（PM 逐行實查）：


  1. `chooseFolder()` 的 `getDirectoryPath()`（file_selector 平台通道）：無 try/catch、無日誌。MissingPluginException 或使用者環境異常時直接往上拋，呼叫端拿到的是未分類例外。

  2. `chooseFolder()` 的 `SharedPreferences.getInstance()` / `setString(_pathKey, path)`：**回傳值完全未檢查**。`setString` 回傳 `Future<bool>`，寫入失敗時函式照樣往下走並回傳 `_inspect(path)`——使用者看到資料夾已選定，但下次 `restore()` 讀不到值會回 `WorkspaceUnset`。這是方法論「典型違反」的加重變體：連日誌都沒有，呼叫端與日誌兩個獨立義務同時缺席。

  3. `restore()` 的 `getInstance()` / `getString()`：無失敗路徑。Phase 1 已修正本條的初始描述——`getString()` 是純記憶體查表無 I/O 失敗路徑，真正的讀取失敗集中在會拋例外的 `getInstance()`，兩者不可混為一談，詳見 Solution 裁決 B。

  4. `_inspect()` 的 `Directory.exists()` 與 `dir.list().first`（dart:io 檔案系統）：現況只有 `FileSystemException` 分支有日誌，屬結果時刻的失敗分支；`exists()` 完全無記錄，兩處皆缺「呼叫發出」事件。本條由 Phase 1 的 SR-1 補入——PM 建票時盤點遺漏，非後續擴張。


  四處皆缺「呼叫發出」事件，判準問句「把外部系統整個拔掉，呼叫端能否得知呼叫已發出但無回應」目前答案為否。


  測試覆蓋現況：`test/` 下無 workspace_repository 的單元測試檔，僅 `integration_test/app_test.dart` 間接引用。


  範圍界線：本票只處理 App 端邏輯軌的回饋契約合規。平台端事實軌（實機驗證 file_selector 與真實檔案系統行為）由 `0.1.0-W3-088` 評估，兩者不重疊。`gap_report_screen._openItem` 的同類缺口由 `0.1.0-W3-112` 承接，不在本票範圍。'
how:
  task_type: Implementation
  strategy: '依〈Port 回饋契約〉三時刻逐一補齊，三個呼叫點各自處理：


    1. **呼叫發出**：三處呼叫前各發一次 `developer.log`（含操作名稱與目標路徑），不依賴外部系統是否回應。使用者取消等提早返回的路徑同樣要有此事件——INV-PORT-OBSERVE-001 明文涵蓋「即使呼叫最終失敗或立即拋出例外」。

    2. **結果**：`setString` 的回傳值必須檢查；寫入失敗時 `chooseFolder()` 的回傳型別要能讓呼叫端區分「已選定且已持久化」與「已選定但未能記住」，不得靜默回傳成功。此處需決定回傳型別如何收窄（新增 state 或以 `WorkspaceState` 既有型別表達），決定與理由寫入 Solution。

    3. **不互相抵扣**：日誌不抵扣呼叫端回饋。每個失敗分支同時要有日誌與可被呼叫端消費的回傳或例外，兩者分別確認。


    `getDirectoryPath()` 與 `getInstance()` 的例外以 try/catch 收攏並記錄後轉為明確的回傳型別，禁止裸傳播。


    測試依方法論〈測試義務〉表建 `test/unit/workspace/workspace_repository_test.dart`：呼叫發出時刻用記錄器（spy）驗證方法被呼叫與參數，不需外部系統替身；結果時刻注入替身模擬成功、寫入失敗、例外三種結局。受理時刻對 SharedPreferences 與 file_selector 無對應概念（無 acknowledged 狀態、無 progress callback），此判定須在 Solution 明寫理由，不是略過。'
acceptance:
- '[x] 三個 driven port 呼叫各有「呼叫發出」日誌事件，含提早返回路徑'
- '[x] setString 回傳值已檢查；chooseFolder 的回傳型別能讓呼叫端區分「已持久化」與「未能記住」，型別決定與理由寫入 Solution'
- '[x] 每個失敗分支同時具備日誌與呼叫端可消費的回傳或例外（不互相抵扣）'
- '[x] 建立 workspace_repository_test.dart：呼叫發出用 spy、結果用替身覆蓋成功/寫入失敗/例外；受理時刻不適用的理由已在 Solution 明寫'
- '[x] _inspect() 的 Directory.exists() 與 dir.list().first 各有「呼叫發出」事件；exists() 的失敗結局有結果時刻記錄'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-08T19:06:35'
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
current_phase: Phase 3b
assignee: parsley-flutter-developer
tdd_phase_source: manual
---

# Execution Log

## Task Summary

補齊 WorkspaceRepository 三個 driven port 呼叫的回饋契約

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-08 19:05] readiness=1 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 功能規格：WorkspaceRepository Port 回饋契約

依 `.claude/methodologies/clean-architecture-implementation-methodology.md`〈Port 回饋契約〉
（INV-PORT-OBSERVE-001、不互相抵扣規則、測試義務表）設計。本階段只產出規格，未改 lib/ test/。

### 0. 框架內建機制驗證（ARCH-010 防護）

| 步驟 | 問題 | 結論 |
|------|------|------|
| 1 | 框架內建機制已解決？ | 否。Dart 無「port 呼叫可觀測性」內建機制；`sealed class` + exhaustive `switch` 是內建能力，本設計用它承載型別收窄，不另建抽象 |
| 2 | 標準庫已有解決方案？ | 部分。`dart:developer` 的 `developer.log` 即日誌投影載體（專案既有用法，見 workspace_repository.dart:81），不引入 logging 套件 |
| 3 | 本地狀態足夠？ | 是。`_HomePageState` 的 `setState` 足以承載新回傳型別，不需 Riverpod Notifier |
| 4 | 需要外部方案？ | 否 |

結論：只新增回傳型別（sealed class）與 log 呼叫，不新增套件、不新增狀態管理層。

### 1. 三項裁決

#### 裁決 A：`chooseFolder()` 回傳型別 —— 新增獨立結果型別，不擴充 `WorkspaceState`

**裁決**：新增 `sealed class ChooseFolderResult`（4 個 variant），`WorkspaceState` 維持現狀不動。

```dart
/// `chooseFolder()` 的結局。與 `WorkspaceState` 正交：
/// `WorkspaceState` 描述「資料夾現在能不能用」，本型別描述「這次選取這個動作的結局」。
sealed class ChooseFolderResult {
  const ChooseFolderResult();
}

/// 使用者關閉面板未選取。對應現行 `null` 回傳。
class ChooseFolderCancelled extends ChooseFolderResult {
  const ChooseFolderCancelled();
}

/// 選取面板本身開不起來（MissingPluginException、PlatformException）。
/// 現行行為是裸傳播例外，呼叫端拿到未分類例外。
class ChooseFolderUnavailable extends ChooseFolderResult {
  const ChooseFolderUnavailable(this.reason);
  final String reason;
}

/// 已選定且已持久化：下次啟動 `restore()` 讀得回來。
class ChooseFolderSelected extends ChooseFolderResult {
  const ChooseFolderSelected(this.state);
  final WorkspaceState state;
}

/// 已選定但未能記住：本次可用，下次啟動會回到 `WorkspaceUnset`，使用者需重選。
class ChooseFolderNotRemembered extends ChooseFolderResult {
  const ChooseFolderNotRemembered({required this.state, required this.reason});
  final WorkspaceState state;
  final String reason;
}
```

**理由（為何不擴充 `WorkspaceState`）**：

1. **兩者是正交軸，合併會產生笛卡兒積**。「資料夾可用性」（Unset / Ready / Unavailable）與「是否已持久化」互相獨立——使用者可能選到一個路徑，寫入失敗（未記住），而該資料夾同時是 `Ready` 或 `Unavailable`。若把「未記住」做成第四個 `WorkspaceState` variant，這兩軸就被壓成一維，必然丟失其中一軸的資訊。
2. **`restore()` 永遠不會產生「未記住」**。該語意只存在於寫入路徑。把它塞進 `WorkspaceState` 會讓 `restore()` 的回傳型別出現一個永不發生的分支，`_WorkspaceBanner` 的 exhaustive switch 被迫處理死分支。
3. **保護 W1-014 的型別空間**。`WorkspaceState` 是 UI 直接消費的展示狀態；把持久化機制的失敗語意塞進去，等於讓 SharedPreferences 的實作細節洩漏到 UI 型別，違反「Ports 不洩漏技術細節」。
4. **`ChooseFolderNotRemembered` 攜帶 `state` 而非取代它**：呼叫端仍然拿得到可用的 `WorkspaceState`，本次工作階段不被降級；只是額外知道「這個選擇沒被記住」。這正是「不互相抵扣」的落地——寫入失敗必須同時有日誌（診斷投影）與呼叫端可消費的回傳（程式決策投影），兩者分別存在。

**呼叫端義務（不可只記 log）**：`main.dart` 的 `_chooseFolder()` 收到 `ChooseFolderNotRemembered` 時，必須對使用者顯示「已選定，但下次啟動需重新選擇」的提示（SnackBar 或 banner 附註）。這是 UI 層的「日誌不抵扣使用者回饋」（ARCH-GPD-001）投影，Phase 3b 需新增對應 l10n key（zh-TW 樣板 + en）。

#### 裁決 B：`restore()` 的「讀取失敗」vs「從未設定過」—— 需要區分，且大部分情況可區分

**裁決**：需要區分。兩者在 SharedPreferences 上「同為 null」的前提並不成立——真正同為 null，只發生在 `getInstance()` 成功之後。

| 情境 | 可觀測差異 | 回傳 |
|------|-----------|------|
| 從未設定過 | `getInstance()` 成功，`getString()` 回 null | `WorkspaceUnset()` |
| 儲存層不可用（平台通道失效、plist 無法載入） | `getInstance()` 拋例外 | `WorkspaceUnavailable(lastKnownPath: null, reason: '無法讀取已儲存的工作資料夾設定')` |
| 殘餘不可區分：`getInstance()` 成功但 plist 內容毀損被平台當空值載入 | 無 | `WorkspaceUnset()`（見下方預設行為） |

**理由**：`SharedPreferences.getString()` 是純記憶體查表（`getInstance()` 已把整份 plist 載入快取），它本身沒有 I/O 失敗路徑；真正的讀取失敗集中在 `getInstance()`，而那是一個會拋例外的 async 呼叫，語意上與 null 完全可分。現行程式碼把 `getInstance()` 的例外裸傳播，才使兩者在呼叫端看起來同形。加上 try/catch 後即分離，**不需要新增型別**：`WorkspaceUnavailable` 的 `lastKnownPath` 本就是 `String?`，`null` 表示「知道有問題但不知道是哪個資料夾」，恰好承載此語意。

**殘餘不可區分情況的預設行為與風險**：

- **預設行為**：視為 `WorkspaceUnset`，App 正常啟動，使用者被要求重選。**不阻擋 App**——與 main.dart:195 既有註解的決策一致（還原失敗不阻擋使用）。
- **風險**：使用者曾設定過資料夾，卻看到「請選擇資料夾」，會誤以為設定遺失且無從得知原因。這是靜默失效，正是 W1-014 why 所指的失效形態。
- **緩解一（本票內）**：`restore()` 的結果時刻無論走哪個分支都留 log，事後從日誌可判讀是「讀到 null」還是「例外」。
- **緩解二（跨 W1-014）**：W1-014 引入 schema 版號 key 後，可用交叉判據收窄——**版號 key 存在但 path key 不存在**，代表這個安裝曾經寫過設定，屬異常，應判為 `WorkspaceUnavailable` 而非 `WorkspaceUnset`。此判據要求 W1-014 在「首次寫入 path 的同一次操作」內寫入版號 key（見第 4 節相容性）。

#### 裁決 C：受理時刻的適用性 —— 對 SharedPreferences 適用，對 file_selector 不適用

本票 how 段的預設是「受理時刻對 SharedPreferences 與 file_selector 無對應概念」。逐一驗證後，**SharedPreferences 部分推翻該預設**，理由如下（差異已回報 PM，見第 6 節）。

| 外部系統 | 受理時刻是否適用 | 理由 |
|---------|----------------|------|
| SharedPreferences | **適用**，載體為 `getInstance()` 成功回傳 | 方法論定義受理為「連線建立、檔案控制碼取得、請求被接收」。`getInstance()` 正是「取得儲存管道」的動作：它成功回傳代表底層 plist 已載入、後續 `setString`/`getString` 有對象可操作。相對於 `setString` 這個真正的寫入請求，`getInstance()` 完成即為「管道已建立、尚未知寫入結局」，三時刻齊備。故三時刻各自記錄，不合併 |
| file_selector `getDirectoryPath()` | **不適用**（缺載體，非略過） | API 只回傳單一 `Future<String?>`，該 Future 在使用者關閉面板時才 complete。面板「已開啟」這個受理事實沒有任何回傳值、callback 或狀態可供觀測。App 端無法從中取得受理訊號，故此時刻在 App 端不可觀測 |

**file_selector 受理缺失的後果與歸屬**：無法區分「面板開啟中、使用者思考」與「面板根本沒開起來、Future 永遠不 complete」。此為 App 端邏輯軌無法覆蓋的缺口，屬平台端事實軌，由 `0.1.0-W3-088` 的實機驗證承接，本票不擴張範圍。

**`getString()` 的受理時刻**：`getInstance()` 之後 `getString()` 是純記憶體查表，受理與結果同一瞬間，不另設事件（否則產生無資訊量的重複日誌）。

### 2. 三個呼叫點的三時刻落點與承擔者

共同約定：所有日誌走 `developer.log(..., name: 'WorkspaceRepository')`；失敗分支 `level: 900`，其餘不指定 level（沿用檔案既有寫法，符合 observability-rules 規則 2）。日誌字面加 `// i18n-exempt: 開發者 debug log` 註解，與 workspace_repository.dart:82 一致。

#### 呼叫點 1：`chooseFolder()` → `getDirectoryPath()`（file_selector 平台通道，現行 :45）

| 時刻 | 落點 | 承擔者 | 產出 |
|------|------|--------|------|
| 呼叫發出 | `getDirectoryPath()` 呼叫前一行 | 呼叫端（`WorkspaceRepository.chooseFolder`） | log「開啟資料夾選取面板」。**無條件發出**，含後續所有提早返回路徑 |
| 受理 | 不適用 | —— | 見裁決 C |
| 結果 | `await` 返回處 / `catch` | adapter（本 repository） | 三分支見下 |

結果三分支（皆同時具備日誌與呼叫端可消費回傳，不互相抵扣）：

| 分支 | 提早返回 | 日誌 | 回傳 |
|------|---------|------|------|
| 使用者取消（`path == null`） | 是 | log「使用者取消選取」 | `ChooseFolderCancelled()` |
| 平台例外（`MissingPluginException` / `PlatformException` / 其他） | 是 | log level 900 + `error: e` | `ChooseFolderUnavailable(reason)` |
| 取得路徑 | 否 | log「已選取：$path」 | 續往呼叫點 2 |

#### 呼叫點 2：`chooseFolder()` → `getInstance()` + `setString()`（現行 :49-50）

| 時刻 | 落點 | 承擔者 | 產出 |
|------|------|--------|------|
| 呼叫發出 | `getInstance()` 呼叫前一行 | 呼叫端 | log「準備持久化工作資料夾路徑，key=$_pathKey」，含目標 path |
| 受理 | `getInstance()` 成功回傳後一行 | adapter | log「偏好設定儲存已就緒」（裁決 C） |
| 結果 | `setString` 回傳值判定 / `catch` | adapter | 三分支見下 |

結果三分支——**此處無「提早返回為失敗」**：資料夾已經選定，本次工作階段一律可用，回傳恆帶 `WorkspaceState`：

| 分支 | 日誌 | 回傳 |
|------|------|------|
| `setString` 回傳 `true` | log「已持久化」 | `ChooseFolderSelected(await _inspect(path))` |
| `setString` 回傳 `false` | log level 900「持久化失敗：setString 回傳 false」 | `ChooseFolderNotRemembered(state: await _inspect(path), reason: ...)` |
| `getInstance()` 或 `setString()` 拋例外 | log level 900 + `error: e` | `ChooseFolderNotRemembered(state: await _inspect(path), reason: 例外訊息)` |

現行缺陷對照：`setString` 的 `Future<bool>` 被 `await` 後丟棄，第 2、3 分支目前都會靜默走到 `return _inspect(path)`，呼叫端與日誌兩個獨立義務同時缺席。

#### 呼叫點 3：`restore()` → `getInstance()` + `getString()`（現行 :55-56）

| 時刻 | 落點 | 承擔者 | 產出 |
|------|------|--------|------|
| 呼叫發出 | `restore()` 進入後、`getInstance()` 前一行 | 呼叫端 | log「還原工作資料夾，key=$_pathKey」。**無條件發出** |
| 受理 | `getInstance()` 成功回傳後一行 | adapter | log「偏好設定儲存已就緒」 |
| 結果 | `getString` 判定 / `catch` | adapter | 三分支見下 |

| 分支 | 提早返回 | 日誌 | 回傳 |
|------|---------|------|------|
| `getString` 回 null（從未設定） | 是 | log「無已儲存路徑」 | `WorkspaceUnset()` |
| `getInstance()` 拋例外（儲存層不可用） | 是 | log level 900 + `error: e` | `WorkspaceUnavailable(lastKnownPath: null, reason: '無法讀取已儲存的工作資料夾設定')` |
| 取得路徑 | 否 | log「已還原：$path」 | `await _inspect(path)` |

判準問句複驗：把 file_selector 與 SharedPreferences 整個拔掉，三個呼叫點皆已在呼叫前留下「何時發出、對誰發出」的事件，逾時或永不回應時可從日誌得知呼叫已發出但無結果記錄。INV-PORT-OBSERVE-001 滿足。

### 3. API 介面契約

```dart
/// 開啟系統面板讓使用者選取資料夾。
/// 不再以 null 表示取消 —— 取消、面板不可用、已記住、未記住四種結局
/// 各有獨立型別，呼叫端以 exhaustive switch 處理，漏接為編譯錯誤。
Future<ChooseFolderResult> chooseFolder();

/// App 啟動時呼叫，還原先前選定的資料夾。簽章不變。
/// 新增語意：儲存層不可用時回傳 WorkspaceUnavailable(lastKnownPath: null)，
/// 不再與「從未設定過」同形，也不再裸傳播例外。
Future<WorkspaceState> restore();
```

**破壞性變更與影響範圍**（Phase 3b 須一併處理）：

| 位置 | 影響 |
|------|------|
| `lib/main.dart:202-205` `_chooseFolder()` | `state != null` 判定失效，改為 `switch (result)` 四分支；`ChooseFolderNotRemembered` 需顯示使用者提示 |
| `integration_test/app_test.dart:263-272` `_StubWorkspaceRepository` | `chooseFolder()` 回傳型別變更，stub 需同步 |
| `lib/l10n/app_*.arb` | 新增「已選定但未能記住」提示文案（zh-TW 樣板 + en），並重新產生 `app_localizations*.dart` |

### 4. 與 `0.1.0-W1-014` 的相容性（該票尚未執行，本票決定成為其前提）

| 面向 | 本票的決定 | 對 W1-014 的前提 / 衝突 |
|------|-----------|------------------------|
| `WorkspaceState` 型別空間 | 完全不動（裁決 A） | 相容。W1-014 可自由決定遷移結果型別，不受本票佔用 |
| `restore()` 簽章 | 不變 | 相容。W1-014 若要在 `restore()` 內先跑遷移，插入點在「呼叫發出 log 之後、`getString` 之前」 |
| 結果型別的形態約定 | 以 sealed class 表達互斥結局，不用 `bool` + nullable reason | **建議 W1-014 採同構形態**：其 how 段要求區分「已遷移／無需遷移／遷移失敗」三結局，宜用 `sealed class MigrationResult`，與 `ChooseFolderResult` 一致，避免同一檔案兩種錯誤表達風格 |
| 讀取失敗 vs 從未設定 | 本票裁決「需要區分且可區分」，判據為 `getInstance()` 例外 vs `getString()` null（裁決 B） | **相容且可複用**：W1-014 how 段第 2 點問的是同一個問題（版號 key 的 null 歧義），同一判據直接適用——版號讀取失敗同樣集中在 `getInstance()` |
| 殘餘不可區分的收窄 | 建議以「版號 key 存在但 path key 不存在」作交叉判據，判為 `WorkspaceUnavailable` | **本票對 W1-014 施加的前提**：版號 key 必須在「首次寫入 path 的同一次操作」內寫入，否則此判據不成立。W1-014 若選擇「僅在遷移時才寫版號」，本緩解失效，需回頭修訂裁決 B 的緩解二 |
| 失敗時是否阻擋 App | 本票定案：`restore()` 任何失敗皆不阻擋 App，降級為要求使用者重選 | **潛在衝突**：W1-014 how 段第 3 點把「遷移失敗是否阻擋」留為該票策略決定。若 W1-014 選擇「阻擋」，將與本票已定案的降級語意在同一條 `restore()` 路徑上互斥。此處明記為衝突點，由 W1-014 執行時對齊；本票的立場是降級（與 main.dart:195 既有決策一致） |

### 5. 驗收標準與行為場景（交接 Phase 2）

**驗收標準**（可驗證，對應 ticket acceptance）：

| # | 標準 | 驗證方式 |
|---|------|---------|
| AC1 | 三個呼叫點各在外部呼叫前產生一次「呼叫發出」日誌，且在使用者取消、平台例外兩條提早返回路徑上同樣存在 | spy 記錄器攔截 log 或注入的 port 替身，斷言呼叫順序 |
| AC2 | `setString` 回傳 `false` 時 `chooseFolder()` 回傳 `ChooseFolderNotRemembered`，且 `state` 欄位仍為可用的 `WorkspaceState` | 注入替身回傳 false |
| AC3 | 每個失敗分支同時有日誌與可消費回傳（分別斷言，不以其一抵扣另一） | 每個失敗案例兩條斷言：一條驗回傳型別、一條驗日誌已發出 |
| AC4 | `restore()` 在 `getInstance()` 例外時回 `WorkspaceUnavailable(lastKnownPath: null)`，在 `getString()` 回 null 時回 `WorkspaceUnset()` | 兩個獨立測試，斷言型別不同 |
| AC5 | 受理時刻：SharedPreferences 路徑有「儲存已就緒」事件；file_selector 路徑無該事件且理由已記錄 | 前者 spy 斷言；後者為文件義務（本 Solution 裁決 C） |

**行為場景（Given-When-Then）**：

```
場景 1: 選取成功且成功記住
Given: 選取面板可用，SharedPreferences 寫入會成功
When:  使用者選取一個存在且可讀的資料夾
Then:  回傳 ChooseFolderSelected，state 為 WorkspaceReady(path)；
       日誌依序含「呼叫發出（面板）」「呼叫發出（持久化）」「儲存已就緒」「已持久化」

場景 2: 選取成功但寫入失敗
Given: 選取面板可用，setString 回傳 false
When:  使用者選取一個存在且可讀的資料夾
Then:  回傳 ChooseFolderNotRemembered，state 仍為 WorkspaceReady(path)；
       日誌含 level 900 的持久化失敗事件；UI 顯示「下次啟動需重新選擇」提示

場景 3: 選取成功但寫入拋例外
Given: getInstance() 拋例外
When:  使用者選取資料夾
Then:  回傳 ChooseFolderNotRemembered（reason 帶例外訊息），例外不外洩至呼叫端

場景 4: 使用者取消（提早返回）
Given: 選取面板可用
When:  使用者關閉面板未選取
Then:  回傳 ChooseFolderCancelled；「呼叫發出（面板）」事件仍存在；不觸碰 SharedPreferences

場景 5: 面板開不起來（提早返回）
Given: getDirectoryPath() 拋 MissingPluginException
When:  呼叫 chooseFolder()
Then:  回傳 ChooseFolderUnavailable(reason)，例外不裸傳播；「呼叫發出」事件存在

場景 6: 還原——從未設定過
Given: getInstance() 成功，getString() 回 null
When:  App 啟動呼叫 restore()
Then:  回傳 WorkspaceUnset；日誌含「呼叫發出」「儲存已就緒」「無已儲存路徑」

場景 7: 還原——儲存層不可用（提早返回）
Given: getInstance() 拋例外
When:  App 啟動呼叫 restore()
Then:  回傳 WorkspaceUnavailable(lastKnownPath: null)，與場景 6 型別不同；
       App 不被阻擋；日誌含 level 900 事件

場景 8: 還原——路徑存在但資料夾已消失
Given: getString() 回一個已被刪除的路徑
When:  App 啟動呼叫 restore()
Then:  回傳 WorkspaceUnavailable(lastKnownPath: 該路徑)（既有 _inspect 行為，不變更）
```

**行為單元**：`ChooseFolderResult` 的四分支判定、`restore()` 的三分支判定、六個 log 發出點（3 呼叫發出 + 2 受理 + 各結果分支）。

**Phase 2 已知邊界**：`_inspect()` 內的 `dart:io` 呼叫（`Directory.exists` / `dir.list()`）是第四個 driven port 呼叫，缺「呼叫發出」事件；不在本票 acceptance 範圍（ticket why 明列三處），已登記 spawn request 交 PM 裁決。
### Phase 2 測試設計：WorkspaceRepository Port 回饋契約

依 `.claude/methodologies/clean-architecture-implementation-methodology.md`〈Port 回饋契約〉的
〈測試義務〉表逐時刻對齊設計。Phase 1 的三項裁決（A 新增 `ChooseFolderResult`、B `restore()` 區分
讀取失敗與從未設定、C 受理時刻對 SharedPreferences 適用／對 file_selector 不適用）為既定前提，
本階段不重新裁決。本階段只產出測試規格，未寫任何 `lib/` 實作。

#### 2.0 前提：現況無測試接縫，必須先定義注入點

`workspace_repository.dart` 現況三個外部呼叫全為靜態或頂層函式（`getDirectoryPath()`、
`SharedPreferences.getInstance()`、`Directory(path)`），無任何注入點；`developer.log` 的輸出
在 `flutter test` 下不可攔截。**沒有接縫則本票 acceptance 的 AC1／AC3／AC5 全部不可驗證**——
呼叫發出事件與「不互相抵扣」的日誌側斷言都需要可觀測的記錄載體。

接縫設計（Phase 3b 實作，生產呼叫點 `WorkspaceRepository()` 簽章不變，全部走預設值）：

| 接縫 | 形態 | 對應 port 呼叫點 | 生產預設 |
|------|------|-----------------|---------|
| `DirectoryPathPicker` | `typedef Future<String?> Function()` | 呼叫點 1 file_selector | `getDirectoryPath` |
| `WorkspacePreferencesPort` | `abstract interface class`，唯一方法 `Future<WorkspacePreferencesHandle> open()` | 呼叫點 2／3 的 `getInstance()`（受理載體） | 包裝 `SharedPreferences.getInstance()` |
| `WorkspacePreferencesHandle` | `String? readString(String key)` + `Future<bool> writeString(String key, String value)` | `getString` / `setString`（結果） | 包裝 `SharedPreferences` 實例 |
| `WorkspaceDirectoryProbePort` | `Future<bool> exists(String path)` + `Future<void> readFirstEntry(String path)` | 呼叫點 4 dart:io | 包裝 `Directory.exists` / `dir.list().first` |
| `WorkspaceLogSink` | `typedef void Function(String message, {int? level, Object? error})` | 日誌投影 | 轉呼 `developer.log(name: 'WorkspaceRepository')` |

兩項形態要求（測試可驗性的前提，非美學偏好）：

1. `open()` 與 `readString`/`writeString` **必須分成兩層**。合併成單一 `read()/write()` 會使受理時刻
   （裁決 C 判定對 SharedPreferences 適用）失去載體，AC5 前半不可驗證。
2. `readFirstEntry` 必須原樣傳播 `FileSystemException` 與 `StateError`——現行 `_inspect` 以這兩個
   例外型別區分「不可讀」與「空資料夾」，替身要能重現這兩條分支。

#### 2.1 分層測試決策

| 目標 | 層級 | 測試型態 | 位置 |
|------|------|---------|------|
| `WorkspaceRepository` 的四個 port 呼叫點三時刻 | Infrastructure adapter | 單元測試（手寫替身，專案無 mocktail／mockito） | `test/unit/workspace/workspace_repository_test.dart`（新建） |
| `_chooseFolder()` 的四分支 switch 與 `ChooseFolderNotRemembered` 的使用者提示 | Layer 1 UI 關鍵流程 | widget 測試 | `test/unit/app/`（新建或併入既有檔，Phase 3a 決定） |
| `_StubWorkspaceRepository` 簽章相容 | 整合層 | 既有 `integration_test/app_test.dart` 編譯即驗證 | 見 2.5 |

不做端對端測試：file_selector 與真實檔案系統的平台端事實軌由 `0.1.0-W3-088` 承接，本票只覆蓋 App 端邏輯軌。

#### 2.2 三時刻與測試裝置的對應（逐時刻對齊〈測試義務〉表，不一律用替身）

| 時刻 | 需要外部系統替身？ | 本票裝置 | 斷言對象 |
|------|------------------|---------|---------|
| 呼叫發出 | **不需要** | 記錄器（spy）：`_PickerRecorder`／`_PreferencesRecorder`／`_DirectoryProbeRecorder`／`_LogRecorder` | 「方法被呼叫了、參數是什麼」與對應日誌事件已發出 |
| 外部系統受理 | 需要 | 行為替身 `_FakePreferencesPort`（`open()` 成功／拋例外） | 「儲存已就緒」事件存在且在 `writeString`／`readString` 之前 |
| 結果 | 需要 | 行為替身 `_FakePreferencesHandle`／`_FakePicker`／`_FakeDirectoryProbe`（成功／false／例外／不存在） | 回傳型別與 `state` 欄位、失敗分支日誌 |

三條裝置紀律（方法論明寫記錄器不屬於行為替身分類，兩者不可混用）：

1. **記錄器不模擬行為**。記錄器類別只累積 `List<_Call>(method, args)`，不接受「結局」建構參數；行為
   替身是**另一組類別**，只出現在受理／結果測試。同一個測試不得把記錄器當結局替身用。
2. **記錄器的回傳是技術必要，不是被驗證的行為**。Dart 要求方法有回傳值，記錄器一律回中性值
   （`null`／`true`／`Future.value()`）；呼叫發出測試的斷言**只針對記錄內容與日誌**，禁止對記錄器
   的中性回傳做任何結局斷言（否則就是把記錄器偷渡成行為替身）。
3. **上游驅動替身不是被觀測對象**。要抵達呼叫點 2／3／4 必須讓上游 port 先產出路徑，此時上游用
   最小成功路徑替身（arrange fixture），測試不對它斷言。被觀測的 port 才用記錄器。

#### 2.3 GWT 場景群組

功能職責歸屬與跨群組依賴標註於各群組標題（供 Phase 3b 按 SRP 拆分派發）。共用 fixture 抽為
`_arrangeSuccessPath()` 等 helper，各群組獨立呼叫，群組間不共享可變狀態。

**G1｜呼叫發出時刻（職責：INV-PORT-OBSERVE-001 覆蓋；依賴：無）**

```
G1-1 開啟選取面板前發出呼叫事件
Given: picker 為記錄器
When:  呼叫 chooseFolder()
Then:  picker 記錄到 1 次呼叫；log 記錄到「開啟資料夾選取面板」事件

G1-2 使用者取消時「呼叫發出」事件仍存在（提早返回路徑）
Given: picker 回傳 null（結局替身），log 為記錄器
When:  呼叫 chooseFolder()
Then:  log 含「開啟資料夾選取面板」與「使用者取消選取」兩個事件

G1-3 面板拋例外時「呼叫發出」事件仍存在（提早返回路徑）
Given: picker 拋 MissingPluginException
Then:  log 仍含「開啟資料夾選取面板」事件，且其發出順序早於失敗事件

G1-4 持久化前發出呼叫事件（含目標 key 與 path）
Given: picker 產出 '/tmp/ws'（上游驅動替身），preferences 為記錄器
When:  呼叫 chooseFolder()
Then:  preferences 記錄到 open() 呼叫；log 含「準備持久化」事件且訊息含 'workspace.path' 與 '/tmp/ws'

G1-5 restore() 進入即發出呼叫事件
Given: preferences 為記錄器
When:  呼叫 restore()
Then:  log 含「還原工作資料夾」事件且訊息含 'workspace.path'；記錄到 open() 呼叫

G1-6 _inspect 的 Directory.exists 有呼叫發出事件（acceptance 第 5 條）
Given: 走到 _inspect 的成功前置，directoryProbe 為記錄器
Then:  directoryProbe 記錄到 exists('/tmp/ws')；log 含「探測資料夾是否存在」事件

G1-7 _inspect 的 dir.list().first 有呼叫發出事件（acceptance 第 5 條）
Given: exists 回 true（上游驅動替身），readFirstEntry 為記錄器
Then:  directoryProbe 記錄到 readFirstEntry('/tmp/ws')；log 含「讀取資料夾內容」事件

G1-8 restore() 儲存層拋例外時「呼叫發出」事件仍存在（提早返回路徑）
Given: open() 拋例外
Then:  log 仍含「還原工作資料夾」事件
```

**G2｜chooseFolder 結局與四個 variant（職責：`ChooseFolderResult` 判定；依賴：無，自帶行為替身）**

```
G2-1 選取成功且成功記住（Phase 1 場景 1）
Given: picker 回 '/tmp/ws'；open() 成功；writeString 回 true；資料夾存在且可讀
Then:  回傳 ChooseFolderSelected；state 為 WorkspaceReady('/tmp/ws')；
       log 依序含 呼叫發出(面板)→已選取→呼叫發出(持久化)→儲存已就緒→已持久化

G2-2 寫入回 false → ChooseFolderNotRemembered（Phase 1 場景 2）
Given: 同 G2-1，但 writeString 回 false
Then:  回傳 ChooseFolderNotRemembered；reason 非空；
       state 為 WorkspaceReady('/tmp/ws')（攜帶而非取代，見 G2-4）

G2-3 寫入拋例外 → ChooseFolderNotRemembered（Phase 1 場景 3）
Given: open() 或 writeString 拋例外
Then:  回傳 ChooseFolderNotRemembered，reason 含例外訊息；
       例外不外洩（expect 呼叫本身 returnsNormally／completes）

G2-4 NotRemembered 攜帶而非取代 state（跨案例等價斷言）
Given: 除 writeString 回傳值外，G2-1 與 G2-2 的輸入完全相同
Then:  兩者的 state 為同一個 WorkspaceState 值（同 runtimeType 且 path 相等）
說明：本條是裁決 A 第 4 點的直接驗證——單看 G2-2 只能知道「state 欄位有東西」，
      與 G2-1 對比才驗得到「本次工作階段未被降級」。

G2-5 使用者取消 → ChooseFolderCancelled 且不觸碰 SharedPreferences（Phase 1 場景 4）
Given: picker 回 null；preferences 為記錄器
Then:  回傳 ChooseFolderCancelled；preferences 記錄的呼叫串為空（負向斷言）

G2-6 面板不可用 → ChooseFolderUnavailable（Phase 1 場景 5）
Given: picker 拋 MissingPluginException
Then:  回傳 ChooseFolderUnavailable，reason 非空；例外不裸傳播；不觸碰 SharedPreferences
```

**G3｜restore 結局三分支（職責：裁決 B 的區分判定；依賴：無）**

```
G3-1 從未設定過（Phase 1 場景 6）
Given: open() 成功；readString 回 null
Then:  回傳 WorkspaceUnset；log 含 呼叫發出→儲存已就緒→無已儲存路徑

G3-2 儲存層不可用（Phase 1 場景 7）
Given: open() 拋例外
Then:  回傳 WorkspaceUnavailable，lastKnownPath 為 null，reason 非空；
       型別／欄位與 G3-1 可區分（斷言 G3-1 與 G3-2 結果不同型別）；App 不被阻擋（無例外外洩）

G3-3 路徑存在且可讀
Given: readString 回 '/tmp/ws'；資料夾存在且可讀
Then:  回傳 WorkspaceReady('/tmp/ws')；log 含「已還原」事件

G3-4 路徑已消失（Phase 1 場景 8，既有行為不變更）
Given: readString 回 '/tmp/gone'；exists 回 false
Then:  回傳 WorkspaceUnavailable(lastKnownPath: '/tmp/gone')
```

**G4｜受理時刻（職責：裁決 C；依賴：無）**

```
G4-1 SharedPreferences 受理事件存在且順序正確
Given: open() 成功
Then:  log 含「偏好設定儲存已就緒」，且其索引 > 呼叫發出事件、< 結果事件

G4-2 open() 失敗時無受理事件（負向）
Given: open() 拋例外
Then:  log 不含「偏好設定儲存已就緒」

G4-3 file_selector 無受理事件（負向，鎖定裁決 C 的不適用判定）
Given: picker 為記錄器
Then:  log 中 picker 相關事件只有「呼叫發出」與結果兩類，無受理類事件
說明：不適用的理由（API 只回單一 Future，面板已開啟無可觀測載體）為文件義務，
      已在 Phase 1 裁決 C 記載；本測試只鎖定「不會有人日後補一個假的受理事件」。
```

**G5｜不互相抵扣（職責：AC3 橫切檢查；依賴：G2/G3 的替身，複用同一組 helper）**

每個失敗分支（G2-2／G2-3／G2-6／G3-2／G6-2）各需**兩條獨立斷言**：
一條驗呼叫端可消費的回傳型別，一條驗對應日誌事件（level 900）已發出。
禁止以「回傳正確」推論日誌存在，或反之。Phase 3a 應把這對斷言寫成明示的兩行 expect，
不得包成單一 helper 掩蓋兩者的獨立性。

**G6｜_inspect 探測結局（職責：呼叫點 4 結果時刻；依賴：無）**

```
G6-1 資料夾不存在 → WorkspaceUnavailable(lastKnownPath: path)
G6-2 readFirstEntry 拋 FileSystemException → WorkspaceUnavailable + level 900 日誌（既有行為，補結果時刻記錄）
G6-3 readFirstEntry 拋 StateError（空資料夾）→ WorkspaceReady（不是失敗，不得記為 level 900）
G6-4 探測成功 → WorkspaceReady
```

**G7｜UI 消費端四分支（職責：裁決 A 的呼叫端義務；依賴：需 `ChooseFolderResult` 型別，Phase 3b 與 G2 同批）**

```
G7-1 ChooseFolderSelected → banner 更新為 Ready 狀態
G7-2 ChooseFolderNotRemembered → banner 更新為其攜帶的 state，且顯示「下次啟動需重新選擇」提示
G7-3 ChooseFolderCancelled → 狀態不變、無提示
G7-4 ChooseFolderUnavailable → 顯示錯誤提示，狀態不變
```

G7-2 的文案由新增 l10n key 提供（zh-TW 樣板 + en）；斷言以 l10n key 對應的文字查找，不硬編字面。

#### 2.4 Fake／Spy 清單（Phase 3a 產出）

| 類別 | 角色 | 可配置項 |
|------|------|---------|
| `_PickerRecorder` | 記錄器 | 無（回中性 null） |
| `_PreferencesRecorder` / `_HandleRecorder` | 記錄器 | 無（`writeString` 回中性 true） |
| `_DirectoryProbeRecorder` | 記錄器 | 無（`exists` 回中性 true） |
| `_LogRecorder` | 記錄器 | 無；累積 `(message, level, error)` 供順序斷言 |
| `_FakePicker` | 行為替身 | 回傳路徑／null／拋指定例外 |
| `_FakePreferencesPort` / `_FakePreferencesHandle` | 行為替身 | open 成功或拋例外；readString 值；writeString 回 true/false/拋例外 |
| `_FakeDirectoryProbe` | 行為替身 | exists 真假；readFirstEntry 正常／FileSystemException／StateError |

日誌斷言一律以 `_LogRecorder` 的訊息**索引順序**驗證（G2-1、G4-1 需要順序），不只驗存在。
訊息比對用穩定子字串（如 'workspace.path'、路徑值），不逐字綁定完整中文句子，避免文案微調即紅燈。

#### 2.5 `integration_test/app_test.dart` 的 `_StubWorkspaceRepository` 更新策略

現況（:263-272）`chooseFolder()` 宣告為 `Future<WorkspaceState?>`，裁決 A 改為
`Future<ChooseFolderResult>` 後**編譯失敗**（回傳型別不相容）。更新方式：

| 項目 | 做法 |
|------|------|
| 簽章 | `Future<ChooseFolderResult> chooseFolder() async => ChooseFolderSelected(_state);` |
| `restore()` | 不變（裁決 B 未改簽章） |
| `implements WorkspaceRepository` 是否受新接縫影響 | 否。接縫全為建構子具名參數與私有欄位，`integration_test` 是另一個 library，`implements` 不需實作私有成員；只要不新增公開方法即相容 |
| 是否需新增 stub 變體 | 是。G7-2 需要一個回傳 `ChooseFolderNotRemembered` 的變體；建議加具名建構子 `_StubWorkspaceRepository.notRemembered(state)`，供 widget 測試複用（integration 版面測試本身不需用它） |
| 執行順序 | 型別新增（Phase 3b 前段）→ stub 更新必須與 `lib/main.dart` 同一批修改，否則 `flutter test` 與 `integration_test` 之間出現半編譯狀態 |

Phase 3b 驗證指令：`fvm flutter analyze | tail -20` 需零錯誤（stub 未更新會在此暴露），
再跑 `fvm flutter test test/unit/workspace/ | tail -20`。

#### 2.6 acceptance 對應表

| acceptance | 覆蓋群組 |
|-----------|---------|
| 1 三處呼叫發出日誌含提早返回 | G1-1~G1-5、G1-8 |
| 2 setString 回傳值已檢查、型別可區分 | G2-2、G2-4 |
| 3 每個失敗分支日誌與回傳不互相抵扣 | G5（橫切 G2-2/2-3/2-6、G3-2、G6-2） |
| 4 建立 workspace_repository_test.dart：spy／替身／受理理由 | 全體；受理判定見 G4 與 Phase 1 裁決 C |
| 5 _inspect 兩處呼叫發出 + exists 結果時刻 | G1-6、G1-7、G6-1~G6-4 |

#### 2.7 Phase 3a 交接（pepper-test-implementer）

- 先建接縫型別（2.0 表）與 `ChooseFolderResult`（Phase 1 §3），再寫測試；測試檔先行紅燈，禁寫 `lib/` 邏輯實作。
- 記錄器與行為替身**分開兩組類別**，禁止合併為「可配置的萬用 fake」——合併後 2.2 的紀律一無法在程式碼層被看見。
- 群組可獨立派發：G1／G2＋G5／G3／G4／G6 各自只需自身 helper；G7 依賴 `ChooseFolderResult` 型別，須排在 G2 之後或同批。
- 命令一律前景執行並加 `| tail`。
### Phase 3a 實作策略與 3b 拆分裁定

Phase 1 裁決（A/B/C）與 Phase 2 測試設計為既定前提，本階段不重新設計。本階段未寫任何
`lib/` 或 `test/` 檔案。

#### 3a.1 3b 拆分裁定：**必須拆分**，切為四批循序派發

依 `.claude/rules/core/cognitive-load.md`「3b 派發前閾值」逐項量測：

| 閾值 | 門檻 | 本票實測 | 判定 |
|------|------|---------|------|
| 功能職責數 | > 2 須拆 | 4（型別與接縫遷移／adapter 三時刻回饋／UI 消費端回饋／測試裝置建置） | **超標** |
| 修改檔案數 | > 5 須拆 | 8（where.files 6 檔 + `lib/l10n/app_localizations*.dart` 生成物 + 新建 `test/unit/app/` widget 測試檔） | **超標** |
| Context Bundle tokens | > 3000 軟上限／> 5000 強制拆 | 單就 Solution 的 Phase 1＋Phase 2 兩節即約 12000 字元（逾 5000 tokens），一次派發須整段送入 | **超標** |
| 需追蹤概念數 | > 7 須拆 | 4 接縫型別 + `ChooseFolderResult` 4 variant + `WorkspaceState` 3 variant + 6 日誌落點 + 8 替身類別 = 25 | **超標** |
| 跨架構層級數 | > 2 須拆 | 2（Infrastructure adapter、UI）——未超標 | 通過 |
| 目標檔案行數 | > 300 須評估 | `workspace_repository.dart` 96 → 預估 250；`main.dart` 397 → 預估 415 | 見 3a.5 |

四項超標，其中兩項為「必須拆分」等級。**裁定：拆為 A/B/C/D 四批**，循序派發，前一批綠燈為
後一批的派發前置。

#### 3a.2 切分邊界與各批驗收點

切分主軸是**編譯完整性**，不是檔案數平均：Dart 的破壞性簽章變更會讓 `main.dart`、
`integration_test` stub、`test/` 三處同時失去編譯能力，這三處必須同批；而「測試能編譯但斷言
失敗（紅燈）」與「測試不能編譯（analyze 紅）」是兩種不同狀態，前者是 TDD 要的 RED，後者不是。
因此簽章變更必須**早於**測試批，這是本切分與直覺（先寫測試）相反的關鍵理由。

| 批次 | 職責（1 項） | 修改檔案 | 驗收點（測試變綠條件） |
|------|-------------|---------|----------------------|
| **3b-A 遷移批** | 型別與接縫遷移，**不新增任何行為** | `lib/workspace/workspace_repository.dart`、`lib/main.dart`、`integration_test/app_test.dart`、`lib/l10n/app_zh.arb`、`lib/l10n/app_en.arb`、`lib/l10n/app_localizations*.dart`（生成） | `fvm flutter analyze \| tail -20` 零錯誤；`fvm flutter test \| tail -20` 既有測試全綠（**零回歸**，本批不新增測試） |
| **3b-B RED 批** | 測試裝置與場景建置 | `test/unit/workspace/workspace_repository_test.dart`（新建）、`test/unit/app/home_page_workspace_test.dart`（新建） | `analyze` 零錯誤（測試必須可編譯）；兩新檔出現預期紅燈，**其餘既有測試維持全綠** |
| **3b-C GREEN 批一** | adapter 三時刻回饋實作 | `lib/workspace/workspace_repository.dart` | G1／G2／G3／G4／G5／G6 共 26 場景全綠：`fvm flutter test test/unit/workspace/ \| tail -20` |
| **3b-D GREEN 批二** | UI 消費端回饋實作 | `lib/main.dart` | G7 共 4 場景全綠：`fvm flutter test test/unit/app/ \| tail -20`；再跑全量 `fvm flutter test \| tail -20` 與 `integration_test` |

**為何 C 與 D 不合併**（合併後為 2 檔 2 職責，形式上未超標）：兩批的紅燈範圍互不重疊
（C 對 `test/unit/workspace/`、D 對 `test/unit/app/`），分開派發時任一批紅燈即可直接定位到
單一 `lib/` 檔；合併後 30 個場景同時紅燈，失敗歸因需人工分辨是 adapter 回傳錯還是 UI 分支錯。
分批的成本是一次額外派發，收益是失敗定位從 30 場景收斂到單檔。

#### 3b-A 的「不新增行為」界線（本批最易越界處）

A 批做且只做：新增 `ChooseFolderResult` 四 variant、新增五個接縫型別、`WorkspaceRepository`
建構子改為具名可選參數並全部走預設值、`chooseFolder()` 簽章改為 `Future<ChooseFolderResult>`、
三個消費端同步適配、arb 新增文案 key。

A 批**不做**：任何 `developer.log` 新增、`setString` 回傳值檢查、`getInstance()` 的 try/catch。
A 批結束時 `chooseFolder()` 的行為必須與現況等價——只回 `ChooseFolderCancelled` 與
`ChooseFolderSelected` 兩個 variant，`ChooseFolderUnavailable` 與 `ChooseFolderNotRemembered`
定義了但無人產出（由 C 批接手產出）。這條界線是 A 批「零回歸」驗收點成立的前提：一旦 A 批
夾帶行為變更，既有 integration 測試的綠燈就不再能證明遷移無損。

A 批 `main.dart` 的 `NotRemembered` / `Unavailable` 分支在本批寫為「更新 state、提示留待 3b-D」
——分支必須存在（exhaustive switch 是編譯要求），提示的實作由 3b-D 承接，此為
`decision-trigger-binding` 規則 1 狀態 (b)：trigger 為 3b-D 批次。

#### 3a.3 實作順序策略（語言無關）

```
步驟 1（A）：定義結局型別
  宣告「選取動作結局」的互斥集合 = {取消, 面板不可用, 已選定且已記住, 已選定但未記住}
  後兩者攜帶「資料夾可用性狀態」；「未記住」另攜帶原因字串

步驟 2（A）：抽出四個外部系統接縫 + 一個日誌接縫
  對每個外部依賴，定義最小介面（只暴露本 repository 實際用到的動作）
  「開啟儲存管道」與「讀寫」必須是兩個動作（受理時刻的載體，見 Phase 2 §2.0 形態要求 1）
  repository 建構子接受這些接縫，未提供時取生產預設值 → 生產呼叫點簽章不變

步驟 3（A）：消費端適配（與步驟 1、2 同批，否則編譯斷裂）
  UI 消費端：null 判定 → 對結局集合做窮舉分支
  整合測試替身：回傳型別同步，並加一個回傳「未記住」的具名建構子變體
  文案資源：新增「已選定但未能記住」key（樣板語系 + 英文），重新產生 l10n

步驟 4（B）：建測試裝置與場景（禁寫任何 lib 邏輯）
  記錄器組：只累積 (方法名, 參數) 序列，回中性值，無結局設定入口
  行為替身組：獨立的另一組類別，只設定結局
  日誌記錄器：累積 (訊息, 等級, 錯誤)，供索引順序斷言

步驟 5（C）：adapter 三時刻
  對每個外部呼叫，依序：
    發出事件（無條件，位於呼叫敘述之前）
    → 呼叫
    → 若該外部系統有「管道就緒」概念則記受理事件
    → 對結果的每個分支：記日誌 且 回傳可被消費的值（兩者分別完成，不互抵）

步驟 6（D）：UI 回饋
  「已選定但未記住」分支：更新狀態 + 顯示「下次啟動需重新選擇」提示
  「面板不可用」分支：顯示錯誤提示，狀態不變
```

#### 3a.4 控制流程（`chooseFolder` 三時刻骨架，語言無關虛擬碼）

```
函式 選取資料夾() -> 選取結局:
    記錄("開啟資料夾選取面板")              # 呼叫發出，無條件，先於呼叫
    嘗試:
        路徑 <- 選取器.取得資料夾路徑()
    捕捉 任何例外 e:
        記錄("面板不可用", 等級=失敗, 錯誤=e)   # 結果（失敗）
        回傳 面板不可用(原因=e 的訊息)          # 與日誌並存，不互抵
    若 路徑 為空:
        記錄("使用者取消選取")                 # 結果（提早返回仍有結果事件）
        回傳 取消()                            # 不觸碰儲存層
    記錄("已選取: " + 路徑)

    記錄("準備持久化, key=" + KEY + ", path=" + 路徑)   # 呼叫發出（呼叫點 2）
    嘗試:
        管道 <- 偏好設定.開啟()
        記錄("偏好設定儲存已就緒")                       # 受理（僅此外部系統有此時刻）
        成功 <- 管道.寫入(KEY, 路徑)
    捕捉 任何例外 e:
        記錄("持久化失敗（例外）", 等級=失敗, 錯誤=e)
        回傳 已選定但未記住(狀態=探測(路徑), 原因=e 的訊息)
    若 非 成功:
        記錄("持久化失敗：寫入回報 false", 等級=失敗)
        回傳 已選定但未記住(狀態=探測(路徑), 原因=...)
    記錄("已持久化")
    回傳 已選定且已記住(狀態=探測(路徑))
```

三條在實作時最容易被抹掉的不變量：

1. **發出事件先於呼叫敘述**，不可挪到 `try` 之後或與結果事件合併——挪動後「外部系統整個拔掉」
   時就再也看不到呼叫已發出。
2. **失敗分支的日誌與回傳各自獨立成句**，禁止寫成「回傳一個內含訊息的錯誤物件並期待上層記錄」。
3. **兩條「未記住」分支都攜帶 `探測(路徑)` 的結果**，不得因為寫入失敗就回傳降級狀態——本次工作
   階段仍然可用（Phase 1 裁決 A 第 4 點，由 G2-4 的跨案例等價斷言鎖定）。

`restore()` 與 `_inspect()` 的骨架同構，差異僅在結果分支對應 `WorkspaceState` 而非選取結局，
不另列虛擬碼。

#### 3a.5 資料結構與檔案配置決定

| 項目 | 決定 | 理由 |
|------|------|------|
| 接縫型別與 `ChooseFolderResult` 的檔案位置 | 與 `WorkspaceState` 同置於 `lib/workspace/workspace_repository.dart` | 該檔現為 96 行，加上 4 個結局 variant 與 5 個接縫後預估約 250 行，仍低於 300 行門檻；另置新檔會使本票 `where.files` 擴張，而 `ticket track commit` 以 where.files 為子集約束 |
| `implements WorkspaceRepository` 的替身相容性 | 接縫一律走**建構子具名參數 + 私有欄位**，不新增公開方法 | 私有成員不構成 `implements` 的實作義務，`integration_test` 與 widget 測試的 stub 不需跟進接縫（已於 Phase 2 §2.5 確認，本階段複核成立） |
| 記錄器與行為替身 | 維持 Phase 2 紀律，**兩組獨立類別**，不合併為可配置萬用 fake | 合併後「記錄器不模擬行為」只剩註解，程式碼層無從辨識；且萬用 fake 會誘使呼叫發出測試對中性回傳做結局斷言 |
| 日誌斷言比對方式 | 穩定子字串（key 名、路徑值）+ 索引順序 | 逐字綁定完整中文句子會使文案微調即紅燈 |

#### 3a.6 技術債務與已知限制（交 Phase 4）

| 項目 | 說明 | Phase 4 方向 |
|------|------|-------------|
| `WorkspaceRepository` 單檔承載 3 個概念群 | 狀態型別、結局型別、接縫定義、adapter 實作同置一檔 | 若後續再增 port（如 W1-014 的版號遷移）使該檔逾 300 行，依 domain 邊界拆為 `workspace_ports.dart` + `workspace_repository.dart` |
| 日誌訊息為中文字面且分散於方法本體 | 六個落點各自硬編字串，測試以子字串比對 | 評估是否收斂為具名常數，使測試改綁常數而非字面 |
| file_selector 受理時刻結構性缺失 | Phase 1 裁決 C 判定 App 端無載體 | 非本專案可修，已歸屬 `0.1.0-W3-088` 平台端事實軌 |
| `_inspect` 的兩個 dart:io 呼叫改走接縫後，生產路徑多一層間接 | 預設實作為薄包裝 | 確認無效能或行為差異即可，無需處理 |

#### 3a.7 交接 Phase 3b（parsley-flutter-developer）的執行約束

- 四批**循序**派發，前一批的驗收指令綠燈為後一批派發前置；禁止並行（四批中三批觸及
  `lib/workspace/workspace_repository.dart` 或 `lib/main.dart`）。
- 每批 prompt 只引用對應章節：A 引 Phase 1 §3 與 Phase 2 §2.0／§2.5；B 引 Phase 2 §2.2～§2.4；
  C 引 Phase 1 §2 與本節 3a.4；D 引 Phase 1 裁決 A「呼叫端義務」與 Phase 2 G7。
- 測試與其結果為後續步驟前置的命令一律前景執行並加 `| tail`。
- **B 批禁止**修改任何 `lib/` 檔案使測試轉綠；紅燈是該批的正確產出。

#### 3a.8 需 PM 處置的兩項（不自行變更票面）

1. `where.files` 缺兩項實際會被修改的檔案：`lib/l10n/app_localizations*.dart`（arb 變更後的生成
   產物）與 `test/unit/app/home_page_workspace_test.dart`（G7 的新建 widget 測試檔，Phase 2 §2.1
   將位置決定權留給 Phase 3a，本階段裁定放此路徑，理由：`test/unit/app/` 已存在且承載
   `global_error_handling_test.dart`、`router_test.dart` 等 App 層測試）。`ticket track commit` 以
   where.files 為子集約束，未補會使 3b 無法提交。
2. 本票 Phase 3b 將產生四次派發，非單次。
### Phase 3b-A 執行紀錄：型別與接縫遷移（零行為變更）

依 Phase 3a §3a.2/§3a.3 執行，本批只做型別與接縫遷移，不新增任何行為。

**變更檔案**：`lib/workspace/workspace_repository.dart`、`lib/main.dart`、
`integration_test/app_test.dart`、`lib/l10n/app_zh.arb`、`lib/l10n/app_en.arb`、
`lib/l10n/app_localizations*.dart`（生成物）。

**做了什麼**：
1. 新增 `ChooseFolderResult` sealed class 四 variant（`ChooseFolderCancelled`／
   `ChooseFolderUnavailable`／`ChooseFolderSelected`／`ChooseFolderNotRemembered`）。
2. 新增五個接縫型別：`DirectoryPathPicker`、`WorkspacePreferencesPort`／
   `WorkspacePreferencesHandle`（受理載體與讀寫分離兩層）、
   `WorkspaceDirectoryProbePort`、`WorkspaceLogSink`；各自的生產預設實作
   （`_DefaultWorkspacePreferencesPort`／`Handle`、`_DefaultWorkspaceDirectoryProbePort`、
   `_defaultLogSink`）。
3. `WorkspaceRepository` 建構子改為具名可選參數，未提供時全部走預設值，
   生產呼叫點（`WorkspaceRepository()`）簽章不變。
4. `chooseFolder()` 簽章改為 `Future<ChooseFolderResult>`；本批只回
   `ChooseFolderCancelled` 與 `ChooseFolderSelected` 兩個 variant，
   `ChooseFolderUnavailable`／`ChooseFolderNotRemembered` 定義但無人產出
   （C 批接手產出）。
5. `restore()` 改用 `_preferencesPort`／handle，簽章不變、行為等價。
6. `_inspect()` 改用 `_directoryProbe.exists`／`readFirstEntry`、`_log` 取代
   `Directory` 與 `developer.log` 直接呼叫；既有 `FileSystemException` 分支
   的日誌內容與行為完全不變，只是改走接縫。
7. `main.dart` 的 `_chooseFolder()` 改為 exhaustive switch 四分支；
   `NotRemembered`／`Unavailable` 兩分支本批只更新 state（`Unavailable` 分支
   為 no-op break，因本批不會產出此 variant），使用者提示留待 3b-D
   （decision-trigger-binding 規則 1 狀態 b，trigger 為 3b-D 批次）。
8. `integration_test/app_test.dart` 的 `_StubWorkspaceRepository.chooseFolder()`
   同步改回 `ChooseFolderSelected(_state)`。
9. `lib/l10n/app_zh.arb`／`app_en.arb` 新增 `workspaceNotRemembered` key
   （C/D 批準備用），並執行 `fvm flutter gen-l10n` 重新產生
   `app_localizations*.dart`。

**驗收**：`fvm flutter analyze | tail -20` 零錯誤；`fvm flutter test | tail -20`
全數 1175 個測試通過，零回歸（本批未新增任何測試檔）。

**下一批**：3b-B（RED 批，建 `test/unit/workspace/workspace_repository_test.dart`
與 `test/unit/app/home_page_workspace_test.dart`，禁改 `lib/`）。
### Phase 3b-B 執行紀錄：RED 批（測試裝置與 29 個場景）

依 Phase 2 §2.1-2.4 與 Phase 3a 3b-B 批次列執行。本批不改任何 `lib/` 檔案。

**新建檔案**：
- `test/unit/workspace/workspace_repository_test.dart`（G1-G6，25 場景）
- `test/unit/app/home_page_workspace_test.dart`（G7，4 場景）

**裝置設計**（Phase 2 §2.2 紀律 1，記錄器與行為替身為兩組獨立類別）：
- 記錄器：`_PickerRecorder`／`_PreferencesRecorder`+`_HandleRecorder`／
  `_DirectoryProbeRecorder`／`_LogRecorder`。只累積 `(方法名, 參數)` 或
  `(訊息, 等級, 錯誤)`，回中性值；斷言只針對記錄內容與日誌索引順序，未對
  記錄器的中性回傳做任何結局斷言。
- 行為替身：`_FakePicker`／`_FakePreferencesPort`+`_FakePreferencesHandle`／
  `_FakeDirectoryProbe`。獨立於記錄器之外的另一組類別，只用於受理／結果
  測試，結局透過建構子參數配置（成功／回傳值／拋例外）。
- G7 用 `_StubWorkspaceRepository extends WorkspaceRepository`，覆寫
  `restore()`／`chooseFolder()` 兩個公開方法，固定回傳指定結局；建構子接縫
  走預設值但實際不會被呼叫到（兩方法皆已覆寫）。

**關鍵斷言設計**：
- G2-4：跨案例等價斷言，比較 G2-1（`writeString` 回 true）與 G2-2（回
  false）兩次獨立呼叫的 `state.runtimeType` 與 `path`，而非單案例驗
  `state` 非 null。
- G2-1／G3-1／G4-1：日誌斷言以 `_LogRecorder.indexOfSubstring()` 取得索引
  後比較大小，驗證發出順序，非只驗存在。
- G5（不互相抵扣，橫切 G2-2／G2-3／G2-6／G3-2／G6-2）：每個失敗分支皆寫成
  兩行獨立 `expect`——一行驗回傳型別／欄位，一行驗 `log.entries` 含
  `level == 900` 的項目，未包成單一 helper。
- G7-2／G7-4：斷言使用 `AppLocalizations.of(context)` 取字串（非硬編中文
  字面），符合 D3／文案微調不紅燈的要求。

**驗收**：
- `fvm flutter analyze | tail -20`：零錯誤（兩新檔皆可編譯）。
- `fvm flutter test | tail -20`：`+1184 -20`。
  - 既有 1175 個測試維持全綠（1184 - 9 = 1175，零回歸）。
  - 兩新檔共 29 個場景：9 個綠燈（編譯期即成立的中性斷言，例如
    G2-5／G2-6 的取消／不可用路徑、G6 系列在現有 `_inspect` 邏輯下已正確、
    G7-1／G7-3 的 Selected／Cancelled 分支）、20 個紅燈——18 個在
    `workspace_repository_test.dart`（G1 全數 8 個、G2 的 G2-1/2/3/4、
    G3 的 G3-1/2/3、G4 全數 3 個；因 A 批未新增任何 `developer.log` 或
    `setString`／`getInstance` 例外處理，這些場景預期落空）、2 個在
    `home_page_workspace_test.dart`（G7-2 的「下次啟動需重新選擇」提示、
    G7-4 的 `SnackBar` 錯誤提示；因提示留待 3b-D，符合預期）。

**下一批**：3b-C（GREEN 批一，adapter 三時刻回饋實作，僅改
`lib/workspace/workspace_repository.dart`，目標 G1-G6 共 25 場景全綠）。
### Phase 3b-C 執行紀錄：GREEN 批一（adapter 三時刻回饋實作）

依 Phase 1 §2 三個呼叫點的三時刻落點表與 Phase 3a §3a.4 骨架實作。本批只改
`lib/workspace/workspace_repository.dart`，未動 `lib/main.dart`（G7 屬 D 批）
與任何測試檔。

**做了什麼**：
1. `chooseFolder()`：`_pickDirectoryPath()` 前一律 `_log('開啟資料夾選取
   面板')`（呼叫發出，無條件、先於 try），例外時 `_log('面板不可用',
   level:900, error:e)` 並回 `ChooseFolderUnavailable`；取消時 `_log('使用者
   取消選取')` 並回 `ChooseFolderCancelled`。取得路徑後 `_log('已選取:
   $path')`，再發出 `_log('準備持久化，key=..., path=...')`；`open()`
   成功後 `_log('偏好設定儲存已就緒')`（受理）；`writeString` 回 false 或
   拋例外皆走 `_log(..., level:900)` + 回傳 `ChooseFolderNotRemembered(
   state: await _inspect(path), reason: ...)`——兩條「未記住」分支都攜帶
   `_inspect(path)` 的結果而非取代（3a.4 不變量 3，G2-4 鎖定）；成功則
   `_log('已持久化')` 並回 `ChooseFolderSelected(await _inspect(path))`。
2. `restore()`：進入即 `_log('還原工作資料夾，key=...')`（無條件），
   `open()` 拋例外時 `_log('還原失敗（例外）', level:900)` 並回
   `WorkspaceUnavailable(lastKnownPath: null, reason: '$e')`；成功則
   `_log('偏好設定儲存已就緒')`，`readString` 回 null 時 `_log('無已儲存
   路徑')` 並回 `WorkspaceUnset`，取得路徑則 `_log('已還原: $path')` 並轉
   `_inspect`。
3. `_inspect()`：`exists()` 前補 `_log('探測資料夾是否存在: $path')`，
   `readFirstEntry()` 前補 `_log('讀取資料夾內容: $path')`；既有
   `FileSystemException` 分支的 level 900 日誌與 reason 邏輯不變。

**不變量落實**（3a.4）：發出事件一律位於呼叫敘述之前、不與結果事件合併；
每個失敗分支的日誌與回傳各自獨立成句（無「回傳內含訊息物件並期待上層
記錄」的寫法）；兩條「未記住」分支都攜帶 `_inspect(path)` 結果，工作階段
不因寫入失敗而降級。

**驗收**：
- `fvm flutter analyze | tail -20`：零錯誤。
- `fvm flutter test test/unit/workspace/ | tail -20`：G1-G6 共 25 個場景
  全綠（`+25`）。
- `fvm flutter test | tail -20`：`+1202 -2`。既有 1175 + 3b-B 已綠的 9 個
  + 本批新轉綠的 18 個 = 1202；剩餘 2 個紅燈皆為
  `test/unit/app/home_page_workspace_test.dart` 的 G7-2（「下次啟動需重新
  選擇」提示）與 G7-4（`SnackBar` 錯誤提示），符合預期——提示留待 3b-D。

**Acceptance 判定**：
- 第 1 條（三處呼叫發出日誌含提早返回路徑）：已滿足，勾選。
- 第 3 條（每個失敗分支同時具備日誌與可消費回傳，不互相抵扣）：已滿足
  （G5 橫切斷言全綠），勾選。
- 第 5 條（`_inspect()` 兩處呼叫發出事件與 `exists()` 結果時刻記錄）：已
  滿足，勾選。
- 第 2 條（`setString` 回傳型別／main.dart 呼叫端提示）與第 4 條（測試檔
  建立完成，但需 D 批 G7 全綠 + 全量零紅燈才算完整達成）留給 3b-D 完成
  後由 D 批或全量驗證判定。

**下一批**：3b-D（GREEN 批二，UI 消費端回饋實作，只改 `lib/main.dart`，
目標 G7 共 4 場景全綠，再跑全量 `fvm flutter test` 與
`integration_test`）。
### Phase 3b-D 執行紀錄：GREEN 批二（UI 消費端回饋，最後一批）

依 Phase 1 裁決 A「呼叫端義務」與 Phase 2 G7 實作。本批只改 `lib/main.dart`
與兩份 arb（+ 重新產生的 `app_localizations*.dart`），未再動
`lib/workspace/workspace_repository.dart` 或任何測試檔。

**做了什麼**：
1. `_HomePageState` 新增 `_showNotRememberedNotice`（bool）欄位，落實
   Phase 1 裁決 A 對呼叫端施加的義務——`ChooseFolderNotRemembered` 不得只
   記 log，必須有使用者可見提示，且與日誌分別成立（不互相抵扣）。
2. `_chooseFolder()` 四分支：
   - `ChooseFolderCancelled`：無動作（狀態與提示皆不變）。
   - `ChooseFolderUnavailable`：`ScaffoldMessenger.of(context).showSnackBar`
     顯示固定文案 `l10n.chooseFolderUnavailableMessage`，**不顯示
     `result.reason`**——見下方「陷阱與處置」。
   - `ChooseFolderSelected`：更新 `_workspace`，同時清除
     `_showNotRememberedNotice`（避免舊提示殘留到下次成功選取）。
   - `ChooseFolderNotRemembered`：更新 `_workspace = state`（state 已由
     C 批的 `_inspect(path)` 攜帶，非取代）並設
     `_showNotRememberedNotice = true`。
3. `build()` 在 `_WorkspaceBanner` 之下新增條件渲染的 `AppText`，內容為
   `l10n.workspaceNotRemembered`，僅在 `_showNotRememberedNotice` 為
   true 時顯示。
4. `lib/l10n/app_zh.arb`／`app_en.arb` 新增 `chooseFolderUnavailableMessage`
   key（zh：「無法開啟資料夾選取面板，請稍後再試」／en 對應譯文），並
   重新執行 `fvm flutter gen-l10n`。

**陷阱與處置（PM 提醒）**：`WorkspaceUnavailable.reason` 與
`ChooseFolderUnavailable.reason` 的內容不同質——`_inspect()` 放中文固定
文案或 `osError.message`，C 批 `restore()` 失敗分支放的是 `'$e'`（例外
字串插值）。若把 `reason` 直接丟進 UI，`restore()` 路徑會讓例外字串
（如 `Exception: 儲存層失效`）出現在畫面上。本批**未在任何 widget 使用
`result.reason` 或 `state` 的 `reason` 欄位**——`ChooseFolderUnavailable`
分支的 SnackBar 文案是固定 ARB key，`WorkspaceUnavailable` 分支的畫面
文字沿用既有 `l10n.workspaceUnavailable(reason)`（`_WorkspaceBanner` 既有
邏輯，C 批未變更其呼叫；該分支的 `reason` 目前仍可能含 `'$e'`——此為
既有行為非本批引入，已列入下方技術債務）。

**驗收**：
- `fvm flutter analyze | tail -20`：零錯誤。
- `fvm flutter test test/unit/app/home_page_workspace_test.dart`：G7 四個
  場景全綠。
- `fvm flutter test | tail -20`：**全綠 1204**（1175 既有 + 29 新增，零
  回歸、零殘留紅燈）。
- `fvm flutter test integration_test/ -d macos`：12/12 全綠（含既有工作
  資料夾狀態三情境、多語系、scan_notifier 原生端接線）。

**Acceptance 判定（全量綠燈後複查）**：
- 第 1 條：仍成立（C 批已勾）。
- 第 2 條：本批完成，`setString` 回傳值已檢查（C 批）且 `chooseFolder`
  回傳型別可區分「已持久化」與「未能記住」（A 批型別 + C 批產出邏輯 +
  D 批呼叫端消費），型別決定與理由已寫入本 Solution 各批段落，勾選。
- 第 3 條：仍成立（C 批已勾）。
- 第 4 條：`workspace_repository_test.dart` 已建立（B 批），G1-G6 全綠
  （C 批），受理時刻不適用理由已在 Phase 1 裁決 C 明寫，勾選。
- 第 5 條：仍成立（C 批已勾）。

**技術債務（交 Phase 4，3a §3a.6 四項現況）**：
1. `WorkspaceRepository` 單檔承載狀態型別／結局型別／接縫定義／adapter
   實作，現況約 250+ 行——**已在實作中處理**：本次四批循序完成後檔案
   行數在 3a 預估範圍內，未逾 300 行門檻，暫不拆分，留待下次新增 port
   （如 W1-014 版號遷移）時重新評估。
2. 日誌訊息為中文字面且分散於方法本體——**仍在**：六個以上落點各自
   硬編字串，測試以子字串比對；是否收斂為具名常數未處理，Phase 4 可
   評估。
3. file_selector 受理時刻結構性缺失——**不可修**：非本專案可修的平台
   限制，已歸屬 `0.1.0-W3-088` 平台端事實軌，非技術債務。
4. `_inspect` 的 dart:io 呼叫改走接縫後生產路徑多一層間接——**已確認
   無影響**：C 批實作後未觀察到行為或效能差異，3a 已預先判定「無需
   處理」。
5. **本批新增一項**：`restore()` 失敗分支的 `WorkspaceUnavailable.reason`
   使用 `'$e'`（例外字串插值），與 `_inspect()` 既有的中文固定文案／
   `osError.message` 不同質；`_WorkspaceBanner` 對 `WorkspaceUnavailable`
   狀態顯示 `l10n.workspaceUnavailable(reason)` 時，走 `restore()` 例外
   路徑會讓例外字串直接出現在畫面上。範圍超出本票（`_WorkspaceBanner`
   邏輯屬既有程式碼，C 批只新增了會產生此值的呼叫路徑），建議 Phase 4
   或另開 IMP 統一 `reason` 語意（區分「診斷用」與「使用者可見」兩個
   欄位，或 `restore()` 失敗分支改用固定文案）。

**下一步**：Phase 4 重構評估由 PM 決定如何進行；本票不 complete。
### Phase 3b-E 執行紀錄：修正批（C 批引入的使用者可見缺陷，即時修正，非延後項）

PM 實查 `lib/main.dart:310`（`l10n.workspaceUnavailable(reason)` 原樣渲染
`reason`），指出 3b-D 回報的「技術債第 5 項」歸屬判斷有誤：餵給
`_WorkspaceBanner` 的原始例外字串是 **3b-C 新增**的
`restore()` 例外分支 `reason: '$e'`，`_WorkspaceBanner` 本身是既有顯示邏輯
沒有錯。依 `quality-baseline` 規則 3（設計問題立即修正），這是本票 C 批
引入的使用者可見缺陷，不得帶著它收尾——本批當場修正，撤回原先列為
Phase 4 技術債的判斷。

**做了什麼**：
1. `lib/workspace/workspace_repository.dart` 的 `restore()` catch 分支：
   `reason` 從 `'$e'`（例外字串插值）改為固定文案「無法讀取已儲存的工作
   資料夾設定」，與 `_inspect()` 既有的中文固定文案同形態；`_log('還原
   失敗（例外）', level: 900, error: e)` 維持不變，原始例外仍完整進日誌。
   落地「不互相抵扣」：日誌拿例外細節（診斷用），`reason`（可能外露給
   使用者）拿穩定文案，兩者分別完整、互不取代。
2. 依 PM 判斷「硬編中文與 `_inspect()` 現況一致」，未新增 arb key（維持
   `_inspect()` 既有慣例，不擴大範圍）。
3. 新增測試（PM 授權，未修改任何既有測試）：
   `test/unit/workspace/workspace_repository_test.dart` 的 G3-2b——鎖定
   `restore()` 例外路徑的 `reason` 不含例外型別名（`Exception`／`Error`
   字樣）也不含原始例外訊息文字，防止此缺陷回歸。既有 G1-G6、G7 全數
   測試檔未變更（PM 已查核無任何既有斷言釘死原始例外字串）。

**驗收**：
- `fvm flutter analyze | tail -20`：零錯誤。
- `fvm flutter test | tail -20`：**全綠 1205**（1204 + 新增回歸測試 1 條）。

**技術債清單更新**（3a §3a.6 四項 + 3b-D 新增第 5 項）：
1. `WorkspaceRepository` 單檔承載狀態型別／結局型別／接縫定義／adapter
   實作——已在實作中處理，未逾 300 行門檻，維持。
2. 日誌訊息為中文字面且分散於方法本體——仍在，Phase 4 可評估收斂為
   具名常數，維持。
3. file_selector 受理時刻結構性缺失——不可修，已歸屬
   `0.1.0-W3-088`，維持。
4. `_inspect` 接縫化後多一層間接——已確認無影響，維持。
5. **原「`restore()` reason 例外字串外露」——已於 3b-E 修正，非技術債**。
   這是修正 3b-C 引入的使用者可見缺陷，不是留給 Phase 4 的項目；從技術
   債清單移除。

**下一步**：Phase 4 重構評估由 PM 決定如何進行；本票仍不 complete。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
四批循序驗收數字：

| 批次 | analyze | test 全量 | 備註 |
|------|---------|-----------|------|
| 3b-A（型別與接縫遷移） | 零錯誤 | +1175（既有，零回歸，未新增測試） | 只回 Cancelled/Selected 兩 variant |
| 3b-B（RED 批） | 零錯誤 | +1184 -20（既有 1175 全綠 + 9 新綠 + 20 預期紅） | 新建 29 場景（G1-G7） |
| 3b-C（GREEN 一：adapter） | 零錯誤 | +1202 -2（剩 G7-2/G7-4 紅） | G1-G6 共 25 場景全綠 |
| 3b-D（GREEN 二：UI 消費端） | 零錯誤 | **+1204 全綠**（1175 既有 + 29 新增） | G7 共 4 場景全綠；`integration_test/` 12/12 全綠 |

最終 commit：3b-D `lib/main.dart` + `lib/l10n/app_zh.arb`／`app_en.arb`／
`app_localizations*.dart`。四批 commit SHA 依序見本票 git 歷史（各批
append-log 段落已各自記錄批內驗收）。

3b-E（修正批）驗收：analyze 零錯誤；`fvm flutter test | tail -20` **全綠 1205**
（3b-D 的 1204 + 新增回歸測試 G3-2b 共 1 條）。五批合計最終數字：**1205 全綠**。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-08 19:10)
  - what: 補齊 _inspect() 內 dart:io 呼叫的 Port 回饋契約
  - why: _inspect() 的 Directory.exists 與 dir.list().first 是 WorkspaceRepository 的第四個 driven port 呼叫點，依 INV-PORT-OBSERVE-001 缺「呼叫發出」事件（現況只有 FileSystemException 分支有日誌，屬結果時刻的失敗分支）。0.1.0-W3-121 的 why 明列範圍為三個呼叫點，故不擴張。請 PM 裁決是否另開 IMP 補齊，或併入 0.1.0-W3-088 的檔案系統事實軌評估。
  - suggested_type: IMP
  - suggested_priority: P2
  - related_files: lib/workspace/workspace_repository.dart
  - context: 
  - status: dismissed（併入本票，不另開票：_inspect() 的 dart:io 呼叫與既有三處同屬 WorkspaceRepository 的 driven port 回饋契約合規，拆票會讓兩個代理人並行改同一檔案且型別風格分歧。本票 why 原列三處是 PM 盤點時的遺漏，補進去是修正遺漏而非擴張範圍。W3-088 是平台端事實軌（實機驗證），與本票的 App 端邏輯軌不同維度，不併入該票。）


## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-014,0.1.0-W3-088,0.1.0-W3-114 | chars: 1948 -->

### Task Reference
- 0.1.0-W3-114 what: 新增 clean-architecture-implementation-methodology.md Port 回饋契約 section（來源已完成，結論見 Solution）
- 0.1.0-W1-014 what: 定義 shared_preferences 的 key 版本化與遷移策略
- 0.1.0-W3-088 what: 評估是否需為 WorkspaceRepository 與 gap_report_screen._openItem 建實機驗證票並回填平台端事實（比照 0.1.0-W3-078 的模式）

### Rationale Chain
- 0.1.0-W3-114 why: 用戶裁示（2026-09-08）：回饋契約的三層結構要應用到 Clean Architecture 的六角結構，**不只輸入端**——服務往外呼叫（開檔案、開資料庫、打 API）也適用同一原則，也都應列入測試。

`0.1.0-W3-107` 據此完成設計並裁定載體歸屬為**擴充本檔**，理由：非 UI 層的實作者在設計 port 時會讀這份方法論，回饋契約放這裡才會在正確時機被讀到。元件庫方法論不搬移任何內容（其三個子節標題已被 skill 與範本逐字引用為跨檔錨點，搬移會斷鏈），只在〈掛載點〉加一行架構層引用——那一行由 `0.1.0-W3-115` 處理。

現況：本檔對「回饋／adapter／port」三詞**零命中**（PM 實查）。它有依賴方向、介面契約、業務邏輯位置的檢查清單，但沒有「這個 port 的呼叫有沒有可觀測的三個時刻」。

`observability-rules` 是從日誌側寫的（catch 要有日誌、統一工具、全域攔截、關鍵流程三階段），回答「發生了要記下來」；不回答「哪些時刻**必須**是可觀測的回饋點」。兩者互補但目前沒有接線。

用戶另指出的時序約束是本節存在的核心理由：**log 與 APM 追蹤不是事後可補的能力**，它們消費的是設計期就要決定的回饋點。沒有預先定義回饋點，追蹤工具能拿到的只有例外堆疊，拿不到「呼叫發出了、外部系統收到了、結果是什麼」這三個時刻。（來源已完成，結論見 Solution）
- 0.1.0-W1-014 why: 本專案唯一的持久化是 shared_preferences（pubspec.yaml:20），存的是使用者選定的專案路徑字串
（PROP-001 定案：security-scoped bookmark 已移除，改存路徑）。無 schema 檔，直覺會把資料庫維度
判成 N/A。但 foundation-design 判的是產物不是維度：權威的 migration 版本化紀律投影到 key-value
形態，就是「key 名或值格式變更時的遷移」。
難在失效是靜默的：0.1.x 若改了 key 名或把單一路徑改成清單結構，既有使用者開啟 App 會看到
空的專案清單，不會有錯誤、不會有紅燈，使用者只會以為資料掉了。桌面 App 沒有伺服器端可以
比對，發現不了就是發現不了。
- 0.1.0-W3-088 why: 0.1.0-W3-081 盤點跨邊界驗證形態時發現：MacosScanNotifier 已有 0.1.0-W3-078 承接實機驗證，但 WorkspaceRepository 與 gap_report_screen 的平台端事實同樣零覆蓋且無對應承接票（見 docs/tech-decisions.md「2026-09-08：跨邊界驗證形態定案」補記段）

### Related Files
- .claude/methodologies/clean-architecture-implementation-methodology.md  # from 0.1.0-W3-114（來源已完成，結論見 Solution）
- lib/main.dart  # from 0.1.0-W1-014
- pubspec.yaml  # from 0.1.0-W1-014
- docs/tech-decisions.md  # from 0.1.0-W1-014
- lib/screens/gap_report/gap_report_screen.dart  # from 0.1.0-W3-088

### Source Acceptance (reference)
- [ref] [x] clean-architecture 方法論含 Port 回饋契約 section：輸出端三層定義、INV-PORT-OBSERVE-001 與其判準問句、三個消費者的投影關係、時序約束、測試義務  # from 0.1.0-W3-114（來源已完成，結論見 Solution）
- [ref] [x] 既有〈介面契約〉檢查清單補一項指向新 section，寫觸發時機而非泛稱連結  # from 0.1.0-W3-114（來源已完成，結論見 Solution）
- [ref] [x] 全檔為型別化描述，無本專案類別名或情節；不以層數／消費者數命名章節或作跨檔錨點  # from 0.1.0-W3-114（來源已完成，結論見 Solution）
- [ref] [x] 新 section 的確切標題字串已記入 Completion Info（0.1.0-W3-115 要從三處引用）  # from 0.1.0-W3-114（來源已完成，結論見 Solution）
- [ref] [ ] 存在 schema 版號常數與其讀取比對邏輯；至少一個測試以舊版格式的假資料驗證遷移後結果正確；tech-decisions 記錄 key 命名與版號規則  # from 0.1.0-W1-014
- [ref] [ ] 遷移回傳型別能區分「已遷移」「無需遷移」「遷移失敗」；版號讀取失敗與首次啟動是否需區分、遷移失敗是否降級，兩項決定已寫入 tech-decisions  # from 0.1.0-W1-014
- [ref] [ ] 評估是否需建對應實機驗證票；若需要則建票並回填 spawned_tickets，若不需要則說明理由  # from 0.1.0-W3-088
  ... (truncated, see source ticket)
