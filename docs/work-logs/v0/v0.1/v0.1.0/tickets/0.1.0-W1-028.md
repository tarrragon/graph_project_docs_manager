---
id: 0.1.0-W1-028
title: '[ADJ] HomePage 於路由殼掛載後成為無引用死碼'
type: ADJ
status: pending
version: 0.1.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.1-W1-005
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W1-002 合併後 PM 覆核引用鏈
  final_decision: 建 ADJ 追蹤，不當下刪除
  rationale: 可能含 W1-005 元件庫可回收的素材；先刪再重寫的成本高於延後裁決
who:
  current: parsley-flutter-developer
  history: {}
what: 裁決 HomePage 及其助手 widget 是移除、或改造為某個佔位頁的內容，並執行 lib/main.dart 的 HomePage 及其助手 widget
when: W1-005 元件庫定案後——該票會決定這些 widget 的形態是否有可回收部分
where:
  layer: Presentation
  files:
  - lib/main.dart
  - test/unit/app/home_page_workspace_test.dart
why: W1-002 把 app 進入點自 HomePage 改為 AppShell 後，HomePage 不再被任何程式碼實例化。實測其剩餘引用：integration_test 一處為解釋錨點移動的註解、lib/main.dart 五處全為它自身的定義（class / constructor / State / createState / pageKey），無任何外部呼叫端。連同 _DocumentList、_StatCard 等助手 widget 約 200 行成為無引用碼。Dart 不對未使用的 public class 發出警告，fvm flutter analyze 實測 0 issues——此死碼沒有任何工具訊號，只能靠人查。
how:
  task_type: Implementation
  strategy: 先判斷可回收性：HomePage 現有的工作資料夾狀態渲染（未選取／已授權／授權失效三態）與 SPEC-001 §1 的部分狀態重疊，可能是 W1-005 元件庫的素材而非純廢棄物。裁決在 W1-005 之後做，避免先刪再重寫。無論裁決為何，須在 Solution 寫出理由——「留著以後可能有用」不算理由，那是無 trigger 延後。
acceptance:
- '[ ] HomePage 及其助手 widget 的去留已裁決並執行，且 lib/main.dart 無任何無引用的 widget 定義'
tdd_phase: phase2
tdd_stage:
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-08-28'
updated: '2026-08-28'
protocol_version: '2.0'
scope_blocker: 0.1.0 契約：main.dart HomePage 殘留與六畫面殼衝突，須裁決移除或改造
---

# Execution Log

## Task Summary

裁決 HomePage 及其助手 widget 是移除、或改造為某個佔位頁的內容，並執行 lib/main.dart 的 HomePage 及其助手 widget

---

## Problem Analysis

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-23 12:30] compare how with SPEC before implementing：how 欄凍結於建票時，SPEC 之後仍有演進；以 SPEC 現行版為準，差異寫入 Problem Analysis。收尾用 finish 不用 complete。｜readiness=0 validate=2

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
### PM 裁決：移除（2026-09-23）

**裁決**：刪除 `lib/main.dart` 的 `HomePage`、`_HomePageState`、`_WorkspaceBanner`、`_StatsRow`、`_StatCard`、`_DocumentList`，以及只驗證這些死碼的 `test/unit/app/home_page_workspace_test.dart`。

**依據**：
1. how 欄的回收前提是「W1-005 之後再判斷」。W1-005 已 completed，SPEC-001 §1 的狀態已由元件庫（SPEC-004）與六畫面殼承接，HomePage 的三態渲染沒有回收對象。
2. 唯一有行為價值的是 `_chooseFolder()` 對 `ChooseFolderResult` 四變體的 UI 處理。它在正式路徑上對應的是專案切換器的「選擇其他資料夾」（`action-switcher-choose-folder`），該按鈕目前只 dismiss 浮層、未接 `WorkspaceRepository`，且查無承接票。接線另建 0.1.1 票，HomePage 的實作留在 git 歷史中供參考。`WorkspaceRepository` 本身的測試在 `test/unit/workspace/workspace_repository_test.dart`，不受影響。
3. 「留著以後可能有用」不是理由（how 欄已明文排除）。

**ARB key 處置**：只被 HomePage 使用、且不在 SPEC-003／SPEC-004 key 表中的 key 一併移除；有規格承諾或有 pending 票引用的保留（例：`chooseFolderUnavailableMessage` 被 `0.1.0-W3-248`／`0.1.0-W3-368` 引用）。逐 key 判定結果記入 Solution。

**連帶**：`0.1.0-W3-177`（HomePage 直呼原生 SnackBar）的對象隨本票消失，本票完成後以 superseded_by 關閉。

（本段以 --force 在 pending 狀態寫入：PM 裁決須在派發前落票，供執行者讀取。）

### PM 裁決修正：ARB key 不在本票刪除（2026-09-23）

撤回上段「ARB key 處置」。原因：同批並行的 `0.1.0-W3-382` 改同一組 arb 與生成檔，兩分支並行刪改會在生成檔產生衝突。本票範圍收窄為 `lib/main.dart` 與 `test/unit/app/home_page_workspace_test.dart`。執行者在 Solution 列出「移除 HomePage 後已無程式碼引用的 ARB key」清單（只列、不刪），由 PM 另建 0.1.1 清理票。

（--force 理由同上段：派發前落票。）
## Test Results

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending
