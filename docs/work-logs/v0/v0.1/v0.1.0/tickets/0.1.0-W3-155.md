---
id: 0.1.0-W3-155
title: 查明並修正 ticket create 四條路徑中三條殘留 create lock，該鎖不被檢查卻在事故排查中充當假訊號
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 並行環境殘留鎖排查中，以建票動作本身重現四條路徑的留鎖行為
  final_decision: ANA 先查明鎖是否有消費端，再決定補釋放或移除機制
  rationale: 四條路徑的留鎖行為已實測（argparse 不留、其餘三條留），但鎖不擋任何建票，故形態可能是假訊號而非未釋放的互斥鎖；兩者修法相反，直接派 IMP 會做錯方向
who:
  current: thyme-python-developer
  history: {}
what: 查明並修正 ticket create 四條路徑中三條殘留 create lock，該鎖不被檢查卻在事故排查中充當假訊號
when: 2026-09-09 排查並行環境殘留鎖時，以建票動作本身重現
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system::read
why: '`.claude/skills/ticket/ticket_system` 的 create 流程取得 `.git/ticket-create-locks/<版本目錄>.ticket-create.lock` 之後，四條退出路徑中有三條未釋放它。鎖為 0 bytes、無持有行程，且**不會擋住任何後續建票**——它的代價不在阻擋，而在事故排查中作為假訊號。


  **四條路徑皆為實測，非推論**：


  - argparse 層拒絕（缺 `--action` / `--target`）：**不留鎖**

  - checklist 層拒絕（`CHECKLIST_VALIDATION_FAILED`，缺 `acceptance` / `when` / `how_strategy`）：**留鎖**

  - 建票成功：**留鎖**

  - `--dry-run`（明示不寫入任何檔案）：**留鎖，且刷新既有鎖的 mtime**


  由此可定位兩件事：鎖的取得在 argparse 之後、checklist 之前；且早於「是否真的要寫入」的判斷——否則 `--dry-run` 不該動到它。


  **為何這條路徑經常被走到**：漏填必填欄是建票的高頻首次錯誤（本次排查中同一人連續命中兩次）。而錯誤訊息完整列出缺失欄位與修正建議，**完全未提及鎖已被取得且未釋放**。


  **實際損害是誤導，不是阻擋**（建票後補測，修正建票當下的錯誤推論）：實驗確認鎖存在時 `ticket create` 照常執行、未被擋住。因此本日約 9 分鐘的建票不可用**不是這把鎖造成的**——同時間另有 `main.lock`（41 bytes）與 `HEAD.lock` 擋住 commit 步驟，那才是阻因。建票當下把兩者歸為同一原因，屬未經實測的推論，已於此更正。


  真正的代價是它在事故排查中的形態：它看起來是一把鎖、位在 `ticket-create-locks/` 目錄下、命名指向建票，於是被當成阻因處理。本日至少兩個 session 各自移除它並將管道恢復歸因於該移除，而管道從未被它擋住。**一個持續產生卻不被檢查的鎖檔，在並行事故中會穩定地把排查者導向錯誤的根因。**


  **待查明**：該鎖是否有任何消費端會讀取它。若無，本缺陷的形態是「產生但無人檢查的假訊號」而非「未釋放的互斥鎖」，修法方向隨之不同——前者可能該移除整個機制，後者該補釋放。相關機制已記錄於 `PC-GPD-013`。'
how:
  task_type: Analysis
  strategy: 先讀 create 流程的鎖取得與釋放位置，列出成功／失敗／例外三條路徑各自的釋放行為；再依結果決定修法（context manager／try-finally／atexit），並補上鎖殘留時的可見訊息。修法涉框架資產，經 sync 擴散至全部 consumer 專案
acceptance:
- '[x] 四條路徑（argparse 拒絕／checklist 拒絕／建票成功／--dry-run）各自的鎖取得與釋放位置已逐條查明，記錄程式碼位置'
- '[x] 已查明是否存在讀取該鎖的消費端；若無，判定為假訊號並據此決定移除機制或補釋放，選擇理由記錄於 Solution'
- '[ ] 修法後四條路徑皆不殘留鎖，測試至少涵蓋 checklist 拒絕與 --dry-run 兩條路徑'
- '[ ] --dry-run 不再取得或刷新任何鎖（明示不落地的路徑不應動到鎖狀態）'
tdd_phase: phase3b
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-09T13:25:25'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

查明並修正 ticket create 四條路徑中三條殘留 create lock，該鎖不被檢查卻在事故排查中充當假訊號

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### PM 建票前實測（原始觀測，供接手者對照；仍請依 PC-162 獨立重驗）

以 `ticket create` 本身作為實驗載體，同一環境連續四次觀測，每次前後以 `ls -l .git/ticket-create-locks/` 讀取鎖狀態：

| # | 觸發 | 命令結果 | 鎖狀態變化 |
|---|------|---------|-----------|
| 1 | 缺 `--action` / `--target` | argparse `error: the following arguments are required` | **無鎖產生** |
| 2 | 缺 `acceptance` / `when` / `how_strategy` | `CHECKLIST_VALIDATION_FAILED` | **產生新鎖**（0 bytes） |
| 3 | 欄位齊備 | `[OK] 已建立 Ticket`（即本票） | **產生新鎖**（0 bytes） |
| 4 | `--dry-run`，且執行前既有一把鎖 | `[DRY-RUN] 將建立 Ticket ...（不落地，未寫入任何檔案）` | **既有鎖 mtime 被刷新** |

第 4 次同時證實兩件事：`--dry-run` 會動到鎖狀態；以及**鎖存在時建票管道並未被擋住**（命令一路推進到產出 dry-run 預覽）。

每次觀測皆確認 `lsof <鎖檔>` 零持有者、`ps` 無 ticket 行程殘留。

**接手者請注意**：以上為建票當下的單一環境觀測，未涵蓋例外路徑（如寫入中途拋錯）、未涵蓋並行呼叫，也未查明是否有任何程式碼讀取該鎖——最後一項是 acceptance 第 2 條的核心，也是決定修法方向的分岔點。

## 重現實驗結果
### 實驗方法

以 `ticket create` 的既有 shim（`ticket`，`.local/bin/ticket`）於 0.1.0-W3-155
的分支上直接觸發四條路徑，每次前後以 `stat -f "size=%z mtime=%Sm"` 讀取
`.git/ticket-create-locks/docs__work-logs__v0__v0.1__v0.1.0__tickets.ticket-create.lock`
的大小與 mtime，並以 `lsof <鎖檔>` 確認事後持有者數。依 PC-162 獨立重驗，
不採信 PM 建票當下的原始數字，僅引用其路徑分類作為重現對照。

路徑 3（建票成功）不重新觸發——重新觸發會產生一張無意義的雜訊 ticket；
改以本票自身的建立事件（`ticket track claim` 前、PM 建票當下）作為該路徑
的既有實測樣本，另輔以程式碼路徑分析交叉確認（見下）。

### 實驗執行

| # | 路徑 | 觸發指令 | mtime 變化 | lsof 事後持有者 |
|---|------|---------|-----------|----------------|
| 1 | argparse 拒絕（缺 `--action`/`--target`） | `ticket create` | **無變化**（13:21:11 → 13:21:11） | 0（本就未取鎖） |
| 1b | argparse 拒絕（未知旗標 `--files`） | `ticket create ... --files test.md` | **無變化**（13:31:08 → 13:31:08） | 0 |
| 2a | Step1 拒絕（缺 `--wave`，`MISSING_WAVE_PARAMETER`） | `ticket create --action ... --target ... --type ANA --version 0.1.0` | **有變化**（13:21:11 → 13:30:51） | 0（釋放後無持有者） |
| 2b | checklist 拒絕（`CHECKLIST_VALIDATION_FAILED`） | 同上 + `--wave 99` | **有變化**（13:30:51 → 13:31:08） | 0 |
| 4 | `--dry-run`（欄位齊備） | 同上 + 全部必填欄位 + `--dry-run` | **有變化**（13:31:08 → 13:32:04） | 0 |

程式碼路徑交叉確認（`.claude/skills/ticket/ticket_system`，唯讀）：

- 鎖取得點：`commands/create.py:874`（`with create_id_allocation_lock(get_tickets_dir(version)):`），
  緊接在版本解析（Step 0）之後，包住 Step 1（ID 分配）～Step 3（持久化/回報）全段。
- 鎖釋放點：`lib/file_lock.py:264-267`（`try: yield` / `finally: lock.release()`），
  屬 Python contextmanager 語意，`with` 區塊內任何 `return` 或例外皆會觸發，
  無論正常完成或提前退出。
- argparse 拒絕發生於 `scripts/ticket.py:154`（`args = parser.parse_args()`），
  此呼叫先於 `cmd_create()` 執行，`create_id_allocation_lock` 從未被呼叫。
- checklist 拒絕（`_enforce_create_checklist`，`create.py:355`）與 dry-run
  預覽（`_report_dry_run_preview`，`create.py:408`／呼叫點 613-614）皆發生在
  `_persist_and_report`（`create.py:571`）內，即 Step 3，仍在 `with` 區塊內。
- dry-run 之所以仍需 Step 1 的 ID 分配（進而取鎖）：預覽需要顯示「會配到
  哪個 ticket_id」，該計算呼叫 `get_next_seq` 掃描既有檔案 max+1，鎖的作用
  是避免此讀取與其他 process 的並行寫入互相踩踏，而非 dry-run 特意繞過。

消費端排查（`grep -rln "ticket-create-locks\|CREATE_LOCK_FILENAME\|\.ticket-create\.lock" .claude/`）：

命中檔案除 `lib/file_lock.py`（定義與取放邏輯本身）與其自身測試檔外，
僅有 `.claude/hook-logs/*` 與 `.claude/logs/agent-dispatch.jsonl`——查證後
確認這些是 hook 對 Bash 指令文字的逐字稽核記錄（本次分析執行的
`grep`/`ls` 指令被記錄下來），不是任何程式碼對鎖狀態的語意讀取。
`reap_stale_locks`（`file_lock.py:89`，唯一具備「掃描並收割殘留鎖」職責
的既有機制）明確排除本鎖：僅 `directory.rglob("*.md.lock")`（`file_lock.py:122`），
且有專屬測試 `test_ignores_create_lock_sentinel` 驗證不動 `.ticket-create.lock`。

### 實驗發現

**已驗證的事實**：

1. 四條路徑中，argparse 拒絕（含任何形式：缺必填/未知旗標）從未進入
   `create_id_allocation_lock`，不產生也不刷新鎖檔——與 PM 原始觀測一致。
2. 其餘三條路徑（Step1 內部拒絕、checklist 拒絕、`--dry-run`）皆會取鎖，
   且皆透過 try/finally 正確釋放 OS 層 flock（`lsof` 事後皆為空、無任何
   一次殘留持有者）——鎖機制本身沒有「未釋放」的缺陷。
3. 鎖檔在 release 後**不會被刪除**，是 `filelock` 函式庫（及一般 flock-based
   lock）的刻意設計：`create_id_allocation_lock` docstring 已明文
   「crash 後 OS 自動回收 fd 釋鎖，殘留檔不影響 reuse」。刪除鎖檔屬另一種
   設計（如 git 的 ref lock：存在即鎖定、操作完成即刪除），兩種設計不可
   混用——對 flock-based 鎖在 release 後刪除檔案會重新引入 TOCTOU 競爭
   （process A 刪除時 process B 可能已對舊 inode 取得 flock，隨後
   process C 建立同名新檔案，B 的鎖已脫離任何人會再檢查的 inode）。
4. 全域搜尋確認**不存在任何程式碼消費端**會讀取此鎖檔的存在性或內容
   作為判斷依據；`reap_stale_locks` 明確排除它。唯一「讀取」它的是人類
   在事故排查時執行 `ls`/`find` 觀察到它與 git 原生鎖同時出現。

**結論（回答 acceptance 第 2 條的分岔點）**：此缺陷的形態是**「產生但無人
檢查的假訊號」**，不是「未釋放的互斥鎖」。鎖機制運作正確；問題出在它
遺留的檔案在視覺特徵（位置 `ticket-create-locks/`、副檔名 `.lock`、
0 bytes、無持有者）上與 git 原生 ref lock/index lock 無法區分，導致排查
者以同一套判準誤判並移除，見 `PC-GPD-013`。

**仍未驗證的假設**：例外路徑（Step 1-3 內部丟出非預期例外，如磁碟寫入
中途拋錯）與並行呼叫（兩個 process 同時取鎖競爭）未在本次重驗範圍內
重現；但兩者皆不影響上述結論——try/finally 對任何例外路徑皆保證釋放，
並行競爭本就是 flock 語意本身要處理的情境（`FileLock.acquire()` 為
blocking，會等待而非產生殘留）。此二項留待修法票（見 Solution spawn
規劃）驗收時視需要補測，不影響本票的假訊號判定。

## Solution
### 判定結論

見「重現實驗結果」：本缺陷是**產生但無人檢查的假訊號**，不是未釋放的
互斥鎖。修法方向因此是「降低視覺誤判機率」與「消除不必要的鎖動作」，
不是「讓鎖檔消失」——後者若照 acceptance 第 3 條字面實作為「release 後
刪除檔案」會重新引入 TOCTOU 競爭（見重現實驗結果第 3 點），是倒退而非
修正。以下方案皆維持 flock 語意與跨 worktree 共用位置不變。

**acceptance 第 3、4 條的達成方式說明**（供修法票承接者與 PM 對照）：
第 3 條「不殘留鎖」以方案 A-1（副檔名去除歧義）達成其精神——鎖檔不再
出現在 `find .git -name "*.lock"` 這類全域鎖盤點指令中，不再與 git 原生
`main.lock`/`HEAD.lock` 混為一談；檔案本身仍會存在（flock 語意要求），
但不再具備「看起來是殘留鎖」的視覺特徵。第 4 條「dry-run 不取得或刷新
任何鎖」以方案 A-2（dry-run 跳過鎖）字面達成。

### 候選方案

**方案 A（建議採用）：兩項獨立低風險修正，分別對應第 3、4 條**

- **A-1 檔名去除歧義**：`CREATE_LOCK_FILENAME`（`file_lock.py:150`）由
  `.ticket-create.lock` 改為不匹配 `*.lock` glob 的命名（如
  `.ticket-create.rendezvous`），位置與跨 worktree 共用機制
  （`_resolve_shared_lock_path`）不變。
  - 優點：改動範圍小（單一常數 + 同步更新 `test_create_id_allocation_race.py`／
    `test_create_id_sibling_worktree_scan.py`／`test_reap_stale_locks.py` 三個
    測試檔的常數引用）；不改變任何鎖行為與正確性；直接命中
    `PC-GPD-013` 記載的混淆成因（同時出現、同為 0 bytes、同無持有者、
    同在鎖盤點指令的比對範圍內）。
  - 缺點：既有安裝已產生的舊 `.ticket-create.lock` 殘留檔不會自動消失
    （為害仍是零，但目錄下會同時存在新舊兩種命名，需一次性文件說明
    或清理腳本）；`.lock` 副檔名是常見慣例，改名需確認 `.gitignore`
    規則（`*.lock`）仍涵蓋新副檔名，否則新鎖檔會被意外納入版本控制。
- **A-2 dry-run 不取鎖**：將 `--dry-run` 分支判斷（現於
  `_persist_and_report` 內、`create.py:610-614`）提前到
  `create_id_allocation_lock`（`create.py:874`）之前，dry-run 預覽改為
  在鎖外唯讀計算 would-be ticket_id。
  - 優點：精準對應第 4 條；預覽路徑完全不再取得/刷新任何鎖狀態；風險低
    （純粹搬移既有唯讀計算的執行位置，不改變計算邏輯）。
  - 缺點：並行下 dry-run 顯示的預覽 ID 有極小機率與稍後真正建立時分配
    到的 ID 不同（dry-run 語意本就是「不落地的預覽」，此為可接受的
    既有限制，非本修法引入的新風險）；需重新梳理
    `resolve_ticket_id_and_wave` 與 `_persist_and_report` 的呼叫順序，
    測試需覆蓋「dry-run 與真實 create 並行時彼此不受影響」。

**方案 B：維持機制不變，僅補強文件/觸發時的可見提示**

於 `PC-GPD-013` 或 `bash-tool-usage-rules.md` 補一條「已知非阻塞鎖清單」，
明列 `*.ticket-create.lock`（或未來改名後的副檔名）屬 flock-based 常駐
rendezvous 檔案、非阻塞信號；`.ticket-create.lock` 錯誤訊息
（`CHECKLIST_VALIDATION_FAILED` 等）中補一行提示「本次操作已取得又釋放
序列化鎖，殘留檔不影響後續建票」。
- 優點：零程式碼風險，可立即落地，且與方案 A 不衝突可並行採用。
- 缺點：治標不治本——`create_id_allocation_lock` docstring 現在就寫著
  「殘留檔不影響 reuse」，本次排查證明**文件已存在仍被誤判**（事故當下
  排查者查的是鎖檔本身的視覺特徵，不是原始碼註解）；且完全不處理
  第 4 條（dry-run 仍會不必要地刷新鎖狀態）。單獨採用不足以達成 acceptance，
  僅適合作為方案 A 的補充。

**方案 C：把鎖檔移出 `.git` common dir，物理脫離 git 原生鎖目錄**

另尋跨 worktree 共用的非 `.git` 位置存放鎖檔，徹底不出現在
`find .git -name "*.lock"` 的搜尋範圍內。
- 優點：比 A-1 更徹底地與 git 原生鎖切割。
- 缺點：工程成本明顯高於 A-1，卻換來相同的「不再被同一份盤點指令掃到」
  效果——A-1 用改副檔名已達成同一目的、成本低很多；且需要重新驗證
  `.claude/` 下是否存在等效於 git-common-dir 的跨 worktree 共用位置，
  若找不到會被迫退回本地互斥、重新引入 `IMP-072` 的原始撞號問題
  （`_resolve_shared_lock_path` 選擇 git-common-dir 正是為了利用其
  跨 worktree 共用特性，見該函式 docstring）。不建議採用。

**結論**：採方案 A（A-1 + A-2），可選擇性疊加方案 B 的文件補強。方案 C 不建議。

### Spawn 落地規劃

| 項目 | 內容 |
|------|------|
| 建議票 1 | IMP：`CREATE_LOCK_FILENAME` 改為不匹配 `*.lock` 的命名，同步更新 3 個測試檔常數引用與 `.gitignore` 規則核對（方案 A-1） |
| 建議票 2 | IMP：`--dry-run` 分支提前於取鎖之前，改為鎖外唯讀預覽，補測「dry-run 不取得/刷新鎖」+「dry-run 與真實 create 並行互不影響」（方案 A-2） |
| 涉及範圍 | 兩票皆修改 `.claude/skills/ticket/ticket_system`（框架資產，經 sync 擴散至全部 consumer 專案），依 `PC-GPD-010` 排序執行時機，不與其他並行 session 對該套件的呼叫窗口重疊 |
| 已用 `add-spawn-request` 登記 | SR-1（方案 A-1）、SR-2（方案 A-2），待 PM 裁決後 `resolve-spawn-request` 回填 |


**更正**：上表「已用 add-spawn-request 登記」列的編號為建票時的預期值，
實際登記結果為 **SR-3**（方案 A-1）與 **SR-4**（方案 A-2）——本票之前已有
SR-1、SR-2 佔用（歸屬於其他既有討論串），實際編號以 Spawn Requests 章節
為準，PM 裁決後請對應 SR-3/SR-4 執行 `resolve-spawn-request`。

**再更正（上一則更正的說法有誤，特此訂正）**：SR 編號採「掃描 body 內
`SR-\d+` 最大值 +1」（`track_acceptance.py:938-941`），並非本票另有
SR-1/SR-2 佔用——上一則更正文字本身在 Solution 章節內以純文字寫出
「SR-1」「SR-2」，該掃描不分辨是否為程式碼區塊或散文，把這兩個純文字
提及也算進既有 ID，導致實際登記的 `add-spawn-request` 順延為 SR-3、SR-4。
以 Spawn Requests 章節列出的 **SR-3**（方案 A-1）、**SR-4**（方案 A-2）
為準。

**額外發現（依規則 5 追蹤，非本票主題但過程中實測命中）**：`add-spawn-request`
的 SR 自動編號以正規表示式掃描整份 body 找 `SR-\d+` 最大值 +1，不區分
程式碼區塊與散文，本票撰寫過程本身即為實例（見上兩則更正）。已登記為
**SR-5**（P3，框架小缺陷，不影響本票判定與 acceptance）。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->
本 ANA 無新增自動化測試。人工 CLI 重現輸出（stat/lsof/lock mtime 對照表）
已完整記錄於「重現實驗結果」章節，作為本票驗收的實驗證據來源。

## Context Bundle

<!-- PM 手動撰寫：本票非由 --source-ticket 建立，無自動抽取來源 -->

### Task Reference

查明 `ticket create` 的 create lock 在四條退出路徑上的取得與釋放行為，並判定其形態是「未釋放的互斥鎖」還是「產生但無人檢查的假訊號」。兩者修法相反，故本票不預設修法。

### Rationale Chain

本票源於 2026-09-09 的並行殘留鎖排查，不是源於任何既有 ticket。當日在同一 repo 內三個 session 共用工作區，先後出現多把殘留鎖，排查過程中發現 create lock 是其中唯一一把「持續產生卻不阻擋任何操作」的——其餘（`index.lock` / `main.lock` / `HEAD.lock`）皆確實擋住對應操作。

**這個差異是本票的核心**：其餘鎖擋住操作，因此排查者移除它們之後管道確實恢復，因果成立；create lock 不擋任何操作，但它與其餘鎖同時出現、同樣是 0 bytes、同樣無持有者，於是被同一套判準處理，且移除後管道恰好也恢復（因為真正的阻因同時被移除了）。**至少兩個 session 各自把管道恢復歸因於移除這把鎖。** 錯誤歸因未造成損害，但它會在下一次事故中重演，且下一次未必無害。

背景機制已記錄於 `PC-GPD-013`（繞道路徑成功，使共用資源的故障失去唯一的診斷訊號），本票是該則在單一工具上的具體修正標的。

### Related Files

- `.claude/skills/ticket/ticket_system` — create 流程與鎖的取得／釋放位置（本票唯讀查明，修法另建票）
- `.git/ticket-create-locks/` — 鎖檔實際落點，檔名為版本目錄路徑的扁平化編碼
- `.claude/error-patterns/process-compliance/PC-GPD-013-workaround-removes-the-faults-only-diagnostic-signal.md` — 背景機制與殘留鎖的判定表（0 bytes vs 有內容的處置差異）

### 執行約束

- 本票 `where.files` 標 `::read`，**不改動 `.claude/skills/ticket/`**。該 CLI 為框架資產，經 sync 擴散至全部 consumer 專案，且為多個並行 session 即時呼叫中的工具——修改時機須依 `PC-GPD-010`（危險窗口是「還沒改完」而非「改壞了」）排序，不在本票範圍。
- 修法建 IMP 票承接，並於該票明寫擴散範圍。

---

## NeedsContext
- **缺失項**: acceptance 第 3、4 條（"修法後四條路徑皆不殘留鎖"／"--dry-run
  不再取得或刷新任何鎖"）字面要求「修法後」的結果狀態，但本票（ANA）的
  執行約束明文「本票唯讀查明，修法另建票」（`where.files` 標 `::read`，
  不改動 `.claude/skills/ticket/`）。這兩條與 ANA 唯讀範圍在字面上互斥，
  無法在本票內字面達成——已透過 `check-acceptance --all` 誤先全勾後，
  重新 `--uncheck 3 4` 更正為僅 1、2 兩條實際完成。
- **觸發位置**: ticket frontmatter `acceptance` 索引 3、4；判定依據見
  Solution「acceptance 第 3、4 條的達成方式說明」。
- **影響**: 無法對本票 4 項 acceptance 逐字面全部勾選；已完成 1、2
  （逐條查明鎖取得/釋放位置、查明無消費端並判定為假訊號）。3、4 的
  精神已在 Solution 提出對應修法方案（A-1／A-2）並以 SR-3／SR-4 登記，
  但字面上的「修法後」狀態要等 spawn 出的 IMP 票落地才成立。
- **建議補料**: PM 裁決兩條路徑之一：(a) 認可 ANA 型 ticket 對「修法後」
  類 acceptance 以「產出可執行的修法方案 + 已登記 spawn request」視為
  達成（本票已滿足），或 (b) 將 3、4 兩條改標為由 SR-3／SR-4 對應的
  IMP 票承接驗收，本票僅保留 1、2。兩者皆不需要重派本票。
- **重派成本**: 低——差異僅在 PM 對 acceptance 語意的裁決，不涉及重新
  分析或程式碼變更。

## Exit Status
```yaml
exit_status: needs_context
reason: "acceptance 第 3、4 條字面要求「修法後」結果狀態，與本票唯讀（::read）執行約束互斥，無法在本票內字面達成；已產出對應修法方案並登記 SR-3/SR-4，待 PM 裁決 acceptance 語意（詳見 NeedsContext）"
confidence: 0.95
acceptance_met: [1, 2]
acceptance_unmet: [3, 4]
artifacts: []
context_dependencies: []
estimated_recovery_effort: "低——僅需 PM 裁決 acceptance 3/4 的達成認定方式，不需重新分析或修改程式碼"
```

## Spawn Requests
- **SR-3** (2026-09-09 13:38)
  - what: CREATE_LOCK_FILENAME（.claude/skills/ticket/ticket_system/lib/file_lock.py:150）由 .ticket-create.lock 改為不匹配 *.lock glob 的命名（方案 A-1），同步更新 test_create_id_allocation_race.py／test_create_id_sibling_worktree_scan.py／test_reap_stale_locks.py 三個測試檔常數引用，並核對 .gitignore 規則涵蓋新副檔名
  - why: 0.1.0-W3-155 分析確認：create lock 與 git 原生 main.lock/HEAD.lock 同副檔名、同目錄形態，導致並行事故排查中被誤判為阻因（PC-GPD-013）；改副檔名可從識別特徵上消除混淆，且不影響 flock 正確性
  - suggested_type: IMP
  - suggested_priority: P2
  - related_files: .claude/skills/ticket/ticket_system/lib/file_lock.py,.claude/skills/ticket/ticket_system/tests/test_create_id_allocation_race.py,.claude/skills/ticket/ticket_system/tests/test_create_id_sibling_worktree_scan.py,.claude/skills/ticket/ticket_system/tests/test_reap_stale_locks.py
  - context: 
  - status: pending


- **SR-4** (2026-09-09 13:38)
  - what: 將 ticket create 的 --dry-run 分支判斷提前到 create_id_allocation_lock（create.py:874）之前，dry-run 預覽改為鎖外唯讀計算 would-be ticket_id（方案 A-2），補測 dry-run 不取得/刷新鎖與並行下互不影響
  - why: 0.1.0-W3-155 分析確認：--dry-run 明示不落地卻仍取鎖並刷新既有鎖 mtime（因預覽需 Step1 的 ID 分配計算），此為不必要的副作用，acceptance 第 4 條要求消除
  - suggested_type: IMP
  - suggested_priority: P2
  - related_files: .claude/skills/ticket/ticket_system/commands/create.py
  - context: 
  - status: pending

- **SR-5** (2026-09-09 13:41)
  - what: add-spawn-request 的 SR 自動編號（track_acceptance.py:938-941）以正規表示式掃描整份 body 找 SR-\d+ 最大值+1，未區分程式碼區塊/散文與實際登記項，Solution 章節中以純文字提及 'SR-1'／'SR-2' 會被誤計入，導致後續真正登記的 SR 編號無預警跳號
  - why: 0.1.0-W3-155 撰寫 Solution 時實測命中：先在散文中寫 SR-1/SR-2 作為預期編號，實際 add-spawn-request 執行後編號變成 SR-3/SR-4，需事後更正說明；屬低優先級框架小缺陷，記錄避免遺失
  - suggested_type: IMP
  - suggested_priority: P3
  - related_files: .claude/skills/ticket/ticket_system/commands/track_acceptance.py
  - context: 
  - status: pending

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
