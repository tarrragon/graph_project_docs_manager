---
id: 0.1.0-W3-155
title: 查明並修正 ticket create 失敗路徑未釋放 create lock，使後續同版本建票全數受阻
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 並行環境殘留鎖排查中，以建票動作本身重現失敗路徑留鎖
  final_decision: ANA 查明三條路徑的釋放行為後修正，不先預設修法
  rationale: 已有可重現最小案例（argparse 不留鎖／checklist 留鎖），但成功路徑是否同樣缺口未定，直接派 IMP 會遺漏另兩條分支
who:
  current: thyme-python-developer
  history: {}
what: 查明並修正 ticket create 失敗路徑未釋放 create lock，使後續同版本建票全數受阻
when: 2026-09-09 排查並行環境殘留鎖時，以建票動作本身重現
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system::read
why: '`.claude/skills/ticket/ticket_system` 的 create 流程取得 `.git/ticket-create-locks/<版本目錄>.ticket-create.lock` 之後，於 checklist 驗證失敗的退出路徑上未釋放該鎖。鎖為 0 bytes、無持有行程，但存在期間該版本目錄下的一切建票失敗。


  **已重現，且分層明確**（同一次排查中連續兩次觀測）：


  | 拒絕發生的層 | 觸發方式 | 是否留鎖 |

  |-------------|---------|---------|

  | argparse | 缺 `--action` / `--target` | **否** |

  | checklist（`CHECKLIST_VALIDATION_FAILED`） | 缺 `acceptance` / `when` / `how_strategy` | **是** |


  由此可定位：鎖的取得在 argparse 之後、checklist 之前，而 checklist 失敗的退出路徑跳過釋放。


  **為何影響大於表面**：漏填必填欄是建票的高頻首次錯誤（本次排查中同一人連續命中兩次），因此這條路徑經常被走到；而錯誤訊息完整列出缺失欄位與修正建議，**完全未提及鎖已被取得且未釋放**。


  **實際損害不是阻擋，是誤導**（建票後補測，修正建票當下的錯誤推論）：後續實驗確認**鎖存在時 `ticket create` 照常執行**，未被擋住，且該次執行還把鎖的 mtime 刷新。因此本日約 9 分鐘的建票不可用**不是這把鎖造成的**——同時間另有 `main.lock`（41 bytes）與 `HEAD.lock` 擋住 commit 步驟，那才是真正的阻因。建票當下把兩者歸為同一原因，屬未經實測的推論，已於此更正。


  真正的代價是**它在事故排查中作為假訊號**：它看起來是一把鎖、位在 `ticket-create-locks/` 目錄下、命名指向建票，於是被當成阻因處理。本日至少兩個 session 各自移除它並將管道恢復歸因於該移除，而管道從未被它擋住。一個不被檢查卻持續產生的鎖檔，在並行事故中會**穩定地把排查者導向錯誤的根因**。


  **三條路徑的觀測現況**（皆為本次實測，非推論）：


  | 路徑 | 是否留鎖 | |------|---------| | argparse 層拒絕 | 否 | | checklist 層拒絕 | 是 | | 建票成功 | 是 | | `--dry-run`（不落地） | 是（且刷新既有鎖的 mtime） |


  四條路徑中三條留鎖，且 `--dry-run` 明示不寫入任何檔案卻仍動到鎖，指向鎖的取得位置早於「是否真的要寫入」的判斷。


  **待查明**：該鎖是否有任何消費端會讀取它。若無，本缺陷的形態是「產生但無人檢查的假訊號」而非「未釋放的互斥鎖」，修法方向隨之不同——前者可能該移除整個機制，後者該補釋放。相關機制已記錄於 `PC-GPD-013`。'
how:
  task_type: Implementation
  strategy: 先讀 create 流程的鎖取得與釋放位置，列出成功／失敗／例外三條路徑各自的釋放行為；再依結果決定修法（context manager／try-finally／atexit），並補上鎖殘留時的可見訊息。修法涉框架資產，經 sync 擴散至全部 consumer 專案
acceptance:
- '[ ] 四條路徑（argparse 拒絕／checklist 拒絕／建票成功／--dry-run）各自的鎖取得與釋放位置已逐條查明，記錄程式碼位置'
- '[ ] 已查明是否存在讀取該鎖的消費端；若無，判定為假訊號並據此決定移除機制或補釋放，選擇理由記錄於 Solution'
- '[ ] 並行 session 成功建票亦留鎖的觀測已查明成因或明確排除，不以推測結案'
- '[ ] 鎖殘留時對呼叫者可見（錯誤訊息或警告明示鎖狀態），不再是與呼叫者操作無關的靜默失敗'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

查明並修正 ticket create 失敗路徑未釋放 create lock，使後續同版本建票全數受阻

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
