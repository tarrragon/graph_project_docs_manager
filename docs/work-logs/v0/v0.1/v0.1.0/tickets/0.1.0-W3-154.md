---
id: 0.1.0-W3-154
title: 補齊 ticket CLI 的 spawned_tickets 誤填撤銷路徑
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-148
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-148 落地 SR-1 時誤填 spawned_tickets，發現無撤銷路徑
  final_decision: IMP 補 remove-spawned 子命令並為 resolve-spawn-request 加血緣驗證
  rationale: 欄位 append-only 而 ticket md 受 read guard 保護，誤填成本從改一行變成永久污染；並行 session 下 ID 撞號為常態，非一次性意外
who:
  current: thyme-python-developer
  history: {}
what: 補齊 ticket CLI 的 spawned_tickets 誤填撤銷路徑
when: 任何 session 誤填 spawned_tickets 之後
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/commands/fields.py
  - .claude/skills/ticket/ticket_system/lib/command_tracking_messages.py
why: '`spawned_tickets` 欄位目前只進不出：CLI 有 `add-spawned`，`set-*` 系列無對應項，`resolve-spawn-request` 只追加不移除。誤填後無撤銷路徑，且 ticket md 受 read guard 保護不可直接編輯，錯誤連結因此永久留存。


  本缺口已在 `0.1.0-W3-148` 實際命中：PM 落地 SR-1 時未先查證新建票的實際 ID，逕以下一號推定，而該號已由並行 session 佔用，於是 148 的 `spawned_tickets` 出現一筆與本票無關的連結。目前只能以 Completion Info 的文字說明撤銷，讀者須讀完該段才知道 frontmatter 的某一筆是假的——結構化欄位與其撤銷記錄分處兩地，任何機器消費者（dashboard、runqueue、衍生鏈追溯）都只看得到錯誤那一筆。


  並行 session 環境下 ID 撞號是常態而非意外：配號在建立當下才確定，任何先推定號碼再落地的流程都會週期性命中。撤銷路徑缺席使單次誤填的成本從改一行變成永久污染。'
how:
  task_type: Implementation
  strategy: '最小解為新增 `ticket track remove-spawned <ticket-id> <value...>`，對稱於既有 `add-spawned`（同樣支援一次多值，實作位置參照 `commands/fields.py`）。移除時須同步處理兩處：frontmatter 的 `spawned_tickets` 陣列，以及 body 內 Spawn Requests 區塊中引用該 ID 的 status 行（若有）——只改其一會製造新的不一致。


    另需判定是否一併補 `resolve-spawn-request` 的改派語意：目前對同一 SR 重跑會覆寫 status 文字但不移除前次追加的 ID，這正是本缺口的觸發路徑之一。若補上，改派時應移除前次 ID 再追加新 ID。


    反向防護（與移除路徑正交，兩者皆需）：`resolve-spawn-request` 的 spawned-ticket 參數應驗證該 ID 存在且其 source_ticket 或 discovered_during 指回本票，不符即拒絕並提示。這使誤填在寫入前就被擋下，而非事後補救。驗證邏輯需容許 dismissed 狀態不帶 ID 的既有用法。


    測試須涵蓋：移除單值與多值、移除不存在的值（應為 no-op 或明確錯誤而非靜默）、移除後 body 與 frontmatter 一致、反向防護對合法與非法 ID 的兩種結果。既有 `tests/test_add_spawned_multi.py` 為對稱參照。'
acceptance:
- '[ ] remove-spawned 子命令已實作，支援一次傳多值，與 add-spawned 對稱'
- '[ ] 移除時 frontmatter 陣列與 body 內 Spawn Requests 的 status 行同步處理，無單邊殘留'
- '[ ] resolve-spawn-request 的 spawned-ticket 參數已驗證目標票存在且血緣指回本票，不符時拒絕寫入'
- '[ ] 測試涵蓋移除單值/多值/不存在值三種輸入與反向防護的合法與非法兩種結果'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補齊 ticket CLI 的 spawned_tickets 誤填撤銷路徑

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
