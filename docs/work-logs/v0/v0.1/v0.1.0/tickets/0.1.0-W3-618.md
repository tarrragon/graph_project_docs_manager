---
id: 0.1.0-W3-618
title: 驗收 0.1.0-W3-617 兩條 doc 條文改動 + 重出 7.2(3-2)①乙 覆蓋題
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-619
source_ticket: 0.1.0-W3-617
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-617 完成 Step 1（兩條條文改動落地、改動清單與指名覆蓋題已定）
  final_decision: 下一輪自 Step 2 起，出三題
  rationale: 團隊指示明寫下一輪 Step 2 另票；其餘 15 題本輪已 3/3，依規格通過即結案不重測
who:
  current: 待指派
  history: {}
what: 依出題規格 1.1.0 執行 Step 2 與 Step 3，出三題：R2-Q01（doc 射程判定表第二列管轄欄的指涉改寫）、R2-Q02（第二列依據欄的 disjunction 拆分註記）、R2-Q03（重出 7.2(3-2)①乙 窮盡要求，題幹須交出前段 (a)(b)(c) 檢查已逐項執行且命中這個前置結論，使正解在該款次內只剩乙目一條理由）。產出整批通過／不通過單一布林
when: 0.1.0-W3-617 完成後
where:
  layer: 待定義
  files:
  - docs/probes/probe-authoring-spec.md
  - .claude/methodologies/component-library-bidirectional-constraint-methodology.md
  - .claude/skills/ux-design-evaluation/references/interaction-feedback-component-semantics.md
why: 0.1.0-W3-617 已完成 Step 1：兩條條文改動（D1 第二列管轄欄補指涉、D2 第二列依據欄補 disjunction 拆分註記，皆為甲類）已落地並各自指名覆蓋題，零覆蓋清單為空且附禁區逐條對照記錄；改動清單全文見該票 Solution 第三節。另有一題非改動但必出——7.2(3-2)①乙 的窮盡要求本輪讀數為「引用 2/3、套用 2/3」，未達 3/3 須重測，且依新增的規格 1.7 須在題幹交出前段檢查的結論以排除旁路。其餘 15 題本輪已 3/3，依規格「通過即結案」不重測。本輪同時是規格 1.1.0 新增條文（1.7 遮蔽推導、3.1 邊界段登記、4.6 命中分類、5.8 寫入探測）的首次執行
how:
  task_type: Analysis
  strategy: 依 docs/probes/probe-authoring-spec.md 1.1.0：Step 2 自當輪 HEAD 重建節錄、現出三題、套 §7 全部檢查（含新增第 21/22/23 項）；Step 3 每題三輪 fresh agent、逐題獨立 context、兩名獨立判定者先由條文推導再比對，產出整批布林
acceptance:
- '[x] 三題全數通過 §7 檢查清單（含新增第 21/22/23 項），紅燈者已改題材而非改條文'
- '[x] R2-Q03 的題幹已交出前段 (a)(b)(c) 檢查的結論，遮蔽推導確認正解在該款次內只剩乙目一條理由'
- '[x] 九份作答與兩名判定者報告已產出，整批布林已給出；每題的引用與套用兩個讀數分開記錄'
- '[x] 規格 1.1.0 新增條文的可執行性已回報：1.7 遮蔽推導／3.1 交出界線登記／4.6 命中分類／5.8 寫入探測各自照做時是否有歧義'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-17T21:54:26'
completed_at: null
created: '2026-09-17'
updated: '2026-09-17'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

依出題規格 1.1.0 執行 Step 2 與 Step 3，出三題：R2-Q01（doc 射程判定表第二列管轄欄的指涉改寫）、R2-Q02（第二列依據欄的 disjunction 拆分註記）、R2-Q03（重出 7.2(3-2)①乙 窮盡要求，題幹須交出前段 (a)(b)(c) 檢查已逐項執行且命中這個前置結論，使正解在該款次內只剩乙目一條理由）。產出整批通過／不通過單一布林

---

## Problem Analysis
### 問題根因

本票不是修 bug，是執行驗收：`0.1.0-W3-617` 完成 Step 1（D1 第二列管轄欄補指涉、D2 第二列依據欄補 disjunction 拆分註記，皆甲類），本票自 Step 2 起出三題並驗一次，產出整批布林。

同時是規格 1.1.0 四項新增條文（1.7 遮蔽推導、3.1 交出界線登記、4.6 命中分類、5.8 寫入探測）的首次執行，其可執行性由本輪回答。

### 影響範圍

**已驗證的事實**：

- 受測條文兩處落在 `.claude/methodologies/component-library-bidirectional-constraint-methodology.md` 1.24.0 的〈容器亦為元件〉射程判定表第二列（管轄欄與依據欄）。節錄取自 HEAD `75b3c00c4`，`git diff --stat HEAD` 對該檔與 ux 檔皆為空。
- `.claude/skills/ux-design-evaluation/references/interaction-feedback-component-semantics.md` 的 7.2(3-2)①乙 本輪未改動，只重出覆蓋題。

**仍未驗證的假設**：無。三題各三輪讀數已產出，兩名判定者獨立比對。

### 相關 Error Pattern

不適用。本票的知識來源是 `docs/probes/probe-known-limitations.md` 的 L19／L20／L22／L23／L24，逐則已在出題時套用（見 Solution）。

## 重現實驗結果
### 實驗方法

依 `docs/probes/probe-authoring-spec.md` 1.1.0：

1. Step 2——自當輪 HEAD 重建節錄（`ex/EX-A`＝doc〈容器亦為元件〉全節、`ex/EX-B`＝ux 第 7 項全節＋2.3 節），現寫三題，套 §7 全部 23 項檢查。
2. Step 3——每題三輪、每輪一個 fresh agent、逐題獨立 context、非 fork；派發當下跑 §7 第 18／19／20／23 項。
3. 兩名獨立判定者各自先由條文推出正解與依據句，再比對九份作答；判定者甲另做 1.7 的外部驗證（讀受測款次之前先以其餘條文推一次）。

拋棄式工作目錄：`<scratchpad>/r618/`（`q/` 題目、`ex/` 節錄、`out/` 九份作答、`key/` 答案鍵與兩份判定報告、`tools/` 量測器材與派發腳本）。

### 實驗執行

**Step 2 的機械檢查（`MIN_LEN` = 5）**

| # | 結果 |
|---|---|
| 5 題材 ∩ 例示 span | 正向命中 0；反向窮舉（例示 span 的 2..6 字候選 ∩ 題幹）命中逐則人讀，全數為條文規範詞或受測步驟的必要實例化。初版 R2-Q03 的「按鈕」命中 2.2(1) 頓號列舉與 7.4 例示，已改題材移除 |
| 6 題材兩兩交集 | 三對皆空 |
| 7 題幹 ∩ 失敗實例題材 | 空。初版 R2-Q02 的「格位定義」含 `0.1.0-W3-616` Q15 禁區詞「格」，已改寫為「每一個位置的內緣留白」 |
| 11／12／13 | class (iii) 命中 0／0／0；D1 與 D2 兩條新增字串 ∩ 三份題幹全部為空 |
| 14／15 | 11 個被排除區塊各附原因；86 ＋ 11 ＝ 97 ＝ 區塊總數 |
| 16 | 構造「寫法不同而應納入」的儲存格列，驗得 3 格全納入、0 丟棄 |
| 4.6 配套 3 正向對照輸入 | 三題各構造一則已知該紅的命中（判定語 span 內整句塞進題幹草稿），三題皆被判為 (iii) |

**4.6 位置判準的第一次實戰命中**：R2-Q02 初版題幹逐字引「先數兩端各屬哪個元件，再選列」以交出前置結論（3.1 邊界段所要求），該命中落在 D2 註記的 span（「依兩端各屬哪個元件擇一，不並存」）內被判 (iii)。依 Step 2「任一項紅時改題材或改題，不改條文」，題幹改寫為「兩端各自長在誰身上」，重跑後 class (iii) 歸零。

**派發當下（§7 第 18／19／20／23 項）**

| 項 | 結果 |
|---|---|
| 18 | 節錄檔頭 commit ＝ `75b3c00c49235b4e2268fb82df937444c1b80fa7` ＝ 當前 HEAD |
| 19 | 一次派發 3 題 ≤ 8（輪次不計入） |
| 20 | prompt 由 `tools/dispatch.sh` 從單一路徑變數產生，只含題目檔路徑、節錄目錄、作答格式、輸出路徑四項；無其他題號、無本輪題數；九輪皆 fresh agent、非 fork |
| 23 | 哨兵檔寫入探測在派發當下對 `$OUT` 這個實際變數執行：建檔、驗非空、刪除、驗已刪，回報 `SENTINEL OK` |

**對帳（在全部九批回報完成之後才做）**：實收 9 檔 ＝ 3 題 × 3 輪，逐檔非空（1571～2920 bytes）。另查 `0.1.0-W3-616` L23 所指的底線版遺留目錄，派發後零新增檔案。

### 實驗發現

**已驗證的事實**：

1. 九輪讀數：兩名判定者獨立比對後一致——R2-Q01 3/3、R2-Q02 3/3、R2-Q03 3/3，九輪的引用與套用兩讀數全為「有」。
2. R2-Q03 三輪皆逐字引乙目末句並指名端乙漏寫的那一項，`0.1.0-W3-616` 的 L22 旁路形態（改引同款前段的 (a)(b)(c)）本輪零次。
3. R2-Q01 三輪皆寫出具體落點（〈組合規則〉的「作為表格或列表的一欄時」列），未停在粗粒度的「屬本元件的組合規則」。
4. R2-Q02 三輪皆答「主張不成立」並處理了選擇結構（已拆為第二／三列、依兩端擇一、不並存），`0.1.0-W3-616` Q15-r1 被外部原句第二支帶回第三列答案的形態本輪零次。

**仍未驗證的假設**：1.7 對「位置前移型改動」（L25）的代價是否有其他出路，本輪未探；D2 的實際效力由讀數支持而形式上不可宣告為覆蓋。

## Solution
### 一、整批布林

**通過（true）。** 三題各 3/3，兩名判定者獨立得出同一結果，零分歧。

| 題 | 受測款次 | 引用 | 套用 | 判定欄 | 結果 |
|---|---|---|---|---|---|
| R2-Q01 | doc 射程判定表第二列**管轄欄**的例外分支句（D1） | 3/3 | 3/3 | 3/3 | 3/3 |
| R2-Q02 | 同表第二列**依據欄**的 disjunction 拆分註記（D2） | 3/3 | 3/3 | 3/3 | 3/3 |
| R2-Q03 | ux 7.2(3-2)**①乙**的窮盡要求（本輪未改動，重測） | 3/3 | 3/3 | 3/3 | 3/3 |

依規格 Step 3「通過即結案」，不追問是哪一句條文讓它通過，不進 Step 4。

### 二、規格 1.1.0 新增條文的可執行性回報

#### 1.7 遮蔽推導：有效，但兩個粒度未界定，同一題可得相反判讀

**有效的證據（兩則，方向不同）**：

- R2-Q02 抓到真旁路：遮掉第二列依據欄後，管轄欄＋第三列＋表外綜述句仍可推出同向確定結論。四條出路逐條試過——(a) 換題材改不掉本表結構、(b) 交出該前提等於交出受測步驟的結論（違反 3.1 核心禁令）、(c) 換款次等於放棄測 D2——只剩 (d)。第 (d) 條的存在使本題不必作廢，這一條在本輪確實被用上。
- R2-Q03 確認旁路已排除：題幹交出前段 (a)(b)(c) 檢查已逐項執行且命中 (c) 之後，遮掉乙目推不出任何確定結論，正解「本步未完成」在該款次內只剩乙目一條理由。讀數佐證：三輪皆引用並套用乙目，`0.1.0-W3-616` 的前段旁路零次。

**兩個粒度歧義（已開票 `0.1.0-W3-619`）**：

| 粒度 | 歧義 | 本輪實測 |
|---|---|---|
| 遮蔽單位 | 「受測步驟所在的最小編號單位」在無編號載體上指不出唯一切法 | R2-Q03：出題者遮整個乙目 → 推不出任何結論（綠）；判定者甲改遮乙目的窮盡要求句 → 推得出**反向**結論（依判讀表屬條文缺陷，須回 Step 1 改條文）。兩側對同一題得到相反判讀 |
| 「確定結論」 | 以正解粒度判或以任何結論判 | R2-Q01：遮掉管轄欄後可推出粗粒度同向結論「屬本元件的組合規則」，但推不出具體落點。前者判紅、後者判綠 |

兩個分支的處置完全相反（放行 vs 回 Step 1 改條文），故依 `probe-known-limitations` 收納判準第一列開票，不進限制清單。本輪的處置是：R2-Q01 取正解粒度判綠，並把計分收窄為「須寫出具體落點」使粗粒度結論不足以得分；R2-Q03 取出題者的遮蔽粒度判綠，讀數（三輪皆套用）事後佐證該判讀無害。

**誤殺**：無。判紅的 R2-Q02 依 (d) 保留出題並計入布林，未因該關作廢任何題目。

#### 4.6 命中分類：有效，抓到一則真 (iii)；三類對一種命中沒有格

- **位置判準有效**：R2-Q02 初版題幹為滿足 3.1「前置步驟的結論必須交出」而逐字引「先數兩端各屬哪個元件，再選列」，該命中落在 D2 註記 span 內被判 (iii)。這是位置判準（byte-exact、可複核）與語意判準會分歧的一個實例——按語意它明明是 (ii) 前置既定事實，按位置它是 (iii)。處置依 Step 2 改題幹不改條文，重跑歸零。
- **分類器的正向對照輸入（配套 3）有效**：三題各構造一則已知該紅的命中，三題皆被判 (iii)，分類器未滑向 (ii)。
- **缺口（進限制清單 L26）**：受測步驟是「某份逐項清單有沒有寫完」時，題幹對清單項名的實例化既非 (i) 也非 (ii)——它是受測步驟**判定對象**的實例化，三類無格可填。勉強歸 (ii) 會使配套 2（須指名對應的前置款次）指不出來而應降為 (iii)，與位置判準的結論相反。本輪的處置是位置判準優先、類別標籤只作記錄。

#### 5.8 寫入探測與對帳：照做無歧義，本輪零損害

三項全數執行且無歧義：哨兵檔探測在派發當下對實際路徑變數跑（非事前對手打字串）、prompt 路徑由 `tools/dispatch.sh` 從單一變數產生、對帳在九批全數回報完成之後才做（9/9、逐檔非空、錯誤樹零新增）。

與 L23 同形：本輪實際損害為零，故這三項的價值仍是「關掉一段無訊號區間」而非「抓到了什麼」。此處無法由本輪證成其必要性——這正是 L4 所指的零觀測兩種成因，本輪的設計看得到「寫錯地方」（哨兵探測會當場失敗），故零觀測可記為「本輪沒發生」而非「測不到」。

#### 3.1 交出界線登記：可執行，且與 4.6 有一處結構性碰撞

三題逐題登記「題幹交出了哪一步的結論」與「受測步驟是哪一步」，兩者皆不同；「前置步驟須指得出條文中規定其順序的那一句」三題皆指得出（R2-Q01／Q02 指向「先數兩端各屬哪個元件，再選列」；R2-Q03 指向「兩句寫完之後、進 ② 之前，先逐項檢查下列 (a)(b)(c)……改寫完成才進 ②」）。

**碰撞**：3.1 要求交出前置結論，而改文者為求對症，最自然的措辭就是複述那句前置句的用詞（D2 即是），於是「照規矩交出前置」與「4.6 不得命中判定語 span」會互相衝突。本輪以改寫題幹措辭解消，成本為零（拋棄式題庫）。此形態與 2.3（修法措辭繼承實例題材）同源，但發生在前置句而非實例上。

### 三、零覆蓋清單（Step 1 的更新）

| 改動 | 指名覆蓋題 | 本輪狀態 |
|---|---|---|
| D1 第二列管轄欄補指涉 | R2-Q01 | **有覆蓋**，3/3 |
| D2 第二列依據欄補 disjunction 拆分註記 | R2-Q02 | **零覆蓋**——依 1.7 第 (d) 條，題目仍出且 3/3，但不得列為該改動的覆蓋題 |

D2 的零覆蓋不是缺題，是 1.7 對「位置前移型改動」的結構性後果，已登記為 L25。

### 四、發現的停損處置

| 發現 | 判準 | 處置 |
|---|---|---|
| 1.7 的遮蔽單位與「確定結論」兩個粒度未界定 | 兩個分支處置相反（放行 vs 回 Step 1 改條文），使一道本該回 Step 1 的題可被放行 | **開票** `0.1.0-W3-619` |
| 位置前移型改動在 1.7 下必然零覆蓋 | 使覆蓋率被低估，不使布林反向 | 限制清單 **L25** |
| 4.6 三類對受測步驟判定對象的實例化沒有格 | 位置判準仍可判，不使布林反向 | 限制清單 **L26** |
| 引用讀數無計分後果，「照做不引」的組合未定規則 | 本輪兩讀數零分歧，不使布林反向 | 限制清單 **L27** |

`docs/probes/probe-known-limitations.md` 1.4.0 → 1.5.0。

### 五、Spawn 落地確認

| 規劃項 | 落地 |
|---|---|
| 1.7 兩個粒度的界定 | 已建 `0.1.0-W3-619`（`--source-ticket 0.1.0-W3-618`，CLI 建立當下回填雙向血緣） |
| L25／L26／L27 | 依收納判準寫入 `probe-known-limitations.md` 第五節。**無需建 ticket**：三則皆不使任一輪的整批布林反向 |
| 判定者兩份報告各自列出的 brief 留白 | 可歸入 L27（引用讀數死欄）與 `0.1.0-W3-619`（粒度）兩者之一；其餘（計分紀律 3 的射程、(3)(4) 欄與紀律 1 的邊界）屬判定者 brief 的逐輪器材，拋棄式不常駐。**無需建 ticket**：brief 每輪重寫，不是常駐資產 |
| 條文改動 | 本輪零條文改動。Step 2 兩次紅燈（R2-Q02 的 class (iii)、R2-Q03 的「按鈕」例示詞）皆依規格改題材、不改條文 |


---

### 六、多視角審查與逐項處置

multi_view_status: reviewed
reviewers: basil-writing-critic (文字品質／三明示／引用錨點／讀數不支撐的斷言), linux (結構／鑑別力／規格自洽／架空偵測)
conclusion: 兩視角各自獨立指出同一處最硬的缺陷——L25 把走 (d) 的題描述為「題目有效、覆蓋率被低估」，而 1.7 判紅的實質語意就是該題對受測步驟無鑑別力，(d) 只免除處置義務、不回復鑑別力，故本則與 L22 同向（皆為覆蓋率高估），差別只在本則已被零覆蓋清單擋下一半。linux 另抓到三處本票原先記為「執行過」而實際上是裁量放行的項目，其中最要緊的是 1.7 外部驗證被它要驗的那一方覆蓋。三處已逐項修正或改記，四個未界定處已全數併入 `0.1.0-W3-619` 的射程。

#### 逐項處置

| 來源 | 發現 | 處置 |
|---|---|---|
| 兩者一致 | L25 的「與 L22 方向相反／覆蓋率被低估」是誤讀；(d) 不回復鑑別力 | **採納**：L25 改寫為「與 L22 同向，差別在有沒有被擋下」，並明寫「不得把走 (d) 的題描述為題目有效」 |
| linux | 走 (d) 的題是否計入 Step 3 布林分母，規格未規定；本票當成已定的正交關係處理 | **採納**：列為第三個未界定處，併入 `0.1.0-W3-619`（而非另開票，四者同源）；L25 同批補一句指向 |
| linux | 1.7 外部驗證（判定者甲的遮蔽推導）指向「回 Step 1 改條文」，被出題者側的粒度選擇覆蓋；本票原記「1.7 有效、零誤殺」 | **採納**：本票對 R2-Q03 的「零條文改動」正確讀法是**在粒度歧義下選了不需改條文的那一邊**，不是規格判定不需改。「零誤殺」只在出題者粒度下成立。外部驗證的裁決力列為第四個未界定處，併入 `0.1.0-W3-619` |
| linux | 兩名判定者記為「零分歧」，而甲在 1.7 外部驗證上與出題者判讀相反，該項被歸入可執行性回報而未計入分歧 | **採納（改記）**：「零分歧」的射程限於**九輪讀數的計分**（引用／套用／判定欄三欄逐輪一致）；1.7 遮蔽推導的判讀方向上兩側相反，依 L13 屬判準留白的表現，已併入 `0.1.0-W3-619` |
| linux | §7 第 5 項的反向窮舉命中以人讀放行，而 4.6 射程段明寫該理由不適用於第 5 項 | **部分採納**：本輪的人讀刪除的是**器材的 false positive**（`tools/check2.py` 的 `(?:例如\|如)` 正則把任何「如」字後的內容當例示 span，括號一律視為例示），不是放寬規則——真例示命中（「按鈕」）確實觸發了改題材。但 linux 的指認成立之處在於：本票原文「全數為條文規範詞或受測步驟的必要實例化」讀起來像放寬。已於本節改記；器材的 span 過寬屬 L9 所指的空格，不使布林反向，不開票 |
| linux | L26 的「位置判準優先於類別標籤」推翻 4.6 配套 2，而 §7 第 11 項仍記為通過 | **採納**：L26 已補「此優先序是本輪的自訂規則，不是 4.6 的字面」，並要求 §7 第 11 項記為通過時同批註明以此規則放行的命中數 |
| linux | §7 第 1／2／3／4／8／9／10／17 項在票面零記錄 | **不採納（非缺漏）**：八項全數執行且逐題寫在 `key/answer-key.md` 的三題檢查表與「機械檢查彙總（§7）」23 列表中，該檔是規格 Step 2 的指定產出。票面只摘錄機械檢查是體例（拋棄式材料不複製進票）。但 linux 的可複核性顧慮成立——**已於本節記明八項的落點**，後續輪次的票面宜直接附 23 列摘要 |
| linux | R2-Q02 的「套用 3/3」不可鑑別（L19 形態：複本使引用位置無法區分） | **採納（改記）**：R2-Q02 的三個讀數只證明該題的讀者答對且執行了選擇結構的處置，不證明他取用的是 D2 依據欄。這與 D2 零覆蓋是同一件事的兩面，已在 L25 與第三節寫明 |
| linux | R2-Q01 的計分收窄（須寫出具體落點）時序未記錄 | **採納（補記）**：收窄寫在 `key/answer-key.md` 的 R2-Q01「計分」欄，該檔於派發**之前**定稿；判定者 brief 的同一條亦在派發前寫成。時序可由 `out/` 的檔案時間晚於 `key/` 覆驗 |
| linux | 5.5 另兩條計分紀律（推理牴觸、明示自我更正）無執行痕跡 | **採納（補記）**：兩條寫在判定者 brief 的「兩條計分紀律」節，兩名判定者報告皆回報「紀律 1 無命中、紀律 3 未觸發」。票面原未轉錄 |
| basil | L25 依賴對 1.7 (d)「複本」的射程擴張解釋，未外顯 | **採納**：L25 補「前提」兩項（(d) 射程解釋由 `0.1.0-W3-619` 界定；「必然」是演繹非讀數） |
| basil | L26 的「三類」是可變計數，4.6 增類後本則靜默失效 | **採納**：L26 補「前提：以 4.6 現行分類集合為準」 |
| basil | L27 的「5.5 第三條」是序數錨點，而該節引言寫「兩條」卻列三條 | **採納**：改為條款名稱錨點。規格側的「兩條／三條」不符屬規格缺陷，`0.1.0-W3-619` 順帶修正，不另開票 |
| basil | L25「四條出路」是可變計數；L27「本輪若遇到」指向已關閉的時窗 | **採納**：改為「1.7 的出路中」與「後續輪次若遇到」 |
| basil | Test Results 的 zhtw 讀數未標口徑（L8／L15） | **採納**：已補記掃描參數與兩種口徑的差額成因 |
| basil | L25／L26／L27 首段以「實測：」開場，示例先於原則 | **不採納**：三則的 `###` 標題本身即完整的原則陳述句，與 L18～L24 既有體例一致；單獨改寫本次新增三則會使第五節不一致 |
| basil | L25／L26 定語堆疊 6 處、翻譯腔綜合分 0.23 | **不採納**：與本檔既有段落體例一致，分數低於閾值 |

**不採納三項的共同處置**：皆不使任一輪的整批布林反向，依停損判準不開票。

### 七、自檢結果

| 檢查項 | 結果 |
|---|---|
| 產出是否只回答受託範圍（Step 2 三題 + Step 3 布林 + 三項新條文評價） | 是。未重測其餘 15 題，未改條文 |
| 布林是否由讀數支撐而非推測 | 是。九份作答與兩份判定報告在 `<scratchpad>/r618/`，兩名判定者獨立比對、計分三欄逐輪一致 |
| 是否遵守「不得重建已捨棄的機制」 | 是。未加歷輪對照欄、未做跨輪比較、未量帶入幅度 |
| 新發現是否已依停損判準分邊 | 是，且經 linux 複查分邊；原分為 L25 的「布林分母」一問改判為開票項，併入 `0.1.0-W3-619` |
| 未驗證的假設是否已標明 | 是。R2-Q03 的「零條文改動」已改記為粒度歧義下的選擇；「必然」的演繹性質已在 L25 外顯 |
| 是否有規格條文被架空卻記成執行過 | 經 linux 專項審查，四處指認中三處已改記或補記，一處（§7 八項）查明為落在答案鍵而非票面 |
| 用字檢查 | 見 Test Results |

### 自檢結果

（與上節「七、自檢結果」同一份，此處為 schema 要求的標準標題。）

| 檢查項 | 結果 |
|---|---|
| 產出只回答受託範圍 | 是。Step 2 三題 + Step 3 布林 + 三項新條文評價；未重測其餘 15 題、未改條文 |
| 結論由讀數支撐而非推測 | 是。九份作答與兩份判定報告在 `<scratchpad>/r618/`，兩名判定者獨立比對 |
| 未重建已捨棄的機制 | 是。無歷輪對照欄、無跨輪比較、無帶入幅度量測 |
| 發現已依停損判準分邊 | 是，且經 linux 複查；一問由清單改判為開票項，併入 `0.1.0-W3-619` |
| 未驗證的假設已標明 | 是。R2-Q03 的「零條文改動」改記為粒度歧義下的選擇；L25 的「必然」已標為演繹 |
| 自報的時序陳述已用固定值覆驗 | 是，且查出一處有誤（判定者 brief 晚於作答落地），已於 Test Results 更正並寫出後果 |
## Test Results
本票的產出是讀數，不是測試。九份作答與兩份判定報告在拋棄式工作目錄 `<scratchpad>/r618/`（驗完即棄，依規格前提不保存）。

| 驗證 | 命令／材料 | 結果 |
|---|---|---|
| 節錄取自當輪 HEAD | `git rev-parse HEAD`；`git diff --stat HEAD -- <doc 檔> <ux 檔>` | `75b3c00c4`；diff 為空（兩份來源檔在 HEAD 與工作區一致） |
| 派發路徑可寫 | `tools/dispatch.sh` 的哨兵檔探測（建檔、驗非空、刪除、驗已刪） | `SENTINEL OK` |
| 實收份數 | 全部九批回報完成後對 `out/` 取計數 | 9 ＝ 3 題 × 3 輪；逐檔 1571～2920 bytes，無空檔 |
| 錯誤樹零新增 | `find <底線版遺留目錄> -newermt <派發時刻>` | 零命中 |
| 機械檢查守恆式 | `tools/check.py` | 86 比對 ＋ 11 dropped ＝ 97 區塊總數 |
| 分類器正向對照輸入 | `tools/check.py` 的 `positive_control` | 三題皆判 (iii) |
| 過濾條件對照輸入 | `tools/check.py` 的 `filter_control` | 「寫法不同而應納入」的儲存格列 3 格全納入、0 丟棄 |

**用字檢查**：本次寫入 `probe-known-limitations.md` 的新增文字經 `zhtw` 掃描，回報 errors 2、warnings 10，全數為 cross_strait 且皆在 `@domain 程式設計` 情境下命中——「對象」（建議「物件」）與「類」（建議「類別」）。兩者在本文的語意不是 object 與 class：前者是「判定對象」「比較對象」，後者是「這一類改動」。依 CLAUDE.md 規則 2 邊界，與框架既有用語一致者判定為專案 canonical、不列違規；已 grep 覆驗兩詞在 `.claude/rules/core/` 與 `docs/probes/` 既有條文中的既存用法（「對象」命中 6 檔、「一類／該類」命中 3 檔）。另 5 則「通過」warning 同屬 canonical（規格全文以「整批通過／不通過」為判準名稱）。


**zhtw 掃描口徑（L8／L15 要求標明）**：`content_type: markdown`、`profile: base`、`detect_ai: false`、`detect_translationese: false`。同一段文字改以 `profile: strict` 並開啟翻譯腔與 AI 偵測複驗，得 errors 2、warnings 17——errors 兩口徑一致，warnings 的 7 則差額全落在翻譯腔一類（被動語態 5.3/千字、翻譯腔 6.1/千字，皆為本檔既有體例）。兩口徑並列，不擇一。

**§7 逐項檢查的落點**：23 項的逐項結果寫在 `<scratchpad>/r618/key/answer-key.md` 的「機械檢查彙總（§7）」表與三題各自的檢查表。票面的「重現實驗結果」章只摘錄機械檢查與派發側四項；第 1／2／3／4／8／9／10／17 項（受測步驟一行、只用條文推正解、位置分類、非主觀要件、槓桿逐句掃描兩輪、誘因項差異與 3.4 出路、答案鍵未引已刪事實、結構對應人讀複核）逐題記在答案鍵，未轉錄進票面。

**5.5 另兩條計分紀律的執行痕跡**：寫在 `key/judge-brief.md` 的「兩條計分紀律」節，兩名判定者報告皆回報「紀律 1（推理或補述與條文牴觸）無命中、紀律 3（明示自我更正）本批未觸發」。

**計分收窄的時序**：R2-Q01 的「須寫出具體落點」與 R2-Q02 的「須觸及選擇結構的處置」寫在 `key/answer-key.md` 與 `key/judge-brief.md`，兩檔皆於派發前定稿；`out/` 九檔的寫入時間晚於兩檔，可覆驗。

**上一段的時序陳述有誤，以下為實測值更正**：`key/answer-key.md` 的 mtime 為 22:11:04，九份作答最早一份為 22:13:01，`key/judge-brief.md` 為 22:14:48。

| 檔 | 相對於派發 | 後果 |
|---|---|---|
| `key/answer-key.md`（載有兩題的計分收窄） | **早於**派發 | 收窄不是事後標定，可覆驗 |
| `key/judge-brief.md` | **晚於**九份作答落地 | 該檔的計分條文逐字取自答案鍵（內容不變），但它成檔時作答已在磁碟上——**這一點不能由檔案時間排除「知道讀數之後才寫」的解釋** |

判定語 span 的標定（`tools/spans.json`）與機械檢查同批，早於派發，不受此影響。後續輪次應在派發前一併寫完判定者 brief，使這一格與 4.6 的「span 須在跑比對之前標定」同樣可複核。此缺口不使本輪布林反向（brief 的計分條文與答案鍵逐字相同，無新增判準），依停損判準不開票，處置寫在此。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "Step 2 三題全數通過 §7 檢查清單（兩處紅燈皆改題材、零條文改動）；Step 3 三題各 3/3，兩名獨立判定者零分歧，整批布林為通過"
confidence: 0.9
acceptance_met: [0,1,2,3]
acceptance_unmet: []
artifacts: []
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
**Completion Time**: 2026-09-17 22:27
**Executing Agent**: general-purpose（W3-618-verify）
**Review Status**: pending
**Summary**: 整批布林：通過。三題各 3/3，兩名獨立判定者零分歧。R2-Q01（D1 第二列管轄欄補指涉）與 R2-Q03（7.2(3-2)①乙 窮盡要求重測）各自有覆蓋且達標；R2-Q02 雖 3/3，但依 1.7 第 (d) 條不得列為 D2 的覆蓋題，D2 登入零覆蓋清單。規格 1.1.0 四項新增條文的實測：1.7 有效（抓到 R2-Q02 的真旁路、確認 R2-Q03 的旁路已排除、零誤殺），但遮蔽單位與「確定結論」兩個粒度未界定且兩個分支處置相反，已開票 0.1.0-W3-619；4.6 位置判準有效（抓到 R2-Q02 初版題幹為交出前置結論而落入 D2 span 的真 (iii)，正向對照輸入三題皆判 (iii)），缺口是三類對「受測步驟判定對象的實例化」沒有格；5.8 三項照做無歧義，本輪零損害。本輪零條文改動，Step 2 的兩處紅燈皆依規格改題材。發現依停損處置：一則開票 0.1.0-W3-619，三則進限制清單 L25／L26／L27（1.4.0 → 1.5.0）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-617 | chars: 422 -->

### Task Reference
- 0.1.0-W3-617 what: 二分定位 W3-616 整批不通過的兩題（Q02 乙目窮盡／Q15 射程判定表第二列）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-617 why: 0.1.0-W3-616 依 docs/probes/probe-authoring-spec.md Step 3 驗收 17 題，兩名獨立判定者一致判 Q02 與 Q15 各 2/3，整批布林為不通過。依規格 Step 4，只重跑失敗的那幾題，把 W3-613 的 15 條改動清單對半切回退定位。Q15 的失分落在 W3-592 改寫的第二列被讀者接上未動的管轄欄「作為表格或列表一欄時填在該列」，讀成「填在所屬容器的格位」；Q02 的失分是 W3-563 新增的乙目窮盡要求零套用（作答改引同款前段的 (a)(b)(c)）。兩者都須先確認失敗是否由本輪改動造成。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 定位到單一改動，或已寫出「失敗不由本輪改動造成」的結論  # from 0.1.0-W3-617（來源已完成，結論見 Solution）
- [ref] [x] Q02 的題目設計缺陷（同款次內另有充分理由，L22）已在重出題時排除  # from 0.1.0-W3-617（來源已完成，結論見 Solution）
- [ref] [x] Q15 的失分是否落在未動的管轄欄已有結論  # from 0.1.0-W3-617（來源已完成，結論見 Solution）
