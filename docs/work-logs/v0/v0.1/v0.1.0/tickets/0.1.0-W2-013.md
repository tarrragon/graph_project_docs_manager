---
id: 0.1.0-W2-013
title: 補齊 UC Flow flow 未結構化態與步驟列跳轉的 SPEC-004 差距
type: IMP
status: completed
version: 0.1.0
wave: 2
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W2-010
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W2-010 驗收時比對 SPEC-004 §3.6 §2 發現組成缺三件
  final_decision: 獨立承接票補齊，不回改 W2-010 狀態
  rationale: W2-010 依其 how 欄完成且誠實記錄簡化範圍，缺口在 how 欄本身；回改已完成票的狀態不改變程式碼現況，承接票才是落地路徑
who:
  current: parsley-flutter-developer
  history: {}
what: flow 未結構化態補 ListRow.meta 與 AppText.title 與 ButtonRow 兩動作；步驟列點擊補跳轉節點詳情
when: W2-010 驗收時發現後
where:
  layer: Presentation
  files:
  - lib/screens/uc_flow/uc_flow_screen.dart
  - lib/screens/uc_flow/uc_flow_fixtures.dart
why: SPEC-004 §3.6 §2 flow 未結構化列要求 TwoColumnLayout 內為 ListRow.meta 加 AppText.title 加 EmptyState.section 含 ButtonRow 兩動作（開啟原始檔、檢視關聯）；W2-010 交出的組成與尚未選定 UC 態相同，只有 EmptyState.section，缺 UC 基本資訊兩件與兩個動作。步驟列點步驟跳節點詳情亦簡化為空操作。成因是本票上游 W2-010 的票面 how 欄由 PM 凍結時寫成簡化版，與 SPEC-004 §3.6 §2 不一致——代理人照 how 句型實作並誠實記於 Solution，但 acceptance 第三項（五態各斷言 §3.6 指定元件型別存在）仍被勾滿，因為句型本身就沒列那些元件。此為 0.1.0-W3-637 診斷的 how 句型與規格不一致模式的再次發生，且發生於該診斷完成之後新寫的票
how:
  task_type: Implementation
  strategy: flow 未結構化態依 SPEC-004 §3.6 §2 對應行補齊：Panel 內依序 ListRow.meta、AppText.title、EmptyState.section（ButtonRow 動作為開啟原始檔與檢視關聯，不含返回 Domain 視圖）。所需 UC 基本資訊欄位擴充 uc_flow_fixtures.dart。步驟列點擊接 navigateTo 至節點詳情。widget test 對該態補 §3.6 全部指定元件型別的存在斷言，不只斷言 EmptyState.section
acceptance:
- '[x] flow 未結構化態組成與 SPEC-004 §3.6 §2 對應行逐元件一致'
- '[x] 步驟列點步驟可跳轉節點詳情，退出路徑可實際操作走通'
- '[x] widget test 對該態斷言 §3.6 指定的全部元件型別存在，並含一組會因缺件而翻紅的對照'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-21T14:32:29'
completed_at: '2026-09-21T14:48:11'
created: '2026-09-21'
updated: '2026-09-21'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

flow 未結構化態補 ListRow.meta 與 AppText.title 與 ButtonRow 兩動作；步驟列點擊補跳轉節點詳情

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 逐元件比對（SPEC-004 §3.6 §2「flow 未結構化」列 vs 修改前實作）

| SPEC-004 §3.6 §2 要求 | 修改前 uc_flow_screen.dart | 差距 |
|---|---|---|
| `ListRow.meta`（型別 + 原始檔路徑） | 無 | 缺 |
| `AppText.title`（UC 標題） | 無 | 缺 |
| `EmptyState.section` | 有 | 一致 |
| `ButtonRow`：開啟原始檔 | 無 | 缺 |
| `ButtonRow`：檢視關聯 | 無 | 缺 |
| 〈UC 選擇入口〉 | 有 | 一致 |
| 步驟列「點步驟→節點詳情」 | `onTap: () {}`（空操作） | 缺 |

根因：上游 `0.1.0-W2-010` 的 how 欄簡化為與「尚未選定 UC」同一組成，
Solution 誠實記錄該簡化，但 acceptance 句型未涵蓋這些元件，故未被攔下。
本票依 SPEC-004 §3.6 §2 原文（非 how 欄轉述）逐項補齊，未發現本票 how
欄本身另有遺漏。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
`uc_flow_fixtures.dart`：`UcFlowFixtureUc` 新增 `filePath`（`docs/usecases/
<UC id>.md`，沿用 `domain_view_screen.dart._openSource` 既有路徑慣例）。

`uc_flow_screen.dart` `_UnstructuredView`：
- 補 `ListRow.meta`（leading `Badge.type(label: 'UC')`，primary 原始檔
  路徑 mono）+ `AppText.title`（UC 標題）
- `EmptyState.section` 的 `actions` 補兩個 `AppButton`（開啟原始檔
  `action-ucFlow-open-source`、檢視關聯 `action-ucFlow-view-relations`，
  經 `EmptyState` 內建包裝即為 `ButtonRow`）
- `_openSource`：完整複製 `domain_view_screen.dart._openSource` 的三結局
  邏輯（找不到檔案／opened／failed），新增 `ucFlowOpenSourceProcessRunnerProvider`
  作行程執行接縫（同慣例，本檔獨立一份，未跨檔共用——ticket 範圍限
  `lib/screens/uc_flow/`）
- 檢視關聯：`navigateTo(..., AppDestination.nodeDetail, NavIntent.jump)`
  （簡化：不額外設定 `nodeDetailStateProvider`，沿用 `trace_screen.dart`
  `onTapNode`／`gap_report_screen.dart._openItem` 既有的相同簡化——
  0.1 假資料階段 UC／步驟未帶對應節點 id，節點詳情固定顯示其
  `nodeDetailStateProvider` 預設值，非本票新增的簡化模式）

`_stepRow` 的 `onTap`：由空操作改為同一 `navigateTo` 呼叫，理由同上。

### 自檢結果

- `fvm dart analyze lib test`：0 issues
- `fvm flutter test test/ --reporter compact`：1460 通過，0 失敗
- `grep -rn "package:.*components/" lib/screens/uc_flow | grep -v
  components.dart`：0 命中（頁面層 import 僅來自
  `components/components.dart`）
- `lib/components/`、`lib/app/` 未修改

### Phase 4 結論

程式碼品質：A（完整複製既有 `domain_view_screen.dart._openSource` 與
`trace_screen.dart` onTap 簡化慣例，無新模式）。重構需要：否。技術債務：
無新增——「檢視關聯不設定 nodeDetailStateProvider」與「步驟點擊不設定
nodeDetailStateProvider」為既有簡化模式的延伸，非本票新造技術債。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
- `fvm dart analyze lib test 2>&1 | tail -1` → `No issues found!`
- `fvm flutter test test/unit/screens/uc_flow_test.dart --reporter compact` → 19 通過，0 失敗（含新增的
  鑑別力對照斷言、`action-ucFlow-view-relations` 跳轉測試、步驟列點擊跳轉測試）
- `fvm flutter test test/ --reporter compact` → 1460 通過，0 失敗（全量套件）

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-21T14:48:11
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W2-010 | chars: 654 -->

### Task Reference
- 0.1.0-W2-010 what: W3-634／W3-635 落地後，補齊 uc_flow_screen.dart 三個 _BlockedView 分支（尚未選定 UC／flow 未結構化／正常）的完整組成與對應 widget test 型別斷言（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W2-010 why: W2-002 交出 SPEC-001 §2 五態中的兩態，其餘三態因 ListRow.option（UC 選擇入口）與 TableRow.eventFlow + AppDataTable.appendix（事件流小表）兩項元件庫缺件而停手，以 SizedBox.shrink 誠實標記未完成而非湊出不符契約的替代品。資料層（state／fixtures／providers）已完整，缺的只是畫面組成。W2-002 acceptance 第 2、3、4 項因此僅 2/5 達成，由本票承接補齊（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] uc_flow_screen.dart 三個分支依 SPEC-004 §3.6 §2 對應行組成，無 SizedBox.shrink 佔位  # from 0.1.0-W2-010（來源已完成，結論見 Solution）
- [ref] [x] SPEC-001 §2 五個狀態列皆可渲染且退出路徑可實際操作走通  # from 0.1.0-W2-010（來源已完成，結論見 Solution）
- [ref] [x] widget test 五態各斷言 §3.6 指定元件型別存在  # from 0.1.0-W2-010（來源已完成，結論見 Solution）
- [ref] [x] W2-002 的 acceptance 第 2、3、4 項在本票完成後皆成立（於本票 Solution 記錄核對結果）  # from 0.1.0-W2-010（來源已完成，結論見 Solution）
- [ref] [x] uc_flow_test.dart 補 UcFlowProjectUnready 狀態的 widget test（現為零覆蓋，來源 0.1.0-W3-637 差集項 3 的測試缺口）  # from 0.1.0-W2-010（來源已完成，結論見 Solution）
