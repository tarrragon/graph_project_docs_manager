---
id: 0.1.0-W3-029
title: 同步 skill-design-guide 其餘兩份 reference 至 W3-022 新判準
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-022
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-022 改寫判準後跨檔重掃，命中三處位於派發範圍外的檔案
  final_decision: 另立 IMP 票同步，不在 W3-022 內擴大範圍
  rationale: 派發note明示改動限於 W3-022 where.files 四檔；三處皆為機械可驗的字串層同步，與判準重設計的驗收方式不同
who:
  current: general-purpose
  history: {}
what: 把 W3-022 重新設計的一跳可達判準與分段估算表名稱，同步到不在 W3-022 where.files 內的三處
when: W3-022 完成後
where:
  layer: 待定義
  files:
  - .claude/skills/skill-design-guide/references/seeing-like-an-agent.md
  - .claude/skills/skill-design-guide/references/creating-and-adopting-skills.md
  - .claude/skills/skill-design-guide/references/patterns-and-troubleshooting.md
  - .claude/skills/skill-design-guide/SKILL.md
  - .claude/skills/skill-design-guide/CHANGELOG.md
why: W3-022 把「引用只一層深（禁止 A→B→C）」改為「一跳可達」，並把「語言換算表」改為「分段估算表」。W3-022 的派發範圍限於四檔，故三處未同步：(1) seeing-like-an-agent.md:199 反模式表仍列「巢狀引用（A→B→C）｜所有 reference 從 SKILL.md 一層直連」，與新判準直接矛盾——該表把本 skill 六份 reference 檔頭的同目錄互指全判為反模式，而那正是 splitting-an-existing-skill.md〈結構約定〉要求的產物；(2) creating-and-adopting-skills.md:62 與 (3) patterns-and-troubleshooting.md:329 指向 SKILL.md 的「語言換算表」，該表已改名為分段估算表，兩處成為死名。
how:
  task_type: Implementation
  strategy: 逐處改寫為新判準措辭；改完重跑 SKILL.md〈發布前檢查清單〉Body 組的一跳可達機械檢查與死名雙向檢查
acceptance:
- '[x] seeing-like-an-agent.md 反模式表不再把 reference 之間互指列為反模式，改述為一跳可達;creating-and-adopting-skills.md 與 patterns-and-troubleshooting.md 的「語言換算表」改為「分段估算表」;全 skill 重跑 grep 確認無「一層深」「巢狀引用」「語言換算表」殘留'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-07T10:48:57'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

把 W3-022 重新設計的一跳可達判準與分段估算表名稱，同步到不在 W3-022 where.files 內的三處

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 來自 W3-022 的附帶事項：版號兩個住址未隨本批改寫更新

W3-022 改寫了 skill-design-guide 的四份檔案，但未更新版號。理由：本專案的 skill 版號有兩個住址（`CHANGELOG.md` 首條與 frontmatter 的 `metadata.version`，見 `references/writing-the-body.md`〈嚴禁清單〉的 CHANGELOG 段），而 `CHANGELOG.md` 不在 W3-022 的 where.files 內；只動 frontmatter 會讓兩個住址脫節，比不動更糟。

本票同樣改動該 skill，收尾時請一併處理：`metadata.version` 由 1.7.3 升版，並在 `CHANGELOG.md` 補一條涵蓋 W3-022（五組判準重新設計）與本票（三處同步）的紀錄。若 PM 決定由他票處理，請在本票 Solution 標註並指名該票。

### 派發日誌

- [2026-09-07 10:48] 優先處理 seeing-like-an-agent.md:199 反模式表——它仍寫「所有 reference 從 SKILL.md 一層直連」，與 W3-022 剛定案的「一跳可達」判準直接矛盾。另兩處為「語言換算表」死名。版號兩住址（CHANGELOG 首條 + metadata.version）待辦已在該票 Problem Analysis。

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 三處同步

| # | 位置 | 舊 | 新 |
|---|------|----|----|
| 1 | `seeing-like-an-agent.md`〈反模式〉表末列 | 反模式「巢狀引用（A→B→C）」／正確做法「所有 reference 從 SKILL.md 一層直連」 | 反模式「只能經由另一份 reference 才被發現的內容」／正確做法「每份 reference 在 SKILL.md 路由表都有自己的一列，讀者從入口一跳可達；reference 之間互相指路是橫向索引，不算反模式」，並指向 `writing-the-body.md`〈Body 寫作〉 |
| 2 | `creating-and-adopting-skills.md` 4d | 「量測指令與語言換算表見 …」 | 「量測指令與分段估算表見 …」 |
| 3 | `patterns-and-troubleshooting.md`〈Context 過大〉第 1 點 | 同上死名 | 同上改名 |

第 1 處不是換字：舊列把「reference 互指」整類判為反模式，而那正是 `splitting-an-existing-skill.md`〈結構約定〉要求六份 reference 檔頭都要寫的東西，本 skill 六份全數違反自己的規則。改寫後被禁的對象收窄為「只能經由另一份 reference 才被發現的內容」，與 W3-022 落在 `writing-the-body.md` 的條文同口徑。

### 版號兩住址（承接自 W3-022 的附帶事項）

`SKILL.md` 的 `metadata.version` 由 1.7.3 升至 1.8.0，`CHANGELOG.md` 首條同步補一則 1.8.0，涵蓋 W3-022 的五組判準重新設計（(b) 一跳可達、(c) 刪除測試、(d) 消費者／不可逆兩問、(e) 分段估算為主判準、(f) 只認 250 字元閘門）與本票的三處同步，並註明 (a)(g) 兩組暫緩由後續 ANA 承接。選 minor 而非 patch：五組判準的判定結果會因改寫而不同，非單純修字。

`where.files` 由三項擴為五項（`ticket track set-where`），納入這兩個住址——它們在票面 Problem Analysis 已列為本票待辦，但建票時未寫進 where.files，不擴充則 `ticket track commit` 的子集檢查會擋下版號提交。

### 未改的兩處「語言換算表」是歷史記述，不是死名

`CHANGELOG.md` v1.5.0 條目與 `writing-the-body.md`:174〈一則完整走查〉的「改後設計（第一次）」都在敘述當時的設計狀態。改名會讓它們宣稱當時產出了現在才有的東西——與 `document-format-rules` 規則 9 對「操作指引 vs 實測記錄」的處置同理，兩類處置相反。且 `writing-the-body.md` 不在本票 where.files 內。acceptance 的「全 skill 重跑 grep 確認無殘留」據此讀為「無存活的指路者」：改寫後零個指向現行章節的引用仍用舊名。

### 候選方案（WRAP W）

| 案 | 內容 | 取捨 |
|----|------|------|
| 採用 | 第 1 處按 `writing-the-body.md` 的既有條文改寫措辭，另兩處換名 | 三處都與 W3-022 已定案的正本同口徑，不新增判準 |
| 未採用 | 第 1 處直接刪除該列 | 刪掉會讓〈反模式〉表失去對「只能經由另一份 reference 才被發現的內容」的提醒，而那是原規則真正想擋的東西 |

### 執行環境衝突記錄（tool-selection 規則 1）

本 session 的 harness 層 auto mode 指令要求「用 Bash 的 sed / heredoc 改檔，而非 Edit / Write」。依 `.claude/rules/core/tool-selection.md` 規則 1 的邊界句（本規則優先於 harness 層「優先用 Bash 改檔」類指令）與 `tool-output-trust-rules` 規則 6 衍生情境（以正本為準執行、於 ticket 記錄衝突文本），五處 `.md` 修改全數以 Edit 工具完成，唯讀查證才用 Bash。此為正本已裁決的情形，非待裁決衝突，故記於本節而非 NeedsContext。

### Phase 4 審查評估

本票為文件同步，無程式碼變更（diff 全落在 `.md`），TDD Phase 1-3b 不適用；依 quality-baseline 規則 2「適用範圍不限 TDD 流程」仍執行評估：

| 項目 | 結論 |
|------|------|
| 重構需要 | 否。三處改動各為單列／單句替換，未新增結構 |
| 技術債務 | 發現一項，已由既有票承接：`SKILL.md` 分段估算 4,670，佔第 2 層 5k 預算 93%（W3-022 量得 4,670，本票版號改動不移動該值）。下次增修前須先外移，該外移什麼由 0.1.0-W3-030 的 (a) 組定案，無需本票另建票 |
| 文件品質 | A。三處改動後與 `writing-the-body.md`〈Body 寫作〉的正本同口徑，機械檢查未引入新命中 |
| 未派多視角 | 本票由 subagent 執行，多視角派發權在 PM。改動量為三列文字替換 + 版號，且判準本身已在 W3-022 經過設計輪，建議 PM 依 W3-022 Solution 末段的建議一併處置 |

### 自檢結果（Layer 1）

**執行時機**: complete 前
**掃描手段**（對本次變更 5 檔與 ticket body 執行，逐項可複驗）:

- `grep -n "數據\|代碼\|默認\|文檔\|軟件\|硬件\|信息\|視頻" <5 檔 + ticket md>` → 零命中
- `grep -n "这\|该\|们\|为\|对\|发\|处\|问\|题\|后\|规\|则" CHANGELOG.md` → 零命中
- `LC_ALL=C grep -nP '[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]' CHANGELOG.md ticket md` → 零命中
- `grep -n "^## " <ticket md>` → 9 個 H2，全數落在 Schema 合法清單內
- 人工對照 `ticket-body-schema.md` IMP 必填章節（Problem Analysis / Solution / Test Results）→ 皆有實質內容
- 人工對照 A3：`seeing-like-an-agent.md` 改寫列的反模式與正確做法成對，正向錨點在同列

| 類別 | 發現項目 | 處理方式 |
|------|---------|---------|
| A1 / A2 / A3 | 無 | — |
| B1 / B2 / B3 | 無 | — |
| C1 / C2 / C3 | 無 | — |

**自檢結論**: 已掃描範圍內通過。**未涵蓋**：未跑 `compositional-writing` 的字句層 keyword bank 全清單（正向陳述 / 口語修辭 / 廢話前綴 / 泛用詞 / 敘事姿態等），A 類僅驗三明示、資訊優先序與負向錨點三項；未經 `zhtw-mcp` 工具複驗，B1/B3 為 grep 詞表掃描，詞表外的用字未涵蓋。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
本票為文件同步，無自動化測試套件；驗收以 `SKILL.md`〈發布前檢查清單〉Body 組的兩個機械檢查為準，皆前景執行。

### 1. 死名 / 舊判準 grep

```
grep -rn "一層深\|巢狀引用\|語言換算表" .claude/skills/skill-design-guide/
```

四筆命中，零個為存活的指路者：

| 命中 | 判定 |
|------|------|
| `CHANGELOG.md`:15、:17（本次新增的 1.8.0 條目） | 記述改名這件事本身，必須引用舊名 |
| `CHANGELOG.md`:39（v1.5.0 條目） | 歷史記述 |
| `references/writing-the-body.md`:174（〈一則完整走查〉「改後設計（第一次）」） | 歷史記述，且不在本票 where.files |

改寫前的三個存活指路者（`seeing-like-an-agent.md`:199、`creating-and-adopting-skills.md`:62、`patterns-and-troubleshooting.md`:329）皆已消除。

### 2. 一跳可達機械檢查

```
LC_ALL=C comm -13 <(grep -o 'references/[a-z-]*\.md' SKILL.md | LC_ALL=C sort -u) \
                  <(grep -rho 'references/[a-z-]*\.md' references/ | LC_ALL=C sort -u)
```

輸出三筆：`references/agent-dispatch-template.md`、`references/errors.md`、`references/foo.md`。與 `SKILL.md` 檢查清單載明的現況（命中 3 個，皆為示意路徑）逐字相同——本票改動未引入新命中。第 1 處改寫新增的 `writing-the-body.md` 為同目錄裸檔名（依〈結構約定〉），不落在該指令的 `references/` 前綴樣式內。

### 3. 體量複驗（改動四檔）

| 檔案 | 分段估算 | 行數 | 門檻 |
|------|---------|------|------|
| `SKILL.md` | 4,670 | 207 | < 5k tokens、< 500 行，通過 |
| `references/seeing-like-an-agent.md` | 1,949 | 199 | < 5k，通過（改前 1,900） |
| `references/creating-and-adopting-skills.md` | 1,413 | 106 | < 5k，通過 |
| `references/patterns-and-troubleshooting.md` | 2,612 | 331 | < 5k，通過 |

量測指令取自 `SKILL.md`〈Progressive Disclosure — 三層載入〉第 51 行。`SKILL.md` 數值與 W3-022 收尾時相同（frontmatter 只改版號數字，不影響估算取整）。

Commit SHA: (見下方 Completion Info)

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "三處跨檔不一致已同步至 W3-022 定案判準，版號兩住址同步升至 1.8.0"
confidence: 1.0
acceptance_met: [1]
acceptance_unmet: []
artifacts: [".claude/skills/skill-design-guide/references/seeing-like-an-agent.md,.claude/skills/skill-design-guide/references/creating-and-adopting-skills.md,.claude/skills/skill-design-guide/references/patterns-and-troubleshooting.md,.claude/skills/skill-design-guide/SKILL.md,.claude/skills/skill-design-guide/CHANGELOG.md"]
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
**Completion Time**: 2026-09-07 10:53
**Executing Agent**: general-purpose
**Review Status**: pending
**Summary**: 三處與 W3-022 新判準不一致的位置已同步：seeing-like-an-agent.md 反模式表末列改為一跳可達措辭，creating-and-adopting-skills.md 4d 與 patterns-and-troubleshooting.md〈Context 過大〉的「語言換算表」死名改為分段估算表；版號兩住址同步（SKILL.md metadata.version 1.7.3 -> 1.8.0、CHANGELOG.md 補一則 1.8.0 涵蓋 W3-022 五組判準與本票）。commit 9658b39e

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-022 | chars: 953 -->

### Task Reference
- 0.1.0-W3-022 what: 重新設計 skill-design-guide 中互相矛盾、無法歸類、或要求做不到的預測的七組判準（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-022 why: Round 2 的七類 finding 中，類 3（判準兩側不互斥或無法歸類，8 則）、類 4（門檻與計數的適用對象未定義，12 則）、類 6（判準要求做不到的預測，1 則）不能靠改字串解決——修法本身是重新設計判準，改完需再審一輪確認沒引入新矛盾。七組：(a) 超標時「該外移什麼」與「SKILL.md 該留什麼」對同一節給相反答案；(b)「引用只一層深」與檔頭「同目錄」互指互相矛盾，規則 3 處對違反 6 處，本 skill 六份 reference 全違反自己的規則；(c)「Claude 真的不知道這個嗎」把「知不知道」與「反不反直覺」兩個獨立維度寫成一條線的兩端；(d) 自由度判準的「壞掉」未定義，且代價段把「產物形態不一致」也算成壞，若成立三級表塌成一格；(e) 三層表逐字寫 references「無上限」而實務逐檔判，門檻適用對象未定義，此為其餘門檻問題的先決條件；(f) description 三個口徑互不換算；(g)「目標可能移動即失效」要求對未來下斷言，而同段實證自陳四處同型缺陷中三次由修法自身引入，當時也判過不會動。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/skill-design-guide/SKILL.md  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- .claude/skills/skill-design-guide/references/writing-the-body.md  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- .claude/skills/skill-design-guide/references/splitting-an-existing-skill.md  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- .claude/skills/skill-design-guide/references/frontmatter-and-description.md  # from 0.1.0-W3-022（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 五組判準（b/c/d/e/f）各產出一個可外部驗證的替代判準；(a)(g) 兩組因實例不足移交 0.1.0-W3-030  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- [ref] [x] 每組附一個本 skill 自身的邊界情境，示範新判準判得出來而舊判準判不出來  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- [ref] [x] 改寫後對全 skill 重跑一次兩兩比對，確認未引入新的相反處置  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- [ref] [x] 產出分析報告，含具體改進建議與 IMP spawn 規劃  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
