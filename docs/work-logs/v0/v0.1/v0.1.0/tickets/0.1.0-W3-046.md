---
id: 0.1.0-W3-046
title: 修正 gitignore 的 .claude/**/*.lock 把依賴鎖定檔與執行期鎖檔歸為一類
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-044
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-044 查出 uv.lock 被判孤兒的機制是 gitignore 而非他方貢獻
  final_decision: 成 IMP 票，與 W3-044 分開（一在判定端一在忽略規則）
  rationale: 兩者可各自獨立修復；即使孤兒判定修好，pattern 仍會使 uv.lock 無法經 push 傳播
who:
  current: thyme-python-developer 或主線程
  history: {}
what: 把該 pattern 由副檔名分類改為用途分類，使 uv.lock 納入版控而執行期鎖檔仍被忽略
when: 本票建立後可執行；每次 sync-push 都因此把 uv.lock 判為孤兒
where:
  layer: framework
  files:
  - .gitignore::write
why: 第 99 行的 .claude/**/*.lock 註解寫「框架執行期鎖檔的類別式涵蓋」，用意是 dispatch-active.lock 與 scheduled_tasks.lock 那類執行期鎖檔。但 uv.lock 是 uv 的依賴鎖定檔，性質相反、應納版控。該 pattern 以副檔名分類而非用途分類，把兩種語意相反的檔案歸為一類。實測後果：本專案的 .claude/scripts/uv.lock 存在於磁碟且與 canonical 同為 40004 bytes，卻因被忽略而不進 git archive HEAD，使 sync-push 的孤兒偵測判定本專案沒有該檔並建議清除——執行後該檔自 canonical 消失。與 framework issue 57「uv.lock 未納版控」為同一件事，本票查出其機制
how:
  task_type: Implementation
  strategy: 盤點 .claude/ 下所有 .lock 檔並分為執行期鎖檔與依賴鎖定檔兩類 -> pattern 改為逐類指定（執行期鎖檔續忽略，uv.lock 納入版控）-> git add 該檔 -> 以 git check-ignore 驗兩類判定正確 -> 確認 W3-036 改過的 gitignore 行為測試仍通過
acceptance:
- '[ ] uv.lock 納入版控，git check-ignore 對它回未忽略'
- '[ ] 既有執行期鎖檔（dispatch-active.lock、scheduled_tasks.lock、.allocator.lock）仍被忽略'
- '[ ] test_save_ticket_race.py 的 gitignore 行為測試通過（該測試已於 W3-036 改為驗 git check-ignore）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

把該 pattern 由副檔名分類改為用途分類，使 uv.lock 納入版控而執行期鎖檔仍被忽略

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer 或主線程
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-044 | chars: 635 -->

### Task Reference
- 0.1.0-W3-044 what: 孤兒偵測改為以 git 歷史判定推送方是否真的刪除過該路徑，未擁有過的不列為孤兒、不建議 --clean

### Rationale Chain
- 0.1.0-W3-044 why: 孤兒提醒的文字是「本地已刪除但遠端殘留的 tracked 檔」，而該集合實際包含兩種成員：推送方刪除後未傳播的，以及其他 consumer 推上 canonical 而推送方從未擁有的。兩者在推送方視角下完全同形。實測後果：一個 consumer 於 08-28 推上 scripts/uv.lock，本專案於 09-07 帶 --clean 推送時工具將其列為孤兒並建議清除，執行後該檔自 canonical 消失，該 consumer 下次 pull 時本地檔被刪。PM 當時不但照做，還向使用者複述工具的理由「不清會隨 full overlay sync 被其他 consumer pull 回去」——把他方貢獻描述成該清的殘留。措辭誤導與判定缺陷同時存在，且措辭使人不會去質疑判定

### Related Files
- .claude/scripts/sync-claude-push.py::write  # from 0.1.0-W3-044
- .claude/scripts/tests::write  # from 0.1.0-W3-044

### Source Acceptance (reference)
- [ref] [ ] canonical 有而推送方 git 歷史從未擁有的路徑不被列為孤兒，且提醒文字明示它可能是他方貢獻  # from 0.1.0-W3-044
- [ref] [ ] 推送方確有刪除記錄的路徑仍正常列為孤兒並建議 --clean  # from 0.1.0-W3-044
- [ref] [ ] 新增測試覆蓋上述兩類，既有 sync_claude_push 測試全數通過  # from 0.1.0-W3-044
- [ref] [ ] 分類為三格而非兩格，且第三格（磁碟存在但未追蹤）的判別排在歷史查詢之前  # from 0.1.0-W3-044

