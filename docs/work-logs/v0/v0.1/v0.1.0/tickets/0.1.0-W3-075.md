---
id: 0.1.0-W3-075
title: 設計 測試對象目錄與測試契約欄位：從規格推導要測什麼、每項定義到什麼程度
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-074
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-074
dispatch_reason: ''
decision_tree_path:
  entry_point: 框架缺口（測試側缺元件庫 skill 的等價物）
  final_decision: ANA 評估是否建立測試對象目錄與契約欄位，並給切分
  rationale: 測試設計品質現依賴 Phase 2 代理人當下判斷，無可查目錄；元件庫 skill 已證明「把設計決策前移為查表」的形態有效
who:
  current: saffron-system-analyst
  history: {}
what: 設計 測試對象目錄與測試契約欄位：從規格推導要測什麼、每項定義到什麼程度
when: 0.1.0-W3-074 分類結論定案後（目錄的欄位須含形態，形狀取決於該結論）
where:
  layer: Domain
  files:
  - .claude/skills/tdd/references/doc-handoff.md::read
  - .claude/skills/tdd/references/layered-test-strategy.md::read
  - .claude/skills/component-contract-design::read
  - docs/spec/ui/SPEC-004-component-library.md::read
why: '`component-contract-design` 的核心產出是元件目錄加逐元件契約欄位，畫面票因此只剩「選件與排位」。測試側無對應物：`doc-handoff.md` 有規格→測試的映射與 GWT 轉換，`layered-test-strategy.md` 有「哪一層要測」，但沒有「**要測哪些對象**」的目錄，也沒有「**每個測試對象要定義到什麼程度**」的欄位表。


  三處具體落差（對照元件庫 skill）：起點只有「有規格」一條，缺接手模式（既有測試如何盤點、補契約）；完成判準只有覆蓋率數字（行為場景 100%、分支 100%），那是量的判準，沒有質的可機械檢查；無派發語言，測試票仍要實作者自行決定測什麼。


  後果：測試設計品質依賴 Phase 2 代理人當下的判斷，不同票之間無一致性可言，且「這個測試對應規格的哪一條」無處記載——這正是「設計出與專案無關的測試」的結構成因（用戶 2026-09-08 指出）。'
how:
  task_type: Analysis
  strategy: '先讀 `component-contract-design` skill 全套（三種起點、契約欄位表、齊全定義、派發語言、分工邊界）建立對照基準，再逐項判斷測試側的對應物是否成立、該長什麼樣：


    1. **測試對象的單位是什麼**（UC 場景／元件／呼叫點／其他）——單位選錯會使目錄與既有 spec 重複，此判定須附「與既有 spec 不重複」的論證。

    2. **契約欄位**：對象、來源規格條目、測試形態、斷言來源、fixture 需求、跳過條件與理由、反例；每欄附缺失後果。

    3. **齊全定義**：可機械檢查的完成判準，與既有覆蓋率數字（行為場景 100%、分支 100%）的關係須釐清——是取代、補充，或兩者測不同的東西。

    4. **三種起點的推導程序**：有規格／有既有測試無契約／有程式碼無測試，各自的步驟。

    5. **派發語言**：測試票的 how 應可寫成什麼句型（元件庫的對應物是「用元件 X 的變體 Y 放進容器 Z 的第 N 格」）。


    落點依 0.1.0-W3-074 的結論。**不在本票**：實作任何目錄或 skill；本票只出設計與切分。'
acceptance:
- '[ ] 測試對象的單位已判定並附理由（與既有 spec 不重複的論證）'
- '[ ] 契約欄位表草案完成，每欄附缺失後果'
- '[ ] 齊全定義為可機械檢查的形式；與既有覆蓋率判準的關係已釐清'
- '[ ] 三種起點的推導程序各有步驟；派發語言給出測試票 how 的目標句型'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

設計 測試對象目錄與測試契約欄位：從規格推導要測什麼、每項定義到什麼程度

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-074 | chars: 1214 -->

### Task Reference
- 0.1.0-W3-074 what: 為跨 app 邊界的功能補上測試分類（框架層：tdd 五層分工表缺測試形態軸；專案層：三個呼叫點的驗證形態與 W3-064／W1-036 待驗項承接），並評估改進成果的落點（改既有 skill 或新建）

### Rationale Chain
- 0.1.0-W3-074 why: **框架層缺口**：`tdd/references/layered-test-strategy.md` 的五層分工表以**架構層級**為軸（UI／Application／UseCase／Interface／Domain），缺**測試形態**軸。跨 app 邊界的功能在表上落於 Layer 1（整合測試），但該格指定的方法做不到——`integration_test` 無頭執行，觸不到系統權限對話框、通知中心、原生檔案選擇器、真實 process。表上有位置、位置上的方法無效，且無任何條文說明此情形，讀者依表執行會得到「已覆蓋」的假象。

**專案層實例**：CLAUDE.md §6 定測試策略為雙層（`test/` 契約 + `integration_test/` 行為），本專案三個跨邊界呼叫點（`macos_scan_notifier.dart` 系統通知、`workspace_repository.dart` 資料夾存取、`gap_report_screen.dart` 外部開啟與 doc CLI process）**只有 fake 測試**，平台行為從未被驗證。SPEC-003 §2.2「實作票驗證」段明文要求四項平台事實由實機測得後回填，0.1.0-W3-064 的 acceptance 2、3 因此未勾；`ExternalOpener`（W1-036）同類。`.claude/skills/verify/` 不存在，session 啟動每次警告「驗證環境未就緒」而無人承接。

**為何是框架問題而非單票問題**：三個功能落在同一個縫隙，逐票各自決定會產生三種不一致的做法；且此缺口與載體無關（任何有平台互動的專案都會撞到），替換專案名稱與路徑後仍成立。

### Related Files
- docs/tech-decisions.md  # from 0.1.0-W3-074
- CLAUDE.md  # from 0.1.0-W3-074
- integration_test::read  # from 0.1.0-W3-074
- test::read  # from 0.1.0-W3-074
- lib/services/macos_scan_notifier.dart::read  # from 0.1.0-W3-074
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [ ] 跨邊界判準已定義，並判定它與既有五層的關係（新的一層或正交屬性），該判定的依據寫入結論  # from 0.1.0-W3-074
- [ref] [ ] layered-test-strategy 表已補形態軸或等效結構；doc-handoff 的分工框架已檢查並同步或說明不需同步  # from 0.1.0-W3-074
- [ref] [ ] 落點評估兩案（改既有 skill／新建 skill）各附可執行切分與四項判準的比較，並給結論  # from 0.1.0-W3-074
- [ref] [ ] 本專案三個呼叫點逐項標驗證形態；決議寫入 tech-decisions，CLAUDE.md §6 同步或說明不需  # from 0.1.0-W3-074
- [ref] [ ] W3-064 與 W1-036 的待驗項各有承接票或 spawn 落地紀錄  # from 0.1.0-W3-074
**前置票 0.1.0-W3-074 的 A2 結論（2026-09-08）**：跨邊界判定為**正交屬性**而非新的一層。依據：跨邊界功能散布多個架構層（MethodChannel 在 Domain 層、UI 觸發在 Presentation 層、權限 gate 在 Application 層），不佔單一位置；本專案 31 個可互動元素中僅 3 個跨邊界，獨立成層則多數格為空；且 layered-test-strategy 既有〈分層之外的兩個補位形態〉（stub 盲區與語意級假後端、characterization test）已是正交先例，新增第三個補位形態與該結構一致。**對本票的影響**：測試對象目錄不需獨立的跨邊界層級，改以欄位承載（每個對象標其驗證形態）。落點結論為改既有 skill（方案 a），本票的設計產出亦應以此為前提。
