---
id: 0.1.0-W3-335.38
title: 補齊 V4 歸因的 49 項 spec 真缺口：以 WRAP 定案內容並分 spec 檔寫追修草案
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy:
- 0.1.0-W3-335.37
relatedTo:
- 0.1.0-W3-335.36
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-335.36 歸類 D
  final_decision: WRAP 定案後分檔追修
  rationale: 用戶裁示簽核改由 WRAP
who:
  current: general-purpose
  history: {}
what: 補齊 V4 歸因的 49 項 spec 真缺口：以 WRAP 定案內容並分 spec 檔寫追修草案
when: W3-335.37 完成後
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.38.md
why: W3-335.36 歸類 D：49 項 SPEC-001/003/004 皆未描述，照文件實作會各自假設；用戶裁示簽核改由 WRAP 評估（風格偏好除外）。blockedBy .37，因為部分缺口與矛盾裁決相鄰，先定矛盾再補缺口避免互相推翻
how:
  task_type: Analysis
  strategy: 讀 .36 Solution 各題新猜測點歸因表中歸類 D 的項次與 .37 裁決 → 先判定每項該由哪一層承接（SPEC-001 狀態／SPEC-003 互動／SPEC-004 元件／SPEC-002 token／畫面票 how／domain 規格）→ 需決定內容者跑 WRAP（快速模式，影響多畫面者完整模式）→ 依承接檔分組寫追修草案
acceptance:
- '[x] 49 項逐項：承接層、WRAP 結論（或「屬實作期決定，不入規格」及理由）、追修改法'
- '[x] 牽涉用戶寫程式碼風格偏好者標「風格偏好，交用戶」'
- '[x] 依承接檔分組的追修清單，每組項數與改動章節；本票不改 spec、不建票'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T17:33:14'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補齊 V4 歸因的 49 項 spec 真缺口：以 WRAP 定案內容並分 spec 檔寫追修草案

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

W3-335.36 歸類 D（spec 未描述）的項次，照 SPEC-001／003／004 實作時各自需要假設。成因分三型：

| 型 | 說明 |
|----|------|
| 真缺口 | 上游與元件庫皆無條文（如搜尋比對欄位、排序比較語意、多父節點） |
| 答案已在未納入探針輸入的文件 | SPEC-004 條目內文、SPEC-003 §2.13／§3.5、PROP-004、框架 `track_board.py` 已有答案，.36 對照時漏查 |
| 已被 .37 裁決連帶消解 | R1、R3、R5 改寫後該猜測點不再存在 |

### 影響範圍

- 已驗證：.36 表格中主歸類標 D 的列實數為 59（仍猜 1、V4-01 17、V4-02 17、V4-03 12、V4-04 12），與 .36〈各歸類計數〉表的「49」不符；計數表逐欄皆偏低（V4-01 16／V4-02 14／V4-03 7／V4-04 11）。本票逐項處理全部 59 列
- 已驗證：Domain／UC Flow／Ticket 清單／追溯／破洞畫面尚未實作（.37 Problem Analysis 已查），改規格不牽動既有畫面程式碼
- 未驗證：PRIORITY_LEVELS 的實際成員值（只引用常數名，未列舉）

### 相關 Error Pattern

無新增。「計數表與逐項表不一致」屬 bash-tool-usage 規則八同族（聚合統計未交叉驗證），本次以逐列重數發現，不另立 pattern。

## 重現實驗結果
### 實驗方法

本票為規格補缺，「重現」＝逐項以猜測點原文（探針 V4-01～04〈猜測點表〉）查證答案是否已存在，再判承接層。查證來源：SPEC-001 全文；SPEC-003 §2.2–§2.11、§2.13〈因判準而不顯示時的日誌承接〉、§3.1–§3.7、§4、§5；SPEC-004 §3.6、§3.7 與 4.5、4.6、4.8、4.9、4.10、4.12、4.19、4.23、4.25、4.35、4.36、4.38、4.39、4.40、4.41 條目（grep 關鍵詞後讀相關列）；SPEC-005 全文；PROP-004〈Ticket 清單的分組軸〉；UC-04、UC-05；`docs/tech-decisions.md`〈載入策略：依視圖惰性〉；框架 `track_board.py` 主題排序與 `topic_assignments.py` 讀取語意；`lib/l10n/app_zh.arb` 相關 key。

### 實驗執行

逐列查證結果摘要（完整逐項見 Solution）：

| 結果 | 項數 | 項次 |
|------|------|------|
| 已由 .37 R 項消解 | 4 | V4-01 G20、G29（R1）；V4-02 G27（R3）、G43（R5） |
| 非缺口（答案已在規格內） | 13 | V4-01 G15、G23、G31、G37；V4-02 G25、G28、G29、G35、G41；V4-03 #10、#13、#18；V4-04 G15 |
| 屬 CLAUDE.md §6／domain-map §9 現行待決，不入規格 | 3 | V4-02 G07；V4-03 #20；V4-04 G08 |
| 真缺口，需補規格 | 39 | 其餘 |

### 實驗發現

- 已驗證：59 列中 20 列不需追修（4 消解、13 非缺口、3 屬待決），39 列需補規格
- 已驗證：Ticket 解析依視圖惰性（tech-decisions），Domain 載入不含 ticket；此事實決定 V4-01 G1（N 為檔案數）與 V4-03 #23（追溯 Ticket 層依 Ticket 清單載入）的裁決方向
- 已驗證：SPEC-001 §8.2 自述為 0.1 窮盡清單；本票新增三則「不改變狀態的 SnackBar」（選擇器不可用、資料夾不可讀、帶目標跳入目標不存在），須同步入 §8.2，否則 R12 改寫後的 FR-08(b) 會把它們判為違反
- 未驗證：選擇器回傳的資料夾在實機上是否可能不可讀（沙盒關閉下 file_selector 只列可見目錄）；V4-04 G22 的裁決不依賴其出現率

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 評估框架

WRAP 快速模式逐項（錨點 → W 含反向框架 → R 事證 → A 牽動處數與 UC 損益 → P 失敗訊號）；牽動兩個以上畫面或跨三檔者（S-10、S-18、S-27、S-28、S-17／S-32、S-13／S-38）走完整模式，列出選定方案三個缺點。錨點同 .37：UC 使用者目標與成功保證、PROP-004、SPEC-001 FR-01／FR-03／FR-08、SPEC-003 §2.2 INV-FEEDBACK-001。

承接層代碼：`S1`＝SPEC-001 狀態／顯示；`S3`＝SPEC-003 互動；`S4`＝SPEC-004 元件；`S2`＝SPEC-002 token；`HOW`＝畫面票 how；`DOM`＝domain 規格；`IMPL`＝屬實作期決定不入規格；`PEND`＝屬 CLAUDE.md §6／domain-map §9 現行待決。

### 範圍更正

.36〈各歸類計數〉表記 D 為 49，逐項表主歸類標 D 的列實數為 59。本票處理全部 59 列，並把其中 20 列判為不需追修（下表），其餘 39 列編號 S-01～S-39。

### 不需追修的 20 列

| 項 | 判定 | 依據（檔名＋章節） |
|----|------|------------------|
| V4-01 G20 主題態是否受搜尋篩選作用 | 已由 R1 消解 | .37 R1 |
| V4-01 G29 搜尋隱藏且位於收合節的順序 | 已由 R1 消解 | R1 使主題態不套用搜尋篩選、列表態無收合節，兩條件不可能同時成立 |
| V4-02 G27 三個阻擋態根錨點名 | 已由 R3 消解 | .37 R3（錨點名見 SPEC-003 §4 #2） |
| V4-02 G43 選中格 UC ≠ 選定 UC 時在泳道中檢視 | 已由 R5 消解 | .37 R5 |
| V4-01 G15 blockedBy ID 是否可點、過長截斷 | 非缺口 | SPEC-004 4.35 `ticket` 變體：blockedBy 為 `AppText.mono` 以「, 」串接、單行截斷；朗讀標籤含完整值；整列唯一可點處為開票 |
| V4-01 G23 主題態每節是否有欄首列 | 非缺口 | SPEC-004 §3.6 §4 正常 · 主題列無 `TableRow.header`；SPEC-003 §3.4 S6 排序只於列表態 |
| V4-01 G31 Tab 進入虛擬捲動表格 | 非缺口 | SPEC-004 4.36 回饋契約焦點列：表頭可排序格 → 列依序，`virtual` 下焦點移至未建構列時自動捲入 |
| V4-01 G37 可點徽章與非互動標記形態區分 | 非缺口 | SPEC-004 4.6 `IssueMarker`（`InkWell`、`Semantics.button`、三變體皆可點）vs 4.5 `Badge`（元件樹無 `InkWell`、非互動） |
| V4-02 G25 載入完成時畫面不可見的日誌 | 非缺口 | SPEC-003 §2.13〈因判準而不顯示時的日誌承接〉不觸發列：走承接 (b) 者結果為持久狀態，不記；SPEC-001 §8.1 §1 正常 · 矩陣列判 (b1) |
| V4-02 G28 泳道兩軸捲動 | 非缺口 | SPEC-004 4.38 用途：二維捲動 + 拖曳 |
| V4-02 G29 泳道節點是否可點 | 非缺口 | SPEC-004 4.16 `SwimlaneNode`：0.1 不可點、不可拖（.36 已自註為 A） |
| V4-02 G35 空圖內容區是否另設切換專案按鈕 | 非缺口 | SPEC-003 §3.1〈導航跳轉與退出〉空圖列：切換專案 = `project-switcher-entry`；SPEC-004 §3.6 §1 空圖列 `ButtonRow` 無切換專案 |
| V4-02 G41 檢視詳情展開後標籤是否變化 | 非缺口 | SPEC-004 4.23 無障礙：按鈕 `expanded` 等於 `isDetailExpanded`，label 固定 `viewSchemaDetailAction` |
| V4-03 #10 正常態是否顯示 UC 基本資訊 | 非缺口 | SPEC-001 §2 正常顯示欄未列；SPEC-004 §3.6 §2 正常列無 `ListRow.meta`／`AppText.title` |
| V4-03 #13 domain 標籤與整列可點的巢狀命中 | 非缺口 | SPEC-004 4.35 回饋契約按下確認列：點 domain 格或標記格不觸發列 `onTap`，由 4.19／4.6 承載 |
| V4-03 #18 孤立事件判準 | 非缺口 | SPEC-003 §3.5〈孤立事件判準〉（探針輸入範圍外，非規格缺） |
| V4-04 G15 斷邊關聯項呈現 | 非缺口 | SPEC-004 4.19 `damaged` 狀態：外包 `IssueMarker.damagedEdge`（虛線框），點擊跳破洞報告 |
| V4-02 G07 矩陣列來源與骨架列數 | PEND | CLAUDE.md §6「Domain 視圖的列無來源」；骨架列數由 SPEC-004 4.24 `skeleton` `matrix` 版位固定，不依賴真實列數 |
| V4-03 #20 各跳父子欄位 | PEND | domain-map §9；UC → Ticket 資料來源由 `0.1.0-W3-365` 承接，第二跳欄位同列 §9 |
| V4-04 G08 破洞項指向節點型別的判定 | PEND | CLAUDE.md §6「破洞分類」；SPEC-003 §3.5〈破洞項的指向節點〉已明示不在規格 |

風格偏好：59 列皆為產品行為、版面語意或資料規則，無一牽涉用戶寫程式碼的風格偏好，0 項交用戶。

### 逐項評估：Ticket 清單（S-01～S-12，探針 V4-01）

#### S-01（仍猜 V4-01 G1）、S-02（V4-01 G03）N 值來源與取得方式

承接層：S1（SPEC-001 §4 未載入）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | N＝Ticket 檔案數：列舉 ticket 目錄下的票檔，不解析；含日後解析失敗者 |
| O2 | 新增 | N＝有效票數：需先輕量解析 |
| O3 | 零新增（反向框架） | 未載入態不顯示 N，只顯示「載入 ticket」 |

**R**：`docs/tech-decisions.md`〈載入策略：依視圖惰性〉——frontmatter 無法只解析半個 block，O2 等於提前付 1.9 秒，破壞惰性；SPEC-003 §2.6 `state-tickets-loading` 為 determinate，分母「總數 N 已於未載入態顯示」，分母必須在解析前可得，唯 O1 滿足；O3 違反 UC-05 驗收「載入提示標明筆數」。
**裁決**：O1。**被放棄選項優點**：O2 的數字與載入後列數一致（O1 在全部檔案可解析時亦一致，見 S-07）。
**P 失敗訊號**：列舉在大量檔案下造成首次可見卡頓 → 改為背景列舉並定義列舉中呈現。
**改法**：SPEC-001 §4 未載入顯示欄「N 為該專案實際票數」改「N 為 ticket 檔案數（列舉不解析，含之後解析失敗者；與載入中進度的分母相同）」。S-02 同 S-01；i18n key `ticketsLoadPrompt` 既有，不改。

#### S-03（V4-01 G04）N=0 時的行為

承接層：S3（SPEC-003 §3.4〈生命週期〉）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | N=0 仍進未載入態，開始載入可按，載入立即完成落無 ticket |
| O2 | 改造既有（反向框架：讓未載入態不存在） | N=0 時首次可見直接落無 ticket |
| O3 | 改造既有 | N=0 時開始載入 disabled + 常駐原因 |

**R**：SPEC-001 §4 無 ticket 進入條件「載入完成但票數為 0」、SPEC-003 §3.4〈生命週期〉「首次可見進入未載入、不觸發解析」——O2 須改兩處進入條件與 §4 #15／#19；O3 使未載入態失去前進動作，且 FR-03 對應的無 ticket 態永遠不可達；使用本框架的專案幾乎必有 ticket，N=0 為低基本率路徑。§2.6 最短顯示時間仍使載入態可斷言。
**裁決**：O1。**被放棄選項優點**：O2 省一次無意義點擊。
**P**：實機驗證時使用者在 N=0 未載入態停頓不知所措 → 重評 O2。
**改法**：SPEC-003 §3.4〈生命週期〉補一列「N 為 0」：「仍進入 `state-tickets-unloaded`；按 `action-tickets-start-load` 後依 §2.6 最短顯示時間渲染載入態，完成落 `state-tickets-empty`」。

#### S-04（V4-01 G07）整批 IO 錯誤、ticket 目錄於載入中消失

承接層：S1（SPEC-001 §4 註記，併 S-07）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | 逐檔讀取失敗依 Gate〈文件解析〉「單檔失敗不中止整輪」歸為損壞票；目錄消失＝全部檔案讀取失敗＝全部損壞 |
| O2 | 新增 | 新增「載入失敗」狀態 + 重試 |
| O3 | 改造既有 | 回未載入態並 SnackBar |

**R**：SPEC-001〈Gate 三問對照〉文件解析列已定單檔失敗不中止；SPEC-003 §3.1 開啟 docs 目錄列對「目錄消失屬外部變更，由下一次重新載入承接，本畫面不設偵測點」已有同型先例；O2 增一狀態，連動〈狀態總數〉與 §4 對照表（R9 已 34→39，再加使 .40 範圍變動）；O3 的 SnackBar 屬狀態轉換伴隨提示，與 R12 改寫後 FR-08(b)「進入轉換本身不觸發」衝突。
**裁決**：O1。**被放棄選項優點**：O2 對整批失敗給出明確說明與重試。
**P**：假資料「損壞資料」快照在實機出現整批失敗時，全列損壞的列表無法讓使用者判斷原因 → 重評 O2。
**改法**：併入 S-07 的 SPEC-001 §4 註記。

#### S-05（V4-01 G10）搜尋比對欄位與規則

承接層：S3（SPEC-003 §3.4〈互動反應〉搜尋列）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增條文 | 比對 ID 與標題；子字串；不分大小寫；搜尋詞前後空白忽略 |
| O2 | 新增條文 | 比對全部可見欄（含狀態、優先、blockedBy） |
| O3 | 新增條文（反向框架：最小） | 只比對標題 |

**R**：UC-05 替代場景〈列表模式篩選〉「以搜尋與篩選定位特定票」——狀態與優先已有篩選，O2 與篩選功能重疊；O2 含 blockedBy 時搜尋某 ID 會混入「被它阻擋的票」，結果語意不一；O3 無法以 ID 定位，而 ID 是框架中定位一張票的主要字串。
**裁決**：O1。**被放棄選項優點**：O2 的 blockedBy 比對可反查「誰被 X 阻擋」。
**P**：使用者以 blockedBy ID 搜尋得零筆 → 重評是否加 blockedBy 篩選（非搜尋）。
**改法**：SPEC-003 §3.4〈互動反應〉搜尋列可觀察結果補「比對範圍為 ID 與標題兩欄；子字串比對、不分大小寫、忽略搜尋詞前後空白；任一欄命中即保留該列」。

#### S-06（V4-01 G12）排序比較語意

承接層：S3（SPEC-003 §3.4〈排序欄首的元件級契約〉新增 S8）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增條文 | ID 自然序（以 `-`、`.` 分段，數字段依數值比）；標題依 Unicode code point；狀態依框架 ticket 生命週期序；優先依框架 `PRIORITY_LEVELS` 序（P0 在前為 asc）；同值依載入序（穩定） |
| O2 | 新增條文 | 全部欄位字串字典序 |
| O3 | 新增條文 | 標題依 locale collation，其餘同 O1 |

**R**：O2 使 `335.30` 排在 `335.4` 前，本專案 ticket ID 大量含多段子序號，直接失效；O3 的 locale collation 使測試結果依執行環境語系而變（`test-assertion-design-rules` 不可靠斷言判準：環境因素）；S3 已定 `none` 為載入序，同值取載入序與之一致；優先序比照框架 `track_board.py` 主題排序使用的 `PRIORITY_LEVELS`。
**裁決**：O1。**被放棄選項優點**：O3 對中文標題的排列較符合閱讀者直覺。
**P**：畫面票測試中狀態值出現框架生命週期序未涵蓋的字面 → 補「未知值排最後」。
**改法**：SPEC-003 §3.4〈排序欄首的元件級契約〉新增 S8「比較語意」一列，內容同 O1；未知狀態或優先值排最後（asc 與 desc 皆排最後）。

#### S-07（V4-01 G18）解析失敗票是否計入票數、全部失敗時的落點

承接層：S1（SPEC-001 §4 無 ticket 與含損壞列）；S4 附帶（4.35 損壞列內容政策）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 失敗票計入票數並以損壞列列出（ID 取檔名）；全部失敗 → 正常 · 列表 + 含損壞；無 ticket 只在檔案數為 0 |
| O2 | 改造既有 | 失敗票不計入；全部失敗 → 無 ticket 疊加損壞徽章 |
| O3 | 零新增（反向框架） | 失敗票不列出，只以徽章計數 |

**R**：SPEC-004 §3.6 §4 含損壞列「各損壞列末欄 `IssueMarker.damagedDetail`（圖示）」——損壞票本就以列出現；UC-05 例外場景〈含損壞票〉「解析失敗的票在清單以 … 標示」；O2 須讓無 ticket 態承載疊加態，SPEC-001 §4 疊加只寫在正常態；O3 與上述兩處矛盾。
**裁決**：O1。**被放棄選項優點**：O2 讓「全部壞掉」與「有資料」外觀不同。
**P**：損壞列因 ID 以外全缺而使虛擬捲動列高不一 → 由 4.35 內容政策補「缺值格顯示—」。
**改法**：SPEC-001 §4 無 ticket 進入條件「載入完成但票數為 0」改「ticket 檔案數為 0」；§4 註記新增一段「解析失敗的票計入票數並以損壞列出現（ID 取檔名），全部失敗時為正常 · 列表疊加含損壞；逐檔讀取失敗（含 ticket 目錄於載入期間消失）同此處理，不另設載入失敗狀態（S-04）」。SPEC-004 4.35 `ticket` 內容政策補「解析失敗列：ID 格取檔名、其餘文字格顯示「—」、標記格 `IssueMarker.damagedDetail`」。

#### S-08（V4-01 G21）主題節預設展開、未歸屬節錨點

承接層：S3（SPEC-003 §3.4〈互動反應〉主題節展開收合列、〈生命週期〉）。節標題文字格式非缺口：PROP-004〈Ticket 清單的分組軸〉節標題列。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增條文 | 首次進入主題態全部節收合（含未歸屬）；未歸屬節錨點 `expander-tickets-unassigned`（不用 topic 族） |
| O2 | 新增條文 | 全部展開 |
| O3 | 新增條文 | 首節展開其餘收合；未歸屬錨點 `expander-tickets-topic-unassigned` |

**R**：UC-05 主要成功場景步驟 3–4「依主題呈現各節 → 使用者展開主題」，收合為初始；PROP-004 此視圖存在理由是「一次看到所有主題」，全展開在真實規模（1300 票）下節首被票列推出視野；主題名來自 `topics-registry.txt` 任意文字，O3 的錨點會與名為 `unassigned` 的主題撞名。
**裁決**：O1。**被放棄選項優點**：O2 少一次點擊即見票；O3 首屏有內容。
**P**：帶目標跳入以外的主要路徑中，使用者進主題態後逐一展開全部節 → 重評 O2。
**改法**：SPEC-003 §3.4〈互動反應〉主題節展開收合列補「未歸屬節錨點為 `expander-tickets-unassigned`」；〈生命週期〉補「首次切至主題：全部節（含未歸屬）收合；之後展開狀態依 §2.8 保留；切換專案重置為全部收合」。

#### S-09（V4-01 G22）有效 priority、同值 tie-break、節內順序、一票多主題

承接層：S1（SPEC-001 §4 註記，比照框架）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有（比照框架） | 有效 priority＝值屬 `PRIORITY_LEVELS`；主題排序鍵（最高優先級、票數降冪）同值時維持主題首次出現序（穩定排序）；節內票依載入序；`topic-assignments.txt` 同一票多筆取最後一筆 |
| O2 | 新增 | 同值再依主題名排序；節內票依優先再依 ID |
| O3 | 零新增 | 不定義，交實作 |

**R**：PROP-004 明示「依框架規格呈現」；框架 `track_board.py` 以 `(priority_rank, -len)` 排序（Python 穩定排序即首次出現序）；`topic_assignments.py` 讀取語意為「多筆回傳最後一筆（改派後的產物）」——App 若不同，同一專案的 CLI 輸出與 App 畫面順序不一致；O3 使測試無法斷言順序。
**裁決**：O1。**被放棄選項優點**：O2 順序可由畫面資訊推出，不依賴載入序。
**P**：框架 `track_board.py` 排序鍵變更時 App 未跟隨 → 在 SPEC-001 註記寫明比照對象，便於以 grep 追蹤。
**改法**：SPEC-001 §4 註記新增「主題模式的排序與歸屬比照框架：有效 priority 為 `PRIORITY_LEVELS` 成員；節序鍵同值時維持主題在載入資料中的首次出現序；節內票列依載入序；一票多筆指派取 `topic-assignments.txt` 最後一筆。比照對象：`.claude/skills/ticket/ticket_system/commands/track_board.py` 主題分組函式與 `lib/topic_assignments.py` 讀取函式」。

#### S-10（V4-01 G25）已載入後的重新載入入口（完整模式）

承接層：S3（SPEC-003 §3.4〈生命週期〉註記）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | 不設入口；重新載入經專案切換浮層再選目前專案（全頁重置） |
| O2 | 新增 | header `ButtonRow` 增「重新載入」→ `state-tickets-loading` |
| O3 | 改造既有 | 再次可見時自動重新解析 |

**R**：SPEC-004 4.9 `selected` 列「點選重載同一專案，SPEC-003 §3.7 未區分，視同選取」——O1 路徑已存在；Domain 視圖同樣無重新載入入口，兩畫面一致；SPEC-003 §2.8「視窗失焦不觸發任何重新載入（0.1 無檔案監看）」；O3 與 §3.4〈生命週期〉「再次可見不重新解析」及 FR-02 惰性成本衝突；O2 無 UC 出處（UC-05 無重新整理步驟）。
**裁決**：O1。**被放棄選項優點**：O2 不重置其他五頁的狀態與捲動位置，維護者改完票後回看成本低。
**選定方案缺點**：(1) 重載使六頁狀態全部重置；(2) 入口不在 Ticket 清單內，可發現性低；(3) 0.1 期間資料過期只能靠使用者自覺。
**P**：實機驗證時使用者修改 ticket 後在清單內找不到更新方式 → 重評 O2。
**改法**：SPEC-003 §3.4〈生命週期〉「再次可見」列補「0.1 不提供畫面內重新載入；需反映外部修改時經 `project-switcher-entry` 選取目前專案（§3.7，全頁重置）」。

#### S-11（V4-01 G28）帶目標跳入時目標不在清單

承接層：S3（SPEC-003 §3.4〈帶目標跳入〉）；S1 附帶（§8.2）；S4 附帶（i18n）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增 | 不定位；SnackBar `ticketsTargetNotFoundMessage`（`AppSnackBar.plain`） |
| O2 | 零新增 | 靜默不定位 |
| O3 | 改造既有 | 改 jump 節點詳情 |

**R**：依 S-07 損壞票以檔名 ID 出現，目標不在清單只剩「檔案於掃描後被刪除」一路；SPEC-003 §2.13 判準 T2：狀態不變且使用者看不出為何沒定位，須暫態提示；O3 的節點詳情同樣找不到該節點（會落原始檔已消失，語意偏移）；SPEC-001 §8.2 為窮盡清單，O1 須同步。
**裁決**：O1。**被放棄選項優點**：O2 不新增 key 與 §8.2 列。
**P**：假資料難以構造該路徑致測試缺席 → 測試基座以 fake 移除目標檔。
**改法**：SPEC-003 §3.4〈帶目標跳入〉新增一列「載入完成後目標 `<ticketId>` 不在清單」：不捲動、不切模式、不清除搜尋篩選；SnackBar `ticketsTargetNotFoundMessage`，停留 `Motion.snackBar`。SPEC-001 §8.2 新增一列（同畫面狀態不變）。SPEC-004 4.26 i18n 表補 key（zh「找不到該 ticket，可能已被移除」）。

#### S-12（V4-01 G30）UC-05「列表篩選」完成後回主題的觸發

承接層：S3（SPEC-003 §5 規格判讀註記）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | 判讀為使用者點 `mode-tickets-topic`；系統不自動切模式 |
| O2 | 新增 | 選取篩選值後自動切回主題 |

**R**：UC-05 flow `filter-in-list-mode` 的 `return_to: switch-to-topic` 描述流程拓撲回到該步驟，未指觸發者；SPEC-003 §1.2 模式切換只有 `mode-*` 錨點；R1 定主題態不套用篩選，O2 自動切回會讓篩選結果立即不可見。
**裁決**：O1。**被放棄選項優點**：O2 貼合 UC flow 的字面回返。
**P**：無（純判讀）。
**改法**：SPEC-003 §5 表新增一列：UC-05 `filter-in-list-mode` 的 `return_to` 判讀為使用者經 `mode-tickets-topic` 手動切回，系統不自動切換。
### 逐項評估：Domain 視圖（S-13～S-21，探針 V4-02）

#### S-13（V4-02 G03）與 S-38（V4-04 G22）資料夾選擇器開不起來／選到不可讀資料夾（完整模式）

承接層：S3（SPEC-003 §3.1、§3.7）；S1 附帶（§8.2）；S4 附帶（4.26 i18n）；DOM 已承載資料層（SPEC-005 `ChooseFolderUnavailable`、`WorkspaceUnavailable`）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增條文 | 兩者皆停在觸發處（`state-domain-unset` 或浮層展開），不轉狀態、不寫入已存路徑與最近清單；SnackBar 說明原因（選擇器不可用 `folderPickerUnavailableMessage`；資料夾不可讀或不存在 `folderUnavailableMessage`，原因文字取 `WorkspaceUnavailable.reason`） |
| O2 | 新增 | 選到不可讀資料夾進入 `state-domain-loading` 後落新阻擋狀態「資料夾無法讀取」 |
| O3 | 零新增（反向框架） | 選擇器失敗視同取消；不可讀資料夾歸入「不是框架專案」 |
| O4 | 改造既有 | 不可讀資料夾加入最近清單並標不可用（Gate 失敗欄形態） |

**R**：SPEC-005 §2.1 已有 `ChooseFolderUnavailable`、§2.3 已有 `_reasonFolderUnreadable`／`_reasonFolderMissing` 兩個結局，資料層有、UI 無；SPEC-001〈Gate 三問對照〉資料夾可用性失敗欄「顯示該項不可用 + 原因，其餘項仍可選」——失敗不轉 Domain 狀態；O2 增狀態（連動 R9 範圍）；O3 使「不是框架專案」的說明（此資料夾沒有 `docs/`）對不可讀資料夾為假；SPEC-003 §3.1 選擇器取消列「不出現 SnackBar」只適用使用者意圖取消，失敗不同義；O4 讓一個從未成功開啟的資料夾進入「最近專案」語意不符。§2.13 判準 T2：狀態不變 → 需暫態提示。
**裁決**：O1。**被放棄選項優點**：O2 原因可常駐閱讀；O4 與 Gate 失敗欄形態一致。
**選定方案缺點**：(1) 原因只在 SnackBar 停留期間可讀；(2) §8.2 多兩列；(3) 兩個觸發處（unset 內容區、浮層）的 SnackBar 顯示位不同，浮層展開時 SnackBar 可能被浮層遮蔽。
**P**：畫面票實作時浮層展開下的 SnackBar 被 `SwitcherOverlay` 覆蓋 → 改為浮層內常駐原因列。
**改法**：
- SPEC-003 §3.1〈互動反應〉選擇資料夾之後新增兩列：「選擇器無法開啟」「選定資料夾不可讀或不存在」：仍為 `state-domain-unset`、不寫入已存路徑；SnackBar 分別為 `folderPickerUnavailableMessage`、`folderUnavailableMessage`（placeholder `reason`），停留 `Motion.snackBar`
- SPEC-003 §3.7〈互動反應〉選擇其他列補同兩分支：浮層維持展開、清單不變、SnackBar 同上
- SPEC-001 §8.2 新增兩列
- SPEC-004 4.26 i18n 表補兩 key

#### S-14（V4-02 G12）泳道模式下選 domain 的觸發元素

承接層：S4（4.38 `SwimlaneGrid`）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 泳道列首比照 4.37：由 `SwimlaneGrid` 包成可點，錨點同 `action-domain-select-<domainId>` |
| O2 | 零新增（反向框架） | 泳道不提供選 domain，SPEC-001 §1 泳道可用操作刪「選 domain」 |
| O3 | 新增 | 列首旁另設選取鈕 |

**R**：SPEC-001 §1 正常 · 泳道與泳道 · 尚未選定 UC 兩列可用操作皆含「選 domain」；SPEC-004 4.38 已有 `laneHighlight` 呈現選中列，但同條目焦點列「本容器為唯一停留點」、無列首點擊條文——有呈現無觸發；R6 定選中 domain 為兩模式共用值，觸發錨點共用與之一致；O3 在 0.1 無差異行為支撐拆件。
**裁決**：O1。**被放棄選項優點**：O2 泳道維持零互動、焦點停留點唯一。
**P**：泳道列首可點與拖曳畫布衝突（按下即拖）→ 以位移閾值區分點擊與拖曳，寫入 4.38 不變式。
**改法**：SPEC-004 4.38 用途補「列首 `AppText` 由本容器包成可點（`InkWell` + `action-domain-select-<domainId>`，同 4.37）」；回饋契約焦點列改為「列首依序為停留點，其後為容器本身」；拖曳不變式補「在列首按下後位移未超過拖曳閾值時視為點擊」。

#### S-15（V4-02 G21）空圖、阻擋、載入中是否渲染模式切換

承接層：S4（SPEC-004 §3.6 註記）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | 不渲染：`SplitRow.header`[`PageTitle`, `SegmentedControl`] 只出現在 §1 正常 · 矩陣、已選格、三個泳道列 |
| O2 | 新增 | 渲染為 disabled + 常駐原因 |

**R**：SPEC-001 §1 未選專案、載入中、空圖、三個阻擋列可用操作皆無切模式；SPEC-003 §2.2 未接線動作合法形態 (a) 不渲染；SPEC-004 §3.6 上述列歸屬皆為全頁狀態元件，無 header。
**裁決**：O1。**被放棄選項優點**：O2 頁首版位在各狀態間穩定。
**P**：cross-fade 時頁首跳動被實機驗證判為干擾 → 重評 O2。
**改法**：SPEC-004 §3.6 §1 表下補一句「`SplitRow.header` 與 `SegmentedControl` 僅渲染於正常 · 矩陣、已選格、正常 · 泳道、泳道 · 尚未選定 UC、泳道 · flow 未結構化五列；其餘 §1 狀態不渲染頁首模式切換（SPEC-003 §2.2 形態 a）」。

#### S-16（V4-02 G22）關閉鈕清除選取、schema 面板 Esc 後的焦點去向

承接層：S3（SPEC-003 §2.10）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 關閉鈕清除後焦點回原選中格（與 Esc 同）；schema 面板 Esc／再次點擊收合後焦點停在 `action-domain-schema-detail` |
| O2 | 新增條文 | 兩者焦點回內容區首個可聚焦元素 |
| O3 | 零新增 | 交 Flutter 預設（焦點元素移除後落 null） |

**R**：SPEC-003 §2.10 矩陣 Esc 列「焦點停在原格」；SPEC-004 4.23 焦點列已提案「焦點停在檢視詳情按鈕（沿用焦點停在原元素慣例）」；O3 使鍵盤使用者失去位置，違反 §2.10 焦點下界的精神；關閉鈕移除後唯一可恢復的語意位置是原格。
**裁決**：O1。**被放棄選項優點**：O2 位置固定易測。
**P**：原格已捲出 viewport 時焦點移入造成矩陣跳動 → 補「不捲動，只移焦點」。
**改法**：SPEC-003 §2.10 表新增兩列：「`action-domain-cell-clear` 觸發後：焦點移至原選中格，矩陣 offset 不變」「`panel-domain-schema-detail` 收合後（Esc 或再次點擊）：焦點停在 `action-domain-schema-detail`」；SPEC-004 4.23 焦點列「提案」字樣改為引用本列。

#### S-17（V4-02 G26）與 S-32（V4-04 G11）取消逾期、外部程序掛住（完整模式）

承接層：S3（SPEC-003 §2.2〈外部開啟契約〉、§2.5）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | 明寫「不存在逾時結果」：取消依 C4 實作約束保證抵達；`ExternalOpener.open` 0.1 不設逾時，外部程序不回傳時無 UI 結局、按鈕維持可按，入口日誌承載可觀測性 |
| O2 | 新增 | `ExternalOpener` 設逾時 token，逾時轉 `failed` + SnackBar |
| O3 | 新增 | 取消逾期後強制落目標態並記 warning |

**R**：SPEC-003 §2.2 外部開啟契約等待指示列實測回傳 200–550 ms；`/usr/bin/open` 交給 LaunchServices 即返回；§2.2〈呼叫發出日誌〉為何不可省列已承認掛住情境並以入口日誌承接；SPEC-003 §3.6〈重新整理的三分支〉已有同型寫法「不存在逾時這個結果，也沒有逾時後的行為可定義」；C4 為測試斷言（假時鐘）與批次實作約束，逾期即實作違約而非執行期分支；O2 的 token 無量測依據（§2.6 禁止無依據數值精神），且 `Process.run` 無法中止外部程序；ExternalOpener 不可用（找不到 `/usr/bin/open`）已由 `exitCode != 0` 歸 `failed`。
**裁決**：O1。**被放棄選項優點**：O2 使四結局齊全、使用者得到回饋；O3 使取消在實作違約時仍保證可離開。
**選定方案缺點**：(1) 外部程序掛住時使用者無任何回饋；(2) 重複點擊會累積多個掛住的程序；(3) C4 違約只能靠測試發現，執行期無自救。
**P**：實機或日誌出現「入口日誌有、結果日誌無」的紀錄 → 以 O2 重評並量測逾時值。
**改法**：
- SPEC-003 §2.2〈外部開啟契約〉表新增「逾時」列：「0.1 不設逾時；外部程序不回傳時無 UI 結局，觸發元素 `enabled` 不變（每次點擊各發一次呼叫，冪等由外部程序承擔）；可觀測性由〈呼叫發出日誌〉承載；結局齊全性的例外依據同 §3.6〈重新整理的三分支〉段」
- SPEC-003 §2.5 C4 斷言方式段後補一句「C4 為實作約束，不存在『取消逾期』的執行期分支，本規格不定義逾期後行為」

#### S-18（V4-02 G32）與 S-22（V4-03 #9）開啟原始檔「檔案不存在」SnackBar 的 `refreshAction` 語意（完整模式）

承接層：S3（SPEC-003 §3.1 泳道開啟原始檔列、§3.2 開啟原始檔（檔案不存在）列）；S4 附帶（4.26 用例列）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增 | 重新整理＝重新載入整個專案（→ `state-domain-loading`，全頁重置） |
| O2 | 新增 | 重新整理＝重新解析該 UC 單檔（比照 §3.6 三分支），依結果重判選定 UC 所在狀態 |
| O3 | 零新增（反向框架：移除動作） | 兩處改 `AppSnackBar.plain`（`sourceFileNotFoundSnackbarMessage`，停留 `Motion.snackBar`），不帶動作；圖的整體更新經浮層選取目前專案（同 S-10） |

**R**：節點詳情的重新整理有 `state-nodeDetail-missing` 可落，UC Flow 與 Domain 無「原始檔已消失」狀態，O2 的「仍不存在」分支唯一可觀察結果是再出同一 SnackBar，「解析有斷點」分支在 UC Flow 無 partial 狀態可落，需新增狀態；O1 在使用者只想確認一個檔案時重置六頁，代價與動作名「重新整理」不符；SPEC-003 §2.2 i18n 列 `refreshAction` 既有且仍由 §3.6 使用，移除兩處用例不影響 key 存續；§2.2 禁止無回饋佔位 handler，未定義語意的動作不得上線。
**裁決**：O3。**被放棄選項優點**：O2 讓使用者在外部還原檔案後就地恢復；O1 語意明確、實作單一。
**選定方案缺點**：(1) 使用者還原檔案後需經浮層重選專案；(2) SnackBar 只告知不給下一步；(3) Domain 與節點詳情對同一結局的處置不對稱。
**P**：畫面票驗證中使用者在 notFound 後重複點開啟原始檔 → 重評 O2 並補 UC Flow 缺檔狀態。
**改法**：SPEC-003 §3.1〈互動反應〉泳道開啟原始檔列 `notFound` 分支改「SnackBar `sourceFileNotFoundSnackbarMessage`，不帶動作，停留 `Motion.snackBar`」；§3.2〈互動反應〉開啟原始檔（檔案不存在）列同改；SPEC-004 4.26 `withAction` 用例與 i18n 動作列中與此兩處相關的 `refreshAction` 用例刪除（§3.6 用例保留）。

#### S-19（V4-02 G39）以 App 內建型別表檢視是否經過載入中

承接層：S3（SPEC-003 §3.1〈導航跳轉與退出〉無可消費的型別表列註記）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | 不經載入中，同畫面直接轉換（現行字面），並補理由 |
| O2 | 改造既有 | 經 `state-domain-loading`（可取消） |

**R**：`docs/tech-decisions.md`〈載入策略〉圖譜節點 16 檔約 20 ms，ticket 不參與；SPEC-003 §2.6「0.1 無非狀態級短暫等待」；O2 的取消目標依 §2.5 為 `state-domain-unset`，會丟棄剛通過 schema gate 的專案，與使用者「改用內建表檢視」的意圖不符。
**裁決**：O1。**被放棄選項優點**：O2 在節點檔數成長時仍有等待指示與取消。
**P**：tech-decisions tripwire「專案檔案數超過萬檔級」觸發 → 以 O2 重評。
**改法**：SPEC-003 §3.1〈導航跳轉與退出〉無可消費的型別表列補「轉換不經 `state-domain-loading`：節點重新解析屬幾乎即時帶（圖譜節點檔數量級見 `docs/tech-decisions.md`〈載入策略：依視圖惰性〉）」。

#### S-20（V4-02 G46）由 UC Flow domain 欄 jump 進入 Domain 視圖的進入行為

承接層：S3（SPEC-003 §3.1〈生命週期〉）；S4 附帶（4.37／4.38 定位場景）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 不切模式；選中 domain 寫入該 domain（依 R6：與選中格不同列即清除選中格）；該 domain 列首於當前模式 scroll-into-view（§4.0.10） |
| O2 | 改造既有 | 一律切至泳道並定位 |
| O3 | 零新增 | 只寫入選中 domain，不捲動 |

**R**：SPEC-003 §3.2 domain 欄列「jump 至 `nav-page-domain` 且該 domain 呈選中態」為唯一條文；UC-03 步驟 4「切換至 Domain 視圖並定位該 domain」——「定位」要求可見，O3 在真實規模矩陣下列可能在 viewport 外；O2 覆蓋使用者在 Domain 視圖的模式選擇，且 §3.1〈生命週期〉定模式跨導覽保留；UC-02 替代場景〈由 ticket 切入〉在 0.1 無入口，不在本項。
**裁決**：O1。**被放棄選項優點**：O2 使 UC Flow → Domain 泳道的連續閱讀少一次切換。
**P**：R6 清除選中格使使用者自 UC Flow 回 Domain 後失去先前格詳情 → 與 R6 P 訊號合併重評。
**改法**：SPEC-003 §3.1〈生命週期〉新增列「由 jump 帶 domain 進入（`action-ucFlow-goto-domain-<domainId>`）」：模式不變；選中 domain 寫入，依「選 domain」列規則處理選中格；該 domain 列首 rect 與 viewport rect 有交集（`jumpTo`）。SPEC-004 §4.0.10 定位場景表補一列（矩陣由 4.37、泳道由 4.38 執行）。

#### S-21（V4-02 G47）取消載入後已存路徑是否清除

承接層：S3（SPEC-003 §3.1 取消載入列）；DOM 附帶（workspace 偏好設定需清除操作）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 取消載入清除已存路徑，不移除最近專案清單中的該項 |
| O2 | 零新增 | 保留已存路徑 |

**R**：SPEC-001 §1 未選專案進入條件「無已存路徑」，取消目標為未選專案——O2 使該狀態與其進入條件字面不一致；SPEC-003 §3.1〈生命週期〉「App 啟動且有已存路徑 → 直接進入載入中」，O2 下使用者因專案過大而取消，每次啟動仍自動載入同一專案；最近清單保留使重新開啟只需一次浮層操作。
**裁決**：O1。**被放棄選項優點**：O2 誤觸取消後重啟可自動恢復。
**P**：使用者回報重啟後需重選專案 → 重評 O2。
**改法**：SPEC-003 §3.1〈互動反應〉取消載入列補「已存路徑清除（下次啟動落 `state-domain-unset`）；最近專案清單不變」。workspace domain：SPEC-005 §2.2 現只有 `writeString`，清除操作的接縫與回饋點由 workspace domain 補（不在 .40／.41／.42 範圍）。
### 逐項評估：UC Flow 與追溯視圖（S-22～S-29，探針 V4-03）

#### S-22（V4-03 #9）`refreshAction` 按下後的結果

同 S-18（O3：移除該動作，改 `AppSnackBar.plain`）。承接層與改法同 S-18。

#### S-23（V4-03 #11）步驟表列序與序號

承接層：S1（SPEC-001 §2 註記）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增條文 | 列序＝flow YAML 陣列序；序號自 1 連續；分支步驟（`branch_from` 非 null）照陣列位置排、不另編號 |
| O2 | 新增條文 | 依 `next` 拓撲序，主線優先、分支附於 `branch_from` 之後 |
| O3 | 新增條文（反向框架） | 分支步驟不列入步驟表 |

**R**：同一「步驟序號」出現在三處——步驟表、事件流小表「步驟序號 · domain」（SPEC-001 §2〈事件流小表〉）、格詳情卡「依 flow 順序的編號列」（SPEC-003 §3.1〈格詳情卡的內容契約〉）——三處須同一編號規則，陣列序是唯一不需演算即一致的來源；O2 在 `next` 含多值或 `return_to` 形成回圈時無唯一拓撲序（UC-05 flow 即有 `return_to`）；O3 使 UC-05 `cancel-loading`、`damaged-tickets` 等例外步驟從表中消失，其事件（`EVT-CORPUS-003`）在事件流小表失去發出／消費序號。
**裁決**：O1。**被放棄選項優點**：O2 的閱讀順序貼近實際流程走向。
**P**：UC 撰寫者把分支步驟放在陣列前段致表序難讀 → 在 doc CLI 或 UC 模板加排列建議，不改規格。
**改法**：SPEC-001 §2 註記新增「步驟序號：依 UC flow 區塊陣列順序自 1 連續編號，分支步驟不另編；步驟表、事件流小表、§1 格詳情卡的編號同源」。

#### S-24（V4-03 #17）同一事件有多個發出步驟時的格式與列序

承接層：S1（SPEC-001 §2〈事件流小表〉）；S3 附帶（§3.2 列）；S4 附帶（4.35 `eventFlow`）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 發出格比照消費格列出全部（「、」串接）；列序依該事件在步驟表中首次出現的步驟序號 |
| O2 | 新增 | 多個發出步驟拆多列 |
| O3 | 新增條文 | 列序依 EVT ID |

**R**：SPEC-001 §2〈事件流小表〉「一個事件一列；多個消費步驟時消費欄列出全部」——O2 違反一事件一列；SPEC-004 4.35 `eventFlow` 消費格已定「多個以『、』串接」，發出格未寫；SPEC-003 §3.2 列數等於相異 EVT ID 數，O2 會破壞該斷言；O3 使小表列序與上方步驟表閱讀順序脫鉤。
**裁決**：O1。**被放棄選項優點**：O3 列序與 EVT 節點列表一致、與步驟改動無關。
**P**：無。
**改法**：SPEC-001 §2〈事件流小表〉「列」列補「多個發出步驟時發出欄同樣列出全部；列序依該事件首次出現（發出或消費）的步驟序號」；SPEC-003 §3.2〈事件流小表〉列補同句；SPEC-004 4.35 `eventFlow` 發出格補「多個以『、』串接」。

#### S-25（V4-03 #19）UC Flow 狀態間 cross-fade 的範圍

承接層：S3（SPEC-003 §3.2〈動畫提示〉）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 尚未選定 UC／flow 未結構化／正常三者之間只有主欄 cross-fade，右欄〈UC 選擇入口〉不參與；進出無 UC 態時整體 cross-fade |
| O2 | 零新增 | 整個狀態根節點含右欄一併 cross-fade |

**R**：SPEC-004 §3.6〈UC 選擇入口〉渲染範圍：三態共用同一右欄清單；O2 使使用者點選 UC 的清單本身淡出再淡入，被點的列選中態閃爍；無 UC 態不渲染右欄，其進出必然整體轉換。
**裁決**：O1。**被放棄選項優點**：O2 實作單一（狀態根一層 `AnimatedSwitcher`）。
**P**：畫面票實作時右欄 `ScrollController` 因狀態根重建而重置 → 說明右欄必須在狀態根之外持有。
**改法**：SPEC-003 §3.2〈動畫提示〉第一列改為兩列：「尚未選定 UC / flow 未結構化 / 正常 三者之間：主欄 cross-fade `Motion.transition`，右欄選擇入口不重建、`selected` 直接切換」「無 UC 與其他三態之間：整體 cross-fade」。

#### S-26（V4-03 #21）哪幾層算父節點

承接層：S1（SPEC-001 §3 註記）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增條文 | PROP、SPEC、UC 三層為父節點，Ticket 為葉；UC 無 Ticket 下游即缺口（受 S-28 條件限制） |
| O2 | 新增條文 | 只 PROP、SPEC 為父節點，UC 為葉，直到 UC → Ticket 邊定案 |
| O3 | 新增條文（反向框架） | 四層皆為父節點，Ticket 以 `children` 為下游 |

**R**：SPEC-001 §3 正常顯示「PROP→SPEC→UC→Ticket」四層；UC-04 驗收「四層、缺口層以視覺差異標示而非省略」——O2 省略第四層缺口；O3 使絕大多數 Ticket 永遠斷裂，鏈路斷裂態恆成立；0.1 以假資料驅動，UC → Ticket 邊在假資料中可構造，真實資料的邊來源由 `0.1.0-W3-365` 承接。
**裁決**：O1。**被放棄選項優點**：O2 在真實資料接入前不產生「每個 UC 都缺口」的大量標示。
**P**：`0.1.0-W3-365` 定案結果使 UC → Ticket 為 0..1 或需反查 → 重評缺口判定。
**改法**：SPEC-001 §3 註記新增「父節點層級：PROP、SPEC、UC 為父節點，Ticket 為葉節點不產生缺口；UC 層的缺口判定受 Ticket 載入狀態限制（見本節『Ticket 層的資料來源』註記，S-28）」。

#### S-27（V4-03 #22）同一節點有多個父節點（完整模式）

承接層：S1（SPEC-001 §3 註記）；S3 附帶（§3.3 錨點）；S4 附帶（4.39）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 節點在每個父節點下各出現一次；錨點維持 `<nodeId>`（`ValueKey` 只需兄弟間唯一），展開集合以節點 ID 共用；缺口標示單位改為「缺下游父節點的每個出現位置」 |
| O2 | 新增 | 各出現位置錨點改含路徑（如 `<rootId>--…--<nodeId>`），展開集合以路徑為鍵 |
| O3 | 零新增 | 只在第一個父節點（依 ID 序）下出現 |
| O4 | 新增（反向框架） | 追溯視圖改 DAG 圖而非樹 |

**R**：UC-04 驗收前提段「UC-01 自報來自 PROP-003，但經 SPEC-001 展開會出現在 PROP-004 底下」——多父節點在本專案語料即存在；成功保證「該需求展開為哪些規格、用例與 ticket」要求每個 PROP 分支完整，O3 使第二個 PROP 分支缺節點；SPEC-004 4.39 展開集合為 `StateProvider<Set<String>>`（節點 ID 集合），O1 不改其型別，O2 須改 provider 型別、SPEC-003 §3.3 三族錨點與 4.39 初始值規則；O4 無 PROP-004 版型出處（PROP-004 追溯視圖為樹）。
**裁決**：O1。**被放棄選項優點**：O2 各出現位置可獨立展開、錨點唯一；O3 錨點與斷言最簡。
**選定方案缺點**：(1) 展開一處同 ID 的其他出現位置一併展開，長樹中可能推開視野；(2) 測試對多父節點以 `findsNWidgets` 或祖先限定 finder 斷言，不能一律 `findsOneWidget`；(3) 同一 `badge-traceability-broken-<nodeId>` 可有多個實例，「每個父節點一個」的計數語意改為「每個出現位置一個」。
**P**：畫面票測試因重複錨點無法穩定定位特定出現位置 → 改 O2。
**改法**：SPEC-001 §3 註記新增「多父節點：節點於每個父節點下各出現一次，其子樹與展開狀態在各出現位置相同」；SPEC-003 §3.3〈互動反應〉表下補「同一 `<nodeId>` 可有多個實例（多父節點），斷言以祖先限定 finder 區分；缺口虛線框『每個此類父節點一個』指每個出現位置一個」；SPEC-004 4.39 補「展開集合以節點 ID 為鍵，同 ID 的全部出現位置共用展開狀態」。

#### S-28（V4-03 #23）Ticket 層在 Ticket 清單未載入時是否在圖中（完整模式）

承接層：S1（SPEC-001 §3 註記）；S3 附帶（§3.3）；S4 附帶（§3.6 §3、i18n）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | Ticket 層只在 Ticket 清單已載入後出現；未載入時樹止於 UC 層、UC 不因無 Ticket 判缺口，樹上方常駐一行說明 `traceabilityTicketsNotLoadedHint` 並附前往 Ticket 清單動作 `action-traceability-goto-tickets`（jump） |
| O2 | 新增 | 追溯視圖首次可見自動觸發 Ticket 載入（新增載入態） |
| O3 | 改造既有 | Domain 載入時一併解析 ticket 的邊 |
| O4 | 零新增（反向框架） | 追溯視圖不顯示 Ticket 層 |

**R**：`docs/tech-decisions.md`〈載入策略：依視圖惰性〉——ticket 邊與詳情同在 frontmatter，無法只解析邊，O3 等於開 App 即付 1.9 秒，推翻已定案策略；O2 使 SPEC-001 §3 增載入中狀態、FR-02 長時操作增第四處，並與 Ticket 清單載入重複觸發路徑；O4 違反 UC-04 驗收四層；O1 不增狀態（說明列屬正常／鏈路斷裂態內容），且 Ticket 清單載入後回到本頁即見第四層（§2.8 L1）。
**裁決**：O1。**被放棄選項優點**：O2 一次到位看到完整鏈；O4 最簡且無條件式缺口。
**選定方案缺點**：(1) 同一棵樹的層數依另一畫面的載入狀態而變，使用者可能不理解；(2) 追溯視圖多一個動作錨點與 i18n key，SPEC-003 §4 #12／#13 退出路徑增列；(3) 缺口判定多一個條件，測試需分「Ticket 已載入／未載入」兩組。
**P**：實機驗證時使用者未注意說明列、誤以為專案無 Ticket → 重評 O2。
**改法**：
- SPEC-001 §3 註記新增「Ticket 層的資料來源：Ticket 節點來自 §4 的載入結果（依視圖惰性）。§4 未載入時樹止於 UC 層，UC 層不以缺 Ticket 判為缺口，樹上方常駐說明並提供前往 Ticket 清單；§4 載入完成後第四層與 UC 層缺口判定生效」；§3 正常與鏈路斷裂列可用操作補「前往 Ticket 清單（Ticket 未載入時）」
- SPEC-003 §3.3〈互動反應〉新增 `action-traceability-goto-tickets` 列（jump 至 `nav-page-tickets`，`returnTo` 設為 `traceability`，僅於 Ticket 未載入時渲染）；〈生命週期〉補「Ticket 清單載入完成後再次可見：Ticket 層出現，展開集合依初始值規則對新增缺口補展開」；〈導航跳轉與退出〉與 §4 #12、#13 補該錨點
- SPEC-004 §3.6 §3 正常與鏈路斷裂列補說明列歸屬（`AppText.caption` + `AppButton.text`）；§4.0.6 新 key 表補 `traceabilityTicketsNotLoadedHint`、`gotoTicketsAction`
- 與 .40（R9）交界：「專案未就緒」時追溯視圖落 R9 新狀態，本項說明列不出現

#### S-29（V4-03 #26）「使每個 badge 首次渲染即可見」的解讀

承接層：S3（SPEC-003 §3.3〈生命週期〉展開集合初始值列）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 「可見」＝結構可見：該缺口列的全部祖先在展開集合內；斷言為展開集合包含全部祖先，捲動至該列後 `findsOneWidget` |
| O2 | 零新增 | 「可見」＝與 viewport rect 相交 |

**R**：缺口數多於一屏時 O2 不可能對每個 badge 同時成立；4.39 不持有捲動、捲動委派 4.30 `Panel.scrollable`，若以 lazy builder 建構，視窗外 badge 不在元件樹，`findsOneWidget` 須先捲動；SPEC-004 4.39〈展開集合初始值與缺口列〉的規則本身即以祖先集合表述。
**裁決**：O1。**被放棄選項優點**：O2 對單一缺口的小樹更貼近使用者所見。
**P**：無。
**改法**：SPEC-003 §3.3〈生命週期〉展開集合初始值列「使每個 `badge-traceability-broken-<nodeId>` 首次渲染即可見」改「使每個 `badge-traceability-broken-<nodeId>` 的全部祖先於首次渲染時皆在展開集合內（結構可見；不要求與 viewport 相交，斷言時先捲動至該列）」。
### 逐項評估：破洞報告、節點詳情、專案切換浮層（S-30～S-39，探針 V4-04）

#### S-30（V4-04 G02）取消掃描後再次點 `nav-item-gaps`

承接層：S3（SPEC-003 §3.5〈生命週期〉）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 取消後無結果，下次可見重新自動掃描（比照切換專案後「下次可見時重新自動掃描」） |
| O2 | 新增 | 新增「未掃描」狀態 + 開始掃描動作 |
| O3 | 改造既有 | 取消保留上一輪結果 |

**R**：SPEC-003 §3.5〈生命週期〉「再次可見不重新掃描，顯示既有結果」以「有既有結果」為前提，取消後依 §2.5 C6 部分結果全數丟棄，前提不成立；同節切換專案列已有「結果清空 → 下次可見時重新自動掃描」同型規則；O2 增狀態；O3 違反 C6，且重新掃描列定舊結果「當下即被判定為待汰換」。
**裁決**：O1。**被放棄選項優點**：O2 讓使用者決定是否再付掃描成本。
**P**：使用者因掃描過久而取消，回來又被自動掃描 → 重評 O2。
**改法**：SPEC-003 §3.5〈生命週期〉「再次可見」列改「有既有結果時不重新掃描；無既有結果（上一輪被取消）時自動進入 `state-gaps-scanning`」。

#### S-31（V4-04 G10）事件類破洞項沒有任何引用它的 UC

承接層：S3（SPEC-003 §3.5〈互動反應〉事件類列）；S1 附帶（§5 破洞項指向表）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 退為「指向其他圖節點」列：jump 至節點詳情顯示該 EVT 節點 |
| O2 | 改造既有 | 退為「無指向」列：開啟 EVT 原始檔 |
| O3 | 改造既有 | 切至 UC Flow，不寫入選定 UC |

**R**：`event-declaration-mismatch` 在全專案零 FlowStep 引用該 EVT 時成立（SPEC-003 §3.5 判準：宣告 domain 無對應 flow），此時「引用該 EVT 的 UC」為空集合；EVT 為圖節點型別之一（CLAUDE.md §7 EVT 節點），節點詳情可顯示其 `consumers`／`producers` 宣告，正是該破洞需要檢視的內容；O2 離開 App 且保留次要操作的一致性較差；O3 落在與該 EVT 無關的選定 UC 上，誤導。
**裁決**：O1。**被放棄選項優點**：O2 直接到可修改處。
**P**：無。
**改法**：SPEC-003 §3.5〈互動反應〉事件類列補「無任何 UC 引用該 EVT 時，依『指向其他圖節點』列 jump 至 `nav-page-nodeDetail`（payload 為 EVT ID），次要操作不變」；SPEC-001 §5〈破洞項的主操作與次要操作〉表事件類列補同一分支。

#### S-32（V4-04 G11）外部程序掛住無逾時定義

同 S-17。

#### S-33（V4-04 G16）FR-05「圖示加計數徽章」在節點詳情部分損壞的位置

承接層：S1（SPEC-001 FR-05 驗收）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | FR-05 驗收補範圍：計數徽章屬清單級（Ticket 清單工具列）；節點詳情以欄位級圖示 + 說明文字標示，不另設計數 |
| O2 | 新增 | 節點詳情 header 增圖示 + 損失欄位計數徽章 |

**R**：SPEC-004 4.6 `damagedDetail` 何時選用列「工具列右側計數、票列末欄圖示、節點詳情欄位級標示」三處分工已定；SPEC-004 §3.6 §6 部分損壞列「欄位級 `IssueMarker.damagedDetail`（說明文字 slot）」；UC-05 例外場景〈含損壞票〉v1.2 已對齊清單級計數 vs 詳情欄位級說明的兩級分離；O2 與上述三處不一致。
**裁決**：O1。**被放棄選項優點**：O2 在欄位多時一眼得知損失數量。
**P**：無。
**改法**：SPEC-001 FR-05 驗收「詳情損壞以圖示加計數徽章呈現」改「詳情損壞於清單以圖示加計數徽章呈現（§4 含損壞），於節點詳情以欄位級圖示加說明文字逐欄標示（§6 部分損壞）」。

#### S-34（V4-04 G18）降級旗標在浮層「該項」的呈現

承接層：S4（4.9 `RecentProjectItem`）。入口無專案時的文字非缺口：SPEC-004 4.8 `projectSwitcherEntryLabel`；收合入口不掛降級標示依 SPEC-001 §1 註記字面「專案切換浮層該項亦標示」（限清單項，各畫面 header 已常駐 `badge-<screen>-degraded-schema`）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 4.9 增 `isDegraded` 傳值，摘要列後附 `Badge.tag`（`degradedSchemaShortLabel`） |
| O2 | 改造既有 | 降級以 `reason` 常駐文字列承載 |
| O3 | 零新增（反向框架） | 刪除 SPEC-001「浮層該項亦標示」 |

**R**：SPEC-001 §1 降級註記 (4) 定浮層該項須標示，4.9 條目無任何降級 slot（grep「降級」「degraded」零命中）——上游有、元件無；O2 的 `reason` 在 4.9 綁定 disabled 狀態，降級項為可用項，混用使 enabled 項出現原因文字；4.5 `Badge.tag` 為 neutral 非互動標籤，語意相符；O3 移除定案（`0.1.0-W1-035`）的一部分。
**裁決**：O1。**被放棄選項優點**：O2 不增新 key。
**P**：`kMinWindowSize` 下浮層寬不足以容納摘要 + 健康徽章 + 降級標籤 → 改為摘要列換行。
**改法**：SPEC-004 4.9 slot 契約增 `isDegraded`（`bool`，預設 `false`）；內容政策增降級標籤列；§4.0.6 新 key 表補 `degradedSchemaShortLabel`（zh「內建型別表」）；測試點補「有／無降級」。

#### S-35（V4-04 G19）健康徽章計數的來源

承接層：DOM（workspace domain）；S1 附帶（§7 註記）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 零新增 | 0.1 以假資料驅動；計數語意「問題數」保留（`healthBadgeA11yLabel` 既有），計數來源由 workspace domain 定義 |
| O2 | 新增條文 | 計數＝該專案上一次破洞掃描的破洞總數（需跨工作階段持久化） |
| O3 | 零新增（反向框架） | 0.1 移除健康徽章 |

**R**：SPEC-004 4.9 資料態欄「0.1 為 fixture 固定值，真實探測由 workspace domain 後續票承接」；`app_zh.arb` `healthBadgeA11yLabel`「{count} 個問題」，語意為計數而非狀態列舉（探針猜的六值值域與既有元件不符）；O2 需持久化掃描結果，SPEC-005 無此接縫，且 tech-decisions 明定 0.1 不做 cache；O3 刪 SPEC-001 §7 顯示欄既有項。
**裁決**：O1。**被放棄選項優點**：O2 讓徽章有真實意義；O3 避免 0.1 顯示無來源數字。
**P**：假資料數字在實機驗證被誤讀為真實狀態 → 重評 O3。
**改法**：SPEC-001 §7 表下補註記「健康徽章為問題計數；0.1 以假資料驅動，計數來源由 workspace domain 定義」。workspace domain 的計數來源定義不在 .40／.41／.42 範圍，需另立承接（見〈不在三檔的項目〉）。

#### S-36（V4-04 G20）最近專案清單的排序與上限

承接層：S3（SPEC-003 §3.7）；DOM 附帶（清單持久化）。上限非缺口：SPEC-004 4.42 `RecentProjectItem` 數量無上限、空間不足策略為捲動。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增條文 | 依最近一次開啟時間降冪；目前專案列入（4.9 `selected`）；選取後移至頂端 |
| O2 | 新增條文 | 目前專案排除於清單 |
| O3 | 新增條文 | 依專案名排序 |

**R**：SPEC-004 4.9 有 `selected（目前專案）` 狀態與「點選重載同一專案」行為——目前專案在清單中，O2 與之衝突並使 S-10 的重新載入路徑消失；「最近專案」字面即時間序；O3 在清單長時使最近開過的專案不在頂端。
**裁決**：O1。**被放棄選項優點**：O3 位置穩定可記憶。
**P**：無。
**改法**：SPEC-003 §3.7〈互動反應〉最近專案項列補「清單依最近一次開啟時間降冪；目前專案列於清單內；成功載入後該項移至頂端」。清單持久化接縫由 workspace domain 補（SPEC-005 現只有單一路徑的 `writeString`）。

#### S-37（V4-04 G21）可用性探測的時機與探測中呈現

承接層：S3（SPEC-003 §3.7）；S4 附帶（4.9）。逾時值維持 SPEC-003 §3.7 既述「屬 workspace domain」。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 新增條文 | 浮層展開時逐項探測；探測未完成的項 `enabled` 為 `false`，同列常駐 `probingReason` 文字；完成後依結果轉 enabled 或維持 disabled（附原因） |
| O2 | 新增條文 | 探測中 `enabled` 為 `true`，點擊後交由載入流程處理 |
| O3 | 新增條文 | App 啟動時預先探測並保留結果 |

**R**：SPEC-004 4.9 狀態矩陣 enabled 列回饋時機「等確認才顯示（可用性為探測完成後的資料態）」——探測完成前不呈 enabled，與 O1 一致；O2 讓不可用資料夾進入載入中，而 SPEC-001 §1 無「資料夾不可用」落點（S-13 已定此類不轉狀態）；O3 的結果在展開時可能過期（外接磁碟卸載），SPEC-003 §2.8 0.1 無檔案監看；SPEC-003 §3.7「探測逾時期間浮層仍可操作、其餘項不受影響」與 O1 相容。
**裁決**：O1。**被放棄選項優點**：O2 對可用專案少一段等待；O3 展開即可點。
**P**：探測普遍慢於浮層展開動畫，使用者每次展開都看到整排「檢查中」→ 重評 O3 並定過期條件。
**改法**：SPEC-003 §3.7〈生命週期〉新增列「展開時：對每個最近專案項發起可用性探測；探測未完成的項 `enabled` 為 `false`、同列常駐 `probingReason`；各項探測互不阻塞」；SPEC-004 4.9 disabled 列進入條件補「探測未完成（reason 為 `probingReason`）」，§4.0.6 新 key 表補 `probingReason`（zh「檢查中…」）。

#### S-38（V4-04 G22）選到不可讀或不存在的資料夾

同 S-13。

#### S-39（V4-04 G23）選取專案後可見頁是否切到 Domain 視圖

承接層：S3（SPEC-003 §3.7 最近專案項列、§2.8 L2）。

| 選項 | 類別 | 內容 |
|------|------|------|
| O1 | 改造既有 | 切換專案一律以 rail 語意切至 `nav-page-domain`（`returnTo` 為 `null`） |
| O2 | 零新增 | 維持目前可見頁，Domain 在背景載入 |
| O3 | 改造既有 | 可見頁重置到預設頁（即 Domain）但以「重置」而非 rail 描述 |

**R**：SPEC-003 §2.8 L2「六頁狀態全部重置為各自初始狀態；`returnTo` 設為 `null`」、§2.8 App 啟動「落地於 `nav-page-domain`（`selectedDestinationProvider` 預設值）」——「初始狀態」含可見頁預設值，O1／O3 同結果；UC-01 成功保證「Domain 視圖顯示該專案的圖譜，或明確說明為何無法顯示」；O2 下其他頁在載入中落 R9「專案未就緒」，使用者必須再點一次前往 Domain，且阻擋結果（不是框架專案等）只在 Domain 呈現，停在他頁時使用者看不到 gate 結論。O1 與 O3 差在描述是否引用既有 rail 定義，O1 可直接用 §2.4 rail 的斷言形式。
**裁決**：O1。**被放棄選項優點**：O2 保留使用者所在位置。
**P**：與 .40（R9）交界——若 R9 定案後「專案未就緒」狀態被判定為足以承接載入期，可重評 O2。
**改法**：SPEC-003 §2.8 L2 列補「可見頁以 rail 語意切至 `nav-page-domain`」；§3.7〈互動反應〉最近專案項與選擇其他（選定後）兩列補「`IndexedStack` 可見頁為 `nav-page-domain`」。
### 承接層計數

| 承接層 | 項數 | 項次 |
|--------|------|------|
| S1 SPEC-001 狀態／顯示 | 11 | S-01、S-02、S-04、S-07、S-09、S-23、S-24、S-26、S-27、S-28、S-33 |
| S3 SPEC-003 互動 | 24 | S-03、S-05、S-06、S-08、S-10、S-11、S-12、S-13、S-16、S-17、S-18、S-19、S-20、S-21、S-22、S-25、S-29、S-30、S-31、S-32、S-36、S-37、S-38、S-39 |
| S4 SPEC-004 元件 | 3 | S-14、S-15、S-34 |
| S2 SPEC-002 token | 0 | — |
| HOW 畫面票 how | 0 | — |
| DOM domain 規格 | 1 | S-35 |
| IMPL 屬實作期決定不入規格 | 0 | 39 項皆改變可斷言的畫面行為或資料規則，無一可留給實作者各自決定 |
| （不需追修）R 項消解／非缺口／PEND | 4／13／3 | 見〈不需追修的 20 列〉 |
| **合計** | **59** | |

主承接層之外另有附帶改動（如 S-11 主在 S3、附帶 S1 §8.2 與 S4 i18n），分組清單依實際觸及檔案計列。

### 追修清單（依承接檔分組）

#### 票 A `0.1.0-W3-335.40`（R9 專案未就緒）：0 項

本票無項目歸入。交界兩處供 .40 執行者知悉：S-28（專案未就緒時追溯視圖不出現 Ticket 未載入說明列）、S-39（切換專案後可見頁切至 Domain，其他頁在載入期落 R9 狀態的機會降低）。

#### 票 B `0.1.0-W3-335.41`（SPEC-001／SPEC-003）：36 項

SPEC-001（16 項，12 個改動點）：

| # | 位置（檔名＋章節） | 改法摘要 | 來源 |
|---|------------------|---------|------|
| 1 | SPEC-001 §4 未載入 顯示欄 | N 定義改為 ticket 檔案數（列舉不解析） | S-01、S-02 |
| 2 | SPEC-001 §4 無 ticket 進入條件；§4 註記 | 進入條件改「檔案數為 0」；新增解析失敗票計入並以損壞列出現、逐檔 IO 失敗同此 | S-07、S-04 |
| 3 | SPEC-001 §4 註記 | 主題排序與歸屬比照框架（`PRIORITY_LEVELS`、首次出現序、載入序、最後一筆指派） | S-09 |
| 4 | SPEC-001 §8.2 | 新增三列：帶目標跳入目標不存在、選擇器無法開啟、資料夾不可讀或不存在 | S-11、S-13、S-38 |
| 5 | SPEC-001 §2 註記 | 步驟序號依 flow 陣列序，三處同源 | S-23 |
| 6 | SPEC-001 §2〈事件流小表〉「列」列 | 多個發出步驟列出全部；列序依首次出現步驟序號 | S-24 |
| 7 | SPEC-001 §3 註記 | 父節點層級：PROP／SPEC／UC 為父，Ticket 為葉 | S-26 |
| 8 | SPEC-001 §3 註記 | 多父節點：各父節點下各出現一次 | S-27 |
| 9 | SPEC-001 §3 註記；§3 正常與鏈路斷裂 可用操作欄 | Ticket 層依 §4 載入；未載入時止於 UC 並提供前往 Ticket 清單 | S-28 |
| 10 | SPEC-001 §5〈破洞項的主操作與次要操作〉表 事件類列 | 無 UC 引用時 jump 節點詳情顯示 EVT | S-31 |
| 11 | SPEC-001〈功能需求〉FR-05 驗收 | 計數徽章屬清單級，節點詳情為欄位級圖示 + 說明 | S-33 |
| 12 | SPEC-001 §7 表下註記 | 健康徽章為問題計數，0.1 假資料，來源歸 workspace domain | S-35 |

SPEC-003（27 項，25 個改動點）：

| # | 位置（檔名＋章節） | 改法摘要 | 來源 |
|---|------------------|---------|------|
| 1 | SPEC-003 §2.2〈外部開啟契約〉表 | 新增「逾時」列：0.1 不設逾時、無 UI 結局 | S-17、S-32 |
| 2 | SPEC-003 §2.5〈C4 的斷言方式〉段後 | 補「不存在取消逾期的執行期分支」 | S-17 |
| 3 | SPEC-003 §2.8 生命週期契約 L2 列 | 可見頁以 rail 語意切至 `nav-page-domain` | S-39 |
| 4 | SPEC-003 §2.10 表 | 新增兩列：關閉鈕後焦點回原格、schema 面板收合後焦點停在觸發鈕 | S-16 |
| 5 | SPEC-003 §3.1〈互動反應〉選擇資料夾之後 | 新增兩列：選擇器無法開啟、資料夾不可讀或不存在（停留、SnackBar） | S-13、S-38 |
| 6 | SPEC-003 §3.1〈互動反應〉取消載入列 | 補清除已存路徑、最近清單不變 | S-21 |
| 7 | SPEC-003 §3.1〈互動反應〉泳道開啟原始檔列 | `notFound` 改不帶動作的 SnackBar | S-18 |
| 8 | SPEC-003 §3.1〈導航跳轉與退出〉無可消費的型別表列 | 補「不經載入中」與理由 | S-19 |
| 9 | SPEC-003 §3.1〈生命週期〉 | 新增「由 jump 帶 domain 進入」列 | S-20 |
| 10 | SPEC-003 §3.2〈互動反應〉開啟原始檔（檔案不存在）列 | 改不帶動作的 SnackBar | S-18、S-22 |
| 11 | SPEC-003 §3.2〈事件流小表〉「列」列 | 發出格多步驟與列序 | S-24 |
| 12 | SPEC-003 §3.2〈動畫提示〉第一列 | 拆兩列：三態間只主欄 cross-fade；進出無 UC 整體 | S-25 |
| 13 | SPEC-003 §3.3〈互動反應〉〈生命週期〉〈導航跳轉與退出〉；§4 #12、#13 | 新增 `action-traceability-goto-tickets` 與 Ticket 層出現規則 | S-28 |
| 14 | SPEC-003 §3.3〈互動反應〉表下 | 多父節點錨點多實例與缺口計數單位 | S-27 |
| 15 | SPEC-003 §3.3〈生命週期〉展開集合初始值列 | 「首次渲染即可見」改為結構可見 | S-29 |
| 16 | SPEC-003 §3.4〈互動反應〉搜尋列 | 比對 ID 與標題、子字串、不分大小寫 | S-05 |
| 17 | SPEC-003 §3.4〈排序欄首的元件級契約〉 | 新增 S8 比較語意 | S-06 |
| 18 | SPEC-003 §3.4〈生命週期〉 | 新增 N=0 列；首次切主題全部收合；再次可見列補重新載入路徑 | S-03、S-08、S-10 |
| 19 | SPEC-003 §3.4〈互動反應〉主題節展開收合列 | 未歸屬節錨點 `expander-tickets-unassigned` | S-08 |
| 20 | SPEC-003 §3.4〈帶目標跳入〉 | 新增「目標不在清單」列 | S-11 |
| 21 | SPEC-003 §3.5〈互動反應〉事件類破洞項列 | 無 UC 引用時 jump 節點詳情 | S-31 |
| 22 | SPEC-003 §3.5〈生命週期〉再次可見列 | 無既有結果時自動掃描 | S-30 |
| 23 | SPEC-003 §3.7〈互動反應〉最近專案項、選擇其他兩列 | 清單排序；可見頁切 Domain；選擇器失敗與資料夾不可讀分支 | S-36、S-39、S-38 |
| 24 | SPEC-003 §3.7〈生命週期〉 | 新增展開時探測列（探測中 disabled + `probingReason`） | S-37 |
| 25 | SPEC-003 §5 規格判讀註記表 | UC-05 `filter-in-list-mode` 的 `return_to` 判讀為手動切回 | S-12 |

注意：#13 改動 §4 對照表列內容，須在 .40（R9 補五列、改編號同步）之後執行，與 .37 建票分組依賴一致。

#### 票 C `0.1.0-W3-335.42`（SPEC-004）：15 項

| # | 位置（檔名＋章節） | 改法摘要 | 來源 |
|---|------------------|---------|------|
| 1 | SPEC-004 4.35 `TableRow` `ticket` 內容政策 | 解析失敗列：ID 取檔名、其餘文字格「—」 | S-07 |
| 2 | SPEC-004 4.35 `TableRow` `eventFlow` 發出格 | 多個以「、」串接 | S-24 |
| 3 | SPEC-004 4.26 `AppSnackBar` i18n 表；§4.0.6 新 key 總表 | 補 `ticketsTargetNotFoundMessage`、`folderPickerUnavailableMessage`、`folderUnavailableMessage` | S-11、S-13、S-38 |
| 4 | SPEC-004 4.26 `AppSnackBar` `withAction` 用例與動作列 | 刪除 UC Flow／Domain 開啟原始檔 notFound 的 `refreshAction` 用例（§3.6 保留） | S-18、S-22 |
| 5 | SPEC-004 4.38 `SwimlaneGrid` 用途、回饋契約焦點列、拖曳不變式 | 列首可點 `action-domain-select-<domainId>`；點擊與拖曳以位移閾值區分 | S-14 |
| 6 | SPEC-004 §3.6 §1 表下 | `SplitRow.header` 與 `SegmentedControl` 僅渲染於五個正常模式列 | S-15 |
| 7 | SPEC-004 4.23 `BlockedState` 回饋契約焦點列 | 「提案」改引用 SPEC-003 §2.10 新列 | S-16 |
| 8 | SPEC-004 §4.0.10 0.1 場景 | 新增場景：jump 帶 domain 進入時列首定位（4.37／4.38 執行） | S-20 |
| 9 | SPEC-004 4.39 `Tree` | 展開集合以節點 ID 為鍵，多父節點出現位置共用 | S-27 |
| 10 | SPEC-004 §3.6 §3 正常與鏈路斷裂列；§4.0.6 新 key 總表 | Ticket 未載入說明列歸屬與 `traceabilityTicketsNotLoadedHint`、`gotoTicketsAction` | S-28 |
| 11 | SPEC-004 4.9 `RecentProjectItem` slot 契約、內容政策、測試點；§4.0.6 | 增 `isDegraded` 與 `degradedSchemaShortLabel` | S-34 |
| 12 | SPEC-004 4.9 `RecentProjectItem` disabled 列；§4.0.6 | 探測未完成為 disabled，reason `probingReason` | S-37 |

（15 項對應 12 個改動點：S-07、S-11、S-13、S-14、S-15、S-16、S-18、S-20、S-22、S-24、S-27、S-28、S-34、S-37、S-38。）

#### 不在三檔的項目

| 項 | 承接 | 內容 | 綁定 |
|----|------|------|------|
| S-21 附帶 | workspace domain（SPEC-005 §2.2） | 清除已存路徑的接縫與其回饋點 | 需 PM 指派承接票 |
| S-35 | workspace domain | 健康徽章問題計數的來源定義 | 需 PM 指派承接票 |
| S-36 附帶 | workspace domain（SPEC-005） | 最近專案清單的持久化接縫（現僅單一路徑） | 需 PM 指派承接票 |
| V4-02 G07 | CLAUDE.md §6 Domain 視圖的列無來源 | 不入規格 | 既有待決項 |
| V4-03 #20 | domain-map §9 | 不入規格 | `0.1.0-W3-365` |
| V4-04 G08 | CLAUDE.md §6 破洞分類 | 不入規格 | 既有待決項 |

UC 文件：0 項（本票裁決均不需改 UC 步驟或驗收）。

### 交用戶項目

0 項。
multi_view_status: skipped
reason: 依派發指示以單一代理人 WRAP 取代用戶逐項簽核，未派發多視角審查；規格改動由 .40／.41／.42 執行，其審查可覆核本票裁決。改動面最大、建議優先覆核者：S-27（多父節點錨點）、S-28（追溯 Ticket 層依載入狀態）、S-18／S-22（移除 refreshAction）、S-13／S-38（選擇器失敗與不可讀資料夾）

### 自檢結果

| 類別 | 結果 | 說明 |
|------|------|------|
| 59 列逐項 | 通過 | 20 列判不需追修（附依據），39 列 S-01～S-39 各含承接層、選項（含反向框架或零新增類）、R 事證、裁決、被放棄選項優點、P 失敗訊號、改法；完整模式六組另列三個缺點 |
| 範圍數字 | 通過（附更正） | 派發敘述與 .36 計數表記 49，逐列重數為 59，已於〈範圍更正〉記錄 |
| 已由 R 項消解 | 通過 | 4 列標明對應 R1／R3／R5 |
| 風格偏好 | 通過 | 0 項牽涉用戶寫程式碼風格偏好 |
| 依檔分組 | 通過 | .40 0、.41 36（SPEC-001 16／SPEC-003 27，重疊 7）、.42 15；不在三檔 6（workspace domain 3、既有待決 3） |
| 位置標示 | 通過 | 以檔名＋章節標題／表格列名，未用行號 |
| 唯讀 | 通過 | 未改 spec／UC／程式碼，未建票；僅寫本票 |
| 事實復核 | 通過 | 「非缺口」13 列逐一以 SPEC-004 條目或 SPEC-003 章節原文確認；S-09 以框架 `track_board.py`、`topic_assignments.py` 原文確認；S-34「4.9 無降級 slot」以 grep 確認 |

### Spawn 落地確認

本票不建票：派發約束明定唯讀、不建票，追修票 A／B／C 已建且 blockedBy 本票，依本票〈追修清單〉落檔。〈不在三檔的項目〉中 workspace domain 3 項需 PM 指派承接（新建或併入 workspace domain 既有票），本票不代建；3 項既有待決已綁定 CLAUDE.md §6、domain-map §9 與 `0.1.0-W3-365`。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "59 列逐項處理（.36 計數表記 49 為誤數）：不需追修 20（R 消解 4、非缺口 13、既有待決 3），真缺口 39 以 WRAP 定案；承接層 S1 11／S3 24／S4 3／DOM 1；分組 .40 0、.41 36、.42 15、workspace domain 3；交用戶 0"
confidence: 0.8
acceptance_met: [1, 2, 3]
acceptance_unmet: []
artifacts: []
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-335.36,0.1.0-W3-335.37 | chars: 743 -->

### Task Reference
- 0.1.0-W3-335.37 what: 裁決 V4 歸因的 12 項 spec 間矛盾（以 WRAP 評估取代用戶逐項裁決）（來源已完成，結論見 Solution）
- 0.1.0-W3-335.36 what: 歸因 V4 走查 143 個新猜測點與 5 項仍猜原猜測點，對照 SPEC-004 分出真缺口並提出 spec 追修草案（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-335.37 why: 用戶裁示（2026-09-14）：簽核改由 WRAP 評估，除非牽涉用戶寫程式碼的風格偏好。W3-335.36 列出 12 項需裁決的 spec 間矛盾（來源已完成，結論見 Solution）
- 0.1.0-W3-335.36 why: W3-335.30 判定 V4 未通過（原猜測點仍猜 5 項）；凍結門檻要求新猜測點逐項歸因、spec 缺描述者建追修票。探針禁讀 SPEC-004，多數「spec 未描述」可能在 SPEC-004 已有答案，需對照後才知道真缺口（來源已完成，結論見 Solution）

### Related Files
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.37.md  # from 0.1.0-W3-335.37（來源已完成，結論見 Solution）
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.36.md  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 12 項逐項 WRAP 評估：候選選項（含反向框架）、事證、裁決、被放棄選項優點、改動位置（檔名＋章節）與改法  # from 0.1.0-W3-335.37（來源已完成，結論見 Solution）
- [ref] [x] 牽涉用戶寫程式碼風格偏好者標「風格偏好，交用戶」，不代為裁決  # from 0.1.0-W3-335.37（來源已完成，結論見 Solution）
- [ref] [x] Solution 含依 spec 檔分組的追修清單（連同 .36 不需裁決的 8 項同步），供建追修票；本票不改 spec  # from 0.1.0-W3-335.37（來源已完成，結論見 Solution）
- [ref] [x] 143 個新猜測點與 5 項仍猜點逐項歸類表（題號＋猜測點編號、歸類、答案所在檔名＋章節或缺口說明）  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）
- [ref] [x] 各歸類計數；spec 間矛盾逐項列出兩處原文位置  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）
- [ref] [x] spec 間矛盾與真缺口的追修草案，依 spec 檔分組，每項標明是否需用戶裁決；不建票、不改檔  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）
- [ref] [x] skill 缺引導／缺範例者列出與 W3-335.31～.35 已涵蓋項次的重疊關係  # from 0.1.0-W3-335.36（來源已完成，結論見 Solution）
