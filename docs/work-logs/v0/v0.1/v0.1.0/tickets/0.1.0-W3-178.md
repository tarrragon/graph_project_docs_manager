---
id: 0.1.0-W3-178
title: 驗證 framework issue 102 的 spawned_tickets 撤銷與驗證路徑修復是否抵達
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: framework issue 102 依升級流程路徑 A 建立，需要一張驗證票承接修復抵達後的確認
  final_decision: 建 IMP 驗證票，trigger 為 sync-pull 帶回含 fix-version 的版本
  rationale: issue ref 不是合法 trigger，決策 trigger 綁定規則只接受 ticket ID；驗證票須在 close 當下建立而非等修復發生
who:
  current: thyme-python-developer
  history: {}
what: 驗證 framework issue 102 的 spawned_tickets 撤銷與驗證路徑修復是否抵達
when: sync-pull 帶回含 fix-version 的框架版本後
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/commands/fields.py
  - .claude/skills/ticket/ticket_system/lib/acceptance_auditor.py
  - .claude/skills/ticket/ticket_system/lib/audit_version.py
why: 本專案已於 tarrragon/claude#102 記錄 spawned_tickets 只進不出的三個缺口：無撤銷路徑、寫入前無血緣驗證、孤兒偵測只覆蓋子到父單向。三者咬合使 ANA 票的 spawned_tickets 一旦被填入不存在的 ID 即永久無法 complete 且無恢復手段。原分析票依 framework-issue 升級流程路徑 A 已 close，本票是該路徑要求的驗證票——issue ref 與「sync-pull 帶回修復」都不是合法 trigger，合法 trigger 只有 ticket，故於 close 當下建立本票承接，不等修復發生。
how:
  task_type: Implementation
  strategy: 以 sync-pull 帶回的版本實測三項：remove-spawned 子命令存在且支援多值、移除後 frontmatter 與 body 的 Spawn Requests 區塊無單邊殘留；resolve-spawn-request 對血緣不符的 ID 拒絕寫入且對 dismissed 不帶 ID 的既有用法仍放行；audit 能偵測父票列出不存在子票的反向孤兒。三項各以一次實際 CLI 呼叫驗證，不以讀原始碼代替執行。若修復未涵蓋某項，於 issue 以 observe 附加實測結果，不在本專案就地補實作。
acceptance:
- '[ ] remove-spawned 以實際 CLI 呼叫驗證單值與多值移除，且移除後 frontmatter 與 body 無單邊殘留'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

驗證 framework issue 102 的 spawned_tickets 撤銷與驗證路徑修復是否抵達

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 2026-09-10 抵達驗證：未抵達

**上游狀態**：`tarrragon/claude#102` 仍為 OPEN（updatedAt 2026-09-09T04:51:48Z），無 fix-version。

**本地事實**（`.claude/` VERSION 2.59.9，last_synced_base_sha `2c66df21`）：

| issue 記錄的缺口 | 本地查證 | 結論 |
|---|---|---|
| 無撤銷路徑 | `commands/track.py:535` 的子命令派發表只有 `add-spawned`，全庫無 `remove-spawned` / `del-spawned` 定義 | 未修復 |
| 寫入前無血緣驗證 | `commands/fields.py:705` `execute_add_spawned` 僅做去重後 append，不驗證目標 ticket 是否存在 | 未修復 |
| 孤兒偵測單向 | 未另行查證（前兩項已足以判定本票 acceptance 不可能滿足） | 未查證 |

**處置**：票保留於 pending，不 close。本票 acceptance（以實際 CLI 呼叫驗證 `remove-spawned` 單值與多值移除）在該子命令不存在時不可能滿足；trigger 仍為「sync-pull 帶回含 fix-version 的框架版本」，下次 sync-pull 後重驗。

**查證指令**：`gh issue view 102 --repo tarrragon/claude --json state`、`grep -rn "remove-spawned" .claude/`

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
