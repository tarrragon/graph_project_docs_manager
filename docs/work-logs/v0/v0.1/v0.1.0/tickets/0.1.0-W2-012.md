---
id: 0.1.0-W2-012
title: 修正 _builtinSchemaVersion 寫死且無對應資產與漂移偵測
type: IMP
status: pending
version: 0.1.0
wave: 2
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W2-011
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W2-011 驗收時發現門檻常數已落後 repo 自身 VERSION 二十個版本
  final_decision: 改為讀自資產並建立漂移偵測
  rationale: 寫死值加上只用低版本 fixture 測試，使這條門檻的真實行為永遠不被測到；而它控制的是一個已裁定必須提供的出口
who:
  current: parsley-flutter-developer
  history: {}
what: 把內建型別表版本由寫死常數改為讀自實際內建資產，並建立與框架版本的漂移偵測
when: W2-011 驗收時發現後
where:
  layer: Presentation
  files:
  - lib/screens/domain_view/domain_view_screen.dart
why: W2-011 在 domain_view_screen.dart 引入 const _builtinSchemaVersion 等於 2.40.3，作為降級出口的門檻：掃描到的專案 VERSION 高於它時不提供降級檢視。兩個問題：(1) 該常數宣稱對應 build 內嵌型別表副本的產生版本，但內嵌副本尚未資產化（W2-011 已在 Solution 記錄此為既有邊界），故 2.40.3 無任何資產背書；(2) 本 repo 的 .claude/VERSION 現值為 2.60.1，已高於該常數——真實專案走到此狀態時降級出口會被拒絕提供，W1-035 裁定的降級在實機上仍不可達，而 fixture 測試用 0.0.3 通過，綠燈掩蓋此事。現行無任何一處偵測此漂移
how:
  task_type: Implementation
  strategy: 兩段：(1) 版本值改為讀自實際內嵌的型別表副本（其 schema_generated_at_framework_version 欄），資產尚未建立時本段先建立該資產；(2) 建立漂移偵測——內嵌副本的產生版本落後 .claude/VERSION 達一定幅度時發出訊號，承擔者與門檻在票內指名，不得留為無 trigger 的待觀察（decision-trigger-binding 規則 1）。測試須含一組以真實 VERSION 值驅動的案例，不得只用遠低於門檻的 fixture 值
acceptance:
- '[ ] 內建型別表版本讀自實際內嵌資產而非寫死常數'
- '[ ] 測試含一組以現行 .claude/VERSION 實值驅動的案例，驗證降級出口在該情境下的行為符合 SPEC-001 §1 與 SPEC-003 §3.1'
- '[ ] 漂移偵測已建立，承擔者與門檻在票面指名'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-21'
updated: '2026-09-21'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

把內建型別表版本由寫死常數改為讀自實際內建資產，並建立與框架版本的漂移偵測

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W2-011 | chars: 526 -->

### Task Reference
- 0.1.0-W2-011 what: Domain 視圖在 state-domain-schema-unconsumable 傳入 onDegradedView callback，按下後切換為降級檢視並設定 DomainReady.isDegraded（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W2-011 why: W1-074 已交出 BlockedState.plain 的 onDegradedView slot，但畫面側未傳入該 callback，該退出路徑在 App 中仍不可操作——W1-074 的 acceptance 第三項（該退出路徑可實際操作走通）因此實際未達成，雖已被勾選。W1-035 定案的「型別表缺席時降級而非拒絕」在本票完成前仍未落地，該狀態只剩切換專案一個出口。降級後的常駐徽章另需 0.1.0-W2-009 的返回列容器（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] Domain 視圖 schema-unconsumable 狀態傳入 onDegradedView，按下後進入降級檢視且 isDegraded 為真  # from 0.1.0-W2-011（來源已完成，結論見 Solution）
- [ref] [x] VERSION 高於內建表產生版本時不提供該出口  # from 0.1.0-W2-011（來源已完成，結論見 Solution）
- [ref] [x] widget test 斷言該退出路徑操作後狀態確實改變（非僅斷言按鈕存在）  # from 0.1.0-W2-011（來源已完成，結論見 Solution）
- [ref] [x] 回填 0.1.0-W1-074 的 acceptance 第三項核對結果於本票 Solution  # from 0.1.0-W2-011（來源已完成，結論見 Solution）

