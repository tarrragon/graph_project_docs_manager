---
id: 0.1.0-W3-323
title: 附加 0.1.0-W3-321 判定的四則跨 session 觀測至既有 framework issue
type: ANA
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-321
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-321 判定完成後，五個面向的觀測尚無載體
  final_decision: 建 ANA 票承接觀測撰寫與附加，不在判定票內撰寫 issue 正文
  rationale: 派發範圍明訂本票判範圍與落點、不撰寫 issue 正文；觀測撰寫是獨立執行單位且不需 owner 識別，可與 owner 修法裁決並行
who:
  current: framework-issue-curator
  history: {}
what: 附加 0.1.0-W3-321 判定的四則跨 session 觀測至既有 framework issue
when: 隨時（observe 不需 owner 識別，無前置依賴）
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/section_comment.py
why: '0.1.0-W3-321 已完成五個面向的範圍與落點判定（不新開 issue，全部併入既有），但依派發範圍不撰寫 issue 正文。四則觀測（A 身分→#95、B 載體→#77、D1 區段 owner 生命週期→#81／#95 切分、D2 孤兒回收→#77、E 交叉驗證→#105）尚未落地；C 併發已由 0.1.0-W3-286 附加至 #56／#55，不重複。本方無合法 owner 識別（實測全部候選值 exit 3），四則一律走 observe，且該降級本身即 A 面向的實例，須於觀測內文記錄。'
how:
  task_type: Implementation
  strategy: 依 0.1.0-W3-321 Solution 的落點表逐則撰寫觀測並以 observe 附加；每則首行寫來源 session 與落點判定所在票；不得捏造 owner 以 add／init；不改任何 issue body；不改 .claude/ 資產；不跑 sync-push。
acceptance:
- '[x] 四則觀測已附加並記錄 comment 連結'
- '[x] 每則觀測已寫明與該 issue 既有區段的關係（重複／切分／引用）'
- '[x] C 面向未重複附加，已於 Solution 註明理由'
- '[x] 載體選擇（add 自有區段或 observe）已依 comment-as-section-protocol 的分界逐則判定並記錄理由；若仍降級 observe，原因寫入該則內文'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T20:21:10'
completed_at: '2026-09-11T20:32:44'
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

附加 0.1.0-W3-321 判定的四則跨 session 觀測至既有 framework issue

---

## Problem Analysis
### 問題根因

本票不是修缺陷，是把 `0.1.0-W3-321` 已完成的落點判定寫成 issue 上可讀的內容。直接根因是判定與內容落在不同執行單位：判定票依派發範圍不撰寫 issue 正文，四個面向的知識因此只存在於本地票面，其他 consumer 讀不到。

**票面 `why` 的一項前提已於執行當下失效**：票面（與 `0.1.0-W3-321` Solution 第五節）記「本方無合法 owner 識別，四則一律走 observe」。`0.1.0-W3-320`／`324` 已合併進 main，`init`／`add` 改為 CLI 自行推導 owner，本 session 實測 `resolve_write_owner(None)` 回 `graph-project-docs-manager-dcead428`，並實際以 `add` 建立兩則本方擁有的區段。降級因此不再是結構性限制，載體改為逐則依協定分界判定。

### 影響範圍

不改任何專案檔案與 `.claude/` 資產，產出全在 canonical repo 的 issue comment：`#95`（A 觀測 + D1 區段）、`#77`（B 區段 + D2 觀測）、`#105`（E 觀測）、`#81`（D1 切分互標觀測）。

已驗證的事實：四個面向的承重事實在本票逐項重讀原始碼或實跑複核（見「重現實驗結果」），未僅採信來源票轉述。未驗證：其他 consumer 端是否已取得 owner 修法；`orphan_confirmed` 在本 consumer 的歷史發生頻率。

### 相關 Error Pattern

未查到對應 pattern。相鄰者為 `#105` 記錄的骨架（檢查量的軸與缺陷所在的軸不同），本票四則中有兩則（D1、E）以其為引用或歸屬對象，不另記錄。

## 重現實驗結果
### 實驗方法

四個面向的承重事實全部重新查證，一律唯讀：讀 `section_comment.py` 的常數與函式本體、讀 `session-registry-start-hook.py` 的三分判定字串、`grep` 條文所在位置與 hook 執法面、實跑 `show`／`check`／`sessions`、以模組匯入實測 owner 推導值。寫入命令只在查證完成後執行（2 次 `add`、4 次 `observe`），未對任何 issue body 手動 PATCH、未 `init`、未 `update` 他方區段。

### 實驗執行

**owner 取值（推翻票面前提）**——以 `sys.path` 加入 `scripts/` 後匯入 `section_comment.py`，`resolve_write_owner(None)` 回 `graph-project-docs-manager-dcead428`；`CLAUDE_CODE_SESSION_ID` 環境變數在子代理人進程內可讀（值 `dcead428-e1c9-45e1-b5a1-9fc558daa8ff`）。其後兩次 `add` 皆 exit 0，回顯 `owner=graph-project-docs-manager-dcead428`，未傳 `--owner`。

**既有擁有關係**——`show 95` 實得 6 則既有區段，其中三則 owner 為 `graph-project-docs-manager-66`（他方 session，已不在 `ticket track sessions`），依協定不得 `update`，本票對其只以 `observe` 附加並在內文指名。

**D1 機制**——`_check_single_conclusion` 命中需同時滿足「存在晚於區段 `updated_at` 的觀測」與「時距 > `DEFAULT_STALE_DAYS = 7`」；`_owner_of` 只從首行標記取字串（無標記回 `?`），無任何存活性查詢。`check 95` 三則當前結論全未觸發。

**D2 事實**——`session-registry-start-hook.py` 的 `orphan_confirmed` 處置字串逐字含「本框架僅是無查證或回收路徑，非『已清除』」；同檔「來源」欄自陳 `framework issue tarrragon/claude#77`。`ticket track sessions` 實跑：一筆 STALE（141 分）、一筆 FRESH，`reclaimable` 欄皆 `-`。

**B 條文與執法面**——`grep` 實得禁令兩處：`pm-rules/parallel-dispatch.md`（PM 按需讀取）與 `references/cross-session-coordination-details.md`（按需讀取）。`grep -rln "SendMessage" .claude/hooks/` 只命中 `session-registry-start-hook.py`（回收請求說明），與該禁令無關——執法層為零。

**E 歸屬**——讀 `#105` body 全文，確認其已分「來源一：工具的宣稱與量測不符」與「來源二：使用者臨時發明的代理指標」兩類，本形態為後者的未列舉子型；`show 105` 實得 0 則區段、0 則觀測（body 即結論）。

### 實驗發現

已驗證：(1) owner 自行推導可用，子代理人可 `add` 自有區段，票面「一律降級 observe」前提失效；(2) `#95` 三則他方區段確實不可 `update`，A 與 D2 走 observe 的理由是射程重疊而非取不到 owner；(3) D1、D2、B 的機制事實與來源票記載一致，本票逐項獨立複核通過；(4) `#105` 無區段，body 即結論，補實例的正確載體是 observe。

仍未驗證：其他 consumer 是否已取得 owner 修法；「取樣面是否相同」有無機械判準（E 的防護方向）；`TaskStop` 對他 session 的 agent 是否可用（D2 回收端）。

## Solution
### 一、產出：六則 comment，四個面向，四張 issue

| # | 面向 | issue | 載體 | 連結 |
|---|------|-------|------|------|
| 1 | A 身分（owner 取值） | `#95` | observe | https://github.com/tarrragon/claude/issues/95#issuecomment-5634445079 |
| 2 | D1 區段 owner 生命週期（實作層） | `#95` | **add 自有區段**「當前結論（graph-project-docs-manager：check 主警訊無 owner 存活性維度）」 | https://github.com/tarrragon/claude/issues/95#issuecomment-5634441226 |
| 3 | D1 切分互標（協定層提醒） | `#81` | observe | https://github.com/tarrragon/claude/issues/81#issuecomment-5634447726 |
| 4 | B 載體（訊息落票執法層） | `#77` | **add 自有區段**「當前結論（graph-project-docs-manager：跨 session 訊息落票的條文執法層）」 | https://github.com/tarrragon/claude/issues/77#issuecomment-5634443129 |
| 5 | D2 孤兒回收 | `#77` | observe | https://github.com/tarrragon/claude/issues/77#issuecomment-5634447228 |
| 6 | E 交叉驗證獨立性 | `#105` | observe | https://github.com/tarrragon/claude/issues/105#issuecomment-5634447503 |

**「四則」與六則的對帳**：票面的「四則」是面向數（A／B／D／E，C 已完成不計）。D 在 `0.1.0-W3-321` 的落點表已拆為 D1（`#95` 實作層，與 `#81` 協定層切分）與 D2（`#77`），兩者根源資產與落點 issue 皆不同，無法共用一則載體；另依協定「切分的分工邊界寫入本方區段末段、既有 owner 的區段以 `observe` 提醒」，D1 另需一則指向 `#81` 的互標觀測。四個面向因此展開為六則 comment，無任何面向遺漏或重複。

### 二、載體逐則判定（依 comment-as-section-protocol〈三種關係處置〉，非沿用前票的降級）

前置條件已變更：`0.1.0-W3-320`／`324` 合併後 `init`／`add` 自行推導 owner，本 session 實測可得 `graph-project-docs-manager-dcead428` 並實際建立兩則區段。**因此本票不套用票面預設的「一律 observe」**，逐則判定如下：

| # | 關係（取自 `0.1.0-W3-321`，未重做） | 判定的射程比對對象 | 載體與理由 |
|---|---|---|---|
| 1 | 重複，射程重疊 | `#95`「當前結論（graph-project-docs-manager：skill 自身缺陷）」（owner `graph-project-docs-manager-66`） | `observe`。協定對射程重疊的處置即 observe 並指名重疊區段；該 owner 為他方 session，本方不得 `update`。**理由是重疊，不是取不到 owner** |
| 2 | 重複，射程不重疊 | 同上區段：其四項缺陷共同根因為「操作者的意圖是否真的被執行」，與本項根因（警訊的代理指標與被代理性質脫鉤）不同主題 | `add` 帶 `（<consumer>：<主題>）` 後綴，本方成為 owner。同領域同層級而主題不同，協定處置即 `add` |
| 3 | 切分（協定層對實作層） | `#81` 為 comment-as-section 協定層，既有區段皆他方 owner | `observe`。協定明訂分工邊界寫入本方區段末段、既有 owner 的區段以 observe 提醒 |
| 4 | 切分，但不新開 issue | `#77` body 功能缺口表（訊息格式與協商機制）與既有「當前結論」（並行協調現況、通道可達性） | `add`。`0.1.0-W3-321` 已判定切分的另一半併入同一 issue 而非另立；相對既有當前結論區段主題不同（內容持久性對通道可達性），故為區段而非觀測 |
| 5 | 重複，射程重疊 | `#77`「當前結論」第二節（dispatch-active 記錄生命週期與回收） | `observe`，內文指名重疊節次 |
| 6 | 實例（新增來源子型） | `#105` 無任何區段（`show` 實得 0 則），body 即結論 | `observe`。貢獻形態是補一個實例與一個未列舉子型，不改寫既有結論、不 `init`（不接管他人主題的結論位置） |

### 三、C 面向未重複附加的理由

C（共用 index 整棵樹快照）已由 `0.1.0-W3-286` 於 2026-09-10 完成升級：兩條 AND 判準逐條套過，觀測已附 `#56`（`issuecomment-5628952523`，條文面）與 `#55`（`issuecomment-5628962387`，實作面），並已建驗證票 `0.1.0-W3-287`。本票今日可補的新事實（`topic-assignments.txt` 兩方皆判定無法安全提交而擱置、commit `22727b80` 的回滾）屬同一結論的再次實例，不改變任何既有判定——重複附加只增加 comment 數而不增加資訊，且會使同一結論出現第二個承擔者。**無需建 ticket**：落地驗證已由 `0.1.0-W3-287` 承接。

### 四、每則與既有區段的關係（acceptance 第 2 項）

上表第三欄即為逐則的關係與比對對象；每則 comment 內文亦各自寫明一次（observe 三則於首段指名重疊區段名稱或說明 issue 無區段，兩則 add 區段於「分工邊界」與「與本 issue 既有內容的邊界」節指名引用與切分對象）。跨 issue 的引用關係已在內文記錄：D1 與 `#105` 為引用（同形骨架、不同資產）、D2 與 `#89` 為引用（方向相反，不附加）、B 與 `#105` 為引用。

### 五、驗證

| 項目 | 結果 |
|---|---|
| `show 95` | 區段 7 則，新區段在索引內，owner=`graph-project-docs-manager-dcead428` |
| `check 95` | 主警訊三則皆未觸發；comment 數 16 < 30；索引一致 7 筆 |
| `show 77` | 區段 6 則，新區段在索引內 |
| `check 77` | 主警訊三則皆未觸發；comment 數 20 < 30；索引一致 6 筆 |
| 他方區段 | 未執行任何 `update`；`#95` 三則 `-66` 區段內容未變 |
| 專案檔案 | 未修改任何專案檔或 `.claude/` 資產；未 `sync-push`／`skill-sync push`；未 `close` 任何 ticket（本票非收束票，無範圍票） |

### 六、Spawn 落地確認

| 規劃項 | 落地 |
|---|---|
| 四個面向的內容撰寫與附加 | 本票已完成（六則 comment，連結見第一節） |
| C 面向新增事實補附 | **無需建 ticket**：不改變 `0.1.0-W3-286` 的既有判定，落地驗證已有 `0.1.0-W3-287` 承接 |
| D1 的三個候選方向、B 的三個候選方向 | **無需建 ticket**：皆為 `.claude/` 通用資產修法，依升級流程路徑 A 修復地點在 canonical repo；候選與代價已寫入對應 issue 區段供該處裁決，本地建票會把未定案方案固化為工作項 |
| owner 修法的殘留（前綴層無維護者訊號、SessionStart 粗篩漏檢面） | **無需建 ticket**：兩者皆為上述 D1 方向 B／C 的內容，已寫入 issue 區段，不另立本地票 |

### 七、來源票對照

| 來源票 | 承載內容 | 本票如何使用 | 處置 |
|---|---|---|---|
| `0.1.0-W3-321` | 五面向的 dedup、AND 判準與落點判定 | 落點與關係判定直接取用，未重做 | 不動（非派發範圍，已 completed） |
| `0.1.0-W3-311` | owner 驗證器證據（156/256、跨 consumer） | A 觀測的前序記錄，本票記其前提已被修法解除 | 不動 |
| `0.1.0-W3-315`／`319` | owner 修法方向與尾碼裁決 | A 觀測的前序記錄 | 不動 |
| `0.1.0-W3-320`／`324` | owner 自行推導的落地 | 本票實測其生效，據以改變載體選擇 | 不動 |
| `0.1.0-W3-297`／`306` | `check` 主警訊累積期不觸發 | D1 區段的先前狀態與實測來源 | 不動 |
| `0.1.0-W3-286`／`287` | C 面向已升級與驗證 | 據以判定不重複附加 | 不動 |

本票未對任何 ticket 執行 `close`（無範圍票），未編輯 `.claude/` 資產，未改任何 issue body（`add` 的索引回填由 CLI 執行）。

### 自檢結果

| 類別 | 項目 | 結果 |
|---|---|---|
| A1 | 三明示 | 通過。載體判定表逐列給比對對象與理由；C 未附加段給後果（第二個承擔者）與替代動作 |
| A2 | 資訊優先序 | 通過。Solution 首節即產出與連結，判定理由在後 |
| A3 | 負向對比有正向錨點 | 通過。第二節明寫兩則走 observe 的理由是射程重疊而非能力受限，並對照兩則 add 的成立條件 |
| B1 | 禁用詞 | 通過（`數據`／`代碼`／`默認`／`文檔`／`軟件`／`硬件`／`信息`／`視頻` 全文 0 命中） |
| B2 | Emoji | 通過 |
| B3 | 簡體／日文漢字 | 通過 |
| C1 | Schema 必填章節 | 通過（Problem Analysis／重現實驗結果／Solution／Test Results／Completion Info 皆有實質內容） |
| C2 | 自定義 H2 | 通過（自訂標題一律 H3） |

補充自檢（本代理人職責）：未 `update` 他方 owner 區段、未第二次 `init`、未手動改 body、未操作派發範圍外的 ticket、未修改 `.claude/` 框架檔案；`git status` 中本票只動 `0.1.0-W3-323.md`（CLI auto-commit），其餘變更屬並行 session，未觸碰。

## Test Results
寫入命令（皆 exit 0，未傳 `--owner`）：

- `add 95 --name "當前結論（graph-project-docs-manager：check 主警訊無 owner 存活性維度）"` → `owner=graph-project-docs-manager-dcead428`，comment `5634441226`
- `add 77 --name "當前結論（graph-project-docs-manager：跨 session 訊息落票的條文執法層）"` → 同 owner，comment `5634443129`
- `observe 95`（A）→ comment `5634445079`
- `observe 77`（D2）→ comment `5634447228`
- `observe 105`（E）→ comment `5634447503`
- `observe 81`（D1 互標）→ comment `5634447726`

唯讀驗證：

- `show 95`：區段 7 則（新區段在索引內）、觀測流 9 則；`check 95`：主警訊三則未觸發、comment 16 < 30、索引一致 7 筆
- `show 77`：區段 6 則（新區段在索引內）、觀測流 14 則；`check 77`：主警訊三則未觸發、comment 20 < 30、索引一致 6 筆
- `show 105`（附加前）：0 則區段、0 則觀測，確認 body 即結論
- `resolve_write_owner(None)` → `graph-project-docs-manager-dcead428`；`CLAUDE_CODE_SESSION_ID` 於子代理人進程內可讀
- `ticket track sessions`：STALE 141 分 / FRESH，`reclaimable` 皆 `-`
- `grep -rln "SendMessage" .claude/hooks/`：僅 `session-registry-start-hook.py`（回收請求），跨 session 訊息落票禁令無執法層

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "四個面向展開為六則 comment 附加完成（2 則 add 自有區段 + 4 則 observe）；載體逐則依 comment-as-section 協定分界判定，未沿用票面預設的一律降級；C 面向未重複附加並記錄理由；show／check 驗證通過"
confidence: 0.9
acceptance_met: [1, 2, 3, 4]
acceptance_unmet: []
artifacts:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-323.md
  - https://github.com/tarrragon/claude/issues/95#issuecomment-5634441226
  - https://github.com/tarrragon/claude/issues/95#issuecomment-5634445079
  - https://github.com/tarrragon/claude/issues/77#issuecomment-5634443129
  - https://github.com/tarrragon/claude/issues/77#issuecomment-5634447228
  - https://github.com/tarrragon/claude/issues/105#issuecomment-5634447503
  - https://github.com/tarrragon/claude/issues/81#issuecomment-5634447726
context_dependencies:
  - 0.1.0-W3-321
  - 0.1.0-W3-320
  - 0.1.0-W3-324
  - 0.1.0-W3-286
  - tarrragon/claude#77 #81 #95 #105
estimated_recovery_effort: ""
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
**Completion Time**: 2026-09-11
**Executing Agent**: framework-issue-curator
**Review Status**: pending

六則 comment 已附加（連結見 Solution 第一節）；兩則為本方擁有的區段（owner `graph-project-docs-manager-dcead428`），四則為觀測。未 `update` 他方區段、未 `init`、未改任何 issue body、未 close 任何 ticket、未改專案檔與 `.claude/` 資產。

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-321 | chars: 1284 -->

### Task Reference
- 0.1.0-W3-321 what: 判定 跨 session 的 skill 設計與運作問題該成一個 issue 或一族，並完成查重與升級判定（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-321 why: **今日在單一工作日內累積了五個面向的跨 session 事實，全部可複驗，且全部指向同一個前提：框架的 skill 假設單一 session。**

| 面向 | 今日實測事實 | 來源 |
|---|---|---|
| 身分 | owner 尾碼含字母即 exit 3（均勻兩碼十六進位下不通過比例 156/256）；子代理人無取值管道；PM 短名與 scratchpad uuid 屬兩個命名空間且兩者都出現在觀測流 | `0.1.0-W3-311`／`315` |
| 載體 | 跨 session 訊息不是載體——平行 session 的發現、PM 的更正、判定的互相推翻，全靠人工落票才留存 | 本 session 多次 |
| 併發 | 共用 index 是整棵樹快照，未被任何人 stage 的檔案可隨 HEAD 推進無聲進入 commit；`topic-assignments.txt` 兩方皆判定無法安全提交而擱置；ticket md 曾被掃入並回滾 | `0.1.0-W3-286`；commit `22727b80` |
| 生命週期 | 區段 owner 為已結束的 session 時無觸發器（`check` 主警訊七天內不報，六張 issue 實跑全未觸發）；孤兒 agent 無查證或回收路徑 | `0.1.0-W3-297`／`306`；session-start 掃描 |
| 交叉驗證 | 兩個獨立 session 對同一問題各自得出同一個錯的全稱結論——**共用的不是結論而是取樣面**（三個活著的 session 名） | 本 session 與平行 session 的 owner 判定 |

**為何不直接開 issue**：五個面向若併為一個 issue，即 `0.1.0-W3-289` 對 `#56` 指出的「無邊界收納桶」——條目持續增長而無收斂條件，任何新發現都塞得進去。**範圍界定本身是本票的產出。**

**一個必須在票面記錄的自指條件**：本主題的 `init` 需要 owner 識別，而 owner 識別損壞正是本主題的內容之一。子代理人無法 `init`，降級 `observe` 只能附到既有 issue——**載體選擇因此被受測缺陷本身所限制**。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/framework-issue/  # from 0.1.0-W3-321（來源已完成，結論見 Solution）
- .claude/pm-rules/parallel-dispatch.md  # from 0.1.0-W3-321（來源已完成，結論見 Solution）
- .claude/rules/core/bash-tool-usage-rules.md  # from 0.1.0-W3-321（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] dedup 已逐面向分別查，五個面向與既有 issue 的涵蓋關係逐一判定  # from 0.1.0-W3-321（來源已完成，結論見 Solution）
- [ref] [x] 範圍形狀已判定（一個／一族／併入既有），判準寫明  # from 0.1.0-W3-321（來源已完成，結論見 Solution）
- [ref] [x] 若判定新開，已寫明該 issue 的收斂條件；答不出收斂條件者已改判併入既有  # from 0.1.0-W3-321（來源已完成，結論見 Solution）
- [ref] [x] 兩條 AND 判準已逐面向套用並留痕，非整包套一次  # from 0.1.0-W3-321（來源已完成，結論見 Solution）
- [ref] [x] 交叉驗證獨立性一項已判定歸屬  # from 0.1.0-W3-321（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
