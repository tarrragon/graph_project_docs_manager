---
id: 0.1.0-W3-095
title: 修復 頁首標題「Domain 視圖」左側缺內距，文字貼齊內容區左緣
type: IMP
status: completed
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-127
- 0.1.0-W3-128
source_ticket: 0.1.0-W3-078
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 實機目視發現排版缺陷
  final_decision: IMP 修頁首內距
  rationale: 六個畫面共用同一頁首容器，缺內距在每個畫面都可見
who:
  current: parsley-flutter-developer
  history: {}
what: 修復 頁首標題「Domain 視圖」左側缺內距，文字貼齊內容區左緣
when: 2026-09-08 實機驗證時由用戶目視發現（W3-078 執行期間）
where:
  layer: Presentation
  files:
  - lib/components/split_row.dart
  - test/unit/components/split_row_test.dart
  - test/unit/components/page_column_test.dart
  - test/widget/app/shell_test.dart
why: '實機執行時 Domain 視圖的頁首標題文字貼近內容區左緣，缺少內距。用戶目視發現（2026-09-08）。


  `page_column.dart:57` 有 `padding: EdgeInsets.all(Space.xl)`，因此內距在該容器層級是存在的——**問題可能不在「沒寫」而在「標題不在那個容器內」**（例如標題落在 `SplitRow.header` 而該 slot 未套用同一內距），需實查渲染樹確認，不要憑 grep 到的那一行就判定已有內距。


  頁首由六個畫面共用（SPEC-003 §2.4：`action-<screen>-back` 統一置於該畫面 `PageColumn` 的 `SplitRow.header`，由頁面框架容器單一渲染），因此本缺陷在每個畫面都可見，且修一處即全部修好。


  **測試為何沒攔截**：內距缺失不拋例外、不觸發溢位，既有的「不溢位」類斷言對它零鑑別力（與 `0.1.0-W3-094` 同源）。視覺對齊在現行測試形態下沒有任何斷言承載。'
how:
  task_type: Implementation
  strategy: '先以 DevTools 的 widget inspector 或 `debugDumpRenderTree` 確認標題實際落在哪個容器、該容器的內距為何——**不要憑 `page_column.dart` 已有 `EdgeInsets.all(Space.xl)` 就判定內距存在**，實機呈現與該行不符，兩者必有一處不是你以為的樣子。


    定位後修正，內距值取自 `Space` token，不硬編數字。若發現是 `SplitRow.header` slot 未套用內距，判斷該修 slot 本身（影響全部使用者）或該畫面（僅影響一處），並在 Solution 論證選擇。


    SPEC-004 若對頁首內距有明訂契約，以規格為準；規格未明訂則依既有 `Space` 階梯選值並在 Solution 記錄理由。規格與實作衝突時停手於 NeedsContext 上報。


    補一個能攔截的斷言：比對標題文字 `RenderBox` 的左緣與其容器左緣的距離等於預期 token 值。單純的「渲染成功」斷言不算數。'
acceptance:
- '[x] 標題左緣與容器內緣的距離為 Space token 值，六個畫面一致'
- '[x] 已實查渲染樹確認標題實際所在容器，Solution 記錄「為何 page_column 的 EdgeInsets.all(Space.xl) 未生效於標題」'
- '[x] 新增斷言比對標題左緣與容器內緣距離，修復前會紅燈'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-08T16:56:38'
completed_at: '2026-09-08T18:08:43'
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

修復 頁首標題「Domain 視圖」左側缺內距，文字貼齊內容區左緣

---

## Problem Analysis
### 問題根因

`SplitRow` 完全沒有實作水平內距，而頁首正是由它承載。

`page_column.dart:57` 的 `padding: EdgeInsets.all(Space.xl)` 只包住 `Expanded` 那一格（內容 slot 與其 `AnimatedSwitcher`）；頁首那一格是同層 `Column` 的另一個 child，寫法為 `SizedBox(height: LayoutSize.headerHeight, child: header)`，不在 `Padding` 之內。所以 ticket why 段的假設成立：內距在 `PageColumn` 存在，但標題不在那個容器內。

`SplitRow.build` 修復前為 `Row` → 直接 `SizedBox`/`DecoratedBox`，中間沒有任何 `Padding`。`PageTitle` 置於 `Expanded(child: leading)`，左緣即列的左緣。

**渲染樹實查證據**（非推論，`test/widget/app/shell_test.dart` 修復前紅燈輸出）：

```
Expected: <20.0>
  Actual: <0.0>
畫面「Domain 視圖」的頁首標題內距不符
```

`test/unit/components/split_row_test.dart` 四項水平內距斷言同時紅燈，四項的 `Actual` 皆為 `0.0`。

**規格契約**：SPEC-004 §4.29「使用 design token」間距列明訂「水平內距 `Space.xl`（頁首）/ `Space.sm`（底列、詳情卡）；左右格最小間距 `Space.md`」。三項中只有 `Space.md` 有實作，兩項水平內距從未落地。§4.28 `PageColumn` 的 token 表則只列「內容區內距 `Space.xl`」，未涵蓋頁首——規格對責任歸屬本身沒有歧義，是實作漏了。

### 影響範圍

`SplitRow` 的三種形態全部受影響，非只有頁首：

| 形態 | 規格值 | 修復前實測 | 使用點 |
|------|--------|-----------|--------|
| `header`（頁首） | `Space.xl` | 0 | `lib/app/shell.dart:120`，六個畫面共用 |
| `header` + `compactHeader`（詳情卡標題列） | `Space.sm` | 0 | 元件庫，畫面票尚未接線 |
| `footer`（Ticket 清單摘要底列） | `Space.sm` | 0 | 元件庫，畫面票尚未接線 |

六個畫面的頁首由 `lib/app/shell.dart:118-125` 的單一迴圈產生，共用同一份 `SplitRow.header`，故修 `SplitRow` 一處六畫面全修。

### 相關 Error Pattern

與 `0.1.0-W3-094` 同源：內距缺失不拋例外、不觸發溢位，既有的 `expectNoOverflow` 類斷言對它零鑑別力。`split_row_test.dart` 修復前已有 12 項測試全綠（含四項不溢位、兩項固定高、一項最小間距 `Space.md`），沒有任何一項會因水平內距為 0 而紅燈。

## Solution
### 修在 slot 而非畫面

ticket how 段要求論證「該修 `SplitRow.header` slot 本身（影響全部使用者）或該畫面（僅一處）」。選 slot，三個理由：

1. **規格已指名承擔者**。SPEC-004 §4.29 的 token 表把水平內距寫在 `SplitRow` 條目下，§4.28 `PageColumn` 的 token 表只列內容區內距。規格已經決定由誰承載，實作照做即可，不需重新裁量。
2. **改畫面修不完**。頁首雖由 `shell.dart` 單點渲染，但 `footer` 與 `compactHeader` 兩個變體的水平內距同樣缺失，且規格值不同（`Space.sm`）。修畫面只解一個變體，另兩個留給未來的畫面票各自踩一次。
3. **呼叫端不得覆寫**。§5.3 排列不變式對間距的措辭是「呼叫端不得覆寫」，把內距下放給呼叫端與該條文相斥。

### 改動

`lib/components/split_row.dart`：

- 新增 `_horizontalPadding` getter，與既有的 `_fixedHeight` / `_border` / `_background` 同一組變體衍生屬性，回傳 `Space.xl`（`header` 且非 `compactHeader`）或 `Space.sm`（其餘）。值取自 `Space` token，無硬編數字。
- `build` 的 `row` 外包一層 `Padding(padding: EdgeInsets.symmetric(horizontal: _horizontalPadding))`。**只設水平方向**：兩個固定高變體的高由 `_fixedHeight` 鎖定，垂直內距會壓縮子件可用高，且規格 token 表只講水平內距。
- 檔頭 dartdoc 補一句水平內距歸屬，並點名「父容器 `PageColumn` 的 `Space.xl` 只包內容 slot，不及於頁首 slot」——這正是本次的誤判點，寫在檔頭讓下一個讀者不必重查一次。

`test/unit/components/page_column_test.dart`：既有測試「間距使用 Space.xl token（非硬編碼）」原以 `find.descendant(of: PageColumn, matching: Padding)` 取唯一 `Padding`，`SplitRow` 加內距後該 finder 命中兩個而拋 `Bad state`。改為 `find.ancestor(of: AnimatedSwitcher, matching: Padding).first` 精確指向內容區那一個。**收窄而非放寬**：原斷言驗的是內容區內距，改後仍驗同一件事，只是不再依賴「樹裡只有一個 Padding」這個與斷言無關的巧合。

### 新增斷言

`test/unit/components/split_row_test.dart` 新增群組「水平內距（§4.29「使用 design token」間距列）」四項，量的是文字 `RenderBox` 左緣（或 trailing 右緣）與 `SplitRow` 自身 `RenderBox` 邊緣的距離，比對 token 值：

| 測試 | 斷言 |
|------|------|
| header：標題文字左緣與容器左緣的距離為 Space.xl | `titleRect.left - rowRect.left == Space.xl` |
| header：trailing 右緣與容器右緣的距離為 Space.xl | `rowRect.right - trailingRect.right == Space.xl` |
| compactHeader（詳情卡標題列）：水平內距為 Space.sm | `titleRect.left - rowRect.left == Space.sm` |
| footer（摘要底列）：左右水平內距皆為 Space.sm | 左右兩側各一 |

`test/widget/app/shell_test.dart` 新增「六個畫面的頁首標題左緣與頁面容器左緣距離皆為 Space.xl」，對應 acceptance 的「六個畫面一致」：起真實 `AppShell`，逐頁量標題文字左緣到該頁 `PageColumn` 左緣的距離。

三個測試設計決定：

- **取 `AppShell.pages` 的實例而非按型別搜尋**。`find.byType(PageColumn)` 在 `AppShell` 下命中 7 個而非 6 個——`TraceabilityScreen` 自建了一個巢狀 `PageColumn`（已建 `0.1.0-W3-127`）。改用 `find.byWidget(page)` / `find.byWidget(page.header)` 以身分定位，斷言不會因該缺陷修復與否而擺盪。
- **各處 `skipOffstage: false`**。`IndexedStack` 只繪製目前頁，其餘五頁預設會被 finder 跳過（實測：不加時第 2 頁「UC Flow」即找不到 `PageTitle`）；五頁皆已完成佈局，量測有效。
- **先 `expect(titleFinder, findsOneWidget)` 再取 widget**。直接 `tester.widget<PageTitle>` 在找不到時只拋 `Bad state: No element`，不說是哪個畫面；多一道斷言把失敗訊息換成畫面名。

### Spawn 落地確認

執行期發現兩項，皆已建票，無未落地項：

| 發現 | 落地 |
|------|------|
| `TraceabilityScreen` 自建巢狀 `PageColumn`，追溯視圖渲染兩條頁首、內容區內距疊加 | `0.1.0-W3-127`（IMP，P1） |
| SPEC-004 §4.29「尺寸契約」最小尺寸公式漏計兩側水平內距，與同節 token 表矛盾 | `0.1.0-W3-128`（DOC，P3） |

### 執行環境

本 session 在共用並行修復 worktree `.claude/worktrees/w3-110-search-debounce`（分支 `worktree-w3-110-search-debounce`）執行，該分支已有 `0.1.0-W3-112` / `0.1.0-W3-110` / `0.1.0-W3-094` 三票的 fix commit，本票 commit 同處。過程中對該 worktree 歸屬有一次誤判並已更正，記於 NeedsContext 第 1 項。


### Phase 4 重構評估

| 項目 | 結論 |
|------|------|
| 重構需要 | 否 |
| 技術債務 | 發現 2 項，皆已建票（`0.1.0-W3-127`、`0.1.0-W3-128`），非本次改動引入 |
| 程式碼品質 | A |

**結論說明**：`_horizontalPadding` 與既有的 `_fixedHeight` / `_border` / `_background` 是同一個模式——依 `variant` + `compactHeader` 衍生單一屬性，四者形狀一致，新增第四個不引入新概念。認知負擔指數：變數 0、分支 1、巢狀 1、依賴 1（`Space`），合計 3，落在「優良」區間。

考慮過但沒做的重構：四個 getter 都在做「依變體查表」，可收斂為單一 `_variantSpec` 物件（如 `record` 或 private class）一次回傳四個值。不做的理由是此改動會擴大到既有三個 getter 與其所有呼叫點，超出本票範圍；且四個 getter 各自語意清楚、無重複邏輯，收斂的收益是形式整齊而非可讀性提升。若日後變體增至三個以上再評估。

**未執行**：`/parallel-evaluation` 多視角外部審查未派發——本 session 為 worktree 隔離的 subagent，派發代理人屬 PM 職責。改動規模為單檔 30 行（1 個 getter + 1 層 `Padding` + 3 行 dartdoc），若 PM 認為需補外部審查請告知。

### 自檢結果（Layer 1）

**執行時機**：commit 後、complete 前（commit 由 `ticket track commit` 執行，自檢在 `complete` 閘門觸發時補做）

**掃描手段**（逐項可複驗）：

- `LC_ALL=C grep -n "數據\|代碼\|默認\|文檔\|軟件\|硬件\|信息\|視頻"` 掃三張 ticket md + 四個改動檔 → exit 1，零命中
- `LC_ALL=C grep -n` 簡體/日漢字常見混淆字元（这该们为对发处实后会个将应经说间时电区图统页签变层組結構測試執設計種規則）掃本票 md + 四個改動檔 → exit 1，零命中
- `LC_ALL=C grep -nP "[\x{1F000}-\x{1FAFF}\x{2700}-\x{27BF}\x{2B00}-\x{2BFF}\x{2600}-\x{26FF}]"` 掃 emoji → exit 1，零命中
- `grep -n "^## "` 列本票 body 所有 H2 → 9 個全為 Schema 合法章節名，無自定義 H2
- `zhtw` MCP（`content_type: markdown`、`translationese_domain: technical`）掃 Solution 全文 → errors 0 / warnings 0 / info 1，`detected_script: traditional`、`s2t_applied: false`
- `fvm flutter analyze` → No issues found
- 人工對照 `ticket-body-schema.md` IMP 必填章節（Problem Analysis / Solution / Test Results）→ 三者皆有實質內容，無殘留佔位符

| 類別 | 發現項目 | 處理方式 |
|------|---------|---------|
| A1 三明示 | 無 | — |
| A2 資訊優先序 | 無 | — |
| A3 負向錨點 | 無 | — |
| B1 禁用詞 | 無 | — |
| B2 emoji | 無 | — |
| B3 簡體／日漢字 | 無 | — |
| C1 必填章節 | 無 | — |
| C2 自定義 H2 | 無 | — |
| C3 Test Results 執行摘要 | 無 | — |
| zhtw ai_style（info） | 「三個理由」列表三項全以粗體關鍵字開頭，判為機械式列表 | 不修正：三項為並列論證，粗體是各項的論點標籤，去掉會讓讀者需讀完整段才知道該項在講什麼。此為 info 級非 error/warning |

**自檢結論**：已掃描範圍內通過（A1–A3 文字品質、B1–B3 字元集、C1–C3 Schema 結構，加上 zhtw 全規則掃描與 `flutter analyze`）。

**未涵蓋維度**（明列以免讀者誤讀為全維度合格）：

- 實機目視驗證（屬 PM 職責，見 Test Results「未執行項」）
- Layer 2 外部委員審查（`/parallel-evaluation` 未派發，見上節）
- `compositional-writing` 字句層 keyword bank 全量比對（僅跑 zhtw 的 1754 條規則，未逐條對照 keyword bank）

### 本票性質：補實作，不是調參數

這個區分決定了改動範圍與斷言對象，故獨立成節。

**不是「內距值錯了」**。SPEC-004 §4.29「使用 design token」間距列早已明訂 `SplitRow` 的三項間距：水平內距 `Space.xl`（頁首）/ `Space.sm`（底列、詳情卡），左右格最小間距 `Space.md`。三項之中只有 `Space.md` 有對應程式碼，兩項水平內距從未被實作——`SplitRow.build` 修復前是 `Row` 直接進 `SizedBox` / `DecoratedBox`，中間不存在任何 `Padding` 節點。沒有錯的值可以調，是整個契約條款缺席。

**後果差異**：若當成調參數，合理的修法是在出問題的那個畫面補內距，改完 Domain 視圖就收工；當成補實作，修的是契約承擔者本身，一次補齊三種形態。實測三種形態的內距皆為 0，證實後者才是實情——`footer` 與 `compactHeader` 尚未被任何畫面接線，缺陷還沒機會被目視發現，但缺席程度與頁首相同。

**斷言對象因此分兩層**，不是同一件事驗兩次：

| 層 | 檔案 | 驗什麼 | 為何需要 |
|----|------|--------|---------|
| 契約層 | `test/unit/components/split_row_test.dart` | `SplitRow` 對三種形態各自套用了規格指定的 token 值——量文字 `RenderBox` 邊緣到 `SplitRow` 自身 `RenderBox` 邊緣的距離 | 這是契約本身。不依賴任何畫面，`footer` 與 `compactHeader` 在無畫面接線的情況下也受保護 |
| 一致性層 | `test/widget/app/shell_test.dart` | 六個畫面的頁首標題左緣到各自 `PageColumn` 左緣皆為 `Space.xl` | 對應 acceptance 第一項「六個畫面一致」。驗的是「共用同一容器」這個結構事實沒有被個別畫面繞過 |

契約層是主斷言；一致性層若單獨存在，會退化成「驗某個畫面的標題位置」，畫面改用別的容器時它仍可能綠燈。

### 三變體缺失查證

依 PM 指示一併查底列與詳情卡，結果為同一處的同一個缺陷，本票一併修，未另開票：

| 形態 | 規格值（§4.29） | 修復前實測 | 修復後 | 對應斷言 |
|------|----------------|-----------|--------|---------|
| `header`（頁首） | `Space.xl` | 0 | `Space.xl` | 左緣 + 右緣兩項 |
| `header` + `compactHeader`（詳情卡標題列） | `Space.sm` | 0 | `Space.sm` | 左緣一項 |
| `footer`（摘要底列） | `Space.sm` | 0 | `Space.sm` | 左右緣各一項 |

`_horizontalPadding` 以 `variant` + `compactHeader` 分流，與既有的 `_fixedHeight` / `_border` / `_background` 同一組變體衍生屬性，不新增分流概念。
## Test Results
測試前景執行，環境為 FVM 釘選的 Flutter 3.47.1。

### 修復前（RED）

```
fvm flutter test test/unit/components/split_row_test.dart test/widget/app/shell_test.dart
```

五項新斷言全紅，`Actual` 皆為 `0.0`：

```
Failing tests:
  test/unit/components/split_row_test.dart: 水平內距（§4.29「使用 design token」間距列） header：標題文字左緣與容器左緣的距離為 Space.xl
  test/unit/components/split_row_test.dart: 水平內距（§4.29「使用 design token」間距列） header：trailing 右緣與容器右緣的距離為 Space.xl
  test/unit/components/split_row_test.dart: 水平內距（§4.29「使用 design token」間距列） compactHeader（詳情卡標題列）：水平內距為 Space.sm
  test/unit/components/split_row_test.dart: 水平內距（§4.29「使用 design token」間距列） footer（摘要底列）：左右水平內距皆為 Space.sm
  test/widget/app/shell_test.dart: 六個畫面的頁首標題左緣與頁面容器左緣距離皆為 Space.xl
```

shell 測試的紅燈訊息即渲染樹實測值：

```
Expected: <20.0>
  Actual: <0.0>
畫面「Domain 視圖」的頁首標題內距不符
```

### 修復後（GREEN）

```
fvm flutter test test/widget/app/shell_test.dart
00:01 +3: All tests passed!
```

全套：

```
fvm flutter test
07:06 +1175: All tests passed!
```

靜態分析：

```
fvm flutter analyze
No issues found! (ran in 52.6s)
```

失敗數 0，跳過數 0。

### 未執行項

實機目視複驗未做——本 session 為 worktree 隔離的 subagent，`/verify`、`/run` 類實機驗證依 `pm-rules/pm-role.md` 屬 PM 職責。widget test 量的是 `RenderBox` 座標，與實機為同一佈局管線，但「用戶目視確認貼齊感消失」這一步需 PM 起 app 複驗。

## NeedsContext
### 1. 本 session 的 worktree 歸屬（複查後已排除，無需 PM 動作）

執行前曾以 SendMessage 向 PM 回報「本 session 被隔離在別票的 worktree、commit 會落在別票分支」。**該判斷有誤，此處更正**：

- 首次觀測 `.claude/worktrees/w3-110-search-debounce` 時，工作區有 6 個屬 `0.1.0-W3-110` 的未提交檔案，據此推論該 worktree 為 W3-110 私有。
- 收尾前複查該 worktree 的分支歷史，`worktree-w3-110-search-debounce` 上依序有 `0.1.0-W3-112`、`0.1.0-W3-110`、`0.1.0-W3-094` 三票的 fix commit。這是一條多票共用的並行修復分支，worktree 名稱取自最早建立它的票，不代表歸屬。
- 首次觀測到的 6 個未提交檔案已由 W3-110 自行 commit（`c66576f2`），非被任何人掃走；本票的 4 個改動檔至今仍完整保留在工作區。

依 `tool-output-trust-rules.md` 規則 5 延伸：工作區狀態屬瞬時平面，`git log` 屬持久平面。前述誤判正是只憑瞬時觀測下持久結論所致——只看 `git status` 的 6 個檔案就認定分支歸屬，未查該分支的 commit 歷史。已補上交叉驗證，結論翻轉。

本票 commit 落在此共用分支，與 W3-094 / W3-110 / W3-112 同一處置，屬既有慣例。

### 2. harness 層指令與框架正本衝突（已依規則處置，僅記錄）

session 啟動時的 harness 指令要求「auto mode 下盡量用 Bash（sed / heredoc / 短腳本）改檔，Read / Edit / Write 只在 Bash 做不到時才用」。此與 `.claude/rules/core/tool-selection.md` 規則 1 相斥，該規則明文「本規則優先於 harness 層『優先用 Bash 改檔』類指令」。

依 `tool-output-trust-rules.md` 規則 6 衍生情境（harness 層行為指令與框架正本衝突時以正本為準，並於 NeedsContext 記錄衝突文本），本票所有檔案修改一律走 Edit 工具，唯讀操作（`cat` / `grep` / `sed -n`）不受此限照常用 Bash。此處只作記錄，不需 PM 動作。

### 3. where.files 已改寫為實際改動範圍（僅記錄）

建票時 `where.files` 記的是 `page_column.dart` / `page_title.dart` / `page_title_test.dart`，那是「假設內距缺在 PageColumn 或 PageTitle」的推測值。實查渲染樹後根因落在 `SplitRow`，已以 `ticket track set-where` 改寫為四個實際改動檔。原推測值不再保留於 frontmatter，其推測過程記於 Problem Analysis。

## Exit Status
```yaml
exit_status: success
reason: "SplitRow 三種形態的水平內距依 SPEC-004 §4.29 token 表落地，六個畫面頁首標題左緣距離皆為 Space.xl。五項新斷言修復前全紅（Actual 0.0）、修復後綠；全套 1175 測試通過，flutter analyze 無問題。實機目視複驗未做（屬 PM 職責），故非 1.0。"
confidence: 0.95
acceptance_met: [1, 2, 3]
acceptance_unmet: []
artifacts: ["lib/components/split_row.dart", "test/unit/components/split_row_test.dart", "test/unit/components/page_column_test.dart", "test/widget/app/shell_test.dart"]
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-08T18:08:43
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-078 | chars: 500 -->

### Task Reference
- 0.1.0-W3-078 what: 實機驗證 MacosScanNotifier 四項平台事實並回填 SPEC-003

### Rationale Chain
- 0.1.0-W3-078 why: W3-064 acceptance 2、3 承接，平台事實只有實機能驗

### Related Files
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W3-078
- lib/services/macos_scan_notifier.dart::read  # from 0.1.0-W3-078
- macos/Runner/AppDelegate.swift::read  # from 0.1.0-W3-078

### Source Acceptance (reference)
- [ref] [ ] 實機測得 UNUserNotificationCenter 在沙盒關閉（PROP-001）+ Developer ID 簽章下的可用性，並記錄 debug build 未簽章時授權請求的實際回應（是否直接回錯）  # from 0.1.0-W3-078
- [ref] [ ] Flutter 端載體的選擇已定案並附實測依據（原生 channel 或第三方套件，二擇一）  # from 0.1.0-W3-078
- [ref] [ ] 實機測得撤回已送達通知的 API，以及它對「通知中心已收合」狀態的效果  # from 0.1.0-W3-078
- [ref] [ ] 四項實測結果已回填 SPEC-003 §2.2「實作票驗證」段（原文改為實測記錄，附量測環境與日期）  # from 0.1.0-W3-078
- [ref] [ ] 0.1.0-W3-064 的 acceptance 2、3 已可勾選或已說明為何仍不可勾  # from 0.1.0-W3-078
