---
id: 0.1.0-W3-304
title: 落地 0.1.0-W3-300 的改進設計至 framework-issue 條文與引導層
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-301
- 0.1.0-W3-303
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-300
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-300 產出改進設計並明示「落地應為一張票而非四張」
  final_decision: 單一落地票，blockedBy 兩項停手條件
  rationale: 300 指出四項改進彼此有依賴（2-A 依賴擴散面操作化、4-B 與 3-A 共用地址、1-A 與 7-1-B 重疊），分四票會使同一位址被改四次且形狀不一
who:
  current: thyme-documentation-integrator
  history: {}
what: 落地 0.1.0-W3-300 的改進設計至 framework-issue 條文與引導層
when: 0.1.0-W3-301 與 0.1.0-W3-303 皆完成後
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/references/escalation-flow.md
  - .claude/skills/framework-issue/references/ticket-intake.md
  - .claude/skills/framework-issue/SKILL.md
  - .claude/agents/framework-issue-curator.md
  - .claude/skills/framework-issue/references/comment-as-section-protocol.md
why: '**`0.1.0-W3-300` 的改進設計已完成分析，本票是其落地**——修法形狀在此票可寫定，因為它經過了三組實測與一次反向檢驗，不是發現當下的判斷。


  **為何是一張票**：`0.1.0-W3-300` 明示四項改進彼此有依賴——2-A 依賴擴散面的操作化、4-B 與 3-A 共用同一個位址、1-A 與 7-1-B 重疊。分成四票會使同一位址被改四次且形狀不一致。


  **兩項停手條件必須先解**（`0.1.0-W3-300` 逐項標明）：


  - `0.1.0-W3-301`：`.claude/` 全樹的 sync 排除比例。候選 7-1-A（兩檔分界錨在 `should_exclude()`）的成立與否完全取決於這個數字——若第一檔接近空集合，該候選復活已被否決的票型閘門。

  - `0.1.0-W3-303`：`add` 與 `observe` 的分界。候選 3-A 的載體依賴它；分界未定則第二檔處置無載體。


  **反向檢驗的結果必須帶進落地**：`0.1.0-W3-300` 對收斂項一做了反向檢驗，**部分推翻了三張測試票的共同框架**——三者皆以「這一問能不能保證修法正確」為尺，而條文自述的職責是「使證據狀態可見」。依後者，`163/164` 通過第三問是**合法的**（它是可重跑的主張，且事後確實被重跑為 `316/320`）。**存活下來的問題比原報告窄，且落在第二問而非第三問。**


  **由此得到一項對落地直接有效的警告**：三張票隱含指向的修法（在第三問下加一個關於分母的子問）**是錯的位置且有實際副作用**——前提查證會遞迴，第三問將永遠為「否」，三問退化為「一律先開分析票」，再次復活候選 C。


  **不做則後果**：測試產出停在四張已完成的票裡，條文維持現形狀擴散至所有 consumer。'
how:
  task_type: Documentation
  strategy: '**先讀三份原文再動手**：`0.1.0-W3-300` 的 Solution（改進設計與反向檢驗）、`0.1.0-W3-301` 與 `0.1.0-W3-303` 的結論。**條文形狀取自 300，兩個停手條件的答案取自 301／303，不由本票重新設計。**


    **逐項落地時的硬要求**：


    1. **不得在第三問下加分母子問**（300 的反向檢驗已判定為錯誤位置，副作用是三問退化為一律開分析票）。存活的問題落在第二問——觀測本身的鑑別力。

    2. **不得破壞已確認有效的三項**：第二問把「須有明確結論」升級為「須做一個會失敗的觀測」；原則二堵死「規模小」的豁免路徑；三問對同一 issue 的不同缺口給出不同答案。**逐項說明改動後這三項是否仍成立。**

    3. **可證偽性是硬要求**：收斂項二（「消費端」兩種讀法）的處置須使該判準對同一輸入只能得出一種判定。300 已給拆分方案與書面 fallback——若拆分後仍讀得出兩義，**降為提示句移出豁免閘門，而非保留在閘門內**。

    4. **引導層須指名載體**：`ticket-intake.md` 步驟三為主（curator 唯一的必經路徑）、curator agent 定義一行路由、`SKILL.md` 觸發條件。判準是「執行下游動作的人會不會被指到」，不是「條文有沒有被引用」。

    5. **條文自身的規則 2 違反**（第 86 行以 issue ref 作 trigger）一併處置，處置後須自身合規。


    **載體與預算**：主文留在 `escalation-flow.md`（按需讀取層）。**自動載入層（`rules/core/`）本次不再新增任何行**——`quality-baseline.md` 規則 5 的接縫註記已於 `0.1.0-W3-293` 落地，除非 301／303 的結論使該註記失準，否則不動。


    **不做的事**：不改任何 CLI 或 hook（候選 B 仍待議）、不對 canonical issue 寫入、**不執行 `sync-push`／`skill-sync push`**（由 `0.1.0-W3-295` 承接時機）。


    **收尾**：`grep -rl` 檢查其他 `.claude/` 檔案是否描述同一流程而需同步；範圍過大者建 DOC ticket 追蹤，不口頭延後。'
acceptance:
- '[x] 四項改進已落地於指名載體，形狀取自 0.1.0-W3-300 未重新設計'
- '[x] 第三問未新增分母子問，存活問題落在第二問並已處置'
- '[x] 已確認有效的三項逐項說明改動後是否仍成立'
- '[x] 收斂項二的判準對同一輸入只能得出一種判定，否則已降為提示句並移出豁免閘門'
- '[x] 引導層改動已使 curator 的必經路徑指向條文，以 grep 驗證非零命中'
- '[x] 條文第 86 行的 trigger 違反已處置且處置本身合規'
- '[x] 自動載入層未新增行，或已說明為何必須新增'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T14:02:52'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

落地 0.1.0-W3-300 的改進設計至 framework-issue 條文與引導層

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### 停手條件一已解：`0.1.0-W3-301` 的量測結果與 PM 判讀（2026-09-11）

**量測結果**（`0.1.0-W3-301`，全樹 10991 檔、0 例外）：排除 8262 檔（75.17%）、未排除 2729 檔（24.83%）。但全樹比例對本問題無鑑別力，**有鑑別力的是分層結果**：

- `rules/`、`references/`、`agents/`、`methodologies/`、`pm-rules/`、`error-patterns/` 的排除比例**全為 0.000**。
- 高排除比例的目錄（`scripts/` 0.924、`hooks/` 0.803、`skills/` 0.775、`lib/` 0.619）經第二層拆解，來源是各 skill 的 `.venv/`、`__pycache__/`、`tests/` 產物，**不是規則或 skill 的定義本體**。
- 被排除的 8262 檔中，8253 檔落在已知執行期產物目錄；其餘 9 檔為 per-project 整合檔與本地狀態。**被排除的檔案中，規則／skill／references／agent 定義本體為零。**

**PM 獨立複核**（非採信回報）：直接呼叫 `lib.sync_exclude_manifest.should_exclude()` 對六個定義本體檔取樣，全數回傳 `False`；對 `.venv/pyvenv.cfg` 與 `hook-logs/` 回傳 `True`。方向與量測一致。

**判讀：候選 7-1-A 在本條文的主要射程內退化，`0.1.0-W3-297` F5 的預測成立。** 若兩檔分界錨在 `should_exclude()`，則第一檔（限本 consumer 且可逆，同票內寫成待驗證即可）在 `.claude/` 內**只含執行期產物**——而執行期產物不會是任何發現票的修法對象。**對任何真實的框架發現，第一檔是空的。**

**但退化的射程比 F5 的表述窄，這點落地時不可混淆**：`should_exclude()` 的定義域是 `.claude/` 樹內的路徑。落在 `docs/` 等專案檔的發現不受它管轄，第一檔對那些發現仍然有成員。**故 7-1-A 復活的不是候選 C 的全部效果**（C 的射程是全部「來源非分析票的實作票」，實測樣本 74 張），**而是其中限於框架資產的子集**。本票不據此裁定 7-1-A 的存廢——那是落地時依 `0.1.0-W3-300` 的候選比較決定，但**不得再以「未量」為由迴避**。


### 落地範圍擴充：comment-as-section-protocol.md 納入 where.files（2026-09-11）

`0.1.0-W3-303` 精確定位收斂項三（3-A 載體）的缺口不在 escalation-flow.md 或
ticket-intake.md，而在 `comment-as-section-protocol.md`〈init 前查重：三種關係處置〉
的「重複」格——該格明文列兩個選項（「以區段或觀測附加」）卻無擇一條件，303 的候選 A
（射程重疊 → observe 指名既有區段；不重疊 → add 帶後綴）直接補這一格。派發任務指示
「兩項停手條件已解，結論帶進落地」與「要補的是那一格，不是整張表」明確要求本票落地
此項。原始 where.files（4 檔，取自 0.1.0-W3-300 的設計射程）未列此檔，因 303 是在
300 之後才定位到精確位置。已以 `ticket track set-where --files` 補入該檔，維持
`ticket track commit` 隔離索引提交路徑可用。303 同時發現的跨檔斷裂錨點
（〈已知限制〉指向〈以 add 加進他方已 init 的 issue 時〉，該節實際位於
`ticket-intake.md` 非本檔）一併於此檔修正。
## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
**Completion Time**: 2026-09-11 14:19
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
**Summary**: 落地 0.1.0-W3-300 的改進設計，形狀取自其 Solution 第三至八節，未重新設計；兩項停手條件（301 全樹排除比例量測、303 add/observe 分界）已解並帶入。

檔案與章節：
1. escalation-flow.md〈修法時點分離〉：三問表第一問拆為「執行條件已觀測」／「使用者群已觀測」兩列並補合成規則（候選2-A，解可證偽性缺口）；第二問「反例已試」加強為「若修法或其前提為錯就會產生不同輸出」（候選1-A，第三問不變、加註「本問只管數字來源」，未在第三問下加分母子問）；擴散面判準由未操作化描述改為 should_exclude() 機械事實 + 本consumer機械訊號兩軸組合表；第二檔處置改寫為「待辦與來源」待裁列 + 「問題與方案」區段回填，移除「發現票降為ANA」（候選3-A+7-2-A）；三個檢驗案例表同步改寫為四問對照；第86行issue-ref trigger改寫為明確結論「候選B現不採用」+條件式操作規範（候選8-A，decision-trigger-binding規則2合規）。
2. ticket-intake.md〈步驟三：時序改狀態〉新增「修法閘門」段（三明示），呼應escalation-flow第二檔處置；〈派發curator〉owner識別行補範例格式與填寫紀律段（三明示，含漏填後果）。
3. comment-as-section-protocol.md〈init前查重：三種關係處置〉「重複」格拆為「射程不重疊→add」／「射程重疊→observe」兩列並補覆寫條款（303候選A）；〈已知限制〉修正跨檔斷裂錨點（指向ticket-intake.md非本檔）。
4. SKILL.md 按需讀取表escalation-flow.md觸發條件擴充涵蓋收束時點（候選4-A）；metadata.version 2.5.0→2.6.0。
5. framework-issue-curator.md「允許產出」收束列補一句路由指向修法時點分離判準（候選4-B+4-C，原路由0命中）；版本2.1.0→2.2.0。
6. CHANGELOG.md 新增2.6.0條目。

grep驗證（acceptance 5）：ticket-intake.md/curator.md對escalation-flow關鍵字引用由0→1/2；comment-as-section-protocol.md對ticket-intake引用由既有4（含新增1）；escalation-flow.md對comment-as-section-protocol/ticket-intake引用由0→3（〈修法時點分離〉段內由0→1）。四項收斂缺口的路由此前皆為0命中，現皆非零。

已確認有效三項逐項復算（acceptance 3）：
(a) 第二問「須做會失敗觀測」：候選1-A為強化非破壞，原句後半保留；297 C1（92/159可產生不同輸出的觀測→是）與298 C2（restore --staged無反例觀測→否）結論不變。
(b) 原則二堵死「規模小」豁免：宣告句位置不動；298 C3在新擴散面判準下復算——bash-tool-usage-rules.md措辭改動屬should_exclude()=False且規則層無機械訊號，仍落第二檔，結論不變。
(c) 三問對同一issue不同缺口給不同答案：1-B（第三問加子問，遞迴恆否）與2-B（刪第一問）皆未採，正是為保留此分辨力；三個檢驗案例表在新四問結構下仍呈現豁免/不豁免的差異化判定（案例一豁免、案例二三不豁免但各自否的問題不同：案例二敗在使用者群、案例三敗在執行條件與使用者群皆否），分辨力保留。

自動載入層（acceptance 7）：本次未修改任何rules/core/檔案，quality-baseline.md規則5接縫註記維持0.1.0-W3-293落地版本不變，未新增行。

未落地項（依300/303票面「本票不落地」的判定，留給後續裁票，非本票遺漏）：comment-as-section-protocol.md〈增長語意與close語意〉本身仍不含add/observe字樣（303已判定該節射程只是issue張數與close語意，不擴充其射程，缺口已在〈三種關係處置〉補齊，非該節本身）；7-2-B（ticket track commit對ANA型票面提交範圍的CLI變更）不在本票範圍，已知留給既有CLI契約issue家族。

驗證：uv run pytest .claude/skills/framework-issue/tests/ 139 passed；scan_links.py broken=10（皆為既存、與本次5檔無關）；未修改任何CLI或hook；未對canonical issue寫入；未執行sync-push/skill-sync push。

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-300,0.1.0-W3-301 | chars: 2006 -->

### Task Reference
- 0.1.0-W3-300 what: 統整 三組測試的條文與引導層缺口，產出改進設計（來源已完成，結論見 Solution）
- 0.1.0-W3-301 what: 量測 .claude 全樹的 sync 排除比例，作為兩檔分界能否錨在機械事實的判準（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-300 why: **三組測試獨立收斂於數項缺口，逐票各自修會使同一項被修三次且形狀不一。**

**收斂項一：第三問「範圍已量」問錯對象**（`0.1.0-W3-297` F3、`0.1.0-W3-298` G1 獨立命中）。條文只分「量到 vs 估計」，不問**分母如何定義**與**量測手段的前提是否成立**。實證：`163/164` 是量到的數字、第三問答「是」即放行，而複驗為 `316/320` 且整類 12 檔不在舊分母內。**這正是受測 issue 自己的主題**——失效與成功在可觀測面不可區分。

**收斂項二：核心詞在條文自己的案例中有兩種互不相容的讀法**（`0.1.0-W3-299`）。檢驗案例一把「消費端」讀為執行條件，案例二讀為使用者族群。對同一輸入套用兩種讀法得出**相反判定且兩者皆可引條文為據**——該判準因此不可證偽。

**收斂項三：第二檔處置指定了目的地，沒指定承擔者、載體與回程**（`297` F1、`298` G2、`299` #92 三組命中）。非區段擁有者只能 `observe`，而觀測流不是決策載體；區段 owner 為已結束的 session 時無觸發器（`check` 主警訊 7 天內不報）；`#92` 的修法已在 body 內，而協定禁止改寫 body——**條文要求的動作被協定禁止，且兩份檔案互不引用**。

**收斂項四：引導層路由指向錯的人**（`297` S1、`298` R1、`299` 各自 grep 實測；PM 已複核 `ticket-intake.md` 與 `framework-issue-curator.md` 對條文關鍵詞皆 0 命中）。**會讀到條文的是發現者，執行下游動作的是 curator，而 curator 的路徑不指向條文。**

**單組但實質的兩項**：兩檔分界「限本 consumer 且可逆」未定義，取擴散事實讀法則第一檔在 `.claude/` 下近乎空集合，條文退化為已被否決的票型閘門（`297` F5）；條文降 ANA 被當成較安全的處置，但 ANA 票在現行 CLI 下 `where.files` 預設唯讀、無合法提交範圍，提交路徑**比實作票更差**（`297` 附帶、`298` 實際觸發）。

**條文自身違反它引用的規則**（`298` G4，PM 已複核）：第 86 行「待 `#81` 區段討論後另行決定」以 issue ref 作 trigger，命中 `decision-trigger-binding.md` 規則 2（合法 trigger 限 ticket ID）；而同節三段前才聲稱不觸及該規則。

**不做則後果**：條文以現形狀擴散至所有 consumer，其中兩個判準不可證偽、一項處置指向執行不了的動作、引導層不指向執行者。（來源已完成，結論見 Solution）
- 0.1.0-W3-301 why: **這是一個數字決定一個條文候選存不存在的情形。**

`escalation-flow.md`〈修法時點分離〉的兩檔擴散面處置表，其分界「限本 consumer 且可逆」未定義。`0.1.0-W3-300` 的候選 7-1-A 提議錨在機械事實——`lib.sync_exclude_manifest.should_exclude()` 回傳 `False`（即在 sync 範圍）者落第二檔。**該候選的利是二值、可機械查證、無判讀空間。**

**但 `0.1.0-W3-297` F5 預測第一檔會接近空集合**：若 `.claude/` 下絕大多數檔案都在 sync 範圍，則第一檔（同票內寫成待驗證形態即可）幾乎沒有成員，條文退化為「一律先開分析票」——**正是 `0.1.0-W3-289` 以「票池近乎倍增」為由否決的那個候選的效果**。

**該預測至今無人量過**。三張測試票與統整票皆將其標為未驗證項。`0.1.0-W3-300` 明寫「這是本項唯一的停手條件——量測前不得落地」。

**不做則後果**：落地一個可能自我否定的條文，或因為不確定而放棄一個唯一無判讀空間的候選。兩者都是靠猜。（來源已完成，結論見 Solution）

### Related Files
- .claude/lib/sync_exclude_manifest.py  # from 0.1.0-W3-301（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] 四項收斂缺口各有至少兩個改進候選，各附利弊與對已確認有效部分的影響  # from 0.1.0-W3-300（來源已完成，結論見 Solution）
- [ref] [x] 收斂項二的處置使該判準對同一輸入只能得出一種判定，或明確判定應移除  # from 0.1.0-W3-300（來源已完成，結論見 Solution）
- [ref] [x] 收斂項三已回答承擔者、觸發器、與 comment-as-section-protocol 的相容性三問  # from 0.1.0-W3-300（來源已完成，結論見 Solution）
- [ref] [x] 收斂項四的處置已指名載體，判準為「執行下游動作的人會不會被指到」  # from 0.1.0-W3-300（來源已完成，結論見 Solution）
- [ref] [x] 條文自身違反 decision-trigger-binding 規則 2 的處置已提出且自身合規  # from 0.1.0-W3-300（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
