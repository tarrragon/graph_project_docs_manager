---
id: 0.1.0-W3-030
title: 重新設計 skill-design-guide 暫緩的 (a)(g) 兩組判準並處理 SKILL.md 體量餘裕
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-022
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-022 派發note 標示 (a)(g) 實例不足暫緩，未綁 trigger
  final_decision: 另立 ANA 票承接暫緩，trigger 為實例累積或 SKILL.md 下次增修
  rationale: 無 trigger 延後在「以後」與「永不」之間沒有可驗證邊界（decision-trigger-binding 規則 1）；SKILL.md 已用掉 93% 預算使 (a) 組成為下次增修的前置
who:
  current: general-purpose
  history: {}
what: 重新設計 W3-022 因實例不足暫緩的兩組判準：(a) 超標時該外移什麼與 SKILL.md 該留什麼互相矛盾；(g)「目標可能移動即失效」要求對未來下斷言
when: 累積到足夠實例後，或 SKILL.md 下一次需要增修內容時（以先到者為準）
where:
  layer: 待定義
  files:
  - .claude/skills/skill-design-guide/SKILL.md::write
  - .claude/skills/skill-design-guide/references/splitting-an-existing-skill.md::write
why: W3-022 派發時 PM 判定 (a)(g) 實例不足暫緩，該暫緩需要一個 trigger 才不會變成無限期延後（decision-trigger-binding 規則 1）。兩組的內容：(a) SKILL.md〈Progressive Disclosure〉的「超標時外移一次只用其中一段的內容⋯⋯SKILL.md 留路由與判準」對同一節給相反答案——判準本身就是「一次只用其中一段」的東西，兩句話會把同一節同時判成該外移與該留下。(g) splitting-an-existing-skill.md〈拆分特有的高頻缺陷〉的「目標可能移動即失效」要求撰寫者對未來下斷言，而同段自陳的實證是四處同型缺陷中三次由修法自身引入、當時也判過不會動——判準要求的預測正是實證顯示做不到的那一種。附帶一項量測：W3-022 改寫後 SKILL.md 由約 3,522 tokens 升至約 4,670，為 5k 預算的 93%，下一次增修前必須先外移，而該外移什麼正是 (a) 組要定的事，兩者綁在一起。
how:
  task_type: Analysis
  strategy: (a) 對「該外移什麼」與「該留什麼」各找出至少三個現存的實際判例，看兩句能否收斂為單一可外部驗證的判準；(g) 蒐集位置性指涉在後續修法中是否真的移動的實例，把「可能移動」換成可事後查證的條件
acceptance:
- '[ ] (a) 產出一個可外部驗證的替代判準，並示範它對本 skill 至少三節判得出同一個答案;(g) 產出一個以事後可查證條件表述的替代判準，取代對未來下斷言;改寫後對全 skill 重跑兩兩比對，確認未引入新的相反處置;SKILL.md 分段估算回到 5k 預算的 80% 以下，或明確記錄為何不需外移'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-07T11:12:33'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

重新設計 W3-022 因實例不足暫緩的兩組判準：(a) 超標時該外移什麼與 SKILL.md 該留什麼互相矛盾；(g)「目標可能移動即失效」要求對未來下斷言

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-07 11:12] SKILL.md 現佔第 2 層預算 93%（4,670/5k、203 行），而 W3-024 正等著為六項缺口補內容——本票的外移決定是它的前置。where.files 已補 ::write（ANA 型預設唯讀，不補則 ticket track commit 會擋）。

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-07 11:23)
  - what: 把 SKILL.md 的〈Degrees of Freedom〉〈Opinionated Defaults〉〈三類 bundled resource 的分工〉三節（合計 1,145 tokens）外移至 references/patterns-and-troubleshooting.md 與 references/creating-and-adopting-skills.md，SKILL.md 各留一列路由
  - why: W3-030 定案的 (a) 判準（splitting-an-existing-skill.md〈外移什麼、留什麼〉四問）對這三節判為問 3「外移到那幾列的目標檔」，但兩個目標檔不在 W3-030 的 where.files 內，無法在本票內落地。SKILL.md 現為 4,566 tok（91%），三節外移後為 3,421 tok（68%）；僅外移〈Degrees of Freedom〉一節即達 3,844 tok（77%），已滿足 80% 門檻。W3-024 待補六項缺口需要這份餘裕
  - suggested_type: IMP
  - suggested_priority: P2
  - related_files: 
  - context: 
  - status: pending


## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-022 | chars: 953 -->

### Task Reference
- 0.1.0-W3-022 what: 重新設計 skill-design-guide 中互相矛盾、無法歸類、或要求做不到的預測的七組判準（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-022 why: Round 2 的七類 finding 中，類 3（判準兩側不互斥或無法歸類，8 則）、類 4（門檻與計數的適用對象未定義，12 則）、類 6（判準要求做不到的預測，1 則）不能靠改字串解決——修法本身是重新設計判準，改完需再審一輪確認沒引入新矛盾。七組：(a) 超標時「該外移什麼」與「SKILL.md 該留什麼」對同一節給相反答案；(b)「引用只一層深」與檔頭「同目錄」互指互相矛盾，規則 3 處對違反 6 處，本 skill 六份 reference 全違反自己的規則；(c)「Claude 真的不知道這個嗎」把「知不知道」與「反不反直覺」兩個獨立維度寫成一條線的兩端；(d) 自由度判準的「壞掉」未定義，且代價段把「產物形態不一致」也算成壞，若成立三級表塌成一格；(e) 三層表逐字寫 references「無上限」而實務逐檔判，門檻適用對象未定義，此為其餘門檻問題的先決條件；(f) description 三個口徑互不換算；(g)「目標可能移動即失效」要求對未來下斷言，而同段實證自陳四處同型缺陷中三次由修法自身引入，當時也判過不會動。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/skill-design-guide/SKILL.md  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- .claude/skills/skill-design-guide/references/writing-the-body.md  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- .claude/skills/skill-design-guide/references/splitting-an-existing-skill.md  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- .claude/skills/skill-design-guide/references/frontmatter-and-description.md  # from 0.1.0-W3-022（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 五組判準（b/c/d/e/f）各產出一個可外部驗證的替代判準；(a)(g) 兩組因實例不足移交 0.1.0-W3-030  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- [ref] [x] 每組附一個本 skill 自身的邊界情境，示範新判準判得出來而舊判準判不出來  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- [ref] [x] 改寫後對全 skill 重跑一次兩兩比對，確認未引入新的相反處置  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
- [ref] [x] 產出分析報告，含具體改進建議與 IMP spawn 規劃  # from 0.1.0-W3-022（來源已完成，結論見 Solution）
