---
id: 0.1.0-W3-298
title: 測試 新條文在 issue 54 / 55 / 56 上的行為，產出條文缺口清單
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-293
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: '用戶裁示先用幾個 issue 測試；選定標的含 #54 / #55 / #56'
  final_decision: 三個 issue 同票，因其共同構成「觀測累積期」這一個測試條件
  rationale: 這三者今天都剛收到觀測且尚未統整，是新條文的正向使用時機；分成三票會使「累積期的共同行為」無法對照
who:
  current: framework-issue-curator
  history: {}
what: 測試 新條文在 issue 54 / 55 / 56 上的行為，產出條文缺口清單
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/references/escalation-flow.md
why: '**這三個 issue 處在觀測累積期而非統整期**，與 `#97` 正好互補：`#97` 測「統整時條文有沒有用」，這一組測「**累積時條文能不能擋住修法被提前寫死**」。


  今天各自剛收到觀測——`#56` 與 `#55` 來自 `0.1.0-W3-286`（條文面與 CLI 實作面分開落點），`#54` 來自平行 session（登記表第二個具名缺項）。**它們是新條文的正向使用時機：此刻若有人建一張實作票，條文應該攔下。**


  **`#54` 另帶一個條文可能處理不了的形態**：平行 session 指出缺口分「已漂移」與「未漂移但未登記」兩類，若修復方式是掃出當前不一致再補登記，**第二類全數抓不到**（它們今天完全正確）。三問裡的「範圍已量」對這種「量得到的部分不是全部」的情形有沒有說法，這裡會暴露。


  **`#55` 帶一個自指條件**：`0.1.0-W3-286` 實測 `ticket track commit` 對 ANA 型票拒絕提交票面以外路徑，把執行者推回裸 commit 路徑——那正是 `#55` 自己記錄的缺口。**條文要求的產出方式，可能被該 issue 描述的缺口所阻擋。**'
how:
  task_type: Analysis
  strategy: '**這是條文的測試，不是 issue 的修復。產出是條文的缺口清單，不是修法。**


    **先讀條文**：`.claude/skills/framework-issue/references/escalation-flow.md`〈修法時點分離：發現票何時能寫定修法〉（`0.1.0-W3-293` 於 `51662bb2` 落地）。連同 `SKILL.md` 的路由與 `quality-baseline.md` 規則 5 的接縫註記一併讀。


    **執行方式**：對指定的每一個 issue，實際走一次現行流程（讀 body 與全部 comments、跑 `dedup`、套三問、套兩檔擴散面處置表），**逐步記錄條文在哪一步給不出答案、給了模稜兩可的答案、或給了你認為錯誤的答案**。


    **測試期間禁止寫入 canonical issue**：不執行 `init`／`add`／`update`／`observe`，不改任何 `.claude/` 資產，不跑 `sync-push`。**改為在 Solution 記錄「依條文我會貼什麼、貼到哪個區段、為什麼」**——那份記錄本身就是受測產出。唯讀操作（`dedup`／`check`／`show`／讀 comments）不受此限。


    **要記錄的三類發現，缺一不可**：


    1. **條文失效點**：具體指出條文的哪一句在哪個 issue 的哪一步失效，附該步驟的實際輸入。禁止「條文大致可用」這種無法被推翻的結論。

    2. **條文正確攔下的**：哪些地方條文確實給出了與你直覺不同、而且更對的答案。**沒有這一類就無法分辨條文有效還是條文無效**——全是缺口的報告與全是肯定的報告同樣不可信。

    3. **skill 引導層的缺口**：`SKILL.md` 的路由是否把你導到對的地方、`escalation-flow.md` 的章節順序是否符合實際執行順序、是否有該讀而沒被指到的檔案。


    **不做的事**：不修條文（統整票承接）、不建修復票、不對 issue 做任何寫入。發現屬本專案的問題則依規則 5 建票，但**票面不得寫定修法**——這本身是條文的自我適用測試。'
acceptance:
- '[ ] 三個 issue 各自的條文失效點已分開記錄，未合併為單一結論'
- '[ ] 條文正確攔下的項目已記錄，非只列缺口'
- '[ ] SKILL.md 路由與 escalation-flow 章節順序的引導缺口已記錄'
- '[ ] 「依條文我會貼什麼、貼到哪個區段、為什麼」已寫入 Solution，且全程未對 canonical issue 寫入'
- '[ ] 三問的「範圍已量」對 #54 的「未漂移但未登記」類，是否給得出答案，已判定並附依據'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T12:36:33'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

測試 新條文在 issue 54 / 55 / 56 上的行為，產出條文缺口清單

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

受測對象是 `51662bb2` 落地的 `.claude/skills/framework-issue/references/escalation-flow.md`〈修法時點分離：發現票何時能寫定修法〉，連同 `SKILL.md` 的一行路由與 `quality-baseline.md` 規則 5 的接縫註記。本票不修條文、不修 issue，只在三個處於觀測累積期的 canonical issue（`#54` / `#55` / `#56`）上實跑一次現行流程，量條文在哪一步給不出答案。

直接原因：條文的三問（消費端已觀測／反例已試／範圍已量）與兩檔擴散面處置表，是在「發現者建票當下」這一個時點寫的，未涵蓋它自己指定的下游動作——「修法形狀移至 framework issue 區段決定」——由誰承擔、用什麼載體、決定完成後如何回到票。三個受測 issue 今天各自剛收到一則觀測，正是這個下游動作的實際發生時機。

### 影響範圍

- `.claude/skills/framework-issue/references/escalation-flow.md`（受測條文本體與章節順序）
- `.claude/skills/framework-issue/SKILL.md`（路由表一列）
- `.claude/skills/framework-issue/references/ticket-intake.md`（curator 路由的目的地，與新條文零交集）
- `.claude/rules/core/decision-trigger-binding.md`（新條文在銜接段聲稱不觸及，實測觸及）

已驗證的事實：條文全文與三個 issue 的 body、區段索引、全部 comment（`#54` 13 則、`#55` 23 則、`#56` 10 則）、三則今日觀測全文、三次 `check` 輸出、三組 `dedup` 輸出、`escalation-flow.md` 的章節順序與交叉引用計數（`grep -c`）。

仍未驗證的假設：本票只在三個 issue 上實跑，未涵蓋「發現當下建票」這一側（`#97` 一組與並行的 `0.1.0-W3-297`／`0.1.0-W3-299` 覆蓋其他 issue）。本票結論的射程限於「累積期」。

### 相關 Error Pattern

`PC-093`（無 trigger 延後）與 `.claude/rules/core/decision-trigger-binding.md` 規則 2 直接相關——條文〈現況〉段的延後寫法命中該規則，詳見 Solution 缺口 G4。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-293 | chars: 888 -->

### Task Reference
- 0.1.0-W3-293 what: 落條文 framework-issue 升級流程的時點分離判準，與品質基線規則 5 的接縫註記（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-293 why: **用戶裁示（2026-09-11）**：先調整 framework-issue skill 的說明與引導、以及相關 rule 的規範，再回頭評估目前是否已可開始實作、或仍須回頭檢查 issue 內容再做分析。

**條文內容已由 `0.1.0-W3-289` 產出，本票只做落地**，不重新設計。該票的機械量測：443 張票中 275 張為發現衍生，其中 108 張 IMP，其中 74 張來源票非 ANA；這 74 張建票當下 `how.strategy` 全部非空，僅 12 張（16%）帶待驗證／停手語彙——**62/74（84%）在發現當下寫定了一個沒有任何欄位記載其驗證狀態的修法**。

**289 原本的定位是「未定案，供討論」**，理由寫在該票：條文形狀是它自己的修法，其消費端（其他 consumer 的建票實務）未經觀測，故依自身判準不寫成結論。用戶裁示先落地，此裁示即為本票的依據；**`#81` 的區段討論不因此關閉**，後續若該處給出不同形狀，以其為準。

**不做則後果**：判準只存在於一張已完成的票與一則 issue 觀測裡。下一個 session 建發現票時不會讀到它，84% 的比例不會改變。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/framework-issue/SKILL.md  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/CHANGELOG.md  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- .claude/rules/core/quality-baseline.md  # from 0.1.0-W3-293（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三問與兩檔擴散面處置表已落於 escalation-flow.md，判準內容與 0.1.0-W3-289 一致  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- [ref] [x] 三個檢驗案例已收錄，且能分辨豁免與不豁免（非全面禁令）  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- [ref] [x] SKILL.md 有一行路由指向該節，未複製判準內容  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- [ref] [x] quality-baseline 規則 5 的接縫註記不超過一行且含路由，未在自動載入層展開三明示  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
- [ref] [x] 單位是欄位不是票、豁免軸是證據狀態不是規模，兩項邊界已明寫  # from 0.1.0-W3-293（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
