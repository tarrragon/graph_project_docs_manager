---
id: 0.1.0-W3-417
title: 實證 範例審查探針閘門第一輪（W2 開工閘門）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-418
- 0.1.0-W3-419
- 0.1.0-W3-420
- 0.1.0-W3-421
- 0.1.0-W3-422
- 0.1.0-W3-423
- 0.1.0-W3-424
- 0.1.0-W3-425
- 0.1.0-W3-426
- 0.1.0-W3-433
- 0.1.0-W3-435
- 0.1.0-W3-436
- 0.1.0-W3-437
- 0.1.0-W3-439
- 0.1.0-W3-446
- 0.1.0-W3-447
- 0.1.0-W3-448
- 0.1.0-W3-449
- 0.1.0-W3-450
- 0.1.0-W3-451
- 0.1.0-W3-452
- 0.1.0-W3-454
- 0.1.0-W3-457
- 0.1.0-W3-459
- 0.1.0-W3-460
- 0.1.0-W3-461
- 0.1.0-W3-463
- 0.1.0-W3-465
- 0.1.0-W3-466
- 0.1.0-W3-468
- 0.1.0-W3-469
- 0.1.0-W3-472
- 0.1.0-W3-473
- 0.1.0-W3-474
- 0.1.0-W3-475
- 0.1.0-W3-477
source_ticket: 0.1.0-W3-410
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 審查方式 WRAP 與用戶裁示
  final_decision: 建探針閘門實證票
  rationale: 取代逐字可溯性閘門
who:
  current: PM 派發出題者、探針與判定者
  history: {}
what: 依用戶裁示以讀者探針為範例審查閘門跑第一輪實證：涵蓋本輪補過範例的 skill（component-contract-design、ux-design-evaluation、foundation-design、test-assertion-design、tdd、doc、version-bootstrap、dart-style-guardian、skill-design-guide），每則含判定的範例出題並含判準相反分支的對照版，題目凍結後各 3 探針作答，判定者先從條文寫出正確答案與依據句再比對；門檻 3/3；判定者同時標出依據只在範例內的判準
when: 立即
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-336.md
why: 用戶裁示：W2 開工條件為探針 3/3 通過且全部範例零無依據句；chained-examples 待本輪實證後再改
how:
  task_type: Analysis
  strategy: 先派出題者產出凍結題組並經 PM 核對覆蓋，再派探針，最後派判定者；未通過與無依據句逐項建修補票
acceptance:
- '[ ] 凍結題組覆蓋每則含判定範例且含相反分支'
- '[ ] 逐題判定表（3/3 生效或未生效）與依據只在範例內清單寫入 Solution'
- '[ ] 未通過與無依據句逐項建修補票或記錄豁免理由'
- '[ ] W2 開工閘門達成與否的結論'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-15T13:09:11'
completed_at: null
created: '2026-09-15'
updated: '2026-09-15'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

依用戶裁示以讀者探針為範例審查閘門跑第一輪實證：涵蓋本輪補過範例的 skill（component-contract-design、ux-design-evaluation、foundation-design、test-assertion-design、tdd、doc、version-bootstrap、dart-style-guardian、skill-design-guide），每則含判定的範例出題並含判準相反分支的對照版，題目凍結後各 3 探針作答，判定者先從條文寫出正確答案與依據句再比對；門檻 3/3；判定者同時標出依據只在範例內的判準

---

## Problem Analysis
### 出題者盤點（2026-09-15，出題前）

**方法**：以 `grep '【輸入】'` 列出九個 skill 的三段鏈式範例位置（排除 CHANGELOG 與格式說明句），分五組並行讀範例全文與所屬章節，寫出每則教的判定，再在範例三段、程式碼圍欄、逐問檢查表以外的條文以 `grep -F` 找依據句（chained-examples〈驗證段機械檢查〉第 2 點）。一則範例教多個判定時拆列（編號加 a/b/c）。出題者只標記，不評判。

**規模**

| skill | 標記格式範例數 | 判定列數 | 條文查無列 | 部分列 |
|---|---|---|---|---|
| component-contract-design | 7（另 5 則非標記格式） | 12 | 1（A-11） | 3 |
| dart-style-guardian | 4 | 4 | 0 | 1 |
| skill-design-guide | 5 | 6 | 1（A-17b） | 1 |
| version-bootstrap | 8 | 8 | 0 | 4 |
| ux-design-evaluation | 29 | 33 | 3 | 11 |
| foundation-design | 19 | 40 | 5 | 13 |
| tdd | 18 | 22 | 6 | 3 |
| test-assertion-design | 6 | 7 | 1 | 0 |
| doc | 24 | 27 | 3 | 7 |
| 合計 | 120（另 5 則非標記） | 159 | 20 | 43 |

依步驟 2「每則含判定範例 1 題 + 相反分支對照 1 題」，標記格式範例即需 240 題，超過步驟 4 門檻 60。**依步驟 4 停在盤點，未出題**，分批建議見文末。

### 盤點表

#### A 組：component-contract-design／dart-style-guardian／skill-design-guide／version-bootstrap

| 編號 | skill | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|---|
| A-01 | ccd | step-1-pick-starting-point.md〈情況二：A 與 C 之間猶豫〉 | C 三訊號都無法核對時判「待決」不判 A；規格提及既有檔只算請求核對理由；走查暫判 A 須加標記 | step-1〈情況二〉：「「無法核對」不等於「全否」，不得判 A」 |
| A-02 | ccd | step-2-path-a-spec-derivation.md〈反向規則〉畫面甲／浮層乙狀態表 | 本列未提控制項不渲染（不以停用渲染）；他列有提的退出動作標待決建追修票；頁面框架不受限 | step-2-path-a〈反向規則〉：「控制項不渲染，不以停用狀態渲染」「不得逕判存在或不存在」 |
| A-03 | ccd | step-3-fill-contract.md〈關卡五〉矩陣格點選顯示詳情卡 | 事件不經服務時失敗／逾時／服務不可用用固定寫法，不寫「同成功」；按需讀取時不適用 | 部分：skill 內只有固定寫法；禁止句與不適用句在方法論 component-library-bidirectional-constraint-methodology〈失敗路徑覆蓋〉 |
| A-04 | ccd | step-5-write-tickets.md〈格位起算〉 | 已具名 slot 寫 slot 名；未具名依書寫方向起算；容器無 slot 契約不得在畫面票自訂編號 | step-5〈格位起算〉：「寫 slot 名，不另行編號」「先補該容器的 slot 契約」 |
| A-05 | ccd | step-5〈覆蓋層〉篩選下拉 | 元件自帶覆蓋層不列；呼叫端觸發者寫狀態列末子句不佔格位；定位寫在元件契約 | step-5〈覆蓋層〉：「不寫進格位句」「畫面票不述」 |
| A-06 | ccd | step-5〈疊加態〉 | 疊加態獨立一列並標疊加底層，列內只寫增量；底層列不動 | step-5〈疊加態〉：「寫成獨立一列，列首標明」「列內只寫增量」 |
| A-07 | ccd | step-5〈畫面票〉非狀態進入情境 | 不在狀態矩陣的進入情境不新增 how 列，拆到既有列、「被程式定位」格與覆蓋層子句 | 部分：缺「非狀態情境不新增 how 列／拆解方式」 |
| A-08 | ccd | step-2-path-a 元件候選判定（非標記格式） | 列集合相同合為一元件；真子集為可選 slot；互有差集各為候選；單畫面共現為容器子件 | 依據句位於程式碼圍欄內（第 2 點判否，待 PM 裁定） |
| A-09 | ccd | step-2-path-a 容器盤點（非標記格式） | 同一佈局結構下全部子件算一個容器候選 | 依據句位於程式碼圍欄內（同上） |
| A-10 | ccd | step-3〈關卡二〉載入指示消失（非標記格式） | 疊加指示消失、內容未變時第 2 問答否；內容改變時答是 | step-3〈關卡二〉：「第 2 問的「可見的狀態轉換」不含進行中指示本身的出現與消失」 |
| A-11 | ccd | step-5〈畫面票〉巢狀容器（非標記格式） | how 逐層展開到葉元件；資料驅動格數標「數量依資料筆數」 | **條文查無** |
| A-12 | ccd | step-5〈遷移票〉巢狀 feature 目錄（非標記格式） | 以葉目錄為拆票單位 | 部分：缺「葉目錄」定義 |
| A-13 | dsg | color-system.md〈顏色歸類判準〉刪除按鈕／已刪除提示 | 依元件本身功能類型分類，不因前置操作破壞性改判 | color-system〈顏色歸類判準〉：「分類依據是元件本身的功能類型」 |
| A-14 | dsg | color-system.md〈Theme 與 Token 判準〉 | 內建主題插槽已覆蓋語意用插槽，否則用 token | color-system〈Theme 與 Token 判準〉：「對應的內建語意插槽存在時用主題插槽，不存在時用 token」 |
| A-15 | dsg | typography-system.md〈字級判準：headline 與 title 同值時如何選〉 | 頁面子節標題用 headline4，卡片／對話框標題用 titleLarge；同值不可互換 | 部分：缺「容器內標題 → titleLarge」對映句（定義註解在程式碼圍欄內） |
| A-16 | dsg | i18n-guidelines.md〈判準：標籤＋識別符組合時如何分割〉 | 只有數值豁免，標籤走參數化翻譯 | i18n-guidelines〈Exceptions〉：「僅數值本身列入本豁免；標籤文字仍是使用者可見文字」 |
| A-17a | sdg | chained-examples.md〈【驗證】段〉內嵌範例 | 候選判定表屬靜態產出物，驗證段用檢驗問句 | chained-examples〈【驗證】段〉判定規則表設計產出物列 |
| A-17b | sdg | 同上 | 退出路徑層級不同不合併；層級同目的地不同為 slot | **條文查無**（skill 內；跨 skill 見 ccd step-2-extract-components〈共用規則一〉） |
| A-18 | sdg | chained-examples〈驗證段機械檢查〉正例 | 省略輸入段判違規 | chained-examples〈三段鏈式格式〉：「三段皆不可省略」 |
| A-19 | sdg | chained-examples〈驗證段機械檢查〉反例 | 慣例描述引文、出處在範例自身屬橋接判否，缺引導 | chained-examples〈驗證段機械檢查〉第 2、3a 點與處置表 |
| A-20 | sdg | chained-examples〈完整示範〉 | 執行期行為產出用 Given/When/Then（以格式示範為主） | chained-examples〈【驗證】段〉判定規則表執行期行為列 |
| A-21 | sdg | splitting-an-existing-skill.md〈補範例後超標〉 | 超標成因為新補範例時不刪範例不壓規則；互斥路徑各自外移，入口留判斷條件與路由 | 部分：缺「互斥路徑各自成檔」 |
| A-22 | vb | version-shift-sop.md〈判斷準則〉範例一 | 三問任一為是即契約檔案，與檔案類型無關 | version-shift-sop〈契約檔案的界定〉：「三問任一為「是」即屬契約檔案」「檔案類型不決定歸屬」 |
| A-23 | vb | version-shift-sop.md 範例二 | 同儲存庫各自部署時判準 2 命中；同次提交同次部署時不成立 | version-shift-sop〈契約檔案的界定〉判準 2：「判定單位是發布或部署單位，不是儲存庫」 |
| A-24 | vb | v02-walkthrough.md〈範例一：UI 類提案判別〉 | 依呈現通道判定：彈窗 UI 類、終端文字輸出非 UI 類 | SKILL.md Step 2〈UI 類判別〉呈現通道表 |
| A-25 | vb | v02-walkthrough〈範例二：三項前置檢查的「存在」判準〉 | 檔案存在不等於齊備，以對應載體內容判定 | 部分：缺「檔案存在不等於內容齊備」 |
| A-26 | vb | v02-walkthrough〈範例三：地基波依賴編排〉 | i18n／design-system 並行；UX 審查依賴兩者；元件契約依賴 UX；元件庫依賴前三塊加契約 | 部分：缺「UX 審查依賴 i18n／design-system」 |
| A-27 | vb | v02-walkthrough〈範例四：兩軸測試交集去重〉 | 與 UC 同斷言的不變式不另建測試；未涵蓋者各建 | SKILL.md Step 5：「兩軸交集去重、聯集為完整覆蓋」 |
| A-28 | vb | v02-walkthrough〈範例五：GREEN 票粒度〉 | 共用狀態且未超職責閾值按模組合併；「或」由閾值決定 | 部分：缺「共用狀態 → 合併」「「或」由閾值決定」 |
| A-29 | vb | v02-walkthrough〈範例六：並行安全〉 | 共用匯出檔只由整合票修改 | 部分：缺「各票不得直接修改」 |

（ccd＝component-contract-design、dsg＝dart-style-guardian、sdg＝skill-design-guide、vb＝version-bootstrap）

#### B 組：ux-design-evaluation

| 編號 | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|
| B-01 | SKILL.md〈與相鄰資產的交界〉p90 600ms | 產出須為可引用具體值：600ms 填按鈕 loading | SKILL.md：「不能只寫「要有回饋」」；waiting-and-notification〈時間門檻〉400ms-1s 列 |
| B-02 | input-mechanism.md〈四個決策維度〉無頻率資料 | 依是否在核心操作必經路徑排序 | input-mechanism〈四個決策維度〉：「以完成核心操作的臨界路徑上是否必經此鍵作為排序依據」 |
| B-03a | input-mechanism〈四個決策維度〉CLI 輸入 | CLI：隱碼型鍵盤、整行送出、IME 全關、底部工具列 | 部分：缺「整行送出」依據 |
| B-03b | 同上 驗證 Q1 | 決策表值寫中性描述，不寫框架列舉名 | **條文查無** |
| B-04a | input-mechanism〈場景一：表單〉Keyboard type | 格式不固定用一般文字鍵盤 | 〈場景一〉：「格式不固定…則維持一般文字鍵盤」 |
| B-04b | 同上 驗證 Q1 | 決策欄寫中性描述，框架對應放旁註 | **條文查無** |
| B-05 | 〈場景一〉驗證時機規則交界 | 本地可算屬格式驗證 on blur；需查伺服器屬業務驗證 on submit | 部分：缺「能否本地算出」分類判準 |
| B-06 | 〈場景二：搜尋〉搜尋成本 | 依成本不依是否本地；成本高採送出或 debounce | 〈場景二〉表列：「搜尋成本高：DB 全文搜尋」 |
| B-07 | 〈IME 安全清單〉confidential | 日記 confidential 非 secret：關學習、保留自動校正 | 部分：缺「自然語言時保留開啟」方向句 |
| B-08 | interaction-feedback-component-semantics.md〈六項判準〉第 1 項 | 三值、不增減元素、可並排 → segmented control | 同檔第 1 項：「能 → 改用 segmented control」 |
| B-09 | 同檔第 2 項 | 圓角淺底文字 chip 屬非按鈕形態，邊框非判準 | 同檔第 2 項：「指示用明顯非按鈕形態（文字 chip、圓點）」 |
| B-10 | 同檔第 5 項 | 關鍵計數不得省略號；先縮格式再設最小寬度 | 同檔第 5 項：「先縮短格式…仍不足才設定最小寬度」 |
| B-11 | waiting-and-notification〈時間門檻〉p90／p95 | 兩者落不同帶時須記錄選用統計量 | 同檔：「須顯式記錄選用的統計量（p90 或 p95）」 |
| B-12a | 同檔〈畫面級回饋〉 | 本節只填 UI 呈現欄 | interaction-feedback.md 檔頭：「狀態的枚舉與退出路徑歸狀態矩陣」 |
| B-12b | 同上 驗證 Q2 | error 與 disconnected 皆須重連與返回 | 部分：缺「disconnected 屬 error 類」 |
| B-13 | interaction-feedback.md〈按鈕級回饋〉timeout | 取值略大於後端逾時，緩衝無固定公式 | 部分：缺「緩衝量由誰依什麼決定」 |
| B-14a | screen-state-matrix.md〈填寫範例〉遠端終端機 | 無 enum 時由視覺差異反推狀態 | 〈填寫步驟〉1：「每種不同的視覺呈現就是一個狀態」 |
| B-14b | 同上 | 退出路徑空格即死胡同 | 〈填寫步驟〉3：「空格 = 死胡同」 |
| B-14c | 同上 驗證 Q3 | 可用操作中離開本狀態者須同名出現在退出路徑欄 | **條文查無** |
| B-15 | screen-state-matrix〈容易漏列的狀態：initializing〉 | 常駐服務有獨立生命週期需 initializing；單次請求不算 | 部分：缺「單次請求不算獨立生命週期」 |
| B-16 | screen-state-matrix〈反模式〉反向操作 | 可逆性由中斷連線提供，導覽返回不算 | 部分：缺「導覽返回不算反向操作」 |
| B-17 | screen-state-matrix-selection-prestate.md〈三段鏈式範例〉 | 三條件成立時各畫面「未選定」獨立成列、四欄填齊、記錄設定入口與清除時機 | 同檔：「三項皆成立即屬「選取類前置呈現」，須獨立成列」 |
| B-18 | error-recovery.md〈錯誤訊息的兩個職責〉 | 逾時屬網路層：檢查網路後重試 | 同檔：「不可只給「重試」，也不升級為執行環境層行動」 |
| B-19a | error-recovery〈重試機制〉 | 前景連線瞬斷採固定間隔 | 同檔：「用固定間隔並設上限」 |
| B-19b | 同上 驗證 | 重試中顯示次數、下次時間，並提供立即重試與返回 | 部分：缺「固定間隔場景也要立即重試鈕」 |
| B-20 | error-recovery〈Degraded mode〉未用過功能 | 判準是是否用過，非上線多久 | 同檔：「只適合使用者從未用過的功能」 |
| B-21 | error-recovery〈Degraded mode〉防抖 N | N 以單次逾時為下界 | 〈進入與退出〉：「N 以該功能單次請求的逾時值為下界」 |
| B-22 | navigation-patterns.md〈go vs push vs pushReplacement〉tab | tab 間切換不屬三種堆疊操作 | 部分：缺「tab 間切換不屬三種堆疊操作」 |
| B-23 | navigation-patterns〈常見誤用〉 | 一次性配對 push、登入後 go、送出後 pushReplacement | 同檔語意表三情境列 |
| B-24 | navigation-patterns〈iOS HIG vs Material〉 | top tabs 屬視覺風格差異可不適配 | 部分：缺「未列舉元件如何歸類」 |
| B-25 | navigation-patterns〈Deep link〉 | 有既有入口重建堆疊；進入方式不影響 | 〈Deep link〉：「目標畫面在 app 內是否有既有入口可重複到達」 |
| B-26 | gate-fallback.md〈每道 gate 的必答問題〉 | 失敗欄依 fallback 是否相同分列 | 同檔：「fallback 相同的類型合併為一列」 |
| B-27 | gate-fallback〈網路 gate〉 | 第一問否第二問是 → Degraded mode 並標示非最新 | 〈網路 gate〉判別順序句 |
| B-28 | gate-fallback〈權限 gate〉 | 核心權限首次開啟請求；次要延到使用時 | 〈權限 gate〉：「首次開啟請求只適合權限少（1-2 個）且與核心功能直接相關」 |
| B-29 | gate-fallback〈破壞性操作確認 gate〉 | 名實落差大的「匯入」要確認並寫具體後果；誠實刪除也要確認 | 部分：缺「落差小的不可逆操作是否仍須確認」 |

#### C 組：foundation-design

| 編號 | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|
| C-01a | examples.md〈問作用不問存在〉 | 只讀標題檔名不能判作用，須搜內文 | SKILL.md〈判準的通用形式〉：「文件存在不代表它回答得了問題」 |
| C-01b | 同上 | 錯誤承接宣告比錯誤缺口宣告更難被發現 | **條文查無** |
| C-02a | examples.md〈萃取揭露規格漂移〉 | 規格散文與畫布實測衝突以畫布為準 | 部分：handoff-mode 原句講程式碼；缺畫布優先 |
| C-02b | 同上 | 規格改寫後指向 token 不複述數值 | 部分：缺「規格不得複述 token 數值」 |
| C-03a | examples.md〈萃取不等於照抄〉 | 值來自設計畫布時萃取當下歸納為離散階 | handoff-mode〈命名與收斂分兩步〉：「萃取當下即歸納為離散具名階」 |
| C-03b | 同上 | 值已被程式碼引用時逐值命名、收斂另開票 | handoff-mode〈命名與收斂分兩步〉：「第 2 步仍逐值命名、值不變」 |
| C-04a | examples.md〈權威形態不符時的正確取用〉 | 部分段落不適用不等於整維度不適用 | SKILL.md〈權威缺席時〉形態 2：「來源是 SaaS skill 不構成整條跳過的理由」 |
| C-04b | 同上 | 改寫產物須對應現況真缺口 | 部分：缺「須對應現況缺口」 |
| C-05a | examples.md〈產物有而判準無〉 | 判準層缺口不推翻產物層已存在；另標無判準建缺口票 | SKILL.md〈權威缺席時〉：「『產物有、判準無』是常見狀態」 |
| C-05b | 同上 | 兩層合併填「待定：缺口」是錯的 | 部分：缺合併填寫的後果 |
| C-06a | examples.md〈UI 維度適用條件不限時的處置〉 | 純 CLI 以無 GUI 框架為由不構成失效不存在 | dimension-product-notes〈判定「失效不存在」的理由〉：「沒有圖形介面框架）不構成失效不存在」 |
| C-06b | 同上 | UI 維度走改寫產物：輸出格式契約 | 部分：缺「語意在純文字輸出對應欄位順序、色碼語意」 |
| C-07a | examples.md〈產物欄「無」與「待定」的拆兩問判斷〉 | 無／待定區分軸是是否已判完 | SKILL.md〈產物欄的五種合法答案〉：「「無」的語意是判完了、結案」 |
| C-07b | 同上 | 填無須附理由與重評條件 | SKILL.md 同表「無」列 |
| C-07c | 同上 | 無的理由須指出權威原句對象不存在 | dimension-product-notes：「須指出權威原句所講的對象在本專案不存在」 |
| C-07d | 同上 | 產物層判無時不需再查判準層 | **條文查無** |
| C-08a | examples.md〈權威缺席的查證對象〉 | 權威是否存在查 session 端 | SKILL.md〈權威缺席時〉：「看的是執行本流程的 session 的 `.claude/`」 |
| C-08b | 同上 | 被盤點 repo 無框架目錄不構成權威缺席 | SKILL.md 形態 1：「它沒有框架是常態，不構成權威缺席」 |
| C-08c | 同上 | 產物落在被盤點 repo | SKILL.md：「產物的載體…看的是被盤點的 repo」 |
| C-09a | examples-process.md〈文件回答不了地基問題〉 | 任一格答不出即命中 | SKILL.md〈與 orchestration 的協作〉：「無法回答維度表任一格的產物」 |
| C-09b | 同上 | 空殼 scaffold 不算相當程度實作 | **條文查無** |
| C-10 | examples-process〈規劃波進行中與之後〉 | 地基票不算該版票已建 | 部分：缺「地基票不計入」 |
| C-11a | examples-process〈產物落為地基票的粒度〉 | 一產物一票 | 部分：缺非 UI 維度通則 |
| C-11b | 同上 | 已存在產物不建票 | SKILL.md〈工作流〉步驟 3：「不建票」 |
| C-12 | examples-process〈執法載體掃描範圍〉 | 部分涵蓋即前置條件第三項不通過 | 部分：缺「部分涵蓋即不通過」 |
| C-13a | examples-process〈降級記錄須為缺項對照步驟表〉 | 須記缺項與不成立步驟 | SKILL.md〈移植前置條件〉：「記下缺哪一項、因此哪幾步在本專案不成立」 |
| C-13b | 同上 | 缺決策文件先建立 | SKILL.md：「連決策文件都沒有時，第一個動作是建立它」 |
| C-13c | 同上 | 降級記錄須為對照表，散文不合格 | **條文查無** |
| C-14a | examples-process〈人工複核「複核者」欄〉 | 不得留白等待指派 | SKILL.md〈工作流〉步驟 5：「不得留白等待指派」 |
| C-14b | 同上 | 權威未訂審查角色退至框架審查代理人；個人不合格 | 部分：缺先後順序與「個人不是既有角色」 |
| C-15 | handoff-mode.md〈萃取的前提是既有 artifact 可信〉 | 作者在職但兩週無回應也算決策者不可得 | **條文查無** |
| C-16a | handoff-mode〈補特徵測試有規模閘門〉 | 閘門對象是抽取範圍覆蓋，非總行數 | 部分：缺明文 |
| C-16b | 同上 | 抽取範圍覆蓋 0% 走替代路徑 | 部分：缺「補不起來」判定條件 |
| C-17a | dimension-product-notes.md〈複合產物與多子樹分列〉 | 三命令任一兩套以上即分列 | 同節：「有沒有兩套以上的測試命令、建置命令或部署目標才重要」 |
| C-17b | 同上 | 分列後各子樹獨立填答 | 同節：「單一格會把落後那棵子樹的缺口吃掉」 |
| C-18a | dimension-product-notes〈可觀測性維度〉 | 無背景執行緒不構成失效不存在 | 〈判定「失效不存在」的理由〉發生途徑句 |
| C-18b | 同上 | 對象系統存在不得填無 | 同節：「對象在本專案存在時不得填「無」」 |
| C-18c | 同上 | 失效長成輸出失敗不可見，走降級顯示契約 | 部分：失效形狀本身條文查無 |
| C-19a | dimension-product-notes〈UI 維度〉 | 目前只有一種輸出樣式不構成失效不存在 | 同節：「失效目前尚未發生（如目前只有一種輸出樣式）不構成失效不存在」 |
| C-19b | 同上 | 語意對應欄位順序／色碼／截斷，走輸出格式契約 | 部分：缺對應判準 |

#### D 組：tdd／test-assertion-design

| 編號 | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|
| D-01 | tdd test-object-catalogue.md〈一、測試對象的單位判定〉 | 一 FR 多宣告時每宣告一行 | 同檔候選單位表：「以每個可獨立驗證的規格宣告為單位」 |
| D-02 | test-object-catalogue〈三、契約欄位表〉反例欄 | 反例欄寫具體誤判情境，不只寫類別名 | 部分：缺「不得只寫類別名」（只見範例引言句） |
| D-03a | 同檔〈三〉斷言來源欄 | 兩欄同條照抄、不同條分填 | **條文查無**（只見範例引言句） |
| D-03b | 同上 | 重疊時斷言來源欄不可留空 | 部分：缺「重疊時仍須填」 |
| D-04a | 同檔〈六、派發語言〉範例一 | When 只寫規格觸發條件不寫機制 | **條文查無**（句型在程式碼圍欄內） |
| D-04b | 同上 | Given 對前一狀態、Then 對後一狀態 | **條文查無** |
| D-05 | 同檔〈六〉範例二 跨邊界 | 離開 process 子步驟歸實機，app 內歸 widget，填「兩者」 | doc-handoff-test-tier-assignment〈跨邊界步驟：平台決定〉 |
| D-06a | 同檔〈六〉範例三 | 出現、內容、可讀性三類驗收不合併 | 同檔〈二、來源類別〉：「是否出現、內容、可讀性三類驗收各對應不同形態」 |
| D-06b | 同上 | Then 只寫可觀察結果不寫框架節點 | **條文查無**（tdd 內；同義判準只在 skill-design-guide） |
| D-07 | test-object-catalogue-starting-points.md〈起點 B〉 | 任一斷言追溯不到即整則標無規格來源並拆兩則 | 同檔 B3：「整則即標記『無規格來源』」 |
| D-08 | 同檔〈起點 C〉 | characterization 以公開進入點為單位 | 同檔 C2：「每個可呼叫的公開進入點一條」 |
| D-09 | doc-handoff-test-tier-assignment.md〈跨邊界步驟〉 | 對話框互動屬平台決定，結果後畫面更新屬自己決定 | 同節：「落在外面才問平台決定」 |
| D-10 | 同檔〈與 UC 資訊鏈的關係〉 | 節點內邏輯歸單元，送出後查得到屬串接歸整合 | 〈判斷原則〉：「串接決定 = 整合測試」 |
| D-11 | bdd-behavior-testing.md〈BDD→單元拆分標準〉 | 多訊號時先拆單元，Given 仍過長才拆 Module | 同節：「移除後 Given 設置仍 > 10 行 Mock，才考慮拆分 Module」 |
| D-12a | doc-handoff.md〈前置閘門〉 | 完整場景步驟須有動作與預期結果 | **條文查無** |
| D-12b | 同上 | 含使用者操作即涉及 UI | **條文查無** |
| D-13 | doc-handoff〈Phase 1 輸入映射〉 | 可觀察轉量化；主觀原文照抄標〔不可量化〕 | 同節映射表：「原文照抄並標『〔不可量化〕』」 |
| D-14 | doc-handoff〈追溯矩陣初始化〉 | entry 以 UC 場景為單位 | 同節步驟 2：「為每個場景建立 entry」 |
| D-15 | doc-handoff-uc-to-gwt-conversion.md〈轉換注意事項範例〉範例一 | Given 承接依賴的前序狀態，未依賴不列 | 〈轉換注意事項〉：「不符合此判準的前序狀態不必列入 Given」 |
| D-16 | 同上 範例二 | 操作者換成系統時 When 標「系統」 | 同節：「When 欄位應標注操作者」 |
| D-17 | 同檔〈轉換範例〉 | 同步驟多結果 When 不同各拆種子 | 〈無 UC 步驟的 FR → GWT 種子〉：「每組 Given／When 各拆為一個種子」 |
| D-18 | 同檔〈GWT 種子與行為單元的分工邊界〉 | 產出只到種子，欄位僅四欄 | 部分：缺「欄位僅限四欄」 |
| D-19 | test-assertion-design SKILL.md〈判讀範例〉聚焦性 | 一宣告一面向；未提屬性不加斷言 | 〈斷言品質三問〉：「一條宣告一個面向」 |
| D-20 | 同〈判讀範例〉判斷軸 | 以識別信號欄判命中；已 await 不命中類型 5；同時命中兩類都處置 | 〈判斷軸〉：「以〈問題類型表〉『識別信號』欄逐一核對是否成立為準」 |
| D-21 | 同〈判讀範例〉類型 9 | 同宣告內條件各一斷言不算過度集中 | 〈斷言品質三問〉：「斷言數依類型 9 隨條件數成比例」 |
| D-22 | 同〈判讀範例〉類型 8 | 無格式監測驗收不保留全結構快照 | 類型表類型 8：「全結構快照只在規格另有…驗收條件時設置」 |
| D-23 | 同〈判讀範例〉類型 6 | 一般隨機替換為可控源；要求不可預測時不替換 | 類型表類型 6 例外句 |
| D-24a | 同〈判讀範例〉元件執行期行為 | 「不顯示清單容器」歸結構驗證，「最多 20 筆」歸邏輯不變式 | 〈確定性斷言的基礎形態〉兩形態定義句 |
| D-24b | 同上 | 不斷言長度為 0、不斷言筆數等於 20 | **條文查無** |

#### E 組：doc（examples/component-library-chained-examples.md；條文＝templates/component-library-spec-template.md 範例以外文字）

| 編號 | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|
| E-01 | 〈§1〉支援的形態 | 外接鍵盤成主要輸入才算獨立形態 | template〈1. 形態因素矩陣〉：「完成主要任務所用的輸入方式改為該裝置時，才構成獨立形態」 |
| E-02 | 〈§1〉每形態各一份粒度 | 第 4 章加欄加列不另立子節；第 5 章容器每形態一份 | template〈1〉：「加欄或加列，不另立平行子節」 |
| E-03 | 〈§1〉尺寸適應策略 | 使用者手動切版合法；依寬度自動切換違規改填維持 | template〈尺寸契約〉(b) 句與「不得選，改填「維持」」 |
| E-04 | 〈§1〉回饋通道子表 | 三選一；通道具體到形式；桌機不豁免播報替代 | template〈回饋通道子表〉：「單填「視覺」等籠統詞不構成」 |
| E-05 | 〈§2〉樂觀更新 | 低風險與可逆 AND，看後果不看位置 | template〈2〉：「「低風險」與「可逆」須同時成立（AND）」 |
| E-06 | 〈§3〉L2／L3 | 依語意判層級，出現畫面數不作判準 | template〈3〉：「層級依語意判定，出現畫面數」「不作判準」 |
| E-07 | 〈§4〉內容角色複合粒度 | 描述同一件事歸同角色；狀態圖示＋標題為兩角色 | 部分：缺「描述狀態的圖示歸標籤」 |
| E-08 | 〈§4〉變體與 disabled | 變灰不可點擊入狀態矩陣，不列變體 | template〈變體〉：「一律入狀態矩陣，不得列為變體」 |
| E-09 | 〈§4〉退出路徑欄格式 | 固定寫「{觸發事件} → {目標狀態}」 | **條文查無** |
| E-10a | 〈§4〉來源欄 前提 A | 存 store 可跨元件讀 → 資料態 | template〈狀態矩陣〉：「值存於元件之外、可被其他元件讀取者為資料態」 |
| E-10b | 同上 前提 B | 只在複合元件內部 → 互動瞬態 | 部分：缺「元件內部不暴露的推導值歸互動瞬態」 |
| E-11 | 〈§4〉同步策略欄 | 三選項適用判準、互斥 | 部分：只有選項名 |
| E-12a | 〈§4〉回饋契約對應狀態邊 | 狀態名逐字取自狀態矩陣 | template〈回饋契約〉：「狀態名須取自上方狀態矩陣的狀態集」 |
| E-12b | 同上 | 一事件多邊逐條列 | **條文查無** |
| E-13 | 〈§4〉等待與結果四段 | 離線與 5xx 歸服務不可用；缺段即未覆蓋 | template〈回饋契約〉服務不可用定義句與「缺一段即視為未覆蓋失敗路徑」 |
| E-14 | 〈§4〉降級不解除回饋義務 | 前提指通道可用性；降級須標示 | 部分：缺「前提＝通道可用性」 |
| E-15 | 〈§4〉三層回饋模型承載層 | 3 秒落第 2 層；只填層名不完整 | 部分：缺時間帶門檻與「只填層名不完整」 |
| E-16 | 〈§4〉操作機制精度 | 兩欄填數值比較；不足配替代機制 | template〈操作機制〉：「須填該形態的具體精度數值」 |
| E-17a | 〈§4〉slot 文字來源強語意 | 強語意判準是文案是否因呼叫情境需不同用字 | **條文查無** |
| E-17b | 同上 | 無插值參數屬無可覆蓋對象 | template〈slot 契約〉：「文案即無可覆蓋對象，非「禁止覆蓋」）」 |
| E-18 | 〈§4〉i18n 表與 slot | 呼叫端傳入 slot 列一列標註來源 | template〈i18n〉：「slot 列一列標註「呼叫端傳入；本元件無自有 key」」 |
| E-19 | 〈§4〉組合規則內距 | 欄內距照填；欄間距歸容器 | template〈組合規則〉：「與鄰件的間距不在此定義，由所在容器的排列不變式承載」 |
| E-20 | 〈§4〉測試點粒度 | 每事件一則；不可用＋替代測替代通道 | template〈測試點〉：「回饋契約每個已定義事件各自一則測試」 |
| E-21 | 〈§4〉實作註記語意不變 | 等值字面量取代 token 屬註記；改數值屬契約變更 | 部分：缺「數值改變一律屬契約值變更」 |
| E-22 | 〈§5〉框架容器安全區 | 頁面根堆疊與殼層加安全區列，對話框不用 | 部分：缺「對話框避讓由尺寸契約承接」 |
| E-23 | 〈§6〉原生元件禁用對照表 | 元件庫內部使用不列；頁面層排列與互動元件列入 | template〈6〉：「元件庫內部實作使用屬正常，不列本表」 |
| E-24 | 〈§7〉結構性無法收斂 | 能包裝達成的不可豁免；理由欄記錄嘗試路徑 | template〈7〉：「理由欄記錄已嘗試的封裝路徑與失敗原因」 |

### 依據只在範例內清單（W2 閘門第二條件，出題者只標記）

**條文查無（20 列）**：A-11、A-17b、B-03b、B-04b、B-14c、C-01b、C-07d、C-09b、C-13c、C-15、D-03a、D-04a、D-04b、D-06b、D-12a、D-12b、D-24b、E-09、E-12b、E-17a

**依據句位於程式碼圍欄內（2 列，待裁定是否算條文）**：A-08、A-09

**部分覆蓋（43 列，缺的片段只在範例內）**：A-03、A-07、A-12、A-15、A-21、A-25、A-26、A-28、A-29；B-03a、B-05、B-07、B-12b、B-13、B-15、B-16、B-19b、B-22、B-24、B-29；C-02a、C-02b、C-04b、C-05b、C-06b、C-10、C-11a、C-12、C-14b、C-16a、C-16b、C-18c、C-19b；D-02、D-03b、D-18；E-07、E-10b、E-11、E-14、E-15、E-21、E-22

### 待 PM 裁定

1. **非標記格式 5 則**（A-08～A-12）是否納入出題範圍。
2. **程式碼圍欄內的條文**（A-08、A-09）與**範例引言句**（D-02、D-03a 的判定只見於【輸入】前「以下示範…」引言）是否算條文。
3. **跨 skill 依據**（A-03 在方法論、A-17b 在 ccd、A-28 在 rules/core/cognitive-load）是否算該 skill 的條文；影響餵入範圍。
4. **出題粒度**：以範例為單位（120 則 → 240 題）或以判定列為單位（159 列 → 318 題）。

### 分批建議（每批 ≤ 60 題，以範例為單位）

| 批 | skill | 範例數 | 題數 |
|---|---|---|---|
| 1 | dart-style-guardian 4 + skill-design-guide 5 + version-bootstrap 8 + component-contract-design 7 | 24 | 48 |
| 2 | ux-design-evaluation | 29 | 58 |
| 3 | foundation-design 19 + test-assertion-design 6 | 25 | 50 |
| 4 | tdd | 18 | 36 |
| 5 | doc | 24 | 48 |

另一種排法：條文查無的 20 列，閘門第二條件本來就不通過，這些範例的探針結果改變不了閘門結論。可以先補條文再出題，把本輪題量降到約 200 題。

### 各檔題數

本輪 0 題，`probes/q/W3-417-*`、`probes/key/W3-417-*` 都還沒建立，原因是觸發了步驟 4 的規模門檻。

### WRAP 認領三問

- W：候選做法有兩個。一是全量一次出題，二是依步驟 4 先盤點再分批，採後者。
- A：全量出 240 題會讓凍結題組的 PM 核對工作量超過一輪能負荷的量。
- P：最可能的失敗原因是五組並行盤點的依據句判定標準不一致，例如引言句、圍欄、跨 skill 依據各組處理方式不同。防護做法是把不一致處列入「待 PM 裁定」。

### 用戶裁示與 PM 定義裁決（2026-09-15，出題前）

**用戶裁示**
- 條文完全查無依據的 20 個判定，和只有部分依據的 43 個判定，共 63 個，全部先補條文，補完才出題。
- 探針分 5 批跑，每批最多 60 題，每則範例出兩題：新輸入一題、相反分支一題。

**PM 定義裁決**（屬於盤點口徑，不涉及用戶的程式碼風格偏好）
1. **沒有三段標記的範例**（A-08～A-12）：只要教了判定，就納入範圍。
2. **什麼算條文**：
   - 範例區塊外、緊接在範例上方的引言句，算條文。
   - 程式碼圍欄如果本身就是規則或範本，而且位在範例區塊外，算條文。
   - 範例內的圍欄不算條文。
3. **跨 skill 的依據**：本 skill 在該判定處明文路由到那份文件時，才算有依據，探針的餵入範圍也要包含那份文件。沒有路由時，修法是補路由句，不是複製條文。

**修補分工**：每個 skill 開一張修補票，每張都走兩階段。
- 階段 1：權威條文已經寫明，只是本 skill 沒有引用或路由到它。直接補路由或補句，引用時附出處。
- 階段 2：需要新增判準、會改變既有判定。只寫草稿後停手，交給 WRAP；不是用戶程式碼風格偏好的，不上呈用戶。
### 階段 2 裁決（PM，2026-09-15；E-09 格式經用戶裁示）

六組 WRAP 評估的建議全部採用，評估內容寫在各來源票 Problem Analysis 的〈階段 2 WRAP 評估〉：G1 W3-421/422、G2 W3-426/424/418/420、G3 W3-423、G4a/G4b W3-419、G5 W3-420。

**逐項例外與補充**
- **E-09**：採用戶裁示，退出路徑格式寫死為「{觸發事件} → {目標狀態}」，並寫進 ux-design-evaluation `screen-state-matrix.md`〈四欄定義〉。範例 Q2 保留並改引這條條文。
- **D-12b**：不另外寫「呈現通道表第二列交給誰」，依評估者的傾向處理。
- **E-07**：doc 範本與 component-contract-design 的內容角色定義同批修改，定義文字以 W3-423 評估草稿為準，兩處逐字一致。
- **A-28、C-11a**：合併判準路由到 task-splitting〈拆分後檢查清單〉B 與 cognitive-load〈速查三閾值〉。component-contract-design 標記為 portable，所以 A-12 條文自足撰寫，但內容要與前述合併判準一致。
- **W3-419**：B-14c 與 E-09 改的是同一個檔案的同一節，由 W3-419 一併落檔；B-12b 範例與修復後矩陣畫面元素不一致的問題也一併修正。

**評估中登記的 spawn request，由各落檔票在 complete 前轉為票**
- W3-419 SR-1：退出路徑指離開狀態還是離開畫面（ANA）
- W3-420 SR-1：空殼 scaffold 的「相當程度」（ANA）
- W3-422 SR-1：doc-handoff「三項檢查」與閘門表四列不一致（DOC）
- W3-423 SR-1：component-contract-design 內容角色與範本矛盾（併入 E-07 同批落檔）
- W3-423 SR-2：step-3 路由錯位（DOC）
- W3-423 SR-3：浮層容器邊緣避讓由誰承接（ANA）
- G2 兩項發現：問 3 首次拆分的缺口（ANA）、version-bootstrap 與 task-splitting 對 GREEN 票拆分單位的衝突（ANA）

**落檔後的非撰寫者複核**：依用戶裁示，閘門改為探針。本輪落檔不再派七欄表複核，由 5 批探針驗證。
### 第 1 批題組（出題者，2026-09-15，凍結）

條文基準 commit `09bdb258`。範圍：dart-style-guardian、skill-design-guide、version-bootstrap、component-contract-design（盤點表 A-01～A-29，含 A-08～A-12）。每則範例 2 題：a＝範例未出現的新輸入，b＝判準相反分支。餵入只給範例區塊外條文，跨 skill 依據依定義 3 只在有明文路由時列入（CCD-03 方法論〈失敗路徑覆蓋〉、SDG-01b ccd〈共用規則一〉、VB-07／08 task-splitting〈拆分後檢查清單〉B 與 cognitive-load〈速查三閾值〉、VB-04 step-rationale〈Step 2 的 UI 前置檢查〉）。答案鍵引文共 100 句，已逐句以 `git grep -F <引文> 09bdb258` 確認命中出處檔。

**各檔題數**（scratchpad `probes/q/` 與 `probes/key/`，檔名 `W3-417-b1-<skill>.md`）

| skill | 範例數 | 題數 |
|---|---|---|
| component-contract-design | 12 | 24 |
| dart-style-guardian | 4 | 8 |
| skill-design-guide | 5 | 10 |
| version-bootstrap | 8 | 16 |
| 合計 | 29 | 58 |

**範例到題號對照**

| 盤點列 | 題號 | 盤點列 | 題號 |
|---|---|---|---|
| A-01 | CCD-01a／01b | A-16 | DSG-04a／04b |
| A-02 | CCD-02a／02b | A-17a | SDG-01a |
| A-03 | CCD-03a／03b | A-17b | SDG-01b |
| A-04 | CCD-04a／04b | A-18 | SDG-02a／02b |
| A-05 | CCD-05a／05b | A-19 | SDG-03a／03b |
| A-06 | CCD-06a／06b | A-20 | SDG-04a／04b |
| A-07 | CCD-07a／07b | A-21 | SDG-05a／05b |
| A-08 | CCD-08a／08b | A-22 | VB-01a／01b |
| A-09 | CCD-09a／09b | A-23 | VB-02a／02b |
| A-10 | CCD-10a／10b | A-24 | VB-03a／03b |
| A-11 | CCD-11a／11b | A-25 | VB-04a／04b |
| A-12 | CCD-12a／12b | A-26 | VB-05a／05b |
| A-13 | DSG-01a／01b | A-27 | VB-06a／06b |
| A-14 | DSG-02a／02b | A-28 | VB-07a／07b |
| A-15 | DSG-03a／03b | A-29 | VB-08a／08b |

A-17 一則範例教兩個判定，a 題出 A-17a、b 題出 A-17b 的相反分支。

**依據缺失清單**：0 題。58 題皆可由範例外條文作答。

**出題時避開、未出題的分支**（範例或條文本身的問題，非本批題目，供 PM 裁決是否開票）
1. version-bootstrap `examples/v02-walkthrough.md` 範例五第 4 問的依據是 task-splitting〈策略 7〉「不以依賴為由合併」，SKILL.md Step 6 只路由到〈拆分後檢查清單〉B 與〈速查三閾值〉，依定義 3 屬未路由。
2. version-bootstrap Step 6：共用檔案要合併，但合併後職責數 > 2 又要拆，兩條同時命中時怎麼處理沒有交代；拆開後各票又回到共用檔案，與 task-splitting B 的 Write-Write 衝突檢查衝突。
3. version-bootstrap Step 4.5：「其餘依順序表預設相依」有沒有讓元件契約（3.5）也預設依賴 i18n／design-system，沒有寫明。
4. dart-style-guardian i18n-guidelines：〈Exceptions〉把錯誤碼列為技術識別符豁免，〈Violation 2〉卻要求錯誤碼交給呈現層轉譯；錯誤碼直接顯示給使用者時兩條的交界沒有交代。DSG-04b 題幹已限定只顯示錯誤碼、無標籤。
5. component-contract-design 還有含判定的範例沒列進 A 組盤點，本批沒出題：SKILL.md 兩則、step-2-extract-components〈共用規則二〉歸併過頭、step-2-path-b 兩則、step-2-path-c 一則、step-3-fill-contract-conditional-cases 一則、step-4-verify-completeness 一則，以及 appendix-examples 多則驗證問句。

**探針規則區塊**沿用 V1-nongate-dual-r 格式，改動三處：(1) 餵入章節內的範例區塊逐檔寫明跳過邊界；version-bootstrap 題組直接禁開 `examples/`。(2) 作答四項改問判定與依據句。(3) 拿掉矛盾判定第 5 項。
## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 第 1 批判定（獨立判定者，2026-09-15）

**判定方法**：條文一律以 `git show 09bdb258:<path>` 讀取。每題先只讀題目與餵入條文寫出正確判定，再開答案鍵比對，最後逐份比對 3 份答案。3/3 才算通過（用戶裁示）。

**計分口徑**（本批自訂，影響 5 格）：
- 以 (1) 判定句為準。作答者明寫「更正／修正判定」者，以修正後的判定計分。
- 判定句與依據段矛盾、且沒有明寫更正者，判不通過。
- 題目有兩個子問、只答一問者，判不通過。
- 引文與實例值取自範例、但判定形式正確者，照判定計分，另列入探針異常。

**答案檔位置**：12 份都讀了。其中 `version-bootstrap-s2` 取自 `-graph_project_docs_manager` 目錄，`component-contract-design-s1` 取自 `-graph-project-docs_manager` 目錄。

#### 逐題表：dart-style-guardian（8/8）

| 題號 | 正確判定與依據 | s1 | s2 | s3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| DSG-01a | 按鈕 negative、確認訊息 positive。依據：color-system〈顏色歸類判準〉「功能類型不因觸發它的前置操作是否具破壞性而改變」 | 是 | 是 | 是 | 是 | — |
| DSG-01b | negative，不改判。依據：同上；〈Three-Color System〉「warnings, errors, destructive actions」 | 是 | 是 | 是 | 是 | — |
| DSG-02a | 主題插槽。依據：〈Theme 與 Token 判準〉「對應的內建語意插槽存在時用主題插槽，不存在時用 token」 | 是 | 是 | 是 | 是 | — |
| DSG-02b | token。依據：同上 | 是 | 是 | 是 | 是 | — |
| DSG-03a | titleLarge。依據：typography〈字級判準〉「卡片或對話框等容器元件內的標題對映 `titleLarge`」 | 是 | 是 | 是 | 是 | — |
| DSG-03b | headline4，不可任選。依據：同節「頁面內的階層子節標題對映 `headline4`」「數值相同不代表語意相同」 | 是 | 是 | 是 | 是 | — |
| DSG-04a | 不整體豁免：數值豁免，標籤走參數化翻譯。依據：i18n〈Exceptions〉「僅數值本身列入本豁免；標籤文字仍是使用者可見文字…」 | 是 | 是 | 是 | 是 | — |
| DSG-04b | 不需 i18n。依據：〈Exceptions〉「Technical identifiers - Error codes, keys」 | 是 | 是 | 是 | 是 | — |

#### 逐題表：skill-design-guide（10/10）

| 題號 | 正確判定與依據 | s1 | s2 | s3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| SDG-01a | 檢驗問句 + 預期答案。依據：chained-examples〈【驗證】段〉判定表「設計產出物」列 | 是 | 是 | 是 | 是 | — |
| SDG-01b | 不構成拆分理由，屬 slot 不拆。依據：ccd step-2-extract〈共用規則一〉「只是目的地不同者為 slot，不拆」 | 是 | 是 | 是 | 是 | — |
| SDG-02a | 違規。依據：〈三段鏈式格式〉「三段皆不可省略」 | 是 | 是 | 是 | 是 | — |
| SDG-02b | 合格。依據：同節 Action「三段都要寫全，順序固定…」 | 是 | 是 | 是 | 是 | — |
| SDG-03a | 填否；判「缺引導，非缺範例」；先把判準寫進條文。依據：〈驗證段機械檢查〉第 2 點與處置表 | 是 | 是 | 是 | 是 | — |
| SDG-03b | 合格，保留範例。依據：處置表「三項確認欄皆「是」｜合格｜保留範例…」 | 是 | 是 | 是 | 是 | — |
| SDG-04a | Given/When/Then。依據：判定表「執行期行為」列 | 是 | 是 | 是 | 是 | — |
| SDG-04b | 檢驗問句 + 預期答案。依據：判定表「設計產出物」列（含「起點判定」） | 是 | 是 | 是 | 是 | — |
| SDG-05a | 互斥不能當依據，改依四問逐段指名路由表的列。依據：splitting〈外移什麼、留什麼〉「互斥性因此不進入判定」「四個判定各自都要求你指名路由表的具體某一列」 | 是 | 是 | 是 | 是 | — |
| SDG-05b | 不落入觸發條件。依據：〈補範例後超標〉「超標成因是新補入的範例，不是原有規則文字本身過長」 | 是 | 是 | 是 | 是 | — |

#### 逐題表：version-bootstrap（9/16）

| 題號 | 正確判定與依據 | s1 | s2 | s3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| VB-01a | 屬契約檔案。依據：version-shift-sop 判準 1「…寫入並保存資料（資料庫列、使用者端檔案…）」「三問任一為「是」即屬契約檔案」 | 否（判非契約：新增鍵是執行期設定，改形狀不需遷移） | 是 | 是（先判否，後明寫更正為是） | 否 | 條文歧義 P1 |
| VB-01b | 非契約，多模組引用不改判。依據：「三問皆否：屬純程式邏輯，即使被多個模組引用…也不列入步驟 1」 | 是 | 是 | 是 | 是 | — |
| VB-02a | 判準 2 成立。依據：判準 2 列「…以 key 對接的翻譯提供者」 | 是 | 否（判不成立：key 改名可在本方單次提交完成） | 是 | 否 | 條文歧義 P1 |
| VB-02b | 不成立。依據：判準 2「判定單位是發布或部署單位，不是儲存庫」 | 是 | 是 | 是 | 是 | — |
| VB-03a | 非 UI 類，不過三項前置；Checkpoint 記呈現通道並交 foundation-design，不得只記「非 UI 類」。依據：SKILL Step 2 呈現通道表第二列 | 否（判定句寫「需過三項前置」，依據段寫「不過」，未更正） | 否（判 UI 類） | 否（判 UI 類） | 否 | 條文歧義 P2 |
| VB-03b | UI 類，要過三項前置。依據：表第一列「…含唯讀畫面」 | 是 | 是 | 是 | 是 | — |
| VB-04a | 不通過；補齊前不可繼續 UI 實作票規劃。依據：前置檢查表 L3 列「元件清單 + 原生元件禁用對照表 + 豁免清單」；「缺則先補齊才可繼續本提案的 UI 實作票規劃」 | 是 | 是 | 否（判通過；把表格列錯置，並自行假設豁免清單為選填） | 否 | 探針誤讀 |
| VB-04b | 通過。依據：L3 列「本步驟得先只到此，逐元件元件契約欄位表與容器條目於 Step 4.5 第 3 塊後補齊」 | 是（明寫修正為通過） | 否 | 否（判定句寫不通過，依據段卻支持通過） | 否 | 條文歧義 P3 |
| VB-05a | 不可開工；blockedBy 含 i18n 與 design-system。依據：Step 4.5 PM 工作「UX 審查因此以兩者為 `blockedBy`」 | 否（判定句寫「可以開工」，依據段矛盾；另虛構順序表依賴欄） | 是 | 是 | 否 | 探針作答異常 |
| VB-05b | 可開工，不以 i18n 為 blockedBy。依據：「唯 i18n 與 design-system 可並行」 | 是 | 否（判定句寫「不能」，依據段寫可獨立開工） | 是 | 否 | 探針作答異常 |
| VB-06a | 不另建。依據：Step 5「兩軸交集去重、聯集為完整覆蓋」 | 是 | 是 | 是 | 是 | — |
| VB-06b | 須建。依據：同句；「避免 domain 規則只靠「剛好出現於某 UC 場景」被動覆蓋」 | 是 | 是 | 是 | 是 | — |
| VB-07a | 1 張，不再拆。依據：Step 6「或」句；task-splitting B「共用檔案的問題已合併到同一 ticket」；cognitive-load「> 2 須拆分」 | 是 | 是 | 是 | 是 | — |
| VB-07b | 各 1 張。依據：Step 6「每個 spec FR 或功能模組 1 張」 | 是 | 是 | 是 | 是 | — |
| VB-08a | 不可各自修改，由整合票負責。依據：Step 6 PM 工作「共用檔案需整合票，歸屬後其餘票不得直接修改」 | 是 | 是 | 是 | 是 | — |
| VB-08b | 不需整合票。依據：同句前件「共用檔案」不成立 | 是 | 是 | 是 | 是 | — |

#### 逐題表：component-contract-design（19/24）

| 題號 | 正確判定與依據 | s1 | s2 | s3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| CCD-01a | 待決，回覆前不進 step 2。依據：step-1〈情況二〉「「無法核對」不等於「全否」，不得判 A」「回覆前不進 step 2」 | 否（判 A，可進 step 2） | 否（判待決，但沒答「可否進 step 2」） | 是 | 否 | 探針誤讀（s1）＋漏答（s2） |
| CCD-01b | C。依據：「任一命中即為 C」 | 是 | 是 | 是 | 是 | — |
| CCD-02a | 不渲染。依據：path-a〈反向規則〉「控制項不渲染，不以停用狀態渲染」 | 是 | 是 | 是 | 是 | — |
| CCD-02b | 待決，建規格追修票。依據：〈反向規則〉例外句 | 是 | 是 | 是 | 是 | — |
| CCD-03a | 三段用「不經服務：讀取的資料為 X，於 Y 時已載入；資料缺值時的表現見 Z」，不得寫同成功。依據：方法論〈失敗路徑覆蓋〉 | 是 | 是（Y 值取自範例） | 是（X、Y 值取自範例） | 是 | — |
| CCD-03b | 不適用固定寫法，四段逐一填表現。依據：同節「所讀資料需按需讀取、可能等待者不適用本寫法」 | 是 | 是 | 是 | 是 | — |
| CCD-04a | 第 3 格。依據：step-5〈格位起算〉「垂直堆疊以最上方…為第 1 格」 | 是 | 是 | 是 | 是 | — |
| CCD-04b | 不合格，寫「動作列 slot」。依據：「容器已具名 slot 者…寫 slot 名，不另行編號」 | 是 | 否（稱題目沒給 slot 契約，改寫成「內容 slot」或「第 3 格」） | 是 | 否 | 探針誤讀題幹 |
| CCD-05a | 不佔格位；寫在列末覆蓋層子句；置中不寫進畫面票。依據：〈覆蓋層〉 | 是 | 是 | 是 | 是 | — |
| CCD-05b | 不列。依據：「屬觸發元件自帶者…不列」 | 是 | 是 | 是 | 是 | — |
| CCD-06a | 離線獨立一列，標疊加於、只寫增量，底層列不動。依據：〈疊加態〉 | 是 | 是 | 是 | 是 | — |
| CCD-06b | 不在畫面票裁決，回狀態矩陣。依據：〈疊加態〉末句 | 是 | 是 | 是 | 是 | — |
| CCD-07a | 不新增列；定位與聚焦寫進該項元件回饋契約「被程式定位」格。依據：〈非狀態進入情境〉 | 是 | 是 | 是 | 是 | — |
| CCD-07b | 寫一列。依據：「how 的列固定為狀態矩陣的列」 | 是 | 是 | 是 | 是 | — |
| CCD-08a | 合為一個元件，兩者都不是對方的 slot。依據：path-a 步驟 2「兩集合完全相同為總是」「總是一起出現的單元合為一個元件」 | 否（合一，且一方為 slot） | 否（同 s1） | 否（同 s1） | 否 | 條文缺引導 P4 |
| CCD-08b | 不合併、非 slot，各自為候選（或容器子件）。依據：path-a 步驟 2「兩集合互有對方沒有的列時…各自為候選」 | 是 | 是 | 是 | 是 | — |
| CCD-09a | 1 個容器候選，子件為搜尋欄、狀態下拉、日期下拉、清除鈕。依據：步驟 4「每個佈局結構一個容器候選」 | 否（算 2 個，自行把主區也算進去） | 是 | 否（算 2 個，自行把清單也算進去） | 否 | 題目設計問題 |
| CCD-09b | 不是容器候選。依據：「單一子件的包裝…無排列關係，不是容器候選」 | 是 | 是 | 是 | 是 | — |
| CCD-10a | 否。依據：step-3〈關卡二〉「…指示消失後內容與事件前相同即答否」 | 是 | 是 | 是 | 是 | — |
| CCD-10b | 是。依據：同段「進行中狀態取代了該區塊的原內容…即答是」 | 是 | 是 | 是 | 是 | — |
| CCD-11a | 展開到分頁項的圖示 slot 放檔案類型圖示；關閉鈕不寫；數量寫「逐格放…，數量依資料筆數」。依據：step-5〈巢狀容器〉 | 否（判定句寫範例的「清單項徽章 slot」） | 否（判定句寫「關閉鈕應寫」，未更正） | 否（修正後關閉鈕不寫，但展開層寫成範例的「狀態徽章」） | 否 | 探針讀範例 |
| CCD-11b | 不續寫。依據：「直到被放入的元件不再有呼叫端須填的 slot 為止」 | 是 | 是 | 是 | 是 | — |
| CCD-12a | 2 張：report/ 與 report/export/；shared/ 不建。依據：〈遷移票〉段首 | 是 | 是 | 是 | 是 | — |
| CCD-12b | 1 張。依據：「「目錄」指直接含待改頁面檔的目錄」 | 是 | 是 | 是 | 是 | — |

#### 各 skill 通過率

| skill | 通過／題數 | 未通過中屬條文問題 |
|---|---|---|
| dart-style-guardian | 8／8 | 0 |
| skill-design-guide | 10／10 | 0 |
| version-bootstrap | 9／16 | 4 題（VB-01a、VB-02a、VB-03a、VB-04b），歸成 3 個條文問題 |
| component-contract-design | 19／24 | 1 題（CCD-08a），1 個條文問題 |
| 合計 | 46／58 | 5 題，4 個條文問題 |

#### 答案鍵錯誤清單

先自行推判定、再與答案鍵比對，58 題的結論全部一致，沒有判定錯誤。只有 1 處依據不完整：

- **CCD-08b**：輸入中兩單元只在「乙·正常」這一列同時出現，屬單一畫面內共現。path-a 步驟 2 對這種情形另有優先句：「單一畫面內共現的情形先依上句判為容器子件，不進入本比較」。答案鍵只引集合互有差集句，沒有處理這條優先句。兩條路徑的結論都是「不合併、非 slot」，因此不影響計分。這屬於題目設計問題：b 題的輸入意外命中了優先句。

另有 1 題屬題目設計問題，不計入條文問題：

- **CCD-09a**：題幹寫到「主區」「清單上方」，卻沒有說明主區與清單的結構不在問題範圍內。2 份答案把主區或清單也算成容器候選。

#### 未通過的條文問題清單

| 編號 | skill | 題號 | 條文位置 | 問題 |
|---|---|---|---|---|
| P1 | version-bootstrap | VB-01a、VB-02a | `references/version-shift-sop.md`〈契約檔案的界定〉判準 1、判準 2 問句 | 問句前段有括號列舉（使用者端檔案、以 key 對接的翻譯提供者），後段有「使日後改形狀必須遷移或相容讀取」「使改形狀無法在同一次提交內一併修改」。兩段的關係沒有寫明：命中列舉是否即算成立，還是要另外驗證後段。有探針把後段當成獨立條件，推出列舉對象「不需遷移」「本方可單次提交完成」，判成不成立。VB-01a 另一份答案也先判錯才更正 |
| P2 | version-bootstrap | VB-03a | `SKILL.md` Step 2〈UI 類判別〉呈現通道表 | 第一列「由本應用程式渲染的視窗、頁面…」與第二列「非圖形的人讀呈現：…匯出文件版面」有交界：應用程式產出、有固定版面的 PDF 兩列都讀得進去。「非圖形」這個標籤與 PDF 版面的圖形性在字面上衝突，表格也沒有寫兩列命中時何者優先。2 份判 UI 類，1 份判定句自相矛盾 |
| P3 | version-bootstrap | VB-04b | `SKILL.md` Step 2 前置檢查表 L3 列「對應載體」欄；`references/step-rationale.md`〈Step 2 的 UI 前置檢查〉「存在」判準句 | 載體欄的括號裡同時寫了逐元件契約欄位表與容器條目。「本步驟得先只到此」的「得」是允許語氣，不是 Step 2 檢查範圍的定義句。step-rationale 又要求「內容是否符合對應載體欄的定義」，讀者因此把整格都當成 Step 2 的要求，判不通過（2 份） |
| P4 | component-contract-design | CCD-08a | `references/step-2-path-a-spec-derivation.md` 程式碼圍欄步驟 2 | 「總是一起出現的單元合為一個元件；有時一起出現的單元是該元件的可選 slot」只講合併與否，沒有交代合併後兩個單元是什麼關係（各為一個內容角色，都不是 slot）。這層依據只在範例理由欄（「是一個空狀態元件的兩個內容角色」）。3 份答案都寫「合為一個元件，其中一方是另一方的可選 slot」。依 chained-examples〈「缺範例」與「缺引導」的分界〉，屬缺引導 |

#### 探針行為異常

| 類型 | 發生處 |
|---|---|
| 路徑錯 | version-bootstrap-s2 寫入 `-graph_project_docs_manager/.../probes/a/`；component-contract-design-s1 寫入 `-graph-project-docs_manager/.../probes/a/` |
| 讀範例（違反「跳過範例區塊」） | DSG s1（02a／02b 引決策表、03b 引驗證問句）、s2（02a 引決策表理由句）、s3（01a／01b／03b 引驗證段、02a 引【輸入】段、02b／03a 引決策表）；VB s1、s2 的 VB-02b 引 version-shift-sop 範例二驗證問句；CCD s2 10a 引範例 Given/When/Then、s2／s3 03a 的 X、Y 值取自範例（「格資料」「載入中狀態完成時」）、s1／s3 11a 判定句用範例元件名、s2／s3 07a 用範例的「清單卡」 |
| 讀出餵入範圍 | VB s2 VB-04a 引 step-rationale〈Step 2 為什麼多一項 design-system spec〉；VB s3 VB-08a 引 task-splitting〈拆分策略速查〉第 6 列 |
| 虛構引文（出處檔 grep 無命中） | DSG s3 04a `Text(context.l10n!.versionLabel(version))`；VB s1 05a／05b 順序表「i18n + design-system」依賴欄；VB s3 07b「每個 ticket 應聚焦單一功能面」；CCD s1 08a「判為一個元件或 slot 的核心是「出現列集合的比較」」；CCD s2 12b 把題目輸入改寫成範例引文 |
| 判定句與依據矛盾 | VB s1 03a、05a；VB s2 05b；VB s3 04b；CCD s2 11a（以上皆未更正）；VB s3 01a、VB s1 04b、CCD s3 11a（明寫更正） |
| 出處章節標錯 | CCD s1 10a／10b 把 step-3〈關卡二〉標成方法論 |
| 讀答案鍵 | 無跡象 |

### 第 1 批判定與覆蓋率複查後的 PM 處置（2026-09-15）

**用戶裁示**
- W3-437.2：案例檔不算閘門範例。tdd `references/cases/`、version-bootstrap 與 doc 的動機案例都屬證據敘事，不列入閘門範圍。
- W3-436：畫面上不能單獨顯示錯誤碼。

**PM 裁決**
- **計分口徑**：採判定者的規則。答案明寫「更正」的，以更正後的判定計分；判定句與依據矛盾又沒有更正的，判不通過。
- **第 1 批條文問題 P1～P4 的修補分工**
  - version-bootstrap：P1、P2、P3
  - component-contract-design：P4
- **W3-437 漏列阻擋 16 項**：每個 skill 建一張修補票（component-contract-design、tdd、skill-design-guide），走兩階段。component-contract-design 的 F-16、F-18 與條文牴觸，由 W3-437.1 評估，修補票不直接改這兩項。
- **重測**：修補完成後，第 1 批未通過的 12 題重新出題重測，門檻仍是 3/3。56 則漏列範例併入第 2～5 批出題。
- **並行上限**：文件落檔代理人同時最多 5 個（W3-433 候選 1）。
### 修補完成，進入重測與第 2 批出題（PM，2026-09-15）

**修補已落檔**
- W3-438：dart-style-guardian 1.12.0
- W3-439：version-bootstrap 1.7.5（P1～P3）
- W3-440：component-contract-design 3.9.8（F-01／02／05／06／08、P4、F-18 選項 B、命中區片段移除）
- W3-441：tdd 2.10.1（F-25～F-37，含 F-28）
- W3-442：skill-design-guide 1.23.0
- W3-443：F-16／F-18 範例與射程子句

W3-437、W3-437.1、W3-437.3、W3-440.1、W3-441.1 均已完成。W3-444（F-05 過細圖層）為 P2 追蹤票，不阻擋閘門。

**下一步分兩路並行出題（出題者只寫題與答案鍵，不跑探針）**

1. 第 1 批重測
   - 對象：〈第 1 批判定〉未通過的 12 題，對應範例重新出題，每則 2 題（新輸入＋相反分支）。
   - 依據句一律取修補後的現行條文。
   - 檔名：`probes/q/W3-417-b1r-<skill>.md`、`probes/key/W3-417-b1r-<skill>.md`。
2. 第 2 批
   - 對象：ux-design-evaluation 全部範例，加上 W3-437 漏列清單中屬 ux-design-evaluation 的範例。
   - 抓範例改用 W3-437 的判準，不只找 `【輸入】` 標記。
   - 檔名：`probes/q/W3-417-b2-ux-design-evaluation*.md`，依範例數分檔，每檔 ≤ 30 題。
- 出題規則沿用〈第 1 批題組〉凍結規則：每題答案鍵附依據句原文，並以 `git grep -F` 驗證。
- 題組凍結後由 PM 核對，再派 haiku 探針，每單位 3 次。
### 第 1 批重測判定（獨立判定者，2026-09-15）

**判定方法**：條文一律讀 HEAD `4d5a045c`。version-bootstrap 在 `b383ca1b..HEAD` 之間沒有變更；component-contract-design 只多了 `60bab317`（W3-445，3.9.9），這個 commit 只改 path-a 步驟 2 的路由錨點用詞。每題先依題目的餵入範圍自行推出判定與依據句，再開答案鍵比對，最後逐份判 3 份答案（`probes/a/W3-417-b1r-<skill>-r{1,2,3}.md`）。門檻 3/3。計分口徑沿用〈第 1 批判定〉：
- 明寫更正的，以更正後的判定計分。
- 判定句與依據矛盾、又沒有更正的，判不通過。
- 題目有多個子問、漏答任一子問的，判不通過。
- 引文錯誤但判定正確的，照判定計分，另列探針異常。

**結果**：16／20。version-bootstrap 8／10，component-contract-design 8／10。

#### 逐題表：version-bootstrap（8/10）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| VB-01a | 屬契約檔案；「丟棄重建」不改變判準 1。依據：version-shift-sop〈契約檔案的界定〉判準 1 列舉「快取」；〈列舉與後段子句的關係〉「命中列舉任一項即判該問為「是」，不需要為當下情境重新驗證後段子句是否成立」 | 是 | 是 | 是 | 是 | — |
| VB-01b | 非契約檔案；檔案類型不影響判定。依據：「檔案類型不決定歸屬」「三問皆否：屬純程式邏輯」 | 是 | 是 | 是 | 是 | — |
| VB-02a | 判準 2 成立；產品負責人的說法不改變判定。依據：判準 2 列舉「第三方整合」＋列舉關係句 | 是 | 是 | 是 | 是 | — |
| VB-02b | 不成立。依據：「是否有消費方位於另一個獨立發布或部署的單位」「判定單位是發布或部署單位，不是儲存庫」 | 是 | 是 | 是 | 是 | — |
| VB-03a | 非 UI 類，不過三項前置；Checkpoint 記呈現通道並交 foundation-design。依據：SKILL Step 2 呈現通道表第二列「匯出文件版面（優先本列）」；step-rationale「改由外部程式（PDF 閱讀器、試算表軟體等）另開檔案呈現，一律歸第二列」 | 否（判定句寫「UI 類，須過三項前置」，依據段歸第二列，沒有更正） | 否（判定句寫 UI 類，重新檢視後結論是「無法給出確定判定」） | 否（判定句同時寫「UI 類，應過三項前置」與「非圖形人讀呈現，交 foundation-design」） | 否 | 題目設計 |
| VB-03b | UI 類，要過三項前置。依據：表第一列；「只有仍在本應用程式視窗內即時繪製、可操作或查看者才歸第一列」 | 是 | 是 | 是 | 是 | — |
| VB-04a | 不通過；補齊前不可繼續 UI 實作票規劃。依據：L3 列「僅元件清單 + 原生元件禁用對照表 + 豁免清單三項屬本步驟範圍」；「標題存在但內容為空殼（如僅一句「待補」）不算通過」；「缺則先補齊才可繼續本提案的 UI 實作票規劃」 | 是 | 是 | 是（第二子問寫在依據段） | 是 | — |
| VB-04b | 通過；容器條目「待補」不影響。依據：step-rationale「契約欄位表與容器條目不算本步驟範圍…缺這兩項不影響本步驟通過判定」 | 是 | 是（引文竄改，見異常） | 是 | 是 | — |
| VB-05a | 不可開工；blockedBy 含 i18n、design-system、UX 審查、元件契約。依據：SKILL Step 4.5「元件庫依賴前三塊與元件契約為 `blockedBy`」 | 否（只列元件契約＋UX 審查，i18n／design-system 以遞移為由省略） | 否（列前三塊，並明說元件契約「不可包含」） | 否（只列元件契約） | 否 | 條文不清 |
| VB-05b | 只列 UX 審查；不需另列 i18n／design-system。依據：step-rationale〈元件契約遞移依賴〉「元件契約的 `blockedBy` 只需列 UX 審查一項」 | 是 | 是 | 是 | 是 | — |

#### 逐題表：component-contract-design（8/10）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| CCD-01a | 待決；附錄檔路徑只列為請求核對理由，不據以判 C；回覆前不進 step 2。依據：step-1〈情況二〉「「無法核對」不等於「全否」，不得判 A」「列為請求核對的理由」「回覆前不進 step 2」 | 是 | 否（判定只答「待決」，C 與 step 2 兩子問沒有作答，只在引文中出現） | 否（「進 step 2」子問沒有作答，引文也截在該句之前） | 否 | 題目設計 |
| CCD-01b | 可暫判 A；標「暫判，C 訊號未核對」；產出不得直接轉入。依據：同段演練句 | 是 | 是 | 是 | 是 | — |
| CCD-04a | 第 3 格。依據：step-5〈格位起算〉「水平並排以書寫方向起始側為第 1 格」 | 是 | 是 | 是 | 是 | — |
| CCD-04b | 不合格；先補 slot 契約。依據：「容器契約尚未寫出 slot 者，先補該容器的 slot 契約，不在畫面票內自訂編號。」 | 是 | 是 | 是 | 是 | — |
| CCD-08a | 合為一個元件；兩者各為內容角色，不是對方的 slot。依據：path-a 步驟 2「兩集合完全相同為總是」「合為一個元件時，原單元各自是該元件的一個內容角色…彼此不是對方的 slot」 | 是 | 是 | 是 | 是 | — |
| CCD-08b | 排序切換是清單標題所屬元件的可選 slot。依據：「一方集合是另一方的真子集時，較少的一方是較多一方的可選 slot（有時）」 | 是 | 是 | 是（另寫「一個元件、兩個內容角色」，與「該元件的可選 slot」相容） | 是 | — |
| CCD-09a | 1 個候選；子件為名稱欄、說明欄、通知開關、儲存鈕。依據：步驟 4「每個佈局結構一個容器候選」 | 是 | 是 | 是 | 是 | — |
| CCD-09b | 2 個候選（外層含按鈕列容器；按鈕列含取消鈕、儲存鈕）。依據：同句；「容器的子件可以是容器」 | 是 | 是 | 是 | 是 | — |
| CCD-11a | 展開到頭像 slot 放頭像變體 small；圓點不寫；數量「依資料筆數」不逐筆列出。依據：step-5〈巢狀容器〉 | 是 | 是 | 否（判定句「停止於成員列」，依據段又說「需繼續展開」，沒有更正） | 否 | 讀者失誤 |
| CCD-11b | 不寫。依據：「直到被放入的元件不再有呼叫端須填的 slot 為止；元件自帶的子件不寫。」 | 是 | 是 | 是 | 是 | — |

#### 失敗歸因

| 題號 | 歸類 | 具體句 |
|---|---|---|
| VB-03a | 題目設計 | 題幹「這個提案應判為 UI 類嗎？」是是非問句，3 份的判定句都先回「是／UI 類」，依據段卻全部正確引到第二列。r2 明寫「題目問『是否應判為 UI 類……須過三項前置檢查』，暗示預期答案為肯定」。條文本身清楚：3 份都引到 step-rationale「一律歸第二列」與表第二列「（優先本列）」。修法：題幹改成選擇式問句（「判為 UI 類或非 UI 類？」），不改條文 |
| VB-05a | 條文不清 | SKILL Step 4.5 PM 工作句「元件庫依賴前三塊與元件契約為 `blockedBy`」全數列出；step-rationale〈元件契約遞移依賴〉對元件契約卻要求「不必重複列出；重複列出反而製造兩條可能不同步的依賴宣告」。這條理由的射程沒有限定只適用元件契約。r1 把同一原則套到元件庫，寫出「元件庫實作票的直接 `blockedBy` 為：元件契約票（3.5）+ UX 審查票（3），經此鏈涵蓋前三塊」。兩處對遞移依賴要不要重複列出的態度相反，沒有交代元件庫為何例外。另有兩個次因：r3 只列未完成的元件契約，是把 blockedBy 讀成「目前仍擋住的票」，這個讀法由題幹同時給出完成狀態誘發（題目設計）；r2 寫「元件契約 DOC 票仍在進行中，故…不可包含元件契約」，屬讀者失誤 |
| CCD-01a | 題目設計 | 一題三子問（起點判定欄、可否據以判 C、可否進 step 2），探針規則第 3 點卻要求「(1) 你的判定（一句）」。r2 判定句只有「起點判定欄應填「待決」。」，r3 判定句只有「起點判定欄應填「待決」，無法判定為 A 或 C。」。兩份的引文都含正解句，第 1 批 s2 也是同樣漏答，已連續兩輪同型失敗。條文不需修改。修法：多子問的題目改為判定欄逐子問編號作答，或拆成單一子問 |
| CCD-11a | 讀者失誤 | r3 寫「成員列無由呼叫端填入的 slot（頭像 slot 由成員列本身定義），頭像元件本身有由呼叫端填入的 slot（inline slot）」，與題幹「每個成員列有一個由呼叫端填入的頭像 slot」「線上狀態圓點屬元件自帶、不是 slot」正好相反，判定句也停在成員列。r1、r2 依〈巢狀容器〉「直到被放入的元件不再有呼叫端須填的 slot 為止」正確展開到頭像，條文沒有歧義 |

#### 答案鍵疑誤

20 題的判定與獨立推導全部一致，沒有判定錯誤。有 1 處引文與 HEAD 不符，1 處條文衝突附註：
- **CCD-08a 引文**：答案鍵引的片段「內容角色（見 `step-3-fill-contract.md`〈語意與內容角色〉四類判準）」，在 HEAD 已由 W3-445 改為「內容角色（見 `step-3-fill-contract.md`〈元件契約欄位填寫問句〉表「語意與內容角色」列的四類判準）」。`git grep -F '〈語意與內容角色〉四類判準' HEAD` 命中 0。判定不受影響，r1 與 r2 引的是 HEAD 用詞。
- **VB-05a**：答案鍵依 SKILL 明文「元件庫依賴前三塊與元件契約為 `blockedBy`」判定，有明文支持，不算答案鍵錯誤。它與 step-rationale 的去重原則衝突，已列在失敗歸因。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 虛構或竄改引文 | VB r1 04a「三項皆有實際內容」（出自 VB-04b 題幹，不是條文）；VB r2 04b「『原生元件禁用對照表』不算本步驟範圍」（原文是「契約欄位表與容器條目不算本步驟範圍」）；CCD r3 01a 把題幹問句標成「第 55 行」引文 |
| 出處章節標錯 | VB r3 02a 把「判定單位是發布或部署單位，不是儲存庫」標成〈列舉與後段子句的關係〉段；CCD r1 09b 把「一組」句標成範例驗證問句（實際在步驟 4 圍欄） |
| 誤判題目轉述不符 | VB r2 03a 把是非問句當成題目預設答案，宣稱「題目轉述與現文不符」 |
| 讀範例／讀出餵入範圍 | 未見跡象 |
### 第 1 批重測判定後的處置（PM，2026-09-15）

**用戶裁示**：讀者失誤一律記不通過，並改題重測，維持 3/3。理由：題目也是讀者會遇到的文字，被讀反仍算訊號。

**4 題未通過的處置**
- **VB-05a**（條文不清）：W3-447 已補去重原則的射程，version-bootstrap 升 1.7.6。依修補後條文改題重測。
- **VB-03a**（題目設計）：題幹改為選擇式問句，重測。
- **CCD-01a**（題目設計，連續兩輪同型）：原本三個子問拆成三題，每題只問一個判定，重測。
- **CCD-11a**（讀者失誤）：題幹前提改成條列，避免讀反，重測。

**題號規則**：重測題號加後綴 r2，例如 VB-03a-r2；檔名為 `W3-417-b1r2-<skill>.md`。

**不需處置**：CCD-08a 答案鍵中的舊錨點用詞不影響計分，隨本輪改題順手更新。
### 第 2 批判定（獨立判定者，2026-09-15）

**判定方法**：條文讀 HEAD `f1eaffd6`。`git diff b383ca1b HEAD -- .claude/skills/ux-design-evaluation/` 為空，題目基準與 HEAD 一致。每題先依餵入範圍自行推出判定與依據句，再開答案鍵比對，最後逐份判 3 份答案。之 1 讀 `probes/a/W3-417-b2-ux-design-evaluation-1-r{1,2,3}.md`；之 2 的 r2 在同目錄，r1、r3 被探針寫到另外兩個錯拼的 scratchpad 目錄，照 PM 指示直接讀。門檻 3/3。計分口徑沿用〈第 1 批重測判定〉，另依〈第 1 批重測判定後的處置〉裁示，讀者失誤也記不通過。本批補一條統一口徑：判定句另附與條文不符的替代判定（「或…」「若只能選一種則…」），又沒有更正的，判不通過。

**結果**：47／58。之 1 為 21／30，之 2 為 26／28。

#### 逐題表：之 1（21/30）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-01a | 1-10s 帶，進度指示 + spinner；不可只寫「要有回饋」；標明統計量 p90。依據：表 1-10s 列；SKILL「產出必須是可被引用的具體值」；「須顯式記錄選用的統計量」 | 否（400ms-1s） | 否（400ms-1s，漏答兩子問） | 否（寫「3 秒落在 400ms-1s」） | 否 | 讀者失誤 |
| UX-01b | 100-400ms 帶，動畫過渡；不用 spinner；仍須交值 | 是 | 是 | 是 | 是 | — |
| UX-02a | 依臨界路徑；Tab、`;` 放拇指自然停留處 | 是 | 是 | 是 | 是 | — |
| UX-02b | 有實測資料，依頻率；Esc 最外側。依據：「排列依該場景下的實際使用頻率排列」「使用頻率無法確定時，以…臨界路徑」 | 否（依臨界路徑，Esc 靠拇指） | 否（依據答臨界路徑） | 否（依據答臨界路徑） | 否 | 讀者失誤 |
| UX-03a | visiblePassword；三項全關；Submit model 在 protocol spec 階段 | 是 | 是 | 是 | 是 | — |
| UX-03b | 一般文字鍵盤；不關；不適用安全清單 | 是 | 是 | 是 | 是 | — |
| UX-04a | 一般文字鍵盤 | 是 | 是 | 是 | 是 | — |
| UX-04b | 數字鍵盤 | 是 | 是 | 是 | 是 | — |
| UX-05a | 格式驗證，on blur | 是 | 是 | 是 | 是 | — |
| UX-05b | 交叉驗證，on submit | 是 | 是 | 是 | 是 | — |
| UX-06a | 按送出（或 debounce，見答案鍵疑誤）；本機不足以選即時 | 是 | 是 | 是 | 是 | — |
| UX-06b | 即時；來源不改變判定 | 是 | 是 | 是 | 是 | — |
| UX-07a | 學習關；校正與建議保留開啟；iOS 顯式記錄風險 | 否（三項皆關，(3) 列出正解讀法但判定句未更正） | 否（三項皆關，引文卻是「自然語言保留開啟」） | 是 | 否 | 讀者失誤 |
| UX-07b | 三項皆關 | 是 | 是 | 是 | 是 | — |
| UX-08a | segmented control | 是 | 是 | 是 | 是 | — |
| UX-08b | 拆狀態顯示＋動作鈕 | 是 | 是 | 否（另附「或改用 switch 若設計成兩個獨立開關」） | 否 | 讀者失誤 |
| UX-09a | 合格，圓點屬明顯非按鈕形態。依據：「指示用明顯非按鈕形態（文字 chip、圓點）或移出動作列」 | 否 | 否（(3) 列兩讀法，判不合格） | 否 | 否 | 條文不清 |
| UX-09b | 不合格；描邊 vs 實心不足以區分 | 是 | 是 | 是 | 是 | — |
| UX-10a | 不可省略號；先縮格式，仍不足才設最小寬度並由鄰近元件讓步 | 是 | 是 | 是 | 是 | — |
| UX-10b | 不須設最小寬度；讓步規則路由至 component-contract-design〈空間不足策略〉 | 否（「不一定需要，取決於空間是否已足夠」，未就題幹前提作答） | 否（「仍須依最小寬度策略考慮」） | 是 | 否 | 讀者失誤 |
| UX-11a | 不足，須顯式記錄統計量；p95 → 進度指示 + spinner | 是 | 是 | 是（把「若選定」寫成「應選定」，見異常） | 是 | — |
| UX-11b | 不可，仍須記錄 | 是 | 是 | 是 | 是 | — |
| UX-12a | 權威定義在 `screen-state-matrix.md`；本節只負責每個狀態的 UI 呈現。依據：檔首不適用「畫面狀態的完整枚舉與退出路徑（狀態矩陣範疇，見 `screen-state-matrix.md`）」 | 是（章節名標錯，見異常） | 否（答 `interaction-feedback.md` 或 gate-fallback、error-recovery，與自己的引文矛盾） | 否（答 screen-state-matrix「或 `gate-fallback.md`」） | 否 | 讀者失誤 |
| UX-12b | 屬本檔；spinner（形狀 > 時長）；1-10s 進度指示 + spinner | 是 | 是 | 是 | 是 | — |
| UX-13a | 略大於 60 秒；逾時後恢復 idle + 錯誤訊息 | 是 | 是 | 是 | 是 | — |
| UX-13b | 不合格 | 是 | 是（理由方向寫反，見異常） | 是 | 是 | — |
| UX-14a | 4 狀態（視覺反推）；全空＝4 個死胡同；只寫「返回」缺目標，未定義 | 是 | 是 | 否（寫 4 個狀態，卻說「五個死胡同」） | 否 | 讀者失誤 |
| UX-14b | 從 enum 取；「返回 → 首頁」格式合格 | 否（判不合格，理由是 error 狀態應有重試） | 否（同左） | 是 | 否 | 題目設計 |
| UX-15a | 需要 initializing；網路不影響 | 是 | 是 | 是 | 是 | — |
| UX-15b | 不需要，一般 loading | 是 | 是 | 是 | 是 | — |

#### 逐題表：之 2（26/28）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-16a | 可逆，由「關閉勿擾」提供 | 是 | 是 | 是 | 是 | — |
| UX-16b | 不可逆；「關閉面板」仍滿足退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-17a | 獨立成列；兩畫面各一列；退出至少「返回」或「離開此畫面」；記錄設定入口與清除時機 | 是 | 是 | 是 | 是 | — |
| UX-17b | 不屬（條件 2 或 1 不成立） | 是 | 是 | 是 | 是 | — |
| UX-18a | 只給重試不足（須含原因提示）；重啟 app 不合適；須含退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-18b | 不合適；配資料層行動 | 是 | 是 | 是 | 是 | — |
| UX-19a | 固定間隔並設上限；正在重試、已重試幾次、下一次時間 | 是 | 是 | 是 | 是 | — |
| UX-19b | 指數退避 + jitter；倒數與立即重試按鈕 | 是 | 是 | 是 | 是 | — |
| UX-20a | 不可靜默隱藏；明確標示（或替代方案） | 是 | 是 | 是 | 是 | — |
| UX-20b | 可以 | 是 | 是 | 是 | 是 | — |
| UX-21a | 不合格（3 < 8） | 是 | 是 | 是 | 是 | — |
| UX-21b | 合格 | 是 | 是 | 是 | 是 | — |
| UX-22a | 三種皆不套用；tab 切換不屬任何一種。依據：「tab 之間的切換不屬其中任何一種，因每個 tab 維護獨立堆疊」 | 否（替換整個堆疊） | 否（先答不屬，再附「若只能選一種，替換整個堆疊最接近」） | 是 | 否 | 題目設計 |
| UX-22b | 推入堆疊頂端 | 是 | 是 | 是 | 是 | — |
| UX-23a | 替換堆疊頂端 | 是 | 是 | 是 | 是 | — |
| UX-23b | 替換整個堆疊 | 是 | 是 | 是 | 是 | — |
| UX-24a | 不必適配（視覺風格類） | 是（判定句先寫「是」，同題後段寫「重讀後…可不適配」，以更正計） | 是 | 是 | 是 | — |
| UX-24b | 必須適配（modal dismiss 手勢） | 是 | 是 | 是 | 是 | — |
| UX-25a | 重建到設定選單的堆疊 | 是 | 是 | 是 | 是 | — |
| UX-25b | 只放目標畫面 | 是 | 是 | 是 | 是 | — |
| UX-26a | 分兩列 | 是 | 是 | 是 | 是 | — |
| UX-26b | 合併一列；列名寫出涵蓋類型（iOS 拒絕一次、Android 拒絕兩次） | 是 | 是 | 否（範例列名「系統對話框不再顯示」「永久拒絕」沒寫出涵蓋類型，與自己引的條文不符） | 否 | 讀者失誤 |
| UX-27a | Degraded mode；標示可能非最新 | 是 | 是（出處檔標錯，見異常） | 是 | 是 | — |
| UX-27b | Retry with feedback；狀態 + 手動重試 + 退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-28a | 可以 | 是 | 是 | 是 | 是 | — |
| UX-28b | 使用語音輸入時請求 | 是 | 是 | 是 | 是 | — |
| UX-29a | 需要確認；文案點出覆蓋未同步筆記、無法復原 | 是 | 是 | 是 | 是 | — |
| UX-29b | 仍需確認；未渲染時不執行 | 是 | 是 | 是 | 是 | — |

#### 失敗歸因

| 題號 | 歸類 | 具體句 |
|---|---|---|
| UX-01a | 讀者失誤 | 條文表清楚列「1-10s｜明顯等待｜進度指示 + spinner」，3 份卻都交出 400ms-1s。這個值正是 SKILL.md〈三段鏈式範例：本 skill 的產出必須是可被引用的具體值〉（p90 600ms）的結論。r3 直接寫「3 秒落在『400ms-1s』」，並把範例【驗證】段標為出處。題幹情境（按鈕觸發、p90、交 component-contract-design）和範例幾乎同構，範例結論蓋過了新輸入 |
| UX-02b | 讀者失誤 | 條文先說「排列依該場景下的實際使用頻率排列」，只在「使用頻率無法確定時」才以臨界路徑排序，先後有明文。3 份都把依據答成臨界路徑，而且都引了〈三段鏈式範例：無使用頻率資料時的排列依據〉結論「排列依臨界路徑判定」。該範例的鍵組（終端機 Esc／Tab／Ctrl／方向鍵）和題幹相同。修法：重測時換用與範例不同的鍵組 |
| UX-07a | 讀者失誤 | 條文「自然語言時兩者保留開啟」清楚。r1 把題幹「不希望內容被輸入法詞庫記住」擴大到校正與建議；r2 引了正解句，判定仍寫三項皆關。r3 正確 |
| UX-08b | 讀者失誤 | 條文「三值以上且切換會連動畫面其他區域增減元素時，拆成狀態顯示＋動作鈕」只有一個做法。r3 另附「或改用 switch 若設計成兩個獨立開關」，等於改寫題幹前提。r1、r2 正確 |
| UX-09a | 條文不清 | 第 2 項開頭「非互動的狀態指示不與動作按鈕同形混排 — 同列有按鈕時使用者預設整列可點，點到指示的「沒反應」被讀成壞掉」，理由句不分形態。合格形態「指示用明顯非按鈕形態（文字 chip、圓點）或移出動作列」接在「描邊 vs 實心不是可靠的互動性訊號」之後，讀起來像同形情境的補救，而不是「chip／圓點與按鈕同列即合格」。3 份都判不合格；r2 還讀了範例，在 (3) 列出兩種讀法後仍判不合格。明說「與同列有無按鈕無關」的句子只在〈三段鏈式範例：第 2 項〉【驗證】Q2 預期，屬答案鍵未標記的依據只在範例內 |
| UX-10b | 讀者失誤 | 條文「仍不足才設定最小寬度」清楚，題幹也已給定「已能在窄螢幕完整顯示」。r2 寫「仍須依最小寬度策略考慮」；r1 寫「不一定需要（取決於空間是否已足夠）」，沒有依題幹前提作答。r3 正確 |
| UX-12a | 讀者失誤 | 檔首不適用段明寫「畫面狀態的完整枚舉與退出路徑（狀態矩陣範疇，見 `screen-state-matrix.md`）」。r2 引了這句，判定卻寫「`interaction-feedback.md` 或其他相關 reference（如 gate-fallback、error-recovery）」；r3 另加「或 `gate-fallback.md`」。兩份都被題幹的「failed 狀態」帶到錯誤恢復或 gate 檔。沒有人把退出路徑寫進本節回饋表 |
| UX-14a | 讀者失誤 | r3 判定句同時寫「應列四個狀態」與「五個死胡同」，後者照抄〈填寫範例〉步驟 3「全空，五個死胡同」。r1、r2 正確 |
| UX-14b | 題目設計 | 題幹「failed 狀態的退出路徑欄寫「返回 → 首頁」是否合格？」沒有指明合格的標準是格式還是完整性。餵入的〈填寫步驟〉第 4 步又有「error 狀態通常應有「重試」和「返回」」。r1 在 (3) 明列兩種讀法，r1、r2 依完整性判不合格。但重試屬可用操作欄，不是退出路徑，所以讀者本身也有誤讀。修法：題幹改為「這條退出路徑的格式是否合格」 |
| UX-22a | 題目設計 | 題幹「應套用「替換整個堆疊」「推入堆疊頂端」「替換堆疊頂端」中的哪一種？」預設三者之一必然適用，條文卻是「tab 之間的切換不屬其中任何一種」。r1 被迫三選一；r2 先答不屬，又補「若限制只能選一種，「替換整個堆疊」最接近」。和第 1 批 VB-03a 同型。修法：選項加「以上皆非」，或改問「是否屬於三種堆疊操作之一」 |
| UX-26b | 讀者失誤 | 條文「列名寫出涵蓋的類型」清楚。r3 照引條文，給的列名範例卻是「系統對話框不再顯示」「永久拒絕」，沒有寫出 iOS／Android 兩個類型。r1、r2 正確 |

#### 三項條文風險的探針證據

| 風險 | 是否證實 | 證據 |
|---|---|---|
| UX-20a（「從未用過」的判定單位，答案鍵標為條文歧義） | 未證實 | 3 份都判「不可靜默隱藏、明確標示」，沒有人答成「對未用過的使用者隱藏」。r1、r3 沒有引範例，只憑條文「只適合使用者從未用過的功能 — 用過的功能突然消失會被當成 bug」就答出功能層級；r2 引了範例 Q1 預期。本批沒有讀出歧義的樣本。3/3 通過不足以證明歧義不存在，只能說本題幹（0.5% 用過）沒有誘發使用者層級讀法 |
| UX-06（「本地過濾」被讀成本地即低成本） | 未證實，證據受污染 | 3 份都判按送出或 debounce，沒有人選即時。但 r1、r2、r3 都引了〈三段鏈式範例：判斷搜尋成本的依據〉的「『本地』不等於『低成本』」。這個澄清只在範例裡，所以本批不能證明單讀條文不會誤判。重測前須先解決範例閱讀問題（見探針異常第 1 列） |
| UX-12a（〈畫面級回饋〉表「退出路徑｜每個中間狀態都必須有」與檔首歸屬衝突） | 預測的誤讀型態未證實；本題另以他型失敗 | 沒有人判「寫在本節回饋表」。r1、r2、r3 都引了〈三段鏈式範例：畫面級回饋只負責 UI 呈現欄〉的「權威值只在 `screen-state-matrix.md` 定義一處」，所以同樣受範例污染。實際失敗是 r2、r3 把「別的地方」指到 gate-fallback／error-recovery（見 UX-12a 歸因）。檔首「見 `screen-state-matrix.md`」有明文，不另列條文問題 |

#### 答案鍵疑誤

58 題的判定與獨立推導一致，沒有判定錯誤。有 2 處附註：
- **UX-06a 接受「debounce」**：餵入條文的觸發模式表中，debounce 的適用欄是「遠端搜尋的常見選擇」，本機高成本搜尋依表只對到「按送出」。「採按送出或 debounce」的結論出自範例【產出】。本批依答案鍵計分，r2 答 debounce 記通過，但這個計分點的依據部分只在範例內。
- **UX-09a**：答案鍵的判定正確，但沒有標記「圓點與按鈕同列仍合格」這個讀法的明文只在範例（見失敗歸因）。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 違反「跳過範例區塊」規則，大量引範例 | 之 1 的 r1、r2、r3 幾乎每題都引〈三段鏈式範例〉【產出】／【驗證】（例：01a r3、02b 三份、03a r1／r2、04a 三份、06a 三份、12a 三份、15a r1／r2）；之 2 的 r2 引範例（19a、20a、21b、22a、26b、29b）；之 2 的 r3 29a 引文「操作名稱給人的直覺印象…落差大」出自範例表。範例與條文在同一檔、同一節內，探針規則只靠文字要求跳過，實際無效。影響：之 1 的通過題不能證明單讀條文可答出；01a、02b、14a 的失敗則直接由範例錨定造成。建議：後續批次改餵已實體刪除範例區塊的節錄檔 |
| 答卷寫錯目錄 | 之 2 的 r1、r3 寫到兩個錯拼的 scratchpad 路徑（`graph_project_docs_manager`、`graph-project-docs_manager`） |
| 虛構或竄改引文 | 之 1 r1 05b「『退房日期須晚於入住日期』代表格式不固定」（條文沒有這句）；之 1 r1 07b 引文中出現自我更正的「否（內容為自然語言）——錯」；之 1 r1 02b 把題幹句當依據句 |
| 出處章節或檔名標錯 | 之 1 r1 12a 標成「`screen-state-matrix.md`〈畫面級回饋〉」；之 2 r2 27a／27b 把〈網路 gate〉標在 `error-recovery.md`（實際在 `gate-fallback.md`） |
| 判定理由方向寫反 | 之 1 r2 13b 寫「20 秒…會被後端的明確錯誤回應蓋過」，實際是 20 秒兜底會先蓋過後端錯誤 |
| 誤判題目轉述不符 | 之 2 r1 24a 把自己的誤判標成「題目與現文不符」 |
| 條件式問句改寫為建議 | 之 1 r3 11a 把「若規格選定 p95」寫成「應選定 p95」 |
### 第 2 批判定後的處置（PM，2026-09-15）

**探針餵入方法變更（影響閘門效度）**
- 第 2 批判定者發現：之 1 的 3 份答案幾乎每題都引用範例，之 2 的 r2、r3 也有。
- 原因：範例和條文在同一節內，「跳過範例」的文字指示沒有生效。
- 後果：這些題的通過不能證明條文本身讀得懂；UX-01a、02b、14a 更是被範例結論直接帶錯。
- 裁定：第 2 批起，餵入改為「刪除範例區塊的節錄檔」。節錄檔由出題者用腳本產生，放在 `probes/excerpt/`；題目的餵入欄只指向節錄檔。
- 第 1 批與第 1 批重測：兩位判定者都回報沒有讀範例的跡象，結果維持。

**計分口徑增補**：判定句附帶與條文不符的替代答案（「或…」），又沒有更正的，記不通過。採第 2 批判定者的口徑。

**第 2 批結果：47/58 題，整批重測**
- 範例污染使本批通過題不具證明力，改用節錄檔全部 58 題重跑。
- 11 題未通過一律改題：
  - 題目設計：UX-14b 改問「格式是否合格」；UX-22a 補「都不屬於」選項。
  - 讀者失誤：UX-01a、02b、07a、08b、10b、12a、14a、26b，依用戶裁示改題，前提改成條列。
- 答案鍵修正：UX-06a 的 debounce 只有範例支持，答案鍵改依條文表格。若條文不足以支持，併入 W3-446。

**W3-446 範圍增補**
- UX-09a：「圓點與按鈕同列仍合格」只寫在範例中，理由句沒有區分形態。
- UX-06a：本機高成本搜尋是否可用 debounce，條文表格只對應到「按送出」。
- UX-06、UX-12a、UX-20a 三項風險：受範例污染無法判定，W3-446 仍須逐項檢視條文。

**順序**：W3-446 落檔 → 出題者產節錄檔並改題 → 58 題以 3 份探針重跑 → 判定。
### 第 1 批改題重測判定（獨立判定者，2026-09-15）

**判定方法**：條文讀 HEAD `012abaf0`。題目基準 `f1eaffd6` 到 HEAD 之間，version-bootstrap 沒有變更；component-contract-design 只多了 `e2215fa8`（W3-448，3.9.10），在 step-1〈情況二〉「三訊號無法核對時」段補一句「提及本身不在上列三訊號之內，不據以判 C」。3 份 CCD 答案都引了這句，可見探針讀的是 HEAD。判定依 HEAD 條文。每題先依餵入範圍自行推出判定與依據句，再開答案鍵比對，最後逐份判 3 份答案（`probes/a/W3-417-b1r2-<skill>-r{1,2,3}.md`）。門檻 3/3。計分口徑沿用〈第 1 批重測判定〉，並依〈第 2 批判定後的處置〉增補兩條：讀者失誤記不通過；附帶與條文不符的替代答案、又沒有更正的，記不通過。答案鍵 27 則引文已用 `git grep -F` 在 HEAD 逐一核對，全部命中；其中「起點判定標「待決」」計數為 2，與答案鍵的註記相符。

**結果**：6／6。version-bootstrap 2／2，component-contract-design 4／4。

#### 逐題表：version-bootstrap（2/2）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| VB-03a-r2 | (1) 非 UI 類；(2) 不過三項前置；(3) 記錄呈現通道，交 foundation-design UI 維度判改寫產物，不得只記「非 UI 類」。依據：呈現通道表第二列「匯出文件版面（優先本列）」「非 UI 類，不過三項前置」與其動作欄；step-rationale「改由外部程式（PDF 閱讀器、試算表軟體等）另開檔案呈現，一律歸第二列，即使該檔案版面固定或排版複雜」 | 是 | 是 | 是 | 是 | — |
| VB-05a-r2 | 列四張：i18n、design-system、UX 審查、元件契約；PM 理由不成立。依據：PM 工作「元件庫依賴前三塊與元件契約為 `blockedBy`」；〈元件契約遞移依賴〉「去重原則的射程」段「此原則不適用於 Step 4.5 元件庫的 `blockedBy`」「故元件庫的 `blockedBy` 仍需並列前三塊與元件契約四項」 | 是 | 是 | 是 | 是 | — |

#### 逐題表：component-contract-design（4/4）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| CCD-01a-1-r2 | 待決。依據：「「無法核對」不等於「全否」，不得判 A」「起點判定標「待決」」 | 是 | 是 | 是 | 是 | — |
| CCD-01a-2-r2 | 不可；附錄路徑只能列為請求核對的理由。依據：HEAD「列為請求核對的理由；提及本身不在上列三訊號之內，不據以判 C」 | 是 | 是 | 是 | 是 | — |
| CCD-01a-3-r2 | 不可；於 NeedsContext 請求核對，回覆前不進 step 2。產出會寫進 spec 檔，不適用演練暫判 A 的例外。依據：「於 NeedsContext 請求有讀取權者核對三訊號，回覆前不進 step 2」 | 是 | 是 | 是 | 是 | — |
| CCD-11a-r2 | (1) 展開到「成員列的頭像 slot 放頭像變體 small」為止；(2) 圓點不寫；(3) 寫「逐格放成員列變體 default，數量依資料筆數」。側欄寫 slot 名。依據：〈格位起算〉「寫 slot 名，不另行編號」；〈巢狀容器〉三句 | 是 | 是（數量寫「依團隊成員人數」，屬答案鍵允許的同義形式；引文混入範例句，見範例污染） | 是（依據段引範例，見範例污染） | 是 | — |

#### 失敗歸因

無未通過題。前一輪 4 題的歸因與這一輪結果對照：
- VB-03a（題目設計，是非問句）：改成選擇式並編號後，3/3 通過。證實失敗來自問句形式，不是條文。
- VB-05a（條文不清）：W3-447 補上射程段後 3/3 通過，而且 3 份都引到射程段。題幹放入的「比照元件契約去重」陷阱沒有誘發誤判。
- CCD-01a（題目設計，一題三子問）：拆成三題後 3/3 通過。
- CCD-11a（讀者失誤）：前提改成條列後 3/3 通過。

#### 答案鍵疑誤

判定與獨立推導一致，沒有判定錯誤。有 1 處附註：
- **CCD-01a-2-r2 的判定者註已過期**：答案鍵寫在 W3-448 補句之前，所以註明「直接否定句只出現在範例【驗證】、需合讀推出」。HEAD 已有明文「提及本身不在上列三訊號之內，不據以判 C」，這條註記不再成立；判定本身不受影響。3 份都直接引了新句，沒有人回報需要自行假設。

#### 範例污染

| 答案 | 位置 | 引用的範例內容 | 是否影響通過 |
|---|---|---|---|
| CCD r3 | CCD-11a-r2 子問 2、3 | 在依據段明標「範例驗證」，引〈範例（巢狀容器）〉的兩條檢驗問句：「清單卡若內建固定圖示（非 slot），how 是否寫該圖示？」「資料驅動的格數（依資料筆數）是否以固定寫法標明…」 | 否 |
| CCD r2 | CCD-11a-r2 子問 1 | 引文「逐層展開到葉元件…直到被放入的元件不再有呼叫端須填的 slot 為止」標為〈巢狀容器〉段；前半「逐層展開到葉元件」出自同節範例判定表合格列（step-5 範例表），不在條文段 | 否 |

判定不受影響，理由有三：
1. 兩份的判定句都沒有出現範例的元件名（清單卡、徽章 slot、狀態徽章），結論也不是範例的結論，而是依題幹前提停在頭像元件。
2. 兩份都另外引了足以單獨支持判定的條文句（「元件自帶的子件不寫」「同質子件數量隨資料而變者…」「直到被放入的元件不再有呼叫端須填的 slot 為止」）。
3. r1 沒有讀範例，也答出同樣的三個子問。

限制：CCD-11a-r2 的 3 份答案中，只有 r1 是乾淨樣本。這個範例的結構（三層巢狀 slot、數量依資料筆數、自帶子件）與題幹同構，和第 2 批 UX-01a 的污染型態相同。本題條文可讀，是由 r1 單份證明的，不是 3/3 證明的。是否要改用節錄檔重跑這一題，由 PM 裁決。其餘 5 題 3 份答案都沒有範例引文；VB 兩題的餵入章節本身沒有範例區塊。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 讀出餵入範圍 | VB-03a-r2 的 r1、r2、r3 都引了 SKILL.md Step 2 的 Checkpoint 段（不在餵入的「UI 類判別段與呈現通道表」內）；r2、r3 另引「非 UI 類提案略過本檢查。」（在前置檢查表之後的段落）。答案由表第二列即可得出，不影響判定 |
| 虛構或竄改引文 | VB r3 03a 子問 3 依據 2：「判為 UI 類提案者須先確認…缺則先補齊；[此為正向路徑]…非 UI 類判別依…此原則不適用…」。「[此為正向路徑]」條文沒有；「此原則不適用」出自〈元件契約遞移依賴〉，不在〈Step 2 的 UI 前置檢查〉。同題依據 1 在表列中插入條文沒有的「檢查項」 |
| 出處標錯 | VB r2 03a 子問 2 把「非 UI 類提案略過本檢查。」標成「表格直後（第 101-102 行）」，實際在第 112 行；CCD r2 11a 見範例污染第 2 列 |
| 引文加粗 | CCD r3 01a-3、11a 在引文中自行加粗，原文沒有 |
### 第 1 批改題重測判定後的處置（PM，2026-09-15）

**b1r2 結果：6/6 通過**
- VB-03a、VB-05a、CCD-01a 三題全數通過，確認上一輪的失敗歸因正確。

**CCD-11a-r2 改用節錄檔重跑**
- 3 份答案中，r2、r3 引用了巢狀容器範例，只剩 r1 是乾淨樣本。
- 依〈第 2 批判定後的處置〉的範例污染口徑，本題通過不具證明力，改用刪除範例的節錄檔重跑 3 份，併入第 2 批重跑一起執行。

**第 1 批原始 46 題通過補做污染稽核**
- 原因：第 1 批原始判定當時沒有逐份檢查是否引用範例。
- 做法：派稽核者逐份比對探針引文是否出自範例區塊。受污染的題依同一口徑，改用節錄檔重跑。
### 第 1 批範例污染稽核（2026-09-15）

**對象**：〈第 1 批判定〉判為通過的 46 題（DSG 8、SDG 10、VB 9、CCD 19）。答案檔 12 份，`version-bootstrap-s2` 與 `component-contract-design-s1` 取自拼錯路徑的兩個目錄（同〈第 1 批判定〉記載）。

**範例區塊界定**：條文一律以 `git show 09bdb258:<path>` 核對。範例區塊指【輸入】【產出】【驗證】三段、「範例：」「**範例（…）**」起頭的段落，以及其中的表格與圍欄。以下三處不算範例：dart-style-guardian i18n〈Exceptions〉的程式碼圍欄（不在 【】 三段內，也不以「範例：」起頭）、component-contract-design path-a 步驟 1～5 的圍欄（是條文本體，題目有餵入）、version-bootstrap SKILL Step 5 條文句內的「例：」列舉。

**分類口徑**：先逐份判斷是否算乾淨樣本。這份答案沒有引用範例，或雖引用範例、但它自己引的條文句足以支撐判定，都算乾淨樣本。
- 乾淨：3 份都沒引用範例。
- 受污染但不影響：有引用範例，但 3 份都各自引了足以支撐判定的條文句。
- 受污染須重跑：判定依賴範例內容，或乾淨樣本少於 3 份。

#### 逐題分類

| 題號 | 分類 | 污染位置（份別：出處） | 支撐判定的條文句 |
|---|---|---|---|
| DSG-01a | 不影響 | s3：color-system〈顏色歸類判準〉【驗證】Q「『已刪除』提示…」 | 3 份皆引判準段首句「功能類型不因觸發它的前置操作是否具破壞性而改變」或 Three-Color 列項 |
| DSG-01b | 不影響 | s3：同上【驗證】Q | 同上；s1、s2 另引 Three-Color「errors」 |
| DSG-02a | 不影響 | s1、s2、s3：〈Theme 與 Token 判準〉【產出】決策表列與理由句；s3 另把【輸入】的「Theme 由 token 組裝而成」併入引文 | 3 份皆引判準段「對應的內建語意插槽存在時用主題插槽，不存在時用 token」 |
| DSG-02b | 不影響 | s1、s2、s3：同上決策表「未覆蓋」列與理由句 | 同上 |
| DSG-03a | 不影響 | s1、s3：〈字級判準〉【產出】決策表 | 3 份皆引 UIFontSizes 定義註解或判準段「卡片或對話框等容器元件內的標題對映 `titleLarge`」 |
| DSG-03b | 不影響 | s1、s3：〈字級判準〉【驗證】Q「兩個 token 是否因數值相同…」；s2：同 Q 的預期答案句「定義註解已區分文件階層與元件情境兩種語意」 | 3 份皆引判準段「數值相同不代表語意相同」與 headline4 對映句 |
| DSG-04a | 乾淨 | — （s3 的 `versionLabel` 引文是虛構的，不是範例內容，已列入〈第 1 批判定〉探針異常） | Exceptions 標籤＋數值分割句 |
| DSG-04b | 乾淨 | — | Exceptions「Technical identifiers - Error codes, keys」 |
| SDG-01a～05b（10 題） | 乾淨 | — （SDG-01b s1 轉引的「> 上例」引註段有餵入，不在範例三段內） | 各題答案鍵依據句 |
| VB-01b | 乾淨 | — | version-shift-sop「三問皆否：屬純程式邏輯…」 |
| VB-02b | **須重跑** | s1：兩則依據句都出自 version-shift-sop 範例二【驗證】（「判準 2 的成立條件是…」「若服務甲與服務乙改為同一次提交…」），沒有引判準表條文句；s2：另以同一【驗證】段作「逆向驗證」 | s2、s3 引了判準 2 問句「判定單位是發布或部署單位，不是儲存庫」；s1 沒有引，乾淨樣本 2 份 |
| VB-03b | 乾淨 | — | Step 2 呈現通道表第一列「含唯讀畫面」 |
| VB-06a | 乾淨 | — | Step 5「兩軸交集去重、聯集為完整覆蓋」 |
| VB-06b | 乾淨 | — | 同句與「避免 domain 規則只靠…被動覆蓋」 |
| VB-07a | 乾淨 | — | Step 6「或」句、task-splitting B、cognitive-load「> 2 須拆分」 |
| VB-07b | 乾淨 | — （s3「每個 ticket 應聚焦單一功能面」是虛構引文，不是範例內容） | task-splitting B；s1 另引 Step 6 |
| VB-08a | 乾淨 | — （s3 讀出餵入範圍的內容取自 task-splitting〈拆分策略速查〉，不是範例） | Step 6 PM 工作「共用檔案需整合票…」／task-splitting B |
| VB-08b | 乾淨 | — | task-splitting B「共用檔案的問題已合併到同一 ticket」 |
| CCD-01b | 乾淨 | — | step-1〈情況二〉「任一命中即為 C」 |
| CCD-02a | 乾淨 | — | path-a〈反向規則〉 |
| CCD-02b | 乾淨 | — | 同上例外句 |
| CCD-03a | 不影響 | s2：判定句的 Y 值「『載入中』狀態完成時」取自 step-3〈關卡五〉【產出】；s3：X、Y 值「格資料」「『載入中』狀態完成時」取自同段 | 3 份皆引固定寫法「不經服務：讀取的資料為 X，於 Y 時已載入；資料缺值時的表現見 Z」與「不得寫『同成功』」。判定只問形式，填入的值不影響判定 |
| CCD-03b | 乾淨 | — | 「所讀資料需按需讀取、可能等待者不適用本寫法，四段逐一填表現」 |
| CCD-04a | 乾淨 | — | step-5〈格位起算〉 |
| CCD-05a | 乾淨 | — | step-5〈覆蓋層〉 |
| CCD-05b | 乾淨 | — | 同上「屬觸發元件自帶者…不列」 |
| CCD-06a | 乾淨 | — | step-5〈疊加態〉 |
| CCD-06b | 乾淨 | — | 同上末句 |
| CCD-07a | 不影響 | s2：判定句的元件名「清單卡」取自〈非狀態進入情境〉範例【產出】 | 3 份皆引「不得為此新增獨立列」與「需要程式定位或聚焦者寫進該元件回饋契約『被程式定位』格」 |
| CCD-07b | 乾淨 | — | 「how 的列固定為狀態矩陣的列」（s3 以同段拆解句推出） |
| CCD-08b | 不影響 | s2：出處章節標成範例標題〈元件候選判定：總是／有時／僅單畫面共現〉，顯示讀到範例起頭行；引文本身是步驟 2 圍欄內的條文句 | 3 份皆引「兩集合互有對方沒有的列時…各自為候選」 |
| CCD-09b | 乾淨 | — | 步驟 4「單一子件的包裝…不是容器候選」 |
| CCD-10a | 不影響 | s2：引 step-3〈關卡二〉範例的 Given/When/Then 與 And 句 | 3 份皆引「進行中指示…疊加在事件結果所在區塊上、原內容全程可見時，指示消失後內容與事件前相同即答否」 |
| CCD-10b | 乾淨 | — （s2 標的章節名是條文粗體句，不是範例） | 「進行中狀態取代了該區塊的原內容…即答是」 |
| CCD-11b | 乾淨 | — | step-5〈巢狀容器〉「直到被放入的元件不再有呼叫端須填的 slot 為止」 |
| CCD-12a | 乾淨 | — | step-5〈遷移票〉段首 |
| CCD-12b | 不影響 | s2：出處標為〈遷移票〉範例，並寫成【輸入】「`features/login/`…」／【產出】「一票」。09bdb258 的範例輸入是 `features/project/`、`features/settings/`，沒有 login，這段是把題目輸入改寫成範例引文（虛構），但出處自稱範例 | 3 份皆引「『目錄』指直接含待改頁面檔的目錄」 |

#### 彙總

| 分類 | 題數 | 題號 |
|---|---|---|
| 乾淨 | 34 | DSG-04a、04b；SDG 全 10 題；VB-01b、03b、06a、06b、07a、07b、08a、08b；CCD-01b、02a、02b、03b、04a、05a、05b、06a、06b、07b、09b、10b、11b、12a |
| 受污染但不影響 | 11 | DSG-01a、01b、02a、02b、03a、03b；CCD-03a、07a、08b、10a、12b |
| 受污染須重跑 | 1 | VB-02b |

**須重跑清單**：VB-02b（version-bootstrap `references/version-shift-sop.md`〈契約檔案的界定〉判準 2，相反分支）。重跑時沿用原題與原餵入，並依〈第 1 批判定〉的 3/3 門檻重新判定。

**觀察**：受污染的 12 題全部集中在「範例緊接在判準段之後、同一標題下」的檔案（color-system、typography-system、step-3、step-5、version-shift-sop）。DSG 3 份答案都引了決策表或驗證問句。「跳過範例區塊」的指示在這種排版下擋不住探針。
### 第 1 批污染稽核後的處置（PM，2026-09-15）

**重跑範圍：12 題，改用節錄檔**
- 「須重跑」1 題：VB-02b。
- 「受污染但不影響」11 題：DSG-01a、01b、02a、02b、03a、03b；CCD-03a、07a、08b、10a、12b。

**為何「不影響」類也重跑**
- 稽核者判「不影響」的依據是「條文句足以單獨支撐」。
- PM 對 CCD-11a-r2 已定口徑：乾淨樣本少於 3 份即重跑。這 11 題的乾淨樣本都少於 3，其中 DSG 6 題為 0。
- 條文句足以支撐，是判定者的推論；乾淨樣本是讀者行為的實測。閘門採後者，與「探針為閘門」的用戶裁示一致。

**其餘 34 題**：乾淨，結果維持。

**稽核者的觀察**
- 受污染題全部出在「範例緊接判準段、同一標題下」的檔案：color-system、typography-system、step-3、step-5、version-shift-sop。
- 這個排版特徵已由節錄檔餵入解決，不另建票。
- 已記錄為 chained-examples.md 實證輪結束後的修訂候選：探針方法應規定以節錄檔餵入。

**執行**：併入節錄檔出題者的工作，輸出 `W3-417-b1x-<skill>.md`。
### 節錄檔重跑派發（PM，2026-09-15）

**題組**
- 節錄檔 42 份。出題者自檢兩項均通過：156 句依據全部逐字存在於節錄檔；被刪除的 857 行沒有任何一行出現在節錄檔中。
- 條文基準為 HEAD d8f36ee5。
- 題量：b2x ux 58 題、b2x CCD-11a 1 題、b1x 12 題。

**探針**
- b2x ux 兩檔，各跑 3 份。
- 其餘四個小檔合併：每份探針依序處理四檔，共 3 份。

**範圍外待處理：DSG-04b**
- 第 1 批判為乾淨通過，但作答依據的是 W3-438 之前的條文。
- W3-438 在 i18n-guidelines 新增〈錯誤碼與使用者可見錯誤訊息的交界〉，與題目「錯誤碼要不要進 i18n」相鄰。
- 閘門須以現行條文為準，DSG-04b 併入第 3 批出題時以節錄檔重跑，題幹不變。
### b1x 與 CCD-11a 判定（獨立判定者，2026-09-15）

**判定方法**：只讀 `probes/excerpt/` 節錄檔（基準 d8f36ee5），不讀原始 skill 檔。每題先依題目餵入的節錄檔自行推出判定與依據句，再開答案鍵比對，最後逐份判 3 份答案（`probes/a/W3-417-b1x-<skill>-r{1,2,3}.md`、`probes/a/W3-417-b2x-ccd-11a-r{1,2,3}.md`）。門檻 3/3。計分口徑沿用〈第 1 批改題重測判定〉，並依〈第 2 批判定後的處置〉：
- 讀者失誤記不通過。
- 附帶與條文不符的替代答案、又沒有更正的，記不通過。
- 越界（答案含答案鍵的污染檢查字串，或引用節錄檔以外的內容）記不通過。

答案鍵 29 則 ⟪⟫ 引文已用腳本比對全部節錄檔（忽略空白與換行），全部命中。

**結果**：13／13。DSG 6／6，CCD 5／5，VB 1／1，CCD-11a-r2 1／1。越界 0 次。

#### 逐題表

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 |
|---|---|---|---|---|---|
| DSG-01a | 按鈕 negative；確認訊息 positive。依據：〈Three-Color System〉「warnings, errors, destructive actions」「success states, confirmations」；〈顏色歸類判準〉「功能類型不因觸發它的前置操作是否具破壞性而改變」 | 是 | 是 | 是 | 是 |
| DSG-01b | negative，不改判。依據：〈顏色歸類判準〉「元件本身的功能類型」與同上句 | 是 | 是 | 是 | 是 |
| DSG-02a | 主題插槽。依據：〈Theme 與 Token 判準〉「對應的內建語意插槽存在時用主題插槽，不存在時用 token」 | 是 | 是 | 是 | 是 |
| DSG-02b | token。依據：同上 | 是 | 是 | 是 | 是 |
| DSG-03a | `titleLarge`。依據：〈字級判準〉「卡片或對話框等容器元件內的標題對映 `titleLarge`」 | 是 | 是 | 是 | 是 |
| DSG-03b | `headline4`，不可任選。依據：「頁面內的階層子節標題對映 `headline4`」「數值相同不代表語意相同」 | 是 | 是 | 是 | 是 |
| CCD-03a | 三段用固定寫法「不經服務：讀取的資料為 X，於 Y 時已載入；資料缺值時的表現見 Z」，不得寫「同成功」。依據：方法論〈失敗路徑覆蓋〉末段 | 是 | 是 | 是 | 是 |
| CCD-07a | 不新增列；寫進該元件回饋契約「被程式定位」格。依據：〈非狀態進入情境〉「不得為此新增獨立列」「需要程式定位或聚焦者寫進該元件回饋契約「被程式定位」格」 | 是 | 是 | 是 | 是 |
| CCD-08b | 各自為候選（非合併、非 slot）。依據：步驟 2「兩集合互有對方沒有的列時，兩者都不是對方的一部分，各自為候選」 | 是 | 是 | 是 | 是 |
| CCD-10a | 否。依據：〈關卡二〉「疊加在事件結果所在區塊上、原內容全程可見時，指示消失後內容與事件前相同即答否」 | 是 | 是 | 是 | 是 |
| CCD-12b | 一張。依據：〈遷移票〉「「目錄」指直接含待改頁面檔的目錄，不是其上層目錄」 | 是 | 是 | 是 | 是 |
| VB-02b | 不成立。依據：判準 2「是否有消費方位於另一個獨立發布或部署的單位」「判定單位是發布或部署單位，不是儲存庫。」 | 是 | 是 | 是 | 是 |
| CCD-11a-r2 | (1) 展開到「成員列的頭像 slot 放頭像變體 small」為止；(2) 圓點不寫；(3)「逐格放成員列變體 default，數量依資料筆數」。依據：〈巢狀容器〉三句 | 是 | 是（數量寫「依團隊成員人數」，答案鍵允許的同義形式） | 是（(3) 附「或…數量依團隊成員人數」，屬同義形式，不與條文衝突） | 是 |

#### 失敗歸因

無未通過題。

#### 越界檢查

對 12 份答案逐一 grep 答案鍵列出的全部污染檢查字串（DSG：「已刪除」「按鈕文字顏色為主要品牌色」「兩個 token 是否因數值相同」`colorScheme.primary`；CCD：「格資料」「載入中」「清單卡」「帶目標進入」「訊息前進組」「輔助連結」Given／When／Then、「原 5 筆」`features/project/`、`features/settings/`；CCD-11a：「逐層展開到葉元件」「徽章 slot」「狀態徽章」；VB：「服務甲」「服務乙」），另加「範例」「SKILL.md」「.claude/」「判斷準則」「docs/」。
- 命中 1 處，不計越界：CCD r3 CCD-03a 判定句「資料缺值時的表現見 清單卡的內容政策」。「清單卡」是 CCD-07a 的檢查字串，但 CCD-03a 題幹本身寫「清單卡的內容政策寫…」，屬引用題幹。CCD-07a 的 3 份判定句都沒有出現範例元件名。
- 其餘 0 命中。所有依據句的出處都標節錄檔名或其段落。「只讀…」限定段落的題（CCD-07a、08b、12b、11a）引文都落在限定段落內。
- 越界：0 次。

#### 答案鍵疑誤

判定與獨立推導一致，無判定錯誤。CCD-08b 答案鍵已註「單一畫面內共現」優先句的另一條路徑（兩條路徑結論都是不合併、非 slot）。CCD r2 同時引了這兩句，判定取「各自為候選」，(3) 答「否」，沒有察覺兩句的先後關係；結論在答案鍵允許範圍內，不影響計分。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引文竄字 | CCD r2 CCD-12b：「互相阻擋」，節錄檔原文為「互相阻塞」 |
| 引文刪字 | DSG r2 DSG-03a：「`titleLarge   // 20.rsp …」少了 `UIFontSizes.` 前綴 |
| 引文加粗 | DSG r2、CCD r2、CCD-11a r2、VB r2、VB r3、CCD r3 的部分引文自行加粗，原文沒有 |
| 引號改寫 | DSG r1、CCD r1、CCD r3 把原文「」改成『』 |
| 值填入 | CCD r2 CCD-03a 的 Y 值寫「進入頁面時」，題幹是「進入『正常』狀態時」。X、Y、Z 不計分，也不是範例值 |
| 自行假設 | CCD r1、r2 CCD-08b 假設「甲」「乙」是不同畫面，屬題幹表述未明寫，與答案鍵的讀法一致 |
〈b1x 與 CCD-11a 判定〉探針異常表更正：「引文加粗」列誤列「CCD r3」，CCD r3 的引文沒有自行加粗，應為「DSG r2、CCD r2、CCD-11a r2、VB r2、VB r3」。計分不受影響。
### b2x ux 判定（獨立判定者，2026-09-15）

**判定方法**：條文讀節錄檔（`probes/excerpt/`，HEAD d8f36ee5 產生，範例區塊已刪除）。每題先依該題餵入的節錄檔自行推出判定與依據句，再開答案鍵（`probes/key/W3-417-b2x-ux-design-evaluation-{1,2}.md`）比對，最後逐份判 6 份答案（`probes/a/W3-417-b2x-ux-design-evaluation-{1,2}-r{1,2,3}.md`）。門檻 3/3。計分口徑沿用〈第 2 批判定〉，並依〈第 2 批判定後的處置〉：讀者失誤記不通過；判定句附帶與條文不符的替代答案、又沒有更正的，記不通過；子問漏答記不通過。

**越界檢查方法**：兩份 ux 答案鍵沒有附「範例污染檢查字串」（只有 `key/W3-417-b2x-ccd-11a.md` 有）。改用機械比對代替：(1) 取 `probes/tools/removed/` 中 28 份 ux 被刪範例文字，去掉節錄檔也有的片段，以 22 字元視窗比對每份答案的每題，另掃「範例」「【輸入】【產出】【驗證】」「三段鏈式」與第 2 批已知污染結論（「600ms」「五個死胡同」「不等於」「權威值只在」）；(2) 抽出依據句引號內片段，逐段比對全部 ux 節錄檔。

**結果**：49／58。之 1 為 23／30，之 2 為 26／28。越界 0 次。

#### 逐題表：之 1（23/30）

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-01a | 1-10s，進度指示 + spinner；不可只寫要有回饋；標明統計量 | 是 | 是 | 是 | 是 | — |
| UX-01b | 100-400ms，動畫過渡；不用 spinner；仍須交值 | 是 | 是 | 是 | 是 | — |
| UX-02a | 臨界路徑；Tab、`;` 放拇指處 | 是 | 是 | 是 | 是 | — |
| UX-02b | 依頻率；Esc 最外側 | 是 | 否（依臨界路徑） | 否（依臨界路徑，Esc 放拇指處） | 否 | 讀者失誤 |
| UX-03a | visiblePassword；三項全關；protocol spec 階段 | 是 | 是 | 是 | 是 | — |
| UX-03b | 一般文字；不關；不適用安全清單 | 是 | 是 | 是 | 是 | — |
| UX-04a | 一般文字鍵盤 | 是 | 是 | 是 | 是 | — |
| UX-04b | 數字鍵盤 | 是 | 是 | 是 | 是 | — |
| UX-05a | 格式驗證，on blur | 是 | 是 | 是 | 是 | — |
| UX-05b | 交叉驗證，on submit | 是 | 是 | 是 | 是 | — |
| UX-06a | 按送出；本機不足以選即時 | 否（即時） | 否（即時，判定句自相矛盾） | 是 | 否 | 讀者失誤 |
| UX-06b | 即時；來源不改變判定 | 是 | 是 | 是 | 是 | — |
| UX-07a | 學習關；校正、建議保留開啟；iOS 顯式記錄風險 | 否（校正、建議皆關） | 是 | 是 | 否 | 讀者失誤 |
| UX-07b | 三項皆關 | 是 | 是 | 是 | 是 | — |
| UX-08a | segmented control | 是 | 是 | 是 | 是 | — |
| UX-08b | 拆狀態顯示＋動作鈕 | 是 | 是 | 是 | 是 | — |
| UX-09a | 合格 | 否 | 否 | 是 | 否 | 條文不清 |
| UX-09b | 不合格；描邊 vs 實心不足 | 是 | 是 | 是 | 是 | — |
| UX-10a | 不可省略號；先縮格式，仍不足才設最小寬度 | 是 | 是 | 是 | 是 | — |
| UX-10b | 不須設最小寬度；讓步規則路由 CCD〈空間不足策略〉 | 否（仍須設定） | 否（視讓步情況） | 否（視後續內容變化） | 否 | 題目設計 |
| UX-11a | 不足，須記錄統計量；p95 2.5s → 進度指示 + spinner | 否（對到 > 10s） | 否（對到 > 10s） | 是 | 否 | 讀者失誤 |
| UX-11b | 不可 | 是 | 是 | 是 | 是 | — |
| UX-12a | 寫在 `screen-state-matrix.md`；本節負責 UI 呈現形式 | 是 | 是 | 是 | 是 | — |
| UX-12b | 屬本檔；spinner（形狀優先）；1-10s 進度指示 + spinner | 否（附「至少 spinner」） | 是 | 是 | 否 | 讀者失誤 |
| UX-13a | 略大於 60 秒；恢復 idle + 錯誤訊息 | 是 | 是 | 是 | 是 | — |
| UX-13b | 不合格 | 是 | 是 | 是 | 是 | — |
| UX-14a | 4 狀態；全空＝死胡同；只寫「返回」缺目標未定義 | 是 | 是 | 是 | 是 | — |
| UX-14b | 從 enum 取；格式合格 | 是 | 是 | 是 | 是 | — |
| UX-15a | 需要 initializing；網路不影響 | 是 | 是 | 是 | 是 | — |
| UX-15b | 不需要，一般 loading | 是 | 是 | 是 | 是 | — |

#### 逐題表：之 2（26/28）

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-16a | 可逆，由「關閉勿擾」提供 | 是 | 是 | 是 | 是 | — |
| UX-16b | 不可逆；「關閉面板」仍滿足退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-17a | 獨立成列；兩畫面各一列；退出至少返回或離開此畫面；記錄設定入口與清除時機 | 是 | 是 | 否（漏答退出路徑子問） | 否 | 讀者失誤 |
| UX-17b | 不屬 | 是 | 是（以條件 1 作答，答案鍵允許） | 是 | 是 | — |
| UX-18a | 只給重試不足；重啟 app 不合適；須含退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-18b | 不合適；配資料層行動 | 是 | 是 | 是 | 是 | — |
| UX-19a | 固定間隔並設上限；重試狀態三件事 | 是 | 是 | 是 | 是 | — |
| UX-19b | 指數退避 + jitter；倒數與立即重試按鈕 | 是 | 是 | 是 | 是 | — |
| UX-20a | 不可靜默隱藏；明確標示或替代方案 | 是 | 是 | 是 | 是 | — |
| UX-20b | 可以 | 是 | 是 | 是 | 是 | — |
| UX-21a | 不合格（3 < 8） | 是 | 是 | 是 | 是 | — |
| UX-21b | 合格 | 是 | 是 | 是 | 是 | — |
| UX-22a | (D) 都不屬於 | 是 | 是 | 是 | 是 | — |
| UX-22b | 推入堆疊頂端 | 是 | 是 | 是 | 是 | — |
| UX-23a | 替換堆疊頂端 | 是 | 是 | 是 | 是 | — |
| UX-23b | 替換整個堆疊 | 是 | 是 | 是 | 是 | — |
| UX-24a | 不必適配 | 是 | 是 | 是 | 是 | — |
| UX-24b | 必須適配 | 是 | 是 | 是 | 是 | — |
| UX-25a | 重建到設定選單的堆疊 | 是 | 是 | 是 | 是 | — |
| UX-25b | 只放目標畫面 | 是 | 是 | 是 | 是 | — |
| UX-26a | 分兩列 | 是 | 是 | 是 | 是 | — |
| UX-26b | 合併一列；列名寫出 iOS／Android 兩類型 | 否（判定寫分兩列，列名另附「平台權限限制」） | 否（另附「永久拒絕」） | 否（另附「永久拒絕」） | 否 | 條文不清 |
| UX-27a | Degraded mode；標示可能非最新 | 是 | 是（出處標錯，見異常） | 是（出處標錯，見異常） | 是 | — |
| UX-27b | Retry with feedback；狀態 + 手動重試 + 退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-28a | 可以 | 是 | 是 | 是 | 是 | — |
| UX-28b | 使用語音輸入時請求 | 是 | 是 | 是 | 是 | — |
| UX-29a | 需要確認；文案點出覆蓋未同步筆記、無法復原 | 是 | 是 | 是 | 是 | — |
| UX-29b | 仍需確認；未渲染時不執行 | 是 | 是 | 是 | 是 | — |

#### 失敗歸因

| 題號 | 歸類 | 具體句 |
|---|---|---|
| UX-02b | 讀者失誤 | 條文「排列依該場景下的實際使用頻率排列」在前，「使用頻率無法確定時」才退回臨界路徑。題幹已給頻率，r2 仍在假設欄寫「此統計不是排列的決策依據」，r3 依臨界路徑把 Esc 放拇指處 |
| UX-06a | 讀者失誤 | r1、r2 都引了「本地資料若運算量大（如大量記錄的全文搜尋）仍屬高成本，適用『按送出』」，判定仍寫即時。r1 稱 50 萬筆全文模糊比對屬低成本；r2 同一判定句先寫「應採即時」、再寫「資料在本機不足以直接判定採即時」 |
| UX-07a | 讀者失誤 | 條文「自然語言時兩者保留開啟」明確。r1 把「不希望被詞庫記住」擴大到自動校正與輸入建議，依據句沒有引這句 |
| UX-09a | 條文不清 | W3-446 補句後仍有 2/3 判不合格（第 2 批 3/3）。第 2 項開頭的理由「同列有按鈕時使用者預設整列可點，點到指示的『沒反應』被讀成壞掉」不分形態；豁免「兩者擇一即合格、與是否和按鈕同列無關」接在同一長句的尾端。r1 引文截在「或移出動作列」，沒讀到豁免；r2 引到「兩者擇一即合格」，仍判同列違反「移出動作列」 |
| UX-10b | 題目設計 | 3/3 沒有依題幹前提作答（第 2 批 2/3），三份都以計數日後可能變長為由保留最小寬度，r3 明列「99/100」。題幹用動態計數「12/30」當作「已完整顯示」的前提，但單一值顯示完整，不代表這個欄位的寬度需求已定。修法：改用長度固定的文字，或在題幹寫明「內容長度固定」 |
| UX-11a | 讀者失誤 | 題幹 p95 為 2.5 秒，條文表 1-10s 列是「進度指示 + spinner」。r1、r2 都對到「> 10s｜進度百分比 + 預估時間 + 取消選項」 |
| UX-12b | 讀者失誤 | r1 判定句寫「進度指示 + spinner（但此處無預知的進度百分比，所以至少spinner）」，把進度指示讀成進度百分比，再降為只要 spinner。這是與表列不符的附帶替代答案，沒有更正 |
| UX-17a | 讀者失誤 | 條文「退出路徑至少要有『返回』或『離開此畫面』一項」在餵入範圍內，r3 漏答這個子問。題幹四問沒有編號，r1、r2 都答全 |
| UX-26b | 條文不清 | 3/3 都另附「永久拒絕」「平台權限限制」這類總稱作為列名選項。條文「列名寫出涵蓋的類型」沒有說清楚：要逐一寫出成員類型，還是一個涵蓋性總稱就算。r1 另有判定句寫「應分成兩列」，與自己依據句的「故應合併為一列」矛盾 |

#### 與第 2 批的對照

- 第 2 批未通過、本批通過：UX-01a、08b、12a、14a、14b、22a。
- 兩批都未通過：UX-02b、07a、09a、10b、26b。
- 本批新增未通過：UX-06a、11a、12b、17a。
- 答案鍵的兩項條文風險：UX-06 有 2/3 判即時，沒有人判 debounce，W3-446 補句沒有擋住這一型；UX-12a 3/3 答 `screen-state-matrix.md`，沒有人答寫在本節。UX-20a 3/3 通過，沒有使用者層級的讀法。

#### 答案鍵疑誤

58 題的判定與獨立推導一致，沒有判定錯誤。有 1 處附註：兩份 ux 答案鍵沒有附「範例污染檢查字串」，本批改用上方的機械比對，結果為 0 命中。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 讀出餵入範圍 | 之 1 r1 13a 引「逾時後轉入 error」，出自〈畫面級回饋〉節錄檔，不在 13a 的餵入內；答題內容是節錄檔條文，不算越界 |
| 出處標錯 | 之 2 r2、r3 27a 把「呈現快取內容時須標示內容可能不是最新」標為 Degraded-mode.md，實際在〈Gate 類型與各自的設計重點〉節錄檔 |
| 拼接引文 | 之 2 r2、r3 19b 把指數退避項的「（使用者可跳過等待）」接到間隔策略段句尾；之 2 r2 17a 在引文中自加「[設定入口][清除時機]」 |
| 判定與依據矛盾 | 之 1 r2 06a；之 2 r1 26b |
| 日文字形 | 之 2 r1 26b「系統対話框」 |
| 同義附註，不扣分 | 之 1 r1 03a「或等義的結構化內容鍵盤型」、r2 04b「或專用的數字鍵盤」 |
| 判定用詞偏弱，以通過計 | 之 1 r2 14a 子問 2 寫「導航缺口」、r3 寫「還需要設計補充」，都沒寫出「死胡同」，但都寫出須補退出路徑 |
### b2x ux 判定後的處置（PM，2026-09-15）

**b2x 結果**
- ux 49/58，越界 0。
- b1x 與 CCD-11a 13/13，越界 0。

**用戶裁示：讀者失誤是內容問題**
- 事實：UX-06a、11a、12b、17a 上一輪 3/3，這輪條文與題目都沒變卻失敗。PM 提出這代表 haiku 讀者本身有雜訊。
- 用戶裁示：「那這就不是讀者失誤的問題，確實是內容的問題」。
- 據此調整：
  - 閘門維持 haiku、3/3。
  - 判定歸因為「讀者失誤」的失敗，不再只改題，改為追查條文哪裡讓讀者讀歪，並修改條文。
  - 條文要寫到最弱的讀者也讀不歪。
  - 題目設計問題仍然改題。

**9 題處置：開 W3-449 修 ux-design-evaluation 條文**

| 題號 | 判定者看到的讀歪點 |
|---|---|
| UX-09a | 例外條件接在長句尾端，開頭的理由句卻適用所有形態 |
| UX-26b | 「列名寫出涵蓋的類型」沒說要逐一列出，還是可用總稱 |
| UX-06a | 讀者引對了「仍屬高成本，適用按送出」卻選即時搜尋；結論與表格的對應關係不夠直接 |
| UX-02b | 有頻率資料時仍依臨界路徑排列；條件分支的先後次序不夠顯眼 |
| UX-07a | 自然語言欄位把自動校正與建議都關閉，漏讀保留開啟的條件 |
| UX-11a | p95 = 2.5 秒被歸到大於 10 秒的帶，時間帶邊界的寫法容易錯位 |
| UX-12b | 讀者自行加上「至少 spinner」，表格組合「進度指示＋spinner」的讀法不夠明確 |
| UX-17a | 題目的四個子問沒有編號，屬題目設計；同時檢查條文是否也有並列未編號的問題 |
| UX-10b | 題目設計：計數器長度可能變化，題幹改為固定長度文字 |

**完成條件**：W3-449 落檔後，這 9 題用節錄檔重跑（10b 改題），3/3 才通過。

**已通過題的處理**：上一輪通過、這輪才失敗的 4 題證明通過本身也有雜訊。已通過的題不回頭重跑；各批判定完後，再評估是否整體重跑一次。

**後續批次**：沿用本口徑。
更正上一則〈b2x ux 判定後的處置〉「已通過題的處理」：原句「各批判定完後，再評估是否整體重跑一次」沒有綁定觸發條件，改為已決策——第 5 批判定完成後、W2 開工前，所有已通過題用節錄檔整體回歸重跑一輪（haiku，3/3）。回歸中失敗的題依本口徑修改條文。這一輪回歸屬於 W2 開工閘門的一部分。
### 第 3 批出題後的處置（PM，2026-09-15）

**第 3 批規模**：foundation-design 22 則範例（原盤點 19，漏列 3），test-assertion-design 6 則，共 56 題，加 DSG-04b 1 題。兩項自檢皆通過。

**探針**：foundation-design-1（28 題，C-01～C-14）不涉及標記項，先派 3 份。foundation-design-2、test-assertion-design-1、dsg-04b 等 W3-450 修補完成、DSG-04b 改題後再派。

**依據只在範例內**：FD-20a、FD-20b（另有字面衝突）、TAD-03a 共 3 則，轉 W3-450。

**DSG-04b**
- 依現行〈交界〉，答案鍵已由「不需 i18n」翻轉為「只顯示錯誤碼屬違規」，與 W3-436 用戶裁示一致。
- 題幹為是非問句，正解需分兩層作答，與 VB-03a、UX-22a 失敗同型。依題目設計口徑改為選擇式後再派。

**i18n-guidelines〈交界〉補盤點**
- 該節由 W3-438 新增，含 Violation／Fix 對照，依 W3-437 判準屬範例，原盤點未列。
- 閘門範圍是全部範例，因此補入盤點，出 2 題（新輸入、相反分支），與 DSG-04b 改題同批處理。
### b3 foundation-design-1 判定（獨立判定者，2026-09-15）

**判定方法**：沿用〈b2x ux 判定〉。每題先只讀該題餵入的節錄檔（`probes/excerpt/`，HEAD e19b5cb4），自己推出判定與依據句。之後開答案鍵 `key/W3-417-b3-foundation-design-1.md` 比對，再逐題判 3 份答案 `a/W3-417-b3-foundation-design-1-r{1,2,3}.md`，須 3/3 才通過。口徑依〈b2x ux 判定後的處置〉：讀者讀歪記為條文問題，不再歸因「讀者失誤」。另沿用兩條計分規則：
- 整個子問在判定句中沒有作答，記不通過。只漏了子問內的某個元素，不扣分。
- 判定句附帶與條文或題幹條件不符的內容，又沒有更正，記不通過。

**答案鍵引文核對**：答案鍵共 71 段 ⟪⟫ 引文，去掉空白後逐段比對節錄檔，71 段都逐字存在。

**越界檢查**：
- (1) 答案鍵附的 14 條污染檢查字串，外加「範例」「【輸入】【產出】【驗證】」。只有 r3 FD-11a 出現「三張地基票」。這是依題幹推出的張數（建三張地基票：design-system、UX 審查、元件庫），不是 C-10 範例的內容，不算越界。
- (2) `tools/removed/` 中 40 份非空的被刪文字，先去掉節錄檔或題目裡也有的片段，再以 22 字元視窗比對（忽略空白與標點）。命中的片段都是「依據句」標籤接上節錄檔條文原句，不是範例特有文字。
- 越界 0 次。

**結果**：19／28。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| FD-01a | 不可；先確認該處確實承擔該產物 | 是 | 是 | 是 | 是 | — |
| FD-01b | 可，附位置；不建票 | 否（漏答子問 2；另把「確認記錄」列為必附） | 否（漏答子問 2） | 是 | 否 | 條文讀歪點 |
| FD-02a | 以畫布為準；不寫數值，指向 token 名 | 否（子問 2 答「是」，接著又寫「而非複述新的數值」） | 是 | 是 | 否 | 條文讀歪點 |
| FD-02b | 以規格為準；不刪，標漂移送 PM | 是 | 是 | 是 | 是 | — |
| FD-03a | 萃取當下歸納為離散具名階 | 是 | 是 | 是 | 是 | — |
| FD-03b | 不可；逐值命名值不變，收斂另開票同批建立 | 是 | 是 | 是 | 是 | 見答案鍵疑誤 |
| FD-04a | 不成立；取與形態無關部分，不整條跳過 | 是 | 是 | 是 | 是 | — |
| FD-04b | 不可填改寫產物，改標待定 | 是 | 是 | 是 | 是 | — |
| FD-05a | 已存在於 X；判準層標無判準＋框架缺口票 | 是 | 是 | 是 | 是 | — |
| FD-05b | 不能；產物層依「該產物本專案有沒有，照四種答案填」 | 是 | 否（未答依據，直接給「無或改寫產物」） | 否（未答依據，直接判「無」） | 否 | 條文讀歪點 |
| FD-06a | 不構成；不可填無 | 是 | 是 | 是 | 是 | — |
| FD-06b | 照預設；必附：無 | 是 | 否（判定句未答附帶；只在依據句引了該格「無」） | 是 | 否 | 條文讀歪點 |
| FD-07a | 無；理由＋重評條件 | 是 | 是 | 是 | 是 | — |
| FD-07b | 待定；缺什麼＋補票 | 是 | 是 | 是 | 是 | — |
| FD-08a | 不缺席；落在被盤點 repo | 是 | 是 | 是 | 是 | — |
| FD-08b | 待定：權威缺席；補權威票或指定替代權威 | 是 | 是 | 是 | 是 | — |
| FD-09a | 答案鍵：命中 | 是 | 否（判不命中） | 是 | 否 | 條文讀歪點（另見答案鍵疑誤） |
| FD-09b | 其餘列；逐維度定產物交 version-sequencing | 否（判規劃波之後） | 是 | 是 | 否 | 條文讀歪點 |
| FD-10a | 規劃波進行中；地基票不計入 | 是 | 是 | 是 | 是 | — |
| FD-10b | 規劃波之後；補判未涵蓋產物，不重排既有票 | 否（漏答「不做什麼」） | 是 | 是 | 否 | 條文讀歪點 |
| FD-11a | 3 張：design-system、UX 審查、元件庫 | 是 | 是 | 是 | 是 | — |
| FD-11b | 1 張 | 是 | 是 | 是 | 是 | — |
| FD-12a | 不通過 | 是 | 是 | 是 | 是 | — |
| FD-12b | 通過 | 是 | 是 | 是 | 是 | — |
| FD-13a | 缺 ticket 系統＋步驟 4 與權威缺席時建票動作不成立 | 是 | 是 | 否（另加形態 4 的四欄決策記錄） | 否 | 條文讀歪點 |
| FD-13b | 不另建 | 是 | 是 | 是 | 是 | — |
| FD-14a | 權威訂的測試設計審查者；不可任選 | 是 | 是 | 是 | 是 | — |
| FD-14b | 不可填張三；退至本框架既有審查代理人 | 否（另附「或該維度的權威審查角色」，題幹已寫權威未訂） | 是 | 是 | 否 | 條文讀歪點 |

#### 失敗歸因

| 題號 | 歸類 | 讀歪點 |
|---|---|---|
| FD-01b | 條文讀歪點 | 「已存在於 X」的後果分在兩處：表格「必須附帶」欄只寫位置；「確認該處確實承擔該產物，不建票」寫在工作流 code block 步驟 3 的同一行箭頭後，確認動作和不建票兩項並列、沒有編號。r1 把「確認」讀成必附項；r1、r2 都引了「不建票」，但沒有把它當成子問 2 的判定。題目子問 1 內含兩問，放大了漏答 |
| FD-02a | 條文讀歪點 | 〈畫布漂移〉寫「以畫布為準並更新規格」，「引用 token 名而非數值」的主詞卻是「契約填寫時」。專講規格改寫後指向 token 名的句子，只出現在 examples.md 的判準來源引言。讀者從「更新規格」推出「是，寫出新值」，再接上契約的 token 規則，判定句因此自相矛盾 |
| FD-05b | 條文讀歪點 | 形態 3 產物層寫成二元存在問句「該產物本專案有沒有」，又說「照四種答案填」，沒有指明是五種答案中的哪四種。讀者把「沒有 seeder」直接對到答案「無」，跳過了「無」須證明失效不存在的要求 |
| FD-06b | 條文讀歪點 | 五種答案表中，「照預設」列的「必須附帶」欄填的是「無」，與答案形態「無」同一個字。r2 引了該格，判定句卻沒有把它當成「不需附帶」的答案 |
| FD-09a | 條文讀歪點 | 「既有文件**無法回答維度表任一格的產物**」是否定句加「任一」，有兩種讀法：一格答不出就命中，或每一格都答不出才命中。條文沒有寫明量詞。r2 讀成後者，本判定者的獨立推導也得出後者 |
| FD-09b | 條文讀歪點 | 「其餘」列只用「以上皆不命中」反向定義，沒有正面描述；「規劃波之後」列卻加粗「仍由本 skill 驅動」，並寫「這是真實 repo 最常見的狀態」。題幹寫「已有相當程度的實作」，r1 依時間語感把它歸入「之後」，沒有核對「該版票已建」這個判別條件 |
| FD-10b | 條文讀歪點 | 「規劃波之後」角色格把做什麼（補判）、不做什麼（不重排既有票）、狀態評語（最常見、不是收手）三句接在一起，沒有分項。禁止句夾在中間，r1 只摘了「仍由本 skill 驅動」。題目也把「做什麼、不做什麼」放在同一個子問 |
| FD-13a | 條文讀歪點 | 移植前置條件第四項把「形態 1、形態 4……以及下方的降級記錄」並列為落在決策文件上的內容；降級段又沒有說明缺 ticket 系統時產物改落在哪裡。r3 用形態 4（skill 缺席時產物改為文件形式的四欄記錄）補上這個空缺，但形態 4 的觸發條件是 skill 缺席，不是 ticket 系統缺席 |
| FD-14b | 條文讀歪點 | 附註先寫「兩個來源是先後順序而非任選」，同一段後句又寫「仍須落在上述兩個既有角色之一」；工作流步驟 5 也用「或」並列兩個來源。「之一」和「或」把有先後的順序讀回任選。r1 引的正是「之一」句，並附上題幹已排除的權威角色 |

#### 答案鍵疑誤

- **FD-09a**：答案鍵判「命中（任一格答不出即命中）」，本判定者的獨立推導是「不命中」（「無法回答任一格」＝一格都答不出）。兩種讀法在現文下都成立，不判答案鍵錯，但修條文時須由 PM 決定本意，再寫明量詞。旁證：該列角色欄寫「它們假設已有規格」，較接近「全部答不出」的讀法。
- **FD-03b**：判定者註要求子問 2「至少須答出『逐值命名、值不變』」。這個元素其實是子問 1「不可同時收斂」的內容。r1、r2 已答「不可同時收斂／應分兩步」與「收斂另開票同批建立」，本判定依獨立推導記通過。建議判定者註改為：子問 1 須含「值不變／分兩步」其一，子問 2 須含「另開票」。
- 其餘 26 題的判定與獨立推導一致。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 讀出餵入範圍（內容為節錄檔條文，不算越界） | r2 FD-04a 引維度表資料庫列（該題限讀〈權威缺席時〉）；r1、r2、r3 FD-07b 引〈改寫產物是主路徑〉（未餵入）；r3 FD-13a 引〈權威缺席時〉形態 4（未餵入，且是失敗原因） |
| 依據句混入己見 | r1 FD-01b「條文要求超過位置填寫，需附帶確認內容」寫在依據句欄 |
| 自行補完 | r2 FD-14b 舉「code-reviewer、bay-quality-auditor」為審查代理人實例，節錄檔沒有這些名稱 |
| 禁用詞 | r2 FD-07b「文檔」 |
| 引文中略去關鍵句 | r1 FD-03b 引文以「……」略去「第 2 步仍逐值命名、值不變」 |
### b3 foundation-design-1 判定後的處置（PM，2026-09-15）

**結果**：19/28 通過，越界 0。

**FD-03b 計分**
- 採判定者的推導，記通過。
- 理由：答案鍵的判定者註把子問 1 的內容（逐值命名、值不變）歸到子問 2 去要求，屬答案鍵誤植。r1、r2 已答出子問 2 的核心：分兩步、收斂另開票。

**9 題讀歪點**
- 開 W3-452 修 foundation-design 條文。
- W3-452 設 blockedBy W3-450：兩票同 skill，避免同檔並行寫入。
- FD-09a 的本意由 PM 裁定為「一格答不出即命中」，依據為 examples-process 範例驗證段。這個本意原本只寫在範例中，W3-452 要把量詞寫進條文。

**W3-449 的 UX-26b 讀法選擇**
- 撰寫者把「列名寫出涵蓋的類型」定為「逐一列出、排除總稱」，這是替兩種讀法做選擇。
- PM 接受。理由：用總稱時，無法核對涵蓋範圍。
- 已向用戶說明，用戶可推翻。
### b3 dsg 判定（獨立判定者，2026-09-15）

**判定方法**：沿用〈b3 foundation-design-1 判定〉。三題餵入相同，都是 3 份 i18n 節錄檔（`probes/excerpt/`，HEAD 5f1616de）。先只讀節錄檔，逐一推出各子問的判定。之後開答案鍵 `key/W3-417-b3-dsg.md` 比對，再逐題判 3 份答案 `a/W3-417-b3-dsg-r{1,2,3}.md`，3/3 才通過。兩條計分規則照舊：整個子問沒答記不通過；附帶與條文不符的內容且沒有更正，記不通過。

**獨立推導與答案鍵比對**：三題推導分別為 DSG-04b 1B／2B、DSG-05a 1B／2B／3B、DSG-05b 1B／2B／3A，與答案鍵的預期判定全部一致，沒有發現答案鍵誤。

**答案鍵引文核對**：答案鍵共 18 段 ⟪⟫ 引文。去掉空白、表格分隔符與粗體標記後，逐段比對節錄檔，18 段都逐字存在。

**探針引文核對**：r1 有 10 段「」引文，r2 有 11 段，r3 有 16 段。以「...」切段後比對節錄檔與題目，只有 1 段不在其中：r3 DSG-05b 子問 3 的「呈現層以 `ErrorHandler.getUserMessage(...)` 轉譯後顯示訊息，畫面上不顯示錯誤碼」。這段自標為題目前提，是把前提略寫後引用，內容沒有超出題目，不算越界。

**越界檢查**：
- (1) 答案鍵附的 6 條污染檢查字串，外加「ISBN」，在 3 份答案中都沒有出現。
- (2) `tools/removed/` 中 3 份 i18n 被刪文字，先去掉節錄檔與題目裡也有的片段，再以 22 字元視窗比對（忽略空白與標點），命中 0。
- 越界 0 次。

**結果**：3／3。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| DSG-04b | 1 (B) 不進 ARB；2 (B) 不合格，改顯示轉譯後訊息 | 是 | 是 | 是 | 是 | — |
| DSG-05a | 1 (B)；2 (B) 不在豁免；3 (B) 參數化翻譯 | 是 | 是 | 是 | 是 | — |
| DSG-05b | 1 (B)；2 (B) 呈現層轉譯；3 (A) 合格 | 是 | 是 | 是 | 是 | — |

判定者註套用情形：
- DSG-05a：3 份答案都沒有註明〈Violation 3〉沒有餵入，都依〈Exceptions〉說明段「不得以字串插值直接拼接整句」答出參數化翻譯，依註記通過。沒有人把「：」歸入 Formatting characters。
- DSG-05b：沒有人另外討論日誌字串，子問 3 都判合格。出題者附記的題目設計風險 2 沒有發生。

#### 失敗歸因

無失敗題。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 依據句混入己見 | r1 DSG-05a 子問 3（「程式碼使用了字串插值直接拼接…違反上述規範」）；r1 DSG-05b 子問 3（「題目中三層設計符合此規範…」）；r3 DSG-05b 子問 3（把前提略寫後放進依據句） |
| 引文檔名縮寫 | r3 把節錄檔名寫成「錯誤碼與交界.md」，引文內容可在〈交界〉節錄檔中逐字找到 |
| 引文中略去內容 | r3 DSG-05a 子問 3 兩段引文以「...」略去中段，被略去的部分不影響判定 |
### b3 dsg 判定與 b2y 出題後的處置（PM，2026-09-15）

**b3 dsg**：3/3 通過，越界 0。DSG-04b、DSG-05a、DSG-05b 全數過關；dart-style-guardian 已沒有待重跑題。

**b2y 出題風險：條文例示與題目輸入同構**
- 現象：W3-449 修條文時新增的例示，與失敗題的輸入幾乎相同。
  - UX-26b：iOS／Android 拒絕次數。
  - UX-02b：必經鍵。
  - UX-12b：「配對中」。
- 後果：探針可能是照抄例示答對，而非讀懂判準。這樣的 3/3 不能證明條文可讀。
- 裁定：這三題改用與條文例示不同構的新輸入，判定點不變。

**方法規則（本輪起適用）**
- 條文修補之後的重跑題，出題者要比對題目輸入與修補新增的例示；兩者同構就換輸入。
- 修條文的撰寫者可以為讀歪點加例示，但例示不得照搬探針題目的輸入。這條寫進後續修條文票的派發 context。

**UX-12b 的 2-10s 列**：表後已有裁決順序「形狀 > 觸發 > 時長」，不構成條文衝突，本輪不修。如果探針仍依時長判定，就把「裁決順序句位於表後」當成讀歪結構處理。
### b2y 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3 foundation-design-1 判定〉，並依〈b3 dsg 判定與 b2y 出題後的處置〉加查同構抄答。每題先只讀餵入節錄檔（`probes/excerpt/`，HEAD 647be9bb）自行推出判定，再開答案鍵 `key/W3-417-b2y-ux-design-evaluation.md` 比對，最後逐題判 3 份答案 `a/W3-417-b2y-ux-design-evaluation-r{1,2,3}.md`，3/3 才通過。兩條計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過。

**獨立推導與答案鍵比對**：9 題推導與答案鍵預期判定全部一致，沒有發現答案鍵誤。

**答案鍵引文核對**：37 段 ⟪⟫ 引文，去掉空白與標點後逐段比對節錄檔，37 段都逐字存在。

**探針引文核對**：r1 38 段、r2 47 段、r3 41 段「」引文，以「...」切段後比對節錄檔與題目。不在其中的片段分三類，都沒有超出節錄檔內容：
- 作答者自擬的列名（r1、r2、r3 UX-26b）。
- 表格列攤平後引用（r1 UX-17a 設定入口／清除時機兩列；r3 UX-17a「設定入口、清除時機」）。
- 放進引號的改寫（r3 UX-07a「iOS 無法單獨關閉個人化學習」，原文為「此平台無法單獨關閉學習」；r3 UX-10b「文字有版面保障」，原文為「關鍵回饋文字要有版面保障」）。

**越界檢查**：`tools/removed/` 中 ux-design-evaluation 被刪文字，先去掉節錄檔與題目裡也有的片段，再以 22 字元視窗比對（忽略空白與標點），3 份答案命中 0。越界 0 次。

**結果**：8／9。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-09a | 合格；圓點屬明顯非按鈕形態，同列不影響 | 是 | 是 | 是 | 是 | 見同構抄答 |
| UX-26b | (1) 合併一列；(2) 列名逐一列出 MDM 政策與家長監護 | 是 | 是（「MDM 停用、家長監護限制相機」） | 是（「MDM 政策停用或家長監護設定關閉」） | 是 | — |
| UX-06a | 按送出；本機不足以選即時 | 是 | 是 | 是 | 是 | 見同構抄答 |
| UX-02b | (1) 依頻率；(2) Tab 放外側 | 是 | 是 | 是 | 是 | — |
| UX-07a | (1) 學習明確關閉；(2)(3) 校正、建議保留開啟；(4) 規格記錄無法單獨關閉與接受風險 | 否（(2)(3) 判關閉） | 是 | 是 | 否 | 條文讀歪點 |
| UX-11a | 不足，須寫明統計量；p95 落 1s～10s 帶，進度指示 + spinner | 是 | 是 | 是 | 是 | — |
| UX-12b | (1) 屬本檔；(2) spinner；(3) 1s～10s 帶，進度指示 + spinner | 是 | 是 | 是 | 是 | — |
| UX-17a | 須獨立成列；兩畫面各自需要；退出至少返回或離開；設定入口與清除時機 | 是 | 是 | 是 | 是 | — |
| UX-10b | (1) 不須最小寬度；(2) 不由本項決定，路由 ccd〈空間不足策略〉 | 是 | 是 | 是 | 是 | — |

判定者註套用情形：
- UX-12b：沒有人附「至少 spinner」「只用 spinner」，沒有人依「等待較長（2-10s）」列選 skeleton。處置段預設的「裁決順序句位於表後」讀歪結構沒有發生。r1 子問 1 另寫「退出路徑須在狀態矩陣中決定」，與〈畫面級回饋〉表後段一致，不扣分。
- UX-10b：沒有人答「不一定、視空間而定」。r1 判定句寫「應不需要」，判定方向明確，記通過。

#### 失敗歸因

| 題號 | 歸類 | 讀歪點 |
|---|---|---|
| UX-07a | 條文讀歪點 | 〈IME 安全清單〉的非 secret 規則寫了兩次，表前段只到「改依內容是否為自然語言決定」，沒有寫出自然語言時的結果；「自然語言時兩者保留開啟」只在表後段。兩段之間夾著唯一的設定表，表的欄只有「secret 類欄位設定」，三列全是關閉。r1 引了表前段，得不到結果，就改用表格補上，寫出「表格顯示個人化學習『明確關閉』，自動校正『關閉』，輸入建議『關閉』」，沒有核對欄名限 secret。W3-449 補的表後句沒有被讀到，原因是表前段看起來已經完整 |

#### 同構抄答跡象

- **UX-09a**：題幹輸入「實心小圓點」與判準 (a) 例示「圓點」相同，未在 b2y 換輸入。3 份答案都是把圓點對到例示字面（r2「符合『明顯非按鈕形態』中的『圓點』」），沒有依「非按鈕形態」本身推導。本題 3/3 只能證明讀者讀到「與是否同列無關」，不能證明讀懂形態判準。歸題目設計。
- **UX-06a**：題幹「50 萬筆詞條、全文模糊比對」與 W3-449 改寫句的例示「如大量記錄的全文搜尋」近乎同構。r2、r3 的推導就是對到例示（「50 萬筆詞條的全文模糊比對屬於運算量大的情況」）。本題判定點（成本而非資料位置）有依判準作答，但「運算量大」的辨認靠例示完成。歸題目設計。
- **UX-11a（輕度）**：題幹「平均 350ms、p95 2.5 秒」與條文例示「平均 300ms、p95 卻 2s」數值相近。條文例示沒有給出門檻帶，探針仍須自行對表，判定不受影響，只記錄。
- PM 已換輸入的 UX-26b、02b、12b：沒有抄例示跡象。3 份 26b 都自擬列名，沒有沿用 iOS／Android 句型。r3 02b 寫「即使 Tab 是完成核心操作的必經鍵」是把題幹前提套進條文用語，屬推導。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 依據句混入己見 | r2 UX-06a、26b、02b、10b，r3 UX-09a，在依據句欄後接推論句 |
| 引號內改寫 | r3 UX-07a「iOS 無法單獨關閉個人化學習」；r3 UX-10b「文字有版面保障」 |
| 漏子問內元素 | r1、r2 UX-07a 子問 4 未寫「不以關閉全部輸入輔助代替」（元素層，不扣分） |
| 多餘假設 | r3 UX-07a 標「假設症狀描述符合 confidential」，題幹已給「不希望被詞庫記住」，條文以此為判準，不需假設 |
### b2y 判定後的處置（PM，2026-09-16）

**結果**：8/9，越界 0，答案鍵無誤。

**處置**

| 題 | 狀況 | 處置 |
|----|------|------|
| UX-07a | 條文讀歪：非 secret 規則寫成兩處，表前段沒寫結果；中間表格欄名只適用 secret，三列都是關閉，讀者就拿表格補出結果 | 開 W3-454 修條文，落檔後重跑 |
| UX-09a | 題目「實心小圓點」與條文判準 (a) 例示「圓點」同詞，3 份都照例示字面對應 | 依同構規則作廢本題通過，換輸入重跑 |
| UX-06a | 題目「50 萬筆詞條全文模糊比對」與 W3-449 例示「大量記錄的全文搜尋」同構 | 依同構規則作廢本題通過，換輸入重跑 |
| UX-26b、02b、10b、11a、12b、17a | 3/3，無抄例示跡象（11a 輕度同構，判定仍需自行對表，維持通過） | 通過 |

**第 2 批 ux 現況**
- 除 UX-07a、09a、06a 外皆已通過，其中含 b2x 首輪 49 題與 b2y 的 6 題。
- 這 3 題待 W3-454 落檔、出題者換輸入後，用節錄檔重跑。
### 撰寫者自評階段 1 不可靠：派發約束增補（PM，2026-09-16）

**現象**：W3-450 和 W3-452 兩位撰寫者都自評「未新增判準」，但 PM 核對 diff 後發現疑似新判準，已分別開 W3-450.1、W3-452.1 交 WRAP 複核。W3-450.1 確認其中兩處是新判準，且各有一種情況判錯。

**原因**：撰寫者為了消除讀歪點，會順手補上射程延伸或新的處置規則，而且把「讓讀法變清楚」和「只補依據」視為同一件事。目前沒有機械核對點，只能靠 PM 讀 diff 攔下。

**約束**：本輪起，修條文票的派發 context 一律要求 Solution 附「新增句出處表」，每一句新增或改寫的句子各列一行：

| 新增句 | 出處（檔名與節名，git grep -F 命中，不得是範例） | 讀法是否改變（是／否） |

- 出處欄空白，或讀法欄為「是」的句子，視同階段 2：草稿寫入 NeedsContext，不直接落檔。
- PM 驗收時逐行核對這張表，不再依賴撰寫者的整體自評。
### b2z 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b2y 判定〉。每題先只讀餵入節錄檔（`probes/excerpt/`，HEAD 9605e678）自行推出判定，再開答案鍵 `key/W3-417-b2z-ux-design-evaluation.md` 比對，最後逐題判 3 份答案 `a/W3-417-b2z-ux-design-evaluation-r{1,2,3}.md`，3/3 才通過。計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過。另查同構抄答。

**獨立推導與答案鍵比對**：3 題推導與答案鍵預期判定一致，沒有發現答案鍵誤。

**答案鍵引文核對**：13 段 ⟪⟫ 引文，去掉空白與標點後逐段比對節錄檔，13 段都逐字存在。

**探針引文核對**：r1 19 段、r2 12 段加 8 行 blockquote、r3 16 段引文，比對節錄檔與題目。不在其中的片段有 2 處，都在 r1，都沒有超出節錄檔內容：
- 放進引號的改寫：r1 UX-07a 子問 4 判定句「此平台無法單獨關閉個人化學習」，原文為「此平台無法單獨關閉學習」。
- 作答者自擬語句加引號：r1 UX-09a 假設欄「3px 直條是否夠明顯地傳達『不可點』訊號」。

**越界檢查**：`tools/removed/` 中 ux-design-evaluation 被刪文字，先去掉節錄檔與題目裡也有的片段，再以 22 字元視窗比對（忽略空白與標點），3 份答案命中 0。越界 0 次。

**結果**：2／3。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-07a | (1) 學習明確關閉；(2)(3) 自然語言，校正與建議保留開啟；(4) 規格記錄無法單獨關閉與接受風險，不以關閉全部輸入輔助代替 | 是 | 是 | 是 | 是 | — |
| UX-09a | 合格；3px 直條屬明顯非按鈕形態，同列不影響 | 否（判不合格） | 是 | 否（判不合格） | 否 | 條文讀歪點 |
| UX-06a | 按送出；手機上執行不足以選即時 | 是 | 是 | 是 | 是 | — |

判定者註套用情形：
- UX-07a：沒有人把 (2)(3) 答成關閉，W3-454 改寫後表前段加的「下表只適用 secret 類欄位」與表後段結果句都被讀到。r2、r3 子問 4 判定句未寫「不以關閉全部輸入輔助代替」，但依據句已完整引用，且沒有答「關閉全部輸入輔助」，屬元素層，不扣分。
- UX-06a：r1 判定句為「應採按送出，不應採即時。Debounce 在此場景價值較低」，不是「按送出或 debounce」的替代答案，記通過；但 r1 在 (3) 另列「應採 Debounce」為可能判定，沒有引用 Debounce 列適用欄「遠端搜尋」來排除。這只記錄，不計分。
- UX-09a：答案鍵判定者註要求區分「括號是例示還是窮舉」。r1、r3 都沒有明寫「直條不在列舉中」，但都把括號內的例子當成「明顯」的比較基準。詳見失敗歸因。

#### 失敗歸因

| 題號 | 歸類 | 讀歪點 |
|---|---|---|
| UX-09a | 條文讀歪點 | 第 2 項有兩處結構讓讀者判不合格。(1) 判準 (a)「明顯非按鈕形態（文字 chip、圓點）」沒有寫明括號是例示還是窮舉，也沒有定義什麼算「明顯」，只有括號內兩個例子可參照。r1 的假設是「參照了判準列舉的具體例子（圓點、文字 chip）的明確度水準」，推出直條「未達成明顯非按鈕形態」。r3 寫「節錄檔未提供充分視覺細節判斷」。(2) 理由句「同列有按鈕時使用者預設整列可點，點到指示的『沒反應』被讀成壞掉」緊接在 (a)(b) 之後，而且寫成無條件句，沒有限定只適用同形指示。讀者因此把它讀成反向判準，蓋過前面的「與是否同列無關」。r3 的判定句直接以這句理由判不合格，r1 也寫「正是判準所警示的問題」 |

次要因素（題目設計，不改歸類）：題幹「按下這條直條不會有任何反應」與理由句「點到指示的『沒反應』」同義。r1 把兩句對上，這為讀歪點 (2) 提供了觸發詞。若只為交代指示不可互動，可改寫成「非互動」，避免與理由句同義。

#### 同構抄答跡象

- 三題換輸入後，都沒有照抄例示的跡象。
- UX-07a：3 份都沒有把申訴內容對應到「日記、私人備忘」，而是依「不希望被記住」與「完整中文句子＝自然語言」兩條判準各自推導。
- UX-06a：3 份都沒有把影像辨識對應到「大量記錄的全文搜尋」例示。r2 把「高於本地過濾、記憶體內資料集」寫成假設，r3 直接判為「運算量大」，兩者都是依成本判準推導。
- UX-09a：r2 直接判直條屬明顯非按鈕形態，沒有對應例示字面。r1、r3 的問題方向相反：不是抄例示，而是把例示當成門檻，歸入上方讀歪點 (1)。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引號內改寫 | r1 UX-07a 子問 4「此平台無法單獨關閉個人化學習」 |
| 依據句混入己見 | r1 UX-09a、UX-06a；r3 UX-07a、UX-06a：在依據句欄後接推論句 |
| 多餘假設 | r2 UX-07a 標「假設屬 confidential 但不屬 secret」。題幹已給「不想讓輸入法記住」與「不能用來登入或付款」，條文以此為判準，不需要假設 |
| 子問未編號 | r1 UX-06a 判定句沒有分開回答「是否足以選即時」，答案寫在依據句欄後的推論（「不足以判定為低成本」），記已答 |
### b3x TAD 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b2z 判定〉。每題先只讀該題餵入的節錄檔（`probes/excerpt/`，HEAD a375b87c），自行推出判定，再比對答案鍵 `key/W3-417-b3x-test-assertion-design-1.md`。之後逐題判 3 份答案 `a/W3-417-b3x-test-assertion-design-1-r{1,2,3}.md`，3/3 才通過。計分規則照舊：
- 整個子問沒答，記不通過。
- 題目明確提問的部分完全沒有作答，也記不通過。
- 判定句附帶與條文或題幹不符的內容且沒有更正，記不通過。

另查越界（含跨題餵入）與同構抄答。核對腳本：`probes/tools/b3x_tad_check.py`。

**獨立推導與答案鍵比對**：12 題的推導都與答案鍵預期判定一致，沒有發現答案鍵誤。

**答案鍵引文核對**：27 段 ⟪⟫ 引文，去掉空白與標點後，逐段比對「該題的餵入節錄檔」（不是全部節錄檔），27 段都逐字存在。

**探針引文核對**：r1 25 段、r2 33 段、r3 24 段，逐題比對該題的餵入節錄檔與題幹。不在其中的片段如下：
- 跨題餵入：r2 TAD-04a、r3 TAD-04a 引〈確定性斷言的基礎形態〉的邏輯不變式定義句，但該題只餵〈9 類型問題斷言分類決策表〉。
- 引號內加前綴：r3 TAD-02a、05a、05b 把「類型 N 的識別信號是」「類型 6 的設計原則：」放進引號。其後內容逐字存在。
- 引號內拼接：r3 TAD-03a 把段名「類型 9 識別信號與聚焦性的關係：」直接接上 (1) 項，略去中間的例外句。
- 自擬語句加引號：r2 TAD-06a「長度為 5」。

**越界檢查**：
- `tools/removed/` 下 3 份 test-assertion-design 被刪文字檔都是空檔（1 byte），與答案鍵「節錄檔未作任何刪改」一致，因此被刪文字命中 0。
- 答案鍵列的 6 條範例污染字串，3 份答案命中 0。
- 讀了非餵入節錄檔的有 3 處：r2 TAD-04a、r3 TAD-04a（引文），以及 r3 TAD-05a（判定句用「邏輯不變式」，這個詞只出現在非餵入節錄檔）。

**結果**：10／12。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| TAD-01a | 不放入；未提及屬性不構成面向 | 是 | 是 | 是 | 是 | — |
| TAD-01b | 2 個面向 | 是 | 是 | 是 | 是 | — |
| TAD-02a | (1) 僅類型 1；(2) 類型 5 信號不成立（斷言在 `join()` 之後） | 是 | 是 | 是 | 是 | — |
| TAD-02b | (1) 類型 1＋5；(2) 逐一處置：類型 1 門檻移至隔離執行環境，類型 5 等待最終狀態再斷言 | 否（未答各類型處置） | 是 | 是 | 否 | 題目設計 |
| TAD-03a | 不屬過度集中；門檻只作篩檢，11 條件同一宣告 | 是 | 是 | 是 | 是 | — |
| TAD-03b | 屬過度集中；排序斷言移到另一測試案例 | 是 | 是 | 是 | 是 | — |
| TAD-04a | 不保留全結構快照；用欄位／屬性斷言 | 是 | 否（答「邏輯不變式或欄位斷言」） | 否（答「邏輯不變式」） | 否 | 題目設計 |
| TAD-04b | (1) 設置；(2) 並存、互不替代 | 是 | 是 | 是 | 是 | — |
| TAD-05a | (1) 類型 6；(2) 隨機源可控，斷言 52 張／無重複性質，不斷言特定輸出 | 是 | 是 | 是 | 是 | — |
| TAD-05b | (1) 不替換；(2) 只驗長度、字元集 | 是 | 是 | 是 | 是 | — |
| TAD-06a | (1) 結構驗證，不以長度／筆數代替；(2) 邏輯不變式，不以固定值代替 | 是 | 是 | 是 | 是 | — |
| TAD-06b | 功能正確性 | 是 | 是 | 是 | 是 | — |

判定者註套用情形：
- TAD-02a：沒有人以「沒有 `await`」判類型 5 成立。r2 引了條文的 `await` 例句，但判定依據是 `join()` 位置，屬推導。
- TAD-03a：沒有人依識別信號字面判命中。3 份都引〈類型 9 識別信號與聚焦性的關係〉(1) 項。
- TAD-06b：沒有人判成邏輯不變式或結構驗證（陣列長度）。
- TAD-05a：r3 判定句把「52 張、無重複」稱為「邏輯不變式」，這個詞來自非餵入節錄檔。判定點（可控隨機源、斷言性質、不斷言特定輸出）完整，而「牌組保持 52 張」可讀成前後數量不變的關係，不構成與條文不符，記通過；越界另記。
- TAD-02b r1：判定 (2) 只寫「應逐一套用各類型對應的設計原則分別處置」。依據句只引兩類型的識別信號，沒有引設計原則。題目明問的「各類型的處置是什麼」整段缺席，不是元素層缺漏，記不通過。若 PM 改依元素層處理，本題改記通過（11/12）。
- TAD-04a r2：「邏輯不變式斷言或欄位斷言」是並列替代答案，並列的一方來自非餵入節錄檔，也不在類型 8 設計原則內，記不通過。

#### 失敗歸因

| 題號 | 歸類 | 說明 |
|---|---|---|
| TAD-02b | 題目設計 | 子問 (2) 在同一編號下問兩件事：擇一還是逐一，以及各類型處置是什麼。r1 答了前半，引的條文句「分別處置」讀起來像已經答完，後半就漏掉了；條文沒有被讀歪。可改為把「各類型的處置」拆成 (3) |
| TAD-04a | 題目設計 | 題幹問「用什麼形態斷言」，其中「形態」與節錄檔〈確定性斷言的基礎形態〉的節名同詞；情境「total 等於加總」又是數學關係，正好對上該節邏輯不變式的定義「驗證數學/邏輯關係」。r2、r3 都跳到非餵入節錄檔，改答另一個分類軸。餵入的類型 8 設計原則「業務邏輯驗收用欄位/屬性斷言」字面無歧義，r1 直接照答。可改為「應以欄位／屬性斷言還是全結構快照驗收業務邏輯」 |

次要觀察（不改歸類、不計分）：類型 8 的「欄位/屬性斷言」與〈確定性斷言的基礎形態〉的 4 種確定性類型是兩條分類軸，條文中沒有互相指明關係。同時讀兩節的讀者，會把其中一軸當成另一軸的替代答案。本批 12 題是一次交給探針，逐題餵入限制沒有被遵守；這個交界在實際使用時（讀者通常讀整份 SKILL）同樣存在。是否開票由 PM 裁決。

#### 同構抄答跡象

- 換過輸入的 TAD-02a、02b、06b，都沒有照抄例示的跡象。02a、02b 依「等待機制之前／之後」判定，沒有人以 `await` 字面作答。06b 依「具體值」判功能正確性，沒有對到「顯示前 N」字面。
- TAD-05b：題幹「兌換碼」與條文例示「安全權杖」不同詞，3 份都依「明確要求輸出不可預測」判定。
- 其餘 7 題的字面重疊只限判準用語，沒有看到照例示字面作答。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引錯欄位 | r2 TAD-05a 把類型 6「問題本質」欄的「斷言由不可控隨機源驅動的特定輸出值」標為識別信號。〈歸類判準〉明定問題本質欄不作為判準；本題識別信號也成立，判定不受影響 |
| 引號內加前綴／拼接 | r3 TAD-02a、03a、05a、05b |
| 依據句混入己見 | r1 TAD-02a (2)、r3 TAD-02a：在依據句欄寫推論句 |
| 自擬語句加引號 | r2 TAD-06a「長度為 5」 |
| 多餘附加 | r2 TAD-05b「隨機源保持不可控」（條文為「不替換」，不矛盾） |
### b3x FD-1 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b2z 判定〉。
- 每題先只讀餵入節錄檔（`probes/excerpt/`，HEAD a375b87c），自行推出判定與依據句。
- 再開答案鍵 `key/W3-417-b3x-foundation-design-1.md` 比對，最後逐題判 3 份答案 `a/W3-417-b3x-foundation-design-1-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹條件不符的內容且沒有更正，記不通過。

**獨立推導與答案鍵比對**：7 題推導與答案鍵預期判定一致，沒有發現答案鍵誤。

**答案鍵引文核對**：24 段 ⟪⟫ 引文，去掉空白後逐段比對節錄檔，24 段都逐字存在。

**越界檢查**：
- (1) 答案鍵 6 條污染檢查字串，外加「範例」「【」：3 份答案命中 0。
- (2) `tools/removed/` 中本題組相關的 21 份被刪文字，先去掉節錄檔與題目裡也有的片段，再以 22 字元視窗比對（忽略空白、標點與符號），3 份答案命中 0。
- 越界 0 次。

**同構抄答**：沒有照抄例示的跡象。
- FD-02a：3 份判定句都沒有帶入例示「圓角 10、畫布量到 7」，r3 (1) 寫的是題幹的 12／20。
- FD-09a：3 份都引「一格即命中」判準句並對到題幹「只有 DevOps 一格」。這是判準句的直接套用，不是對例示字面。

**結果**：5／7。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| FD-01b | (1) 可；附位置；(2) 不建票 | 是 | 是 | 是 | 是 | — |
| FD-02a | (1) 以畫布為準並更新規格；(2) 否，指向 token 名 | 是 | 是 | 是 | 是 | — |
| FD-06b | 照預設；不需附帶 | 是 | 是 | 是 | 是 | — |
| FD-09a | 命中（一格即命中） | 是 | 是 | 是 | 是 | — |
| FD-09b | (1) 其餘；(2) 逐維度定產物交 version-sequencing | 是 | 是 | 是 | 是 | — |
| FD-10b | (1) 規劃波之後；(2) 做：補判未涵蓋產物；不做：不重排既有票 | 是 | 否（判規劃波進行中；不做什麼答「材料未指明」） | 是 | 否 | 條文讀歪點 |
| FD-14b | 不可填張三；退至本框架既有審查代理人 | 是 | 否（判定句主句寫「應填該維度權威來源指定的審查角色」，(4) 又概括為「……或本框架既有審查代理人」，題幹已寫權威未訂） | 是 | 否 | 條文讀歪點 |

判定者註套用情形：
- FD-01b：沒有人把「確認」列為必附項；3 份都明確作答「不建票」。
- FD-02a：r1 (1) 只寫「以畫布為準」未寫「並更新規格」，(1) 問的是以哪方為準，屬元素層，不扣分。
- FD-09b：答案鍵風險 1（「無相當程度的既有實作缺口」兩種斷句）沒有在探針上發作。3 份都判其餘列，(3) 都答「否」。但 3 份都沒有逐列排除首列，也沒有引「是否已有相當程度的既有實作與本列判定無關」；只有 r3 引了「由上而下」。本題通過不能當作斷句風險已消除的證據。

#### 失敗歸因

| 題號 | 歸類 | 讀歪點 |
|---|---|---|
| FD-10b | 條文讀歪點 | 「該版票」指哪些票，只在 examples-process〈規劃波進行中與之後〉以排除句定義：標題寫「地基票不算『該版票已建』」，正面範圍「問的是 `version-bootstrap` pipeline 自身的產出」塞在排除句中段的破折號子句裡。SKILL.md 表的判別欄只寫「該版票已建」「該版尚未建票」，沒有寫出計入的是哪些票。r2 抓住「地基票」與「該版尚未建票」兩個詞，把排除方向讀反：「已建出功能實作票（非地基票），尚未建地基票，符合『該版尚未建票』」。另外，「規劃波進行中」列判別欄括號「（`version-bootstrap` 的 pipeline 確實跑過）」與題幹「`version-bootstrap` 已為該版建出」詞面相近，給了進行中列一個觸發詞 |
| FD-14b | 條文讀歪點 | 6.6.15 新增的附註括注「（權威已訂審查角色時用該角色，未訂時用本框架既有審查代理人）」，和工作流步驟 5 的括注「已訂者用該角色；未訂時退至本框架既有審查代理人」都是把兩個分支並排寫在同一個括號裡。r2 把這組條件句整組搬進判定句，沒有用題幹「權威來源皆未明訂」選掉第一分支。它在 (4) 又把兩者概括成「或」，讀回任選。「先後順序而非任選」這層意思修到了，但「套用時只輸出一個分支」沒有條文句承載 |

#### 答案鍵疑誤

無。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引文出處標錯 | r1 FD-01b 把工作流步驟 3 的「(1) 確認該處確實承擔該產物；(2) 不建票」標為維度與產物.md〈五種合法答案〉表「已存在於 X」列 |
| 引號內略去字面未標刪節 | r1 FD-02a「見同 skill 〈範本中……〉」略去原文 `references/step-3-fill-contract.md` 路徑，沒有加刪節號 |
| 依據句混入己見 | r2 FD-06b、FD-09a、FD-09b、FD-10b 在依據句後接推論句 |
| 引用行號 | r2 多題附「第 N 行」，對應節錄檔行號，不影響判定 |
| 不必要的假設 | r1、r2、r3 FD-14b (4) 都答「是」，假設內容為「具體代理人名稱未列出」。題目不要求具體名稱 |

#### 與 b3 的逐題對照

b3 這 7 題全部未通過（b3 總計 19/28）；b3x 通過 5 題。

| 題號 | b3 結果與失敗點 | 6.6.15 修補點 | b3x 結果 | 對照結論 |
|---|---|---|---|---|
| FD-01b | 否：r1、r2 漏答子問 2，r1 把「確認」列為必附 | 工作流步驟 3 兩項編號 | 3/3 | 修補生效：3 份都答出「不建票」，沒有人把確認列為必附。題幹改為直接寫入 repo 前提，也減少了一個變因 |
| FD-02a | 否：r1 子問 2 答「是」後又寫「而非複述新的數值」 | 引言補路由 step-3-fill-contract | 3/3 | 修補生效：3 份 (2) 都答不寫數值、指向 token 名；r1 直接引了新增的路由句 |
| FD-06b | 否：r2 判定句未答附帶（該格原填「無」） | 必須附帶欄改為「不需附帶」 | 3/3 | 修補生效：3 份判定句都寫「不需附帶」 |
| FD-09a | 否：r2 判不命中（量詞歧義） | 量詞明寫「至少一格」「一格即命中」 | 3/3 | 修補生效：3 份都引量詞句判命中 |
| FD-09b | 否：r1 依「已有相當程度的實作」判規劃波之後 | 其餘列改正向定義；規劃波之後列寫明唯一判別依據 | 3/3 | 表面生效：沒有人再判「之後」。但沒有探針引「唯一判別依據」句，新斷句風險也沒有被觸及，見上方判定者註 |
| FD-10b | 否：r1 漏答「不做什麼」 | 角色格做什麼／不做什麼分項 | 2/3 | 原失敗點已修：r1、r3 分項答出不做什麼。新失敗點不同：r2 在子問 1 就判錯列。原 b3 三份子問 1 都判對，這次是同題幹下新出現的讀法 |
| FD-14b | 否：r1 另附「或該維度的權威審查角色」 | 步驟 5 與附註改寫為先後查找順序，刪「之一」 | 2/3 | 部分生效：失敗形態相同（判定句帶入權威角色分支），觸發句從「之一」換成新增的兩分支括注。刪「之一」沒有消除並列兩分支的輸出 |
### b3x TAD、FD-1 判定與 UX-09a 用戶裁示後的處置（PM，2026-09-16）

**b3x TAD：10/12**
- **TAD-02b（題目設計）**：同一個子問編號裡問了兩件事。維持嚴格口徑，整段明問沒答即不通過。改題時拆成兩個子問。
- **TAD-04a（題目設計＋條文）**：題幹「形態」一詞撞到另一節的節名，探針讀了非餵入節錄檔。改題時換詞。另外，類型 8 與確定性斷言形態兩條分類軸的關係，條文沒有交代，開 W3-459 補寫（P2）。
- **每題只讀指定節錄檔的限制**：12 題一次交給探針時，這條限制沒有被遵守（3 處）。後續批次在探針規則補一句「每題作答前重讀該題餵入欄」。目前越界沒有影響判定，所以不改成逐題派發。

**b3x FD-1：5/7**
- FD-01b、02a、06b、09a 修補後 3/3，修補生效。
- FD-09b 3/3，但探針沒有逐列排除，只算表面通過。第 5 批後的整體回歸重跑會再驗一次。
- FD-10b、FD-14b 條文讀歪，開 W3-460 修正：「該版票」的範圍改為正向寫出；兩分支並列的括注拆成先後順序句。

**UX-09a 用戶裁示**
- 「明顯」要有正向定義。
- 「文字 chip」例示的去留，交給 WRAP 分析。
- W3-457.1 評估的草稿不採用（4 個判定詞中有 3 個推不出判定）。改由 W3-457.2 重新起草，要求逐詞可推，並對 UX-09a 題幹與判否反例各推導一次。
- 另外兩項判定依 W3-457.1 修正：循環限定語、「非窮舉」出處不成立。由 W3-457 在落檔時一併處理。

**W3-455 已落檔**（foundation-design 6.6.17）
- FD-05b、FD-13a 的最小處置完成。
- FD-20b 依據原本指向範例，已改指條文。
### b3x FD-2 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b2z 判定〉。
- 每題先只讀餵入節錄檔（`probes/excerpt/`，HEAD a375b87c），自行推出判定。再開答案鍵 `key/W3-417-b3x-foundation-design-2.md` 比對，最後逐題判 3 份答案 `a/W3-417-b3x-foundation-design-2-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過。答案寫在依據句欄內者記已答（同 b2z 前例）。
- 核對腳本：`probes/tools/b3x_fd2_check.py`。

**獨立推導與答案鍵比對**：16 題推導與答案鍵預期判定一致，沒有發現答案鍵誤。

**答案鍵引文核對**：47 段 ⟪⟫ 引文，去掉空白與標點後逐段比對該題餵入節錄檔，47 段都逐字存在。

**探針引文核對**：r1 43 段、r2 53 段、r3 66 段「」引文，比對該題餵入節錄檔與題目。不在其中的片段：
- 作答者自擬語句加引號：r1 FD-19b「圖形介面元件庫對純文字報表無可應用部分」、r1 FD-20b 理由與重評條件整句、r1/r2 FD-22b 產物欄填法、r3 FD-18b「無法判定失效具體形態」。
- 引號內改寫：r1 FD-19a「一年來未發生過」、r2 FD-20b「執行後結束」（皆改寫題幹）；r2 FD-18a「可觀測性要防的失效是『系統壞了…』」（改寫〈各維度要防的失效〉表列）；r1 FD-16a 引文刪去括號內容後拼接。
- 引用餵入外節錄檔：r2 FD-19b 引〈工作流〉步驟 3「答得出推導問句的具體形狀即完成，不再往返比對」，FD-19 未餵入 `SKILL--工作流`（該檔屬同批 FD-22 餵入）。內容不在 tools/removed，同句意在已餵入的〈權威缺席時〉形態 2 列也有，不計分。
- 出處檔名誤植（不計分）：r1、r3 FD-19b 把〈權威缺席時〉句標為 `改寫產物是主路徑.md`；r1 FD-18a 把〈各維度要防的失效〉句標為 `維度與產物.md`；r2 FD-22a 把〈工作流〉步驟 3 句標為 `維度與產物.md`。

**越界檢查**：
- (1) 答案鍵 10 條範例污染檢查字串：3 份答案命中 0。
- (2) `tools/removed/` 中本題組餵入的 13 份被刪文字，先去掉節錄檔與題目裡也有的片段，再以 22 字元視窗比對（忽略空白與標點）。原始命中 r1 9、r2 9、r3 12，逐一檢視皆為作答欄標籤「依據句」接節錄檔原文所拼出的跨界視窗（removed 範例內亦有「依據句」字樣），排除後實質命中 0。
- 越界 0 次。

**結果**：13／16。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| FD-15a | (1) 屬不可得；(2) 保留原值原名，疑義落決策文件待確認項，不在地基波內改 | 否（未答 (2)） | 否（未答 (2)） | 是 | 否 | 題目設計 |
| FD-15b | 不屬（未詢問） | 是 | 是 | 是 | 是 | — |
| FD-16a | (1) 符合；(2) 替代路徑：已覆蓋模組內命名，其餘只擋新增不動既有，未命名範圍記入決策文件 | 是 | 是 | 是 | 是 | — |
| FD-16b | 不需要；判準是抽取範圍覆蓋率 | 是 | 是 | 是 | 是 | — |
| FD-17a | (1) 分列；(2) 各子樹各自獨立填答 | 否（(2) 答無法給出） | 否（(2) 判「內容一致」） | 是 | 否 | 題目設計 |
| FD-17b | 不分列 | 是 | 是 | 是 | 是 | — |
| FD-18a | 不構成；不可填無 | 是 | 是 | 是 | 是 | — |
| FD-18b | 待定 | 是 | 是 | 是 | 是 | — |
| FD-19a | 不合格；不可填無 | 是 | 是 | 是 | 是 | — |
| FD-19b | 改寫產物＋改寫後產物＋一句理由；不需回頭比對步驟 3 | 是 | 是 | 是 | 是 | — |
| FD-20a | 不可填無；稀缺通道為語音輸出（使用者注意力） | 是 | 是 | 是 | 是 | — |
| FD-20b | 無＋理由＋重評條件 | 是 | 是 | 是 | 是 | — |
| FD-21a | 不符合；仍應翻權威原文 | 是 | 是 | 是 | 是 | — |
| FD-21b | 不成立；照同一形式增列一行 | 是 | 否（處置改走形態 3，預設框架無承接者） | 是 | 否 | 條文讀歪點（邊界判定，見下） |
| FD-22a | 並填；已存在部分不建票，照預設部分建地基票 | 是 | 是 | 是 | 是 | — |
| FD-22b | 已存在於兩處路徑；不建票 | 是 | 是 | 是 | 是 | — |

判定者註套用情形：
- FD-15a：r3 判定欄只答 (1)，但依據句欄引了第四列處置句，記已答。r1、r2 判定欄與依據句欄都沒有處置內容。
- FD-16a：r2 判定欄只寫「應走替代路徑」，替代路徑內容在依據句欄引文，記已答。
- FD-16b：r1、r3 判定句附「應照補特徵測試（進行）」。題幹抽取範圍已達 90%，「補」已無必要，但這是 FD-16a 題面二分的沿用，與條文不衝突，記通過。
- FD-17b：3 份都依「只有一套測試／建置／部署」判不分列，都沒有明寫「自稱 monorepo、工具設定檔屬名稱」。答案鍵只要求判定方向，記通過。
- FD-18a：r3 (1) 引「失效目前尚未發生」段與「某一種發生途徑」段並列，依答案鍵註記通過。r3 (2) 另寫「應填改寫產物或待定」，漏「照預設」，但本題只問可否填無，不計分。
- FD-18b：r3 附「標註『權威缺席』或『無法判定失效具體形態』」。本題權威存在，「權威缺席」不適用；但以「或」並列且另一項正確，屬複述「待定」列兩種觸發，記通過。
- FD-20a：沒有人主張「模組不是請求來源」。r1、r3 都引方法論「來自不同租戶的請求彼此競爭」例句，把三個模組類比成「請求來源」軸，沒有使用「發出請求的 domain」軸。答案鍵條文風險 2 標記的讀法在本批沒有觸發，但 3 份都沒有分辨兩軸。
- FD-20b（W3-455 相關）：3/3 通過。3 份都引用「附〈事件流仲裁的四種答案形態〉表『無』列的理由句」這個指向已刪範例的句子，但沒有人指出該理由句不在節錄檔中。r1、r3 自擬理由句，r2 判定欄只寫「附帶理由與重評條件」，理由內容是依據句欄的事實對應（「符合僅一條事件流且無並行請求」），記已答。指向範例的依據沒有造成失敗，但 r2 顯示這個指向會讓讀者只答出「附帶理由」而不寫出理由內容。
- FD-22b：r1、r2 只引步驟 4「全部維度皆為『已存在』或『無』時不建票」。這句的範圍是全部維度，不是單一維度；單一維度不建票的依據是步驟 3「已存在於 X → (2) 不建票」，r3 兩句都引了。結論正確，不計分。

#### 失敗歸因

| 題號 | 歸類 | 具體結構 |
|---|---|---|
| FD-15a | 題目設計 | 題幹子問編號 (1)(2) 與作答規則第 3 條四項 (1) 判定 (2) 依據句同號，且 FD-15a 是首題。r2 在單一判定後直接接「(2) 依據句」，子問 (2) 被作答格式的 (2) 吞掉；r1 同樣只答子問 (1)。後續題 r1、r2 都改用「(1)(2)」或「1. 2.」分開作答，失誤只出在首題。條文第四列處置句明確，不是讀歪。可改寫子問為「甲、乙」或在作答規則中寫明子問需在判定欄逐一作答 |
| FD-17a | 題目設計 | 子問 (2)「各子樹的產物欄怎麼填」要求填法，但題幹沒有給出任何維度在兩棵子樹的狀態，節錄檔也只有「按子樹分列」與「單一格會把落後那棵子樹的缺口吃掉」兩句，沒有具體填法。答案鍵預期只是「各自獨立填答」。題目檔抬頭已告知「範例區塊已刪除」，r1 因此判「無法給出，範例已刪除」；r3 也寫範例已刪除，但仍答出各自獨立填答；r2 則拿題幹「共用測試命令」自造「各子樹產物欄內容一致」，與分列理由相反。可把子問改為「是否各子樹各自一格」這類可從條文判定的問法 |
| FD-21b | 條文讀歪點（邊界判定） | 〈維度與產物〉「照同一形式增列一行」只給出增列動作，沒有交代新增列的「權威來源」欄怎麼填，也沒有指向〈權威缺席時〉的哪一種形態。r2 依據句欄引了增列句，判定欄卻寫「應按照〈權威缺席時〉形態 3 處理……建框架缺口票」，並在「自行假設」欄答否。題幹沒有給出框架有無承接者，形態 3「框架內確實無承接者」的名稱與「表沒有這一列」字面相近，讀者把表外項目等同於無承接者。r2 判定句有「框架無承接者時」的條件語，若 PM 認為條件語已限定範圍，可改記通過，結果為 14／16 |

#### 條文風險（本批未造成失敗，供 PM 知悉）

- FD-22a：`複合產物與多子樹分列` 的「『待定』的部分依〈產物欄的五種合法答案〉建補票」。這裡的「待定」語意是「待建」，但字面與五種合法答案中的「待定」同名。題幹的待建部分是「照預設」，3 份都把它套到這句，其中 r2、r3 逐字引用後自行改稱「照預設的部分建補票」，沒有指出兩者名稱不一致。若讀者照字面讀，可能把「照預設」的待建部分誤填成「待定」。
- FD-20a：答案鍵條文風險 2（請求來源軸與 domain 軸）在本批沒有觸發失敗，見上方判定者註。

#### 同構抄答跡象

- 16 題換輸入後，都沒有照抄例示的跡象。答案鍵 10 條污染字串命中 0。
- FD-18a、FD-19a：3 份都引用含條文例示的句子（「如沒有背景執行緒」「如目前只有一種輸出樣式」），但判定都是依類別（發生途徑不存在、失效尚未發生）推導，沒有把題幹對應到例示字面。
- FD-20a：沒有人使用「一次一則」「單線程」等已換掉的字面。r1、r3 用「不同租戶」例句作類比，屬引用判準例句，不是照抄範例結論。
- FD-20b：r1 的重評條件照搬條文括號例示（背景排程、外部推送、並行請求），這是條文指定的重評條件原文，不算抄例示。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 自擬語句加引號 | r1 FD-19b、FD-20b、FD-22b；r2 FD-22b；r3 FD-18b |
| 引號內改寫 | r1 FD-19a、FD-16a（刪括號拼接）；r2 FD-18a、FD-20b |
| 引用餵入外節錄檔 | r2 FD-19b（`SKILL--工作流`） |
| 出處檔名誤植 | r1 FD-18a、FD-19b；r2 FD-22a；r3 FD-19b |
| 多餘假設 | r1 FD-17a、r2 FD-17a「部署目標即獨立部署單元」（條文已並列）；r2 FD-19b、FD-20b 把題幹事實與判準的對應寫成假設 |
| 依據句混入己見 | r1 FD-15b、FD-16b、FD-18a；r2 FD-18a、FD-20a；r3 FD-16b、FD-17a、FD-19a、FD-20a：在依據句欄後接推論句 |
### b3x FD-2 判定後的處置（PM，2026-09-16）

**結果**：13/16，越界 0。

**失敗題處置**
| 題 | 歸因 | 處置 |
|---|---|---|
| FD-21b | 條文讀歪點：「照同一形式增列一行」沒交代權威來源欄怎麼填，r2 讀成框架無承接者 | 維持嚴格口徑記不通過（依用戶「讀歪是內容問題」裁示，不以邊界判定放寬）→ W3-461 |
| FD-15a | 題目設計：子問編號 (1)(2) 與作答四項編號同號 | 改題：子問改用 (a)(b) |
| FD-17a | 題目設計：題幹沒給子樹狀態，卻問各子樹怎麼填 | 改題：補上子樹狀態 |

**額外發現（規則 5）**：多子樹段的「待定的部分依…建補票」，「待定」實指待建，與五種合法答案中的「待定」同名異義，併入 W3-461。

**FD-20a、FD-20b 的標註風險**：兩題都 3/3，風險未觸發，也沒有被排除。第 5 批後整體回歸時再驗一次。

**第 3 批待重跑清單**（等 W3-459、460、461、457 落檔與改題）
- TAD-02b、TAD-04a
- FD-05b、FD-13a、FD-10b、FD-14b、FD-15a、FD-17a、FD-21b
- UX-09a
### UX-09a 定義第二輪用戶裁示（PM，2026-09-16）

W3-457.2 草稿以「構成要素」判定：文字或圖示＋區塊，缺任一項即屬明顯非按鈕形態。三個輸入皆可逐詞推出，但依這版草稿，填色的文字 chip 會被判為不合格。

**用戶裁示**
- U1：填色 chip 要仍屬合格，不接受草稿的結論。
- U2：判定時與畫面上實際的按鈕比較，不單獨判定指示本身。
- U3：例示措辭採「不加底色與外框的純文字」。

**處置**：開 W3-457.3，依 U1、U2 起草第二版。要求如下：
- 四個輸入逐詞推導：填色 chip 合格、描邊「已同步」標籤不合格、UX-09a 直條合格、圓點合格。
- 若與「描邊 vs 實心不是可靠的互動性訊號」一句的衝突無法消解，寫明並提出條文調整選項，交用戶決定。

**另記**：test-assertion-design SKILL.md 經撰寫者粗估已超過 5000 tokens（既有狀態），開 W3-462 評估拆分（P2）。拆分會移動節錄檔出處，排在 W2 閘門整體回歸之前。
### UX-09a 定義第三輪用戶裁示（PM，2026-09-16）

W3-457.3 第二版以兩軸判定：構成要素不同，或區塊高度低於按鈕。依此版，填色 chip 須比按鈕矮才合格。

用戶裁示：
- U4：採 B，理由句改寫為列出不可靠訊號（描邊 vs 實心、圓角、底色深淺），並寫明比較依據只看構成要素與區塊高度。
- U5：區塊高度不設門檻。
- U6（多出一個按鈕沒有的要素算不算不同）：用 WRAP 分析兩案後果。
- U7（矮的過、等高不過是否符合本意）：「可能有我沒設想到的情況」，用 WRAP 分析。

處置：開 W3-457.4，以 premortem 列舉第二版定義在真實畫面上會誤判的情境，U6 兩案逐一推導後果，結果交用戶決定。W3-457 的落檔等這一輪完成後才進行。

W3-461 已落檔（foundation-design 6.6.19）：FD-21b 補上權威來源欄的填法，FD-22a「待定」改為「待建」。出處表 3 句都有出處。
### 第 4 批出題後的處置（PM，2026-09-16）

**規模**：43 則範例、86 題（原盤點 18、W3-437 漏列 18、兩次盤點皆漏 7）。自檢兩項與 verify_head 通過，163 句引文命中。

**兩處是否算範例（PM 裁定：不列入）**
- task-granularity-rules「案例——拆分邊界未對齊測試變綠」、phase2-test-design「坑 1～6」皆為事件或除錯敘事，屬證據敘事，比照用戶裁示「案例檔不算閘門範例」，不列入閘門範例。
- 若其中含條文沒有的判準，由各批判定時以「依據只在範例內」標記追蹤。

**依據只在範例內與條文風險**：TDD-02b、32b，另 5 項條文衝突或射程風險，轉 W3-463。

**探針**：3 檔各 3 份立即派出（讀快照，與 W3-463 並行）。受 W3-463 影響的題，待 W3-463 落檔後重跑。
### b4 tdd-2 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3x FD-2 判定〉。
- 每題先只讀餵入節錄檔（`probes/excerpt/`，條文基準 5d994e10），自行推出判定；再開答案鍵 `key/W3-417-b4-tdd-2.md` 比對；最後逐題判 3 份答案 `a/W3-417-b4-tdd-2-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過；答案寫在依據句欄內者記已答。
- 核對腳本：`probes/tools/b4_tdd2_check.py`。

**獨立推導與答案鍵比對**：30 題推導與答案鍵預期判定一致，沒有發現答案鍵誤。TDD-20b 推導時已讀出「序號是否等同場景」的歧義，與答案鍵判定者註同。

**答案鍵引文核對**：62 段 ⟪⟫ 引文，去掉空白與標點後逐段比對該題餵入節錄檔，62 段都逐字存在。

**探針引文核對**：r1 66 段、r2 61 段、r3 74 段「」引文，比對該題餵入節錄檔與題目。不在其中的片段：
- 表格改寫成句子後加引號（不計分）：r1 TDD-21a、23a、23b；r2 TDD-21a（併「同一批次」「無 blockedBy」兩列）、23a、23b；r3 TDD-23a。
- 引號內改寫或刪句拼接：r1 TDD-21b「依賴多批次時，最後依賴的下一批」；r1 TDD-29b、r2 TDD-29a／29b 刪去「任一項不成立即選日誌，catch 後回傳預設值者記 warning」後拼接；r3 TDD-17a 刪去「不可假設各 GWT 場景獨立」後拼接。
- 自擬語句或題幹改寫加引號：r1 TDD-26b「業務概念不同」（26b 題幹沒有這句，字面在 27b 題幹）；r3 TDD-17b「深色主題不影響 CSV 匯出結果」「匯出全部聯絡人」、TDD-22b（題幹改寫）、TDD-26a「語義相同」、TDD-29a「寫入持久狀態」。
- 引用餵入外節錄檔：r1 TDD-26a 引「Q1 回答『是』時必答」並標 `tdd--rules--phase1-Q2共用或各自.md`，該檔不在 TDD-26 餵入欄（屬 TDD-27 餵入）。內容不在 tools/removed，判定不依賴此句（Q1 行動路徑的反面即可推出），比照 b3x FD-19b 前例不計分。
- 出處檔名誤植（不計分）：r1 TDD-26a 把 Q1 行動路徑句標為 PC-006 檔；r1 TDD-27b「不共用元件」標為 PC-006 檔（實在 Q2 檔）；r2 TDD-24a／24b 把 task-granularity 句標為 `phase1-行為單元識別.md`；r3 TDD-24a 把題幹句當依據句並標為節錄檔。

**越界檢查**：
- (1) 答案鍵 15 條範例污染檢查字串：3 份答案命中 0。
- (2) `tools/removed/` 中本題組餵入的 17 份被刪文字，去掉節錄檔與題目裡也有的片段後，以 22 字元視窗比對（忽略空白與標點）：r1、r2、r3 命中皆 0。
- 讀了非本題餵入的節錄檔：r1 TDD-26a 1 次（見上）。被刪文字越界 0 次。

**結果**：28／30。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| TDD-16a | 4 個 | 是 | 是 | 是 | 是 | — |
| TDD-16b | (a) 是；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-17a | (a) 是；(b) 是 | 是 | 是 | 是 | 是 | — |
| TDD-17b | (a) 不必列入；(b) 不算錯 | 是 | 是 | 是 | 是 | — |
| TDD-18a | (a) 系統；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-18b | (a) 使用者；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-19a | 3 個 | 是 | 是 | 是 | 是 | — |
| TDD-19b | (a) 相同；(b) 多個種子 | 是 | 是 | 是 | 是 | — |
| TDD-20a | (a) 不符合；(b) Given、When、Then、來源 | 是 | 是 | 是 | 是 | — |
| TDD-20b | 符合（序號為列索引） | 是 | 否（判不符合，「多了序號欄位」） | 否（判定句附「實際欄位仍為場景、Given…」，與題幹和條文不符） | 否 | 條文讀歪點 |
| TDD-21a | (a) A、B 一；C、D 二；E 三；(b) 可並行 | 否（判定欄「不可並行」，依據句欄結尾「確實無依賴可並行」，未更正判定） | 是 | 是 | 否 | 條文讀歪點 |
| TDD-21b | (a) 第三批；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-22a | (a) 成立；(b) 介面定義任務 | 是 | 是 | 是 | 是 | — |
| TDD-22b | (a) 不成立；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-23a | (a) 缺異常、邊界；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-23b | (a) 滿足；(b) 否，缺異常 | 是 | 是 | 是 | 是 | — |
| TDD-24a | (a) 3；(b) 3 | 是 | 是 | 是 | 是 | — |
| TDD-24b | (a) 1；(b) 1 | 是 | 是 | 是 | 是 | — |
| TDD-25a | (a) 不合格；(b) 可量化、可驗證 | 是 | 是 | 是 | 是 | — |
| TDD-25b | 合格 | 是 | 是 | 是 | 是 | — |
| TDD-26a | (a) 是；(b) 是 | 是 | 是 | 是 | 是 | — |
| TDD-26b | (a) 否；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-27a | 共用元件 | 是 | 是 | 是 | 是 | — |
| TDD-27b | (a) 各自實作；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-28a | (a) 有；(b) timestamp+random | 是 | 是 | 是 | 是 | — |
| TDD-28b | (a) 否；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-29a | (a) 否；(b) warning | 是 | 是 | 是 | 是 | — |
| TDD-29b | (a) 可；(b) 註解說明靜默原因 | 是 | 是 | 是 | 是 | — |
| TDD-30a | (a) 是；(b) 完整映射表 | 是 | 是 | 是 | 是 | — |
| TDD-30b | 否 | 是 | 是 | 是 | 是 | — |

判定者註套用情形：
- TDD-17b：r3 在 (4) 寫出假設「深色主題不影響 CSV 匯出結果」，但判定方向是「不必列入」，不是答案鍵註記的「以假設推出須列入」，記通過。
- TDD-20a：r3 (b) 列「場景（列索引）、Given、When、Then、來源」，場景已標為列索引，與條文括號句一致，記通過。
- TDD-22a：3 份都同時引 OCP 核心問題與拆分信號「需要 switch/case 或 if/else 分岐」；題幹沒有 switch/case，判定依核心問題推出，記通過。
- TDD-26a：3 份判「是／是」。r2、r3 以 Q1 行動路徑語義句推出，r1 另引 Q2 節錄檔觸發條件句（餵入外），判定不依賴該句，記通過。
- TDD-28b：r2 只引 Q4 觸發條件，沒有引 Q3；判定方向正確，記通過。
- TDD-29a：r2 引文刪去 warning 子句，改由 observability 規則 1 表推出 warning，記通過。

#### 失敗歸因

| 題號 | 歸類 | 具體結構 |
|---|---|---|
| TDD-20b | 條文讀歪點 | 〈GWT 種子與行為單元的分工邊界〉只寫「種子表欄位僅限 Given、When、Then、來源四欄」與範本表頭後的括號「（場景為列索引，非欄位本身）」。列索引這個判定只綁在「場景」這個名稱上，沒有寫出「不承載內容、只標列序的欄即為列索引」的一般判準。r2 只引「僅限四欄」，把序號當成第五欄；r3 判定正確，卻寫「實際欄位仍為場景、Given…」，把場景讀成固定欄位、序號另計為索引。兩份讀歪方向不同，根源都是列索引只以名稱舉出、沒有給出判準。答案鍵判定者註已標記此風險 |
| TDD-21a | 條文讀歪點 | 〈並行判斷〉表四列條件不互斥（同一批次／無 blockedBy／不同架構層但無依賴／有 blockedBy），第三列用「但」連接，暗示不同架構層原則上不可並行；同節〈架構層級執行順序〉又依層排序。r1 依據句寫「判定基準為『不同架構層但無依賴』時才是」，判定欄先寫「不可並行」，推論到最後才得出「可並行」，但沒有回頭更正判定。C、D 同屬第二批，第一列就能判定，r1 卻繞到第三列。若 PM 將依據句欄的結論視為更正，本題改記通過，結果為 29／30 |

#### 答案鍵附記條文風險的觸發情形

| 風險（答案鍵判定者註） | 本輪觸發 | W3-463 對應 |
|---|---|---|
| TDD-17b 以題幹外假設判須列入 | 未觸發 | — |
| TDD-19b 分拆段位於 FR 節、主詞為場景步驟 | 未觸發：3 份在 19a、19b 都直接套用該段，沒有人質疑適用範圍 | 項 6 |
| TDD-20b 列索引是否限於「場景」 | 觸發：r2 判不符合；r3 判定正確但把場景當欄位 | 不在 7 項內 |
| TDD-21b 分配原則表與架構層級表何者優先 | 未觸發：3 份都依依賴關係判第三批。但 21a 的 r1、r2 以架構層級表作分批依據，因為兩表結果一致而沒有暴露 | 項 3 |
| TDD-23b 25:00 誤歸邊界條件 | 未觸發 | — |
| TDD-24a「每 1-2 個操作一個實作 Ticket」合併單元 | 未觸發：3 份都沒有引〈禁止的模式〉表 | 項 4 |
| TDD-26a Q2 觸發條件句不在餵入 | 未造成失敗；r1 讀了 Q2 節錄檔並引用（越界讀取，見上） | — |
| TDD-29a 子問與三項條件同號 | 未觸發 | — |
| TDD-29b 規則 1「回傳預設值記 warning」壓過靜默條件 | 未觸發：3 份都判可靜默；r3 另引規則 1 空 catch 列作支持 | 項 5 |

失敗題與 W3-463 的 7 項：TDD-20b 不重疊；TDD-21a 不重疊（同節錄檔，與項 3 同段表格，但問題在並行判斷表，不在批次優先序）。

#### 同構抄答跡象

- 答案鍵 15 條污染字串命中 0，30 題換輸入後都沒有照抄範例的跡象。
- TDD-17b：r3 依據句引了條文例示（步驟 3 查詢事件、session），判定依依賴判準推出，沒有把題幹對應到例示字面。
- TDD-21：r1、r2 使用架構層名稱（Domain／Application…第一批），這是判準表本身，不是範例結論。
- TDD-26b：r3 引 PC-006 例示「git vs flutter」作為語義不同的類比，屬引用判準例句。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 判定欄與依據句欄結論矛盾 | r1 TDD-21a（已計分） |
| 表格改寫加引號 | r1 TDD-21a、23a、23b；r2 TDD-21a、23a、23b；r3 TDD-23a |
| 引號內刪句拼接 | r1 TDD-29b；r2 TDD-29a、29b；r3 TDD-17a |
| 自擬或題幹改寫加引號 | r1 TDD-26b；r3 TDD-17b、22b、26a、29a |
| 引用餵入外節錄檔 | r1 TDD-26a（`phase1-Q2共用或各自`） |
| 引用他題題幹 | r1 TDD-26b（27b 題幹的「不同的業務概念」） |
| 出處檔名誤植 | r1 TDD-26a、27b；r2 TDD-24a、24b；r3 TDD-24a |
| 多餘假設 | r2 TDD-19a（選格式屬不同 When）；r3 TDD-20b、22b、28b：把題幹事實與判準的對應寫成假設 |
UX-09a 定義第四輪用戶裁示（2026-09-16）：U6 採 C 加徽章條款；U9 設門檻，至少低一個尺寸階；O3 高度比較須在所有字級、斷點、密度下成立；O4／O5 比較對象為同一畫面所有可點元素。完整規格彙整於 0.1.0-W3-457 Problem Analysis〈落檔依據彙整〉，由 W3-457 落檔，落檔前以 4 個輸入加 15 個情境逐詞驗證。
### b4 tdd-3 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3x FD-2 判定〉。
- 每題先只讀餵入節錄檔（`probes/excerpt/`，條文基準 5d994e10），自行推出判定。再開答案鍵 `key/W3-417-b4-tdd-3.md` 比對，最後逐題判 3 份答案 `a/W3-417-b4-tdd-3-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答或缺子問要求的核心內容，記不通過；判定句與條文或題幹不符且沒有更正，記不通過；答案寫在依據句欄內者記已答。
- 核對腳本：`probes/tools/b4_tdd3_check.py`（餵入清單由題目檔「餵入」欄解析；污染字串 `probes/tools/b4_tdd3_pol.txt`）。

**獨立推導與答案鍵比對**：26 題推導與答案鍵預期判定一致，沒有發現答案鍵誤。

**答案鍵引文核對**：41 段 ⟪⟫ 引文，去掉空白與標點後逐段比對該題餵入節錄檔，41 段都逐字存在。

**探針引文核對**：r1 42 段、r2 55 段、r3 47 段「」引文，比對該題餵入節錄檔與題目。不在其中的片段：
- 引號內拼接：r1 TDD-37b 把 Q_new4「建立『異常處理路徑 vs 失敗測試』對照表」接上 Q10「未覆蓋的路徑必須補充測試案例」；r2 TDD-40b 刪去「，各子任務 import」後拼接。
- 引號內改寫：r2 TDD-34a 把 Q9 標題「如果實作讀錯了欄位名」改成題幹用語「若實作誤讀了欄位名」。
- 自擬語句加引號：r2 TDD-39a「下載按鈕存在」、r3 TDD-39a「下載按鈕存在且已啟用」。
- 出處檔名誤植（不計分）：r2 TDD-34b 把題幹句「資料中沒有其他可能被誤讀為顯示名稱的欄位」標為 `tdd--rules--phase2-組1中間步驟.md`。

**越界檢查**：
- (1) 答案鍵 14 條範例污染檢查字串：3 份答案命中 0。
- (2) `tools/removed/` 中本題組餵入的 10 份被刪文字，先去掉節錄檔與題目裡也有的片段，再以 22 字元視窗比對（忽略空白與標點）：r1、r2、r3 原始命中皆 0。
- (3) 引文比對未發現引用本題未餵入的節錄檔。r1、r2、r3 TDD-32b 引 Q8「只呼叫一個子方法的簡單委派不觸發本題」，該句與 Q7 同在 `phase2-組1中間步驟` 節錄檔，屬餵入範圍。
- 越界 0 次。

**結果**：21／26。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| TDD-31a | (a) 不合格；(b) 可量化、可驗證 | 是 | 是（(b) 在依據句欄） | 是 | 是 | — |
| TDD-31b | 合格 | 是 | 是 | 是 | 是 | — |
| TDD-32a | (a) 成立；(b) 三個子方法各至少一個獨立測試 | 是 | 是 | 是 | 是 | — |
| TDD-32b | (a) 不成立；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-33a | (a) 不滿足；(b) 透過 `register()` 以斷言驗證正規化後電話號碼 | 是 | 是 | 否（(a) 判「滿足」，與自己的 (b) 矛盾，未更正） | 否 | 條文讀歪點 |
| TDD-33b | (a) 不觸發；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-34a | (a) 會；(b) `nickname` 與 `displayName` | 是 | 是 | 是 | 是 | — |
| TDD-34b | 否 | 是 | 是 | 是 | 是 | — |
| TDD-35a | (a) 未覆蓋；(b) Mock logger，驗證 call count 和參數 | 是 | 是 | 否（(b) 只寫驗證「有 error 日誌被記錄（含原因）」，未答驗證方式，也未引推導表） | 否 | 條文讀歪點 |
| TDD-35b | 否，組 2 不觸發 | 是 | 是 | 是 | 是 | — |
| TDD-36a | (a) 存在性驗證；(b) 不足 | 是 | 否（(a) 判「值驗證」） | 是 | 否 | 條文讀歪點 |
| TDD-36b | (a) 會翻紅；(b) 足夠 | 是 | 是 | 是 | 是 | — |
| TDD-37a | (a) StorageFull 路徑；(b) 未覆蓋 | 是 | 是 | 是 | 是 | — |
| TDD-37b | (a) 否；(b) 是，仍須建表 | 是 | 是 | 否（(b) 判「不需建立對照表」） | 否 | 條文讀歪點 |
| TDD-38a | (a) 是；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-38b | 否 | 是 | 是 | 是 | 是 | — |
| TDD-39a | (a) When 前以斷言驗前置條件；(b) 操作可執行 | 是 | 是 | 否（(b) 判「UI 元素存在」） | 否 | 條文讀歪點 |
| TDD-39b | (a) 否；(b) 驗證「檔案已刪除」的預期狀態 | 是 | 是 | 是 | 是 | — |
| TDD-40a | (a) 否；(b) 先建共用模組再派發 | 是 | 是 | 是 | 是 | — |
| TDD-40b | 可直接並行 | 是 | 是 | 是 | 是 | — |
| TDD-41a | (a) 不符合；(b) 共用模組並匯出，各子任務 import | 是 | 是 | 是 | 是 | — |
| TDD-41b | (a) 沒有；(b) 否 | 是 | 是 | 是 | 是 | — |
| TDD-42a | (a) 不通過；(b) `lodash` | 是 | 是 | 是 | 是 | — |
| TDD-42b | (a) 通過；(b) 否，須實際執行 | 是 | 是 | 是 | 是 | — |
| TDD-43a | (a) 不對應；(b) Phase 3b 實作完成、準備提交前 | 是 | 是 | 是 | 是 | — |
| TDD-43b | 對應 | 是 | 是 | 是 | 是 | — |

判定者註套用情形：
- TDD-39a：答案鍵註「答出以斷言驗證前置條件即通過」。r1 (a) 只寫「缺少對前置條件的驗證斷言」，未列具體前置條件，但 (b) 已對到「操作可執行」，記通過。
- TDD-43a：r1、r3 (b) 寫「Phase 3b 完成前」，r2 寫「Phase 3b 完成前執行（準備提交前）」。「完成前」取自清單標題〈Phase 3b 完成前執行清單〉，與觸發條件「實作完成，準備提交前」不衝突，記通過。
- TDD-32b：r1 依據只引 Q8 簡單委派句，把 Q8 的例外句套回 Q7；結論正確，不計分，見下方條文風險。
- TDD-37b：r1 理由「對照表是驗證所有路徑覆蓋狀態的標準工具」、r2 理由「確保所有異常處理路徑都已覆蓋」都是自擬，條文沒有寫明無盲點時仍須建表的理由。判定方向正確，記通過。

#### 失敗歸因

| 題號 | 歸類 | 具體結構 |
|---|---|---|
| TDD-33a | 條文讀歪點 | Q8 題目句寫「是否被至少一個測試的斷言直接覆蓋」，說明句卻寫「透過公開入口呼叫」。「直接」與「透過入口」字面不一致，「覆蓋」又可讀成執行覆蓋。r3 把 `expect(register(), true)` 當成「至少一個測試的斷言」，且 `_normalizePhone()` 有被執行到，所以判「滿足」。判準句「確認私有方法的邏輯被斷言驗證」被 r3 當成 (b) 的途徑使用，沒有回頭套到 (a)。題目把同一句拆成 (a) 判定、(b) 途徑兩問，助長了這個讀法 |
| TDD-35a | 條文讀歪點 | Q10 行動路徑只寫「未覆蓋的路徑必須補充測試案例」，沒有路由到同節另一標題下的〈非功能性需求測試推導表〉。驗證方式「Mock logger，驗證 call count 和參數」只在該表。r3 只依 Q10 作答，寫出要驗證什麼（日誌被記錄、含原因），沒有寫出驗證方式。若 PM 認為「驗證日誌被記錄含原因」已等同驗 call count 與參數，可改記通過，結果為 22／26 |
| TDD-36a | 條文讀歪點 | Q_new3 的「值驗證／存在性驗證」二分沒有給分類判準，只用括號例示 API 名稱（isNotNull, containsKey）界定存在性驗證。`expect(x.length, greaterThan(0))` 字面上是對數值做比較，不是例示中的 API，r2 因此歸為值驗證，但 (b) 仍判不足。答案鍵同構比對記錄題幹原擬 `isNotNull`，為避開例示字面改成 `length` 大於 0；改題後，分類要靠語意（驗的是「有沒有」還是「是什麼」），條文沒有寫出這個語意 |
| TDD-37b | 條文讀歪點 | Q_new4 行動路徑一句寫「建立…對照表，空格代表測試盲點」，建表只交代了偵測盲點這一個用途，沒有寫無盲點時表是否仍須產出。r3 以用途反推，判「三條都覆蓋時已無識別需求，不需建立」。答案鍵理由「與是否有盲點無關」不在條文內，是從祈使句沒有附條件推出來的 |
| TDD-39a | 條文讀歪點 | 前置條件驗證表的類型名稱混用兩個分類軸：「UI 元素存在」以對象命名，「操作可執行」以狀態命名。按鈕同時屬 UI 元素，也是操作是否可執行的載體。r3 (a) 把「按鈕存在且已啟用」合成一條前置條件，(b) 依對象（按鈕＝UI 元素）歸類，沒有依驗證方式欄「驗證按鈕未被禁用」反查類型 |

**與 W3-463 七項的重疊**：5 題失敗都不在 02b、32b、21b、24a、29b、19b、36b 之內。
- TDD-36a 與 W3-463 項 7（TDD-36b）同在 Q_new3。項 7 談「存在性驗證何時足夠」；36a 的失敗點是「值驗證／存在性驗證」分類沒有判準。兩者結構不同，不重疊，但修 Q_new3 時可一併處理。
- TDD-35a 與項 5（TDD-29b，observability 規則 1）主題相近（日誌），失敗結構是 Q10 沒有路由到推導表，不重疊。

#### 答案鍵條文風險的觸發情形

| 答案鍵標記 | 本輪是否觸發 | 說明 |
|---|---|---|
| TDD-32b 依據部分只在範例（W3-463 項 2） | 未觸發失敗 | 3/3 通過。r1、r2、r3 都引 Q8「只呼叫一個子方法的簡單委派不觸發本題，不需要額外測試」，r1 只引這一句。Q7 本身沒有單一委派的明文，讀者會拿 Q8 的例外句補 Q7 的缺口；該句寫「本題」指 Q8，套到 Q7 屬跨題援用。W3-463 補 Q7 句時可參考 |
| TDD-33b 註：未說明 `_moveToArchive()` 內部邏輯 | 未觸發 | 3 份都依子方法數判不觸發 |
| TDD-36b 註：只給不足條件，未寫足夠判定（W3-463 項 7） | 未觸發失敗 | 3/3 判足夠。r1、r3 依鑑別力定義，r2 依不足條件的反面。沒有人主張值驗證一律較佳。風險未排除 |
| TDD-39a 註：(a) 至少列按鈕存在或可操作其一 | 部分觸發 | r1 未列具體前置條件，依註首句記通過；r3 把存在與可操作合成一條，(b) 歸類錯誤（見失敗歸因） |
| TDD-41b 註：(b) 由條件反面推出 | 未觸發 | 3/3 判否；r1 明寫「隱含反面」 |
| 同構比對 TDD-36（isNotNull 換成 `length`、`isA<String>()`） | 觸發 | 換題後 r2 36a 分類失敗，見失敗歸因 |
| 同構比對 TDD-38、TDD-39b、TDD-35a（不換） | 未觸發 | 見下方同構抄答跡象 |

#### 同構抄答跡象

- 26 題換輸入後，都沒有照抄例示的跡象。答案鍵 14 條污染字串命中 0。
- TDD-38a：r3 依據句引「檢查測試資料工廠（createBookV2、buildTestTag 等）」，是整句引用條文，判定依「目標版本工廠」類別推導，沒有把題幹對應到例示名稱。
- TDD-39b：3 份都引含「如空清單顯示空狀態訊息」的句子，判定都改寫為「檔案已刪除」，沒有照搬例示。
- TDD-35a：r1、r2 答「Mock logger，驗證 call count 和參數」，出自推導表判準本身，不算抄例示。
- TDD-36a：r1 依據句含例示「如 isNotNull, containsKey」，判定依失效時不翻紅推導。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引號內拼接 | r1 TDD-37b；r2 TDD-40b |
| 引號內改寫 | r2 TDD-34a |
| 自擬語句加引號 | r2 TDD-39a；r3 TDD-39a |
| 出處檔名誤植 | r2 TDD-34b（題幹句標為節錄檔） |
| 多餘假設 | r2 TDD-33a「假設題目要求補充測試使得私有方法邏輯被驗證，而非另寫單元測試」 |
| 依據句混入己見 | r1 TDD-31b、TDD-35b、TDD-36a、TDD-36b、TDD-37b、TDD-41b；r2 TDD-31a、TDD-31b、TDD-33a、TDD-34a、TDD-36a、TDD-36b、TDD-37b、TDD-38a、TDD-42a；r3 TDD-31b、TDD-36a、TDD-36b、TDD-37b、TDD-41b：在依據句欄後接推論句 |
**〈b4 tdd-3 判定〉更正**：越界檢查 (2) 的「本題組餵入的 10 份被刪文字」實為 9 份（本題組餵入 9 個節錄檔，`tools/removed/` 各有對應檔），命中數不變（皆 0）。
### b4 tdd-2／tdd-3 判定與 b3y 出題後的處置（PM，2026-09-16）

**b4 tdd-2：28/30；b4 tdd-3：21/26。** 兩檔越界 0，答案鍵無誤。

**計分**：TDD-21a、TDD-35a 判定者提出可改判通過，PM 維持嚴格口徑，記不通過：
- 21a 判定欄與依據欄矛盾，且未更正。
- 35a 缺少明問的驗證方式。

**7 題讀歪點轉 W3-465**（blockedBy W3-463，同一 skill）：TDD-20b、21a、33a、35a、36a、37b、39a。

**W3-463 參考**：TDD-32b 雖 3/3 通過，但 3 份答案都借用 Q8 的句子來補 Q7 缺的明文，W3-463 補 Q7 時一併參考。

**b3y 出題**
- FD-05b 題幹與 W3-455 新增句同構，出題者換輸入。雖與 PM「題幹不動」指示衝突，仍依同構規則接受。
- FD-10b 風險：題幹用語與條文定義字面相同，通過不能證明原先的讀反已消失。加一題「只有地基票已建」，併入下一輪出題。
- FD-21b 引號內字串不在所指節，已由 W3-464 修正（foundation-design 6.6.20）。b3y 的 FD-21b 節錄檔需依 6.6.20 重建後再派探針。
### b4 tdd-1 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3x FD-2 判定〉。
- 每題先只讀餵入節錄檔（`probes/excerpt/`，HEAD 5d994e10）自行推出判定，再比對答案鍵 `key/W3-417-b4-tdd-1.md`，最後逐題判 3 份答案 `a/W3-417-b4-tdd-1-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過；答案寫在依據句欄內記已答。
- 核對腳本：`probes/tools/b4_tdd1_check.py`（餵入表由題目檔「餵入」欄解析）。

**獨立推導與答案鍵比對**：30 題推導與答案鍵預期判定一致。TDD-09a (b) 的預期理由有缺口，見失敗歸因。

**答案鍵引文核對**：60 段 ⟪⟫ 引文，去掉空白與標點後比對該題餵入節錄檔，60 段都逐字存在。

**探針引文核對**（r1 68、r2 132、r3 82 段「」引文）：不在該題餵入節錄檔與題目中的片段，分成以下幾類：
- 引號內改寫：r1 TDD-02a 加「反例欄填法：」前綴、TDD-08a 加「步驟 C2 |」、TDD-12a/12b 把表頭改成「| YES →」；r2 TDD-05b 把「而是先問」改成「而非先問」、TDD-12a/12b 改成「是 →」；r3 TDD-03a 把兩句拼成一句、TDD-09a「自己決定 = 單元測試；串接決定 = 整合測試」屬合併改寫。
- 自擬語句加引號：r1 TDD-04a「表單狀態變為已儲存」；r2 TDD-02b、04a、06a、08b、11b、14b、15a 多處；r3 TDD-04a「系統接收到儲存成功回應」。
- 表格引用混入欄位分隔符（r1 TDD-04a/06b、r3 TDD-04a/06b 的「禁止 |」）：文字本身存在於節錄檔，不計。

**越界檢查**：
- (1) 答案鍵 16 條範例污染檢查字串：3 份答案都沒有命中。
- (2) `tools/removed/` 中本題組餵入的被刪文字：先去掉節錄檔與題目中也出現的片段，再以 22 字元視窗比對，3 份命中都是 0。
- (3) 讀了非本題餵入的節錄檔：r2 TDD-09a 依據句引〈與UC資訊鏈的關係〉（「決定不同、測試層級不同」），並標出處 `tdd--doc-handoff-test-tier-assignment--與UC資訊鏈的關係.md`；該檔屬 TDD-10 餵入，不屬 TDD-09。內容不在 tools/removed，而且 r2 此題失敗的原因不是這句，不計分，記越界 1 次。
- (4) 跨題題幹挪用：r3 TDD-05a 的「自行假設」欄寫「本 app 依結果顯示私密內容或顯示『驗證失敗』」。這是 TDD-09a 的題幹，r3 據此把 TDD-05a 的驗證形態縮到 app 端而答 fake，屬跨題干擾。r3 TDD-06a 判定欄的「終端回饋訊息的是否出現」與 TDD-01 餵入檔 C3 附註字面相同，可能是跨題帶入，也可能只是自然措辭，不計。
- 越界合計：跨檔 1 次（r2 TDD-09a），跨題題幹 1 次（r3 TDD-05a）。

**結果**：19／30。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| TDD-01a | 2 行；否，加分支描述 | 是 | 是 | 是 | 是 | — |
| TDD-01b | 1 行；否，違反 C3 | 是 | 否（(b) 判符合來源唯一） | 是 | 否 | 條文讀歪點 |
| TDD-02a | 不合格 | 是 | 是 | 是 | 是 | — |
| TDD-02b | 合格 | 是 | 是 | 是 | 是 | — |
| TDD-03a | SPEC-C §3.1；不可留空 | 是 | 是 | 是 | 是 | — |
| TDD-03b | §3.4；§1.2 | 是 | 是 | 是 | 是 | — |
| TDD-04a | 不合格；改寫為「儲存請求回傳成功」 | 否（(b) 保留 isSaved 旗標） | 否（(b) 保留 isSaved 旗標） | 是 | 否 | 條文讀歪點 |
| TDD-04b | 不合格；未連線 | 是 | 是 | 是 | 是 | — |
| TDD-05a | 否（平台決定）；兩者 | 否（(b) 實機） | 否（(b) 實機） | 否（(b) fake） | 否 | 條文讀歪點（判準只在範例內） |
| TDD-05b | 否；fake | 否（fake 或兩者） | 否（fake 或兩者） | 否（fake 或兩者） | 否 | 條文讀歪點 |
| TDD-06a | 三類；不可合併為一條斷言 | 否（(b) 判可以） | 是 | 是 | 否 | 條文讀歪點（判準只在範例內） |
| TDD-06b | 不合格；框架測試 API 方法名或選擇器語法 | 是 | 是 | 是 | 是 | — |
| TDD-07a | 無規格來源；拆兩則 | 是 | 否（(a) 有規格來源） | 否（(a) 有規格來源） | 否 | 條文讀歪點 |
| TDD-07b | 無規格來源；不拆 | 是 | 是 | 是 | 是 | — |
| TDD-08a | 3 條；start 不另立 | 是 | 是 | 是 | 是 | — |
| TDD-08b | 不另立；1 條 | 是 | 否（另立，2 條） | 否（另立，2 條） | 否 | 條文讀歪點 |
| TDD-09a | 平台驗證；單元 | 是 | 否（(b) 單元或整合） | 否（(b) 整合或單元） | 否 | 題目設計 |
| TDD-09b | 否；自己 | 是 | 是 | 是 | 是 | — |
| TDD-10a | 串接；整合 | 是 | 是 | 是 | 是 | — |
| TDD-10b | 自己；單元 | 是 | 是 | 是 | 是 | — |
| TDD-11a | 先拆 Domain 單元；移除後仍 > 10 行才考慮拆 Module | 是 | 是 | 否（(b) 答 > 3 條件組合） | 否 | 條文讀歪點 |
| TDD-11b | 考慮拆分 Module；否 | 是 | 否（(a) 檢查 Given 可否移除） | 否（(a) 評估是否過大，否則接受現設計） | 否 | 條文讀歪點 |
| TDD-12a | 實作細節；會 | 是 | 是 | 是 | 是 | — |
| TDD-12b | 行為；能 | 是 | 是 | 是 | 是 | — |
| TDD-13a | 不允許；真實 Coupon | 是 | 是 | 是 | 是 | — |
| TDD-13b | 允許；外部系統依賴 | 是 | 是 | 是 | 是 | — |
| TDD-14a | 不算；不通過 | 是 | 是 | 是 | 是 | — |
| TDD-14b | 第二列；否 | 是 | 否（第一列，觸發） | 否（第一列，觸發） | 否 | 條文讀歪點 |
| TDD-15a | 可；否 | 是 | 是 | 是 | 是 | — |
| TDD-15b | 照抄標〔不可量化〕；否 | 是 | 是 | 是 | 是 | — |

判定者註：
- TDD-03b：r1 把兩欄答案都寫在 (a)，(b) 寫「不同條」；兩欄內容齊全，記已答。
- TDD-07b：r2 (a) 寫「無規格來源或 characterization test」，後者是 B3 的後續判定類別，不是標記值；以「或」並列且另一項正確，比照 FD-18b 前例記通過。
- TDD-13a：r2 (b) 附「以有效期限已過的測試資料構造實例」，方向與題幹 mock 回傳 false 相反；但題幹沒有限定真實物件的測試情境，不構成與題幹衝突，記通過。r2 另稱 Coupon 為「Domain Entity 的一個例子」（實為值物件），表列同列，不計分。
- TDD-05a：3 份 (a) 都正確判定分享面板屬平台決定。

#### 失敗歸因

| 題號 | 歸類 | 具體結構 |
|---|---|---|
| TDD-01b | 條文讀歪點 | 判準名「來源唯一」與機械檢查「同一條規格條目不被兩行引用」分處兩欄。名稱字面可讀成「每行只有一個來源」。r2 本題只引「多個規格宣告」句，沒有引 C3 列，並以「只有一個宣告」判符合。r2 在 TDD-01a 引過 C3 列，說明也有探針前後不一致的成分 |
| TDD-04a | 條文讀歪點 | 〈六〉書寫邊界句把 When 與 Then 合寫成「觸發條件與可觀察結果」，禁止項只列「觸發機制的具體實作方式」與「框架內部表示方式」。app 自有的狀態旗標落在兩類之外。When 取自狀態矩陣觸發條件欄這件事，只寫在句型佔位符 `{來源規格條目描述的觸發動作}`。r1、r2 只刪了輪詢，保留 isSaved 旗標；r2 還把旗標改寫當成「可觀測結果」 |
| TDD-05a | 條文讀歪點（判準只在範例內） | 驗證形態欄只列 fake／實機／兩者，括號各給一句定義，沒有寫「步驟同時含離開 app process 的子步驟與 app 內子步驟時填兩者」。這條判準只在已刪除的〈六〉範例二【驗證】段（「本步驟含一個離開 app process 的子步驟……也含一個 app 內部子步驟」）。〈跨邊界步驟〉的指引「app 端收到平台結果後的後續處理，另見〈六〉第二則範例」也指向已刪除的範例。r1、r2 只看到平台部分，填實機；r3 挪用 TDD-09a 題幹，填 fake。答案鍵附記「依據只在範例內」寫其餘題 0 處，漏列本題 |
| TDD-05b | 條文讀歪點 | 「實機」一詞與測試形態欄的「實機驗證」、widget 並列。探針把「UI 渲染」當成實機的觸發條件，都沒有援引括號定義「驗證平台端事實」。和 TDD-05a 同源：驗證形態三值只有括號定義，沒有選值判準。答案鍵判定者註「答兩者而未指出平台端事實記不通過」3/3 觸發 |
| TDD-06a | 條文讀歪點（判準只在範例內） | 〈二〉只寫「三類驗收各對應不同形態」（講的是測試形態欄的組合值），緊接著寫「不得拆成三行」。條文沒有明寫斷言層可否合併。「合併會使某一類驗收失去對應的測試方法」只在已刪除的〈六〉範例三【驗證】段。r1 引同一段後判「可以一條斷言完成」。答案鍵附記同樣漏列 |
| TDD-07a | 條文讀歪點 | B3 在同一格內依序寫了「整則即標記無規格來源」「可追溯的斷言保留為有規格來源的測試」「拆為兩則：一則……對應規格來源」，沒有分步，也沒有交代標記作用在拆分前的原測試。r2、r3 都引了「整則即標記無規格來源」，(a) 卻答拆分後可追溯那一則的狀態「有規格來源」；r2 理由寫「至少有兩個斷言可追溯」，與 B3 首句直接相反 |
| TDD-08b | 條文讀歪點 | C2「事件、串流等由進入點觸發的輸出，記入觸發它的進入點條目，不另立條目」只給結論，沒有寫理由（輸出的驗收面向屬進入點條目的斷言，不是新的進入點）。r2 以「持續送出、多次觸發、獨立驗收維度」自設例外。r3 判定欄答另立，(3) 欄卻寫應記入 start()，自相矛盾，屬探針輸出不一致 |
| TDD-09a | 題目設計 | 子問 (b) 要求在自己／串接之間判定，但題幹「本 app 依驗證結果切換顯示內容」沒有給出是否牽涉其他元件。答案鍵理由「落在 app process 內，由自己決定」把「在 process 內」直接等同「自己決定」，但條文〈與既有三值的關係〉只推到「繼續問自己/串接/配置三選一」。r2、r3 以條件式答單元或整合，符合條文的推理深度。可在題幹寫明「切換顯示只依驗證結果的布林值，不讀取其他元件」 |
| TDD-11a | 條文讀歪點 | 訊號表「Given 設置過長 → 考慮拆分 Module」一格沒有附後置條件。「移除後 Given 設置仍 > 10 行 Mock，才考慮拆分 Module」只在表後〈多訊號同時成立時〉段的第二句。r3 只引第一句「優先於」，(b) 答成 Domain 訊號的觸發條件 |
| TDD-11b | 條文讀歪點 | 〈多訊號同時成立時〉段只規定多訊號的順序，沒有寫單一 Given 訊號時直接走表列動作。r2 把該段的後置步驟「Given 設置可移除」套到本題，本題沒有任何可拆出的邏輯（答案鍵判定者註標記的讀法在此觸發）。r3 把表格同一格內的診斷「Module 過大」當成待評估的假設，另加「若不過大則接受現設計」，條文沒有這條分支 |
| TDD-14b | 條文讀歪點 | 第一列「由本應用程式渲染的……」與第二列「匯出文件版面」對 app 產生的 PDF 版面同時成立。優先序只以括號「（優先本列）」夾在第二列列舉中間，沒有以句子寫明兩列同時成立時取第二列。r2 以「版面由本應用程式設計」判第一列，並把題幹的「本次新增」當成佐證；條文已明寫「不以該畫面是否本次新增」，但該句在〈前置閘門〉，不在通道表旁 |

#### 答案鍵附記條文風險逐條

| 附記項 | 本輪觸發 |
|---|---|
| TDD-01b（自行切段答 2 行以上） | 未觸發，3 份都答 1 行 |
| TDD-02b（依據只在範例內：理由須指出驗證對象差異）＝W3-463 項 1 | 未觸發。3/3 通過；r1、r2 在依據欄提到理由，但判定依據是引言句或反例列問句，沒有把理由當成合格條件 |
| TDD-04a（例示「回呼、事件監聽」被當成窮舉，輪詢判合格） | 字面讀法未觸發，3 份都判輪詢不合格；變體觸發：r1、r2 保留 isSaved 旗標，屬「框架內部表示方式（如節點樹結構）」範圍外的 app 狀態被放行，見歸因 |
| TDD-05a（分享面板不在列舉而判屬 app 內） | 未觸發，(a) 3/3 正確 |
| TDD-05b（答兩者或實機而未指出平台端事實） | 觸發 3/3 |
| TDD-06a（改答「不得拆成三行」而沒回答斷言可否合併） | 字面讀法未觸發，3 份都回答了斷言層；變體觸發：r1 以「不得拆成三行」段推出「可以一條斷言完成」 |
| TDD-09a（臉部辨識不在列舉而判屬 app 內） | 未觸發，(a) 3/3 正確 |
| TDD-11b（因多訊號判先做不移動邊界動作） | 觸發 r2（套用多訊號段的 Given 移除步驟） |
| 同構比對 TDD-10a（輕度同構） | 未觸發。3 份都依〈分工決策框架〉跨元件資料流列判串接，沒有照搬 SDK／collector 例示結論 |
| 同構比對其餘項（TDD-04a、05a、09a、06b、13b、14b、01b） | 未發現照抄例示字面 |

**與 W3-463 的 7 項重疊**：本批只含 TDD-02b（項 1），3/3 通過。11 題失敗都不屬 W3-463 的 7 項。

**新發現（供 PM 依規則 5 處置，本判定不建票）**：
- 依據只在範例內，答案鍵附記漏列 2 處：TDD-05a（驗證形態填「兩者」的判準，在〈六〉範例二【驗證】段）、TDD-06a（三類驗收不可合併為一條斷言，在〈六〉範例三【驗證】段）。TDD-05b 與 TDD-05a 同源（驗證形態三值沒有選值判準）。
- TDD-09a 答案鍵理由缺口，見上方歸因。

#### 同構抄答跡象

- 範例污染字串命中 0，removed 視窗命中 0。
- r2 TDD-08a 引「以可呼叫的公開進入點（此例為公開方法）為單位」。該句是節錄檔保留的範例引言，不是被刪的範例內容，判定依據 C2 條文，不算抄例示。
- 沒有發現照搬例示結論的情形。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引號內改寫 | r1 TDD-02a、08a、12a、12b；r2 TDD-05b、12a、12b；r3 TDD-03a、09a |
| 自擬語句加引號 | r1 TDD-04a；r2 TDD-02b、04a、06a、08b、10a、11b、14b、15a；r3 TDD-04a |
| 引用非餵入節錄檔 | r2 TDD-09a（〈與UC資訊鏈的關係〉） |
| 跨題題幹挪用 | r3 TDD-05a（TDD-09a 題幹） |
| 自相矛盾 | r3 TDD-08b（判定欄與 (3) 欄相反） |
| 多餘假設 | r1 TDD-05b；r2 TDD-04a、05b、06a、08b、14b；r3 TDD-08a |
| 依據句混入己見 | r1 TDD-02b、04b、08a、09b、12a、12b、15a；r2 多數題 |
### b4 tdd-1 判定後的處置（PM，2026-09-16）

**結果**：19/30，越界 0。

**處置**
- 10 題條文讀歪點，另含 05a、06a 兩則「判準只在範例內」（答案鍵漏標），全數轉 W3-466。W3-466 設 blockedBy W3-465，與 tdd 修補串行。
- TDD-09a（題目設計）：題幹未交代切換顯示是否牽涉其他元件。改題時補上前提，答案鍵理由改依條文推導。

**第 4 批小計**：tdd-1 19/30、tdd-2 28/30、tdd-3 21/26，共 68/86。

**待落檔修補**（皆為 tdd，須依序處理）：W3-463（7 項）→ W3-465（7 題）→ W3-466（10 題）。修補完成後，所有失敗題依節錄檔重跑。
### W3-457 與 W3-463.1 完成後的處置（PM，2026-09-16）

**W3-457 落檔**（ux-design-evaluation 1.11.7）
- 第 2 項 (a) 的最終定義已落檔；以 19 組輸入逐詞推導，結果與用戶四輪裁示全部一致，無不一致項。
- 風險：定義寫成單段長文，最弱讀者可能讀歪。依用戶「讀歪是內容問題」的裁示，交由 b2w 探針驗證（UX-09a、UX-09b，另新增 UX-09c 驗高度軸）；若失敗，再改為編號結構。

**W3-463.1 WRAP**
- TDD-21b：不需新判準，補一句路由即可。架構層級表是分配原則的派生結果，說明欄不成立時改依分配原則。原草稿「以較晚者為準」刪除。
- TDD-29b：規則之間本身不衝突，問題出在 tdd Q5 的適用範圍未寫與引錯列；只改 tdd，`.claude/rules/core` 不動。
- 用戶偏好項由 PM 依既有裁示處理：
  - U1：不另設「畫面票固定在最後」。
  - U2：維持 2.10.1 允許靜默的裁定。
  - U3：探針雖未觸發，屬實際條文缺陷，本輪修正。
- 改文由 W3-463 落檔。
- WRAP 另建 W3-467（P3），處理三表逐字重複的收斂，以及腳本常數與文件不一致。
### b3y 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b4 tdd-1 判定〉。
- 每題先只讀餵入節錄檔（`probes/excerpt/`；`SKILL--維度與產物`、`SKILL--本skill不做什麼` 檔頭 811720cb，其餘 d935df9e）自行推出判定，再比對答案鍵 `key/W3-417-b3y.md`，最後逐題判 3 份答案 `a/W3-417-b3y-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過；答案寫在依據句欄內記已答；以「或」並列且另一項正確，比照 FD-18b／TDD-07b 前例記通過。
- 核對腳本：`probes/tools/b3y_check.py`（由 `b4_tdd1_check.py` 改題號樣式，餵入表由題目檔「餵入」欄解析）。

**獨立推導與答案鍵比對**：10 題推導與答案鍵預期判定一致，答案鍵無誤。

**答案鍵引文核對**：50 段 ⟪⟫ 引文，去掉空白與反引號後比對該題餵入節錄檔，50 段都逐字存在。

**探針引文核對**（r1 27、r2 30、r3 37 段「」引文）：不在該題餵入節錄檔與題目中的片段如下，皆屬引號內改寫，不計分：
- FD-10b 三份、r2 FD-10c：引「規劃波之後」列判別欄時，括號內都刪去「不含本 skill 自身產出的地基票；」，其餘逐字。
- r1 FD-05b：括號內刪去「，因無權威可提供預設產物」，並跳過「選「無」仍須符合……」句直接接判準層，未加省略號。
- r3 FD-10c：「既有實作回答不了地基問題」是首列列名的改寫。
- 出處標錯：r1 FD-10b 依據句 2 標 examples-process，實際文字是 SKILL〈與 orchestration 的協作〉表「規劃波進行中」列；r1 TAD-02b 依據句 1 檔名寫成 `foundation-design--SKILL--9類型問題斷言分類決策表.md`（該檔不存在，內容屬本題餵入檔）。

**越界檢查**：
- (1) 答案鍵 9 條範例污染檢查字串：3 份答案都沒有命中。
- (2) `tools/removed/` 中本題組餵入的被刪文字：先去掉節錄檔與題目中也出現的片段，再以 22 字元視窗比對。r1、r2 命中 0。r3 命中 3 個重疊視窗，都是答卷格式標籤「依據句」緊接〈工作流〉步驟 5 條文句「複核者——先查該維度權威來源 skill／方法論」（節錄檔內有）。被刪範例【驗證】段恰好也是「依據句「複核者——先查……」」的寫法，屬字面巧合，不計。
- (3) 讀了非本題餵入的節錄檔：0。
- (4) 跨題題幹挪用：0。r1 FD-10b (2) 與 r2 FD-10c (2) 的角色句，分別與同份答卷相鄰題的預期角色同形，見失敗歸因。
- 越界合計：0。

**結果**：6／10。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| FD-05b | 不能；產物層依四種答案另判，選「無」須證明對象不存在 | 是 | 是 | 否（推導不出必要性時填「無」；答得出時填「改寫產物或待定」） | 否 | 條文讀歪點 |
| FD-10b | 規劃波之後；做：補判未涵蓋產物，不做：不重排既有票 | 否（(2) 做什麼寫「為 DevOps 與可觀測性各建盤點票，本流程結束」） | 是 | 是 | 否 | 題目設計 |
| FD-10c | 規劃波進行中；建 DevOps／可觀測性盤點票後交回 `version-bootstrap`，本流程結束 | 是 | 否（判規劃波之後） | 否（判其餘，角色交 `version-sequencing`） | 否 | 條文讀歪點 |
| FD-13a | 缺 ticket 系統＋步驟 4 與〈權威缺席時〉建票動作不成立 | 是 | 是 | 是 | 是 | — |
| FD-14b | 不可填張三；本框架既有審查代理人 | 是 | 是 | 是 | 是 | — |
| FD-15a | (a) 屬不可得；(b) 保留原值原名，疑義落決策文件待確認項，不在地基波內改 | 是 | 是 | 是 | 是 | — |
| FD-17a | (1) 分列；(2) server 已存在於 `deploy/server.sh`，web 照預設（待建） | 否（web 填「改寫產物」） | 是（web「照預設（或改寫產物）」） | 是 | 否 | 題目設計 |
| FD-21b | 不成立；增列一行，權威來源欄先指名既有權威，指名不到才轉形態 3 | 是 | 是 | 是 | 是 | — |
| TAD-02b | (1) 類型 1＋5；(2) 逐一；(3) 類型 1 移至隔離執行環境，類型 5 等最終狀態再斷言 | 是 | 是 | 是 | 是 | — |
| TAD-04a | (1) 不保留全結構快照；(2) 欄位／屬性斷言 | 是 | 是 | 是 | 是 | — |

判定者註：
- FD-05b r1：判定欄只寫「不能」，產物層依據寫在依據句欄（「產物層：依〈產物欄的五種合法答案〉四種作答」），記已答。r2 判定欄寫「產物層應依〈判定「失效不存在」的理由〉判定」，另引「答得出……走改寫產物或照預設；答不出時標待定」，雖然只寫了四種答案中「無」的判準，但沒有給出單一答案，也沒有以「沒有腳本」為由判「無」，記通過。
- FD-05b r3：判定句附帶兩處與條文不符：「推導不出具體必要性，應填「無」」與「答不出時標「待定」」相反；「答得出……應填「改寫產物」或「待定」」與「答得出……走「改寫產物」或「照預設」」不符。r3 (3) 答「是」，但沒有列出兩種判定。記不通過。
- FD-10c：r2、r3 在 (3) 都答「否」，沒有列出兩種判定，不適用 FD-10c 判定者註的題目設計歸類。r3 在推導中已把本 skill 前次的票排除（「地基票不算」），對應題幹與條文用語無誤；它的錯出在列的選擇。
- FD-13a r3：判定句第 (3) 項「為什麼形態 4 不套用」，條文沒有要求寫進降級記錄，也不是形態 4 的四欄決策記錄。不計分，記探針異常「無依據附加」。
- FD-14b r3：(4) 假設「本框架既有審查代理人的具體身份」，依答案鍵不扣分。
- FD-17a r2：以「或」並列，照預設一項正確，比照 FD-18b／TDD-07b 前例記通過。附帶理由「因為沒有任何部署配方」不符「改寫產物」的使用條件，但本題未餵入該表，見 FD-17a 歸因。若 PM 改採嚴格口徑，本題 r2 也不通過，通過數不變。
- FD-21b 權威來源欄填法（6.6.19 新增句是否讀到）逐份：r1 判定句「應先指名它到既有權威，指名不到才轉入」，讀到；r2 判定句「尋找該地基工作的權威來源填入權威來源欄」，讀到，但未寫「指名不到才轉形態 3」分支，屬元素層，不扣分；r3 三步分項寫出，讀到。三份都未斷言具體 skill。
- TAD-02b：三份 (3) 都寫出兩類型各自的處置；r1、r2、r3 都另附類型 1 的「功能測試套件禁用」，同格元素，不扣分。
- TAD-04a：三份都未引〈確定性斷言的基礎形態〉獨有句，也沒有把確定性類型寫成並列答案。

#### 失敗歸因

| 題號 | 歸類 | 具體結構 |
|---|---|---|
| FD-05b | 條文讀歪點 | 〈產物欄的五種合法答案〉「待定」列「何時使用」格裡，觸發條件「說不出它要防的失效在本專案長成什麼樣子」與排除句「權威存在但無可用部分屬判準層……單獨不構成填「待定」的理由」寫在同一格。本題幹命中排除句，r3 把排除範圍擴大到整個「待定」，推導不出時改填「無」。可承接「推導不出」的句子有兩處：〈判定「失效不存在」的理由〉「答不出時標「待定」」前面有「對象在本專案存在時」這個前置條件，本題對象存不存在正是答不出的部分；SKILL「判不出來一律填「待定」」在〈產物欄的五種合法答案〉表後段，不在本題「只讀」範圍內 |
| FD-10b | 題目設計 | r1 (1) 判對，依據句也逐字引出「規劃波之後」角色欄的做／不做兩項，條文沒有讀歪。(2) 的「做什麼」卻寫成「規劃波進行中」列的盤點票動作。FD-10b 與 FD-10c 餵入相同、題幹結構同形、題號相鄰，r1 下一題 FD-10c 的角色句正是這個字面，屬相鄰對照題之間的答案帶入。表後兩段「交回下游不等於……」「盤點票的驗收條件……」沒有標明只屬進行中列，也可能助長這個帶入，但 r1 沒有引這兩段，無法確認 |
| FD-10c | 條文讀歪點 | 「規劃波之後」列判別欄的粗體主詞只有「**該版票已建**」，排除句「不含本 skill 自身產出的地基票」藏在「定義同上列：」括號中段。三列判別欄都重複同一條件（已建／尚未建票＋定義同上），列與列的區分條件（`spec_refs` 非空對「未進入規劃波 pipeline」）沒有獨立成一欄。r2 引「規劃波之後」列時刪去括號裡的排除句，並以粗體主詞判已建票（題幹明寫 `version-bootstrap` 還沒開票）。r3 已排除地基票，卻用「尚未建票」這個兩列共用的條件去排除進行中列，改判「其餘」，也無視自己所引「也未進入規劃波 pipeline」與 `spec_refs` 非空的衝突（推導本身含自相矛盾） |
| FD-17a | 題目設計 | 子問 (2) 要求寫出產物欄答案形態，但餵入只有〈複合產物與多子樹分列〉，沒有〈產物欄的五種合法答案〉表。「照預設」「改寫產物」的使用條件（預設對形態成立／預設不適用或有害）不在餵入中，「照預設」一詞只見於題幹「照權威預設補建」。r1 把「補建」讀成改寫產物，r2 也附了同一個錯誤選項。改題時把該表加入 FD-17a 餵入 |

#### 與前一輪（b3／b3x）的失敗形態對照

| 題號 | 前一輪失敗形態 | 修補 | 本輪 | 生效情形 |
|---|---|---|---|---|
| FD-05b | b3 1/3：r2 未答依據、直接給「無或改寫產物」；r3 直接判「無」（沒有 seeder） | W3-455（6.6.17）形態 3 產物層改列四種答案、補「不得僅因不存在就代入」；題幹換輸入 | 2/3 | 部分生效：「因目前沒有該產物而判無」0/3 觸發，r1、r2 都答出產物層依據；r3 仍把「無」當推導不出時的出路，失敗點從「不存在即無」移到「判不出即無」。答案鍵條文風險 2（以「權威缺席」為由填待定）未觸發 |
| FD-10b | b3 r1 漏答「不做什麼」；b3x r2 把排除方向讀反，判規劃波進行中 | W3-460（6.6.18）「該版票」正向定義 | 2/3 | (1) 3/3 判對，「不做什麼」3/3 答出，原失敗點都消失；新失敗是 r1 (2) 帶入相鄰題角色。答案鍵條文風險 1 成立：FD-10b 的 (1) 全對不能證明排除方向已修好，要與 FD-10c 合看 |
| FD-10c | 新題（驗 b3x r2 讀反排除句是否消失） | 同上 | 1/3 | 未生效：排除方向仍有 2/3 讀錯。r2 刪掉括號內的排除句後判已建票；r3 排除地基票正確，列的選擇卻出錯。正向定義只寫在「規劃波進行中」列，「規劃波之後」列以「定義同上列」加括號轉述，粗體主詞沒有帶到排除 |
| FD-13a | b3 r3 另加形態 4 四欄決策記錄 | W3-455 降級段補「不套用形態 4」 | 3/3 | 生效 |
| FD-14b | b3 r1 附「或權威審查角色」；b3x r2 判定句帶權威角色分支 | W3-460 附註改為先後查找順序句 | 3/3 | 生效：三份判定句都只輸出「本框架既有審查代理人」一個分支 |
| FD-15a | b3x r1、r2 子問 (2) 被作答格式 (2) 吞掉 | 子問改 (a)(b) | 3/3 | 生效 |
| FD-17a | b3x r1 (2) 答無法給出；r2 自造「內容一致」 | 題幹補兩棵子樹的部署配方狀態 | 1/3 | 原失敗點生效：(1) 3/3 分列，(2) 3/3 寫出兩格且 server 格全對。新失敗點來自餵入缺五種答案表（web 格選錯形態） |
| FD-21b | b3x r2 處置直接走形態 3 | W3-461（6.6.19）補權威來源欄填法與轉形態 3 條件；W3-464（6.6.20）修正引號字串 | 3/3 | 生效：三份都沒有直接走形態 3，也都讀到權威來源欄填法 |
| TAD-02b | b3x r1 未答各類型處置 | 子問 (2) 拆成 (2)(3) | 3/3 | 生效 |
| TAD-04a | b3x r2、r3 跳到非餵入〈確定性斷言的基礎形態〉答邏輯不變式 | 題幹去掉「形態」、換輸入；W3-459 補兩軸關係 | 3/3 | 生效：跨檔引用 0 |

#### 答案鍵條文風險逐條

| 附記項 | 本輪觸發 |
|---|---|
| 1. FD-10b 字面對上即判規劃波之後 | 成立：FD-10b (1) 3/3，FD-10c 1/3 |
| 2. FD-05b 以「權威缺席」為由填待定 | 未觸發 |
| 3. FD-10c 無法把前次執行的票對到條文用語而列兩種判定 | 未觸發：r2、r3 (3) 都答否；r3 已正確對應 |

#### 同構抄答跡象

- 範例污染字串命中 0；removed 視窗命中 r3 3 個，屬格式標籤巧合（見越界檢查）。
- FD-05b、TAD-04a 換輸入後，答卷沒有出現 seeder、加總、邏輯不變式等被換掉的字面。
- 沒有發現照搬例示結論的情形。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引號內刪節 | r1、r2、r3 FD-10b，r2 FD-10c（都刪去「規劃波之後」列括號內的排除句）；r1 FD-05b |
| 引號內改寫 | r3 FD-10c（首列列名） |
| 出處標錯 | r1 FD-10b 依據句 2；r1 TAD-02b 依據句 1 檔名 |
| 無依據附加 | r3 FD-13a 第 (3) 項 |
| 自相矛盾推導 | r3 FD-10c |
| 多餘假設 | r3 FD-14b（依答案鍵不扣分） |
| (3) 答「是」但沒有列出判定 | r3 FD-05b |

**新發現（供 PM 依規則 5 處置，本判定不建票）**：
- FD-10c：「規劃波之後」列與「其餘」列的判別欄以「定義同上」轉述排除句，粗體主詞不含排除；三份 FD-10b 與 r2 FD-10c 的引文都在同一位置刪掉排除句。這與 W3-460 的修補範圍相鄰，但現行條文未涵蓋。
- FD-05b：「待定」列同一格內觸發條件與排除句並排；〈判定「失效不存在」的理由〉「答不出時標待定」受「對象存在時」限定，對象存否說不出時沒有承接句。
- FD-17a：題目設計，餵入缺〈產物欄的五種合法答案〉表。
- FD-10b：題目設計，相鄰對照題之間的答案帶入。
### b3y 判定後的處置（PM，2026-09-16）

**結果**：6/10，越界 0。
- 修補完全生效：FD-13a、14b、15a、21b、TAD-02b、04a。
- 部分生效：FD-05b、10b、17a，原失敗點已消失，但出現新的失敗點。
- 未生效：FD-10c。

**處置**
- FD-05b、FD-10c：條文讀歪點，開 W3-468。
  - FD-05b：「待定」列的排除句與觸發條件寫在同一格。
  - FD-10c：「規劃波之後」列的粗體主詞沒帶上「排除本 skill 產出的地基票」。
  - W3-460 的正向定義只寫在進行中列，沒有延伸到之後列。
- FD-10b：題目設計。與 FD-10c 相鄰、同形、餵入相同，答案互相帶入。改題時把 FD-10b 與 FD-10c 分到不同題目檔，由不同探針作答。
- FD-17a：題目設計。餵入缺〈產物欄的五種合法答案〉表，改題時補上。

**方法規則增補**：相鄰且同形的對照題不要放在同一題目檔，避免答案互相帶入。
### b2w 出題後的處置（PM，2026-09-16）

**b2w 出題者在已落檔的 1.11.7 定義中找到六處結構問題**

| # | 問題 | 性質 |
|---|---|---|
| 1 | 例示「不加底色與外框的純文字、圓點」讀起來像無條件合格，但依定義，純文字與文字連結比較時會不合格 | 例示與定義衝突 |
| 2 | 高度軸不比或不成立時，算「相同」還是「不適用」未寫明 | 讀歪風險 |
| 3 | 「多出一項」規則在第三個元素沒有區塊時，結果未定義 | 定義缺口 |
| 4 | (a) 要與全畫面比較，(b) 只要移出動作列，兩者射程不一致；段首段尾寫「按鈕」，定義內寫「可點元素」 | 邏輯衝突 |
| 5 | 單段長文沒有編號 | 讀歪結構 |
| 6 | 「圖示」與「純幾何形狀」的界線模糊（勾號、箭頭、三角形） | 定義缺口 |

**處置**
- #1、#3、#4 是邏輯缺陷，先跑探針必然失敗，所以先修再跑探針。
- 開 W3-457.5：WRAP 改寫為編號結構，逐項解決六處問題，並以 22 組輸入（19 組 + b2w 3 題）重新推導。#3、#4、#6 的選項交用戶裁示。
- b2w 探針延後到修正落檔之後。

**UX-09b 題幹同構**：題幹與 W3-457.3 第 2 輸入同構，也與被刪範例同構。依同構規則換輸入，修正落檔後再出題。
### W3-463／W3-465 完成後的處置（PM，2026-09-16）

**W3-463 已結案**（tdd 2.10.3）：TDD-21b 路由句已放入兩處，TDD-29b 已改寫 Q5「靜默」段。

**W3-465（tdd 2.10.4，停在 needs_context）**
- 已落檔：TDD-33a（Q8 標題改用行動路徑既有字詞）、TDD-35a（Q10 補路由），出處表 2 句都有出處。
- 轉草稿：TDD-20b、36a、37b、39a，查無非範例出處，交 W3-465.1 WRAP 評估。

**並行寫入核對（PM 查 HEAD）**
- TDD-21a：〈並行判斷〉表已改為「不同架構層，且無依賴」。這是 W3-465 的編輯，因同檔並行被 W3-463 的 commit cb288c921 一併提交，兩張票的出處表都沒有記這一句。
  - 出處判定：修改只把「但」改為「且」，屬措辭，未新增判準，不另補出處表。
- W3-463 的兩處新增（StarRatingBar 反例、清除本機過期推播紀錄反例）在 HEAD 都存在。

**教訓**
- W3-463 與 W3-465 同 skill 並行，雖然各有禁區，仍在同一檔（phase1/rules.md）互相覆寫與夾帶。
- 禁區只切了段落，沒有切檔案。之後同一 skill 的並行修條文票，禁區一律以檔案為單位劃分。
### 第 5 批出題後的處置（PM，2026-09-16）

**規模**：26 則範例（原盤點 24、W3-437 漏列 2、兩次皆漏 0），52 題。自檢兩項與 verify_head 通過，124 句引文全部命中。

**是否算範例的裁定**
- 範本〈回饋通道子表〉預填值、〈狀態矩陣〉預填列、legacy-code-workflow 錯誤／正確順序圍欄：依 PM 定義 2，屬範例外的範本或規則圍欄，算條文，不算範例。
- E-04 範例後附註：視為 E-04 範例的一部分；其判定是否在條文中有出處，交 W3-469 核對。

**條文風險 15 條、依據部分只在範例 2 則**：轉 W3-469。

**探針**：兩檔各 3 份立即派出，讀快照，與 W3-469 並行。
### UX-09a 第五輪用戶裁示與 b3z／b5 派發（PM，2026-09-16）

**W3-457.5 發現**：1.11.7 落檔時把 U6-C「與那個按鈕不同」收窄成「在區塊高度軸上不同」，找不到出處。19 組推導無法區分兩種寫法，所以沒有攔下來。

**用戶裁示**
- #3：3-B，至少一軸不同，回復用戶原意。
- #4：4-A，(b) 只看指示所在的列。
- #6：6-B，純幾何形狀採封閉清單，勾號、箭頭、三角形算圖示。
- #7：7-a，高度不在 token 階梯上時，當作沒有 token，高度軸記為相同。

**落檔**：開 W3-471，以 W3-457.5 編號結構草稿為準。落檔前以 22 組＋D3、D3'、D4 共 25 組逐詞推導。

**方法教訓**：推導集合要包含能區分各選項的輸入。只拿 19 組驗證「與裁示一致」，攔不下落檔時的無出處收窄。

**b3z 計分口徑（接受出題者提案）**：FD-17a 餵入已補表，且題幹寫明「照權威預設補建」。判定句用「或」並列「照預設」與「改寫產物」者，改記不通過，因為並列的另一項與題幹條件相反。

**派發**：b5 兩檔判定已派出。b3z-1 與 b3z-2 由不同探針各跑 3 份。
### b5 doc-2 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3y 判定〉。
- 每題先只讀該題餵入的節錄檔（`probes/excerpt/`，HEAD 17ab03bf）自行推出判定，再比對答案鍵 `key/W3-417-b5-doc-2.md`，最後逐題判 3 份答案 `a/W3-417-b5-doc-2-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過；答案寫在依據句欄內記已答；以「或」並列且另一項正確，比照 FD-18b／TDD-07b 前例記通過。
- 核對腳本：`probes/tools/b5_doc2_check.py`（由 `b3y_check.py` 改題號樣式與檔名，餵入表由題目檔「餵入」欄解析）。
- 為確認 DOC-19a 歸因，判定者在推導與計分完成後查閱了原始範例檔 `doc/examples/component-library-chained-examples.md` 對應條目（非探針餵入）。

**獨立推導與答案鍵比對**：22 題推導與答案鍵預期判定一致，答案鍵判定無誤。答案鍵附記「依據只在範例內」漏列 DOC-19a，見失敗歸因。

**答案鍵引文核對**：49 段 ⟪⟫ 引文，去掉空白與標點後比對該題餵入節錄檔，49 段都逐字存在。

**探針引文核對**（r1 46、r2 41、r3 52 段「」引文）：腳本列出的未命中片段，多數是 yaml 註解 `#`、佔位符 `{}` 未被正規化剝除所致，原文存在。實際偏離如下，都不計分：
- 出處混用：r1 DOC-17a 把方法論句與 slot 契約引言句（「key 本身由元件持有不開放替換……」）以「...」接成一段，整段標為方法論檔；r2 DOC-17a「強語意...得由元件引用 i18n key 作預設值，三條件 AND」標為 slot 契約檔，但「作預設值」是方法論表的字面。
- 引號內改寫：r3 DOC-16b「有無 / 步進按鈕 / 輸入彈窗」多一個「有」；r2 DOC-25a「traverses：必填，欄位必存在」、DOC-25b「branch_from：僅替代場景步驟填寫……」加了欄名前綴；r1 DOC-16b「滑桖軌道」錯字。

**越界檢查**：
- (1) 答案鍵 28 條範例污染檢查字串：3 份答案都沒有命中。
- (2) `tools/removed/` 中本題組餵入的被刪文字：先去掉節錄檔與題目中也出現的片段，再以 22 字元視窗比對，3 份命中都是 0。
- (3) 讀了非本題餵入的節錄檔：0（逐題比對答卷引用的檔名，全部屬該題餵入）。
- (4) 跨題題幹挪用：0。
- 越界合計：0。

**結果**：21／22。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| DOC-16a | 否；不足；替代機制欄 | 是 | 是 | 是 | 是 | — |
| DOC-16b | 足夠；無 | 是 | 是 | 是 | 是 | — |
| DOC-17a | 元件預設 key；插值參數 | 是 | 是 | 是 | 是 | — |
| DOC-17b | 呼叫端；不違反 | 是 | 是 | 是 | 是 | — |
| DOC-18a | 否；「呼叫端傳入；本元件無自有 key」 | 是 | 是 | 是 | 是 | — |
| DOC-18b | 否；是 | 是 | 是 | 是 | 是 | — |
| DOC-19a | 可；可 | 是 | 否（(a) 判不可） | 否（(a) 判不可） | 否 | 條文讀歪點（判準只在範例內） |
| DOC-19b | 否；所在容器（表格）排列不變式 | 是 | 是 | 是 | 是 | — |
| DOC-20a | 否；各一則共三則 | 是 | 是 | 是 | 是 | — |
| DOC-20b | 替代通道；否 | 是 | 是 | 是 | 是 | — |
| DOC-21a | 實作註記；回饋契約表格之後 | 是 | 是 | 是 | 是 | — |
| DOC-21b | 契約值變更；否 | 是 | 是 | 是 | 是 | — |
| DOC-22a | 是；加一列「安全區」 | 是 | 是 | 是 | 是 | — |
| DOC-22b | 否；是 | 是 | 是 | 是 | 是 | — |
| DOC-23a | 列入；第 5 章對應容器 | 是 | 是 | 是 | 是 | — |
| DOC-23b | 不列；列入；封裝元件 | 是 | 是 | 是 | 是 | — |
| DOC-24a | 否；不成立 | 是 | 是 | 是 | 是 | — |
| DOC-24b | 可以；「本表即白名單」 | 是 | 是 | 是 | 是 | — |
| DOC-25a | [Order, Inventory]；[]；否 | 是 | 是 | 是 | 是 | — |
| DOC-25b | 否；confirm-payment | 是 | 是 | 是 | 是 | — |
| DOC-26a | 否；`core/design_system/` | 是 | 是 | 是 | 是 | — |
| DOC-26b | 合格；不合格 | 是 | 是 | 是 | 是 | — |

判定者註：
- DOC-19b：r1「所在容器的排列不變式」、r2「容器的排列不變式」、r3「表格容器的排列不變式」，都沒寫括號內的「最小間距」。答案鍵把最小間距放在括號裡，屬補充說明，記通過。
- DOC-23a：r1、r3 在 (3) 答「是」，列出的另一種讀法是「指向待建立的格線容器」，仍然指向第 5 章容器。依答案鍵判定者註「補述須新建第 5 章容器條目不扣分」，記通過。
- DOC-16a／16b：r2 把比較寫成「6px < ±8px 故不足」「1px < 6px 故足夠」，兩句不等號方向的語意不同（前句比間距與誤差，後句比精度與間距），但結論都對，不扣分。
- DOC-26a：r2、r3 另外引「跨 2+ domain 或跨層使用」列作輔助依據，與強制規則指向同一目錄，不扣分。

#### 失敗歸因

| 題號 | 歸類 | 具體結構 |
|---|---|---|
| DOC-19a | 條文讀歪點（判準只在範例內） | 〈組合規則〉引言首句「與鄰件的間距不在此定義」寫在表前，沒有限定範圍。允許句「內距（引用 token）」只出現在表格「作為表格或列表的一欄時」列的佔位符大括號內。「內距屬元件邊界內部的空間、與鄰件間距不同」這條分界只寫在已刪範例〈組合規則「作為表格或列表一欄時」內距與「不得另定間距」的分界〉的【驗證】段；範本的範例路由句也把這條分界指向範例。r2、r3 引首句判 (a) 不可，但同一佔位符內的「欄比例」判可，可見兩份把佔位符裡的「內距」讀成首句禁止的「間距」。答案鍵附記的條文風險 11 在本題觸發 2/3；答案鍵「依據只在範例內」寫其餘題 0 處，漏列本題 |

#### 答案鍵附記條文風險逐條

「W3-469」欄：15 條風險都在 W3-469 的 18 項內（第 1–15 項），DOC-22b 另對應其中的「依據部分只在範例」項。

| 附記項 | 本輪觸發 | 與 W3-469 重疊 |
|---|---|---|
| 1. 三層模型同名異義 | 本檔無對應題（屬 doc-1 DOC-15），不適用 | 重疊（項 1） |
| 2. 第 2 層時間點與 ≥ 10s 列 | 本檔無對應題，不適用 | 重疊（項 2） |
| 3. 樂觀更新「低風險」時點 | 本檔無對應題（DOC-05），不適用 | 重疊（項 3） |
| 4. 框架容器括號是否窮舉 | 未觸發：DOC-22a、22b 3/3，題幹沒有使用浮層輸入 | 重疊（項 4，另含 DOC-22b 項） |
| 5. 單一 domain token 放 domain/constants 與集中規則衝突 | 未觸發：DOC-26a 題幹寫三個 feature 避開；3 份都判集中目錄 | 重疊（項 5） |
| 6. 跨 domain 純業務常數位置兩說 | 未觸發：DOC-26b (a) 只考單一 domain | 重疊（項 6） |
| 7. 「跨層」未定義 | 未觸發；DOC-26b (b) 題幹與例示同構，3/3 都引「如 infrastructure 層 import domains/ 的常數」，只能證明讀到例示 | 重疊（項 7） |
| 8. 降級須標示只在範例 | 本檔無對應題（DOC-14），不適用 | 重疊（項 8） |
| 9. 互動瞬態定義句與推導值句 | 本檔無對應題，不適用 | 重疊（項 9） |
| 10. 播報替代填在哪格 | 本檔無對應題，不適用 | 重疊（項 10） |
| 11. 規則內容位於佔位符 `{…}` | 觸發：DOC-19a r2、r3（組合規則佔位符）；DOC-20a 3/3 把測試點佔位符讀作規則，未觸發 | 重疊（項 11） |
| 12. 操作機制佔位例示複寫範例 | 未觸發：題幹改用座位圖；r1、r3 DOC-16b 引滑桿佔位例示作輔助依據，但判定仍依兩欄數值比較 | 重疊（項 12） |
| 13. 範例與範本表格欄位格式不一致 | 未觸發：答卷沒有產出表格 | 重疊（項 13） |
| 14. 封裝失敗後主張豁免的類別未交代 | 未觸發：DOC-24a 沒有問類別歸屬 | 重疊（項 14） |
| 15. branch_from 依實際分岔點只以案例敘事呈現 | 未觸發：DOC-25b (b) 3/3 答 confirm-payment | 重疊（項 15） |
| 依據只在範例內：DOC-22b（窮舉讀法） | 未觸發：3/3 依「不屬頁面根堆疊或殼層」判否 | 重疊（DOC-22b 項） |

#### 同構抄答跡象

- 範例污染字串命中 0，removed 視窗命中 0。
- DOC-17b：題幹「關閉」與例示 confirm／cancel 同類。3 份 (b) 都以「無可覆蓋對象，非禁止覆蓋」為依據，沒有援引例示。
- DOC-26b (b)：3 份的依據都是強制規則例示句本身，見風險 7。
- DOC-16b：r1、r3 引 `{無 / 步進按鈕 / 輸入彈窗}` 與滑桿佔位例示，屬條文佔位內容，不是被刪範例；結論沒有照搬例示。
- 沒有發現照搬被刪範例結論的情形。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 出處混用 | r1 DOC-17a；r2 DOC-17a |
| 引號內改寫 | r3 DOC-16b；r2 DOC-25a、25b |
| 錯字 | r1 DOC-16b（滑桖） |
| (3) 答「是」 | r1 DOC-23a；r3 DOC-23a（另一讀法仍指向第 5 章容器） |
| 多餘假設 | r1 DOC-21a、23a、26a；r2 DOC-23b；r3 DOC-23a |

**新發現（供 PM 依規則 5 處置，本判定不建票）**：
- DOC-19a：內距與鄰件間距的分界只寫在範例內，答案鍵「依據只在範例內」漏列。條文只有佔位符「內距（引用 token）」，引言首句「與鄰件的間距不在此定義」與〈5〉「子件的組合規則不得另定間距」都沒有排除內距。
- DOC-24b（未觸發）：〈7〉表「白名單登記」欄佔位符是 `{是/否}`，引言卻要求該欄填工具登記鍵或「本表即白名單」，兩者不一致。本輪 3 份都依引言作答。
- DOC-26b（未觸發）：強制規則末句「純業務常數……不在本規則範圍」裡的「本規則」可以讀成整份強制規則清單，這樣跨層 import 禁令是否約束純業務常數就不明確。與風險 7 相鄰；本輪 3 份都判不合格。
### b5 doc-1 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3y 判定〉。
- 每題先只讀該題餵入的節錄檔（`probes/excerpt/`，HEAD 17ab03bf），自己推出判定；再比對答案鍵 `key/W3-417-b5-doc-1.md`；最後逐題判 3 份答案 `a/W3-417-b5-doc-1-r{1,2,3}.md`，3 份全對才算通過。
- 計分規則沿用 b3y：
  - 整個子問沒答，記不通過。
  - 判定句附帶與條文或題幹不符的內容且沒有更正，記不通過。
  - 以「或」並列且另一項正確，記通過。
- 核對腳本：`probes/tools/b5_doc1_check.py`（由 `b3y_check.py` 改題號樣式與檔名）。

**獨立推導與答案鍵比對**：30 題推導都與答案鍵預期判定一致，答案鍵無誤。DOC-04b (b) 起初考慮過「只有視覺通道，要不要另補播報」，依子表格內容三選一的 (a)「原生具備此回饋管道，填具體通道形式」判合格，與答案鍵一致。

**答案鍵引文核對**：75 段 ⟪⟫ 引文，去掉空白與標點後比對該題餵入的節錄檔，75 段都逐字存在。

**探針引文核對**（r1 99、r2 69、r3 70 段「」引文）。不在該題餵入節錄檔與題目中的片段如下，都不計分：
- 引號內刪節：r1 DOC-01b 把「是否支援仍屬用戶簽核決策」與「支援幾個形態屬用戶簽核決策……」接成一句，中間跳過「每個支援的形態各自設計容器版型……」整句，沒有加省略號；r3 DOC-08b「loading 一律入狀態矩陣」是把括號清單壓縮成單項。
- 引號內改寫或改字：r2 DOC-04b 把「游標」寫成「遊標」；r1 DOC-15a／15b 在表格列引文前加上表頭「等待時間」。
- 引號用於自己的用語或轉述題幹：r1 DOC-02b／07b／15b，r3 DOC-03a／04b／14b。
- 出處標錯：r2 DOC-14a 依據句 2 標為方法論節錄檔，實際文字在範本〈回饋契約〉節錄檔。兩檔都屬本題餵入，不算越界。
- 依據與判定錯位：r2 DOC-11a (b) 判定「只填一項」正確，依據句卻引「多條途徑拆列」，沒有引「每列只填一項」。

**越界檢查**：
- (1) 答案鍵 28 條範例污染檢查字串：3 份答案都沒有命中。
- (2) `tools/removed/` 中本題組餵入的被刪文字，以 22 字元視窗比對（先去掉節錄檔與題目中也有的片段）：r1、r2、r3 都命中 0。
- (3) 讀了非本題餵入的節錄檔：0。依據句標注的檔名都屬該題餵入。
- (4) 挪用其他題題幹：0。r1 DOC-15a 答案 (c) 與依據句 2 和下一題 DOC-15b 的表格列逐字相同，屬相鄰同構題之間的答案帶入（兩題餵入相同），不是挪用題幹，見失敗歸因。
- 越界合計：0。

**照抄同構例示**：
- 污染字串與 removed 視窗都命中 0。
- DOC-05 清空回收筒、DOC-07b 圖示＋文字這兩處輕度同構：3 份答案的判定都依定義句作答，沒有照搬例示結論。
- DOC-06 三份都沒有把例示方向（出現一次→L2、出現於每個畫面→L3）直接當答案。
- 沒有發現抄例示的情形。

**結果**：21／30。

#### 逐題表（只列未通過題；其餘 21 題 3/3）

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 |
|---|---|---|---|---|---|
| DOC-02b | (a) 否，第 5 章容器每形態各一份；(b) 否，變體跨形態共用 | 否（(a) 合格） | 是 | 否（(a) 合格；(b) 應加欄或加列） | 否 |
| DOC-03a | (a) 可；(b) 使用者於設定項切換 | 否（(a) 不可填換版型，(b) 卻答使用者設定） | 是（(4) 假設「{視窗尺寸} 下的行為」列可填使用者驅動的換版型） | 是 | 否 |
| DOC-04a | (a)(b)(c) 皆否 | 是 | 是 | 否（(b) 判合格：「已確認無等價替代」） | 否 |
| DOC-04b | (a) 不成立；(b) 合格 | 否（(b) 判不合格：「應具體寫通道形式而非籠統詞」） | 是 | 否（(b) 判不合格：「只寫了視覺回饋，應補充其他回饋通道」） | 否 |
| DOC-07b | (a) 一項：數值；(b) 可 | 否（(a) 兩項：數值＋標籤，電池圖示表示狀態） | 是 | 是 | 否 |
| DOC-09b | (a) 合格；(b) 未定義 | 否（(a) 判不合格：缺觸發事件） | 是 | 是 | 否 |
| DOC-12b | (a) 否；(b) 同一列同一格逐條列出 | 是 | 是 | 否（(a) 判合格，(b) 又答「併列為一列三條邊」） | 否 |
| DOC-13a | (a) 服務不可用；(b) 失敗 | 否（(a) 逾時，括號寫「無法送達，結果未知」） | 是 | 否（(a) 失敗、(b) 服務不可用，兩者對調） | 否 |
| DOC-15a | (a) 第 2 層；(b) 否；(c) Spinner／按鈕 loading 狀態 | 否（(c) 進度指示 + spinner） | 否（(a) 第 2 層和第 3 層） | 是 | 否 |

**判定者註**
- r1 DOC-15b (b)：判定句寫「否，4 秒對應「進度指示 + spinner」而非「進度指示 + spinner」；但根據表格……對應「進度指示 + spinner」，撰寫者填寫正確」。句首「否」與句內更正互相矛盾，但同一句已改正為正確結論，依「附帶不符內容但有更正」口徑記通過。(3) 另列「+ 可能是兩個選項」的歧義，但節錄檔已明寫「+」是組合策略，屬探針異常。若 PM 改採嚴格口徑，DOC-15b 不通過，通過數變為 20/30。
- r2 DOC-14a／14b、r1 DOC-14b：(b) 答「不只通道可用性，還需考慮元件寬度」，沒有斷言通道可用性一定是前提，記通過。
- r2 DOC-14a (3) 另列「應用在前景」「使用者未禁用通知」，屬同一機制的細分，不扣分。
- r3 DOC-12a (b) 只寫「改為 selected」，沒有寫出整條邊，但題幹只有一個狀態名錯誤，記通過。
- 答案鍵判定者註觸發情形：
  - DOC-02b「答要加欄記不通過」：r3 觸發。
  - DOC-07b「答標籤記不通過」：r1 觸發。
  - DOC-04b「以非游標變手形為由判不合格」：未觸發（r1、r3 的理由不同，見歸因）。
  - DOC-13a「以括號列舉為由改判」：未觸發（r1、r3 都沒有提括號列舉）。
  - DOC-05b（AND 讀成 OR）、DOC-10b（以非輸入事件期間為由判資料態）、DOC-12a（新增 checked 狀態）、DOC-14a（答伺服器或網路可用）、DOC-15b（以題外欄位判不完整）：都未觸發。

#### 失敗歸因

| 題號 | 歸類 | 具體結構 |
|---|---|---|
| DOC-02b | 條文讀歪點 | 〈1. 形態因素矩陣〉多形態段把三個規則用分號串成一句：第 4 章「於同一子節表格內以形態為維度加欄或加列」、第 5 章「每形態各一份完整條目（含排列不變式）」、「其餘子節跨形態共用」。問題有兩處。一是兩種展開方式緊鄰，沒有說明容器為何不能比照第 4 章在表內加欄：r1 引了第 5 章子句仍判合格，r3 直接套用第 4 章加欄規則。二是「其餘子節」緊接在第 5 章子句後面，指的是第 4 章未列名子節還是第 5 章其他內容，字面不明：r3 (b) 對變體答加欄；r1 (b) 雖然答對，(4) 仍需假設「其餘子節包含變體」 |
| DOC-03a | 條文讀歪點 | 〈尺寸契約〉表列名是「{視窗尺寸 N} 下的行為」，引言也說「每種視窗或裝置尺寸各填一列」，列以視窗尺寸為鍵；但「換版型」只在第 1 章策略 (b)「使用者可選版型」時可選，觸發來源是設定項，不是視窗尺寸。在以視窗尺寸為鍵的列裡填使用者觸發的換版型，字面上像依視窗尺寸切版。r1 (a) 因此判不可填，(b) 卻答出使用者設定；r2 雖判對，也要在 (4) 假設該列可填使用者驅動的值 |
| DOC-04a | 無法歸入三類（最接近題目設計，證據弱） | 條文 (c)「完全無等價替代，不得留空」與題幹都沒有歧義。r3 (b) 判合格，與它自己引的依據句直接矛盾，也與同份 (c)「未附追蹤票視同留空，不合格」矛盾。可能的誘因是 (b) 題幹「已確認……完全沒有等價的替代通道，撰寫者因此把該格留空」，用「因此」把留空寫成確認後的結果；(c) 又以「承 (b)」接續，探針把 (b) 當成 (c) 之前的合法中間步驟。這個誘因沒有直接證據 |
| DOC-04b | 條文讀歪點 | 兩份失敗答卷落在兩個不同的結構點。r1：〈回饋通道子表〉「通道須具體到形式」只有正例（游標變手形）與反例（視覺）各一，沒有給判準區分「具體的視覺形式」與籠統詞「視覺」，r1 把「邊框轉強調色」判為籠統詞。r3：子表引言要求「播報替代不因該形態非觸控裝置而豁免」，但格＝「可用通道集合」，沒有交代播報替代要填在哪一格，r3 因此要求同一格補其他通道（答案鍵附記條文風險 10 觸發） |
| DOC-07b | 條文讀歪點 | 內容角色引言先列四類定義，後寫「兩部分描述同一件事……歸同一角色」，但沒有寫明合併句優先於逐部分歸類。標籤「標記對象……目前狀態」與數值「可計量或比較的量」的邊界也沒有界定（答案鍵 DOC-07b 判定者註所列結構）。r1 先逐部分歸類（圖示＝狀態→標籤，文字＝數值），(3) 雖列出「一種或兩種角色」的歧義，判定仍取兩種 |
| DOC-09b | 題目設計（證據弱） | r1 (a) 對「使用者點擊重試 → uploading」判「缺少觸發事件」，這個理由本身不成立；(b) 寫「缺少觸發事件同樣視為未定義」，「同樣」一詞表示 (a) 的判定是從 (b) 帶入。兩個子問問的是同一欄位的正反兩例，且 (b) 的缺陷正是「缺觸發事件」，屬子問之間的答案帶入。條文〈四欄定義〉退出路徑列沒有讀歪點 |
| DOC-12b | 條文讀歪點 | 〈回饋契約〉引言「同一事件有多條邊時於同一格逐條列出，不另增列」裡，「逐條列出」的「列」是條列，「不另增列」的「列」是表格列，兩者同字；題幹「拆成三列」也可讀成「逐條列出三條」。r3 (a) 引這一句判合格，(b) 又答「一列三條邊」，前後矛盾正好落在「列」的兩種字義上 |
| DOC-13a | 條文讀歪點 | 〈回饋契約〉四結局定義以「逾時＝請求已送出」與「服務不可用＝請求無法送達」區分，沒有界定客戶端在發出前或解析階段就失敗（網域名稱解析失敗）算不算「已送出」。r1 同時套用兩個定義（判逾時，括號卻寫「無法送達，結果未知」）。r3 把 (a)(b) 對調，(b) 優惠碼過期判服務不可用，這無法以該結構解釋，屬探針隨機錯誤疊加 |
| DOC-15a | r1 題目設計；r2 條文讀歪點 | r1：DOC-15a 與 DOC-15b 餵入相同、題幹同形、題號相鄰，r1 在 15a (c) 寫出 15b 的時間帶與策略，依據句也逐字取自 15b 那一列，屬相鄰同構題的答案帶入。〈b3y 判定後的處置〉方法規則增補（相鄰且同形的對照題不放同一題目檔）在本批沒有落實。r2：範本表列名「等待與結果」同時涵蓋等待（第 2 層）與結果（第 3 層結果通知），四段結局也是第 3 層內容；但引言要求「判定本元件承載哪一層」（單數），沒有交代結果段不另計層。r2 因此判「第 2 層和第 3 層」，(3) 也列出這個歧義。與答案鍵附記條文風險 1（三層模型同名異義）相鄰，但不是同一個點 |

#### 答案鍵附記條文風險逐條（doc 第 5 批共用清單，本表只記 doc-1 題組）

| 附記項 | 本輪觸發 | 與 W3-469 18 項重疊 |
|---|---|---|
| 1. 三層模型同名異義（範本路由 ux 三層 vs 方法論〈掛載點〉第二／三層 vs E-15 SPEC） | 未直接觸發（本題組沒有餵入方法論〈掛載點〉）。相鄰點觸發：r2 DOC-15a 因列名「等待與結果」跨第 2、3 層而答兩層 | 重疊（W3-469 第 1 項）；建議處置時一併涵蓋「承載層是否含結果段」 |
| 2. 第 2 層 100ms-10s 與 ≥10s 列承載層不明 | 未觸發（題幹 700ms、4 秒避開） | 重疊（第 2 項） |
| 3. 樂觀更新「低風險」時點 | 未觸發（DOC-05 以可逆分支出題，3/3 通過） | 重疊（第 3 項） |
| 4. 〈5〉框架容器括號是否窮舉 | 不適用（doc-2 題組） | 重疊（第 4 項） |
| 5. 〈10〉單一 domain 常數位置衝突 | 不適用（doc-2） | 重疊（第 5 項） |
| 6. 〈10〉跨 domain 純業務常數位置 | 不適用（doc-2） | 重疊（第 6 項） |
| 7. 〈10〉跨層 import 未定義 | 不適用（doc-2） | 重疊（第 7 項） |
| 8. 降級後須標示已降級只在 E-14 | 未觸發（DOC-14 未問此點，6 格全對） | 重疊（第 8 項，同 W3-469「DOC-14b 依據部分只在範例」） |
| 9. 互動瞬態定義句與推導值句的張力 | 未觸發（DOC-10b 3/3 判互動瞬態） | 重疊（第 9 項） |
| 10. 播報替代填在子表哪一格未交代 | 觸發：r3 DOC-04b (b) 要求同一格補其他通道 | 重疊（第 10 項） |
| 11. 〈測試點〉〈組合規則〉規則位於佔位符大括號內 | 不適用（doc-1 未餵入） | 重疊（第 11 項） |
| 12. 〈操作機制〉佔位例示複寫 E-16 | 不適用（doc-1 未餵入） | 重疊（第 12 項） |
| 13. 範例與範本表格格式不一致 | 不適用 | 重疊（第 13 項） |
| 14. 〈7〉套件元件豁免類別 | 不適用（doc-2） | 重疊（第 14 項） |
| 15. usecase branch_from | 不適用（doc-2） | 重疊（第 15 項） |
| W3-469 其餘 3 項：DOC-14b 依據部分只在範例 | 未觸發 | W3-469 本身項目 |
| W3-469 其餘 3 項：DOC-22b 框架容器括號窮舉 | 不適用（doc-2） | W3-469 本身項目 |
| W3-469 其餘 3 項：E-04 範例後附註（CLI 預填值為合法示範答案） | 未觸發（DOC-04 三份都沒有涉及 CLI 欄預填值） | W3-469 本身項目 |

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引號內刪節 | r1 DOC-01b；r3 DOC-08b |
| 引號內改字 | r2 DOC-04b（游→遊） |
| 出處標錯 | r2 DOC-14a 依據句 2 |
| 依據與判定錯位 | r2 DOC-11a (b) |
| 自相矛盾判定句 | r1 DOC-15b (b)（已同句更正）；r3 DOC-12b；r3 DOC-04a (b) 與 (c) |
| (3) 列出條文已排除的歧義 | r1 DOC-15b（「+」＝組合已明寫） |

**新發現（供 PM 依規則 5 處置，本判定不建票）**：
- DOC-02b：多形態段三規則以分號串成一句；「其餘子節」的指涉不明；容器「每形態各一份」與子節「表內加欄」並列，沒有對照說明。
- DOC-03a：〈尺寸契約〉列以視窗尺寸為鍵，與「換版型」只能由使用者設定觸發衝突。
- DOC-04b：「具體到形式」缺判準（r1）；條文風險 10 觸發（r3）。
- DOC-07b：合併句與逐部分歸類的先後未寫明；標籤「目前狀態」與數值的邊界未界定。
- DOC-12b：「逐條列出」與「不另增列」同用「列」字。
- DOC-13a：「已送出」與「無法送達」之間，客戶端發出前失敗的歸屬未界定。
- DOC-15a（r2）：列名「等待與結果」跨第 2、3 層，與「承載哪一層」單數要求衝突；建議併入 W3-469 第 1 項。
- 題目設計：DOC-15a／15b 相鄰同構且餵入相同，r1 答案帶入，〈b3y 判定後的處置〉方法規則在本批沒有落實；DOC-09b、DOC-04a 子問之間疑似帶入（證據弱）。
### 第 5 批判定、tdd 用戶裁示與多票完成後的處置（PM，2026-09-16）

**第 5 批**：doc-1 21/30、doc-2 21/22，合計 42/52，越界 0。
- 條文讀歪（開 W3-472，blockedBy W3-469）：DOC-02b、03a、04b、07b、12b、13a、15a（r2）、19a（判準只在範例，答案鍵漏標）。
- 題目設計：DOC-04a、09b、15a（r1）。15a r1 的失敗是相鄰同構題答案帶入；「相鄰同構題不放同一檔」這條規則在 b5 出題之後才定，所以 b5 題組沒有套用，改題時拆檔。
- 計分：r1 DOC-15b 在同一句內自行更正，依既有口徑記通過。

**W3-465.1 四題的用戶裁示**
- 20b：場景欄只放編號，作為列索引。
- 36a：範圍斷言與固定長度都算值驗證。
- 37b：組 2 未觸發時，照樣產出對照表並標「不適用」。
- 39a：「存在」與「可操作」拆成兩條前置條件；bdd-testing-methodology 的對應修改由 W3-470 處理。
- 以上交 W3-465 落檔。

**W3-471 已落檔**（ux 1.11.8）：第 2 項改為 2.1～2.11 編號結構。以 25 組輸入逐詞推導（含 D3、D3'、D4），結果全部一致。

**W3-466**（tdd 2.10.5）：落檔 5 題（01b、05a/05b、07a、11a、14b）；另 4 則草稿（04a、06a、08b、11b）交 W3-466.1 WRAP。

**W3-469**（doc 1.22.1）：落檔 4 句（風險 1、7、9、15）。
- 風險 2、E-04 附註：查到既有依據。
- 風險 4、DOC-22b：由既有票 W3-428 承接。
- 風險 12：屬觀察，非缺口。
- 其餘 8 則草稿交 W3-469.1 WRAP。風險 5、6 為直接衝突。

**b3z**：6 份探針已交卷，待判定。

**b2w**：依 1.11.8 重出 UX-09a／09b／09c，並加入 D3、D3'、D4 三題，這三題能區分各選項。
### b3z 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3y 判定〉，計分口徑另加〈UX-09a 第五輪用戶裁示與 b3z／b5 派發〉的 FD-17a 條款。
- 每題先只讀該題餵入節錄檔（`probes/excerpt/`，檔頭 HEAD 98a1bac7）自行推出判定，再比對答案鍵 `key/W3-417-b3z-{1,2}.md`，最後逐題判 3 份答案 `a/W3-417-b3z-{1,2}-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過；答案寫在依據句欄內記已答；以「或」並列且另一項正確，比照 FD-18b／TDD-07b 前例記通過。例外是 FD-17a：`web/` 格以「或」並列「照預設」與「改寫產物」記不通過。
- 核對腳本：`probes/tools/b3z_check.py`（由 `b3y_check.py` 改為接收題組名參數，兩個題目檔分別執行）。

**獨立推導與答案鍵比對**：4 題推導都與答案鍵預期判定一致，答案鍵無誤。

**答案鍵引文核對**：b3z-1 21 段、b3z-2 6 段 ⟪⟫ 引文，去掉空白與標點後比對該題餵入節錄檔，27 段都逐字存在。

**探針引文核對**（b3z-1：r1 20、r2 16、r3 21 段；b3z-2：r1 10、r2 3、r3 8 段「」引文）：不在該題餵入中的片段如下，都不計分。
- 引號內改寫：b3z-1 r2 FD-17a 把〈產物欄的五種合法答案〉表「已存在於 X」「照預設」兩列攤平成「【已存在於 X】 答案形態……何時使用：……必須附帶：……」，儲存格內容逐字。
- 自造詞加引號：b3z-1 r3 FD-17a (3) 的「改寫為補建」「尚未定義」是探針自己的用語，不是引文。
- 行號標錯：b3z-1 r3 FD-05b 標「第29行」，實為第 30 行；標「第44-45行」，實為第 44 行。

**越界檢查**
- (1) 答案鍵範例污染檢查字串（b3z-1 3 條、b3z-2 1 條）：6 份答案都沒有命中。
- (2) `tools/removed/` 中本題組餵入檔的被刪文字，以 22 字元視窗比對：只有 b3z-1 r2 命中 3 個重疊視窗，內容是答卷格式標籤「依據句」緊接「規劃波進行中」列條文「該版提案的 spec_refs 或 usecase_refs……」（節錄檔內有）。被刪範例 examples-process 第 20 行恰好也是「依據句「該版提案的……」」的寫法，和 b3y r3 一樣屬字面巧合，不計。其餘 5 份命中 0。
- (3) 讀了非本題餵入的節錄檔：0。
- (4) 讀了餵入檔「只讀」範圍以外的段落：1 處。b3z-1 r1 FD-05b 引「不可遺失的正式狀態放哪裡、怎麼保護」，這句只出現在 `dimension-product-notes--改寫產物是主路徑` 的〈各維度要防的失效（權威出處）〉表資料庫列，本題只讀〈判定「失效不存在」的理由〉。這句用於推導產物層，不影響本題判定，因此不改計分。
- (5) 跨題題幹挪用：0。
- 越界合計：1（上列第 (4) 項）。

**結果**：2／4。

#### 逐題表

| 題號 | 題目檔 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|---|
| FD-05b | b3z-1 | 不能；產物層依四種答案另判（不含照預設），選「無」須證明對象不存在 | 是 | 是 | 是 | 是 | — |
| FD-10c | b3z-1 | (1) 規劃波進行中；(2) 為 DevOps 與可觀測性各建盤點票後交回 `version-bootstrap`，本流程結束 | 是 | 是 | 是 | 是 | — |
| FD-17a | b3z-1 | (1) 分列；(2) `server/` 已存在於 `deploy/server.sh`，`web/` 照預設（待建） | 是 | 是 | 否（`web/` 填「改寫產物（需補建部署配方）」） | 否 | 條文讀歪點 |
| FD-10b | b3z-2 | (1) 規劃波之後；(2) 做：逐維度補判未被既有票涵蓋的產物，不做：不重排既有票 | 否（(2) 另附「並須……交回前先為這兩個維度各建一張盤點票」） | 是 | 是 | 否 | 條文讀歪點 |

判定者註：
- FD-05b r1：判定句寫「應按〈產物欄的五種合法答案〉四種作答」，已回答產物層依什麼判定。依據句欄另寫「若成立則產物層應填『改寫產物』、『已存在於 X』或『待定』」。這是「對象存在時」的可選範圍（排除「無」），不是答案鍵所列「答得出時填改寫產物或待定」的形態，也沒有給出單一答案，記通過。
- FD-05b r2：判定句寫「產物欄的填法應根據『這個維度要防的失效在本專案會長成什麼樣子』來判定」。這個推導問句也出現在〈權威缺席時〉「權威存在但形態不符」列，屬只讀範圍內。依據句另引「產物層：依〈產物欄的五種合法答案〉四種作答」。沒有寫到「選無須證明對象不存在」，但答案鍵沒有把這點列為不通過，記通過。
- FD-05b r3：判定句只寫「拆兩問分開填」，產物層依四種作答寫在依據句欄的引文中，記已答。
- 三份 FD-05b 都沒有出現 b3 形態（沒有腳本就判「無」），也沒有出現 b3y r3 形態（判不出填「無」、答得出填「改寫產物或待定」）。
- FD-10c：三份都沒有明寫首列不命中，答案鍵沒有要求。三份都引了排除地基票的定義句，並把題幹「前一次執行 foundation-design 流程時建出的票」對到地基票，(4) 都答「否」。
- FD-17a r3：依據句引了「改寫產物」列「維度成立但預設產物對本形態不適用或有害」，沒有引「照預設」列；(4) 的假設卻寫「預設產物適用但尚未實現，故填『改寫產物』」，假設內容本身符合「照預設」的使用條件，推導自相矛盾。(3) 列出的兩種可能是「改寫產物」與「待定」，不含「照預設」。
- FD-10b r1：(1) 判對，做／不做兩項也逐字引出，但接著用「並須」加上表後段落「交回下游不等於下游覆蓋全部維度」的建盤點票動作。「規劃波之後」列沒有交回動作，該段對本列不成立。這對應答案鍵不通過形態「(2) 做什麼寫成規劃波進行中列的『為 DevOps 與可觀測性各建盤點票』」。若 PM 認為引文內的「交回前」已自帶適用條件，可改記通過，通過數變為 3/4。

#### 失敗歸因

| 題號 | 歸類 | 具體結構 |
|---|---|---|
| FD-17a | 條文讀歪點 | 餵入的〈複合產物與多子樹分列〉首段寫「逼自己擇一時，選「已存在」會漏掉待建的部分，選「改寫產物」會重造已有的部分」。這句把「已存在」與「改寫產物」當成已有／待建的一組對照，讀起來就是「待建的部分填改寫產物」。同段第 2 句「「待建」的部分依〈產物欄的五種合法答案〉建補票」的「待建」不是表中任何一種答案形態的名稱。表中「照預設」列的使用條件只寫「預設產物對本專案形態成立」，必須附帶欄是「不需附帶」，看不出它涵蓋「尚未存在、要補建」的產物。「照預設要落為地基票」寫在〈與 orchestration 的協作〉盤點票段落，不在本題餵入中。r3 因此把題幹的「補建」對到「改寫產物」。本輪餵入已補上答案表，排除了 b3y 歸因的題目設計因素，失敗仍出現，歸條文。被刪範例的正確答案也是「已存在於色碼常數表＋改寫產物：寬度截斷規則待落為文件」，同樣是已存在／改寫產物的配對，但 r3 的被刪文字視窗命中是 0，不構成抄答證據 |
| FD-10b | 條文讀歪點 | 〈與 orchestration 的協作〉表後段落「交回下游不等於下游覆蓋全部維度……交回前先為這兩個維度各建一張盤點票」沒有標明只適用於「規劃波進行中」列。四列中只有該列的角色欄有「交回」動作，但段落以通則形式放在表後。r1 在只有 FD-10b 一題的題目檔中逐字引了這段，並把它加進「規劃波之後」列的做什麼。b3y 判定曾把 r1 的同型失敗歸為相鄰題帶入，並記「表後兩段……也可能助長這個帶入，但 r1 沒有引這兩段，無法確認」。本輪已分檔，不存在相鄰題，r1 又直接引了這段，因此改歸條文讀歪點 |

#### 與前一輪（b3y）的失敗形態對照

| 題號 | b3y 失敗形態 | 修補 | 本輪 | 生效情形 |
|---|---|---|---|---|
| FD-05b | r3 判不出填「無」、答得出填「改寫產物或待定」 | W3-468（6.6.21）「待定」列補「判不出來一律填待定」與「那是判準層的結論，不是產物欄的答案，兩層分開填」 | 3/3 | 生效：b3y r3 兩種形態都沒有出現，3 份都答「不能」；r3 逐字引了新增句。答案鍵條文風險 2（對象存否說不出時的承接）未觸發 |
| FD-10c | r2 刪去括號內排除句，判規劃波之後；r3 以兩列共用的「尚未建票」判其餘 | W3-468「規劃波之後」列粗體主詞改為「該版票已建，不含本 skill 自身產出的地基票」 | 3/3 | 生效：3 份都判進行中，排除句沒有再被刪節。答案鍵條文風險 1（進行中與其餘兩列的區分條件未獨立成欄）未觸發 |
| FD-17a | r1 `web/` 填「改寫產物」；r2「照預設（或改寫產物）」 | 餵入補〈產物欄的五種合法答案〉表；計分口徑改為並列記不通過 | 2/3 | 部分生效：r1、r2 都只填「照預設」，沒有並列。r3 重現 b3y r1 形態，見失敗歸因：補表之後仍出錯，說明原因不只餵入缺表 |
| FD-10b | r1 (2) 做什麼寫成進行中列的建盤點票動作（歸相鄰題帶入） | 與 FD-10c 分檔，由不同探針作答；W3-468 粗體主詞 | 2/3 | 部分生效：(1) 3/3 判對，「不做什麼」3/3 答出。分檔之後 r1 仍附加建盤點票動作，而且逐字引了表後段落，說明 b3y 的相鄰題帶入歸因不完整，真正的來源是表後段落沒有標明適用列 |

#### 同構抄答跡象

- 範例污染字串命中 0；被刪文字視窗只有 b3z-1 r2 命中，屬格式標籤巧合（見越界檢查）。
- FD-17a r3 的「已存在＋改寫產物」配對和被刪範例同形，但沒有字面命中，而且餵入中的〈複合產物與多子樹分列〉首段已經提供這組配對，不判為照抄。
- 沒有發現照搬例示結論的情形。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引號內改寫 | b3z-1 r2 FD-17a（表格列攤平） |
| 自造詞加引號 | b3z-1 r3 FD-17a (3) |
| 行號標錯 | b3z-1 r3 FD-05b |
| 只讀範圍外引用 | b3z-1 r1 FD-05b |
| 自相矛盾推導 | b3z-1 r3 FD-17a（假設符合照預設，判定卻選改寫產物） |
| 依據句混入己見 | b3z-1 r1 FD-05b、FD-10c |

**新發現（供 PM 依規則 5 處置，本判定不建票）**
- FD-17a：〈複合產物與多子樹分列〉首段把「已存在」與「改寫產物」當成已有／待建的對照；「待建」不是答案形態名稱；「照預設」列看不出涵蓋待補建的產物。
- FD-10b：〈與 orchestration 的協作〉表後段落「交回下游不等於下游覆蓋全部維度」與「盤點票的驗收條件……」沒有標明適用列。b3y 歸為題目設計（相鄰題帶入），依本輪結果改為條文讀歪點。
- 方法：只讀範圍靠探針自律，核對腳本只檢查檔案層。本輪出現 1 次同一檔案內讀了只讀範圍外的段落（b3z-1 r1 FD-05b）。
b3z 判定後處置（PM，2026-09-16）：2/4。FD-05b、FD-10c 修補生效，3/3。FD-17a（r3）、FD-10b（r1）分檔後仍失敗，判定者改歸條文讀歪點。FD-10b 判定者提出可改判，PM 維持嚴格口徑記不通過，兩題轉 W3-473。越界 1 處（b3z-1 r1 FD-05b 引了非本題節錄段），不影響判定，不計分。
### b2w 出題後的處置（PM，2026-09-16，1.11.8 版）

**題組**：6 題分兩檔。09d／09e 分檔，09a／09f 分檔。自檢 32 句引文，0 失敗。

**出題者標出的讀歪風險**
1. 2.8 (b) 與 2.11「同一圖形只承載一個語意」誰優先，條文沒寫。09f 可能因此失敗。
2. 2.3(2)「不比較」「不成立」記為相同，讀者可能在該軸寫「不適用」。
3. 2.4 需要同時追三個元素；「一併檢查」可能被讀成不必另外核對。
4. 「列」「動作列」只有清單式版面才有定義；整列可點時，指示算不算在可點元素內部，也沒有寫。
5. 2.9(1) 仍以「純文字」開頭，讀者可能記成「純文字＝合格」。
6. 2.1～2.11 用 4 格縮排，Markdown 渲染後會變成程式碼區塊。

**處置**
- 第 1～5 項先讓探針驗證，失敗的題再依讀歪點修條文。
- 第 6 項確定是渲染缺陷，併入探針後的 ux 修補一次處理，避免 ux 連續兩次升版。
- 探針讀的是節錄檔快照，不受渲染影響。

**b3z 後續**：FD-17a、FD-10b 轉 W3-473，已派撰寫者處理。
### b2w-1 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3z 判定〉。
- 每題先只讀餵入節錄檔 `probes/excerpt/ux-design-evaluation--interaction-feedback-component-semantics--六項判準.md`（檔頭 HEAD 047bb242，只讀第 2 項）自行推出判定，再比對答案鍵 `key/W3-417-b2w-1.md`，最後逐題判 3 份答案 `a/W3-417-b2w-1-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過；答案鍵計分點以外的省略不扣分。
- 核對腳本：`probes/tools/b2w_check.py`（由 `b3z_check.py` 改為 UX 題號；本答案鍵無〈範例污染檢查字串〉段，該項略過）。

**獨立推導與答案鍵比對**：3 題推導都與答案鍵預期判定一致，答案鍵無誤。
- UX-09a：直條屬 2.3(3) 純幾何形狀，對帶文字的「分享」按鈕（及可選取的列，若視為可點元素）缺少文字，構成要素軸不同，屬 (a)，合格。
- UX-09b：兩者都有文字與區塊、都是 md 階，兩軸相同，不屬 (a)；同列有按鈕，不屬 (b)，不合格。底色深淺依 2.7 不作為依據。
- UX-09e：指示多出文字，但畫面上沒有其他可點元素帶文字，2.4(1) 第一點不滿足，構成要素軸相同；雙方都沒有區塊，高度軸相同；不屬 (a)、不屬 (b)，不合格。

**答案鍵引文核對**：16 段 ⟪⟫ 引文，去掉空白與標點後比對餵入節錄檔，16 段都逐字存在。

**探針引文核對**（腳本以「」外層計數，含題幹轉述：r1 25、r2 12、r3 22 段）：不在餵入中的片段如下，都不計分。
- 引號內未標刪節：r3 UX-09a 引 2.3(1)「文字或圖示（文字與圖示算同一項；純幾何形狀不算圖示）、區塊」，括號內刪去「，界線見 (3)」沒有標刪節號。其餘片段逐字存在。
- 行號標錯：r1 UX-09a 標 2.1(2) 為「第 10-11 行」，實為第 10～12 行；r1 UX-09e 標 2.8(1) 為「第 43-44 行」，實為第 43 行。

**越界檢查**
- (1) 答案鍵範例污染檢查字串：本答案鍵沒有此段；另查計分點提到的「文字 chip」，3 份都沒有出現。
- (2) `tools/removed/` 中本題餵入檔的被刪文字，以 22 字元視窗比對：3 份命中都是 0。
- (3) 讀了非本題餵入的節錄檔：0。
- (4) 讀了只讀範圍（第 2 項）以外的段落：0。3 份引文都出自 2.1～2.8，沒有引第 1、3～6 項，也沒有引 2.9 例示。
- (5) 跨題題幹挪用：0。
- 越界合計：0。

**結果**：3／3。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-09a | 合格，屬 (a)：直條為純幾何形狀，對帶文字的可點元素缺少文字，構成要素軸不同 | 是 | 是 | 是 | 是 | — |
| UX-09b | (1) 不合格：兩軸都相同，不屬 (a)，同列有按鈕，不屬 (b)；(2) 不能，底色深淺不作為比較依據 | 是 | 是 | 是 | 是 | — |
| UX-09e | (1) 相同（2.4(1) 第一點不滿足）；(2) 相同（任一方沒有區塊）；(3) 不合格 | 是 | 是 | 是 | 是 | — |

判定者註：
- UX-09a：3 份都以構成要素軸不同判合格，沒有人以區塊高度軸或 (b) 作為依據。r1、r3 另寫了假設：r1 假設「該列沒有其他可點元素」，r3 假設「列不是比較對象」。題幹「使用者選取某一列」隱含列可能可點，兩份都迴避了這一點；因為直條對任何帶文字的元素都缺少文字，這個假設不影響判定，記通過。r2 沒有引 2.3(3) 純幾何形狀界線，只寫「藍色直條無文字或圖示」，結論正確，記通過。
- UX-09b：r2 沒有寫「不屬 (b)」，只以兩軸相同判不合格。答案鍵計分點沒有要求寫出 (b)，比照 b3z FD-05b r2 前例記通過。r1、r3 都寫出「同列有可點元素，不屬 (b)」。
- UX-09e：r2 同樣沒有寫「不屬 (b)」，記通過，理由同上。3 份 (2) 都寫「相同」，沒有人寫「不適用」或「不比較」作為軸結果。3 份 (1) 都以 2.4(1) 第一點不滿足判相同，沒有人以「多出文字即不同」判不同。

#### 失敗歸因

本題組 3 題都通過，沒有失敗歸因。

#### 〈b2w 出題後的處置（1.11.8 版）〉讀歪風險 1～5 的觸發情形

| 風險 | 內容 | 本輪 | 說明 |
|---|---|---|---|
| 1 | 2.8 (b) 與 2.11 誰優先 | 未觸發（本題組未測） | 題目設計預期由 09f 驗證，09f 在 b2w-2；本題組 3 份答案都沒有引 2.11 |
| 2 | 「不比較」「不成立」記為相同，讀者在該軸寫「不適用」 | 未觸發 | 09e (2) 3 份都答「相同」；09a r3 也寫「區塊高度軸因直條無區塊記為相同」 |
| 3 | 2.4 追三個元素，「一併檢查」被讀成不必核對 | 未觸發，但本題組無鑑別力 | 09e 在 2.4(1) 第一點就不滿足，第二點不會被用到；3 份都只引第一點。「一併檢查」的讀法須由第一點成立、第二點不成立的題目驗證 |
| 4 | 「列」「動作列」只在清單式版面有定義；整列可點時指示算不算在可點元素內部 | 出現讀者端訊號，未造成失敗 | 09a 題幹是可選取的列。r1、r3 都在 (4) 寫了關於列的假設（見判定者註），表示讀者注意到「列是否算可點元素」條文沒有交代；r2 沒有提。3 份都沒有引 2.5 徽章。本題直條沒有文字，列是否可點不改變結論，所以本題無法區分這個風險會不會造成誤判 |
| 5 | 2.9(1)「純文字」開頭被記成「純文字＝合格」 | 未觸發 | 09e 指示是純文字，3 份都判不合格，沒有人引 2.9 |

#### 同構抄答跡象

- 被刪文字 22 字元視窗命中：3 份都是 0。
- UX-09b 題幹與被刪〈三段鏈式範例：第 2 項〉的第二個檢驗問句同構：都是指示與按鈕同為文字＋區塊、高度相同、只差外觀。被刪預期寫的是「須移出動作列（2.8）或改為其他形態」，3 份都沒有出現這個修正建議，也沒有出現「chip」。不判為照抄。
- 沒有發現照搬例示結論的情形。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引號內未標刪節 | r3 UX-09a（2.3(1) 括號內） |
| 行號標錯 | r1 UX-09a、UX-09e |
| 依據句混入己見 | r2 UX-09a、UX-09b、UX-09e（推導句列在依據句清單內） |
| 未寫 (b) 判斷 | r2 UX-09b、UX-09e |

**新發現（供 PM 依規則 5 處置，本判定不建票）**
- 風險 4 在 09a 出現讀者端訊號：2/3 探針自行假設列是否算比較對象。本題無法區分風險 4 會不會導致誤判，需要一題「列可點且指示本身帶文字或與列同形」的題目才能驗證。
- 風險 3 在本題組沒有鑑別力，需要一題 2.4(1) 第一點成立、第二點不成立的題目。
### b2w-2 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3z 判定〉。
- 每題先只讀餵入節錄檔 `ux-design-evaluation--interaction-feedback-component-semantics--六項判準`（檔頭 HEAD 047bb242，第 2 項 2.1～2.11）自行推出判定，再比對答案鍵 `key/W3-417-b2w-2.md`，最後逐題判 3 份答案 `a/W3-417-b2w-2-r{1,2,3}.md`，3/3 才通過。
- 計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過；答案寫在依據句欄內記已答；答案鍵沒列為不通過的依據缺漏，比照〈b3z 判定〉FD-05b r2 前例記通過。
- 核對以臨時腳本進行（去空白與反引號後比對節錄檔；被刪文字以 22 字元視窗比對），未改動 `probes/tools/`。

**獨立推導與答案鍵比對**：3 題推導與答案鍵預期判定一致，答案鍵無誤。
- UX-09c：對兩顆按鈕，構成要素軸相同（皆文字＋區塊），區塊高度軸 sm 比 md 低一階、全條件成立、皆在階梯上，記為不同 → 屬 (a)，合格；同列不影響（2.1(3)）。
- UX-09d：直條屬純幾何形狀（2.3(3)），無文字、圖示、區塊。「已記住」對直條多出文字 → 依 2.4：「套用」具備文字，且「已記住」對「套用」缺區塊、構成要素軸不同 → 多出算不同。對「套用」缺區塊 → 不同。對全部可點元素都至少一軸不同 → 屬 (a)，合格；所在列有直條，不屬 (b)。
- UX-09f：對「湯品」「甜點」兩軸都相同 → 不屬 (a)（2.2(1) 全畫面、2.2(3)）；標題列無可點元素 → 屬 (b)，合格（2.8(1)(2)）。2.11 不改變判定。

**答案鍵引文核對**：17 段 ⟪⟫ 引文，去掉空白與反引號後比對節錄檔，17 段都逐字存在。

**探針引文核對**（r1 36、r2 31、r3 22 段「」引文）：未命中片段如下，都不計分。
- 題幹字面：三份的「館藏僅剩 1 本」。
- 巢狀引號改『』：r2、r3 引 2.2(2)「『不同』」、r3 引 2.2(3)「『相同』」，其餘逐字。
- 引號內刪節：r3 UX-09f 引 2.8(2) 刪去後半句「；全畫面比較只屬於 (a)」，改以句號收尾，未加省略號。
- 出處：三份皆標節錄檔名或條號，無標錯。

**越界檢查**
- (1) 答案鍵範例污染檢查字串：本答案鍵未列（出題者附記稱同構比對結果見 PM 回報），不適用。
- (2) `tools/removed/` 中 interaction-feedback 系被刪文字（含本題餵入檔的〈三段鏈式範例：第 2 項〉），先去掉節錄檔與題目中也出現的片段，再以 22 字元視窗比對：3 份命中 0。
- (3) 讀了非本題餵入的節錄檔：0。
- (4) 讀了只讀範圍（第 2 項）以外的段落：0。所有命中引文都落在 2.1～2.11；三份都沒有引第 1、3～6 項。
- (5) 跨題題幹挪用：0。
- 越界合計：0。

**結果**：3／3。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-09c | (1) 合格，屬 (a)；(2) 對兩顆按鈕：構成要素軸相同，區塊高度軸不同 | 是 | 是 | 是 | 是 | — |
| UX-09d | (1) 不同（多出文字，2.4 兩點經「套用」滿足）；(2) 不同（缺區塊）；(3) 合格，屬 (a) | 是 | 是 | 是（(1) 依據不全） | 是 | — |
| UX-09f | (1) 不屬 (a)；(2) 合格，依 (b) | 是 | 是（(1) 推導路徑錯置，見判定者註） | 是 | 是 | — |

判定者註：
- UX-09c：三份都沒有寫出 2.6(3)「所有字級、斷點、密度」與 2.1(3)「同列無關」，答案鍵未列為計分點，不扣分。三份都沒有以「兩者外觀都是填色」判不合格。
- UX-09d r1、r2：(1) 都寫出「套用」按鈕作為 2.4 的第二參照，且指出「已記住」與「套用」缺區塊不同，兩點俱全。
- UX-09d r3：(1) 只寫「「已記住」有文字，直條沒有文字或圖示」，依據句引 2.3(1) 含「依 2.4」一段，但沒有引 2.4、沒有提「套用」按鈕。依答案鍵計分點記「依據不全」，判定計通過。
- UX-09d：三份 (3) 都依 (a) 判合格，沒有人以 (b) 作依據。三份都沒有寫區塊高度軸，本題也沒有問該軸。
- UX-09f r2：(1) 判「不屬於」正確，但判定句的理由是「位於標題列，該列沒有任何可點元素，屬於 (b)」，依據句引 2.8(2)「全畫面比較只屬於 (a)」，沒有做 2.2 的全畫面比較，也沒有提 chip 兩軸相同。這是以「屬 (b)」推出「不屬 (a)」，與 2.1(2)「任一形態即合格」及 2.1(3)「(a) 的判定與……是否排在同一列無關」所示的 (a)(b) 各自獨立不符。判定句內容本身（標題列無可點元素、屬 (b)）與題幹及條文都相符，答案鍵計分點也沒有把這種路徑列為不通過，比照前例記通過。若 PM 改採「子問依據與判定不對應即不通過」的嚴格口徑，UX-09f 記 2/3，本批通過數變為 2/3，歸因見下方「潛在讀歪點」。
- UX-09f：三份都沒有引 2.11，也沒有以「chip 在別列、不在比較範圍」判屬 (a)。r1、r3 (1) 都寫出兩軸相同；r3 另寫出與兩個 chip 的比較。

#### 失敗歸因

本批無不通過題。

潛在讀歪點（不計分，供 PM 判斷是否納入 ux 修補）：
| 題號 | 歸類候選 | 具體結構 |
|---|---|---|
| UX-09f（r2） | 條文讀歪點 | 2.8(2) 後半句「全畫面比較只屬於 (a)」是 (a) 的範圍說明，卻寫在 (b) 條內；r2 以這一句作為 (1)「是否屬 (a)」的依據，把 (a)(b) 讀成二擇一：先判 (b) 成立，再推出不屬 (a)。2.1(3) 只說明 (a) 與同列無關，沒有反向寫明「屬 (b) 不影響 (a) 是否成立，(a) 仍須依 2.2 全畫面比較判定」。本題 (a) 的正確結論恰好也是「不屬」，所以錯誤路徑得出正確判定，本題無法鑑別；要鑑別須用「屬 (b) 且屬 (a)」的輸入 |

#### 讀歪風險逐條（〈b2w 出題後的處置（PM，2026-09-16，1.11.8 版）〉第 1～5 項）

| 風險 | 本輪觸發 | 說明 |
|---|---|---|
| 1. 2.8 (b) 與 2.11 誰優先 | 未觸發 | UX-09f 三份都依 2.8 判合格，0/3 引 2.11，也沒有列出兩種可能判定。限制：探針 (3) 全答「否」，表示沒有人注意到 2.11 與本題相關，不代表讀者理解了優先序；本題只證明 2.11 沒有被自發套用 |
| 2. 「不比較」「不成立」寫成「不適用」 | 未觸發（鑑別力弱） | 三份全文沒有出現「不適用」。但本批唯一會落入「不比較」的 UX-09d 只問構成要素軸，三份都沒有寫區塊高度軸，無答卷需要寫出該軸結果 |
| 3. 2.4 三元素追蹤；「一併檢查」被讀成不必核對 | 部分跡象，未確認 | r1、r2 完整核對 2.4 兩點；r3 判定正確但沒有引 2.4、沒有提「套用」按鈕（依據不全）。r3 未引 2.4(2)「一併檢查」，無法確認是否由該句引起 |
| 4. 「列」「動作列」定義範圍、整列可點 | 未觸發（本批未涵蓋） | 三題的列都是明確並排的單列，無整列可點情境；三份對「標題列」「同一列」都沒有提出定義疑義 |
| 5. 2.9(1) 記成「純文字＝合格」 | 未觸發（本批無鑑別力） | UX-09d 的「已記住」是純文字，三份都依 2.3／2.4 比較判定，0/3 引 2.9。但本題正解本來就是合格，誤讀也會得出相同結論；要鑑別須用「純文字對不帶區塊的可點元素」的輸入（例如 UX-09d 另加一個文字連結） |

#### 同構抄答跡象

- 被刪文字視窗命中 0。
- UX-09c 題幹與被刪〈三段鏈式範例：第 2 項〉同形（同列、指示與按鈕都是文字＋區塊、指示低一個尺寸 token 階）；三份答卷沒有出現範例字面（「已同步」「刪除」「高度 40／28」等），推導所需條文都在節錄檔內，未發現照搬例示結論。同構本身屬題目設計面，記為觀察，不影響計分。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引號內刪節 | r3 UX-09f（2.8(2) 後半句） |
| 依據不全 | r3 UX-09d (1)（未引 2.4、未提「套用」） |
| 推導路徑錯置 | r2 UX-09f (1)（以屬 (b) 推不屬 (a)） |

**新發現（供 PM 依規則 5 處置，本判定不建票）**
- UX-09f r2：2.8(2)「全畫面比較只屬於 (a)」位於 (b) 條內，可被讀成 (a)(b) 二擇一；條文沒有寫明屬 (b) 時 (a) 仍獨立判定。本批以錯誤路徑得出正確判定，需「兩者皆屬」的輸入才能鑑別。
- 題目設計：風險 2、4、5 在本批沒有鑑別力（風險 2 無題問高度軸不比較的結果；風險 4 無整列可點情境；風險 5 的正解與誤讀同為合格）。若要驗證這三項，需補能區分誤讀與正解的輸入。
- 題目設計：UX-09c 題幹與被刪範例第 2 項同構，與〈b2w 出題後的處置〉對 UX-09b 的同構換輸入規則同型。
W3-466.1 草稿評估後的用戶裁示（2026-09-16）：04a 允許用業務語言改寫；06a 三類驗收必須拆成不同案例；08b 事件掛在發出它的公開方法；11b 用戶要求以 WRAP 評估，另開 W3-466.2。04a、06a、08b 交 W3-466 依 W3-466.1 建議改文加上用戶裁示落檔；11b 等 W3-466.2 結果出來再落。W3-465 已結案（tdd 2.10.6）。
### b2w 判定後的處置（PM，2026-09-16）

**結果**：b2w-1 3/3、b2w-2 3/3，越界 0。UX-09 系列 6 題依 1.11.8 全數通過，D3、D3'、D4 三個鑑別輸入也都讀對。

**計分**：09f r2 的判定正確，但推導把 (b) 當成 (a) 的替代。依既有前例「答案鍵未列為不通過者記通過」，本題記通過；推導讀歪的部分另外轉條文修正。

**轉 W3-474（ux 條文）**
- 風險 6：4 格縮排會渲染成程式碼區塊。
- 2.8(2)「全畫面比較只屬於 (a)」這句放在 (b) 條內，讀起來像 (a)、(b) 只能二選一。
- 風險 1：2.11 與 2.8 誰優先沒有寫。

**鑑別題**（W3-474 落檔後出題）：以下風險本輪沒有能分辨讀對與讀歪的輸入，需要另出題驗證。
- 風險 2：高度軸「不比較」卻寫成「不適用」。
- 風險 3：2.4(1) 第一點成立、第二點不成立。
- 風險 4：整列可點，且指示帶文字或與列同形。
- 風險 5：純文字對上不帶區塊的可點元素（應判不合格）。
- (a)、(b) 同時成立。
### W3-469.1 評估後的用戶裁示與 b3w 派發（PM，2026-09-16）

**doc 八則的用戶裁示**
- 風險 5：視覺 token 一律集中管理，「依 domain 數」只適用非視覺常數。
- 風險 11：採 C，整份範本加一條約定：`{…}` 只放待替換內容，規則一律寫在括號外，並逐處檢查。
- 風險 14：不新增豁免類別；封裝失敗時開元件票，E-24 範例同步修改。
- 風險 10：定位提示的「焦點移入＋播報」寫在無障礙子節「焦點順序與操作路徑」列。
- 風險 3、6、8、13：無用戶偏好，依 W3-469.1 建議改文落檔。

**W3-469 範圍擴充**
- 加入方法論〈降級不免除回饋義務〉（風險 8）。
- 加入 `component-library-chained-examples.md`，涵蓋 E-16、E-18、E-24（風險 13、14）。

**b3w**
- 出題者刪掉 W3-473 新句中「範例中的寬度截斷規則填改寫產物…」子句。PM 接受：該子句的主詞是已刪除的範例，它引用的使用條件在五種答案表中逐字存在。
- 另記：W3-473 在條文句裡寫了「範例中…」這種直接指向範例的說法，不作為前例。
- FD-10b 的附記風險「盤點票驗收段未標適用列」：本輪若因此失敗，歸因條文讀歪點。
- 探針 2 檔各 3 份，分由不同探針作答。
### b3w 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3z 判定〉。
- 每題先只讀該題餵入節錄檔（`probes/excerpt/`）自行推出判定，再比對答案鍵 `key/W3-417-b3w-{1,2}.md`，最後逐題判 3 份答案 `a/W3-417-b3w-{1,2}-r{1,2,3}.md`，3/3 才通過。
- 節錄檔基準：〈複合產物與多子樹分列〉、〈與 orchestration 的協作〉檔頭 HEAD ec60e207；〈維度與產物〉、〈規劃波進行中與之後〉檔頭仍記 98a1bac7（答案鍵已說明該段無變更）。
- 計分規則照舊，含 FD-17a `web/` 以「或」並列記不通過、FD-10b 以「並須」附加建盤點票記不通過。
- 核對腳本：`probes/tools/b3z_check.py`，參數改為 `b3w-1`、`b3w-2` 分別執行。

**獨立推導與答案鍵比對**：2 題推導都與答案鍵預期判定一致，答案鍵無誤。
- FD-17a：(1) 分列，部署目標有兩套即成立，測試命令共用不影響；(2) `server/` 填「已存在於 `deploy/server.sh`」，`web/` 題幹已寫明「照權威預設補建」，對應「照預設」列使用條件，不需附帶。
- FD-10b：(1) 規劃波之後，功能票已建，「不論該票是否已開工」；(2) 做：逐維度補判未被既有票涵蓋的產物；不做：不重排既有票。表後段落已明文「僅適用「規劃波進行中」列」，不附建盤點票。

**答案鍵引文核對**：b3w-1 8 段、b3w-2 7 段 ⟪⟫ 引文，去掉空白與標點後比對該題餵入節錄檔，15 段都逐字存在（腳本 KEY MISS 0）。

**探針引文核對**（b3w-1：r1 17、r2 8、r3 8 段；b3w-2：r1 3、r2 9、r3 6 段「」引文）：腳本 NOT-IN-FEED 0、other-excerpt 0。不計分的異常如下。
- 引號內改寫：b3w-1 r2 把表格列攤平為「照預設：權威的預設產物對本專案形態成立」，儲存格內容逐字；b3w-1 r1 把「改寫產物：說不出……」的冒號改為句號。
- 出處標錯：b3w-2 r3 把「不論該票是否已開工」一致的推論標為出自 `examples-process--規劃波進行中與之後.md`，該句實在 `SKILL--與orchestration的協作.md`。
- 簡體字混入：b3w-2 r2「题目说」。

**越界檢查**
- (1) 答案鍵範例污染檢查字串（b3w-1 3 條、b3w-2 1 條）：6 份答案都沒有命中。
- (2) `tools/removed/` 被刪文字 22 字元視窗比對：6 份命中 0。
- (3) 讀了非本題餵入的節錄檔：0。
- (4) 讀了餵入檔「只讀」範圍以外的段落：1 處。b3w-1 r1 (4) 引「CI gate、部署與還原配方」，這句只出現在 `SKILL--維度與產物.md`〈維度與產物〉表 DevOps 列（節錄檔第 12 行），本題只讀〈產物欄的五種合法答案〉表。r1 用它質疑預設產物對 CDN 形態是否成立，與失敗推導相連；同列適用條件欄「SaaS/伺服器端專案照預設；非 SaaS 專案見下方處置」r1 沒有引，無法確認越界是否促成失敗。
- (5) 跨題題幹挪用：0。
- 越界合計：1（上列第 (4) 項）。

**結果**：1／2。

#### 逐題表

| 題號 | 題目檔 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|---|
| FD-17a | b3w-1 | (1) 分列；(2) `server/` 已存在於 `deploy/server.sh`，`web/` 照預設（待建） | 否（`web/` 填「「改寫產物」或「待定」」） | 是 | 是 | 否 | 條文讀歪點 |
| FD-10b | b3w-2 | (1) 規劃波之後；(2) 做：逐維度補判未被既有票涵蓋的產物，不做：不重排既有票 | 是 | 是 | 是 | 是 | — |

判定者註：
- FD-17a r1：(1) 判對，`server/` 判對。`web/` 以「或」並列「改寫產物」與「待定」，理由寫「題目資訊不足」。依據句逐字引了 6.6.22 新句全文（含「不是待建就等於改寫產物」與「說不出……應填「待定」」），(3) 列兩種可能也只有改寫產物與待定。(4) 的假設卻寫出「盤點者認為預設產物……完全適用（→填「照預設」）」，即 r1 看得到照預設這個選項，但沒有列入判定與 (3)，推導與 b3z r3 同樣自相矛盾。對應答案鍵不通過形態「`web/` 寫「待定」「無」或「改寫產物」」與「據此把 `web/` 改填「待定」」。
- FD-17a r2、r3：`web/` 都只填「照預設」，沒有並列；r2 引「照預設」列使用條件與 6.6.22 判準句前半，r3 引「照預設」列使用條件。
- FD-10b：3 份 (1)(2) 都判對，做／不做兩項逐字引出，沒有附加建盤點票、交回、本流程結束。3 份都沒有引表後兩段（含 6.6.22 新增的「本段僅適用「規劃波進行中」列」）。

#### 失敗歸因

| 題號 | 歸類 | 具體結構 |
|---|---|---|
| FD-17a | 條文讀歪點 | 〈複合產物與多子樹分列〉首段 6.6.22 新句替「待建」的部分點名的答案形態只有兩個：「不是待建就等於改寫產物」（排除式提到改寫產物）與「說不出……應填「待定」」（條件式提到待定）。全段沒有任何一句正面提到「照預設」；〈產物欄的五種合法答案〉表「照預設」列也只寫「權威的預設產物對本專案形態成立」，看不出涵蓋尚未存在、要補建的產物。r1 逐字引了新句，把判定限縮在新句點名的兩個形態之間，再用「說不出」分支蓋過題幹已寫明的「盤點者已判定照權威預設補建」。不歸題目設計：題幹前提明確，同題幹在 b3z、b3w 共 6 份中 4 份填照預設。不歸答案鍵誤：答案鍵已預先列出此形態記不通過 |

#### 與前一輪（b3z）的失敗形態對照

| 題號 | b3z 失敗形態 | 修補 | 本輪 | 生效情形 |
|---|---|---|---|---|
| FD-17a | r3 把題幹的「補建」對到「改寫產物」（首段「已存在／改寫產物」對照讀成「待建＝改寫產物」） | W3-473（6.6.22）首段新增「「待建」的部分……不是待建就等於改寫產物」與「說不出……應填「待定」，不得逕自代入改寫產物」 | 2/3 | 部分生效，失敗形態轉移：沒有探針再單獨把待建等同改寫產物，r1 還引了排除句；但新句的「待定」分支開出新形態，r1 改為「改寫產物或待定」並列。修補從反面排除一個錯答，沒有正面指向照預設，讀者仍停在新句列出的兩個形態之間 |
| FD-10b | r1 (2) 以「並須」附加建盤點票動作，並逐字引表後段落 | W3-473 表後段落首尾補適用範圍限定（僅適用規劃波進行中；之後與其餘不適用） | 3/3 | 生效（證據屬缺席型）：b3z r1 形態沒有出現。3 份都沒有引表後段落，無法區分探針是讀到限定句後排除，還是沒有取用該段；結論是修補後未再觸發，不是觀察到限定句被正確套用 |

#### 答案鍵附記條文風險的觸發情形

| 題組 | 風險 | 本輪是否觸發 |
|---|---|---|
| b3w-1 | 1. 「照預設」列沒有寫明涵蓋尚未存在、要補建的產物；若探針以「尚未存在」為由不選照預設，歸條文讀歪點 | 部分觸發：r1 沒有選照預設，但理由不是「尚未存在」，而是「預設產物對 CDN 發布形態是否成立」不明，並援引新句「說不出」分支。結構上仍是同一缺口（照預設沒有被正面指向待建情形），本輪失敗歸因即以此為核心 |
| b3w-2 | 1. 表後「盤點票的驗收條件與完成定義……」段仍沒有標明適用列；若探針引此段把盤點票帶入「規劃波之後」列，歸條文讀歪點 | 未觸發：3 份都沒有引此段，也沒有提盤點票 |

#### 同構抄答跡象

- 範例污染字串命中 0；被刪文字視窗命中 0。
- 沒有發現照搬例示結論的情形。出題者自節錄檔刪除的範例說明子句（「範例中的寬度截斷規則填改寫產物……」）在 6 份答案中都沒有出現。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 只讀範圍外引用 | b3w-1 r1 FD-17a（〈維度與產物〉表 DevOps 列預設產物） |
| 自相矛盾推導 | b3w-1 r1 FD-17a（(4) 寫出照預設的讀法，判定與 (3) 卻不含照預設） |
| 題幹前提被覆寫 | b3w-1 r1 FD-17a（題幹「已判定照權威預設補建」，答「題目資訊不足」） |
| 引號內改寫 | b3w-1 r2 FD-17a（表格列攤平）、b3w-1 r1 FD-17a（標點改動） |
| 出處標錯 | b3w-2 r3 FD-10b |
| 簡體字混入 | b3w-2 r2 FD-10b |

**新發現（供 PM 依規則 5 處置，本判定不建票）**
- FD-17a：6.6.22 新句只以排除與條件兩種方式點名「改寫產物」「待定」，全段與〈產物欄的五種合法答案〉表都沒有正面寫出「待建但預設適用者填照預設」。修補使失敗形態由「改寫產物」轉為「改寫產物或待定」並列。
- FD-10b：修補後 3/3，但通過證據是舊形態缺席，3 份都沒有取用表後段落，限定句本身的效力本輪沒有被直接驗證。
- 方法：只讀範圍靠探針自律，核對腳本只檢查檔案層；本輪再次出現同一檔案內讀了只讀範圍外的段落（b3w-1 r1），且該段與失敗推導相連。
b3w 判定後處置（PM，2026-09-16）：結果 1/2。FD-10b 通過 3/3，但本輪沒有探針引用表後限定句，所以只能證明舊錯法沒再出現，無法證明限定句本身有效；這點併入整體回歸時再驗。FD-17a 第三輪仍失敗（r1），錯誤形態已從「待建＝改寫產物」變成「改寫產物或待定並列」。原因是修補只排除了錯誤答案，沒有正向指出正解「照預設」，轉 W3-475 補正向句並限定「待定」分支的射程。越界 1 處（r1 引了 DevOps 列），不影響計分。
W3-466.2 後用戶裁示（2026-09-16）：TDD-11b 採 A＋B1 為前提（單一訊號直接照訊號表處理，行數只計允許 Mock 的外層依賴）。用戶選擇寫出不拆 Module 的判準，這是新判準，開 W3-466.3 交 WRAP 起草。抽到 Helper／Builder 的替身設置要計入行數，〈常見挑戰速查〉的 Builder 列一併補寫。W3-466 的 11b 部分等 W3-466.3 完成後才落檔。
### b3v 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3w 判定〉。
- 每題先只讀該題餵入節錄檔（`probes/excerpt/`）自行推出判定，再比對答案鍵 `key/W3-417-b3v-{1,2}.md`，最後逐題判 3 份答案 `a/W3-417-b3v-{1,2}-r{1,2,3}.md`，3/3 才通過。
- 節錄檔基準：兩份節錄檔檔頭皆記 HEAD 0e4ab35c（答案鍵說明本輪以 `tools/specs-b3v.json` 重建，前一輪備份於 `excerpt-bak-b3w/`）。
- 計分規則照兩份答案鍵的判定者註，含 FD-17a `web/` 以「或」並列記不通過、FD-17c 以「還沒有／目前沒有配方」為由排除照預設記不通過。
- 核對腳本：`probes/tools/b3z_check.py`，參數 `b3v-1`、`b3v-2`。b3v-2 的 r1、r2 標題未用 `### FD-17c` 形態，腳本切不到答案區塊（blocks 0），這兩份引文另以同一規則手動比對補齊。

**獨立推導與答案鍵比對**：2 題推導都與答案鍵預期判定一致，答案鍵無誤。
- FD-17a：(1) 分列，三項判別條件中「部署目標」有兩套即成立，測試命令共用不影響；(2) `server/` 填「已存在於 `deploy/server.sh`」並附位置，`web/` 填「照預設」，尚未存在不排除照預設，不合併成單一格。
- FD-17c：填「照預設」，不需附帶。權威提供的部署與還原配方對 `console/` 的發布方式成立，目前沒有該配方不排除照預設；排除「待定」（其使用條件是該判而未能判）與「改寫產物」（其使用條件是預設產物對本形態不適用或有害）。

**答案鍵引文核對**：b3v-1 11 段、b3v-2 6 段 ⟪⟫ 引文，去掉空白與標點後比對該題餵入節錄檔，17 段全數逐字存在（腳本 KEY MISS 0）。

**探針引文核對**（b3v-1：r1 8、r2 11、r3 9 段；b3v-2：r1 5、r2 9、r3 9 段「」引文）：檔案層 NOT-IN-FEED 與 other-excerpt 皆 0。不計分的異常如下。
- 引號內改寫：b3v-1 r1 把判別句縮為「有兩套以上的部署目標」（原文為「有沒有兩套以上的測試命令、建置命令或部署目標才重要」）；b3v-1 r3 另有「兩個獨立的部署目標」；b3v-1 r3 把 `dimension-product-notes` 首段句與表格「照預設」列使用條件揉成一句「權威的預設產物對本專案形態成立時填「照預設」，這包含產物尚未存在、需要補建的情形……」；b3v-2 r2 引題幹時加一個「已」字。
- 出處標錯：b3v-1 r3 把上述揉合句整段標為出自 `SKILL--維度與產物.md`，該句可辨識的主幹實在 `dimension-product-notes--複合產物與多子樹分列.md`。

**越界檢查**
- (1) 答案鍵範例污染檢查字串（每份 3 條）：6 份答案都沒有命中。
- (2) `tools/removed/` 被刪文字 22 字元視窗比對：6 份命中 0。
- (3) 讀了非本題餵入的節錄檔：0（腳本 cites non-fed file 無輸出，人工掃檔名引用亦只見兩份餵入檔）。
- (4) 讀了餵入檔「只讀」範圍以外的段落：2 處。b3v-1 r3 引〈維度與產物〉主表 DevOps 列「CI gate、部署與還原配方」；b3v-2 r2 引同一列的預設產物欄與適用條件欄「SaaS/伺服器端專案照預設」。兩處都用於確認「權威的預設產物」指涉何物，與正確判定相連，未改變判定。
- (5) 跨題題幹挪用：0。b3v-1 三份提及 `ingest`／`console`／容器映像／滾動更新 0 次；b3v-2 三份提及 `web/`／`server/`／CDN／雲端主機 0 次。
- 越界合計：2（上列第 (4) 項）。

**結果**：2／2。

#### 逐題表

| 題號 | 題目檔 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|---|
| FD-17a | b3v-1 | (1) 分列；(2) `server/` 已存在於 `deploy/server.sh`，`web/` 照預設 | 是 | 是 | 是 | 是 | — |
| FD-17c | b3v-2 | 照預設；不需附帶 | 是 | 是 | 是 | 是 | — |

判定者註：
- FD-17a：3 份 (1) 都以「兩套以上的部署目標」判分列，沒有一份被「共用同一套測試命令」帶偏。(2) 3 份都逐題幹寫出兩格內容，`server/` 都附了 `deploy/server.sh`，`web/` 都只填「照預設」——沒有「或」並列、沒有合併單一格、沒有寫建票處置（寫反者才記不通過，本輪無人寫）。r1、r2 逐字引了 6.6.23 新句「「尚未存在」不是排除「照預設」的理由」，r2 另引表格「照預設」列的新括注。
- FD-17c：3 份都填「照預設」並明寫「不需附帶」，都逐字引了表格「照預設」列使用條件與 6.6.23 新句；沒有一份以「目前還沒有配方」為由排除照預設，也沒有出現待定／改寫產物／無。答案鍵預留可豁免的 (4) 假設（把題幹「權威為這個維度提供的部署與還原配方」對到條文「權威的預設產物」）3 份都沒有聲明，其中 r2 改以越界讀 DevOps 列直接取得字面對應。

#### W3-475 修補的生效情形

| 題號 | 前一輪（b3w）失敗形態 | 修補 | 本輪 | 生效情形 |
|---|---|---|---|---|
| FD-17a | r1 把 `web/` 填成「「改寫產物」或「待定」」並列；歸因是 6.6.22 只從反面排除，全段沒有正面指向「照預設」 | W3-475（6.6.23）首段補正向句「權威預設產物對本形態成立時填「照預設」，這包含產物尚未存在、需要補建的情形」「「尚未存在」不是排除「照預設」的理由」「已判定為「照預設」或「改寫產物」者，不因它是待建而回頭改填「待定」」；〈產物欄的五種合法答案〉表「照預設」列補「（含尚未存在、需要補建者）」 | 3/3 | 生效，且證據屬在場型而非缺席型：r1、r2 逐字引了新增句，r2 另引表格新括注，b3z 的「待建等同改寫產物」與 b3w 的「改寫產物或待定並列」兩種形態皆未再出現 |

#### 兩題對照（答案鍵 b3v-1〈條文風險〉1 提議的鑑別題）

- FD-17a 題幹寫「盤點者已判定照權威預設補建」，與新判準句在「權威預設」「補建」兩個用語上字面重疊；單看這題 3/3，排除不掉探針靠字面對上就答照預設的可能。
- FD-17c 題幹經出題者 grep 確認不含「照預設」「補建」「尚未存在」「預設產物」「待建」，只給兩項事實（`console/` 目前沒有部署與還原配方；權威提供的配方適用於它的發布方式），作答須自行把「適用」對到表格的「對本專案形態成立」、把「還沒有」對到「含尚未存在、需要補建者」。
- **兩題皆 3/3，且 FD-17c 無一份以「還沒有」排除照預設，因此本輪不是靠字面對應答對**：避開判準用語後讀法仍成立，FD-17a 的 3/3 可採信為條文本身可讀，非題幹提示所致。
- 保留一點：FD-17c 的 r2 為確認「權威的預設產物」指的是什麼，越界去讀〈維度與產物〉主表 DevOps 列。三份中有一份需要只讀範圍外的字面錨點才下判，說明「權威的預設產物」在只讀範圍內指涉何物這一步仍靠讀者自行對應，非條文明示。

**新發現（供 PM 依規則 5 處置，本判定不建票）**
- 〈產物欄的五種合法答案〉表「照預設」列以「權威的預設產物」為條件，但該表不說明任一維度的預設產物是什麼（那在〈維度與產物〉主表的「權威提供的預設產物」欄）。只餵這張表時，讀者要判「照預設」必須自行把題幹的權威描述對到「預設產物」這個詞，b3v-2 r2 因此越界取字面。這是兩題對照結論的唯一保留點。
- 方法：只讀範圍仍靠探針自律，核對腳本只檢查檔案層，本輪 2 處同檔案內讀了只讀範圍外的段落（b3v-1 r3、b3v-2 r2），與 b3w 同型。
- 腳本相容性：`b3z_check.py` 以正規式切答案區塊，要求井字號後緊接題號，b3v-2 r1、r2 的標題為「# 探針作答 FD-17c」「# 答案 FD-17c」，井字號與題號之間夾了文字即切不到，blocks 回 0 且該份引文完全不進入檢查而腳本不報錯。本輪以手動比對補齊，後續輪次若沿用此腳本宜在 blocks 為 0 時出聲。
### 2.11 與顏色語意的用戶裁示（PM，2026-09-16）

- P1：採 E，2.11 拉出成獨立判準。「六項判準」改為七項，所有指向的路由同步。由 W3-474 落檔。
- P2：語意只看圖形代表什麼，不含可點性。
- P3：用戶反問「顏色本來就應該代表語意，是不是哪裡的規則沒寫清楚」。PM 查證：
  - dart-style-guardian `color-system.md` 定義三色語意（primary／positive／negative）。
  - 但「顏色不得為唯一區分依據」這條無障礙要求，在 ux 六項判準、color-system、元件契約無障礙欄三處都查無。
  - 因此 P3 是表面問題，先開 W3-476 盤點三處並決定落點，P3 待該票結論。
- DOC-19a：用戶裁示先交 WRAP，開 W3-472.1。
- b3v 判定的越界訊號（「權威的預設產物」無逐維度說明）：開 W3-477（P2）。
### 兩張 WRAP 的裁示落地（W3-476 / W3-472.1）

**W3-476（顏色不得作為唯一區分依據）**

| 項目 | 結論 |
|---|---|
| 三處盤點 | ux 判準層與 `color-system.md` 確無此條；元件契約無障礙欄的前提不成立——方法論、ccd step-3、ccd 範例、doc 範本四處都有，但形態是「欄位要填的內容」而非「可判不合格的條件」 |
| 真正缺口 | 射程缺口不是條文缺口：逐元件填表覆蓋不到「同一元件兩實例用兩色」「跨元件」「非元件載體（文字色／底色／邊框色）」 |
| 落點 | ux 判準層新增獨立第 8 項（方案 A）；ccd 既有問句保留並補路由句，兩層不重複定義 |
| 判定式 | 灰階測試：轉灰階後無法區分 **且** 旁邊沒有文字標明，兩點同時成立才違反；8.2(3) 列灰階後仍可區分的差異清單；8.4 明寫「改變顏色本身不構成處置」 |
| P3 答案 | 紅圓與藍圓**算同一圖形**，2.3(3) 全句以形狀界定、無一項以顏色界定，逐詞即可推出，不需新增子句 |
| 對 W3-474.1 草稿的更動 | **刪除 2.11(5) 兩句**——前半是複述（2.3(3) 日後增修形狀類別時會漏跟），後半把文字混進圖形身分判定、與 2.3(3) 架構衝突；原意由第 8 項的 8.2(4)、8.3(2) 承接 |
| 承接票 | 0.1.0-W3-478（DOC，P1，blockedBy W3-474） |

**W3-472.1（DOC-19a 內距與鄰件間距的分界）**

| 項目 | 結論 |
|---|---|
| 是否新判準 | 不是。四處非範例出處可逐詞推得，缺的是「沒有一處把前提合寫成一句分界」，唯一合寫載體是範例檔——屬既有風險類別「依據只在範例內」 |
| 事實更正 | W3-472 NeedsContext 記「範例已刪」為誤述，範例檔第 601 行於 HEAD 仍在；DOC-19a 那一題的具體失敗已由 W3-469 消除，剩餘缺口是表格欄以外的情形 |
| 條文形態 | 四列管轄表，判別問句「這段距離的兩端各是什麼」＋兜底句（未落入者對共同上層重問一次） |
| 第四列 | 「本元件內容 ↔ 本元件另一處內容 → design token 依賴表間距列」為本次新增、無直接出處；與用戶既有裁示「視覺 token 一律集中」同向，據此採用，落檔時該列須標「本次新增」供逐列核 |
| 落點 | 主文落 `component-library-bidirectional-constraint-methodology.md`〈容器亦為元件〉；doc 範本引言與 ccd step-3 各補路由句（須寫出判別問句本身，只給檔名視為未達 acceptance）；範例檔【驗證】段改引用條文 |
| 承接票 | 0.1.0-W3-479（DOC，P1） |

**本輪派發**：W3-474（ux 判準檔）、W3-479（doc 方法論／範本／ccd）、W3-466（tdd）三票檔案不重疊，並行；W3-478 與 W3-474 同檔，待其完成後序列派發。
### W3-479 落檔完成（DOC-19a）與對重測的影響

commit `5babbac58`，四檔：方法論〈容器亦為元件〉補四列管轄表 + 兜底句；doc 範本〈組合規則〉引言補路由句（已寫出判別問句本身）；ccd `step-3-fill-contract.md` 組合規則列補同向路由；範例檔【驗證】段改引用表列判定，原自帶判準句「內距是元件邊界內部的空間，兩者不同」已刪——「依據只在範例內」在本題已消除。

**對重測的影響（出題時必須照辦）**：條文主體現在落在**方法論**，doc 範本只有一句路由。DOC-19a 重新出題時，餵給探針的 excerpt **必須同時包含**範本〈組合規則〉引言與方法論〈容器亦為元件〉該段——只餵範本會讓讀者拿到一句指向別處的路由而無條文可讀，該失敗屬餵料缺漏不是內容缺陷，會污染判定。凡條文與消費端分居兩檔者一律適用此規則。

**第四列（元件內部兩處內容 → design token 依賴表間距列）標記為「本次新增、無直接出處」**，依用戶既有裁示「視覺 token 一律集中」採用；出處表第四列的「讀法是否改變」欄記為「是」——填寫者原本無處可填。此為本批次唯一一筆帶「讀法改變」仍落檔者，理由是它補的是空缺而非改寫既有讀法。

判否反例（容器內縮誤判為內距、元件內部兩處內容誤判為內距）留在 W3-472.1 Solution 作佐證，未複製進條文；四列表本身已機械涵蓋兩者的判定路徑。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: PM 派發出題者、探針與判定者
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-410 | chars: 390 -->

### Task Reference
- 0.1.0-W3-410 what: 為 dimension-product-notes 可觀測性維度、UI 維度（失效樣貌依形態改變）、examples 拆兩問步驟 1（無獨立部署時 DevOps 失效是否成立）三處推導補條文依據，或判定該推導不應存在並改寫範例；需新增判準者走兩階段草稿停手（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-410 why: W3-408 兩輪複核後刪除問句，三處標註本則推導無條文依據句（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/foundation-design/references/dimension-product-notes.md  # from 0.1.0-W3-410（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/references/examples.md  # from 0.1.0-W3-410（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/CHANGELOG.md  # from 0.1.0-W3-410（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/SKILL.md  # from 0.1.0-W3-410（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三處逐一判定可引或需新判準並記錄  # from 0.1.0-W3-410（來源已完成，結論見 Solution）
