---
id: 0.1.0-W3-417
title: 實證 範例審查探針閘門第一輪（W2 開工閘門）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-418
- 0.1.0-W3-419
- 0.1.0-W3-420
- 0.1.0-W3-421
- 0.1.0-W3-422
- 0.1.0-W3-423
- 0.1.0-W3-424
- 0.1.0-W3-425
- 0.1.0-W3-426
- 0.1.0-W3-433
- 0.1.0-W3-435
- 0.1.0-W3-436
- 0.1.0-W3-437
- 0.1.0-W3-439
- 0.1.0-W3-446
- 0.1.0-W3-447
- 0.1.0-W3-448
- 0.1.0-W3-449
- 0.1.0-W3-450
- 0.1.0-W3-451
- 0.1.0-W3-452
source_ticket: 0.1.0-W3-410
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 審查方式 WRAP 與用戶裁示
  final_decision: 建探針閘門實證票
  rationale: 取代逐字可溯性閘門
who:
  current: PM 派發出題者、探針與判定者
  history: {}
what: 依用戶裁示以讀者探針為範例審查閘門跑第一輪實證：涵蓋本輪補過範例的 skill（component-contract-design、ux-design-evaluation、foundation-design、test-assertion-design、tdd、doc、version-bootstrap、dart-style-guardian、skill-design-guide），每則含判定的範例出題並含判準相反分支的對照版，題目凍結後各 3 探針作答，判定者先從條文寫出正確答案與依據句再比對；門檻 3/3；判定者同時標出依據只在範例內的判準
when: 立即
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-336.md
why: 用戶裁示：W2 開工條件為探針 3/3 通過且全部範例零無依據句；chained-examples 待本輪實證後再改
how:
  task_type: Analysis
  strategy: 先派出題者產出凍結題組並經 PM 核對覆蓋，再派探針，最後派判定者；未通過與無依據句逐項建修補票
acceptance:
- '[ ] 凍結題組覆蓋每則含判定範例且含相反分支'
- '[ ] 逐題判定表（3/3 生效或未生效）與依據只在範例內清單寫入 Solution'
- '[ ] 未通過與無依據句逐項建修補票或記錄豁免理由'
- '[ ] W2 開工閘門達成與否的結論'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-15T13:09:11'
completed_at: null
created: '2026-09-15'
updated: '2026-09-15'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

依用戶裁示以讀者探針為範例審查閘門跑第一輪實證：涵蓋本輪補過範例的 skill（component-contract-design、ux-design-evaluation、foundation-design、test-assertion-design、tdd、doc、version-bootstrap、dart-style-guardian、skill-design-guide），每則含判定的範例出題並含判準相反分支的對照版，題目凍結後各 3 探針作答，判定者先從條文寫出正確答案與依據句再比對；門檻 3/3；判定者同時標出依據只在範例內的判準

---

## Problem Analysis
### 出題者盤點（2026-09-15，出題前）

**方法**：以 `grep '【輸入】'` 列出九個 skill 的三段鏈式範例位置（排除 CHANGELOG 與格式說明句），分五組並行讀範例全文與所屬章節，寫出每則教的判定，再在範例三段、程式碼圍欄、逐問檢查表以外的條文以 `grep -F` 找依據句（chained-examples〈驗證段機械檢查〉第 2 點）。一則範例教多個判定時拆列（編號加 a/b/c）。出題者只標記，不評判。

**規模**

| skill | 標記格式範例數 | 判定列數 | 條文查無列 | 部分列 |
|---|---|---|---|---|
| component-contract-design | 7（另 5 則非標記格式） | 12 | 1（A-11） | 3 |
| dart-style-guardian | 4 | 4 | 0 | 1 |
| skill-design-guide | 5 | 6 | 1（A-17b） | 1 |
| version-bootstrap | 8 | 8 | 0 | 4 |
| ux-design-evaluation | 29 | 33 | 3 | 11 |
| foundation-design | 19 | 40 | 5 | 13 |
| tdd | 18 | 22 | 6 | 3 |
| test-assertion-design | 6 | 7 | 1 | 0 |
| doc | 24 | 27 | 3 | 7 |
| 合計 | 120（另 5 則非標記） | 159 | 20 | 43 |

依步驟 2「每則含判定範例 1 題 + 相反分支對照 1 題」，標記格式範例即需 240 題，超過步驟 4 門檻 60。**依步驟 4 停在盤點，未出題**，分批建議見文末。

### 盤點表

#### A 組：component-contract-design／dart-style-guardian／skill-design-guide／version-bootstrap

| 編號 | skill | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|---|
| A-01 | ccd | step-1-pick-starting-point.md〈情況二：A 與 C 之間猶豫〉 | C 三訊號都無法核對時判「待決」不判 A；規格提及既有檔只算請求核對理由；走查暫判 A 須加標記 | step-1〈情況二〉：「「無法核對」不等於「全否」，不得判 A」 |
| A-02 | ccd | step-2-path-a-spec-derivation.md〈反向規則〉畫面甲／浮層乙狀態表 | 本列未提控制項不渲染（不以停用渲染）；他列有提的退出動作標待決建追修票；頁面框架不受限 | step-2-path-a〈反向規則〉：「控制項不渲染，不以停用狀態渲染」「不得逕判存在或不存在」 |
| A-03 | ccd | step-3-fill-contract.md〈關卡五〉矩陣格點選顯示詳情卡 | 事件不經服務時失敗／逾時／服務不可用用固定寫法，不寫「同成功」；按需讀取時不適用 | 部分：skill 內只有固定寫法；禁止句與不適用句在方法論 component-library-bidirectional-constraint-methodology〈失敗路徑覆蓋〉 |
| A-04 | ccd | step-5-write-tickets.md〈格位起算〉 | 已具名 slot 寫 slot 名；未具名依書寫方向起算；容器無 slot 契約不得在畫面票自訂編號 | step-5〈格位起算〉：「寫 slot 名，不另行編號」「先補該容器的 slot 契約」 |
| A-05 | ccd | step-5〈覆蓋層〉篩選下拉 | 元件自帶覆蓋層不列；呼叫端觸發者寫狀態列末子句不佔格位；定位寫在元件契約 | step-5〈覆蓋層〉：「不寫進格位句」「畫面票不述」 |
| A-06 | ccd | step-5〈疊加態〉 | 疊加態獨立一列並標疊加底層，列內只寫增量；底層列不動 | step-5〈疊加態〉：「寫成獨立一列，列首標明」「列內只寫增量」 |
| A-07 | ccd | step-5〈畫面票〉非狀態進入情境 | 不在狀態矩陣的進入情境不新增 how 列，拆到既有列、「被程式定位」格與覆蓋層子句 | 部分：缺「非狀態情境不新增 how 列／拆解方式」 |
| A-08 | ccd | step-2-path-a 元件候選判定（非標記格式） | 列集合相同合為一元件；真子集為可選 slot；互有差集各為候選；單畫面共現為容器子件 | 依據句位於程式碼圍欄內（第 2 點判否，待 PM 裁定） |
| A-09 | ccd | step-2-path-a 容器盤點（非標記格式） | 同一佈局結構下全部子件算一個容器候選 | 依據句位於程式碼圍欄內（同上） |
| A-10 | ccd | step-3〈關卡二〉載入指示消失（非標記格式） | 疊加指示消失、內容未變時第 2 問答否；內容改變時答是 | step-3〈關卡二〉：「第 2 問的「可見的狀態轉換」不含進行中指示本身的出現與消失」 |
| A-11 | ccd | step-5〈畫面票〉巢狀容器（非標記格式） | how 逐層展開到葉元件；資料驅動格數標「數量依資料筆數」 | **條文查無** |
| A-12 | ccd | step-5〈遷移票〉巢狀 feature 目錄（非標記格式） | 以葉目錄為拆票單位 | 部分：缺「葉目錄」定義 |
| A-13 | dsg | color-system.md〈顏色歸類判準〉刪除按鈕／已刪除提示 | 依元件本身功能類型分類，不因前置操作破壞性改判 | color-system〈顏色歸類判準〉：「分類依據是元件本身的功能類型」 |
| A-14 | dsg | color-system.md〈Theme 與 Token 判準〉 | 內建主題插槽已覆蓋語意用插槽，否則用 token | color-system〈Theme 與 Token 判準〉：「對應的內建語意插槽存在時用主題插槽，不存在時用 token」 |
| A-15 | dsg | typography-system.md〈字級判準：headline 與 title 同值時如何選〉 | 頁面子節標題用 headline4，卡片／對話框標題用 titleLarge；同值不可互換 | 部分：缺「容器內標題 → titleLarge」對映句（定義註解在程式碼圍欄內） |
| A-16 | dsg | i18n-guidelines.md〈判準：標籤＋識別符組合時如何分割〉 | 只有數值豁免，標籤走參數化翻譯 | i18n-guidelines〈Exceptions〉：「僅數值本身列入本豁免；標籤文字仍是使用者可見文字」 |
| A-17a | sdg | chained-examples.md〈【驗證】段〉內嵌範例 | 候選判定表屬靜態產出物，驗證段用檢驗問句 | chained-examples〈【驗證】段〉判定規則表設計產出物列 |
| A-17b | sdg | 同上 | 退出路徑層級不同不合併；層級同目的地不同為 slot | **條文查無**（skill 內；跨 skill 見 ccd step-2-extract-components〈共用規則一〉） |
| A-18 | sdg | chained-examples〈驗證段機械檢查〉正例 | 省略輸入段判違規 | chained-examples〈三段鏈式格式〉：「三段皆不可省略」 |
| A-19 | sdg | chained-examples〈驗證段機械檢查〉反例 | 慣例描述引文、出處在範例自身屬橋接判否，缺引導 | chained-examples〈驗證段機械檢查〉第 2、3a 點與處置表 |
| A-20 | sdg | chained-examples〈完整示範〉 | 執行期行為產出用 Given/When/Then（以格式示範為主） | chained-examples〈【驗證】段〉判定規則表執行期行為列 |
| A-21 | sdg | splitting-an-existing-skill.md〈補範例後超標〉 | 超標成因為新補範例時不刪範例不壓規則；互斥路徑各自外移，入口留判斷條件與路由 | 部分：缺「互斥路徑各自成檔」 |
| A-22 | vb | version-shift-sop.md〈判斷準則〉範例一 | 三問任一為是即契約檔案，與檔案類型無關 | version-shift-sop〈契約檔案的界定〉：「三問任一為「是」即屬契約檔案」「檔案類型不決定歸屬」 |
| A-23 | vb | version-shift-sop.md 範例二 | 同儲存庫各自部署時判準 2 命中；同次提交同次部署時不成立 | version-shift-sop〈契約檔案的界定〉判準 2：「判定單位是發布或部署單位，不是儲存庫」 |
| A-24 | vb | v02-walkthrough.md〈範例一：UI 類提案判別〉 | 依呈現通道判定：彈窗 UI 類、終端文字輸出非 UI 類 | SKILL.md Step 2〈UI 類判別〉呈現通道表 |
| A-25 | vb | v02-walkthrough〈範例二：三項前置檢查的「存在」判準〉 | 檔案存在不等於齊備，以對應載體內容判定 | 部分：缺「檔案存在不等於內容齊備」 |
| A-26 | vb | v02-walkthrough〈範例三：地基波依賴編排〉 | i18n／design-system 並行；UX 審查依賴兩者；元件契約依賴 UX；元件庫依賴前三塊加契約 | 部分：缺「UX 審查依賴 i18n／design-system」 |
| A-27 | vb | v02-walkthrough〈範例四：兩軸測試交集去重〉 | 與 UC 同斷言的不變式不另建測試；未涵蓋者各建 | SKILL.md Step 5：「兩軸交集去重、聯集為完整覆蓋」 |
| A-28 | vb | v02-walkthrough〈範例五：GREEN 票粒度〉 | 共用狀態且未超職責閾值按模組合併；「或」由閾值決定 | 部分：缺「共用狀態 → 合併」「「或」由閾值決定」 |
| A-29 | vb | v02-walkthrough〈範例六：並行安全〉 | 共用匯出檔只由整合票修改 | 部分：缺「各票不得直接修改」 |

（ccd＝component-contract-design、dsg＝dart-style-guardian、sdg＝skill-design-guide、vb＝version-bootstrap）

#### B 組：ux-design-evaluation

| 編號 | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|
| B-01 | SKILL.md〈與相鄰資產的交界〉p90 600ms | 產出須為可引用具體值：600ms 填按鈕 loading | SKILL.md：「不能只寫「要有回饋」」；waiting-and-notification〈時間門檻〉400ms-1s 列 |
| B-02 | input-mechanism.md〈四個決策維度〉無頻率資料 | 依是否在核心操作必經路徑排序 | input-mechanism〈四個決策維度〉：「以完成核心操作的臨界路徑上是否必經此鍵作為排序依據」 |
| B-03a | input-mechanism〈四個決策維度〉CLI 輸入 | CLI：隱碼型鍵盤、整行送出、IME 全關、底部工具列 | 部分：缺「整行送出」依據 |
| B-03b | 同上 驗證 Q1 | 決策表值寫中性描述，不寫框架列舉名 | **條文查無** |
| B-04a | input-mechanism〈場景一：表單〉Keyboard type | 格式不固定用一般文字鍵盤 | 〈場景一〉：「格式不固定…則維持一般文字鍵盤」 |
| B-04b | 同上 驗證 Q1 | 決策欄寫中性描述，框架對應放旁註 | **條文查無** |
| B-05 | 〈場景一〉驗證時機規則交界 | 本地可算屬格式驗證 on blur；需查伺服器屬業務驗證 on submit | 部分：缺「能否本地算出」分類判準 |
| B-06 | 〈場景二：搜尋〉搜尋成本 | 依成本不依是否本地；成本高採送出或 debounce | 〈場景二〉表列：「搜尋成本高：DB 全文搜尋」 |
| B-07 | 〈IME 安全清單〉confidential | 日記 confidential 非 secret：關學習、保留自動校正 | 部分：缺「自然語言時保留開啟」方向句 |
| B-08 | interaction-feedback-component-semantics.md〈六項判準〉第 1 項 | 三值、不增減元素、可並排 → segmented control | 同檔第 1 項：「能 → 改用 segmented control」 |
| B-09 | 同檔第 2 項 | 圓角淺底文字 chip 屬非按鈕形態，邊框非判準 | 同檔第 2 項：「指示用明顯非按鈕形態（文字 chip、圓點）」 |
| B-10 | 同檔第 5 項 | 關鍵計數不得省略號；先縮格式再設最小寬度 | 同檔第 5 項：「先縮短格式…仍不足才設定最小寬度」 |
| B-11 | waiting-and-notification〈時間門檻〉p90／p95 | 兩者落不同帶時須記錄選用統計量 | 同檔：「須顯式記錄選用的統計量（p90 或 p95）」 |
| B-12a | 同檔〈畫面級回饋〉 | 本節只填 UI 呈現欄 | interaction-feedback.md 檔頭：「狀態的枚舉與退出路徑歸狀態矩陣」 |
| B-12b | 同上 驗證 Q2 | error 與 disconnected 皆須重連與返回 | 部分：缺「disconnected 屬 error 類」 |
| B-13 | interaction-feedback.md〈按鈕級回饋〉timeout | 取值略大於後端逾時，緩衝無固定公式 | 部分：缺「緩衝量由誰依什麼決定」 |
| B-14a | screen-state-matrix.md〈填寫範例〉遠端終端機 | 無 enum 時由視覺差異反推狀態 | 〈填寫步驟〉1：「每種不同的視覺呈現就是一個狀態」 |
| B-14b | 同上 | 退出路徑空格即死胡同 | 〈填寫步驟〉3：「空格 = 死胡同」 |
| B-14c | 同上 驗證 Q3 | 可用操作中離開本狀態者須同名出現在退出路徑欄 | **條文查無** |
| B-15 | screen-state-matrix〈容易漏列的狀態：initializing〉 | 常駐服務有獨立生命週期需 initializing；單次請求不算 | 部分：缺「單次請求不算獨立生命週期」 |
| B-16 | screen-state-matrix〈反模式〉反向操作 | 可逆性由中斷連線提供，導覽返回不算 | 部分：缺「導覽返回不算反向操作」 |
| B-17 | screen-state-matrix-selection-prestate.md〈三段鏈式範例〉 | 三條件成立時各畫面「未選定」獨立成列、四欄填齊、記錄設定入口與清除時機 | 同檔：「三項皆成立即屬「選取類前置呈現」，須獨立成列」 |
| B-18 | error-recovery.md〈錯誤訊息的兩個職責〉 | 逾時屬網路層：檢查網路後重試 | 同檔：「不可只給「重試」，也不升級為執行環境層行動」 |
| B-19a | error-recovery〈重試機制〉 | 前景連線瞬斷採固定間隔 | 同檔：「用固定間隔並設上限」 |
| B-19b | 同上 驗證 | 重試中顯示次數、下次時間，並提供立即重試與返回 | 部分：缺「固定間隔場景也要立即重試鈕」 |
| B-20 | error-recovery〈Degraded mode〉未用過功能 | 判準是是否用過，非上線多久 | 同檔：「只適合使用者從未用過的功能」 |
| B-21 | error-recovery〈Degraded mode〉防抖 N | N 以單次逾時為下界 | 〈進入與退出〉：「N 以該功能單次請求的逾時值為下界」 |
| B-22 | navigation-patterns.md〈go vs push vs pushReplacement〉tab | tab 間切換不屬三種堆疊操作 | 部分：缺「tab 間切換不屬三種堆疊操作」 |
| B-23 | navigation-patterns〈常見誤用〉 | 一次性配對 push、登入後 go、送出後 pushReplacement | 同檔語意表三情境列 |
| B-24 | navigation-patterns〈iOS HIG vs Material〉 | top tabs 屬視覺風格差異可不適配 | 部分：缺「未列舉元件如何歸類」 |
| B-25 | navigation-patterns〈Deep link〉 | 有既有入口重建堆疊；進入方式不影響 | 〈Deep link〉：「目標畫面在 app 內是否有既有入口可重複到達」 |
| B-26 | gate-fallback.md〈每道 gate 的必答問題〉 | 失敗欄依 fallback 是否相同分列 | 同檔：「fallback 相同的類型合併為一列」 |
| B-27 | gate-fallback〈網路 gate〉 | 第一問否第二問是 → Degraded mode 並標示非最新 | 〈網路 gate〉判別順序句 |
| B-28 | gate-fallback〈權限 gate〉 | 核心權限首次開啟請求；次要延到使用時 | 〈權限 gate〉：「首次開啟請求只適合權限少（1-2 個）且與核心功能直接相關」 |
| B-29 | gate-fallback〈破壞性操作確認 gate〉 | 名實落差大的「匯入」要確認並寫具體後果；誠實刪除也要確認 | 部分：缺「落差小的不可逆操作是否仍須確認」 |

#### C 組：foundation-design

| 編號 | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|
| C-01a | examples.md〈問作用不問存在〉 | 只讀標題檔名不能判作用，須搜內文 | SKILL.md〈判準的通用形式〉：「文件存在不代表它回答得了問題」 |
| C-01b | 同上 | 錯誤承接宣告比錯誤缺口宣告更難被發現 | **條文查無** |
| C-02a | examples.md〈萃取揭露規格漂移〉 | 規格散文與畫布實測衝突以畫布為準 | 部分：handoff-mode 原句講程式碼；缺畫布優先 |
| C-02b | 同上 | 規格改寫後指向 token 不複述數值 | 部分：缺「規格不得複述 token 數值」 |
| C-03a | examples.md〈萃取不等於照抄〉 | 值來自設計畫布時萃取當下歸納為離散階 | handoff-mode〈命名與收斂分兩步〉：「萃取當下即歸納為離散具名階」 |
| C-03b | 同上 | 值已被程式碼引用時逐值命名、收斂另開票 | handoff-mode〈命名與收斂分兩步〉：「第 2 步仍逐值命名、值不變」 |
| C-04a | examples.md〈權威形態不符時的正確取用〉 | 部分段落不適用不等於整維度不適用 | SKILL.md〈權威缺席時〉形態 2：「來源是 SaaS skill 不構成整條跳過的理由」 |
| C-04b | 同上 | 改寫產物須對應現況真缺口 | 部分：缺「須對應現況缺口」 |
| C-05a | examples.md〈產物有而判準無〉 | 判準層缺口不推翻產物層已存在；另標無判準建缺口票 | SKILL.md〈權威缺席時〉：「『產物有、判準無』是常見狀態」 |
| C-05b | 同上 | 兩層合併填「待定：缺口」是錯的 | 部分：缺合併填寫的後果 |
| C-06a | examples.md〈UI 維度適用條件不限時的處置〉 | 純 CLI 以無 GUI 框架為由不構成失效不存在 | dimension-product-notes〈判定「失效不存在」的理由〉：「沒有圖形介面框架）不構成失效不存在」 |
| C-06b | 同上 | UI 維度走改寫產物：輸出格式契約 | 部分：缺「語意在純文字輸出對應欄位順序、色碼語意」 |
| C-07a | examples.md〈產物欄「無」與「待定」的拆兩問判斷〉 | 無／待定區分軸是是否已判完 | SKILL.md〈產物欄的五種合法答案〉：「「無」的語意是判完了、結案」 |
| C-07b | 同上 | 填無須附理由與重評條件 | SKILL.md 同表「無」列 |
| C-07c | 同上 | 無的理由須指出權威原句對象不存在 | dimension-product-notes：「須指出權威原句所講的對象在本專案不存在」 |
| C-07d | 同上 | 產物層判無時不需再查判準層 | **條文查無** |
| C-08a | examples.md〈權威缺席的查證對象〉 | 權威是否存在查 session 端 | SKILL.md〈權威缺席時〉：「看的是執行本流程的 session 的 `.claude/`」 |
| C-08b | 同上 | 被盤點 repo 無框架目錄不構成權威缺席 | SKILL.md 形態 1：「它沒有框架是常態，不構成權威缺席」 |
| C-08c | 同上 | 產物落在被盤點 repo | SKILL.md：「產物的載體…看的是被盤點的 repo」 |
| C-09a | examples-process.md〈文件回答不了地基問題〉 | 任一格答不出即命中 | SKILL.md〈與 orchestration 的協作〉：「無法回答維度表任一格的產物」 |
| C-09b | 同上 | 空殼 scaffold 不算相當程度實作 | **條文查無** |
| C-10 | examples-process〈規劃波進行中與之後〉 | 地基票不算該版票已建 | 部分：缺「地基票不計入」 |
| C-11a | examples-process〈產物落為地基票的粒度〉 | 一產物一票 | 部分：缺非 UI 維度通則 |
| C-11b | 同上 | 已存在產物不建票 | SKILL.md〈工作流〉步驟 3：「不建票」 |
| C-12 | examples-process〈執法載體掃描範圍〉 | 部分涵蓋即前置條件第三項不通過 | 部分：缺「部分涵蓋即不通過」 |
| C-13a | examples-process〈降級記錄須為缺項對照步驟表〉 | 須記缺項與不成立步驟 | SKILL.md〈移植前置條件〉：「記下缺哪一項、因此哪幾步在本專案不成立」 |
| C-13b | 同上 | 缺決策文件先建立 | SKILL.md：「連決策文件都沒有時，第一個動作是建立它」 |
| C-13c | 同上 | 降級記錄須為對照表，散文不合格 | **條文查無** |
| C-14a | examples-process〈人工複核「複核者」欄〉 | 不得留白等待指派 | SKILL.md〈工作流〉步驟 5：「不得留白等待指派」 |
| C-14b | 同上 | 權威未訂審查角色退至框架審查代理人；個人不合格 | 部分：缺先後順序與「個人不是既有角色」 |
| C-15 | handoff-mode.md〈萃取的前提是既有 artifact 可信〉 | 作者在職但兩週無回應也算決策者不可得 | **條文查無** |
| C-16a | handoff-mode〈補特徵測試有規模閘門〉 | 閘門對象是抽取範圍覆蓋，非總行數 | 部分：缺明文 |
| C-16b | 同上 | 抽取範圍覆蓋 0% 走替代路徑 | 部分：缺「補不起來」判定條件 |
| C-17a | dimension-product-notes.md〈複合產物與多子樹分列〉 | 三命令任一兩套以上即分列 | 同節：「有沒有兩套以上的測試命令、建置命令或部署目標才重要」 |
| C-17b | 同上 | 分列後各子樹獨立填答 | 同節：「單一格會把落後那棵子樹的缺口吃掉」 |
| C-18a | dimension-product-notes〈可觀測性維度〉 | 無背景執行緒不構成失效不存在 | 〈判定「失效不存在」的理由〉發生途徑句 |
| C-18b | 同上 | 對象系統存在不得填無 | 同節：「對象在本專案存在時不得填「無」」 |
| C-18c | 同上 | 失效長成輸出失敗不可見，走降級顯示契約 | 部分：失效形狀本身條文查無 |
| C-19a | dimension-product-notes〈UI 維度〉 | 目前只有一種輸出樣式不構成失效不存在 | 同節：「失效目前尚未發生（如目前只有一種輸出樣式）不構成失效不存在」 |
| C-19b | 同上 | 語意對應欄位順序／色碼／截斷，走輸出格式契約 | 部分：缺對應判準 |

#### D 組：tdd／test-assertion-design

| 編號 | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|
| D-01 | tdd test-object-catalogue.md〈一、測試對象的單位判定〉 | 一 FR 多宣告時每宣告一行 | 同檔候選單位表：「以每個可獨立驗證的規格宣告為單位」 |
| D-02 | test-object-catalogue〈三、契約欄位表〉反例欄 | 反例欄寫具體誤判情境，不只寫類別名 | 部分：缺「不得只寫類別名」（只見範例引言句） |
| D-03a | 同檔〈三〉斷言來源欄 | 兩欄同條照抄、不同條分填 | **條文查無**（只見範例引言句） |
| D-03b | 同上 | 重疊時斷言來源欄不可留空 | 部分：缺「重疊時仍須填」 |
| D-04a | 同檔〈六、派發語言〉範例一 | When 只寫規格觸發條件不寫機制 | **條文查無**（句型在程式碼圍欄內） |
| D-04b | 同上 | Given 對前一狀態、Then 對後一狀態 | **條文查無** |
| D-05 | 同檔〈六〉範例二 跨邊界 | 離開 process 子步驟歸實機，app 內歸 widget，填「兩者」 | doc-handoff-test-tier-assignment〈跨邊界步驟：平台決定〉 |
| D-06a | 同檔〈六〉範例三 | 出現、內容、可讀性三類驗收不合併 | 同檔〈二、來源類別〉：「是否出現、內容、可讀性三類驗收各對應不同形態」 |
| D-06b | 同上 | Then 只寫可觀察結果不寫框架節點 | **條文查無**（tdd 內；同義判準只在 skill-design-guide） |
| D-07 | test-object-catalogue-starting-points.md〈起點 B〉 | 任一斷言追溯不到即整則標無規格來源並拆兩則 | 同檔 B3：「整則即標記『無規格來源』」 |
| D-08 | 同檔〈起點 C〉 | characterization 以公開進入點為單位 | 同檔 C2：「每個可呼叫的公開進入點一條」 |
| D-09 | doc-handoff-test-tier-assignment.md〈跨邊界步驟〉 | 對話框互動屬平台決定，結果後畫面更新屬自己決定 | 同節：「落在外面才問平台決定」 |
| D-10 | 同檔〈與 UC 資訊鏈的關係〉 | 節點內邏輯歸單元，送出後查得到屬串接歸整合 | 〈判斷原則〉：「串接決定 = 整合測試」 |
| D-11 | bdd-behavior-testing.md〈BDD→單元拆分標準〉 | 多訊號時先拆單元，Given 仍過長才拆 Module | 同節：「移除後 Given 設置仍 > 10 行 Mock，才考慮拆分 Module」 |
| D-12a | doc-handoff.md〈前置閘門〉 | 完整場景步驟須有動作與預期結果 | **條文查無** |
| D-12b | 同上 | 含使用者操作即涉及 UI | **條文查無** |
| D-13 | doc-handoff〈Phase 1 輸入映射〉 | 可觀察轉量化；主觀原文照抄標〔不可量化〕 | 同節映射表：「原文照抄並標『〔不可量化〕』」 |
| D-14 | doc-handoff〈追溯矩陣初始化〉 | entry 以 UC 場景為單位 | 同節步驟 2：「為每個場景建立 entry」 |
| D-15 | doc-handoff-uc-to-gwt-conversion.md〈轉換注意事項範例〉範例一 | Given 承接依賴的前序狀態，未依賴不列 | 〈轉換注意事項〉：「不符合此判準的前序狀態不必列入 Given」 |
| D-16 | 同上 範例二 | 操作者換成系統時 When 標「系統」 | 同節：「When 欄位應標注操作者」 |
| D-17 | 同檔〈轉換範例〉 | 同步驟多結果 When 不同各拆種子 | 〈無 UC 步驟的 FR → GWT 種子〉：「每組 Given／When 各拆為一個種子」 |
| D-18 | 同檔〈GWT 種子與行為單元的分工邊界〉 | 產出只到種子，欄位僅四欄 | 部分：缺「欄位僅限四欄」 |
| D-19 | test-assertion-design SKILL.md〈判讀範例〉聚焦性 | 一宣告一面向；未提屬性不加斷言 | 〈斷言品質三問〉：「一條宣告一個面向」 |
| D-20 | 同〈判讀範例〉判斷軸 | 以識別信號欄判命中；已 await 不命中類型 5；同時命中兩類都處置 | 〈判斷軸〉：「以〈問題類型表〉『識別信號』欄逐一核對是否成立為準」 |
| D-21 | 同〈判讀範例〉類型 9 | 同宣告內條件各一斷言不算過度集中 | 〈斷言品質三問〉：「斷言數依類型 9 隨條件數成比例」 |
| D-22 | 同〈判讀範例〉類型 8 | 無格式監測驗收不保留全結構快照 | 類型表類型 8：「全結構快照只在規格另有…驗收條件時設置」 |
| D-23 | 同〈判讀範例〉類型 6 | 一般隨機替換為可控源；要求不可預測時不替換 | 類型表類型 6 例外句 |
| D-24a | 同〈判讀範例〉元件執行期行為 | 「不顯示清單容器」歸結構驗證，「最多 20 筆」歸邏輯不變式 | 〈確定性斷言的基礎形態〉兩形態定義句 |
| D-24b | 同上 | 不斷言長度為 0、不斷言筆數等於 20 | **條文查無** |

#### E 組：doc（examples/component-library-chained-examples.md；條文＝templates/component-library-spec-template.md 範例以外文字）

| 編號 | 範例位置 | 教的判定 | 條文依據句位置 |
|---|---|---|---|
| E-01 | 〈§1〉支援的形態 | 外接鍵盤成主要輸入才算獨立形態 | template〈1. 形態因素矩陣〉：「完成主要任務所用的輸入方式改為該裝置時，才構成獨立形態」 |
| E-02 | 〈§1〉每形態各一份粒度 | 第 4 章加欄加列不另立子節；第 5 章容器每形態一份 | template〈1〉：「加欄或加列，不另立平行子節」 |
| E-03 | 〈§1〉尺寸適應策略 | 使用者手動切版合法；依寬度自動切換違規改填維持 | template〈尺寸契約〉(b) 句與「不得選，改填「維持」」 |
| E-04 | 〈§1〉回饋通道子表 | 三選一；通道具體到形式；桌機不豁免播報替代 | template〈回饋通道子表〉：「單填「視覺」等籠統詞不構成」 |
| E-05 | 〈§2〉樂觀更新 | 低風險與可逆 AND，看後果不看位置 | template〈2〉：「「低風險」與「可逆」須同時成立（AND）」 |
| E-06 | 〈§3〉L2／L3 | 依語意判層級，出現畫面數不作判準 | template〈3〉：「層級依語意判定，出現畫面數」「不作判準」 |
| E-07 | 〈§4〉內容角色複合粒度 | 描述同一件事歸同角色；狀態圖示＋標題為兩角色 | 部分：缺「描述狀態的圖示歸標籤」 |
| E-08 | 〈§4〉變體與 disabled | 變灰不可點擊入狀態矩陣，不列變體 | template〈變體〉：「一律入狀態矩陣，不得列為變體」 |
| E-09 | 〈§4〉退出路徑欄格式 | 固定寫「{觸發事件} → {目標狀態}」 | **條文查無** |
| E-10a | 〈§4〉來源欄 前提 A | 存 store 可跨元件讀 → 資料態 | template〈狀態矩陣〉：「值存於元件之外、可被其他元件讀取者為資料態」 |
| E-10b | 同上 前提 B | 只在複合元件內部 → 互動瞬態 | 部分：缺「元件內部不暴露的推導值歸互動瞬態」 |
| E-11 | 〈§4〉同步策略欄 | 三選項適用判準、互斥 | 部分：只有選項名 |
| E-12a | 〈§4〉回饋契約對應狀態邊 | 狀態名逐字取自狀態矩陣 | template〈回饋契約〉：「狀態名須取自上方狀態矩陣的狀態集」 |
| E-12b | 同上 | 一事件多邊逐條列 | **條文查無** |
| E-13 | 〈§4〉等待與結果四段 | 離線與 5xx 歸服務不可用；缺段即未覆蓋 | template〈回饋契約〉服務不可用定義句與「缺一段即視為未覆蓋失敗路徑」 |
| E-14 | 〈§4〉降級不解除回饋義務 | 前提指通道可用性；降級須標示 | 部分：缺「前提＝通道可用性」 |
| E-15 | 〈§4〉三層回饋模型承載層 | 3 秒落第 2 層；只填層名不完整 | 部分：缺時間帶門檻與「只填層名不完整」 |
| E-16 | 〈§4〉操作機制精度 | 兩欄填數值比較；不足配替代機制 | template〈操作機制〉：「須填該形態的具體精度數值」 |
| E-17a | 〈§4〉slot 文字來源強語意 | 強語意判準是文案是否因呼叫情境需不同用字 | **條文查無** |
| E-17b | 同上 | 無插值參數屬無可覆蓋對象 | template〈slot 契約〉：「文案即無可覆蓋對象，非「禁止覆蓋」）」 |
| E-18 | 〈§4〉i18n 表與 slot | 呼叫端傳入 slot 列一列標註來源 | template〈i18n〉：「slot 列一列標註「呼叫端傳入；本元件無自有 key」」 |
| E-19 | 〈§4〉組合規則內距 | 欄內距照填；欄間距歸容器 | template〈組合規則〉：「與鄰件的間距不在此定義，由所在容器的排列不變式承載」 |
| E-20 | 〈§4〉測試點粒度 | 每事件一則；不可用＋替代測替代通道 | template〈測試點〉：「回饋契約每個已定義事件各自一則測試」 |
| E-21 | 〈§4〉實作註記語意不變 | 等值字面量取代 token 屬註記；改數值屬契約變更 | 部分：缺「數值改變一律屬契約值變更」 |
| E-22 | 〈§5〉框架容器安全區 | 頁面根堆疊與殼層加安全區列，對話框不用 | 部分：缺「對話框避讓由尺寸契約承接」 |
| E-23 | 〈§6〉原生元件禁用對照表 | 元件庫內部使用不列；頁面層排列與互動元件列入 | template〈6〉：「元件庫內部實作使用屬正常，不列本表」 |
| E-24 | 〈§7〉結構性無法收斂 | 能包裝達成的不可豁免；理由欄記錄嘗試路徑 | template〈7〉：「理由欄記錄已嘗試的封裝路徑與失敗原因」 |

### 依據只在範例內清單（W2 閘門第二條件，出題者只標記）

**條文查無（20 列）**：A-11、A-17b、B-03b、B-04b、B-14c、C-01b、C-07d、C-09b、C-13c、C-15、D-03a、D-04a、D-04b、D-06b、D-12a、D-12b、D-24b、E-09、E-12b、E-17a

**依據句位於程式碼圍欄內（2 列，待裁定是否算條文）**：A-08、A-09

**部分覆蓋（43 列，缺的片段只在範例內）**：A-03、A-07、A-12、A-15、A-21、A-25、A-26、A-28、A-29；B-03a、B-05、B-07、B-12b、B-13、B-15、B-16、B-19b、B-22、B-24、B-29；C-02a、C-02b、C-04b、C-05b、C-06b、C-10、C-11a、C-12、C-14b、C-16a、C-16b、C-18c、C-19b；D-02、D-03b、D-18；E-07、E-10b、E-11、E-14、E-15、E-21、E-22

### 待 PM 裁定

1. **非標記格式 5 則**（A-08～A-12）是否納入出題範圍。
2. **程式碼圍欄內的條文**（A-08、A-09）與**範例引言句**（D-02、D-03a 的判定只見於【輸入】前「以下示範…」引言）是否算條文。
3. **跨 skill 依據**（A-03 在方法論、A-17b 在 ccd、A-28 在 rules/core/cognitive-load）是否算該 skill 的條文；影響餵入範圍。
4. **出題粒度**：以範例為單位（120 則 → 240 題）或以判定列為單位（159 列 → 318 題）。

### 分批建議（每批 ≤ 60 題，以範例為單位）

| 批 | skill | 範例數 | 題數 |
|---|---|---|---|
| 1 | dart-style-guardian 4 + skill-design-guide 5 + version-bootstrap 8 + component-contract-design 7 | 24 | 48 |
| 2 | ux-design-evaluation | 29 | 58 |
| 3 | foundation-design 19 + test-assertion-design 6 | 25 | 50 |
| 4 | tdd | 18 | 36 |
| 5 | doc | 24 | 48 |

另一種排法：條文查無的 20 列，閘門第二條件本來就不通過，這些範例的探針結果改變不了閘門結論。可以先補條文再出題，把本輪題量降到約 200 題。

### 各檔題數

本輪 0 題，`probes/q/W3-417-*`、`probes/key/W3-417-*` 都還沒建立，原因是觸發了步驟 4 的規模門檻。

### WRAP 認領三問

- W：候選做法有兩個。一是全量一次出題，二是依步驟 4 先盤點再分批，採後者。
- A：全量出 240 題會讓凍結題組的 PM 核對工作量超過一輪能負荷的量。
- P：最可能的失敗原因是五組並行盤點的依據句判定標準不一致，例如引言句、圍欄、跨 skill 依據各組處理方式不同。防護做法是把不一致處列入「待 PM 裁定」。

### 用戶裁示與 PM 定義裁決（2026-09-15，出題前）

**用戶裁示**
- 條文完全查無依據的 20 個判定，和只有部分依據的 43 個判定，共 63 個，全部先補條文，補完才出題。
- 探針分 5 批跑，每批最多 60 題，每則範例出兩題：新輸入一題、相反分支一題。

**PM 定義裁決**（屬於盤點口徑，不涉及用戶的程式碼風格偏好）
1. **沒有三段標記的範例**（A-08～A-12）：只要教了判定，就納入範圍。
2. **什麼算條文**：
   - 範例區塊外、緊接在範例上方的引言句，算條文。
   - 程式碼圍欄如果本身就是規則或範本，而且位在範例區塊外，算條文。
   - 範例內的圍欄不算條文。
3. **跨 skill 的依據**：本 skill 在該判定處明文路由到那份文件時，才算有依據，探針的餵入範圍也要包含那份文件。沒有路由時，修法是補路由句，不是複製條文。

**修補分工**：每個 skill 開一張修補票，每張都走兩階段。
- 階段 1：權威條文已經寫明，只是本 skill 沒有引用或路由到它。直接補路由或補句，引用時附出處。
- 階段 2：需要新增判準、會改變既有判定。只寫草稿後停手，交給 WRAP；不是用戶程式碼風格偏好的，不上呈用戶。
### 階段 2 裁決（PM，2026-09-15；E-09 格式經用戶裁示）

六組 WRAP 評估的建議全部採用，評估內容寫在各來源票 Problem Analysis 的〈階段 2 WRAP 評估〉：G1 W3-421/422、G2 W3-426/424/418/420、G3 W3-423、G4a/G4b W3-419、G5 W3-420。

**逐項例外與補充**
- **E-09**：採用戶裁示，退出路徑格式寫死為「{觸發事件} → {目標狀態}」，並寫進 ux-design-evaluation `screen-state-matrix.md`〈四欄定義〉。範例 Q2 保留並改引這條條文。
- **D-12b**：不另外寫「呈現通道表第二列交給誰」，依評估者的傾向處理。
- **E-07**：doc 範本與 component-contract-design 的內容角色定義同批修改，定義文字以 W3-423 評估草稿為準，兩處逐字一致。
- **A-28、C-11a**：合併判準路由到 task-splitting〈拆分後檢查清單〉B 與 cognitive-load〈速查三閾值〉。component-contract-design 標記為 portable，所以 A-12 條文自足撰寫，但內容要與前述合併判準一致。
- **W3-419**：B-14c 與 E-09 改的是同一個檔案的同一節，由 W3-419 一併落檔；B-12b 範例與修復後矩陣畫面元素不一致的問題也一併修正。

**評估中登記的 spawn request，由各落檔票在 complete 前轉為票**
- W3-419 SR-1：退出路徑指離開狀態還是離開畫面（ANA）
- W3-420 SR-1：空殼 scaffold 的「相當程度」（ANA）
- W3-422 SR-1：doc-handoff「三項檢查」與閘門表四列不一致（DOC）
- W3-423 SR-1：component-contract-design 內容角色與範本矛盾（併入 E-07 同批落檔）
- W3-423 SR-2：step-3 路由錯位（DOC）
- W3-423 SR-3：浮層容器邊緣避讓由誰承接（ANA）
- G2 兩項發現：問 3 首次拆分的缺口（ANA）、version-bootstrap 與 task-splitting 對 GREEN 票拆分單位的衝突（ANA）

**落檔後的非撰寫者複核**：依用戶裁示，閘門改為探針。本輪落檔不再派七欄表複核，由 5 批探針驗證。
### 第 1 批題組（出題者，2026-09-15，凍結）

條文基準 commit `09bdb258`。範圍：dart-style-guardian、skill-design-guide、version-bootstrap、component-contract-design（盤點表 A-01～A-29，含 A-08～A-12）。每則範例 2 題：a＝範例未出現的新輸入，b＝判準相反分支。餵入只給範例區塊外條文，跨 skill 依據依定義 3 只在有明文路由時列入（CCD-03 方法論〈失敗路徑覆蓋〉、SDG-01b ccd〈共用規則一〉、VB-07／08 task-splitting〈拆分後檢查清單〉B 與 cognitive-load〈速查三閾值〉、VB-04 step-rationale〈Step 2 的 UI 前置檢查〉）。答案鍵引文共 100 句，已逐句以 `git grep -F <引文> 09bdb258` 確認命中出處檔。

**各檔題數**（scratchpad `probes/q/` 與 `probes/key/`，檔名 `W3-417-b1-<skill>.md`）

| skill | 範例數 | 題數 |
|---|---|---|
| component-contract-design | 12 | 24 |
| dart-style-guardian | 4 | 8 |
| skill-design-guide | 5 | 10 |
| version-bootstrap | 8 | 16 |
| 合計 | 29 | 58 |

**範例到題號對照**

| 盤點列 | 題號 | 盤點列 | 題號 |
|---|---|---|---|
| A-01 | CCD-01a／01b | A-16 | DSG-04a／04b |
| A-02 | CCD-02a／02b | A-17a | SDG-01a |
| A-03 | CCD-03a／03b | A-17b | SDG-01b |
| A-04 | CCD-04a／04b | A-18 | SDG-02a／02b |
| A-05 | CCD-05a／05b | A-19 | SDG-03a／03b |
| A-06 | CCD-06a／06b | A-20 | SDG-04a／04b |
| A-07 | CCD-07a／07b | A-21 | SDG-05a／05b |
| A-08 | CCD-08a／08b | A-22 | VB-01a／01b |
| A-09 | CCD-09a／09b | A-23 | VB-02a／02b |
| A-10 | CCD-10a／10b | A-24 | VB-03a／03b |
| A-11 | CCD-11a／11b | A-25 | VB-04a／04b |
| A-12 | CCD-12a／12b | A-26 | VB-05a／05b |
| A-13 | DSG-01a／01b | A-27 | VB-06a／06b |
| A-14 | DSG-02a／02b | A-28 | VB-07a／07b |
| A-15 | DSG-03a／03b | A-29 | VB-08a／08b |

A-17 一則範例教兩個判定，a 題出 A-17a、b 題出 A-17b 的相反分支。

**依據缺失清單**：0 題。58 題皆可由範例外條文作答。

**出題時避開、未出題的分支**（範例或條文本身的問題，非本批題目，供 PM 裁決是否開票）
1. version-bootstrap `examples/v02-walkthrough.md` 範例五第 4 問的依據是 task-splitting〈策略 7〉「不以依賴為由合併」，SKILL.md Step 6 只路由到〈拆分後檢查清單〉B 與〈速查三閾值〉，依定義 3 屬未路由。
2. version-bootstrap Step 6：共用檔案要合併，但合併後職責數 > 2 又要拆，兩條同時命中時怎麼處理沒有交代；拆開後各票又回到共用檔案，與 task-splitting B 的 Write-Write 衝突檢查衝突。
3. version-bootstrap Step 4.5：「其餘依順序表預設相依」有沒有讓元件契約（3.5）也預設依賴 i18n／design-system，沒有寫明。
4. dart-style-guardian i18n-guidelines：〈Exceptions〉把錯誤碼列為技術識別符豁免，〈Violation 2〉卻要求錯誤碼交給呈現層轉譯；錯誤碼直接顯示給使用者時兩條的交界沒有交代。DSG-04b 題幹已限定只顯示錯誤碼、無標籤。
5. component-contract-design 還有含判定的範例沒列進 A 組盤點，本批沒出題：SKILL.md 兩則、step-2-extract-components〈共用規則二〉歸併過頭、step-2-path-b 兩則、step-2-path-c 一則、step-3-fill-contract-conditional-cases 一則、step-4-verify-completeness 一則，以及 appendix-examples 多則驗證問句。

**探針規則區塊**沿用 V1-nongate-dual-r 格式，改動三處：(1) 餵入章節內的範例區塊逐檔寫明跳過邊界；version-bootstrap 題組直接禁開 `examples/`。(2) 作答四項改問判定與依據句。(3) 拿掉矛盾判定第 5 項。
## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 第 1 批判定（獨立判定者，2026-09-15）

**判定方法**：條文一律以 `git show 09bdb258:<path>` 讀取。每題先只讀題目與餵入條文寫出正確判定，再開答案鍵比對，最後逐份比對 3 份答案。3/3 才算通過（用戶裁示）。

**計分口徑**（本批自訂，影響 5 格）：
- 以 (1) 判定句為準。作答者明寫「更正／修正判定」者，以修正後的判定計分。
- 判定句與依據段矛盾、且沒有明寫更正者，判不通過。
- 題目有兩個子問、只答一問者，判不通過。
- 引文與實例值取自範例、但判定形式正確者，照判定計分，另列入探針異常。

**答案檔位置**：12 份都讀了。其中 `version-bootstrap-s2` 取自 `-graph_project_docs_manager` 目錄，`component-contract-design-s1` 取自 `-graph-project-docs_manager` 目錄。

#### 逐題表：dart-style-guardian（8/8）

| 題號 | 正確判定與依據 | s1 | s2 | s3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| DSG-01a | 按鈕 negative、確認訊息 positive。依據：color-system〈顏色歸類判準〉「功能類型不因觸發它的前置操作是否具破壞性而改變」 | 是 | 是 | 是 | 是 | — |
| DSG-01b | negative，不改判。依據：同上；〈Three-Color System〉「warnings, errors, destructive actions」 | 是 | 是 | 是 | 是 | — |
| DSG-02a | 主題插槽。依據：〈Theme 與 Token 判準〉「對應的內建語意插槽存在時用主題插槽，不存在時用 token」 | 是 | 是 | 是 | 是 | — |
| DSG-02b | token。依據：同上 | 是 | 是 | 是 | 是 | — |
| DSG-03a | titleLarge。依據：typography〈字級判準〉「卡片或對話框等容器元件內的標題對映 `titleLarge`」 | 是 | 是 | 是 | 是 | — |
| DSG-03b | headline4，不可任選。依據：同節「頁面內的階層子節標題對映 `headline4`」「數值相同不代表語意相同」 | 是 | 是 | 是 | 是 | — |
| DSG-04a | 不整體豁免：數值豁免，標籤走參數化翻譯。依據：i18n〈Exceptions〉「僅數值本身列入本豁免；標籤文字仍是使用者可見文字…」 | 是 | 是 | 是 | 是 | — |
| DSG-04b | 不需 i18n。依據：〈Exceptions〉「Technical identifiers - Error codes, keys」 | 是 | 是 | 是 | 是 | — |

#### 逐題表：skill-design-guide（10/10）

| 題號 | 正確判定與依據 | s1 | s2 | s3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| SDG-01a | 檢驗問句 + 預期答案。依據：chained-examples〈【驗證】段〉判定表「設計產出物」列 | 是 | 是 | 是 | 是 | — |
| SDG-01b | 不構成拆分理由，屬 slot 不拆。依據：ccd step-2-extract〈共用規則一〉「只是目的地不同者為 slot，不拆」 | 是 | 是 | 是 | 是 | — |
| SDG-02a | 違規。依據：〈三段鏈式格式〉「三段皆不可省略」 | 是 | 是 | 是 | 是 | — |
| SDG-02b | 合格。依據：同節 Action「三段都要寫全，順序固定…」 | 是 | 是 | 是 | 是 | — |
| SDG-03a | 填否；判「缺引導，非缺範例」；先把判準寫進條文。依據：〈驗證段機械檢查〉第 2 點與處置表 | 是 | 是 | 是 | 是 | — |
| SDG-03b | 合格，保留範例。依據：處置表「三項確認欄皆「是」｜合格｜保留範例…」 | 是 | 是 | 是 | 是 | — |
| SDG-04a | Given/When/Then。依據：判定表「執行期行為」列 | 是 | 是 | 是 | 是 | — |
| SDG-04b | 檢驗問句 + 預期答案。依據：判定表「設計產出物」列（含「起點判定」） | 是 | 是 | 是 | 是 | — |
| SDG-05a | 互斥不能當依據，改依四問逐段指名路由表的列。依據：splitting〈外移什麼、留什麼〉「互斥性因此不進入判定」「四個判定各自都要求你指名路由表的具體某一列」 | 是 | 是 | 是 | 是 | — |
| SDG-05b | 不落入觸發條件。依據：〈補範例後超標〉「超標成因是新補入的範例，不是原有規則文字本身過長」 | 是 | 是 | 是 | 是 | — |

#### 逐題表：version-bootstrap（9/16）

| 題號 | 正確判定與依據 | s1 | s2 | s3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| VB-01a | 屬契約檔案。依據：version-shift-sop 判準 1「…寫入並保存資料（資料庫列、使用者端檔案…）」「三問任一為「是」即屬契約檔案」 | 否（判非契約：新增鍵是執行期設定，改形狀不需遷移） | 是 | 是（先判否，後明寫更正為是） | 否 | 條文歧義 P1 |
| VB-01b | 非契約，多模組引用不改判。依據：「三問皆否：屬純程式邏輯，即使被多個模組引用…也不列入步驟 1」 | 是 | 是 | 是 | 是 | — |
| VB-02a | 判準 2 成立。依據：判準 2 列「…以 key 對接的翻譯提供者」 | 是 | 否（判不成立：key 改名可在本方單次提交完成） | 是 | 否 | 條文歧義 P1 |
| VB-02b | 不成立。依據：判準 2「判定單位是發布或部署單位，不是儲存庫」 | 是 | 是 | 是 | 是 | — |
| VB-03a | 非 UI 類，不過三項前置；Checkpoint 記呈現通道並交 foundation-design，不得只記「非 UI 類」。依據：SKILL Step 2 呈現通道表第二列 | 否（判定句寫「需過三項前置」，依據段寫「不過」，未更正） | 否（判 UI 類） | 否（判 UI 類） | 否 | 條文歧義 P2 |
| VB-03b | UI 類，要過三項前置。依據：表第一列「…含唯讀畫面」 | 是 | 是 | 是 | 是 | — |
| VB-04a | 不通過；補齊前不可繼續 UI 實作票規劃。依據：前置檢查表 L3 列「元件清單 + 原生元件禁用對照表 + 豁免清單」；「缺則先補齊才可繼續本提案的 UI 實作票規劃」 | 是 | 是 | 否（判通過；把表格列錯置，並自行假設豁免清單為選填） | 否 | 探針誤讀 |
| VB-04b | 通過。依據：L3 列「本步驟得先只到此，逐元件元件契約欄位表與容器條目於 Step 4.5 第 3 塊後補齊」 | 是（明寫修正為通過） | 否 | 否（判定句寫不通過，依據段卻支持通過） | 否 | 條文歧義 P3 |
| VB-05a | 不可開工；blockedBy 含 i18n 與 design-system。依據：Step 4.5 PM 工作「UX 審查因此以兩者為 `blockedBy`」 | 否（判定句寫「可以開工」，依據段矛盾；另虛構順序表依賴欄） | 是 | 是 | 否 | 探針作答異常 |
| VB-05b | 可開工，不以 i18n 為 blockedBy。依據：「唯 i18n 與 design-system 可並行」 | 是 | 否（判定句寫「不能」，依據段寫可獨立開工） | 是 | 否 | 探針作答異常 |
| VB-06a | 不另建。依據：Step 5「兩軸交集去重、聯集為完整覆蓋」 | 是 | 是 | 是 | 是 | — |
| VB-06b | 須建。依據：同句；「避免 domain 規則只靠「剛好出現於某 UC 場景」被動覆蓋」 | 是 | 是 | 是 | 是 | — |
| VB-07a | 1 張，不再拆。依據：Step 6「或」句；task-splitting B「共用檔案的問題已合併到同一 ticket」；cognitive-load「> 2 須拆分」 | 是 | 是 | 是 | 是 | — |
| VB-07b | 各 1 張。依據：Step 6「每個 spec FR 或功能模組 1 張」 | 是 | 是 | 是 | 是 | — |
| VB-08a | 不可各自修改，由整合票負責。依據：Step 6 PM 工作「共用檔案需整合票，歸屬後其餘票不得直接修改」 | 是 | 是 | 是 | 是 | — |
| VB-08b | 不需整合票。依據：同句前件「共用檔案」不成立 | 是 | 是 | 是 | 是 | — |

#### 逐題表：component-contract-design（19/24）

| 題號 | 正確判定與依據 | s1 | s2 | s3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| CCD-01a | 待決，回覆前不進 step 2。依據：step-1〈情況二〉「「無法核對」不等於「全否」，不得判 A」「回覆前不進 step 2」 | 否（判 A，可進 step 2） | 否（判待決，但沒答「可否進 step 2」） | 是 | 否 | 探針誤讀（s1）＋漏答（s2） |
| CCD-01b | C。依據：「任一命中即為 C」 | 是 | 是 | 是 | 是 | — |
| CCD-02a | 不渲染。依據：path-a〈反向規則〉「控制項不渲染，不以停用狀態渲染」 | 是 | 是 | 是 | 是 | — |
| CCD-02b | 待決，建規格追修票。依據：〈反向規則〉例外句 | 是 | 是 | 是 | 是 | — |
| CCD-03a | 三段用「不經服務：讀取的資料為 X，於 Y 時已載入；資料缺值時的表現見 Z」，不得寫同成功。依據：方法論〈失敗路徑覆蓋〉 | 是 | 是（Y 值取自範例） | 是（X、Y 值取自範例） | 是 | — |
| CCD-03b | 不適用固定寫法，四段逐一填表現。依據：同節「所讀資料需按需讀取、可能等待者不適用本寫法」 | 是 | 是 | 是 | 是 | — |
| CCD-04a | 第 3 格。依據：step-5〈格位起算〉「垂直堆疊以最上方…為第 1 格」 | 是 | 是 | 是 | 是 | — |
| CCD-04b | 不合格，寫「動作列 slot」。依據：「容器已具名 slot 者…寫 slot 名，不另行編號」 | 是 | 否（稱題目沒給 slot 契約，改寫成「內容 slot」或「第 3 格」） | 是 | 否 | 探針誤讀題幹 |
| CCD-05a | 不佔格位；寫在列末覆蓋層子句；置中不寫進畫面票。依據：〈覆蓋層〉 | 是 | 是 | 是 | 是 | — |
| CCD-05b | 不列。依據：「屬觸發元件自帶者…不列」 | 是 | 是 | 是 | 是 | — |
| CCD-06a | 離線獨立一列，標疊加於、只寫增量，底層列不動。依據：〈疊加態〉 | 是 | 是 | 是 | 是 | — |
| CCD-06b | 不在畫面票裁決，回狀態矩陣。依據：〈疊加態〉末句 | 是 | 是 | 是 | 是 | — |
| CCD-07a | 不新增列；定位與聚焦寫進該項元件回饋契約「被程式定位」格。依據：〈非狀態進入情境〉 | 是 | 是 | 是 | 是 | — |
| CCD-07b | 寫一列。依據：「how 的列固定為狀態矩陣的列」 | 是 | 是 | 是 | 是 | — |
| CCD-08a | 合為一個元件，兩者都不是對方的 slot。依據：path-a 步驟 2「兩集合完全相同為總是」「總是一起出現的單元合為一個元件」 | 否（合一，且一方為 slot） | 否（同 s1） | 否（同 s1） | 否 | 條文缺引導 P4 |
| CCD-08b | 不合併、非 slot，各自為候選（或容器子件）。依據：path-a 步驟 2「兩集合互有對方沒有的列時…各自為候選」 | 是 | 是 | 是 | 是 | — |
| CCD-09a | 1 個容器候選，子件為搜尋欄、狀態下拉、日期下拉、清除鈕。依據：步驟 4「每個佈局結構一個容器候選」 | 否（算 2 個，自行把主區也算進去） | 是 | 否（算 2 個，自行把清單也算進去） | 否 | 題目設計問題 |
| CCD-09b | 不是容器候選。依據：「單一子件的包裝…無排列關係，不是容器候選」 | 是 | 是 | 是 | 是 | — |
| CCD-10a | 否。依據：step-3〈關卡二〉「…指示消失後內容與事件前相同即答否」 | 是 | 是 | 是 | 是 | — |
| CCD-10b | 是。依據：同段「進行中狀態取代了該區塊的原內容…即答是」 | 是 | 是 | 是 | 是 | — |
| CCD-11a | 展開到分頁項的圖示 slot 放檔案類型圖示；關閉鈕不寫；數量寫「逐格放…，數量依資料筆數」。依據：step-5〈巢狀容器〉 | 否（判定句寫範例的「清單項徽章 slot」） | 否（判定句寫「關閉鈕應寫」，未更正） | 否（修正後關閉鈕不寫，但展開層寫成範例的「狀態徽章」） | 否 | 探針讀範例 |
| CCD-11b | 不續寫。依據：「直到被放入的元件不再有呼叫端須填的 slot 為止」 | 是 | 是 | 是 | 是 | — |
| CCD-12a | 2 張：report/ 與 report/export/；shared/ 不建。依據：〈遷移票〉段首 | 是 | 是 | 是 | 是 | — |
| CCD-12b | 1 張。依據：「「目錄」指直接含待改頁面檔的目錄」 | 是 | 是 | 是 | 是 | — |

#### 各 skill 通過率

| skill | 通過／題數 | 未通過中屬條文問題 |
|---|---|---|
| dart-style-guardian | 8／8 | 0 |
| skill-design-guide | 10／10 | 0 |
| version-bootstrap | 9／16 | 4 題（VB-01a、VB-02a、VB-03a、VB-04b），歸成 3 個條文問題 |
| component-contract-design | 19／24 | 1 題（CCD-08a），1 個條文問題 |
| 合計 | 46／58 | 5 題，4 個條文問題 |

#### 答案鍵錯誤清單

先自行推判定、再與答案鍵比對，58 題的結論全部一致，沒有判定錯誤。只有 1 處依據不完整：

- **CCD-08b**：輸入中兩單元只在「乙·正常」這一列同時出現，屬單一畫面內共現。path-a 步驟 2 對這種情形另有優先句：「單一畫面內共現的情形先依上句判為容器子件，不進入本比較」。答案鍵只引集合互有差集句，沒有處理這條優先句。兩條路徑的結論都是「不合併、非 slot」，因此不影響計分。這屬於題目設計問題：b 題的輸入意外命中了優先句。

另有 1 題屬題目設計問題，不計入條文問題：

- **CCD-09a**：題幹寫到「主區」「清單上方」，卻沒有說明主區與清單的結構不在問題範圍內。2 份答案把主區或清單也算成容器候選。

#### 未通過的條文問題清單

| 編號 | skill | 題號 | 條文位置 | 問題 |
|---|---|---|---|---|
| P1 | version-bootstrap | VB-01a、VB-02a | `references/version-shift-sop.md`〈契約檔案的界定〉判準 1、判準 2 問句 | 問句前段有括號列舉（使用者端檔案、以 key 對接的翻譯提供者），後段有「使日後改形狀必須遷移或相容讀取」「使改形狀無法在同一次提交內一併修改」。兩段的關係沒有寫明：命中列舉是否即算成立，還是要另外驗證後段。有探針把後段當成獨立條件，推出列舉對象「不需遷移」「本方可單次提交完成」，判成不成立。VB-01a 另一份答案也先判錯才更正 |
| P2 | version-bootstrap | VB-03a | `SKILL.md` Step 2〈UI 類判別〉呈現通道表 | 第一列「由本應用程式渲染的視窗、頁面…」與第二列「非圖形的人讀呈現：…匯出文件版面」有交界：應用程式產出、有固定版面的 PDF 兩列都讀得進去。「非圖形」這個標籤與 PDF 版面的圖形性在字面上衝突，表格也沒有寫兩列命中時何者優先。2 份判 UI 類，1 份判定句自相矛盾 |
| P3 | version-bootstrap | VB-04b | `SKILL.md` Step 2 前置檢查表 L3 列「對應載體」欄；`references/step-rationale.md`〈Step 2 的 UI 前置檢查〉「存在」判準句 | 載體欄的括號裡同時寫了逐元件契約欄位表與容器條目。「本步驟得先只到此」的「得」是允許語氣，不是 Step 2 檢查範圍的定義句。step-rationale 又要求「內容是否符合對應載體欄的定義」，讀者因此把整格都當成 Step 2 的要求，判不通過（2 份） |
| P4 | component-contract-design | CCD-08a | `references/step-2-path-a-spec-derivation.md` 程式碼圍欄步驟 2 | 「總是一起出現的單元合為一個元件；有時一起出現的單元是該元件的可選 slot」只講合併與否，沒有交代合併後兩個單元是什麼關係（各為一個內容角色，都不是 slot）。這層依據只在範例理由欄（「是一個空狀態元件的兩個內容角色」）。3 份答案都寫「合為一個元件，其中一方是另一方的可選 slot」。依 chained-examples〈「缺範例」與「缺引導」的分界〉，屬缺引導 |

#### 探針行為異常

| 類型 | 發生處 |
|---|---|
| 路徑錯 | version-bootstrap-s2 寫入 `-graph_project_docs_manager/.../probes/a/`；component-contract-design-s1 寫入 `-graph-project-docs_manager/.../probes/a/` |
| 讀範例（違反「跳過範例區塊」） | DSG s1（02a／02b 引決策表、03b 引驗證問句）、s2（02a 引決策表理由句）、s3（01a／01b／03b 引驗證段、02a 引【輸入】段、02b／03a 引決策表）；VB s1、s2 的 VB-02b 引 version-shift-sop 範例二驗證問句；CCD s2 10a 引範例 Given/When/Then、s2／s3 03a 的 X、Y 值取自範例（「格資料」「載入中狀態完成時」）、s1／s3 11a 判定句用範例元件名、s2／s3 07a 用範例的「清單卡」 |
| 讀出餵入範圍 | VB s2 VB-04a 引 step-rationale〈Step 2 為什麼多一項 design-system spec〉；VB s3 VB-08a 引 task-splitting〈拆分策略速查〉第 6 列 |
| 虛構引文（出處檔 grep 無命中） | DSG s3 04a `Text(context.l10n!.versionLabel(version))`；VB s1 05a／05b 順序表「i18n + design-system」依賴欄；VB s3 07b「每個 ticket 應聚焦單一功能面」；CCD s1 08a「判為一個元件或 slot 的核心是「出現列集合的比較」」；CCD s2 12b 把題目輸入改寫成範例引文 |
| 判定句與依據矛盾 | VB s1 03a、05a；VB s2 05b；VB s3 04b；CCD s2 11a（以上皆未更正）；VB s3 01a、VB s1 04b、CCD s3 11a（明寫更正） |
| 出處章節標錯 | CCD s1 10a／10b 把 step-3〈關卡二〉標成方法論 |
| 讀答案鍵 | 無跡象 |

### 第 1 批判定與覆蓋率複查後的 PM 處置（2026-09-15）

**用戶裁示**
- W3-437.2：案例檔不算閘門範例。tdd `references/cases/`、version-bootstrap 與 doc 的動機案例都屬證據敘事，不列入閘門範圍。
- W3-436：畫面上不能單獨顯示錯誤碼。

**PM 裁決**
- **計分口徑**：採判定者的規則。答案明寫「更正」的，以更正後的判定計分；判定句與依據矛盾又沒有更正的，判不通過。
- **第 1 批條文問題 P1～P4 的修補分工**
  - version-bootstrap：P1、P2、P3
  - component-contract-design：P4
- **W3-437 漏列阻擋 16 項**：每個 skill 建一張修補票（component-contract-design、tdd、skill-design-guide），走兩階段。component-contract-design 的 F-16、F-18 與條文牴觸，由 W3-437.1 評估，修補票不直接改這兩項。
- **重測**：修補完成後，第 1 批未通過的 12 題重新出題重測，門檻仍是 3/3。56 則漏列範例併入第 2～5 批出題。
- **並行上限**：文件落檔代理人同時最多 5 個（W3-433 候選 1）。
### 修補完成，進入重測與第 2 批出題（PM，2026-09-15）

**修補已落檔**
- W3-438：dart-style-guardian 1.12.0
- W3-439：version-bootstrap 1.7.5（P1～P3）
- W3-440：component-contract-design 3.9.8（F-01／02／05／06／08、P4、F-18 選項 B、命中區片段移除）
- W3-441：tdd 2.10.1（F-25～F-37，含 F-28）
- W3-442：skill-design-guide 1.23.0
- W3-443：F-16／F-18 範例與射程子句

W3-437、W3-437.1、W3-437.3、W3-440.1、W3-441.1 均已完成。W3-444（F-05 過細圖層）為 P2 追蹤票，不阻擋閘門。

**下一步分兩路並行出題（出題者只寫題與答案鍵，不跑探針）**

1. 第 1 批重測
   - 對象：〈第 1 批判定〉未通過的 12 題，對應範例重新出題，每則 2 題（新輸入＋相反分支）。
   - 依據句一律取修補後的現行條文。
   - 檔名：`probes/q/W3-417-b1r-<skill>.md`、`probes/key/W3-417-b1r-<skill>.md`。
2. 第 2 批
   - 對象：ux-design-evaluation 全部範例，加上 W3-437 漏列清單中屬 ux-design-evaluation 的範例。
   - 抓範例改用 W3-437 的判準，不只找 `【輸入】` 標記。
   - 檔名：`probes/q/W3-417-b2-ux-design-evaluation*.md`，依範例數分檔，每檔 ≤ 30 題。
- 出題規則沿用〈第 1 批題組〉凍結規則：每題答案鍵附依據句原文，並以 `git grep -F` 驗證。
- 題組凍結後由 PM 核對，再派 haiku 探針，每單位 3 次。
### 第 1 批重測判定（獨立判定者，2026-09-15）

**判定方法**：條文一律讀 HEAD `4d5a045c`。version-bootstrap 在 `b383ca1b..HEAD` 之間沒有變更；component-contract-design 只多了 `60bab317`（W3-445，3.9.9），這個 commit 只改 path-a 步驟 2 的路由錨點用詞。每題先依題目的餵入範圍自行推出判定與依據句，再開答案鍵比對，最後逐份判 3 份答案（`probes/a/W3-417-b1r-<skill>-r{1,2,3}.md`）。門檻 3/3。計分口徑沿用〈第 1 批判定〉：
- 明寫更正的，以更正後的判定計分。
- 判定句與依據矛盾、又沒有更正的，判不通過。
- 題目有多個子問、漏答任一子問的，判不通過。
- 引文錯誤但判定正確的，照判定計分，另列探針異常。

**結果**：16／20。version-bootstrap 8／10，component-contract-design 8／10。

#### 逐題表：version-bootstrap（8/10）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| VB-01a | 屬契約檔案；「丟棄重建」不改變判準 1。依據：version-shift-sop〈契約檔案的界定〉判準 1 列舉「快取」；〈列舉與後段子句的關係〉「命中列舉任一項即判該問為「是」，不需要為當下情境重新驗證後段子句是否成立」 | 是 | 是 | 是 | 是 | — |
| VB-01b | 非契約檔案；檔案類型不影響判定。依據：「檔案類型不決定歸屬」「三問皆否：屬純程式邏輯」 | 是 | 是 | 是 | 是 | — |
| VB-02a | 判準 2 成立；產品負責人的說法不改變判定。依據：判準 2 列舉「第三方整合」＋列舉關係句 | 是 | 是 | 是 | 是 | — |
| VB-02b | 不成立。依據：「是否有消費方位於另一個獨立發布或部署的單位」「判定單位是發布或部署單位，不是儲存庫」 | 是 | 是 | 是 | 是 | — |
| VB-03a | 非 UI 類，不過三項前置；Checkpoint 記呈現通道並交 foundation-design。依據：SKILL Step 2 呈現通道表第二列「匯出文件版面（優先本列）」；step-rationale「改由外部程式（PDF 閱讀器、試算表軟體等）另開檔案呈現，一律歸第二列」 | 否（判定句寫「UI 類，須過三項前置」，依據段歸第二列，沒有更正） | 否（判定句寫 UI 類，重新檢視後結論是「無法給出確定判定」） | 否（判定句同時寫「UI 類，應過三項前置」與「非圖形人讀呈現，交 foundation-design」） | 否 | 題目設計 |
| VB-03b | UI 類，要過三項前置。依據：表第一列；「只有仍在本應用程式視窗內即時繪製、可操作或查看者才歸第一列」 | 是 | 是 | 是 | 是 | — |
| VB-04a | 不通過；補齊前不可繼續 UI 實作票規劃。依據：L3 列「僅元件清單 + 原生元件禁用對照表 + 豁免清單三項屬本步驟範圍」；「標題存在但內容為空殼（如僅一句「待補」）不算通過」；「缺則先補齊才可繼續本提案的 UI 實作票規劃」 | 是 | 是 | 是（第二子問寫在依據段） | 是 | — |
| VB-04b | 通過；容器條目「待補」不影響。依據：step-rationale「契約欄位表與容器條目不算本步驟範圍…缺這兩項不影響本步驟通過判定」 | 是 | 是（引文竄改，見異常） | 是 | 是 | — |
| VB-05a | 不可開工；blockedBy 含 i18n、design-system、UX 審查、元件契約。依據：SKILL Step 4.5「元件庫依賴前三塊與元件契約為 `blockedBy`」 | 否（只列元件契約＋UX 審查，i18n／design-system 以遞移為由省略） | 否（列前三塊，並明說元件契約「不可包含」） | 否（只列元件契約） | 否 | 條文不清 |
| VB-05b | 只列 UX 審查；不需另列 i18n／design-system。依據：step-rationale〈元件契約遞移依賴〉「元件契約的 `blockedBy` 只需列 UX 審查一項」 | 是 | 是 | 是 | 是 | — |

#### 逐題表：component-contract-design（8/10）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| CCD-01a | 待決；附錄檔路徑只列為請求核對理由，不據以判 C；回覆前不進 step 2。依據：step-1〈情況二〉「「無法核對」不等於「全否」，不得判 A」「列為請求核對的理由」「回覆前不進 step 2」 | 是 | 否（判定只答「待決」，C 與 step 2 兩子問沒有作答，只在引文中出現） | 否（「進 step 2」子問沒有作答，引文也截在該句之前） | 否 | 題目設計 |
| CCD-01b | 可暫判 A；標「暫判，C 訊號未核對」；產出不得直接轉入。依據：同段演練句 | 是 | 是 | 是 | 是 | — |
| CCD-04a | 第 3 格。依據：step-5〈格位起算〉「水平並排以書寫方向起始側為第 1 格」 | 是 | 是 | 是 | 是 | — |
| CCD-04b | 不合格；先補 slot 契約。依據：「容器契約尚未寫出 slot 者，先補該容器的 slot 契約，不在畫面票內自訂編號。」 | 是 | 是 | 是 | 是 | — |
| CCD-08a | 合為一個元件；兩者各為內容角色，不是對方的 slot。依據：path-a 步驟 2「兩集合完全相同為總是」「合為一個元件時，原單元各自是該元件的一個內容角色…彼此不是對方的 slot」 | 是 | 是 | 是 | 是 | — |
| CCD-08b | 排序切換是清單標題所屬元件的可選 slot。依據：「一方集合是另一方的真子集時，較少的一方是較多一方的可選 slot（有時）」 | 是 | 是 | 是（另寫「一個元件、兩個內容角色」，與「該元件的可選 slot」相容） | 是 | — |
| CCD-09a | 1 個候選；子件為名稱欄、說明欄、通知開關、儲存鈕。依據：步驟 4「每個佈局結構一個容器候選」 | 是 | 是 | 是 | 是 | — |
| CCD-09b | 2 個候選（外層含按鈕列容器；按鈕列含取消鈕、儲存鈕）。依據：同句；「容器的子件可以是容器」 | 是 | 是 | 是 | 是 | — |
| CCD-11a | 展開到頭像 slot 放頭像變體 small；圓點不寫；數量「依資料筆數」不逐筆列出。依據：step-5〈巢狀容器〉 | 是 | 是 | 否（判定句「停止於成員列」，依據段又說「需繼續展開」，沒有更正） | 否 | 讀者失誤 |
| CCD-11b | 不寫。依據：「直到被放入的元件不再有呼叫端須填的 slot 為止；元件自帶的子件不寫。」 | 是 | 是 | 是 | 是 | — |

#### 失敗歸因

| 題號 | 歸類 | 具體句 |
|---|---|---|
| VB-03a | 題目設計 | 題幹「這個提案應判為 UI 類嗎？」是是非問句，3 份的判定句都先回「是／UI 類」，依據段卻全部正確引到第二列。r2 明寫「題目問『是否應判為 UI 類……須過三項前置檢查』，暗示預期答案為肯定」。條文本身清楚：3 份都引到 step-rationale「一律歸第二列」與表第二列「（優先本列）」。修法：題幹改成選擇式問句（「判為 UI 類或非 UI 類？」），不改條文 |
| VB-05a | 條文不清 | SKILL Step 4.5 PM 工作句「元件庫依賴前三塊與元件契約為 `blockedBy`」全數列出；step-rationale〈元件契約遞移依賴〉對元件契約卻要求「不必重複列出；重複列出反而製造兩條可能不同步的依賴宣告」。這條理由的射程沒有限定只適用元件契約。r1 把同一原則套到元件庫，寫出「元件庫實作票的直接 `blockedBy` 為：元件契約票（3.5）+ UX 審查票（3），經此鏈涵蓋前三塊」。兩處對遞移依賴要不要重複列出的態度相反，沒有交代元件庫為何例外。另有兩個次因：r3 只列未完成的元件契約，是把 blockedBy 讀成「目前仍擋住的票」，這個讀法由題幹同時給出完成狀態誘發（題目設計）；r2 寫「元件契約 DOC 票仍在進行中，故…不可包含元件契約」，屬讀者失誤 |
| CCD-01a | 題目設計 | 一題三子問（起點判定欄、可否據以判 C、可否進 step 2），探針規則第 3 點卻要求「(1) 你的判定（一句）」。r2 判定句只有「起點判定欄應填「待決」。」，r3 判定句只有「起點判定欄應填「待決」，無法判定為 A 或 C。」。兩份的引文都含正解句，第 1 批 s2 也是同樣漏答，已連續兩輪同型失敗。條文不需修改。修法：多子問的題目改為判定欄逐子問編號作答，或拆成單一子問 |
| CCD-11a | 讀者失誤 | r3 寫「成員列無由呼叫端填入的 slot（頭像 slot 由成員列本身定義），頭像元件本身有由呼叫端填入的 slot（inline slot）」，與題幹「每個成員列有一個由呼叫端填入的頭像 slot」「線上狀態圓點屬元件自帶、不是 slot」正好相反，判定句也停在成員列。r1、r2 依〈巢狀容器〉「直到被放入的元件不再有呼叫端須填的 slot 為止」正確展開到頭像，條文沒有歧義 |

#### 答案鍵疑誤

20 題的判定與獨立推導全部一致，沒有判定錯誤。有 1 處引文與 HEAD 不符，1 處條文衝突附註：
- **CCD-08a 引文**：答案鍵引的片段「內容角色（見 `step-3-fill-contract.md`〈語意與內容角色〉四類判準）」，在 HEAD 已由 W3-445 改為「內容角色（見 `step-3-fill-contract.md`〈元件契約欄位填寫問句〉表「語意與內容角色」列的四類判準）」。`git grep -F '〈語意與內容角色〉四類判準' HEAD` 命中 0。判定不受影響，r1 與 r2 引的是 HEAD 用詞。
- **VB-05a**：答案鍵依 SKILL 明文「元件庫依賴前三塊與元件契約為 `blockedBy`」判定，有明文支持，不算答案鍵錯誤。它與 step-rationale 的去重原則衝突，已列在失敗歸因。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 虛構或竄改引文 | VB r1 04a「三項皆有實際內容」（出自 VB-04b 題幹，不是條文）；VB r2 04b「『原生元件禁用對照表』不算本步驟範圍」（原文是「契約欄位表與容器條目不算本步驟範圍」）；CCD r3 01a 把題幹問句標成「第 55 行」引文 |
| 出處章節標錯 | VB r3 02a 把「判定單位是發布或部署單位，不是儲存庫」標成〈列舉與後段子句的關係〉段；CCD r1 09b 把「一組」句標成範例驗證問句（實際在步驟 4 圍欄） |
| 誤判題目轉述不符 | VB r2 03a 把是非問句當成題目預設答案，宣稱「題目轉述與現文不符」 |
| 讀範例／讀出餵入範圍 | 未見跡象 |
### 第 1 批重測判定後的處置（PM，2026-09-15）

**用戶裁示**：讀者失誤一律記不通過，並改題重測，維持 3/3。理由：題目也是讀者會遇到的文字，被讀反仍算訊號。

**4 題未通過的處置**
- **VB-05a**（條文不清）：W3-447 已補去重原則的射程，version-bootstrap 升 1.7.6。依修補後條文改題重測。
- **VB-03a**（題目設計）：題幹改為選擇式問句，重測。
- **CCD-01a**（題目設計，連續兩輪同型）：原本三個子問拆成三題，每題只問一個判定，重測。
- **CCD-11a**（讀者失誤）：題幹前提改成條列，避免讀反，重測。

**題號規則**：重測題號加後綴 r2，例如 VB-03a-r2；檔名為 `W3-417-b1r2-<skill>.md`。

**不需處置**：CCD-08a 答案鍵中的舊錨點用詞不影響計分，隨本輪改題順手更新。
### 第 2 批判定（獨立判定者，2026-09-15）

**判定方法**：條文讀 HEAD `f1eaffd6`。`git diff b383ca1b HEAD -- .claude/skills/ux-design-evaluation/` 為空，題目基準與 HEAD 一致。每題先依餵入範圍自行推出判定與依據句，再開答案鍵比對，最後逐份判 3 份答案。之 1 讀 `probes/a/W3-417-b2-ux-design-evaluation-1-r{1,2,3}.md`；之 2 的 r2 在同目錄，r1、r3 被探針寫到另外兩個錯拼的 scratchpad 目錄，照 PM 指示直接讀。門檻 3/3。計分口徑沿用〈第 1 批重測判定〉，另依〈第 1 批重測判定後的處置〉裁示，讀者失誤也記不通過。本批補一條統一口徑：判定句另附與條文不符的替代判定（「或…」「若只能選一種則…」），又沒有更正的，判不通過。

**結果**：47／58。之 1 為 21／30，之 2 為 26／28。

#### 逐題表：之 1（21/30）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-01a | 1-10s 帶，進度指示 + spinner；不可只寫「要有回饋」；標明統計量 p90。依據：表 1-10s 列；SKILL「產出必須是可被引用的具體值」；「須顯式記錄選用的統計量」 | 否（400ms-1s） | 否（400ms-1s，漏答兩子問） | 否（寫「3 秒落在 400ms-1s」） | 否 | 讀者失誤 |
| UX-01b | 100-400ms 帶，動畫過渡；不用 spinner；仍須交值 | 是 | 是 | 是 | 是 | — |
| UX-02a | 依臨界路徑；Tab、`;` 放拇指自然停留處 | 是 | 是 | 是 | 是 | — |
| UX-02b | 有實測資料，依頻率；Esc 最外側。依據：「排列依該場景下的實際使用頻率排列」「使用頻率無法確定時，以…臨界路徑」 | 否（依臨界路徑，Esc 靠拇指） | 否（依據答臨界路徑） | 否（依據答臨界路徑） | 否 | 讀者失誤 |
| UX-03a | visiblePassword；三項全關；Submit model 在 protocol spec 階段 | 是 | 是 | 是 | 是 | — |
| UX-03b | 一般文字鍵盤；不關；不適用安全清單 | 是 | 是 | 是 | 是 | — |
| UX-04a | 一般文字鍵盤 | 是 | 是 | 是 | 是 | — |
| UX-04b | 數字鍵盤 | 是 | 是 | 是 | 是 | — |
| UX-05a | 格式驗證，on blur | 是 | 是 | 是 | 是 | — |
| UX-05b | 交叉驗證，on submit | 是 | 是 | 是 | 是 | — |
| UX-06a | 按送出（或 debounce，見答案鍵疑誤）；本機不足以選即時 | 是 | 是 | 是 | 是 | — |
| UX-06b | 即時；來源不改變判定 | 是 | 是 | 是 | 是 | — |
| UX-07a | 學習關；校正與建議保留開啟；iOS 顯式記錄風險 | 否（三項皆關，(3) 列出正解讀法但判定句未更正） | 否（三項皆關，引文卻是「自然語言保留開啟」） | 是 | 否 | 讀者失誤 |
| UX-07b | 三項皆關 | 是 | 是 | 是 | 是 | — |
| UX-08a | segmented control | 是 | 是 | 是 | 是 | — |
| UX-08b | 拆狀態顯示＋動作鈕 | 是 | 是 | 否（另附「或改用 switch 若設計成兩個獨立開關」） | 否 | 讀者失誤 |
| UX-09a | 合格，圓點屬明顯非按鈕形態。依據：「指示用明顯非按鈕形態（文字 chip、圓點）或移出動作列」 | 否 | 否（(3) 列兩讀法，判不合格） | 否 | 否 | 條文不清 |
| UX-09b | 不合格；描邊 vs 實心不足以區分 | 是 | 是 | 是 | 是 | — |
| UX-10a | 不可省略號；先縮格式，仍不足才設最小寬度並由鄰近元件讓步 | 是 | 是 | 是 | 是 | — |
| UX-10b | 不須設最小寬度；讓步規則路由至 component-contract-design〈空間不足策略〉 | 否（「不一定需要，取決於空間是否已足夠」，未就題幹前提作答） | 否（「仍須依最小寬度策略考慮」） | 是 | 否 | 讀者失誤 |
| UX-11a | 不足，須顯式記錄統計量；p95 → 進度指示 + spinner | 是 | 是 | 是（把「若選定」寫成「應選定」，見異常） | 是 | — |
| UX-11b | 不可，仍須記錄 | 是 | 是 | 是 | 是 | — |
| UX-12a | 權威定義在 `screen-state-matrix.md`；本節只負責每個狀態的 UI 呈現。依據：檔首不適用「畫面狀態的完整枚舉與退出路徑（狀態矩陣範疇，見 `screen-state-matrix.md`）」 | 是（章節名標錯，見異常） | 否（答 `interaction-feedback.md` 或 gate-fallback、error-recovery，與自己的引文矛盾） | 否（答 screen-state-matrix「或 `gate-fallback.md`」） | 否 | 讀者失誤 |
| UX-12b | 屬本檔；spinner（形狀 > 時長）；1-10s 進度指示 + spinner | 是 | 是 | 是 | 是 | — |
| UX-13a | 略大於 60 秒；逾時後恢復 idle + 錯誤訊息 | 是 | 是 | 是 | 是 | — |
| UX-13b | 不合格 | 是 | 是（理由方向寫反，見異常） | 是 | 是 | — |
| UX-14a | 4 狀態（視覺反推）；全空＝4 個死胡同；只寫「返回」缺目標，未定義 | 是 | 是 | 否（寫 4 個狀態，卻說「五個死胡同」） | 否 | 讀者失誤 |
| UX-14b | 從 enum 取；「返回 → 首頁」格式合格 | 否（判不合格，理由是 error 狀態應有重試） | 否（同左） | 是 | 否 | 題目設計 |
| UX-15a | 需要 initializing；網路不影響 | 是 | 是 | 是 | 是 | — |
| UX-15b | 不需要，一般 loading | 是 | 是 | 是 | 是 | — |

#### 逐題表：之 2（26/28）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-16a | 可逆，由「關閉勿擾」提供 | 是 | 是 | 是 | 是 | — |
| UX-16b | 不可逆；「關閉面板」仍滿足退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-17a | 獨立成列；兩畫面各一列；退出至少「返回」或「離開此畫面」；記錄設定入口與清除時機 | 是 | 是 | 是 | 是 | — |
| UX-17b | 不屬（條件 2 或 1 不成立） | 是 | 是 | 是 | 是 | — |
| UX-18a | 只給重試不足（須含原因提示）；重啟 app 不合適；須含退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-18b | 不合適；配資料層行動 | 是 | 是 | 是 | 是 | — |
| UX-19a | 固定間隔並設上限；正在重試、已重試幾次、下一次時間 | 是 | 是 | 是 | 是 | — |
| UX-19b | 指數退避 + jitter；倒數與立即重試按鈕 | 是 | 是 | 是 | 是 | — |
| UX-20a | 不可靜默隱藏；明確標示（或替代方案） | 是 | 是 | 是 | 是 | — |
| UX-20b | 可以 | 是 | 是 | 是 | 是 | — |
| UX-21a | 不合格（3 < 8） | 是 | 是 | 是 | 是 | — |
| UX-21b | 合格 | 是 | 是 | 是 | 是 | — |
| UX-22a | 三種皆不套用；tab 切換不屬任何一種。依據：「tab 之間的切換不屬其中任何一種，因每個 tab 維護獨立堆疊」 | 否（替換整個堆疊） | 否（先答不屬，再附「若只能選一種，替換整個堆疊最接近」） | 是 | 否 | 題目設計 |
| UX-22b | 推入堆疊頂端 | 是 | 是 | 是 | 是 | — |
| UX-23a | 替換堆疊頂端 | 是 | 是 | 是 | 是 | — |
| UX-23b | 替換整個堆疊 | 是 | 是 | 是 | 是 | — |
| UX-24a | 不必適配（視覺風格類） | 是（判定句先寫「是」，同題後段寫「重讀後…可不適配」，以更正計） | 是 | 是 | 是 | — |
| UX-24b | 必須適配（modal dismiss 手勢） | 是 | 是 | 是 | 是 | — |
| UX-25a | 重建到設定選單的堆疊 | 是 | 是 | 是 | 是 | — |
| UX-25b | 只放目標畫面 | 是 | 是 | 是 | 是 | — |
| UX-26a | 分兩列 | 是 | 是 | 是 | 是 | — |
| UX-26b | 合併一列；列名寫出涵蓋類型（iOS 拒絕一次、Android 拒絕兩次） | 是 | 是 | 否（範例列名「系統對話框不再顯示」「永久拒絕」沒寫出涵蓋類型，與自己引的條文不符） | 否 | 讀者失誤 |
| UX-27a | Degraded mode；標示可能非最新 | 是 | 是（出處檔標錯，見異常） | 是 | 是 | — |
| UX-27b | Retry with feedback；狀態 + 手動重試 + 退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-28a | 可以 | 是 | 是 | 是 | 是 | — |
| UX-28b | 使用語音輸入時請求 | 是 | 是 | 是 | 是 | — |
| UX-29a | 需要確認；文案點出覆蓋未同步筆記、無法復原 | 是 | 是 | 是 | 是 | — |
| UX-29b | 仍需確認；未渲染時不執行 | 是 | 是 | 是 | 是 | — |

#### 失敗歸因

| 題號 | 歸類 | 具體句 |
|---|---|---|
| UX-01a | 讀者失誤 | 條文表清楚列「1-10s｜明顯等待｜進度指示 + spinner」，3 份卻都交出 400ms-1s。這個值正是 SKILL.md〈三段鏈式範例：本 skill 的產出必須是可被引用的具體值〉（p90 600ms）的結論。r3 直接寫「3 秒落在『400ms-1s』」，並把範例【驗證】段標為出處。題幹情境（按鈕觸發、p90、交 component-contract-design）和範例幾乎同構，範例結論蓋過了新輸入 |
| UX-02b | 讀者失誤 | 條文先說「排列依該場景下的實際使用頻率排列」，只在「使用頻率無法確定時」才以臨界路徑排序，先後有明文。3 份都把依據答成臨界路徑，而且都引了〈三段鏈式範例：無使用頻率資料時的排列依據〉結論「排列依臨界路徑判定」。該範例的鍵組（終端機 Esc／Tab／Ctrl／方向鍵）和題幹相同。修法：重測時換用與範例不同的鍵組 |
| UX-07a | 讀者失誤 | 條文「自然語言時兩者保留開啟」清楚。r1 把題幹「不希望內容被輸入法詞庫記住」擴大到校正與建議；r2 引了正解句，判定仍寫三項皆關。r3 正確 |
| UX-08b | 讀者失誤 | 條文「三值以上且切換會連動畫面其他區域增減元素時，拆成狀態顯示＋動作鈕」只有一個做法。r3 另附「或改用 switch 若設計成兩個獨立開關」，等於改寫題幹前提。r1、r2 正確 |
| UX-09a | 條文不清 | 第 2 項開頭「非互動的狀態指示不與動作按鈕同形混排 — 同列有按鈕時使用者預設整列可點，點到指示的「沒反應」被讀成壞掉」，理由句不分形態。合格形態「指示用明顯非按鈕形態（文字 chip、圓點）或移出動作列」接在「描邊 vs 實心不是可靠的互動性訊號」之後，讀起來像同形情境的補救，而不是「chip／圓點與按鈕同列即合格」。3 份都判不合格；r2 還讀了範例，在 (3) 列出兩種讀法後仍判不合格。明說「與同列有無按鈕無關」的句子只在〈三段鏈式範例：第 2 項〉【驗證】Q2 預期，屬答案鍵未標記的依據只在範例內 |
| UX-10b | 讀者失誤 | 條文「仍不足才設定最小寬度」清楚，題幹也已給定「已能在窄螢幕完整顯示」。r2 寫「仍須依最小寬度策略考慮」；r1 寫「不一定需要（取決於空間是否已足夠）」，沒有依題幹前提作答。r3 正確 |
| UX-12a | 讀者失誤 | 檔首不適用段明寫「畫面狀態的完整枚舉與退出路徑（狀態矩陣範疇，見 `screen-state-matrix.md`）」。r2 引了這句，判定卻寫「`interaction-feedback.md` 或其他相關 reference（如 gate-fallback、error-recovery）」；r3 另加「或 `gate-fallback.md`」。兩份都被題幹的「failed 狀態」帶到錯誤恢復或 gate 檔。沒有人把退出路徑寫進本節回饋表 |
| UX-14a | 讀者失誤 | r3 判定句同時寫「應列四個狀態」與「五個死胡同」，後者照抄〈填寫範例〉步驟 3「全空，五個死胡同」。r1、r2 正確 |
| UX-14b | 題目設計 | 題幹「failed 狀態的退出路徑欄寫「返回 → 首頁」是否合格？」沒有指明合格的標準是格式還是完整性。餵入的〈填寫步驟〉第 4 步又有「error 狀態通常應有「重試」和「返回」」。r1 在 (3) 明列兩種讀法，r1、r2 依完整性判不合格。但重試屬可用操作欄，不是退出路徑，所以讀者本身也有誤讀。修法：題幹改為「這條退出路徑的格式是否合格」 |
| UX-22a | 題目設計 | 題幹「應套用「替換整個堆疊」「推入堆疊頂端」「替換堆疊頂端」中的哪一種？」預設三者之一必然適用，條文卻是「tab 之間的切換不屬其中任何一種」。r1 被迫三選一；r2 先答不屬，又補「若限制只能選一種，「替換整個堆疊」最接近」。和第 1 批 VB-03a 同型。修法：選項加「以上皆非」，或改問「是否屬於三種堆疊操作之一」 |
| UX-26b | 讀者失誤 | 條文「列名寫出涵蓋的類型」清楚。r3 照引條文，給的列名範例卻是「系統對話框不再顯示」「永久拒絕」，沒有寫出 iOS／Android 兩個類型。r1、r2 正確 |

#### 三項條文風險的探針證據

| 風險 | 是否證實 | 證據 |
|---|---|---|
| UX-20a（「從未用過」的判定單位，答案鍵標為條文歧義） | 未證實 | 3 份都判「不可靜默隱藏、明確標示」，沒有人答成「對未用過的使用者隱藏」。r1、r3 沒有引範例，只憑條文「只適合使用者從未用過的功能 — 用過的功能突然消失會被當成 bug」就答出功能層級；r2 引了範例 Q1 預期。本批沒有讀出歧義的樣本。3/3 通過不足以證明歧義不存在，只能說本題幹（0.5% 用過）沒有誘發使用者層級讀法 |
| UX-06（「本地過濾」被讀成本地即低成本） | 未證實，證據受污染 | 3 份都判按送出或 debounce，沒有人選即時。但 r1、r2、r3 都引了〈三段鏈式範例：判斷搜尋成本的依據〉的「『本地』不等於『低成本』」。這個澄清只在範例裡，所以本批不能證明單讀條文不會誤判。重測前須先解決範例閱讀問題（見探針異常第 1 列） |
| UX-12a（〈畫面級回饋〉表「退出路徑｜每個中間狀態都必須有」與檔首歸屬衝突） | 預測的誤讀型態未證實；本題另以他型失敗 | 沒有人判「寫在本節回饋表」。r1、r2、r3 都引了〈三段鏈式範例：畫面級回饋只負責 UI 呈現欄〉的「權威值只在 `screen-state-matrix.md` 定義一處」，所以同樣受範例污染。實際失敗是 r2、r3 把「別的地方」指到 gate-fallback／error-recovery（見 UX-12a 歸因）。檔首「見 `screen-state-matrix.md`」有明文，不另列條文問題 |

#### 答案鍵疑誤

58 題的判定與獨立推導一致，沒有判定錯誤。有 2 處附註：
- **UX-06a 接受「debounce」**：餵入條文的觸發模式表中，debounce 的適用欄是「遠端搜尋的常見選擇」，本機高成本搜尋依表只對到「按送出」。「採按送出或 debounce」的結論出自範例【產出】。本批依答案鍵計分，r2 答 debounce 記通過，但這個計分點的依據部分只在範例內。
- **UX-09a**：答案鍵的判定正確，但沒有標記「圓點與按鈕同列仍合格」這個讀法的明文只在範例（見失敗歸因）。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 違反「跳過範例區塊」規則，大量引範例 | 之 1 的 r1、r2、r3 幾乎每題都引〈三段鏈式範例〉【產出】／【驗證】（例：01a r3、02b 三份、03a r1／r2、04a 三份、06a 三份、12a 三份、15a r1／r2）；之 2 的 r2 引範例（19a、20a、21b、22a、26b、29b）；之 2 的 r3 29a 引文「操作名稱給人的直覺印象…落差大」出自範例表。範例與條文在同一檔、同一節內，探針規則只靠文字要求跳過，實際無效。影響：之 1 的通過題不能證明單讀條文可答出；01a、02b、14a 的失敗則直接由範例錨定造成。建議：後續批次改餵已實體刪除範例區塊的節錄檔 |
| 答卷寫錯目錄 | 之 2 的 r1、r3 寫到兩個錯拼的 scratchpad 路徑（`graph_project_docs_manager`、`graph-project-docs_manager`） |
| 虛構或竄改引文 | 之 1 r1 05b「『退房日期須晚於入住日期』代表格式不固定」（條文沒有這句）；之 1 r1 07b 引文中出現自我更正的「否（內容為自然語言）——錯」；之 1 r1 02b 把題幹句當依據句 |
| 出處章節或檔名標錯 | 之 1 r1 12a 標成「`screen-state-matrix.md`〈畫面級回饋〉」；之 2 r2 27a／27b 把〈網路 gate〉標在 `error-recovery.md`（實際在 `gate-fallback.md`） |
| 判定理由方向寫反 | 之 1 r2 13b 寫「20 秒…會被後端的明確錯誤回應蓋過」，實際是 20 秒兜底會先蓋過後端錯誤 |
| 誤判題目轉述不符 | 之 2 r1 24a 把自己的誤判標成「題目與現文不符」 |
| 條件式問句改寫為建議 | 之 1 r3 11a 把「若規格選定 p95」寫成「應選定 p95」 |
### 第 2 批判定後的處置（PM，2026-09-15）

**探針餵入方法變更（影響閘門效度）**
- 第 2 批判定者發現：之 1 的 3 份答案幾乎每題都引用範例，之 2 的 r2、r3 也有。
- 原因：範例和條文在同一節內，「跳過範例」的文字指示沒有生效。
- 後果：這些題的通過不能證明條文本身讀得懂；UX-01a、02b、14a 更是被範例結論直接帶錯。
- 裁定：第 2 批起，餵入改為「刪除範例區塊的節錄檔」。節錄檔由出題者用腳本產生，放在 `probes/excerpt/`；題目的餵入欄只指向節錄檔。
- 第 1 批與第 1 批重測：兩位判定者都回報沒有讀範例的跡象，結果維持。

**計分口徑增補**：判定句附帶與條文不符的替代答案（「或…」），又沒有更正的，記不通過。採第 2 批判定者的口徑。

**第 2 批結果：47/58 題，整批重測**
- 範例污染使本批通過題不具證明力，改用節錄檔全部 58 題重跑。
- 11 題未通過一律改題：
  - 題目設計：UX-14b 改問「格式是否合格」；UX-22a 補「都不屬於」選項。
  - 讀者失誤：UX-01a、02b、07a、08b、10b、12a、14a、26b，依用戶裁示改題，前提改成條列。
- 答案鍵修正：UX-06a 的 debounce 只有範例支持，答案鍵改依條文表格。若條文不足以支持，併入 W3-446。

**W3-446 範圍增補**
- UX-09a：「圓點與按鈕同列仍合格」只寫在範例中，理由句沒有區分形態。
- UX-06a：本機高成本搜尋是否可用 debounce，條文表格只對應到「按送出」。
- UX-06、UX-12a、UX-20a 三項風險：受範例污染無法判定，W3-446 仍須逐項檢視條文。

**順序**：W3-446 落檔 → 出題者產節錄檔並改題 → 58 題以 3 份探針重跑 → 判定。
### 第 1 批改題重測判定（獨立判定者，2026-09-15）

**判定方法**：條文讀 HEAD `012abaf0`。題目基準 `f1eaffd6` 到 HEAD 之間，version-bootstrap 沒有變更；component-contract-design 只多了 `e2215fa8`（W3-448，3.9.10），在 step-1〈情況二〉「三訊號無法核對時」段補一句「提及本身不在上列三訊號之內，不據以判 C」。3 份 CCD 答案都引了這句，可見探針讀的是 HEAD。判定依 HEAD 條文。每題先依餵入範圍自行推出判定與依據句，再開答案鍵比對，最後逐份判 3 份答案（`probes/a/W3-417-b1r2-<skill>-r{1,2,3}.md`）。門檻 3/3。計分口徑沿用〈第 1 批重測判定〉，並依〈第 2 批判定後的處置〉增補兩條：讀者失誤記不通過；附帶與條文不符的替代答案、又沒有更正的，記不通過。答案鍵 27 則引文已用 `git grep -F` 在 HEAD 逐一核對，全部命中；其中「起點判定標「待決」」計數為 2，與答案鍵的註記相符。

**結果**：6／6。version-bootstrap 2／2，component-contract-design 4／4。

#### 逐題表：version-bootstrap（2/2）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| VB-03a-r2 | (1) 非 UI 類；(2) 不過三項前置；(3) 記錄呈現通道，交 foundation-design UI 維度判改寫產物，不得只記「非 UI 類」。依據：呈現通道表第二列「匯出文件版面（優先本列）」「非 UI 類，不過三項前置」與其動作欄；step-rationale「改由外部程式（PDF 閱讀器、試算表軟體等）另開檔案呈現，一律歸第二列，即使該檔案版面固定或排版複雜」 | 是 | 是 | 是 | 是 | — |
| VB-05a-r2 | 列四張：i18n、design-system、UX 審查、元件契約；PM 理由不成立。依據：PM 工作「元件庫依賴前三塊與元件契約為 `blockedBy`」；〈元件契約遞移依賴〉「去重原則的射程」段「此原則不適用於 Step 4.5 元件庫的 `blockedBy`」「故元件庫的 `blockedBy` 仍需並列前三塊與元件契約四項」 | 是 | 是 | 是 | 是 | — |

#### 逐題表：component-contract-design（4/4）

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| CCD-01a-1-r2 | 待決。依據：「「無法核對」不等於「全否」，不得判 A」「起點判定標「待決」」 | 是 | 是 | 是 | 是 | — |
| CCD-01a-2-r2 | 不可；附錄路徑只能列為請求核對的理由。依據：HEAD「列為請求核對的理由；提及本身不在上列三訊號之內，不據以判 C」 | 是 | 是 | 是 | 是 | — |
| CCD-01a-3-r2 | 不可；於 NeedsContext 請求核對，回覆前不進 step 2。產出會寫進 spec 檔，不適用演練暫判 A 的例外。依據：「於 NeedsContext 請求有讀取權者核對三訊號，回覆前不進 step 2」 | 是 | 是 | 是 | 是 | — |
| CCD-11a-r2 | (1) 展開到「成員列的頭像 slot 放頭像變體 small」為止；(2) 圓點不寫；(3) 寫「逐格放成員列變體 default，數量依資料筆數」。側欄寫 slot 名。依據：〈格位起算〉「寫 slot 名，不另行編號」；〈巢狀容器〉三句 | 是 | 是（數量寫「依團隊成員人數」，屬答案鍵允許的同義形式；引文混入範例句，見範例污染） | 是（依據段引範例，見範例污染） | 是 | — |

#### 失敗歸因

無未通過題。前一輪 4 題的歸因與這一輪結果對照：
- VB-03a（題目設計，是非問句）：改成選擇式並編號後，3/3 通過。證實失敗來自問句形式，不是條文。
- VB-05a（條文不清）：W3-447 補上射程段後 3/3 通過，而且 3 份都引到射程段。題幹放入的「比照元件契約去重」陷阱沒有誘發誤判。
- CCD-01a（題目設計，一題三子問）：拆成三題後 3/3 通過。
- CCD-11a（讀者失誤）：前提改成條列後 3/3 通過。

#### 答案鍵疑誤

判定與獨立推導一致，沒有判定錯誤。有 1 處附註：
- **CCD-01a-2-r2 的判定者註已過期**：答案鍵寫在 W3-448 補句之前，所以註明「直接否定句只出現在範例【驗證】、需合讀推出」。HEAD 已有明文「提及本身不在上列三訊號之內，不據以判 C」，這條註記不再成立；判定本身不受影響。3 份都直接引了新句，沒有人回報需要自行假設。

#### 範例污染

| 答案 | 位置 | 引用的範例內容 | 是否影響通過 |
|---|---|---|---|
| CCD r3 | CCD-11a-r2 子問 2、3 | 在依據段明標「範例驗證」，引〈範例（巢狀容器）〉的兩條檢驗問句：「清單卡若內建固定圖示（非 slot），how 是否寫該圖示？」「資料驅動的格數（依資料筆數）是否以固定寫法標明…」 | 否 |
| CCD r2 | CCD-11a-r2 子問 1 | 引文「逐層展開到葉元件…直到被放入的元件不再有呼叫端須填的 slot 為止」標為〈巢狀容器〉段；前半「逐層展開到葉元件」出自同節範例判定表合格列（step-5 範例表），不在條文段 | 否 |

判定不受影響，理由有三：
1. 兩份的判定句都沒有出現範例的元件名（清單卡、徽章 slot、狀態徽章），結論也不是範例的結論，而是依題幹前提停在頭像元件。
2. 兩份都另外引了足以單獨支持判定的條文句（「元件自帶的子件不寫」「同質子件數量隨資料而變者…」「直到被放入的元件不再有呼叫端須填的 slot 為止」）。
3. r1 沒有讀範例，也答出同樣的三個子問。

限制：CCD-11a-r2 的 3 份答案中，只有 r1 是乾淨樣本。這個範例的結構（三層巢狀 slot、數量依資料筆數、自帶子件）與題幹同構，和第 2 批 UX-01a 的污染型態相同。本題條文可讀，是由 r1 單份證明的，不是 3/3 證明的。是否要改用節錄檔重跑這一題，由 PM 裁決。其餘 5 題 3 份答案都沒有範例引文；VB 兩題的餵入章節本身沒有範例區塊。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 讀出餵入範圍 | VB-03a-r2 的 r1、r2、r3 都引了 SKILL.md Step 2 的 Checkpoint 段（不在餵入的「UI 類判別段與呈現通道表」內）；r2、r3 另引「非 UI 類提案略過本檢查。」（在前置檢查表之後的段落）。答案由表第二列即可得出，不影響判定 |
| 虛構或竄改引文 | VB r3 03a 子問 3 依據 2：「判為 UI 類提案者須先確認…缺則先補齊；[此為正向路徑]…非 UI 類判別依…此原則不適用…」。「[此為正向路徑]」條文沒有；「此原則不適用」出自〈元件契約遞移依賴〉，不在〈Step 2 的 UI 前置檢查〉。同題依據 1 在表列中插入條文沒有的「檢查項」 |
| 出處標錯 | VB r2 03a 子問 2 把「非 UI 類提案略過本檢查。」標成「表格直後（第 101-102 行）」，實際在第 112 行；CCD r2 11a 見範例污染第 2 列 |
| 引文加粗 | CCD r3 01a-3、11a 在引文中自行加粗，原文沒有 |
### 第 1 批改題重測判定後的處置（PM，2026-09-15）

**b1r2 結果：6/6 通過**
- VB-03a、VB-05a、CCD-01a 三題全數通過，確認上一輪的失敗歸因正確。

**CCD-11a-r2 改用節錄檔重跑**
- 3 份答案中，r2、r3 引用了巢狀容器範例，只剩 r1 是乾淨樣本。
- 依〈第 2 批判定後的處置〉的範例污染口徑，本題通過不具證明力，改用刪除範例的節錄檔重跑 3 份，併入第 2 批重跑一起執行。

**第 1 批原始 46 題通過補做污染稽核**
- 原因：第 1 批原始判定當時沒有逐份檢查是否引用範例。
- 做法：派稽核者逐份比對探針引文是否出自範例區塊。受污染的題依同一口徑，改用節錄檔重跑。
### 第 1 批範例污染稽核（2026-09-15）

**對象**：〈第 1 批判定〉判為通過的 46 題（DSG 8、SDG 10、VB 9、CCD 19）。答案檔 12 份，`version-bootstrap-s2` 與 `component-contract-design-s1` 取自拼錯路徑的兩個目錄（同〈第 1 批判定〉記載）。

**範例區塊界定**：條文一律以 `git show 09bdb258:<path>` 核對。範例區塊指【輸入】【產出】【驗證】三段、「範例：」「**範例（…）**」起頭的段落，以及其中的表格與圍欄。以下三處不算範例：dart-style-guardian i18n〈Exceptions〉的程式碼圍欄（不在 【】 三段內，也不以「範例：」起頭）、component-contract-design path-a 步驟 1～5 的圍欄（是條文本體，題目有餵入）、version-bootstrap SKILL Step 5 條文句內的「例：」列舉。

**分類口徑**：先逐份判斷是否算乾淨樣本。這份答案沒有引用範例，或雖引用範例、但它自己引的條文句足以支撐判定，都算乾淨樣本。
- 乾淨：3 份都沒引用範例。
- 受污染但不影響：有引用範例，但 3 份都各自引了足以支撐判定的條文句。
- 受污染須重跑：判定依賴範例內容，或乾淨樣本少於 3 份。

#### 逐題分類

| 題號 | 分類 | 污染位置（份別：出處） | 支撐判定的條文句 |
|---|---|---|---|
| DSG-01a | 不影響 | s3：color-system〈顏色歸類判準〉【驗證】Q「『已刪除』提示…」 | 3 份皆引判準段首句「功能類型不因觸發它的前置操作是否具破壞性而改變」或 Three-Color 列項 |
| DSG-01b | 不影響 | s3：同上【驗證】Q | 同上；s1、s2 另引 Three-Color「errors」 |
| DSG-02a | 不影響 | s1、s2、s3：〈Theme 與 Token 判準〉【產出】決策表列與理由句；s3 另把【輸入】的「Theme 由 token 組裝而成」併入引文 | 3 份皆引判準段「對應的內建語意插槽存在時用主題插槽，不存在時用 token」 |
| DSG-02b | 不影響 | s1、s2、s3：同上決策表「未覆蓋」列與理由句 | 同上 |
| DSG-03a | 不影響 | s1、s3：〈字級判準〉【產出】決策表 | 3 份皆引 UIFontSizes 定義註解或判準段「卡片或對話框等容器元件內的標題對映 `titleLarge`」 |
| DSG-03b | 不影響 | s1、s3：〈字級判準〉【驗證】Q「兩個 token 是否因數值相同…」；s2：同 Q 的預期答案句「定義註解已區分文件階層與元件情境兩種語意」 | 3 份皆引判準段「數值相同不代表語意相同」與 headline4 對映句 |
| DSG-04a | 乾淨 | — （s3 的 `versionLabel` 引文是虛構的，不是範例內容，已列入〈第 1 批判定〉探針異常） | Exceptions 標籤＋數值分割句 |
| DSG-04b | 乾淨 | — | Exceptions「Technical identifiers - Error codes, keys」 |
| SDG-01a～05b（10 題） | 乾淨 | — （SDG-01b s1 轉引的「> 上例」引註段有餵入，不在範例三段內） | 各題答案鍵依據句 |
| VB-01b | 乾淨 | — | version-shift-sop「三問皆否：屬純程式邏輯…」 |
| VB-02b | **須重跑** | s1：兩則依據句都出自 version-shift-sop 範例二【驗證】（「判準 2 的成立條件是…」「若服務甲與服務乙改為同一次提交…」），沒有引判準表條文句；s2：另以同一【驗證】段作「逆向驗證」 | s2、s3 引了判準 2 問句「判定單位是發布或部署單位，不是儲存庫」；s1 沒有引，乾淨樣本 2 份 |
| VB-03b | 乾淨 | — | Step 2 呈現通道表第一列「含唯讀畫面」 |
| VB-06a | 乾淨 | — | Step 5「兩軸交集去重、聯集為完整覆蓋」 |
| VB-06b | 乾淨 | — | 同句與「避免 domain 規則只靠…被動覆蓋」 |
| VB-07a | 乾淨 | — | Step 6「或」句、task-splitting B、cognitive-load「> 2 須拆分」 |
| VB-07b | 乾淨 | — （s3「每個 ticket 應聚焦單一功能面」是虛構引文，不是範例內容） | task-splitting B；s1 另引 Step 6 |
| VB-08a | 乾淨 | — （s3 讀出餵入範圍的內容取自 task-splitting〈拆分策略速查〉，不是範例） | Step 6 PM 工作「共用檔案需整合票…」／task-splitting B |
| VB-08b | 乾淨 | — | task-splitting B「共用檔案的問題已合併到同一 ticket」 |
| CCD-01b | 乾淨 | — | step-1〈情況二〉「任一命中即為 C」 |
| CCD-02a | 乾淨 | — | path-a〈反向規則〉 |
| CCD-02b | 乾淨 | — | 同上例外句 |
| CCD-03a | 不影響 | s2：判定句的 Y 值「『載入中』狀態完成時」取自 step-3〈關卡五〉【產出】；s3：X、Y 值「格資料」「『載入中』狀態完成時」取自同段 | 3 份皆引固定寫法「不經服務：讀取的資料為 X，於 Y 時已載入；資料缺值時的表現見 Z」與「不得寫『同成功』」。判定只問形式，填入的值不影響判定 |
| CCD-03b | 乾淨 | — | 「所讀資料需按需讀取、可能等待者不適用本寫法，四段逐一填表現」 |
| CCD-04a | 乾淨 | — | step-5〈格位起算〉 |
| CCD-05a | 乾淨 | — | step-5〈覆蓋層〉 |
| CCD-05b | 乾淨 | — | 同上「屬觸發元件自帶者…不列」 |
| CCD-06a | 乾淨 | — | step-5〈疊加態〉 |
| CCD-06b | 乾淨 | — | 同上末句 |
| CCD-07a | 不影響 | s2：判定句的元件名「清單卡」取自〈非狀態進入情境〉範例【產出】 | 3 份皆引「不得為此新增獨立列」與「需要程式定位或聚焦者寫進該元件回饋契約『被程式定位』格」 |
| CCD-07b | 乾淨 | — | 「how 的列固定為狀態矩陣的列」（s3 以同段拆解句推出） |
| CCD-08b | 不影響 | s2：出處章節標成範例標題〈元件候選判定：總是／有時／僅單畫面共現〉，顯示讀到範例起頭行；引文本身是步驟 2 圍欄內的條文句 | 3 份皆引「兩集合互有對方沒有的列時…各自為候選」 |
| CCD-09b | 乾淨 | — | 步驟 4「單一子件的包裝…不是容器候選」 |
| CCD-10a | 不影響 | s2：引 step-3〈關卡二〉範例的 Given/When/Then 與 And 句 | 3 份皆引「進行中指示…疊加在事件結果所在區塊上、原內容全程可見時，指示消失後內容與事件前相同即答否」 |
| CCD-10b | 乾淨 | — （s2 標的章節名是條文粗體句，不是範例） | 「進行中狀態取代了該區塊的原內容…即答是」 |
| CCD-11b | 乾淨 | — | step-5〈巢狀容器〉「直到被放入的元件不再有呼叫端須填的 slot 為止」 |
| CCD-12a | 乾淨 | — | step-5〈遷移票〉段首 |
| CCD-12b | 不影響 | s2：出處標為〈遷移票〉範例，並寫成【輸入】「`features/login/`…」／【產出】「一票」。09bdb258 的範例輸入是 `features/project/`、`features/settings/`，沒有 login，這段是把題目輸入改寫成範例引文（虛構），但出處自稱範例 | 3 份皆引「『目錄』指直接含待改頁面檔的目錄」 |

#### 彙總

| 分類 | 題數 | 題號 |
|---|---|---|
| 乾淨 | 34 | DSG-04a、04b；SDG 全 10 題；VB-01b、03b、06a、06b、07a、07b、08a、08b；CCD-01b、02a、02b、03b、04a、05a、05b、06a、06b、07b、09b、10b、11b、12a |
| 受污染但不影響 | 11 | DSG-01a、01b、02a、02b、03a、03b；CCD-03a、07a、08b、10a、12b |
| 受污染須重跑 | 1 | VB-02b |

**須重跑清單**：VB-02b（version-bootstrap `references/version-shift-sop.md`〈契約檔案的界定〉判準 2，相反分支）。重跑時沿用原題與原餵入，並依〈第 1 批判定〉的 3/3 門檻重新判定。

**觀察**：受污染的 12 題全部集中在「範例緊接在判準段之後、同一標題下」的檔案（color-system、typography-system、step-3、step-5、version-shift-sop）。DSG 3 份答案都引了決策表或驗證問句。「跳過範例區塊」的指示在這種排版下擋不住探針。
### 第 1 批污染稽核後的處置（PM，2026-09-15）

**重跑範圍：12 題，改用節錄檔**
- 「須重跑」1 題：VB-02b。
- 「受污染但不影響」11 題：DSG-01a、01b、02a、02b、03a、03b；CCD-03a、07a、08b、10a、12b。

**為何「不影響」類也重跑**
- 稽核者判「不影響」的依據是「條文句足以單獨支撐」。
- PM 對 CCD-11a-r2 已定口徑：乾淨樣本少於 3 份即重跑。這 11 題的乾淨樣本都少於 3，其中 DSG 6 題為 0。
- 條文句足以支撐，是判定者的推論；乾淨樣本是讀者行為的實測。閘門採後者，與「探針為閘門」的用戶裁示一致。

**其餘 34 題**：乾淨，結果維持。

**稽核者的觀察**
- 受污染題全部出在「範例緊接判準段、同一標題下」的檔案：color-system、typography-system、step-3、step-5、version-shift-sop。
- 這個排版特徵已由節錄檔餵入解決，不另建票。
- 已記錄為 chained-examples.md 實證輪結束後的修訂候選：探針方法應規定以節錄檔餵入。

**執行**：併入節錄檔出題者的工作，輸出 `W3-417-b1x-<skill>.md`。
### 節錄檔重跑派發（PM，2026-09-15）

**題組**
- 節錄檔 42 份。出題者自檢兩項均通過：156 句依據全部逐字存在於節錄檔；被刪除的 857 行沒有任何一行出現在節錄檔中。
- 條文基準為 HEAD d8f36ee5。
- 題量：b2x ux 58 題、b2x CCD-11a 1 題、b1x 12 題。

**探針**
- b2x ux 兩檔，各跑 3 份。
- 其餘四個小檔合併：每份探針依序處理四檔，共 3 份。

**範圍外待處理：DSG-04b**
- 第 1 批判為乾淨通過，但作答依據的是 W3-438 之前的條文。
- W3-438 在 i18n-guidelines 新增〈錯誤碼與使用者可見錯誤訊息的交界〉，與題目「錯誤碼要不要進 i18n」相鄰。
- 閘門須以現行條文為準，DSG-04b 併入第 3 批出題時以節錄檔重跑，題幹不變。
### b1x 與 CCD-11a 判定（獨立判定者，2026-09-15）

**判定方法**：只讀 `probes/excerpt/` 節錄檔（基準 d8f36ee5），不讀原始 skill 檔。每題先依題目餵入的節錄檔自行推出判定與依據句，再開答案鍵比對，最後逐份判 3 份答案（`probes/a/W3-417-b1x-<skill>-r{1,2,3}.md`、`probes/a/W3-417-b2x-ccd-11a-r{1,2,3}.md`）。門檻 3/3。計分口徑沿用〈第 1 批改題重測判定〉，並依〈第 2 批判定後的處置〉：
- 讀者失誤記不通過。
- 附帶與條文不符的替代答案、又沒有更正的，記不通過。
- 越界（答案含答案鍵的污染檢查字串，或引用節錄檔以外的內容）記不通過。

答案鍵 29 則 ⟪⟫ 引文已用腳本比對全部節錄檔（忽略空白與換行），全部命中。

**結果**：13／13。DSG 6／6，CCD 5／5，VB 1／1，CCD-11a-r2 1／1。越界 0 次。

#### 逐題表

| 題號 | 正確判定與依據 | r1 | r2 | r3 | 通過 |
|---|---|---|---|---|---|
| DSG-01a | 按鈕 negative；確認訊息 positive。依據：〈Three-Color System〉「warnings, errors, destructive actions」「success states, confirmations」；〈顏色歸類判準〉「功能類型不因觸發它的前置操作是否具破壞性而改變」 | 是 | 是 | 是 | 是 |
| DSG-01b | negative，不改判。依據：〈顏色歸類判準〉「元件本身的功能類型」與同上句 | 是 | 是 | 是 | 是 |
| DSG-02a | 主題插槽。依據：〈Theme 與 Token 判準〉「對應的內建語意插槽存在時用主題插槽，不存在時用 token」 | 是 | 是 | 是 | 是 |
| DSG-02b | token。依據：同上 | 是 | 是 | 是 | 是 |
| DSG-03a | `titleLarge`。依據：〈字級判準〉「卡片或對話框等容器元件內的標題對映 `titleLarge`」 | 是 | 是 | 是 | 是 |
| DSG-03b | `headline4`，不可任選。依據：「頁面內的階層子節標題對映 `headline4`」「數值相同不代表語意相同」 | 是 | 是 | 是 | 是 |
| CCD-03a | 三段用固定寫法「不經服務：讀取的資料為 X，於 Y 時已載入；資料缺值時的表現見 Z」，不得寫「同成功」。依據：方法論〈失敗路徑覆蓋〉末段 | 是 | 是 | 是 | 是 |
| CCD-07a | 不新增列；寫進該元件回饋契約「被程式定位」格。依據：〈非狀態進入情境〉「不得為此新增獨立列」「需要程式定位或聚焦者寫進該元件回饋契約「被程式定位」格」 | 是 | 是 | 是 | 是 |
| CCD-08b | 各自為候選（非合併、非 slot）。依據：步驟 2「兩集合互有對方沒有的列時，兩者都不是對方的一部分，各自為候選」 | 是 | 是 | 是 | 是 |
| CCD-10a | 否。依據：〈關卡二〉「疊加在事件結果所在區塊上、原內容全程可見時，指示消失後內容與事件前相同即答否」 | 是 | 是 | 是 | 是 |
| CCD-12b | 一張。依據：〈遷移票〉「「目錄」指直接含待改頁面檔的目錄，不是其上層目錄」 | 是 | 是 | 是 | 是 |
| VB-02b | 不成立。依據：判準 2「是否有消費方位於另一個獨立發布或部署的單位」「判定單位是發布或部署單位，不是儲存庫。」 | 是 | 是 | 是 | 是 |
| CCD-11a-r2 | (1) 展開到「成員列的頭像 slot 放頭像變體 small」為止；(2) 圓點不寫；(3)「逐格放成員列變體 default，數量依資料筆數」。依據：〈巢狀容器〉三句 | 是 | 是（數量寫「依團隊成員人數」，答案鍵允許的同義形式） | 是（(3) 附「或…數量依團隊成員人數」，屬同義形式，不與條文衝突） | 是 |

#### 失敗歸因

無未通過題。

#### 越界檢查

對 12 份答案逐一 grep 答案鍵列出的全部污染檢查字串（DSG：「已刪除」「按鈕文字顏色為主要品牌色」「兩個 token 是否因數值相同」`colorScheme.primary`；CCD：「格資料」「載入中」「清單卡」「帶目標進入」「訊息前進組」「輔助連結」Given／When／Then、「原 5 筆」`features/project/`、`features/settings/`；CCD-11a：「逐層展開到葉元件」「徽章 slot」「狀態徽章」；VB：「服務甲」「服務乙」），另加「範例」「SKILL.md」「.claude/」「判斷準則」「docs/」。
- 命中 1 處，不計越界：CCD r3 CCD-03a 判定句「資料缺值時的表現見 清單卡的內容政策」。「清單卡」是 CCD-07a 的檢查字串，但 CCD-03a 題幹本身寫「清單卡的內容政策寫…」，屬引用題幹。CCD-07a 的 3 份判定句都沒有出現範例元件名。
- 其餘 0 命中。所有依據句的出處都標節錄檔名或其段落。「只讀…」限定段落的題（CCD-07a、08b、12b、11a）引文都落在限定段落內。
- 越界：0 次。

#### 答案鍵疑誤

判定與獨立推導一致，無判定錯誤。CCD-08b 答案鍵已註「單一畫面內共現」優先句的另一條路徑（兩條路徑結論都是不合併、非 slot）。CCD r2 同時引了這兩句，判定取「各自為候選」，(3) 答「否」，沒有察覺兩句的先後關係；結論在答案鍵允許範圍內，不影響計分。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 引文竄字 | CCD r2 CCD-12b：「互相阻擋」，節錄檔原文為「互相阻塞」 |
| 引文刪字 | DSG r2 DSG-03a：「`titleLarge   // 20.rsp …」少了 `UIFontSizes.` 前綴 |
| 引文加粗 | DSG r2、CCD r2、CCD-11a r2、VB r2、VB r3、CCD r3 的部分引文自行加粗，原文沒有 |
| 引號改寫 | DSG r1、CCD r1、CCD r3 把原文「」改成『』 |
| 值填入 | CCD r2 CCD-03a 的 Y 值寫「進入頁面時」，題幹是「進入『正常』狀態時」。X、Y、Z 不計分，也不是範例值 |
| 自行假設 | CCD r1、r2 CCD-08b 假設「甲」「乙」是不同畫面，屬題幹表述未明寫，與答案鍵的讀法一致 |
〈b1x 與 CCD-11a 判定〉探針異常表更正：「引文加粗」列誤列「CCD r3」，CCD r3 的引文沒有自行加粗，應為「DSG r2、CCD r2、CCD-11a r2、VB r2、VB r3」。計分不受影響。
### b2x ux 判定（獨立判定者，2026-09-15）

**判定方法**：條文讀節錄檔（`probes/excerpt/`，HEAD d8f36ee5 產生，範例區塊已刪除）。每題先依該題餵入的節錄檔自行推出判定與依據句，再開答案鍵（`probes/key/W3-417-b2x-ux-design-evaluation-{1,2}.md`）比對，最後逐份判 6 份答案（`probes/a/W3-417-b2x-ux-design-evaluation-{1,2}-r{1,2,3}.md`）。門檻 3/3。計分口徑沿用〈第 2 批判定〉，並依〈第 2 批判定後的處置〉：讀者失誤記不通過；判定句附帶與條文不符的替代答案、又沒有更正的，記不通過；子問漏答記不通過。

**越界檢查方法**：兩份 ux 答案鍵沒有附「範例污染檢查字串」（只有 `key/W3-417-b2x-ccd-11a.md` 有）。改用機械比對代替：(1) 取 `probes/tools/removed/` 中 28 份 ux 被刪範例文字，去掉節錄檔也有的片段，以 22 字元視窗比對每份答案的每題，另掃「範例」「【輸入】【產出】【驗證】」「三段鏈式」與第 2 批已知污染結論（「600ms」「五個死胡同」「不等於」「權威值只在」）；(2) 抽出依據句引號內片段，逐段比對全部 ux 節錄檔。

**結果**：49／58。之 1 為 23／30，之 2 為 26／28。越界 0 次。

#### 逐題表：之 1（23/30）

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-01a | 1-10s，進度指示 + spinner；不可只寫要有回饋；標明統計量 | 是 | 是 | 是 | 是 | — |
| UX-01b | 100-400ms，動畫過渡；不用 spinner；仍須交值 | 是 | 是 | 是 | 是 | — |
| UX-02a | 臨界路徑；Tab、`;` 放拇指處 | 是 | 是 | 是 | 是 | — |
| UX-02b | 依頻率；Esc 最外側 | 是 | 否（依臨界路徑） | 否（依臨界路徑，Esc 放拇指處） | 否 | 讀者失誤 |
| UX-03a | visiblePassword；三項全關；protocol spec 階段 | 是 | 是 | 是 | 是 | — |
| UX-03b | 一般文字；不關；不適用安全清單 | 是 | 是 | 是 | 是 | — |
| UX-04a | 一般文字鍵盤 | 是 | 是 | 是 | 是 | — |
| UX-04b | 數字鍵盤 | 是 | 是 | 是 | 是 | — |
| UX-05a | 格式驗證，on blur | 是 | 是 | 是 | 是 | — |
| UX-05b | 交叉驗證，on submit | 是 | 是 | 是 | 是 | — |
| UX-06a | 按送出；本機不足以選即時 | 否（即時） | 否（即時，判定句自相矛盾） | 是 | 否 | 讀者失誤 |
| UX-06b | 即時；來源不改變判定 | 是 | 是 | 是 | 是 | — |
| UX-07a | 學習關；校正、建議保留開啟；iOS 顯式記錄風險 | 否（校正、建議皆關） | 是 | 是 | 否 | 讀者失誤 |
| UX-07b | 三項皆關 | 是 | 是 | 是 | 是 | — |
| UX-08a | segmented control | 是 | 是 | 是 | 是 | — |
| UX-08b | 拆狀態顯示＋動作鈕 | 是 | 是 | 是 | 是 | — |
| UX-09a | 合格 | 否 | 否 | 是 | 否 | 條文不清 |
| UX-09b | 不合格；描邊 vs 實心不足 | 是 | 是 | 是 | 是 | — |
| UX-10a | 不可省略號；先縮格式，仍不足才設最小寬度 | 是 | 是 | 是 | 是 | — |
| UX-10b | 不須設最小寬度；讓步規則路由 CCD〈空間不足策略〉 | 否（仍須設定） | 否（視讓步情況） | 否（視後續內容變化） | 否 | 題目設計 |
| UX-11a | 不足，須記錄統計量；p95 2.5s → 進度指示 + spinner | 否（對到 > 10s） | 否（對到 > 10s） | 是 | 否 | 讀者失誤 |
| UX-11b | 不可 | 是 | 是 | 是 | 是 | — |
| UX-12a | 寫在 `screen-state-matrix.md`；本節負責 UI 呈現形式 | 是 | 是 | 是 | 是 | — |
| UX-12b | 屬本檔；spinner（形狀優先）；1-10s 進度指示 + spinner | 否（附「至少 spinner」） | 是 | 是 | 否 | 讀者失誤 |
| UX-13a | 略大於 60 秒；恢復 idle + 錯誤訊息 | 是 | 是 | 是 | 是 | — |
| UX-13b | 不合格 | 是 | 是 | 是 | 是 | — |
| UX-14a | 4 狀態；全空＝死胡同；只寫「返回」缺目標未定義 | 是 | 是 | 是 | 是 | — |
| UX-14b | 從 enum 取；格式合格 | 是 | 是 | 是 | 是 | — |
| UX-15a | 需要 initializing；網路不影響 | 是 | 是 | 是 | 是 | — |
| UX-15b | 不需要，一般 loading | 是 | 是 | 是 | 是 | — |

#### 逐題表：之 2（26/28）

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-16a | 可逆，由「關閉勿擾」提供 | 是 | 是 | 是 | 是 | — |
| UX-16b | 不可逆；「關閉面板」仍滿足退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-17a | 獨立成列；兩畫面各一列；退出至少返回或離開此畫面；記錄設定入口與清除時機 | 是 | 是 | 否（漏答退出路徑子問） | 否 | 讀者失誤 |
| UX-17b | 不屬 | 是 | 是（以條件 1 作答，答案鍵允許） | 是 | 是 | — |
| UX-18a | 只給重試不足；重啟 app 不合適；須含退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-18b | 不合適；配資料層行動 | 是 | 是 | 是 | 是 | — |
| UX-19a | 固定間隔並設上限；重試狀態三件事 | 是 | 是 | 是 | 是 | — |
| UX-19b | 指數退避 + jitter；倒數與立即重試按鈕 | 是 | 是 | 是 | 是 | — |
| UX-20a | 不可靜默隱藏；明確標示或替代方案 | 是 | 是 | 是 | 是 | — |
| UX-20b | 可以 | 是 | 是 | 是 | 是 | — |
| UX-21a | 不合格（3 < 8） | 是 | 是 | 是 | 是 | — |
| UX-21b | 合格 | 是 | 是 | 是 | 是 | — |
| UX-22a | (D) 都不屬於 | 是 | 是 | 是 | 是 | — |
| UX-22b | 推入堆疊頂端 | 是 | 是 | 是 | 是 | — |
| UX-23a | 替換堆疊頂端 | 是 | 是 | 是 | 是 | — |
| UX-23b | 替換整個堆疊 | 是 | 是 | 是 | 是 | — |
| UX-24a | 不必適配 | 是 | 是 | 是 | 是 | — |
| UX-24b | 必須適配 | 是 | 是 | 是 | 是 | — |
| UX-25a | 重建到設定選單的堆疊 | 是 | 是 | 是 | 是 | — |
| UX-25b | 只放目標畫面 | 是 | 是 | 是 | 是 | — |
| UX-26a | 分兩列 | 是 | 是 | 是 | 是 | — |
| UX-26b | 合併一列；列名寫出 iOS／Android 兩類型 | 否（判定寫分兩列，列名另附「平台權限限制」） | 否（另附「永久拒絕」） | 否（另附「永久拒絕」） | 否 | 條文不清 |
| UX-27a | Degraded mode；標示可能非最新 | 是 | 是（出處標錯，見異常） | 是（出處標錯，見異常） | 是 | — |
| UX-27b | Retry with feedback；狀態 + 手動重試 + 退出路徑 | 是 | 是 | 是 | 是 | — |
| UX-28a | 可以 | 是 | 是 | 是 | 是 | — |
| UX-28b | 使用語音輸入時請求 | 是 | 是 | 是 | 是 | — |
| UX-29a | 需要確認；文案點出覆蓋未同步筆記、無法復原 | 是 | 是 | 是 | 是 | — |
| UX-29b | 仍需確認；未渲染時不執行 | 是 | 是 | 是 | 是 | — |

#### 失敗歸因

| 題號 | 歸類 | 具體句 |
|---|---|---|
| UX-02b | 讀者失誤 | 條文「排列依該場景下的實際使用頻率排列」在前，「使用頻率無法確定時」才退回臨界路徑。題幹已給頻率，r2 仍在假設欄寫「此統計不是排列的決策依據」，r3 依臨界路徑把 Esc 放拇指處 |
| UX-06a | 讀者失誤 | r1、r2 都引了「本地資料若運算量大（如大量記錄的全文搜尋）仍屬高成本，適用『按送出』」，判定仍寫即時。r1 稱 50 萬筆全文模糊比對屬低成本；r2 同一判定句先寫「應採即時」、再寫「資料在本機不足以直接判定採即時」 |
| UX-07a | 讀者失誤 | 條文「自然語言時兩者保留開啟」明確。r1 把「不希望被詞庫記住」擴大到自動校正與輸入建議，依據句沒有引這句 |
| UX-09a | 條文不清 | W3-446 補句後仍有 2/3 判不合格（第 2 批 3/3）。第 2 項開頭的理由「同列有按鈕時使用者預設整列可點，點到指示的『沒反應』被讀成壞掉」不分形態；豁免「兩者擇一即合格、與是否和按鈕同列無關」接在同一長句的尾端。r1 引文截在「或移出動作列」，沒讀到豁免；r2 引到「兩者擇一即合格」，仍判同列違反「移出動作列」 |
| UX-10b | 題目設計 | 3/3 沒有依題幹前提作答（第 2 批 2/3），三份都以計數日後可能變長為由保留最小寬度，r3 明列「99/100」。題幹用動態計數「12/30」當作「已完整顯示」的前提，但單一值顯示完整，不代表這個欄位的寬度需求已定。修法：改用長度固定的文字，或在題幹寫明「內容長度固定」 |
| UX-11a | 讀者失誤 | 題幹 p95 為 2.5 秒，條文表 1-10s 列是「進度指示 + spinner」。r1、r2 都對到「> 10s｜進度百分比 + 預估時間 + 取消選項」 |
| UX-12b | 讀者失誤 | r1 判定句寫「進度指示 + spinner（但此處無預知的進度百分比，所以至少spinner）」，把進度指示讀成進度百分比，再降為只要 spinner。這是與表列不符的附帶替代答案，沒有更正 |
| UX-17a | 讀者失誤 | 條文「退出路徑至少要有『返回』或『離開此畫面』一項」在餵入範圍內，r3 漏答這個子問。題幹四問沒有編號，r1、r2 都答全 |
| UX-26b | 條文不清 | 3/3 都另附「永久拒絕」「平台權限限制」這類總稱作為列名選項。條文「列名寫出涵蓋的類型」沒有說清楚：要逐一寫出成員類型，還是一個涵蓋性總稱就算。r1 另有判定句寫「應分成兩列」，與自己依據句的「故應合併為一列」矛盾 |

#### 與第 2 批的對照

- 第 2 批未通過、本批通過：UX-01a、08b、12a、14a、14b、22a。
- 兩批都未通過：UX-02b、07a、09a、10b、26b。
- 本批新增未通過：UX-06a、11a、12b、17a。
- 答案鍵的兩項條文風險：UX-06 有 2/3 判即時，沒有人判 debounce，W3-446 補句沒有擋住這一型；UX-12a 3/3 答 `screen-state-matrix.md`，沒有人答寫在本節。UX-20a 3/3 通過，沒有使用者層級的讀法。

#### 答案鍵疑誤

58 題的判定與獨立推導一致，沒有判定錯誤。有 1 處附註：兩份 ux 答案鍵沒有附「範例污染檢查字串」，本批改用上方的機械比對，結果為 0 命中。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 讀出餵入範圍 | 之 1 r1 13a 引「逾時後轉入 error」，出自〈畫面級回饋〉節錄檔，不在 13a 的餵入內；答題內容是節錄檔條文，不算越界 |
| 出處標錯 | 之 2 r2、r3 27a 把「呈現快取內容時須標示內容可能不是最新」標為 Degraded-mode.md，實際在〈Gate 類型與各自的設計重點〉節錄檔 |
| 拼接引文 | 之 2 r2、r3 19b 把指數退避項的「（使用者可跳過等待）」接到間隔策略段句尾；之 2 r2 17a 在引文中自加「[設定入口][清除時機]」 |
| 判定與依據矛盾 | 之 1 r2 06a；之 2 r1 26b |
| 日文字形 | 之 2 r1 26b「系統対話框」 |
| 同義附註，不扣分 | 之 1 r1 03a「或等義的結構化內容鍵盤型」、r2 04b「或專用的數字鍵盤」 |
| 判定用詞偏弱，以通過計 | 之 1 r2 14a 子問 2 寫「導航缺口」、r3 寫「還需要設計補充」，都沒寫出「死胡同」，但都寫出須補退出路徑 |
### b2x ux 判定後的處置（PM，2026-09-15）

**b2x 結果**
- ux 49/58，越界 0。
- b1x 與 CCD-11a 13/13，越界 0。

**用戶裁示：讀者失誤是內容問題**
- 事實：UX-06a、11a、12b、17a 上一輪 3/3，這輪條文與題目都沒變卻失敗。PM 提出這代表 haiku 讀者本身有雜訊。
- 用戶裁示：「那這就不是讀者失誤的問題，確實是內容的問題」。
- 據此調整：
  - 閘門維持 haiku、3/3。
  - 判定歸因為「讀者失誤」的失敗，不再只改題，改為追查條文哪裡讓讀者讀歪，並修改條文。
  - 條文要寫到最弱的讀者也讀不歪。
  - 題目設計問題仍然改題。

**9 題處置：開 W3-449 修 ux-design-evaluation 條文**

| 題號 | 判定者看到的讀歪點 |
|---|---|
| UX-09a | 例外條件接在長句尾端，開頭的理由句卻適用所有形態 |
| UX-26b | 「列名寫出涵蓋的類型」沒說要逐一列出，還是可用總稱 |
| UX-06a | 讀者引對了「仍屬高成本，適用按送出」卻選即時搜尋；結論與表格的對應關係不夠直接 |
| UX-02b | 有頻率資料時仍依臨界路徑排列；條件分支的先後次序不夠顯眼 |
| UX-07a | 自然語言欄位把自動校正與建議都關閉，漏讀保留開啟的條件 |
| UX-11a | p95 = 2.5 秒被歸到大於 10 秒的帶，時間帶邊界的寫法容易錯位 |
| UX-12b | 讀者自行加上「至少 spinner」，表格組合「進度指示＋spinner」的讀法不夠明確 |
| UX-17a | 題目的四個子問沒有編號，屬題目設計；同時檢查條文是否也有並列未編號的問題 |
| UX-10b | 題目設計：計數器長度可能變化，題幹改為固定長度文字 |

**完成條件**：W3-449 落檔後，這 9 題用節錄檔重跑（10b 改題），3/3 才通過。

**已通過題的處理**：上一輪通過、這輪才失敗的 4 題證明通過本身也有雜訊。已通過的題不回頭重跑；各批判定完後，再評估是否整體重跑一次。

**後續批次**：沿用本口徑。
更正上一則〈b2x ux 判定後的處置〉「已通過題的處理」：原句「各批判定完後，再評估是否整體重跑一次」沒有綁定觸發條件，改為已決策——第 5 批判定完成後、W2 開工前，所有已通過題用節錄檔整體回歸重跑一輪（haiku，3/3）。回歸中失敗的題依本口徑修改條文。這一輪回歸屬於 W2 開工閘門的一部分。
### 第 3 批出題後的處置（PM，2026-09-15）

**第 3 批規模**：foundation-design 22 則範例（原盤點 19，漏列 3），test-assertion-design 6 則，共 56 題，加 DSG-04b 1 題。兩項自檢皆通過。

**探針**：foundation-design-1（28 題，C-01～C-14）不涉及標記項，先派 3 份。foundation-design-2、test-assertion-design-1、dsg-04b 等 W3-450 修補完成、DSG-04b 改題後再派。

**依據只在範例內**：FD-20a、FD-20b（另有字面衝突）、TAD-03a 共 3 則，轉 W3-450。

**DSG-04b**
- 依現行〈交界〉，答案鍵已由「不需 i18n」翻轉為「只顯示錯誤碼屬違規」，與 W3-436 用戶裁示一致。
- 題幹為是非問句，正解需分兩層作答，與 VB-03a、UX-22a 失敗同型。依題目設計口徑改為選擇式後再派。

**i18n-guidelines〈交界〉補盤點**
- 該節由 W3-438 新增，含 Violation／Fix 對照，依 W3-437 判準屬範例，原盤點未列。
- 閘門範圍是全部範例，因此補入盤點，出 2 題（新輸入、相反分支），與 DSG-04b 改題同批處理。
### b3 foundation-design-1 判定（獨立判定者，2026-09-15）

**判定方法**：沿用〈b2x ux 判定〉。每題先只讀該題餵入的節錄檔（`probes/excerpt/`，HEAD e19b5cb4），自己推出判定與依據句。之後開答案鍵 `key/W3-417-b3-foundation-design-1.md` 比對，再逐題判 3 份答案 `a/W3-417-b3-foundation-design-1-r{1,2,3}.md`，須 3/3 才通過。口徑依〈b2x ux 判定後的處置〉：讀者讀歪記為條文問題，不再歸因「讀者失誤」。另沿用兩條計分規則：
- 整個子問在判定句中沒有作答，記不通過。只漏了子問內的某個元素，不扣分。
- 判定句附帶與條文或題幹條件不符的內容，又沒有更正，記不通過。

**答案鍵引文核對**：答案鍵共 71 段 ⟪⟫ 引文，去掉空白後逐段比對節錄檔，71 段都逐字存在。

**越界檢查**：
- (1) 答案鍵附的 14 條污染檢查字串，外加「範例」「【輸入】【產出】【驗證】」。只有 r3 FD-11a 出現「三張地基票」。這是依題幹推出的張數（建三張地基票：design-system、UX 審查、元件庫），不是 C-10 範例的內容，不算越界。
- (2) `tools/removed/` 中 40 份非空的被刪文字，先去掉節錄檔或題目裡也有的片段，再以 22 字元視窗比對（忽略空白與標點）。命中的片段都是「依據句」標籤接上節錄檔條文原句，不是範例特有文字。
- 越界 0 次。

**結果**：19／28。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| FD-01a | 不可；先確認該處確實承擔該產物 | 是 | 是 | 是 | 是 | — |
| FD-01b | 可，附位置；不建票 | 否（漏答子問 2；另把「確認記錄」列為必附） | 否（漏答子問 2） | 是 | 否 | 條文讀歪點 |
| FD-02a | 以畫布為準；不寫數值，指向 token 名 | 否（子問 2 答「是」，接著又寫「而非複述新的數值」） | 是 | 是 | 否 | 條文讀歪點 |
| FD-02b | 以規格為準；不刪，標漂移送 PM | 是 | 是 | 是 | 是 | — |
| FD-03a | 萃取當下歸納為離散具名階 | 是 | 是 | 是 | 是 | — |
| FD-03b | 不可；逐值命名值不變，收斂另開票同批建立 | 是 | 是 | 是 | 是 | 見答案鍵疑誤 |
| FD-04a | 不成立；取與形態無關部分，不整條跳過 | 是 | 是 | 是 | 是 | — |
| FD-04b | 不可填改寫產物，改標待定 | 是 | 是 | 是 | 是 | — |
| FD-05a | 已存在於 X；判準層標無判準＋框架缺口票 | 是 | 是 | 是 | 是 | — |
| FD-05b | 不能；產物層依「該產物本專案有沒有，照四種答案填」 | 是 | 否（未答依據，直接給「無或改寫產物」） | 否（未答依據，直接判「無」） | 否 | 條文讀歪點 |
| FD-06a | 不構成；不可填無 | 是 | 是 | 是 | 是 | — |
| FD-06b | 照預設；必附：無 | 是 | 否（判定句未答附帶；只在依據句引了該格「無」） | 是 | 否 | 條文讀歪點 |
| FD-07a | 無；理由＋重評條件 | 是 | 是 | 是 | 是 | — |
| FD-07b | 待定；缺什麼＋補票 | 是 | 是 | 是 | 是 | — |
| FD-08a | 不缺席；落在被盤點 repo | 是 | 是 | 是 | 是 | — |
| FD-08b | 待定：權威缺席；補權威票或指定替代權威 | 是 | 是 | 是 | 是 | — |
| FD-09a | 答案鍵：命中 | 是 | 否（判不命中） | 是 | 否 | 條文讀歪點（另見答案鍵疑誤） |
| FD-09b | 其餘列；逐維度定產物交 version-sequencing | 否（判規劃波之後） | 是 | 是 | 否 | 條文讀歪點 |
| FD-10a | 規劃波進行中；地基票不計入 | 是 | 是 | 是 | 是 | — |
| FD-10b | 規劃波之後；補判未涵蓋產物，不重排既有票 | 否（漏答「不做什麼」） | 是 | 是 | 否 | 條文讀歪點 |
| FD-11a | 3 張：design-system、UX 審查、元件庫 | 是 | 是 | 是 | 是 | — |
| FD-11b | 1 張 | 是 | 是 | 是 | 是 | — |
| FD-12a | 不通過 | 是 | 是 | 是 | 是 | — |
| FD-12b | 通過 | 是 | 是 | 是 | 是 | — |
| FD-13a | 缺 ticket 系統＋步驟 4 與權威缺席時建票動作不成立 | 是 | 是 | 否（另加形態 4 的四欄決策記錄） | 否 | 條文讀歪點 |
| FD-13b | 不另建 | 是 | 是 | 是 | 是 | — |
| FD-14a | 權威訂的測試設計審查者；不可任選 | 是 | 是 | 是 | 是 | — |
| FD-14b | 不可填張三；退至本框架既有審查代理人 | 否（另附「或該維度的權威審查角色」，題幹已寫權威未訂） | 是 | 是 | 否 | 條文讀歪點 |

#### 失敗歸因

| 題號 | 歸類 | 讀歪點 |
|---|---|---|
| FD-01b | 條文讀歪點 | 「已存在於 X」的後果分在兩處：表格「必須附帶」欄只寫位置；「確認該處確實承擔該產物，不建票」寫在工作流 code block 步驟 3 的同一行箭頭後，確認動作和不建票兩項並列、沒有編號。r1 把「確認」讀成必附項；r1、r2 都引了「不建票」，但沒有把它當成子問 2 的判定。題目子問 1 內含兩問，放大了漏答 |
| FD-02a | 條文讀歪點 | 〈畫布漂移〉寫「以畫布為準並更新規格」，「引用 token 名而非數值」的主詞卻是「契約填寫時」。專講規格改寫後指向 token 名的句子，只出現在 examples.md 的判準來源引言。讀者從「更新規格」推出「是，寫出新值」，再接上契約的 token 規則，判定句因此自相矛盾 |
| FD-05b | 條文讀歪點 | 形態 3 產物層寫成二元存在問句「該產物本專案有沒有」，又說「照四種答案填」，沒有指明是五種答案中的哪四種。讀者把「沒有 seeder」直接對到答案「無」，跳過了「無」須證明失效不存在的要求 |
| FD-06b | 條文讀歪點 | 五種答案表中，「照預設」列的「必須附帶」欄填的是「無」，與答案形態「無」同一個字。r2 引了該格，判定句卻沒有把它當成「不需附帶」的答案 |
| FD-09a | 條文讀歪點 | 「既有文件**無法回答維度表任一格的產物**」是否定句加「任一」，有兩種讀法：一格答不出就命中，或每一格都答不出才命中。條文沒有寫明量詞。r2 讀成後者，本判定者的獨立推導也得出後者 |
| FD-09b | 條文讀歪點 | 「其餘」列只用「以上皆不命中」反向定義，沒有正面描述；「規劃波之後」列卻加粗「仍由本 skill 驅動」，並寫「這是真實 repo 最常見的狀態」。題幹寫「已有相當程度的實作」，r1 依時間語感把它歸入「之後」，沒有核對「該版票已建」這個判別條件 |
| FD-10b | 條文讀歪點 | 「規劃波之後」角色格把做什麼（補判）、不做什麼（不重排既有票）、狀態評語（最常見、不是收手）三句接在一起，沒有分項。禁止句夾在中間，r1 只摘了「仍由本 skill 驅動」。題目也把「做什麼、不做什麼」放在同一個子問 |
| FD-13a | 條文讀歪點 | 移植前置條件第四項把「形態 1、形態 4……以及下方的降級記錄」並列為落在決策文件上的內容；降級段又沒有說明缺 ticket 系統時產物改落在哪裡。r3 用形態 4（skill 缺席時產物改為文件形式的四欄記錄）補上這個空缺，但形態 4 的觸發條件是 skill 缺席，不是 ticket 系統缺席 |
| FD-14b | 條文讀歪點 | 附註先寫「兩個來源是先後順序而非任選」，同一段後句又寫「仍須落在上述兩個既有角色之一」；工作流步驟 5 也用「或」並列兩個來源。「之一」和「或」把有先後的順序讀回任選。r1 引的正是「之一」句，並附上題幹已排除的權威角色 |

#### 答案鍵疑誤

- **FD-09a**：答案鍵判「命中（任一格答不出即命中）」，本判定者的獨立推導是「不命中」（「無法回答任一格」＝一格都答不出）。兩種讀法在現文下都成立，不判答案鍵錯，但修條文時須由 PM 決定本意，再寫明量詞。旁證：該列角色欄寫「它們假設已有規格」，較接近「全部答不出」的讀法。
- **FD-03b**：判定者註要求子問 2「至少須答出『逐值命名、值不變』」。這個元素其實是子問 1「不可同時收斂」的內容。r1、r2 已答「不可同時收斂／應分兩步」與「收斂另開票同批建立」，本判定依獨立推導記通過。建議判定者註改為：子問 1 須含「值不變／分兩步」其一，子問 2 須含「另開票」。
- 其餘 26 題的判定與獨立推導一致。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 讀出餵入範圍（內容為節錄檔條文，不算越界） | r2 FD-04a 引維度表資料庫列（該題限讀〈權威缺席時〉）；r1、r2、r3 FD-07b 引〈改寫產物是主路徑〉（未餵入）；r3 FD-13a 引〈權威缺席時〉形態 4（未餵入，且是失敗原因） |
| 依據句混入己見 | r1 FD-01b「條文要求超過位置填寫，需附帶確認內容」寫在依據句欄 |
| 自行補完 | r2 FD-14b 舉「code-reviewer、bay-quality-auditor」為審查代理人實例，節錄檔沒有這些名稱 |
| 禁用詞 | r2 FD-07b「文檔」 |
| 引文中略去關鍵句 | r1 FD-03b 引文以「……」略去「第 2 步仍逐值命名、值不變」 |
### b3 foundation-design-1 判定後的處置（PM，2026-09-15）

**結果**：19/28 通過，越界 0。

**FD-03b 計分**
- 採判定者的推導，記通過。
- 理由：答案鍵的判定者註把子問 1 的內容（逐值命名、值不變）歸到子問 2 去要求，屬答案鍵誤植。r1、r2 已答出子問 2 的核心：分兩步、收斂另開票。

**9 題讀歪點**
- 開 W3-452 修 foundation-design 條文。
- W3-452 設 blockedBy W3-450：兩票同 skill，避免同檔並行寫入。
- FD-09a 的本意由 PM 裁定為「一格答不出即命中」，依據為 examples-process 範例驗證段。這個本意原本只寫在範例中，W3-452 要把量詞寫進條文。

**W3-449 的 UX-26b 讀法選擇**
- 撰寫者把「列名寫出涵蓋的類型」定為「逐一列出、排除總稱」，這是替兩種讀法做選擇。
- PM 接受。理由：用總稱時，無法核對涵蓋範圍。
- 已向用戶說明，用戶可推翻。
### b3 dsg 判定（獨立判定者，2026-09-15）

**判定方法**：沿用〈b3 foundation-design-1 判定〉。三題餵入相同，都是 3 份 i18n 節錄檔（`probes/excerpt/`，HEAD 5f1616de）。先只讀節錄檔，逐一推出各子問的判定。之後開答案鍵 `key/W3-417-b3-dsg.md` 比對，再逐題判 3 份答案 `a/W3-417-b3-dsg-r{1,2,3}.md`，3/3 才通過。兩條計分規則照舊：整個子問沒答記不通過；附帶與條文不符的內容且沒有更正，記不通過。

**獨立推導與答案鍵比對**：三題推導分別為 DSG-04b 1B／2B、DSG-05a 1B／2B／3B、DSG-05b 1B／2B／3A，與答案鍵的預期判定全部一致，沒有發現答案鍵誤。

**答案鍵引文核對**：答案鍵共 18 段 ⟪⟫ 引文。去掉空白、表格分隔符與粗體標記後，逐段比對節錄檔，18 段都逐字存在。

**探針引文核對**：r1 有 10 段「」引文，r2 有 11 段，r3 有 16 段。以「...」切段後比對節錄檔與題目，只有 1 段不在其中：r3 DSG-05b 子問 3 的「呈現層以 `ErrorHandler.getUserMessage(...)` 轉譯後顯示訊息，畫面上不顯示錯誤碼」。這段自標為題目前提，是把前提略寫後引用，內容沒有超出題目，不算越界。

**越界檢查**：
- (1) 答案鍵附的 6 條污染檢查字串，外加「ISBN」，在 3 份答案中都沒有出現。
- (2) `tools/removed/` 中 3 份 i18n 被刪文字，先去掉節錄檔與題目裡也有的片段，再以 22 字元視窗比對（忽略空白與標點），命中 0。
- 越界 0 次。

**結果**：3／3。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| DSG-04b | 1 (B) 不進 ARB；2 (B) 不合格，改顯示轉譯後訊息 | 是 | 是 | 是 | 是 | — |
| DSG-05a | 1 (B)；2 (B) 不在豁免；3 (B) 參數化翻譯 | 是 | 是 | 是 | 是 | — |
| DSG-05b | 1 (B)；2 (B) 呈現層轉譯；3 (A) 合格 | 是 | 是 | 是 | 是 | — |

判定者註套用情形：
- DSG-05a：3 份答案都沒有註明〈Violation 3〉沒有餵入，都依〈Exceptions〉說明段「不得以字串插值直接拼接整句」答出參數化翻譯，依註記通過。沒有人把「：」歸入 Formatting characters。
- DSG-05b：沒有人另外討論日誌字串，子問 3 都判合格。出題者附記的題目設計風險 2 沒有發生。

#### 失敗歸因

無失敗題。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 依據句混入己見 | r1 DSG-05a 子問 3（「程式碼使用了字串插值直接拼接…違反上述規範」）；r1 DSG-05b 子問 3（「題目中三層設計符合此規範…」）；r3 DSG-05b 子問 3（把前提略寫後放進依據句） |
| 引文檔名縮寫 | r3 把節錄檔名寫成「錯誤碼與交界.md」，引文內容可在〈交界〉節錄檔中逐字找到 |
| 引文中略去內容 | r3 DSG-05a 子問 3 兩段引文以「...」略去中段，被略去的部分不影響判定 |
### b3 dsg 判定與 b2y 出題後的處置（PM，2026-09-15）

**b3 dsg**：3/3 通過，越界 0。DSG-04b、DSG-05a、DSG-05b 全數過關；dart-style-guardian 已沒有待重跑題。

**b2y 出題風險：條文例示與題目輸入同構**
- 現象：W3-449 修條文時新增的例示，與失敗題的輸入幾乎相同。
  - UX-26b：iOS／Android 拒絕次數。
  - UX-02b：必經鍵。
  - UX-12b：「配對中」。
- 後果：探針可能是照抄例示答對，而非讀懂判準。這樣的 3/3 不能證明條文可讀。
- 裁定：這三題改用與條文例示不同構的新輸入，判定點不變。

**方法規則（本輪起適用）**
- 條文修補之後的重跑題，出題者要比對題目輸入與修補新增的例示；兩者同構就換輸入。
- 修條文的撰寫者可以為讀歪點加例示，但例示不得照搬探針題目的輸入。這條寫進後續修條文票的派發 context。

**UX-12b 的 2-10s 列**：表後已有裁決順序「形狀 > 觸發 > 時長」，不構成條文衝突，本輪不修。如果探針仍依時長判定，就把「裁決順序句位於表後」當成讀歪結構處理。
### b2y 判定（獨立判定者，2026-09-16）

**判定方法**：沿用〈b3 foundation-design-1 判定〉，並依〈b3 dsg 判定與 b2y 出題後的處置〉加查同構抄答。每題先只讀餵入節錄檔（`probes/excerpt/`，HEAD 647be9bb）自行推出判定，再開答案鍵 `key/W3-417-b2y-ux-design-evaluation.md` 比對，最後逐題判 3 份答案 `a/W3-417-b2y-ux-design-evaluation-r{1,2,3}.md`，3/3 才通過。兩條計分規則照舊：整個子問沒答記不通過；判定句附帶與條文或題幹不符的內容且沒有更正，記不通過。

**獨立推導與答案鍵比對**：9 題推導與答案鍵預期判定全部一致，沒有發現答案鍵誤。

**答案鍵引文核對**：37 段 ⟪⟫ 引文，去掉空白與標點後逐段比對節錄檔，37 段都逐字存在。

**探針引文核對**：r1 38 段、r2 47 段、r3 41 段「」引文，以「...」切段後比對節錄檔與題目。不在其中的片段分三類，都沒有超出節錄檔內容：
- 作答者自擬的列名（r1、r2、r3 UX-26b）。
- 表格列攤平後引用（r1 UX-17a 設定入口／清除時機兩列；r3 UX-17a「設定入口、清除時機」）。
- 放進引號的改寫（r3 UX-07a「iOS 無法單獨關閉個人化學習」，原文為「此平台無法單獨關閉學習」；r3 UX-10b「文字有版面保障」，原文為「關鍵回饋文字要有版面保障」）。

**越界檢查**：`tools/removed/` 中 ux-design-evaluation 被刪文字，先去掉節錄檔與題目裡也有的片段，再以 22 字元視窗比對（忽略空白與標點），3 份答案命中 0。越界 0 次。

**結果**：8／9。

#### 逐題表

| 題號 | 正確判定 | r1 | r2 | r3 | 通過 | 原因 |
|---|---|---|---|---|---|---|
| UX-09a | 合格；圓點屬明顯非按鈕形態，同列不影響 | 是 | 是 | 是 | 是 | 見同構抄答 |
| UX-26b | (1) 合併一列；(2) 列名逐一列出 MDM 政策與家長監護 | 是 | 是（「MDM 停用、家長監護限制相機」） | 是（「MDM 政策停用或家長監護設定關閉」） | 是 | — |
| UX-06a | 按送出；本機不足以選即時 | 是 | 是 | 是 | 是 | 見同構抄答 |
| UX-02b | (1) 依頻率；(2) Tab 放外側 | 是 | 是 | 是 | 是 | — |
| UX-07a | (1) 學習明確關閉；(2)(3) 校正、建議保留開啟；(4) 規格記錄無法單獨關閉與接受風險 | 否（(2)(3) 判關閉） | 是 | 是 | 否 | 條文讀歪點 |
| UX-11a | 不足，須寫明統計量；p95 落 1s～10s 帶，進度指示 + spinner | 是 | 是 | 是 | 是 | — |
| UX-12b | (1) 屬本檔；(2) spinner；(3) 1s～10s 帶，進度指示 + spinner | 是 | 是 | 是 | 是 | — |
| UX-17a | 須獨立成列；兩畫面各自需要；退出至少返回或離開；設定入口與清除時機 | 是 | 是 | 是 | 是 | — |
| UX-10b | (1) 不須最小寬度；(2) 不由本項決定，路由 ccd〈空間不足策略〉 | 是 | 是 | 是 | 是 | — |

判定者註套用情形：
- UX-12b：沒有人附「至少 spinner」「只用 spinner」，沒有人依「等待較長（2-10s）」列選 skeleton。處置段預設的「裁決順序句位於表後」讀歪結構沒有發生。r1 子問 1 另寫「退出路徑須在狀態矩陣中決定」，與〈畫面級回饋〉表後段一致，不扣分。
- UX-10b：沒有人答「不一定、視空間而定」。r1 判定句寫「應不需要」，判定方向明確，記通過。

#### 失敗歸因

| 題號 | 歸類 | 讀歪點 |
|---|---|---|
| UX-07a | 條文讀歪點 | 〈IME 安全清單〉的非 secret 規則寫了兩次，表前段只到「改依內容是否為自然語言決定」，沒有寫出自然語言時的結果；「自然語言時兩者保留開啟」只在表後段。兩段之間夾著唯一的設定表，表的欄只有「secret 類欄位設定」，三列全是關閉。r1 引了表前段，得不到結果，就改用表格補上，寫出「表格顯示個人化學習『明確關閉』，自動校正『關閉』，輸入建議『關閉』」，沒有核對欄名限 secret。W3-449 補的表後句沒有被讀到，原因是表前段看起來已經完整 |

#### 同構抄答跡象

- **UX-09a**：題幹輸入「實心小圓點」與判準 (a) 例示「圓點」相同，未在 b2y 換輸入。3 份答案都是把圓點對到例示字面（r2「符合『明顯非按鈕形態』中的『圓點』」），沒有依「非按鈕形態」本身推導。本題 3/3 只能證明讀者讀到「與是否同列無關」，不能證明讀懂形態判準。歸題目設計。
- **UX-06a**：題幹「50 萬筆詞條、全文模糊比對」與 W3-449 改寫句的例示「如大量記錄的全文搜尋」近乎同構。r2、r3 的推導就是對到例示（「50 萬筆詞條的全文模糊比對屬於運算量大的情況」）。本題判定點（成本而非資料位置）有依判準作答，但「運算量大」的辨認靠例示完成。歸題目設計。
- **UX-11a（輕度）**：題幹「平均 350ms、p95 2.5 秒」與條文例示「平均 300ms、p95 卻 2s」數值相近。條文例示沒有給出門檻帶，探針仍須自行對表，判定不受影響，只記錄。
- PM 已換輸入的 UX-26b、02b、12b：沒有抄例示跡象。3 份 26b 都自擬列名，沒有沿用 iOS／Android 句型。r3 02b 寫「即使 Tab 是完成核心操作的必經鍵」是把題幹前提套進條文用語，屬推導。

#### 探針異常（不影響計分）

| 類型 | 發生處 |
|---|---|
| 依據句混入己見 | r2 UX-06a、26b、02b、10b，r3 UX-09a，在依據句欄後接推論句 |
| 引號內改寫 | r3 UX-07a「iOS 無法單獨關閉個人化學習」；r3 UX-10b「文字有版面保障」 |
| 漏子問內元素 | r1、r2 UX-07a 子問 4 未寫「不以關閉全部輸入輔助代替」（元素層，不扣分） |
| 多餘假設 | r3 UX-07a 標「假設症狀描述符合 confidential」，題幹已給「不希望被詞庫記住」，條文以此為判準，不需假設 |
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: PM 派發出題者、探針與判定者
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-410 | chars: 390 -->

### Task Reference
- 0.1.0-W3-410 what: 為 dimension-product-notes 可觀測性維度、UI 維度（失效樣貌依形態改變）、examples 拆兩問步驟 1（無獨立部署時 DevOps 失效是否成立）三處推導補條文依據，或判定該推導不應存在並改寫範例；需新增判準者走兩階段草稿停手（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-410 why: W3-408 兩輪複核後刪除問句，三處標註本則推導無條文依據句（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/foundation-design/references/dimension-product-notes.md  # from 0.1.0-W3-410（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/references/examples.md  # from 0.1.0-W3-410（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/CHANGELOG.md  # from 0.1.0-W3-410（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/SKILL.md  # from 0.1.0-W3-410（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三處逐一判定可引或需新判準並記錄  # from 0.1.0-W3-410（來源已完成，結論見 Solution）
