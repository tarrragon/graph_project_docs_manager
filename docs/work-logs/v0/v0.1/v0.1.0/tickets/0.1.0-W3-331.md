---
id: 0.1.0-W3-331
title: 寫入 318 的 C1 群 15 項與 326 的推送形態至待裁列
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-318
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-318 判定 C1 群 15 項須寫入待裁列；0.1.0-W3-326 的推送形態一列亦由 PM 裁定併入本步
  final_decision: 一票寫入 16 列，不分票
  rationale: 16 列同屬一次寫入動作與同一批載體；分票會使本地 state 的 delta 分兩次推送，且 326 那一列本來就是 PM 在本步併入的
who:
  current: framework-issue-curator
  history: {}
what: 寫入 318 的 C1 群 15 項與 326 的推送形態至待裁列
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/references/escalation-flow.md
why: '**`0.1.0-W3-318` 的 C1 群（條文層須裁決的 15 項）目前只存在於該票的 Solution。** 那是一份已完成的分析票，不會在任何 session 啟動時出現在任何人眼前——與它要治的問題同形。


  **`0.1.0-W3-326` 另有一列需併入**：推送形態的實作標的是 `.claude/` 框架通用資產，其載體應為待辦列一列，而該票被禁止對 canonical issue 寫入，故以豁免宣告記錄。PM 裁定併入本步，不另建票。


  **前置皆已就緒**：載體形態由 `0.1.0-W3-300`／`304` 定為「待辦與來源」列（`階段=待條件`／`狀態=待裁票`）；觸發器由 `0.1.0-W3-326` 裁為 ack-based delta，並由 `0.1.0-W3-329` 實作（SessionStart 只讀本地 state，0.210 秒，PM 實測）；owner 由 `0.1.0-W3-324` 改為自行推導，**不需手動傳 `--owner`**。


  **預期觀測（非缺陷）**：本次寫入後，本地 state 將首次持有真實列，**下一次 session 啟動的 delta 推送會印出這些列**。此前 state 僅含一列 smoke test 殘留（由 `0.1.0-W3-330` 清理）。**若寫入後 delta 未印出，那才是缺陷**——本次寫入同時是 `0.1.0-W3-329` 那條通道的首次真實驗證（其測試全為合成資料）。


  **不做則後果**：15 項待裁事項停在一張已完成票的 Solution 裡；`0.1.0-W3-326` 的推送形態無 canonical 記錄，其他 consumer 無從得知本 consumer 已做過該裁決。'
how:
  task_type: Documentation
  strategy: '**逐列寫入，不改寫判定。** 先讀 `0.1.0-W3-318` 的 Solution（C1 群 15 項與其層級判準）與 `0.1.0-W3-326` 的 Solution（推送形態三項定案）。


    1. **落點逐項判定**：15 項的主題不必相同，落點依 `0.1.0-W3-321` 的落點表與 `comment-as-section-protocol.md`〈三種關係處置〉決定；**不得為求方便全部塞進同一張 issue**。推送形態那一列的落點依其主題（待裁列機制）判定。

    2. **載體為「待辦與來源」列**，`階段=待條件`、`狀態=待裁票`（`0.1.0-W3-300`／`304` 定；`validate_todo_table` 會拒絕非列舉值）。**每列須可單獨被裁決**——一列承載兩個決定即為合併帶過。

    3. **owner 自行推導，不得手動傳 `--owner`**（`0.1.0-W3-324`）。若 `add` 因任何原因失敗，**停手回報，不得捏造 owner 或改走 observe 充數**——降級 observe 會使這些列不進 body 索引，即本次寫入要解決的那個問題。

    4. **寫入後驗證通道**：確認本地 state（`.claude/state/framework-issue-todo-pending.json`）已含新列，且 `show` 的索引一致。**不必手動跑 SessionStart hook**——下次啟動即為真實驗證。

    5. **既有 owner 為他方 session 的區段不得 `update`**（`#95` 上有三則 `graph-project-docs-manager-66`）。


    **不改任何 `.claude/` 資產、不改 CLI 或 hook、不跑 `sync-push`。**'
acceptance:
- '[x] 15 項逐列寫入，落點逐項判定且未為求方便集中於單一 issue'
- '[x] 推送形態一列已寫入，落點依其主題判定'
- '[x] 每列可單獨被裁決，無一列承載兩個決定'
- '[x] owner 為自行推導，未手動傳 --owner；未降級 observe 充數'
- '[x] 本地 state 已含新列，show 索引一致，並記錄列數'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T08:25:07'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

寫入 318 的 C1 群 15 項與 326 的推送形態至待裁列

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
### 寫入結果（16 列，兩張 issue）

| 落點 | 區段（本次 `add` 建立） | comment | 列數 |
|------|------------------------|---------|------|
| `tarrragon/claude#81` | 待辦與來源（graph-project-docs-manager：升級判準與協定定義的條文層待裁項） | issuecomment-5657443955 | 13 |
| `tarrragon/claude#95` | 待辦與來源（graph-project-docs-manager：收束配方、check 主警訊與待裁列推送） | issuecomment-5657445247 | 3 |

owner 兩則皆為 `graph-project-docs-manager-dcead428`，由 CLI 自行推導（未傳 `--owner`）；兩次 `add` 皆 rc=0，未降級 `observe`。

### C1 群 15 項的枚舉依據（不改寫 `0.1.0-W3-318` 的判定）

318 第三節共出現 17 個 C1 標籤，小計句為「C1 十五項」。差額兩項是該票自身標明的同址項：`構造一次生`＝`N3`（票面逐字「同 N3」）、`構造一`＝`F5`（票面逐字「同址／見 F5」）。折疊後為 15 個相異決定，與小計一致。第四節的修補甲＝`F3` 的修法、修補乙屬 `F5`／`N3` 同一位址，均已含在 15 項內，未另立列。

### 逐項落點判定（處置：本票以 `add` 寫入一列）

| 項 | 落點 | 判定理由（位址） |
|----|------|----------------|
| F3 | #81 | 修法位址為升級流程三問的第二問，屬工作流定義層；#81 已有同層先例（`0.1.0-W3-289` 的「工作流定義未規範修法形狀的定案時點」觀測落於此） |
| F4 | #81 | 第一問「範圍已量」的判準形狀，同上 |
| F5 | #81 | 擴散面二軸表（免答券來源），同上 |
| F6 | #81 | 升級流程的票類別適用性，同上 |
| G1 | #81 | 待裁列入表判準，位址為 escalation-flow 第二檔 |
| G2 前半 | #81 | 待裁列裁決者，同上。不落 #56：#56 的「驗收爭議無裁決判準」對象是 acceptance 爭議與 `.claude/rules/` 條文，與待裁列機制不同層，判為引用非落點 |
| 1-1 | #81 | 三問作用單位，同上 |
| 2-3 | #81 | 位址為 `comment-as-section-protocol.md`〈三種關係處置〉，#81 即該協定層 issue |
| N2 | #81 | 擴散面機械訊號的方向性，同 F5 位址 |
| N3 | #81 | 四問與擴散面的次序，同 F5 位址；與 F5 同批裁（列內已標） |
| 構造二 | #81 | 第三問的母體邊界 |
| 構造三 | #81 | 第二問的可充數觀測形態。不落 #105：#105 為「檢查量的軸與缺陷所在的軸不同」的模式 issue，依 `0.1.0-W3-321` 對該 issue 的處置慣例為 `observe`（實例），待裁的條文決定不在其射程 |
| 修法時點分離的證據狀態（W3-296 型） | #81 | 該節所需證據狀態的定義 |
| F2 | #95 | 位址為 `ticket-intake.md`〈步驟五〉後段，「ticket 收束配方」逐字在 #95 標題內 |
| R5／S5 | #95 | `check` 主警訊的訊號定義；本 consumer 在 #95 已擁有「當前結論（graph-project-docs-manager：check 主警訊無 owner 存活性維度）」，本列為其一般形態，射程重疊故同 issue |
| 326 推送形態 | #95 | 實作標的為 skill 的 `todo` 子命令與 SessionStart hook，屬 #95 的 CLI 與實作層。不落 #97（hook 執行前提不成立與正常執行同形）：該 issue 涵蓋主通道的靜默失效樣態，屬引用非落點 |

未集中於單一 issue 的證據：13／3 分列兩張 issue，且每一列的理由是位址歸屬而非批次方便；#56／#105／#97 三處候選逐一判為引用並記錄理由。

### 查重與關係判定

`dedup` 兩組：`升級判準 三問 擴散面 待裁列`（48 則命中）、`待裁列 推送 SessionStart 通報`（同量級命中）。兩組命中多數為聯集切詞假陽性（「命中位置：（無）」），逐一讀過有實際命中位置者：#81（body 與 comments 皆中，判為落點）、#95（comments 中，判為落點）、#56／#52／#53／#92／#54／#55／#58／#77／#84／#96／#70（判為引用，機制或對象不共用，不附加）。全量標題掃描未見任何 issue 以「待裁列無推送通道」或「升級判準條文缺口」為題，故無新開需求，全部併入既有 issue。

### 階段與狀態的填法

- 15 列 C1（含 F2／R5／S5）：`階段=待條件`、`狀態=待裁票`。待條件的等待對象逐列寫在「做什麼」欄（註一＝第四軸可行性判定；註二＝累積期真實案例的觀測），符合〈步驟四〉的待條件進入門檻。
- 326 推送形態列：`階段=可立即執行`、`狀態=待裁票`。理由：形態三項已由 `0.1.0-W3-326` 定死且本 consumer 已有實作（`0.1.0-W3-329`），指不出等待對象；依〈步驟四〉「指不出對象就不是待條件」須改標可立即執行。此為填欄判定，非改寫 326 的裁定。
- `acceptance 條數` 與 `優先級` 全列為 `—`：尚無實作票可計條數，來源票未裁定優先級，不代為指定（`—` 為該節明示的補值方式）。

### 驗證

| 項目 | 結果 |
|------|------|
| `validate_todo_table`（寫入前本地跑） | 兩份內容皆通過，抽出 13／3 列 |
| `show 81` | 索引 6 筆，新區段在索引內 |
| `show 95` | 索引 8 筆，新區段在索引內 |
| `check 81` | 警訊 C 索引一致（6 筆）；警訊 B 對 flutter-balance-99 的「當前結論（讀者入口）」觸發——該區段 owner 為他方且落後的觀測全部早於本次寫入（最新者 2026-09-11），非本次寫入造成，依禁令未 `update`，僅在此記錄 |
| `check 95` | 三則「當前結論」系列主警訊全未觸發；警訊 C 索引一致（8 筆） |
| 本地 state | `framework-issue-todo-pending.json` 共 17 列：#81／13 列 加 #95／3 列（owner 皆 `graph-project-docs-manager-dcead428`）加 1 列 smoke 殘留（owner `test-session-1a2b3c4d`，屬 `0.1.0-W3-330` 射程，本票未動） |
| `todo --status 待裁票` | 16 列全數出現在聚合輸出（#81 13 列、#95 3 列） |
| 專案檔變更 | 僅本票 md（`git status` 核對） |

預期觀測（非缺陷）：本次寫入後本地 state 首次持有真實列，下一次 session 啟動的 delta 會印出這 16 列。未手動觸發 SessionStart hook（依派發指示）。

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-14
**Executing Agent**: framework-issue-curator
**Review Status**: pending

### 變更摘要

無專案檔案內容變更（除本票 md）。變更發生在 canonical repo 的兩則新 comment 與一份本地 state：

| 目標 | 動作 | 內容 |
|------|------|------|
| `tarrragon/claude#81` | `add` 新區段「待辦與來源（graph-project-docs-manager：升級判準與協定定義的條文層待裁項）」（issuecomment-5657443955），body 區段索引同步回填一列 | `0.1.0-W3-318` C1 群 13 列（`階段=待條件`／`狀態=待裁票`），含註一等待對象定義 |
| `tarrragon/claude#95` | `add` 新區段「待辦與來源（graph-project-docs-manager：收束配方、check 主警訊與待裁列推送）」（issuecomment-5657445247），body 索引同步回填一列 | C1 群 2 列（F2、R5／S5，`待條件`／`待裁票`）加 `0.1.0-W3-326` 推送形態 1 列（`可立即執行`／`待裁票`） |
| `.claude/state/framework-issue-todo-pending.json` | CLI 寫入端自動同步（`+13`／`+3`） | 由 1 列 smoke 殘留增至 17 列 |

未動：兩張 issue 的 body 正文、他方 owner 的任何區段（#95 上三則 `graph-project-docs-manager-66` 與 #81 上五則 flutter-balance 區段）、`.claude/` 資產、CLI 與 hook；未跑 `sync-push`；未手動傳 `--owner`；未降級 `observe`。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-318 | chars: 1121 -->

### Task Reference
- 0.1.0-W3-318 what: 統整 第二輪三個 frame 的結果，判定剩餘缺口該由哪一層處理（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-318 why: **第二輪三個 frame 的結果不收斂，而那是設計上要的**——第一輪三張同 agent 定義、互相印證，結果其中一項被證實是方法共振。本輪三個 frame 各說各話，每一項因此各自站得住。

**三者的結論並列**：

- **`0.1.0-W3-306`（回歸）**：22 項既有缺口 → 6 消失／2 位移／8 換形態／6 未變。**消失的集中在「條文沒說」，未變的集中在「純條文層改不到」**——章節順序、`check` 的 7 天觸發器（六張 issue 實跑主警訊全未觸發）、`dedup` 命中規模（複測數字與第一輪完全相同：規則七 20／登記表 15／共用 30）。另有新缺口 N2–N5。
- **`0.1.0-W3-307`（對抗）**：條文**可被完全合規地穿過**，三個構造成立。共用機制是二軸表第 2 列——`should_exclude()` 對定義本體一律 False，但那些位置都有機械訊號，故一律判進第一檔。**第一檔不是恆空，是幾乎恆滿**，第二檔因此幾乎不可達。四項失敗嘗試反證條文的其他部分穩健。
- **`0.1.0-W3-308`（自我適用）**：23 張票僅 3 張寫定修法，唯一待議的 1 張非本 session 所建。**但該票同時指出本樣本無鑑別力**——條文落地後的目標人群僅 2 張，且落地前作者已實踐同一原則，**條文固化的是已有行為**。

**由此得到本票要判的問題**：第一輪統整已做過一次條文層修訂並落地（`dd24bce4`）。**第二輪若再做一次同層修訂，等於用同一個工具對它改不到的東西再試一次。** 306 的「未變」分類、307 的「幾乎不可達」、308 的「樣本無鑑別力」三者，從三個方向指向同一件事：**條文層的邊際效益已經到了某個位置**，而那個位置在哪、下一步該由哪一層承擔，是本票要回答的。

**不做則後果**：第三輪重複第二輪的形狀，或在沒有判準的情況下跳到 CLI 層（候選 B），而該候選的提示強度與誤報處置至今未定。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/ticket/ticket_system/commands/create.py  # from 0.1.0-W3-318（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三份產出的每一項發現已標到層級，標註附可被推翻的依據  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
- [ref] [x] 307 的兩處最小修補已依現行三問評估證據狀態，據以決定可否當下寫定  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
- [ref] [x] 候選 B 的前置是否已備已判定，含 306 未變清單是否構成所需證據  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
- [ref] [x] 308 的樣本限制已帶進結論，未以自身合規支持條文有效  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
- [ref] [x] 是否需要第三輪已判定；需要者指明 frame 與理由，不需要者說明停止判準  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
