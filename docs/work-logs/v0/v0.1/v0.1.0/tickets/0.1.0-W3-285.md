---
id: 0.1.0-W3-285
title: 判定 hook_logging 的 project_root 漏接形態是否升級為 framework issue
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-273
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 平行 session 於 0.1.0-W3-273 收尾回報中交還的一項判斷，本端未做任何動作
  final_decision: 另立 ANA 票承接升級判定，不併入 0.1.0-W3-284
  rationale: 284 修的是本專案四個呼叫點的接線，屬具體修復；本票判的是該形態是否為框架問題，兩者不同層。且跨 session 訊息不是載體，不落票即消失
who:
  current: framework-issue-curator
  history: {}
what: 判定 hook_logging 的 project_root 漏接形態是否升級為 framework issue
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/lib/hook_logging.py
why: '平行 session 於 `0.1.0-W3-273` 收尾時交還的一項判斷，**目前只存在於一則跨 session 訊息——訊息不是載體**。


  **已量到的成本**：`.claude/lib/hook_logging.py` 的 `project_root` 參數 docstring 早已逐字寫明這個失效模式，該參數即為此而加；兩個呼叫點（`track_dashboard.py::_get_handoff_gc_logger` / `track_dispatch_check.py::_get_prune_logger`）照樣漏接，造成 linked worktree 內清理動作寫主倉庫、日誌落 worktree 自身的 hook-logs 目錄，測試在隔離目錄找不到檔案。Phase 4 第二視角另查出四個 hook 今日處於同型錯配（經 `handoff_utils` / `worklog_appender` 傳遞），四處皆無測試斷言日誌落點、無紅燈無 stderr 差異，可無限期潛伏。


  **為何需要獨立判定**：`0.1.0-W3-284` 承接的是本專案 ticket skill 四個呼叫點的接線，屬具體修復；本票判定的是「參數存在且 docstring 已寫明失效模式，呼叫點仍漏接」這個形態是否構成框架問題——兩者不同層，修掉 284 不會回答本題，兩票之間**無前置依賴**。


  **不做則後果**：該形態的根源若確在會 sync 至所有 consumer 的共用資產，本地修會被下次 sync-pull 以上游舊版覆蓋且無 canonical 記錄；其他 consumer 專案的同型漏接無從被發現。'
how:
  task_type: Analysis
  strategy: '依 `.claude/skills/framework-issue/SKILL.md` 的升級流程執行，**不預設答案**：


    1. **先跑 dedup**，記錄關係判定（重複／切分／引用）。CLAUDE.md 的免徵詢授權以此為生效條件之一，未跑不得 init/observe。

    2. **逐條套兩條 AND 判準**（抽象可攜 + 根源在 `.claude/` 通用資產），**逐條在票面留下套用記錄**，不憑印象。特別查證：`hook_logging.py` 是否確在 sync 範圍內、`setup_hook_logging` 的 `project_root` 預設回退行為是否為所有 consumer 共用。

    3. 判定升級者 init 新區段或 observe 至既有 issue；判定不升級者於 Solution 附**逐條**理由。


    **本票不改 `.claude/` 通用資產本體**，亦不執行 sync-push（跨專案動作，不在授權內）。'
acceptance:
- '[ ] dedup 已跑，關係判定（重複／切分／引用）有記錄'
- '[ ] 兩條 AND 判準已逐條套用並在票面留下記錄，非憑印象'
- '[ ] 判定為升級者，已 init 新區段或 observe 至既有 issue，票面附 issue 連結'
- '[ ] 判定為不升級者，Solution 逐條附兩條 AND 判準各自不成立的理由'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

判定 hook_logging 的 project_root 漏接形態是否升級為 framework issue

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-273 | chars: 1555 -->

### Task Reference
- 0.1.0-W3-273 what: 修復 三項既存紅燈：hook-logs 檔案未產生使 dashboard GC 與 dispatch-check 的日誌斷言失敗（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-273 why: `ticket` skill 全套件現況 `1721 passed / 1 skipped / 3 failed`，違反品質基線規則 1（測試通過率 100%）。

**三項**：`test_dashboard_auto_gc_logging.py` 兩項、`test_track_dispatch_check.py::TestPruneFlag::test_prune_writes_hook_log_via_real_logger`。共同徵狀為 `.claude/hook-logs/` 下的預期檔案未產生。

**已確認為既存而非新引入**：`0.1.0-W3-271` 與 `0.1.0-W3-272` 兩個實作代理人在各自隔離的 worktree 中獨立撞到同三項，且各自以還原變更後重跑（一方用修改前的檔案版本、另一方用 `git stash`）確認 baseline 同樣紅。兩次獨立驗證，非單一觀測。

**為何獨立成票**：三項與那兩張票的改動範圍無交集（`git_ops.py`／`topic_inference.py`／`create.py`），把它們併進任一張會使該票的驗收混入無關的紅燈，且兩張票的代理人都正確地沒有擅自修範圍外的檔案。

**成因未判定，不可假設三項同源**：「檔案未產生」有三種處置相反的成因——功能真的沒寫入、測試斷言的路徑與實際落點不同、或測試假設了一個非必然的副作用。三項徵狀相同不表示成因相同；本票要求逐項確認實際落點再判斷。

**附帶一項環境落差**：`uv run --project .claude/hooks pytest` 在該 venv 缺 `filelock`，collection 即 ImportError（兩個代理人皆撞到，其中一個因此 64 個 collection error）。`filelock>=3.12` 只在 ticket skill 自身的 pyproject 宣告。派發 prompt 若沿用前者會使代理人先撞一次環境錯誤，須在 SKILL.md 明寫測試指令應用哪個 project。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/ticket/ticket_system/tests/test_dashboard_auto_gc_logging.py  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- .claude/skills/ticket/ticket_system/tests/test_track_dispatch_check.py  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- .claude/skills/ticket/ticket_system/commands/track_dashboard.py  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- .claude/skills/ticket/ticket_system/commands/track_dispatch_check.py  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- .claude/skills/ticket/references/architecture.md  # from 0.1.0-W3-273（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三項紅燈的成因逐項判定：測試假設錯誤、日誌路徑漂移、或功能實際失效，三者處置不同不可混為一談  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- [ref] [x] 若為功能失效則修功能；若為測試假設錯誤則修測試並在測試內註明日誌落點的來源  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- [ref] [x] ticket skill 全套件回到 0 failed（現況 1721 passed / 1 skipped / 3 failed）  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
- [ref] [x] 查證 uv run --project .claude/hooks 缺 filelock 是否為預期（ticket skill 自身 pyproject 才宣告），並在 SKILL.md 明寫測試指令應用哪個 project  # from 0.1.0-W3-273（來源已完成，結論見 Solution）
