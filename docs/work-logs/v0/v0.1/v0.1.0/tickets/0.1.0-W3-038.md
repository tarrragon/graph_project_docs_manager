---
id: 0.1.0-W3-038
title: 拆分 skill-sync push 的 --force 雙重語意
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-039
source_ticket: 0.1.0-W3-032
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: issue 85 第 4 項，balance 側確認無對應票
  final_decision: 本側持有，成 IMP 票
  rationale: 跨 consumer 對照確認該側只有 --force 留痕位置的票（已完成），語意拆分無人持有
who:
  current: thyme-python-developer
  history: {}
what: 把「跳過互動確認」與「旁路 portability 閘門」拆為兩個旗標，或至少在旁路閘門時於 stdout 明示
when: 本票建立後可執行
where:
  layer: framework
  files:
  - .claude/skills/skill-sync/skill_sync/cli.py::write
  - .claude/skills/skill-sync/tests/test_cli.py::write
  - .claude/skills/skill-sync/SKILL.md::write
why: --help 只寫 Apply changes without confirmation，而 SKILL.md 另載明 --force 同時旁路 portability 閘門（違規清單寫入 hook-logs 的 jsonl）。一次實測中代理人為跳過互動確認而加 --force，未造成損害純粹因為目標 skill 未宣告 metadata.portable true——未宣告者本就不會 abort，是條件湊巧不成立而非旗標安全。同一指令換一支已宣告 portable 的 skill，我只是想跳過確認會靜默變成我旁路了違規檢查，而唯一的痕跡在使用者不會去看的 jsonl。經 flutter_balance 專案交叉檢視確認該側無對應票，本項由本側持有
how:
  task_type: Implementation
  strategy: 先寫紅燈測試（旁路閘門時 stdout 應有明示）-> 二擇一：拆為 --yes 與 --force-portability 兩旗標（--force 保留為兩者兼具以維持相容），或維持單旗標但旁路時於 stdout 印出違規摘要 -> 同步 --help 與 SKILL.md 文字
acceptance:
- '[x] 旁路 portability 閘門時，違規摘要出現在 stdout 而非僅寫入 jsonl'
- '[x] --help 文字涵蓋該旗標的全部語意，不只跳過確認'
- '[x] 既有 test_cli.py 全數通過，新增測試覆蓋旁路時的 stdout 輸出'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-07T11:30:16'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

把「跳過互動確認」與「旁路 portability 閘門」拆為兩個旗標，或至少在旁路閘門時於 stdout 明示

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-07 11:29] 同檔的 _stale_version_warning（68fbfcc2 剛加）刻意未接 --force，改動時別把它捲進來。二擇一方案見票面 how；若選拆旗標，--force 須保留為兩者兼具以維持既有呼叫相容。

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 選擇方案

採票面 how 的二擇一之「維持單旗標但旁路時於 stdout 明示」（非拆分為兩旗標）：
--force 保留原有語意（跳過確認 + 旁路 portability 閘門），改善點是讓兩種效果
都可被觀察到，而非改變其行為邊界。理由：既有呼叫端（cmd_push、既有腳本、
使用者肌肉記憶）全部維持相容，且驗收條件本身用「該旗標」單數措辭，指向此路徑。

### 修改內容

`.claude/skills/skill-sync/skill_sync/cli.py`：

1. 新增 `_format_violation_lines(violations)` 共用格式化 helper（前 20 筆 +
   `... and N more` 截斷邏輯），供既有 stderr 完整報告與新增的 stdout 摘要
   共用，避免兩處各自維護一份會漂移的格式。
2. `_report_portability` 的 `if force:` 分支新增一段 stdout 印出（沿用同一
   `_format_violation_lines`），內容為「--force bypassed N consumer-specific
   reference(s) in '<name>'」+ 違規清單。既有 stderr 完整報告與 jsonl
   落地（`_write_portability_force_log`）行為不變。
3. `push_parser` 的 `--force` `--help` 文字改為同時說明「跳過確認」與「旁路
   portability 閘門（違規仍會印到 stdout/stderr 並落地記錄）」兩種效果。
   `pull_parser` 的 `--force` 不變（pull 無 portability 閘門）。

`.claude/skills/skill-sync/SKILL.md`：同步更新 push 列與 portability 閘門說明
段，加入 stdout 摘要與 --help 文字涵蓋範圍的敘述。

`.claude/skills/skill-sync/CHANGELOG.md`：新增 1.14.0 版本紀錄。

### 未選擇方案

拆分為 `--yes`（跳過確認）與 `--force-portability`（旁路閘門）兩個獨立旗標，
`--force` 保留為兩者兼具以維持相容。未採用原因：驗收條件措辭指向單旗標路徑，
且拆分旗標對現有呼叫端零風險但引入兩個新旗標需同步維護三種呼叫組合
（`--force` / `--yes` / `--force-portability`）的說明與測試，對本票要解決的
「可見性不足」問題無額外幫助。若未來需要更嚴格分權（如：允許自動化跳過確認
但不允許旁路 portability），可另開票採此方案，屆時 `--force` 相容性約束
（票面派發note）已在此路徑天然滿足（未變更旗標定義）。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-032 | chars: 316 -->

### Task Reference
- 0.1.0-W3-032 what: 刪除表格第 5 列（daemon-rooted 寫入工具洩漏 的第二次出現），修好後重推發佈庫（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-032 why: 該表第 2 列與第 5 列的議題欄與對策欄逐字相同，第 5 列的事實欄只寫「同上一列」。三欄結構（議題／事實／對策）在重複列上退化為指回自身，讀者無從判斷第二次出現是筆誤還是另一種洩漏路徑。該重複已隨 W3-025 的 push 傳播到發佈庫與所有 consumer（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/worktree/SKILL.md  # from 0.1.0-W3-032（來源已完成，結論見 Solution）
- .claude/skills/worktree/CHANGELOG.md  # from 0.1.0-W3-032（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 表內 daemon-rooted 寫入工具洩漏 只出現一次，且無任何欄位寫「同上一列」  # from 0.1.0-W3-032（來源已完成，結論見 Solution）
- [ref] [x] 版號已 bump 且已推送至發佈庫  # from 0.1.0-W3-032（來源已完成，結論見 Solution）
