---
id: 0.1.0-W3-104
title: 補寫 layered-test-strategy 的角色化替身形態：依回饋三層列出各層所需角色
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-102
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-100
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 補寫 layered-test-strategy 的角色化替身形態：依回饋三層列出各層所需角色
when: 0.1.0-W3-102 定案三層歸屬後（角色分層須與該表一致）
where:
  layer: Framework Docs
  files:
  - .claude/skills/tdd/references/layered-test-strategy.md
why: '`0.1.0-W3-100` 的題二結論：現行替身只扮演「可用」一角，這是 `ARCH-GPD-001` 能在全套測試綠燈下發生的機制——替身讓服務的成功路徑恆為真，掛在該路徑上的回饋因此恆存在，測試驗證的是一個永遠不會失敗的世界。


  該票依用戶的三層裁示，把角色按層組織：


  | 層 | 所需替身角色 |

  |----|-------------|

  | 一、互動元件層 | **零角色**——第一層必須在完全沒有服務的情況下可測 |

  | 二、服務受理層 | acknowledged／processing／neverAcknowledges |

  | 三、event 效果層 | succeeds／fails／timesOut／throwsPlatformError |


  第一層的「零角色」是本票最容易被漏掉的一項：它看起來像是沒有工作要做，實際上它是判準問句（把服務拔掉還看得到反應嗎）的機械化實現，也是最容易寫的一類測試——正因為容易，更沒有人會特地為它建立形態。


  第二層的三個角色是新維度，`0.1.0-W3-100` 原分析未覆蓋——「已受理、處理中、尚未回應」需要一個**可停留的狀態**，而不只是「回傳假資料」。現行替身沒有這個能力。


  與既有〈分層之外的補位形態〉中「stub 盲區與語意級假後端」的關係（100 已判定）：**元件級特化延伸**，同根（只扮演可用的替身結構上盲區）不同尺度（語意級假後端是整合層，角色化替身是元件層），不互相取代。'
how:
  task_type: Documentation
  strategy: '在〈分層之外的補位形態〉節內，於既有「stub 盲區與語意級假後端」條目後補角色化替身的說明（不新增 H2 節，避免與 `0.1.0-W3-079` 建立的節標題錨點衝突——`doc-handoff.md` 已逐字引用該標題）。


    內容依 `ticket track full 0.1.0-W3-100` 的 Solution 修訂段（該票已完成），含：各層所需角色、第一層零角色的理由與其為何最容易被漏、第二層可停留狀態與「回傳假資料」的差別、與 stub 盲區形態的同根不同尺度關係。


    **角色命名**：100 給的是英文具名建構子（acknowledged 等）。條文屬框架資產會 sync 至不同語言的專案，命名慣例須寫成語言無關的角色語意描述，具體建構子名只作為示例並標明語言。


    **可攜性**：不寫本專案的類別名（`FakeScanNotifier` 等），以型別化描述取代。**不寫數字**：不以角色數命名或作跨檔錨點（`reference-stability-rules` 規則 10）。


    **成對檢查**：若新增指向 `test-object-catalogue.md` 或回饋契約的引用，兩端同時開啟確認措辭對應。


    依賴 `0.1.0-W3-102`：該票定案三層歸屬表；本票的層次劃分與命名須與其一致，不得各自表述。'
acceptance:
- '[x] 〈分層之外的補位形態〉節含角色化替身說明，逐層列出所需角色'
- '[x] 第一層零角色的理由與「為何最容易被漏」已寫明；第二層的可停留狀態與「回傳假資料」的差別已釐清'
- '[x] 角色以語言無關的語意描述定義，具體建構子名只作示例並標明語言；不含本專案類別名'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T16:08:06'
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補寫 layered-test-strategy 的角色化替身形態：依回饋三層列出各層所需角色

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-08 16:07] readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-08
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

變更摘要：

`.claude/skills/tdd/references/layered-test-strategy.md`〈分層之外的補位形態〉節，
於「stub 盲區與語意級假後端」條目後補「角色化替身（依回饋分層分工）」條目（未新增
H2/H3 節，`doc-handoff.md` 對該 H2 標題的錨點引用未受影響）：

- 三層歸屬表（層／所需角色）：一、互動元件層——零角色；二、服務受理層——已受理／
  處理中／永不受理；三、event 效果層——成功／失敗／逾時／拋出平台例外。層次命名與
  `0.1.0-W3-102` 落地的〈掛載點〉三層歸屬表逐字一致。
- 第一層零角色為何最容易被漏掉：看起來無工作可做、又是最容易寫的一類測試，正因為
  容易而沒人特地建立形態；並釐清「完整 provider 環境驗證元件有回饋」與「零角色驗證
  元件自身有回饋」判準不同。
- 第二層「可停留狀態」與「回傳假資料」的差別：已受理／處理中／永不受理要求替身能
  表達「已回應但未完成」或「持續回報但未完成」，現行替身建構當下即決定單一終局結果，
  無法表達「尚在途中」。
- 與 stub 盲區形態的關係：同根不同尺度的元件級特化延伸，非取代關係。
- 角色以行為定義、語言無關，具體建構子名 `Fake{Service}.acknowledged()` 等以 Dart
  示例標明語言，未寫本專案類別名。
- Version footer 新增 1.4.0 條目。

驗收：3/3 acceptance 已勾選。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-100,0.1.0-W3-102 | chars: 1981 -->

### Task Reference
- 0.1.0-W3-100 what: 設計 回饋契約的測試義務與替身的不可用角色，補測試設計缺口（來源已完成，結論見 Solution）
- 0.1.0-W3-102 what: 修正 回饋責任的三層歸屬：服務受理層的承擔者是服務不是元件（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-100 why: 用戶提問（2026-09-08）：互動回饋與例外收斂後的回饋要加入測試，這是不是測試設計中缺少的部分。**是，缺兩塊，而且兩塊互為前提。**

**缺口一：回饋契約不是可測對象。** 本 session 為元件契約新增了回饋契約欄（六類回饋事件），`0.1.0-W3-075` 定案的測試對象目錄有六個來源類別（畫面狀態轉換／使用案例行為場景／互動規則／元件契約／token 不變式／平台契約），其中「元件契約」類指向的是 SPEC-004 的測試契約欄，而該欄的內容是渲染正確性（全變體全狀態渲染不溢位、最長文案截斷）。**回饋契約新增之後，沒有任何測試對象承載它。** 契約欄位表多了一欄，測試側沒有對應的記載位置。

**缺口二：替身只會扮演可用。** `test/` 的替身（如 `FakeScanNotifier`）模擬的是「服務可用且回傳假資料」，不模擬「服務不可用」——拋例外、逾時、永不回應皆無角色。這正是 `ARCH-GPD-001` 能夠在 1159 個測試全綠下發生的機制：替身讓成功路徑恆為真，掛在該路徑上的回饋因此恆存在，測試驗證的是一個永遠不會失敗的世界。

兩塊互為前提：沒有「不可用」的替身，就寫不出「服務拔掉後仍有回饋」的斷言；沒有回饋作為可測對象，也沒有地方登記那條斷言該存在。

**斷言形態本身也未定**。「按下後在時間門檻內出現 pressed 視覺」涉及時間，而 `test-assertion-design-rules` 規則 D1 禁止主套件用絕對計時門檻作 pass-fail——回饋契約的時間門檻要怎麼斷言而不違反該規則，需要設計，不是照抄既有形態就有答案。（來源已完成，結論見 Solution）
- 0.1.0-W3-102 why: `0.1.0-W3-098` 落地的〈掛載點〉條文寫「按下確認與等待指示的觸發點是互動事件本身與『已送出、尚未回應』這個狀態」——把**兩層都歸給元件**。用戶隨後的裁示（2026-09-08）給出三層，且中間那層的承擔者是服務不是元件：

| 層 | 內容 | 承擔者 |
|----|------|--------|
| 一、互動元件層 | 互動當下的回饋：震動／音效／變形／變色／光環 | **元件自身**，與背後是否掛服務無關 |
| 二、服務受理層 | command 發送 event 給服務後，服務自己的處理回饋：toast、loading 動畫 | **服務** |
| 三、event 效果層 | 服務處理完之後才觸發 event 應該觸發的效果，無論成功失敗或其他 | 事件處理路徑 |

現行條文與此的差距有兩處：

1. **歸屬錯置**：「等待指示」（第二層）被寫成元件的義務。實際上 command 送出之後，是**服務**要對「我收到了、正在處理」負責。把它歸給元件，會讓服務側完全沒有回饋義務——服務可以什麼都不做而條文仍被滿足。

2. **第一層的無條件性未寫明**：條文的判準問句「把服務拔掉還看得到反應嗎」隱含了第一層必須存在，但沒有明說**每個可互動元件都必須有互動當下的回饋，沒有例外**。PM 在派發存量盤點票時因此把「互動不觸發跨層呼叫」誤讀為「判準自動成立」——純本地狀態切換的元件若按下時無任何表現，只有狀態改變後的結果，一樣是缺陷，而該誤讀在現行條文下讀得通。

`ARCH-GPD-001` 同樣只寫了兩層（回饋 vs 服務回應），未涵蓋服務受理層的獨立歸屬，須一併補。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/tdd/references/test-object-catalogue.md::read  # from 0.1.0-W3-100（來源已完成，結論見 Solution）
- .claude/skills/tdd/references/layered-test-strategy.md::read  # from 0.1.0-W3-100（來源已完成，結論見 Solution）
- .claude/rules/core/test-assertion-design-rules.md::read  # from 0.1.0-W3-100（來源已完成，結論見 Solution）
- test::read  # from 0.1.0-W3-100（來源已完成，結論見 Solution）
- .claude/methodologies/component-library-bidirectional-constraint-methodology.md  # from 0.1.0-W3-102（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] 題一結論：回饋契約在測試對象目錄中是新欄位或新來源類別，附與既有斷言來源的同源性論證  # from 0.1.0-W3-100（來源已完成，結論見 Solution）
- [ref] [x] 題二結論：替身角色集合（至少含不可用／逾時／永不回應／回傳失敗）各有判準與命名慣例，並釐清與既有 stub 盲區補位形態的關係  # from 0.1.0-W3-100（來源已完成，結論見 Solution）
- [ref] [x] 題三結論：時間門檻的斷言形態不違反 test-assertion-design-rules 規則 D1，並確認既有假時鐘慣例是否足夠  # from 0.1.0-W3-100（來源已完成，結論見 Solution）
- [ref] [x] 落地載體依結論以 add-spawn-request 登記，complete 前逐項 resolve 為終態  # from 0.1.0-W3-100（來源已完成，結論見 Solution）
