---
id: 0.1.0-W3-309
title: 判定 bare-commit-guard 未涵蓋規則七四項禁令中的 -i / --include 的影響範圍
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-307
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-307 對抗測試的附帶實測；PM 以 grep 複核 include 於該檔出現 0 次
  final_decision: 建票記錄現象與證據，修法形狀不在本票決定
  rationale: 依 escalation-flow 修法時點分離；且本案的三問答案為「消費端未觀測、反例未試、範圍僅量了一半」，正是不得當下寫定修法的情形
who:
  current: framework-issue-curator
  history: {}
what: 判定 bare-commit-guard 未涵蓋規則七四項禁令中的 -i / --include 的影響範圍
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/hooks/bare-commit-guard-hook.py
why: '**現象**：`.claude/rules/core/bash-tool-usage-rules.md` 規則七禁用四項——`-- <path>` pathspec、`--only`（`-o`）、`-i`／`--include`、以及廣域 staging。`.claude/hooks/bare-commit-guard-hook.py` 的偵測涵蓋前兩者與廣域 staging，**對 `-i`／`--include` 無任何偵測**。


  **查證方式**（PM 獨立複核，非採信轉述）：`grep -n ''include'' .claude/hooks/bare-commit-guard-hook.py` 回傳 **0 行**；該檔註解逐字列出的豁免與攔截對象為「`-- pathspec` / `--only` / `-o`」，`-i`／`--include` 不在其中。規則七條文則明寫 `--include`（`-i`）「不丟棄 index，但把指定路徑併入現有 index 一併提交，會連同 index 中他人已 staged 的檔案一起送出」，且「並行環境（共用 working tree）三者皆禁用」。


  **影響面尚未量化**，本票要判的就是這件事。未知項至少三個：`-i` 在本專案與其他 consumer 的實際使用頻率（可能為零）；守衛放行 `-i` 時的實際後果規模；此缺口是刻意（例如判定 `-i` 風險較低）或遺漏。


  **本案同時是條文漏洞的活體實例**：依 `escalation-flow.md` 改版後的二軸表，`.claude/hooks/` 雖 `should_exclude()` 為 False，但該位置有機械訊號（`.claude/hooks/tests/`），落第一檔——**條文允許在本票當下寫定修法形狀**。而三問的實際答案是「消費端未觀測、反例未試、範圍只量了一半（量到守衛缺什麼，沒量到有多少人用 `-i`）」。`0.1.0-W3-307` 構造一的機制與此逐點相同。**本票刻意不寫定修法，並把這件事記錄下來作為該漏洞的實例。**'
how:
  task_type: Analysis
  strategy: '**本票不決定修法形狀**，產出為現象、證據、影響範圍、處置層級四項。


    1. **量使用面**：`-i`／`--include` 在本專案的 git 歷史與現行文件中的實際出現次數；若為零，影響面的判斷隨之改變——**先查有沒有人用，再談守衛該不該擋**。

    2. **判缺口性質**：查該 hook 的變更歷史與註解，判定 `-i` 未涵蓋是刻意取捨或遺漏。註解中「三種自然豁免」與「修正」段記錄了 pathspec 的決策轉折，`-i` 是否曾被考慮過，有跡可循。

    3. **判與規則七的不一致屬哪一側**：條文禁四項而守衛擋三項——是守衛該補，或條文該說明為何 `-i` 不在強制層射程。**兩個方向都要評估，不預設是守衛的錯。**

    4. **升級判定**：依 `framework-issue/SKILL.md` 流程，先跑 `dedup`，兩條 AND 判準逐條套用並留痕。


    不改 `.claude/` 資產、不對 canonical issue 寫入、不跑 `sync-push`。'
acceptance:
- '[ ] -i / --include 在本專案的實際使用次數已量，量測命令可複驗'
- '[ ] 缺口性質已判定（刻意取捨或遺漏），附變更歷史或註解依據'
- '[ ] 不一致屬哪一側已評估，兩個方向皆有評估記錄'
- '[ ] dedup 已跑，兩條 AND 判準已逐條套用並留痕'
- '[ ] Solution 未寫入修法形狀'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

判定 bare-commit-guard 未涵蓋規則七四項禁令中的 -i / --include 的影響範圍

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-307 | chars: 721 -->

### Task Reference
- 0.1.0-W3-307 what: 對抗測試 改版條文的漏洞：能否完全合規而仍寫定未驗證的修法（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-307 why: **第一輪三張測試全是正向使用 frame**：拿條文去處理真實 issue，記錄它在哪裡給不出答案。**沒有任何一張嘗試繞過它。**

**缺口與漏洞是不同的東西**：缺口是條文給不出答案；漏洞是條文給得出答案，而那個答案可以被構造成錯的。一個滿是缺口的條文至少會讓人卡住並求助；**一個有漏洞的條文會發出通行證**——使用者逐條照做、每一問都答「是」、產出一個未經驗證的修法，而且票面上與一個嚴謹的判定完全不可分辨。

**這個形態在本專案已有實證**：`163/164` 通過第三問是合法的（可重跑的主張），事後複驗為 `316/320`。條文當時沒有失效，**它正確地放行了一個錯的數字**。

**改版新增的兩個結構特別值得測**：「消費端」拆為執行條件／使用者群後多了一條綜合規則——**綜合規則是新的可操弄面**；兩檔分界改為 `should_exclude()` 加機械訊號的二軸表，而 `0.1.0-W3-301` 已量出定義本體全在 sync 範圍，**二軸表的實際分流結果是否如設計所想**未經檢驗。

**不做則後果**：條文以「已測過」的身分擴散，而測過的只有它答不出的那一半。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/framework-issue/references/escalation-flow.md  # from 0.1.0-W3-307（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 已嘗試構造通行證案例，成功者具體到可寫成一張真實的票（含三問答案）  # from 0.1.0-W3-307（來源已完成，結論見 Solution）
- [ref] [x] 構造失敗者已說明嘗試方向與各自被哪一句擋下  # from 0.1.0-W3-307（來源已完成，結論見 Solution）
- [ref] [x] 三個重點攻擊面（綜合規則／二軸表另一軸／反例最低標準）各有實際嘗試記錄  # from 0.1.0-W3-307（來源已完成，結論見 Solution）
- [ref] [x] 每個成功構造附條文中最小的修補位置，且未改任何檔案  # from 0.1.0-W3-307（來源已完成，結論見 Solution）

