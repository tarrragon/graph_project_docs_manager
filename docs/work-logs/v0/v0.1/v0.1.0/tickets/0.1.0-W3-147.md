---
id: 0.1.0-W3-147
title: 提取 workspace value types 至 workspace_types.dart
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-133
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-133 acceptance 4：結論為維持現狀時，309 行超標問題須一併裁定處置，不得無承接者
  final_decision: IMP 提取 value types 至獨立檔案
  rationale: 裁定選 A 不改架構，但檔案行數問題不隨結論消失；提取 value types 是不動架構的最小處置，且分離的是「狀態建模」與「外部系統互動」兩個責任，非機械切割
who:
  current: parsley-flutter-developer
  history: {}
what: 提取 workspace value types 至 workspace_types.dart
when: 0.1.0-W3-133 裁定結論 A 之後（該票 acceptance 4 要求結論 A 時 309 行問題須有承接者）
where:
  layer: Domain
  files:
  - lib/workspace/workspace_repository.dart
  - lib/workspace/workspace_types.dart
why: '`lib/workspace/workspace_repository.dart` 已達 309 行，超過 `.claude/rules/core/cognitive-load.md` 的 300 行門檻（該事實由 `0.1.0-W3-121` Phase 4b 記錄）。


  `0.1.0-W3-133` 的 acceptance 第 4 條明訂：若該票結論為「維持現狀」，須一併裁定行數處置或明寫豁免理由，**不得因結論 A 而使此項無承接者**。該票已裁定為 A 並登記 SR-3，本票即其承接者。


  **為何提取 value types 而非其他切法**：該檔目前承載兩個責任——狀態建模（`ChooseFolderResult` 等 value types，第 7 至 76 行）與外部系統互動（三個 port、其 private default adapter、`WorkspaceRepository` 本體）。前者是純資料形狀、無外部依賴；後者是接縫。兩者之間沒有共用的私有狀態，提取不需改動任何呼叫端的邏輯，只需調整 import。


  `0.1.0-W3-133` 同時裁定**不建立 `ports/` 與 `adapters/` 目錄約定**——本票是在該裁定之下的最小處置，不引入目錄結構、不把 private adapter 移出（移出需改為 public，破壞封裝，該票已否決此方向）。'
how:
  task_type: Implementation
  strategy: '先實查目前行數與 value types 的實際行段——本票 why 段記的 309 行與第 7 至 76 行來自 `0.1.0-W3-133` 的裁定當下，該檔可能已被其他票改動，以動手當下的實查為準。


    提取後主檔應降至 300 行以下；若實查後發現提取 value types 不足以降到門檻以下，**不要為湊行數多切一刀**——停手於 ticket 的 NeedsContext 記錄實際行數與差額，交 PM 判斷是否另需處置。門檻的目的是控制認知負擔，不是行數本身。


    `lib/workspace/workspace_types.dart` 為新檔，需與既有檔案的 library 註解慣例一致。


    三個 port 介面、其 private `_Default*` adapter、`WorkspaceRepository` 本體全部留在原檔——`0.1.0-W3-133` 已裁定 port 與其 private default adapter 同檔為本專案的接縫形態，提取它們會與該裁定衝突。


    既有測試不得修改斷言，只允許調整 import。若提取導致任何測試需要改斷言，那表示提取改變了行為，停手回報。'
acceptance:
- '[ ] value types 已移至 lib/workspace/workspace_types.dart，主檔降至 300 行以下（實測行數記入 Test Results）'
- '[ ] 三個 port 介面與其 private _Default* adapter 全部留在原檔，未移出'
- '[ ] 既有測試僅調整 import，零斷言改動；全套件通過（指令與通過數記入 Test Results）'
- '[ ] 若提取後仍未低於 300 行，已停手並於 NeedsContext 記錄實際行數與差額，未為湊行數多切'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

提取 workspace value types 至 workspace_types.dart

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-133 | chars: 1612 -->

### Task Reference
- 0.1.0-W3-133 what: 裁定 是否為 driven port 建立 adapter 層與 ports 目錄約定（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-133 why: Phase 4a 兩個視角對同一問題給出方向相反的意見，需要裁定。

**linux（Acceptable 偏低）主張補 adapter 層**：`WorkspaceRepository` 同時是 repository 又是三個 adapter。方法論〈Port 回饋契約〉自己寫了承擔者分工——呼叫發出歸呼叫端，受理與結果歸 **adapter**——但本專案沒有 adapter 層，repository 直接寫 `SharedPreferences.getInstance()`、`Directory(path).list()`。於是要讓 adapter 內部的「受理」時刻可觀測，唯一手段是往 repository 身上鑿洞：`WorkspacePreferencesHandle` 的存在理由只是 `getInstance()` 是兩段式 API，那是 SharedPreferences 的實作細節，卻被提升成 repository 的公開介面。它主張三個真 adapter 類別（各自獨立檔案）可讓接縫從 5 降到 3，可觀測性一條不減。

**Coupling 視角的相反證據**：實查 `WorkspacePreferencesPort`／`Handle` 與 `DirectoryPathPicker` 兩處**未發現型別洩漏**——`SharedPreferences` 與 `file_selector` 的型別沒有出現在任何介面簽章或呼叫端的具體型別比對中，呼叫端一律 generic `catch (e)`。以隔離效果衡量，這兩個接縫是乾淨的。

**Complexity 視角的第三種意見**：量測到單檔 19 個頂層宣告、267 行，但明確標註信心 55、未達回報門檻、**不建議據此拆檔**——理由是 `cognitive-load.md` 沒有「單檔型別數上限」條文，把任務拆分閾值（需追蹤概念數 > 7）類比套用到檔案內型別數缺乏明文依據；且五個 port 加四個 default adapter 是同一個 ports-adapters 圖式的必然展開，全檔僅一個 domain，未命中「單一檔案承載 domain 數 > 2」。

三者不是簡單的對錯：linux 談的是結構歸屬（實作細節該不該出現在 repository 介面上），Coupling 談的是洩漏事實（型別有沒有跑出來），Complexity 談的是有無明文閾值依據。**本票要裁定的是該用哪個標準。**

決定的外溢範圍：`0.1.0-W1-068`（ExternalOpener）、`0.1.0-W3-112`、`0.1.0-W1-014` 三個 driven port 都會照本形態實作。linux 的推估是複製後約 4 個 port × 8 個測試替身 ≈ 30 個 bespoke 類別，每個 port 各自發明接縫形態。（來源已完成，結論見 Solution）

### Related Files
- docs/tech-decisions.md::read  # from 0.1.0-W3-133（來源已完成，結論見 Solution）
- lib/workspace/workspace_repository.dart::read  # from 0.1.0-W3-133（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三問各有論證且引方法論原文，不憑印象  # from 0.1.0-W3-133（來源已完成，結論見 Solution）
- [ref] [x] 三個候選結論（維持現狀／建 adapter 層／部分）各有論證並擇一  # from 0.1.0-W3-133（來源已完成，結論見 Solution）
- [ref] [x] 裁定對 W1-068、W3-112、W1-014 三票的影響已明寫；若結論為 B 或 C，已登記 spawn request 交 PM  # from 0.1.0-W3-133（來源已完成，結論見 Solution）
- [ref] [x] workspace_repository.dart 已達 309 行（超過 cognitive-load.md 的 300 行門檻，0.1.0-W3-121 Phase 4b 記錄）——若本票結論為「維持現狀」，須一併裁定該檔行數的處置或明寫豁免理由，不得因結論 A 而使此項無承接者  # from 0.1.0-W3-133（來源已完成，結論見 Solution）

