---
id: 0.1.0-W3-263
title: 補正 仲裁器設計文件的兩處缺陷：Phase 2 對 released 等級的覆蓋缺口與 3a.3 虛擬碼的等級選擇條文
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-207
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-207
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-207 Phase 4a 建議於 4b 完成後登記兩項設計文件缺陷
  final_decision: 另立 DOC 票補正 ticket 內的設計記錄，不改程式碼與規格檔
  rationale: 修掉實作不會使設計文件的缺陷消失，下一個依這兩份文件工作的人會再犯一次
who:
  current: thyme-documentation-integrator
  history: {}
what: 補正 仲裁器設計文件的兩處缺陷：Phase 2 對 released 等級的覆蓋缺口與 3a.3 虛擬碼的等級選擇條文
when: 0.1.0-W3-207 Phase 4b 完成後（該階段會修 F-1，本票記錄它為何得以潛伏）
where:
  layer: Presentation
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-207.md
why: '`0.1.0-W3-207` Phase 4a 建議在 4b 完成後登記的兩項，**皆屬設計文件缺陷而非實作缺陷**——修掉實作不會使它們消失，下一個依這兩份文件工作的人會再犯一次。


  **一、Phase 2 對 `released` 事件等級的覆蓋缺口**——這是 F-1（背景發起的請求，其正常自願的釋放被記為 warning）**得以潛伏的成因**。


  測試對等級的斷言分布不均：`skipped`／`deferred`／`heldAndRequested`／`releasedNonHolder` 皆有明確的 `level` 斷言，且 `:169` 明確斷言 `spontaneous` 的 `accepted` **不得為 900**；唯獨 `released` 沒有任何等級斷言。**同一個請求的「接受」有斷言、「釋放」沒有**，兩條等級路徑從未被對起來看。


  **二、3a.3 虛擬碼 `RELEASE_HOLDER` 的等級與事件選擇條文**——**同時是 Phase 3b 自陳 bug (b) 與 F-1 的來源**。


  該處原寫 `原因 == expired ? expired : released`，照抄會使 T-3 紅燈（Phase 3b 依 Phase 2 測試判定為準改為依觸發路徑區分，Phase 4a 判定該裁量正確）；而等級部分則被實作沿用為 `_levelForOrigin(leaving.arrival)`，未被任何測試攔下。**同一段虛擬碼的兩個面向，一個被測試擋下、一個沒有。**'
how:
  task_type: Documentation
  strategy: '在 `0.1.0-W3-207` 的 Solution 補正兩處，**追加不改寫既有段落**（Phase 1／2／3a 為當時的設計記錄，保留原文）：


    1. **Phase 2 測試設計**之後補一節，載明等級斷言的覆蓋缺口與其後果（F-1 因此潛伏至 Phase 4a 才被發現），並寫明補救已由 Phase 4b 落地（`released` 對 `spontaneous` 持有者的等級斷言）。**一般化一句**：日誌等級屬可觀測性契約的一部分，事件有斷言而其配對事件沒有時，兩條路徑會各自漂移。

    2. **Phase 3a §3a.3** 之後補一節，載明 `RELEASE_HOLDER` 的兩個面向（事件選擇、等級選擇）各自的實際落地與判定：事件選擇經 Phase 3b 正確裁量（依觸發路徑而非 `reason` 值）、等級選擇被沿用且錯誤。


    **本票不改任何程式碼、不改任何規格檔**，只補正 ticket 內的設計記錄。


    **判斷是否升級**：兩項皆為本專案的設計文件缺陷，初判不符框架問題的「抽象可攜」判準（形態雖通用但實例綁本票）。完成後仍依 `.claude/skills/framework-issue/SKILL.md` 逐條套一次判準，判定不升級者於 Solution 附理由。'
acceptance:
- '[x] Phase 2 之後已補等級斷言覆蓋缺口一節，含後果與 Phase 4b 的補救，並有一般化陳述'
- '[x] 3a.3 之後已補一節，事件選擇與等級選擇兩面向各自的落地與判定分開記載'
- '[x] 兩處皆為追加，Phase 1／2／3a 既有段落未被改寫'
- '[x] 已逐條套過框架問題兩判準，判定不升級者附理由'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T09:39:31'
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補正 仲裁器設計文件的兩處缺陷：Phase 2 對 released 等級的覆蓋缺口與 3a.3 虛擬碼的等級選擇條文

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-11 09:38] readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-11
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

**變更摘要**：於 `docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-207.md` 追加兩節，皆為純追加（diff 核對零刪除行，既有段落逐字保留）：

1. 「### Phase 2 覆蓋缺口補記：`released` 等級無斷言」——插於 Phase 2 §12「移交 Phase 3a」table 之後、「### PM 裁決：Phase 2 第 11 節的兩項分歧與三項缺口」之前。內容：現象（T-23／T-2 皆有 level 斷言，T-3／T-4／T-5 涉及 `released` 的場景無一斷言 level）、為何構成缺口（接受／釋放一對觀測點只驗證了一半）、未斷言的後果（F-1 潛伏至 Phase 4a 才被發現）、已補救（Phase 4b 修正 `_releaseHolder` 並補測試）、一般化陳述（等級屬可觀測性契約，應以事件對為單位檢查）。
2. 「#### 3a.3 補記：`RELEASE_HOLDER` 事件選擇與等級選擇的落地與判定」——插於 §3a.3 決策管線虛擬碼段落之後、`#### 3a.4` 之前。以表格分開記載事件選擇（Phase 3b 依 Phase 2 測試改為依觸發路徑區分，Phase 4a 判定裁量正確）與等級選擇（沿用 `_levelForOrigin`，未被測試攔下，即 F-1，已由 Phase 4b 修正）兩面向，並附一般化陳述。

**框架問題升級判準逐條核對**（`.claude/skills/framework-issue/references/escalation-flow.md`，兩條 AND）：

- 抽象可攜性：兩則缺陷的形態（「事件有等級斷言、其配對事件沒有」「虛擬碼同時承載兩個判斷面向、只鎖住其一」）替換掉本案例後仍可成立，此軸滿足。
- 資產範圍：兩則缺陷的根源皆在本專案的 `docs/work-logs` ticket 內容（Phase 2 測試設計、Phase 3a 虛擬碼），屬專案專屬產物，不在 `.claude/` 下的 hooks／skills／rules／methodologies／agents 等通用資產，此軸不滿足。

**判定：不升級為框架 issue**。兩項為本專案設計文件缺陷，AND 判準未全數成立（資產範圍軸不成立）。

**範圍確認**：未觸碰 `0.1.0-W3-207.md` 的 Phase 1 §2.2／§2.5 區段（由 `0.1.0-W3-262` 承接）；未修改任何程式碼或規格檔（`docs/spec/`）。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-207 | chars: 1193 -->

### Task Reference
- 0.1.0-W3-207 what: 實作 注意力仲裁點：單一持有者依級別裁決跨 domain 的提示請求（來源已完成，結論見 Solution）
- 0.1.0-W3-207 what: 實作 注意力仲裁點：單一持有者依級別裁決跨 domain 的提示請求（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-207 why: `0.1.0-W3-184` 第三節裁定協調形態為**單一仲裁點而非分散式協商**：各 domain 不互相確認，改為向注意力通道的持有者請求，由後者依級別規則裁決。

**為何不能讓 domain 互相確認**：注意力競爭發生在任何兩個會產生注意力請求的 domain 之間、不分層級（協調圖與 `docs/domain-map.md` §2 的依賴圖正交），因此互相確認需要 N² 條協調邊，且迫使 domain 互相知道彼此存在——直接違反 §3 Bundle 界定表的公開面設計。仲裁只需 N 條，且 domain 之間維持互不知情。

**不做則後果**：`0.1.0-W3-204` 的級別規則會停在單一載體內（`AppSnackBar`），跨 domain 的請求仍各自直達載體，「現在是不是請求使用者注意的適當時機」沒有任何地方在回答。（來源已完成，結論見 Solution）
- 0.1.0-W3-207 why: `0.1.0-W3-184` 第三節裁定協調形態為**單一仲裁點而非分散式協商**：各 domain 不互相確認，改為向注意力通道的持有者請求，由後者依級別規則裁決。

**為何不能讓 domain 互相確認**：注意力競爭發生在任何兩個會產生注意力請求的 domain 之間、不分層級（協調圖與 `docs/domain-map.md` §2 的依賴圖正交），因此互相確認需要 N² 條協調邊，且迫使 domain 互相知道彼此存在——直接違反 §3 Bundle 界定表的公開面設計。仲裁只需 N 條，且 domain 之間維持互不知情。

**不做則後果**：`0.1.0-W3-204` 的級別規則會停在單一載體內（`AppSnackBar`），跨 domain 的請求仍各自直達載體，「現在是不是請求使用者注意的適當時機」沒有任何地方在回答。（來源已完成，結論見 Solution）

### Related Files
- lib/app/attention_arbiter.dart  # from 0.1.0-W3-207（來源已完成，結論見 Solution）
- test/unit/app/attention_arbiter_test.dart  # from 0.1.0-W3-207（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] domain 側介面只表達級別，不表達載體與時機  # from 0.1.0-W3-207（來源已完成，結論見 Solution）
- [ref] [x] 仲裁決策依 SPEC-003 的級別規則，未在仲裁點另立一套  # from 0.1.0-W3-207（來源已完成，結論見 Solution）
- [ref] [x] 仲裁點未落入任何 domain 內部，落層符合 0.1.0-W3-203 的指定  # from 0.1.0-W3-207（來源已完成，結論見 Solution）
- [ref] [x] 本票未實作第三類接收緩衝，且在 Solution 明記該範疇歸屬與 trigger  # from 0.1.0-W3-207（來源已完成，結論見 Solution）
- [ref] [x] domain 側介面只表達級別，不表達載體與時機  # from 0.1.0-W3-207（來源已完成，結論見 Solution）
- [ref] [x] 仲裁決策依 SPEC-003 的級別規則，未在仲裁點另立一套  # from 0.1.0-W3-207（來源已完成，結論見 Solution）
- [ref] [x] 仲裁點未落入任何 domain 內部，落層符合 0.1.0-W3-203 的指定  # from 0.1.0-W3-207（來源已完成，結論見 Solution）
- [ref] [x] 本票未實作第三類接收緩衝，且在 Solution 明記該範疇歸屬與 trigger  # from 0.1.0-W3-207（來源已完成，結論見 Solution）
