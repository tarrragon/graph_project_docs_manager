---
id: 0.1.0-W3-159
title: 評估 互指文件對的同步偵測手段
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-157
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-157 建立的雙向註記只有自律層，任一側被改時另一側靜默失準
  final_decision: ANA 評估同步偵測手段的形態與涵蓋範圍，成案則衍生實作票
  rationale: 互指對是為修補單向引用而建立的，若因無偵測而退化回單向，會退回它本要修補的狀態且更難察覺（讀者看到有註記會以為已對齊）
who:
  current: saffron-system-analyst
  history: {}
what: 評估 互指文件對的同步偵測手段
when: 0.1.0-W3-157 完成後
where:
  layer: Infrastructure
  files:
  - docs/spec/workspace/SPEC-005-workspace-driven-ports.md::read
  - docs/tech-decisions.md::read
  - .claude/hooks/skill-cli-sync-check-hook.py::read
why: '`0.1.0-W3-157` 在 `docs/spec/workspace/SPEC-005-workspace-driven-ports.md` §1／§1.1 與 `docs/tech-decisions.md` 補記段之間建立了一對互指註記，說明同一組接縫在兩份文件中切分方式不同且兩邊都不該被改成對方的粒度。


  兩份文件內都寫了「任一處被修改時另一處須同步」，但那是自律層，**無強制層**。任一側被改動時另一側靜默失準，且失準後的外觀比原本更糟——讀者看到兩處都有註記，會以為已經對齊過，不會再交叉查證。


  這一對本身是為了修補單向引用（`DOC-GPD-002`）而建立的。若因無偵測而退化回單向，等於退回它本要修補的狀態，且多了一層誤導。


  同型問題不限本對。專案內其他「兩份文件描述同一事實、切分方式不同」的配對是否已存在、是否也只有自律層，本票一併盤點——若只針對本對做偵測，得到的是又一個單點防護。'
how:
  task_type: Analysis
  strategy: '**先盤點母體再設計手段**，不從本對直接跳到實作。


    一、盤點：找出專案內互指文件對的現有實例。已知一對（SPEC-005 ↔ tech-decisions 形態約定段）。搜尋手段可用「以標題文字引用另一份文件的段落」這個形態特徵；`docs/spec/` 之間、`docs/` 與 `.claude/` 之間、`.claude/rules/` 與 `.claude/references/` 之間各自檢查。盤點結果決定手段該做成通用機制或單點。


    二、既有先例分析：`.claude/hooks/skill-cli-sync-check-hook.py` 已實作同型機制——CLI src 變更時提示檢查 SKILL.md 與 pm-rules 是否同步，commit-level INFO 不阻擋，內含 7-skill registry。判定該形態能否直接套用於文件對，或差異在哪（該 hook 的配對是「程式碼 → 文件」單向，本情境是文件對文件雙向）。


    三、手段評估至少涵蓋三種並各附取捨：註冊表式 hook（如上述先例，需人工維護配對清單）、標記式（在文件內以特定 marker 宣告配對，hook 掃描 marker 自動配對，免維護清單但需既有文件補標記）、不做（接受自律層，理由須寫明為何本情境的失準成本低到可接受）。


    四、若評估結論為成案，衍生實作票；若為不做，於 Solution 寫明判準與重評條件（不得是無 trigger 延後，見 `decision-trigger-binding` 規則 1）。


    本票為分析票，不改動任一份文件內容。'
acceptance:
- '[ ] 專案內互指文件對的母體已盤點並列出實例，搜尋手段記於 Solution'
- '[ ] skill-cli-sync-check-hook 先例的可套用性已分析，文件對文件雙向與程式碼對文件單向的差異已寫明'
- '[ ] 三種手段（註冊表式/標記式/不做）各附取捨並給出結論'
- '[ ] 結論為成案則已衍生實作票；為不做則已寫明判準與重評條件且非無 trigger 延後'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

評估 互指文件對的同步偵測手段

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-157 | chars: 1319 -->

### Task Reference
- 0.1.0-W3-157 what: 對齊 SPEC-005 於 146／147 落地後的現況敘述（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-157 why: `0.1.0-W3-147` 已合併進 `origin/main`。PM 實查現況：`lib/workspace/workspace_types.dart` 存在（79 行），承載九個 value type（`WorkspaceState` 及其三個 variant、`ChooseFolderResult` 及其四個 variant）；`lib/workspace/workspace_repository.dart` 縮為 240 行、以 `import 'workspace_types.dart'` 引用且**全檔無 `export`**（不 re-export 為用戶裁定）；三個 port 介面、其 private `_Default*` adapter、兩個 typedef 皆留在原檔。

SPEC-005 §概述「載體」段寫的是「value types 將移至 `lib/workspace/workspace_types.dart`（`0.1.0-W3-147` 分離後；該檔於本規格撰寫時尚不存在）」。該敘述在撰寫當下正確且誠實標註了時點，但該時點已過——現在讀到的人會以為分離尚未發生。同段「全部五個接縫與其呼叫端集中於 `workspace_repository.dart`」仍為真，不需改動。

第二項與 `0.1.0-W3-146` 有關。兩份文件對同一組接縫給出不同的列數：SPEC-005 §1 列五個接縫（`WorkspacePreferencesHandle` 獨立成列），`docs/tech-decisions.md` 的 146 對照表列四列（`Port` 與 `Handle` 併列）。兩者回答不同問題故皆非錯誤，但無任何一方註記此事，後續讀者交叉比對時會判為矛盾並試圖「修正」其中一方。

同源的第三項：`WorkspaceLogSink` 在 SPEC-005 §1.1 被排除於受盤點 port 之外，在 146 的形態判準下則正常得到 typedef 形態。兩者不衝突——遞迴發生在可觀測性義務軸（列為受盤點 port 則記錄它自己的呼叫發出需要另一個投影載體），146 問的是介面形態軸（操作總數為 1 即 typedef），該計數不觸發遞迴。此判讀由並行 session 提出，PM 認為成立，但同樣未見於任一方文件。（來源已完成，結論見 Solution）

### Related Files
- docs/spec/workspace/SPEC-005-workspace-driven-ports.md  # from 0.1.0-W3-157（來源已完成，結論見 Solution）
- docs/tech-decisions.md  # from 0.1.0-W3-157（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 載體段已改為完成式並補入 workspace_types.dart 的實際承載內容與主檔無 export 這項約束  # from 0.1.0-W3-157（來源已完成，結論見 Solution）
- [ref] [x] 五個接縫與呼叫端集中於主檔一句經實查確認仍為真且未被誤改  # from 0.1.0-W3-157（來源已完成，結論見 Solution）
- [ref] [x] §1 已補與 146 的粒度差異交叉註記，明說非矛盾及各自回答的問題  # from 0.1.0-W3-157（來源已完成，結論見 Solution）
- [ref] [x] §1.1 已補軸別釐清，說明排除發生在可觀測性義務軸而非形態軸  # from 0.1.0-W3-157（來源已完成，結論見 Solution）
- [ref] [x] tech-decisions 是否對稱補註記已判定，需要時以 spawn request 交 PM 未自行改動  # from 0.1.0-W3-157（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

