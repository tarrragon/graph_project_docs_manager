---
id: 0.1.0-W3-335.8
title: 修補 test-assertion-design 原文矛盾裁決與過期描述更新（C11、S08）
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 修補 test-assertion-design 原文矛盾裁決與過期描述更新（C11、S08）
when: 前驅票完成後
where:
  layer: 待定義
  files:
  - .claude/skills/test-assertion-design/SKILL.md
why: W3-332 審查與走查確認 agent 依此文件推導元件與測試時被迫猜測或讀出兩種做法；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
how:
  task_type: Documentation
  strategy: 依 chained-examples 規格與 Context Bundle 執行；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正
acceptance:
- '[ ] 斷言品質三問與 9 類型分族對類型 5、6 的歸類提出統一建議並經用戶簽核後改文'
- '[ ] rules 層描述更新為實際涵蓋（含 Dart 與跨語言規則）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T11:55:56'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修補 test-assertion-design 原文矛盾裁決與過期描述更新（C11、S08）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-14 11:55] readiness=1 validate=1

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
### 提案（待用戶簽核，本票不改檔）

依 Context Bundle〈矛盾類票的簽核流程〉，本節僅列矛盾位置、兩方原文、建議、理由與改後文字草稿，供 PM 取得用戶簽核後回填本票再執行改文。C11（矛盾裁決）與 S08（過期描述，事實修正）一併列入。錨點一律用標題／表格列名，不用行號（document-format-rules 規則 11）。

#### C11：〈斷言品質三問〉與〈9 類型問題斷言分類決策表〉對類型 5、6 歸類不一致

**矛盾位置**：`.claude/skills/test-assertion-design/SKILL.md`〈斷言品質三問〉表格「確定性」列 vs 〈9 類型問題斷言分類決策表〉〈判斷軸〉節。

**兩方原文**：
- 甲（〈斷言品質三問〉表格「確定性」列）：「確定性｜同一程式碼在任何執行環境下，pass/fail 結果一致？｜類型 1–6（確定性失效）」——將類型 5（非同步時序）、6（亂數輸出）與 1–4 同歸「確定性失效」。
- 乙（〈判斷軸〉節）：「環境依賴 flaky 族（類型 1–4）：核心問題是『確定性』」／「斷言設計族（類型 5–9）：核心問題是『驗什麼』」——將類型 5、6 與 7–9 同歸「斷言設計族」。

**證據**：W3-332.3 Solution〈需範例位置表〉第 21 列的分歧模擬已具體示範此矛盾的操作後果——讀者甲依三問將類型 5（非同步時序）歸「確定性失效」，選環境隔離類處置；讀者乙依判斷軸歸「斷言設計族」，選「等待最終狀態」處置。兩處置不同，後者才是〈問題類型表〉類型 5 列「設計原則」欄所寫的正確處置；類型 6 同理，其〈問題類型表〉「設計原則」欄寫「隨機源必須可控；斷言驗證演算法行為，不斷言特定隨機輸出」，屬「驗什麼」的設計問題，非環境隔離可解。

**旁證**：`CHANGELOG.md` 記載 1.2.0 版意圖已是「三問『確定性』對應標籤由『環境依賴』改為『確定性失效』（類型 5-6 屬設計族）」——即上一輪審查已判定類型 5、6 實質歸屬設計族，但 SKILL.md 正文未落實這句連結說明，才留下本次矛盾。

**建議以哪方為準**：以〈判斷軸〉（乙）的「斷言設計族」歸類為準；三問維持「類型 1–6（確定性失效）」不變動邊界數字（三問是初篩症狀，5、6 確實表現為確定性症狀），但須在三問表格下方新增澄清句，明示類型 5、6 的處置依〈判斷軸〉之斷言設計族為準，不得套用類型 1–4 的環境隔離類處置。此法與 CHANGELOG 既有意圖一致，且改動幅度最小（不需重新編號任一表格）。

**改後文字草稿**：

於〈斷言品質三問〉表格後新增一句：

> 確定性問題細分兩種處置：類型 1–4 屬環境依賴 flaky 族，處置為隔離執行環境或改用差值；類型 5、6 雖同樣呈現確定性症狀，但依〈判斷軸〉歸屬斷言設計族，處置依個別設計原則（等待最終狀態／固定可控隨機源），不套用類型 1–4 的環境隔離做法。

〈判斷軸〉節「此分族與斷言品質三問為不同視角」句改為：

> 此分族與斷言品質三問為不同視角且邊界不完全重疊：三問是初篩症狀分流（任一問答否即先歸入對應症狀組，類型 5、6 會先被「確定性」問題篩出）；分族才是依問題本質與處置方式的最終歸類（類型 5、6 因處置屬「驗什麼」而非「環境隔離」，最終歸斷言設計族）。讀者判斷處置時以分族為準。

---

#### S08：專案 rules 層描述過期

**現況原文**：
- 〈不在範圍聲明〉節「專案 rules 層」句：「專案 rules 層：`.claude/rules/core/test-assertion-design-rules.md`（本專案 Chrome Extension/JS/Jest 專屬規則）」
- 〈定位說明〉表格 `.claude/rules/core/test-assertion-design-rules.md` 列：「本專案（Chrome Extension/JS/Jest）專屬規則，含具體精度數字與目錄規定」

**事實現況**（實測 `.claude/rules/core/test-assertion-design-rules.md` 全文）：該檔現含三段落地規則，非僅 Chrome Extension/JS/Jest 專屬——「JS/Jest 落地規則（四規則速查）」、「Dart/Flutter 落地規則」（D1-D5）、「鑑別力規則（跨語言，E 系列）」（E1-E2，明載「適用語言不限，凡新增旗標或守衛皆適用」）。且本 skill 為跨專案共用框架資產（`.claude/skills/`），「本專案」措辭亦違反框架檔不得寫本專案識別符的通則（描述應為 consumer 專案落地層級，非單一專案綁定）。

**建議以哪方為準**：以事實現況為準，修正兩處描述並移除「本專案」與具體專案類型（Chrome Extension/JS/Jest）的綁定措辭，改為通用的「consumer 專案落地規則」描述。

**改後文字草稿**：

〈不在範圍聲明〉節「專案 rules 層」句改為：

> 專案 rules 層：`.claude/rules/core/test-assertion-design-rules.md`（consumer 專案內的具體規則落地，依語言分節：JS/Jest 落地規則、Dart/Flutter 落地規則、跨語言鑑別力規則）

〈定位說明〉表格 `.claude/rules/core/test-assertion-design-rules.md` 列改為：

> consumer 專案內的具體規則落地層，依語言分節（JS/Jest／Dart/Flutter／跨語言），含具體精度數字與目錄規定

---

### 版本紀錄規劃（待簽核後執行，本票不執行）

改文若經簽核採納，需同步 `SKILL.md` frontmatter `metadata.version`（由 1.2.0 提升）與同目錄 `CHANGELOG.md`，版號與異動摘要依當時採納的最終文字定案，不在本提案階段預先寫定。

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## Context Bundle

### 修補票共同 Context（PM，2026-09-14）

**缺口清單在哪**：本票涵蓋的缺口編號見 `ticket track full 0.1.0-W3-332.13` Solution；每個缺口的位置錨點、兩位讀者的分歧模擬、子票草稿見其來源子票（W3-332.2–.10）Solution。**W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 優先於 .13 草稿**——草稿漏收、誤歸或數字不符處以 P 項為準。

**規格**：`.claude/skills/skill-design-guide/references/chained-examples.md`（三段鏈式、驗證段兩形態、缺範例與缺引導分界）。

**已定案的用戶裁示**：
- 語言專屬 skill 的違規／修正程式碼對照不受「禁程式語法」約束（例外條款由 W3-335.15 寫入規格）
- 元件庫規格範本佔位文字改為技術中立，框架名移到 L2 註記
- version-bootstrap 的 v02-walkthrough 依新規格重寫整篇

**矛盾類票的簽核流程**：先在 Solution 寫「矛盾位置｜兩方原文｜建議以哪方為準｜理由｜改後文字草稿」，**不改檔**，以 `ticket track set-exit-status` 設 needs_context 並停手回報；PM 取得用戶簽核寫回本票後再續做改文。

**通則**：
- 框架檔不得寫本專案識別符（票號、SPEC 編號、畫面名），範例一律去識別化
- 集合大小改引用權威名稱而非寫死數字；錨點用標題不用行號
- 改動 skill 須同步其 CHANGELOG 與 SKILL.md 版號
- 只改本票 where.files；發現範圍外缺口登記 spawn request，不就地修

---

## NeedsContext
- **缺失項**：C11（斷言品質三問 vs 分族對類型 5、6 歸類矛盾）與 S08（rules 層過期描述）的改文方向需用戶簽核（依 Context Bundle〈矛盾類票的簽核流程〉，矛盾類票只提案不改檔）。
- **觸發位置**：`.claude/skills/test-assertion-design/SKILL.md`〈斷言品質三問〉表格「確定性」列 / 〈判斷軸〉節（C11）；〈不在範圍聲明〉節「專案 rules 層」句 / 〈定位說明〉表格對應列（S08）。
- **影響**：acceptance 1（三問與分族類型 5、6 歸類統一建議並簽核後改文）尚未取得簽核，無法改檔；acceptance 2（rules 層描述更新）已備妥草稿，一併等待同批簽核後與 C11 同次落地。
- **建議補料**：PM 將本票 Solution 提案呈用戶裁決「建議以哪方為準」是否採納，簽核結果回填本票（或另建執行子票）後，由代理人依定案文字執行改文與 CHANGELOG／版號同步。
- **重派成本**：低——提案與改後文字草稿已在 Solution 完整備妥，簽核後執行僅為依草稿落地兩處 Edit + 版號/CHANGELOG 同步，無需重新分析。

## Exit Status
```yaml
exit_status: needs_context
reason: "矛盾類票依流程只提案不改檔：C11（三問 vs 分族類型5/6歸類）與 S08（rules層過期描述）改文草稿已備妥於 Solution，待用戶簽核"
confidence: 0.9
acceptance_met: []
acceptance_unmet: [1, 2]
artifacts: []
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
