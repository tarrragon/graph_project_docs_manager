---
id: 0.1.0-W3-330
title: 補強 待裁列推送的三項缺口：冒牌列、遺失無訊號與 state 錨定、baseline 重建
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-329
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: PM 對 0.1.0-W3-329 的退化路徑跑 WRAP，Step 0 實測拆出三件成因；用戶裁定連 A 一起處置
  final_decision: 三項併一票，因 A 的範圍依賴 C 的 state 錨定決定
  rationale: C 若把 state 錨定改為主倉庫，重建的寫入落點與 worktree 行為隨之改變；分票會使 A 在 C 未定時無標的
who:
  current: basil-hook-architect
  history: {}
what: 補強 待裁列推送的三項缺口：冒牌列、遺失無訊號與 state 錨定、baseline 重建
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/todo_pending_registry.py
  - .claude/hooks/session-start-todo-delta-hook.py
  - .claude/skills/framework-issue/scripts/section_comment.py
why: '**PM 對 `0.1.0-W3-329` 的退化路徑跑 WRAP，Step 0 實測把「退化為沉默」拆成三件成因，處置相反。**


  **B（實際缺陷）**：`.claude/state/framework-issue-todo-pending.json` 目前含一列冒牌資料——`owner: test-session-1a2b3c4d`、`source_ticket: imp-1`、issue 81、task「對帳工具：讀三份清單輸出差集」，`updated_at` 為 2026-09-12T13:00:06Z。**成因不是測試碼**：`tests/test_todo_pending_registry.py` 第 4 行 docstring 明寫以 `tmp_path` 避免真實路徑，實測亦如此；該列來自 `0.1.0-W3-329` 執行期的手動 smoke test 寫入真實路徑。


  **C（真缺口）**：state 遺失／損毀時退化為沉默，唯一記錄是 `logger.info`（不進 stderr、不進對話）。**且遺失比預期常見**——`_project_root()` 解析序為 `CLAUDE_PROJECT_DIR` → `git rev-parse --show-toplevel` → `cwd`；PM 實測 `CLAUDE_PROJECT_DIR` **未設**，故吃 `--show-toplevel`，**在 linked worktree 內指向 worktree 自己的根**，得到空 state。而本專案每次 IMP 派發都開 worktree。


  **A（設計邊界，用戶裁定一併處置）**：canonical 待裁列有 **101 列**（`0.1.0-W3-326` 實測），state 僅記本專案自己 CLI 寫入者，故既有列永不被推送。**PM 原判此為設計非缺口**（印全列 fail `0.1.0-W3-326` 的 N2 測），並對「加重建命令」提出保留——它需要有人記得跑，正是 `0.1.0-W3-320`／`324` 花一整串消滅的失效模式。**用戶裁定一併處置，本票照辦**；保留意見記於此處，不影響執行。'
how:
  task_type: Implementation
  strategy: '**順序：C 的 state 錨定先定，A 的落點才有標的。**


    **C-1 state 錨定**：使 state 在主倉庫與 linked worktree 解析到**同一個位置**（待裁列是全域資料，per-worktree 分裂使它在每個 worktree 都是空的）。**不得新增第七個 root 解析器**——`0.1.0-W3-288` 實測本專案已有六個互異解析器，其中三個是呼叫檔自行實作。既有可主倉庫錨定者為 `ticket_system/lib/paths.py::get_ticket_state_root()`（`0.1.0-W3-273` 記其反向回推主倉庫）；**但跨 skill import 與該登記檔 docstring 自述的 zero-coupling 相斥**——兩者取捨須在票面說明，不得默默選一邊。


    **C-2 遺失訊號**：state 遺失／損毀時發 **stderr 可見訊號**（`quality-baseline` 規則 4 雙通道），**但只在遺失／損毀時，不在 baseline 稀疏時**——後者 fail `0.1.0-W3-326` 的 N2 測（恆真即非訊號）。C-1 完成後 worktree session 會讀到主倉庫的 state，該訊號的觸發頻率隨之下降。


    **A 重建**：提供重建 baseline 的能力，**且不得掛在 SessionStart 路徑**（`0.1.0-W3-326` 實測 `todo --json` 13.63 秒）。**評估能否讓「命令存在」與「有人記得跑」脫鉤**——例如 `todo` 自身發現 state 缺失時惰性重建，或寫入端偵測到 state 不存在時重建一次。**採或不採並記錄理由**；若判定只能是顯式命令，說明為何無法自動觸發。


    **B 清理**：移除冒牌列，並使手動 smoke test 不再寫入真實路徑（例如要求顯式 `project_root`、或提供一個明示的 dry-run 出口）。**只刪那一列不算完成**——同一個 smoke test 下次會再寫一次。


    **測試**：C-1 的錨定在 worktree 與主倉庫各有斷言；C-2 的觸發與不觸發成對斷言（遺失時印／稀疏時不印）；A 的重建結果與 `todo` 輸出對帳；B 的 smoke test 出口有斷言不寫真實路徑。'
acceptance:
- '[ ] state 在主倉庫與 linked worktree 解析到同一位置，兩處各有斷言；未新增第七個 root 解析器'
- '[ ] 跨 skill import 與 zero-coupling 的取捨已在票面說明，非默默選一邊'
- '[ ] 遺失／損毀時發 stderr 可見訊號；baseline 稀疏時不發，成對斷言'
- '[ ] 重建能力已提供且不在 SessionStart 路徑；「能否與記憶脫鉤」已評估並記錄理由'
- '[ ] 冒牌列已移除，且手動 smoke test 不再寫入真實路徑並有斷言'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-12'
updated: '2026-09-12'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補強 待裁列推送的三項缺口：冒牌列、遺失無訊號與 state 錨定、baseline 重建

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: basil-hook-architect
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-329 | chars: 1013 -->

### Task Reference
- 0.1.0-W3-329 what: 實作 待裁列的 ack-based delta 推送，含成本壓不下來時的停手條件（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-329 why: **`0.1.0-W3-326` 已裁定三項，本票不重裁**：形態為 delta 而非清單；ack-based 而非 seen-based（後者 at-most-once，印過未處理即永久沉默）；主通道 SessionStart、輔以寫入端回印、條文層不承擔。

**通道的依據是讀者身分不對稱**：寫入者是 curator，裁決者是 PM，而 PM 的固定入口只有 session 啟動。

**delta 的 predicate 已驗**：逐列身分 `(issue, owner, 正規化後的來源票)` 在 109 列上唯一、零碰撞。**時間軸必須由本地快照製造**——`todo` 逐列無任何日期欄位。

**本票的主要風險是成本，而該風險可能否決 326 選的通道**：`0.1.0-W3-326` 實測 `todo --json` 預設範圍 **real 13.63 秒**，SessionStart **已註冊 44 支 hook**。326 自述該值不可原樣帶入。**一支 13 秒的 hook 會改變整個啟動序列的成本結構**——屆時正確做法是回頭重裁通道，不是出貨一支慢 hook。

**四項未決（`0.1.0-W3-326` 明列，本票承接）**：ack 訊號的取得方式與載體、成本控制、推送範圍、狀態遺失後的退化行為與誤報定義。**四項皆須在票面留下量測依據，不得憑設計直覺定案。**（來源已完成，結論見 Solution）

### Related Files
- .claude/hooks/session-start-issue-check-hook.py  # from 0.1.0-W3-329（來源已完成，結論見 Solution）
- .claude/settings.json  # from 0.1.0-W3-329（來源已完成，結論見 Solution）
- .claude/hooks/tests/test_session_start_todo_delta_hook.py  # from 0.1.0-W3-329（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/tests/test_todo_pending_registry.py  # from 0.1.0-W3-329（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] SessionStart 的新增成本已量測並低於票面先寫明的預算；若無方向達標，已停手回報而未出貨  # from 0.1.0-W3-329（來源已完成，結論見 Solution）
- [ref] [x] ack 訊號載體已定，且未寫入 canonical issue  # from 0.1.0-W3-329（來源已完成，結論見 Solution）
- [ref] [x] 狀態遺失後的退化行為已明訂，兩個相反失效方向的取捨有說明  # from 0.1.0-W3-329（來源已完成，結論見 Solution）
- [ref] [x] 誤報定義已寫明  # from 0.1.0-W3-329（來源已完成，結論見 Solution）
- [ref] [x] 測試含「應印而印」與「不該印而沒印」成對斷言，並涵蓋狀態遺失與 subagent 略過  # from 0.1.0-W3-329（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

