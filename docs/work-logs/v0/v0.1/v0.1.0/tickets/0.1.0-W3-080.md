---
id: 0.1.0-W3-080
title: 補寫 doc-handoff.md 判斷原則加平台決定一列
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-074
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 補寫 doc-handoff.md 判斷原則加平台決定一列
when: 待定義
where:
  layer: Framework Docs
  files:
  - .claude/skills/tdd/references/doc-handoff.md
why: 框架層缺口修補，W3-074 方案 a 切分項 2
how:
  task_type: Implementation
  strategy: 待定義
acceptance:
- '[ ] 〈整合測試 vs 單元測試分工〉的判斷原則加第三值「平台決定」，並說明它與既有兩值（自己決定／串接決定）的關係——跨邊界功能對原二分給不出答案是本次修改的動因'
- '[ ] 分工決策框架表加對應列，測試層級欄指向 layered-test-strategy 的跨邊界補位形態'
- '[ ] footer 版本條目與 skill 可攜性（無 consumer-specific 內容、無專案 ticket ID）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T14:00:57'
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補寫 doc-handoff.md 判斷原則加平台決定一列

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-08 13:56] readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

### 問題根因

〈整合測試 vs 單元測試分工〉的判斷原則對跨 app process 邊界步驟（系統權限對話框、
外部 process、原生檔案選擇器）給不出答案：呼叫離開 app process 後，正確性由平台
決定，不是自己或串接。

**prompt 動因與正本現況的落差（已記錄非阻塞）**：prompt 描述判斷原則「現為二分
（自己決定／串接決定）」，但實測 `doc-handoff.md` 現況已是三分（自己決定／串接
決定／配置決定，`git log` 顯示此三值至少自 cffdda1d 即存在，非近期變更）。此落差
不影響任務可執行性——新增「平台決定」的動作與說明其關係的要求依然明確，故未停手
上報 NeedsContext，僅在此據實記錄：本次修改是「三值加第四軸」而非 prompt 描述的
「二值加第三值」。

### 影響範圍

`.claude/skills/tdd/references/doc-handoff.md`〈整合測試 vs 單元測試分工〉小節：
判斷原則、分工決策框架表、footer 版本記錄。
## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-08
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

**變更摘要**：`.claude/skills/tdd/references/doc-handoff.md`〈整合測試 vs 單元測試分工〉小節：

1. 判斷原則加第四值「平台決定」+ 新小節「跨邊界步驟：平台決定」，說明其與既有
   自己/串接/配置三值的關係為**正交**（先問「決定發生在 app process 內還是外
   面」，非並列第四同軸選項），非 prompt 所述的「二值加第三值」（實測正本已是
   三值，見 Problem Analysis 落差記錄）。
2. 分工決策框架表加一列「跨 app process 邊界」，測試層級指向
   `references/layered-test-strategy.md` 跨邊界補位形態。
3. footer 補 Version 1.1.3 條目。

**引用待補**：本檔對 `layered-test-strategy.md` 的引用目前是檔案層級 + HTML
comment TODO 標記（未指向具體章節標題），因為 0.1.0-W3-079（新增該文件第三個
補位形態「跨邊界驗證」）於本票完成時仍在進行中、章節標題尚未定案。**引用待
0.1.0-W3-079 的節標題定案後補**——未依 prompt 指示自行猜測標題字串。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-074 | chars: 1211 -->

### Task Reference
- 0.1.0-W3-074 what: 為跨 app 邊界的功能補上測試分類（框架層：tdd 五層分工表缺測試形態軸；專案層：三個呼叫點的驗證形態與 W3-064／W1-036 待驗項承接），並評估改進成果的落點（改既有 skill 或新建）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-074 why: **框架層缺口**：`tdd/references/layered-test-strategy.md` 的五層分工表以**架構層級**為軸（UI／Application／UseCase／Interface／Domain），缺**測試形態**軸。跨 app 邊界的功能在表上落於 Layer 1（整合測試），但該格指定的方法做不到——`integration_test` 無頭執行，觸不到系統權限對話框、通知中心、原生檔案選擇器、真實 process。表上有位置、位置上的方法無效，且無任何條文說明此情形，讀者依表執行會得到「已覆蓋」的假象。

**專案層實例**：CLAUDE.md §6 定測試策略為雙層（`test/` 契約 + `integration_test/` 行為），本專案三個跨邊界呼叫點（`macos_scan_notifier.dart` 系統通知、`workspace_repository.dart` 資料夾存取、`gap_report_screen.dart` 外部開啟與 doc CLI process）**只有 fake 測試**，平台行為從未被驗證。SPEC-003 §2.2「實作票驗證」段明文要求四項平台事實由實機測得後回填，0.1.0-W3-064 的 acceptance 2、3 因此未勾；`ExternalOpener`（W1-036）同類。`.claude/skills/verify/` 不存在，session 啟動每次警告「驗證環境未就緒」而無人承接。

**為何是框架問題而非單票問題**：三個功能落在同一個縫隙，逐票各自決定會產生三種不一致的做法；且此缺口與載體無關（任何有平台互動的專案都會撞到），替換專案名稱與路徑後仍成立。（來源已完成，結論見 Solution）

### Related Files
- docs/tech-decisions.md  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- CLAUDE.md  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- integration_test::read  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- test::read  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- lib/services/macos_scan_notifier.dart::read  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] 跨邊界判準已定義，並判定它與既有五層的關係（新的一層或正交屬性），該判定的依據寫入結論  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- [ref] [x] layered-test-strategy 表已補形態軸或等效結構；doc-handoff 的分工框架已檢查並同步或說明不需同步  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- [ref] [x] 落點評估兩案（改既有 skill／新建 skill）各附可執行切分與四項判準的比較，並給結論  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
- [ref] [x] 本專案三個呼叫點逐項標驗證形態；決議寫入 tech-decisions，CLAUDE.md §6 同步或說明不需；W3-064 與 W1-036 的待驗項各有承接票或 spawn 落地紀錄  # from 0.1.0-W3-074（來源已完成，結論見 Solution）
