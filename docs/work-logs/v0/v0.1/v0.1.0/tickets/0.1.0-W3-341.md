---
id: 0.1.0-W3-341
title: 定案 FlowStep 所屬 domain 的資料來源（CLAUDE.md §6 待決項）
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-335.19
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-335.19 回報 FlowStep 無 domain 來源
  final_decision: 建 ANA 承接 CLAUDE.md 待決項
  rationale: decision-trigger-binding：延後決策須綁 ticket；三處規格與一張實作票依賴它
who:
  current: saffron-system-analyst
  history: {}
what: 定案 FlowStep 所屬 domain 的資料來源（CLAUDE.md §6 待決項）
when: 事件宣告與 flow 不符偵測、UC Flow 步驟表 domain 欄與事件流小表實作前
where:
  layer: 待定義
  files:
  - docs/domain-map.md::read
  - docs/usecases/UC-01-open-project-and-reach-usable-state.md::read
  - .claude/skills/doc/doc_system/core/tracking_schema.py::read
why: W3-335.19 回寫時確認 FlowStep 無 domain 欄位；SPEC-001 §2 步驟表 Domain 欄、事件流小表「步驟序號 · domain」、破洞類別 event-declaration-mismatch 判準皆依賴此資料，而 CLAUDE.md §6 將「Domain 視圖的列與格無來源、FlowStep 無 domain 欄位」列為待決、至今無承接票
how:
  task_type: Analysis
  strategy: 盤點候選來源（FlowStep 新增欄位並回報上游 schema、由 emits/consumes 的 EVT producers/consumers 推導、由 domain-map 對照、由步驟所實作 FR 推導）→ 各自的上游影響與資料完整度實測 → 交用戶裁決
acceptance:
- '[ ] 候選來源逐項列出並各附本專案 39 個 FlowStep 的可推導率實測'
- '[ ] 標明每個方案是否需改上游 tracking_schema 並依框架問題判準分流'
- '[ ] 用戶裁決結果記錄並回寫 CLAUDE.md §6 待決清單'
- '[ ] 依賴此資料的規格位置與實作票已標 blockedBy 或指向本票結論'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

定案 FlowStep 所屬 domain 的資料來源（CLAUDE.md §6 待決項）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-335.19 | chars: 622 -->

### Task Reference
- 0.1.0-W3-335.19 what: 回寫 泳道 flow 未結構化狀態、UC Flow 事件流小表與孤立事件破洞、破洞項點擊跳票與票列 blockedBy（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-335.19 why: 用戶裁示（2026-09-14）：SR-2 比照 UC Flow 新增泳道 flow 未結構化狀態；SR-3 經 WRAP 判定事件是 domain 間連線而非步驟屬性，採事件流小表並將孤立事件列入破洞報告；SR-4 以 UC-05 為準改破洞項點擊為跳票、外部開啟降為次要操作，票列加 blockedBy（來源已完成，結論見 Solution）

### Related Files
- docs/spec/ui/SPEC-001-screen-state-matrix.md  # from 0.1.0-W3-335.19（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W3-335.19（來源已完成，結論見 Solution）
- docs/usecases/UC-05-find-blocked-work.md  # from 0.1.0-W3-335.19（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] SPEC-001 §1 新增「泳道 · flow 未結構化」列四欄齊全並計入狀態總數，SPEC-003 同步錨點與完成矩陣  # from 0.1.0-W3-335.19（來源已完成，結論見 Solution）
- [ref] [x] SPEC-001 §2 正常態顯示欄含事件流小表，SPEC-003 §3.2 定義小表錨點、欄位與「本 UC 外」判定  # from 0.1.0-W3-335.19（來源已完成，結論見 Solution）
- [ref] [x] SPEC-001 §5 與 SPEC-003 §3.5 新增孤立事件破洞類別與判準  # from 0.1.0-W3-335.19（來源已完成，結論見 Solution）
- [ref] [x] SPEC-003 §3.5 破洞項主操作改為跳票並定位、外部開啟為次要操作；SPEC-001 §4 票列含 blockedBy 欄  # from 0.1.0-W3-335.19（來源已完成，結論見 Solution）
- [ref] [x] UC-05 步驟 1 移除預估耗時與 SPEC-003 §3.4 一致  # from 0.1.0-W3-335.19（來源已完成，結論見 Solution）
### 背景（PM，2026-09-14）

- CLAUDE.md §6 現行待決：「Domain 視圖的列與格無來源：個別 domain 不是圖節點（`DomainBundle` 的 carrier 是整份 domain-map.md），`FlowStep` 亦無 `domain` 欄位」
- 上游 schema：`.claude/skills/doc/doc_system/core/tracking_schema.py` 的 `FlowStep` 型別定義與 `FLOWSTEP_REQUIRED_FIELDS`；`emits`／`consumes` 邊的 forward_field
- EVT 節點 frontmatter 有 `producers`／`consumers`（domain 名清單），例：`docs/events/corpus/EVT-CORPUS-001-corpus-parsed.md`
- 依賴本結論的位置：SPEC-001 §1（Domain 矩陣格、泳道「步驟置於所屬 domain 列」）、§2（步驟表 Domain 欄、事件流小表「步驟序號 · domain」）、§5／SPEC-003 §3.5 `event-declaration-mismatch` 判準；實作票 W3-340
- 用戶裁示：閘門組複驗（V4 走查）等本票定案後才跑

### 需產出（寫入 Solution，不改檔、不建票）

1. **候選方案表**：至少涵蓋——(a) FlowStep 新增 `domain` 欄位；(b) 由 FlowStep `emits` 的 EVT `producers` 推導；(c) 由 FlowStep `implements` 的 FR 所屬 SPEC 的 domain 推導；(d) 由 domain-map.md 對照；(e) 反向框架：Domain 視圖不以 FlowStep 為格來源（重新定義「格」）。每列：推導規則、上游是否需改（tracking_schema／doc CLI）、本專案 39 個 FlowStep 的可推導率（實測，附計算方式）、推導出多個或零個 domain 時的處置、失敗模式
2. **框架問題判準**：凡需改 `.claude/skills/doc/` 者，逐條套 `.claude/skills/framework-issue/references/escalation-flow.md` 兩條 AND 判準並記錄
3. **對既有規格的影響**：各方案下 SPEC-001 §1、§2、§5 需改什麼
4. **給用戶的裁決題**：選項、各選項後果；不替用戶選
