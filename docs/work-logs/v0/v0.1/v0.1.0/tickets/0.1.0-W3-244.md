---
id: 0.1.0-W3-244
title: 定義 本專案 developer.log 的等級級距語意（900 現被稱為 error 而規格要求 warning）
type: DOC
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-176
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: PM 覆核 0.1.0-W3-176 Phase 1 時發現規則文字、SPEC-003 與 Dart 慣例對 900 的稱呼三方不一致
  final_decision: 另立 DOC 票定義級距並更正規則文字，不改 176 的規格（其指派依慣例正確）
  rationale: 規則文字把 900 稱為 error 會讓後續每個要寫 warning 的地方繼續選 900，級距永遠建立不起來；且實查 lib/ 只有 900 一個值，現況即無級距
who:
  current: thyme-documentation-integrator
  history: {}
what: 定義 本專案 developer.log 的等級級距語意（900 現被稱為 error 而規格要求 warning）
when: 0.1.0-W3-176 進入 Phase 3b 之前（該票規格已指派 background→900／userInitiated→800）
where:
  layer: Presentation
  files:
  - .claude/rules/core/observability-rules.md
why: '三處對同一個數值的稱呼不一致，而 `0.1.0-W3-176` 的 Phase 1 規格正要依它指派等級：


  | 來源 | 對 900 的語意 |

  |------|-------------|

  | `.claude/rules/core/observability-rules.md` | 「level 900 = **error**」 |

  | `SPEC-003` §2.13 | 截斷日誌「背景發起為 **warning**」 |

  | `dart:developer` 慣例（`package:logging` 級距） | 900 = **WARNING**，1000 = SEVERE |


  `0.1.0-W3-176` Phase 1 依 §2.13 把 background 截斷指派為 900，並新增 800 給 userInitiated。**依 Dart 慣例這是對的（900=WARNING、800=INFO）；依專案規則文字這會把一則例行的背景截斷記成 error**，與 `lib/workspace/workspace_repository.dart` 的「持久化失敗（例外）」落在同一桶。


  **實查現況**：`lib/` 全部既有 `level:` 用法只有 900 一個值（8 處），無 800、無 1000——專案實際上沒有級距，只有「預設」與「900」兩檔。`176` 新增 800 是本專案第一個非 900 的等級值。


  **為何要處理**：等級的作用是讓讀日誌的人能過濾。全部非預設訊息都是 900 時，過濾不出任何東西；而規則文字把 900 稱為 error，會讓後續每一個要寫 warning 的地方都繼續選 900，級距永遠建立不起來。'
how:
  task_type: Documentation
  strategy: '定義本專案 `developer.log` 的等級級距語意，寫入 `.claude/rules/core/observability-rules.md` 規則 2（統一日誌工具）之下。


    內容三項：

    (1) 級距表：至少涵蓋本專案會用到的等級與各自的語意，對齊 `dart:developer` 所依據的慣例（900=WARNING、1000=SEVERE、800=INFO），並**更正現行「level 900 = error」的表述**——該表述與慣例及 `SPEC-003` §2.13 的「warning」皆不一致。

    (2) 各等級的判別問句（讀日誌的人用什麼過濾、什麼情況該升到 1000）。

    (3) 存量處置：`lib/` 現有 8 處 900 逐一判定是否應改為 1000（如「持久化失敗（例外）」）或維持——**本票只列判定結果與理由，不改 `lib/`**（PM 與 thyme 皆不寫產品碼），改動另開 IMP 票或併入既有票。


    **邊界**：不改 `SPEC-003` §2.13（其「warning」用詞在更正後即與級距一致）；不改 `0.1.0-W3-176` 的 Phase 1 規格（其 900/800 指派依慣例正確，本票是讓規則文字追上它）。'
acceptance:
- '[ ] observability-rules 已載明等級級距表，且「level 900 = error」的表述已更正'
- '[ ] 各等級附判別問句，讀者能據以選擇等級而非沿用單一值'
- '[ ] lib/ 現有 8 處 900 已逐一判定應否改為其他等級，附理由，未改動 lib/'
- '[ ] 未改動 SPEC-003 §2.13 與 0.1.0-W3-176 的 Phase 1 規格'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

定義 本專案 developer.log 的等級級距語意（900 現被稱為 error 而規格要求 warning）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-176 | chars: 1006 -->

### Task Reference
- 0.1.0-W3-176 what: 補齊 AppSnackBar 元件層日誌的三項可稽核欄位

### Rationale Chain
- 0.1.0-W3-176 why: `0.1.0-W3-167` 的確定性重現實驗（四組，`fvm flutter test`）證實 `0.1.0-W3-165` 落地後仍有三項可稽核缺口，使「程式主動決定不告知使用者」這件事無法被事後追究：

| 缺口 | 現況 | 後果 |
|------|------|------|
| G1 訊息識別 | `closed` 事件的 `fields` 只有 `reason`，無 message 或關聯識別 | 無法回答「被截斷的是哪一則」。只能靠 `shown`／`closed` 的 FIFO 順序推斷，與其他 tag 的日誌交錯時不可靠 |
| G2 實際可見時長 | `closed` 不帶「這則實際被看到多久」 | 實驗 F4：「一幀都沒顯示」與「顯示滿 1 秒後被截斷」的日誌逐字相同，而兩者對使用者的嚴重度不同 |
| G3 未顯示的決定 | 因判準而不顯示的事件完全無事件 | 程式決定不告知使用者時零痕跡，該決定不可稽核 |

實驗同時確立判別式：全 repo 呼叫 `hideCurrentSnackBar` 只有兩處（`app_snack_bar.dart:121` 取預設 `reason: hide`、`:166` 明確 `reason: action`），故 `closed{reason=hide}` 恆等於「被後續一次 `show` 截斷」。G1／G2 補齊後，此判別式才足以支撐 `0.1.0-W3-167` 規劃的頻率量測協定——否則量測拿不到「被截斷的是不是 withAction」與「使用者實際看到多久」兩項指標。

### Related Files
- lib/components/app_snack_bar.dart  # from 0.1.0-W3-176
- test/unit/components/app_snack_bar_test.dart  # from 0.1.0-W3-176

### Source Acceptance (reference)
- [ref] [ ] shown 與 closed 事件攜帶可配對的關聯識別，配對不依賴 FIFO 假設  # from 0.1.0-W3-176
- [ref] [ ] 既有四個事件值的語意與既有測試斷言形態未被改動  # from 0.1.0-W3-176
- [ref] [ ] 截斷相關日誌的等級判別依據為事件發起者（使用者發起／背景發起），未以 variant 近似替代；元件內判定不到時由呼叫端傳參並於 Solution 記錄該選擇  # from 0.1.0-W3-176
- [ref] [ ] 重現 0.1.0-W3-167 實驗 A 的情境：同一同步區塊內連續兩次 show，兩則訊息在日誌上可配對且可辨識何者被截斷  # from 0.1.0-W3-176

