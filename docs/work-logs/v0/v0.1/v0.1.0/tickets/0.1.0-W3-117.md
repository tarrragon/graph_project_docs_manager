---
id: 0.1.0-W3-117
title: 重命名 「隔離索引提交的完整性三要件」章節去計數並同步五處錨點
type: DOC
status: closed
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-089
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-089
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 重命名 「隔離索引提交的完整性三要件」章節去計數並同步五處錨點
when: 0.1.0-W3-089 落地新要件後（屆時標題與內容的矛盾已具體存在，重命名的必要性可被驗證）
where:
  layer: Framework Docs
  files:
  - .claude/references/bash-tool-usage-details.md
  - .claude/references/hook-system-reference.md
  - .claude/skills/ticket/hooks/ticket-md-auto-commit-hook.py
  - .claude/skills/ticket/ticket_system/lib/git_ops.py
  - .claude/skills/ticket/ticket_system/commands/track_commit.py
why: '`.claude/references/bash-tool-usage-details.md` 的章節標題「隔離索引提交的完整性三要件」把計數寫進標題——這是 `reference-stability-rules` 規則 10（可變計數不實例化）的違例，只是至今沒有人需要新增第四項所以沒被觸發。


  `0.1.0-W3-089` 要補的兩項要件（基準釘選、CAS 後清理共用 index 過期 entry）一旦寫入，標題與內容立刻矛盾。由並行 session 於派發前查證發現。


  **為何不在 089 一起做**：該標題被 5 個實質檔案引用為跨檔錨點（`PC-BAL-008`、`hook-system-reference.md`、`ticket-md-auto-commit-hook.py` 兩處、`git_ops.py`、`track_commit.py`），改字即斷鏈（`DOC-V1-001`）。把「補內容」與「改跨檔錨點」放同一張票，是兩件性質完全不同的事，其中一件出問題會拖累另一件。089 因此採平行小節、標題不動，重命名由本票承接。


  這也是規則 10 的一個教學實例：**寫進標題的計數比寫進內文的更難修**，因為標題會成為錨點。內文的「三要件」改成別的說法只影響該句；標題改字要同步全部引用點。'
how:
  task_type: Documentation
  strategy: '先以 `rg -F -l -- "隔離索引提交的完整性三要件" .claude/` 重新確認引用點清單——本票 where.files 記的 5 處來自並行 session 的查證，但 `0.1.0-W3-089` 落地後可能新增引用，以實查為準（`DOC-GPD-004` 預防措施的錨點判定法）。


    新標題不帶計數，以語意命名（如「隔離索引提交的完整性要件」）。命名後**與全部引用點同一次改**——改字即斷鏈，分兩次做會留下一段時間的斷鏈期。


    程式碼檔（`.py`）中的引用多為註解或錯誤訊息字串，改動不影響行為；仍須確認沒有任何邏輯依賴該字串比對。


    改完後重跑一次 `rg -F` 確認舊標題字面零殘留。


    **不寫數字**：新標題與本次新增的說明皆不以要件數描述（規則 10）。'
acceptance:
- '[ ] 新標題不帶計數，且全部引用點於同一次變更中同步'
- '[ ] 引用點清單以 rg -F -l 實查取得，非沿用票面記載'
- '[ ] 改完後 rg -F 確認舊標題字面零殘留；程式碼檔中的引用確認無邏輯依賴該字串比對'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
closed_at: '2026-09-10T13:53:19'
closed_by: none
close_reason: not_executable_knowledge_captured
close_reason_note: 分析 context 已遷至 tarrragon/claude#55（區段：當前結論（graph-project-docs-manager：ticket CLI 契約與提交路徑））
---

# Execution Log

## Task Summary

重命名 「隔離索引提交的完整性三要件」章節去計數並同步五處錨點

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-089 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-089 what: 補強 隔離索引 CAS 配方：基準必須釘選為變數，用 HEAD 符號會同時廢掉 parent 與 CAS 舊值
- 0.1.0-W3-089 what: 補強 隔離索引 CAS 配方：基準必須釘選為變數，用 HEAD 符號會同時廢掉 parent 與 CAS 舊值

### Rationale Chain
- 0.1.0-W3-089 why: `bash-tool-usage-details.md` 的隔離索引 CAS 配方寫得對——步驟 1 明訂 `OLD_HEAD=$(git rev-parse HEAD)` 釘選基準，步驟 3 `read-tree "$OLD_HEAD"`、步驟 6 `commit-tree -p "$OLD_HEAD"`、步驟 8 `update-ref HEAD "$COMMIT_SHA" "$OLD_HEAD"` 全部用該變數。但配方沒有說明**不釘選會怎樣**，而「把 `$OLD_HEAD` 換成 `HEAD`」是一個看起來完全等價的替換。

實測命中（commit `4bd0e2ac`，PM 執行）：`read-tree HEAD` 與 `commit-tree -p HEAD` 分屬兩次 Bash 呼叫，其間並行 session 提交了 `eab601b2`。結果是 tree 取自舊 HEAD、parent 指向新 HEAD——**該 commit 靜默回滾了 `eab601b2` 對 `0.1.0-W3-088.md` 的變更**（`blockedBy` 被清回 `[]`）。

三層防護同時失效，而每一層單看都「通過」：

| 防護 | 為何沒擋住 |
|------|-----------|
| 要件 2（`GIT_INDEX_FILE`） | 確實生效——共用 index 全程未被觸碰。它保護的是寫入端，不保護基準的新鮮度 |
| 要件 3（tree 層級自檢） | 用 `$OLD_HEAD` 比對時必然通過：tree 相對它就是只有預期的檔案。要察覺得比對**新** HEAD |
| 步驟 8 的 CAS | 舊值寫 `HEAD` 時自我解析為當前 ref，比較必然相等——CAS 的全部保護力來自釘選舊值，不釘選等於沒有 |

現有的「完整性三要件」章節涵蓋不到這個模式：三要件談的是清單來源、寫入隔離、範圍自檢，全都是**空間維度**（提交了哪些檔案）；本案是**時間維度**（基準有多新），與規則七既有的「版本邊界（過期 index 快照）」同源，但發生在隔離索引路徑上——而該路徑正是被推薦用來取代共用 index 的做法，讀者不會預期它有同型風險。
- 0.1.0-W3-089 why: `bash-tool-usage-details.md` 的隔離索引 CAS 配方寫得對——步驟 1 明訂 `OLD_HEAD=$(git rev-parse HEAD)` 釘選基準，步驟 3 `read-tree "$OLD_HEAD"`、步驟 6 `commit-tree -p "$OLD_HEAD"`、步驟 8 `update-ref HEAD "$COMMIT_SHA" "$OLD_HEAD"` 全部用該變數。但配方沒有說明**不釘選會怎樣**，而「把 `$OLD_HEAD` 換成 `HEAD`」是一個看起來完全等價的替換。

實測命中（commit `4bd0e2ac`，PM 執行）：`read-tree HEAD` 與 `commit-tree -p HEAD` 分屬兩次 Bash 呼叫，其間並行 session 提交了 `eab601b2`。結果是 tree 取自舊 HEAD、parent 指向新 HEAD——**該 commit 靜默回滾了 `eab601b2` 對 `0.1.0-W3-088.md` 的變更**（`blockedBy` 被清回 `[]`）。

三層防護同時失效，而每一層單看都「通過」：

| 防護 | 為何沒擋住 |
|------|-----------|
| 要件 2（`GIT_INDEX_FILE`） | 確實生效——共用 index 全程未被觸碰。它保護的是寫入端，不保護基
  ... (truncated, see source ticket)

### Related Files
- .claude/rules/core/bash-tool-usage-rules.md  # from 0.1.0-W3-089

### Source Acceptance (reference)
- [ref] [ ] details.md 的三要件章節後新增一項要件，含失效機制、為何三要件擋不住、CAS 舊值為唯一偵測點三點  # from 0.1.0-W3-089
- [ref] [ ] 實測案例（tree 與 parent 分屬不同基準導致靜默回滾）以最小重現形式記入，含可執行的偵測與處置  # from 0.1.0-W3-089
- [ref] [ ] bash-tool-usage-rules.md 規則七「版本邊界」邊界段補交叉引用，指出隔離索引路徑有同型風險  # from 0.1.0-W3-089
- [ref] [ ] 既有三要件的內容未被改寫（章節標題的去計數重命名由 0.1.0-W3-116 另行處理）  # from 0.1.0-W3-089
- [ref] [ ] 配方新增收尾步驟：CAS 成功後檢查共用 index 是否留下相對新 HEAD 已過期的 entry，並給出偵測與處置命令  # from 0.1.0-W3-089
  ... (truncated, see source ticket)
