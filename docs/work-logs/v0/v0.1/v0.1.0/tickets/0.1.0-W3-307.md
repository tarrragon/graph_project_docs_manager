---
id: 0.1.0-W3-307
title: 對抗測試 改版條文的漏洞：能否完全合規而仍寫定未驗證的修法
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-304
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第一輪三張測試皆為正向使用 frame，未有任何一張嘗試繞過條文
  final_decision: 本票為對抗 frame，指派非 curator 執行者
  rationale: 找缺口與找漏洞是不同動作：缺口是條文給不出答案，漏洞是條文給得出答案而答案可被構造成錯的；第一輪只測了前者
who:
  current: linux
  history: {}
what: 對抗測試 改版條文的漏洞：能否完全合規而仍寫定未驗證的修法
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/references/escalation-flow.md
why: '**第一輪三張測試全是正向使用 frame**：拿條文去處理真實 issue，記錄它在哪裡給不出答案。**沒有任何一張嘗試繞過它。**


  **缺口與漏洞是不同的東西**：缺口是條文給不出答案；漏洞是條文給得出答案，而那個答案可以被構造成錯的。一個滿是缺口的條文至少會讓人卡住並求助；**一個有漏洞的條文會發出通行證**——使用者逐條照做、每一問都答「是」、產出一個未經驗證的修法，而且票面上與一個嚴謹的判定完全不可分辨。


  **這個形態在本專案已有實證**：`163/164` 通過第三問是合法的（可重跑的主張），事後複驗為 `316/320`。條文當時沒有失效，**它正確地放行了一個錯的數字**。


  **改版新增的兩個結構特別值得測**：「消費端」拆為執行條件／使用者群後多了一條綜合規則——**綜合規則是新的可操弄面**；兩檔分界改為 `should_exclude()` 加機械訊號的二軸表，而 `0.1.0-W3-301` 已量出定義本體全在 sync 範圍，**二軸表的實際分流結果是否如設計所想**未經檢驗。


  **不做則後果**：條文以「已測過」的身分擴散，而測過的只有它答不出的那一半。'
how:
  task_type: Analysis
  strategy: '**目標是構造反例，不是評論條文品質。** 產出是具體的構造，不是觀感。


    1. **讀改版後的條文全文**（`.claude/skills/framework-issue/references/escalation-flow.md`，commit `dd24bce4`）與其引用的 `ticket-intake.md`、`comment-as-section-protocol.md` 相關段。

    2. **嘗試構造至少三個通行證案例**：一個發現，其修法未經驗證，但走完條文三問全部答「是」、兩軸表落在不需降級的一側、票面完全合規。**每個案例要具體到可以寫成一張真實的票**（給出 action／target／why 的要點與三問的答案）。構造不出來也是結果，但須說明嘗試過哪些方向、各自被哪一句擋下。

    3. **重點攻擊面**：(a)「消費端」拆分後的綜合規則——兩個子問的組合是否存在一種答法，使實質未觀測的情形通過；(b) 二軸表——`should_exclude()` 為機械事實無判讀空間，但**另一軸是否有判讀空間**，兩軸如何合成；(c) 第二問「反例已試」——「試過」的最低標準是什麼，能否以一個不會失敗的觀測冒充。

    4. **對每個成功的構造，指出條文中最小的修補位置**，但**不改任何檔案**。


    **不對 canonical issue 寫入，不改 `.claude/` 資產，不跑 `sync-push`。**

    禁止的結論形態：「條文大致穩健」「建議持續觀察」——**無法被推翻的結論不是本票的產出**。'
acceptance:
- '[ ] 已嘗試構造通行證案例，成功者具體到可寫成一張真實的票（含三問答案）'
- '[ ] 構造失敗者已說明嘗試方向與各自被哪一句擋下'
- '[ ] 三個重點攻擊面（綜合規則／二軸表另一軸／反例最低標準）各有實際嘗試記錄'
- '[ ] 每個成功構造附條文中最小的修補位置，且未改任何檔案'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T14:29:03'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

對抗測試 改版條文的漏洞：能否完全合規而仍寫定未驗證的修法

---

## Problem Analysis
### 問題根因

第一輪三張測試皆為正向使用 frame（拿條文處理真實 issue，記錄它答不出的地方）。本票測的是相反的東西：**條文答得出、而答案可被構造成錯的**。

直接原因：改版條文（`escalation-flow.md`〈修法時點分離〉）的豁免閘門由四問 + 一條合成規則 + 一張二軸擴散面表構成。四問中有三問的字面允許作答者選擇「以什麼為證據」，而條文沒有限定所選證據必須與該問所問的對象同軌。

影響範圍：條文以「已測過」身分隨 sync 擴散至全部 consumer；通過閘門的票在票面上與嚴謹判定完全不可分辨。

### 影響範圍

受影響檔案：`.claude/skills/framework-issue/references/escalation-flow.md`〈修法時點分離：發現票何時能寫定修法〉全節。

**已驗證的事實**（本票實際執行的觀測）：

| 事實 | 觀測手段 | 結果 |
|------|---------|------|
| `should_exclude()` 對 hooks／rules／skills 定義本體皆為 False | `python3` 直接呼叫 `lib.sync_exclude_manifest.should_exclude` 於 5 個代表路徑 | 5/5 皆 False（與 `0.1.0-W3-301` 量測一致） |
| 本 consumer 的 `.claude/hooks/` 有機械訊號 | `ls .claude/hooks/tests` | `test_bare_commit_guard_hook.py` 存在 |
| `bare-commit-guard-hook.py` 未偵測 `-i`／`--include` | `grep -n` 該檔，第 298 行 `any(tok in ("--", "--only", "-o") ...)` | 確認遺漏；而 `bash-tool-usage-rules` 規則七明列 `--include`／`-i` 為禁用 |

**仍未驗證的假設**：本票構造的三張票皆為「可寫出」而非「已送出」——未實際建票、未實際送 issue。構造的有效性在於票面逐條對照條文字面成立，不在於它被誰接受過。

### 相關 Error Pattern

與 PC-165（測試綠燈不等於 runtime 正確）同型：本票揭露的是「判準綠燈不等於判定正確」——同一結構，載體由測試換成條文。另與 `escalation-flow.md` 自身引述的 `163/164 → 316/320` 案例同型（構造二即其可複製版）。

## 重現實驗結果
### 實驗方法

本票的「實驗」是對抗構造：以條文字面為規則書，嘗試產出一張**逐條合規、四問全綠、二軸表落在第一檔、而修法實質未驗證**的票。判定標準不是觀感，而是能否對每一問給出一個字面成立的答案，以及該答案的鑑別力是否涵蓋該問所問的對象。

三個指定攻擊面各獨立嘗試：(a) 消費端拆分後的合成規則；(b) 二軸表的非機械軸；(c)「反例已試」的最低標準。

### 實驗執行

**共用前提（三個構造都用到，已實測）**：

```
should_exclude('.claude/hooks/bare-commit-guard-hook.py')            -> False
should_exclude('.claude/rules/core/quality-baseline.md')             -> False
should_exclude('.claude/skills/framework-issue/references/escalation-flow.md') -> False
ls .claude/hooks/tests/test_bare_commit_guard_hook.py                -> 存在
ls -d .claude/skills/framework-issue/tests                           -> 存在
```

即：這些位置一律落在「隨 sync 擴散」一側（二軸表第 1 列不可達），但本 consumer 內**都有機械訊號**，故一律由第 2 列判為**第一檔**。第一檔一旦成立，「使用者群已觀測」即可合法填「不適用」，合成規則放行。**跨 consumer 這一維在此被完全移出檢查範圍，而移除它的依據（本地有測試）與它所問的事（別人是否在用）無關。**

---

#### 構造一（攻擊面 a + b）：正交事實升格為免答券

**可寫成的真實票**

- **action**：擴充 `bare-commit-guard-hook.py` 的 index-discarding 形式偵測，納入 `-i`／`--include`
- **target**：`.claude/hooks/bare-commit-guard-hook.py:298`（`_is_index_discarding_form`）
- **why 要點**：`bash-tool-usage-rules` 規則七明列四項禁用形式（`-- pathspec`／`--only`／`-o`／`-i`），強制層只實作前三項；`-i` 會把 index 中他人已 staged 的檔案一併送出，並行環境下與 `--only` 同等危險。

**四問作答（逐條字面成立）**

| 問 | 答 | 依據 |
|----|----|------|
| 執行條件已觀測 | 是 | 實跑該 hook，餵 `git commit -i <path>` 的 PreToolUse payload，實測放行（exit 0），非推定 |
| 使用者群已觀測 | **不適用** | 擴散面第一檔。判定依據：`should_exclude()`＝False，但本 consumer 內有機械訊號（`.claude/hooks/tests/test_bare_commit_guard_hook.py`，修法錯誤時紅燈）→ 二軸表第 2 列。依據與擴散面判準同址可查，符合條文對「不適用」的全部要求 |
| 反例已試 | 是 | 修法前餵 `-i` payload 得 exit 0、修法後得 exit 2；前提為錯（regex 未生效）輸出即不同。非沉默、非與「本來就沒問題」同形 |
| 範圍已量 | 是 | 1 檔、1 條判斷式、既有測試案例數由 `grep -c` 量到 |

合成規則：執行條件＝是 ∧ 使用者群＝不適用 → 消費端已觀測＝是。**四問全綠 → 豁免 → 發現當下寫定修法。票面完全合規。**

**未驗證之處**：`-i` 在其他 consumer 的用法零觀測。若任一 consumer 的票務／提交腳本正以 `git commit -i` 實作限縮提交（本專案沒有，但條文沒要求去看），此修法一旦 sync-push 即硬擋該 consumer 的全部提交路徑。**「使用者群已觀測」問的正是這件事，而豁免它的事實是「本地有測試」——本地測試對其他 consumer 的呼叫端鑑別力為零。**

**次生缺陷（順序循環）**：擴散面表的入口句是「三問任一為否時，**先依下表判定擴散面歸檔**」，即擴散面應在出現「否」之後判定；但「使用者群已觀測」列又說「擴散面判定為第一檔時填不適用」，要求擴散面在作答之前判定。兩種讀法都成立：先判擴散面則永遠不出現「否」，第二檔處置（issue 待裁列）成為不可達分支。這正是條文自己所寫「一個兩種讀法都成立的判準比沒有判準更糟」的實例。

---

#### 構造二（攻擊面 c + 範圍）：`163/164` 形態的可複製版

**可寫成的真實票**

- **action**：補註冊未列入 `settings.json` 的 hook
- **target**：`.claude/settings.json`
- **why 要點**：盤點 `.claude/hooks/` 下 122 個 hook 檔，其中 N 個未出現在 `settings.json` 的任何 matcher，形同已寫但從未觸發。

**四問作答**

| 問 | 答 | 依據 |
|----|----|------|
| 執行條件已觀測 | 是 | 實讀 `settings.json`，實跑那 N 個 hook 確認可執行 |
| 使用者群已觀測 | 不適用 | 同構造一路徑（第 2 列，本 consumer 有 hook 測試） |
| 反例已試 | 是 | 故意把盤點腳本的 hook 名打錯重跑，計數由 N 變 N−1；量測手段為錯時輸出不同，非沉默 |
| 範圍已量 | 是 | 122／N 由 `ls \| wc -l` 與 `grep -c` 量到，可重跑 |

**未驗證之處**：分母錯。`settings.json` 不是唯一註冊入口（hook 可被其他 hook 以子程序呼叫、可被 skill 內部呼叫、可由 `settings.local.json` 註冊），且「未註冊」名單中可能含刻意不註冊的 library 型 hook。**數字是量到的、可重跑的、反例觀測也真的做了——但反例驗的是「量測程式沒壞」，不是「量測範圍是對的」。** 與條文自己引述的 `163/164 → 316/320` 完全同形。

**條文對此明文放行**：「範圍已量」列寫「本問只管數字來源；量測手段自身的鑑別力由『反例已試』一問管」；而「反例已試」的字面是「若**修法或其前提**為錯就會產生不同輸出」——「**或**」允許作答者挑一個弱前提（量測程式可用）去證偽，不必碰承重前提（分母完整）。兩問各自把對方當守門人，「母體邊界是否正確」無人負責。

---

#### 構造三（攻擊面 c，純形態）：不會失敗的觀測

**可寫成的真實票**

- **action**：於〈回報前查重 SOP〉的 `dedup` 命令區塊補上 `--all`／`--issue <N>` 的說明
- **target**：`.claude/skills/framework-issue/references/escalation-flow.md`〈回報前查重 SOP〉
- **why 要點**：`--all` 只在第二檔段落出現一次，查重 SOP 自身的命令區塊未提，讀者照著 SOP 跑會只掃到本 consumer 擁有的 issue。

**四問作答**

| 問 | 答 | 依據 |
|----|----|------|
| 執行條件已觀測 | 是 | 實跑 `section_comment.py dedup --help`，實測旗標存在且 SOP 區塊未載 |
| 使用者群已觀測 | 不適用 | 第一檔（`should_exclude()`＝False，但 `.claude/skills/framework-issue/tests/` 存在 → 第 2 列） |
| 反例已試 | 是 | 若「該旗標存在」為錯，`--help` 輸出會不同。非沉默、非與「本來就沒問題」同形，且確實做過一個觀測 |
| 範圍已量 | 是 | 單一區塊，`grep -c` 量到 |

**未驗證之處**：該觀測的鑑別力只覆蓋「旗標是否存在」，完全不覆蓋修法的承重前提——「讀者是因為 SOP 沒寫才漏用 `--all`」。那是一個關於消費端行為的斷言，而消費端兩問已被「不適用」放掉。**「反例已試」的字面只要求觀測有輸出差異、不沉默；它沒有要求該差異與修法的正確性相關。** 任何修法都能配一個廉價的可證偽觀測。

---

#### 失敗的嘗試（記錄擋下的句子）

| 嘗試方向 | 被哪一句擋下 |
|---------|------------|
| 讓「執行條件已觀測」在物件實際未被執行的情況下答「是」 | 「在什麼情況下被執行、**執行時讀到什麼**，是**已實測而非推定**」——兩個合取項都具體且都指向被改物件本身，找不到字面成立的迴避讀法。四問中最穩健的一問 |
| 偽造擴散面第 1 列（`should_exclude()`＝True）以取得第一檔 | `should_exclude()` 是機械事實、無判讀空間；且 `0.1.0-W3-301` 已量出定義本體全在 sync 範圍，第 1 列對規則／skill 類發現恆不可達。此軸無漏洞 |
| 以「規模很小」直接取得豁免 | 「豁免軸是證據狀態，不是問題規模」＋「『修法看似顯然』不構成豁免依據」——明文封死 |
| 在第二檔處置上取巧（寫進 `how.strategy` 仍宣稱合規） | 第二檔處置寫得具體到動作層級（區段名、`階段=待條件`、`狀態=待裁票`、`add` vs `observe` 的判準指向 `comment-as-section-protocol.md`），無模糊空間。**條文的弱點不在第二檔怎麼做，而在幾乎沒有東西會被判進第二檔** |

### 實驗發現

**已驗證的事實**：三個攻擊面各構造出一個通行證案例，皆逐條對照條文字面成立，皆可直接寫成一張真實的票。條文在這三處**沒有失效——它正確地放行了三個未驗證的修法**。

**根因收斂為兩句**：

1. **二軸表第 2 列把一個與「使用者群」正交的事實（本地有機械訊號）升格為該問的免答券。** 這一列不是疏漏，是刻意加的——條文明寫「單靠 `should_exclude()` 會使第一檔恆空，須與機械訊號軸並用」。為了讓第一檔非空而引入的軸，恰好吃掉了跨 consumer 這一維。**第一檔從「恆空」直接翻到「幾乎恆滿」**（凡本 consumer 有測試／hook／CI 覆蓋的位置皆入第一檔）。
2. **「反例已試」只要求觀測有輸出差異，未要求該差異與修法的承重前提相關。** 加上「修法**或**其前提」的選言，作答者可自選最容易證偽的那一個。

**仍未驗證的假設**：本票未實際建票送審，故未驗證「這樣的票在人工複核下是否會被擋」。但條文的作用點正是取代人工複核——若結論依賴複核者眼力，該條文即未提供條文所承諾的判準。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: linux
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-304 | chars: 1240 -->

### Task Reference
- 0.1.0-W3-304 what: 落地 0.1.0-W3-300 的改進設計至 framework-issue 條文與引導層（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-304 why: **`0.1.0-W3-300` 的改進設計已完成分析，本票是其落地**——修法形狀在此票可寫定，因為它經過了三組實測與一次反向檢驗，不是發現當下的判斷。

**為何是一張票**：`0.1.0-W3-300` 明示四項改進彼此有依賴——2-A 依賴擴散面的操作化、4-B 與 3-A 共用同一個位址、1-A 與 7-1-B 重疊。分成四票會使同一位址被改四次且形狀不一致。

**兩項停手條件必須先解**（`0.1.0-W3-300` 逐項標明）：

- `0.1.0-W3-301`：`.claude/` 全樹的 sync 排除比例。候選 7-1-A（兩檔分界錨在 `should_exclude()`）的成立與否完全取決於這個數字——若第一檔接近空集合，該候選復活已被否決的票型閘門。
- `0.1.0-W3-303`：`add` 與 `observe` 的分界。候選 3-A 的載體依賴它；分界未定則第二檔處置無載體。

**反向檢驗的結果必須帶進落地**：`0.1.0-W3-300` 對收斂項一做了反向檢驗，**部分推翻了三張測試票的共同框架**——三者皆以「這一問能不能保證修法正確」為尺，而條文自述的職責是「使證據狀態可見」。依後者，`163/164` 通過第三問是**合法的**（它是可重跑的主張，且事後確實被重跑為 `316/320`）。**存活下來的問題比原報告窄，且落在第二問而非第三問。**

**由此得到一項對落地直接有效的警告**：三張票隱含指向的修法（在第三問下加一個關於分母的子問）**是錯的位置且有實際副作用**——前提查證會遞迴，第三問將永遠為「否」，三問退化為「一律先開分析票」，再次復活候選 C。

**不做則後果**：測試產出停在四張已完成的票裡，條文維持現形狀擴散至所有 consumer。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/framework-issue/references/ticket-intake.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/SKILL.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- .claude/agents/framework-issue-curator.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/references/comment-as-section-protocol.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/CHANGELOG.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 四項改進已落地於指名載體，形狀取自 0.1.0-W3-300 未重新設計  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- [ref] [x] 第三問未新增分母子問，存活問題落在第二問並已處置  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- [ref] [x] 已確認有效的三項逐項說明改動後是否仍成立  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- [ref] [x] 收斂項二的判準對同一輸入只能得出一種判定，否則已降為提示句並移出豁免閘門  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- [ref] [x] 引導層改動已使 curator 的必經路徑指向條文，以 grep 驗證非零命中  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
