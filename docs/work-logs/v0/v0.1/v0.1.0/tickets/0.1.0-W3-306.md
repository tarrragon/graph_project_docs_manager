---
id: 0.1.0-W3-306
title: 回歸測試 改版條文對第一輪六項缺口的實際效果：消失、位移或換形態
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-304
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第一輪三張測試票與統整票已落地改版（dd24bce4）；用戶裁示跑第二輪
  final_decision: 本票為回歸 frame，對照第一輪的既有缺口清單逐項複測
  rationale: 改版的效果至今是推論——0.1.0-W3-300 自述「不落條文因此全部改進候選的效果皆為推論」；不複測則無從分辨缺口消失與缺口位移
who:
  current: framework-issue-curator
  history: {}
what: 回歸測試 改版條文對第一輪六項缺口的實際效果：消失、位移或換形態
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/references/escalation-flow.md
why: '**改版的效果至今全是推論**。`0.1.0-W3-300` 自述「本票不落條文，因此全部改進候選的效果皆為推論」。`0.1.0-W3-304` 落了地，但它驗的是「有沒有照設計改」，不是「改了之後缺口還在不在」。


  **缺口有三種去向，外觀相同**：真的消失、位移到別處、換一種形態留在原處。**只有逐項複測分得出來**，而第一輪留下的六項失效點（`297` F1–F6）、四項（`298` G1–G4）、七項（`299`）是現成的對照基線。


  **特別需要複測的兩項**：


  - 第二檔處置已由「降為分析票」改寫為「待辦與來源／問題與方案」配對。**新形態有沒有載體、有沒有觸發器、非擁有者能不能執行**——這三問正是舊形態失效的原因，不會因為換了名字就自動解決。

  - 「消費端」已拆為執行條件／使用者群並加綜合規則。**拆分後是否仍讀得出兩義**是條文自己寫下的 fallback 條件（讀得出兩義則降為提示句移出豁免閘門），該條件成立與否須實測。


  **不做則後果**：帶著一份效果未經觀測的條文進入下一階段，而它的前一版正是因為同樣的理由才需要測。'
how:
  task_type: Analysis
  strategy: '**對照基線是第一輪的三份產出**：`0.1.0-W3-297`／`298`／`299` 的 Solution。逐項複測，**每項給出四選一的判定**：消失／位移（指出新位置）／換形態（描述新形態）／未變。


    1. **讀改版後的條文**（`git show dd24bce4` 或工作區現況，兩者若不同以工作區為準並記錄差異）。

    2. **重跑第一輪的同一批 issue**（`#97`、`#54`／`#55`／`#56`、`#103`／`#92`），對每一項既有缺口實際走一次該步驟。**不接受「條文已改所以應該好了」這種推論**——沒有實走的項目標記為未測，不標為已解。

    3. **兩項重點必測**：第二檔新處置的載體／觸發器／非擁有者可執行性；「消費端」拆分後的兩義性。

    4. **新缺口一併記錄**，但與既有缺口分開列——改版引入的新問題與舊問題未解，是不同的事。


    **測試期間禁止對 canonical issue 寫入**（唯讀操作不受限），不改 `.claude/` 資產，不跑 `sync-push`。

    **本 session 的 owner 識別為 `graph-project-docs-manager-5d`**，若流程需要它，用這個值；不需要則不用。'
acceptance:
- '[ ] 第一輪每一項既有缺口皆有四選一判定（消失／位移／換形態／未變），位移者指出新位置'
- '[ ] 未實走的項目標記為未測，未以推論標為已解'
- '[ ] 第二檔新處置的載體、觸發器、非擁有者可執行性三問已實測'
- '[ ] 「消費端」拆分後是否仍讀得出兩義已實測，並對照條文自訂的 fallback 條件'
- '[ ] 改版引入的新缺口與既有缺口分開列'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T14:28:52'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

回歸測試 改版條文對第一輪六項缺口的實際效果：消失、位移或換形態

---

## Problem Analysis
### 問題根因

改版（`dd24bce4`）的效果在本票之前全屬推論：`0.1.0-W3-300` 自述不落條文，`0.1.0-W3-304` 驗的是「有沒有照設計改」。缺口的三種去向（消失／位移／換形態）在票面外觀上相同，只有對第一輪的同一批輸入逐項複走才分得出來。

### 影響範圍

受測資產（工作區現況與 `dd24bce4` **無差異**：`git diff dd24bce4 -- .claude/skills/framework-issue/ .claude/agents/framework-issue-curator.md` 為空輸出，故以工作區為準即等同該 commit）：

- `.claude/skills/framework-issue/references/escalation-flow.md`（135 行，〈修法時點分離〉為主要改動面）
- `.claude/skills/framework-issue/references/ticket-intake.md`（新增〈步驟三〉修法閘門）
- `.claude/skills/framework-issue/SKILL.md`（路由表第三列新增收束時點）
- `.claude/agents/framework-issue-curator.md`（2.2.0 收束路由）
- `.claude/skills/framework-issue/references/comment-as-section-protocol.md`（「重複」格拆為射程重疊／不重疊）
- `.claude/skills/framework-issue/scripts/section_comment.py`（`todo` 子命令、`validate_todo_table`、`validate_owner`）

**已驗證的事實**：全部判定皆有實走的命令或實際輸入對應（見〈重現實驗結果〉）。
**仍未驗證的假設**：本票禁止對 canonical issue 寫入，因此 `add`／`init` 的實際 POST 行為未實跑，只驗到其前置檢查（`validate_owner`／`validate_todo_table`）這一層；前置檢查之後的行為以原始碼閱讀為據，於對應判定列標明。

### 相關 Error Pattern

`test-assertion-design-rules` 規則 E2（守衛的失效方向是放行，放行與「輸入本來就沒問題」在輸出上同形）——本票新缺口 N2 與該規則同型：擴散面判準第二列的「機械訊號」未區分修法錯誤的方向。實測 `escalation-flow.md` 查 `test-assertion\|E2` 為 0、反向於 `test-assertion-design-rules.md` 查 `framework-issue\|修法時點` 亦為 0，兩側仍無互指（第一輪 `0.1.0-W3-299` 4-3 列出的同一項）。

## 重現實驗結果
### 實驗方法

對照基線為 `0.1.0-W3-297`（F1–F6／S1–S5）、`0.1.0-W3-298`（G1–G4／R1–R5）、`0.1.0-W3-299`（`#103` 1-1～1-4、`#92` 2-1～2-3、引導層 4-1～4-4）三票的 Solution。每一項以「第一輪的同一個輸入」重走一次該步驟，四選一判定（消失／位移／換形態／未變），未實走者標未測。全程對 canonical issue 唯讀（`show`／`check`／`dedup`／`todo`／`gh api ... --jq .body`），無 `init`／`add`／`update`／`observe`／`create`／`close`。

### 實驗執行

逐條命令與原始輸出要點：

1. `git diff dd24bce4 --stat -- .claude/skills/framework-issue/ .claude/agents/framework-issue-curator.md` → 空輸出（工作區＝受測版本）。
2. `section_comment.py todo --status 待裁票` → rc=0，輸出「範圍：owned-issues 登記檔，20 張 issue，83 列」，逐列含 `[優先級][階段][狀態] owner=... 來源票=... acceptance=N`。
3. `todo --stage 待條件 --json` → 12 列，其中 10 列的「做什麼」欄指得出等待對象（「等待對象：…」「等 `0.1.0-W3-229`…」「（#60）」），2 列指不出。
4. `grep -rln` 於 `.claude/hooks/`、`.claude/settings.json` 查 `todo` 呼叫 → `todo` 未被任何 hook 或 settings 呼叫；`session-start-issue-check-hook.py` 呼叫的是 `check`（`settings.json` L588 註冊 SessionStart）。
5. `check 97 54 55 56 92 103` → 全部主警訊「未觸發」，最大時距 `3 days, 19:58:33`，門檻仍為 7 天；`#92` 有一則 `0:20:21` 的新觀測（並行 session）；`#103` 為「找不到『當前結論』區段」「索引缺失」。
6. `validate_owner()` 實跑五個值：`flutter-balance-77` OK、`graph-project-docs-manager-66` OK；`graph-project-docs-manager-5d` FAIL、`graph-project-docs-manager-dcead428` FAIL、`framework-issue-curator` FAIL。regex 為 `^[a-z0-9]+(-[a-z0-9]+)*-[0-9]+$`（序號段限純數字）。
7. `validate_todo_table()` 實跑：合法列 PASS；把「狀態」改成自由文字「尚未決定」→ ValueError，訊息含合法值集合與違規列原文；名稱非「待辦與來源*」的區段內容不受檢。
8. `should_exclude(Path(...))` 實跑 14 條路徑：`.claude/skills/**`／`.claude/rules/**`／`.claude/hooks/**`／`.claude/agents/**`／`.claude/references/**`／`.claude/state/**` 皆 False；`.claude/settings.local.json`、`.claude/hook-logs/x.log` 為 True；`docs/todolist.yaml`、`test/unit/foo_test.dart` **亦為 False**。函式簽名為 `(path: Path) -> bool`，傳 `str` 直接 AttributeError。
9. `dedup --keywords` 複測第一輪同三組：「規則七」20 筆（18 筆標「命中位置：（無）」）、「登記表」15 筆、「共用」30 筆；`dedup --help` 只有 `--keywords`，無上限／過濾旗標。
10. `show 97`（6 區段全 owner=`flutter-balance-77`、4 則觀測）、`show 92`（6 區段，3 則為 `graph-project-docs-manager-66`）、`show 103`（0 區段、1 則觀測）。
11. `gh api repos/tarrragon/claude/issues/comments/5629086207 --jq .body` 取回 `#103` 四項探針觀測全文，作為「消費端」兩義複測的輸入。
12. 交叉引用 grep：`ticket-intake.md` 對 `escalation-flow` 命中 1（〈步驟三〉修法閘門）、`framework-issue-curator.md` 命中（2.2.0 收束列）、`escalation-flow.md` 對 `comment-as-section`／`待辦與來源` 命中 2；`escalation-flow.md` 對 `test-assertion\|E2` 命中 0；`escalation-flow.md` 對 `取得證據\|觀測票` 命中 0。

### 實驗發現

**已驗證的事實**：上列 12 組輸出即判定依據，逐條可複跑。

**仍未驗證的假設**（不得以推論補）：
- `add`／`init` 的 POST 與索引回填未實跑（禁寫 canonical），只驗到前置檢查層；`cmd_add` 不要求呼叫者為既有區段 owner 這一點取自原始碼（`cmd_add` 僅呼叫 `validate_owner`／`validate_todo_table` 後即 `post_comment`），非 POST 實測。
- `todo` 的「預設只掃本 consumer 擁有的 open issue」與實際輸出（含 `owner=flutter-balance-77` 的列）不一致，成因為登記檔範圍語意，未進一步追。此為觀察，不作為任何缺口判定的依據。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-304 | chars: 1240 -->

### Task Reference
- 0.1.0-W3-304 what: 落地 0.1.0-W3-300 的改進設計至 framework-issue 條文與引導層（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-304 why: **`0.1.0-W3-300` 的改進設計已完成分析，本票是其落地**——修法形狀在此票可寫定，因為它經過了三組實測與一次反向檢驗，不是發現當下的判斷。

**為何是一張票**：`0.1.0-W3-300` 明示四項改進彼此有依賴——2-A 依賴擴散面的操作化、4-B 與 3-A 共用同一個位址、1-A 與 7-1-B 重疊。分成四票會使同一位址被改四次且形狀不一致。

**兩項停手條件必須先解**（`0.1.0-W3-300` 逐項標明）：

- `0.1.0-W3-301`：`.claude/` 全樹的 sync 排除比例。候選 7-1-A（兩檔分界錨在 `should_exclude()`）的成立與否完全取決於這個數字——若第一檔接近空集合，該候選復活已被否決的票型閘門。
- `0.1.0-W3-303`：`add` 與 `observe` 的分界。候選 3-A 的載體依賴它；分界未定則第二檔處置無載體。

**反向檢驗的結果必須帶進落地**：`0.1.0-W3-300` 對收斂項一做了反向檢驗，**部分推翻了三張測試票的共同框架**——三者皆以「這一問能不能保證修法正確」為尺，而條文自述的職責是「使證據狀態可見」。依後者，`163/164` 通過第三問是**合法的**（它是可重跑的主張，且事後確實被重跑為 `316/320`）。**存活下來的問題比原報告窄，且落在第二問而非第三問。**

**由此得到一項對落地直接有效的警告**：三張票隱含指向的修法（在第三問下加一個關於分母的子問）**是錯的位置且有實際副作用**——前提查證會遞迴，第三問將永遠為「否」，三問退化為「一律先開分析票」，再次復活候選 C。

**不做則後果**：測試產出停在四張已完成的票裡，條文維持現形狀擴散至所有 consumer。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/framework-issue/references/ticket-intake.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/SKILL.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- .claude/agents/framework-issue-curator.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/references/comment-as-section-protocol.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/CHANGELOG.md  # from 0.1.0-W3-304（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 四項改進已落地於指名載體，形狀取自 0.1.0-W3-300 未重新設計  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- [ref] [x] 第三問未新增分母子問，存活問題落在第二問並已處置  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- [ref] [x] 已確認有效的三項逐項說明改動後是否仍成立  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- [ref] [x] 收斂項二的判準對同一輸入只能得出一種判定，否則已降為提示句並移出豁免閘門  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
- [ref] [x] 引導層改動已使 curator 的必經路徑指向條文，以 grep 驗證非零命中  # from 0.1.0-W3-304（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
