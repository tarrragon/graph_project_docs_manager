---
id: 0.1.0-W3-647
title: 修正 ticket track commit 的隔離索引基準選取造成分支繞過 metadata commit
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-638
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-638 收尾時命中分支繞過，已就地 rebase 修復
  final_decision: 修基準選取並補同型回歸測試
  rationale: 本次靠執行者察覺票面回退為 pending 才發現；若它沒察覺，分支會帶著缺漏的歷史被合併且無任何訊號
who:
  current: thyme-python-developer
  history: {}
what: 修正 ticket track commit 的隔離索引基準 ref 選取，使其不會繞過同 session 先前由 finish 與 append-log 產生的 commit
when: W3-638 收尾時實際命中後
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system
why: W3-638 實際命中並就地以 rebase 修復：ticket track finish 先產生五個 metadata commit（Problem Analysis／Solution／Test Results／check-acceptance／completion sync），隨後以 ticket track commit 提交測試檔變更時，該命令的隔離索引基準落在那五個 commit 之前，提交後分支歷史繞過它們——working tree 內容不受影響，但 commit 歷史遺漏，且 ticket md 一度回退為 pending。這是 bash-tool-usage-rules 規則七「隔離索引 CAS 的時間維度要件」所述的同型風險在本 CLI 內的實例：CAS 推進 HEAD 後，以符號 HEAD 或過期變數為基準的後續操作即落在舊基準上。危害形態是安靜的——git status 乾淨、測試全綠、只有歷史與票面狀態不對
how:
  task_type: Implementation
  strategy: 查 ticket track commit 取得基準 ref 的時機與來源：若取自命令啟動時的快照而非執行寫入前的即時值，改為寫入前重讀；若已釘選變數，檢查該變數是否在 finish 系列 commit 之後才被更新。修法後以回歸測試覆蓋本次形態——同一 session 內先跑 finish 系列再跑 commit，斷言最終分支歷史含全部 metadata commit 且票面狀態為 completed。另評估 commit 成功後是否應驗證 HEAD 的祖先鏈含本次寫入前的 HEAD，不含則拒絕並回報
acceptance:
- '[ ] ticket track commit 的隔離索引基準取自寫入前的即時 HEAD，不會落在同 session 先前 commit 之前'
- '[ ] 回歸測試覆蓋本次形態：同 session 先 finish 系列再 commit，斷言歷史完整且票面為 completed'
- '[ ] 以修法前的程式碼跑該回歸測試確認其翻紅（E2 正向對照）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-21'
updated: '2026-09-21'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修正 ticket track commit 的隔離索引基準 ref 選取，使其不會繞過同 session 先前由 finish 與 append-log 產生的 commit

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-638 | chars: 639 -->

### Task Reference
- 0.1.0-W3-638 what: domain_view_test.dart 補泳道·尚未選定UC（swimlaneUcUnsetPrompt）與泳道·flow未結構化（flowUnstructuredMessage）兩狀態的 widget test 型別斷言。實作已存在於 domain_view_screen.dart:495 與 :531，缺的是測試覆蓋（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-638 why: W3-637 三軸重判項 1 與 2：domain_view_screen.dart 已渲染 swimlaneUcUnsetPrompt(:495) 與 flowUnstructuredMessage(:531)，但 domain_view_test.dart 對兩者零命中。W2-001 how 句型未含這兩個狀態，因此原票的 widget test 也未覆蓋（來源已完成，結論見 Solution）

### Related Files
- test/unit/screens/domain_view_test.dart  # from 0.1.0-W3-638（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] domain_view_test.dart 含 swimlaneUcUnset 狀態的 widget test，斷言 EmptyState.section 與 SwimlaneGrid 存在且 BadgeRow.legend 不存在  # from 0.1.0-W3-638（來源已完成，結論見 Solution）
- [ref] [x] domain_view_test.dart 含 flow未結構化 狀態的 widget test，斷言 EmptyState.section 存在  # from 0.1.0-W3-638（來源已完成，結論見 Solution）
- [ref] [x] flutter test test/unit/screens/domain_view_test.dart 全綠  # from 0.1.0-W3-638（來源已完成，結論見 Solution）

