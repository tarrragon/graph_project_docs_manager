---
id: 0.1.0-W3-025
title: 判定 skill 發佈庫 6 支版號相同但內容分歧的同步方向
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: general-purpose
  history: {}
what: 逐支比對本地與 claude-skills.git 的差異，判定該 push 或 pull，並釐清為何改了內容卻未 bump 版號
when: skill 發佈庫同步時；下次 sync-push 前
where:
  layer: framework
  files:
  - .claude/skills/ticket/SKILL.md::write
  - .claude/skills/ticket/CHANGELOG.md::write
  - .claude/skills/framework-issue/SKILL.md::write
  - .claude/skills/framework-issue/CHANGELOG.md::write
  - .claude/skills/tdd/SKILL.md::write
  - .claude/skills/tdd/CHANGELOG.md::write
  - .claude/skills/wrap-decision/SKILL.md::write
  - .claude/skills/wrap-decision/CHANGELOG.md::write
  - .claude/skills/worktree/SKILL.md::write
  - .claude/skills/worktree/CHANGELOG.md::write
  - .claude/skills/skill-sync/CHANGELOG.md::write
why: 版號相同而內容不同代表至少一邊改了東西沒 bump 版號。版號在此情況下不只無鑑別力，而是誤導性的——它主動宣稱兩邊一致。sync-push 的措辭已明示內容雜湊只證明不同、不證明誰較新
how:
  task_type: Documentation
  strategy: clone claude-skills.git 至暫存 -> 逐支讀 diff -> 判定方向 -> skill-sync push 或 pull -> 對未 bump 的一方補版號
acceptance:
- '[x] 六支各有明確方向判定與依據，非憑版號推斷'
- '[ ] 已執行對應的 skill-sync push 或 pull，再跑 sync-push 時該六支不再出現在分歧清單'
- '[ ] 改了內容未 bump 版號的一方已補上版號'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-07T10:22:21'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

逐支比對本地與 claude-skills.git 的差異，判定該 push 或 pull，並釐清為何改了內容卻未 bump 版號

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### 已完成的比對（2026-09-07，clone 自 claude-skills.git @ 2159e8d / 2026-09-04）

比對方法：clone 發佈庫後逐檔 sha1 比對，排除 `.git` / `.venv` / `__pycache__` /
`node_modules` / `.pytest_cache` / `*.pyc` / `*.lock` / `*.egg-info`。

**注意數字不一致**：本比對得 17 支分歧，sync-push 腳本回報 12 支。差異來自過濾規則
不同（腳本額外排除 `.skill-sync-base` 等同步元資料）。下表以本次實際比對為準，
不宣稱與腳本數字相符。

#### 已處理（不在本票範圍）

| skill | 方向 | 處置 |
|-------|------|------|
| skill-design-guide | 本地 1.7.3 > 遠端 1.2.0 | 已 push |
| design-decision-framework | 本地 1.3.0 > 遠端 1.2.0 | 已 push |
| multi-round-review | 遠端 1.75.1 > 本地 1.64.0 | 已 pull |
| compositional-writing | 遠端 1.7.1 > 本地 1.5.2 | 已 pull |
| component-contract-design | 本地 1.5.2 > 遠端 1.0.0 | **push 被 portability 閘門擋下**，另票追蹤 |

#### 本票範圍：版號相同但內容分歧（6 支）

| skill | 版號 | 差異 | 已知線索 |
|-------|------|------|---------|
| ticket | 2.20.0 | 本地多 11 檔、65 檔內容不同 | 最大宗，需優先 |
| framework-issue | 1.4.0 | 本地多 `scripts/owned_issues_registry.py` 與其測試；`section_comment.py` 兩邊不同 | 本地疑似有未發佈功能且未 bump 版號 |
| tdd | 2.2.7 | 遠端多 `examples/flutter-sdk-tdd-walkthrough.md`；`layer-boundary-validator-hook.py` 與 `references/phase4-refactor.md` 兩邊不同 | 遠端疑似有未拉回的內容 |
| wrap-decision | 2.11.0 | 兩個 hook 兩邊不同 | 本地多的 `references/project-integration/` 依該 skill 自述屬各專案自備、**不該推**，比對時須排除 |
| skill-sync | 無版號 | `skill_sync/cli.py` 與 `tests/test_cli.py` 兩邊不同 | 無版號可比，只能讀 diff |
| worktree | 1.1.0 | `SKILL.md` 兩邊不同 | 本地多的 `egg-info` 為建置產物、不該推 |

#### 非實質分歧（不需處理，記錄以免重查）

`doc` / `foundation-design` / `neurodivergent-output` / `version-bootstrap` /
`version-sequencing` 只差本地產生的 `.skill-sync-base`；`version-release` 只差
遠端的 `.version-release.yaml`。

#### 本票要回答的根本問題

六支都是「改了內容卻沒 bump 版號」。**版號在這種情況下不只是無鑑別力，而是誤導性的**
——它主動宣稱兩邊一致，使分歧不會被例行檢查發現。判完方向之後應一併問：是否需要在
`skill-sync push` 加一道「內容變更但版號未變」的閘門。

### 派發日誌

- [2026-09-07 10:21] 比對結果已寫在票的 Problem Analysis，含 clone 指令與六支的已知線索，不需重跑調查。wrap-decision 的 references/project-integration/ 與 worktree 的 egg-info 屬不該推的本地產物，比對時排除。

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-07 10:35)
  - what: skill-sync push 加一道「內容變更但版號未變」閘門：偵測本次 push 的內容雜湊與已記錄的 .skill-sync-base 不同、但 SKILL.md frontmatter 的 metadata.version（或 CHANGELOG 首條版號）與遠端相同時，於 preview 印出警告並要求確認
  - why: 本票六支的分歧全數是這個形態：內容改了、版號沒動。版號相同時它不只是無鑑別力，而是主動宣稱兩邊一致，使分歧不會被例行同步檢查發現——六支累積到需要一張票逐支讀 diff 才判得出方向，成本全部來自這一點。閘門放在 push 端（寫入者手上仍握有變更脈絡）比放在事後比對端便宜
  - suggested_type: IMP
  - suggested_priority: P2
  - related_files: 
  - context: 
  - status: pending


- **SR-2** (2026-09-07 10:35)
  - what: 修正 worktree/SKILL.md「Base ref 與隔離邊界」表的重複列：daemon-rooted 寫入工具洩漏 出現兩次（第二列的說明欄只寫「同上一列」），修好後重推發佈庫
  - why: 重複列已隨本票的 push 傳播到發佈庫與所有 consumer。表格的三欄結構（項目／機制／對策）在重複列上退化為指回自身，讀者無從判斷第二次出現是筆誤還是另一種洩漏路徑
  - suggested_type: DOC
  - suggested_priority: P3
  - related_files: 
  - context: 
  - status: pending

- **SR-3** (2026-09-07 10:35)
  - what: 盤點發佈庫（claude-skills）中存在、但框架 canonical repo（tarrragon/claude）沒有的檔案：逐 skill 比對兩庫的檔案集合，判定各筆屬「應回收進 canonical」或「應 prune」
  - why: 本票在 tdd 發現一筆這種殘留（examples/flutter-sdk-tdd-walkthrough.md），來源是繞過 canonical repo 直接寫入發佈庫。此類檔案不會出現在任何 consumer 的 sync-pull 結果中，卻會在每次 skill-sync push（未帶 --prune）時被保留下來，形成只在發佈庫存在的分叉。既然已知不只一種寫入路徑，殘留數量無法由單一案例推斷
  - suggested_type: ANA
  - suggested_priority: P2
  - related_files: 
  - context: 
  - status: pending

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程或 thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
