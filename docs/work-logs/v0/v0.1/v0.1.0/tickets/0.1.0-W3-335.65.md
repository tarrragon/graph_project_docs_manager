---
id: 0.1.0-W3-335.65
title: 裁決 V4 第四輪 11 項門檻外 spec 間矛盾（WRAP）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-335
children: []
blockedBy: []
relatedTo:
- 0.1.0-W3-335.64
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-335.64 門檻外矛盾
  final_decision: WRAP 裁決後追修
  rationale: 用戶裁示先清矛盾
who:
  current: general-purpose
  history: {}
what: 裁決 V4 第四輪 11 項門檻外 spec 間矛盾（WRAP）
when: W3-335.64 完成後
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.65.md
why: 用戶裁示（2026-09-15）解除 W2 前先清矛盾與實作落差；W3-335.64 列出 11 項未裁決矛盾，其中 2 項疑為前輪裁決未落實；簽核改由 WRAP（風格偏好除外）
how:
  task_type: Analysis
  strategy: 讀 .64 Solution〈本輪 spec 間矛盾〉與 V4R4 探針表 → 核對 spec 現文 → 疑未落實者對照 .47/.53/.59 裁決確認 → WRAP 裁決 → 依檔分組追修清單；依賴 CLAUDE.md §6 現行待決者標明綁定票不裁決
acceptance:
- '[x] 11 項逐項：原文位置、WRAP 候選（含反向框架）、裁決或綁定待決票、改動位置與改法'
- '[x] 每項標註是否落在 W2-001/002/003/005 或已完成 W2-004/006/007 的畫面範圍'
- '[x] 依檔分組追修清單；風格偏好項交用戶；本票不改 spec、不建票'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-15T01:17:22'
completed_at: null
created: '2026-09-15'
updated: '2026-09-15'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

裁決 V4 第四輪 11 項門檻外 spec 間矛盾（WRAP）

---

## Problem Analysis
### 問題根因

V4 第四輪（`0.1.0-W3-335.64`）列出 11 項門檻外 spec 間矛盾，逐項讀 spec 現文後成因分四型：
- 上游定案未機械同步（6 項）：`FlowStep.traverses` 定案（`0.1.0-W3-345`）後設計約束「寫死座標」未改（V4R4-02 G18）；`.56` 移除 docs/ 判準但空圖進入條件殘留「`docs/` 存在」（V4R4-02 G23）；`.19` 破洞事件類寫入選定 UC 未回寫 SPEC-003 §2.8 入口表（V4R4-03 G15）；SPEC-004 已定 `LoadingState` 骨架版位 slot 與實作 `SkeletonLayout`，SPEC-003 §2.11 仍寫「兩個參數」（V4R4-04 G04）；SPEC-004 畫布與 §3.6 已有矩陣圖例，SPEC-001 §1 矩陣顯示欄未回寫（V4R4-02 G08 前半）；SPEC-004 將重新整理歸 `MissingSourceState` 內，SPEC-003 §2.4 括號仍列為頁面級動作（V4R4-04 G32）
- 欄位語意範圍不足（2 項）：〈返回為殼層通則〉只涵蓋退出路徑欄，未涵蓋可用操作欄的「切換專案」（V4R4-02 G22）；錨點動態段 `<domainId>` 無取值來源，而資料面有 PascalCase（traverses）與小寫（`depends_on_domains`）兩套（V4R4-03 G04）
- 條文與判準未對齊（2 項）：§2.13 (b1) 機械條件對破洞掃描 `denied` 分支推出「不提示」，與用戶簽核的 §2.2 fallback 相反且無具名例外（V4R4-04 G17）；SPEC-001 §6 原始檔已消失進入條件寫世界狀態，SPEC-003 §3.6 只有一個偵測點（V4R4-04 G22）
- 依賴 CLAUDE.md §6 待決（1 項）：「間接依賴」判定式未定義（V4R4-02 G09，含 G08 後半的二值／三值）

### 影響範圍

- 已驗證：SPEC-001 v1.15 §1、§2、§5、§6、〈返回為殼層通則〉、§8.1、〈Gate 三問對照〉、FR-03／FR-07、〈設計約束〉；SPEC-003 v1.36 §2.2〈系統層通知〉、§2.4、§2.5、§2.6、§2.7、§2.8〈選定 UC〉、§2.9、§2.11、§2.13、§3.1、§3.2、§3.5、§3.6、〈設計約束〉；SPEC-004 v1.42 §3.1 總表、§3.6 §1 列、§3.7 第 17、18 項、4.24、4.28、4.38；UC-01〈空專案〉、UC-02 驗收條件；`docs/domain-map.md` §2.5「貫穿」；`lib/components/loading_state.dart`（`SkeletonLayout`）、`lib/components/missing_source_state.dart`（重新整理在元件內）、`lib/components/swimlane_grid.dart`（`SwimlaneLane.nodes` 為 (節點, 欄) 對）、`lib/screens/gap_report/`（無 ucFlow／nodeDetail／tickets 跳轉）
- 前輪裁決核對：G23 屬 `.56` E6 前置裁決「docs/ 存在性不再影響判定」的落實缺口（該票同步 grep 只查「資料夾可讀但無」「沒有 `docs/`」字面）；G18 前輪無裁決——`.47` D6 僅以該約束作 R 事證，非「已裁未落實」，而是 `W3-345` 定案後無人回寫
- 未驗證假設：`depends_on_domains` 小寫與 traverses PascalCase 在 Graph 聚合時是否需正規化（屬 graph／schema 資料面，UI 規格不裁）

### 相關 Error Pattern

`reference-stability-rules` 規則 10（可變計數不實例化：「兩處設定入口」「兩個參數」皆為寫死計數，增列即失效）；`.59` S3-P1／`.53` E11 同型（列舉承載的集合在上游增列時未同步）。

## 重現實驗結果
### 實驗方法

本票為規格裁決，「重現」＝逐項以原文核對探針卡點是否仍成立。探針：scratchpad `probes/a/V4R4-02.md`（G08、G09、G18、G22、G23）、`V4R4-03.md`（G04、G15）、`V4R4-04.md`（G04、G17、G22、G32）猜測點表。對每項 Read 兩端原文（檔名＋章節標題），再以 grep 找同一規則詞在 `docs/spec`、`docs/usecases`、`docs/domain-map.md`、`lib/` 的全部命中，列同步點。前輪裁決對照 `.47`／`.53`／`.56`／`.59` Solution（以 grep 關鍵詞「寫死」「靜態版型」「docs/」「switch-project」「設定入口」核對）。

### 實驗執行

- grep 實跑：`寫死座標\|靜態版型`（SPEC-001〈設計約束〉、SPEC-003〈設計約束〉、SPEC-004 §3.1 `SwimlaneGrid` 列、4.38 用途／狀態矩陣資料態／步驟欄寬列）；`設定入口`（SPEC-001 §1 泳道 · 尚未選定 UC 註記、SPEC-003 §2.8 首段與入口表、§3.1、§3.2）；`(b1)`（SPEC-001 §8.1 兩列與段落、SPEC-003 §2.13、§2.17）；`兩個參數\|「目標態」與「進度型別」`（SPEC-003 §2.11、SPEC-004 4.24 用途）；`重新整理.{0,6}重新掃描\|頁面級動作`（SPEC-003 §2.4、§2.7；SPEC-004 §3.1 `ButtonRow`、§3.7 第 17／18 項、4.28）；`外部刪除`（僅 SPEC-001 §6）；`legendIndirect\|間接依賴`（SPEC-003 §3.1、SPEC-004 §3.2／§4.0.6／4.15、UC-02 驗收、CLAUDE.md §6、ARB）；`domainId`（SPEC-003 §3.1／§3.2、SPEC-004 4.15／4.19／4.37／4.38）
- 綁定票查找：`^title: .*定案` 全票掃描——有 `0.1.0-W3-364`（App 已知範圍）、`W3-365`、`W3-369`；**無**「間接依賴」與「泳道布局演算法」承接票
- 實作面：`lib/components/loading_state.dart` 已有 `enum SkeletonLayout`；`missing_source_state.dart` 內建 `action-nodeDetail-refresh`；`lib/screens/gap_report/` 只有 `AppDestination.domain`／`gaps` 導向

### 實驗發現

- 11 項矛盾於現文全數仍成立（無「已被後續版本消解」者）
- 前輪衝突：推翻 0；G23 為 `.56` 落實缺口；G22（V4R4-02）與 `.59` S1-P2／S4-P3 同向延伸；G17 與 `W3-063` 用戶簽核同向（補具名例外而非改行為）
- 依賴待決 1 項（G09），G08 後半併入
- 風格偏好 0 項：G32 的按鈕位置已由 SPEC-004 §3.1 `ButtonRow`、4.22 與實作定案，屬同步而非偏好
- 觀察（本票範圍外，交 W2 落差盤點）：`W2-004` 破洞報告實作無依指向型別跳轉（ticket／nodeDetail／ucFlow），晚於 `.19` 裁示

## Solution
### 裁決總表

W2 畫面範圍：待實作 `W2-001` Domain（SPEC-001 §1）、`W2-002` UC Flow（§2）、`W2-003` Ticket（§4）、`W2-005` 節點詳情（§6）；已完成 `W2-004` 破洞報告（§5）、`W2-006` 浮層（§7）、`W2-007` 追溯（§3）。

| # | 探針項 | 一句話裁決 | 類型 | W2 範圍 | 前輪關係 |
|---|-------|-----------|------|--------|---------|
| K1 | V4R4-02 G08 | SPEC-001 §1 正常 · 矩陣顯示欄回寫「底部圖例」；圖例值域（二值／三值）併 K2 綁定 | 裁決＋部分綁定 | W2-001 | 無衝突 |
| K2 | V4R4-02 G09 | 「間接依賴」判定式屬 CLAUDE.md §6 待決，不裁；無承接票，建議建票；UC-02 驗收「直接貫穿判定式未定義」已過時可同步 | 綁定待決 | W2-001 | `.37` 同判（`.36` #4 由待決承接） |
| K3 | V4R4-02 G18 | 改寫 SPEC-001〈設計約束〉：節點所屬泳道列依 `traverses` 計算，列序／欄序由 fixture 給定、欄寬列高靜態，刪「寫死座標」「泳道指派」 | 裁決 | W2-001 | 前輪無裁決（非未落實） |
| K4 | V4R4-02 G22 | 〈返回為殼層通則〉擴及可用操作欄的「切換專案」，並明寫內容區切換專案鈕僅限三個阻擋狀態 | 裁決 | W2-001 | 延伸 `.59` S1-P2、S4-P3、`.53` E9 |
| K5 | V4R4-02 G23 | 空圖進入條件刪「`docs/` 存在」，改「解析成功、節點數為 0（`docs/` 不存在時亦同，此時不渲染開啟 docs）」 | 裁決 | W2-001 | 落實 `.56` E6 前置 |
| K6 | V4R4-03 G04 | SPEC-003 §2.9 補動態段取值規則：`<domainId>` 取 `FlowStep.traverses` 元素字面值、不轉大小寫；`<ucId>`／`<nodeId>`／`<evtId>` 取節點 ID 字面值 | 裁決 | W2-001、W2-002 | 無衝突 |
| K7 | V4R4-03 G15 | SPEC-003 §2.8 補設定入口 4（破洞項事件類），首段「兩處設定入口」改不寫數；SPEC-001 §1 泳道 · 尚未選定 UC 註記改限「使用者主動選擇」 | 裁決 | W2-002（讀方）、W2-004（寫方，已完成） | 回寫 `.19` 第二輪、`.38` S-31 |
| K8 | V4R4-04 G04 | SPEC-003 §2.11 改為目標態、進度型別、骨架版位三項差異，並註明破洞目標態為執行期 `returnTo`；SPEC-004 4.24 用途同步 | 裁決 | W2-001、W2-003；W2-004（已完成，實作已符） | 無衝突 |
| K9 | V4R4-04 G17 | 維持 `denied` fallback；SPEC-003 §2.13 (b1) 補具名例外「(a) 適用範圍內的事件不走 (b1)」，對照表 #5／#6 與 SPEC-001 §8.1 段落理由同步 | 裁決 | W2-004（已完成，實作已符） | 與 `W3-063` 用戶簽核同向；不推翻 `.37` R12 |
| K10 | V4R4-04 G22 | SPEC-001 §6 原始檔已消失進入條件改為偵測點式：「正常／部分損壞下開啟原始檔得 `notFound`」；jump 與點關聯不檢查檔案存在 | 裁決 | W2-005 | 與 `.59` S1-P1(a) 同向 |
| K11 | V4R4-04 G32 | SPEC-003 §2.4 括號刪「重新整理」，改列重新掃描與開啟原始檔，並註明狀態元件內動作不屬頁面級 | 裁決 | W2-005 | 與 SPEC-004 §3.7 第 17 項同向 |

統計：裁決 10（K1 部分）、綁定待決 1（K2）、交用戶 0、推翻前輪 0。

### 逐項評估（WRAP 完整模式）

#### K1（V4R4-02 G08）矩陣圖例存在性與值域

**原文位置**：SPEC-001 §1 正常 · 矩陣顯示欄（無圖例）；SPEC-003 §3.1〈格詳情卡的內容契約〉關係種類列「圖例三值之一…與該格符號一致」；SPEC-004 §3.2 視覺單元盤點 Main 列「底部圖例：● 直接貫穿 ○ 間接依賴 · 無關」、§3.6 §1 正常 · 矩陣列 `Panel`[`MatrixGrid`, `BadgeRow.legend`]；SPEC-001 §1〈矩陣格與泳道列的判定依據〉「traverses 包含即屬該格」。
**現文核對**：兩個子矛盾。(a) 圖例存在性：SPEC-004 畫布與歸屬表皆有，SPEC-001 顯示欄漏——SPEC-001 §1 註記自述顯示欄結構出處為 SPEC-004 §3.2，屬回寫缺漏。(b) 值域：traverses 包含比對只產生「屬／不屬」兩值，`indirect` 無產生式——即 K2。
**選項**：O1 SPEC-001 顯示欄補圖例（改造既有）；O2 SPEC-004 移除矩陣圖例、符號改 tooltip（改造既有，另一端）；O3 反向框架：矩陣不區分關係種類，格只標有／無。
**R 事證**：SPEC-004 §3.6 列與 4.15 `MatrixCell` `indirect` 變體、ARB `legendIndirect` 皆已存在；O2 使格符號無說明（SPEC-003 §2.2 可點性辨識精神）；O3 違反 UC-02 驗收「矩陣的每一格明確區分直接貫穿、間接依賴、無關三種狀態」。
**A**：本項只補 SPEC-001 一格，不預設 K2 結果——即使 K2 定案為 0.1 無 indirect，圖例仍存在。
**裁決**：(a) O1；(b) 併 K2，不裁。
**被放棄選項優點**：O3 使二值判準自洽、無待決依賴。
**選定方案缺點**：K2 定案前圖例列出一個無產生式的值。
**P 失敗訊號**：`W2-001` 實作時無 fixture 能產生 `indirect` 格 → 由 K2 承接票決定 0.1 fixture 是否直接指定關係種類（比照 SPEC-001 §3〈第二跳欄位待決〉「fixture 直接給定」先例）。
**改法**：SPEC-001 §1 正常 · 矩陣顯示欄「+ 右欄格詳情卡區（…）」前補「+ 底部圖例（關係種類：直接貫穿／間接依賴／無關）」。
**同步點**（`grep -rn "圖例" docs/spec/ui/SPEC-001*`）：§1 正常 · 泳道顯示欄「底部圖例」（泳道圖例為 active／inactive，不同值域，不改）。

#### K2（V4R4-02 G09）「間接依賴」判定式

**原文位置**：SPEC-003 §3.1〈格詳情卡的內容契約〉`legendIndirect`；UC-02〈驗收條件〉「矩陣的每一格明確區分直接貫穿、間接依賴、無關三種狀態（「直接貫穿」與「間接依賴」的判定式皆未定義，見 §9）」；`docs/domain-map.md` §2.5「貫穿」定義「經依賴邊間接到達者不列」、§9「`鄰接查詢` 的簽章未定…矩陣的『間接依賴』判定會落在這個 API 上」；CLAUDE.md §6 五項空殼判準之「間接依賴」。
**判定**：依賴 CLAUDE.md §6 現行待決，本票不裁決。綁定票：**無**（全票 `^title: .*定案` 掃描僅 `W3-364`／`W3-365`／`W3-369`／`W3-367` 等，無此項）。
**建議建票**：「定案『間接依賴』判準（矩陣格關係種類第三值）」——範圍含判定式（候選方向：traverses × domain-map §2 依賴邊的 1-hop 推導；需先定 §9 `鄰接查詢` 簽章）、0.1 fixture 是否直接指定關係種類、定案後回寫 SPEC-001 §1〈矩陣格與泳道列的判定依據〉補「traverses 包含只判定直接貫穿」。
**可同步（不依賴待決）**：UC-02 驗收註「『直接貫穿』與『間接依賴』的判定式皆未定義」中「直接貫穿」已由 `0.1.0-W3-345` 定案（traverses 包含），改「『直接貫穿』判定式為 `FlowStep.traverses` 包含（`0.1.0-W3-345`）；『間接依賴』判定式未定義，見 §9」。

#### K3（V4R4-02 G18）0.1 泳道「寫死座標」與依 traverses 放節點

**原文位置**：SPEC-001〈設計約束〉「0.1 的泳道只做靜態版型…泳道以寫死座標的假資料畫出形狀即可。真正的排序、泳道指派、邊繞線需要真實的 flow 資料」；SPEC-001 §1 正常 · 泳道顯示欄與〈矩陣格與泳道列的判定依據〉「該步驟…置於該 domain 的泳道列」；SPEC-003 §3.1「切至泳道」三分支、「選 domain」列 active／inactive；SPEC-003〈設計約束〉「0.1 的泳道以寫死座標的假資料畫出」；SPEC-004 §3.1 `SwimlaneGrid` 列、4.38 用途、4.38 狀態矩陣 default 列資料態「`lanes`，0.1 為寫死座標的假資料」、4.38「步驟欄寬 由假資料座標決定」；實作 `SwimlaneLane.nodes` 為 `(SwimlaneNode, int column)`。
**現文核對**：矛盾成立。約束寫於 2026-08-27，早於 `W3-345`（2026-09-14）定 traverses；定案後「泳道指派」已非待決，而約束仍把它列為後推項，並以「寫死座標」使讀者無從判斷換選 UC 時節點是否移動。
**前輪核對**：`.47` D6 以本約束作 R 事證（O2 需多 UC 共用欄位布局）而未改動；`.53`／`.59` 無相關項。故非「已裁未落實」，而是 `W3-345` 同步缺漏。
**選項**：O1 改寫約束：列歸屬依 traverses 計算，列序與欄序由 fixture 給定，欄寬列高靜態（改造既有）；O2 維持寫死：每條假 UC 各一組手填 (列, 欄) 座標（零新增，原文一端）；O3 反向框架：0.1 泳道只一組固定版型，不隨選定 UC 變（原文字面極端）；O4 0.1 即定列序與欄序規則（新增）。
**R 事證**：O3 與 SPEC-003 §3.1「切至泳道」依選定 UC 三分支及面板標題 `<UC id> <UC 標題>` 直接衝突；O2 使 fixture 的 traverses 與座標成兩份來源，`W2-001` 驗收「traverses 含 D 的步驟之全部節點 active」須對手填座標交叉核對，兩者不一致時無權威；O4 的列序（沿用矩陣列序或依首次貫穿序）與 `[]` 步驟佔欄屬 CLAUDE.md §6「泳道布局演算法」待決（`.64` 已如此歸因 V4R4-02 G13／G15），本票不裁；O1 只把已定案部分（歸屬）移出後推清單，未定部分保持後推，且與實作元件 (節點, 欄) 介面相容——欄由呼叫端給、列由 traverses 決定。
**A**：改一條約束與三份下游字面，不改任何行為條文；不擠壓 W2 其他項。
**裁決**：O1。
**被放棄選項優點**：O2 fixture 完全可控、畫面票不需寫歸屬計算。
**選定方案缺點**：(1) `W2-001` 須實作「traverses → 列」計算（單純包含比對）；(2) 列序／欄序仍無規則，fixture 作者須自選並固定。
**P 失敗訊號**：`W2-001` fixture 需為同一 UC 在不同列給不同欄序才能畫出合理形狀 → 欄序非純陣列序，提前觸發泳道布局演算法待決。
**改法**：
- SPEC-001〈設計約束〉末條「泳道以寫死座標的假資料畫出形狀即可。真正的排序、泳道指派、邊繞線…」改「泳道以假資料畫出形狀即可：步驟節點所屬的泳道列依假資料 `FlowStep.traverses` 計算（§1〈矩陣格與泳道列的判定依據〉，`0.1.0-W3-345`），泳道列序與步驟欄序由假資料直接給定，欄寬與列高為靜態值。真正的排序、邊繞線需要真實的 flow 資料…」（其餘句不動）
- SPEC-003〈設計約束〉「0.1 的泳道以寫死座標的假資料畫出」改「0.1 的泳道以假資料畫出（節點所屬列依 traverses、列序與欄序由假資料給定，SPEC-001〈設計約束〉）」
- SPEC-004 §3.1 總表 `SwimlaneGrid` 列、4.38 用途「0.1 以寫死座標的假資料靜態排版」改「0.1 以假資料靜態排版（節點所屬列依 traverses，列序與欄序由假資料給定；SPEC-001 設計約束）」；4.38 狀態矩陣 default 列資料態「0.1 為寫死座標的假資料」改「0.1 為假資料，欄序由假資料給定」
**同步點**（`grep -rn "寫死座標\|靜態版型" docs/ CLAUDE.md`）：上列四處（改）；SPEC-004 4.38「步驟欄寬 由假資料座標決定（SPEC-001 設計約束「寫死座標」）」改引用字面為「（SPEC-001 設計約束：欄寬靜態）」，不設 token 的結論不變；SPEC-004 §3.3 第 20 項「靜態座標泳道」（提案原文記錄，`.59` S4-P2 節首註記涵蓋，不改）。
**建議建票（非本項必要條件）**：泳道布局演算法（CLAUDE.md §6）目前以「後推至 Layout 接真資料的版本」為 trigger，非 ticket（`decision-trigger-binding` 規則 2）；建議建承接票，範圍含列序、欄序、`[]` 步驟佔欄、分支步驟欄位。

#### K4（V4R4-02 G22）空圖內容區是否有切換專案鈕

**原文位置**：SPEC-001 §1 空圖可用操作「開啟 `docs/` 目錄…、切換專案、導覽至破洞報告」；UC-01〈替代場景〉〈空專案〉「…導覽至破洞報告與切換專案的動作」；SPEC-003 §2.7 阻擋狀態欄必備動作「`action-domain-switch-project`；三個阻擋狀態皆有」、§3.1 互動反應「阻擋狀態切換專案…僅於三個阻擋狀態渲染」、〈導航跳轉與退出〉空圖列只有 `project-switcher-entry`；SPEC-004 §3.6 §1 空圖列「切換專案由殼層入口承載」；SPEC-001〈返回為殼層通則〉第二段「各列退出路徑欄的『導覽』『切換專案』為此通則的既有列舉」。
**現文核對**：SPEC-003 與 SPEC-004 已一致（無內容區鈕），SPEC-001 通則段只涵蓋退出路徑欄，可用操作欄的「切換專案」無歸屬說明，讀者對照 §1 未選專案「開啟切換浮層」與阻擋狀態「切換專案」（有本體鈕）無法區分。
**選項**：O1 通則段擴及可用操作欄，並明寫內容區切換專案鈕僅限三個阻擋狀態（改造既有）；O2 空圖可用操作改「切換專案（殼層入口）」逐列標注（改造既有）；O3 反向框架：刪空圖可用操作的「切換專案」；O4 空圖加內容區鈕（新增）。
**R 事證**：`.59` UC-P1 判準「UC 場景動作須為可用操作欄子集」——O3 使 UC-01〈空專案〉違反；O4 與 `.53` E9 理由（本體鈕只給「不適用本 App」的狀態）及 `.59` S4-P3 衝突；O2 只修一列，§2–§6 專案未就緒與 §4 未載入「切換專案」同型未標，下輪同型再猜；O1 一處改動覆蓋全表，與 `.59` S1-P2 同一手法。
**裁決**：O1。
**被放棄選項優點**：O2 讀者單讀該列即知。
**選定方案缺點**：可用操作欄的「切換專案」須跨段讀通則才知載體。
**P 失敗訊號**：第五輪探針（若有）仍對任一非阻擋狀態猜內容區切換專案鈕 → 改 O2 逐列標注。
**改法**：SPEC-001〈返回為殼層通則〉第二段「各列退出路徑欄的『導覽』『切換專案』為此通則的既有列舉，未寫者亦成立」改「各列可用操作欄與退出路徑欄的『導覽』『切換專案』（含『開啟切換浮層』）為此通則的既有列舉，未寫者亦成立，不表示內容區另有按鈕；內容區的切換專案按鈕（`action-domain-switch-project`）僅三個阻擋狀態渲染（SPEC-003 §2.7、`0.1.0-W3-335.53` E9）」。
**同步點**（`grep -n "切換專案\|開啟切換浮層" SPEC-001` 可用操作欄）：§1 未選專案、載入中「開啟切換浮層」（通則涵蓋，不改）；§1 空圖、§2–§6 專案未就緒退出路徑（不改）；§1 三個阻擋狀態（本體鈕與殼層入口並存，不改）；SPEC-003 §2.4 殼層常駐出口段（已一致，不改）。

#### K5（V4R4-02 G23）有設定檔但無 `docs/` 的落點

**原文位置**：SPEC-001 §1 空圖進入條件「`docs/` 存在、解析成功、節點數為 0」；同列可用操作「開啟 `docs/` 目錄（僅在該目錄存在時提供）」；§1 不是框架專案進入條件「…docs/ 目錄存在性不再作為判準」；〈Gate 三問對照〉〈判定順序〉只用兩訊號；SPEC-003 §3.1「開啟 docs 目錄（渲染時目錄不存在）→ 該錨點不渲染」；SPEC-004 §3.6 §1 空圖列「開啟 docs（目錄存在時）」；SPEC-001〈設計約束〉空狀態「現造一個空 `docs/`」。
**現文核對**：矛盾成立。VERSION 與 JSON 存在、無 `docs/` 時：阻擋三態皆不符、空圖要求 `docs/` 存在、正常要求有節點——無狀態可落，違反 FR-01 精神（死胡同）。
**前輪核對**：`.56` E6 前置裁決「docs/ 存在性不再影響判定」；該票同步 grep 字面為「資料夾可讀但無」「唯一出口，恆可用」「沒有 \`docs/\`」，未命中「`docs/` 存在」，故殘留。屬落實缺口，本項為落實而非新裁。
**選項**：O1 刪空圖進入條件的 docs/ 前提（改造既有）；O2 新增「無 docs/」狀態（新增）；O3 無 docs/ 落不是框架專案（反向框架，推翻 `.56`）；O4 無 docs/ 視為解析失敗落破洞。
**R 事證**：SPEC-003 §3.1「渲染時目錄不存在 → 錨點不渲染」與 SPEC-004「目錄存在時」已預設空圖可在無 docs/ 下出現，O1 使三檔一致；O2 無 UC 出處且 FR-03 要求的前進動作（前往破洞報告）與空圖相同，外觀差異只在 open-docs，已由條件渲染表達；O3 推翻 `.56` 無新事證；O4 的破洞報告在「專案未就緒」外無入口，且解析器對缺目錄的語意屬 corpus domain，UI 規格不宜定。
**裁決**：O1（落實 `.56`，不推翻）。
**被放棄選項優點**：O2 可給無 docs/ 專屬說明文案。
**選定方案缺點**：無 docs/ 的空圖只剩前往破洞報告一個前進動作，說明文字未區分原因。
**P 失敗訊號**：真實語料出現大量「有 `.claude/` 無 `docs/`」專案且使用者回報不知道缺什麼 → 評估 O2 或空圖說明依 docs/ 存在性分文案。
**改法**：SPEC-001 §1 空圖進入條件「`docs/` 存在、解析成功、節點數為 0」改「解析成功、節點數為 0（`docs/` 不存在時亦同，此時不提供開啟 `docs/` 目錄；`0.1.0-W3-335.56` E6 docs/ 不作判準的落實）」。
**同步點**（`grep -rn "docs/\` 存在\|docs/ 存在" docs/spec docs/usecases`）：僅本列命中（改）；SPEC-003 §4 #5（只抄退出路徑，不改）；UC-01〈空專案〉「解析成功但無任何圖譜節點時」（已一致，不改）；SPEC-003 §3.1 open-docs 兩列、SPEC-004 §3.6 空圖列（已一致，不改）。

#### K6（V4R4-03 G04）錨點 `<domainId>` 取值

**原文位置**：SPEC-003 §2.9 命名規範（只定 `<screen>` 取 `AppDestination.name`，未定動態 ID 段）；§3.1 `action-domain-select-<domainId>`、`cell-domain-<rowId>-<colId>`；§3.2 `action-ucFlow-goto-domain-<domainId>`；UC flow 區塊 `traverses: ["TicketDetail"]`（PascalCase，`docs/domain-map.md` §2.5 定義為 domain 名）；SPEC-001／SPEC-003 frontmatter `depends_on_domains: [..., ticketdetail, ...]`（小寫）。
**現文核對**：矛盾成立於「兩套資料字面、錨點未指明取哪套」。`<rowId>`／`<colId>` 同一缺口（V4R4-02 G07，spec 未描述類，本項一併涵蓋）。
**選項**：O1 取 `traverses` 字面值、不轉換（改造既有）；O2 一律轉小寫（對齊 frontmatter）；O3 取列索引／欄索引（反向框架：錨點不含語意 ID）；O4 另立 domain ID 對照表（新增）。
**R 事證**：矩陣列與泳道列的歸屬以 traverses 包含比對（SPEC-001 §1），UC Flow domain 標籤列出的也是 traverses 元素（SPEC-003 §3.2 事件流小表「domain 取該步驟 traverses 全部元素」），畫面內所有 domain 識別字來源即 traverses；`depends_on_domains` 是 SPEC 節點對 domain 的邊欄位，UI 不以它產生列；O2 使 finder 須做轉換、且 traverses 值域由 `W3-346` schema 驗證為 domain-map 名，大小寫正規化屬資料面決策；O3 使錨點在列序變動（布局待決）時失效，違反錨點穩定性；O4 無出處且與 domain-map 名重複。
**A**：`depends_on_domains` 與 traverses 大小寫不一致本身是資料面問題（Graph 聚合時是否需正規化），本項不裁，列入〈範圍外發現〉。
**裁決**：O1。
**被放棄選項優點**：O2 與 frontmatter 慣例一致、對大小寫筆誤容錯。
**選定方案缺點**：traverses 填寫大小寫不一致時同一 domain 產生兩個錨點（由 `W3-346` 值域驗證攔截）。
**P 失敗訊號**：`W3-346` 值域驗證採大小寫不敏感比對 → 改 O2 並同步本規則。
**改法**：SPEC-003 §2.9 表後「`<screen>` 一律取…」段後補「**動態 ID 段取值**：`<domainId>`、`<rowId>`（矩陣列）取 `FlowStep.traverses` 元素字面值（`docs/domain-map.md` 的 domain 名，不轉換大小寫）；`<ucId>`、`<colId>`（矩陣欄）、`<nodeId>`、`<evtId>`、`<stepId>` 取節點或步驟 ID 字面值；`<index>`、`<category>`、`<key>` 依各畫面節定義（`0.1.0-W3-335.65` K6）」。
**同步點**（`grep -rn "domainId\|rowId\|colId" docs/spec/ui`）：SPEC-003 §3.1 三列、§3.2 一列、§3.1 生命週期 jump 列（引用錨點，不改）；SPEC-004 4.15、4.19、4.37、4.38 `locateLane`（引用，不改）。
#### K7（V4R4-03 G15）破洞事件類跳 UC Flow 是否寫入選定 UC

**原文位置**：SPEC-003 §2.8〈選定 UC：App 層共用值〉首段「兩處設定入口寫同一個值」、入口表設定入口 1–3；SPEC-003 §3.5 破洞項（事件類）列「選定 UC 寫入引用該 EVT 的 UC，多條時取 UC 編號最小者（§2.8〈選定 UC〉）」；SPEC-001 §5〈破洞項的主操作與次要操作〉事件類列「選定 UC 設為引用該事件的 UC」；SPEC-001 §1〈泳道 · 尚未選定 UC 獨立成列〉註記「裁示的設定入口只有矩陣交叉格與 UC Flow 視圖兩處」；UC-06 主要成功場景步驟 4。
**現文核對**：矛盾成立。§3.5 引用 §2.8 作依據，§2.8 入口表卻無此入口；首段「兩處」與表內三列亦已不符（入口 3 `.37` R5 增列時未改首段，規則 10 型）。
**選項**：O1 §2.8 增設定入口 4、首段改不寫數（改造既有）；O2 §3.5 刪寫入，jump 後落前次值或尚未選定 UC（改造既有，另一端）；O3 反向框架：破洞事件類不跳 UC Flow，一律跳節點詳情。
**R 事證**：事件類寫入選定 UC 出自 `.19` 第二輪用戶裁示（SPEC-001 §5 表與 SPEC-003 §3.5 兩處皆記），O2／O3 推翻用戶裁示；O2 使「定位事件列」在 `state-ucFlow-uc-unset` 下無事件流小表可定位，§3.5 可觀察結果「`state-ucFlow-normal` 出現」無法成立。
**A**：SPEC-001 §1 註記的「兩處」語意是「使用者在 UC 選擇介面上主動選擇的入口」，用來說明為何泳道內不提供選擇入口；入口 3、4 是動作的附帶寫入，不改變該理由——改措辭即可，不動結論。
**裁決**：O1。
**被放棄選項優點**：O2 使選定 UC 只有使用者顯式選擇才改變，可預期性較高。
**選定方案缺點**：使用者自破洞報告返回 Domain 泳道時，泳道已換成事件所屬 UC（與入口 3 同型，屬「後寫者覆蓋」既定語意）。
**P 失敗訊號**：`W2-004` 補實作後，整合測試「破洞事件類 → 返回 → 泳道」出現使用者回報泳道 UC 意外改變 → 評估入口 4 改為只設 UC Flow 頁面暫態、不寫共用值。
**改法**：
- SPEC-003 §2.8〈選定 UC〉首段「Domain 視圖泳道模式與 UC Flow 視圖讀同一個值，兩處設定入口寫同一個值，後寫者覆蓋先寫者」改「…讀同一個值，下表各設定入口寫同一個值，後寫者覆蓋先寫者」
- 入口表設定入口 3 後增列「設定入口 4｜破洞報告破洞項（類別 `orphan-event` 或 `event-declaration-mismatch`，`card-gaps-<itemId>`，§3.5）：jump 前寫入引用該 EVT 的 UC，多條時取 UC 編號最小者；無任何 UC 引用時不寫入（改 jump 節點詳情）（`0.1.0-W3-335.19` 第二輪、`0.1.0-W3-335.38` S-31）」
- SPEC-001 §1〈泳道 · 尚未選定 UC 獨立成列〉註記「裁示的設定入口只有矩陣交叉格與 UC Flow 視圖兩處，提示文字指向兩者即可」改「使用者主動選擇 UC 的介面只有矩陣交叉格與 UC Flow 視圖（其餘設定入口為動作的附帶寫入，見 SPEC-003 §2.8），提示文字指向兩者即可」
**同步點**（`grep -rn "設定入口" docs/spec docs/usecases`）：SPEC-003 §3.1 選格列、在泳道中檢視列，§3.2 選擇 UC 列（引用入口編號 1–3，不改）；SPEC-003 §3.5 事件類列「（§2.8〈選定 UC〉）」補「設定入口 4」；SPEC-004 §4.0.6 `swimlaneUcUnsetPrompt` 文案（指向兩個主動選擇介面，已一致，不改）；`docs/spec/ui/audit/counts.py` 樣式含「設定入口」（改後重跑 counts 確認首段無寫死數）。
**實作面**：`W2-004` 已完成但 `lib/screens/gap_report/` 無依指向型別跳轉（見〈範圍外發現〉），入口 4 的寫入端待落差盤點承接；`W2-002` 為讀方，照 §2.8 讀取即可。

#### K8（V4R4-04 G04）`LoadingState` 參數

**原文位置**：SPEC-003 §2.11「三處載入態的差異只有『目標態』與『進度型別』兩個參數」；§2.6（Domain 骨架矩陣版位、破洞骨架分節版位，皆 indeterminate）；§2.5〈目標態對照〉破洞目標為「`returnTo` 指定的畫面；null 時 `nav-page-domain`」；SPEC-004 §3.1 總表 `LoadingState` 列「骨架版位 slot：`matrix` / `sections`」、4.24 slot 契約 `skeletonLayout`、4.24 用途「三處以『目標態』與『進度型別』參數差異化」；實作 `enum SkeletonLayout`。
**現文核對**：矛盾成立。兩處 indeterminate 版位不同，由進度型別推不出；另「目標態」在破洞為執行期畫面而非狀態錨點，參數型別與另兩處不同。
**選項**：O1 SPEC-003 §2.11 與 SPEC-004 4.24 用途改三項差異並註明目標態型別差（改造既有）；O2 版位由呼叫畫面決定、不算元件參數（改造既有，維持「兩個參數」字面）；O3 反向框架：三處共用同一骨架版位。
**R 事證**：SPEC-004 4.24 slot 契約與實作已有 `skeletonLayout` 必填參數，O2 與元件介面不符；O3 違反 §2.6「結果形狀已知」理由與 SPEC-004 §3.6 兩列歸屬；O1 只追元件層已定事實，無行為變更。
**裁決**：O1。
**被放棄選項優點**：O2 保留 SPEC-003 不描述元件介面的邊界。
**選定方案缺點**：SPEC-003 多描述一個元件參數，與 SPEC-004 形成雙處列舉（以不寫數降低漂移）。
**P 失敗訊號**：新增第四處載入態或第三種骨架版位時兩檔未同步 → 將 §2.11 改為只引用 SPEC-004 4.24 slot 契約。
**改法**：
- SPEC-003 §2.11 段「三處載入態的差異只有『目標態』與『進度型別』兩個參數，其餘行為…完全相同」改「三處載入態的差異為目標態、進度型別與骨架版位（`matrix`／`sections`，僅骨架型；SPEC-004 4.24 `skeletonLayout`），其中破洞的目標態為執行期 `returnTo` 指定畫面（§2.5〈目標態對照〉）而非固定狀態錨點；其餘行為…完全相同」
- SPEC-004 4.24 用途「三處以『目標態』與『進度型別』參數差異化」改「三處以目標態、進度型別與骨架版位差異化（SPEC-003 §2.11）」
**同步點**（`grep -rn "兩個參數\|目標態」與「進度型別" docs/spec`）：上列兩處（改）；SPEC-004 §3.3 第 5 項（提案原文記錄，節首註記涵蓋，不改）。

#### K9（V4R4-04 G17）破洞掃描 `denied` 分支與 (b1) 機械推導

**原文位置**：SPEC-003 §2.13〈T1 判否的三個承接〉(b1)「結果為持久狀態時由狀態本身滿足…不另發提示」及〈(b1) 的可行條件是可機械判定的〉；〈判準與既有條文的對照〉#5、#6「承接 (a) 不可行（`denied`）→ (b2)」；§2.2〈系統層通知〉權限 gate `denied` 列（`0.1.0-W3-063` 用戶簽核）；SPEC-003 §3.5〈生命週期〉「再次可見：有既有結果時顯示既有結果」；SPEC-001 §8.1 §5 兩列與段落「§5 兩列是全表唯一的條件式，理由是…Domain 載入與 Ticket 載入…結果為持久狀態，回來即見」。
**現文核對**：矛盾成立。破洞結果寫入 `state-gaps-none`／`state-gaps-found`，再次可見時仍成立，(b1) 依字面可行，應先於 (b2) 採用而得「不提示」；#5／#6 跳過 (b1) 無理由。§8.1 段落以「結果持久、回來即見」區分 §5 與 §1／§4，但破洞結果同樣持久——實際區分依據是 (a) 授權。
**選項**：O1 §2.13 補具名例外：(a) 適用範圍內的事件不走 (b1)，(a) 不可行時續試 (b2)；§8.1 段落理由改指 (a) 授權（改造既有）；O2 依機械推導改 §2.2 `denied` fallback 為不提示（改造既有，另一端）；O3 承接序改 (b2) 先於 (b1)（反向框架）；O4 `denied` 時改用持續性 App 內指示（新增元件）。
**R 事證**：O2 推翻 `W3-063` 用戶簽核且使 `denied` 使用者對掃描完成零訊號——§2.13〈顯示位爭用〉自述「權限 `denied` 分支下系統通知不可用，該 SnackBar 是唯一訊號」；O3 使 Domain／Ticket 載入完成改走 (b2) 延後 SnackBar，違反 §2.2〈適用範圍〉不升級理由與 §8.1 十餘列判否；O4 為 §2.2 已記的 spawn request（Banner、NavItem 徽章），元件庫無此元件；O1 符合 §2.13 自身要求「判準的例外必須有具名理由，否則下一個事件會照抄這個例外而無人察覺」（`openedExternallyMessage` 段），且 (a) 授權本身即「此事件需主動喚回使用者」的判定，持久狀態不喚回。實作 `scan_notification_controller.dart` 已走 `withAction`，O1 無行為變更。
**A**：只補理由，不擴 (a) 範圍、不動任何載體。
**裁決**：O1。
**被放棄選項優點**：O2 判準無例外、最自洽；O3 無例外且規則更短。
**選定方案缺點**：判準多一條例外；未來新事件獲 (a) 授權時自動繼承「不走 (b1)」，須在授權簽核時意識到。
**P 失敗訊號**：新增第二個獲 (a) 授權的事件，而其 (a) 不可行時使用者認為不需提示 → 例外改為逐事件標注而非依 (a) 範圍。
**改法**：
- SPEC-003 §2.13〈(b1) 的可行條件是可機械判定的〉段末補「**具名例外**：(a) 列適用範圍內的事件（0.1 為破洞掃描完成）不走 (b1)——(a) 的授權即判定該事件的結果需要主動喚回使用者，持久狀態不喚回，故不足以承接；(a) 不可行時跳過 (b1) 續試 (b2)。§2.2 權限 gate `denied` 列的 fallback 即此例外的落地（`0.1.0-W3-063` 用戶簽核，`0.1.0-W3-335.65` K9）」
- 同節〈判準與既有條文的對照〉#5、#6 判準結論「承接 (a) 不可行（`denied`）→ (b2)」改「承接 (a) 不可行（`denied`）→ (b1) 依具名例外不適用 → (b2)」
- SPEC-001 §8.1 表後段落「Domain 載入與 Ticket 載入同樣可在使用者離開後完成，但兩者的結果落點就是使用者觸發時停留的畫面且結果為持久狀態，回來即見——判準 T1 判否後由承接 (b1) 滿足，不需提示」改「Domain 載入與 Ticket 載入同樣可在使用者離開後完成，結果亦為持久狀態；區分依據是承接 (a) 升級通道只授權給破洞掃描（用戶簽核）：未獲授權者 T1 判否後由 (b1) 滿足、不需提示，獲授權者依 SPEC-003 §2.13 (b1) 具名例外不走 (b1)，(a) 不可行時走 (b2)」
**同步點**（`grep -rn "(b1)" docs/spec/ui`）：SPEC-001 §8.1 §1 正常 · 矩陣、§4 正常 · 列表兩列（未獲授權事件，已一致，不改）；SPEC-003 §2.13〈因判準而不顯示時的日誌承接〉觸發條件（不涉，不改）；§2.17 決策欄與「跳過」列（不涉，不改）；§2.14 列 3（(b2) 延後顯示，已一致，不改）。

#### K10（V4R4-04 G22）jump 至外部已刪檔的節點落哪一狀態

**原文位置**：SPEC-001 §6 原始檔已消失進入條件「外部刪除且尚未重新載入」；SPEC-001 §6 正常退出路徑「開啟原始檔（檔案不存在）→ 原始檔已消失」；SPEC-003 §3.6 開啟原始檔（檔案不存在）列「此為對外部變更的第一手偵測點」、〈生命週期〉「由 jump 進入：主欄與右欄依 payload 的 nodeId 渲染」；SPEC-003 §2.8「視窗失焦／前景切換：不觸發任何重新載入（0.1 無檔案監看）」；UC-06〈例外場景〉〈原始檔已消失〉「經跳轉進入節點詳情後開啟時檔案已不存在：顯示最後已知路徑並提供重新整理」（`.59` UC-06 裁決）。
**現文核對**：矛盾成立。SPEC-001 以世界狀態定義進入條件，jump 至已刪檔節點時條件成立，但 SPEC-003 無此偵測點、生命週期渲染記憶體內容。
**選項**：O1 SPEC-001 進入條件改偵測點式（改造既有）；O2 jump 與點關聯時檢查檔案存在，不存在直接落原始檔已消失（新增偵測點）；O3 反向框架：進入條件維持世界狀態，渲染前一律同步檢查。
**R 事證**：UC-06 經 `.59` 已定「開啟時檔案已不存在」為路徑，與 O1 一致；O2／O3 在每次 jump 與關聯替換加入檔案 I/O，使「關聯項」由本地即時轉為短暫非同步（§2.12），牽動 `RelationItem` 回饋契約與 §3.6 動畫提示「cross-fade 不顯示等待」，且 0.1 無檔案監看的前提下仍無法涵蓋「顯示中被刪除」；O1 只改一格字面，行為與現行 SPEC-003 相同。
**裁決**：O1。
**被放棄選項優點**：O2 使用者不需按開啟原始檔即知檔案已消失。
**選定方案缺點**：記憶體內容可能與磁碟不一致而顯示為正常，直到使用者開啟原始檔。
**P 失敗訊號**：接真資料版本加入檔案監看時 → 監看事件成為第二偵測點，本格補列。
**改法**：SPEC-001 §6 原始檔已消失進入條件「外部刪除且尚未重新載入」改「正常或部分損壞下開啟原始檔得 `notFound`（外部刪除且尚未重新載入；jump 與點關聯不檢查檔案存在，SPEC-003 §3.6 第一手偵測點）」。
**同步點**（`grep -rn "外部刪除" docs/spec docs/usecases`）：僅本格（改）；SPEC-003 §4 #26（抄退出路徑，不改）；SPEC-004 4.22 狀態矩陣進入欄（核對為「開啟原始檔 notFound」同義者不改，否則同批改）；UC-06（已一致，不改）。

#### K11（V4R4-04 G32）重新整理鈕位置

**原文位置**：SPEC-003 §2.4〈渲染位置統一〉「`action-<screen>-back` 一律置於…`SplitRow.header` 右側 `ButtonRow`（與重新整理、重新掃描等頁面級動作同一區塊）」；§2.11「原始檔已消失（訊息 + 路徑 + 重新整理；SPEC-004 4.22）」；SPEC-004 §3.1 `ButtonRow` 列「承載狀態元件的動作區（開始載入；重新整理；…）、頁首右側頁面級動作區（§3.7 第 17、18 項）」、§3.6 §6 原始檔已消失列「`MissingSourceState`（`ButtonRow`[`AppButton.primary` 重新整理]）；返回由頁面框架渲染」、§3.7 第 17 項（§6 返回／開啟原始檔在頁首）、4.28 用途「頁面級動作列（返回、重新掃描、開啟原始檔）」；實作 `missing_source_state.dart` 內建 `action-nodeDetail-refresh`。
**現文核對**：矛盾成立於 SPEC-003 §2.4 單處括號；SPEC-003 §2.11、SPEC-004 全檔與實作一致（元件內）。
**選項**：O1 §2.4 括號刪「重新整理」（改造既有）；O2 重新整理移至頁首（改造既有，另一端）；O3 兩處皆渲染（新增）。
**R 事證**：SPEC-004 §3.3 第 3 項拆 `MissingSourceState` 的理由即「重新整理留在畫面內，退出層級不同」；O2 使 `MissingSourceState` 無動作、違反 §2.7 同型元件必備動作精神並改動已完成元件；O3 同一錨點兩實例，違反錨點唯一。
**裁決**：O1。非風格偏好：位置已由 SPEC-004 核定並實作。
**被放棄選項優點**：O2 使所有「頁面內重試」動作集中頁首。
**選定方案缺點**：節點詳情頁首在原始檔已消失時只剩返回（開啟原始檔不渲染）。
**P 失敗訊號**：無（純字面同步）。
**改法**：SPEC-003 §2.4〈渲染位置統一〉括號「（與重新整理、重新掃描等頁面級動作同一區塊）」改「（與重新掃描、開啟原始檔等頁面級動作同一區塊；狀態元件自身的動作，如 `MissingSourceState` 的重新整理、`LoadPrompt` 的開始載入，不屬頁面級動作）」。
**同步點**（`grep -rn "重新整理.\{0,6\}重新掃描\|頁面級動作" docs/spec`）：SPEC-003 §2.7 必備動作欄「重新掃描為頁面級動作」（已一致，不改）；SPEC-004 §3.7 第 17、18 項、4.28 用途（已一致，不改）。
### 寫入後復核與更正

| 項 | 更正 | 理由 |
|----|------|------|
| K10 同步點 | SPEC-004 4.22 由「核對後決定」定為「已一致，不改」 | 4.22 狀態矩陣進入條件為「呼叫端渲染（`state-nodeDetail-missing`）」，回饋契約 state-change 列已寫「開啟原始檔回 `notFound` 時渲染本元件」，與 K10 偵測點式同義 |
| SPEC-001 組標題項數 | 「6 項」改「7 項」 | 表列 1-01～1-07 實數為 7 |

### 追修清單（依檔分組）

執行順序建議 SPEC-001 → SPEC-003 → SPEC-004 → UC（K7、K9 跨檔引用以 SPEC-003 條目為錨）。每張追修票沿用 `.59` 驗收建議：改動的規則詞以 grep 列出命中處並逐處標改或不改；落檔後重跑 `docs/spec/ui/audit/matrix.py`／`counts.py`。承接票由 PM 依本清單另建（本票不建票）。

#### SPEC-001-screen-state-matrix.md（7 項，另版本與變更歷史）

| # | 來源 | 位置 | 改法 | 跨檔連動 | W2 |
|---|------|------|------|---------|----|
| 1-01 | K1 | §1 正常 · 矩陣 顯示欄 | 右欄格詳情卡區前補「+ 底部圖例（關係種類：直接貫穿／間接依賴／無關）」 | 無（SPEC-004 §3.6 已有） | W2-001 |
| 1-02 | K3 | 〈設計約束〉泳道末條 | 「泳道以寫死座標的假資料畫出形狀即可。真正的排序、泳道指派、邊繞線…」改為 K3 改法第 1 點全文 | 3-06、4-02 | W2-001 |
| 1-03 | K4 | 〈返回為殼層通則〉第二段 | 「各列退出路徑欄的…既有列舉，未寫者亦成立」改為 K4 改法全文（擴及可用操作欄、內容區鈕限三個阻擋狀態） | 無 | W2-001 |
| 1-04 | K5 | §1 空圖 進入條件 | 改「解析成功、節點數為 0（`docs/` 不存在時亦同，此時不提供開啟 `docs/` 目錄；`0.1.0-W3-335.56` E6 docs/ 不作判準的落實）」 | 無 | W2-001 |
| 1-05 | K7 | §1〈泳道 · 尚未選定 UC 獨立成列〉註記 | 「裁示的設定入口只有矩陣交叉格與 UC Flow 視圖兩處」改為 K7 改法第 3 點全文 | 3-04 | W2-001 |
| 1-06 | K9 | §8.1 表後段落（「§5 兩列是全表唯一的條件式」段） | 第二句起改為 K9 改法第 3 點全文 | 3-05 | W2-004（已完成，無實作改動） |
| 1-07 | K10 | §6 原始檔已消失 進入條件 | 改「正常或部分損壞下開啟原始檔得 `notFound`（外部刪除且尚未重新載入；jump 與點關聯不檢查檔案存在，SPEC-003 §3.6 第一手偵測點）」 | 無 | W2-005 |

#### SPEC-003-interaction-response.md（7 項，另版本與變更歷史）

| # | 來源 | 位置 | 改法 | 跨檔連動 | W2 |
|---|------|------|------|---------|----|
| 3-01 | K11 | §2.4〈渲染位置統一〉括號 | 改為 K11 改法全文 | 無 | W2-005 |
| 3-02 | K6 | §2.9 表後「`<screen>` 一律取」段後 | 補〈動態 ID 段取值〉段（K6 改法全文） | 無 | W2-001、W2-002 |
| 3-03 | K8 | §2.11 表後「取消契約由載入態元件單一承擔」段 | 改為 K8 改法第 1 點全文 | 4-03 | W2-001、W2-003；W2-004（實作已符） |
| 3-04 | K7 | §2.8〈選定 UC〉首段；入口表 | 首段「兩處設定入口」改「下表各設定入口」；表增「設定入口 4」列（K7 改法第 2 點） | 1-05、3-07 | W2-002；W2-004（寫方待落差盤點） |
| 3-05 | K9 | §2.13〈(b1) 的可行條件是可機械判定的〉段末；〈判準與既有條文的對照〉#5、#6 判準結論欄 | 段末補具名例外（K9 改法第 1 點）；#5、#6 改「承接 (a) 不可行（`denied`）→ (b1) 依具名例外不適用 → (b2)」 | 1-06 | W2-004（實作已符） |
| 3-06 | K3 | 〈設計約束〉泳道條 | 「0.1 的泳道以寫死座標的假資料畫出」改為 K3 改法第 2 點 | 1-02 | W2-001 |
| 3-07 | K7 | §3.5〈互動反應〉破洞項（類別 `orphan-event` 或 `event-declaration-mismatch`）列 | 「（§2.8〈選定 UC〉）」改「（§2.8〈選定 UC〉設定入口 4）」 | 3-04 | W2-004 |

#### SPEC-004-component-library.md（3 項，另版本與變更歷史）

| # | 來源 | 位置 | 改法 | 跨檔連動 | W2 |
|---|------|------|------|---------|----|
| 4-01 | K3 | §3.1 總表 `SwimlaneGrid` 列用途欄；4.38 用途 | 「0.1 以寫死座標的假資料靜態排版」改「0.1 以假資料靜態排版（節點所屬列依 traverses，列序與欄序由假資料給定；SPEC-001 設計約束）」 | 1-02 | W2-001 |
| 4-02 | K3 | 4.38〈狀態矩陣〉default 列資料態；4.38 步驟欄寬列 | 資料態「0.1 為寫死座標的假資料」改「0.1 為假資料，欄序由假資料給定」；步驟欄寬「（SPEC-001 設計約束「寫死座標」）」改「（SPEC-001 設計約束：欄寬靜態）」 | 1-02 | W2-001 |
| 4-03 | K8 | 4.24 用途 | 「三處以『目標態』與『進度型別』參數差異化」改「三處以目標態、進度型別與骨架版位差異化（SPEC-003 §2.11）」 | 3-03 | W2-001、W2-003 |

#### UC（1 項，無承接票，交 PM）

| # | 來源 | 位置 | 改法 | W2 |
|---|------|------|------|----|
| U-01 | K2 可同步部分 | UC-02〈驗收條件〉矩陣三種狀態列的括號註 | 改「（『直接貫穿』判定式為 `FlowStep.traverses` 包含，`0.1.0-W3-345`；『間接依賴』判定式未定義，見 §9）」；`version` 升一版、〈變更歷史〉補列 | W2-001 |

依檔項數：SPEC-001 7、SPEC-003 7、SPEC-004 3、UC 1，合計 18 個改動點，對應 K1、K3–K11 共 10 項裁決（K2 為綁定待決，僅 U-01 可同步）。

### 綁定待決與建議建票（本票不建）

| 項 | 待決出處 | 綁定票 | 建議 |
|----|---------|-------|------|
| K2（含 K1 值域） | CLAUDE.md §6 五項空殼判準「間接依賴」；`docs/domain-map.md` §9 `鄰接查詢` 簽章 | 無 | 建「定案『間接依賴』判準（矩陣格關係種類第三值）」；範圍見 K2 建議建票段；`W2-001` 的 indirect 格 fixture 依此票結論 |
| K3 延伸 | CLAUDE.md §6「泳道布局演算法」 | 無（現 trigger 為版本敘述，非 ticket） | 建承接票，範圍含列序、欄序、`traverses: []` 步驟佔欄、分支步驟欄位（亦承接 V4R4-02 G13／G15） |

### W2 畫面範圍對照

| W2 票 | 狀態 | 本票相關項 | 追修列 | 實作影響 |
|-------|------|----------|-------|---------|
| W2-001 Domain | 待實作 | K1、K2、K3、K4、K5、K6、K8 | 1-01～1-04、3-02、3-03、3-06、4-01～4-03、U-01 | 票面更新時引用追修後版本；K2 未定前 indirect 格待綁定票 |
| W2-002 UC Flow | 待實作 | K6、K7（讀方） | 3-02、3-04 | 無額外實作 |
| W2-003 Ticket | 待實作 | K8 | 3-03 | 無（`LoadingState.progressBar` 無版位） |
| W2-005 節點詳情 | 待實作 | K10、K11 | 1-07、3-01 | 無（元件 `MissingSourceState` 已符） |
| W2-004 破洞報告 | 已完成 | K7（寫方）、K8、K9 | 1-06、3-03、3-04、3-05、3-07 | K8、K9 實作已符；K7 入口 4 寫入端未實作（見下） |
| W2-006 浮層 | 已完成 | 無 | — | — |
| W2-007 追溯 | 已完成 | 無 | — | — |

### 範圍外發現（交 PM，本票不建票）

1. `lib/screens/gap_report/` 無依指向型別跳轉（ticket／nodeDetail／ucFlow，`AppDestination` 僅 `domain`、`gaps`），`W2-004` 完成時點早於 `.19` 第二輪裁示——屬用戶裁示中「已完成 W2 票與四輪 spec 變更的落差盤點」範圍，含 K7 入口 4 寫入
2. `depends_on_domains`（SPEC／UC frontmatter，小寫）與 `FlowStep.traverses`（domain-map 名，PascalCase）大小寫不一致；Graph 聚合兩者時是否正規化屬 graph／schema 資料面，UI 規格 K6 不裁
3. V4R4-04 G13 `<itemId>` 取值未定義（spec 未描述類，非本票 11 項），K6 動態段規則未涵蓋，依賴「破洞分類」待決

### 交用戶項目

無。11 項皆非風格偏好：K11 按鈕位置已由 SPEC-004 §3.1、§3.6、4.22 核定並實作；K1 圖例已在畫布。

multi_view_status: n_a
reason: 規格裁決票，WRAP 完整模式逐項產出；追修票落檔後由稽核腳本與同步 grep 覆驗

### 自檢結果

| 類別 | 結果 | 說明 |
|------|------|------|
| 原文核對 | 通過 | 11 項皆 Read 兩端原文（SPEC-001 §1–§8／Gate／FR／設計約束全讀；SPEC-003 §2.2、§2.4–§2.13、§3.1、§3.2、§3.5–§3.7 全讀；SPEC-004 相關列 grep＋定點讀）；K3、K8、K11 另核實作 |
| 前輪衝突 | 通過 | 對 `.37`／`.47`／`.53`／`.56`／`.59` 逐項標關係；推翻 0；落實缺口 1（K5 對 `.56`）；前輪無裁決而疑未落實者 1（K3，判定為 `W3-345` 同步缺漏） |
| 待決依賴 | 通過 | K2 依 CLAUDE.md §6 不裁，無綁定票已列建議建票；K3 列序／欄序、K6 `<itemId>` 未越界裁決 |
| 反向框架 | 通過 | 每項至少一個反向框架選項 |
| 三類涵蓋 | 部分 | 「新增」類列於 K3（O4）、K4（O4）、K5（O2）、K6（O4）、K9（O4）、K10（O2）、K11（O3）；K1、K7、K8 無新增類——新增元件或狀態無出處且不消解該不一致 |
| W2 範圍 | 通過 | 11 項逐項標註；追修列逐列標 W2 票；已完成票標實作影響 |
| 同步點 grep | 通過 | `寫死座標`、`設定入口`、`(b1)`、`兩個參數`、`頁面級動作`、`外部刪除`、`docs/ 存在`、`legendIndirect`、`domainId` 皆實跑；寫入後復核更正 2 處（K10 同步點、SPEC-001 組項數） |
| 位置標示 | 通過 | 檔名＋章節標題／表列名／對照表列號，未用行號 |
| 交用戶判定 | 通過 | 0 項，理由已記 |
| 唯讀約束 | 通過 | 未改 spec／UC／程式碼；未建票；產出僅本票 |
| 用字 | 通過 | 無 emoji；未用禁用詞 |

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "11 項門檻外矛盾以 WRAP 完整模式逐項處理：裁決 10（K1 含部分綁定）、綁定待決 1（K2 間接依賴，無承接票，建議建票）；追修清單 SPEC-001 7／SPEC-003 7／SPEC-004 3／UC 1；交用戶 0；推翻前輪 0；G23 判為 .56 落實缺口、G18 判為 W3-345 同步缺漏（前輪無裁決）；未改 spec、未建票"
confidence: 0.8
acceptance_met: [1, 2, 3]
acceptance_unmet: []
artifacts: ["docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.65.md"]
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-335.64 | chars: 330 -->

### Task Reference
- 0.1.0-W3-335.64 what: 重驗 V4 走查第四輪（同步稽核後，同一份凍結題目）並依 W3-335.22 門檻判定（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-335.64 why: 用戶裁示加同步稽核再跑第四輪：第三輪 .52 未通過（仍猜 2 項），.53 WRAP 裁決經 .55～.58 落檔，.54 窮舉稽核與 .59 裁決經 .60～.63 落檔（SPEC-001 v1.15、SPEC-003 v1.36、SPEC-004 v1.42）（來源已完成，結論見 Solution）

### Related Files
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-335.64.md  # from 0.1.0-W3-335.64（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 原猜測點逐項判定表（仍猜／已解決／漏讀），已知必猜點另列不計  # from 0.1.0-W3-335.64（來源已完成，結論見 Solution）
- [ref] [x] 依門檻（原猜測點仍猜 = 0）給出通過或未通過結論  # from 0.1.0-W3-335.64（來源已完成，結論見 Solution）
- [ref] [x] 新猜測點數與 spec 間矛盾數四輪比較；本輪同步缺漏逐項列出  # from 0.1.0-W3-335.64（來源已完成，結論見 Solution）
