---
id: 0.1.0-W3-051
title: 外送 Wave 3 的框架與 skill 類任務為 framework issue，交由 flutter_balance 承接
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: framework-issue-curator
  history: {}
what: 把 14 張框架/skill 票分族提為 framework issue（含既有 issue 的附加而非另開），本地票標記移交後結案
when: 本 session 專注元件庫，框架票不在此消化
where:
  layer: framework
  files:
  - docs/work-logs/v0/v0.1/v0.1.0/tickets/
why: '本 session 的目標已由用戶收窄為元件庫 skill。Wave 3 的 27 張 pending 中有 14 張的根源在 .claude/ 通用資產（sync-pull 衝突不落地、SubagentStop 提前清空、skill 體量與 description 執法層、error-pattern 可發現性、ticket CLI 截斷無提示、worktree 路徑誤判、代理人自檢與派發判準），依 framework-issue skill 的介入判斷屬框架問題——敘述替換專案名後仍成立、根源在共享資產。在本專案逐張修只解本地徵狀，其他 consumer 仍帶同一缺陷，且下次 sync-pull 可能覆蓋。既有 40 張 open issue 中 #85（skill-sync 四訊號）已涵蓋其中三張的主題，須走附加而非另開。'
how:
  task_type: Documentation
  strategy: 先對每一族跑 list_issues.py --search 查重，命中 open 者以 framework-issue-curator 的 observe 附加證據、不另開；查無者以 create_issue.py 建新張。分族後每張本地票在 Solution 記錄對應 issue ref，再由 PM 統一 close。
acceptance:
- '[x] 14 張票各有明確去處（新 issue ref / 既有 issue 附加 ref / 判定不需外送並附理由），無票懸空'
- '[x] 建新 issue 前皆已跑過查重，且查重命中 open 者一律走附加不另開'
- '[x] 每個新建 issue 的 body 含原票的 why 全文（證據不得在轉寫中流失）與對應本地票 ID'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-07T14:04:10'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

把 14 張框架/skill 票分族提為 framework issue（含既有 issue 的附加而非另開），本地票標記移交後結案

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### Context Bundle：外送清單、查重起點與分族建議

**要外送的 14 張本地票**（票面全文在 `docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-XXX.md`，逐張讀 `why` 與 `how.strategy`，那是證據所在，轉寫時不得摘要掉）：

W3-005、W3-006、W3-007、W3-008、W3-009、W3-010、W3-011、W3-014、W3-015、W3-020、W3-040、W3-042、W3-047、W3-049、W3-050

W3-013 不在此列——它已綁定既有 issue #79，留在本專案做。

**PM 已跑過的查重結果（起點，非結論）**：既有 open issue 中這三張與本批主題重疊，優先判定為附加而非另開。

| 既有 issue | 主題 | 疑似對應本批 |
|---|---|---|
| #85 | skill-sync 同步狀態四訊號皆不可信 | W3-009、W3-040、W3-042 |
| #79 | skill 體系兩個維護缺口：範例只計數、引用單向累積 | W3-014／015／020 的上位？待判 |
| #84 | rule8 守衛只掃 staged diff 新增行 | W3-008？機制可能不同，待判 |

這是 PM 初判不是指令。執行者自行跑 `list_issues.py --search` 重驗，判定不同就照自己的判定走並在票面寫明理由。

**分族建議（同樣是起點，可改）**：

1. skill 體量與 description 的執法層缺口與存量收斂：W3-014 / W3-015 / W3-020
2. sync-pull 非版本檔衝突完全不落地 + 拉取後全樹比對：W3-011
3. SubagentStop 提前清空 dispatch-active.json 使兩道並行防護降級：W3-010
4. worktree 路徑使 rule8／presence guard 誤判 docs/ 與 CLAUDE.md：W3-008
5. error-pattern 可發現性：標題過窄找不到 + 諮詢時機落在 complete 之後：W3-047 / W3-049
6. 代理人自檢與派發判準：W3-005（輸出載體維度）/ W3-007（acceptance 未達成仍勾選）/ W3-006（衝突腳本雙側保留誤用於程式碼檔）
7. ticket CLI 的 `--top` 截斷無提示：W3-050

族的切法可改，但每一族要能回答「這一族的讀者一次要處理的是同一件事嗎」。

**硬性要求**：

1. 每個新建 issue 的 body 必須含原票 `why` 的全文，不得壓縮成一句。那些 why 裡的實測數字（29/58 skill body 超標、24/59 description 超標、7.12x、44 個未排序 pattern、26 筆報成 10 筆）是這批問題唯一的證據，摘要掉就只剩主張。
2. 每個 issue body 標明對應的本地票 ID（`graph_project_docs_manager:0.1.0-W3-XXX`）。
3. 不要 close 或改任何本地 ticket 的 status（代理人定義禁止跨 ticket 物件操作）。只在本票 Solution 寫一張對照表：本地票 ID → 去處（新 issue ref / 既有 issue 附加 ref / 判定不外送＋理由）。

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
### 對照表：15 張本地票（票面稱 14，逐張列出後實為 15）的去處

| 本地票 | 去處 | 說明 |
|--------|------|------|
| 0.1.0-W3-005 | 新 issue `tarrragon/claude#92` | 併入「PM 派發判準與代理人自檢三項缺口」 |
| 0.1.0-W3-006 | 新 issue `tarrragon/claude#92` | 同上，缺口二 |
| 0.1.0-W3-007 | 新 issue `tarrragon/claude#92` | 同上，缺口三 |
| 0.1.0-W3-008 | 新 issue `tarrragon/claude#90` | worktree 路徑誤判 rule8/presence guard；已於 body 交叉引用 `#84`（機制疑似不同，待實測確認） |
| 0.1.0-W3-009 | 既有 issue `tarrragon/claude#85` 附加（observe） | 對應第 3 項訊號（CDN 快取）；observe 記錄跨 consumer 分工現況（flutter_balance `0.2.1-W3-1106` 承接實作），本側不重複建票 |
| 0.1.0-W3-010 | 新 issue `tarrragon/claude#89` | SubagentStop 提前清空 dispatch-active.json；已交叉引用附帶發現 `#80`（不同機制，不重複收錄） |
| 0.1.0-W3-011 | 新 issue `tarrragon/claude#88` | sync-claude-pull.py 衝突丟棄上游變更 |
| 0.1.0-W3-014 | 新 issue `tarrragon/claude#87` | 併入「skill body/description 執法層缺口」，缺口一 |
| 0.1.0-W3-015 | 新 issue `tarrragon/claude#87` | 同上，缺口二 |
| 0.1.0-W3-020 | 新 issue `tarrragon/claude#87` | 同上，缺口三 |
| 0.1.0-W3-040 | 既有 issue `tarrragon/claude#85` 附加（observe） | 對第 5 項訊號的糾正：publish-only 目錄非必然殘留，需先排除其他 consumer 合法存量 |
| 0.1.0-W3-042 | 既有 issue `tarrragon/claude#85` 附加（observe） | 附帶小型發現（sync-claude-push.py 註解懸空引用），與四訊號機制無直接關聯，PM 初判歸入本 issue，執行者核實後維持此判斷 |
| 0.1.0-W3-047 | 新 issue `tarrragon/claude#91` | 併入「error-pattern 可發現性缺口」，缺口一 |
| 0.1.0-W3-049 | 新 issue `tarrragon/claude#91` | 同上，缺口二 |
| 0.1.0-W3-050 | 新 issue `tarrragon/claude#93` | ticket track list --top 截斷無提示 |

### 查重紀錄

7 個新建 issue（`#87`-`#93`）皆於 `section_comment.py init` 呼叫內建的
`--dedup-keywords` 查重後才建立；另對 W3-008、W3-010、W3-011、W3-014 等
族群額外執行過針對性 `gh search issues` 精確詞查證（`SubagentStop`、
`sync-conflicts`、`presence-guard`、`6500`、`file-size-guardian` 等），
確認無現成 issue 覆蓋該具體機制後才建新張。W3-009/040/042 三票經查重命中
既有 open issue `#85`（PM 初判起點確認成立），走 observe 附加不另開。

### 與 PM 初判的差異

PM 初判「#79（skill 體系兩個維護缺口）可能是 W3-014/015/020 的上位」，
經核實 `#79` 內容（範例只計數不看形態、skill 引用單向累積）與 W3-014/
015/020 的實際主題（skill body 字元數與 description 字元數的**執法層**
缺口）不同軸，故未併入，改建新 issue `#87`，並於其 body 單向引用 `#79`
說明關係（引用，非重複，不需互標）。PM 初判「#84 是否為 W3-008 的同源」
機制待判，經 `gh search` 查證仍無法確認，已在 `#90` body 保留待確認狀態，
不強行合併。

### 三項硬性要求核對

1. why 全文：7 個新 issue 的 body 皆以「逐字引用」小節完整收錄來源票的
   `why` 欄位原文，未摘要
2. 本地票 ID：每個新 issue body 末尾「對應本地票」章節皆含
   `graph_project_docs_manager:0.1.0-W3-XXX` 格式引用
3. 未 close 或改動任何本地 ticket 狀態（0.1.0-W3-005/006/007/008/009/
   010/011/014/015/020/040/042/047/049/050 皆維持原狀態不變）

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
**Completion Time**: 2026-09-07 14:24
**Executing Agent**: framework-issue-curator
**Review Status**: pending
**Summary**: 15 張本地 Wave 3 框架/skill 票（票面稱 14）全部完成外送判定：7 個新建 framework issue（tarrragon/claude#87-#93，涵蓋 skill 執法層缺口、sync-pull 衝突丟棄、SubagentStop 提前清空、worktree 路徑誤判、error-pattern 可發現性、PM 派發判準三缺口、ticket list 截斷）、3 筆 observe 附加至既有 issue #85（W3-009/040/042）。每個新 issue 皆先查重（init 內建 dedup + 針對性 gh search）、body 含來源票 why 全文與本地票 ID。對照表已寫入 Solution，未變更任何本地 ticket 狀態。
