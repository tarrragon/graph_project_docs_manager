---
id: 0.1.0-W3-183
title: 評估 元件庫回饋日誌契約條款是否應涵蓋未顯示與顯示失敗
type: DOC
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-166
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-167
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-167 第三問的 C1-C8 分類顯示：三個日誌缺口全部落在 0.1.0-W3-166 條款「有顯示就要有紀錄」的涵蓋範圍之外
  final_decision: 另立 DOC 票評估條款範圍，不由 0.1.0-W3-167 直接改 166 的產出
  rationale: 0.1.0-W3-167 票面 how 的分工約定明訂：本票結論若要求 166 的條款補充適用範圍，登記 spawn request 交 PM，不直接改該票產出
who:
  current: thyme-documentation-integrator
  history: {}
what: 評估 元件庫回饋日誌契約條款是否應涵蓋未顯示與顯示失敗
when: 0.1.0-W3-166 完成後（本票評估的是該票產出的條款範圍）
where:
  layer: Presentation
  files:
  - .claude/methodologies/component-library-bidirectional-constraint-methodology.md
  - docs/spec/ui/SPEC-004-component-library.md
why: '`0.1.0-W3-166` 把元件層內建回饋日誌固化為元件庫通用契約條款，主張是「有顯示就要有紀錄」。`0.1.0-W3-167` 的第三問分類（C1–C8）顯示，該票發現的三個缺口全部落在此涵蓋範圍之外：


  | 缺口 | 為何不被涵蓋 |

  |------|------------|

  | 被後續 `show` 截斷 | 顯示了，但使用者沒看完——「有顯示」成立，故條款判定為已滿足 |

  | 一幀都未顯示 | 呼叫了 `show` 但從未渲染——不是「有顯示」 |

  | 因判準而不顯示 | 根本沒呼叫——不是「有顯示」 |


  `0.1.0-W3-176`（日誌欄位）與 `0.1.0-W3-169`（規格條文）只處理 `AppSnackBar` 這一個元件。條款層不補，未來每一個新的終端回饋元件都會重現同一缺口，且重現時同樣不會有任何機制報出來——這正是 166 設立通用條款而非個案修法的理由。


  `0.1.0-W3-167` 的實驗 F5 提供了此缺口的具體後果：`withAction` 被截斷時動作按鈕消失且從未被呼叫，等同銷毀該功能的唯一入口，而現行條款對此無話可說。'
how:
  task_type: Documentation
  strategy: "1. 讀 `0.1.0-W3-166` 的最終產出，確認條款的實際措辭與涵蓋範圍（不以本票 `why` 的轉述為準）。\n2. 依 `0.1.0-W3-167` 的 C1–C8 分類逐類比對：哪些類別已被現行條款涵蓋、哪些不被涵蓋。\n3. 若確有缺口，評估兩個候選並擇一，於 Solution 記錄未採選項的排除理由：\n   - (a) 擴充條款為「顯示、未顯示、顯示失敗三者皆須有紀錄」——涵蓋完整，但對「未顯示」的定義須可機械判別，否則條款無法被否證。\n   - (b) 維持條款範圍，另立一條獨立的「回饋抑制須留痕」條款——兩條款各自邊界清楚，但讀者須同時記住兩條。\n4. 條款的驗收形態不由本票定義（屬 `0.1.0-W3-170` 管轄）；本票若擴充條款，須在 Solution 標明新增部分需由 170 補驗收形態，或登記 spawn request。\n\n不擴大範圍：本票不改 `AppSnackBar` 實作（屬 `0.1.0-W3-176`）、不改 SPEC 逐元件條目正文（屬 `0.1.0-W3-169`）。"
acceptance:
- '[ ] 已讀 0.1.0-W3-166 最終產出並記錄條款實際措辭，未以轉述為準'
- '[ ] 0.1.0-W3-167 的 C1-C8 逐類比對已完成，涵蓋與未涵蓋分別列出'
- '[ ] 候選 (a)(b) 擇一並記錄未採選項的排除理由；若判定無缺口，記錄判定依據'
- '[ ] 條款擴充部分的驗收形態已標明歸屬 0.1.0-W3-170 或已登記 spawn request'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

評估 元件庫回饋日誌契約條款是否應涵蓋未顯示與顯示失敗

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-166,0.1.0-W3-167 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-167 what: 設計 提示元件的使用時機、重疊處置、類型分化，與不可見時的日誌補位
- 0.1.0-W3-166 what: 固化 元件層內建回饋日誌為元件庫通用契約條款，並定為測試判定依據（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-167 why: **用戶裁示（2026-09-09）**：「也要額外評估一個問題，哪些時機要呼叫 snackbar 或者 bottoast 這類提示元件，**如果大量呼叫提示元件，反而會重疊遮蔽**，那有可能需要依照不同類型的提示需求，設計使用不同的提示元件，以及**如果提示元件看不到，我們要在哪些事件或者流程上要補 log**。」

`0.1.0-W3-166` 固化「有顯示就要有紀錄」，但不回答「何時該顯示」。兩者一起才完整：只有前者會導向「凡事都跳提示」，而提示互相爭用同一個顯示位時，**資訊反而比不提示更少**——後出現的取代前一則，使用者兩則都沒讀完。

**這不是假設性風險。** 本專案目前 `AppSnackBar.show(` 的呼叫點實查為 **3 處、2 個檔案**（`gap_report_screen.dart:199`、`:218`、`scan_notification_controller.dart:202`；`shell.dart` 只有註解提及，無呼叫）。而 `0.1.0-W3-113`（`ScanNotificationController.show` 失敗的 SnackBar fallback）與 `0.1.0-W3-165` 都會再增加觸發時機，編輯能力（`0.1.0-W3-073`）落地後還會更多——**呼叫點只會增加，所以此數為量測當下的值，接手者須自行複驗現值**（`reference-stability-rules` 規則 9）。

> **建票時的數字更正（2026-09-09）**：本段原寫「6 個呼叫點，分佈於三個檔案」。該值來自 `grep 'AppSnackBar.show\|AppSnackBar\.'`——第二個分支匹配任何成員存取（型別引用、列舉、註解），第一個分支的 `.` 為未跳脫的正則萬用字元，故量到的是「提及 `AppSnackBar` 的行數」而非呼叫點數。現值由兩個 session 各自以 `AppSnackBar\.show\(` 獨立實查並互相對上。另有一種口徑是「訊息結局」（同一呼叫點的分支各算一則）為 5 則，可並列參考，但 acceptance 第 2 項的對照對象是**呼叫點**。

**失效形態的更正見 Problem Analysis 的〈PM 前置實查〉第二節**：本專案的 `AppSnackBar.show` 在顯示前無條件呼叫 `hideCurrentSnackBar()`，且 `ScaffoldMessenger` 一次只呈現一則，故版面上的「重疊」不會發生，實際形態是**替換截斷**。用戶關切的後果成立且更嚴重（重疊時兩則都在畫面上，替換是後者把前者抹掉），但處置方向不同——分化元件解決的是「同時要看到兩則」，解決不了「被靜默丟棄」。

**第三個子問題是前兩者的必然對偶**：一旦決定某些時機不顯示提示，那些事件就失去了使用者可見的回饋，此時可觀測性必須由日誌承接——否則等於用「安靜」換掉了「有紀錄」，兩頭皆空。`0.1.0-W3-078` 的實機驗證正是撞上這個：路徑走到了、程式做了對的事，但沒有任何一側留下使用者層級的證據。
- 0.1.0-W3-166 why: **用戶裁示（2026-09-09）**：「元件層內建回饋日誌應成為元件庫的通用契約條款。這些元件庫的回饋可以成為測試的判定依據，尤其這是最終使用者真正看到的資訊，**如果我們只驗邏輯，整個資訊鏈還是有可能中斷**。」

該裁示有一個當日的實證。`0.1.0-W3-078` 第二次實機驗證中，`0.1.0-W3-096` 附帶的診斷測試綠燈（`aut（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Related Files
- docs/spec/ui/SPEC-004-component-library.md::read  # from 0.1.0-W3-167
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-167
- .claude/skills/ux-design-evaluation/references/interaction-feedback.md::read  # from 0.1.0-W3-167

### Source Acceptance (reference)
- [ref] [ ] 何時使用暫態提示的判準已建立且可機械套用，輸入為事件性質而非逐案列舉  # from 0.1.0-W3-167
- [ref] [ ] 若結論為需要新提示元件，已說明與 AppSnackBar 的分界並走 component-contract-design 流程  # from 0.1.0-W3-167
- [ref] [ ] 因判準或遮蔽處置而不顯示的事件集合已逐類指定日誌承接點與等級，含被丟棄的提示  # from 0.1.0-W3-167
- [ref] [ ] 承接的規格撰寫已建 DOC 票或已登記 spawn request  # from 0.1.0-W3-167
- [ref] [ ] 全部 AppSnackBar.show( 呼叫點逐一對照該判準，不符者列出並給處置（派發前實查為 3 處／2 檔，接手者須自行複驗現值）  # from 0.1.0-W3-167
  ... (truncated, see source ticket)

