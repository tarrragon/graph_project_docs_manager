---
id: 0.1.0-W3-336.10
title: 補範例 version-shift-sop.md 盤點步驟1契約檔案未定義（B9）
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: 0.1.0-W3-336
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 補範例 version-shift-sop.md 盤點步驟1契約檔案未定義（B9）
when: 0.1.0-W3-336.6 完成後
where:
  layer: 待定義
  files:
  - .claude/skills/version-bootstrap/references/version-shift-sop.md
why: W3-332.7 判定 B9 為缺範例類型；version-shift-sop.md 不在 0.1.0-W3-336.6 的 where.files 範圍，依規則只改本票 where.files，故另立票承接
how:
  task_type: Documentation
  strategy: 依 chained-examples.md 三段鏈式格式為 version-shift-sop.md 盤點步驟1補範例
acceptance:
- '[ ] 盤點步驟1『其他跨版本共用契約檔案』補三段鏈式範例，界定契約檔案範圍（schema/DDL 之外是否含 API 路由表、i18n key 清單等）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-15T09:41:16'
completed_at: null
created: '2026-09-15'
updated: '2026-09-15'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補範例 version-shift-sop.md 盤點步驟1契約檔案未定義（B9）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

依票面約束，先用 `.claude/skills/skill-design-guide/references/chained-examples.md`
〈驗證段機械檢查：依據句可溯性〉重新檢查 B9（來源：0.1.0-W3-332.7 Solution 需範例位置表）
再動筆，而非直接依 W3-332.7 已定的「缺口類型：缺範例」寫範例。

B9 對應條文：`version-shift-sop.md` 盤點步驟 1「對照移版提案的 checklist，逐項檢查是否觸及
`schema/*.schema.json`、`docs/spec/**/*.md` 的 DDL 章節、或其他跨版本共用契約檔案」（第 15 行）。
若要對「其他跨版本共用契約檔案」寫檢驗問句（設計產出物形態，依 chained-examples.md〈各段允許
與禁止內容〉判定），該問句必須能指回條文中一句界定「契約檔案」範圍的定義句。

逐一查證結果（`grep -rn "契約檔案\|契約 SOT\|SOT 檔案\|契約級" .claude/skills/version-bootstrap/
.claude/skills/foundation-design/`）：

- `version-shift-sop.md` 第 15 行：僅列舉兩個具名項（`schema/*.schema.json`、DDL 章節）與一個
  開放第三類「其他跨版本共用契約檔案」，未界定第三類的邊界。
- 第 9 行（動機段）：「契約 SOT 檔案（如 schema 定義）」——僅舉例，非定義句。
- 第 24-29 行（判斷準則表）：分級對象是「已被步驟 1 判定觸及」的項目，處理的是「觸及項算不算硬
  耦合」，不是「什麼算契約檔案」，無法回頭充當步驟 1 的邊界定義。
- 專案內唯一另一處「契約」相關模板 `.claude/skills/doc/templates/data-contract-template.md`
  定義的是「資料契約文件」這個 SPEC 文件類型本身（A/B 兩區結構），與本處「契約檔案＝觸及即需
  盤點的物件」是不同概念，不能借用。
- SKILL.md 第 92 行僅重複「schema／DDL／契約級的殘留耦合」，同樣未界定第三類開放項邊界。

**結論**：條文中「其他跨版本共用契約檔案」查無任何定義句可作為檢驗問句依據，依
〈「缺範例」與「缺引導」的分界〉判定順序（先問「這個決策點有沒有文字約束存在」）——本處雖有
「盤點觸及項」這個動作的文字約束，但對「第三類（其他契約檔案）的範圍」本身沒有文字約束，讀者
（例如 W3-332.7 甲乙兩讀者：一判僅 schema／DDL、一判含 API 路由表與 i18n key 清單）必須自行
發明範圍才能執行盤點。**此為缺引導，非缺範例**——與 W3-332.7 原判定不同；W3-332.7 判定時
chained-examples.md 尚無〈驗證段機械檢查：依據句可溯性〉一節（該節由 W3-332.12 之後定案），
故原判定未套用此機械檢查，屬規格演進後的重新分類，非 W3-332.7 誤判。

依〈「缺範例」與「缺引導」的分界〉Action：「缺引導的位置額外附上建議的文字約束草稿供 PM
裁決，不可直接以範例代替文字約束」，故不在 `version-shift-sop.md` 內自創定義或範例，草稿見
NeedsContext 章節。

### 影響範圍

`.claude/skills/version-bootstrap/references/version-shift-sop.md` 盤點步驟 1
不受本票影響（未修改）；acceptance 「界定契約檔案範圍」需先由 PM／用戶裁決文字約束草稿，
定案後才能回頭補三段鏈式範例（新 follow-up 或本票續作）。

### 相關 Error Pattern

無新增；本案例屬 `chained-examples.md`〈驗證段機械檢查：依據句可溯性〉Why 段描述的情境
本身（判準只活在範例裡、未回寫條文，下一位撰寫者查無依據），此處是在動筆前攔下，未讓
問題進入文件。

### B9 WRAP 評估結果與 PM 裁決（2026-09-15）

獨立評估者跑完整模式。窄案、寬案與「刪除開放第三類」都是列舉檔案類型，只差清單長短，屬假選項。以 11 個測例做現實檢驗，只有「鎖定點三問」判準全部判對。寬案另有兩個問題：「當下已有消費方」與 SKILL.md 步驟 2 的明文衝突，且草稿含本專案識別符。

**PM 裁決：採鎖定點三問判準（W3）**。本題不是用戶程式碼風格偏好：定義只決定哪些檔案會漏盤，不影響程式碼怎麼寫，因此不上呈用戶。落地內容：
- 步驟 1 動作欄改指〈契約檔案的界定〉。
- 在盤點步驟表與判準表之間新增〈契約檔案的界定〉小節，內容包括：
  - 定義句
  - 三問判準：持久資料、邊界外消費方（判定單位是發布或部署單位，不是儲存庫）、明訂凍結
  - 三問皆否時的排除句
  - 非封閉示例
- 步驟 3 判準表：表頭加一句硬耦合的兩條件，第一、二列合併並泛化路徑，後兩列不動。
- 附帶修正：步驟 1 與判準表寫死的 `schema/*.schema.json`、`docs/spec/**`、`schema/event.schema.json` 都是其他專案殘留（本專案沒有 `schema/`），一併泛化。
- 範例依 premortem 原因 1 補一則前提相反的對照範例：同一儲存庫內各自部署的服務。

改後文字草稿以評估報告第 4 節為準，已轉交本票執行者。
## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
### 變更摘要

- `.claude/skills/version-bootstrap/references/version-shift-sop.md`：
  - 盤點步驟表步驟 1「動作」欄改為指向新節〈契約檔案的界定〉，不再列舉 `schema/*.schema.json`／`docs/spec/**/*.md`（其他專案殘留路徑，本專案無 `schema/` 目錄）。
  - 新增〈契約檔案的界定（步驟 1 盤點範圍與步驟 3 分級共用）〉節：定義句 + 三問判準表（持久資料／邊界外消費方／明訂凍結）+ 三問皆否排除句 + 非封閉常見清單。
  - 判斷準則（步驟 3 分級）表：表頭前加硬耦合兩條件句；原 4 列合併為 3 列（第一、二列合併並泛化為「修改 schema 定義檔、DDL 或其他經三問判定的契約檔案之形狀」）；後兩列不動。
  - 新增〈範例〉節：範例一（正例，判準 1 持久資料命中）與範例二（前提相反對照，同一儲存庫、各自部署的兩服務，判準 2 邊界外消費方命中）。
- `.claude/skills/version-bootstrap/SKILL.md`：〈按需讀取〉路由列同步——「硬耦合四類判斷準則」改「硬耦合三類判斷準則」，補列「契約檔案界定三問」；`metadata.version` 1.6.5 -> 1.6.6。
- `.claude/skills/version-bootstrap/CHANGELOG.md`：新增 1.6.6 條目。

### 依據句可溯性自檢表（chained-examples.md〈驗證段機械檢查：依據句可溯性〉）

| 範例 | 對應判準 | 檢驗問句 | 依據句（逐字或近逐字） | 出處 |
|---|---|---|---|---|
| 範例一 Q1 | 判準 1 | 「變更項 A 判為契約檔案的依據，能否指回〈契約檔案的界定〉判準句？」 | 「本版發布後，是否會以該形狀寫入並保存資料（資料庫列……），使日後改形狀必須遷移或相容讀取？」 | 本節判準 1 問句（逐字） |
| 範例一 Q2 | 定義句 | 「是否需要三問全部為是才判定契約檔案？」 | 「三問任一為『是』即屬契約檔案」 | 本節判準句前導文字（逐字） |
| 範例一 Q3 | 定義句 | 「判為非契約檔案，是否因為它不屬於任何檔案類型清單？」 | 「檔案類型不決定歸屬」 | 本節定義句（逐字） |
| 範例二 Q1 | 判準 2 | 「與服務甲同屬一個程式碼儲存庫，是否因此排除判準 2？」 | 「判定單位是發布或部署單位，不是儲存庫」 | 本節判準 2 問句（逐字） |
| 範例二 Q2 | 判準 2 | 「若服務甲與服務乙改為同一次提交、同一次部署發布，判準 2 是否仍為是？」 | 「消費方位於另一個獨立發布或部署的單位……使改形狀無法在同一次提交內一併修改」 | 本節判準 2 問句（近逐字，省略中段列舉） |
| 範例二 Q3 | 定義句 | 「判為契約檔案，是否代表這一類訊息永遠屬契約檔案？」 | 「同一類檔案（如路由表、多語系 key 清單）在不同專案可以落在任一側，一律依下列三問判定」 | 本節定義句（逐字） |

六則檢驗問句皆能在〈契約檔案的界定〉節找到依據句，判定合格。範例情境（訂單資料表折扣類型欄位、服務甲乙訊息）不含本專案識別符，不含具體程式語法（無 SQL/DDL 語法，僅自然語言描述）。

### B9 缺口狀態更新

原判定「缺範例」經 chained-examples.md〈驗證段機械檢查：依據句可溯性〉重新檢查後，確認條文缺定義句，重分類為「缺引導」；PM 依 WRAP 評估裁決採「鎖定點三問」判準（見 Problem Analysis〈B9 WRAP 評估結果與 PM 裁決〉），本次已補齊定義句與三問判準，並依定案文字補上兩則三段鏈式範例。acceptance 標的「界定契約檔案範圍」已由三問判準取代原「schema/DDL 之外是否含 API 路由表、i18n key 清單」的封閉式列舉框架——是否列入不再依檔案類型，而依三問判定，故不需窮舉 API 路由表／i18n key 清單是否屬於契約檔案（PM 裁決：檔案類型不決定歸屬）。

### 自檢結果

- [x] 步驟 1 動作欄、判斷準則表已依 PM 裁決文字更新，無寫死路徑殘留
- [x] 新增〈契約檔案的界定〉節，定義句 + 三問判準 + 排除句 + 非封閉示例全數逐字採用
- [x] 判斷準則表頭已加硬耦合兩條件句，原 4 列合併為 3 列
- [x] 兩則三段鏈式範例皆含【輸入】【產出】【驗證】三段，驗證段為檢驗問句 + 預期答案（設計產出物形態）
- [x] 範例二為前提相反對照（同一儲存庫、各自部署），命中判準 2
- [x] 依據句可溯性自檢：六則檢驗問句皆可指回條文定義句或判準句（見上表）
- [x] 範例不含本專案識別符、不含具體程式語法
- [x] SKILL.md〈按需讀取〉列同步（四類→三類，補列三問）
- [x] SKILL.md／version-shift-sop.md 實測 tokens（4842／2469）皆低於 5000 門檻，未觸發體量處置
- [x] CHANGELOG 與 metadata.version 已同步（1.6.5 -> 1.6.6）
- [x] 只改本票 where.files（+ SKILL.md／CHANGELOG.md 隨規範同步），未觸碰 .11／dart-style-guardian／foundation-design

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext
- **缺失項**：`version-shift-sop.md` 盤點步驟 1「其他跨版本共用契約檔案」缺文字約束界定範圍
  （依 chained-examples.md〈驗證段機械檢查：依據句可溯性〉重新判定為缺引導，非 W3-332.7
  原判定的缺範例；理由見 Problem Analysis）
- **觸發位置**：`.claude/skills/version-bootstrap/references/version-shift-sop.md`
  盤點步驟 1（標題「契約掃描」一列）
- **影響**：acceptance「盤點步驟1『其他跨版本共用契約檔案』補三段鏈式範例，界定契約檔案範圍」
  無法完成——缺引導狀態下寫範例等於範例自行發明判準，違反 chained-examples.md 明文禁止
  （「不可直接以範例代替文字約束」）
- **建議補料**：PM／用戶裁決下列文字約束草稿（供補進步驟 1 或緊鄰步驟 1 的說明），裁決後
  本票或 follow-up 票依定案文字補三段鏈式範例：

  草稿 A（窄範圍，僅資料層）：
  「其他跨版本共用契約檔案，指本版與至少一個其他版本的實作皆會讀寫、且其欄位或結構一旦
  變更會影響雙方消費邏輯的資料定義檔案，範圍限資料層（如 ORM model 定義、遷移腳本引用的
  型別定義檔）；不含 API 路由表、i18n key 清單等非資料結構的介面清單。」

  草稿 B（寬範圍，含介面層）：
  「其他跨版本共用契約檔案，指任兩個以上模組或版本依賴其定義的欄位、路徑或 key 集合作為
  讀寫依據的檔案，改變其集合會破壞既有消費方，範圍包含資料定義檔、API 路由表、i18n key
  清單；是否列入以本專案當下是否已有該類消費方為準——若本專案尚無 API 層或未把 i18n key
  集合當作跨模組契約，該類檔案不屬本步驟盤點範圍（現況：本專案為 macOS 桌面應用，多語系採
  Flutter 官方 ARB，尚無 API 層，若採此草稿現階段等同窄範圍）。

  兩草稿差異僅在「是否把非資料結構的介面清單（API 路由表、i18n key）預先納入範圍」；草稿 B
  多一句「以本專案當下是否已有該類消費方為準」的判準，避免未來若新增 API 層時需再次修文。
- **重派成本**：低。草稿已備妥，PM／用戶僅需二選一或修訂後裁定；裁定後續作範例撰寫約
  0.5 個 ticket 工作量（單一決策點三段鏈式範例，比照 W3-336.6 已完成的六則範例形式）。

## Exit Status
```yaml
exit_status: needs_context
reason: "B9 重新判定為缺引導（非缺範例）：契約檔案範圍在條文中查無定義句，依 chained-examples.md 不得以範例代替文字約束；已附兩版草稿供 PM/用戶裁決"
confidence: 0.9
acceptance_met: []
acceptance_unmet: [1]
artifacts: []
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
**Completion Time**: (not completed — needs_context)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending PM/用戶裁決

**未完成原因**：依票面約束「若『契約檔案』在條文中根本無定義句可指，停手寫進票面 Problem
Analysis 回報缺引導並附定義草稿，不在範例裡自創定義」——查證確認 version-shift-sop.md
盤點步驟 1「其他跨版本共用契約檔案」查無任何定義句，依 chained-examples.md〈驗證段機械檢查：
依據句可溯性〉與〈「缺範例」與「缺引導」的分界〉判定為缺引導，非 W3-332.7 原判定的缺範例
（規格演進後的重新分類）。未修改任何檔案，僅寫入本票 Problem Analysis（查證過程）與
NeedsContext（草稿 A/B 兩版供裁決）。**本票不 complete**，回報 PM。
