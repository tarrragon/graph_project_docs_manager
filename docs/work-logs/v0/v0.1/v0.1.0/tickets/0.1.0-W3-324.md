---
id: 0.1.0-W3-324
title: 實作 init 與 add 自行推導 owner，使漏填模式就地消滅
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-320
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-320 以 SR-1 切出：該項屬 319 裁決但不在 320 的形式驗收內，且為可分離的行為變更
  final_decision: 獨立成票，不回頭擴充 320 的範圍
  rationale: 320 的執行者未單方決定納入或捨棄，改登記 spawn request 交 PM 裁決，該處置正確；擴充已完成票的範圍會使其驗收記錄與實際產出不符
who:
  current: thyme-python-developer
  history: {}
what: 實作 init 與 add 自行推導 owner，使漏填模式就地消滅
when: 隨時；0.1.0-W3-320 的錨定判準已在 main
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/section_comment.py
why: '**`0.1.0-W3-319` 裁決二的後半尚未落地。** 該裁決定的是兩件事：命名空間（uuid 前 8 碼，已由 `0.1.0-W3-320` 落地）與**取值管道**——`init`／`add` **自行推導** owner，`--owner` 降為覆寫，推導失敗或與 `--owner` 不符時 exit 3 印診斷。後半未實作。


  **現況與裁決的差距**：`validate_owner` 已錨定且本 session 的推導值可通過，但**呼叫端仍須自行組出該值並以 `--owner` 傳入**。漏填時的行為仍是 exit 3 後由執行者自行降級 `observe`——**319 要的是「漏填模式就地消滅」，不是「漏填有訊號」**。


  **為何這個差距有實際後果**：本 session 歷次 curator 派發全數降級 `observe`，結論落進觀測流、不進 body 索引、`show` 與 `check` 的索引一致性皆不涵蓋。錨定修好了「值合不合法」，沒有修「值有沒有被帶上」。**只要它仍須由派發者手動填，同一個漏填就會再發生**——`0.1.0-W3-315` 與 `0.1.0-W3-319` 兩次派發都是實例。


  **取值來源已確認存在**（`0.1.0-W3-319` 實測，PM 複核）：`.claude/lib/hook_logging.py::resolve_session_id`，`ENV_SESSION_ID = "CLAUDE_CODE_SESSION_ID"`，現有 5 支 hook 在用，子代理人的 Bash 程序讀得到，且與 `.git/pm-registry.json` 的 sessions 鍵逐字相同。`section_comment.py` 對它的引用數為 0。


  **切票理由**：`0.1.0-W3-320` 的執行者未單方決定納入或捨棄，改登記 spawn request 交 PM——該處置正確。擴充已完成票的範圍會使其驗收記錄與實際產出不符。'
how:
  task_type: Implementation
  strategy: '**判準已定，本票只實作**：`0.1.0-W3-319` 裁決二。先讀該票 Solution 與 `0.1.0-W3-320` 的 Solution（錨定實作與其拒絕的候選）。


    1. `init`／`add` 在未給 `--owner` 時**自行推導** `<_project_owner_prefix()>-<resolve_session_id() 前 8 碼>`。

    2. `--owner` 降為覆寫：給了就用給的值，但**與推導值不符時 exit 3 並印診斷**（印出：推導值、傳入值、兩者不符的欄位）。

    3. **推導失敗**（環境變數缺席、前綴推導拋錯）時 exit 3 印三項診斷，**禁止靜默降級為 observe**——現況的靜默降級正是本票要消滅的東西。

    4. `transfer-owner` 走既有的 `validate_owner_for_transfer`，**不受本票改動影響**，並以測試斷言之。


    **測試要求**（正向對照輸入為硬要求）：未給 `--owner` 時推導值正確；`--owner` 與推導值相符時通過；**不符時被擋**；環境變數缺席時 exit 3 而非降級；`transfer-owner` 的跨 consumer 路徑不受影響。


    **`0.1.0-W3-319` 標明的未驗項照樣帶著，不得當已證**：`CLAUDE_CODE_SESSION_ID` 僅在單一平台版本觀測（`2-1-267`），跨版本存在性未驗——**第 3 點的 exit 3 診斷即為此準備**，不可因為本機能取到就省略該分支。'
acceptance:
- '[x] 未給 --owner 時 init/add 自行推導，推導值格式為 <前綴>-<uuid 前 8 碼>'
- '[ ] --owner 與推導值不符時 exit 3 並印診斷（推導值／傳入值／不符欄位）'
- '[ ] 推導失敗時 exit 3 印三項診斷，無任何靜默降級路徑'
- '[ ] transfer-owner 的跨 consumer 路徑不受影響，且有測試斷言'
- '[ ] 測試含「--owner 與推導值不符」與「環境變數缺席」兩類正向對照輸入並斷言被擋'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-11T19:59:16'
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

實作 init 與 add 自行推導 owner，使漏填模式就地消滅

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-11 20:14)
  - what: 更新 .claude/skills/framework-issue/references/worked-example.md 的 --owner 範例（現用舊格式 flutter-balance-77，且未反映 init/add 已改自行推導）
  - why: worked-example.md 未在本票 Context Bundle 範圍內，但列於 SKILL.md 導覽表，屬 skill-cli-sync-check 涵蓋範圍；本票已同步 ticket-intake.md 與 comment-as-section-protocol.md，此檔獨漏會使範例與現行行為不一致
  - suggested_type: DOC
  - suggested_priority: P2
  - related_files: 
  - context: 
  - status: dismissed（已在本票內直接修正（一行 CLI 範例，無範圍風險），不需獨立票）


## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-320 | chars: 1001 -->

### Task Reference
- 0.1.0-W3-320 what: 把 OWNER_FORMAT_RE 改為錨定 _project_owner_prefix() 推導值，並同步錯誤訊息與兩份 references 的 owner 範例（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-320 why: 現行 OWNER_FORMAT_RE 要求 owner 尾碼為純數字，而 owner 值的供應端是 session 短名，尾碼實測為兩碼十六進位。13 個真實 session 名跨三個 consumer 實測 4 個被擋（含非本專案的 blog-cd），為跨 consumer 的普遍缺陷。

owner 的三個讀取端全為前綴比對（section_comment.py 的 _discover_owned_issue_numbers_by_prefix、todo --consumer 的列篩選、session-start-issue-check-hook.py 的擁有檢查），尾碼寫入後從未被任何讀取端解讀——純數字要求是寫入端單方面的判準。

ticket-intake.md 的 owner 識別行範例值尾碼含字母，照該範例填寫必然 exit 3；該段加入目的正是修「漏填 owner」，成因因此由漏填換成照範本填。

證據與方向裁決見 0.1.0-W3-315 與 0.1.0-W3-311 的 Solution，以及 tarrragon/claude#95 的觀測。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/framework-issue/tests/test_section_comment.py  # from 0.1.0-W3-320（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/references/ticket-intake.md  # from 0.1.0-W3-320（來源已完成，結論見 Solution）
- .claude/skills/framework-issue/references/comment-as-section-protocol.md  # from 0.1.0-W3-320（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] owner 驗證改為錨定推導前綴，本專案尾碼含字母的 session 名可通過 init 與 add  # from 0.1.0-W3-320（來源已完成，結論見 Solution）
- [ref] [x] 前綴推導失敗時降級為格式檢查而非硬擋，且有對應測試  # from 0.1.0-W3-320（來源已完成，結論見 Solution）
- [ref] [x] transfer-owner 的跨 consumer 移交有明確處置（逃生口或豁免），且有對應測試  # from 0.1.0-W3-320（來源已完成，結論見 Solution）
- [ref] [x] 錯誤訊息印推導前綴，兩份 references 的 owner 範例已改為占位形態且無具體 consumer 名  # from 0.1.0-W3-320（來源已完成，結論見 Solution）
- [ref] [x] 測試含代理人角色名、含底線值、他 consumer 值三類正向對照輸入並斷言被擋  # from 0.1.0-W3-320（來源已完成，結論見 Solution）
