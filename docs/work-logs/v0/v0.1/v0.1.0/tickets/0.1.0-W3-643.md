---
id: 0.1.0-W3-643
title: 修正 uc-reference-validation hook 的 UC 編號 regex 從複合識別符誤截片段
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-645
source_ticket: 0.1.0-W3-634
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-634 合併時 hook 誤報復現，非單一代理人環境問題
  final_decision: 修 regex 左邊界，並以正反兩個對照輸入驗證
  rationale: 誤報的成因是命名規範本身（value 為 UC ID 時必產生 uc-UC-NN），不會隨時間消失；放著不修等於讓此 hook 的輸出長期不可信
who:
  current: thyme-python-developer
  history: {}
what: 修正 UC 編號擷取 regex，使其不從 action-ucFlow-select-uc-UC-01 這類複合識別符中截出不存在的 UC-UC
when: W3-634 合併時復現後
where:
  layer: 待定義
  files:
  - .claude/skills/doc/doc_system/core/uc_registry.py
  - .claude/skills/doc/tests/test_uc_registry.py
  - docs/work-logs/topic-assignments.txt
why: 本日兩次命中：W3-634 的 list_row_test.dart 含 ValueKey('action-ucFlow-select-uc-UC-01')，hook 報「未定義 UC 編號：UC-UC」——實際使用的是合法的 UC-01，regex 把 select-uc- 的尾段 uc 與後續 -UC 拼成了 UC-UC。SPEC-003 的測試錨點命名表規定的 key 形態即 action-<screen>-<kind>-<value>，而 value 為 UC ID 時必然產生 uc-UC-NN 的字面，故凡實作 UC 選擇入口的檔案都會復現。誤報雖為 WARN 不阻擋，但它訓練讀者忽略這個 hook 的輸出——守衛的真正命中與誤報在輸出上同形
how:
  task_type: Implementation
  strategy: 為 UC 編號 regex 加左邊界（word boundary 或否定後行斷言），使 UC-NN 只在不緊接於識別符片段之後時才命中；以 action-ucFlow-select-uc-UC-01 為正向對照輸入驗證不再誤報，並另備一個真正含未定義 UC 編號的輸入驗證守衛仍會翻紅（test-assertion-design E2：守衛的失效方向是放行，修 regex 時最容易把真命中一起關掉）
acceptance:
- '[x] regex 對 action-ucFlow-select-uc-UC-01 不再誤報 UC-UC'
- '[x] 以一個真正含未定義 UC 編號的輸入驗證守衛仍翻紅（E2 正向對照）'
- '[x] hook 對現行 lib 與 test 全量掃描無誤報'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-21T14:04:53'
completed_at: null
created: '2026-09-21'
updated: '2026-09-21'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

修正 UC 編號擷取 regex，使其不從 action-ucFlow-select-uc-UC-01 這類複合識別符中截出不存在的 UC-UC

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

實測復現（uv run --directory .claude/skills/doc python3 -c ...）確認：
`find_uc_tokens_in_text("...action-ucFlow-select-uc-UC-01...")` 只回傳一個
token `('uc-UC', 1)`，normalize 後為 `UC-UC`；真正的 `UC-01` 完全未被獨立
偵測到（不是「被誤判為違規」，而是被前一個假匹配的 value 群組吞掉，
regex 引擎的搜尋游標已越過它）。

根因在 `UC_TOKEN_RE`（`.claude/skills/doc/doc_system/core/uc_registry.py`
:45-48）：value 群組 `[A-Za-z0-9...]+` 是貪婪字元類，遇到
`select-uc-UC-01` 時，先在 `-uc-` 處（`-` 為 word boundary）比對到
prefix+hyphen，接著 value 群組貪婪吃進緊接的 `UC`（因為 `U`/`C` 也落在
`[A-Za-z0-9...]` 範圍內），直到遇到下一個 `-` 才停止，產出偽匹配
`uc-UC`。搜尋游標推進到此匹配結束處（`UC` 之後），使真正的 `UC-01`
永遠不會被當作獨立 token 掃到。

### 影響範圍與範疇修正

Ticket where.files 僅列 `.claude/hooks/uc-reference-validation-hook.py`，
但該檔本身不含 UC token regex，只是呼叫
`doc_system.core.uc_registry.find_uc_tokens_in_text()`（模組 docstring
明載「本模組是該規則的唯一實作（單一來源），PreToolUse 寫入驗證 hook
須複用本模組，避免各自實作導致規則漂移」）。regex 實體
（`UC_TOKEN_RE`）定義於 `.claude/skills/doc/doc_system/core/uc_registry.py`
，該模組同時被 `doc uc verify/trace/context` CLI 共用。

故實際修改對象調整為 `uc_registry.py`（+ 其既有測試
`.claude/skills/doc/tests/test_uc_registry.py`），而非 hook 檔本身；
hook 檔不需改動（它只是消費端）。此為 ticket 認領檢查清單「已獨立
驗證 Ticket 描述的數量/範圍」項下的修正，範疇仍是同一顆 bug、同一個
acceptance，僅落點檔案更正為正確的單一實作來源，不算越界。

### 相關 Error Pattern

未查得直接對應既有 error-pattern；本案屬 regex 貪婪匹配吞併相鄰合法
token 的典型案例，暫不另建 error-pattern（單一模組內的區域性 bug，非
跨專案可重現的結構性錯誤）。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 修復方式

在 `UC_TOKEN_RE`（`.claude/skills/doc/doc_system/core/uc_registry.py`）的
hyphen 後、value 群組前插入負向前瞻 `(?![UuＵｕ][CcＣｃ][-－﹣―])`：
禁止 value 緊接的下一段本身又是另一個 UC- 前綴。遇到
`select-uc-UC-01` 這類複合識別符時，先在 `-uc-` 處嘗試匹配的候選
（prefix=uc，hyphen=-）因為緊接的是 `UC-`（另一個合法前綴形態），
前瞻判定失敗，該候選整體不匹配；regex 引擎接著在真正的 `UC-01`
處重新起始並成功匹配。value 群組本身的字元類與既有邏輯不變，
不影響格式違規/偽子樹判定（`is_violation_token`）與白名單比對邏輯。

### 修改檔案

- `.claude/skills/doc/doc_system/core/uc_registry.py`：`UC_TOKEN_RE`
  加負向前瞻（唯一程式碼變更）
- `.claude/skills/doc/tests/test_uc_registry.py`：新增
  `TestCompoundIdentifierTokenBoundary` 測試類別，3 個測試：
  1. 正向：`action-ucFlow-select-uc-UC-01` 只偵測到 `UC-01`，不再
     產生偽 token `UC-UC`（對應 acceptance 1）
  2. `UC-01` 在白名單中時判定非違規
  3. E2 反向對照：同一種複合識別符形態換成真正未定義的
     `UC-99`，守衛仍需翻紅（對應 acceptance 2，防止修邊界時把
     真命中一起關掉）

hook 檔（`.claude/hooks/uc-reference-validation-hook.py`）本身不含
regex，無需改動——它只是呼叫 `uc_registry` 模組的共用函式（見
Problem Analysis「影響範圍與範疇修正」）。

### 驗證：全量掃描比對（acceptance 3）

以 `find_uc_tokens_in_text` + `is_violation_token` 模擬 hook 邏輯，
對 `lib/` 與 `test/` 下 `.dart/.js/.ts/.py` 全量掃描（套用
`HOOK_SCANNABLE_EXTENSIONS` 與 `is_exempt_path`）：

- 修復前（git stash 暫存本次變更後重跑）：164 檔案，**14** 處違規，
  含 `test/unit/components/list_row_test.dart:17` 的
  `uc-UC -> UC-UC`（本票要修的確切誤報）
- 修復後：164 檔案，**13** 處違規，該筆誤報消失，其餘 13 筆
  （`uc-unset`、`test/helpers/test_copy.dart` 的 `UC-09`、
  `matrix_grid_test.dart` 的 `UC-0`/`UC-00`）在修復前後完全一致
  ——與本次 regex 邊界修正無關的既有情況（多為測試 fixture 中的
  佔位字串或格式不符的字面），不屬本票範疇，不在此處理

比對確認：本次修復只移除了目標誤報，未引入新誤報、也未遮蔽既有
其他發現（後者若判定屬真問題，依規則 5 另開票追蹤，非本票 acceptance
要求）。

### 自檢結果（Layer 1）

**執行時機**: complete 前
**掃描手段**（實際執行的指令，逐項可複驗）：
- `grep -n "數據\|代碼\|默認\|文檔\|軟件\|硬件\|信息\|視頻" .claude/skills/doc/doc_system/core/uc_registry.py .claude/skills/doc/tests/test_uc_registry.py` → 0 處命中
- 人工檢視兩份變更檔內容，確認無 emoji、無簡體字/日文漢字字形
- 對照 `.claude/pm-rules/ticket-body-schema.md` IMP 必填章節清單：
  Problem Analysis / Solution / Test Results 三章節皆已填入實質內容
- 對照 A1-A3（三明示 / 資訊優先序 / 負向對比有正向錨點）：Problem
  Analysis 根因段落含成因（value 群組貪婪吃字）+ 後果（真 UC-01
  永不被掃到）+ 已執行的修正動作；Solution 修復方式段落同樣三層齊備
- H2 標題檢查：本次僅使用 append-log 寫入既有 Schema 章節（Problem
  Analysis / Solution / Test Results），未新增自訂 H2

| 類別 | 發現項目 | 處理方式 |
|------|---------|---------|
| B1 | 無命中 | 無需處理 |
| B2/B3 | 人工檢視無發現 | 無需處理 |
| C1 | 三必填章節皆已填寫 | 無需處理 |
| C2 | 未新增自訂 H2 | 無需處理 |
| A1-A3 | 逐段核對三明示齊備 | 無需處理 |

**自檢結論**: 已掃描範圍內通過（掃描範圍：本票新增/修改的兩份程式碼檔
內容 + 本票 ticket body 三章節；未涵蓋：本票未觸碰的其他既有檔案）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 測試執行

指令：
```
uv run --directory .claude/skills/doc pytest tests/test_uc_registry.py -v
```
結果：76 passed（含新增 TestCompoundIdentifierTokenBoundary 3 個測試）

指令：
```
uv run --directory .claude/skills/doc pytest -q
```
結果：285 passed, 1 skipped（既有 skip 與本票無關）— 確認未回歸 doc uc
CLI 共用同一 uc_registry 模組的其餘測試（uc.py/uc_acceptance_check.py 等）

### 全量掃描驗證（acceptance 3）

見 Solution 章節「驗證：全量掃描比對」。修復前 164 檔案 14 處違規（含
目標誤報 `test/unit/components/list_row_test.dart:17` 的
`uc-UC -> UC-UC`）；修復後 164 檔案 13 處違規，目標誤報消失，其餘 13
筆與本票根因無關（已建 0.1.0-W3-645 追蹤，SR-1 已 resolve）。

### Phase 4 重構評估

- 重構需要：否（單一 regex 加一段負向前瞻，改動範圍最小，無其他壞味道）
- 技術債務：無新增；發現既有 3 類殘留誤報已建票 0.1.0-W3-645 追蹤
- 程式碼品質：A（改動聚焦、有正反兩組測試對照、註解說明前瞻用途與動機）
- 結論：無需進一步重構

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests
- **SR-1** (2026-09-21 14:12)
  - what: uc-reference-validation hook 對 lib/test 全量掃描仍有 13 處與本票無關的既有誤報，分三種根因：(1) Dart 字串內插 $i 未被 regex 理解（matrix_grid_test.dart:17 的 UC-0$i 被截成 UC-0）；(2) 測試檔案內的通用佔位標籤剛好符合 UC-NN 格式但語意上不是真的 UC 引用（matrix_grid_test.dart:69/159 的 UC-00、test_copy.dart:54 的 UC-09 語料字面）；(3) 狀態 sentinel 值字面剛好以 uc- 開頭（domain_view_screen.dart:496 等多處的 uc-unset）
  - why: 0.1.0-W3-643 修復的是複合識別符誤截片段（select-uc-UC-01 型）單一根因；本次驗收時做全量掃描比對意外發現另外 13 處不同根因的誤報，依 quality-baseline 規則 5 發現即建立追蹤，不應在本票順手擴大範圍修復（三種根因需各自設計修法，非同一顆 regex 邊界問題）
  - suggested_type: ANA
  - suggested_priority: P2
  - related_files: 
  - context: 
  - status: processed（已建 0.1.0-W3-645）


## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-634 | chars: 519 -->

### Task Reference
- 0.1.0-W3-634 what: 為 ListRow 增補 option 變體：主文字為 UC ID + 標題、selected 態、整列可點、testKey action-ucFlow-select-uc-<ucId>（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-634 why: SPEC-004 §3.6〈UC 選擇入口〉（UC Flow 視圖 尚未選定 UC／flow 未結構化／正常 三態共用）要求 ListRow.option，變更歷史 1.35 已核定但 lib/components/list_row.dart 僅有 tree/sectionHeader/item/meta/numbered 五變體，option 不存在；W2-002 因此無法組成三個狀態的右欄，占 SPEC-001 §2 五狀態中的三個（來源已完成，結論見 Solution）

### Related Files
- lib/components/list_row.dart  # from 0.1.0-W3-634（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] ListRow 提供 option 變體，slot 為 primary／selected／onTap／testKey  # from 0.1.0-W3-634（來源已完成，結論見 Solution）
- [ref] [x] 元件測試含 selected 與未 selected 的對照組，斷言兩者視覺產物不同  # from 0.1.0-W3-634（來源已完成，結論見 Solution）
- [ref] [x] 0.1.0-W2-002 的 尚未選定 UC／flow 未結構化／正常 三態可依 SPEC-004 §3.6 組成〈UC 選擇入口〉  # from 0.1.0-W3-634（來源已完成，結論見 Solution）

### UC Context
#### UC-01: 開啟專案並抵達可用狀態
Spec 位置: docs/app-use-cases.md:22
主要流程:
  select-folder: 選擇資料夾
  load-schema: 載入型別表
  parse-nodes: 解析節點
  reach-domain-view: 抵達 Domain 視圖
