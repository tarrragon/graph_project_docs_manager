---
id: 0.1.0-W2-005
title: 實作節點詳情（SPEC-001 §6 全部狀態，含部分損壞與原始檔已消失）
type: IMP
status: in_progress
version: 0.1.0
wave: 2
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W1-002
- 0.1.0-W1-005
- 0.1.0-W1-003
- 0.1.0-W1-036
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: SPEC-001 §6 的3個狀態需要實作
  final_decision: 只交狀態渲染與退出路徑，不接真實資料
  rationale: 0.1 的整合測試與資料正確性解耦，因此後續每次加入邏輯時持續有效
who:
  current: parsley-flutter-developer
  history: {}
what: 實作節點詳情（SPEC-001 §6）全部狀態列：專案未就緒／未選節點／正常／部分損壞／原始檔已消失；以 W1-003 fixture 驅動（含 1673 行長文與缺 frontmatter 樣本），不串真實資料
when: 路由殼完成後，與其他畫面票可並行
where:
  layer: Presentation
  files:
  - lib/screens/node_detail/
why: 節點詳情是 markdown 渲染的落點（flutter_markdown_plus 已於 0.0.3 選定），且含部分損壞與原始檔已消失兩個必須明確呈現的狀態
how:
  task_type: Implementation
  strategy: '依 SPEC-004 §3.6 逐狀態組合（狀態來源 SPEC-001 §6；頁面檔只從 lib/components/components.dart 匯入）：

    未選節點：`EmptyState.page`（動作前往追溯視圖）。

    正常：`TwoColumnLayout`[main `Panel.scrollable`[`ListRow.meta`, `AppText.title`, `BadgeRow`, `Divider`, `DocumentBody`], detail `Panel.scrollable`[`Section.static` × N（關聯）]]；返回與開啟原始檔的 `AppButton` 置於 `SplitRow.header` 右側 `ButtonRow`（§3.7 第 17 項；開啟原始檔行為依 W1-036 定案；返回錨點 `action-<destination>-back` 由 AppShell 覆蓋列承載，頁面不重複渲染）。

    部分損壞：同上 + 欄位級 `IssueMarker.damagedDetail`（說明文字 slot，跳轉破洞報告）。

    原始檔已消失：`MissingSourceState`（`ButtonRow`[`AppButton.primary` 重新整理, `AppButton.secondary` 返回]，最後已知路徑 slot）。

    退出路徑依 SPEC-003 §3.6 接 router.dart 既有 provider（returnToProvider）。寫不成上述句型的狀態即為缺件訊號：停手建元件票並回報，不就地自製。'
acceptance:
- '[ ] 頁面檔的元件 import 只來自 lib/components/components.dart 統一匯出入口；頁面層不以原生佈局原語直接包兩個以上元件庫元件（grep 驗收）'
- '[ ] 每個狀態的退出路徑依 SPEC-003 §3.6 可實際操作走通（返回經 returnToProvider）'
- '[ ] widget test 逐狀態渲染一次並斷言 §3.6 指定的元件型別存在（不重驗溢位）'
- '[ ] SPEC-001 §6 全部狀態列皆可渲染，每列以 SPEC-004 §3.6 對應行的契約內元件組成；狀態由 W1-003 fixture 驅動（正常態含 1673 行長文樣本）；原始檔已消失僅由開啟原始檔 notFound 進入，重新整理鈕屬 MissingSourceState 元件內'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-21T12:58:20'
completed_at: null
created: '2026-08-27'
updated: '2026-08-27'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

實作 節點詳情的3個狀態

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 票面改定（PM，2026-09-15）

依 W3-335 閘門組修補（V4 第四輪通過、門檻外矛盾與已完成畫面落差已清）更新：狀態清單改引用 SPEC-001 現行版本（v1.16）該節全部狀態列，不寫死數目（reference-stability 規則 10）；解除 W3-335 封鎖。

- 現行規格：SPEC-001 v1.16、SPEC-003 v1.37、SPEC-004 v1.43
- 同步期新增的共用件可重用：graphBuiltProvider（專案未就緒判定，lib/app/graph_status.dart）、選定 UC 狀態（lib/app/selected_uc.dart），由 W3-379 建立
- 欄位級損壞標記承擔跳轉破洞報告，不另設按鈕（W3-335.53 E15）

### WRAP 三問
- W：候選方案 (1) gap_report 式 Notifier watch graphBuiltProvider；(2) trace/domain_view 式簡單 StateProvider，「專案未就緒」為獨立狀態列舉、不 watch graphBuiltProvider。選 (2)：node_detail ticket how 未提及 graphBuiltProvider 接線，且與最新完成的 trace/domain_view 慣例一致。
- A：機會成本——若後續要求 node_detail 也自動響應 graphBuiltProvider 變化，需追加票把 StateProvider 升級為 Notifier（同 gap_report 模式），非本票範圍。
- P：最大失敗風險是元件庫 slot 型別不符（ListRow.meta leading 型別、BadgeRow 只收 Badge 不收 IssueMarker）。已於實作前逐一讀元件原始碼核對簽章，寫碼一次通過 dart analyze 0 issue。

### 缺件盤點
- `BlockedState.plain` 缺 `onDegradedView` slot（0.1.0-W1-074）、shell.dart 無降級徽章返回列容器（0.1.0-W2-009）：本票 §6 五狀態皆不涉及降級疊加旗標，未觸發此缺件。
- SPEC-004 §3.7 第 18 項「開啟原始檔」頁首右側掛點：尚未由 0.1.0-W2-008 落地，本票比照 W2-004（gap_report）做法，將該按鈕暫置主欄內容頂部 ButtonRow，見 Solution 與 NeedsContext。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
新增 `lib/screens/node_detail/`（state / fixtures / providers / screen 四檔，同 domain_view 慣例），接線 `lib/app/router.dart` 一行（`buildDestinationPage` 新增 `AppDestination.nodeDetail` 分支）。

**狀態設計**：`NodeDetailState` sealed class 五個子類，對應 SPEC-001 §6 五列（專案未就緒／未選節點／正常／部分損壞／原始檔已消失）。正常與部分損壞共用 `NodeDetailReady(nodeId)`，是否為部分損壞由 `NodeDetailFixtures.nodes[nodeId].damagedFields` 決定（同 `DomainReady.isDegraded` 疊加旗標慣例），非獨立子類。`nodeDetailStateProvider` 為簡單 `StateProvider`（同 trace/domain_view，非 gap_report 的 Notifier watch graphBuiltProvider 模式）。

**Fixture**：`e2e-contract`（正常，長文樣本，行數比照 `test/fixtures/corpus/book_overview_v1/docs/spec/extraction/e2e-contract.md` 1673 行數量級生成，非逐字複製）、`csv-export-spec`（部分損壞，借用 `gap_report_provider.dart` 已驗證的缺 frontmatter 樣本，同一 `book_overview_v1` 快照，`damagedFields: ['type']`）。兩者 `filePath` 皆不對應磁碟上任何檔案（`docs/spec/extraction/`、`docs/spec/csv-export-spec.md` 於本專案真實 `docs/spec/` 下皆不存在，已用 `test -e` 驗證），故「開啟原始檔」恆走 `notFound` 分支，同 `domain_view_screen.dart` 泳道開啟原始檔設計。

**退出路徑**：`專案未就緒`／`未選節點` 用 `navigateTo(..., NavIntent.jump)`（自動依 SPEC-003 §2.3 規則 2 設定 `returnTo`，不手動覆寫）；`正常`／`部分損壞` 的關聯項點擊直接改寫 `nodeDetailStateProvider.notifier.state`（同畫面替換，`returnTo` 不變）；「開啟原始檔」`notFound` → 直接轉 `NodeDetailMissing`（無 SnackBar，狀態轉換即回饋，同 SPEC-003 §3.6 定義）；「重新整理」三分支以 `File(path).existsSync()` 判定，存在時恆判完整解析（0.1 無真實 re-parse 邏輯，斷點偵測待真實資料整合）。返回鍵不在本畫面渲染，已由 `AppShell`/`components/app_shell.dart` 統一承載（`action-<destination>-back`，SPEC-003 §2.4，實測確認）。

**缺口記錄（比照 W2-004）**：「開啟原始檔」`AppButton` 依 SPEC-004 §3.7 第 17 項核定應在 `SplitRow.header` 右側 `ButtonRow`，但 `0.1.0-W2-008`（per-screen 頁首 trailing 掛點）尚未落地，暫置主欄內容頂部 `ButtonRow`，見 NeedsContext。

### 自檢結果
- `grep -rn "package:.*components/" lib/screens/node_detail | grep -v components.dart` → 0 筆
- `fvm dart analyze lib test` → No issues found
- `fvm flutter test test/ --reporter compact` → All tests passed（1377 個測試，含本票新增 17 個）
- Phase 4 結論：程式碼品質 A（沿用既有三畫面已驗證的架構模式：sealed state + fixture provider + StateProvider 注入，無新增抽象；`_openSource`／`_refresh` 與 gap_report/domain_view 既有同構函式重複度高但屬既定慣例，非本票技術債）。無需重構；技術債務：無新增，僅延續已知的「§3.7 第 18 項頁首掛點缺件」（0.1.0-W2-008 既有追蹤）。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-002,0.1.0-W1-003,0.1.0-W1-005,0.1.0-W1-036 | chars: 1882 -->

### Task Reference
- 0.1.0-W1-002 what: 建立 六項導覽的路由殼（來源已完成，結論見 Solution）
- 0.1.0-W1-005 what: 建立 跨畫面共用的元件庫（來源已完成，結論見 Solution）
- 0.1.0-W1-003 what: 建立 真實 repo 快照的測試 fixture（來源已完成，結論見 Solution）
- 0.1.0-W1-036 what: 決定 0.1 是否落地外部開啟能力；不落地則須定義 disabled 呈現與常駐說明文案（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W1-002 why: SPEC-001 §1-7 界定六個畫面加一個浮層，共 29 個狀態。各畫面若自帶導覽，切換行為會分歧。本票交出可切換的殼，讓後續每張畫面票只需關心自己的內容。（來源已完成，結論見 Solution）
- 0.1.0-W1-005 why: SPEC-001 的 29 個狀態中，空狀態出現 6 次、阻擋狀態 3 次、載入態 3 次、損壞標記 3 次——同一個概念在七個畫面各自實作會產生七種樣子，而 0.1 的整合測試斷言的正是「每個狀態都畫得出來」，樣子不一致時無從判斷哪個才是對的。（來源已完成，結論見 Solution）
- 0.1.0-W1-003 why: SPEC-001 設計約束已定案假資料採真實 repo 快照而非生成器，理由是生成器產的損壞形態是想像的——0.0.3 的審查證明想像的損壞形態可能整批是假的（130 個 YAML 錯誤為量測 artifact）。快照帶進來的是實際存在的形態。（來源已完成，結論見 Solution）
- 0.1.0-W1-036 why: SPEC-001 §1、§2、§5、§6 共四處有「開啟原始檔」或「開啟 docs 目錄」動作，SPEC-003 §3 已為四處各自定義了成功、檔案不存在兩條反應路徑。

App Sandbox 已關閉（PROP-001 的能力決策），技術上可行，但 0.1 是否實際落地未見任何決策記錄。

依 SPEC-003 FR-06，未接線動作只有兩種合法形態：不渲染，或 enabled 為 false 加同列常駐說明文字。無論落地與否都需要一個決定——若落地，需確認 macOS 開啟外部程序的實作路徑與失敗處理；若不落地，需定義四處各自的 disabled 呈現與說明文案，而該文案又回頭需要 i18n key。決定懸空時，W1-005 的元件無法確定要不要承載這個動作的兩種形態。（來源已完成，結論見 Solution）

### Related Files
- lib/app/router.dart  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- lib/app/shell.dart  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- lib/main.dart  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- integration_test/app_test.dart  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- lib/l10n/app_zh.arb  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
- lib/components/  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- test/unit/components/  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- test/fixtures/corpus/  # from 0.1.0-W1-003（來源已完成，結論見 Solution）
- tool/snapshot_corpus.dart  # from 0.1.0-W1-003（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W1-036（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 導覽列為六項，與 PROP-004 的畫面清單逐項對應  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- [ref] [x] 六項皆可切換且各自渲染標示畫面名的佔位頁  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- [ref] [x] 側欄頂端顯示專案名，點擊有回應（浮層內容可為空殼）  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- [ref] [x] 四種視窗尺寸下導覽列與內容區皆不溢位  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- [ref] [x] lib/components/ 為統一匯出入口（barrel）；SPEC-004 §3.1 定案的元件 26、容器 16 皆自該入口匯出；頁面層元件 import 只來自該入口  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- [ref] [x] 元件內零裸值：lib/（tokens/ 除外）grep 不到色碼、尺寸、字級字面值，亦 grep 不到 Duration(milliseconds: / Duration(seconds: 後接字面數字  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- [ref] [x] 41 張子票（Wave A 原子與文字 / B 容器基礎 / C 狀態與殼層 / D 資料視圖）全部 complete，各自 acceptance 為 SPEC-004 條目投影與測試契約  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- [ref] [x] shell.dart 漂移對齊：_kSidebarWidth 改 LayoutSize.sidebarWidth、_NavItem 改引用 NavItem、_ProjectSwitcherEntry 改 SwitcherOverlay、標題列依 4.27；既有測試錨點不改名  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- [ref] [x] 四個專案的節點檔已入庫為 fixture，且各自的框架版本與 .claude/VERSION 一併記錄  # from 0.1.0-W1-003（來源已完成，結論見 Solution）
- [ref] [x] fixture 涵蓋四種極端：真實規模、空狀態、異常長內容、損壞資料，各自標明來源專案  # from 0.1.0-W1-003（來源已完成，結論見 Solution）
- [ref] [x] ticket 規模項的處置已決定，Solution 記錄三個選項的權衡與選擇理由  # from 0.1.0-W1-003（來源已完成，結論見 Solution）
- [ref] [x] 以框架的逐行 frontmatter 語意解析 fixture，結果與本機原專案一致（不因抽取而改變分類）  # from 0.1.0-W1-003（來源已完成，結論見 Solution）
- [ref] [x] 已實測 macOS 下開啟外部檔案與目錄的可行性，記錄實測環境與結果  # from 0.1.0-W1-036（來源已完成，結論見 Solution）
- [ref] [x] 落地或不落地擇一定案；不落地時四處的 disabled 呈現與說明文案皆已定義  # from 0.1.0-W1-036（來源已完成，結論見 Solution）
- [ref] [x] SPEC-003 §3.1／§3.2／§3.5／§3.6 對應列已回填定案結果  # from 0.1.0-W1-036（來源已完成，結論見 Solution）
- [ref] [x] 若定案產生新文案需求，已建票或併入 i18n 補齊票  # from 0.1.0-W1-036（來源已完成，結論見 Solution）
### 派發要點（PM 於 2026-09-03 依已派發的 W2-004／006／007 prompt 固化，供 context 清除後直接派發）

- 本票畫面目錄：`lib/screens/node_detail`；派發條件：W1-036 定案後派。
- 起始：claim 本票；讀本票 how（已是逐狀態組合句型，照句型組裝不臨場設計）、SPEC-001 對應 §、SPEC-003 對應 §、SPEC-004 §3.6 對應行與所用元件條目；讀 `lib/app/router.dart`／`shell.dart`（`AppDestination`、`selectedDestinationProvider`／`returnToProvider`／`visitedDestinationsProvider`）、`lib/components/components.dart` 匯出清單、`lib/workspace/workspace_repository.dart`（`WorkspaceState`）。
- 結構：畫面狀態以 sealed class 表示並由 Riverpod provider 供給；fixture provider 讀 `test/fixtures/corpus/`（W1-003；ticket 樣本由 W1-023 入庫）；頁面檔元件 import 只來自 `lib/components/components.dart`；`lib/app/` 只加本目的地的一行接線，不重排、不改其他目的地。
- 缺件出口：寫不成 how 句型的狀態時停手，在 Problem Analysis 記錄缺什麼元件／slot 並回報 PM；不就地自製、不改 `lib/components/`。
- 測試：`test/unit/screens/node_detail_test.dart` 逐狀態渲染並斷言 §3.6 指定元件型別存在、退出路徑操作後 provider 狀態改變；前台執行 `fvm flutter test test/ 2>&1 | tail -3` 全綠、`fvm dart analyze lib test 2>&1 | tail -1` 0 issue；`grep -rn "package:.*components/" lib/screens/node_detail | grep -v components.dart` 為 0；禁用 run_in_background；不得留下 debug 測試檔。
- 收尾：append-log Problem Analysis / Solution（必含 `### 自檢結果` 子章節與 Phase 4 結論）/ Test Results；check-acceptance 逐項；狀態轉換子命令在 worktree sandbox 被擋屬預期，回報最後 commit hash 由 PM 代執行；回報前 `git status --porcelain` 為空。
- 合併：PM 在 worktree 分支 merge main（barrel／lib/app 接線衝突取聯集）後 --ff-only 落地，代執行狀態轉換後移除 worktree。
