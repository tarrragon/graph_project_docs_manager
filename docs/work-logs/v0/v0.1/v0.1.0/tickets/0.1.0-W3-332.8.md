---
id: 0.1.0-W3-332.8
title: 走查 SPEC-001 §4 Ticket 清單的元件與測試推導（先導畫面）
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-332
children: []
blockedBy:
- 0.1.0-W3-332.12
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: saffron-system-analyst
  history: {}
what: 走查 SPEC-001 §4 Ticket 清單的元件與測試推導（先導畫面）
when: 範例規格子票完成後
where:
  layer: 待定義
  files:
  - docs/spec/ui/SPEC-001-screen-state-matrix.md::read
  - docs/spec/ui/SPEC-003-interaction-response.md::read
  - docs/spec/ui/SPEC-004-component-library.md::read
why: 走查讓 agent 只持 skill 與規格去推導元件契約與測試情境，被迫猜測之處即缺口；猜測點是區分「skill 缺引導」與「spec 缺描述」的量測依據（先導：六狀態含疊加態，走查方法的缺陷在此先暴露再調整後續批次）
how:
  task_type: Analysis
  strategy: 盲推導：只讀畫面鏈 skill 與 SPEC-001／SPEC-003 對應章節，不讀 SPEC-004；逐狀態產出三段鏈式範例（元件選件與排位、契約關鍵欄、Given/When/Then 驗證）；每個猜測點記錄位置錨點、猜了什麼、依據缺在 skill 或 spec。推導完成後才與 SPEC-004 對照，分歧列為猜測誤差或 SPEC-004 自身缺陷。只產表不改檔
acceptance:
- '[ ] 範圍內每個狀態皆產出三段鏈式推導結果'
- '[ ] 猜測點表每列含位置錨點、猜測內容、初判缺口來源（skill／spec／兩者）'
- '[ ] 與 SPEC-004 的對照分歧逐項列出並初判歸因'
- '[ ] 走查方法本身的問題（規格不足以操作處）另列一節回饋範例規格子票'
tdd_phase: phase0
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-14T10:58:03'
completed_at: null
created: '2026-09-14'
updated: '2026-09-14'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

走查 SPEC-001 §4 Ticket 清單的元件與測試推導（先導畫面）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

本票為實驗性質的盲推導走查，非修正既有問題。目標：持 skill 與 SPEC-001/003 對 §4 Ticket 清單的六狀態（含疊加態）盲推導元件契約與測試情境，記錄猜測點，以量測 skill 引導能力與 spec 描述完整度。

### 影響範圍

- 走查範圍：SPEC-001 §4 全部 6 列（未載入、載入中、正常·列表、正常·主題、無 ticket、含損壞疊加態）
- 不改任何檔案，只產出分析表寫入本票

### 相關 Error Pattern

無直接相關。本票為新實驗方法的先導批次。

## 重現實驗結果
### 實驗方法

盲推導走查：只讀 SPEC-001 §4、SPEC-003 §3.4 與四份畫面鏈 skill（component-contract-design、ux-design-evaluation、foundation-design、test-assertion-design），不讀 SPEC-004。逐狀態產出三段鏈式範例（【輸入】→【產出】→【驗證】），每個猜測點記錄位置錨點、猜測內容、初判缺口來源。

### 實驗執行

#### 狀態 1：未載入（`state-tickets-unloaded`）

**【輸入】** SPEC-001 §4 未載入列：顯示「載入 N 張 ticket」+ 預估耗時；可用操作為開始載入、返回；首次進入此畫面。SPEC-003 §3.4 生命週期：隨 IndexedStack 建構但不觸發解析，首次可見進入此態，須使用者按 `action-tickets-start-load`。退出路徑：`action-tickets-back` → returnTo（null 時不渲染）。

**【產出】** 選件與排位（自頂向下）：
- 頂部工具列：返回按鈕（`action-tickets-back`，returnTo 為 null 時不渲染）+ 專案切換入口（`project-switcher-entry`）
- 主區：載入提示卡——標題文字含 N（票數）+ 預估耗時 + 開始載入按鈕（`action-tickets-start-load`）

| 元件 | 語意 | 關鍵契約欄 |
|------|------|-----------|
| 返回按鈕 | 返回來源畫面 | returnTo 為 null 時整個元件不渲染；非 null 時 enabled 恆 true |
| 載入提示卡 | 告知票數與耗時 | N 值來源：猜測為該專案 docs 下 ticket 檔案數的預掃描結果 |
| 開始載入按鈕 | 觸發解析 | 點擊後自身消失，進入載入中態 |

**猜測點 G1**：N 值（票數）的來源——spec 說「N 為該專案實際票數」但未說明 N 如何取得（預掃描檔名？讀 todolist.yaml？）。skill 與 spec 皆未引導。**缺口來源：spec**。

**猜測點 G2**：預估耗時的計算方式——spec 僅說「預估耗時」但未指出計算依據（每票解析時間 × N？固定公式？）。**缺口來源：spec**。

**猜測點 G3**：載入提示卡的元件形態——是獨立元件還是空狀態元件的一種變體？skill（component-contract-design）的判準問「它對使用者的語意是什麼」，語意為「引導使用者觸發載入」，與空圖/無 ticket 的「告知無資料」不同，推斷為獨立元件。**缺口來源：skill**（元件候選判準未明示此類「引導觸發」元件的歸屬）。

**【驗證】**
Given 首次進入 Ticket 清單畫面且 returnTo 非 null
When  畫面渲染完成
Then  主區顯示載入提示卡含票數 N 與預估耗時，開始載入按鈕可操作，返回按鈕可見

Given 首次進入 Ticket 清單畫面且 returnTo 為 null
When  畫面渲染完成
Then  返回按鈕不存在（不渲染），導覽列仍可用

---

#### 狀態 2：載入中（`state-tickets-loading`）

**【輸入】** SPEC-001 §4：進度條 + 已解析筆數；可取消載入、切至其他畫面。SPEC-003 §3.4：取消 → 未載入；完成 → 正常·列表；載入中可切畫面（載入繼續，§2.8 L1）。動畫：cross-fade 進入，determinate 進度條。

**【產出】** 選件與排位：
- 頂部工具列：取消按鈕（`action-tickets-cancel-load`）+ 專案切換入口
- 主區：進度條（determinate，值 = 已解析筆數 / N）+ 已解析筆數文字（每 Motion.progressTick 更新）

| 元件 | 語意 | 關鍵契約欄 |
|------|------|-----------|
| 取消按鈕 | 中止載入回到未載入態 | 點擊後進度條消失，回到 state-tickets-unloaded |
| 進度條 | 顯示解析進度 | determinate；值來源為已解析/總數；不得自行推進 |
| 筆數文字 | 顯示已解析筆數 | 更新頻率為 Motion.progressTick |

**猜測點 G4**：進度條是線性進度條還是圓形？spec 僅說「進度條」，未指定形態。推測為線性（與骨架版面的水平走向一致）。**缺口來源：spec**。

**猜測點 G5**：取消按鈕的位置——是頂部工具列的圖示按鈕，還是主區內的文字按鈕？SPEC-003 §3.4 導航跳轉表列 `action-tickets-cancel-load` 但未指定位置。推測為頂部工具列（與 §1 載入中的取消同位置一致性）。**缺口來源：spec**。

**【驗證】**
Given 使用者已按開始載入，10 張 ticket 中已解析 3 張
When  畫面更新
Then  進度條值為 3/10，筆數文字顯示 3

Given 使用者按取消
When  取消動作完成
Then  畫面回到未載入態，進度條消失

---

#### 狀態 3：正常·列表（`state-tickets-list`）

**【輸入】** SPEC-001 §4：密集表格 + 虛擬捲動；可搜尋、篩選、排序、切模式、開票。SPEC-003 §3.4 互動反應表：搜尋（input-tickets-search，防抖動）、篩選（action-tickets-filter-<key>，下拉選單 F1-F7 契約）、排序（action-tickets-sort-<key>，三態循環 S1-S7 契約）、切模式（mode-tickets-list/topic）、開票（card-tickets-<ticketId>，jump 至 nodeDetail）。

**【產出】** 選件與排位：
- 頂部工具列：搜尋輸入框（`input-tickets-search`）+ 篩選下拉群（`action-tickets-filter-<key>`）+ 模式切換（`mode-tickets-list` / `mode-tickets-topic`）+ 專案切換入口
- 表格區：欄首帶排序（`action-tickets-sort-<key>`）+ 虛擬捲動列表（`scroll-tickets-list`）+ 各行為 ticket 卡（`card-tickets-<ticketId>`）

| 元件 | 語意 | 關鍵契約欄 |
|------|------|-----------|
| 搜尋輸入框 | 過濾 ticket | 防抖動 Motion.searchDebounce；清空立即還原 |
| 篩選下拉 | 依屬性篩選 | 同時至多一個展開（F1）；觸發器選中態只由 selected 決定（F2）；首項為「全部」（F3） |
| 排序欄首 | 三態循環排序 | none→asc→desc→none（S2）；單欄互斥（S1）；none 時列序為載入順序（S3） |
| 模式切換 | 列表↔主題 | 兩模式各自保留 offset（§2.8） |
| Ticket 卡 | 跳轉至節點詳情 | 點擊 jump 至 nodeDetail，設 returnTo 為 tickets |
| 虛擬捲動容器 | 大量 ticket 效能 | 捲至末端不拋 framework 錯誤 |

**猜測點 G6**：篩選下拉的 `<key>` 有哪些？spec 寫 `action-tickets-filter-<key>` 但未列舉具體的篩選維度（status? type? wave? priority?）。**缺口來源：spec**（SPEC-004 可能有定義）。

**猜測點 G7**：排序欄首的 `<key>` 有哪些？同上，spec 未列舉可排序的欄位。**缺口來源：spec**（SPEC-004 可能有定義）。

**猜測點 G8**：Ticket 卡的顯示內容——至少需要 ID、標題、狀態、類型，但具體欄位組成未在 SPEC-001/003 中定義。**缺口來源：spec**。

**猜測點 G9**：模式切換的元件形態——是 SegmentedButton？Tab？ToggleButton？推測為 SegmentedButton（兩個互斥選項的切換）。**缺口來源：skill + spec**。

**【驗證】**
Given 已載入 100 張 ticket，列表模式
When  在搜尋框輸入「bug」並等待防抖動結束
Then  清單只顯示含「bug」的 ticket，筆數更新

Given 篩選下拉 A 展開中
When  點擊篩選下拉 B 的觸發器
Then  下拉 A 先收合（點外部收合），B 不展開（需再點一次）

Given 欄 A 排序為 asc
When  點擊欄 B 排序
Then  欄 A 回到 none，欄 B 為 asc，offset 歸零

---

#### 狀態 4：正常·主題（`state-tickets-topic`）

**【輸入】** SPEC-001 §4：主題節 + 未歸屬節；可展開收合、切模式、開票。SPEC-003 §3.4：`expander-tickets-topic-<name>` 展開收合，保留自己的 offset。S6：排序與篩選只於列表模式可觸發。

**【產出】** 選件與排位：
- 頂部工具列：模式切換（同上，主題態下搜尋/篩選/排序不可觸發）+ 專案切換入口
- 主區：依主題分節的捲動容器（`scroll-tickets-topics`），各節有展開收合器（`expander-tickets-topic-<name>`）+ 節內各行為 ticket 卡 + 未歸屬節

| 元件 | 語意 | 關鍵契約欄 |
|------|------|-----------|
| 主題節標題 | 節的分隔與識別 | 含展開收合圖示 |
| 展開收合器 | 展開或收合該主題的票行 | 點擊切換，該節票行出現或消失 |
| 未歸屬節 | 無主題的 ticket | 推測恆展開或行為與其他節一致 |

**猜測點 G10**：「主題」的定義來源——ticket 依什麼欄位分主題？推測為 ticket 的 topic/category 欄位，但 SPEC-001/003 未定義。**缺口來源：spec**。

**猜測點 G11**：主題模式下搜尋/篩選/排序是否仍可見但 disabled，還是整組不渲染？S6 只說「只於 state-tickets-list 可觸發」，未指定不可觸發時的視覺呈現。推測為不渲染（切模式時工具列內容變化）。**缺口來源：spec**。

**猜測點 G12**：未歸屬節的排列位置——在所有主題節之後？之前？推測為最後。**缺口來源：spec**。

**【驗證】**
Given 已載入含三個主題的 ticket，主題模式
When  點擊主題 A 的展開收合器
Then  主題 A 的票行消失（收合）

Given 主題模式
When  切至列表模式再切回主題
Then  offset 仍為之前的位置（各自保留）

---

#### 狀態 5：無 ticket（`state-tickets-empty`）

**【輸入】** SPEC-001 §4：「此專案尚無 ticket」；可導覽至破洞報告。SPEC-003 §3.4：`action-tickets-goto-gaps` jump 至 gaps，設 returnTo。§2.7 空狀態動線。

**【產出】** 選件與排位：
- 主區：空狀態元件——訊息「此專案尚無 ticket」+ 導覽至破洞報告按鈕（`action-tickets-goto-gaps`）+ 專案切換入口

| 元件 | 語意 | 關鍵契約欄 |
|------|------|-----------|
| 空狀態元件 | 告知無資料並引導 | 訊息文字為 l10n key；動作按鈕跳轉破洞報告 |

**猜測點 G13**：空狀態元件是否與其他畫面的空狀態（空圖、無 UC、無提案）共用同一元件？推測是（語意相同：告知無資料 + 引導動作）。**缺口來源：skill**（component-contract-design 的「通用元件判準」應引導此判斷但未在 SPEC-001/003 中明示）。

**【驗證】**
Given 載入完成且票數為 0
When  畫面渲染完成
Then  主區顯示空狀態訊息，破洞報告按鈕可操作

Given 無 ticket 態，按導覽至破洞報告
When  跳轉完成
Then  破洞報告畫面顯示，returnTo 為 tickets

---

#### 狀態 6：含損壞（疊加態，`badge-tickets-corrupted`）

**【輸入】** SPEC-001 §4：正常視圖 + 損壞徽章與計數；可跳轉破洞報告。SPEC-003 §3.4：`badge-tickets-corrupted` 與 `state-tickets-list`（或 topic）同時存在。§2.11：損壞標記兩級。動畫：無入場動畫（靜態標記）。

**【產出】** 選件與排位：
- 在正常·列表或正常·主題之上疊加：損壞徽章（`badge-tickets-corrupted`）含計數
- 底層正常態的全部元件不受影響

| 元件 | 語意 | 關鍵契約欄 |
|------|------|-----------|
| 損壞徽章 | 標記存在損壞票 | 顯示損壞票數；點擊 jump 至破洞報告，設 returnTo 為 tickets |

**猜測點 G14**：損壞徽章的位置——在工具列？在表格標題旁？推測為工具列區域（與其他畫面的損壞標記位置一致）。**缺口來源：spec**。

**猜測點 G15**：損壞徽章的兩級（§2.11 提及）——兩級分別是什麼？推測為「部分損壞」與「完全損壞」，但 §4 只提到一種徽章。**缺口來源：spec**（需查 §2.11 完整定義，可能在 SPEC-004）。

**猜測點 G16**：損壞的 ticket 在列表中是否仍然可見？推測為可見但帶標記（類似 §6 部分損壞的處理）。**缺口來源：spec**。

**【驗證】**
Given 已載入 50 張 ticket 其中 3 張解析失敗，列表模式
When  畫面渲染完成
Then  正常列表可見，損壞徽章顯示計數 3，徽章可點擊

Given 含損壞態，點擊損壞徽章
When  跳轉完成
Then  破洞報告畫面顯示，returnTo 為 tickets

### 實驗發現

盲推導完成六狀態（含疊加態），共記錄 16 個猜測點（G1-G16）。猜測點分布：

| 缺口來源 | 數量 | 猜測點 |
|---------|------|--------|
| spec | 11 | G1, G2, G4, G5, G6, G7, G8, G10, G11, G12, G14 |
| skill | 2 | G3, G13 |
| spec + skill | 2 | G9, G15 |
| spec（可能 SPEC-004 已覆蓋） | 5 | G6, G7, G8, G15, G16 |

主要發現：
1. SPEC-001/003 對 §4 的互動反應規格非常詳細（篩選 F1-F7、排序 S1-S7），但元件形態（形狀、位置、尺寸）完全未定義——這是 SPEC-004 的職責
2. 具體資料維度（篩選欄位、排序欄位、卡片欄位、主題定義）在 SPEC-001/003 中為開放狀態
3. 空狀態元件的跨畫面共用性是 skill 層面的引導缺口——component-contract-design 的通用元件判準未在 spec 消費端明示

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-332.12 | chars: 454 -->

### Task Reference
- 0.1.0-W3-332.12 what: 修正 chained-examples 規格的五項 PM 審查發現（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-332.12 why: PM 審查 W3-332.1 產出發現五項缺陷，其中三項會直接讓九張下游票各自發明判讀：判準是詞彙代理而非分歧檢測、缺「缺範例／缺引導」分界、驗證段只容 Given/When/Then 無法套用於設計產出物（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/skill-design-guide/references/chained-examples.md  # from 0.1.0-W3-332.12（來源已完成，結論見 Solution）
- .claude/skills/skill-design-guide/CHANGELOG.md  # from 0.1.0-W3-332.12（來源已完成，結論見 Solution）
- .claude/skills/skill-design-guide/SKILL.md  # from 0.1.0-W3-332.12（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] F1 判準改為分歧檢測，詞彙清單降為輔助訊號，並補一個無模糊詞仍命中的正例  # from 0.1.0-W3-332.12（來源已完成，結論見 Solution）
- [ref] [x] F2 新增缺範例與缺引導的分界判準  # from 0.1.0-W3-332.12（來源已完成，結論見 Solution）
- [ref] [x] F3 驗證段形態依用戶裁示改寫  # from 0.1.0-W3-332.12（來源已完成，結論見 Solution）
- [ref] [x] F4 完整示範的產出段具備真正的契約表，驗證段不引入輸入未定義的狀態、按鈕溢位不錯掛內容政策  # from 0.1.0-W3-332.12（來源已完成，結論見 Solution）
- [ref] [x] F5 相容判定節改為不預先豁免，既有範例的處置交由審查子票判定  # from 0.1.0-W3-332.12（來源已完成，結論見 Solution）
### 走查方法（W3-332.1／.12 定案，2026-09-14）

**權威規格**：`.claude/skills/skill-design-guide/references/chained-examples.md`（三段鏈式格式、驗證段兩形態、缺範例與缺引導的分界）。

**執行順序（順序即實驗設計，不可調換）**

1. 讀畫面鏈 skill：`component-contract-design`、`ux-design-evaluation`、`foundation-design`、`test-assertion-design`（SKILL.md 與按需 references）
2. 讀 SPEC-001 本票範圍章節與 SPEC-003 對應章節。**此時不得讀 SPEC-004**——SPEC-004 已含元件契約，先讀會照抄，量不到猜測點
3. 逐狀態盲推導，以三段鏈式寫出：【輸入】狀態表該列 →【產出】選件與排位＋關鍵契約欄 →【驗證】元件行為用 Given/When/Then
4. 推導中每一次「skill 與 spec 都沒告訴我、我自己決定了」即記一個猜測點
5. 推導全部寫完並提交到票面之後，才讀 SPEC-004 對照

### 產出：寫入本票 Solution

- **推導結果**：逐狀態一則三段鏈式
- **猜測點表**：位置錨點（skill 或 spec 的標題／條款名）｜猜了什麼｜替代選項｜初判缺口來源（skill 缺引導／skill 缺範例／spec 缺描述／兩者）
- **SPEC-004 對照分歧表**：分歧項｜我的推導｜SPEC-004｜初判（我猜錯／SPEC-004 缺陷／兩者皆可）
- **規格回饋**：走查方法或 chained-examples 規格無法操作之處（本票為先導，此節會回饋到 .9/.10）

### 約束

只讀不改 skill 與 spec；缺口一律交由彙整票 W3-332.11。
