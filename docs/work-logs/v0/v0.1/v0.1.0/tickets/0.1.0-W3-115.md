---
id: 0.1.0-W3-115
title: 補 元件庫方法論掛載點架構層引用 + observability-rules 交叉引用 + ARCH-GPD-001 輸出端路由
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-114
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-107
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 補 元件庫方法論掛載點架構層引用 + observability-rules 交叉引用 + ARCH-GPD-001 輸出端路由
when: 0.1.0-W3-114 落地後（引用目標須先存在）
where:
  layer: Framework Docs
  files:
  - .claude/methodologies/component-library-bidirectional-constraint-methodology.md
  - .claude/rules/core/observability-rules.md
  - .claude/error-patterns/architecture/ARCH-GPD-001-feedback-coupled-to-service-success-path.md
why: '`0.1.0-W3-114` 把 Port 回饋契約落在 `clean-architecture-implementation-methodology.md`。該落點是 `0.1.0-W3-107` 裁定的——非 UI 層實作者設計 port 時會讀那份方法論，回饋契約放那裡才會在正確時機被讀到。


  但**新資產指向既有資產、既有資產不指回**正是 `DOC-GPD-002` 記錄的單向累積形態：三個既有資產各自缺一條指向新 section 的路由，而它們的讀者恰好是最需要知道這件事的人。


  三處各自的缺口不同：


  1. **元件庫方法論**〈掛載點〉：它定義的三層是 UI（driving port）特化。讀者讀完不會知道同一結構在輸出端也成立，也不會知道輸出端的條文在別的檔案。本票只加一行架構層引用——**不搬移任何內容**（該檔三個 `#####` 子節標題已被 `component-contract-design` skill 與 `doc` 範本逐字引用為跨檔錨點，搬移會斷鏈）。

  2. **`observability-rules`**：它從日誌側寫（catch 要有日誌、統一工具、全域攔截、關鍵流程三階段），回答「發生了要記下來」，不回答「哪些時刻**必須**是可觀測的回饋點」。兩者互補卻無接線——讀規則的人不會知道回饋點該在設計期決定，而那正是它所要求的日誌能否拿到有用資訊的前提。

  3. **`ARCH-GPD-001`**：解決方案段已由 `0.1.0-W3-102` 補為三層，但那三層仍是輸入端敘述；輸出端的對應形態（消費者為呼叫端執行緒／log／APM）未涵蓋。'
how:
  task_type: Implementation
  strategy: '先讀 `0.1.0-W3-114` 的 Completion Info 取得新 section 的**確切標題字串**，再開該檔核對存在（PM 轉述以檔案為準）。逐處補反向路由：


    1. **元件庫方法論**〈掛載點〉子節內加一行：本節定義的三層是輸入端（driving port）特化，輸出端（driven port）的對應形態見架構層方法論的該 section。**不新增子節標題、不搬移既有內容**。

    2. **`observability-rules`**：補一處說明日誌義務與回饋點設計的關係——回饋點是設計期產物，日誌只是其消費者之一；未先定義回饋點時，日誌能記到的只有例外堆疊。位置選在能被「正在寫 catch 區塊的人」讀到之處，不是附在檔尾相關文件列。

    3. **`ARCH-GPD-001`**：解決方案段補輸出端的對應形態與其消費者，並指向架構層 section。


    **寫觸發時機而非泛稱連結**：「相關：X」不會被讀到，「做完 A 之後由 X 承接」才會在正確時機被看見（`DOC-GPD-002` 預防措施）。


    **成對檢查**：每一處新增的跨檔引用，兩端同時開啟確認措辭互相對應——本票是為了修單向接線而存在，修法本身只補一端會直接重演 `DOC-GPD-002` 的變體。


    **不寫數字**：不以處數或層數命名或作跨檔錨點。'
acceptance:
- '[x] 元件庫方法論〈掛載點〉加一行架構層引用，未新增子節標題、未搬移既有內容'
- '[x] observability-rules 補日誌義務與回饋點設計的關係，位置在寫 catch 區塊的人讀得到之處'
- '[x] ARCH-GPD-001 解決方案段補輸出端對應形態與其消費者'
- '[x] 三處引用皆寫觸發時機而非泛稱連結；每處已做兩端成對檢查'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T16:46:50'
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補 元件庫方法論掛載點架構層引用 + observability-rules 交叉引用 + ARCH-GPD-001 輸出端路由

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "三處反向路由已補齊，兩端成對檢查通過"
confidence: 1.0
acceptance_met: []
acceptance_unmet: []
artifacts: []
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-08
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

**變更摘要**（三處反向路由，均只指向頂層標題文字「Port 回饋契約」，不寫死 URL fragment）：

1. `.claude/methodologies/component-library-bidirectional-constraint-methodology.md`〈掛載點〉節（L96-97）：加一行架構層引用，說明本節三層是輸入端（driving port）特化，輸出端（driven port）對應形態見 `clean-architecture-implementation-methodology.md` 的〈Port 回饋契約〉。未新增子節標題、未搬移既有內容。Version 1.16.0 -> 1.17.0。與該檔 L68 既有正向連結（元件庫回饋契約掛載點）構成雙向配對。

2. `.claude/rules/core/observability-rules.md` 規則 1（catch 區塊必須有日誌）：補一句路由，說明日誌能記到什麼取決於呼叫發出／受理／結果三個回饋點是否已在設計期定義，回饋點不是日誌事後補得回來的；位置緊接規則 1 既有的「延伸自 quality-baseline」附註，即寫 catch 區塊的人會讀到之處。自動載入層形態：一句話 + 路由，論證主文留在方法論檔。相關文件列表同步補一行。Version 1.1.0 -> 1.2.0。

3. `.claude/error-patterns/architecture/ARCH-GPD-001-feedback-coupled-to-service-success-path.md` 解決方案段新增第 6 點：輸出端（driven port）有結構平行、消費者不同的對應形態（呼叫發出／外部系統受理／結果，消費者為呼叫端程式碼／日誌／監控追蹤，互不抵扣），指向〈Port 回饋契約〉。與該檔既有 L100「日誌不抵扣使用者回饋」同構表述呼應，並與 `clean-architecture-implementation-methodology.md` L100 既有反向連結（見 ARCH-GPD-001）構成雙向配對。Version 1.1.0 -> 1.2.0。

**兩端成對檢查結果**：三處新增皆為單向新增「舊資產 -> 新資產」的引用；三個對應的「新資產 -> 舊資產」正向連結，經核對已由 `0.1.0-W3-114` 落地於 `clean-architecture-implementation-methodology.md`（L68 指向元件庫掛載點、L100 指向 ARCH-GPD-001）。observability-rules.md 一側無正式反向連結（該檔屬自動載入層，架構層方法論用「回饋消費者與投影關係」節的日誌消費者描述已涵蓋此語意，未逐字互連——符合 PM 補充指示「該檔只加一至兩句路由」的形態約束）。三處引用皆未寫死 URL fragment，僅引用頂層標題文字「Port 回饋契約」，措辭與被引用端一致。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-107,0.1.0-W3-114 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-107 what: 設計 回饋契約推廣至六角架構全部 port：輸出端的呼叫回饋與其消費者（來源已完成，結論見 Solution）
- 0.1.0-W3-114 what: 新增 clean-architecture-implementation-methodology.md Port 回饋契約 section（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-107 why: 用戶裁示（2026-09-08）：回饋契約的三層結構要應用到 Clean Architecture 的六角結構，**不只輸入端**——服務本身往外呼叫（開檔案、開資料庫、打 API）也適用同一設計原則，也都應該被列入測試。

差別在**回饋的消費者**，不在結構：

| | 輸入端（driving port） | 輸出端（driven port） |
|---|---|---|
| 觸發者 | 使用者 | 應用層／領域層 |
| 回饋消費者 | 使用者（視覺／觸覺／聲音） | 呼叫端執行緒、log、APM（如 sentry） |
| 三層對應 | 按下確認／服務受理／event 效果 | 呼叫發出／外部系統受理／結果 |

用戶另指出一個時序約束：「**都是需要先設定好這個回饋點機制，才做得到**」——log 與 APM 追蹤不是事後可以補上的能力，它們消費的是設計期就要決定的回饋點。沒有預先定義回饋點，追蹤工具能拿到的只有例外堆疊，拿不到「呼叫發出了、外部系統收到了、結果是什麼」這三個時刻。

現況三項缺口：

1. **回饋契約目前只長在元件庫方法論裡**（`component-library-bidirectional-constraint-methodology.md`），那是 UI 層的載體。架構層原則長在 UI 方法論下，其他層的實作者不會讀到。
2. **`clean-architecture-implementation-methodology.md` 完全沒有回饋相關內容**（實查：「回饋」「adapter」「port」三詞零命中）。它有依賴方向、介面契約、業務邏輯位置的檢查清單，但沒有「這個 port 的呼叫有沒有可觀測的三個時刻」。
3. **`observability-rules` 是從日誌側寫的**（catch 要有日誌、統一日誌工具、全域錯誤處理、關鍵流程三階段），回答「發生了要記下來」；不回答「哪些時刻**必須**是可觀測的回饋點」。兩者互補但目前沒有接線——`ARCH-GPD-001` 已寫明日誌與使用者回饋是獨立義務，但沒說輸出端的「執行緒回饋」與「日誌」是什麼關係。（來源已完成，結論見 Solution）
- 0.1.0-W3-114 why: 用戶裁示（2026-09-08）：回饋契約的三層結構要應用到 Clean Architecture 的六角結構，**不只輸入端**——服務往外呼叫（開檔案、開資料庫、打 API）也適用同一原則，也都應列入測試。

`0.1.0-W3-107` 據此完成設計並裁定載體歸屬為**擴充本檔**，理由：非 UI 層的實作者在設計 port 時會讀這份方法論，回饋契約放這裡才會在正確時機被讀到。元件庫方法論不搬移任何內容（其三個子節標題已被 skill 與範本逐字引用為跨檔錨點，搬移會斷鏈），只在〈掛載點〉加一行架構層引用——那一行由 `0.1.0-W3-115` 處理。

現況：本檔對「回饋／adapter／port」三詞**零命中**（PM 實查）。它有依賴方向、介面契約、業務邏輯位置的檢查清單，但沒有「這個 port 的呼叫有沒有可觀測的三個時刻」。

`observability-rules` 是從日誌側寫的（catch 要有日誌、統一工具、全域攔截、關鍵流程三階段），回答「發生了要記下來」；不回答「哪些時刻**必須**是可觀測的回饋點」。兩者互補但目前沒有接線。

用戶另指出的時序約束是本節存在的核心理由：**log 與 APM 追蹤不是事後可補的能力**，它們（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Related Files
- .claude/methodologies/clean-architecture-implementation-methodology.md::read  # from 0.1.0-W3-107（來源已完成，結論見 Solution）
- .claude/methodologies/component-library-bidirectional-constraint-methodology.md::read  # from 0.1.0-W3-107（來源已完成，結論見 Solution）
- .claude/rules/core/observability-rules.md::read  # from 0.1.0-W3-107（來源已完成，結論見 Solution）
- .claude/error-patterns/architecture::read  # from 0.1.0-W3-107（來源已完成，結論見 Solution）
- lib/services::read  # from 0.1.0-W3-107（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 輸出端三層各有定義與判準，並回答輸入端不變式在輸出端的對應形式  # from 0.1.0-W3-107（來源已完成，結論見 Solution）
- [ref] [x] 回饋消費者（呼叫端執行緒／log／APM）各自的介面已釐清，並回答日誌與呼叫端回饋在輸出端是否同構於「不互相抵扣」  # from 0.1.0-W3-107（來源已完成，結論見 Solution）
- [ref] [x] 載體歸屬三候選各有論證並擇一，判準為「非 UI 層實作者能否在正確時機讀到」  # from 0.1.0-W3-107（來源已完成，結論見 Solution）
- [ref] [x] 輸出端三層的測試義務已定，並回答第一層是否如輸入端一樣不需替身  # from 0.1.0-W3-107（來源已完成，結論見 Solution）
---

**PM 補充（0.1.0-W3-114 落地後，2026-09-08）**

**引用目標已存在**（已驗證：`git log --oneline -1 -- <path>` 得 commit `8db1f6d4`；`grep -n "^## Port 回饋契約"` 命中）。

`.claude/methodologies/clean-architecture-implementation-methodology.md` 新增 `## Port 回饋契約`（約 L66-118），下轄五個 `###` 子節：輸出端回饋層級與判準／不變式：INV-PORT-OBSERVE-001／回饋消費者與投影關係／時序約束／測試義務。既有〈介面契約〉檢查清單（約 L52）已補一項觸發時機式勾選項指向新 section。

**三處引用一律只指向頂層標題文字「Port 回饋契約」**，不寫死 URL fragment——三個消費端的渲染器 anchor 規則不同，寫死 fragment 會在部分渲染環境失效。此為 114 執行者的交接建議，PM 採納為約束。

**並行狀態解除**：`0.1.0-W3-107` 已完成（commit `94866248`），裁定**不搬移任何內容**。`component-library-bidirectional-constraint-methodology.md` 的修改限制解除，但本票在該檔**只加一行架構層引用**——該檔三個 `#####` 子節標題已被 `component-contract-design` skill 與 `doc` 範本逐字引用為跨檔錨點，不新增子節標題、不搬移既有內容（acceptance 第 1 條）。

**逐檔的載入層約束不同，形態不可一致處理**

| 目標檔 | 載入層 | 形態要求 |
|--------|--------|---------|
| `component-library-bidirectional-constraint-methodology.md` | 按需 | 三明示可完整展開 |
| `.claude/rules/core/observability-rules.md` | **自動載入** | 禁令速查 + 路由；Why/Consequence 壓一行或路由至按需層，禁止整段展開 |
| `ARCH-GPD-001-*.md` | 按需 | 三明示可完整展開 |

`observability-rules.md` 每回合注入 PM 與所有代理人的 context，session 啟動時的 auto-load 預算已報 ~55.9k／45k tokens 超標。該檔的補充要短：acceptance 第 2 條要求「位置在寫 catch 區塊的人讀得到之處」——現有規則 1 即 catch 區塊條款，補充掛在那附近，一至兩句加路由即可，論證主文留在 `clean-architecture-implementation-methodology.md`。

**acceptance 第 4 條的「兩端成對檢查」**指：加了 A -> B 的引用後，回頭確認 B 端是否也需要指回 A。本票的存在理由正是 `DOC-GPD-002` 記的單向累積形態（新資產指向既有資產、既有資產不指回），若本票自己又製造一組單向引用即為同型復發。

**動手前先讀目標檔當前內容**：三個 session 正在同一 repo 作業，不要依賴任何記載的行號直接定位。
