---
id: 0.1.0-W3-115
title: 補 元件庫方法論掛載點架構層引用 + observability-rules 交叉引用 + ARCH-GPD-001 輸出端路由
type: DOC
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-114
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-107
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 補 元件庫方法論掛載點架構層引用 + observability-rules 交叉引用 + ARCH-GPD-001 輸出端路由
when: 0.1.0-W3-114 落地後（引用目標須先存在）
where:
  layer: Framework Docs
  files:
  - .claude/methodologies/component-library-bidirectional-constraint-methodology.md
  - .claude/rules/core/observability-rules.md
  - .claude/error-patterns/architecture/ARCH-GPD-001-feedback-coupled-to-service-success-path.md
why: '`0.1.0-W3-114` 把 Port 回饋契約落在 `clean-architecture-implementation-methodology.md`。該落點是 `0.1.0-W3-107` 裁定的——非 UI 層實作者設計 port 時會讀那份方法論，回饋契約放那裡才會在正確時機被讀到。


  但**新資產指向既有資產、既有資產不指回**正是 `DOC-GPD-002` 記錄的單向累積形態：三個既有資產各自缺一條指向新 section 的路由，而它們的讀者恰好是最需要知道這件事的人。


  三處各自的缺口不同：


  1. **元件庫方法論**〈掛載點〉：它定義的三層是 UI（driving port）特化。讀者讀完不會知道同一結構在輸出端也成立，也不會知道輸出端的條文在別的檔案。本票只加一行架構層引用——**不搬移任何內容**（該檔三個 `#####` 子節標題已被 `component-contract-design` skill 與 `doc` 範本逐字引用為跨檔錨點，搬移會斷鏈）。

  2. **`observability-rules`**：它從日誌側寫（catch 要有日誌、統一工具、全域攔截、關鍵流程三階段），回答「發生了要記下來」，不回答「哪些時刻**必須**是可觀測的回饋點」。兩者互補卻無接線——讀規則的人不會知道回饋點該在設計期決定，而那正是它所要求的日誌能否拿到有用資訊的前提。

  3. **`ARCH-GPD-001`**：解決方案段已由 `0.1.0-W3-102` 補為三層，但那三層仍是輸入端敘述；輸出端的對應形態（消費者為呼叫端執行緒／log／APM）未涵蓋。'
how:
  task_type: Implementation
  strategy: '先讀 `0.1.0-W3-114` 的 Completion Info 取得新 section 的**確切標題字串**，再開該檔核對存在（PM 轉述以檔案為準）。逐處補反向路由：


    1. **元件庫方法論**〈掛載點〉子節內加一行：本節定義的三層是輸入端（driving port）特化，輸出端（driven port）的對應形態見架構層方法論的該 section。**不新增子節標題、不搬移既有內容**。

    2. **`observability-rules`**：補一處說明日誌義務與回饋點設計的關係——回饋點是設計期產物，日誌只是其消費者之一；未先定義回饋點時，日誌能記到的只有例外堆疊。位置選在能被「正在寫 catch 區塊的人」讀到之處，不是附在檔尾相關文件列。

    3. **`ARCH-GPD-001`**：解決方案段補輸出端的對應形態與其消費者，並指向架構層 section。


    **寫觸發時機而非泛稱連結**：「相關：X」不會被讀到，「做完 A 之後由 X 承接」才會在正確時機被看見（`DOC-GPD-002` 預防措施）。


    **成對檢查**：每一處新增的跨檔引用，兩端同時開啟確認措辭互相對應——本票是為了修單向接線而存在，修法本身只補一端會直接重演 `DOC-GPD-002` 的變體。


    **不寫數字**：不以處數或層數命名或作跨檔錨點。'
acceptance:
- '[ ] 元件庫方法論〈掛載點〉加一行架構層引用，未新增子節標題、未搬移既有內容'
- '[ ] observability-rules 補日誌義務與回饋點設計的關係，位置在寫 catch 區塊的人讀得到之處'
- '[ ] ARCH-GPD-001 解決方案段補輸出端對應形態與其消費者'
- '[ ] 三處引用皆寫觸發時機而非泛稱連結；每處已做兩端成對檢查'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補 元件庫方法論掛載點架構層引用 + observability-rules 交叉引用 + ARCH-GPD-001 輸出端路由

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-107 | chars: 1392 -->

### Task Reference
- 0.1.0-W3-107 what: 設計 回饋契約推廣至六角架構全部 port：輸出端的呼叫回饋與其消費者

### Rationale Chain
- 0.1.0-W3-107 why: 用戶裁示（2026-09-08）：回饋契約的三層結構要應用到 Clean Architecture 的六角結構，**不只輸入端**——服務本身往外呼叫（開檔案、開資料庫、打 API）也適用同一設計原則，也都應該被列入測試。

差別在**回饋的消費者**，不在結構：

| | 輸入端（driving port） | 輸出端（driven port） |
|---|---|---|
| 觸發者 | 使用者 | 應用層／領域層 |
| 回饋消費者 | 使用者（視覺／觸覺／聲音） | 呼叫端執行緒、log、APM（如 sentry） |
| 三層對應 | 按下確認／服務受理／event 效果 | 呼叫發出／外部系統受理／結果 |

用戶另指出一個時序約束：「**都是需要先設定好這個回饋點機制，才做得到**」——log 與 APM 追蹤不是事後可以補上的能力，它們消費的是設計期就要決定的回饋點。沒有預先定義回饋點，追蹤工具能拿到的只有例外堆疊，拿不到「呼叫發出了、外部系統收到了、結果是什麼」這三個時刻。

現況三項缺口：

1. **回饋契約目前只長在元件庫方法論裡**（`component-library-bidirectional-constraint-methodology.md`），那是 UI 層的載體。架構層原則長在 UI 方法論下，其他層的實作者不會讀到。
2. **`clean-architecture-implementation-methodology.md` 完全沒有回饋相關內容**（實查：「回饋」「adapter」「port」三詞零命中）。它有依賴方向、介面契約、業務邏輯位置的檢查清單，但沒有「這個 port 的呼叫有沒有可觀測的三個時刻」。
3. **`observability-rules` 是從日誌側寫的**（catch 要有日誌、統一日誌工具、全域錯誤處理、關鍵流程三階段），回答「發生了要記下來」；不回答「哪些時刻**必須**是可觀測的回饋點」。兩者互補但目前沒有接線——`ARCH-GPD-001` 已寫明日誌與使用者回饋是獨立義務，但沒說輸出端的「執行緒回饋」與「日誌」是什麼關係。

### Related Files
- .claude/methodologies/clean-architecture-implementation-methodology.md::read  # from 0.1.0-W3-107
- .claude/methodologies/component-library-bidirectional-constraint-methodology.md::read  # from 0.1.0-W3-107
- .claude/rules/core/observability-rules.md::read  # from 0.1.0-W3-107
- .claude/error-patterns/architecture::read  # from 0.1.0-W3-107
- lib/services::read  # from 0.1.0-W3-107

### Source Acceptance (reference)
- [ref] [ ] 輸出端三層各有定義與判準，並回答輸入端不變式在輸出端的對應形式  # from 0.1.0-W3-107
- [ref] [ ] 回饋消費者（呼叫端執行緒／log／APM）各自的介面已釐清，並回答日誌與呼叫端回饋在輸出端是否同構於「不互相抵扣」  # from 0.1.0-W3-107
- [ref] [ ] 載體歸屬三候選各有論證並擇一，判準為「非 UI 層實作者能否在正確時機讀到」  # from 0.1.0-W3-107
- [ref] [ ] 輸出端三層的測試義務已定，並回答第一層是否如輸入端一樣不需替身  # from 0.1.0-W3-107
