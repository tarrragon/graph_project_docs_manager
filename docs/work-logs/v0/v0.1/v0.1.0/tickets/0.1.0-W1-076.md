---
id: 0.1.0-W1-076
title: 補齊 AppTableRow.ticket 的 blockedBy 欄
type: IMP
status: pending
version: 0.1.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W2-003
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W2-003 驗收查證時發現元件契約落後 SPEC-001 v1.9 與 SPEC-004 4.35
  final_decision: 獨立成票補元件欄位，並在 why 記錄 how 句型凍結導致的漏檢機制
  rationale: 元件欄序是跨模式共用契約（列表與主題模式共用同一欄序），在畫面層補會使兩模式各補一次而可能不一致
who:
  current: parsley-flutter-developer
  history: {}
what: 為 AppTableRow.ticket 變體增補 blockedBy 欄（位於優先之後、損壞標記之前），並由 Ticket 清單渲染既有的 TicketFixtureItem.blockedBy 資料
when: W2-003 驗收時發現後
where:
  layer: Presentation
  files:
  - lib/components/app_table_row.dart
  - lib/screens/ticket_list/ticket_list_screen.dart
why: SPEC-001 §4〈正常·列表〉明訂票列欄位依序為 ID／標題／狀態／優先／blockedBy／損壞標記（blockedBy 值為被阻擋的 ticket ID 清單，無則「—」），該欄為 SPEC-001 v1.9 依 UC-05 步驟 4「檢視各票的 status 與 blockedBy」新增；SPEC-004 4.35 的 ticket 變體亦列出 blockedBy，§3.6 §4 列明寫「票列含 blockedBy 欄」、表頭該欄為 static 不可排序。實作的 _TableRowVariant.ticket 只處理四格（ID／標題／狀態／優先），無 blockedBy。W2-003 已把資料保留在 TicketFixtureItem.blockedBy 但不渲染，接點已備妥。附帶暴露：W2-003 票面 how 欄的組合句型凍結於 2026-09-03，未含 blockedBy，2026-09-15 的票面改定只更新狀態清單未重校 how 句型——代理人照 how 句型實作即漏該欄且不會察覺
how:
  task_type: Implementation
  strategy: 在 _TableRowVariant.ticket 的欄位組成與 _semanticLabel 串接中插入 blockedBy 格（優先之後、損壞標記之前），值為被阻擋 ticket ID 清單、空值渲染破折號；表頭對應欄設為 static 不可排序（SPEC-001 §4 排序列舉不含 blockedBy）。Ticket 清單頁改為傳入既有的 TicketFixtureItem.blockedBy。既有票列測試同步補該欄斷言
acceptance:
- '[ ] AppTableRow.ticket 含 blockedBy 欄，位置在優先之後、損壞標記之前，空值渲染破折號'
- '[ ] 表頭該欄為 static 不可排序，朗讀標籤含該欄值'
- '[ ] Ticket 清單列表與主題兩模式共用同一欄序且皆渲染該欄，既有測試補對應斷言後全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-21'
updated: '2026-09-21'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

為 AppTableRow.ticket 變體增補 blockedBy 欄（位於優先之後、損壞標記之前），並由 Ticket 清單渲染既有的 TicketFixtureItem.blockedBy 資料

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W2-003 | chars: 586 -->

### Task Reference
- 0.1.0-W2-003 what: 實作 Ticket 清單（SPEC-001 §4）全部狀態列：專案未就緒／未載入（載入提示元件）／載入中／正常·列表／正常·主題／無 ticket／含損壞（疊加態）；以 W1-023 ticket fixture 驅動，不串真實資料（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W2-003 why: 本畫面狀態數第二多，且含惰性載入的四個狀態（未載入／載入中／正常／含損壞）。FR-02 要求取消操作恆可用且可離開，該行為在 0.1 就要能走通（來源已完成，結論見 Solution）

### Related Files
- lib/screens/ticket_list/  # from 0.1.0-W2-003  [路徑已不存在]（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 頁面檔的元件 import 只來自 lib/components/components.dart 統一匯出入口；頁面層不以原生佈局原語直接包兩個以上元件庫元件（grep 驗收）  # from 0.1.0-W2-003（來源已完成，結論見 Solution）
- [ref] [x] 每個狀態的退出路徑依 SPEC-003 §3.4 可實際操作走通（含列表／主題切換與排序點擊）  # from 0.1.0-W2-003（來源已完成，結論見 Solution）
- [ref] [x] widget test 逐狀態渲染一次並斷言 §3.6 指定的元件型別存在，含損壞態斷言 IssueMarker.damagedDetail 出現於工具列與損壞列（不重驗溢位）  # from 0.1.0-W2-003（來源已完成，結論見 Solution）
- [ref] [x] SPEC-001 §4 全部狀態列皆可渲染，每列以 SPEC-004 §3.6 對應行的契約內元件組成；狀態由 W1-023 ticket fixture 驅動（含真實損壞樣本）；未載入由載入提示元件 LoadPrompt 承載（SPEC-003 §2.7／§2.11）  # from 0.1.0-W2-003（來源已完成，結論見 Solution）

### UC Context
#### UC-05: 找出被阻擋的工作
Spec 位置: docs/app-use-cases.md:82
主要流程:
  enter-ticket-list: 進入 ticket 清單
  trigger-load: 觸發載入
  switch-to-topic: 切換至主題模式
  locate-blocked: 定位阻擋

