---
id: 0.1.0-W3-158
title: 擴充 bash-edit-guard 對非 sed 類 Bash 改檔的偵測缺口
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: 0.1.0-W3-157
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-157 代理人以 python3 heredoc 改檔，違反 tool-selection 規則 1 且既有守衛未攔截
  final_decision: IMP 擴充 bash-edit-guard 模式 A 的偵測樣式並評估是否升級為阻擋
  rationale: 規則已自動載入仍被違反兩次，顯示自律層不足以對抗每回合注入的 harness 指令；守衛存在但偵測範圍未涵蓋實際使用的改檔手段
who:
  current: thyme-python-developer
  history: {}
what: 擴充 bash-edit-guard 對非 sed 類 Bash 改檔的偵測缺口
when: 0.1.0-W3-157 回報流程偏差之後
where:
  layer: Infrastructure
  files:
  - .claude/hooks/bash-edit-guard-hook.py
  - .claude/hooks/tests/test_bash_edit_guard_hook.py
why: '`.claude/rules/core/tool-selection.md` 規則 1 邊界明訂本規則優先於 harness 層「優先用 Bash 改檔」類指令，規則 2 第三列另訂 Edit 被非專案來源拒絕時應停手而非改用 Bash 內嵌腳本。該規則位於自動載入層，每回合注入 PM 與所有代理人。


  本 session 兩個代理人各自遇到同一個 harness 指令：`0.1.0-W3-148` 的執行者遵守規則並記入 NeedsContext，`0.1.0-W3-157` 的執行者中途以 `python3` heredoc 改寫檔案內容後自陳並改回。同一規則、同一 session、兩次遭遇、一次違反。


  強制層存在但有缺口。PM 實查 `.claude/hooks/bash-edit-guard-hook.py`：模式 A（原地編輯）只偵測 `sed -i`／`sed --in-place`／`perl -pi`／`perl -i.bak` 四種樣式，**不涵蓋 `python3 -c`、`cat > file <<EOF` heredoc 寫檔、`> file` 重導向**——代理人使用的正是其中之一。且模式 A 的處置為「出警告後放行」，即使命中也不阻擋（模式 B 裸 cd 才阻擋）。


  自律層規則已完備而仍被違反，顯示問題不在規則缺席，而在自動載入的規則與每回合注入的 harness 指令競爭時不保證勝出。此類情形的正解是強制層，非再寫一份文件。'
how:
  task_type: Implementation
  strategy: '先實查現行偵測樣式與處置分級，不採信本票敘述——讀 `_detect_bash_edit_patterns` 與模式 A／B 的 exit code 差異，確認警告與阻擋各自的觸發路徑。


    擴充模式 A 的樣式集，至少涵蓋三類本票已知的缺口：直譯器內嵌腳本寫檔（`python3 -c`／`python -c`／`node -e` 等，判定依據為命令含寫檔語意而非僅執行）、heredoc 寫入既有檔（`cat > <path> <<`、`tee <path> <<`）、輸出重導向覆寫既有檔（`> <path>`，須與建立新檔區分）。


    **須先判定的設計問題**：這三類與 `sed -i` 的可逆性不同，且正當用途遠多於 `sed -i`（產生暫存檔、寫 scratchpad、管線輸出）。因此偵測條件應綁**目標路徑**而非僅綁命令形式——落在專案工作區內的既有檔才觸發，scratchpad 與 `/tmp` 不觸發。此判定寫入 Solution。


    **處置分級一併評估**：模式 A 現為警告放行。本票已實證警告不足以阻止違反（代理人自陳時才被發現）。是否升級為阻擋、或對專案內既有檔阻擋而對其他路徑警告，需給出理由並記錄——升級為阻擋會影響所有既有的正當 Bash 寫檔用途，不可逕自改。


    測試須涵蓋：三類新樣式各自的命中、scratchpad 與 `/tmp` 路徑的不觸發、建立新檔與覆寫既有檔的區分、既有 `sed -i`／`perl -pi` 樣式不回歸。'
acceptance:
- '[ ] 模式 A 已涵蓋直譯器內嵌腳本寫檔、heredoc 寫入既有檔、重導向覆寫既有檔三類樣式'
- '[ ] 偵測條件綁目標路徑：專案工作區內既有檔觸發，scratchpad 與 /tmp 不觸發'
- '[ ] 處置分級（警告或阻擋）已評估並在 Solution 記錄理由，未逕自升級'
- '[ ] 測試涵蓋三類新樣式命中、兩類路徑不觸發、新建與覆寫的區分、既有樣式不回歸'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

擴充 bash-edit-guard 對非 sed 類 Bash 改檔的偵測缺口

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
