---
id: 0.1.0-W3-133
title: 裁定 是否為 driven port 建立 adapter 層與 ports 目錄約定
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-121
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: Phase 4a 兩個視角對「是否該補 adapter 層」給出方向相反的意見
  final_decision: ANA 先裁定架構歸屬，再決定是否開實作票
  rationale: 此決定影響另外三個 driven port 的實作形態，且兩份審查意見衝突，不宜由實作票臨場決定
who:
  current: saffron-system-analyst
  history: {}
what: 裁定 是否為 driven port 建立 adapter 層與 ports 目錄約定
when: 0.1.0-W3-121 Phase 4a 完成後（2026-09-08）
where:
  layer: Domain
  files:
  - docs/tech-decisions.md::read
  - lib/workspace/workspace_repository.dart::read
why: 'Phase 4a 兩個視角對同一問題給出方向相反的意見，需要裁定。


  **linux（Acceptable 偏低）主張補 adapter 層**：`WorkspaceRepository` 同時是 repository 又是三個 adapter。方法論〈Port 回饋契約〉自己寫了承擔者分工——呼叫發出歸呼叫端，受理與結果歸 **adapter**——但本專案沒有 adapter 層，repository 直接寫 `SharedPreferences.getInstance()`、`Directory(path).list()`。於是要讓 adapter 內部的「受理」時刻可觀測，唯一手段是往 repository 身上鑿洞：`WorkspacePreferencesHandle` 的存在理由只是 `getInstance()` 是兩段式 API，那是 SharedPreferences 的實作細節，卻被提升成 repository 的公開介面。它主張三個真 adapter 類別（各自獨立檔案）可讓接縫從 5 降到 3，可觀測性一條不減。


  **Coupling 視角的相反證據**：實查 `WorkspacePreferencesPort`／`Handle` 與 `DirectoryPathPicker` 兩處**未發現型別洩漏**——`SharedPreferences` 與 `file_selector` 的型別沒有出現在任何介面簽章或呼叫端的具體型別比對中，呼叫端一律 generic `catch (e)`。以隔離效果衡量，這兩個接縫是乾淨的。


  **Complexity 視角的第三種意見**：量測到單檔 19 個頂層宣告、267 行，但明確標註信心 55、未達回報門檻、**不建議據此拆檔**——理由是 `cognitive-load.md` 沒有「單檔型別數上限」條文，把任務拆分閾值（需追蹤概念數 > 7）類比套用到檔案內型別數缺乏明文依據；且五個 port 加四個 default adapter 是同一個 ports-adapters 圖式的必然展開，全檔僅一個 domain，未命中「單一檔案承載 domain 數 > 2」。


  三者不是簡單的對錯：linux 談的是結構歸屬（實作細節該不該出現在 repository 介面上），Coupling 談的是洩漏事實（型別有沒有跑出來），Complexity 談的是有無明文閾值依據。**本票要裁定的是該用哪個標準。**


  決定的外溢範圍：`0.1.0-W1-068`（ExternalOpener）、`0.1.0-W3-112`、`0.1.0-W1-014` 三個 driven port 都會照本形態實作。linux 的推估是複製後約 4 個 port × 8 個測試替身 ≈ 30 個 bespoke 類別，每個 port 各自發明接縫形態。'
how:
  task_type: Analysis
  strategy: '本票不改任何程式碼，產出是裁定與理由，落地另建票。


    須回答三問，各附論證：


    1. **方法論的「adapter」是否要求獨立類別**？〈Port 回饋契約〉的承擔者欄寫「adapter 實作」，但未定義 adapter 必須是獨立於 repository 的類別。若方法論容許 repository 內嵌 default adapter，linux 的批評就從「違反方法論」降為「品味偏好」，兩者處置不同。此問須引方法論原文判定，不得憑印象。


    2. **`WorkspacePreferencesHandle` 兩層形態的歸屬**：它是 SharedPreferences 兩段式 API 的實作細節被提升為公開介面（linux），還是受理時刻的必要載體（Phase 2 §2.0 形態要求 1，且 G4-1／G4-2 兩個測試在支撐它）？Redundancy 視角已查證「沒有任何一個介面可以合併而不損失一條現存的綠燈測試」，此事實須納入權衡。


    3. **是否建立 `lib/<domain>/ports/` + `adapters/` 目錄約定**：若建立，須給出約定內容與遷移成本；若不建立，須說明另外三個 port 如何避免各自發明接縫形態。


    三個候選結論擇一：(A) 維持現狀，補一份接縫形態約定文件即可；(B) 建立 adapter 層與目錄約定，本檔重構後作為範本；(C) 部分——指出哪些 port 值得獨立 adapter、哪些不值得，並給判準。


    結論寫入 `docs/tech-decisions.md` 的裁決由本票決定但不執行；落地建票。'
acceptance:
- '[ ] 三問各有論證且引方法論原文，不憑印象'
- '[ ] 三個候選結論（維持現狀／建 adapter 層／部分）各有論證並擇一'
- '[ ] 裁定對 W1-068、W3-112、W1-014 三票的影響已明寫；若結論為 B 或 C，已登記 spawn request 交 PM'
- '[ ] workspace_repository.dart 已達 309 行（超過 cognitive-load.md 的 300 行門檻，0.1.0-W3-121 Phase 4b 記錄）——若本票結論為「維持現狀」，須一併裁定該檔行數的處置或明寫豁免理由，不得因結論 A 而使此項無承接者'
tdd_phase: phase0
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-09T01:40:14'
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

裁定 是否為 driven port 建立 adapter 層與 ports 目錄約定

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

Phase 4a 審查產生三個方向相反的意見：linux 主張建立 adapter 層（結構歸屬），Coupling 實查未發現型別洩漏（隔離效果良好），Complexity 量測到接近閾值但不建議拆檔（無明文依據）。根因不是「某方錯了」，而是三個視角各用不同標準——結構歸屬、洩漏事實、明文閾值——且方法論原文是否支持 linux 的立場尚未查證。

### 影響範圍

- 直接：`lib/workspace/workspace_repository.dart`（309 行，3 個 driven port，5 個 port 介面，4 個 default adapter）
- 間接：`0.1.0-W1-068`（ExternalOpener）、`0.1.0-W3-112`、`0.1.0-W1-014` 三個 driven port 的實作形態將依本裁定為範本
- 阻塞：`0.1.0-W3-139`（輸出端回饋契約 spec 落點）blockedBy 本票

已驗證事實：方法論原文已讀、程式碼現況已讀、測試檔 G4 群組已讀。仍未驗證：另外三個 driven port 是否都需要受理時刻（取決於各自外部系統 API 形態）。

### 相關 Error Pattern

無直接相關的已知錯誤模式。本裁定為架構歸屬判斷，非流程或實作錯誤。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-121 | chars: 1700 -->

### Task Reference
- 0.1.0-W3-121 what: 補齊 WorkspaceRepository 三個 driven port 呼叫的回饋契約（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-121 why: `lib/workspace/workspace_repository.dart` 有四個 driven port 呼叫點，依 `clean-architecture-implementation-methodology.md`〈Port 回饋契約〉的 INV-PORT-OBSERVE-001 全數不合規（PM 逐行實查）：

1. `chooseFolder()` 的 `getDirectoryPath()`（file_selector 平台通道）：無 try/catch、無日誌。MissingPluginException 或使用者環境異常時直接往上拋，呼叫端拿到的是未分類例外。
2. `chooseFolder()` 的 `SharedPreferences.getInstance()` / `setString(_pathKey, path)`：**回傳值完全未檢查**。`setString` 回傳 `Future<bool>`，寫入失敗時函式照樣往下走並回傳 `_inspect(path)`——使用者看到資料夾已選定，但下次 `restore()` 讀不到值會回 `WorkspaceUnset`。這是方法論「典型違反」的加重變體：連日誌都沒有，呼叫端與日誌兩個獨立義務同時缺席。
3. `restore()` 的 `getInstance()` / `getString()`：無失敗路徑。Phase 1 已修正本條的初始描述——`getString()` 是純記憶體查表無 I/O 失敗路徑，真正的讀取失敗集中在會拋例外的 `getInstance()`，兩者不可混為一談，詳見 Solution 裁決 B。
4. `_inspect()` 的 `Directory.exists()` 與 `dir.list().first`（dart:io 檔案系統）：現況只有 `FileSystemException` 分支有日誌，屬結果時刻的失敗分支；`exists()` 完全無記錄，兩處皆缺「呼叫發出」事件。本條由 Phase 1 的 SR-1 補入——PM 建票時盤點遺漏，非後續擴張。

四處皆缺「呼叫發出」事件，判準問句「把外部系統整個拔掉，呼叫端能否得知呼叫已發出但無回應」目前答案為否。

測試覆蓋現況：`test/` 下無 workspace_repository 的單元測試檔，僅 `integration_test/app_test.dart` 間接引用。

範圍界線：本票只處理 App 端邏輯軌的回饋契約合規。平台端事實軌（實機驗證 file_selector 與真實檔案系統行為）由 `0.1.0-W3-088` 評估，兩者不重疊。`gap_report_screen._openItem` 的同類缺口由 `0.1.0-W3-112` 承接，不在本票範圍。（來源已完成，結論見 Solution）

### Related Files
- lib/workspace/workspace_repository.dart  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
- lib/main.dart  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
- lib/l10n/app_zh.arb  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
- lib/l10n/app_en.arb  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
- lib/l10n/app_localizations.dart  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [x] 三個 driven port 呼叫各有「呼叫發出」日誌事件，含提早返回路徑  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
- [ref] [x] setString 回傳值已檢查；chooseFolder 的回傳型別能讓呼叫端區分「已持久化」與「未能記住」，型別決定與理由寫入 Solution  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
- [ref] [x] 每個失敗分支同時具備日誌與呼叫端可消費的回傳或例外（不互相抵扣）  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
- [ref] [x] 建立 workspace_repository_test.dart：呼叫發出用 spy、結果用替身覆蓋成功/寫入失敗/例外；受理時刻不適用的理由已在 Solution 明寫  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
- [ref] [x] _inspect() 的 Directory.exists() 與 dir.list().first 各有「呼叫發出」事件；exists() 的失敗結局有結果時刻記錄  # from 0.1.0-W3-121（來源已完成，結論見 Solution）
