---
id: 0.1.0-W3-226
title: 裁定 跨 consumer 專案關閉本地 ticket 的合法表達形式
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: session-start 稽核噪音追查至 closed_by 值域，讀 6 張票 close_reason_note 後確認語意為跨 consumer 移交而非取消
  final_decision: '拆為三個可分離缺陷；值域議題 observe 至既有 #60，reason code 枚舉議題 init 新 issue，存量處置另票承接'
  rationale: 'dedup 顯示 closed_by 值域已有 #60 承接（另一 consumer 的 framework issue ref 情境），但 close_reason 枚舉三個關鍵字皆零命中；兩者欄位不同、機制不同、修法可各自獨立出貨，故切分而非併入'
who:
  current: 主線程
  history: {}
what: 裁定 跨 consumer 專案關閉本地 ticket 的合法表達形式
when: session-start 稽核連續多日報 6 張票 closed_by 非合法 Ticket ID，用戶指示處理跨專案關閉的設計
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/commands/lifecycle.py
  - .claude/lib/ticket_id_pattern.py
  - .claude/hooks/version-tracking-consistency-guard-hook.py
why: '本專案 6 張 closed 票的 closed_by 為 `flutter_balance:W4-007` 形態（5 張裸形、1 張含版本號），每次 session-start 皆被 version-tracking-consistency-guard 判為非合法 Ticket ID，產出固定噪音。


  實際語意（讀 6 張票的 close_reason_note 確認，非推論）：全部為「用戶裁示移交 flutter_balance 執行。標的 .claude/... 為共用框架資產，兩專案各自修改會造成 sync 衝突，故集中單一 session 處理」。即移交另一個 consumer 執行，不是取消。


  三個可分離的缺陷：


  D1 reason code 枚舉缺「移交」項：6 張全用 cancelled_by_user 記錄移交。需求沒有被取消，只是換執行者。日後盤點「哪些需求被放棄」會把這 6 項仍在進行的工作算進去。這是 PC-MON-002 行為模式的再現——但這次穿透的不是自由文字欄，是 reason code 枚舉：PC-MON-002 的修復把自由文字堵在 resolved_by，卻沒發現同一個「欄位語意不適用當下情境就選語意近似值」的逼迫會轉移到 reason code 上。


  D2 建立端對 ALLOW_NONE 分支零驗證：lifecycle.py 的 elif reason_code in self._CLOSE_REASONS_ALLOW_NONE 分支只檢查 reason_note 非空，完全不驗 resolved_by 的格式。故 cancelled_by_user / requirement_vanished 下任意字串今天仍寫得進去（非存量問題，是現行可重現的洞）；而 guard 不分 reason code 一律要求 FULL_ANCHORED 或 none。兩端值域不一致且無阻擋時機。


  D3 跨 consumer 票號不可解析：flutter_balance:W4-007 對本 repo 讀者無法導航，且跨 repo 無法驗存在性。這與 framework issue ref（owner/repo#N，可解析可驗證）性質不同，兩者不該以同一條放寬規則一併納入值域。


  框架問題兩條 AND 判準（逐條套用，非憑印象）：

  1. 抽象可攜——把本專案與 flutter_balance 換成任兩個 consumer，上述三項論述完全成立。共用框架資產的移交是本框架 multi-consumer 架構的結構性情境，非本專案特例。成立。

  2. 根源在 .claude/ 通用資產——D1/D2 在 .claude/skills/ticket/ticket_system/commands/lifecycle.py，D3 在 .claude/lib/ticket_id_pattern.py 與 .claude/hooks/version-tracking-consistency-guard-hook.py。三者皆隨 sync 分發。成立。


  兩條皆成立，屬框架問題。'
how:
  task_type: Analysis
  strategy: 'dedup 已執行（結果記於 Solution）。關係判定與後續動作：


    - closed_by 值域議題與 tarrragon/claude#60 重複（#60 標的為 framework issue ref 進不了值域，另一 consumer 有 20 張票命中）。動作為 observe，不另建 issue。本票對 #60 的新增資訊是 D2 與 D3：#60 把建立端診斷為「SKILL.md 未規範填法」，但真正的機械洞在 lifecycle.py 的 ALLOW_NONE 分支；且 #60 的「放寬值域」若寫成單一字元類，會把不可驗證的跨 consumer 票號與可驗證的 framework issue ref 一併放進來。

    - close_reason 枚舉議題經 dedup（關鍵字 close_reason / cancelled_by_user / requirement_vanished）零命中，屬新議題，與 #60 為切分關係（不同欄位、不同機制、修法可各自獨立出貨）。動作為 init 新 issue 並互相引用。


    本票不決定 6 張存量票的本地處置——合法值取決於上游裁定，先定值再改值，順序反了會改兩次。存量處置由本票的後續票承接。'
acceptance:
- '[ ] dedup 已執行且關係判定（重複／切分／引用）逐項記錄於 Solution'
- '[ ] D1/D2/D3 三項各自標明證據位置（檔案:行號 或 票面欄位），非推論'
- '[ ] 已 observe 至 tarrragon/claude#60，內容含 D2 建立端診斷更正與 D3 兩類值可解析性差異'
- '[ ] close_reason 枚舉缺口已 init 為獨立 issue，並與 #60 互相引用'
- '[ ] 6 張存量票的本地處置已建後續票承接，本票不自行決定'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

裁定 跨 consumer 專案關閉本地 ticket 的合法表達形式

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending
