---
id: 0.1.0-W3-325
title: 判定 dedup 靜默吞多餘旗標使錯誤與正常不可區分的影響範圍
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-318
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-318 實測發現；PM 獨立複核 rc=0 與旗標歸屬
  final_decision: 建票記錄現象與證據，修法形狀不在本票決定
  rationale: 依 escalation-flow 修法時點分離；三問中「消費端已觀測」與「反例已試」皆未答
who:
  current: framework-issue-curator
  history: {}
what: 判定 dedup 靜默吞多餘旗標使錯誤與正常不可區分的影響範圍
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/section_comment.py
why: '**現象**：`section_comment.py` 的 `dedup` 子命令只有 `--keywords`（`nargs="+"`），多給一個不存在的旗標時**不報錯**——該旗標被當成第二個查重關鍵字，rc=0。


  **PM 獨立複核**：`python3 section_comment.py dedup --help` 的旗標僅 `--keywords`；`--all` 屬 `todo` 子命令；`dedup --keywords zzz --all` 實跑 **rc=0**。


  **後果是可觀測面上的不可區分**：查重跑完、退出碼 0、有輸出——**與一次正確的查重完全同形**。使用者以為查了 `--all` 的範圍，實際查的是 `zzz` 與 `all` 兩個關鍵字。`0.1.0-W3-307` 的構造三正是踩在這裡：它以「實跑 `dedup --help`，`--all` 存在」作為「反例已試」的依據，而被證偽的是「某處存在」不是承重前提「`dedup` 存在」。


  **形態歸屬**：`test-assertion-design-rules` 規則 E2（守衛的失效方向是放行，而放行與「輸入本來就沒問題」在輸出上同形）。該規則與本檔互指仍為 0／0。


  **為何 P1**：`dedup` 是 framework-issue 流程的第一個動作，且 CLAUDE.md 的免徵詢授權以「已跑 dedup」為生效條件之一——**一次被靜默縮小範圍的 dedup 會使該授權的前提失真而無人察覺**。'
how:
  task_type: Analysis
  strategy: '**本票不決定修法形狀**，產出為現象、證據、影響範圍、處置層級四項。


    1. **量影響面**：`section_comment.py` 全部子命令中，有哪些的 `nargs="+"` 參數會吞掉後續旗標。**不只 `dedup` 一處**——同一形態可能散落多處，只修一處等於留下其餘。

    2. **查歷史**：本專案與可查的觀測流中，是否有過因此而範圍失真的 dedup 結果。若無跡可循，據實記為未知，不推測。

    3. **判與 E2 的關係**：同源、實例、或僅形態相似。兩者互指為 0／0，是否構成缺口一併判定。

    4. **升級判定**：先跑 `dedup`（注意：**本票主題正是該命令的缺陷，執行時勿多給旗標**），兩條 AND 判準逐條套用並留痕。


    不改 `.claude/` 資產、不跑 `sync-push`。owner 現已可自行推導，勿手動傳 `--owner`。'
acceptance:
- '[ ] 全部子命令的 nargs 吞旗標面已掃過，非只 dedup 一處'
- '[ ] 歷史上是否有範圍失真的 dedup 結果已查，無跡者記為未知'
- '[ ] 與 test-assertion-design-rules 規則 E2 的關係已判定，互指為 0 是否構成缺口一併判'
- '[ ] dedup 已跑，兩條 AND 判準已逐條套用並留痕；Solution 未寫入修法形狀'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-12T11:26:16'
completed_at: null
created: '2026-09-12'
updated: '2026-09-12'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

判定 dedup 靜默吞多餘旗標使錯誤與正常不可區分的影響範圍

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

**機制不是 argparse 原生的 nargs="+" 貪婪收值**，而是本檔自建的兩道加固共同作用的結果。純 argparse 對未註冊的 `-` 開頭 token 會回 `unrecognized arguments` 並 exit 2（同檔其他子命令實測即如此）；靜默吸收需要以下兩者同時成立：

| 環節 | 位置 | 作用 |
|------|------|------|
| 前處理器 `_escape_dash_prefixed_keyword_values` | `section_comment.py`（`main()` 開頭、`parse_args` 之前） | 收值階段中凡 `-` 開頭且不在停止白名單內的 token，一律加不可見前綴，argparse 因此看不出它是旗標，收為關鍵字值 |
| gh 呼叫的 `--` 分隔符 | `search_issues_by_keyword` | 關鍵字排在全部旗標之後、以 `--` 分隔，使被吸收的 `--all` 成為合法 gh 搜尋詞而非 unknown flag，查詢不失敗，`skipped=0` |

兩道加固各自的目的正當（讓「commit -a」「--force」這類含 `-` 的**字面**查重關鍵字可用），合起來的副作用是把「未知旗標」從硬錯誤變成靜默吸收。

**停止白名單已實際漂移**（非假設）：`_KEYWORD_VALUE_STOP_FLAGS` 的註解自述「集合僅列本檔實際註冊的旗標」，但實測本檔註冊 21 個 option string，白名單只列 12 個，缺 7 個——`--all`／`--consumer`／`--issue`／`--json`／`--priority`／`--stage`／`--status`，**全數為 `todo` 子命令的旗標**（該子命令後加，白名單未同步）。`--all` 正落在此缺口內。白名單無反向多餘項（STOP 減 REGISTERED 為空）。

由此分出兩個性質不同的層次：

1. **實作偏離已明示的契約**：白名單應等於本檔註冊旗標集合（扣除兩個 multi-value 旗標本身），現缺 7 項。predicate 機械可判，補齊後 `dedup --keywords x --all` 會與 `--owner`／`--force` 一樣 exit 2。
2. **未註冊 token 的意圖歧義**：即使補齊白名單，任意拼錯（`--forc`／`--keywrods`）與真正的字面值仍不可區分。此為現行契約**明文允許且已被測試固定**的行為（見「相關 Error Pattern」），改動即改契約。

### 影響範圍

**已驗證的事實**

| 項目 | 實測結果 |
|------|---------|
| 本檔 nargs 為 `+`／`*` 的站點總數 | 2 處：`init --dedup-keywords`、`dedup --keywords`（其餘子命令皆無） |
| 其餘子命令對未知旗標的行為 | `add`／`update`／`transfer-owner`／`observe`／`show`／`check`／`todo` 一律 exit 2（逐一實測） |
| `.claude/` 內 nargs 為 `+`／`*` 的站點 | 23 處（以 ticket CLI 為主） |
| 靜默吸收面是否外溢至其他 CLI | 否。此型前處理器全 `.claude/` 僅本檔一處，且無任何 `parse_known_args`；控制實驗 `ticket track list --status pending --zzz` 與 `--forc` 皆 rc=2 |
| 兩處站點的嚴重度不對稱 | `dedup` 唯讀；`init --dedup-keywords` 同缺陷落在**會寫入 GitHub 的路徑**——查重範圍被靜默縮小，區段 comment 與 body 索引照常建立 |

**可觀測面的精確描述**（比「完全不可區分」弱一度，但實務上仍不可辨）

- 退出碼相同（rc=0）、無 stderr 警告。
- 報告**末行**「查詢失敗略過的 token 數：0」——該行的設計目的正是讓涵蓋缺口從報告本身可見（`tail -1` 即可），在本案卻主動認證「無缺口」。
- 唯一訊號是報告**首行**回顯的關鍵字集合（`['zzzzq', '--all']`），而首行正是 `| tail` 會切掉的位置（`bash-tool-usage-rules` 規則二「輸出過濾方向」）。
- 附帶放大：被吸收的 `--all` 實測命中 **8 則** issue，報告因此比正確執行**看起來更充實**，與「查重做得更廣」同形。

**仍未驗證的假設**：無。本節結論皆有對應實驗（見「重現實驗結果」）。

**歷史是否有範圍失真的 dedup 結果：未知（無跡可循）**

- 全專案 md 中 `dedup --keywords`／`--dedup-keywords` 後帶 `todo` 旗標者共 4 行，全數為本議題的**刻意實驗或其轉述**（`0.1.0-W3-318` 三行、本票 why 一行），無真實失真執行。
- 已記錄的 dedup 輸出中含 `-` 開頭關鍵字者僅上述實驗一筆；`worked-example.md` 兩筆為正常關鍵字。
- skill 文件的範例命令（`escalation-flow.md`）不帶多餘旗標，未把缺陷制度化。
- **偵測上限須明說**：真實失真執行只在「argv 或報告首行被逐字抄進記錄」時才留痕，兩者皆非常規。故「查無」不等於「未發生」，據實記為未知，不推測。

### 相關 Error Pattern

| 關係 | 對象 | 判定 |
|------|------|------|
| 同一般形態（新實例） | `PC-GPD-020` 明示的意圖與明示的失敗，在可觀測面與預設或成功不可區分 | 本案為其未登記的新實例。**一處差異須明說**：該 pattern 的識別特徵要求產物「逐字相同」，本案首行回顯有差異，屬該家族但弱一度——訊號存在卻不顯著且落在常被過濾的位置 |
| 同家族、非重複 | `PC-V1-001`（sync-push 未知參數當 commit 訊息）、`IMP-BAL-002`（sync-pull 未知參數推進 base SHA） | 同為未知參數 fail-open，但那兩者是「完全無參數驗證」，本案是「有 argparse 且被自家前處理器抹掉旗標性」，成因不同 |
| 形態相似 | `PC-BAL-033`（新註冊 hook 對既有 session 惰性） | 僅同屬「失效與正常同形」，機制無關 |

**與 `test-assertion-design-rules` 規則 E2 的關係判定：同源，非實例。**

- E2 治的是**守衛**的測試缺正向對照輸入；`dedup` 不是守衛，其失效方向不是「放行該攔的輸入」而是「把旗標當值」。
- 真正對得上的是**規則 E1**（明示旗標必須有對照測試）：既有測試 `test_cmd_dedup_cli_accepts_hyphen_prefixed_keyword_group_end_to_end` 以未註冊旗標 `--dry-run` 作關鍵字、斷言 rc=0 與末行 skipped 為 0——**現行吞旗標行為已被測試固定為正確**。全套測試中針對旗標的反向對照只有「必填旗標缺少時 exit 2」一例，無任何一例斷言「他子命令的旗標錯放於此應被拒」。正是 E1 所述的單邊斷言。
- 兩者同源於 `PC-GPD-020`（該 pattern 即 E1／E2 的來源）。

**互指缺口複驗（更新 `0.1.0-W3-318` 實驗 8 的數字）**

| 方向 | 實測 |
|------|------|
| `framework-issue` 的 `SKILL.md`／`references/` 指向 `test-assertion-design-rules` | 0 |
| `test-assertion-design-rules` 與其 details 指向 `framework-issue`／`section_comment` | 0／0 |
| `framework-issue/tests/test_section_comment.py` 指向規則 E2 | **1**（該檔內一處明寫「正向對照輸入（test-assertion-design-rules E2）」） |

**判定：文件層互指仍為 0／0（W3-318 的結論在其量測對象上成立），但資產層已非 0——測試碼單向引用 1 次，規則側零回指。** 構成缺口，且缺口的方向性使其難被發現：引用方是測試碼（不入任何自動載入 context），規則的讀者永遠看不到自己已有一個落地實例。條文側該補的回指屬**條文層**，與本案的實作缺陷是兩件事。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: framework-issue-curator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-318 | chars: 1181 -->

### Task Reference
- 0.1.0-W3-318 what: 統整 第二輪三個 frame 的結果，判定剩餘缺口該由哪一層處理（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-318 why: **第二輪三個 frame 的結果不收斂，而那是設計上要的**——第一輪三張同 agent 定義、互相印證，結果其中一項被證實是方法共振。本輪三個 frame 各說各話，每一項因此各自站得住。

**三者的結論並列**：

- **`0.1.0-W3-306`（回歸）**：22 項既有缺口 → 6 消失／2 位移／8 換形態／6 未變。**消失的集中在「條文沒說」，未變的集中在「純條文層改不到」**——章節順序、`check` 的 7 天觸發器（六張 issue 實跑主警訊全未觸發）、`dedup` 命中規模（複測數字與第一輪完全相同：規則七 20／登記表 15／共用 30）。另有新缺口 N2–N5。
- **`0.1.0-W3-307`（對抗）**：條文**可被完全合規地穿過**，三個構造成立。共用機制是二軸表第 2 列——`should_exclude()` 對定義本體一律 False，但那些位置都有機械訊號，故一律判進第一檔。**第一檔不是恆空，是幾乎恆滿**，第二檔因此幾乎不可達。四項失敗嘗試反證條文的其他部分穩健。
- **`0.1.0-W3-308`（自我適用）**：23 張票僅 3 張寫定修法，唯一待議的 1 張非本 session 所建。**但該票同時指出本樣本無鑑別力**——條文落地後的目標人群僅 2 張，且落地前作者已實踐同一原則，**條文固化的是已有行為**。

**由此得到本票要判的問題**：第一輪統整已做過一次條文層修訂並落地（`dd24bce4`）。**第二輪若再做一次同層修訂，等於用同一個工具對它改不到的東西再試一次。** 306 的「未變」分類、307 的「幾乎不可達」、308 的「樣本無鑑別力」三者，從三個方向指向同一件事：**條文層的邊際效益已經到了某個位置**，而那個位置在哪、下一步該由哪一層承擔，是本票要回答的。

**不做則後果**：第三輪重複第二輪的形狀，或在沒有判準的情況下跳到 CLI 層（候選 B），而該候選的提示強度與誤報處置至今未定。（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/framework-issue/references/escalation-flow.md  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
- .claude/skills/ticket/ticket_system/commands/create.py  # from 0.1.0-W3-318（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 三份產出的每一項發現已標到層級，標註附可被推翻的依據  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
- [ref] [x] 307 的兩處最小修補已依現行三問評估證據狀態，據以決定可否當下寫定  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
- [ref] [x] 候選 B 的前置是否已備已判定，含 306 未變清單是否構成所需證據  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
- [ref] [x] 308 的樣本限制已帶進結論，未以自身合規支持條文有效  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
- [ref] [x] 是否需要第三輪已判定；需要者指明 frame 與理由，不需要者說明停止判準  # from 0.1.0-W3-318（來源已完成，結論見 Solution）
