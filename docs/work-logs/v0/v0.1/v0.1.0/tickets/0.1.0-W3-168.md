---
id: 0.1.0-W3-168
title: 補寫 component-contract-design skill 的提示元件設計引導
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-167
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-167
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶指示依前一輪回饋契約的流程：先改元件庫 skill，再改 SPEC，再補測試
  final_decision: DOC 於 component-contract-design 補提示元件的引導問句
  rationale: skill 是元件契約的產生器；引導不含提示元件的設計問題時，後續每個新元件都要重新論證一次
who:
  current: thyme-documentation-integrator
  history: {}
what: 補寫 component-contract-design skill 的提示元件設計引導
when: 0.1.0-W3-167 定案提示元件的使用時機與類型分化之後
where:
  layer: Presentation
  files:
  - .claude/skills/component-contract-design/SKILL.md
  - .claude/skills/component-contract-design/references
why: '`0.1.0-W3-167` 會定案三件事：何時該用暫態提示元件、重疊遮蔽如何處置（含是否需要類型分化）、不顯示時由誰承接可觀測性。這些是**設計決策**，而 `component-contract-design` 是元件契約的產生器——引導流程不問這些問題時，後續每個新元件都要重新論證一次，且各自論證的結果不會一致。


  前一輪回饋契約的落地順序是方法論 → skill → 範本 → SPEC → 測試策略（`0.1.0-W3-056` / `101` / `057` / `105` → `108` / `106` / `100` / `104` / `111`）。**用戶指示本輪照同一順序**：先改元件庫 skill，再改 SPEC，再補測試，實機最後。


  該 skill 已有〈回饋契約〉的填寫引導與判準問句「把這個元件背後的服務整個拔掉，使用者按下去還看得到反應嗎」（`0.1.0-W3-058`／`101`）。本票補的是另一個維度：**這個元件的回饋該不該用暫態提示載體，若是，用哪一種**——前者問回饋存不存在，後者問回饋走哪個通道。'
how:
  task_type: Documentation
  strategy: '先讀 `0.1.0-W3-167` 的 Solution 取定案的判準，**引導問句以該判準為輸入**，不自行發明。


    補的內容至少涵蓋：


    1. **通道選擇問句**：該元件的結果回饋走狀態轉換本身、暫態提示、或持續性指示？依 `0.1.0-W3-167` 的判準（事件性質為輸入），不逐案列舉。

    2. **若走暫態提示，用哪一種**：依 167 是否裁定類型分化而定。若未分化（只有一種提示元件），此問句仍須存在但答案唯一，並註明分化與否的判定來源，使日後新增載體時引導自動適用。

    3. **不顯示時的日誌承接**：判定為「不顯示提示」的元件，其可觀測性由誰承接——與 `0.1.0-W3-166` 的元件層日誌條款銜接，不重述該條款內容，以標題文字引用。


    **沿用該 skill 既有的填寫步驟形態**（`0.1.0-W3-058` 把判準問句做成要求作答的填寫步驟，非背景說明）——新問句同樣要求作答，不得是可略過的提示。


    **不擴張至 SPEC**：全 SPEC 的狀態提示盤點屬 `0.1.0-W3-169`，本票只改 skill。既有元件契約是否需回填新問句的答案，同屬該票範圍。'
acceptance:
- '[ ] 通道選擇問句已加入且要求作答，判準取自 0.1.0-W3-167 未自行發明'
- '[ ] 類型分化的問句存在且註明判定來源，即使 167 裁定不分化亦保留該問句'
- '[ ] 不顯示提示時的日誌承接問句與 0.1.0-W3-166 條款銜接，以標題文字引用未重述'
- '[ ] 新問句沿用既有的要求作答形態，非可略過的背景說明'
- '[ ] 未改動 SPEC（屬 0.1.0-W3-169）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-09T13:28:08'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補寫 component-contract-design skill 的提示元件設計引導

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### PM 前置實查（2026-09-09，派發前）

#### 阻擋條件已自然滿足

本票 acceptance 第 3 項要求「與 `0.1.0-W3-166` 的元件層日誌條款銜接，以標題文字引用」。建票當時 166 為 pending，該標題尚不存在——引用一個不存在的標題本身即是單向引用缺陷的形態。**現已滿足**：166 為 completed，`.claude/methodologies/component-library-bidirectional-constraint-methodology.md` 第 210 行的〈元件層回饋日誌（觀測對偶）〉節存在，且該方法論的元件契約欄位表（第 67 行）已在「回饋契約」列末尾接上該節。以標題文字引用該節即可，不寫章節號。

#### 上游 `0.1.0-W3-167` 的結論取用時，兩處需以更正後版本為準

167 已完成（9/9 acceptance），但其中兩項在完成後經 PM 覆核更正。本票取判準時請直接讀票面，並注意：

**一、判準 T 的四問序列可直接取用**（T1 可見性 → T2 該畫面是否有可見狀態轉換 → T3 是否需保留閱讀 → T4 是否附隨必要操作）。T1 置於 T2 之前是刻意的，該序不可調換。

**二、F5 的支撐理由已更正**。原寫「動作在 0.1 無替代入口」，實查兩者皆有替代入口（`rescanAction` 對應 `gap_report_screen.dart` 的 `action-gaps-rescan` 按鈕，`viewGapsAction` 的目的地在導覽列中）。**真正無替代的是通知本身不是動作**——損失是「使用者不知道事件已發生」，不是「使用者無法執行該動作」。本票若在引導問句中提及此類後果，須用更正後的表述；兩者導出的通道選擇不同（前者指向留痕與可回顧，後者會指向阻擋型載體）。

#### 類型分化的現況：167 未裁定分化，且該項停在待量測

167 的機制層裁定已撤回為待裁定（前提由 `0.1.0-W3-184` 定），真實頻率停在 `0.1.0-W3-179`。因此本票 acceptance 第 2 項的情形成立——**分化與否尚未裁定**，問句仍須存在且註明判定來源，使日後新增載體時引導自動適用。不得因「目前只有一種提示元件」而把該問句寫成可略過。

#### 既有形態的錨點（實查確認存在）

| 位置 | 內容 |
|------|------|
| `.claude/skills/component-contract-design/references/field-questions.md` 第 35 行 | 〈元件契約欄位填寫問句〉的「回饋契約」列，內含 `0.1.0-W3-058`／`101` 建立的要求作答形態判準問句（「把這個元件背後的服務整個拔掉，使用者按下去還看得到反應嗎？」），以及三項掛載點／失敗路徑／降級條款的作答要求 |
| `.claude/skills/component-contract-design/SKILL.md`〈契約齊全的定義〉第 5 條 | 機械可查判準，與上列問句成對——新增問句若需要對應的齊全判準，此處是落點 |

**新問句應沿用第 35 行那一列的形態**：置於填寫特定欄位「之前」、以問句要求作答、答案不合格即指出何處錯誤，而非列為背景說明。

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**變更摘要**：`.claude/skills/component-contract-design/references/field-questions.md` 第 35 行（回饋契約列）填寫問句欄，於既有掛載點判準問句之後、原「使用者接近／按下／被程式定位／資料態變更時，此形態的使用者從哪個通道得知」句之前，插入三組要求作答的新問句：

1. **通道大類判準（四問，先命中先決定，序不可調換）**：(1) 可見性——觸發畫面是否為使用者當前可見且前景？否則不用暫態提示，改走升級通道或延後，兩者皆不可行落入日誌承接。(2) 狀態轉換——承上為是，該畫面是否已有可見狀態轉換？是則不用暫態提示；本問明訂必須排在可見性之後（狀態轉換可能發生在不可見畫面，需先被可見性擋下）。(3) 保留需求——內容需保留/抄錄/逐項閱讀？是則不用會自動消失的暫態提示，改由畫面承載。(4) 附隨操作——是否附帶必須執行才能取得結果的動作？是則列不可截斷級，否則可截斷級。判準取自 `0.1.0-W3-167` Solution〈第一問〉判準 T 的 T1-T4，改寫為不含專案識別符的一般性事件屬性輸入。
2. **類型分化問句**：即使目前元件庫只有一種暫態提示元件、答案唯一，本問句仍不得省略，並須註明判定來源（分化與否取決於顯示位爭用實測與後果分級，未經實測不得逕行分化亦不得逕行斷定不需要）。對應 `0.1.0-W3-167` 裁定 B（待量測，未裁定分化與否）的現況——問句本身不因「目前只有一種載體」而省略。
3. **不顯示時的日誌承接**：判定不使用暫態提示時，已改走升級通道者由該通道既有日誌承接；無任何升級通道者，判斷當下須記錄一則「因判準而未顯示提示」事件並標明命中哪一問。判定為使用暫態提示者，其顯示/靜默早退/使用者互動三類時刻日誌，以標題文字銜接元件庫雙向約束方法論〈元件層回饋日誌（觀測對偶）〉節（`.claude/methodologies/component-library-bidirectional-constraint-methodology.md` 第 210 行），不重述其內容。

同列「常見空格與來源」欄補三組問句對應的常見漏答樣態（逕自預設用暫態提示、因「目前只有一種」而省略分化問句、誤以為升級通道會自動記下判準命中理由）。

**未改動**：SPEC（屬 `0.1.0-W3-169`）；`SKILL.md`〈契約齊全的定義〉第 5 條（該處僅為 Problem Analysis 提示的可選落點，acceptance 未要求，且改動需連動 CHANGELOG.md／SKILL.md 版本號，二者皆在 ticket where.files 宣告範圍外，故不動，避免違反並行環境的檔案範圍限制）。

**commit**：3d9155647eb2417d26455e81baa379cb4b5c3ef4
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-167 | chars: 2000 -->

### Task Reference
- 0.1.0-W3-167 what: 設計 提示元件的使用時機、重疊處置、類型分化，與不可見時的日誌補位（來源已完成，結論見 Solution）
- 0.1.0-W3-167 what: 設計 提示元件的使用時機、重疊處置、類型分化，與不可見時的日誌補位（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-167 why: **用戶裁示（2026-09-09）**：「也要額外評估一個問題，哪些時機要呼叫 snackbar 或者 bottoast 這類提示元件，**如果大量呼叫提示元件，反而會重疊遮蔽**，那有可能需要依照不同類型的提示需求，設計使用不同的提示元件，以及**如果提示元件看不到，我們要在哪些事件或者流程上要補 log**。」

`0.1.0-W3-166` 固化「有顯示就要有紀錄」，但不回答「何時該顯示」。兩者一起才完整：只有前者會導向「凡事都跳提示」，而提示互相爭用同一個顯示位時，**資訊反而比不提示更少**——後出現的取代前一則，使用者兩則都沒讀完。

**這不是假設性風險。** 本專案目前 `AppSnackBar.show(` 的呼叫點實查為 **3 處、2 個檔案**（`gap_report_screen.dart:199`、`:218`、`scan_notification_controller.dart:202`；`shell.dart` 只有註解提及，無呼叫）。而 `0.1.0-W3-113`（`ScanNotificationController.show` 失敗的 SnackBar fallback）與 `0.1.0-W3-165` 都會再增加觸發時機，編輯能力（`0.1.0-W3-073`）落地後還會更多——**呼叫點只會增加，所以此數為量測當下的值，接手者須自行複驗現值**（`reference-stability-rules` 規則 9）。

> **建票時的數字更正（2026-09-09）**：本段原寫「6 個呼叫點，分佈於三個檔案」。該值來自 `grep 'AppSnackBar.show\|AppSnackBar\.'`——第二個分支匹配任何成員存取（型別引用、列舉、註解），第一個分支的 `.` 為未跳脫的正則萬用字元，故量到的是「提及 `AppSnackBar` 的行數」而非呼叫點數。現值由兩個 session 各自以 `AppSnackBar\.show\(` 獨立實查並互相對上。另有一種口徑是「訊息結局」（同一呼叫點的分支各算一則）為 5 則，可並列參考，但 acceptance 第 2 項的對照對象是**呼叫點**。

**失效形態的更正見 Problem Analysis 的〈PM 前置實查〉第二節**：本專案的 `AppSnackBar.show` 在顯示前無條件呼叫 `hideCurrentSnackBar()`，且 `ScaffoldMessenger` 一次只呈現一則，故版面上的「重疊」不會發生，實際形態是**替換截斷**。用戶關切的後果成立且更嚴重（重疊時兩則都在畫面上，替換是後者把前者抹掉），但處置方向不同——分化元件解決的是「同時要看到兩則」，解決不了「被靜默丟棄」。

**第三個子問題是前兩者的必然對偶**：一旦決定某些時機不顯示提示，那些事件就失去了使用者可見的回饋，此時可觀測性必須由日誌承接——否則等於用「安靜」換掉了「有紀錄」，兩頭皆空。`0.1.0-W3-078` 的實機驗證正是撞上這個：路徑走到了、程式做了對的事，但沒有任何一側留下使用者層級的證據。（來源已完成，結論見 Solution）
- 0.1.0-W3-167 why: **用戶裁示（2026-09-09）**：「也要額外評估一個問題，哪些時機要呼叫 snackbar 或者 bottoast 這類提示元件，**如果大量呼叫提示元件，反而會重疊遮蔽**，那有可能需要依照不同類型的提示需求，設計使用不同的提示元件，以及**如果提示元件看不到，我們要在哪些事件或者流程上要補 log**。」

`0.1.0-W3-166`（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)

### Related Files
- docs/spec/ui/SPEC-004-component-library.md::read  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- docs/spec/ui/SPEC-003-interaction-response.md::read  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- .claude/skills/ux-design-evaluation/references/interaction-feedback.md::read  # from 0.1.0-W3-167（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 何時使用暫態提示的判準已建立且可機械套用，輸入為事件性質而非逐案列舉  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- [ref] [x] 若結論為需要新提示元件，已說明與 AppSnackBar 的分界並走 component-contract-design 流程  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- [ref] [x] 因判準或遮蔽處置而不顯示的事件集合已逐類指定日誌承接點與等級，含被丟棄的提示  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- [ref] [x] 承接的規格撰寫已建 DOC 票或已登記 spawn request  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
- [ref] [x] 全部 AppSnackBar.show( 呼叫點逐一對照該判準，不符者列出並給處置（派發前實查為 3 處／2 檔，接手者須自行複驗現值）  # from 0.1.0-W3-167（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
