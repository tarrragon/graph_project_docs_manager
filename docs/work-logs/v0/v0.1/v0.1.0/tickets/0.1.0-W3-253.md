---
id: 0.1.0-W3-253
title: 裁定 被動作觸發之再顯示所截斷的舊提示，其關閉事件應記什麼等級
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W3-205
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-205
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-205 Phase 4a 發現 origin background 時使用者發起的關閉被記為 warning 900
  final_decision: 另立 ANA 票裁定等級判定形態，不併入 Phase 4b
  rationale: 這是行為判定不是守衛修復；hide 在此路徑的語意與其他路徑不同，是否引入第三種輸入屬設計決策，不能在重構票裡順手決定
who:
  current: 主線程
  history: {}
what: 裁定 被動作觸發之再顯示所截斷的舊提示，其關閉事件應記什麼等級
when: 0.1.0-W3-205 Phase 4b 完成後（該票的斷言寫回會固定住現行 reason 值，本票以其為前提）
where:
  layer: Presentation
  files:
  - docs/spec/ui/SPEC-003-interaction-response.md::read
why: '`0.1.0-W3-205` Phase 4a 的新發現，**不屬守衛失效而是行為判定**，故不併入 Phase 4b。


  **形態**：`onAction` 同步再顯示時，舊的那一則其 `closed` 事件的 `reason` **恆為 `hide`**（Phase 4a 查 Flutter 3.47.1 原始碼證實為確定性行為，非競態：`TickerFuture._cancel` 只完成 secondary completer、primary 永不完成）。該事件因此走 `_resolveClosedLevel(hide, origin)`；若該呼叫點的 `origin` 為 `background`，**使用者自己按下動作造成的關閉會被記成 warning(900)**。


  **這正是 `SPEC-003`〈截斷事件的日誌等級〉要避免的誤判方向**——把使用者發起的事件記成背景等級，等於在日誌裡製造一則不存在的異常。


  **生產環境目前不可達**：`gap_report_screen.dart` 的 `onAction` 回傳未 await 的 Future，同步段不顯示 SnackBar，現況由 `0.1.0-W3-205` 的 T-205-12 鎖住。**但本票的來源票存在的理由，正是預先擋住這類形態**——留在審查報告裡等於沒有記錄。


  **為何是 ANA 不是 IMP**：「這種關閉該記什麼等級」需要先有答案。`hide` 這個 `reason` 在此路徑上不代表「被低級別截斷」而是「使用者按了動作」，等級判定的輸入語意在此處與其他路徑不同——是否該引入第三種輸入、或由呼叫端顯式標記，屬設計決策，不能在重構票裡順手決定。'
how:
  task_type: Analysis
  strategy: 'PM 前台。先做重現實驗（ANA 強制）：以測試構造 `onAction` 同步再顯示的路徑，實測舊那一則的 `closed` 事件實際帶的 `reason` 與 `level`，確認 Phase 4a 的推導在本專案的實際輸出上成立——**不以原始碼推導取代實測**。


    裁定的三個候選方向（不預設答案）：

    1. **維持現況**：`hide` + `origin` 決定等級，接受該路徑的誤判。須說明為何可接受，並確認 T-205-12 鎖住的「不可達」是結構性保證還是當前巧合。

    2. **等級判定引入第三種輸入**：區分「被低級別截斷」與「使用者按動作而被替換」兩種 `hide`。代價是 `_resolveClosedLevel` 的輸入從二元變三元，`SPEC-003` 的既有條文須同步。

    3. **由呼叫端顯式標記**：`onAction` 內的再顯示傳入不同的 `origin`。代價是把框架行為的知識推給呼叫端。


    **本票不改任何規格與程式碼**。裁定後依方向建 DOC／IMP 承接票；若裁定為方向 1，須在 `SPEC-003` 留下具名理由（例外必須具名，否則下一個事件會照抄），該補記亦另票承接。'
acceptance:
- '[ ] 已實測該路徑舊訊息 closed 事件的實際 reason 與 level，未以原始碼推導取代實測'
- '[ ] T-205-12 鎖住的不可達性已判定為結構性保證或當前巧合，附依據'
- '[ ] 三個候選方向各有利弊評估，裁定結果附理由'
- '[ ] 本票未改動任何規格或程式碼，承接動作已建票或逐項標註無需建票的理由'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-10'
updated: '2026-09-10'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

裁定 被動作觸發之再顯示所截斷的舊提示，其關閉事件應記什麼等級

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-205 | chars: 1699 -->

### Task Reference
- 0.1.0-W3-205 what: 修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別
- 0.1.0-W3-205 what: 修正 AppSnackBar 無條件 hideCurrentSnackBar 使低級別訊息可截斷高級別

### Rationale Chain
- 0.1.0-W3-205 why: `AppSnackBar.show` 無條件呼叫 `hideCurrentSnackBar()`，因此**任何一則新訊息都會截斷正在顯示的那一則，不分級別**——一則 L3 完成通知會殺掉正在顯示的 L2 失敗訊息，違反 `0.1.0-W3-184` 截斷規則第三條（低級別不得截斷任何東西）。

**且干擾是跨路徑且不對稱的。** `hideCurrentSnackBar()` 作用於 `_snackBars.first`，**不分那一則是誰放進去的**；而 `lib/main.dart:219` 直接呼叫 `ScaffoldMessenger.of(context).showSnackBar(...)` 繞過元件層，走排隊語意。結果是元件路徑殺得掉原生路徑的訊息、反之不行，且被殺的那則**零日誌**（不經過元件層，`0.1.0-W3-165` 的四個事件對它全不適用）。

**使用者可觀察到的徵狀**：訊息閃一下就不見了，而沒有任何紀錄能回答哪一則被殺、被誰殺。
- 0.1.0-W3-205 why: `AppSnackBar.show` 無條件呼叫 `hideCurrentSnackBar()`，因此**任何一則新訊息都會截斷正在顯示的那一則，不分級別**——一則 L3 完成通知會殺掉正在顯示的 L2 失敗訊息，違反 `0.1.0-W3-184` 截斷規則第三條（低級別不得截斷任何東西）。

**且干擾是跨路徑且不對稱的。** `hideCurrentSnackBar()` 作用於 `_snackBars.first`，**不分那一則是誰放進去的**；而 `lib/main.dart:219` 直接呼叫 `ScaffoldMessenger.of(context).showSnackBar(...)` 繞過元件層，走排隊語意。結果是元件路徑殺得掉原生路徑的訊息、反之不行，且被殺的那則**零日誌**（不經過元件層，`0.1.0-W3-165` 的四個事件對它全不適用）。

**使用者可觀察到的徵狀**：訊息閃一下就不見了，而沒有任何紀錄能回答哪一則被殺、被誰殺。

### Related Files
- lib/main.dart  # from 0.1.0-W3-205
- lib/components/app_snack_bar.dart  # from 0.1.0-W3-205
- lib/app/attention_level.dart  # from 0.1.0-W3-205
- lib/screens/gap_report/gap_report_screen.dart  # from 0.1.0-W3-205
- lib/screens/gap_report/scan_notification_controller.dart  # from 0.1.0-W3-205
  ... (truncated, see source ticket)

### Source Acceptance (reference)
- [ref] [ ] lib/main.dart 不再直接呼叫 ScaffoldMessenger.showSnackBar，全部提示經 AppSnackBar  # from 0.1.0-W3-205
- [ref] [ ] 被截斷時日誌可回答被截斷的是哪一則（識別資訊而非僅事件類型）  # from 0.1.0-W3-205
- [ref] [ ] 級別取值以標題文字引用 SPEC-003，元件內未另立一套級別定義  # from 0.1.0-W3-205
- [ref] [ ] 截斷條件綁在級別上：低級別不得 hide、同級別截斷並留痕、僅高級別可截斷，三者各有測試  # from 0.1.0-W3-205
- [ref] [ ] 新建 lib/app/attention_level.dart 承載級別列舉，且 AppSnackBar 與未來的仲裁器皆 import 該檔而非各自定義  # from 0.1.0-W3-205
  ... (truncated, see source ticket)
- [ref] [ ] lib/main.dart 不再直接呼叫 ScaffoldMessenger.showSnackBar，全部提示經 AppSnackBar  # from 0.1.0-W3-205
- [ref] [ ] 被截斷時日誌可回答被截斷的是哪一則（識別資訊而非僅事件類型）  # from 0.1.0-W3-205
- [ref] [ ] 級別取值以標題文字引用 SPEC-003，元件內未另立一套級別定義  # from 0.1.0-W3-205
- [ref] [ ] 截斷條件綁在級別上：低級別不得 hide、同級別截斷並留痕、僅高級別可截斷，三者各有測試  # from 0.1.0-W3-205
- [ref] [ ] 新建 lib/app/attention_level.dart 承載級別列舉，且 AppSnackBar 與未來的仲裁器皆 import 該檔而非各自定義  # from 0.1.0-W3-205
  ... (truncated, see source ticket)

