---
id: 0.1.0-W3-391
title: 評估 foundation-design SKILL.md 全檔超出 skill-design-guide 5k tokens 門檻的外移方案
type: ANA
status: completed
version: 0.1.0
wave: 3
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-336.3
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: SKILL.md 全檔量測超過 5k tokens 門檻
  final_decision: 派 DOC/ANA 票依 splitting-an-existing-skill.md 程序評估外移方案
  rationale: 體量門檻是為避免認知超載，非本票新增所致但需追蹤處理
who:
  current: thyme-documentation-integrator
  history: {}
what: SKILL.md 全檔量測已達約 5441 tokens，超出 skill-design-guide 第 2 層 5k tokens 門檻（本次執行前已達 5426，非本次新增所致）。依 splitting-an-existing-skill.md 程序判定超標成分（敘事段落過長或判準表本身應外移）並提出外移方案
when: 待 PM 排入 wave
where:
  layer: 待定義
  files:
  - .claude/skills/foundation-design/SKILL.md::write
  - .claude/skills/foundation-design/references/adjacent-assets-boundary.md::write
  - .claude/skills/foundation-design/CHANGELOG.md::write
why: 本次執行僅在 examples.md 遇到補範例超標並依規範拆檔，未處理 SKILL.md 本身既有的超標；quality-baseline 規則 5 要求發現即建立追蹤
how:
  task_type: Analysis
  strategy: 依 splitting-an-existing-skill.md〈補範例後超標：觸發條件與處置順序〉步驟 1-2 判定超標成分是否為論證敘事或版本紀錄，是則先外移至 CHANGELOG.md；仍超標則依讀者情境四問判定可外移的目標檔
acceptance:
- '[x] 量測 SKILL.md 現行 tokens 數並確認超標成分來源'
- '[x] 產出外移方案或結論為不需外移'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-15T10:22:22'
completed_at: '2026-09-15T10:41:49'
created: '2026-09-15'
updated: '2026-09-15'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

SKILL.md 全檔量測已達約 5441 tokens，超出 skill-design-guide 第 2 層 5k tokens 門檻（本次執行前已達 5426，非本次新增所致）。依 splitting-an-existing-skill.md 程序判定超標成分（敘事段落過長或判準表本身應外移）並提出外移方案

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因
`.claude/skills/foundation-design/SKILL.md` 全檔（含 frontmatter）實測 5612 tokens（分段估算法，繁中/ASCII 分算），超出 skill-design-guide 第 2 層 5000 tokens 門檻 612 tokens（12.2%）；500 行官方硬限完全未觸及（175 行）。純為 token 體量超標，非官方合規問題。

超標成分溯源（比對 CHANGELOG.md 歷史紀錄）：v6.5.0 時本檔已收斂至 4872 tokens（Common Issues 外移、新增〈與相鄰資產的交界〉表）；其後 v6.5.1–v6.6.1 五筆版本累積新增（多為「補一句缺引導」「補範例」型修正，含 v6.6.0 為 13 處缺範例位置補三段鏈式範例）使全檔回升至 5612 tokens，淨增 740 tokens，未同步做等量外移。

### 影響範圍
單一檔案 `.claude/skills/foundation-design/SKILL.md`。已驗證事實：
- 全檔 5612 tokens（外移前）、175 行
- 8 個 `##` 章節（判準的通用形式、本 skill 不做什麼、維度與產物、與 orchestration 的協作、接手模式、工作流、移植前置條件、按需讀取）
- 僅一處內容自述「按需查」：〈與相鄰資產的交界〉節原文「執行時不需要先讀本節。不確定某件事該由本 skill 還是相鄰資產處理時再查」（skill-design-guide〈外移什麼、留什麼〉四問的優先候選）
仍未驗證假設：無（本票範圍內僅涉及此檔案，未擴及其他 skill）。

### 相關 Error Pattern
執行 `/error-pattern query` 相關關鍵字（skill 體量、token 收斂、SKILL.md 拆分）未查得專屬 error-pattern；本次處置依循 `.claude/skills/skill-design-guide/references/splitting-an-existing-skill.md` 既定程序與 `version-bootstrap` skill 於 CHANGELOG v1.6.5 的同型先例（同一節名「與相鄰資產的交界」、同一自述按需查理由、同一 5000 門檻超標情境）。

## 重現實驗結果
### 實驗方法
用 SKILL.md〈Progressive Disclosure〉指定的分段 token 估算指令，對修改前後的 `foundation-design/SKILL.md` 與新建的 `references/adjacent-assets-boundary.md` 各量測一次：
`python3 -c "import sys;t=open(sys.argv[1],encoding='utf-8').read();a=sum(1 for c in t if ord(c)<128);print(round(a/4+(len(t)-a)/1.3),'tokens (est)')" <file>`

### 實驗執行
1. 外移前：`SKILL.md` 5612 tokens、175 行
2. 依 `splitting-an-existing-skill.md`〈補範例後超標：觸發條件與處置順序〉步驟 1（先查 SKILL.md〈超標了怎麼辦〉診斷表，論證/實證敘事先外移）：
   - 移除「各自完整，但沒有任何一處回答…缺口在可發現性，不在能力」純敘事段（其結論已由前一句「會需要這個入口，是因為那些權威散在四類載體」與後方表格成立，非必要重複）
   - 移除〈本 skill 不做什麼〉指名權威用名字段落中，與 CHANGELOG v6.0.0 已記錄之實測敘事重疊的尾段（框架改版移動 hook 位置、可攜性閘門擋下 25 處路徑），保留主張句與可操作的查找指令
   - 移除〈與 orchestration 的協作〉「判別不用身份也不用二元存在性」段落的舉例說明（AI session 身份判定、changelog 誤觸案例），保留主張句與其後的具體反例
   - 移除「交回下游不等於下游覆蓋全部維度」段落的查證過程句（「實查 version-bootstrap 的步驟…0 命中」）——該事實在步驟 3 已完整移入新建 reference 的 version-bootstrap 列，不重複
3. 步驟 1 處置後複測：SKILL.md 降至約 5391 tokens（估算，未單獨存檔量測），仍超標
4. 依步驟 3（讀者情境四問）：〈與相鄰資產的交界〉整節逐字外移至新建 `references/adjacent-assets-boundary.md`——四問判定：問 1（該節不是路由表任一列的「何時讀」判斷條件）與問 2（並非每個〈按需讀取〉列的讀者都需要）皆不成立，問 3 成立且該節原文已自述按需查（拆分前已存在的讀者情境，非本次新造），故外移到位；入口檔〈按需讀取〉表新增一列路由
5. 〈按需讀取〉表首句「下表四份」因新增第五列而改為「下表各份」，避免可變計數內嵌數量

### 實驗發現
已驗證事實：
- 外移後 `SKILL.md` 4978 tokens、160 行（低於 5000 門檻，餘裕 22 tokens）
- 新建 `references/adjacent-assets-boundary.md` 607 tokens（遠低於單檔 5000 門檻，讀取方式屬選段查閱）
- `grep -rn` 全站掃描確認：無外部位置引用被移除敘事的逐字內容；`dart-style-guardian/references/spacing-system.md` 與 `version-bootstrap` 的引用皆指向 `examples.md`／`handoff-mode.md`／skill 名稱本身，未涉及本次搬動的段落
- `version-bootstrap` skill 於其 CHANGELOG v1.6.5 有完全同型先例（同節名、同自述理由、同 5000 門檻超標情境），佐證本次外移判定方向正確
- 產物欄五種合法答案、權威缺席時四形態、工作流、移植前置條件四個受保護章節逐字未改
仍未驗證假設：無。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 結論
`foundation-design/SKILL.md` 全檔由 5612 tokens 收斂至 4978 tokens（低於 skill-design-guide 5000 門檻），採候選方案 2（見下）落地。

### 候選方案
1. **只做整節外移，不動敘事**：只外移〈與相鄰資產的交界〉（472 tokens），淨省約 380 tokens（扣除〈按需讀取〉新增路由列開銷），結果約 5230 tokens，仍超標。優點：風險最低、100% 對應 splitting-an-existing-skill.md 處置順序步驟 3、與 version-bootstrap 先例完全同構。缺點：不達成票面「5000 tokens 以下」目標，需再開票處理殘餘敘事。
2.（採用）**先敘事後外移，依處置順序步驟 1→3 全走**：先依診斷表移除 4 處純敘事／與 CHANGELOG 重複的實證段落（共約 239 tokens，逐一為完整句子邊界的乾淨切除，不改寫周邊判準/步驟/欄位文字），再外移〈與相鄰資產的交界〉整節。優點：一次到位達成門檻，且步驟 1 為 splitting-an-existing-skill.md 明定的處置順序第一步（先於步驟 3），ticket how.strategy 欄位已預期此順序；被移除敘事的事實內容分別已由前一句結論、CHANGELOG v6.0.0、新建 reference 檔的 version-bootstrap 列承接，未造成資訊真空。缺點：需要逐段判斷「是否為純敘事」而非機械化操作，判斷有一定主觀性（已在重現實驗結果逐段列出判斷依據供覆核）。

### 外移清單
- 新建 `.claude/skills/foundation-design/references/adjacent-assets-boundary.md`（607 tokens）：承接原〈與相鄰資產的交界〉整節（表格逐字未改，僅檔頭補三段式慣例：何時讀／同目錄／溯源）
- `SKILL.md`〈按需讀取〉表新增一列，路由至新檔
- 移除 4 處純敘事／重複實證段落（詳見重現實驗結果步驟 2），無需新建承接檔案——內容已分別由既有前後文、CHANGELOG.md、新建 reference 檔涵蓋

### 未外移項目（票面明定保留）
工作流主幹、產物欄五種合法答案、權威缺席時四形態逐字未改，未觸及票面「若必須外移其一，停手回報理由」的分支。

### Spawn 規劃
無需新建 ticket。理由：
1. 判準/步驟/欄位文字（工作流、產物欄五種合法答案、權威缺席時四形態、維度表本身）逐字未改，未觸發「需改寫才能搬時停手回報」條款
2. 外移後餘裕 22 tokens，若後續版本再有「補一句缺引導」型累積，建議下次維護時比照本票流程重新量測，不需預先開票佔位（違反規則 2「合法 trigger 限 ticket ID」——尚無具體觸發事件）

### 自檢結果（Layer 1）

**執行時機**: complete 前
**掃描手段**（逐項可複驗）：
- `grep -n "數據\|代碼\|默認\|文檔\|軟件\|硬件\|信息\|視頻" .claude/skills/foundation-design/{SKILL.md,references/adjacent-assets-boundary.md,CHANGELOG.md}` → 0 命中
- `grep -nP '[这该们为対発処]' .claude/skills/foundation-design/{SKILL.md,references/adjacent-assets-boundary.md,CHANGELOG.md}` → 0 命中
- 人工比對 ticket-body-schema.md ANA 必填章節清單（Problem Analysis / 重現實驗結果 / Solution）→ 三章節皆已填寫實質內容，無佔位符殘留
- `python3` 分段 token 估算對 SKILL.md 與新建 reference 檔各量測一次（見重現實驗結果）
- `grep -rn` 全站掃描確認外部無引用被移除敘事的逐字內容（見重現實驗結果）

| 類別 | 發現項目 | 處理方式 |
|------|---------|---------|
| B1/B3 | 無 | — |
| C1 | 無（三章節皆有實質內容） | — |
| C2 | 無自訂 H2（僅用 Schema 章節名） | — |

**自檢結論**: 已掃描範圍內通過（未涵蓋：A1-A3 文字品質三明示逐段覆核，因本票產出以量測數字與表格搬移為主、非新撰規則條款，判定不適用；已用資訊優先序原則自查候選方案段落）。

### multi_view_status

multi_view_status: n_a
reason: 本票為純體量搬移（逐字外移 + 4 處敘事乾淨切除），非新設計判準或架構決策；判準/步驟/欄位文字逐字未改，且已有 version-bootstrap 同型先例佐證方向正確，不適用多視角審查門檻。
### PM 驗收未通過的修正（刪除／搬移逐項表）

PM 覆核 f62b8b77 指出兩處誤判：「交回下游不等於下游覆蓋全部維度」段落刪除的查證句實為「這兩個維度」的指涉來源，刪除後指涉懸空；「判別不用身份也不用二元存在性」段落刪除的說明句實為判準引導依據（屬〈超標了怎麼辦〉診斷表「選路之後才用到的細節」），非可捨的舉例敘事。逐項處理如下：

| 原文 | 原判定（f62b8b77） | 修正後去向 | 依據 |
|------|-------------------|-----------|------|
| 〈本 skill 不做什麼〉「各自完整，但沒有任何一處回答『地基這件事整體從哪開始』。主動查重的人讀過 orchestration skill 的章節標題、確認不重疊，仍會判定地基無人承接而重造一份。缺口在可發現性，不在能力。」 | 直接刪除（誤判為與前句重複的純敘事） | **逐字外移**至新建 `references/entry-point-rationale.md`；原處留路由句「（可發現性缺口見 `references/entry-point-rationale.md`）」 | `grep -rn "可發現性\|缺口"` 掃描 CHANGELOG.md 與 references/ 全部既有內容，確認無同義記錄——PM 條件「若沒有，逐字搬進 references」成立 |
| 〈本 skill 不做什麼〉指名權威用名字段落「路徑是『它現在放在哪』，會隨框架改版而移動；名字是『它是什麼』，不會。前一版寫死路徑的後果實測過兩次：框架改版把 hook 移了位置使註冊全數失效，以及可攜性閘門把 25 處路徑判為『指名他專案的檔案』而擋下推送。」 | 直接刪除 | **維持刪除** | CHANGELOG v6.0.0 條目已完整記錄同一實測後果（「路徑是它現在放在哪…可攜性閘門把 25 處路徑判為指名他專案檔案而擋下推送」），PM 明確核可「可以維持刪除，但要在 Solution 註明依據」——依據即此條 v6.0.0 逐字比對確認語意等價 |
| 〈與 orchestration 的協作〉「判別不用身份也不用二元存在性」段落說明句「『你是不是原作者』對 AI session 無從判定，且自己寫的專案同樣會缺可用文件；『有沒有文件』則會被行為記錄與流程規範誤觸——一份 500 行的 changelog 逐版記了改了什麼，卻回答不了『這個維度的產物是什麼』。」 | 直接刪除（誤判為舉例敘事） | 先**逐字還原**回原段落；還原後全檔仍超標（5148 tokens），依 PM 指示改為**逐字外移**至新建 `references/situation-judgment-notes.md`，原處留最小路由句「（見 `references/situation-judgment-notes.md`）」 | PM 判定：此句是判準「不用身份、不用二元存在性」的引導依據，非證據敘事，優先順序為先還原、超標才外移，不可直接刪除 |
| 〈與 orchestration 的協作〉「交回下游不等於下游覆蓋全部維度」段落「實查 `version-bootstrap` 的步驟，`DevOps` 與 `可觀測性` 兩個維度在其全文 0 命中。」 | 直接刪除（誤判為可省略的查證過程，理由是同一事實已見於新建 reference 的 version-bootstrap 列） | **逐字還原**，留在原段落原位置，不移動 | PM 明示：此句是後句「這兩個維度」的指涉來源，刪除後指涉懸空；「這兩個維度」的指涉句一律留在原段，不因 token 壓力外移 |

### 重新量測

- 還原兩句後：SKILL.md 5148 tokens（超標 148）
- 外移「判別不用身份」說明句 + 精簡三處路由文字用字（不改動判準/步驟/欄位文字本身，僅精簡本次新增的路由句與表格列用字）後：SKILL.md **4998 tokens**（低於 5000 門檻，餘裕 2 tokens）
- 新建 `references/entry-point-rationale.md` 302 tokens、`references/situation-judgment-notes.md` 307 tokens，皆遠低於單檔門檻
- `grep -rn` 複驗：兩處恢復的文字與新建 reference 檔內容逐字比對原文一致；`references/` 目錄下所有檔案在 SKILL.md 均有至少一處字串引用（`comm` 雙向檢查通過，僅存量的跨 skill 引用誤判 `chained-examples.md`／`splitting-an-existing-skill.md` 兩項，與本次修改無關，為既存現象）
- CHANGELOG.md 新增 6.6.3 條目說明修正內容與依據；SKILL.md frontmatter 版本同步至 6.6.3
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-15T10:41:49
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-336.3 | chars: 536 -->

### Task Reference
- 0.1.0-W3-336.3 what: 修補 foundation-design 缺範例與 examples 改寫（W3-332.7）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-336.3 why: W3-332 審查確認 agent 依此文件執行時被迫猜測或讀出兩種做法；缺口編號與來源見 W3-332.13 Solution，並以 W3-332 Problem Analysis〈PM 審查彙整草稿〉P1–P9 修正（來源已完成，結論見 Solution）

### Related Files
- .claude/skills/foundation-design/references/examples.md  # from 0.1.0-W3-336.3（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/references/examples-process.md  # from 0.1.0-W3-336.3（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/references/dimension-product-notes.md  # from 0.1.0-W3-336.3（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/references/handoff-mode.md  # from 0.1.0-W3-336.3（來源已完成，結論見 Solution）
- .claude/skills/foundation-design/CHANGELOG.md  # from 0.1.0-W3-336.3（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] W3-332.7 範圍內 foundation-design 缺範例位置補三段鏈式範例  # from 0.1.0-W3-336.3（來源已完成，結論見 Solution）
- [ref] [x] examples.md 需改寫各則依規格改寫  # from 0.1.0-W3-336.3（來源已完成，結論見 Solution）
- [ref] [x] 範例不含本專案識別符  # from 0.1.0-W3-336.3（來源已完成，結論見 Solution）
