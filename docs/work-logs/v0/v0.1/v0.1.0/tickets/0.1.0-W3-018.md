---
id: 0.1.0-W3-018
title: 修補 component-contract-design 的四項既有結構缺陷（多輪審查 Round 2 類別 E）
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: Round 2 審查發現既有結構缺陷，用戶裁定與重構收尾分離
  final_decision: 另立 DOC 票，不併入 Round 2 修法 commit
  rationale: 四項涉及步驟順序與輸入通道的設計變更而非補充說明，與拆分無關；併入會使重構 commit 的邊界模糊，且第四項需先決定表格切分方式
who:
  current: rosemary-project-manager
  history: {}
what: 修補 component-contract-design 的四項既有結構缺陷（多輪審查 Round 2 類別 E）
when: 多輪審查 Round 2 修法之後
where:
  layer: 待定義
  files:
  - .claude/skills/component-contract-design/references/modes-a-b.md
  - .claude/skills/component-contract-design/references/mode-c.md
  - .claude/skills/component-contract-design/references/field-questions.md
why: 多輪審查 Round 2 的四個 reviewer 找出四項既有結構缺陷，性質與拆分無關，且修法涉及步驟順序與輸入通道的設計變更而非補充說明，故與重構收尾分離。四項：其一，模式 B 步驟 5 寫「進模式 A 自存在必要性檢視起」即從步驟 3 進入，結構性跳過步驟 2 元件候選，而狀態表的「顯示」欄與「可用操作」欄兩個候選來源全在步驟 2，同檔又明寫此序顛倒會漏掉只在單一狀態出現的元件，無任何一處提示要補抽。其二，模式 C 步驟 5 的事故清單無輸入通道，五處消費「既有的重疊、截斷、點不到事故」而零處說明來源（issue tracker、ticket、用戶口述），與已修的模式 B 輸入通道同型，且步驟 5 的「零事故專案本步驟為空」分支無從判定是否成立。其三，統一匯出入口無定位作法，而它是元件庫與頁面層的唯一邊界定義，模式 C 第一個動作即依賴它決定目錄範圍，同檔步驟 3 卻給了具體 grep pattern，兩步具體度落差明顯。其四，多形態專案節為全套六檔唯一的整節散文（單一段落九個以上並列命題），七種體例裝置命中 0 至 1 種，而其內容本體正是「哪些欄位跨形態共用、哪些每形態各一份」的對映表形態，被 2-C 與 2-D 兩個 frame 獨立命中。
how:
  task_type: Documentation
  strategy: 四項各自獨立：模式 B 步驟 5 補「由 B 進入且專案有狀態表者先補跑步驟 2 的狀態表兩欄」；模式 C 步驟 5 補事故來源三選一與「三者皆無時視同零事故」的明示；模式 C 步驟 1 前補統一匯出入口的定位作法（找 barrel／index 檔或套件匯出宣告，找不到時的退路）；多形態專案節拆為「欄位 × 跨形態共用／每形態各一份」對映表加覆蓋表面與矩陣三維度小表，剩餘判準句留散文
acceptance:
- '[ ] 模式 B 接回模式 A 時的步驟 2 補抽已明示'
- '[ ] 模式 C 步驟 5 的事故來源已明示，含三者皆無時的分支'
- '[ ] 統一匯出入口的定位作法已給，含找不到時的退路'
- '[ ] 多形態專案節已由整節散文改為表格加判準句，體例與同檔其餘節一致'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-07T14:08:01'
completed_at: null
created: '2026-09-04'
updated: '2026-09-04'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修補 component-contract-design 的四項既有結構缺陷（多輪審查 Round 2 類別 E）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### PM 派發前考古（2026-09-07）

本票寫於該 skill 為單一 245 行主檔時。現況已不同：skill 為 **v1.5.3、8 個檔**，並已由另一 consumer 跑完四輪多輪審查（見該 skill `CHANGELOG.md`）。四項缺陷的現況經 PM 逐項查證：

| 缺陷 | 現況 | 依據 |
|---|---|---|
| 一：模式 B 步驟 5 結構性跳過步驟 2 | **仍存在** | `references/modes-a-b.md:61` 逐字仍為「5 進模式 A｜自存在必要性檢視起」；同檔 :11 與 :16 明寫元件候選的三處來源全在步驟 2 |
| 二：模式 C 步驟 5 事故清單無輸入通道 | **仍存在** | `references/mode-c.md:26` 仍為「既有的重疊、截斷、點不到事故逐一對應」，全檔無來源說明 |
| 三：統一匯出入口無定位作法 | **需你重新查證** | 拆分後新增了 `references/addresses.md` 收該用詞的地址。該表是否已給出「怎麼找到它」的作法，還是只給了「它是什麼」的地址，PM 未判定 |
| 四：多形態專案節為整節散文 | **需你重新查證** | CHANGELOG 記載拆分時該節的 `##` 節界曾遺失並已還原，但那修的是節界不是體例。散文改對映表這件事是否已做，PM 未判定 |

第三、四項若查證後判定已由拆分順帶解決，**在票面寫明依據並勾選即可，不需硬改**；判定仍存在則照票面 Solution 修。

該 skill 的 `CHANGELOG.md:59` 逐字記載這四項「與拆分無關且涉及設計變更，另立票承接」——維護方是刻意留給本票的，不是遺漏。

**版號與並行**：本票只寫 `references/` 下三檔。同期另有 agent 可能動 `SKILL.md`，不要改它，也不要動 CHANGELOG 以外的共用檔。版號由 1.5.3 進到 1.6.0（結構變更），CHANGELOG 與 frontmatter `metadata.version` 兩處都要改。

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）
