---
id: 0.1.0-W3-651
title: 新增 ticket create 的版本範圍凍結閘門
type: IMP
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶裁示：0.1.0 pending 池 83 張無法分辨必要與非必要，先擋膨脹再固化程序
  final_decision: 最小切入：create-time 硬閘門，形態比照 W3-648 portable gate
  rationale: 四選一（methodology 先／PROP 先／兩者同步／CLI 閘門先）中選 CLI 閘門：立即生效、可用 0.1.0 剩餘批次驗證、不需先定案程序文件
who:
  current: thyme-python-developer
  history: {}
what: '在 ticket create 加版本範圍凍結硬閘門：todolist.yaml 版本條目 scope: frozen 時，無 --parent 的根票被阻擋並印出相對凍結版本計算的溢出目標與二擇一處置；--scope-blocker 帶非空理由放行；子票繼承父票版本不經閘門'
when: 'todolist.yaml 版本條目標 scope: frozen 後'
where:
  layer: Application
  files:
  - .claude/skills/ticket/ticket_system/lib/version.py
  - .claude/skills/ticket/ticket_system/lib/field_validators.py
  - .claude/skills/ticket/ticket_system/commands/create.py
  - .claude/skills/ticket/tests/test_create_version_scope_gate.py
  - .claude/skills/ticket/references/create-command.md
  - .claude/skills/ticket/SKILL.md
  - .claude/skills/ticket/CHANGELOG.md
why: 現有版本歸屬引導只有「功能 vs 修復」一個軸，沒有「目標版本現在還收不收票」的軸；todolist.yaml 的 status 只有 active|planned|completed，一旦 active 所有根票（含 spawn 票）預設流入。0.1.0 實測：pending 池 83 張，依交付定義逐張問「不做它整合測試會失敗嗎」只有 8 張必要，其餘 75 張是執行中發現的品質改善、下一版能力、框架修復——版本無法發佈不是因為必要工作沒做完，是必要與非必要在同一池裡無法分辨。與 portable gate（#81，可攜性軸）正交，同在 create 驗證層。canonical 記錄：tarrragon/claude#55 觀測流 2026-09-22。
how:
  task_type: Implementation
  strategy: 'todolist.yaml 版本條目新增選填欄位 scope（值 frozen；缺席即開放，向後相容）。


    lib/version.py 新增兩函式：is_version_scope_frozen(version) 讀 todolist.yaml 回傳該版本是否 scope: frozen；suggest_overflow_version(frozen_version, ticket_type, action) 相對凍結版本計算溢出目標——IMP 且 action 在 _FEAT_ACTIONS 內 → minor+1 .0，其餘 → patch+1——純計算不查註冊，回傳 (version, reason)。不改既有 _suggest_next_patch／_suggest_next_major。


    lib/field_validators.py 新增 validate_version_scope_gate(version, ticket_type, action, scope_blocker)，形態比照 validate_portable_issue_gate：未凍結 → True；凍結且 scope_blocker 為非空字串 → 印 info 後 True；凍結且無 blocker 或 blocker 為空 → 印 ErrorEnvelope（含凍結版本、溢出目標、二擇一處置：--version <溢出目標>（未註冊須先在 todolist.yaml 登記）或 --scope-blocker "<理由>"）後 False。


    commands/create.py：register 新增 --scope-blocker（dest scope_blocker，help 說明須帶理由）；execute 根票分支在 resolve_version 之後、validate_version_registered 之前呼叫閘門；--parent 子票分支不呼叫（繼承父票版本，屬已在範圍內工作的細分）。--version 明示指向凍結版本仍經閘門，避免習慣性繞過。


    tests/test_create_version_scope_gate.py 比照 test_create_portable_issue_gate.py 五層：A is_version_scope_frozen（欄位缺席／open／frozen／版本不存在）；B suggest_overflow_version（feature→minor+1、patch 類→patch+1、IMP+修復→patch+1）；C validate_version_scope_gate（未凍結放行、凍結無 blocker 阻擋且訊息含溢出目標與兩處置、凍結有 blocker 放行、空字串 blocker 仍阻擋）；D argparse 註冊；E execute 整合（凍結版本擋根票、凍結版本放 --parent 子票、--scope-blocker 放行、未凍結版本不受影響作正向對照）。E 層以 tmp todolist.yaml fixture 注入 scope: frozen。


    文件同步（skill-cli-sync-check 規則）：references/create-command.md 補 --scope-blocker 與閘門說明；SKILL.md 版本 2.37.0 → 2.38.0；CHANGELOG 記錄；grep 找 todolist.yaml schema 的權威文件補 scope 欄位。'
acceptance:
- '[x] todolist.yaml 版本條目 scope: frozen 時，無 --parent 的 create 被阻擋，訊息含相對凍結版本計算的溢出目標（feature→minor+1，其餘→patch+1）與二擇一處置'
- '[x] --scope-blocker 須帶非空理由才放行，僅給旗標或給空字串仍阻擋（E1）'
- '[x] --parent 子票不經閘門；scope 欄位缺席或值非 frozen 的版本不受影響（正向對照），並以 tmp fixture 注入 scope: frozen 作為該紅輸入驗證閘門確實命中（E2）'
- '[x] references/create-command.md、SKILL.md 版號、CHANGELOG 與 todolist.yaml schema 權威文件同步，依 skill-cli-sync-check 流程逐一核對'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-22T18:03:28'
completed_at: null
created: '2026-09-22'
updated: '2026-09-22'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

在 ticket create 加版本範圍凍結硬閘門：todolist.yaml 版本條目 scope: frozen 時，無 --parent 的根票被阻擋並印出相對凍結版本計算的溢出目標與二擇一處置；--scope-blocker 帶非空理由放行；子票繼承父票版本不經閘門

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-22 18:02] 形態比照 W3-648 portable gate 與其五層測試；E1（旗標單獨不足）與 E2（tmp fixture 注入 frozen 作紅輸入）為驗收硬條件。不改既有 _suggest_next_patch／_suggest_next_major。｜readiness=2 validate=2

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
lib/version.py 新增 `is_version_scope_frozen(version)`（讀 todolist.yaml 回傳該版本 scope==frozen）與 `suggest_overflow_version(frozen_version, ticket_type, action)`（純計算，不查註冊，沿用 `_FEAT_ACTIONS` 分類：IMP+新功能動詞→minor+1.0，其餘→patch+1）；未改既有 `_suggest_next_patch`／`_suggest_next_major`。

lib/field_validators.py 新增 `validate_version_scope_gate(version, ticket_type, action, scope_blocker)`，形態比照 `validate_portable_issue_gate`：未凍結放行；凍結且 scope_blocker 為非空字串→印 WARNING 後放行；凍結且無 blocker 或空字串→印 ErrorEnvelope(VERSION_SCOPE_FROZEN)（含溢出目標與二擇一處置）後阻擋。

commands/create.py：新增 `--scope-blocker` 旗標；root 分支在 `resolve_version`→`validate_version_registered` 通過後、進入 ID 分配鎖之前呼叫 `validate_version_scope_gate`；`--parent` 子票分支不呼叫（繼承父票版本）。`--version` 明示指向凍結版本仍經閘門。

tests/test_create_version_scope_gate.py 五層（A is_version_scope_frozen／B suggest_overflow_version／C validate_version_scope_gate／D argparse 註冊／E execute 整合，含 E1 空字串 blocker 仍阻擋、E2 tmp fixture 注入 scope: frozen 走真實 yaml 讀取路徑驗證閘門命中）共 24 測試全綠。

文件同步：references/create-command.md 新增〈版本範圍凍結硬閘門〉節（情境表/溢出目標計算表/三情境範例）；SKILL.md 2.37.0→2.38.0；CHANGELOG.md 新增 2.38.0 條目；version-progression.md 補一行路由（scope: frozen 不收新根票）。

### 自檢結果

**執行時機**: finish 前，PM 代執行（代理人於 worktree isolation guard 對 `complete` 子命令的 basename 誤判處停手，未達 body-check；`finish` 別名在派發 prompt 已指定但代理人未採用）
**掃描手段**:
- `uv run --project .claude/skills/ticket --extra test pytest .claude/skills/ticket/tests/ .claude/skills/ticket/ticket_system/tests/ -q` → 4083 passed, 1 skipped（代理人回報，PM 未重跑）
- `ticket track commit --worktree` 越界硬擋 → 範圍收斂為票面 7 檔（世界平面：merge stat 7 files changed）
- `git log 0bc2597ac..HEAD -- .claude/pm-rules/version-progression.md` → 0；`git status --porcelain .claude/pm-rules/` → 0

| 類別 | 發現項目 | 處理方式 |
|------|---------|---------|
| C1 | Solution 文件同步段末句記載「version-progression.md 補一行路由」，但該改動因不在 where.files 內被 `ticket track commit` 擋下、代理人已還原未提交；世界平面查證該檔未進任何 commit。記錄在還原前寫入，事後未更正 | 本子章節更正；原句不改（保留代理人當時記錄，避免考古失真）。該路由需求併入後續 methodology 票，不另開票 |
| C2 | 派發 prompt 指定 `finish` 而代理人用 `complete`，撞 CC runtime 對 bash builtin 同名的 argv 誤判 | PM 於主 repo 代執行 finish；此誤判已有 hook 提示與 template 條文，屬既知 |

**自檢結論**: 發現 2 項，C1 以更正記錄處置、C2 依既定協議處置（未涵蓋：文字品質 A 類——Solution 為技術記錄，未逐段掃三明示）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
新增測試：`uv run --project .claude/skills/ticket --extra test pytest .claude/skills/ticket/tests/test_create_version_scope_gate.py -q` → 24 passed。

全套回歸：`uv run --project .claude/skills/ticket --extra test pytest .claude/skills/ticket/tests/ .claude/skills/ticket/ticket_system/tests/ -q` → 4083 passed, 1 skipped, 5 deselected（142.61s）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
