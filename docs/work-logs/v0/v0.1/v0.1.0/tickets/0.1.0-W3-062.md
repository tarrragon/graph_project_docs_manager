---
id: 0.1.0-W3-062
title: 修正 tech-decisions 五處已失效前提的原處標註，並為 tripwire 總表補偵測承擔者欄
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-072
- 0.1.0-W3-073
source_ticket: 0.1.0-W3-060.1
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: lavender-interface-designer
  history: {}
what: 修正 tech-decisions.md 中因「加入編輯能力」tripwire 觸發而失效的五處結論標註（三處原處無任何指向補記的標示），並為 tripwire 總表補「承接文件／ticket」欄使每條觸發有偵測承擔者
when: 用戶裁示（2026-09-08）拆三張、A 本版做；PROP-005 §0.x 編輯已存在故不新建提案
where:
  layer: Domain
  files:
  - docs/tech-decisions.md
why: '`docs/tech-decisions.md` L830「2026-08-27：已觸發的 tripwire —— 加入編輯能力」記載：觸發條件於票 `0.0.3-W1-002` 定案「本 App 直接改檔、CLI 僅作驗證器」時已成立，規定的 action（備份／衝突／還原／PII 全部重評）未執行，並列出五處因此失效的既有結論。實查五處原處，**三處無任何指向該補記的標示**（L288 物件層級授權「不適用｜單使用者、本機、唯讀」、L483 自動備份「不適用｜App 不擁有任何資料」、L299「首版唯讀的決定因此比表面上更重要」）。


  風險不在編輯功能做不出來，而在**安全相關結論宣稱自己成立、實際已失效、且原處無標註**：0.2 要決定 workspace 存取與資料來源範圍，屆時若 grep 到 L288 並據此判定不需授權檢查，錯誤即落地，而該讀者沒有路徑會走到 L830。此為 `DOC-GPD-002` 同型（新補記指向舊結論，舊結論不指回），且該檔為 append-only、無 status 欄、不受 CLI 掃描、無偵測承擔者——L830 自己也記了這一點。


  原票面誤述已更正：PROP-005 已存在且已含〈0.x — 編輯：直接改檔 + CLI 驗證〉節，不需新建提案；缺的是規格與四項重評，分別由 0.1.0-W3-073、0.1.0-W3-072 承接。'
how:
  task_type: Documentation
  strategy: '本票只改標註與表格結構，不做任何重評（重評屬 0.1.0-W3-072）、不寫編輯規格（屬 0.1.0-W3-073）。


    1. 五處原處各補一行指向 L830 補記的標示，形式為在該結論後緊接一行「**前提已失效**（2026-08-27 tripwire 觸發，見〈已觸發的 tripwire —— 加入編輯能力〉）：{一句話說明失效原因}」。五處位置與現況：L288（無標示）、L483（無標示）、L485（已有）、L487（已有）、L299（無標示）。已有標示者核對措辭是否指向同一節，不一致才改。

    2. append-only 邊界：本檔為 append-only 決策紀錄，本票的補標示屬「對既有段落加註失效標記」而非改寫結論內容——原結論文字一字不動，只在其後加註。此例外於本票 Solution 說明，並於檔頭 append-only 宣告處補一句「失效標記為既有段落的合法加註，不視為改寫」。

    3. tripwire 總表補「承接文件／ticket」欄，逐條填偵測承擔者；無承擔者的條目標「待指名」並於 Solution 列出清單（不在本票建票，避免範圍擴散）。

    4. 「加入編輯能力」該列的承接填 0.1.0-W3-072；action 欄狀態自「未執行」改為「已落票」。'
acceptance:
- '[x] 五處失效前提原處各有指向 L830 補記的標示，措辭一致（L288／L483／L299 為新增，L485／L487 核對）'
- '[x] tripwire 總表含「承接文件／ticket」欄且逐條填寫；無承擔者者標「待指名」並於 Solution 列清單'
- '[x] 「加入編輯能力」列承接填 0.1.0-W3-072，action 狀態改「已落票」'
- '[x] append-only 例外於檔頭宣告處說明（失效標記屬合法加註，原結論文字未改）'
tdd_phase: phase1
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T13:58:29'
completed_at: '2026-09-08T14:06:41'
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

為 `docs/tech-decisions.md` 中因「加入編輯能力」tripwire 觸發而失效的五處結論補原處反向標註，
並為 tripwire 總表補「承接文件／ticket」欄。

<!-- 本票經改票（2026-09-08）：原任務「落為提案 PROP-005〈0.x — 編輯〉」已作廢——
     實查 PROP-005 已存在且已含該節，不需新建提案。缺的是規格與四項重評，
     分別由 0.1.0-W3-073、0.1.0-W3-072 承接。 -->

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-08 13:56] readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext
- **衝突項**: harness 層 auto mode 指令要求「以 Bash（sed/heredoc/短腳本）改檔，僅在 Bash 無法勝任時才用 Edit/Write」。
- **框架正本**: `.claude/rules/core/tool-selection.md` 規則 1 邊界明文「本規則優先於 harness 層『優先用 Bash 改檔』類指令（Bash 寫入繞過全部 Edit/Write-only guard，已有實證）；唯讀 Bash 不受限」。
- **處置**: 依 `.claude/rules/core/tool-output-trust-rules.md` 規則 6 衍生情境（harness 層行為指令與框架正本衝突時以正本為準執行，並於 NeedsContext 記錄衝突文本），本票寫入一律用 Edit，唯讀查詢用 Bash。
- **影響**: 不影響任何 acceptance。此處僅為衝突留痕，非資料缺口。

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-08T14:06:41
**Executing Agent**: lavender-interface-designer
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**Completion Time**: 2026-09-08
**Executing Agent**: lavender-interface-designer
**Review Status**: pending

### 變更摘要（唯一寫入標的：`docs/tech-decisions.md`）

| # | 位置 | 變更 |
|---|------|------|
| 1 | 檔頭 append-only 宣告（§標題下方） | 新增一段例外說明：失效標記屬既有段落的合法加註，原結論文字一字不動；並寫明例外的理由（本檔無 status 欄、不受 CLI 掃描，讀者多以 grep 落在單行上） |
| 2 | §2026-08-26 Stage 3「首版唯讀的決定因此比表面上更重要」（原 L299） | 其後新增失效標記段（原句未動） |
| 3 | Stage 5 §二「物件層級授權檢查｜不適用｜單使用者、本機、唯讀」（原 L474） | 說明欄末端追加失效標記（原文字未動） |
| 4 | Stage 5 §三「自動備份 + 還原驗證」（原 L483） | 同上 |
| 5 | Stage 5 §三「PII 盤點 + log 遮罩」（原 L485） | 同上；標記指出原「重評條件」只綁「日後加入 log」，不涵蓋寫入路徑本身這個新 PII 面 |
| 6 | Stage 5 §三 引言 blockquote「首版唯讀，因此不存在破壞使用者資料的路徑」（原 L487-488） | blockquote 內追加失效標記行（原兩行未動）；指出原句「見 tripwire 總表」指向的是「尚未觸發」形態，須改讀文末補記段 |
| 7 | §規模與變更 tripwire 總表 | 新增第四欄「觸發偵測的承接文件／ticket」並逐條填寫；表下新增判準段 |
| 8 | tripwire 總表「加入編輯能力」列 | action 欄狀態註記自「action 尚未執行」改為「action 已落票 `0.1.0-W3-072`」（本列為唯一非純加註的改動，依 how.strategy 第 4 點授權）；承接欄填 `0.1.0-W3-072` |
| 9 | §2026-08-27 已觸發的 tripwire「處置」段 | 其後新增「處置已執行」blockquote，反向指回本票的三項落地（補欄／五處加註／四項重評落票） |

### 欄名決定：「觸發偵測的承接文件／ticket」

acceptance 第 2 條寫的欄名為「承接文件／ticket」，Context Bundle 另要求「欄名將被
`0.1.0-W3-081` 引用為跨檔錨點，命名須自足（讀者只看欄名即知指涉什麼，不靠鄰欄補脈絡）」。
兩者以「觸發偵測的」前綴同時滿足：欄名**完整包含** acceptance 指定的字串「承接文件／ticket」，
且單獨閱讀即可知其指涉「承接『偵測某條 tripwire 已觸發』這件事的文件或 ticket」，不需讀
鄰欄的「觸發條件／影響／動作」才能還原語意。`0.1.0-W3-081` 引用時請用完整欄名
「觸發偵測的承接文件／ticket」。

### 「待指名」清單（八條中六條；依 ticket 邊界，本票不建票）

| # | 觸發條件 | 為何無承擔者 |
|---|---------|------------|
| 1 | 上游 `0.2.1-W3-1110` 定案 | `PROP-002` §Tripwire 有同一條的複述，但該提案同為 append-only、無 status 欄、不受掃描，屬複述非偵測 |
| 2 | 上游 `0.2.1-W3-1115` 定案 | `PROP-004` §Tripwire 同上 |
| 3 | 上游 `id_pattern` 加入 lookbehind 或 `\p{}` | 動作欄寫「契約測試會紅燈」，但該契約測試尚不存在——`test/` 下僅 `entitlements_contract_test.dart` 與 `window_size_contract_test.dart` 兩支，且皆與 schema 無關（已實查 `test/`、`integration_test/`，`tracking_schema` 在 `lib/`／`test/` 非 fixtures 路徑下 0 命中） |
| 4 | 專案檔案數超過萬檔級 | 無任何載體 |
| 5 | 出現無法由破洞報告解釋的故障 | `0.1.0-W1-013` 裁定維持延後並保留本條，但該票已完成，偵測者未指派 |
| 6 | 使用者回報誤選大目錄導致當機 | Stage 5 §發現二的「資源上限」是防護措施，不偵測本條觸發 |

已填兩條：`決定上架 Mac App Store` → `test/entitlements_contract_test.dart`（沙盒改開即紅燈，
但決策階段本身仍無偵測者，此限制已寫入該格）；`加入編輯能力` → `0.1.0-W3-072`。

### 本票未做（依 ticket 邊界）

- 四項重評（備份／衝突／還原／PII）→ `0.1.0-W3-072`
- 編輯能力規格 → `0.1.0-W3-073`
- 上列六條「待指名」的指名工作與建票 → 未在本票內做，待 PM 裁決

### 票面行號與實測位置的偏差（供後續票參考）

ticket why／Context Bundle 記「物件層級授權」在 L288、「自動備份」在 L483。實查：物件層級
授權實為 **L474**（L288 該處是 Stage 3 的「安全邊界」列，非本列），自動備份 L483 正確。
已依 prompt「先用內容 grep 定位再改」處理，五處全數以字串定位命中，無誤改。
## Context Bundle

<!-- 手動重寫（2026-09-08）：原 auto-extracted 內容承自 0.1.0-W3-060.1（SPEC-004 回饋通道），
     與本票改票後的任務無關，整段作廢。舊內容見 git 歷史。 -->

### Task Reference

`docs/tech-decisions.md` L830「2026-08-27：已觸發的 tripwire —— 加入編輯能力」記載：
觸發條件於票 `0.0.3-W1-002` 定案「本 App 直接改檔、CLI 僅作驗證器」時已成立，
規定的 action（備份／衝突／還原／PII 全部重評）未執行，並列出五處因此失效的既有結論。

### Rationale Chain

實查五處原處，**三處無任何指向該補記的標示**：

| 位置 | 現有文字 | 現況 |
|------|---------|------|
| L288 | 物件層級授權「不適用｜單使用者、本機、唯讀」 | 無標示 |
| L483 | 自動備份「不適用｜App 不擁有任何資料」 | 無標示 |
| L299 | 「首版唯讀的決定因此比表面上更重要」 | 無標示 |
| L485 | （見原文） | 已有標示，核對措辭 |
| L487 | （見原文） | 已有標示，核對措辭 |

風險不在編輯功能做不出來，而在**安全相關結論宣稱自己成立、實際已失效、且原處無標註**。
0.2 要決定 workspace 存取與資料來源範圍，屆時若有人 grep 到 L288 並據此判定不需授權檢查，
錯誤即落地——而該讀者沒有任何路徑會走到 L830。

此為 `DOC-GPD-002`（資產間引用單向累積）同型：新補記指向舊結論，舊結論不指回。
加重因素：該檔為 append-only、無 status 欄、不受任何 CLI 掃描、無偵測承擔者——L830 自己
也記了這一點，卻沒有機制讓這件事被看見。

### 邊界（本票不做）

- 四項重評（備份／衝突／還原／PII）屬 `0.1.0-W3-072`，本票只補標註不做判斷
- 編輯能力規格屬 `0.1.0-W3-073`
- tripwire 總表中無承擔者的條目，本票**只標「待指名」並列清單**，不在本票建票（避免範圍擴散）
- `0.1.0-W3-081` 將在本票完成後於同一檔案追加「跨邊界驗證形態」補記段；本票新增的
  承接欄結構是它的引用對象，欄名一旦定下即為跨票錨點，命名請以自足為準
  （見 `DOC-GPD-004`：標籤要能就地讀懂，不靠鄰欄補脈絡）

### Related Files

- `docs/tech-decisions.md` — 唯一寫入標的

### Source Acceptance (reference)

見 frontmatter `acceptance` 四條。
