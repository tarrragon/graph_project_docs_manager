---
id: 0.1.0-W2-003
title: 實作 Ticket 清單（SPEC-001 §4 全部狀態，含損壞疊加態）
type: IMP
status: completed
version: 0.1.0
wave: 2
priority: P1
parent_id: null
children: []
blockedBy:
- 0.1.0-W1-002
- 0.1.0-W1-005
- 0.1.0-W1-023
relatedTo:
- 0.3.0-W3-124
spawned_tickets:
- 0.1.0-W1-076
- 0.1.0-W1-077
- 0.1.0-W3-637
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: SPEC-001 §4 的6個狀態需要實作
  final_decision: 只交狀態渲染與退出路徑，不接真實資料
  rationale: 0.1 的整合測試與資料正確性解耦，因此後續每次加入邏輯時持續有效
who:
  current: parsley-flutter-developer
  history: {}
what: 實作 Ticket 清單（SPEC-001 §4）全部狀態列：專案未就緒／未載入（載入提示元件）／載入中／正常·列表／正常·主題／無 ticket／含損壞（疊加態）；以 W1-023 ticket fixture 驅動，不串真實資料
when: 路由殼完成後，與其他畫面票可並行
where:
  layer: Presentation
  files:
  - lib/screens/ticket_list/
why: 本畫面狀態數第二多，且含惰性載入的四個狀態（未載入／載入中／正常／含損壞）。FR-02 要求取消操作恆可用且可離開，該行為在 0.1 就要能走通
how:
  task_type: Implementation
  strategy: '依 SPEC-004 §3.6 逐狀態組合（狀態來源 SPEC-001 §4；頁面檔只從 lib/components/components.dart 匯入）：

    未載入：`LoadPrompt`（`ButtonRow`[`AppButton.primary` 開始載入, `AppButton.secondary` 返回]）。

    載入中：`LoadingState.progressBar`（已解析筆數 + 取消）。

    正常·列表：`Panel`[`Toolbar`（搜尋／篩選／排序）, `AppDataTable.virtual`[`AppTableRow.header`（`sortable`）, `AppTableRow.ticket` × N], `SplitRow.footer`（顯示範圍／總數／損壞數）]。

    正常·主題：`Panel.scrollable`[`Section.collapsible` × N（含未歸屬節，`dashedTop`）, `AppText.caption`]。

    無 ticket：`EmptyState.page`（動作導覽至破洞報告）。

    含損壞（疊加於正常態）：底層正常態 + `Toolbar` 右側 `IssueMarker.damagedDetail`（可點，`badge-tickets-corrupted`）+ 各損壞列末欄 `IssueMarker.damagedDetail`（圖示）。

    列表／主題切換用 `SegmentedControl`；退出路徑依 SPEC-003 §3.4 接 router.dart 既有 provider。寫不成上述句型的狀態即為缺件訊號：停手建元件票並回報，不就地自製。'
acceptance:
- '[x] 頁面檔的元件 import 只來自 lib/components/components.dart 統一匯出入口；頁面層不以原生佈局原語直接包兩個以上元件庫元件（grep 驗收）'
- '[x] 每個狀態的退出路徑依 SPEC-003 §3.4 可實際操作走通（含列表／主題切換與排序點擊）'
- '[x] widget test 逐狀態渲染一次並斷言 §3.6 指定的元件型別存在，含損壞態斷言 IssueMarker.damagedDetail 出現於工具列與損壞列（不重驗溢位）'
- '[x] SPEC-001 §4 全部狀態列皆可渲染，每列以 SPEC-004 §3.6 對應行的契約內元件組成；狀態由 W1-023 ticket fixture 驅動（含真實損壞樣本）；未載入由載入提示元件 LoadPrompt 承載（SPEC-003 §2.7／§2.11）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-09-21T12:58:13'
completed_at: '2026-09-21T13:20:02'
created: '2026-08-27'
updated: '2026-08-27'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實作 Ticket 清單的6個狀態

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 票面改定（PM，2026-09-15）

依 W3-335 閘門組修補（V4 第四輪通過、門檻外矛盾與已完成畫面落差已清）更新：狀態清單改引用 SPEC-001 現行版本（v1.16）該節全部狀態列，不寫死數目（reference-stability 規則 10）；解除 W3-335 封鎖。

- 現行規格：SPEC-001 v1.16、SPEC-003 v1.37、SPEC-004 v1.43
- 同步期新增的共用件可重用：graphBuiltProvider（專案未就緒判定，lib/app/graph_status.dart）、選定 UC 狀態（lib/app/selected_uc.dart），由 W3-379 建立
- 損壞跳轉含工具列徽章與各損壞列末欄標記 badge-tickets-corrupted-<ticketId>（SPEC-003 §3.4）

### 規格版本與缺件盤點

實作依 SPEC-001 v1.16 §4、SPEC-003 v1.37 §3.4、SPEC-004 v1.43 §3.6／§3.2 現行版本。逐狀態組合前先核對 how 句型與 SPEC-004 元件現況，發現兩處組合句型無法直接落地：

1. **`blockedBy` 欄無元件 slot**：SPEC-001 §4 現行版本的票列欄位為「ID／標題／狀態／優先／blockedBy／損壞標記」，但 `AppTableRow.ticket`（`lib/components/app_table_row.dart`）固定欄序仍為 ID／標題／狀態／優先／損壞標記（5 欄，SPEC-004 §3.2 `TicketListA` 票列契約亦同）。本票 `how` 欄的句型本身即依 SPEC-004 現行契約撰寫（不含 blockedBy），故依 `how` 句型實作、不新增第 6 欄；資料保留於 `TicketFixtureItem.blockedBy`，缺件記於 NeedsContext。
2. **`FilterDropdown` 缺 SPEC-003 §3.4 F3 測試錨點**：規格 F3／F4 要求選單為 `menu-tickets-filter-<key>`、選項為 `option-tickets-filter-<key>-<value>`；實測 `lib/components/filter_dropdown.dart` 的 `_buildOverlay`／`_buildOption` 未賦予任何對應 `Key`（只有觸發器有 `testKey`）。widget test 已改以 `Semantics.menuItem` 的 `label` 定位選項驗證篩選行為，但測試無法斷言規格明訂的錨點字面值。缺件記於 NeedsContext。

三份規格之間本次未發現互相矛盾之處（僅上述兩處為「元件契約落後 SPEC-001／SPEC-003 現行版本」）。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 實作摘要

新增 `lib/screens/ticket_list/`：
- `ticket_list_state.dart`：`TicketListState` sealed class（專案未就緒／未載入／載入中／無 ticket／正常，正常態以 `TicketsReady.mode` 分列表／主題，`corruptedCount` 為疊加計算）、`TicketFixtureItem` 資料模型（含 `blockedBy`／`topic`／`corrupted`）。
- `ticket_list_fixtures.dart`：固定假資料，`_corrupted` 三筆取自 `0.1.0-W1-023` 版本切片語料真實檔名（`test/fixtures/corpus/book_overview_app/docs/work-logs/README.md` 缺 frontmatter 樣本清單），非虛構。
- `ticket_list_providers.dart`：`ticketListStateProvider`（StateProvider，預設未載入）+ 純函式（`visibleListTickets` 搜尋／篩選／排序、`topicGroups` 主題分節排序、S8 比較邏輯）。
- `ticket_list_screen.dart`：`TicketListScreen`（七狀態切換）+ `TicketsHeaderTrailing`（列表／主題 `SegmentedControl`，接線至 `lib/app/shell.dart` 同 `DomainHeaderTrailing` 慣例）。

`lib/app/router.dart`／`lib/app/shell.dart` 各加一行接線（`buildDestinationPage` 分支、`SplitRow.header.trailing` 改 `switch`）。

### 已知範圍限制（0.1 決策延續）
- 未載入→載入中無自動完成轉場（同 `domain_view_screen.dart` `_LoadingView` 慣例，0.1 不接真實解析）；取消→回未載入為唯一已接線的轉場。
- 搜尋未實作 `Motion.searchDebounce` 防抖動（輸入即時過濾），功能正確但非規格逐字要求的時序；範圍決策同「只交狀態渲染與退出路徑」。

### 自檢結果
- `grep -rn "package:.*components/" lib/screens/ticket_list | grep -v components.dart` → 0
- `fvm dart analyze lib test` → No issues found
- `fvm flutter test test/ --reporter compact` → 1389 passed, 0 failed
- Phase 4 結論：程式碼品質 A；未發現需重構之技術債務（沿用 domain_view／gap_report 既有 provider＋純函式分層慣例，狀態機與渲染分離）；無需重構。

### how 欄記載缺口註記（PM，2026-09-21，來源 0.1.0-W3-637 差集項 #7（專案未就緒））

本票 how 欄的組合句型未記載上列狀態，實作依 what 欄的狀態清單完成且正確。0.1.0-W3-637 的三軸重判判定為「how 欄記載缺口」——實作與測試皆成立，只有票面 how 未寫。

**不回填 how 欄**（PM 裁示）：how 欄記錄的是派發當時 PM 給代理人的指示。完成後編輯它會使記錄失真——代理人從未看過回填的文字，日後考古會得出「代理人未依指示實作」的錯誤結論。本註記即為該缺口的正式記錄，差集表全文見 0.1.0-W3-637 的 Solution。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
- `fvm dart analyze lib test`：No issues found（0 issue）
- `fvm flutter test test/ --reporter compact 2>&1 | tail -3`：1389 passed, 0 failed（含新增 `test/unit/screens/ticket_list_test.dart` 29 案例，涵蓋七狀態列、退出路徑、列表/主題切換、排序三態循環、搜尋/篩選、含損壞疊加）
- `grep -rn "package:.*components/" lib/screens/ticket_list | grep -v components.dart`：0（頁面層元件 import 僅來自 components.dart）

## NeedsContext
### 缺件 1：`blockedBy` 欄無元件 slot
- **缺失項**：`AppTableRow.ticket`（`lib/components/app_table_row.dart`）固定欄序為 ID／標題／狀態／優先／損壞標記（5 欄），無 blockedBy slot。
- **觸發位置**：`lib/components/app_table_row.dart` `ticketColumns`／具名建構子 `AppTableRow.ticket`。
- **影響**：SPEC-001 §4 現行版本票列欄位含 blockedBy，本票依 SPEC-004 §3.6／§3.2 現行元件契約（不含 blockedBy）實作，blockedBy 值保留於 `TicketFixtureItem.blockedBy` 但不渲染。
- **建議補料**：開元件票擴充 `AppTableRow.ticket` 增加第 6 欄（blockedBy），並同步 SPEC-004 §3.2／§4.35 欄規格與 §3.6 契約行；本畫面票落地後直接可用（資料模型已備妥）。
- **重派成本**：中（元件票 + 本畫面接線，約 1 個 Wave 內）。

### 缺件 2：`FilterDropdown` 缺 SPEC-003 §3.4 F3 測試錨點
- **缺失項**：`lib/components/filter_dropdown.dart` 的 `_buildOverlay`／`_buildOption` 未賦予 `menu-tickets-filter-<key>`／`option-tickets-filter-<key>-<value>` 對應 `Key`（僅觸發器有 `testKey`）。
- **觸發位置**：`lib/components/filter_dropdown.dart:_buildOverlay`、`_buildOption`。
- **影響**：widget test 無法以規格明訂錨點斷言選單／選項存在，已改用 `Semantics.menuItem` label 定位驗證功能，但整合測試若依規格錨點斷言會落空。
- **建議補料**：開元件票補上兩個錨點（`menu-tickets-filter-<key>` 於 overlay 根節點、`option-tickets-filter-<key>-<value>` 於各選項節點），`<key>` 由呼叫端傳入或以 `widget.testKey` 衍生。
- **重派成本**：低（單一元件檔小幅修改）。

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-21T13:20:02
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-002,0.1.0-W1-005,0.1.0-W1-023,0.1.0-W3-124 | chars: 1438 -->

### Task Reference
- 0.1.0-W1-002 what: 建立 六項導覽的路由殼（來源已完成，結論見 Solution）
- 0.1.0-W1-005 what: 建立 跨畫面共用的元件庫（來源已完成，結論見 Solution）
- 0.1.0-W1-023 what: 抽取 Ticket 節點檔為測試 fixture（版本切片策略）（來源已完成，結論見 Solution）
- 0.1.0-W3-124 what: 實作 Ticket 清單搜尋防抖於 provider 層

### Rationale Chain
- 0.1.0-W1-002 why: SPEC-001 §1-7 界定六個畫面加一個浮層，共 29 個狀態。各畫面若自帶導覽，切換行為會分歧。本票交出可切換的殼，讓後續每張畫面票只需關心自己的內容。（來源已完成，結論見 Solution）
- 0.1.0-W1-005 why: SPEC-001 的 29 個狀態中，空狀態出現 6 次、阻擋狀態 3 次、載入態 3 次、損壞標記 3 次——同一個概念在七個畫面各自實作會產生七種樣子，而 0.1 的整合測試斷言的正是「每個狀態都畫得出來」，樣子不一致時無從判斷哪個才是對的。（來源已完成，結論見 Solution）
- 0.1.0-W1-023 why: 0.1.0-W1-003 已定案 ticket 規模處置採選項 B（版本切片），但排除於該票範圍（acceptance 明確不含 work-logs），需獨立子任務決定切哪個版本並落地（來源已完成，結論見 Solution）
- 0.1.0-W3-124 why: 0.1.0-W3-110 依用戶裁示自 SearchField 元件層移除防抖後，Motion.searchDebounce 這個 300 ms 時間契約暫時沒有消費者；承擔者須為服務層，而該層隨 Ticket 清單畫面才會出現

### Related Files
- lib/app/router.dart  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- lib/app/shell.dart  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- lib/main.dart  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- integration_test/app_test.dart  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- lib/l10n/app_zh.arb  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
  ... (truncated, see source ticket)
- lib/components/  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- test/unit/components/  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- test/fixtures/corpus/ticket_slice/::read  # from 0.1.0-W1-023  [路徑已不存在]（來源已完成，結論見 Solution）
- lib/screens/tickets/  # from 0.1.0-W3-124  [路徑已不存在]
- lib/components/search_field.dart  # from 0.1.0-W3-124

### Source Acceptance (reference)
- [ref] [x] 導覽列為六項，與 PROP-004 的畫面清單逐項對應  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- [ref] [x] 六項皆可切換且各自渲染標示畫面名的佔位頁  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- [ref] [x] 側欄頂端顯示專案名，點擊有回應（浮層內容可為空殼）  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- [ref] [x] 四種視窗尺寸下導覽列與內容區皆不溢位  # from 0.1.0-W1-002（來源已完成，結論見 Solution）
- [ref] [x] lib/components/ 為統一匯出入口（barrel）；SPEC-004 §3.1 定案的元件 26、容器 16 皆自該入口匯出；頁面層元件 import 只來自該入口  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- [ref] [x] 元件內零裸值：lib/（tokens/ 除外）grep 不到色碼、尺寸、字級字面值，亦 grep 不到 Duration(milliseconds: / Duration(seconds: 後接字面數字  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- [ref] [x] 41 張子票（Wave A 原子與文字 / B 容器基礎 / C 狀態與殼層 / D 資料視圖）全部 complete，各自 acceptance 為 SPEC-004 條目投影與測試契約  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- [ref] [x] shell.dart 漂移對齊：_kSidebarWidth 改 LayoutSize.sidebarWidth、_NavItem 改引用 NavItem、_ProjectSwitcherEntry 改 SwitcherOverlay、標題列依 4.27；既有測試錨點不改名  # from 0.1.0-W1-005（來源已完成，結論見 Solution）
- [ref] [x] 已決定並記錄版本切片標準（切哪個來源專案的哪個版本目錄）；Ticket 節點檔已入庫為 fixture 且標明來源版本區間；fixture 涵蓋已知的真實壞 YAML 樣本  # from 0.1.0-W1-023（來源已完成，結論見 Solution）
- [ref] [ ] 搜尋過濾於 provider 層以 Motion.searchDebounce 延遲觸發，SearchField 不含 Timer  # from 0.1.0-W3-124
- [ref] [ ] 清空輸入立即還原，不等待防抖  # from 0.1.0-W3-124
### 派發要點（PM 於 2026-09-03 依已派發的 W2-004／006／007 prompt 固化，供 context 清除後直接派發）

- 本票畫面目錄：`lib/screens/ticket_list`；派發條件：W1-023 fixture 入庫後派。
- 起始：claim 本票；讀本票 how（已是逐狀態組合句型，照句型組裝不臨場設計）、SPEC-001 對應 §、SPEC-003 對應 §、SPEC-004 §3.6 對應行與所用元件條目；讀 `lib/app/router.dart`／`shell.dart`（`AppDestination`、`selectedDestinationProvider`／`returnToProvider`／`visitedDestinationsProvider`）、`lib/components/components.dart` 匯出清單、`lib/workspace/workspace_repository.dart`（`WorkspaceState`）。
- 結構：畫面狀態以 sealed class 表示並由 Riverpod provider 供給；fixture provider 讀 `test/fixtures/corpus/`（W1-003；ticket 樣本由 W1-023 入庫）；頁面檔元件 import 只來自 `lib/components/components.dart`；`lib/app/` 只加本目的地的一行接線，不重排、不改其他目的地。
- 缺件出口：寫不成 how 句型的狀態時停手，在 Problem Analysis 記錄缺什麼元件／slot 並回報 PM；不就地自製、不改 `lib/components/`。
- 測試：`test/unit/screens/ticket_list_test.dart` 逐狀態渲染並斷言 §3.6 指定元件型別存在、退出路徑操作後 provider 狀態改變；前台執行 `fvm flutter test test/ 2>&1 | tail -3` 全綠、`fvm dart analyze lib test 2>&1 | tail -1` 0 issue；`grep -rn "package:.*components/" lib/screens/ticket_list | grep -v components.dart` 為 0；禁用 run_in_background；不得留下 debug 測試檔。
- 收尾：append-log Problem Analysis / Solution（必含 `### 自檢結果` 子章節與 Phase 4 結論）/ Test Results；check-acceptance 逐項；狀態轉換子命令在 worktree sandbox 被擋屬預期，回報最後 commit hash 由 PM 代執行；回報前 `git status --porcelain` 為空。
- 合併：PM 在 worktree 分支 merge main（barrel／lib/app 接線衝突取聯集）後 --ff-only 落地，代執行狀態轉換後移除 worktree。
