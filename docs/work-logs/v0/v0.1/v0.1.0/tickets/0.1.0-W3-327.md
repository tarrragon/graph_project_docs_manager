---
id: 0.1.0-W3-327
title: 補齊 _KEYWORD_VALUE_STOP_FLAGS 白名單與已註冊旗標的差集，並加自檢
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-325
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-325 的 L1：白名單缺 7 個已註冊旗標，判準為機械可自檢
  final_decision: 獨立成票且可當下寫定修法，不與 L2 合併
  rationale: 三問皆過（消費端即逃逸函式已觀測、控制組實跑過、影響面恰 2 處已量）；與 L2 合併會讓 L2 的未定狀態阻擋 L1
who:
  current: thyme-python-developer
  history: {}
what: 補齊 _KEYWORD_VALUE_STOP_FLAGS 白名單與已註冊旗標的差集，並加自檢
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/scripts/section_comment.py
  - .claude/skills/framework-issue/tests/test_section_comment.py
why: '**`_KEYWORD_VALUE_STOP_FLAGS` 的註解逐字寫「集合僅列本檔實際註冊的旗標」，而實作不滿足該契約。**


  **PM 實測**：白名單 12 個成員（`-h --help --owner --sections-file --content-file --summary --session --comment-threshold --stale-days --name --to --force`）；已註冊旗標中缺漏 **7 個，全部是 `todo` 子命令的**——`--all --consumer --issue --json --priority --stage --status`。反向差集為空。


  **機制不是 argparse 的 `nargs`**（該歸因為 PM 先前的誤述，已更正）：純 argparse 對未註冊的 `-` 前綴 token 回 exit 2。靜默吸收需要兩層自建加固——`_escape_dash_prefixed_keyword_values`（行 1690）為 `-` token 加不可見前綴使 argparse 看不到它，以及關鍵字經 `--` 傳給 gh 使被吸收的 token 成為合法搜尋詞。兩層都是為了讓字面含 `-` 的關鍵字可用而加。


  **影響面恰 2 處**（`0.1.0-W3-325` 實測）：`dedup --keywords` 與 `init --dedup-keywords`；其餘 7 個子命令未知旗標一律 exit 2；`.claude/` 其他 23 處 `nargs` `+`／`*` 無此前處理，無 `parse_known_args`。**`init` 較嚴重**——同一缺陷在 GitHub 寫入路徑上，查重範圍靜默縮小而區段與 body 索引照樣建立。


  **為何本票可當下寫定修法**（依 `escalation-flow.md` 三問）：消費端已觀測（白名單的消費者即該逃逸函式，單一且可讀）；反例已試（`0.1.0-W3-325` 實跑控制組——其餘子命令與其他 CLI 皆 exit 2）；範圍已量（恰 2 處，命令可複驗）。


  **本票不涵蓋**：錯字與字面值的歧義（補齊白名單後仍在，且有測試釘住現行契約）、E1／E2 互指缺口。兩者屬 `0.1.0-W3-325` 的 L2／L3，另票承接——合併會讓未定狀態阻擋本票。'
how:
  task_type: Implementation
  strategy: '1. **補齊白名單**至「已註冊旗標扣除 `_MULTI_VALUE_KEYWORD_FLAGS` 兩者」。

    2. **加自檢使該契約機械成立**：啟動時（或測試中）比對白名單與 argparse 實際註冊的旗標集合，差集非空即拋錯。**契約寫在註解裡而無檢查，正是本次漂移的成因**——只補成員不加檢查則下次新增旗標會再漂一次。

    3. **報告的訊號位置一併處置**：`0.1.0-W3-325` 實測「查詢失敗略過的 token 數：0」在最後一行**主動證明沒有缺口**，而唯一訊號（關鍵字回音）在第一行——`| tail` 會切掉它。使被吸收的 token 在**最後一行**可見，或使 `skipped` 計數反映它。

    4. **測試**：每一個新補的旗標各有一條斷言其不被吸收；自檢的差集非空路徑有測試；報告訊號位置有斷言。


    **不動 `_escape_dash_prefixed_keyword_values` 的既有能力**——字面含 `-` 的關鍵字仍須可用，`0.1.0-W3-325` 指出的那條既有測試（`test_cmd_dedup_cli_accepts_hyphen_prefixed_keyword_group_end_to_end`）**屬 L2 的裁決範圍，本票不得改它也不得使其紅燈**；若補齊白名單會使該測試紅燈，停手回報而非改測試。'
acceptance:
- '[ ] 白名單等於已註冊旗標扣除兩個 multi-value 旗標，差集為空'
- '[ ] 自檢已加：差集非空即拋錯，且該路徑有測試'
- '[ ] 被吸收的 token 在報告最後一行可見，或 skipped 計數反映它，並有斷言'
- '[ ] 每個新補旗標各有一條不被吸收的斷言'
- '[ ] 既有的字面含 - 關鍵字能力未受損；若 L2 的那條既有測試因本票紅燈，已停手回報而非改測試'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-12'
updated: '2026-09-12'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補齊 _KEYWORD_VALUE_STOP_FLAGS 白名單與已註冊旗標的差集，並加自檢

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-325 | chars: 873 -->

### Task Reference
- 0.1.0-W3-325 what: 判定 dedup 靜默吞多餘旗標使錯誤與正常不可區分的影響範圍（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-325 why: **現象**：`section_comment.py` 的 `dedup` 子命令只有 `--keywords`（`nargs="+"`），多給一個不存在的旗標時**不報錯**——該旗標被當成第二個查重關鍵字，rc=0。

**PM 獨立複核**：`python3 section_comment.py dedup --help` 的旗標僅 `--keywords`；`--all` 屬 `todo` 子命令；`dedup --keywords zzz --all` 實跑 **rc=0**。

**後果是可觀測面上的不可區分**：查重跑完、退出碼 0、有輸出——**與一次正確的查重完全同形**。使用者以為查了 `--all` 的範圍，實際查的是 `zzz` 與 `all` 兩個關鍵字。`0.1.0-W3-307` 的構造三正是踩在這裡：它以「實跑 `dedup --help`，`--all` 存在」作為「反例已試」的依據，而被證偽的是「某處存在」不是承重前提「`dedup` 存在」。

**形態歸屬**：`test-assertion-design-rules` 規則 E2（守衛的失效方向是放行，而放行與「輸入本來就沒問題」在輸出上同形）。該規則與本檔互指仍為 0／0。

**為何 P1**：`dedup` 是 framework-issue 流程的第一個動作，且 CLAUDE.md 的免徵詢授權以「已跑 dedup」為生效條件之一——**一次被靜默縮小範圍的 dedup 會使該授權的前提失真而無人察覺**。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 全部子命令的 nargs 吞旗標面已掃過，非只 dedup 一處  # from 0.1.0-W3-325（來源已完成，結論見 Solution）
- [ref] [x] 歷史上是否有範圍失真的 dedup 結果已查，無跡者記為未知  # from 0.1.0-W3-325（來源已完成，結論見 Solution）
- [ref] [x] 與 test-assertion-design-rules 規則 E2 的關係已判定，互指為 0 是否構成缺口一併判  # from 0.1.0-W3-325（來源已完成，結論見 Solution）
- [ref] [x] dedup 已跑，兩條 AND 判準已逐條套用並留痕；Solution 未寫入修法形狀  # from 0.1.0-W3-325（來源已完成，結論見 Solution）

