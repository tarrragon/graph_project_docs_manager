---
id: 0.1.0-W3-093
title: 補上 AppShell.didChangeAppLifecycleState 缺生命週期 log，實機驗證無從觀測狀態轉換
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-078
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 實機驗證所需的觀測點不存在
  final_decision: IMP 補生命週期回調 log
  rationale: observability 規則 5 明列生命週期回調為必須記錄的平台互動點；缺 log 同時是規則違反與驗證阻礙
who:
  current: parsley-flutter-developer
  history: {}
what: 補上 AppShell.didChangeAppLifecycleState 缺生命週期 log，實機驗證無從觀測狀態轉換
when: 0.1.0-W3-078 實機驗證進行中（2026-09-08），缺此 log 使四項平台事實中的生命週期對應無從觀測
where:
  layer: Presentation
  files:
  - lib/app/shell.dart
why: "`lib/app/shell.dart:63` 的 `didChangeAppLifecycleState` 只寫入 provider，**沒有任何 log**：\n\n    void didChangeAppLifecycleState(AppLifecycleState state) {\n      ref.read(appLifecycleStateProvider.notifier).state = state;\n    }\n\n`.claude/rules/core/observability-rules.md` 規則 5 的表格明列「生命週期回調——`didChangeAppLifecycleState` 中涉及平台資源操作」為必須記錄的平台互動點（級別 debug）。此處寫入的 provider 正是系統通知觸發條件 (a) 的判斷依據（SPEC-003 §2.2：完成當下 `AppLifecycleState` 非 `resumed` 才發通知），屬平台資源決策路徑。\n\n規則 5 的立論在此完全成立：**mock 測試環境不走真實平台 API，日誌缺失在測試階段不會被發現，實機才暴露盲區**。`test/` 下的 fake 直接設定 provider 值，從不經過這個回調，因此沒有任何測試會察覺它不發聲。\n\n直接後果：`0.1.0-W3-078` 要驗證的四項平台事實之一是「macOS 上 `AppLifecycleState` 的 `inactive` / `hidden` 各對應何種視窗狀態（失焦／最小化）」。該對應只有實機能測，而實機唯一的觀測管道就是這個 log——它不存在，驗證即無從進行。\n\n`MacosScanNotifier` 的授權查詢／請求／發送／撤回都有 `developer.log`（同檔 doc comment 明寫依 observability 規則 5 辦理），唯獨這個回調沒有。缺的不是慣例，是單點遺漏。"
how:
  task_type: Implementation
  strategy: '在 `didChangeAppLifecycleState` 內以 `developer.log` 記錄狀態轉換，內容須含**轉換前後兩個值**（只記新值無法區分 `inactive → resumed` 與 `hidden → resumed`，而 0.1.0-W3-078 要驗的正是不同轉換路徑的對應關係），並帶元件標籤。


    沿用同專案既有慣例：`dart:developer` 的 `developer.log`，`name: _tag`，字串標 `i18n-exempt`（開發者診斷字串非使用者可見文字）。參考 `lib/services/macos_scan_notifier.dart` 的寫法。


    級別依 observability 規則 5 表格為 debug。


    `AppShell` 目前是否已有 `_tag` 常數需確認；無則新增，命名與既有服務類一致。


    測試：`test/` 現有 widget 測試不經過此回調，本票不強求為 log 新增測試——但須確認既有測試全綠未受影響（`fvm flutter test`）。'
acceptance:
- '[ ] didChangeAppLifecycleState 以 developer.log 記錄轉換，內容含轉換前後兩個狀態值與元件標籤'
- '[ ] 級別為 debug，沿用專案既有的 dart:developer + name 標籤慣例，字串標 i18n-exempt'
- '[ ] fvm flutter test 全綠（既有測試不經過此回調，確認未受影響即可）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-08'
updated: '2026-09-08'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補上 AppShell.didChangeAppLifecycleState 缺生命週期 log，實機驗證無從觀測狀態轉換

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-078 | chars: 524 -->

### Task Reference
- 0.1.0-W3-078 what: 實機驗證 MacosScanNotifier 四項平台事實並回填 SPEC-003

### Rationale Chain
- 0.1.0-W3-078 why: W3-064 acceptance 2、3 承接，平台事實只有實機能驗

### Related Files
- docs/spec/ui/SPEC-003-interaction-response.md  # from 0.1.0-W3-078
- lib/services/macos_scan_notifier.dart::read  # from 0.1.0-W3-078
- macos/Runner/AppDelegate.swift::read  # from 0.1.0-W3-078

### Source Acceptance (reference)
- [ref] [ ] 實機測得 UNUserNotificationCenter 在沙盒關閉（PROP-001）+ Developer ID 簽章下的可用性，並記錄 debug build 未簽章時授權請求的實際回應（是否直接回錯）  # from 0.1.0-W3-078
- [ref] [ ] Flutter 端載體的選擇已定案並附實測依據（原生 channel 或第三方套件，二擇一）  # from 0.1.0-W3-078
- [ref] [ ] 實機測得 macOS 上 AppLifecycleState 的 inactive / hidden 各對應何種視窗狀態（失焦／最小化）  # from 0.1.0-W3-078
- [ref] [ ] 實機測得撤回已送達通知的 API，以及它對「通知中心已收合」狀態的效果  # from 0.1.0-W3-078
- [ref] [ ] 四項實測結果已回填 SPEC-003 §2.2「實作票驗證」段（原文改為實測記錄，附量測環境與日期）  # from 0.1.0-W3-078
  ... (truncated, see source ticket)
