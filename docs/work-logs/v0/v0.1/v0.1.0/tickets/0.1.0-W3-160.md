---
id: 0.1.0-W3-160
title: 接上 破洞掃描的真實資料源（取代 fixture 常數）
type: IMP
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-096
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-096 修復完成時機時發現 fixture 常數本身仍無真實耗時
  final_decision: IMP 延後至真實資料源接上，本票排 pending（P2）
  rationale: 0.1 明訂不接真實資料（W2-001 等票既定範圍），此為 0.1 之後的既定延伸工作，本票承接 W3-096 的 SR-1 spawn request
who:
  current: 待派發
  history: {}
what: 破洞掃描接上真實資料源（取代 gap_report_provider.dart 的 _missingFrontmatterItems fixture 常數），使掃描具真實耗時
when: 0.1 之後的 wave 排入真實資料串接時
where:
  layer: Application
  files:
  - lib/screens/gap_report/gap_report_provider.dart
  - docs/spec/ui/SPEC-003-interaction-response.md
why: SPEC-003 §2.2 系統層通知整節與 §3.5 動畫提示表三列，其可觀察性皆繫於掃描期間存在；0.1.0-W3-096 已修復完成時機的最短存續（Motion.spinnerMinVisible），但 fixture 常數本身仍無真實耗時，兩節規格描述的仍是無法被使用者實際體感的情境。0.1.0-W3-078 四項平台事實中的第 1、2、4 項亦需通知真的送出才能實機驗證，目前無承接票可指名
how:
  task_type: Implementation
  strategy: 解析真實 repo 內文件的 frontmatter 缺失狀態，取代常數列表；掃描完成時機沿用 0.1.0-W3-096 已建立的『與 Motion.spinnerMinVisible 取兩者較長者』機制（而非疊加）
acceptance:
- '[ ] 破洞掃描結果由真實檔案系統掃描產生，非硬編碼常數'
- '[ ] SPEC-003 §2.2 系統層通知與 §3.5 骨架/cross-fade 三列的現階段可達性標注已移除或更新為可達'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

破洞掃描接上真實資料源（取代 gap_report_provider.dart 的 _missingFrontmatterItems fixture 常數），使掃描具真實耗時

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-096 | chars: 1102 -->

### Task Reference
- 0.1.0-W3-096 what: 修復 破洞掃描以 microtask 完成，使掃描中骨架與系統通知觸發條件皆結構性不可達

### Rationale Chain
- 0.1.0-W3-096 why: 實機執行時，用戶按下重新掃描後**沒有授權對話框、沒有任何載入提示或動畫**，`flutter run` 的 runtime log 自 App 啟動起零輸出（`MacosScanNotifier` 的授權查詢 log 從未觸發）。

機制（PM 已讀原始碼確認）：`gap_report_provider.dart` 的 `_scheduleScan()` 是 `Future.microtask(_completeScan)`——掃描在**同一幀的 microtask 內完成**。由此推出兩件事：

1. **`state-gaps-scanning` 骨架存在時間短於一幀**，畫面上不可能看到。SPEC-003 §3.5 明訂「重新掃描時現有結果立即被骨架取代」，該骨架實際從未被繪出。
2. **系統通知的觸發條件結構性不可達**。SPEC-003 §2.2 的觸發條件是「完成當下視窗非前景，或可見頁非 `nav-page-gaps`」。完成發生在按鈕點擊的同一個 microtask 內，而按鈕點擊必然發生在視窗前景且使用者正看著破洞報告頁——**兩個條件都不可能為真**。授權因此永不請求（`notDetermined` 僅於首次觸發時請求），通知永不發送。

後果分三層：

- `0.1.0-W3-078` 要驗的四項平台事實中，至少三項（授權可用性、未簽章 build 的授權回應、撤回 API 效果）**在此實作下無法以任何操作觸達**，實機驗證無從進行。
- SPEC-003 §2.2 整節（觸發條件、不發送情境、權限 gate 三路徑、撤回三時機）與 §3.5 的骨架列，在現行實作下皆為**描述了一個不會發生的情境**的規格。
- `test/` 的 fake 直接設定狀態與 provider 值，繞過真實的完成時機，因此沒有任何測試會察覺這件事——與 `0.1.0-W3-094`（固定高度裁切）同屬「測試綠燈但實機不成立」。

**這不是說掃描應該變慢**。真實掃描（讀取檔案、解析 frontmatter）本來就需要時間；問題在於現行實作以 fixture 常數取代掃描，把耗時降為零，順帶讓所有依賴「掃描期間」的規格行為失去存在空間。

### Related Files
- test/unit/screens  # from 0.1.0-W3-096

### Source Acceptance (reference)
- [ref] [ ] SPEC-003 §2.2 與 §3.5 骨架列已標注現階段可達性，或已改為可達  # from 0.1.0-W3-096
- [ref] [ ] 0.1.0-W3-078 四項平台事實的實機驗證路徑已明確（解開死結或標示阻擋來源並設 blockedBy）  # from 0.1.0-W3-096

