---
id: 0.1.0-W3-322
title: 判定 interaction-feedback.md 超標的拆法，或判定不拆
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-317
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-317 處置四 skill 時測得該 reference 6087 tokens 超標，但主題接縫穿過兩張共用查表
  final_decision: 不在 317 內硬拆，另票先量測選段查閱的實際成本再判定拆或不拆
  rationale: 照主題硬拆會讓兩張共用查表複製或重新切分，兩者都使讀者查一個反模式時不知該開哪一檔；拆過頭與塞太滿違反同一條判準
who:
  current: rosemary-project-manager
  history: {}
what: 判定 interaction-feedback.md 超標的拆法，或判定不拆
when: 隨時
where:
  layer: Presentation
  files:
  - .claude/skills/ux-design-evaluation/references/interaction-feedback.md
why: '`0.1.0-W3-317` 處置四個相鄰 skill 時，`ux-design-evaluation/references/interaction-feedback.md` 實測 **6087 tokens，超過每檔 5000 的判準**（該判準對「整份執行」與「選段查閱」兩種讀取方式同為 5000）。317 未處置它，理由不是遺漏而是**看得見的接縫切不乾淨**：


  該檔十一節的體量分布為——核心原則 106／三層回饋模型 436／時間門檻與回饋策略 716／**按鈕級回饋 1470**／畫面級回饋 464／Spinner vs Skeleton 438／結果通知的形式選擇 920／**反模式速查 729**／檢查清單 407／參考來源 161。


  主題確實有兩群，且該 skill 的舊 description 自己就用冒號把它們分開寫過：「回饋分層、時間門檻、spinner vs skeleton、SnackBar／Dialog／Banner 選擇」是一群，「元件語意與版面：切換標籤／可點性辨識／選中態對比／溢出 affordance／版面保障／完成宣告」是另一群。**但第二群不是獨立章節**——它是〈按鈕級回饋〉裡的一個子節（`**元件語意與版面檢查**`），而兩群共用同一張〈反模式速查〉表與同一份〈檢查清單〉。


  **不做則後果**：照主題硬拆，兩張共用查表必須複製或重新切分——複製會產生兩份會各自漂移的表，重新切分會讓讀者查一個反模式時不知道該開哪一檔。這正是「拆過頭與塞太滿違反的是同一條判準」所指的另一端。維持現狀則該檔持續超標 22%，且**選段查閱型檔案超標的實際危害尚未被量測過**——判準對兩種讀取方式同為 5000，是預設值而非實測值。


  **為何 P2**：超標為 22%，且該檔的讀取方式是跳到單一節，不是整份載入；`0.1.0-W3-317` 的探針對 references 未直接量測，沒有證據顯示現況已造成讀者失效。'
how:
  task_type: Analysis
  strategy: '先判定要不要拆，再談怎麼拆。**不得預設「超標就得拆」**。


    1. **量測選段查閱的實際成本**：對該檔跑一批讀者探針（同 `0.1.0-W3-317` 的 2-B⁗ 形態，門檻派發前寫定），問「你要查『選中態看不到字』該讀哪一節、你需要讀幾節才夠」。若讀者能一次跳到對的一節而不需通讀，超標的實際危害就與整份執行型不同，這是判「不拆」的正當依據，須以量測而非推論支撐。

    2. **若判定要拆，接縫必須先解決兩張共用查表的歸屬**，三個選項各自評估：(a) 反模式速查與檢查清單留在其中一檔、另一檔指過去；(b) 兩張表各自按群切分；(c) 兩張表獨立成第三檔。**選 (b) 前須先確認每一列真的只屬一群**——逐列判定，不得目測。

    3. **若判定不拆**，須在該檔檔頭寫明它超標與為何仍不拆，並回填 `0.1.0-W3-317` 的結論，避免下一輪審查重新發現同一件事又重走一次。


    本票**不得為達標而刪內容**。`ux-design-evaluation` 的 SKILL.md 本身為 2729 tokens，有餘裕，但把 reference 的內容搬進正文不是合法解——正文放操作手冊、references 放依據，搬運會違反該分工。'
acceptance:
- '[ ] 已跑讀者探針量測選段查閱的實際成本，門檻於派發前寫定'
- '[ ] 拆或不拆的判定有量測支撐，非以「超標」單一事實推得'
- '[ ] 判定拆時，兩張共用查表的歸屬已逐列判定而非目測'
- '[ ] 判定不拆時，該檔檔頭已寫明超標事實與不拆理由，並已回填 0.1.0-W3-317'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

判定 interaction-feedback.md 超標的拆法，或判定不拆

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-317 | chars: 1639 -->

### Task Reference
- 0.1.0-W3-317 what: 審查 元件庫相鄰四 skill 的正文可讀性與互指對稱性

### Rationale Chain
- 0.1.0-W3-317 why: `component-contract-design` 已於 `0.1.0-W3-296`／`0.1.0-W3-302` 走完探針審查與依步驟重切，其〈與相鄰資產的交界〉表指名四個相鄰 skill。用戶裁示：**這四個相鄰 skill 要做同樣的審查，確保同樣的脈絡與可讀性，必要時補連結。**

**派發前的靜態量測已顯示問題不是假設**：

互指矩陣（各 SKILL.md 內指名對方的次數）嚴重不對稱——`component-contract-design` 指向其餘四者（3／3／1／2），但 `ux-design-evaluation`→foundation／dart／version 為 0／0／0，`dart-style-guardian`→foundation／ux／version 為 0／0／0，`version-bootstrap`→foundation／ux／dart 為 0／0／0。其中兩處是明確的單向斷裂：`foundation-design` 五處寫「交回 `version-bootstrap`」而後者零處提及前者；`foundation-design` 的載體表列「執法工具」一類、工作流第 5 步要求「接入執法載體」，卻從未指名 `dart-style-guardian`。

體量硬違規兩處：`version-bootstrap` SKILL.md 5132 tokens **超過 L2 的 5000 上限且零 references**；`ux-design-evaluation` description 989 字元，為 250 字元上限的四倍（`dart-style-guardian` 260 字元亦略超）。

孤兒 reference 一處：`dart-style-guardian/references/i18n-guidelines.md`（1507 tokens）在該 skill 的 SKILL.md 內入站連結數為 **0**，無任何管道把讀者導向它。

**不做則後果**：這四個 skill 是元件庫程序的上下游。讀者從 `component-contract-design` 被導過去之後，落在一份沒有〈按需讀取〉路由、沒有交界節、且指不回來的正文裡——`component-contract-design` 這次修好的正是這個形態，只修一端等於在交界處重新製造它。`version-bootstrap` 的超標尤其不能用搬運處理：它零 references 且正文含三件不同觸發條件的事（6 步 pipeline、移版硬耦合盤點 SOP、與早期手動流程對照），依 SRP 應先判斷是否該拆而非硬搬。

**為何 P1**：`component-contract-design` 已推送在即（尚未 push，且需帶 `--prune`），交界處的不對稱會隨它一起擴散到其他 consumer 專案。

### Related Files
- .claude/skills/foundation-design/SKILL.md  # from 0.1.0-W3-317
- .claude/skills/ux-design-evaluation/SKILL.md  # from 0.1.0-W3-317
- .claude/skills/dart-style-guardian/SKILL.md  # from 0.1.0-W3-317
- .claude/skills/version-bootstrap/SKILL.md  # from 0.1.0-W3-317

### Source Acceptance (reference)
- [ref] [ ] 四個 skill 各完成一批探針量測，門檻與作者意圖清單於派發前登記  # from 0.1.0-W3-317
- [ref] [ ] 每項處置皆先做歸因三分類，判為模型問題者已舉出排除材料  # from 0.1.0-W3-317
- [ref] [ ] version-bootstrap 超標已依 SRP 判定為拆 skill 或拆檔並落地，未以刪判斷條件達標  # from 0.1.0-W3-317
- [ref] [ ] 兩處超長 description 已收斂至 250 字元內且觸發詞未流失  # from 0.1.0-W3-317
- [ref] [ ] i18n-guidelines.md 已有入站路由，或已判定不需要並記錄理由  # from 0.1.0-W3-317
  ... (truncated, see source ticket)

