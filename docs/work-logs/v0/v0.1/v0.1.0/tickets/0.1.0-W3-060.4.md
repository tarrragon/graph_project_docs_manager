---
id: 0.1.0-W3-060.4
title: 補 SPEC-004 §4.15–4.26 資料視圖與狀態元件（MatrixCell 至 AppSnackBar）的回饋契約子節與狀態矩陣來源／同步欄
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: 0.1.0-W3-060
children: []
blockedBy:
- 0.1.0-W3-060.1
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: lavender-interface-designer
  history: {}
what: 補 SPEC-004 §4.15–4.26 資料視圖與狀態元件（MatrixCell 至 AppSnackBar）的回饋契約子節與狀態矩陣來源／同步欄
when: 0.1.0-W3-060.1 完成後
where:
  layer: Presentation
  files:
  - docs/spec/ui/SPEC-004-component-library.md
why: 逐元件投影，依第 4 章分段拆票控制單票認知負擔；與同層子票同型屬刻意分段（--allow-duplicate）。
how:
  task_type: Documentation
  strategy: 同 060.2 作法，範圍 §4.15–4.26；MatrixCell／SwimlaneNode／RelationItem 的 locate（搜尋命中、樹展開後定位）與 LoadingState／BlockedState 的等待與結果層為重點。
acceptance:
- '[ ] §4.15–4.26 每條目皆有回饋契約子節'
- '[ ] §4.15–4.26 狀態矩陣皆含來源與同步策略欄'
- '[ ] 不適用者附理由；待決者有 blockedBy 票'
tdd_phase: phase1
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-08T01:55:26'
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

補 SPEC-004 §4.15–4.26 資料視圖與狀態元件（MatrixCell 至 AppSnackBar）的回饋契約子節與狀態矩陣來源／同步欄

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
### 變更摘要

`docs/spec/ui/SPEC-004-component-library.md` 1.20 → 1.21（frontmatter、檔頭版本段、變更歷史同步）：

| 條目 | 回饋契約 | 狀態矩陣來源／同步策略 |
|------|---------|----------------------|
| 4.15 MatrixCell | 六列全填；locate 由 4.37 承載（已選格捲離不捲回、泳道列定位對象非本元件）；press-ack／state-change 時限 `Motion.feedback`（SPEC-003 §3.1 選格列） | default／rowSelected／selected 資料態：Domain 視圖選取 provider 未建，由畫面票補填；瞬態拆三列 |
| 4.16 SwimlaneNode | 不適用 + 理由（明訂無互動；drag 與泳道列定位由 4.38 承載） | —（`SwimlaneGrid` 傳值） |
| 4.17 StepNumber | 不適用 + 理由（所在列點擊由 4.35／4.40 承載） | —（呼叫端傳值） |
| 4.18 ExpanderIcon | 六列全填；locate 由 4.39／4.41 承載（子項定位由容器條目定義，提案） | collapsed／expanded 資料態：`expandedTraceNodesProvider`；破洞報告分節為頁面 `State`（`_isExpanded`）；Ticket 主題節未建；leaf 為「—」；瞬態拆三列 |
| 4.19 RelationItem | 六列全填；damaged 下回饋由 4.6 承載；locate 為跳轉來源非目標 | default／damaged 資料態：節點詳情／UC Flow provider 未建；同步策略等確認才顯示 |
| 4.20 DocumentBody | 不適用 + 理由（無可點連結；捲動與內容替換由 4.30 與畫面承載） | —（呼叫端傳值） |
| 4.21 EmptyState | 前三列路由 4.4；locate 補系統通知導向 `state-gaps-none` 焦點落點（根節點 `Focus`，提案）；state-change `liveRegion` | —（呼叫端傳值；是否渲染為 `gapReportProvider.GapReportNoGaps`／`traceabilityStateProvider.TraceabilityNoProposal`，其餘畫面未建） |
| 4.22 MissingSourceState | 前三列路由 4.4；press-ack／等待與結果 `Motion.cancelDeadline`（§3.6 三分支）；結果通知 SnackBar 經 4.26 | —（節點詳情 provider 未建） |
| 4.23 BlockedState | 前三列路由 4.4；state-change 面板 `Motion.transition`；Esc 後焦點落點標提案 | collapsed／expanded 資料態：`isDetailExpanded` 存 Domain 視圖頁面層，provider 未建 |
| 4.24 LoadingState | 明文為等待指示層本身；C1–C8／L1–L2 分配至各列；時限 `Motion.feedback`／`progressTick`／`spinnerMinVisible`／`cancelDeadline` | loading／cancelling 資料態：`gapReportProvider.GapReportScanning`（`processedCount`／`isCancelling`）；loading 等確認才顯示、cancelling 本地即時（C2）；Domain／Ticket 未建 |
| 4.25 LoadPrompt | 前三列路由 4.4；state-change 取消回復 `Motion.cancelDeadline` | —（Ticket 清單 provider 未建） |
| 4.26 AppSnackBar | 明文為結果通知層載體與 `denied` fallback 載體；locate 不適用（無承載者）；停留 `Motion.snackBar`／`snackBarWithAction` | shown／dismissed 為「—」（`ScaffoldMessenger` 觸發，不存 provider） |

4.21–4.26 互動瞬態由內嵌 4.4 `AppButton` 承載，狀態矩陣下以一句註明不另列（§4.0.1 末段同步明文）。§4.0 對應表落成範圍同步為 4.15–4.26 由 060.4、4.27 以後由 060.5。

### 待決處理

未建待決票：依 PM 裁決，hover／focus／本地即時切換時限填「無」標提案；等待與結果、state-change 過渡的時限皆能自 SPEC-003 抄錄，無「SPEC-003 無對應」需 UX 審查補票的列。標「提案」者：4.18 展開後子項定位由容器條目補、4.21 `state-gaps-none` 焦點落點載體、4.23 Esc 後焦點落點與面板不 scroll-into-view、4.26 動作焦點——皆屬 SPEC-003 未定的細節，依既有慣例填法標提案待 PM 驗收核定，非待決。

### 併修 060.3 審查 warning（basil-writing-critic，PM 轉達）

1. §4.0.1「來源」欄三值定義：「互動瞬態」括號擴為「輸入裝置驅動的瞬態，或元件內部 overlay 開合等不存於 provider 的本地狀態」；退出路徑句限定於 hover／pressed／focused 三列；補 Consequence「混填會使本欄失去『哪些狀態需存 provider（§2 頁面狀態保留）』的直接查表功能，讀者須回讀退出路徑才能判定」。4.13 open 列來源維持不改。
2. 4.11 回饋契約「選中的回饋」→「選取的回饋」。
3. 4.13 回饋契約引言「SPEC-003 §3.4 篩選七列」→「篩選各列」。
4. §4.0.1「由對應畫面票補填」後加「（以畫面名為鍵）」。

本票 §4.15–4.26 依擴充定義歸類：4.26 shown／dismissed 與 4.21–4.25 的 default 列皆非輸入裝置驅動亦非 overlay 本地狀態，填「—」；4.23 面板開合存於頁面層，填資料態。引用他檔表列數處未寫「N 列」（changelog 的「十二條目」「六類」為本檔自身計數）。

### 自審（禁用詞／表格欄數／emoji）

- 新增行 grep 禁用詞（智能／文檔／數據／默認／代碼／視頻／軟件／硬件／信息）與常見簡體字：0 命中。
- emoji（U+1F300–1FAFF、U+2600–27BF）：0 命中。
- 「選中」：新增行 2 處，皆為引用既有名稱（SPEC-003 §3.1 動畫提示列名「格選中態出現」；變更歷史描述 4.11 改字前後）；本票新寫文字一律「選取」。
- 表格欄數：§4.15–4.26 範圍內各表 header 與資料列管線數一致；掃描到的 3 處不一致位於 4.28／4.30（跳脫管線 `\|` 造成的既有計數誤差），不在本票範圍。

### 二次審查

basil-writing-critic 由 PM 於本票 commit 後另派（PM 裁決）。

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: lavender-interface-designer
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**變更摘要**：`docs/spec/ui/SPEC-004-component-library.md` 1.20 → 1.21——4.15–4.26 十二條目「互動反應」子節改為「回饋契約」（範本六類事件表），狀態矩陣加「來源」「同步策略」兩欄並拆瞬態列；§4.0 對應表、§4.0.1 末段（含 060.3 審查四項併修）、檔頭版本段、frontmatter、變更歷史 1.21 同步。4.27 以後不動。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-060.1 | chars: 229 -->

### Task Reference
- 0.1.0-W3-060.1 what: 填 SPEC-004 第 1 章回饋通道子表與第 2 章過渡提示觸發點／樂觀更新政策（先決）（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-060.1 why: 自父票拆出的先決子票：逐元件回饋契約引用第 1 章子表的格與第 2 章的觸發點，兩者未定則元件條目無從引用。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 第 1 章回饋通道為回饋事件 × 桌機子表，macOS 系統通知列標待核定或有簽核記錄  # from 0.1.0-W3-060.1（來源已完成，結論見 Solution）
- [ref] [x] 第 2 章含過渡提示觸發點與樂觀更新政策兩列（提案）  # from 0.1.0-W3-060.1（來源已完成，結論見 Solution）
- [ref] [x] 檔頭與層級定位段不再出現欄位計數字面  # from 0.1.0-W3-060.1（來源已完成，結論見 Solution）
- [ref] [x] 變更歷史 1.17 與 frontmatter 一致  # from 0.1.0-W3-060.1（來源已完成，結論見 Solution）
**需求摘要**: 依 060.1 落成的 SPEC-004 §1 回饋通道子表與 §2 兩列，把本票範圍內每個第 4 章條目的「互動反應」子節改為「回饋契約」（範本 1.13.0 的欄位：事件／觸發者／反應／動畫 token／時間門檻／回饋通道（桌機）／對應狀態邊／填寫者），狀態矩陣加「來源」「同步策略」兩欄並依範本補 hover（僅指標形態）／pressed 列（既有合併列「hover / pressed / focused」拆為範本形狀）。

**相關檔案**:
- `docs/spec/ui/SPEC-004-component-library.md` 本票範圍的 §4.x 條目（見 title）；§1 回饋通道子表（通道值的唯一來源，逐格引用不重抄）；§2 過渡提示觸發點（決定 state-change 列的觸發者寫法）；§4.0 本章通用約定
- `.claude/skills/doc/templates/component-library-spec-template.md`〈回饋契約〉子節與狀態矩陣的欄位形狀
- `docs/spec/ui/SPEC-003-interaction-response.md` §2.2 回饋層級——時間門檻與反應（通知形式）自此抄錄，填寫者欄填「UX 審查」
- `lib/app/router.dart` 與各頁 provider（`ref.watch` 所在頁面層）——資料態的「來源」欄填 provider 名，只讀

**約束**:
- 通道與對應狀態邊由本票填（填寫者欄「規格撰寫者」）；時間門檻與反應抄 SPEC-003，SPEC-003 無對應者標「待決」並建 UX 審查補票設為該元件的 blockedBy，不自行設計
- 純顯示且無資料態變更的元件（AppText／AppIcon／Divider／PageTitle／StepNumber 類）整節填一列「不適用 + 理由」；容器元件的 locate 事件：資料視圖容器為執行端（填 scroll-into-view + 高亮的作法），子件填「由容器承載」
- 狀態矩陣「來源」：hover／pressed／focused 填「互動瞬態」；selected／disabled／loading／error／empty 填 provider 名；「同步策略」依 §2 樂觀更新政策（0.1 提案為不允許樂觀 → 一律「等確認才顯示」或「本地即時」）
- 不寫欄位計數（DOC-GPD-003）；標記慣例依檔頭（提案／待核定／待決）；變更歷史逐票記一列，版號由合併方順延（範本〈變更歷史〉規則）
- 完成後派 basil-writing-critic 二次審查本票新增段落，結論寫入 Solution

**驗收條件**: 見 acceptance。
**PM 裁決（自 060.2 驗收）**：hover／focus／選中切換等 Material 內建態或本地即時的事件，時間門檻填「無」（機制非數值判斷），不走待決、不建 UX 審查票；只有等待與結果、state-change 過渡等有時限語意的列才抄 SPEC-003 時限。basil-writing-critic 二次審查由 PM 於本票 commit 後另派，本票 Solution 只寫自審（禁用詞／表格欄數／emoji）結果，不因無 Agent 工具而標 NeedsContext。填法對齊 060.2（commit 30248e1e）與 §4.0.1 拆三列規則；尚未建立的 provider 名以「由對應畫面票補填」標示，不自行發明。
**basil 審 060.2 的三條填法（本票遵守）**：狀態矩陣「來源」欄枚舉值只有互動瞬態（輸入裝置驅動）／資料態（provider 名）／—；default／enabled 這類建構即渲染或呼叫端傳值的列填「—（…）」不填互動瞬態。用「選取」不用「選中」。通道格「不適用」一律帶括號承載者或理由。
