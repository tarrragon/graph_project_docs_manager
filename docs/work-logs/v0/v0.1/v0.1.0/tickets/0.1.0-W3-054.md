---
id: 0.1.0-W3-054
title: 釐清 component-contract-design 的 addresses.md 與 project-integration 依賴方向矛盾
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 條文寫單向且方向與三次實作相反，擇一改為一致並判斷是否需要執法層
when: 下次動該 skill 的地址表或 project-integration 時
where:
  layer: framework
  files:
  - .claude/skills/component-contract-design/references/addresses.md
  - .claude/skills/component-contract-design/references/project-integration/README.md
why: project-integration/README.md 的〈依賴方向〉節逐字寫「references/addresses.md 不得引用本目錄（那會讓通用地址表反過來綁死單一專案）；本目錄引用 addresses.md 的簡稱作為上游定義，方向單向」。而實際實作三次皆為反方向：v1.5.3 建立該目錄時把 addresses.md 的方法論、元件庫規格範本兩列的地址欄改指向該檔（CHANGELOG 逐字記載），本次新增的 agent 派發範本一列同樣指向它。條文與實作從建立當下即相反，且條文就寫在被引用的那個檔裡，三次皆未被任何機制擋下——該句無執法層，portability gate 只比對 .claude/ 路徑字串對依賴方向無感。這使得讀者依條文推論出的結構與實際結構不符：照條文讀，addresses.md 應是自足的通用表；照實作讀，它有三列必須跳到專案專屬目錄才有值。
how:
  task_type: Documentation
  strategy: 先判哪一個方向是對的，不要預設條文正確。支持實作方向的論據：地址表要對每個受管詞給出可查的地址，專案專屬路徑總得有處寫，由表指過去比讓讀者自己找合理，且 project-integration 已排除於 push 故可攜性不受損。支持條文方向的論據：通用表一旦指向專案目錄，其他 consumer 沿用時那三列即為死名。判定後擇一改：改條文則刪該句並說明為何允許；改實作則三列的地址欄改為不指名任何檔案、由讀者自行在專案內尋找。另判是否值得加執法層——無執法的方向宣告已被違反三次，這一問的答案本身是本票的產物之一。
acceptance:
- '[x] 條文與實作方向一致，且變更的那一方留有為何改它而非改另一方的依據'
- '[x] 三處實例（v1.5.3 兩列、本次一列）皆已對齊新的方向'
- '[x] 已判定是否需要執法層並記錄理由；判定為需要則已建對應票或已實作'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-07T16:20:49'
completed_at: '2026-09-07T16:31:47'
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

條文寫單向且方向與三次實作相反，擇一改為一致並判斷是否需要執法層

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-07T16:31:47
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**變更摘要**：

1. `.claude/skills/component-contract-design/references/project-integration/README.md`〈依賴方向〉節：原文宣告「addresses.md 不得引用本目錄，方向單向（本目錄→addresses.md）」，與 v1.5.3 起的三次實作（addresses.md 方法論／元件庫規格範本／agent 派發範本三列皆指向本目錄）相反。判定實作對、條文錯，改條文為「實際方向雙向：addresses.md 引用本目錄、本目錄以 addresses.md 簡稱為上游定義」，並記錄三層依據：(a) 地址表要給可查地址，專案專屬路徑總得有處寫；(b) addresses.md 的引用文字是 skill 內部相對路徑不含專案識別符，引用穩定性規則 8 明列「框架檔案路徑」允許引用；(c) 回改單向等於拔掉三個受管詞唯一地址來源，可攜性不因此提升（本目錄本身已排除於 skill-sync push）。同節記錄執法層判定：不需要——違反成本止於文件內部敘述失準，可攜性實質不變量已由規則 8 hook 與 skill-sync portability gate 守住，改到一致後無再違反空間。順帶修〈其他專案沿用時〉節的過時列舉（原只列方法論／元件庫規格範本兩列，補上 agent 派發範本第三列，與 addresses.md 現況一致）。
2. `addresses.md` 三處實例（15、16、29 列）未變更——三者本已彼此一致且方向正確，判定為「已對齊新方向」，不需回改。
3. `.claude/skills/component-contract-design/CHANGELOG.md` 新增 1.9.1 條目記錄本次修正與理由；`SKILL.md` frontmatter `metadata.version` 同步由 1.9.0 提升至 1.9.1。

**可攜性閘門複驗**：`skill-sync push component-contract-design < /dev/null` 於修改前後各跑一次。修改前：無違規，於確認提示處中止（未真推）。修改後：初次因 CHANGELOG 新條目直寫 `.claude/references/reference-stability-rules.md` 完整路徑被 portability gate 擋下（consumer-specific reference），改為去除 `.claude/` 前綴、僅稱「引用穩定性規則 8」後複驗通過，於確認提示處中止（未真推）。全程未 commit。
