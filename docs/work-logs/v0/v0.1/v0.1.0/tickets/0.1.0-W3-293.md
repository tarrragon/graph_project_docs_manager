---
id: 0.1.0-W3-293
title: 落條文 framework-issue 升級流程的時點分離判準，與品質基線規則 5 的接縫註記
type: DOC
status: completed
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.1.0-W3-295
- 0.1.0-W3-297
- 0.1.0-W3-298
- 0.1.0-W3-299
- 0.1.0-W3-300
source_ticket: 0.1.0-W3-289
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-289 判定升級成立並送 tarrragon/claude#81；用戶裁示先調整 framework-skill 說明與相關 rule，再回頭評估是否可開始修
  final_decision: '採 289 的候選方案 A（純條文層）先行落地，B 待 #81 區段討論，C 不採'
  rationale: '289 原定最終形狀由 #81 區段決定；用戶為框架擁有者，裁示先落 A。A 不動 CLI 與 hook 無破壞性，且 B 的提示強度與誤報處置未定，先落 A 不排除後續補 B'
who:
  current: thyme-documentation-integrator
  history: {}
what: 落條文 framework-issue 升級流程的時點分離判準，與品質基線規則 5 的接縫註記
when: 隨時
where:
  layer: Infrastructure
  files:
  - .claude/skills/framework-issue/references/escalation-flow.md
  - .claude/skills/framework-issue/SKILL.md
  - .claude/skills/framework-issue/CHANGELOG.md
  - .claude/rules/core/quality-baseline.md
why: '**用戶裁示（2026-09-11）**：先調整 framework-issue skill 的說明與引導、以及相關 rule 的規範，再回頭評估目前是否已可開始實作、或仍須回頭檢查 issue 內容再做分析。


  **條文內容已由 `0.1.0-W3-289` 產出，本票只做落地**，不重新設計。該票的機械量測：443 張票中 275 張為發現衍生，其中 108 張 IMP，其中 74 張來源票非 ANA；這 74 張建票當下 `how.strategy` 全部非空，僅 12 張（16%）帶待驗證／停手語彙——**62/74（84%）在發現當下寫定了一個沒有任何欄位記載其驗證狀態的修法**。


  **289 原本的定位是「未定案，供討論」**，理由寫在該票：條文形狀是它自己的修法，其消費端（其他 consumer 的建票實務）未經觀測，故依自身判準不寫成結論。用戶裁示先落地，此裁示即為本票的依據；**`#81` 的區段討論不因此關閉**，後續若該處給出不同形狀，以其為準。


  **不做則後果**：判準只存在於一張已完成的票與一則 issue 觀測裡。下一個 session 建發現票時不會讀到它，84% 的比例不會改變。'
how:
  task_type: Documentation
  strategy: '**條文取自 `0.1.0-W3-289` Solution 的〈條文形狀：適用與豁免的判準〉與〈與品質基線規則 5 的銜接邊界〉兩節，逐項落地，不改寫其判準內容**。落地前先讀該票原文，不依本票轉述。


    **載體分配（受自動載入預算約束，session-start 實測 auto-load 集合 ~59k / 預算 45k，已超標）**：


    1. **主文 → `.claude/skills/framework-issue/references/escalation-flow.md`**（按需讀取層）：新增一節，含三問（消費端已觀測／反例已試／範圍已量）、兩檔擴散面處置表、三個檢驗案例（`0.1.0-W1-063` 豁免、`0.1.0-W3-284` 與 `0.1.0-W3-290` 不豁免）。**三明示完整展開**（按需層適用）。

    2. **路由 → `.claude/skills/framework-issue/SKILL.md`**：一行指向該節，不複製判準內容。

    3. **接縫註記 → `.claude/rules/core/quality-baseline.md` 規則 5**：**最多一行**，說明優先級對應表的「高→當前版本（IMP Ticket）」應讀作「高＝本版處理」，票型由證據狀態決定而非由優先級決定，並路由至上述主文。**自動載入層禁止展開三明示**（`rules/README.md` 自動載入預算原則），只放禁令速查 + 路由。


    **必須明寫的邊界**（289 已論證，勿省）：時點分離的**單位是欄位不是票**、豁免軸是**證據狀態不是問題規模**、與規則 5 不衝突（作用對象不同、建票時點不變、不新增詢問環節）、不觸及 `decision-trigger-binding.md`（分析票本身即 trigger，非無 trigger 延後）。


    **不做的事**：不採候選 C（票型閘門，289 已否決：74 張變 148 張，放大票池單調增長）；候選 B（建票期 CLI 提示）**不在本票範圍**，待 `#81` 區段討論後另行決定。不改任何 CLI 或 hook。**不執行 `sync-push` 或 `skill-sync push`**——那是跨專案動作，由 PM 另行徵詢後處理。


    **收尾**：`grep -rl` 檢查是否有其他 `.claude/` 檔案描述同一流程而需同步（依 `skill-cli-sync-check` 的反向同步要求）；發現需同步而範圍過大者建 DOC ticket 追蹤，不口頭延後。'
acceptance:
- '[x] 三問與兩檔擴散面處置表已落於 escalation-flow.md，判準內容與 0.1.0-W3-289 一致'
- '[x] 三個檢驗案例已收錄，且能分辨豁免與不豁免（非全面禁令）'
- '[x] SKILL.md 有一行路由指向該節，未複製判準內容'
- '[x] quality-baseline 規則 5 的接縫註記不超過一行且含路由，未在自動載入層展開三明示'
- '[x] 單位是欄位不是票、豁免軸是證據狀態不是規模，兩項邊界已明寫'
- '[x] 已 grep 檢查其他 .claude/ 檔案是否描述同一流程，需同步者已處理或已建票追蹤'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-11T12:14:15'
completed_at: '2026-09-11T12:25:55'
created: '2026-09-11'
updated: '2026-09-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

落條文 framework-issue 升級流程的時點分離判準，與品質基線規則 5 的接縫註記

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-11 12:13] readiness=2 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-09-11T12:25:55
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

**變更摘要**：

1. `.claude/skills/framework-issue/references/escalation-flow.md`：新增〈修法時點分離：發現票何時能寫定修法〉一節（ToC 同步新增條目、章首「四個環節」改「五個環節」）。內容逐項取自 `0.1.0-W3-289` Solution 的〈條文形狀：適用與豁免的判準〉與〈與品質基線規則 5 的銜接邊界〉兩節：時點分離單位是欄位不是票、豁免軸是證據狀態不是規模（消費端已觀測／反例已試／範圍已量三問）、擴散面兩檔處置表、三個檢驗案例、與規則 5 的銜接邊界說明、候選方案 A/B/C 現況。三明示（Why/Consequence/Action）完整展開，符合按需讀取層規範。

2. `.claude/skills/framework-issue/SKILL.md`：按需讀取路由表原有一列擴充「何時讀」觸發條件與「涵蓋章節」欄新增一項，指向 escalation-flow.md 的新節，未複製判準內容。metadata.version 2.4.0 → 2.5.0。

3. `.claude/skills/framework-issue/CHANGELOG.md`：新增 2.5.0 條目說明本次變更。

4. `.claude/rules/core/quality-baseline.md`：規則 5 後補一行「接縫」（單行、含路由，未展開三明示）：優先級對應表「高→當前版本（IMP Ticket）」應讀作「高＝本版處理」，票型由證據狀態決定而非優先級。版本 3.5.0 → 3.6.0，Last Updated 同步。

**規則 8 處置**：三個檢驗案例原稿含專案層級 ticket ID（`0.1.0-W1-063`／`0.1.0-W3-284`／`0.1.0-W3-290`），首次 Edit 被 `reference-stability-rule8-guard-hook.py` 阻擋（案例敘事主詞型，非可用 illustration/relocation 逃生閥情境）。依規則 8 五類分類第 3 類處置：移除 ID，改寫為自足的描述性標籤（如「測試檔對已棄用 API 的直接替換」），內容與判準邏輯不變，只是不再依賴查閱特定票才能理解。

**Grep 同步檢查（acceptance 6）**：`grep -rln "escalation-flow.md"` 限中僅 `SKILL.md`／`CHANGELOG.md`（已處理）。另查 `plan-to-ticket-flow.md`「執行中額外發現」「三問閘門（建 ticket 前必答）」與 `execution-discovery-rules.md`「發現即建立」——兩者關注點分別為「該不該建票／該建哪種票的關係」，不涉及「建票欄位可否寫定修法」，與本節無重疊，不需同步、未建額外 ticket。

**未做的事**：未執行 `sync-push`／`skill-sync push`（不在本票範圍，且 `0.1.0-W3-295` 已由主線程另立票守住延後 sync-push 的決策，不重複建票）；未改任何 CLI 或 hook。
## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-289 | chars: 888 -->

### Task Reference
- 0.1.0-W3-289 what: 分離 framework-issue 升級流程中的發現時點與修法決定時點（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-289 why: **用戶裁示（2026-09-11）**：發現問題當下建票是對的（品質基線規則 5），但**不應在當下決定要怎麼修**。應先開分析與規劃的票釐清原因與解決方案，統整到 issue，再據該統整派發實作。理由三項：避免重複工作與驗證的消耗、更有利於找到真正的癥結點、**避免生成判斷的時候沒有驗證**。

**現行流程無此區分**。`escalation-flow.md` 規範的是「何時升級、怎麼判、寫到哪」，未規範「修法在哪個時點定案」。實務上兩件事在同一個動作裡完成——建票時填 `title` 與 `how.strategy`，修法形狀就此寫定。

**已發生實例**：`0.1.0-W3-284` 的標題含「並收斂為單一 logger 工廠」，該修法決定於 `0.1.0-W3-273` 的 Phase 4 發現當下；`0.1.0-W3-285` 事後實測漏接率 163/164，該修法涵蓋 4 個呼叫點且抽象層級落在 consumer 側，而判定的根源在 `.claude/lib/` 的契約。**若非用戶指出，284 會照票面派出。**

**最深的一層是第三項理由**：寫在 `title` 與 `how` 裡的修法，此後被每一個讀者當成已定案的前提讀，不再被當成待驗證的判斷。**它從未被證明是錯的，只是也從未被檢查過**——而外觀上與一個已驗證的決定完全相同。

**不做則後果**：此形態在每一個 consumer 專案重現。每次發現都產生一張已鎖定修法的實作票，統整永遠在實作之後發生，issue 淪為事後記錄而非決策場所。（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] dedup 已跑，關係判定（重複／切分／引用）有記錄  # from 0.1.0-W3-289（來源已完成，結論見 Solution）
- [ref] [x] 兩條 AND 判準已逐條套用並在票面留下記錄  # from 0.1.0-W3-289（來源已完成，結論見 Solution）
- [ref] [x] 與品質基線規則 5「發現即建立」的銜接邊界已明寫，不留兩條規則的時點衝突  # from 0.1.0-W3-289（來源已完成，結論見 Solution）
- [ref] [x] 已給出適用與豁免的判準，非只給禁令  # from 0.1.0-W3-289（來源已完成，結論見 Solution）
- [ref] [x] 升級與否的判定已落地：升級者票面附 issue 連結，不升級者 Solution 逐條附兩條 AND 判準不成立的理由  # from 0.1.0-W3-289（來源已完成，結論見 Solution）
