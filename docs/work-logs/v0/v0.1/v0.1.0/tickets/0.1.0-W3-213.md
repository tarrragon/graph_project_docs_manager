---
id: 0.1.0-W3-213
title: 接收 跨 domain 事件流與高負載服務設計的產出，並與本專案既有判準對帳
type: ANA
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶 2026-09-09 裁示：跨 spec 事件流管控需更複雜的分析與規劃，已交另一 session
  final_decision: 建立接收票作為 0.1.0-W3-202 的合法 trigger，該鏈暫不啟動
  rationale: 202 與該產出處理同一層；先做會產生兩份涵蓋重疊的權威定義，正是雙向引用漂移的來源
who:
  current: 主線程
  history: {}
what: 接收 跨 domain 事件流與高負載服務設計的產出，並與本專案既有判準對帳
when: 另一 session（fable model）的規劃設計產出抵達後
where:
  layer: Domain
  files:
  - docs/domain-map.md::read
why: '**用戶裁示（2026-09-09）**：跨 SPEC 的事件流管控需要比 `0.1.0-W3-184` 更複雜的分析與規劃，範圍延伸至三個本專案目前不涵蓋的面向——


  > 我們現在雖然是用在單線程的 flutter，但是 flutter 其實也有 isolate 的功能可以跑多線程，而更延伸的說，我們相關的 skill 規劃設計也會用到後端服務的設計上，那伺服器需求的功能在處理這種大量事件流同時啟動需要有更詳細的規劃跟管理，無論是本地排程，或者是大量的外部請求，或者各種本地腳本，或者高負載的查詢或者解碼，都會對整體造成影響。

  >

  > 那高負載的情況下如何維持服務，如何判斷要停止哪些請求，以及維持可觀測性讓工程師做管控跟除錯。


  該需求已交由另一 session（fable model）規劃設計，**本專案的 SPEC 設計待其產出後再回頭檢視**。


  **為何需要本票**：`0.1.0-W3-202`（事件排程與注意力仲裁方法論）與該產出**處理同一層**——`184` 定義的三分判準、L1–L3 級別、競爭的操作性定義、仲裁模型，全部是「大量事件同時發生時如何取捨」的一個切面（稀缺資源為使用者注意力）；該產出的稀缺資源是 CPU、記憶體、連線數，但取捨結構同源。


  **先做 `202` 則後果**：產生兩份涵蓋重疊的權威定義，一份在方法論、一份在該產出，而兩者沒有任何同步機制——**這正是本專案今日花費最多力氣處理的那類漂移**（`framework issue #104` 的主題）。


  `202` 是 `203`／`204`／`205`／`207` 的上游，故整條鏈一併等待。**這是 `decision-trigger-binding` 規則 1 狀態 (b)（明確 trigger 延後），不是懸空**——本票即該 trigger 的承載者。'
how:
  task_type: Analysis
  strategy: 產出抵達後三步。(1) 對帳涵蓋範圍：逐一判定 0.1.0-W3-184 Solution 的五組結論（三分判準、L1-L3 級別、競爭的操作性定義、協調圖與依賴圖正交、單一仲裁點）在該產出中是否已被涵蓋、被推翻、或未觸及；三種結果的處置不同，不得混為一談。(2) 判定 0.1.0-W3-202 的存廢與範圍：若被涵蓋則 202 改為引用而非另立、若未觸及則 202 保留該部分、若被推翻則以該產出為準並記錄本專案既有結論的失效理由。(3) 重新評估 203/204/205/207 的 blockedBy 與 how，其中 205（AppSnackBar 截斷條件）屬具體缺陷修復，可能不受上層設計變動影響、應優先解鎖。本票不寫任何方法論或規格正文。
acceptance:
- '[ ] 184 的五組結論逐一判定為已涵蓋／被推翻／未觸及，三者分別記錄，未混為一談'
- '[ ] 0.1.0-W3-202 的存廢與範圍已裁定，且未產生與該產出涵蓋重疊的第二份權威定義'
- '[ ] 203/204/205/207 的 blockedBy 與 how 已重新評估，並判定 205 可否提前解鎖'
- '[ ] 本票未寫任何方法論或規格正文'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-09T16:51:57'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

接收 跨 domain 事件流與高負載服務設計的產出，並與本專案既有判準對帳

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 交付給規劃 session 的對接材料（PM 整理，2026-09-09）

**這份材料是什麼**：本專案在 `0.1.0-W3-184` 已定案的判準，加上本專案的實測約束與踩過的坑。目的是讓規劃方知道哪些已有結論可沿用、哪些明確不足、以及有哪些現地事實會限制方案。

**這份材料不是什麼**：不是需求，也不是預設的架構方向。**若下列任一結論與規劃方的分析衝突，以規劃方為準**——這些判準的稀缺資源是使用者注意力，樣本只有一個桌面 App，外推到伺服器負載未經驗證。

---

#### 一、可直接沿用的判準（各附「為何這樣定」，理由比結論重要）

| 判準 | 為何這樣定 |
|------|-----------|
| **事件三分，鍵是「到達時機由誰決定」**：使用者觸發／我方自行發起／外部驅動 | 只有這個鍵能決定**可否排程**，而可否排程決定了能用哪些手段。**實作是否非同步與此無關**——一個在我方選定時刻發出的 `await` 屬第二類，一個被推進來的同步回呼屬第三類。這是最容易誤判的一處 |
| **級別的鍵是「接收方必須做什麼」，不是產生方的技術嚴重度** | 稀缺資源屬於接收方。以產生方視角分級會讓「很嚴重但接收方無事可做」的事件搶到高優先序 |
| **競爭 = 未被消費的請求**，不是「同一時刻」也不是到達率 | 本專案的實際案例**不需要兩件事同時發生**——只需一則正在顯示時另一則抵達。後端對應：**依佔用量卸載，不依到達率** |
| **三條截斷規則**：高可截低、同級不互截（排隊或合併）、低不得截任何 | 第三條是實際被違反的那一條。本專案的提示元件無條件覆蓋前一則，使低級別訊息殺得掉高級別 |
| **協調圖不是依賴圖** | 互不呼叫的兩個 domain 仍會搶同一個通道；依賴圖上查起來是「無關係」，**看起來像已確認過** |
| **單一仲裁點，不做分散式協商** | N 條邊取代 N²，且元件之間維持互不知情。互相確認會迫使每個元件知道其他元件存在 |

#### 二、明確不足之處（本專案沒有、後端需要）

| 缺口 | 為何本專案沒有 |
|------|--------------|
| **backpressure** | UI 推不回給使用者；後端必須推得回去 |
| **資源成本異質** | 上述判準假設每個請求成本相同，只分級不分量 |
| **deadline / timeout 傳播** | 「延後至當前請求被消費」沒有上界 |
| **部分降級** | 注意力不可分割（只有一則能被看見），CPU 可以切分。「只有 first 被呈現」沒有後端對應 |
| **跨執行緒的狀態一致性** | 本專案單線程，isolate 未使用 |
| **可觀測性只到元件層日誌** | 後端需要佔用量、卸載計數、分級延遲、以及**被丟棄請求的可追溯性** |

#### 三、本專案的現地約束（避免設計出用不上的東西）

- Flutter macOS 桌面 App，**單機、無伺服器**；`0.1` 目標平台僅 macOS
- App Sandbox **關閉**（需執行專案內 doc CLI）
- 8 個 domain，依賴單向向下，完整依賴邊 8 條（`docs/domain-map.md` §2）
- **目前無 websocket 與輪詢**，外部驅動為空類別；且 `SPEC-003` 明文裁定「目錄消失屬外部變更，由下一次重新載入承接，本畫面不設偵測點」——本專案是刻意不設外部變更監看的
- **structured log 在 `0.1` 維持延後**（`docs/tech-decisions.md` 有裁定與重評條件）。可觀測性方案若以 structured log 為前提，須知道本專案目前不具備該基礎

#### 四、今日實測的三個坑（與可觀測性設計直接相關）

1. **測試綠燈驗到的是「程式做了對的事」，不是「使用者收到了對的資訊」。** 本專案曾出現診斷測試通過、實機上卻無任何紀錄能回答使用者看到什麼的情形——日誌上與靜默失敗完全同形。結論是回饋的最末端載體本身必須自帶日誌，那是資訊鏈唯一可留存的證據。**後端對應**：請求被卸載時，若只有服務端指標而無該請求的可追溯記錄，卸載與靜默失敗同形。

2. **恆真警告等於沒有警告，且比沒有偵測更糟。** 本專案有兩個實例——一個檢查器每個 session 報 81 組未收斂項、一個對已完成任務仍報「檔案使用中」。**輸出恆非空且增量不可見時，資訊量趨近零**，而背景雜訊會讓真正的新事件更難被看見。解法是把當前狀態凍結為 baseline、只報增量。**這對高負載監控直接適用**：一個在高負載時必然亮起的告警，在高負載時沒有資訊量。

3. **正確的結論配上錯誤的理由，當下完全無症狀。** 本專案今日兩度發生：結論成立、驗證通過、但支撐它的機制解釋是錯的；失效發生在有人依那個理由推導下一步時。**對設計文件的要求**：判準要寫出「它回答的是什麼問題」，不只寫結論。

#### 五、我認為值得優先回答的四個問題

1. **卸載的判準是佔用量還是到達率？** 上述「競爭 = 未被消費的請求」指向佔用量，但那是單通道場景的推論，多核心下未必成立。
2. **被卸載的請求如何留痕？** 沒有留痕則卸載與靜默失敗同形（見坑 1）；全部留痕則高負載時留痕本身成為負載。
3. **級別是靜態標註還是動態推導？** 本專案是靜態（每則提示在規格層指定級別）。後端的同一個 API 在不同情境下重要性可能不同。
4. **仲裁點自身的過載怎麼辦？** 單一仲裁點是本專案的結論，但它在後端會成為單點。

---

**取用方式**：本節可整段交給規劃方。回頭對帳時依本票 `how` 的三步，逐一判定第一節六項在其產出中屬「已涵蓋／被推翻／未觸及」。

## 重現實驗結果
### 實驗方法

對帳的前提是「產出真實存在、且我讀到的就是它」。同儕 session 的回報屬記錄平面，不作為 ground truth（`tool-output-trust-rules` 規則 5）；改以固定值命令查證世界平面，再逐項驗證回報中三項可獨立否證的主張。

### 實驗執行

| 待證命題 | 命令 | 觀測值 |
|---------|------|-------|
| 產出已進 `origin/main` | `git cat-file -e origin/main:.claude/methodologies/event-flow-load-arbitration-methodology.md` | 存在（exit 0）；`git show` 計 223 行，與回報一致 |
| 本地已含該 commit | `git rev-list --left-right --count HEAD...origin/main` | `1 0`——本地領先 1、落後 0，故 `origin/main` 全部內容已在本地樹 |
| `0.1.0-W3-215` 為終態 | `ticket track query 0.1.0-W3-215` | 已完成 |
| L1–L3 撞名理由是否成立 | `grep -n "L1\|L2\|L3" .claude/methodologies/component-library-bidirectional-constraint-methodology.md` | 命中〈分層架構〉：L1 通用原則／L2 語言框架規範／L3 專案元件庫章節——同一框架內確已有另一組 L1–L3 語意 |
| 方法論是否涵蓋 `184` 第七節第一項 | `grep -n "互動中\|正在互動\|中斷當前" <方法論>` | 零命中 |

方法論全文 223 行已逐節閱讀，非僅讀回報摘要。

### 實驗發現

**已驗證的事實**：(1) 產出存在於 `origin/main` 且已在本地樹，內容為我方直接讀取；(2) `215` 為終態，其〈預先對帳〉可作為覆核對象而非待驗證的承諾；(3) 級別改名的理由（框架內 L1–L3 已有另一語意）在 `component-library` 方法論中可獨立證實，非僅回報方自述；(4) `184` 第七節第一項確未進方法論。

**仍未驗證的假設**：方法論的後端判準（卸載依佔用量、仲裁器有界、分片不協商）在真實高負載系統中的正確性——本專案無後端執行環境可驗，且不在本票範疇。本票只驗「與 `184` 的對帳關係」，不驗「該方法論對後端是否正確」。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending
