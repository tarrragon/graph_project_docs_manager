---
id: 0.1.0-W3-049
title: 分析 error-pattern 的諮詢時機落在動作之後而非之前
type: ANA
status: pending
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-046
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 同一 session 兩次重新推導既有 pattern
  final_decision: 成 ANA 票分析諮詢時機，與 W3-047 泛化標題並行
  rationale: 兩次的性質不同：一次找不到（泛化可解）、一次找得到而未查（泛化無效，需前置觸發）
who:
  current: general-purpose
  history: {}
what: 分析既有 error-pattern 在高風險動作前無觸發點的結構成因，並提出可執行的前置諮詢機制
when: 本票建立後可執行
where:
  layer: framework
  files:
  - .claude/error-patterns::read
  - .claude/hooks::read
  - .claude/pm-rules::read
why: 同一 session 內兩次重新推導已存在的 error-pattern。其一 PC-APP-006（gitignore 排除該追蹤的檔）標題限定測試 fixture 而機制無關 fixture，查 uv.lock 孤兒 --clean 皆撞不到，屬找不到；其二 PC-BAL-008（共用 git index 掃入他人 staged 檔）標題即為所需關鍵字且含實證八逐字描述過期 index 版本進入 HEAD，屬找得到而未查。兩者的共同結構是諮詢時機——acceptance-gate 的 error-pattern 衝突檢查在 complete 時觸發、工作已做完，且一次列出 44 個未排序；它證實 pattern 在庫裡，不證實它在需要時找得到。第一類的修法是泛化標題（已由 W3-047 承接），第二類的修法必須在動作之前
how:
  task_type: Analysis
  strategy: 盤點現有 error-pattern 觸發點的時機分佈 -> 辨識哪些高風險動作類別有 pattern 但無前置觸發（並行 git 操作、gitignore 變更、sync push clean、跨 consumer 資產修改）-> 評估三種前置機制的可行性（PreToolUse hook 依命令樣式匹配 pattern、ticket dispatch 時依 where.files 路徑匹配、規則層檢查清單）-> 產出建議與後續 IMP 規劃
acceptance:
- '[ ] 已盤點現有觸發點的時機分佈，並列出有 pattern 但無前置觸發的高風險動作類別'
- '[ ] 三種前置機制各有可行性判定與依據，非憑印象'
- '[ ] 產出後續 IMP spawn 規劃或逐項標註無需建票的理由'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-07'
updated: '2026-09-07'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

分析既有 error-pattern 在高風險動作前無觸發點的結構成因，並提出可執行的前置諮詢機制

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: general-purpose
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-046 | chars: 697 -->

### Task Reference
- 0.1.0-W3-046 what: 把該 pattern 由副檔名分類改為用途分類，使 uv.lock 納入版控而執行期鎖檔仍被忽略（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-046 why: 第 99 行的 .claude/**/*.lock 註解寫「框架執行期鎖檔的類別式涵蓋」，用意是 dispatch-active.lock 與 scheduled_tasks.lock 那類執行期鎖檔。但 uv.lock 是 uv 的依賴鎖定檔，性質相反、應納版控。該 pattern 以副檔名分類而非用途分類，把兩種語意相反的檔案歸為一類。實測後果：本專案的 .claude/scripts/uv.lock 存在於磁碟且與 canonical 同為 40004 bytes，卻因被忽略而不進 git archive HEAD，使 sync-push 的孤兒偵測判定本專案沒有該檔並建議清除——執行後該檔自 canonical 消失。與 framework issue 57「uv.lock 未納版控」為同一件事，本票查出其機制（來源已完成，結論見 Solution）

### Related Files
- .gitignore::write  # from 0.1.0-W3-046（來源已完成，結論見 Solution）
- .claude/skills/ticket/ticket_system/tests/test_save_ticket_race.py::read  # from 0.1.0-W3-046（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] uv.lock 納入版控，git check-ignore 對它回未忽略  # from 0.1.0-W3-046（來源已完成，結論見 Solution）
- [ref] [x] 既有執行期鎖檔（dispatch-active.lock、scheduled_tasks.lock、.allocator.lock）仍被忽略  # from 0.1.0-W3-046（來源已完成，結論見 Solution）
- [ref] [x] test_save_ticket_race.py 的 gitignore 行為測試通過（該測試已於 W3-036 改為驗 git check-ignore）  # from 0.1.0-W3-046（來源已完成，結論見 Solution）

