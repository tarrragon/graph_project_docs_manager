---
id: 0.1.0-W1-040
title: 修正 rule8 guard 對 worktree 路徑的誤判
type: IMP
status: pending
version: 0.1.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W1-030
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W1-030 執行中登記 SR-1
  final_decision: 建 P1 IMP 票，由 Python 開發者修正
  rationale: 影響每一次涉及 docs/ 的 worktree 派發，且失敗形態是靜默污染專案文件而非明顯錯誤
who:
  current: thyme-python-developer
  history: {}
what: 將 normalize_relpath 的 .claude/ 比對改為路徑開頭錨定，使 worktree 內的專案文件不被誤判為框架檔案
when: 下次 worktree 派發涉及 docs/ 編輯前
where:
  layer: Infrastructure
  files:
  - .claude/hooks/reference-stability-rule8-guard-hook.py
why: '`normalize_relpath()` 以 `normalized.find(SCAN_PREFIX)` 尋找 `.claude/` 子字串，未錨定於路徑開頭。worktree 的實體路徑為 `<repo>/.claude/worktrees/agent-<id>/...`，因此 worktree 內的任何檔案都含有 `.claude/` 片段。


  實測：編輯 `<repo>/.claude/worktrees/agent-XXX/docs/spec/ui/SPEC-001-screen-state-matrix.md` 時，relpath 被取為 `.claude/worktrees/agent-XXX/docs/spec/ui/SPEC-001-screen-state-matrix.md`，`is_scanned_path()` 回傳 True，於是對該檔套用規則 8「框架檔案禁止引用專案層級識別符」。


  而 `docs/spec/` 與 `docs/work-logs/` 引用 ticket ID 是正常且必要的。被擋的代理人只有兩條路：在專案文件加上框架用的 `rule8-exempt` marker（語意錯置），或刪除正當的 ticket 引用（資訊損失）。兩者都靜默污染專案文件，且擋下時的錯誤訊息指向規則 8 條文，讀者不會懷疑是路徑分類錯誤。


  觸發條件狹窄使此 bug 可長期潛伏：主倉庫直接編輯 `docs/` 不會命中（路徑不含 `.claude/`），只有 worktree 派發才會。本專案的實作代理人一律強制 worktree 隔離，故每一次涉及 `docs/` 的派發都在此風險下。'
how:
  task_type: Implementation
  strategy: 將  改為錨定判斷：先以 repo root 求出真實相對路徑（或至少偵測  片段並剝除其前綴與 agent 目錄段），再判斷剩餘路徑是否以  開頭。修正後須同時涵蓋兩種呼叫情境——主倉庫的絕對路徑、worktree 的絕對路徑。避免僅特判 worktrees 字串，該做法對未來新增的 .claude/ 子目錄同樣脆弱。
acceptance:
- '[ ] worktree 內編輯 docs/ 下含 ticket ID 的檔案不再被 rule8 guard 阻擋'
- '[ ] 主倉庫編輯 .claude/ 下含 ticket ID 的檔案仍被正常阻擋（不得為修 bug 而放行真正的違規）'
- '[ ] worktree 內編輯 .claude/ 下含 ticket ID 的檔案仍被阻擋（worktree 內的框架檔案仍是框架檔案）'
- '[ ] 三種情境皆有對應的 hook 單元測試，測資使用真實路徑形態'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-09-01'
updated: '2026-09-01'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

將 normalize_relpath 的 .claude/ 比對改為路徑開頭錨定，使 worktree 內的專案文件不被誤判為框架檔案

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W1-030 | chars: 813 -->

### Task Reference
- 0.1.0-W1-030 what: 為 SPEC-001 §6 補列未選節點狀態、將 FR-02 覆蓋範圍擴為三處載入態、於 §4 含損壞列加疊加態註記

### Rationale Chain
- 0.1.0-W1-030 why: SPEC-003 逐狀態審查時發現 SPEC-001 有三處與實作現況不符：

一、§6 節點詳情的三個狀態（正常／部分損壞／原始檔已消失）進入條件皆預設已有選定節點，但 lib/app/router.dart 將 nodeDetail 列為六項導覽之一，使用者可直接點擊進入。此路徑無狀態可渲染，使 FR-01（每個狀態皆有退出路徑）在該路徑上無從檢驗，且「29 狀態即全部」的宣稱不成立。SPEC-003 §3.6 已規定過渡行為（渲染空狀態、前進動作跳追溯視圖），但過渡行為不能取代 SPEC-001 補列該狀態。

二、FR-02 驗收寫「Ticket 載入與破洞掃描期間」兩處，但 §1 Domain 視圖狀態表的載入中列亦有取消載入操作。三處載入態中有一處未被 FR-02 覆蓋，SPEC-003 的取消契約據此覆蓋三處。

三、§4 的「含損壞」與「正常 · 列表／主題」是正交關係而非互斥狀態（顯示欄寫「正常視圖 + 損壞徽章」）。以同一欄並列會讓整合測試把疊加態枚舉為獨立互斥態，測出永遠不成立的斷言。

### Related Files
- docs/spec/ui/SPEC-001-screen-state-matrix.md  # from 0.1.0-W1-030

### Source Acceptance (reference)
- [ref] [ ] SPEC-001 §6 含一個涵蓋「經導覽列進入且無選定節點」的狀態，具備進入條件、顯示、可用操作、退出路徑四欄  # from 0.1.0-W1-030
- [ref] [ ] SPEC-001 FR-02 驗收條款涵蓋三處載入態，與 SPEC-003 §2.5 的三處目標態對照一致  # from 0.1.0-W1-030
- [ref] [ ] SPEC-001 §4 含損壞列標明為疊加態，且狀態總數的計算方式在文件內明確  # from 0.1.0-W1-030
- [ref] [ ] SPEC-001 §1 載入中的退出路徑欄涵蓋三個阻擋狀態（不是框架專案／無可消費的型別表／schema 不相容）——三者的進入條件皆發生在資料夾選定後，而選定一律先進載入態  # from 0.1.0-W1-030

