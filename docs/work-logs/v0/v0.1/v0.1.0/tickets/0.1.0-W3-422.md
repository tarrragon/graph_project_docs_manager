---
id: 0.1.0-W3-422
title: 補寫 tdd 範例依據只在範例內的判定補條文（W3-417 盤點）
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-417
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W3-417 用戶裁示
  final_decision: 每 skill 一張修補票
  rationale: 避免跨 skill 同檔衝突
who:
  current: thyme-documentation-integrator
  history: {}
what: 依 W3-417 Problem Analysis 盤點表，為 tdd 所有條文查無與部分依據的判定補條文依據，兩階段處置（已有權威則補路由或補句；需新判準則草稿停手交 WRAP）
when: 立即
where:
  layer: skill-reference
  files:
  - .claude/skills/tdd/references/test-object-catalogue.md
  - .claude/skills/tdd/references/doc-handoff-uc-to-gwt-conversion.md
  - .claude/skills/tdd/CHANGELOG.md
  - .claude/skills/tdd/SKILL.md
why: 用戶裁示 W2 開工閘門：範例零無依據句，63 個判定全部先補條文再出探針題
how:
  task_type: Documentation
  strategy: 逐項判定階段，階段 1 落檔並 git grep -F 查證，階段 2 草稿停手
acceptance:
- '[x] 盤點表中本 skill 的條文查無與部分依據判定逐項處置並記錄'
- '[x] 階段 2 項目草稿已寫入 Problem Analysis 並停手'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-15T13:23:08'
completed_at: null
created: '2026-09-15'
updated: '2026-09-15'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

依 W3-417 Problem Analysis 盤點表，為 tdd 所有條文查無與部分依據的判定補條文依據，兩階段處置（已有權威則補路由或補句；需新判準則草稿停手交 WRAP）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->
### 階段 2 草稿（D-12a、D-12b，草稿即停手，不改檔，非用戶程式碼風格偏好不上呈用戶）

來源：`doc-handoff.md`〈前置閘門〉「UC 主成功場景非空」「畫面狀態矩陣（涉及 UI 時）」兩檢查項，其判定細節目前只見於〈前置閘門判定範例〉的【產出】／【驗證】圍欄內（範例內圍欄依 W3-417 PM 定義裁決 2 第三款不算條文），條文本身（前置閘門表格列）未寫明。逐 grep 全 skill 確認 `doc-handoff-uc-to-gwt-conversion.md`、`doc-handoff-test-tier-assignment.md`、`bdd-behavior-testing.md` 均無同義判準，非漏找而是真缺口。

**D-12a 草稿判準**：UC 主成功場景步驟須同時具備「動作」（使用者或系統操作的描述）與「預期結果」（可觀察的回應或狀態變更）兩者，方視為完整場景步驟，計入前置閘門「至少 1 個完整場景步驟」；僅有動作或僅有結果、或整句籠統敘述而未拆出兩者的步驟，不計入。

**會改變哪些判定**：目前前置閘門表格只寫「UC 有至少 1 個完整場景步驟」，未定義「完整」的判準，執行者需自行推斷。草稿落地後會使部分現行判為「通過」的 UC（步驟僅隱含動作或結果、未逐句拆開）改判為「不通過」，回 doc 端補場景步驟——影響既有 UC 文件的前置閘門判定結果，屬新增判準而非既有條文引用。

**D-12b 草稿判準**：場景步驟只要包含使用者操作，即判定「涉及 UI」，觸發「畫面狀態矩陣（涉及 UI 時）」檢查項；不以「本次是否新增或改動畫面」作為排除或降低檢查等級的理由。

**會改變哪些判定**：目前「涉及 UI」檢查項的觸發時機無明文條件，執行者可能誤以為只有新增/改動畫面的功能才需要畫面狀態矩陣。草稿落地後，任何含使用者操作步驟的 UC（即使操作對象是既有畫面、未新增或改動畫面）都須補畫面狀態矩陣，可能新增此前被視為「不涉及 UI」而跳過此檢查的 UC 的補件負擔——屬新增判準，範圍擴大，非既有條文引用。

**處置**：兩則草稿均不改動 `doc-handoff.md` 或任何 skill 檔案，停手交後續 WRAP 評估是否採用、範圍是否需收斂（如是否排除純系統自動觸發、無使用者互動的步驟）。

### 階段 2 WRAP 評估（獨立評估者，D-12a、D-12b）

**錨點**：W2 開工閘門要求範例零無依據句（W3-417〈用戶裁示與 PM 定義裁決〉）。評估對象為本票〈階段 2 草稿〉。定位一律用標題錨點：`.claude/skills/tdd/references/doc-handoff.md`〈前置閘門〉表與〈前置閘門判定範例〉。

**合併判定**：D-12a（主成功場景非空列）與 D-12b（畫面狀態矩陣列）判別對象不同，不合併為一條判準；兩者可放在〈前置閘門〉表後同一段。與 W3-421 D-24b 無重疊。

---

#### D-12a：完整場景步驟＝動作＋預期結果

**權威查證（W 爬梯子第 0 層）**：同 skill 已有兩處條文可支撐，原草稿判為「條文全域查無」不成立——
- `doc-handoff-uc-to-gwt-conversion.md`〈轉換方法〉表：「場景步驟動作 → When」「場景步驟預期結果 → Then」。
- 同檔〈無 UC 步驟的 FR → GWT 種子〉：「FR 未定義觸發條件時不產生 GWT 種子」（缺一端即無法轉換的既有同形處置）。
- 另 `.claude/skills/doc/templates/usecase-template.md`〈主要成功場景〉每步驟即 `{使用者動作}` + `{系統回應}` 兩行。
- 〈前置閘門〉引言本身已寫「UC 空則無場景步驟可映射」——閘門目的是保證映射可做，「完整」可由映射需求導出。

**反向框架**：若「完整」只要求一句敘述即可，閘門放行後 GWT 轉換在〈轉換方法〉表取不到 When 或 Then，閘門失去「保證映射可做」的目的——相反論述不成立，判準方向正確；需修的是依據來源（改引既有表）而非判準本身。

| 選項 | 做法 | 會改變的判定 | 代價 |
|---|---|---|---|
| A 新增判準（原草稿） | 閘門表後補獨立定義句 | 只有動作或只有結果的步驟改判不通過 | 與〈轉換方法〉表形成兩處定義，日後各自演化 |
| B 改引既有他處條文（建議） | 條件欄補一句路由至〈轉換方法〉表，範例依據欄改引該表 | 同 A（判定結果相同），但定義只存在於轉換表一處 | 補一句，無新判準 |
| C 縮減範例 | 範例依據欄刪「動作與預期結果同時具備」 | 範例只示範「有步驟即通過」 | 相反分支（只有動作的步驟）無題可出 |
| D 移除範例 | 刪「UC 主成功場景非空」列 | 此列無示範 | 閘門判定範例失去一半 |

三類涵蓋：B 改造既有、C/D 零新增。**建議**：B（屬階段 1 性質的補路由，可逕行落檔）。**風格偏好**：否（映射輸入完整性判準）。

**草稿（條文句，改〈前置閘門〉表「UC 主成功場景非空」列條件欄）**：
`UC 有至少 1 個完整場景步驟：該步驟可依 doc-handoff-uc-to-gwt-conversion.md〈轉換方法〉表同時取出「場景步驟動作」（→ When）與「場景步驟預期結果」（→ Then），缺任一者不計入`

**草稿（範例依據欄）**：
`UC 主成功場景非空 | 通過 | 依〈轉換方法〉表可取出場景步驟動作（選取一列）→ When 與場景步驟預期結果（畫面切換為詳情畫面）→ Then，兩者皆可取出`

範例驗證問句維持原意，預期句尾補「（〈前置閘門〉表條件欄）」。

---

#### D-12b：「涉及 UI」如何判定

**與已定案 UI 定義的衝突（反向框架，原草稿不可採）**：
- `version-bootstrap` SKILL.md Step 2〈UI 類判別〉已定案呈現通道三分，且明文「不以字面關鍵字為準」；`foundation-design` `references/adjacent-assets-boundary.md` 的 `version-bootstrap` 列已寫明兩份文件對「UI」的分工。原草稿「含使用者操作即涉及 UI」是第三套定義，與通道表雙向不一致：
  - **過寬**：終端文字互動（通道表第二列「非圖形的人讀呈現：終端文字輸出與終端互動」）含使用者操作，原草稿判涉及 UI、要求畫面狀態矩陣；通道表判非 UI 類並交 foundation-design。
  - **過窄**：無使用者操作、但由本應用程式渲染應用內提示的系統自動步驟（通道表第一列），原草稿判不涉及 UI，漏檢畫面狀態矩陣。
- 「不以是否新增或改動畫面為門檻」一點：通道表第一列「含唯讀畫面」只按通道判定、未以新舊為條件，沿用通道表即自然成立，不需另立判準。

**另見範例缺陷**：〈前置閘門判定範例〉輸入只寫「清單畫面」「詳情畫面」字面，依通道表「FR 未寫明呈現通道時，先補寫通道再判定」，輸入須寫明通道；且產出列把「涉及 UI」（觸發條件）寫成「通過」，與表列檢查項「畫面狀態矩陣」的通過與否混為一談。

| 選項 | 做法 | 會改變的判定 | 代價 |
|---|---|---|---|
| A 新增判準（原草稿：含使用者操作即涉及 UI） | 表後補定義句 | 終端互動步驟要求矩陣；無操作的應用內提示步驟不要求 | 第二套 UI 定義，與 version-bootstrap／foundation-design 分工衝突，不建議 |
| B 改引既有他處條文（建議） | 條件欄路由至 version-bootstrap Step 2〈UI 類判別〉呈現通道表第一列 | 涉及 UI 改以步驟的操作或預期結果所在呈現通道判定；終端互動不觸發本列；應用內提示觸發本列 | 補一句路由；範例輸入需寫明通道 |
| B' 改引＋第二列承接句 | B 之外再寫第二列（非圖形人讀呈現）交 foundation-design UI 維度 | 同 B，另給第二列去向 | 與 version-bootstrap 動作欄重述，若 TDD 銜接不經規劃波則有用 |
| C 縮減範例 | 刪「成立條件不是新增或改動畫面」子句 | 範例只示範通道判定 | 相反分支題減一 |
| D 移除範例的涉及 UI 列 | — | 此列無示範 | 閘門判定範例失去一半 |

三類涵蓋：B/B' 改造既有、C/D 零新增。**建議**：B（依 W3-417 PM 定義裁決 2 第三款，跨 skill 依據須本 skill 明文路由；探針餵入範圍需含 version-bootstrap SKILL.md Step 2〈UI 類判別〉）。第二列去向採 B' 與否屬範圍取捨，交 PM；本評估傾向不加（tdd 閘門只需判定本列是否觸發，第二列處置權威已在 version-bootstrap）。**風格偏好**：否（UI 定義一致性，非程式碼風格）。

**草稿（條文句，改〈前置閘門〉表「畫面狀態矩陣（涉及 UI 時）」列，於表後補一句）**：
`「涉及 UI」依 .claude/skills/version-bootstrap/SKILL.md Step 2〈UI 類判別〉呈現通道表判定：場景步驟的操作或預期結果落在第一列「圖形介面」即涉及 UI，第二、三列不觸發本檢查項；步驟未寫明呈現通道時，先回 doc 補寫通道再判定。`

**草稿（範例改寫）**：
- 【輸入】：`使用者於清單畫面（本應用程式渲染的視窗）選取一列，畫面切換為該列對應的詳情畫面`
- 【產出】列改為：`涉及 UI | 是，須檢查畫面狀態矩陣 | 步驟的操作與預期結果落在呈現通道表第一列「圖形介面」；判定只看呈現通道，不看該畫面是否本次新增或改動（通道表第一列未以新舊為條件）`
- 【驗證】第二題改為：`Q 判定「涉及 UI」是否以「本次是否新增或改動畫面」或「步驟是否含使用者操作」為門檻？預期：否。依 version-bootstrap Step 2 呈現通道表，步驟的操作或預期結果落在第一列「圖形介面」即涉及 UI。`
- 建議追加相反分支題（供 W3-417 出題）：`Q 步驟為「使用者在終端輸入確認字元，終端輸出完成訊息」，是否觸發畫面狀態矩陣檢查？預期：否，屬通道表第二列「非圖形的人讀呈現」。`

「新增或改動畫面」子句若 PM 認為仍需條文句，可在上句後補「判定不以該畫面是否本次新增或改動為條件」——此為現行通道表的直接推論，不改變任何判定。

---

#### 評估中另見（非 D-12 範圍）

`doc-handoff.md`〈執行步驟〉第 2 步與〈檢查清單〉均寫「前置閘門三項全通過」，〈前置閘門〉表實為 4 列（第 4 列為條件列）。計數不一致，已登記 spawn request。
**引用更正（階段 2 WRAP 評估）**：D-12b 建議處「依 W3-417 PM 定義裁決 2 第三款」應為「PM 定義裁決 3（跨 skill 的依據）」。
## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->
### 逐項處置表（W3-417 盤點 D 組，tdd 範圍 9 項）

| ID | 判定 | 階段 | 落點與出處 |
|---|---|---|---|
| D-02 | 反例欄寫具體誤判情境，不只寫類別名 | 1（免補，已生效） | `test-object-catalogue.md`〈反例欄填法範例〉緊接【輸入】上方的引言句「示範反例欄應填具體誤判情境，而非僅寫一個相鄰類別名稱」，依 W3-417 PM 定義裁決 2 第一款（範例區塊外、緊接在範例上方的引言句算條文）已構成條文，未修改檔案 |
| D-03a | 斷言來源欄：兩欄同條照抄、不同條分填 | 1（免補，已生效） | 同檔〈斷言來源欄範例〉緊接【輸入】上方的引言句「兩欄同條則照抄，兩欄不同條則分別填寫」，同依裁決 2 第一款已構成條文，未修改檔案 |
| D-03b | 斷言來源欄重疊時仍須填，不可留空 | 1（補句） | 同檔〈斷言來源欄範例〉引言句已補一句，連結既有「三、契約欄位表」斷言來源列的必填後果「無斷言來源 → 斷言值為測試設計者自行判斷，與規格脫鉤」至重疊情境，不新增判準 |
| D-04a | When 只寫規格觸發條件不寫實作機制 | 1（補路由） | 同檔〈六、派發語言〉範本後補段落，路由至 skill-design-guide `references/chained-examples.md`〈【驗證】段〉執行期行為允許／禁止表（允許「使用者可觀測的事實」、禁止「框架事件 handler 的具體命名」） |
| D-04b | Given 對前一狀態、Then 對後一狀態 | 1（補路由） | 同上段落，路由至本 skill `phase2-test-design.md`〈GWT 格式〉「Given: {系統的前置狀態}」「Then: {預期的可觀察結果}」定義（同一 skill 內既有條文，原僅缺引用） |
| D-06b | Then 只寫可觀察結果不寫框架節點 | 1（補路由） | 與 D-04a 同段落，同一組允許／禁止表覆蓋（禁止「框架測試 API 的具體方法名或選擇器語法」） |
| D-12a | 完整場景步驟須同時具備動作與預期結果 | 2（草稿，停手） | 草稿見本票 Problem Analysis〈階段 2 草稿〉，未改檔，交 WRAP |
| D-12b | 場景步驟含使用者操作即涉及 UI，與是否新增/改動畫面無關 | 2（草稿，停手） | 草稿見本票 Problem Analysis〈階段 2 草稿〉，未改檔，交 WRAP |
| D-18 | GWT 種子表產出只到種子，欄位僅限 Given/When/Then/來源四欄 | 1（補句） | `doc-handoff-uc-to-gwt-conversion.md`〈GWT 種子與行為單元的分工邊界〉範例前段落已補句，路由至 `doc-handoff.md`〈TDD 輸入種子包格式〉種子包範本表頭「\| 場景 \| Given \| When \| Then \| 來源 \|」（場景為列索引非欄位） |

**計數**：階段 1 共 7 項（其中 2 項免補已生效、5 項補句或補路由，落在 3 處編輯：`test-object-catalogue.md` 兩處、`doc-handoff-uc-to-gwt-conversion.md` 一處）；階段 2 共 2 項（D-12a、D-12b，草稿見 Problem Analysis，未改檔）。

**查證**：全部新增句子逐句以 `git grep -F` 核對來源文字存在（chained-examples.md 三段引用、phase2-test-design.md 兩段引用、test-object-catalogue.md 契約欄位表引用、doc-handoff.md 種子包表頭引用），比對結果詳見執行過程，皆命中。


### 階段 2 落檔（D-12a、D-12b，依 WRAP 評估建議採 B；SR-1 一併處置）

依本票〈階段 2 WRAP 評估〉，於 `doc-handoff.md`〈前置閘門〉表後補兩句條文（皆屬「改引既有他處條文」，非新增第二套定義）：
- D-12a：「完整場景步驟」改引 `doc-handoff-uc-to-gwt-conversion.md`〈轉換方法〉表——須同時可取出「場景步驟動作」（→ When）與「場景步驟預期結果」（→ Then），缺任一者不計入。
- D-12b：「涉及 UI」改引 `version-bootstrap` SKILL.md Step 2〈UI 類判別〉呈現通道表——操作或預期結果落在第一列「圖形介面」才涉及 UI，第二、三列不觸發；判定不以畫面是否新增或改動為條件。依 PM 階段 2 裁決「不另外寫呈現通道表第二列交給誰」，未加 B' 承接句。

同步改寫〈前置閘門判定範例〉：輸入補呈現通道；產出列「涉及 UI」欄改為「是，查矩陣」；驗證段兩題改寫為改引新條文，並追加相反分支題（終端互動不觸發本檢查項）。

**SR-1 一併處置**：`doc-handoff.md`〈執行步驟〉第 2 步與檢查清單原寫「前置閘門三項全通過」，與閘門表實為 4 列不一致。依 reference-stability 規則 10（可變計數不實例化）改為「前置閘門表所列檢查項全通過」，不寫死數字，兩處皆已修正。SR-1 判定為 processed，記錄無需另立票。

版本：SKILL.md `metadata.version` 與 CHANGELOG.md 由 2.8.0 → 2.9.0（同步二住址）。

**查證**：`git grep -F` 命中新增兩段條文句原文；SR-1 相關「三項全通過」字串已於全 skill 範圍二次 grep 確認無殘留。
## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext
- **缺失項**: D-12a、D-12b 兩則判定條文全域查無（`doc-handoff.md`〈前置閘門〉「UC 主成功場景非空」「畫面狀態矩陣（涉及 UI 時）」兩檢查項的判定細節），屬新增判準，非落檔可解決，草稿已寫入本票 Problem Analysis〈階段 2 草稿〉
- **觸發位置**: `.claude/skills/tdd/references/doc-handoff.md`〈前置閘門〉表格列「UC 主成功場景非空」與「畫面狀態矩陣（涉及 UI 時）」
- **影響**: 不影響本票兩項 acceptance（已完成），但影響 W3-417 W2 開工閘門「全部範例零無依據句」的整體達成——此二則仍是無依據句
- **建議補料**: PM／WRAP 審閱 Problem Analysis 草稿，裁決是否採用、範圍是否需收斂（如是否排除純系統自動觸發步驟），裁定後另建或沿用本票落地
- **重派成本**: 低，草稿已完整，僅需裁決與落檔（預估 < 500 token）

## Exit Status
```yaml
exit_status: needs_context
reason: "9 項判定中 7 項階段 1 已落檔（2 項免補已生效、5 項補句補路由），2 項（D-12a 完整場景步驟動作+預期結果判準、D-12b 含使用者操作即涉及UI判準）條文全域查無、屬新增判準，草稿寫入 Problem Analysis 停手交後續 WRAP 裁決是否採用，未改檔"
confidence: 0.9
acceptance_met: [1, 2]
acceptance_unmet: []
artifacts: []
```

## Spawn Requests
- **SR-1** (2026-09-15 13:52)
  - what: doc-handoff.md 前置閘門計數不一致：執行步驟第 2 步與檢查清單寫三項全通過，前置閘門表為 4 列（第 4 列為涉及 UI 時的條件列）
  - why: W3-422 階段 2 WRAP 評估比對前置閘門表時發現
  - suggested_type: DOC
  - suggested_priority: P2
  - related_files: .claude/skills/tdd/references/doc-handoff.md
  - context: 
  - status: pending


## Completion Info
**Completion Time**: 2026-09-15 13:40
**Executing Agent**: thyme-documentation-integrator
**Review Status**: n/a
**Summary**: tdd skill 兩檔補條文依據：test-object-catalogue.md（斷言來源欄範例引言句補句；六、派發語言範本後補段落路由至 skill-design-guide chained-examples.md 與本 skill phase2-test-design.md）、doc-handoff-uc-to-gwt-conversion.md（分工邊界範例前補句路由至 doc-handoff.md 種子包表頭）。CHANGELOG.md 與 SKILL.md version 同步 2.8.0。9 項判定中 7 項階段 1（2 項免補已生效、5 項落檔）、2 項階段 2 草稿寫入 Problem Analysis 停手，未改檔。

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-417 | chars: 494 -->

### Task Reference
- 0.1.0-W3-417 what: 依用戶裁示以讀者探針為範例審查閘門跑第一輪實證：涵蓋本輪補過範例的 skill（component-contract-design、ux-design-evaluation、foundation-design、test-assertion-design、tdd、doc、version-bootstrap、dart-style-guardian、skill-design-guide），每則含判定的範例出題並含判準相反分支的對照版，題目凍結後各 3 探針作答，判定者先從條文寫出正確答案與依據句再比對；門檻 3/3；判定者同時標出依據只在範例內的判準

### Rationale Chain
- 0.1.0-W3-417 why: 用戶裁示：W2 開工條件為探針 3/3 通過且全部範例零無依據句；chained-examples 待本輪實證後再改

### Related Files
- docs/work-logs/v0/v0.1/v0.1.0/tickets/0.1.0-W3-336.md  # from 0.1.0-W3-417

### Source Acceptance (reference)
- [ref] [ ] 凍結題組覆蓋每則含判定範例且含相反分支  # from 0.1.0-W3-417
- [ref] [ ] 逐題判定表（3/3 生效或未生效）與依據只在範例內清單寫入 Solution  # from 0.1.0-W3-417
- [ref] [ ] 未通過與無依據句逐項建修補票或記錄豁免理由  # from 0.1.0-W3-417
- [ref] [ ] W2 開工閘門達成與否的結論  # from 0.1.0-W3-417
