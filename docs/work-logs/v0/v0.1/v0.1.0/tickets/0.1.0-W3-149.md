---
id: 0.1.0-W3-149
title: 修訂 SPEC-003 §2.2：ScanNotifier 與 ExternalOpener 的三時刻覆蓋子節
type: DOC
status: in_progress
version: 0.1.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.1.0-W3-139
discovered_during: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 0.1.0-W3-139 裁定雙落點，ScanNotifier 與 ExternalOpener 走修訂 SPEC-003
  final_decision: DOC 於 SPEC-003 §2.2 追加三時刻覆蓋子節
  rationale: 兩者的契約已在 SPEC-003 §2.2（系統層通知、外部開啟契約），追加覆蓋子節即可，另建規格會使同一個 port 的契約散在兩處
who:
  current: star-anise-system-designer
  history: {}
what: 修訂 SPEC-003 §2.2：ScanNotifier 與 ExternalOpener 的三時刻覆蓋子節
when: 0.1.0-W3-139 裁定雙落點之後；與 0.1.0-W3-148 可並行（不同檔案）
where:
  layer: Presentation
  files:
  - docs/spec/ui/SPEC-003-interaction-response.md
  - lib/services/scan_notifier.dart::read
why: '`0.1.0-W3-139` 裁定輸出端規格採雙落點。`ScanNotifier` 與 `ExternalOpener` 的契約已在 SPEC-003 §2.2 內（「系統層通知」與「外部開啟契約」兩個子節，含介面定義、權限 gate 三路徑、測試斷言表），故追加三時刻覆蓋子節而非另建規格——另建會使同一個 port 的契約散在兩處。


  該票盤點出兩處需在規格中處理：


  1. **`ExternalOpener` 的「呼叫發出」時刻，SPEC-003 現行條文未明述其日誌要求。** §2.2 外部開啟契約的「失敗可觀測」列只寫 `failed` 時記 warning log，未涵蓋呼叫發出當下。依 `INV-PORT-OBSERVE-001`，呼叫發出必須產生可觀測事件且不依賴外部系統是否回應——外部程序掛住不回時，現行條文下沒有任何記錄。`ExternalOpener` 尚未實作（`0.1.0-W1-068`），此刻補規格的成本最低。


  2. **`ScanNotifier` 的 `show` / `withdraw` 呼叫端回傳為 void**，`0.1.0-W3-139` 判定為 spec 的設計選擇（fire-and-forget）而非缺口。該判定成立但**須在規格中明說是選擇**，否則後續讀者無從分辨「刻意不回傳」與「忘了回傳」——依方法論的「不互相抵扣規則」，日誌有記不代表呼叫端已被告知，此處正是呼叫端刻意不被告知，理由要寫出來。'
how:
  task_type: Documentation
  strategy: '先讀 `0.1.0-W3-139` 的 Solution 取覆蓋表，**並讀其末尾的「[PM 覆核更正]」段**——監控追蹤欄的引用與語意有修正，不得沿用原寫法（正確寫法見該段）。


    追加位置為 SPEC-003 §2.2 內、既有「系統層通知」與「外部開啟契約」兩子節之後，**不整體後移既有章節編號**——`0.1.0-W3-108` 已實證 SPEC-004 有大量 `SPEC-003 §2.x` 形式的跨檔引用（§2.10 單獨逾百處），後移會全數失效。


    子節內容依 `0.1.0-W3-139` 裁定的分工線：方法論的三時刻定義與 `INV-PORT-OBSERVE-001` 以標題文字引用不重述，本規格寫兩個 port 各自三時刻由什麼承載、`ScanNotifier` fire-and-forget 為設計選擇的理由、`ExternalOpener` 呼叫發出時刻的日誌要求。


    `ExternalOpener` 的新增要求會成為 `0.1.0-W1-068` 的實作依據，寫成可驗收的形態（記什麼、什麼等級、含哪些欄位），不寫「應有適當日誌」這類無法否證的敘述。


    `ScanNotifier` 的四個方法（`authorizationStatus` / `requestAuthorization` / `show` / `withdraw`）逐一列出，不只寫 `show` 與 `withdraw`。


    SPEC-003 有 `## 變更歷史` 章節，依既有格式補一列；版號 `rg` 實查當下值（`0.1.0-W3-145` 已推進至 v1.12）。'
acceptance:
- '[x] ExternalOpener 呼叫發出時刻的日誌要求已寫為可驗收形態（記什麼、等級、欄位），非應有適當日誌'
- '[x] ScanNotifier 四個方法逐一列出三時刻承擔者，fire-and-forget 為設計選擇的理由已寫明'
- '[x] 監控追蹤相關敘述依 PM 更正寫為延後加重評條件，未寫 N/A'
- '[x] 未整體後移既有 §2.x 編號；方法論定義以標題文字引用未重述'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-09-09T09:34:21'
completed_at: null
created: '2026-09-09'
updated: '2026-09-09'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修訂 SPEC-003 §2.2：ScanNotifier 與 ExternalOpener 的三時刻覆蓋子節

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發日誌

- [2026-09-09 09:33] readiness=1 validate=0

### Commit 規範

Recommended: commit via isolated index (files must be a subset of this
ticket's where.files; never touches the shared index):
  ticket track commit <ticket-id> -m "..." -- {exact files}
In a linked worktree (files changed under a worktree dir, not the main repo):
add `--worktree <abs-worktree-path>` to the command above.
Fallback (not recommended; only if `ticket track commit` fails or is
unavailable) — use precise staging + verified bare commit only (no pathspec):
  git add {exact files}
  git diff --cached --name-only   # confirm index contains ONLY {exact files}
  git commit -m "..."             # bare commit; no -- <paths> / --only / -o / -a
Forbidden: git add . / git add -A; git commit -- <paths> / --only / -o / -a
  (pathspec-style commit discards the index and rebuilds it from working-tree
   content for the given paths — it silently absorbs unstaged edits other
   sessions may have on the same path, not just already-staged ones)
Before fallback commit: git diff --cached --name-only to check staged scope; git restore --staged <path> for any non-owned file
If bare `git commit` fallback is DENYed by bare-commit-guard-hook (another
dispatch active), stop and escalate to PM; never switch to pathspec /
--only / -o / -a to get around it.
If a commit is later found to have swept in out-of-scope content (e.g. a
peer session's staged changes):
Forbidden recovery actions: git revert; git reset --soft; git commit
--amend; any "reverse apply"/reapply of the diff. None of these may be
used to undo or rewrite the commit.
The ONLY permitted actions are: stop, record the commit SHA and file
list in the ticket, report to PM.
Reason: swept-in content is diff-indistinguishable from a peer's
legitimate concurrent write in the same window, so any of the forbidden
actions above would also undo the peer's legitimate work.

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: star-anise-system-designer
**Review Status**: pending
（必填：變更摘要——哪些文件或章節更新、新增或刪除）

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.1.0-W3-139 | chars: 1306 -->

### Task Reference
- 0.1.0-W3-139 what: 盤點 全部 driven port 的回饋點覆蓋，並裁定輸出端回饋契約的 spec 落點（來源已完成，結論見 Solution）

### Rationale Chain
- 0.1.0-W3-139 why: 用戶裁示輸出端與輸入端同一設計原則：「不只輸入端應該要有這樣的設計，那包括我們服務本身往外呼叫也是要有一樣的設計，無論是開文件，開資料庫，打 API」，且「也都應該要被列入測試」。

輸入端的落地路徑已完整：方法論（元件庫雙向約束）→ skill（component-contract-design）→ spec（SPEC-003 / SPEC-004）→ 測試策略。輸出端目前只有第一段——`INV-PORT-OBSERVE-001` 與 Port 回饋契約全文在 `.claude/methodologies/clean-architecture-implementation-methodology.md`，那是跨專案的框架方法論，**不記本專案有哪些 port、各自的回饋點是否已定義**。

`docs/spec/` 現有的四份規格全在 `ui/` domain，全部描述輸入端。輸出端無 spec 落點的後果是：驗收無專案側依據可引用，「列入測試」這件事沒有可否證的對象——審查者只能逐票回頭讀方法論再自行推導本專案該有什麼，這正是輸入端在 SPEC-004 逐元件回饋契約子節出現之前的狀態。

實查本專案的 driven port 為五個，覆蓋狀況不一：`WorkspacePreferencesHandle` / `WorkspacePreferencesPort` / `WorkspaceDirectoryProbePort`（`lib/workspace/workspace_repository.dart`，回饋契約由 0.1.0-W3-121 補齊，例外契約由 0.1.0-W3-135 待補）、`ScanNotifier`（`lib/services/scan_notifier.dart`，SPEC-003 2.2 有介面定義與可觀測性列，但未依 Port 回饋契約的三個回饋點編排）、`ExternalOpener`（SPEC-003 2.2 有契約表，實作票 0.1.0-W1-068 未動）。三者的定義散在三處且形態不一，沒有一個地方能回答「本專案全部 port 的呼叫發出時刻是否都可觀測」。

**為何 blockedBy 0.1.0-W3-133**：該票裁定是否建立 adapter 層與 ports 目錄約定，兩個視角意見相反。規格若先寫，就得描述一個尚未定案的結構，等於替 133 選邊。（來源已完成，結論見 Solution）

### Related Files
- docs/spec::read  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
- docs/domain-map.md::read  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
- lib/workspace/workspace_repository.dart::read  # from 0.1.0-W3-139（來源已完成，結論見 Solution）

### Source Acceptance (reference)
- [ref] [x] 五個 driven port 逐一對照三個回饋點的覆蓋表已產出，呼叫端／日誌／監控追蹤三欄分開判定  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
- [ref] [x] spec 落點裁定含 domain 選擇、新開或併入、與框架方法論的分工線三項，各附不選其他方案的理由  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
- [ref] [x] 承接的規格撰寫已建 DOC 票或已登記 spawn request（不停留在 pending）  # from 0.1.0-W3-139（來源已完成，結論見 Solution）
