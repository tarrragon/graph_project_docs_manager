# 讀者探針：已知限制清單

> **來源**：`0.1.0-W3-612`（方法重設計）。素材為 `0.1.0-W3-417` 二十餘批探針。
> **配套**：`docs/probes/probe-authoring-spec.md`（出題規格與驗收流程）。

---

## 收納判準

舊做法的發散來自「每個發現都開一張票」——單一 session 新建 82 張、關閉 35 張、淨增 41 張。本清單是停損機制：

> **只有會使判定翻面的發現才開票，其餘一律進本清單。**

「會使判定翻面」的定義：**若不處理，某一輪驗收的「整批通過／不通過」這個布林值會反向。** 逐條判別：

| 情形 | 處置 |
|---|---|
| 該缺陷會讓一道本該不通過的題被判通過（鑑別力歸零、字面通路、事實槓桿未清） | **開票**——它使通過變成假的 |
| 該缺陷會讓一道本該通過的題被判不通過（判準寫反、答案鍵誤、器材誤判） | **開票**——它使不通過變成假的 |
| 該缺陷只影響「這個結果是由哪一句造成的」 | **進本清單**——歸因不是預設產出 |
| 該缺陷只影響跨輪比較 | **進本清單**——拋棄式題庫不做跨輪比較 |
| 該缺陷是方法的固有代價，無處置手段 | **進本清單**，並在出題規格對應處寫明其後果 |

**不開票不等於不記錄。** 本清單的每一則都必須寫明：限制是什麼、它會讓哪一類結論失去支撐、出題與判定時要怎麼避開被它咬到。

---

## 一、方法的固有代價（無處置手段，只能避開）

### L1 事實槓桿不可完全消除

題目要完整就得交代其餘各項，而交代誘因項就生槓桿；單純刪句只是把槓桿從題幹移進讀者腦中（讀者自行補「未提及即相同」）。

**後果**：任何一題都可能殘留未被發現的槓桿，該題的 3/3 因此帶一個未排除的替代解釋。
**避開方式**：出題規格 3.4 的兩條出路擇一明選；逐句掃描跑兩輪（實測第二輪才抓到決定性的那一階）。

### L2 條文效力取決於位置，而位置判準的射程只到編號單位

「讀者取用的單位是程序中的一個步驟與有編號的款次」經可證偽檢驗成立，但**同一編號單位內末句仍會掉**（實測某款 6/6 被引用、其末句僅 1/6 被抄出）。

**後果**：把內容放進編號單位是必要條件而非充分條件；寫在款末的句子仍可能零套用。
**避開方式**：出題規格 1.4 的位置分類表；登記為位置風險者，零引用不得解釋為措辭問題。

### L3 邏輯上的排除不等於行為上的排除

已發生一例：三句在邏輯上已隱含排除某讀法，但**排除全部位於讀者選支之後**，而選支點的那一句單獨成立地指向被排除的那一支。判定為「已排除，但排除點不在讀者做選擇的那一句」。

**後果**：條文審查時「這裡已經排除了」這個判斷不可靠，須另問「排除發生在讀者的第幾步」。
**避開方式**：判定者須逐輪記錄讀者是在第幾步收斂的，而非只記結論對錯。

### L4 「零觀測」有兩種成因，紀錄上同形

沒測到（樣本不足，補測即可）與測不到（設計上同形，補測無用）在紀錄上長得一模一樣，而處置完全相反。實例：一項風險記為「跨七批零觀測、屬推論風險」，後查明是不可觀測——兩條路徑指向同一輸出值，以輸出值計分時同形。**每多測一批，都會讓人更相信它不存在。**

**後果**：拋棄式題庫下不再累積零觀測清單，故本限制的傷害面縮小；但單輪內的零觀測仍不得逕記為「沒發生」。
**避開方式**：任何「本輪未觀測到 X」的陳述，須同時說明本輪的設計看不看得到 X。

### L5 探針個體差異不可控

同一份條文、同一份題幹、同一份節錄，不同輪次的探針讀數會移動。累積式題庫下實測零變數對照組單批位移達兩輪。

**後果**：單輪 3/3 帶有噪音。
**避開方式**：門檻定在 3/3（全對）而非多數決——全對對噪音的容忍度最低，但它是唯一不需要噪音估計的門檻。拋棄式題庫不做跨輪比較，故不再需要噪音量尺。

### L6 逐題獨立 context 消除跨題帶入，不消除題內跨子問帶入

同一題的子問共用 context（這是刻意的，切到子問級即改寫題目，見出題規格 5.1）。

**後果**：同一題內前一個子問的填法仍可能影響後一個。
**避開方式**：同一題的子問之間不設互為對照的配對；需要對照時分成兩題。

---

## 二、量測工具的盲區

### L7 字面比對法量不到結構對應

窮舉子字串法只量字面。逐項可對應而幾乎無共用字串的**結構對應**是盲區——已實測一例（條文的枚舉與題幹的描述逐項可對應，共用字串近乎為零）。

**後果**：「零新增命中」不等於「零通路」。
**避開方式**：出題規格檢查清單第 17 項，機械檢查全綠後仍須人讀複核一次並寫入答案鍵。

### L8 `MIN_LEN` 參數敏感

調小讓更多題落入「字面對應」，調大讓更少。已實測一例落在參數邊界（兩詞只共用單一字元，恰在最小長度之外）。

**後果**：所有字面判定都應視為「該參數下的結果」。
**避開方式**：同輪固定一個值並寫進記錄。跨輪不比較，故不需跨輪一致。

### L9 例示 span 定義本身沒有對照輸入可驗

能驗的只有「植入的詞報得出」（規則 E2 的正向對照輸入），驗不了「span 涵蓋所有例示位置」。**這一格至今是空的**——下一個被漏掉的位置類型不會有人注意到。已知踩過的位置類型：破折號列舉（工具第四版才撈到，而那正是最強的一條通路）、同詞兩處出現（一次舉例一次規範）。

**後果**：2.1 的檢查通過不等於例示詞全部被查過。
**避開方式**：新增位置類型時同批補一則對照輸入；已知類型清單寫在出題規格 2.1。

### L10 過濾條件、丟棄規則、span 定義都在「處理」之前發生

三者都不在任何檢查的射程內——工具報出的每個數字都正確，只是都建立在沒人檢查過的子集上。此類問題已發生三次。

**後果**：工具自報的分母不可信。
**避開方式**：出題規格 4.4 三項（`dropped` 欄、守恆式、過濾條件的對照輸入）。分母一律以實際檔案數對帳，不取工具自報值。

---

## 三、判定層的限制

### L11 答案鍵可能忠實繼承條文的缺陷

已發生一例：三輪讀者、答案鍵、條文三者完全一致，而三者一致地與該節首段矛盾——出鍵者從條文推出的正解就落在被禁止的狀態上。**任何以答案鍵為準的量測都不可能發現它。**

**後果**：答案鍵不是獨立的真值來源。
**避開方式**：兩名判定者各自**先從條文獨立推出正解與依據句，再讀答案鍵**；兩人推導與答案鍵三者不一致時，該題結論降級為「條文缺口」而非「讀者失分」。

### L12 判準若與被測量相關，會在最需要讀數時宣告無法測量

已明文否決一項候選訊號，理由是以它作判準會讓被測失分形態的每一輪自動不可判讀——**判準吃掉被測量**。

**後果**：判準設計須與被測量正交，而正交性沒有機械檢查法。
**避開方式**：每新增一條判定判準，先問「被測量出現時，這條判準會不會系統性地輸出不可判讀」。

### L13 未明文否決等於留給判定者自行決定

兩名判定者在同一組資料上得出不重疊的有效樣本，成因正是判準只寫了「什麼算命中」，沒寫「什麼不算，以及為什麼」。

**後果**：判定者分歧本身是判準缺陷的表現，不是判定者品質問題。
**避開方式**：判定者 brief 必須同時列「什麼不算問題」。兩人分歧時先查判準有無留白，再查資料。

### L14 「不可判讀」是器材看不到，不是讀者沒做

把觀測到的讀者行為計為不可判讀，等於把觀測到的事實登記成沒觀測到。

**後果**：不可判讀的類別必須逐項枚舉且封閉，不得當作兜底格。
**避開方式**：判定者 brief 明列可判為不可判讀的型別；其餘一律計分。

---

## 四、行為性的限制（對每一種讀者都成立）

### L15 最省力的路徑決定實際行為

已實測三種讀者同形：

| 讀者 | 省力路徑 | 後果 |
|---|---|---|
| 探針 | 一句字面可對 vs 一條抽象規則要先分類 | 取前者 |
| 代理人 | 答案鍵裡有一欄歷史讀數（一行看到）vs 判定報告要另開檔案 | 取前者，而前者落後一批 |
| PM 本人 | 同一組讀數的兩個計量口徑 | 引用方便的那一個，未標明 |

**後果**：只要有兩個來源而其中一個較省力，較省力的那個就會勝出——無論它是否正確。
**避開方式**：正確來源要放在比錯誤來源更省力的位置。拋棄式題庫已移除歷輪對照欄（最主要的一個錯誤省力來源）；引用任何通過率一律標明口徑，兩口徑並存時並列不擇一。

### L16 差集法只抓得到新開的問題

以「本次改動的新增字面」為差集基準的盤點，會漏掉來源條文句從未改過的通路——差集下為零。已實測漏掉五題。

**後果**：「本次變更掃描」與「全量掃描」不可互相替代；前者隱含假設「存量沒問題」，而那個假設沒有被檢查過。
**避開方式**：拋棄式題庫每輪題目全新，全量掃描的成本等於變更掃描，故一律全量。出題規格 4.2 不設差集基準。

### L17 修正動作本身會製造新缺陷

三種已實測形態：修法措辭繼承實例題材使該題退化為字面比對（兩例）；補句使通過率由 2/3 降為 1/3；補句與另一款的問法衝突。

**後果**：改文不是單調改善。
**避開方式**：出題規格 2.3（本輪改動引用的實例題材進禁區）與 4.1 第 2 條（改文側字面檢查）；驗收流程「什麼情況才需要歸因」第 2 項（同一款次連續兩輪不通過須確認新措辭沒有擋錯東西）。

---

## 五、本輪新增（依收納判準逐則登記）

（新發現依判準判為「不開票」時追加於此。每則須含：限制是什麼／會讓哪一類結論失去支撐／出題與判定時如何避開。）

### L18 8.2(4) 與 8.2(2) 第二點的量詞不對稱，現行條文下不改變任一輸出

`0.1.0-W3-569` 診斷：8.2(2) 第二點「各自旁邊沒有文字標明」只要一個元素旁邊有文字即不成立（存在量詞）；
8.2(4) 與依它設定的前置關要求每個元素旁邊都有（全稱量詞）。只有一部分元素旁邊有標明文字時，前置關不擋下、
8.2(4) 不給豁免，而第二點也不成立，兩條路徑的輸出同向（皆判不違反），只是理由不同。

依收納判準，這不會讓任何一輪的整批布林反向，故不開票、不改文。該票自身的 rationale 亦明寫「併入改文會使
改文票背一個無讀數支撐的改動」。**前提**：它依賴「存在與否」這一清單項的射程不被收窄；該項一旦被界定句限縮，
兩款的不同資格會同時暴露且無條文可裁。

**後果**：任何「8.2(4) 與第二點對同一畫面給出一致資格」的陳述都沒有條文支撐，只是現行題材下碰巧同向。
**避開方式**：出題時不以「只有一部分元素旁邊有標明文字」為題材；該題材出現時，判定者不得把兩條路徑的同向
視為條文一致的證據。「存在與否」項的界定句若在後續輪次被改，本則須重新評估。

### L19 同一要件同時寫在程序步驟與清單引言時，引用位置無法區分

`0.1.0-W3-613` 改文後，「這項差異與這些語意有沒有對應關係」這個要件同時存在於兩處：8.2(2) 第二步 (乙)
（程序步驟，本輪新增，為使讀者在判定流程上取用得到）與 8.2(3) 引言（清單的界定，既有）。這是刻意的重複——
`0.1.0-W3-543` 實測純路由句擋不住失分，要件的內容必須落在讀者行走路徑上的那個步驟裡。

**後果**：讀者引用其中任一處都算引用該要件，讀數無法分辨他是在程序步驟取到的還是跳到清單引言取到的。
因此本輪之後，「位置前移是否有效」這個問題在本要件上不再可量測——兩處並存使前移與未前移不可區分。
**避開方式**：本要件的覆蓋題只驗「有沒有執行這一步」（作答有沒有逐項寫出對應關係的判斷），不驗「依據句引的是哪一處」；
判定者 brief 須明列「引 8.2(3) 引言與引 8.2(2) 第二步 (乙) 同等計分」，避免兩名判定者各自給出不重疊的有效樣本（見 L13）。

### L20 「題幹 ∩ 條文交集為空」對兩類受測步驟不可滿足

`0.1.0-W3-616` 首次完整執行出題規格時實測：檢查清單第 11／12／13 項要求題幹與受測款次的交集為空，但下列兩類
受測步驟無論換幾次題材都達不到——受測步驟本身即是條文字串的實例化：

| 類型 | 為何交集不可能為空 | 本輪實例 |
|---|---|---|
| 受測步驟是「某份逐項清單有沒有寫完」 | 題幹必須逐項實例化該清單的項名，讀者才看得出漏了哪一項 | 7.2(3-2)①乙 的窮盡要求 |
| 受測步驟是「某個特定援引動作被禁止」 | 題幹必須呈現該動作，而條文正是以同一組詞指名它 | 7.2(4-3) 的互不援引 |

**後果**：這兩類題的「字面通路已清」無法由機械檢查證成；它們的通過率帶一個未排除的字面對應解釋。
**避開方式**：把命中逐一分為三類再判——(i) 指涉性字串（款號、節名，用來指出受測款次，不提供答案）、
(ii) 前置步驟的既定事實（題幹必須交代前一步已執行）、(iii) 題幹字串直接等於受測款次的判定語或結論語。
只有 (iii) 是紅燈。分類結果須逐題寫進答案鍵，不得只記「已檢查」。
已收進出題規格 4.6：第 (iii) 類改為位置判準（判定語 span 事先標定），另附四條防止分類本身成為新的靜默過濾的配套（逐則附落在哪一句、(ii) 須指名對應的前置款次與規定其順序的那一句、分類器配正向對照輸入、失分時先重查分類）。

### L21 指名覆蓋題與出題禁區可互相矛盾，且矛盾不會被「零覆蓋清單為空」攔下

`0.1.0-W3-613` 的改動清單為每一條改動指名了覆蓋題並宣告零覆蓋清單為空，但其中兩處在出題時才發現不可執行：
8.2(4) 新增的全稱量詞句，其唯一可能題材被 L18 明文列為禁區；doc 第五列承載者資格的指名覆蓋題，其題材
（「兩元件之間只隔著頁面根堆疊」）正是同一張票自列的禁區詞。

**後果**：「零覆蓋清單為空」這句話不構成「每一條新增文字都被驗過」的證據——指名的那道題可能出不出來。
**避開方式**：Step 1 指名覆蓋題時，同批對照禁區清單與已知限制清單逐條檢查一次；指名與禁區衝突者當場列入
零覆蓋清單，不留到 Step 2 才發現。本輪的兩處已依此改列（見 `0.1.0-W3-616` 答案鍵末節）。
已收進出題規格 Step 1 放行判準（`0.1.0-W3-617`）。

### L22 條文內另有充分理由時，覆蓋題可被旁路

`0.1.0-W3-616` 實測：7.2(3-2)①乙（窮盡要求）的覆蓋題，其正解「本步未完成」在同一款次內另有一條充分理由
——該款前段的 (a)(b)(c) 逐項檢查同樣未執行。三輪中有一輪整份依據句只引前段、從未觸及乙目，結論仍正確。

**後果**：判定者若只比對結論，該輪會被記為通過，而受測款次實際零套用；被測改動的覆蓋率因此被高估。
**避開方式**：出題時檢查「正解在同一款次內有沒有第二條充分理由」，有就改題材使其餘理由不成立；
判定者必須分開記「引用幾輪」與「套用幾輪」（規格 1.6），不得只看判定欄。
已收進出題規格 1.7（遮蔽推導 + 四條出路）與 5.5 第三條計分紀律（套用為零的輪次記不通過），
`0.1.0-W3-617`。**射程更正**：第二條充分理由不限於同一款次內——遮蔽推導的對象是受測款次以外的全部條文。

### L23 派發路徑打錯到一個碰巧存在的目錄時無訊號

`0.1.0-W3-616` 實測：scratchpad 的專案目錄段有連字號與底線兩種拼法，而**兩個目錄都真實存在**
（底線版是更早批次的遺留）。路徑在派發 prompt 裡是逐次複製的字串，複製到錯的那一版時，
探針拿不到「目錄不存在」這種明確錯誤——它會在錯的樹下把目錄建起來，讀不到題目就開始猜或發問，
而輸出檔可能寫進一個沒有人會去讀的地方。本輪的實際損害為零（錯的樹下只留下三個空目錄、零檔案，
51 份作答全數落在正確路徑），但無訊號這一點與規格 §4.4 要擋的「靜默丟棄」同形：
數字全部正確，只是建立在沒人檢查過的子集上。

**後果**：分母（應有的作答份數）與實收份數的差，無法從任何一份輸出看出來；
派發進行中對 `out/` 取計數還會讀到一個必然偏低的瞬時值，把「還沒跑完」與「寫錯地方」讀成同一件事。

**避開方式**：三項，成本都接近零——(1) 派發前對輸出目錄做一次寫入探測（建一個哨兵檔再刪），
路徑錯到別處時當場失敗；(2) prompt 裡的路徑由腳本從單一變數產生，不逐次手打；
(3) 收齊後以「題數 × 輪數」對帳實收檔數並逐檔驗非空，**且只在全部批次都回報完成之後才對帳**
——中途的計數是瞬時值，不是缺口。
已收進出題規格 5.8（`0.1.0-W3-617`）。

### L24 條文內的第二條路徑可指向錯誤答案，而非只是旁路

`0.1.0-W3-617` 定位 `0.1.0-W3-616` Q15 一輪失分時查明：`doc` 射程判定表第二列的依據欄逐字引了一句
含 disjunction 的原句（「內距屬子件的組合規則**或**所屬容器的格位」），其第二支正是第三列的答案，
而引用時未說明本表已把該 disjunction 拆成兩列；同列管轄欄的「填在該列」在本表內又無指涉對象。
讀者依本輪新增的第二／三列差別句**正確認出第二列**，再被這兩處帶回「填在所屬容器的格位」。

**與 L22 的差別**：L22 的第二條路徑指向**同一個正解**（故失分不可見，要靠套用計數才抓得到）；
本則的第二條路徑指向**相反的答案**，因而直接翻成失分。兩者的共同結構是「受測款次之外另有一條可走的路」，
規格 1.7 的遮蔽推導對兩者皆有效——遮蔽後推得出**任何**結論者都應處置，不限於推得出正解者。

**後果**：援引外部原句作為依據欄時，若該句的選項在本文件已被拆分或限縮，逐字引用會把已經關掉的支路重新打開。
**避開方式**：依據欄引用含「或」「以及」等選項結構的外部原句時，同批註明本文件對該結構做了什麼處置；
出題時把依據欄視為讀者行走路徑的一部分（位置分類比照 1.4 的「在編號款次內、非末句」一列，不得當作表外散文而不出題）。

### L25 位置前移型的改動在 1.7 下必然零覆蓋

`0.1.0-W3-618` 實測：D2（在射程判定表第二列依據欄補一則「該句的『或』在本表已拆為第二列與第三列」的註記）
是把表內既有的互斥結構複述到讀者會走到的另一個位置。遮蔽推導遮掉該依據欄之後，管轄欄、第三列與表外綜述句
三者合起來仍可推出同向的確定結論，1.7 判紅；1.7 的出路中 (a)(b)(c) 皆不適用（換題材改不掉本表結構、
交出該前提等於交出受測步驟的結論、換款次等於放棄測該改動），只能走 (d) 登記為位置不可區分，
依該條該題不得列為覆蓋題，D2 因此登入零覆蓋清單——即使它的覆蓋題實測 3/3。

**與 L22 同向，差別在有沒有被擋下**：1.7 判紅的實質語意是「這題對受測步驟沒有鑑別力」，(d) 免掉的是
處置義務（因為 (a)(b)(c) 皆不適用），不是回復鑑別力。故本則與 L22 同屬覆蓋率被**高估**，差別在 L22 藏在
通過數字裡無人發現，本則已被零覆蓋清單這道形式記帳擋下一半。**不得把走 (d) 的題描述為「題目有效」**——
它的讀數只證明該位置會不會被取用，不證明該改動被驗到。

**前提（兩項，任一被收窄則本則須重新評估）**：

1. 本則依賴「1.7 第 (d) 條的『複本』涵蓋既有結構在另一位置的複述」這個解釋。(d) 的原文寫的是「同一句話
   刻意寫在兩處」，而本輪 D2 的替代理由是管轄欄、第三列與表外綜述句三者**合起來**——這是一次射程擴張解釋，
   不是 (d) 的字面。**已由 `0.1.0-W3-619` 界定**：(d) 的射程改錨在 Step 1 的改動類別（丙）複述型，不由字串
   相似度界定，故本則所依賴的解釋成立；同批新增 1.7 與 Step 1 的雙向分類對帳——1.7 判同向紅而 Step 1 預判
   （甲）或（乙）者、判綠而預判（丙）者，皆屬分類有誤須回 Step 1 更正（D2 原標（甲），依該表應更正為（丙））。
   **解除的只有本前提的不確定性，本則限制本體不變**——（丙）類改動仍拿不到覆蓋題。
2. 「必然」是由 1.7 出路逐條演繹得出（(a)(b)(c) 對位置前移型改動皆不適用），**不是讀數結論**；本輪只有
   D2 一個實例，其他出路本輪未探。

**後果**：凡改動的形態是「把既有要件補寫到讀者行走路徑上的另一處」（L2 與 L19 所指的位置決定效力這一類），
依上述演繹，1.7 判紅且只剩 (d)，該類改動拿不到覆蓋題。「零覆蓋清單為空」對這類改動不可能成立。
走 (d) 的題是否計入 Step 3 布林的分母**已由 `0.1.0-W3-619` 界定為計入**（它失分同樣使布林反向；退出分母會讓
一道本該不通過的輪次被判通過，且全輪皆 (d) 時分母為空而空集合恆真）。代價由三處承接：布林語意寫死為「本輪出的
題全數達標」而非「改動全數被驗證」、Step 3 產出增列覆蓋比且禁止單引布林、全部改動皆零覆蓋時不得產出布林而回
Step 1。
**避開方式**：Step 1 指名覆蓋題時即辨識改動是否屬位置前移型，是則當場列入零覆蓋清單並註明成因為 1.7 的 (d)
而非缺題；讀數仍照跑，作為該位置實際會不會被取用的證據，但不寫成「該改動已被覆蓋」，也不寫成「該題有鑑別力」。

### L26 4.6 的三類對「受測步驟判定對象的實例化」沒有格

`0.1.0-W3-618` 實測：R2-Q03 的受測步驟是「某份逐項清單有沒有寫完」（L20 第一類），題幹必須逐項實例化
乙目的項名（輪廓由哪幾條線構成、相接處是不是角、閉不閉合等），這些命中既不是 (i) 指涉性字串、
也不是 (ii) 前置步驟的既定事實——它們是受測步驟**判定對象**的實例化。

**前提**：本則以 4.6 現行的分類集合為準；該節新增類別時須重新評估。

**後果**：此類命中只能靠「不落在判定語 span 內」被放行，而 4.6 配套 1 要求逐則寫進答案鍵並標類別，
標類別時無格可填；勉強歸入 (ii) 會使配套 2（須指名對應的前置款次）指不出來，依該條應降為 (iii)，
與位置判準的結論相反。
**避開方式**：位置判準（命中在不在事先標定的 span 內）優先於類別標籤；答案鍵逐則記「落在條文的哪一句」
並註明屬判定對象的實例化，不硬塞進三類。**但此優先序是本輪的自訂規則，不是 4.6 的字面**——§7 第 11 項
記為通過時，須同批註明「本項對 N 則命中以此規則放行」，不得只記 class (iii) 命中為零。

**與 1.7「不遮 span」的關係**：`0.1.0-W3-619` 在 1.7 為同一個概念命名（遮蔽推導時受測步驟的判定對象不遮，否則本關對每題轉綠），
但那只約束遮蔽範圍，未給 4.6 新增類別。本則的缺口不因此消失——4.6 命中落在判定對象的實例化時，仍依本則的自訂優先序
（位置判準優先於類別標籤）放行，並於 §7 第 11 項同批註明以此規則放行的則數。

### L27 判定者的「引用」讀數在現行計分下無後果，其反向組合未定規則

`0.1.0-W3-618` 實測：規格 1.6 要求引用與套用分開計，而 5.5 的「受測款次套用為零的輪次記不通過」一條
只以套用為零觸發不通過，通過與否完全由「判定欄 + 套用」決定，引用讀數不進入任何判準。兩名判定者各自獨立
指出這一點。現行文字未規定的是反方向的組合——**引用為無而套用為有**（沒引那句話但確實執行了那個動作）
算不算通過。

**後果**：1.6 舉的兩種相反形態中，「照做不引」這一種在現行計分下沒有歸屬；`0.1.0-W3-618` 九輪的兩讀數零分歧，
這個空格未被觸發，故也未被發現於讀數之中。
**避開方式**：判定者 brief 明寫「照做不引」的計分（後續輪次若遇到，依 5.5 只罰套用為零的字面，記通過並另記引用為無）；
任何以引用讀數為據的結論都須標明它不影響布林。

### L28 遮蔽 span 錨在極小集合依據句欄，與「改動以條為單位」粒度不一致

`0.1.0-W3-620` 實測（規格 1.2.0 四條新條文的首次執行）：本輪唯一的改動 C1 是 7.2(3-2)② 乙寫法
比對與合成規則的重寫，含多句；其中只有兩句進入答案鍵依據句欄的極小集合（推出正解所必需者），
其餘句——特別是「本款的結論只有三種，互斥且窮盡，不得由排除其中一種逕行認定另一種」——依 1.7
硬性要求 1（極小集合）不得列入依據句欄，因而不被遮，落入「其餘條文」。判定者甲遮掉登載的
遮蔽 span 後，正是靠這一句推得與正解實質相同的結論，判**同向紅**；出題者側判綠，依裁決力條款
取紅。判定者甲在報告中自行指出這一點：若該句與被遮兩句同屬本輪改動，則同向紅是遮蔽登載過窄的
產物，而非條文冗餘。

**與 L25 的差別**：L25 的成因是改動本身不引入新判定（複述型），覆蓋率被**高估**；本則的成因是
遮蔽粒度（句）與改動粒度（條）不一致，一條真有新增內容的改動被判零覆蓋，覆蓋率被**低估**。

**1.7 雙向對帳表在此給不出正確出路**：該表把「同向紅而 Step 1 預判（甲）／（乙）」單一歸因為
「該新增內容沒有引入任何原本推不出的判定」，處置是改寫該條改動或改標（丙）。C1 確有新增內容
（三格合成、「無從比對」的界定、禁止以無內容記為不同，改文前條文對此三者皆推不出），改標（丙）
即是把一條有鑑別力的改動預列零覆蓋，正是該表第二列自己明文禁止的後果。

**後果**：凡一條改動含多句而只有部分句為推出正解所必需者，1.7 必判同向紅，該改動拿不到覆蓋題；
改動總數為 1 時 Step 3 下界隨即觸發而不得產出布林。「零覆蓋清單為空」對這類改動同樣不可能成立。
**避開方式**：出題時把受測步驟選在「該條改動的全部句子都是推出正解所必需」的題材上（本輪未做到）；
在規格層改遮蔽粒度之前，同向紅出現時須先分辨兩個成因——未遮的替代理由是否來自同一條改動——
並把該分辨結果寫進答案鍵，不逕依對帳表改標（丙）。本則已寫成下一輪 Step 1 的規格層輸入。

### L29 零覆蓋回報欄由兩名判定者填寫，而 1.7 推導只由其中一名執行

`0.1.0-W3-620` 實測：規格 1.7〈判定者 brief 的必備項〉第 4 條要求判定者在結論行填零覆蓋回報欄，
而外部驗證（遮蔽推導）依同節只由「其中一名判定者」執行。本輪判定者甲依其 1.7 同向紅判讀回報
「本題不得列為 C1 的覆蓋題」，判定者乙未執行遮蔽推導、依鑑別力直觀回報「有鑑別力，可列為覆蓋題」，
兩人相反。兩人填的不是同一個量：一個是 1.7 的判讀結果，另一個是對題目鑑別力的整體印象。

**後果**：兩份回報並列時看似判定者分歧，實為判準留白（L13 的實例）；若取多數決或取先到者，
1.7 的裁決力即被一個未執行該關的讀數稀釋。
**避開方式**：零覆蓋回報欄只由執行遮蔽推導的那一名判定者填寫，另一名的 brief 不列該欄；
或兩人皆執行遮蔽推導。本輪的處置是以執行該關者（判定者甲）的判讀為準，並記錄此分歧的成因。

### L30 改動橫跨款次時須拆條，而拆後各條互為「其餘條文」

`0.1.0-W3-621` 把 1.7 的遮蔽 span 與改動粒度對齊（來源 (b)：本輪該條改動的新增或改寫字串所在整句），
代價是遮蔽 span 可能含其他款次的字串，牴觸 1.7 硬性要求 2（遮蔽 span 不含其他款次的引句）。
規格的處置是在 Step 1 要求一條改動不得橫跨款次，跨款次者拆為多條。

**後果**：拆條之後，同一份語意分寫兩款的兩條改動**互為其餘條文**——遮掉其中一條時另一條仍可見，
L28 那個形態即以拆條的形式重現，而此時它不是登載過窄（兩條確實是兩條），成因分辨的第二格
（關鍵可見字落在本輪另一條改動之內）正是為此而設。**本輪零觀測**：C1' 未橫跨款次，本輪的設計
看不到這個形態，依 L4 不得記為「沒發生」。
**避開方式**：Step 1 拆條時記明哪幾條源自同一次語意修改；1.7 判同向紅而關鍵可見字落在其中另一條時，
依成因分辨第二格處置（合併重跑或另備一題分離），**不得改標（丙）**。

### L31 正解為「本款不輸出結論」時，1.7 的第三格與計分欄的記通過條件措辭撞車

`0.1.0-W3-621` 以新標定回測 `0.1.0-W3-620` 的 Q01 時發現：該題正解是「本款不輸出兩者是不是
同一個圖形的結論」，而遮蔽推導的第三格是「推不出任何結論」。兩者在自然語言中的表述會撞在一起
——「判不出來」同時是兩者的措辭。W3-620 的計分欄把「② 判不出來」列為記通過的等值表述，
遮蔽後的「推不出」因此自動滿足第一格（足以得分）。

**後果**：三格退化——本關對每一道正解為「不輸出結論」的題必判同向紅，而該紅與真正的旁路
在紀錄上不可區分。粒度對齊（L28 的處置）只關掉其中一條路徑，不關掉這一條；C1 回測必須兩者
同時處置才由紅轉綠。
**避開方式**：計分欄的記通過條件寫成「把它記為**條文給出的結果**」並指名依據的那一格；記不通過
枚舉中明列「答『條文未規定／推不出／不知道』者記不通過」。已收進出題規格 1.7 判讀段
（`0.1.0-W3-621`），同節另明訂遮蔽推導只對照計分欄中標為「結論內容」的條件——標為「依據句」
的條件（引到某一格、依據句零觸及）在本關不適用，被遮的那一句必然引不到，照套會使本關恆判綠。

### L32 作答回報成功而輸出檔未落地，只有對帳看得見

`0.1.0-W3-621` 實測：三輪派發中有一輪的探針在最終訊息裡交出了完整的四欄作答，但未寫入指定的
輸出路徑。派發側拿到的是一則成功回報，內容齊備、外觀與其餘兩輪無異；缺口只在「實收檔數 ＝
題數 × 輪數」這一步才浮出來（實收 2 ≠ 應有 3）。

**與 L23 的差別**：L23 是路徑打錯到一個碰巧存在的目錄（檔案寫了，寫在別處）；本則是檔案根本
沒寫，而作答確實產生了。兩者的共同點是派發側的回報看起來都成功。
**後果**：不跑對帳時，該輪會以「已完成」計入而其讀數不在任何檔案裡；若事後才發現，重跑會得到
另一份讀數，與原讀數不是同一件事。
**避開方式**：5.8 第 3 項的對帳照跑（本輪即由它抓到）。補救時**不重跑**——以 SendMessage 要原
agent 把它已寫好的那份作答原樣寫入檔案，不得更動、不得重推，否則登記的就不是已觀測到的那份
讀數（L14）。

### L33 零改動輪（回歸讀數輪）不在規格射程內，覆蓋比無定義而下界 vacuously 觸發

`0.1.0-W3-623` 實測：本輪受測款次 7.2(3-2)①乙 自 `0.1.0-W3-613` 之後逐字未動，本輪不改條文，
改動總數為 0。照字面套用 Step 3：覆蓋比的分母＝改動總數＝0，分子＝0−0＝0，**0/0 無定義**；
下界的條件「本輪改動全數落在零覆蓋清單」在空的改動集合上 vacuously 為真，**下界觸發、不得產出布林、回 Step 1**。

下界條款的理由逐字是「沒有受驗對象時產出的任一個值對改動零資訊」。該理由在零改動輪成立
（本輪確實對改動零資訊），但推不出它的處置——本輪布林的受驗對象不是改動而是既有條文，
布林仍帶資訊；而「回 Step 1」的語意是去改條文，本輪並不打算改。

**根因**：規格 §6 的形態寫死為「批次改文一次 → 現出題一次 → 驗一次」，Step 1 的產出定義為改動清單。
零改動輪是規格自己製造出來的需求——L4 要求零觀測必須補測，而補測一條既有條文必然是零改動輪。

**後果**：這類輪次照字面走會產不出任何可引用的彙總值；照「不管它」走則會產出一個分母為 0 的覆蓋比。
兩者都不對，而規格對此沒有形態。
**避開方式**：零改動輪不產出改動驗證布林、不產出覆蓋比，改產出**回歸讀數**（各輪計分、引用讀數、套用讀數）
並逐項寫明它證明什麼、不證明什麼；下界的觸發如實記錄並註明它在該輪是誤擋。
依收納判準不開票——它只影響記帳形態，不使任一道題由通過翻不通過、也不使任一道題由不通過翻通過。

### L34 2.1 的例示 span 定義不分「規範性破折號枚舉」與「例示性破折號列舉」

`0.1.0-W3-623` 實測：2.1 的 span 定義列出破折號列舉、頓號列舉、「例如／如」引導句三類位置，
但受測條文 7.2(3-2)①乙 的六項清單本身就是一個破折號枚舉（「改逐項寫出輪廓本身——由哪幾條線構成、…」）。
把它當成例示 span，其項名即成例示詞，而受測步驟是「這份清單有沒有寫完」，題幹必須逐項實例化那些項名
——§7 第 5 項的「交集為空」因此不可滿足。這是 L20 的形態延伸到第 2 節：L20 只處理了 4.2 與 4.3，
4.6 的射程也明文不及第 2 節。

**本輪的自訂判準**：破折號列舉只有在帶開放集合標記（等／這類／之類）時才算例示；
被本文明訂為封閉的枚舉屬規範枚舉，不算例示。該判準依 4.4 第三項配了正向對照輸入
（7.2(6) 的「程度、數量、設定值等…」應被納入），**而該對照輸入第一版即判 false**——
過濾規則當時只看尾端六字，漏掉列在中段的標記，修正後才通過。過濾條件確實會寫錯，且不配對照輸入時無訊號。

**後果**：2.1 的通過與否取決於一個規格沒有定義的判準；不同輪次的執行者可能各自劃線，
而劃線方式決定「交集為空」這一項是綠是紅。
**避開方式**：出題時把破折號列舉的分類判準寫成可複核的規則並配正向對照輸入，逐輪記錄；
規格層的處置（在 2.1 補這個判準）寫成下一輪 Step 1 的輸入。
依收納判準不開票——本輪已由 4.4 第三項的對照輸入擋下該過濾規則的錯誤，且它不使任一道題的計分改變。

### L35 計分欄的記通過條件與被禁的那一格措辭同形時，未寫如何記

`0.1.0-W3-624` 實測：該輪計分欄的 C1（條文明文給出的處理）與 N1（誤歸無從比對而依合成第三格宣告不輸出）
分屬兩格，分界寫在「成因與後續動作」；但有一輪作答**直接沿用第三格的原句措辭**（「這一款此刻不輸出兩者是不是
同一個圖形的結論」）而成因與後續動作皆正確。計分欄沒有為這一格寫明歸屬。

**後果**：兩名判定者可能各取一邊——一邊看措辭、一邊看成因，而這兩個依據在該輪指向相反的計分。
本輪兩名判定者依「成因＋後續動作」的明文分界一致記通過，未觸發。
**避開方式**：計分欄在列出「C1 與 N1 不得合併」的分界時，一併寫明「沿用被禁那一格的措辭而成因與後續動作
皆正確者落哪一格」。依收納判準不開票——本輪兩名判定者一致，不使任一道題的計分改變。

### L36 受測款次與其前置款次在同一題上結論相同時，引用與套用兩個讀數指向相反的計分

`0.1.0-W3-624` 實測：受測款次（7.2(3-2)② 的整項未寫處理句）與其前置款次（①乙 的漏寫句）在該題上
推出同一個結論。若某輪只引前置款次而不引受測款次、結論仍正確，brief 的豁免（照做不引記通過、引用記無）
與計分欄的記不通過（受測款次零套用）會指向相反結果——爭點在於「他套用的是哪一款」在作答上不可區分。

**與 L19 的差別**：L19 是同一要件寫在兩處（複本），本則是兩個不同款次在特定題材上結論重合。
**後果**：該輪的計分取決於判定者先看哪一條，而兩條都在 brief 裡。本輪三輪皆逐字引了受測款次，未觸發。
**避開方式**：出題時挑「前置款次推不出正解」的題材（1.7 的遮蔽推導本來就會把這類題材判同向紅，
見本輪的實測）；或在計分欄明訂兩者衝突時的優先序。依收納判準不開票——本輪未觸發，不使計分改變。

### L37 登載底稿之外的同款次規範句不在分區複核的射程內

`0.1.0-W3-624` 實測：三個登載項的總和只等於**登載底稿**（依據句欄 ∪ 本輪改動的整句），
而受測款次內未被改動、也不在依據句欄的其餘規範句（該輪為「逐項比對只在該項兩端都寫出內容時進行」、
「兩端都寫『看不出』的項，略去不比…」與合成三格句）既不在遮蔽 span、也不在不遮 span——
它們是**未登載**，不是**登記為不遮**。分區複核（硬性要求 4）只查不遮 span 內有沒有規範內容，
對未登載的句子零射程。同輪另一個表現是：不遮 span 為空時，該複核在該題零鑑別力
（與 `0.1.0-W3-618` 三題同形，依 L4 不得記為「要求 4 所擋的誤配置沒發生」）。

**後果**：「三項相加等於底稿全文」這個守恆式看起來覆蓋完整，實際覆蓋的只是底稿，
而底稿由依據句欄的極小集合與本輪改動決定——受測款次的其餘規範句一律落在「其餘條文」。
這正是同向紅的結構來源之一。
**避開方式**：閱讀分區複核的回報時，記得它的分母是底稿不是款次；判斷「其餘條文能不能獨立推得結論」
時，把未登載的同款次句子當成其餘條文的一部分（1.7 的標定法本來就是這樣算的）。
依收納判準不開票——它只影響對複核射程的理解，不使任一道題的計分改變。

### L38 `0.1.0-W3-616`／`618`／`620`／`621` 四輪的引用與套用讀數帶一個未排除的洩題解釋

`0.1.0-W3-624` 的世界平面查證：規格 1.1 原文要求「受測判定步驟一行」寫在題目檔，而 5.2 把題目檔路徑
交給探針，四輪的**全部題目、全部輪次**因此在作答前被告知受測款次編號與受測動作（r616 十七題、r618 三題、
r620 與 r621 各一題，共 22 題 66 份作答）。`0.1.0-W3-623` 是唯一未洩題的一輪；規格前的 `0.1.0-W3-417`
系列（含 `0.1.0-W3-550` 的重測）不經由本通路（140 份題目檔逐份查證）。

**後果**：那四輪的引用讀數不再支持「條文把讀者帶到該款」，套用讀數的高值不再能區分「讀者自行執行該步」
與「照著指示做一遍」。方向是單向的——洩題抬高讀數，故**低讀數與不通過的判定在洩題下仍然成立**
（實測：r616 的 Q02-r3 在被告知款次與動作之後仍零套用）。逐項的降級強度見 `0.1.0-W3-624` 的 Solution 第二節。
**避開方式**：規格 1.1（1.4.0）已把該行外移至出題者側並加兩項零洩題判準；引用那四輪的讀數時一律附本則。
依收納判準本則不另開票——洩題本身已由 `0.1.0-W3-624` 承接，本則是它在清單側的落地。

**零洩題判準恢復的是哪一個量（`0.1.0-W3-625` 實測）**：該輪是本線首個「零洩題判準全綠且讀數為高值」的輪次（識別符 0 次、受測行 ∩ 題目檔 0 命中、第二人於派發前複核為「沒有」，引用 3/3、套用 3/3、兩名判定者零分歧）。這使該輪的高讀數**不帶本則的洩題解釋**，採信力恢復——但恢復的範圍限於「條文把讀者帶到該款並使他執行該步」。它**不**恢復為「該改動有鑑別力」：同一輪的 1.7 遮蔽推導判同向紅（判定者在受測款次的兩段全遮之下仍推得同樣結論）。零洩題判準治的是題幹側的通路（出題規格 §0 第二種成因），1.7 治的是條文自身提供的旁路，前者恢復不了後者。

### L39 1.7 的三格定義在結論內容上，引用軸的鑑別力不進入覆蓋題資格

`0.1.0-W3-625` 實測：該輪的計分欄 C3（依據句引到受測那一段）**只有讀到該段才可能滿足**，三輪全數命中，故該題在**引用軸**上對受測改動有鑑別力；而 1.7 的三格是定義在**結論內容**上的，判同向紅即封鎖覆蓋題資格。執行遮蔽推導的判定者明文指出這一點，並記「要主張覆蓋，須另行成立『引用軸單獨可充當覆蓋』的規則」，未自行擴張。

**後果**：一道題可能同時「對改動的引用有鑑別力」與「對改動的結論無鑑別力」，而現行規則只承認後者。凡以「該改動零覆蓋」描述這類題的輪次，都應同時記下引用軸的讀數，否則會被讀成「讀者根本沒讀到那一段」。
**避開方式**：零覆蓋清單回填時一併記引用讀數與套用讀數；不得把「不得列為覆蓋題」寫成「讀者未讀該段」。依收納判準不開票——現行規則明確（紅即不得列覆蓋題），本則不改變任一題的計分，也不改變本輪的記帳。

### L40 改動清單中的連帶改動可整條沒有指名行，而「零覆蓋清單為空」的宣告不會被攔下

`0.1.0-W3-627`（收官結算）比對 `0.1.0-W3-613` 的十五條改動與 `0.1.0-W3-616` 的十七題受測款次表時查出：
改動 6（8.2(3) 引言／①／⑤ 的極性詞連帶更新）、改動 11（8.3(5) 的極性詞連帶＋補指 7.2(4-3)）、
改動 12（8.5(1) 例示改依新三步敘述）三條，在逐條表的末行**沒有「覆蓋題：」那一行**，
而該票的〈零覆蓋清單〉逐字寫「無。十五條改動全數指名覆蓋題（上表每條末行）」。
`0.1.0-W3-616` 的十七題亦無一以這三條為受測對象（Q08 受測 8.2(3) 引言的清單封閉性，
是引言的既有內容，不是改動 6 的極性詞改寫）。

**與 L21 的差別**：L21 是**指名了而題出不來**（指名的題材落在禁區）；本則是**整條沒有指名**，
而總結句以「上表每條末行」概括，讀者不會逐條回頭數。兩者的共同後果是「零覆蓋清單為空」
這句話不構成證據，但偵測方式不同——L21 要對照禁區清單，本則只要數指名行數是否等於改動數。

**後果**：三條改動自 `0.1.0-W3-613` 落地起（commit `2e66c7383`）至今未被任何一題受測，
而本線七輪的任一輪都沒有把它們列入零覆蓋清單，故也不曾進入下界的評估。凡引用
「`0.1.0-W3-613` 的十五條改動已全數指名覆蓋題」者，須改為「十二條指名、三條未指名」。
**避開方式**：Step 1 放行前加一道機械對帳——指名行數 ＝ 改動清單條數，不相等即差額全數列入
零覆蓋清單；總結句不得以「上表每條末行」概括，須寫出兩個數字。連帶更新型的改動
（為與主改動保持一致而動的其他款次）最容易漏，因為寫的人視之為主改動的一部分。
依收納判準不開票——它只影響覆蓋記帳，不使任一道題的計分改變，且覆蓋比已依出題規格 1.6.0
的〈終止條款〉降為參考值。

### L41 零改動輪下 1.7 的同向紅沒有處置錨點

`0.1.0-W3-628`（tdd／foundation-design 兩線的零改動輪）實測：五題中四題的 1.7 判同向紅，而〈同向紅燈的處置〉四條出路
全部以「本輪要驗一條改動」為前提——(a) 改題材、(b) 題幹交出前提、(c) 換受測款次，三者的目的都是讓該題能覆蓋那條改動；
(d) 登記為位置不可區分更明文要求受測款次在 Step 1 預判為（丙）複述型。零改動輪沒有 Step 1 改動清單，四條皆無從適用。

**與 L33 的關係**：L33 處理的是零改動輪的**產出形態**（覆蓋比無定義、下界誤擋，改產回歸讀數）；本則處理的是零改動輪的
**判讀後續動作**。兩者同源（規格 §6 的形態寫死為「批次改文一次 → 現出題一次 → 驗一次」），但 L33 的處置（改產回歸讀數）
不涵蓋本則——1.7 照跑、照判，判完之後無處可去。

**後果**：零改動輪的同向紅只能照實記錄，不封鎖任何覆蓋題資格（沒有覆蓋題可封鎖），也不構成回頭理由
（出題規格 1.6.0 的〈終止條款〉已把同向紅列入不再觸發回頭的六項）。若往後有人照字面套處置表，會在沒有改動可改的情況下
被逼向 (a)(b)(c)，實際上是改一道本來就無從歸屬的題。
**避開方式**：零改動輪執行 1.7 時，判讀照跑並逐題記錄三格與關鍵可見字，處置欄逐題寫「零改動輪，處置表不適用」；
同批記下該判讀對**引用軸**的意義（L39）——同向紅只否定結論軸的鑑別力。
依收納判準不開票——它只影響記帳形態，不使任一道題的計分改變。

### L42 §7 的字面關卡對「作答格式與受測對象的名字」恆不可滿足

`0.1.0-W3-628` 實測：受測款次為 tdd 的〈判定不拆分 Module〉時，題幹必須寫出 `BDD 場景`、`When`、`Given` 才能呈現待判事實，
而這三個詞正是條文用來寫該款次的詞。§7 第 5 項（題幹 ∩ 例示 span 實詞交集為空）與第 11 項（無第 (iii) 類命中）照字面執行時，
這類題永遠出不來。

**與 L20／L26／L34 的關係**：同一個結構第四次出現——L20 是「逐項清單完成度」與「特定援引動作」兩類受測步驟，L26 是判定對象的
實例化在 4.6 三類中無格，L34 是規範性枚舉被當成例示 span，本則是**作答格式與受測對象的名字**。四者的共同形態是「受測步驟本身
即條文字串的實例化」，而規格每次都要執行者當輪自訂一個判準。

**本輪的自訂判準（兩項，各配一則正向對照輸入）**：(R1) 4.6 的判紅條件收為「span 獨佔」——命中若同時出現在 span 之外，
登記為判定對象／格式詞彙的實例化後放行；對照輸入為「把判定語 span 內的整句植入題幹草稿，驗它被判為 (iii)」。
(R2) 2.1 的比對對象為題材宣告（例示詞不得**作題材**），全題幹交集另附原始數字並逐一標明成員；對照輸入為
「一個寫法與本文各處不同的列舉，驗它仍被例示 span 擷取器納入」。

**後果**：兩項判準的失效方向都是放行，故該輪的通過率帶一個未排除的字面對應解釋（本輪為 Q02 的 2 則放行命中）。
**避開方式**：逐則登記放行的命中與其落在條文的哪一句，並在 §7 對應項記為「本項對 N 則命中以此規則放行」；
不得只記「無第 (iii) 類」。依收納判準不開票——與 L20 同屬已知殘留代價，不使任一道題的計分改變。

### L43 外部驗證的兩階段交付依賴「同一名判定者可被再次喚起」

`0.1.0-W3-628` 實測：規格 1.7 要求執行遮蔽推導的判定者分兩階段收材料，階段 1 於階段 0 的判讀回報之後才交付。
該設計隱含一個前提——**那名判定者在階段 0 之後仍叫得動**。本輪該名判定者回報階段 0 後，交付階段 1 材料即未再回報
（同一 session 內另有一名代理人同樣在被喚起後未回報），階段 1 的計分改由承接者完成。

**後果**：兩項回報因此零觀測——**粒度敏感度一句**（「改遮哪一段會得到不同判讀」）與**階段 1 追記**（讀過被遮字之後判讀有無改變），
兩者都綁在「親自執行過遮蔽推導」這個狀態上，承接者填了不是同一個量。§7 第 28 項的時序要求仍成立（階段 0 早於階段 1 交付，
且階段 0 判讀未被改寫——承接者無從改寫），但該項所保護的「同一人前後對照」在本輪沒有發生。
**避開方式**：階段 0 的回報收到後盡快交付階段 1；交付後若逾時未回報，改由承接者完成計分，並把粒度敏感度與追記兩項**明寫為零觀測**
（L4：不得記為「沒發生」）。依收納判準不開票——它不改變任一輪計分，階段 0 的判讀與兩名判定者的計分皆完整。

### L44 拋棄式器材放在共用 scratchpad，派發前被他方寫入時無訊號

`0.1.0-W3-628` 實測：五份題目檔的子問尾綴在派發前被出題者以外的一方改為另一措辭。器材位於共用 scratchpad、不在版控，
**沒有 diff 可查**；發現它的唯一途徑是出題者在派發前碰巧重讀了題目檔。

**作者已查明**：並行的主線程。它在 compact 之後讀到票為 `in_progress`、`out/` 為空、派發腳本就緒，據此推斷出題者已停擺而接手執行，
並在派發前採納零洩題複核者回報中的收緊建議改寫五份題目檔（一次批次寫入，五檔 mtime 相同）。**空目錄對「這輪還在不在飛」零鑑別力**——
它同時相容於「已停擺」與「正要派發」，而接手的判斷需要前者成立。

**歸因放大因子（本則的主要教訓）**：那次改法**與某份回報裡的建議逐字相同**，於是最像作者的是提出建議的複核者，而不是實際寫入的那一方。
本輪差一步就把帳算到複核者頭上，是它以「本 session 未呼叫過任何寫入工具」加上兩項唯讀時間戳自證才擋下來。
**逐字吻合不是作者證據**——建議是公開文字，任何讀到它的人都能照著改。

**與 L23／L32 的關係**：三者都屬「派發側的無訊號區間」——L23 是路徑打錯到一個碰巧存在的目錄，L32 是作答回報成功而檔案未落地，
本則是器材本身在派發前被改。三者的共同點是**數字全部正確，只是建立在沒人檢查過的內容上**（§4.4 要擋的靜默丟棄同形）。

**後果**：機械檢查（4.2／4.3／2.1／2.2／零洩題兩項）全數跑在出題當下的內容上，題目檔其後被改則那些結果失效，而失效不留痕跡。
**避開方式**：三項——(1) 出題完成時對 `q/` 全部題目檔記一次內容雜湊，**派發當下**重算一次並比對，不相等即停手重跑第 2 至第 4 節的檢查；
(2) 判斷「這輪還在不在飛」不以工作區狀態為據（空目錄零鑑別力），改查代理人清單或直接問出題者；
(3) 追查寫入者時不以「改法與誰的建議吻合」為據，改查該方的工具呼叫序列與檔案 mtime 分佈（一次批次寫入的多檔 mtime 相同，可與逐檔編輯區分）。
本輪的補救即為在改後內容上重跑全部機械檢查並由另一名第二人重新複核零洩題，兩者皆全綠；**該偵測來自出題者在派發前碰巧重讀題目檔，
不是任何一道關卡保證的**。
依收納判準不開票——本輪已在改後內容上重跑並全綠，不使任一道題的計分改變。

### L45 前七輪的「字面通路已排除」帶一個未排除的器材側解釋，且器材逐輪收窄

`0.1.0-W3-632` 依 `0.1.0-W3-628` 的規格 × 腳本判準分岔稽核，回頭查了七輪的檢查腳本。
各輪的 §7 機械檢查，由每輪現寫的拋棄式器材執行，與出題規格的判準逐輪分岔，而**分岔方向
沒有一輪是往變寬走**：

| 輪 | 頓號列舉獨立分支 | 破折號開放集合閘門（規格未載） | 子字串取法 | 4.3 扣除 | 4.4 守恆式與掃描輸入 | 自長紅燈欄位 |
|---|---|---|---|---|---|---|
| `0.1.0-W3-616` | 有 | 無 | 刪全部空白後串接 | 區塊計數法 | 同源 | 無 |
| `0.1.0-W3-618` | 有 | 無 | 以區塊為單位 | 出現次數法 | 同源 | 無 |
| `0.1.0-W3-620` | **無** | 無 | 以非中文字元切段 | 計數法 | 同源 | 無 |
| `0.1.0-W3-621` | **無** | 無 | 以非中文字元切段 | 計數法 | 同源 | 無 |
| `0.1.0-W3-623` | **無** | **有**（另要求含頓號，較 `628` 更嚴） | 以非中文字元切段 | 計數法 | 同源 | 無 |
| `0.1.0-W3-624` | **無** | **有**（同上） | 以非中文字元切段 | 計數法 | 同源 | 無 |
| `0.1.0-W3-625` | **無** | **有**（同上） | 以非中文字元切段 | 計數法 | 同源 | 無 |
| `0.1.0-W3-628` | **無** | **有** | 逐行 `strip()`，不跨行 | **拼接字串整體扣除，空轉** | **脫節** | **有** |

**射程限縮（既有結論原句不動，本則為其附註）**：

| 輪 | 該輪「字面通路已排除」的射程限縮為 |
|---|---|
| `616`／`618` | 例示 span 的四類分支齊備，2.1 的結論射程不縮；子字串取法各有一側失真（`616` 刪空白串接會生出原文不存在的串而多判紅、`618` 以區塊為單位而漏掉跨區塊的串），故 4.2 的「已排除」讀作「在該取法下已排除」 |
| `620`／`621` | 另加：**不含頓號列舉的例示位置未被掃過**，2.1 的「交集為空」讀作「括號、破折號、例如引導句三類之內交集為空」 |
| `623`／`624`／`625` | 再加：**破折號列舉中不帶開放集合標記者未被掃過**，而規格 2.1 原文正指破折號列舉是最強的一條通路。該閘門當輪有自訂判準與對照輸入的記載（L34），但其對照輸入自帶閘門所需特徵，對「閘門是否過窄」零鑑別力 |
| `628` | 再加三項：4.3 獨佔詞在該輪 Q04 為空轉（判讀對象不存在，非無命中）、4.4 守恆式與實際掃描輸入無因果關係（§7 第 14／15 項恆綠）、4.6 判讀取了腳本自長的較窄欄位而非規格的 `in_span`。該輪的「字面通路已排除」在規格字面與自長欄位兩種讀法下皆不成立 |

**後果**：上列各輪的「字面通路已排除」不得讀作「題幹與條文之間沒有字面通路」，只能讀作
「在當輪器材實際掃過的範圍內沒有找到」。方向是**單向的**——器材較窄只會少找到通路，故
**當輪有找到通路而據以改題材的判定仍然成立**，受影響的只有零命中那一類宣告。本則不使
任一輪的三輪計分改變，也不使任一道題由不通過翻為通過：字面通路的處置是改題材，發生在
Step 2；未被找到的通路不曾進入 Step 3 的計分。
**與 L34 的差別**：L34 記的，是破折號閘門這一項**判準本身**帶的代價（規範性枚舉與例示性列舉不分），
本則記的是**判準寫在哪裡**——該閘門自 `623` 起在腳本裡運作而規格 2.1 未收，七輪之中沒有一次
對帳會發現它。偵測方式因此不同：L34 要看閘門的內容對不對，本則只要數腳本的每一個判準
識別符能不能指回一個規格條號。
**避開方式**：出題規格 1.7.0 已新增第 8 節〈判準的單一權威住址〉與 §7 第 29 項（判準住址對帳），
每輪出題放行前逐項把腳本識別符對回規格條號、並逐條號回查分支；2.1 同批改寫為五類 span 的
枚舉表，破折號閘門升格入規格並附收窄代價，對照輸入須落在現行閘門之外。引用上列各輪的
「已排除」結論時一律附本則。
**依收納判準不開票**——本則只影響既有結論的射程標注，不使任一輪的布林反向；判準分岔的本體
已由 `0.1.0-W3-632` 承接並落為規格第 8 節。

**可重跑命令**（`$S` 為存放各輪器材的 scratchpad 根目錄）：

```bash
for f in r616/tools/check.py r618/tools/check2.py r620/check.py r621/check.py \
         r623/check.py r624/check.py r625/check.py r628/tools/check2.py; do
  printf '%-22s dun=%s gate=%s\n' "$f" \
    "$(grep -cE '^\s*for m in re\.finditer\(r"\[\^.*、|、\[\^' "$S/$f")" \
    "$(grep -cE 'dash_kept|OPEN_SET_MARK|dash_list_requires_suffix' "$S/$f")"
done
```

該次輸出：`r616`／`r618` 的 `dun=1 gate=0`；`r620`／`r621` 皆 `dun=0 gate=0`；
`r623`／`r624`／`r625` 為 `dun=0 gate=5`；`r628` 為 `dun=0 gate=2`。

---

**Last Updated**: 2026-09-18 | **Version**: 1.14.1 — L44 補作者與歸因放大因子：寫入者已查明為並行的主線程（compact 後以「票為 in_progress、`out/` 為空」推斷出題者已停擺而接手；空目錄對「這輪還在不在飛」零鑑別力）。該次改法**與零洩題複核者回報中的建議逐字相同**，使最像作者的是提出建議者而非實際寫入者——逐字吻合不是作者證據，本輪由複核者以「本 session 未呼叫過任何寫入工具」與 mtime 分佈自證才擋下誤歸因。避開方式由一項擴為三項（題目檔雜湊比對、在飛判斷不以工作區狀態為據、追查寫入者查工具呼叫序列與 mtime 分佈）。
**Version**: 1.14.0 — `0.1.0-W3-632`（規格與檢查腳本的判準分岔收斂）的一則不開票發現進第五節：L45（前七輪的「字面通路已排除」帶一個未排除的器材側解釋，且器材逐輪收窄——`616`／`618` 有頓號列舉獨立分支而 `620` 起消失，破折號的開放集合閘門自 `623` 起在腳本裡運作而規格 2.1 未收，`628` 另有 4.3 空轉／4.4 脫節／自長紅燈欄位三項；逐輪射程限縮表附可重跑命令與該次輸出）。方向單向——器材較窄只會少找到通路，故當輪找到通路而據以改題材的判定仍成立，受影響的只有零命中那一類宣告；既有結論原句不動，本則為其附註。與 L34 的差別在 L34 記閘門這一項判準本身的代價、本則記判準寫在哪裡，偵測方式因此不同。判準分岔的本體已由 `0.1.0-W3-632` 落為出題規格 1.7.0 的第 8 節〈判準的單一權威住址〉與 §7 第 29 項。
**Version**: 1.13.0 — `0.1.0-W3-628`（tdd／foundation-design 兩線的零改動輪）的四則不開票發現進第五節：L41（零改動輪下 1.7 的同向紅沒有處置錨點，四條出路皆以「本輪要驗一條改動」為前提；與 L33 同源而射程不同，L33 管產出形態、本則管判讀後續動作）、L42（§7 第 5／11 項的字面關卡對作答格式與受測對象的名字恆不可滿足，為 L20／L26／L34 之後同一結構的第四次出現；本輪以兩項自訂判準放行，各配正向對照輸入，代價是未排除的字面對應解釋）、L43（1.7 兩階段交付隱含「同一名判定者可被再次喚起」的前提，本輪不成立，粒度敏感度一句與階段 1 追記兩項零觀測）、L44（拋棄式器材放在共用 scratchpad，派發前被他方寫入時無 diff 可查、無訊號；與 L23／L32 同屬派發側的無訊號區間，避開方式為出題完成與派發當下各記一次題目檔雜湊並比對）。四則皆不使任一道題的計分改變。
**Version**: 1.12.0 — `0.1.0-W3-627`（收官結算）的一則不開票發現進第五節：L40（改動清單中的連帶改動可整條沒有指名覆蓋題行，而「零覆蓋清單為空」的總結句以「上表每條末行」概括、不會被攔下；實測 `0.1.0-W3-613` 十五條中三條未指名，且七輪中從未進入零覆蓋清單或下界評估。與 L21 的差別在「整條沒有指名」對「指名了而題出不來」，偵測方式因此是數指名行數而非對照禁區）。
**Version**: 1.11.0 — `0.1.0-W3-625`（第七輪）的一則不開票發現進第五節：L39（1.7 的三格定義在結論內容上，故一道題可以「引用軸有鑑別力、結論軸無鑑別力」，而只有後者決定覆蓋題資格；零覆蓋清單回填時須一併記引用與套用讀數，不得把「不得列為覆蓋題」寫成「讀者未讀該段」）。同批在 L38 補一段：該輪是本線首個零洩題判準全綠而讀數為高值的輪次，高讀數因此不帶洩題解釋、採信力恢復，但恢復範圍限於「條文把讀者帶到該款並使他執行該步」，不恢復為「該改動有鑑別力」（同輪 1.7 仍判同向紅）。同輪另有兩則依判準第一列開票（改前推導對兼含移除與新增的混合型改動給不出二分結果、受測改動的射程完全落在其前置款次下游而三條出路皆不適用），見該票的 spawn。
**Version**: 1.10.0 — `0.1.0-W3-624`（洩題查證與第六輪驗收）的四則發現進第五節：L35（計分欄記通過條件與被禁那一格措辭同形時未寫如何記）、L36（受測款次與前置款次結論重合時，引用與套用兩讀數指向相反計分）、L37（登載底稿之外的同款次規範句不在分區複核射程，不遮 span 為空時該複核零鑑別力）、L38（`0.1.0-W3-616`／`618`／`620`／`621` 四輪的引用與套用讀數帶未排除的洩題解釋，方向單向、低讀數仍成立）。前三則皆不使任一道題的計分改變；L38 是洩題在清單側的落地，洩題本體由 `0.1.0-W3-624` 承接。同輪另有三則依判準第一列開票（§7 第 24 項與 1.7 外部驗證互斥、1.7 雙向對帳表第一列對「有新增內容而非唯一路徑」無出路、本輪 Q01 須依 1.7 出路 (b) 重出），見該票。
**Version**: 1.9.0 — `0.1.0-W3-623`（7.2(3-2)①乙 的補測輪，本線首個零改動輪）的兩則不開票發現進第五節：L33（零改動輪不在規格射程內，覆蓋比分母為 0 而無定義、下界在空集合上 vacuously 觸發而誤擋；處置為改產回歸讀數並記錄下界的誤擋）、L34（2.1 的例示 span 定義不分規範性破折號枚舉與例示性破折號列舉，受測步驟為「清單有沒有寫完」時交集為空不可滿足；本輪自訂判準並配正向對照輸入，該對照輸入第一版即抓到過濾規則過窄）。兩則皆不使任一道題的計分改變，依收納判準不開票。同輪另有三則依判準第一列開票（受測判定步驟一行隨題目檔送達探針、brief 豁免清單與計分欄可互相矛盾、7.2(3-2)② 的三格對「某項兩端都整項未寫」沒有格），見該票。
**Version**: 1.8.0 — `0.1.0-W3-621`（規格 1.3.0 的粒度對齊與 C1 回測）的三則不開票發現進第五節：L30（改動橫跨款次須拆條，拆後各條互為其餘條文，本輪零觀測）、L31（正解為「本款不輸出結論」時 1.7 第三格與計分欄記通過條件措辭撞車，三格退化；C1 回測須與粒度對齊同時處置才由紅轉綠）、L32（作答回報成功而輸出檔未落地，只有 5.8 的對帳看得見；補救須由原 agent 原樣寫入而非重跑）。三則皆不使本輪三輪計分改變、不使任一題由不通過翻為通過，依收納判準不開票。L28 與 L29 的規格層處置已於本輪落地（見出題規格 1.3.0），兩則限制本體保留為成因記錄。
**Version**: 1.7.0 — `0.1.0-W3-620`（規格 1.2.0 四條新條文的首次執行）的兩則不開票發現進第五節：L28（遮蔽 span 錨在極小集合依據句欄，與改動以條為單位的粒度不一致，一條多句的改動必然把自己的一部分留在其餘條文而被判同向紅；與 L25 同屬記帳失真而方向相反——L25 高估、本則低估；1.7 雙向對帳表把同向紅單一歸因為「無新增內容」，在此給不出正確出路）、L29（零覆蓋回報欄由兩名判定者填寫而遮蔽推導只由其中一名執行，兩人填的不是同一個量，本輪兩人相反）。兩則皆不使三輪計分改變、不使任一道題由不通過翻為通過，依收納判準不開票，寫成下一輪 Step 1 的規格層輸入。
**Version**: 1.6.0 — `0.1.0-W3-619` 回填三處指向自身的待界定標記，不新增限制：L25 前提 1（(d) 的「複本」射程改錨在 Step 1 的（丙）複述型類別，並新增與 1.7 的雙向分類對帳；解除的只有前提的不確定性，本則限制本體不變）、L25 後果段（走 (d) 的題**計入** Step 3 布林分母，代價由布林語意寫死、覆蓋比、全零覆蓋時不得產出布林三處承接）、L26 補一句與 1.7 新增之「不遮 span」的關係並補 Action（同一概念命名於遮蔽側，未給 4.6 新增類別，本則缺口不消失，命中仍依自訂優先序放行並註明則數）。
**Version**: 1.5.0 — `0.1.0-W3-618`（規格 1.1.0 新增條文的首次執行）的三則不開票發現進第五節：L25（位置前移型改動在 1.7 下只剩出路 (d) 而零覆蓋；與 L22 同屬覆蓋率高估，差別在已被零覆蓋清單擋下一半，附兩項前提）、L26（4.6 的分類對受測步驟判定對象的實例化沒有格，位置判準優先為本輪自訂規則而非條文字面）、L27（引用讀數在現行計分下無後果，「照做不引」的組合未定規則）。三則皆不使本輪整批布林反向，依收納判準不開票；同輪的粒度類發現（1.7 的遮蔽單位、「確定結論」的粒度、走 (d) 的題是否計入 Step 3 布林分母、1.7 外部驗證與出題者判讀相反時的裁決力）依判準第一列開票為 `0.1.0-W3-619`。落地後經兩視角審查（文字品質＋結構），同批修正 L25 的框架（原寫「與 L22 方向相反／覆蓋率低估」，實為同向高估）與兩項前提外顯、L26 補前提與自訂規則的標註、L27 的序數錨點改條款名稱。
**Version**: 1.4.0 — `0.1.0-W3-617` 追加 L24（條文內的第二條路徑可指向錯誤答案，而非只是旁路；與 L22 同結構而失分方向相反，兩者皆由規格 1.7 的遮蔽推導涵蓋，故不開票）。L20／L21／L22／L23 各補一行「已收進出題規格何處」的落地指向；L22 另補射程更正——第二條充分理由不限於同一款次內。
**Version**: 1.3.0 — `0.1.0-W3-616` 追加 L23（派發路徑打錯到一個碰巧存在的目錄時無訊號；本輪實際損害為零，避開方式為輸出目錄寫入探測、路徑由腳本產生、全部批次回報完成後才對帳）。
**Version**: 1.2.0 — `0.1.0-W3-616`（出題規格的首次完整執行）的三則不開票發現進第五節：L20（「題幹 ∩ 條文交集為空」對逐項清單完成度與特定援引動作兩類受測步驟不可滿足，改以三類命中分類判讀）、L21（指名覆蓋題與出題禁區可互相矛盾，「零覆蓋清單為空」不構成新增文字全被驗過的證據）、L22（同款次內另有充分理由時覆蓋題可被旁路，須分開記引用與套用）。三則皆不使本輪整批布林反向，依收納判準不開票。
**Version**: 1.1.0 — `0.1.0-W3-613` 批次改文的兩則不開票發現進第五節：L18（8.2(4) 與 8.2(2) 第二點的量詞不對稱，現行條文下輸出同向，源自 `0.1.0-W3-569`）、L19（對應關係要件同時寫在程序步驟與清單引言，引用位置不可區分，為 `0.1.0-W3-543` 改文的已知代價）。
**Version**: 1.0.0 — `0.1.0-W3-612` 初版，自 `0.1.0-W3-417` 的二十餘批探針紀錄提煉。
